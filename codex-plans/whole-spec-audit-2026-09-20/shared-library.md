# Shared-library audit and reuse

All 115 files recursively under `src/lib/` were inventoried. Generated wiring and binary fixtures were inspected by metadata/schema/consumer; they are not edit targets. The following identifies the existing owner to reuse before creating helpers. Audit findings are corrected by [review-decisions.md](review-decisions.md); an unused helper is not, by itself, an in-spec feature gap.

- `src/lib/IngredientCatalogImageContext.tsx` — Context provider batching ingredient catalog image storage-URL resolution for a rendered page Reuse for any catalog surface adding images; no PR03/PR04 criterion depends on it
- `src/lib/api.ts` — Single import point for the generated Convex api + Doc/Id types (src/lib/api.ts:1-3). reuse — every report, storage and auth-status consumer imports through it; no gap.
- `src/lib/catalogUnitGrams.ts` — Gram/ml resolution per unit for nutrition scaling and batch-scale feasibility; count units resolve via serving-grams hint, density hint, food-name density table, or 100 g USDA basis fallback Reused by nutritionUnitScale.ts and (mirrored in convex/lib/catalogUnitGrams.ts) by server nutrition scaling; keep-in-step duplication is intentional per header comment. Fallback estimations (typical density, 750 ml bottle, 100 g) are display/nutrition aids, not purchase conversions — must not be reused for stock or purchasing math
- `src/lib/componentBatchScale.ts` — Exact integer-ratio batch-to-serving scale for the DishComponent attachment (yieldQuantity/batchMultiplier), rejecting unmeasurable values and overflow Reuse for any new per-serving recipe attachment; already the exactness mechanism PR03-05 depends on
- `src/lib/convexActionErrorMessage.ts` — Human-readable failure copy for signed-in UI (unwraps ConvexError) reuse: the CF-1-fw7 plain-language failure path; no duplicate error peelers
- `src/lib/culinaryDemandClient.ts` — Kitchen seam hooks + honest-incomplete-cost copy (recipeCostLine 'Cost unknown · k of n lines priced', UNRESOLVED_KIND_LABEL) Reuse; seam placement required by kitchen guards
- `src/lib/currency.ts` — ISO-4217 normalization + per-row money formatting reuse: any §4.2/§5.4 price rendering; do not hardcode currency codes
- `src/lib/deploymentConfigCheck.ts` — Pure PR12-01 config checker: mismatch blockers (issuer/key environment, pk/sk pair, instance host, Convex audience 'convex', callback origins, localhost-in-prod, deployment-target), redacted missing/placeholder findings, dev-credential warning under VITE_CLERK_ALLOW_DEVELOPMENT_AUTH (src/lib/deploymentConfigCheck.ts:20,79-85,185-458). reuse — verified-historically (AC-028); wired into scripts/vercel-build.sh, scripts/release.sh, src/main.tsx:23-32.
- `src/lib/emailNotifications.ts` — Email category catalog, per-user subscription predicate (isEmailNotificationSubscribed), origin-safe deep-link resolver, branded HTML/text summary renderer REUSE — consumed by convex/emailNotifications.ts:22 prepareMyDelivery (delivery-time preference gate) and rendered in EmailNotificationSettingsPage; GAP: no sender path consumes prepareMyDelivery output, so the preference check is currently a gate on a flow nothing executes
- `src/lib/eventCreateCatalogClient.ts` — useEnsureBuiltInServiceStyle hook over api.eventCreateCatalog.ensureBuiltInServiceStyle reuse: backs CF-3-2 built-in Full Service/Limited Service/Drop Off/Vending registration from the create screen
- `src/lib/eventDayBriefing.ts` — Typed narrow day-of row shapes. Reuse.
- `src/lib/eventImportDraft.ts` — Zod-validated BEO import draft (facts/menu/missing/issues/status) persisted per event reuse — assistant eventImportWorkflow + EventImportDraftPanel
- `src/lib/eventImportExisting.ts` — One-shot read of whole-tenant rows describing an event already in Capsule before a BEO import No consumer found; do not turn dead-helper cleanup into an unrelated implementation task. Reuse only if it fits a required import flow.
- `src/lib/eventMenuHeadcountHint.ts` — Walk 'sized for N of M guests' hint + review question against expectedHeadcount; override/86 aware reuse: consumed by EventMenuTab; relevant to PR06-08 live quantity adjustment surfacing
- `src/lib/eventPacket/buildWorkbook.ts` — Workbook composition: cover/action sheet, brief, timeline, verbatim form pages (sourcePage blocks) with derived header + Y/N/choice overlays for the Event Task Breakdown, menu/production with original units, source-detail pages, packlist by item type (REFERENCE) and category (PACKING DRAFT), staffing/vehicles/equipment/venue sections, source appendix (SHA-256 per artifact) Reuse; overlay derivations are regex heuristics over ops.* native fields; no route/map section, no financial summary, menu not in service order
- `src/lib/eventPacket/extractObservations.ts` — Per-kind observation extraction with evidence references (page/row/cell), occurrence-counted ids, timeline visits, menu quantities with original units, packlist rows with category/stock code/for-association, sourceContent pages, source.recordedTime Reuse; the only place source text becomes observations
- `src/lib/eventPacket/fixtures/event-forms-one-print.b64.ts` — Base64 module containing the verbatim companion PDF; consumed by renderWorkbook.ts. Reuse the pinned fixture bytes; binary/base64 content inspected through metadata and consumer, never treated as generated application output to edit.
- `src/lib/eventPacket/fixtures/event-forms-one-print.overlays.json` — Measured per-page anchor coordinates (header event number/date, Y/N rows, Serving Ware choices) for overlay drawing Reuse; renderWorkbook pushes any unfound anchor into unmatchedOverlays (must stay empty)
- `src/lib/eventPacket/fixtures/event-forms-one-print.pdf` — Original EVENT FORMS ONE PRINT PDF, 167,494 bytes; b64 module (223,549 chars) embeds the verbatim bytes, header comment pins source SHA-256 4466dccd...; consumed only via EVENT_FORMS_ONE_PRINT_B64 in renderWorkbook.ts:225 — the raw .pdf file has no code consumer (kept as source-of-record) Reuse as-is; binary inspected by metadata only, never regenerated
- `src/lib/eventPacket/fixtures/event-workbook.form-definitions.json` — Schema'd inventory (event-workbook-form-definitions/1) of 43 sections: final-lock.* template checks, quartermaster.*, and all field-execution forms (leaving-shop, takeoff-documents, arrival, muda, bins, after-event, return, packing, buffet-drawing, leaving-event, task) with verbatim sanitized sourceText, humanOwner, answerType unanswered_human_input Reuse via requirements.ts (predeparture checks) and buildWorkbook.ts (page composition); every item is human-input by design — no derived answers recorded in the fixture
- `src/lib/eventPacket/fixtures/liberty-mutual-6837.snapshot.json` — 11,190-line sanitized real-event packet snapshot fixture (artifacts with fingerprints, observations, facts, issues) Test/regression input only; consumer is tests/event-packet-*.test.ts
- `src/lib/eventPacket/fixtures/liberty-mutual-6837.sources.sanitized.json` — Sanitized source extracts (sanitized-source-extracts/1) with per-page sanitizedContentSha256 over compact JSON and documented redactions; explicitly not a snapshot or approval Test input; hash scope documented in-file
- `src/lib/eventPacket/groupSources.ts` — Groups extracted sources into packet candidates by labeled invoice#+event-date identity; tracker rows scoped by date+event number with ambiguity review; menus join by unique date + normalized labeled client/contact; shared reference kinds (ops_final_lock, quartermaster, event_forms, training) attach to all Reuse; identityReviews carries the exact ungrouped reasons
- `src/lib/eventPacket/importSources.ts` — Ingest bytes: dedupe by fingerprint, PDF page extraction (injectable), RTF/plain decode (ONE BEO reader via parseBeoText), RFC 4180 CSV, recognition, grouping, per-candidate observation extraction Reuse; shares the BEO parser with the event import (owner rule: one BEO reader)
- `src/lib/eventPacket/model.ts` — Packet domain types (SourceKind, Section, Observation, Fact, PacketIssue, Resolution, ChecklistVerification, Revision) plus identityKey, canonicalJson, SHA-256 fingerprint of bytes/snapshots Reuse. Fact statuses confirmed/candidate/conflicted/missing/not_applicable are NOT the spec's four answer states — no field_confirmation state exists
- `src/lib/eventPacket/packetContract.ts` — parsePacketSnapshot, byte-level verifyPacketArtifacts, portable-bundle parse (base64 artifacts, active-fingerprint pruning, quarantine of imported facts/approvals), mergePacketEvidence, packetEvidenceFromText Reuse; portable bundles reset facts/issues to empty and stage review — imported approvals never authorize native updates
- `src/lib/eventPacket/prepareNativeWorkbook.ts` — Prepare flow over injected ports: read current packet+latest revision, reuse when current fingerprint matches and not stale, else build+render, upload PDF+snapshot (PDF requires input fingerprint), record revision Reuse; server re-checks fingerprint at commit (recordPacketRevision)
- `src/lib/eventPacket/recognizeSource.ts` — Content-signature source-kind recognition (worksheet, beo, event_menu, menu_production, packlist_csv/item_type/category, event_tracker, ops_final_lock, quartermaster, event_forms, training, nowsta_event_timesheet, kitchen_shift) with parserVersion event-packet/1 and recognition evidence Reuse; unknown kinds are kept but never grouped
- `src/lib/eventPacket/reconcile.ts` — Pure reconciliation: groups observations per field, native_finalized authority wins, 2-artifact agreement confirms, conflicts/dash normalization, evidence-fingerprint per issue, resolution replay (supported observation / fact_entry / native choice), requirement checks with dependent-input fingerprints, retired issues kept as info, stage ready only when readiness() passes Reuse; this is the determinism + staleness engine (changed input => new evidenceFingerprint => old resolutions stop applying)
- `src/lib/eventPacket/renderWorkbook.ts` — jsPDF + pdf-lib renderer: page layout audit (bounds/overlap/blank/glyph), verbatim merge of EVENT FORMS ONE PRINT pages from base64 fixture with anchor overlays from overlays.json, chrome-free form pages, footer page numbers over merged binder Reuse; unmatchedOverlays must stay empty — any new form anchor needs overlays.json coordinates
- `src/lib/eventPacket/requirements.ts` — Required facts (12) + requirement list: final-lock.* predeparture checks derived from form-definitions fixture (wrapup excluded), custom checks (menu components/unit-conversion, production placeholders, assignment vehicle/trailer/crew, TPP item-type/category reports, Nowsta timesheet, TPP final, tracker, Goodshuffle, Dropbox, two signatures), canMarkNotApplicable vehicle rule Reuse for readiness; NOTE the packlist/Nowsta report checks mandate re-uploading legacy reports even though native pack lists exist — the §14.3 tension
- `src/lib/eventPacket/resolveIssue.ts` — Decision application: actor+timestamp required, check answers must be legal and not-applicable only where allowed, choices must match an observation or current native fact, firsthand entries need reason+value, unit mismatches over native facts refused, resolution id = hash(decision+evidenceFingerprint) Reuse; implements 'resolution records observation, choice, actor, reason where materially required' — no 'resulting command' field
- `src/lib/eventPacket/schema.ts` — Zod snapshot schema v1: strict artifact/observation/fact/issue/resolution/check/revision shapes, referential integrity (dangling evidence, resolved-issue-must-have-decision, confirmed-fact-needs-authority+2-source agreement, ready-blocked-by-open-required), unique ids Reuse; it is the tamper contract every import/decision path re-validates
- `src/lib/eventPacket/summaryProjection.ts` — Pure cross-event workbook row projection (open blocking/warning, ready, latest revision, source count, client name) over persisted artifact/issue/revision rows Reuse; counts explicitly 'as of the last import or decision', not a live reconcile
- `src/lib/eventPacket/useEventPacket.ts` — Client seam: access/summary queries, getPacket view, upload (generate URL -> storage -> register action), importEvidence, resolve, sourceUrl/revisionUrl, prepare; useAttachPacketSources attaches the BEO that created an event as its first workbook sources Reuse; owner rule 2026-09-20 — BEO import and workbook import are one flow
- `src/lib/eventPrepGroups.ts` — Groups prep tasks under exact event-menu lines; an explicit eventDishId never falls through to another serving plan; ambiguous dishId matches become an unlinked group. Reuse. Consumer: src/features/events/EventPrepList.tsx. No test of the ambiguity fall-through found in tests/.
- `src/lib/eventRecurrence.ts` — Pure recurrence arithmetic (weekly/monthly/annual, boundary checks, first-future-occurrence, sweep scheduling) reuse: consumed by RecurringEventPanel + recurringEventActions; no direct PR05/PR06 clause
- `src/lib/eventScopedQueries.ts` — Indexed event-relationship read hooks (assignments, staff needs, shifts) with 'skip' suspension reuse: consumed by EventDetailPage; the read path a field employee uses for booking details (PR06-07)
- `src/lib/eventTimelineRun.ts` — Run-of-show write hooks (complete/reopen/schedule atomically) over generated EventTimelineActivity commands reuse: consumed by src/features/eventDay/RunOfShowPage.tsx; timeline carry-into-event evidence for PR06-07
- `src/lib/eventTimingMilestones.ts` — Milestone labels/categories plus eventTimingWindows() projection of the Event timing computed fields into staff-on/depart/onsite/service/cleanup/return/staff-off windows reuse — pure projection; all math stays in Event computeds (header comment, line 1). Reuse as-is for any timeline read model
- `src/lib/fileStorageClient.ts` — Authored hooks over the PR12-05 storage seam: useGenerateUploadUrl (staff-only upload URL) and useStorageUrls → api.fileStorage.urlsForStorageIds (src/lib/fileStorageClient.ts:1-16). reuse — backend resolves an id only through a live referencing row in the caller's tenant (convex/fileStorage.ts:96-153); proven by tests/proofs/file-storage-ownership.runtime.test.ts (AC-029).
- `src/lib/format.ts` — Intl date/time/money/count/percent formatters; formatMoney takes per-row currencyCode, formatMoneyExact keeps cents (91 lines). reuse — no estimate/money rendering should roll its own Intl formatting
- `src/lib/heldQueryRows.ts` — HeldQueryRowCache keyed by Clerk account + tenant so Back navigation does not flash empty lists after Convex unsubscribes (44 lines). reuse — any list screen suffering unmount-flash uses useHeldQueryRows, not local caching
- `src/lib/hiringPipeline.ts` — Hire/revoke/email-correction/provision wrappers for Person onboarding. Reuse. Covers the 'deployable Person record' HRIS boundary of 12.2.
- `src/lib/ingredientLookupClient.ts` — Seam hooks for ingredientLookup Convex actions: food-database search, autofill, nutrition/cost/image apply, lookup-cost resolution Reuse; price provenance enters through applyCostToIngredient / resolveLookupCost paths (convex/lib/ingredientLookupApplyCost.ts)
- `src/lib/inventoryAuditClient.ts` — Single seam hook over the authored inventoryAudit.listForItem action Reuse for stock audit-log surfaces (PR04-03 traceability display)
- `src/lib/invoicePaymentActions.ts` — Authored React hooks over api.invoicePayments getPaymentLink/createPaymentLink/syncStripePayments, kept outside finance components reuse for any PR06-09 payment surface; today only staff-side finance pages consume it — no client-facing consumer
- `src/lib/invoiceReminderActions.ts` — Authored action hooks (getSchedule/configureSchedule/sendNow) wrapping api.invoiceReminders, kept outside guarded finance feature roots REUSE — wired in src/features/finance/InvoiceDetailPage.tsx; backs PR07-04/06/07 UI surface
- `src/lib/invoiceReminderEmail.ts` — Render reminder subject/HTML/text with due-today/in-N-days/overdue wording, branded colors, USD amounts, payment URL REUSE — consumed by convex/invoiceReminders.ts:616 sendReminderEmail; the only client-facing branded email that actually sends
- `src/lib/invoiceReminderSchedule.ts` — Offset normalization (1..12 reminders, ±365d, dedup+sort), comma-string parser, offset labels, reminderScheduledAt math REUSE — consumed by convex/invoiceReminders.ts (applySchedule, deliverReminder staleness checks) and the email renderer
- `src/lib/llm-review.test.ts` — Live-model examples; not included by current tests/**/*.test.ts suite Examples target design professionals/finance and missing tmp images; replace/adapt only when actual J qualification is implemented.
- `src/lib/llm-review.ts` — Provider-backed text/image review with structured pass/feedback and registry loader Reuse mechanism with actual Capsule criteria/artifacts; do not treat generic sample verdicts or missing credentials as proof.
- `src/lib/manifest-convex-react.ts` — Manifest client wiring: ~1186 generated use* hooks + Zod command param schemas reuse: the CF-1-fw2 generated-binding path for every §3–§5 UI; never edited; imported via src/lib/api.ts conventions
- `src/lib/nutritionUnitScale.ts` — Scale per-gram nutrition fields to a display unit via CatalogUnitGrams; returns null when the unit cannot be resolved (no fabricated values) Reuse for PR03-09 nutrition display; null return is the unknown-state hook
- `src/lib/offlineAuthContext.tsx` — Offline auth provider + read-only banner ('showing the last confirmed workspace from <time>') (src/lib/offlineAuthContext.tsx:12-45). gap (proof) — wired in src/app/ClaimGate.tsx:75-86 but no dedicated test found (rg offlineAuth in tests/ → no matches).
- `src/lib/offlineAuthSnapshot.ts` — Per-account last-confirmed auth snapshot in localStorage, 7-day max age, restore only offline + same Clerk account (src/lib/offlineAuthSnapshot.ts:3-4,35-48,99-110). gap (proof) — isUsableOfflineSnapshot/OfflineAuthRestorePolicy implemented-unverified; no test exercises stale-snapshot rejection or cross-account restore denial.
- `src/lib/operational-transactions.ts` — eventTimingPlan and related hooks; route/timeline inputs feeding staffing windows. Reuse for route-aware call-time inputs (12.4).
- `src/lib/packingDisplay.ts` — Strips TPP export artifacts from pack descriptions; flags association-missing rows. Reuse in PackListItemTable.
- `src/lib/pdf/extractPdfText.ts` — Browser pdf.js extraction with label-segment collapse for production-worksheet PDFs reuse — browser PDF import path (pdfjs-dist dynamic import)
- `src/lib/pendingOperationKey.ts` — Stable idempotency keys + navigation recovery for client operations reuse: CF-4-3 submit-once/retry-without-duplicate behavior; tests/pending-operation.test.ts covers it
- `src/lib/prepWorkBalance.ts` — Pure one-line balance: required vs completed vs other-open work, unit-checked, never mutates completed/manual records; unresolved when units incomparable Reuse for any remaining-work calculation
- `src/lib/pricing.ts` — The single central proposal pricing path (§5.4): 5 bases incl. percentage, subtotal/tax/discount/total, round2 2dp reuse: consumed by draft form, proposalRevision snapshot, PDF, portal; float round2 carries a documented non-GL caveat (lines 76-78) relevant to CF-2-3-money-precision
- `src/lib/publicErrorMessage.ts` — Safe error text for unauthenticated pages (quote form, share links) reuse: CF-4-3/CF-4-6 public surfaces must render only ConvexError data
- `src/lib/recents.ts` — Per-browser localStorage recents list (push/dedupe/cap 20) with cross-tab sync hook (75 lines). reuse — deliberate per-browser scope, do not move to an entity without need
- `src/lib/recipeDisplay.ts` — Translate imported worksheet note text into kitchen copy (drops TPP markers/xlsx names, 'Yields:' → 'Makes', pipe-row formatting, Oz-Fld→fl oz vs Oz-Dry→oz) and render 4-decimal quantities as readable fractions Reuse wherever recipe source text is shown; source provenance intentionally stays in storage
- `src/lib/recipeUnitConversion.ts` — Exact US-customary mass/volume conversion ratios for the client; returns null across dimensions or for count units — never infers density or count size Reuse for client-side conversions; must stay in step with convex/lib/culinaryModel/units.ts and PrepTask.reconcileRemainingWork per its header
- `src/lib/recurringEventActions.ts` — Authored adapter for api.recurringEvents.configure (scheduler arming) reuse: thin wrapper; lifecycle changes still ride generated commands
- `src/lib/releaseBackendScope.ts` — Detect release backend changes including transitive src imports; list new zero-arg generated probes Reuse; existing release-backend-scope tests passed. Zero-arg anonymous query reachability is not authenticated workflow proof.
- `src/lib/releaseReceipt.ts` — Pure four-leg release receipt and Markdown formatter Preserve partial outcomes; update old cloud-deploy assumption and registry-count-only backend/workflow evidence for self-hosted production.
- `src/lib/review-criteria.md` — Sample nonprogrammatic review registry Needs Capsule-specific audience, design authority and real artifact paths before qualification.
- `src/lib/routeRecord.ts` — URL :id plausibility gate for generated useGetX hooks (NOT driving routes despite the name) reuse (UI routing only); do not confuse with §8.4 route facts
- `src/lib/safeCulinaryOperations.ts` — Seam hooks over convex/lib/culinaryOperations.ts: clone menu, import component, durable component-import review create/save, snapshot restore, event prep work reconcile/review Reuse; these are the governed write paths proven by AC-043/044 runtime proofs
- `src/lib/safeMaterialization.ts` — Durable operationKey receipts for applyPackTemplate/applyLayoutTemplate/draftPurchaseOrder. Reuse. Proven retry safety (AC-034). Template preview/change-exposure still missing.
- `src/lib/sourceProvenance.ts` — useSourceLinksByCapsuleId hook over the authored ExternalRecordLink-by-capsuleId seam reuse — consumed by EventSourceProvenancePanel.tsx and ProposalReadinessNotice.tsx
- `src/lib/staffSelfReviews.ts` — useMyReviews worker-self review reads. Reuse.
- `src/lib/staffShiftSwaps.ts` — Swap reads + useSwapNow clock refreshing at shift boundaries/focus. Reuse. Consumer ShiftSwapCard.tsx.
- `src/lib/staffSignInEmail.ts` — StaffSignInEmailRenderer — branded hire sign-in email (optional plaintext password block, 7-day link notice) REUSE — consumed by convex/lib/staffSignInMailer.ts (Resend send, env-gated). Sends plaintext password when supplied — normative concern worth an owner decision, outside both specs' acceptance text
- `src/lib/statusLabels.ts` — Enum-ish status → label/chip vocabulary reuse: CF-3-3/CF-5-1 readable stage/status rendering; new stages need entries here
- `src/lib/timeOff.ts` — findApprovedTimeOffConflict: half-open overlap of approved time off vs proposed shift. Reuse. Consumer RosterPage.tsx:272. Reusable idiom for a cross-event overlap detector.
- `src/lib/tppMenuCsv.ts` — RFC4180-lite CSV parser + TPP menu-export mapper producing feed rows with stable identity slugs (unique name → slug; collisions → category-qualified suffix) so re-import skips cleanly Reuse for CSV import surfaces; identity rule is the duplicate-prevention half of PR03-02 on the dish/menu side
- `src/lib/tppRecipeRepair.ts` — Project a TPP source recipe into dish/components/ingredients/tasks: nested subrecipe expansion with cycle notes, family-checked yield ratios, fluid-oz-is-volume unit map, serving-basis division only for explicit Serving yields, unmatched amounts retained as notes Consumed by scripts/repair-tpp-recipes.ts and scripts/repair-tpp-component-recipes.ts (offline repair), recipeNameKey reused by convex/lib/dishRecipeRepair.ts; no unit-test coverage found under tests/ — the parser-tested path is the workbench (component-text-parser), not this projection
- `src/lib/tppReports/archiveDelta.ts` — PR01-04 pure workbook-level delta (unchanged/changed/added/removed) from checksum maps reuse — convex/archiveInventory.ts:350-354
- `src/lib/tppReports/beoNoteSections.ts` — Splits BEO prose into the nine fixed note headings; maps to BundleNotes reuse — parseBeoWorkbook + parseBeoText
- `src/lib/tppReports/csvReports.ts` — CSV source detection + row-report parsing for the browser (no Node APIs) reuse — loadEventBundle + loadEventBundleFromText
- `src/lib/tppReports/csvRows.ts` — RFC 4180 CSV reader with TPP backslash-quote handling, blank-row and label/value helpers reuse — consumed by csvReports and all row parsers
- `src/lib/tppReports/eventBundle.ts` — Normalized event-bundle shape (header/client/venue/timeline/menu/prep/pack/orders/staff/payments/totals/notes) reuse — all parsers and merge
- `src/lib/tppReports/loadEventBundle.ts` — Files -> merged bundle by content-shape detection; exports detectWorkbookSource reuse — agent import path; detectWorkbookSource reused by workbookDisposition.ts:71
- `src/lib/tppReports/loadEventBundleFromText.ts` — Browser path: pasted text + CSV/packet files -> bundle reuse — assistant/import UI
- `src/lib/tppReports/mergeEventBundle.ts` — Authority-ordered reconciliation of report parts with disagreement warnings reuse — both loaders
- `src/lib/tppReports/parseBattleBoard.ts` — Battle-board PDF -> staff roster + column-position timeline reuse — loadEventBundle.ts:63-65
- `src/lib/tppReports/parseBeoText.ts` — Pasted BEO/worksheet text -> same bundle shape line by line reuse — loadEventBundleFromText.ts:40-43
- `src/lib/tppReports/parseBeoWorkbook.ts` — BEO xlsx -> header/venue/contact/timeline/menu/notes reuse — loadEventBundle.ts:73
- `src/lib/tppReports/parseEventWorksheet.ts` — Event worksheet -> bundle part (header, client, venue, timeline, menu with servings) reuse — csvReports.ts:52-64
- `src/lib/tppReports/parseOrderList.ts` — Order list -> vendor-grouped order lines reuse — csvReports.ts:57-59
- `src/lib/tppReports/parsePackList.ts` — TPP pack-list report parser: items with qty/unit/|bin|/name, classification headings, For: groupings, printed-date footer (parsePackList.ts:1-60). reuse — the §6.3 extractor feed; commit seam preserves provenance (importCommit.ts:72-80,1391-1480).
- `src/lib/tppReports/parseProductionWorksheet.ts` — Production worksheet -> prep tasks incl. items without tasks reuse — loadEventBundle.ts:74-76
- `src/lib/tppReports/parseProposal.ts` — Proposal -> priced menu, payment history, totals, other contacts reuse — csvReports.ts:60-62
- `src/lib/tppReports/pdfObjects.ts` — Minimal PDF indirect-object scan and font-resource walk reuse — pdfTextReader
- `src/lib/tppReports/pdfTextReader.ts` — Positioned PDF text via per-font ToUnicode CMaps; line/cell reconstruction reuse — parseBattleBoard via loadEventBundle.ts:64
- `src/lib/tppReports/reportValues.ts` — Shared value readers: report dates, clock minutes, money cents, phone/email/address blob splitting, person blob, venue address line reuse — every parser
- `src/lib/tppReports/rtfToText.ts` — TPP .rtf BEO/worksheet -> plain text for parseBeoText reuse — RTF import path; consumer is the RTF-reading caller of parseBeoText
- `src/lib/tppReports/workbookDisposition.ts` — PR01-02 classifier: one disposition + counted row outcomes per workbook, headers/summary separate reuse — convex/archiveDisposition.ts:163-179
- `src/lib/tppReports/workbookProvenance.ts` — PR01-03 evidence document: coordinates, raw vs interpreted, date system, parser version, marked truncation, 256 KiB budget reuse — convex/archiveProvenance.ts:155-165
- `src/lib/tppReports/xlsxReader.ts` — Typed xlsx reader: named cell outcomes, own-cell units, cached formulas recorded not executed, 1900/1904 detection reuse — archive seams and XlsxReportGrid
- `src/lib/tppReports/xlsxReportGrid.ts` — Renders typed workbook as the printed TPP grid (M/D/YYYY dates, h:mm AM/PM, unit suffixes) reuse — loadEventBundle.ts:70
- `src/lib/tppReports/xlsxValues.ts` — Pure serial/date/fraction/accounting interpretation, phantom leap day, format-unit literals, XLSX_PARSER_VERSION reuse — xlsxReader
- `src/lib/tppReports/zipReader.ts` — Hardened stdlib ZIP reader: named ZipArchiveError codes, pre-flight limits, maxOutputLength inflate bound, pure (no writes) reuse — convex/archiveInventory/archiveProvenance/archiveDisposition
- `src/lib/useAuthStatus.ts` — Shared live auth-status read with offline-snapshot fallback for My Day; integration-guard-safe substitute for direct convex/react imports (src/lib/useAuthStatus.ts:5-17). reuse — consumed by ReportsPage.tsx:435 and LiveReportData.tsx:139 for role/capability-gated report reads.
- `src/lib/useEnsureOpenClientOutreach.ts` — Hook for convex/lib/clientOutreach.ts ensureOpen (find-or-create one open ClientOutreachTask per client) REUSE — wired in src/features/clients/ClientRetentionPage.tsx; relevant to PR07-08 'no anniversary/outreach task from imports' because outreach tasks are operator-triggered, not import-triggered
- `src/lib/useEventImportRetry.ts` — Retry hook re-driving saveEventFromBeo for a failed import onto an existing event reuse — EventImportDraftPanel.tsx:66
- `src/lib/useSavedViewOperations.ts` — Thin hook pair wrapping api.lib.savedViewOperations create/setDefault (9 lines). reuse — follows the authored-seam-hook placement rule (facilities/lib seam hooks)
- `src/lib/utils.ts` — clsx, uuid, debounce, throttle helpers (53 lines). reuse — prefer over adding a clsx-type dependency
- `src/lib/vercelInspectParse.ts` — Parse deployment uid/id, commit SHA, URL and ready state across CLI shapes Reuse existing parser and tests; missing identity stays unknown.
- `src/lib/workforceScheduling.ts` — useScheduleShift wrapper over the authored scheduling seam. Reuse. Consumer RosterPage.tsx.
- `src/lib/workspace.ts` — TENANT_PLACEHOLDER for legacy generated validators (server derives tenant from verified identity) + workspace display name (src/lib/workspace.ts:6-11). reuse — placeholder is ignored server-side per getAuthContext; no security gap in the seam itself.
