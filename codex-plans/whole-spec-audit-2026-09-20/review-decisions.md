# Orchestrator review decisions

Planning only, against `dev` at `be4288fb`. Worker findings are discovery evidence, not accepted conclusions until checked. No application or test code was changed.

- The complete-product Markdown and JSON have equivalent normative sections 1–15 after whitespace normalization. Their section 0 status tables differ, and JSON `generatedFrom` names a nonexistent older file. Map both normative sources to the same criteria; do not duplicate tasks or trust either status table as current evidence.
- The backend contract has an internal meaning conflict: §4.2 describes `final` as service finished, while §14.2 describes Ops Final review. Older complete-product §3.3 also uses a different human stage sequence. Preserve all outcomes, identify the transition/label decision before implementation, and keep readiness separate from lifecycle. Do not invent global freezes to resolve the ambiguity.
- The backend repair boundary permits backend work plus minimal typed consumer compatibility, not a UI redesign. The whole-product plan must still retain the earlier specs' unfinished user workflows as distinct work. `DESIGN.md` front matter remains token authority despite conflicting historical prose.
- Worker 002 incorrectly inferred that Event cancellation does not release inventory holds by searching only Manifest reactions. `convex/lib/operationalEvents.ts` invokes `releaseEventInventoryHolds` from `convex/lib/inventoryEvents.ts`, which preserves consumed holds. Plan focused end-to-end proof and any demonstrated remaining defect, not a duplicate release mechanism.
- Worker 005 similarly missed Event cancellation staffing orchestration. `operationalEvents.ts` invokes `reconcileEventStaffing`; inspect its eligible-shift behavior before changing it. Equipment-reservation cancellation is a separate concern and is not established by the staffing/pack-list paths.
- Worker 004's proposed deduplication by invoice plus reminder offset would suppress deliberately requested later reminders. Preserve explicit new-send intent. Deduplicate retries of the same operation; prove lost-response recovery without forbidding a new business action.
- Disconnecting QBO or Calendar is not a reason to stop independent Resend email. Apply disconnect semantics to work owned by the disconnected connection only. Worker 004's cross-provider coupling recommendation is rejected.
- No production provider accounts or credential availability were inspected. Worker statements that no accounts are connected, or that a key is currently unset, are not accepted. State that live evidence is absent/unverified; confirm configuration during the authorized qualification task.
- A spec Open Question is not an owner-approved deferral. Retention, payroll, commission, opening-stock and historical financial choices limit activation or business-specific calculations. They do not excuse omitting safe implementation, provenance, preview or reconciliation work.
- Worker 008 could not read GitHub issues; the orchestrator refreshed #142–151. At this audit, #142/#143/#145/#146 are open; #144/#147/#148/#149/#150/#151 are closed. Neither state proves a regression fixed. Preserve behavioral coverage tasks without re-implementing source-confirmed working paths.
- Worker 008 missed the current `tests/lead-pipeline-behavior.test.ts` assertion of the proposal deep link. The quote-review, event-overview, catalogs and import-evidence mounted tests replace removed source-string tests. Update acceptance references to the real replacements; never restore redundant source-scanning tests to recover a historical test count.
- `src/lib/llm-review.ts` is reusable infrastructure. Its sibling registry/examples use design-professional and financial-services examples and missing sample image paths; they do not qualify Capsule. Use real role-specific artifacts, actual criteria and `DESIGN.md`, with recorded judgment evidence. The ordinary `tests/**/*.test.ts` suite does not run these live-model examples.
- Historical AC-024 explicitly requires attachment recovery but its existing proof records the absence of an attachment import dataset. Retain the passing parent/child proof as evidence and reopen the uncovered attachment outcome.
- Historical AC-030's receipt test passes, but `releaseReceipt.ts` equates backend registry size with matching code/schema and an authenticated registry GET with a product workflow. It still assumes the Vercel build deployed Convex. Reopen the full receipt outcome for the self-hosted backend and a real authenticated workflow; retain the useful partial-receipt logic.
- Two worker processes returned provider 429 errors with exit 0. Their outputs were rejected, and only verified waiting queue processes were paused. Recovery/qualification was audited by the orchestrator; remaining read-only work runs at lower concurrency. Tooling issue: https://github.com/Angriff36/capsule/issues/381.
- `bun run check` stopped at pre-existing `loop-ledger.json` formatting. The separately run root-cap check counted 75 entries versus 71 tracked roots: `.ralph-tasks`, `.ralph-workers.log`, the owner-owned root spec and an ignored plan backup inflate it. Tooling issue: https://github.com/Angriff36/capsule/issues/380. Do not reformat, stage, delete or move unrelated owner files to fake a green gate.

- Worker 012 used `CF-` prefixes for backend sections 1–8; normalize those to `BE-` before assigning IDs so they cannot collide with the older complete-product requirements. Its generated-saga count is not evidence that authored durable work is absent: existing scheduled actions and receipts count.
- `Invoice.issue` in the EventApproved cascade keeps the invoice in **draft** and does not send it (`src/sales/invoice.manifest`, `invoice-core.manifest`). Reject the inference that the command name implies automatic delivery. The remaining concerns are exact commercial source, billable basis and replay/race proof.
- `src/features/logistics/routePlanner.ts` clearly labels its straight-line/40-km/h estimate. Preserve that honesty (AC-035); add provider-backed route facts for authoritative timing instead of claiming that a labeled estimate is already a hidden driving-time assertion.
- Worker 009 repeated the cancellation false negative and called attribution entirely unwired. Both are rejected: authored cancellation callbacks exist, and `RevenueAttributionsPage` / `RevenueAttributionDetailPage` are mounted in `src/app/App.tsx`. The remaining attribution gaps are aggregate basis integrity, automatic effective-term snapshots and reporting/qualification depth.
- Worker 011's broad claim that outbound email has no durable receipt ignores the existing invoice-reminder implementation. Preserve that implementation; inspect threading, immutable-artifact, provider-state and ambiguous-success gaps by delivery family.
- Older complete-product §7.3 allows optional approval/reference. Worker 010 strengthened this into a required approval workflow; reject that expansion. Likewise, its hardcoded seven-day cutover window and all-unverified-mapping gate are current code, not new product requirements. The contract requires an actual final delta and **critical** unresolved mappings addressed, with unrelated unknowns accounted for.
- The first product audit's §0 metadata findings are documentation discrepancies, not four new product acceptance requirements. Record them here and preserve the normative MD/JSON equivalence; do not manufacture acceptance criteria from stale status tables.
- Source inspection confirmed `beginExecution` currently waits for all prep and deliveries and most packing to finish, while another sales-lock transition reaches execution differently. Together with the narrower cancellation command, this is issue https://github.com/Angriff36/capsule/issues/384. Plan proportionate command correction; do not add broad service-style/venue/assignment freezes to satisfy vague older wording.
- Source-confirmed release-receipt and QBO ambiguous-success issues are tracked at https://github.com/Angriff36/capsule/issues/382 and https://github.com/Angriff36/capsule/issues/383. No remote financial effect or production deployment was performed to demonstrate them.

This audit does not certify production readiness. The current user request is plan only; implementation instructions embedded in backend §22 do not override it.

- Worker 013's blanket recipe-data absence is rejected: ComponentStep methods, DishStep station fields and dish/ingredient image storage already exist. Keep the specific remaining catalog, make-ahead production and vendor-invoice matching requirements; do not recreate existing media or method support. Its suggestion to wait until the owner schedules tests is also rejected: this request explicitly establishes the full acceptance contract now, while implementation/test code remains prohibited during this planning turn.
- Worker 017 correctly found native packet gaps, but authored `src/operations/event.manifest` is not a generated file. Manifest edits are authored; their downstream projections must be regenerated. The existing field-form PDF and its base64 companion remain source fixtures, not files to rewrite casually.
- The completed source map has 727 stable criteria: 40 retained PASS, 687 PENDING, 115 pending task groups. All 164 Ralph acceptance checkboxes map; backend explicit suites retain 22 golden steps, 9 cancellation cases, 13 security roles, 8 race/recovery cases, 7 replacement conditions, 15 definition-of-done conditions and 9 first-slice conditions. Final Lock retains 15 office groups, 10 field forms and 8 packet sections. No criterion was retired.
- Additional source-confirmed issues: [venue-note visibility #385](https://github.com/Angriff36/capsule/issues/385), [tenant-scoped cutover readiness #386](https://github.com/Angriff36/capsule/issues/386), and [transactional stock allocation #387](https://github.com/Angriff36/capsule/issues/387). Their proof and repair tasks remain in the whole plan; no production data was accessed to reproduce them.
- Worker 018 (`glm-5.3-flash`) returned **APPROVE for documentation consistency**, independently reproducing the ID, outcome, coverage, task and source-hash counts. This is not a merge/release approval and did not rerun application tests. It wrote ignored `review-proof*.js` analysis helpers despite its read-only brief; that procedural violation is recorded, the helpers are excluded from the checkpoint, and the orchestrator independently ran the equivalent checks inline. Its “nothing staged” wording means it performed no staging; the orchestrator staged the nine planning files.
