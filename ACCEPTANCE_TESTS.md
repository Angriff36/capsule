<!--
Completion contract. Generated/updated by PLAN mode from acceptance criteria in specs/*.
BUILD mode gates completion on this file: every non-RETIRED criterion needs a real,
passing test — no deleting, skipping, or weakening assertions to fake a pass.

One row per acceptance criterion. Ids are stable: assign AC-001, AC-002, ... once and
never renumber. Status: PENDING (required verification incomplete) | PASS (required evidence exists + passes)
| RETIRED (criterion dropped from specs — keep the row, add a one-line reason).

Release: "Booked without re-keying" (plan date 2026-09-03; AC-001 … AC-019). Scope = specs/ralph/
signed-proposal-becomes-the-event.md, public-quote-form.md,
dropdown-lists-and-their-admin-screen.md, plus the feature-spec §4.3 done-when.
known-bugs-142-to-151.md is out of this release; its criteria get ids when
it is scheduled. The client portal / pay-from-phone job has code but no spec
yet (see IMPLEMENTATION_PLAN.md future work); it gets a spec and ids when
scheduled.

Release 2: "Every source record accounted for" (plan date 2026-09-05;
AC-020 … AC-030). Scope = specs/ralph/production-01-import-archive.md
PR01-01..07+09 (PR01-08 excluded — cancel/compensation is owned by the later
import-operations slice with PR13-08), specs/ralph/production-12-access-privacy.md
PR12-01 and PR12-05, and specs/ralph/production-13-release-recovery.md PR13-06.
Ids AC-020 through AC-030 are assigned in that order. Remaining PR01–PR14
criteria get ids when their releases are scheduled.

Verification kind: P = programmatic (runtime proof / unit test). J = human-like
judgment — run the llm-review gate (src/lib/llm-review.ts, criteria table
src/lib/review-criteria.md, UX-01 / TONE-01) on the rendered copy or screenshot
in addition to the programmatic test. `createReview()` THROWS when
ANTHROPIC_API_KEY is unset (llm-review.ts:134,137) and is not wired into
`bun run check` (vitest `include` is tests/**/*.test.ts only, vite.config.ts:103,
so src/lib/llm-review.test.ts never runs in the gate; run it by path:
`bunx vitest run src/lib/llm-review.test.ts`). J reviews run manually with the key present; output is saved
under .artifacts/llm-review/<AC-id>.md; they are never added to `bun run
check`. The P test is the gate the loop halts on; the J review is recorded
evidence.

Runtime proofs live in tests/proofs/*.runtime.test.ts. Everything under
tests/proofs/** runs in the `edge-runtime` environment (vite.config.ts:104
`environmentMatchGlobs` is `[["tests/proofs/**", "edge-runtime"]]`; the
default is `node` for every other test file). Proofs boot
via `createManifestTestContext({ convexTest, schema, modules })` from
`@angriff36/manifest/proof-kit/convex-test` with `modules` from
tests/proofs/convex-test-modules.ts, and need the CONVEX_FIELD_ENCRYPTION_KEY
`beforeAll` fallback (pattern: tests/proofs/event-approve-opens-packlist
.runtime.test.ts:1-30). New proof files for Event/Proposal/QuoteSubmission
need no registry entry: scripts/emit-proof-kit.ts binds `runtimeTest` paths
only for CATALOG_ENTITIES capabilities (:43-52, :232-266) and `check:proof`
validates only those generated artifacts — the booking
proof runs unregistered. Unit tests under tests/features/** are
pure-helper, source-contract, and mounted component interaction tests. React DOM
createRoot/act with installed jsdom supports render/input/click tests without
@testing-library/react; do not substitute source strings for interaction proof.
-->

| Id | Spec | Outcome to verify (WHAT, not HOW) | Required test | Kind | Status |
| ------ | ------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------- | ---- | ------- |
| AC-001 | specs/ralph/signed-proposal-becomes-the-event.md | Creating an event from an accepted proposal copies every live menu selection onto the event with `quantityServings` unchanged; removed selections are not copied. Proof lives under `tests/proofs/`. | `tests/proofs/proposal-event-booking.runtime.test.ts` › "books the event, links the proposal, and copies live menu selections" (moved under `tests/proofs/` 2026-09-04; 7/7 green under edge-runtime) | P | PASS |
| AC-002 | specs/ralph/signed-proposal-becomes-the-event.md | From the created event a user can reach the source proposal (reverse lookup `listProposalByEventId`) and see which revision was accepted (highest `revisionNumber`, or `SignatureRequest.proposalRevisionId` for digital accepts); a proposal with no revision (agent bundle path, issue #241) shows the proposal number and "no revision captured" without throwing; the event does not rely on free-text copies for that link. | `tests/proofs/proposal-event-booking.runtime.test.ts` › "event resolves its proposal and accepted revision"; `tests/features/events/event-proposal-source-card.test.ts` › "event detail links the booking proposal and its accepted revision" | P | PASS |
| AC-003 | specs/ralph/signed-proposal-becomes-the-event.md | When the proposal venue name matches a saved Venue (case/whitespace-insensitive) the event gets that `venueId`; when it does not, the create screen shows a visible mismatch notice and the operator can pick or create a venue. | `tests/features/events/proposal-event-prefill.test.ts` › "venue match and mismatch notice" | P | PASS |
| AC-004 | specs/ralph/signed-proposal-becomes-the-event.md | Date, start time, end time and headcount from the proposal arrive on the event as typed fields (`startsAt`, `endsAt`, `expectedHeadcount`) without re-entry; a proposal with no end time says so on the create screen instead of silently leaving the field blank. | `tests/proofs/proposal-event-booking.runtime.test.ts` › "typed date, times and headcount carry over" | P | PASS |
| AC-005 | specs/ralph/signed-proposal-becomes-the-event.md | Enhancements the client accepted are visible on the event (event-side card or record), not only on the proposal. | `tests/proofs/proposal-event-booking.runtime.test.ts` › "accepted enhancements reachable from the event"; `tests/features/events/event-enhancements-card.test.ts` › "event detail lists live enhancements from the linked proposal" | P | PASS |
| AC-006 | specs/ralph/signed-proposal-becomes-the-event.md | Before committing, the create-event screen lists exactly which proposal values will carry over (title, type, date, times, headcount, venue, menu count, enhancement count) and why linking will or will not happen. | `tests/features/events/proposal-event-prefill.test.ts` › "preview lists carried values" passes; required llm-review UX-01 receipt on the preview copy is missing (2026-09-06 evidence audit) | P+J | PENDING |
| AC-007 | specs/ralph/public-quote-form.md | A submitted quote appears in the sales queue showing contact, event date, guest count, service style, occasion, venue text and menu selections exactly as entered; unset style/occasion reads "Not specified". | `tests/features/sales/quote-submissions-review.test.ts` › "queue shows all seven submitted fields" | P | PASS |
| AC-008 | specs/ralph/public-quote-form.md | One convert action creates or links the client (match by email) and produces a draft proposal pre-filled with the prospect's selections, linked to the event it created (`proposal.eventId == event._id`). | `tests/proofs/quote-conversion.runtime.test.ts` › "convert builds client, lead, event and linked proposal" | P | PASS |
| AC-009 | specs/ralph/public-quote-form.md | Re-submitting the same contact + event date creates no second submission or lead; a different event date for the same contact does. | `tests/proofs/quote-conversion.runtime.test.ts` › "dedup by contact and event date" | P | PASS |
| AC-010 | specs/ralph/public-quote-form.md | A junk or duplicate submission can be dismissed with a reason; the raw submission remains readable, leaves the default queue, and still participates in dedup. | `tests/proofs/quote-conversion.runtime.test.ts` › "dismiss keeps the raw submission" | P | PASS |
| AC-011 | specs/ralph/public-quote-form.md | Conversion succeeds when `serviceStyles`/`occasions` have no matching row; the prospect's value surfaces as text on the submission and proposal, and nothing throws. | `tests/proofs/quote-conversion.runtime.test.ts` › "empty catalogs convert as text" | P | PASS |
| AC-012 | specs/ralph/dropdown-lists-and-their-admin-screen.md | An admin/owner can add, relabel, retire and reactivate service styles and occasions from the app; the event-create selector reflects the change without redeploy or reload. | `tests/features/admin/catalogs-page.test.ts` (new directory) › "wires register, revise, deactivate, activate per catalog" | P | PASS |
| AC-013 | specs/ralph/dropdown-lists-and-their-admin-screen.md | With zero service-style/occasion rows the operator can still create an event; each selector shows an explicit empty state with a path to fix it, never a silent blank or a crash. | `tests/features/events/create-event-blockers.test.ts` › "empty catalogs show an explicit state and do not block create" passes; required llm-review UX-01 receipt on the empty-state copy is missing (2026-09-06 evidence audit) | P+J | PENDING |
| AC-014 | specs/ralph/dropdown-lists-and-their-admin-screen.md | The public quote form submits when catalogs are empty; the free-text style/occasion fallback is captured on the submission; a missing organization row is reported to staff with a link to where it is created (`/admin/branding`), not hidden. | `tests/proofs/quote-conversion.runtime.test.ts` › "public submit with empty catalogs captures free text"; `tests/features/sales/quote-submissions-review.test.ts` › "offline notice when no organization" | P | PASS |
| AC-015 | specs/ralph/dropdown-lists-and-their-admin-screen.md | A retired service style is absent from new-event selectors but still resolves by id on existing events and imported records. | `tests/features/events/service-style-retired.test.ts` › "retired hidden on create, resolved on detail" | P | PASS |
| AC-016 | specs/ralph/dropdown-lists-and-their-admin-screen.md | A runtime proof creates an event with empty catalogs (null ids accepted) and with populated catalogs (ids persist and resolve). | `tests/proofs/event-create-catalogs.runtime.test.ts` › "create with empty and populated catalogs" | P | PASS |
| AC-017 | specs/capsule-complete-feature-spec.md §4.3 done-when + specs/ralph/signed-proposal-becomes-the-event.md (composite journey; §4.3 alone ends at the draft proposal) | End to end: a client submits once, sales sees the lead with all selections, converts without re-entry, the proposal is sent and accepted, and the created event carries date, times, headcount, venue, menu servings and enhancements, with the proposal pointing at the event. | `tests/proofs/quote-to-booked-event.runtime.test.ts` › "quote to booked event journey" | P | PASS |
| AC-018 | specs/ralph/public-quote-form.md | After conversion, sales reaches the created proposal in one click from the quote queue and from the lead pipeline (deep link `/clients/proposals?proposal=<id>`), and the queue is reachable from the pipeline. | `tests/features/sales/quote-submissions-review.test.ts` › "queue and pipeline deep-link to the converted proposal" | P | PASS |
| AC-019 | specs/ralph/public-quote-form.md | A conversion that failed part-way shows which records were already created (client, lead, event, proposal links from the checkpointed ids), can be retried without duplicating those records, and can be dismissed; no partial record is unreachable from the queue. | `tests/features/sales/quote-submissions-review.test.ts` › "failed row shows checkpointed records"; `tests/proofs/quote-conversion.runtime.test.ts` › "retry after partial failure reuses checkpointed records" | P | PASS |
| AC-020 | specs/ralph/production-01-import-archive.md PR01-01 | Uploading an archive whose supplied index describes 70 reports while the archive contains 90 workbooks: Capsule inventories all 90 actually-present workbooks, explains the index-vs-archive discrepancy, and commit does not proceed until the discrepancy is accounted for. Fixture is a synthetic zip (index describes fewer workbooks than it contains), never the private 90-workbook archive. | `tests/proofs/import-archive-inventory.runtime.test.ts` › "upload inventories every workbook and reconciles the supplied index before commit" (green 2026-09-05; 90 artifacts w/ checksums, commit gate = the generated `ImportRun_commit` guard, refused until `explainArchiveDiscrepancy`; extended 2026-09-05 with the R2-5 classify step — the zero-unaccounted guard added by AC-027; extended the same day after the gpt-5.6-sol merge review with two legs: an equal-count index substitution (2==2, disjoint names) blocks commit via the persisted indexNameMismatch flag until explained, and a crash-window artifact draft is repaired in place — re-registered and stamped — never skipped; review round 2: same-run rows are inspected BEFORE the cross-run duplicate short-circuit, so a crashed inventory of these bytes is finished idempotently instead of stranding half-registered rows; review round 3: same-run rows are PROVEN to belong to the held archive (name + checksum) — a retry with different bytes is refused loudly, never mixed) | P | PASS |
| AC-021 | specs/ralph/production-01-import-archive.md PR01-02 | Every workbook ends in exactly one disposition — normalized, linked reference, duplicate view, needs mapping, unsupported, or invalid — and every data row/section has a counted outcome; headers and summaries are classified separately. Counts reconcile to the source with no silently dropped rows. | `tests/proofs/import-archive-disposition.runtime.test.ts` › "every workbook and row has a counted, reconciled disposition" (green 2026-09-05; content-shape classifier reuses detectWorkbookSource — header/summary/normalized/needs_mapping per row, duplicate detection by content checksum, governed manual linked_reference relabel keeps counts; extended 2026-09-05 after the gpt-5.6-sol merge review: duplicate_view order is derived from the full stable artifact list, proven by a mid-pair classification-crash + resume leg) | P | PASS |
| AC-022 | specs/ralph/production-01-import-archive.md PR01-03 | An authorized operator can inspect, from the import record: the preserved original, its checksum, workbook/sheet/cell or section coordinates, raw text/value, date system, parser version, and normalized result — raw evidence stays inspectable separately from the interpreted value. | `tests/proofs/import-provenance.runtime.test.ts` › "source provenance is inspectable from the import record" (green 2026-09-05; governed action `recordArchiveProvenance` merges the R2-7 typed-reader document into ImportArtifact.provenance — raw serial "44927" beside interpreted "2023-01-01", own-cell units only, cached formula recorded not executed, merge coordinates on the anchor, named error for unreadable containers, idempotent re-run, works before or after classification, preserved original = run archiveStorageId/archiveChecksum + per-workbook checksum; extended 2026-09-05 after the gpt-5.6-sol merge review: provenance is byte-bounded — per-field value truncation (marked, typed values stay typed) plus a 256 KiB serialized budget, so one legal 32 KiB cell value can never blow the 1 MiB Convex document; review round 2: merge ranges share the budget and stop at a count cap, flagged mergedRangesTruncated; review round 3: each range is checked against the REMAINING budget before it is taken — a single 300 KB ref stops the loop instead of overshooting; review round 4: the anchor cell's own mergedRange copy is truncated too, and complete cell records are charged against the remaining budget before they are taken — a 1.2 MB anchor ref cannot re-enter through the cell); `tests/features/admin/import-provenance-panel.test.ts` › "operator panel renders coordinates, raw value, date system, parser version, and normalized result" (source-text test) | P | PASS |
| AC-023 | specs/ralph/production-01-import-archive.md PR01-04 | Reuploading identical bytes, restarting a worker after a committed command, or opening the run from another device creates no duplicate business records; a changed source revision produces an explicit delta against the prior import, not a second copy. | `tests/proofs/import-reupload-delta.runtime.test.ts` › "identical bytes no-op; changed revision produces an explicit delta" (green 2026-09-05; whole-archive checksum short-circuit vs a live non-reverted prior run — `status:"duplicate"` + priorImportRunId, registers nothing; worker restart = commit refused on the completed run AND a full quickImport repeat commits 0/skips all via the cross-run ExternalRecordLink dedup; cross-device reopen = same-run re-inventory 0/skipped, completed-run re-inventory rejected; changed revision registers fully + returns the explicit per-workbook delta with real sha256 values for both revisions across two successive revisions, covering unchanged/changed/added/removed) | P | PASS |
| AC-024 | specs/ralph/production-01-import-archive.md PR01-05 | A failure after any parent or child write — proven by fault injection at each checkpoint — leaves a run that resumes by creating only the missing children, deposits, lines, and attachments, never replacing records the user edited after the failure. | `tests/proofs/import-resume-fault-injection.runtime.test.ts` › "failure after any write resumes missing children without replacing newer edits" (green 2026-09-05; faults injected at every write boundary — venues batch stop 2-of-5 after parent writes, crash window between create and link, pack-list parent + partial children with per-item keys + user quantity edit, payments 1-of-2 deposit-reference stop; resume creates only the missing records, user edits survive via the create idempotency cache's zero-write returns; attachments have no ImportDatasetType member — the parent-scoped child write is the PackListItem shape, noted in the proof header; extended 2026-09-05 after the gpt-5.6-sol merge review: Fault A′ loses the stop checkpoint too, and the final checkpoint is floored at the run's live ExternalRecordLink count — committedCount finishes equal to durable links; review round 2: the link count accumulates in bounded 500-doc pages across transactions (501-noise-link leg finishes committedCount 502) and the recovered processedCount includes pending outcomes; review round 3: each page carries maximumRowsRead — a hard scan bound, not just a numItems target) | P | PASS |
| AC-025 | specs/ralph/production-01-import-archive.md PR01-06 | Excel 1900/1904 date systems, leap-day behavior, fractional-day times, timezones, sparse/merged cells, accounting parentheses, fractions, and missing cached formula values are handled explicitly (a named outcome, not silent coercion); missing units are never inferred from unrelated cells; formulas/macros are never executed. | `tests/xlsx-date-formats.test.ts` (green 2026-09-05; 20 pure-parser tests on synthetic TPP-shaped workbooks — pinned 1900/1904 serials incl. real 2024 leap day, phantom 1900-02-29 and pre-epoch serials named not coerced, fractional-day/elapsed/time-only clocks, naive-local assumption recorded, merged-range anchors + sparse grid alignment, accounting parens and fractions from both formats and text, cached/missing/shared formulas never executed, units only from the own-cell format literal, issue #274 shopping-list regression keeps raw serial + interpreted date) | P | PASS |
| AC-026 | specs/ralph/production-01-import-archive.md PR01-07 | Zip traversal, absolute paths, nested archive abuse, excessive expanded bytes, duplicate filenames, encrypted workbooks, and corrupt entries each produce a bounded, named failure with no writes outside source storage; limits and progress are visible before expensive processing begins. | `tests/zip-archive-abuse.test.ts` (crafted malformed buffers; 25 tests green 2026-09-05 — named ZipArchiveError codes incl. traversal/absolute_path/duplicate_name/nested_archive/encrypted/corrupt_directory/corrupt_entry/entry_bytes_exceeded/expanded_bytes_exceeded/too_many_entries, central-directory-only `listZipEntries`, structural no-filesystem assertion; extended 2026-09-05 after the gpt-5.6-sol merge review: inflate-time maxOutputLength bounds the allocation itself — a lying central directory declaring a small uncompressedSize cannot bypass the per-entry expansion cap, it fails `entry_bytes_exceeded`) | P | PASS |
| AC-027 | specs/ralph/production-01-import-archive.md PR01-09 | Archive success requires zero unaccounted records; partial, unsupported, and reference-only content stays visible in the completed run, and no completed badge implies all source data became operational records. | `tests/proofs/import-archive-completion.runtime.test.ts` › "completion requires zero unaccounted records" (green 2026-09-05; inventory seeds unaccountedRecordCount = workbook count, commit Guard refuses at 5>0, passes at 0, completed run keeps the disposition roll-up; review round 2: absence reads as zero — a pre-release legacy run without the R2 fields still completes) | P | PASS |
| AC-028 | specs/ralph/production-12-access-privacy.md PR12-01 | Deployment/startup checks detect a mismatched Clerk issuer, application keys, Convex audience, callback URLs, or environment; missing configuration produces a redacted, actionable error, and a development credential cannot silently qualify as production-ready. | `tests/deployment-config-check.test.ts` › "each mismatch is detected; missing config errors are redacted and actionable" (green 2026-09-05; pure checker `src/lib/deploymentConfigCheck.ts` + CLI `scripts/check-deployment-config.ts` — 11 mismatch blockers: issuer-vs-key environment, pk/sk pair environment, gateway-vs-frontend publishable-key divergence, issuer-vs-frontend-API host, Convex audience vs the auth.config applicationID "convex", Clerk callback allowlist vs site origin, localhost backend/public-origin in production, VITE_CONVEX_URL vs --expected-deployment, redirect-uri origin, public-app-url vs site; `config:missing:`/`config:placeholder:` findings are redacted (variable names + key classes pk_test_* only, never values) and actionable (Vercel project env / `npx convex env set --prod`); `clerk:dev_credential_in_production` = the issue #265 detector, wired into scripts/vercel-build.sh production builds, scripts/release.sh pre-flight, and the src/main.tsx startup console) | P | PASS |
| AC-029 | specs/ralph/production-12-access-privacy.md PR12-05 | File upload association, metadata, URL retrieval, download, and export each verify the file's tenant and an authorized parent record: knowing a storage ID or attachment ID grants nothing, and an expired/revoked share stops issuing usable access under its documented policy. Synthetic tenants and records only. | `tests/proofs/file-storage-ownership.runtime.test.ts` › "a storage id outside authorized parent records yields no URL" (green 2026-09-05; `urlsForStorageIds` resolves an id only through a live referencing row in the caller's tenant — an Attachment row any parent type, or a Dish/Ingredient primary image, the `convex/lib/blobs.ts` walk tenant-scoped and live-only; proven: own-tenant URLs resolve on both surfaces, a stored-but-unreferenced blob id yields null, a foreign tenant holding the exact same ids gets null on all three, generated `listAttachmentByStorageId` metadata stays tenant-filtered both ways, `Attachment_remove`/`Dish_purge` stop the URL; every production caller passes an entity-field id, so no flow broke) | P | PASS |
| AC-030 | specs/ralph/production-13-release-recovery.md PR13-06 | The release receipt ties the exact integrated SHA to the canonical Vercel URL, a READY deployment, matching Convex code/schema, configuration checks, and a successful authenticated production workflow; any missing leg keeps the receipt partial — a stale alias or backend is never called shipped. | `tests/release-receipt.test.ts` › "complete only with READY Vercel + matching Convex + config checks + authenticated workflow; otherwise partial" (green 2026-09-05; pure builder `src/lib/releaseReceipt.ts` — complete requires all four legs verified; 20 degradation scenarios each yield PARTIAL with its named code, incl. `vercel:stale_alias` (canonical URL serves an older sha) and `convex:command_surface_mismatch` (stale backend); unverified ≠ success. Gatherer `scripts/release-receipt.ts` wired into `scripts/release.sh` after the one main push: `vercel inspect <canonical-url> --json` (READY + commit sha === integrated sha), `vercel env pull --environment production` values into a temp file deleted after the check feeding the PR12-01 checker `--json`, GET `/api/manifest/commands` 401-anonymous/200-authenticated (`CAPSULE_API_KEY`; edge + API-key gateway + Clerk + Convex end to end), deployed registry count vs `convex/http.ts` COMMAND_DISPATCH count; every absent credential keeps its leg unverified → partial, no softening; smoke-run on this checkout produced an honest all-unverified partial receipt) | P | PASS |

## Full wiring audit — 2026-09-06

Scope: `specs/ralph/no-fake-data-or-dead-buttons.md`, detailed repairs/coverage in `codex-plans/full-wiring-audit/`. Historical AC-001..030 remain unchanged. Source sweep and generator blocker evidence are recorded in the findings ledger; #113 is not represented as a working seed capability. New rows remain PENDING until their actual tests and independent review establish the stated outcome. Passing these rows does not certify unrelated PR01–PR14 work or deployment.

| Id | Spec | Outcome to verify (WHAT, not HOW) | Required test | Kind | Status |
| --- | --- | --- | --- | --- | --- |
| AC-031 | specs/ralph/no-fake-data-or-dead-buttons.md | Proposal-template revise/archive/reactivate target the generated docId contract; authored command/adaptor scan has no confirmed missing targets. | tests/delivery-honesty.test.ts plus recorded generated-contract/adaptor scan | P | PASS |
| AC-032 | specs/ralph/no-fake-data-or-dead-buttons.md | KDS completion submits entered actual yield including zero, keeps failed input and does not fabricate historical provenance. | tests/features/production/kitchen-display-yield.test.ts | P | PASS |
| AC-033 | specs/ralph/no-fake-data-or-dead-buttons.md | External inbox threads explain unavailable delivery, copy/retain drafts without creating outbound rows, and expose undelivered legacy records; internal notes still save. | tests/features/sales/message-inbox-delivery.test.ts; tests/delivery-honesty.test.ts | P | PASS |
| AC-034 | specs/ralph/no-fake-data-or-dead-buttons.md | Pack/layout template generation and draft-PO recovery cannot duplicate confirmed writes or hide partial completion after failure/retry. | tests/proofs/safe-template-materialization.runtime.test.ts; tests/pending-operation.test.ts; tests/event-draft-po-coordinator.test.ts | P | PASS |
| AC-035 | specs/ralph/no-fake-data-or-dead-buttons.md | Route/vendor/revenue estimates disclose their real basis; commission totals use applied allocations/application time, preserve missing-person amounts, and infer no payout. | tests/features/factual-values.test.ts | P | PASS |
| AC-036 | specs/ralph/no-fake-data-or-dead-buttons.md | Culinary clone/import/restore commits atomically or retains truthful, safe completion after partial failure; snapshot capture failure is visible. | tests/proofs/safe-culinary-operations.runtime.test.ts; tests/culinary-recovery.test.ts; tests/component-snapshot-capture.test.ts (warning helper, page wiring reviewed) | P | PASS |
| AC-037 | specs/ralph/no-fake-data-or-dead-buttons.md | Bulk/event menu/prep/stock/timeline work exposes progress and safe retries without repeated stock consumption or hidden partial writes. | tests/proofs/operational-transactions.runtime.test.ts; tests/event-menu-stock-sync-lifecycle.test.ts; tests/bulk-run-failure.test.ts (runtime/controller proof; page wiring independently reviewed) | P | PASS |
| AC-038 | specs/ralph/no-fake-data-or-dead-buttons.md | Attachment/default-view failures are visible/recoverable, outreach retry avoids duplicate open tasks, and confirmed edited apply amounts survive reactive refresh. | tests/task-6-mounted-interactions.test.ts; tests/proofs/personal-views-and-outreach.runtime.test.ts | P | PASS |
| AC-039 | specs/ralph/no-fake-data-or-dead-buttons.md | Template selection defaults persist through proposal draft/publication/client/PDF rendering without live-template drift, competing fee math or private-data exposure. | tests/proposal-create-form.test.ts; tests/proofs/proposal-template-publication.runtime.test.ts; tests/proposal-pdf-projection.test.ts; tests/proposal-public-renderers.test.ts | P | PASS |
| AC-040 | specs/ralph/no-fake-data-or-dead-buttons.md | Status-only proposal/contract/initial-invoice actions accurately describe publication/manual delivery recording; genuine reminder delivery remains intact. | tests/delivery-honesty.test.ts | P | PASS |
| AC-041 | specs/ralph/no-fake-data-or-dead-buttons.md | Internal operation receipts and child results cannot be retrieved through unrelated generated wrappers or public receipt queries; tenant isolation and atomic retry recovery remain intact. | tests/proofs/materialization-receipt-privacy.runtime.test.ts; safe-template-materialization, safe-culinary-operations and operational-transactions runtime proofs | P | PASS |
