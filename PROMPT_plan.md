0a. Study @AUDIENCE_JTBD.md to understand who we're building for and their Jobs to Be Done.
0b. Study all files recursively under `specs/` with up to ${RALPH_MAX_READ_AGENTS} parallel ${MODEL_REVIEW} subagents to learn the JTBD activities.
0c. Study @IMPLEMENTATION_PLAN.md (if present) to understand the plan so far.
0d. Study all files recursively under `src/lib/` with up to ${RALPH_MAX_READ_AGENTS} parallel ${MODEL_REVIEW} subagents to understand shared utilities & components.
0e. For reference, the application source code is all files recursively under `src/`.

1. Sequence the activities in `specs/` into a user journey (story) map for the audience in @AUDIENCE_JTBD.md. Consider how activities flow into each other and what dependencies exist.

2. Determine the next SLC release. Use up to ${RALPH_MAX_READ_AGENTS} ${MODEL_REVIEW} subagents to compare source code recursively under `src/` against all files recursively under `specs/`. Use one ${MODEL_PLAN} subagent to analyze findings. Ultrathink. Given what's already implemented, recommend which activities (at what capability depths) form the most valuable next release. Prefer thin horizontal slices — the narrowest scope that still delivers real value. A good slice is Simple (narrow, achievable), Lovable (people want to use it), and Complete (fully accomplishes a meaningful job, not a broken preview).

3. Use one ${MODEL_PLAN} subagent (ultrathink) to analyze and synthesize the findings, prioritize tasks, and create/update @IMPLEMENTATION_PLAN.md as a bullet point list sorted in priority of items yet to be implemented for the recommended SLC release. Begin the plan with a summary of the recommended SLC release (what's included and why), then list prioritized tasks for that scope. Consider TODOs, placeholders, minimal implementations, and skipped/flaky tests — but scoped to the release. Note discoveries outside scope as future work. Keep @IMPLEMENTATION_PLAN.md up to date with items considered complete/incomplete using subagents.

4. Parse the acceptance criteria from all files recursively under `specs/` (scoped to the release from step 2) and create/update @ACCEPTANCE_TESTS.md — the completion contract the build loop gates on. Use ${MODEL_REVIEW} subagents to read the specs; use one ${MODEL_PLAN} subagent to derive the entries. Emit one entry per acceptance criterion, each with a STABLE id (`AC-001`, `AC-002`, ...) assigned once and never renumbered. For each criterion record: the source spec, the observable outcome to verify (behavior, performance, edge case), the name of the required test that will verify it, and a status (`PENDING` until a real passing test exists). These are stubs — describe WHAT must be verified (outcomes), not HOW to implement it. When deriving each entry, identify whether verification requires programmatic validation (measurable, inspectable) or human-like judgment (perceptual quality, tone, aesthetics); both are equally valid backpressure. For subjective criteria that resist programmatic validation, explore `src/lib` for non-deterministic evaluation patterns (e.g. `llm-review.ts`) and note the required review in the entry. Do NOT write test code or implementation (plan only). Never delete or renumber an existing criterion; if a criterion is dropped from specs, set its status to `RETIRED` with a one-line reason instead of removing the entry. Cross-reference each in-scope @IMPLEMENTATION_PLAN.md task with the `AC-###` ids it must satisfy.

IMPORTANT: Plan only. Do NOT implement anything. Do NOT assume functionality is missing; confirm with code search first. Treat `src/lib` as the project's standard library for shared utilities and components. Prefer consolidated, idiomatic implementations there over ad-hoc copies.

ULTIMATE GOAL: We want to achieve the most valuable next release of `${PROJECT_NAME}` for the audience in @AUDIENCE_JTBD.md. Consider missing elements and plan accordingly. If an element is missing, search first to confirm it doesn't exist, then if needed author the specification at specs/FILENAME.md. If you create a new element then document the plan to implement it in @IMPLEMENTATION_PLAN.md using a subagent.
