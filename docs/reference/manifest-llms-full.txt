# Manifest — Full Documentation

> This file contains the complete text of all Manifest documentation pages.

> Source: https://manifest-b1e8623f.mintlify.app

> Package: @angriff36/manifest v3.6.41

> Generated: 2026-07-23

<!-- NOTE: This file is automatically generated by scripts/regenerate-llms-full.mjs. Do not edit manually. -->


---

## audit sink

URL: https://manifest-b1e8623f.mintlify.app/adapters/audit-sink

---
title: 'Log command execution with the Manifest audit sink'
description: 'Wire in an AuditSink to receive one AuditRecord per runCommand call — success or failure — for compliance, debugging, and observability.'
sidebarTitle: 'Audit Sink'
---

Every call to `RuntimeEngine.runCommand` produces a single `AuditRecord`, regardless of whether the command succeeds, is denied by a guard or policy, hits a constraint, or throws an unexpected error. You receive that record by providing an `AuditSink` in `RuntimeOptions`.

## What gets recorded

The runtime emits exactly one record per `runCommand` invocation. The record is produced in a `finally` block, so even unhandled errors produce an audit entry before propagating.

The `AuditRecord` shape, as defined in `@angriff36/manifest/audit`:

```typescript
interface AuditRecord {
  /** Stable record id. Generated via RuntimeOptions.generateId (falls back to crypto.randomUUID). */
  recordId?: string;
  /** Wall-clock timestamp — milliseconds since epoch. From RuntimeOptions.now. */
  occurredAt: number;
  /** Tenant the command ran against, when known. */
  tenantId?: string;
  /** Organization the actor belonged to (e.g. Clerk orgId). */
  orgId?: string;
  /** Acting user id. */
  actorId?: string;
  /** Caller-supplied request id for trace correlation. */
  requestId?: string;
  /** Origin surface ('route', 'job', 'cli', ...). */
  source?: string;
  /** Entity name the command targeted, when entity-scoped. */
  entity?: string;
  /** Command name. */
  command: string;
  /** Canonical "<entity>.<command>" identifier, when available. */
  commandId?: string;
  /** Outcome classification. */
  outcome: CommandOutcome;
  /** Structured runtime diagnostics, if any. */
  diagnostics?: unknown;
  /** Names of events emitted by the command, when the outcome was success. */
  emittedEventNames?: string[];
  /** IR provenance hash at execution time, for drift detection. */
  irHash?: string;
}
```

### Outcome values

The `outcome` field is one of:

| Value                    | Meaning                                                      |
| ------------------------ | ------------------------------------------------------------ |
| `success`                | Command completed; state was mutated and events emitted.     |
| `guard_denied`           | A guard expression evaluated to false.                       |
| `policy_denied`          | A policy check rejected the command.                         |
| `rate_limit_denied`      | A command or policy rate limiter blocked execution.          |
| `constraint_failed`      | One or more constraint validations failed.                   |
| `concurrency_conflict`   | Optimistic concurrency check failed.                         |
| `missing_tenant_context` | The command required a tenant context that was not supplied. |
| `error`                  | An unexpected error occurred during evaluation.              |

<Note>
  `diagnostics` carries the structured failure details — `policyDenial`, `guardFailure`,
  `concurrencyConflict`, `constraintOutcomes`, or `error` — so downstream tooling can reason about a
  denial without reshaping the runtime contract.
</Note>

## First-party sinks

Two sinks ship with the package:

<CodeGroup>

```typescript MemoryAuditSink (development)
import { MemoryAuditSink } from '@angriff36/manifest/audit/memory';

const sink = new MemoryAuditSink();

// Retrieve records after commands run:
const records = sink.getRecords();
```

```typescript PostgresAuditSink (production)
import { PostgresAuditSink } from '@angriff36/manifest/audit/postgres';
import { Pool } from 'pg';

const pool = new Pool({ connectionString: process.env.DATABASE_URL });
const sink = new PostgresAuditSink({ pool });
```

</CodeGroup>

`MemoryAuditSink` is idempotent by `recordId` — re-emitting a record with the same `recordId` does not create a duplicate. Use it in tests and sample applications.

`PostgresAuditSink` is backed by `pg` and uses `INSERT … ON CONFLICT (record_id) DO NOTHING` for idempotency. Apply the included schema before using it:

<Tip>
  The schema file `postgres.sql` is included in the published package. Find it at
  `node_modules/@angriff36/manifest/src/manifest/audit/sinks/postgres.sql` and run it against your
  database once during deployment.
</Tip>

## Wiring in an audit sink

Pass your sink via `RuntimeOptions.auditSink`:

```typescript
import { RuntimeEngine } from '@angriff36/manifest';
import { PostgresAuditSink } from '@angriff36/manifest/audit/postgres';
import { Pool } from 'pg';

const pool = new Pool({ connectionString: process.env.DATABASE_URL });

const runtime = new RuntimeEngine(
  ir,
  { actorId: session.userId, tenantId: session.tenantId },
  { auditSink: new PostgresAuditSink({ pool }) },
);

// Every runtime.runCommand(...) call now writes one AuditRecord.
const result = await runtime.runCommand('createOrder', input, {
  entityName: 'Order',
  instanceId: orderId,
});
```

The `RuntimeEngine` constructor is
`(ir, context, options)`. `auditSink` is a `RuntimeOptions` field and must go in
the **options** (3rd) arg, not `context`. Passing it in `context` typechecks —
`RuntimeContext` has an open `[key: string]` index signature — but the engine
only reads `this.options.auditSink`, so the sink is silently never wired and no
`AuditRecord` is written. Identity (`actorId`/`tenantId`) stays in `context`.

## Import paths

| What                                         | Import path                          |
| -------------------------------------------- | ------------------------------------ |
| `AuditSink` interface and `AuditRecord` type | `@angriff36/manifest/audit`          |
| `MemoryAuditSink`                            | `@angriff36/manifest/audit/memory`   |
| `PostgresAuditSink`                          | `@angriff36/manifest/audit/postgres` |

## Implementing a custom sink

You can implement `AuditSink` against any backend — OpenTelemetry, a file logger, a remote webhook, or your own table schema:

```typescript
import type { AuditSink, AuditRecord } from '@angriff36/manifest/audit';

export class OpenTelemetryAuditSink implements AuditSink {
  async emit(record: AuditRecord): Promise<void> {
    // Write to your observability backend
    telemetry.track('manifest.command', {
      outcome: record.outcome,
      entity: record.entity,
      command: record.command,
      actorId: record.actorId,
      tenantId: record.tenantId,
    });
  }
}
```

Sinks **must** be idempotent against `recordId` when one is supplied, so retries and at-least-once delivery from upstream workers do not double-write.

## Failure policy: fail-open

<Warning>
If `AuditSink.emit` throws, the runtime catches the error, logs it to `stderr`, and **continues normally**. The command result returned to your caller is not affected.

The audit subsystem is observability, not enforcement. Monitor for the log line:

```
[Manifest Runtime] AuditSink.emit failed
```

Alert on it in production so you know when audit records are being lost.

</Warning>


---

## custom stores

URL: https://manifest-b1e8623f.mintlify.app/adapters/custom-stores

---
title: 'Connect Manifest to your database with custom stores'
description: 'Implement the Store interface and provide it via storeProvider to persist entity state in any database — Postgres, Prisma, Drizzle, or your own backend.'
sidebarTitle: 'Custom Stores'
---

By default, `RuntimeEngine` stores entity state in memory. That works for tests and local development, but production applications need durable storage. Manifest exposes the `Store` interface and a `storeProvider` hook so you can plug in any backend without changing your domain model.

## The Store interface

Every custom store must implement the following interface, exported from `@angriff36/manifest`:

```typescript
interface Store<T extends EntityInstance = EntityInstance> {
  getAll(): Promise<T[]>;
  getById(id: string): Promise<T | undefined>;
  create(data: Partial<T>, tx?: TransactionHandle): Promise<T>;
  update(id: string, data: Partial<T>, tx?: TransactionHandle): Promise<T | undefined>;
  delete(id: string, tx?: TransactionHandle): Promise<boolean>;
  clear(): Promise<void>;
}
```

Each method maps directly to a runtime operation:

| Method    | When the runtime calls it                        |
| --------- | ------------------------------------------------ |
| `getAll`  | Hydrating entity collections                     |
| `getById` | Loading an instance before a command runs        |
| `create`  | Persisting a new entity instance                 |
| `update`  | Writing state mutations after a command succeeds |
| `delete`  | Removing an instance                             |
| `clear`   | Resetting all instances (typically in tests)     |

## Wiring in a custom store

Pass a `storeProvider` function in `RuntimeOptions` when you construct
`RuntimeEngine`. The function receives the entity name and returns either your
store or `undefined`. `undefined` uses built-in behavior only for `memory` and
browser `localStorage`; durable and custom targets fail instead of falling back
to memory.

```typescript
import { RuntimeEngine } from '@angriff36/manifest';
import { MyCustomStore } from './my-custom-store';

const runtime = new RuntimeEngine(
  ir,
  { actorId: 'user-123', tenantId: 'tenant-456' },
  {
    storeProvider: (entityName) =>
      entityName === 'Recipe' ? new MyCustomStore({/* config */}) : undefined,
  },
);
```

Generated runtime companions can load the same bindings from
`manifest.config.ts` through `runtimeConfigImport`. See the
[complete PostgreSQL setup](/language/stores#complete-generated-companion-postgresql-setup).

## Complete example: Prisma store

Here is a full implementation backed by Prisma. Adapt the delegate type and field mapping to match your schema.

```typescript
import type { Store, EntityInstance } from '@angriff36/manifest';
import type { PrismaClient } from '@prisma/client';

export class PrismaStore<T extends EntityInstance> implements Store<T> {
  constructor(
    private readonly prisma: PrismaClient,
    private readonly model: string,
  ) {}

  private get delegate(): any {
    return (this.prisma as any)[this.model];
  }

  async getAll(): Promise<T[]> {
    return this.delegate.findMany();
  }

  async getById(id: string): Promise<T | undefined> {
    const record = await this.delegate.findUnique({ where: { id } });
    return record ?? undefined;
  }

  async create(data: Partial<T>): Promise<T> {
    return this.delegate.create({ data });
  }

  async update(id: string, data: Partial<T>): Promise<T | undefined> {
    try {
      return await this.delegate.update({ where: { id }, data });
    } catch {
      return undefined;
    }
  }

  async delete(id: string): Promise<boolean> {
    try {
      await this.delegate.delete({ where: { id } });
      return true;
    } catch {
      return false;
    }
  }

  async clear(): Promise<void> {
    await this.delegate.deleteMany();
  }
}
```

Wire it in alongside `RuntimeEngine`:

```typescript
import { RuntimeEngine } from '@angriff36/manifest';
import { PrismaClient } from '@prisma/client';
import { PrismaStore } from './prisma-store';

const prisma = new PrismaClient();

const runtime = new RuntimeEngine(
  ir,
  { actorId: session.userId, tenantId: session.tenantId },
  {
    storeProvider: (entityName) => {
      switch (entityName) {
        case 'Recipe':
          return new PrismaStore(prisma, 'recipe');
        case 'Order':
          return new PrismaStore(prisma, 'order');
        default:
          return undefined;
      }
    },
  },
);
```

## Built-in storage targets

The runtime ships with first-party support for the following targets:

| Target          | Notes                                                                                                                                                                                                                                                                                    |
| --------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `memory`        | Default. In-process; state is lost on restart.                                                                                                                                                                                                                                           |
| `postgres`      | Server-side only. Uses `pg`. Import from `@angriff36/manifest/stores`.                                                                                                                                                                                                                   |
| `supabase`      | Server-side only. Requires the optional `@supabase/supabase-js` peer dependency. Import from `@angriff36/manifest/stores`.                                                                                                                                                               |
| `localStorage`  | Browser only.                                                                                                                                                                                                                                                                            |
| `dynamodb`      | AWS DynamoDB. Single-table design with composite keys. See [DynamoDB adapter](/adapters/dynamodb).                                                                                                                                                                                       |
| `redis`         | Redis string/JSON entity storage (`RedisStore`). Store pub/sub methods throw — use `RedisEventBus` via `RuntimeOptions.eventBus`. See [Redis adapter](/adapters/redis). |
| `turso`         | Turso / libSQL edge database. See [Turso adapter](/adapters/turso).                                                                                                                                                                                                                      |
| `eventSourced`  | IR target string is `eventSourced` (camelCase). Still not a working runtime store — passthrough metadata only. See [Event sourcing patterns](/adapters/event-sourced-store). |

## Unsupported targets throw — there is no silent fallback

<Warning>
If you pass an unsupported storage target (for example `postgres` in a browser environment), the runtime throws a clear error identifying the unsupported target and entity. It does **not** silently fall back to memory storage.

This behavior is required by the Manifest conformance spec. Always verify your target at startup and surface configuration errors early.

</Warning>

## Practical guidance

- **Keep methods deterministic** in test environments — avoid `Date.now()` or random IDs inside the store; let the runtime supply them via `RuntimeOptions.generateId` and `RuntimeOptions.now`.
- **Enforce tenant boundaries at the storage layer** when your domain requires row-level isolation. The runtime passes `tenantId` in context; your store can filter on it.
- **Surface failures explicitly.** Methods like `update` and `delete` return `undefined` / `false` on not-found, and throw on genuine errors. Do not swallow errors silently.


---

## dynamodb

URL: https://manifest-b1e8623f.mintlify.app/adapters/dynamodb

---
title: 'DynamoDB store adapter'
sidebarTitle: 'DynamoDB'
description: 'Persist Manifest entity state in DynamoDB with single-table design, composite keys, optimistic locking, and a transactional outbox — plus CloudFormation, CDK, and Terraform infrastructure projections.'
---

The DynamoDB store adapter persists Manifest entity state in AWS DynamoDB using the `Store<T>` interface. Use it when your deployment target is AWS and you need a fully managed, low-latency key-value store. The adapter supports single-table design patterns, composite primary keys, optimistic locking, and a dedicated transactional outbox store for durable event delivery.

## Configuration

`DynamoDBStore` is exported from `@angriff36/manifest/stores` (Node.js only). It requires the `@aws-sdk/client-dynamodb` and `@aws-sdk/lib-dynamodb` packages as optional peer dependencies, loaded via dynamic import.

```typescript
import { DynamoDBStore } from '@angriff36/manifest/stores';

const store = new DynamoDBStore('Order', {
  tableName: 'manifest-orders',
  region: 'us-east-1',
  partitionKey: 'pk',
  sortKey: 'sk',
  entityPrefix: 'ORDER', // single-table design prefix
});
```

The store declares itself in Manifest source as:

```manifest
entity Order {
  property required id: string
  property total: decimal
  property status: string
}

store Order in dynamodb
```

## Single-table design

DynamoDBStore supports the single-table design pattern through the `entityPrefix` option. When configured, all entities share a single DynamoDB table and are distinguished by their partition key prefix. The `buildDynamoDBKey()` helper constructs composite keys:

```typescript
import { buildDynamoDBKey } from '@angriff36/manifest/stores';

// Composite key: "ORDER#123" / "ORDER#123#DETAIL"
const key = buildDynamoDBKey('order-123', { entityPrefix: 'ORDER' }, 'Order');
```

When `entityPrefix` is set, the store uses `ConditionExpression` for atomic create (`attribute_not_exists`) and delete (`attribute_exists`) operations. Single-table design is auto-detected when `config.singleTable === true` in any store declaration.

## Composite key handling

The store maps Manifest entity IDs to DynamoDB composite primary keys. Partition and sort key names are configurable via `partitionKey` (default `pk`) and `sortKey` (default `sk`) in the store config. Entity instances are serialized as JSON strings and stored as DynamoDB attribute values.

## Optimistic locking

DynamoDBStore does NOT implement optimistic locking — the `update` method is a plain `PutCommand` with no version check. For optimistic locking with version field enforcement, use **MongoDBStore** (`@angriff36/manifest/stores`), which includes a version check in its update filter. DynamoDB atomic create uses `attribute_not_exists` and delete uses `attribute_exists` for existence checks, but there is no version-based concurrency control.

## DynamoDBOutboxStore

A `DynamoDBOutboxStore` exists in the source tree — it implements the `OutboxStore` interface using DynamoDB's `TransactWriteItems` for atomic batch enqueue (up to 100 actions), conditional `UpdateItem` for concurrent claim, and `markDelivered`/`markFailed` for state transitions — but it is **not currently a public export**. The `@angriff36/manifest/outbox/dynamodb` subpath does not resolve, and the store has no test coverage yet, so it is not published on the `exports` map.

For durable at-least-once event delivery on AWS today, use one of the **exported** outbox stores: `PostgresOutboxStore` (`@angriff36/manifest/outbox/postgres`) or `RedisOutboxStore` (`@angriff36/manifest/outbox/redis`).

<Note>
  Unlike the doc's older example, the source `DynamoDBOutboxStore` constructor takes a single
  options object whose `client` field is **required** — it does not accept a `region`. The `client`
  is an initialized AWS SDK document client that also carries the command constructors
  (`TransactWriteCommand`, `ScanCommand`, `UpdateCommand`), matching the convention used by
  `DynamoDBStore`. This shape is documented here for reference only; the store is not importable
  from the package until it is exported.
</Note>

## Infrastructure projections

The DynamoDB adapter includes a dedicated projection that generates infrastructure definitions from Manifest IR. The `DynamoDBProjection` is registered under the name `dynamodb` and emits three surfaces:

- `dynamodb.cloudformation` — CloudFormation YAML template with table definitions, key schema, attribute definitions, billing mode, and stream specification.
- `dynamodb.cdk` — AWS CDK TypeScript code with `dynamodb.Table` constructs.
- `dynamodb.terraform` — Terraform HCL with `aws_dynamodb_table` resources.

All three surfaces support per-entity and single-table designs, and always emit an outbox table.

```ts
import { getProjection } from '@angriff36/manifest/projections';

const projection = getProjection('dynamodb');
const cfn = projection.generate(ir, { surface: 'dynamodb.cloudformation' });
```

## Wiring in DynamoDB

```typescript
import { RuntimeEngine } from '@angriff36/manifest';
import { DynamoDBStore } from '@angriff36/manifest/stores';
import { PostgresOutboxStore } from '@angriff36/manifest/outbox/postgres';
import { Pool } from 'pg';

const runtime = new RuntimeEngine(
  ir,
  { actorId: session.userId, tenantId: session.tenantId },
  {
    storeProvider: (entityName) =>
      entityName === 'Order'
        ? new DynamoDBStore('Order', { tableName: 'manifest-data', region: 'us-east-1' })
        : undefined,
    // The DynamoDB outbox store is not yet an exported subpath; use an exported
    // outbox store (Postgres or Redis) for durable delivery today.
    outboxStore: new PostgresOutboxStore(new Pool({ connectionString: process.env.DATABASE_URL })),
  },
);
```

## Import paths

| What                      | Import path                                                                               |
| ------------------------- | ----------------------------------------------------------------------------------------- |
| `DynamoDBStore` class     | `@angriff36/manifest/stores`                                                              |
| `buildDynamoDBKey` helper | `@angriff36/manifest/stores`                                                              |
| `DynamoDBOutboxStore`     | not exported (source-only) — use `@angriff36/manifest/outbox/postgres` or `/outbox/redis` |
| `DynamoDBProjection`      | `@angriff36/manifest/projections`                                                         |

## Notes & limitations

<Note>
  The `@aws-sdk/client-dynamodb` and `@aws-sdk/lib-dynamodb` packages are optional peer dependencies
  loaded via dynamic import. If they are not installed, the constructor throws a clear error:
  "DynamoDBStore requires '@aws-sdk/client-dynamodb' and '@aws-sdk/lib-dynamodb' to be installed."
  The `clear()` method uses Scan + BatchWrite in groups of 25 (DynamoDB batch limit).
</Note>


---

## event sourced store

URL: https://manifest-b1e8623f.mintlify.app/adapters/event-sourced-store

---
title: 'Event Sourced Store'
description: 'Append-only event log store with snapshotting for Manifest entities. Ships in the reference runtime — auto-wired for `store Entity in eventSourced { ... }` declarations.'
sidebarTitle: 'Event Sourced Store'
---

The `EventSourcedStore` is an in-memory `Store<T>` implementation that persists entity state as an append-only event log. Each `create`/`update`/`delete` operation appends an event to the log, and the current state is maintained by replaying events from the last snapshot.

This store **ships** in the reference runtime (`@angriff36/manifest/runtime-engine`) and is auto-wired for any entity declared with `store Entity in eventSourced { ... }`.

## How it works

1. **Events append to a log** — Each write operation creates an event (`create`/`update`/`delete`) with a sequence number and timestamp.
2. **State is projected** — The current entity state is maintained in memory by applying all events from the log.
3. **Snapshots** — Periodically (default: every 100 events), the current state is snapshotted. Replay starts from the last snapshot instead of the beginning.
4. **Optional event log access** — When `exposeEventLog: true`, `getEventLog(id)` returns the full append-only log for an entity.

## Declaration

```manifest
store Order in eventSourced {
  snapshotInterval: 100    // Take a snapshot every N events (default: 100)
  exposeEventLog: true     // Allow getEventLog() to return the event log (default: false)
}
```

## Usage in TypeScript

The `EventSourcedStore` class is exported from `@angriff36/manifest/stores/event-sourced`. You rarely instantiate it directly — the runtime auto-wires it based on the IR store declaration. For custom instantiation:

```typescript
import { EventSourcedStore } from '@angriff36/manifest/stores/event-sourced';

const store = new EventSourcedStore<Order>({
  snapshotInterval: 100,
  exposeEventLog: true,
  generateId: () => crypto.randomUUID(),
  now: () => Date.now(),
});

// Standard Store interface
await store.create({ id: 'order-1', total: 100 });
await store.update('order-1', { total: 150 });
await store.delete('order-1');
const order = await store.getById('order-1');
const all = await store.getAll();

// Event-sourced specific methods
const events = store.getEventLog('order-1');  // EventSourcedEvent[] | undefined
const snapshot = store.getSnapshot('order-1'); // EventSourcedSnapshot<Order> | undefined
```

## Event structure

Each event in the log has:

| Field    | Type                               | Description                                  |
| -------- | ---------------------------------- | -------------------------------------------- |
| sequence | number                             | Monotonically increasing event number      |
| kind     | `'create'` `'update'` `'delete'`     | Operation type                               |
| at       | number                             | Timestamp (milliseconds since epoch)        |
| payload  | `Record<string, unknown>` \| undefined | For `update` events, the changed fields |

## Snapshot structure

| Field    | Type   | Description                             |
| -------- | ------ | --------------------------------------- |
| sequence | number | Sequence number at snapshot time        |
| state    | T      | Complete entity state at that sequence |

## Limitations

- **In-memory only** — The event log and projected state live in process memory and are lost on restart. For durable event sourcing, use the outbox/audit sinks or build a custom adapter.
- **No distributed replay** — Each process maintains its own log; for multi-process scenarios, use an external event store.

## Related

- [Events and reactions](/language/events) — Event-driven architecture in Manifest
- [Outbox](/adapters/outbox) — Durable, at-least-once event delivery
- [Audit Sink](/adapters/audit-sink) — Compliance-grade command history

Package pin: `@angriff36/manifest@3.6.41+` (FULLY_IMPLEMENTED per `COMPLIANCE_MATRIX.md:133`)


---

## outbox

URL: https://manifest-b1e8623f.mintlify.app/adapters/outbox

---
title: 'Durable event delivery with the Manifest outbox'
description: 'Use OutboxStore to persist emitted events for at-least-once delivery. Covers the enqueue/claim/markDelivered lifecycle, crash recovery, and known limitations.'
sidebarTitle: 'Outbox Store'
---

When a command emits one or more events, you often need to deliver those events to downstream consumers reliably — even if your process crashes immediately after the command succeeds. The `OutboxStore` adapter gives you a durable queue for emitted events that a background worker can drain with at-least-once delivery guarantees.

## How the outbox works

After a successful `runCommand` call that emits events, the runtime calls `outboxStore.enqueue(entries)` once, batching all events from that invocation into a single call. A separate delivery worker then polls the store using `claim`, delivers the events, and calls `markDelivered` or `markFailed`.

```
runCommand → success → enqueue(entries)
                            │
              ┌─────────────▼─────────────┐
              │ OutboxStore │
              │ pending entries queue │
              └─────────────┬─────────────┘
                            │
              delivery worker: claim → deliver → markDelivered
```

## The OutboxStore interface

The full interface, from `@angriff36/manifest/outbox`:

```typescript
interface OutboxStore {
  /**
   * Enqueue one or more outbox entries. Called by the runtime after a
   * successful command. Pass a tx handle to participate in the caller's
   * transaction; without one, enqueue runs independently (best-effort).
   */
  enqueue(entries: OutboxEntry[], tx?: unknown): Promise<void>;

  /**
   * Claim up to batchSize pending entries for delivery. Durable
   * implementations should use SELECT … FOR UPDATE SKIP LOCKED so
   * concurrent workers receive disjoint batches.
   */
  claim(batchSize: number): Promise<OutboxEntry[]>;

  /** Mark entries as successfully delivered. */
  markDelivered(entryIds: string[]): Promise<void>;

  /** Mark entries as failed with a reason. */
  markFailed(entryIds: string[], error: string): Promise<void>;
}
```

## The OutboxEntry shape

Each entry the runtime enqueues has the following shape:

```typescript
interface OutboxEntry {
  /** Stable id assigned at enqueue time. Use this as your dedup token. */
  entryId: string;
  /** Timestamp the entry was enqueued (ms since epoch). */
  enqueuedAt: number;
  /** The full EmittedEvent payload from the command. */
  event: EmittedEvent;
  /** Current status: 'pending' | 'delivered' | 'failed'. */
  status: OutboxEntryStatus;
  /** Number of times this entry has been claimed. */
  attempts: number;
  /** Last error message, present when status === 'failed'. */
  lastError?: string;
}
```

## Delivery semantics: at-least-once

<Warning>
The outbox provides **at-least-once** delivery, not exactly-once. Your consumers **must be idempotent**.

A worker that delivers an event and then crashes before calling `markDelivered` will redeliver the same entry after stale-claim recovery. Use `entryId` — or a domain-level idempotency key — as a dedup token in every consumer.

</Warning>

The three reasons duplication can occur:

1. A worker claims an entry, delivers it, crashes before `markDelivered` runs, and `releaseStaleClaims` recovers the entry for redelivery.
2. `releaseStaleClaims` is called while the original worker is still alive, producing a second concurrent delivery.
3. Network retries from the consumer side can produce repeat reads within a single worker.

## First-party stores

<CodeGroup>

```typescript MemoryOutboxStore (development / tests)
import { MemoryOutboxStore } from '@angriff36/manifest/outbox/memory';

const outboxStore = new MemoryOutboxStore();
```

```typescript PostgresOutboxStore (production)
import { PostgresOutboxStore } from '@angriff36/manifest/outbox/postgres';
import { Pool } from 'pg';

const pool = new Pool({ connectionString: process.env.DATABASE_URL });
const outboxStore = new PostgresOutboxStore({ pool });
```

```typescript RedisOutboxStore (Redis Streams)
import { RedisOutboxStore } from '@angriff36/manifest/outbox/redis';

const outboxStore = new RedisOutboxStore({
  url: process.env.REDIS_URL,
});
```

</CodeGroup>

First-party outbox stores also include
`RedisOutboxStore` (`@angriff36/manifest/outbox/redis`) — see [Redis adapter](/adapters/redis).

`MemoryOutboxStore` is an in-memory analogue of `SELECT FOR UPDATE SKIP LOCKED` — `claim` skips entries that are already claimed or resolved. Use it in tests.

`PostgresOutboxStore` uses `SELECT … FOR UPDATE SKIP LOCKED` combined with a `claimed_at IS NULL` filter so concurrent workers receive disjoint batches. Apply the schema before first use:

<Tip>
  The schema file is included in the published package at
  `node_modules/@angriff36/manifest/src/manifest/outbox/stores/postgres.sql`. Run it against your
  database once during deployment.
</Tip>

## Wiring in an outbox store

Pass your store via `RuntimeOptions.outboxStore`:

```typescript
import { RuntimeEngine } from '@angriff36/manifest';
import { PostgresOutboxStore } from '@angriff36/manifest/outbox/postgres';
import { Pool } from 'pg';

const pool = new Pool({ connectionString: process.env.DATABASE_URL });

const runtime = new RuntimeEngine(
  ir,
  { actorId: session.userId, tenantId: session.tenantId },
  { outboxStore: new PostgresOutboxStore({ pool }) },
);

// After a successful command, entries are automatically enqueued.
const result = await runtime.runCommand('placeOrder', input, {
  entityName: 'Order',
  instanceId: orderId,
});
```

`outboxStore` is a `RuntimeOptions`
field — put it in the **options** (3rd) arg of `new RuntimeEngine(ir, context,
options)`, not in `context`. In `context` it typechecks (the open index
signature) but is never read, so outbox entries are silently never enqueued.

Then run the shipped delivery worker. `runOutboxWorker` owns the
claim/deliver/mark loop — you supply only the `deliver` closure that publishes
each entry:

```typescript
import { runOutboxWorker } from '@angriff36/manifest/outbox/worker';

const worker = runOutboxWorker(store, async (entry) => {
  // Reject to have the entry marked failed with this error; resolve to have
  // it marked delivered. Delivery is at-least-once, so keep this idempotent.
  await publishToEventBus(entry.event);
});

// On shutdown, stop the loop and wait for the in-flight batch to settle.
await worker.stop();
```

Options tune batching and back-off: `batchSize` (default 100), `pollIntervalMs`
(default 1000, the wait between empty polls), an `onError` callback for
store/infrastructure failures, and a `signal: AbortSignal` as an alternative to
`stop()`. For a serverless or cron-driven deployment that wants a single pass
instead of a long-lived loop, call `drainOutboxOnce(store, deliver, { batchSize })`.

`runOutboxWorker` is safe to run in multiple replicas against `PostgresOutboxStore`:
its `claim` uses `SELECT … FOR UPDATE SKIP LOCKED`, so each worker gets a
disjoint batch.

## Crash recovery

If a worker claims entries and then crashes before calling `markDelivered` or `markFailed`, those rows are left with `status='pending'` but a non-null `claimed_at`. The `claim` method will not return them again (the `claimed_at IS NULL` filter excludes them).

Call `releaseStaleClaims(entryIds)` on `PostgresOutboxStore` to reset those entries so a follow-up `claim` picks them up:

```typescript
// Only call this after you are certain the original worker is dead.
await outboxStore.releaseStaleClaims(staleEntryIds);
```

<Warning>
  Only release claims when you are certain the original worker is no longer running. Releasing a
  claim held by a live worker produces a second delivery. The `attempts` counter increments on every
  successful claim, so you can use it to bound retries and dead-letter entries that exceed a
  threshold.
</Warning>

## Transaction boundary

By default — with no `transactionProvider` configured — `RuntimeEngine` does **not** share a transaction boundary between entity state mutation and outbox enqueue. The `enqueue` call runs on a separate database connection from the entity `update` inside `_executeCommandInternal`. In that mode, if the entity mutation succeeds but `outboxStore.enqueue` fails, the state change is persisted without a durable outbox row. The runtime logs the failure to `stderr` as `[Manifest Runtime] OutboxStore.enqueue failed`, and the `CommandResult` is not affected — the command still returns success to your caller.

<Note>
**Recommended: engine-managed atomicity.** Set a `transactionProvider` in `RuntimeOptions` and the engine wraps each command attempt — command body, its outbox enqueue, and its idempotency record — in a single `withTransaction` call, so those writes commit or roll back together. A failed enqueue now rolls the mutation back and surfaces `OUTBOX_ENQUEUE_FAILED` instead of silently dropping the event.

```typescript
import { Pool } from 'pg';
import { RuntimeEngine } from '@angriff36/manifest';
import { PostgresOutboxStore } from '@angriff36/manifest/outbox/postgres';
import { PostgresTransactionProvider } from '@angriff36/manifest/transactions/postgres';

const pool = new Pool({ connectionString: process.env.DATABASE_URL });

const runtime = new RuntimeEngine(ir, context, {
  outboxStore: new PostgresOutboxStore({ pool }),
  transactionProvider: new PostgresTransactionProvider({ pool }),
});
```

</Note>

**Adapter-level alternative:** `PostgresOutboxStore.enqueue(entries, tx)` also accepts a caller-supplied `PoolClient`. If you open your own transaction and pass the same client to both a `PostgresStore` write and `PostgresOutboxStore.enqueue(entries, sameClient)`, you get atomicity without a provider. Use this when you already manage transactions yourself.

## Import paths

| What                                           | Import path                           |
| ---------------------------------------------- | ------------------------------------- |
| `OutboxStore` interface and `OutboxEntry` type | `@angriff36/manifest/outbox`          |
| `MemoryOutboxStore`                            | `@angriff36/manifest/outbox/memory`   |
| `PostgresOutboxStore`                          | `@angriff36/manifest/outbox/postgres` |

## Failure policy: fail-open

Like the audit sink, `OutboxStore.enqueue` errors are caught and logged to `stderr`. They do not alter `CommandResult` or block the caller. Monitor for:

```
[Manifest Runtime] OutboxStore.enqueue failed
```

Alert on this line in production if your application depends on durable event delivery.


---

## redis

URL: https://manifest-b1e8623f.mintlify.app/adapters/redis

---
title: 'Redis store adapter'
sidebarTitle: 'Redis'
description: 'Persist Manifest entity state in Redis with key-based JSON storage, TTL management, and pub/sub event propagation.'
---

The Redis store adapter persists Manifest entity state in Redis using the `Store<T>` interface. Use it when you need high-throughput, low-latency entity storage with built-in TTL management and native pub/sub for event propagation. The adapter loads the `ioredis` library lazily as an optional dependency.

## Configuration

`RedisStore` is exported from `@angriff36/manifest/stores` (Node.js only). The constructor takes the **entity name** as its first argument, followed by a `RedisConfig` object.

```typescript
import { RedisStore } from '@angriff36/manifest/stores';

const store = new RedisStore<User>('User', {
  url: 'redis://localhost:6379',
  keyPrefix: 'manifest:', // defaults to `<entityName>:` lowercased
  defaultTTL: 3600, // 1 hour in seconds (optional)
});
```

`RedisConfig` exposes exactly three optional fields: `url`, `keyPrefix`, and `defaultTTL`. When `keyPrefix` is omitted it defaults to the lowercased entity name followed by a colon (for example, `user:`).

The store declares itself in Manifest source as:

```manifest
entity Session {
  property required id: string
  property userId: string
  property data: string
}

store Session in redis
```

## Key naming conventions

Each entity instance is stored as its own Redis string key holding a JSON-serialized value. The key pattern is `<keyPrefix><id>`. With `keyPrefix: 'manifest:'`, an entity with ID `user-123` is stored at key `manifest:user-123`. Collection reads use `KEYS <keyPrefix>*` followed by `MGET`.

## Key-based JSON storage

`RedisStore` implements the `Store<T>` interface using Redis string commands:

- `getAll()` runs `KEYS <keyPrefix>*` then `MGET`, deserializing each JSON value.
- `getById(id)` runs `GET` on `<keyPrefix><id>`.
- `create(data)` assigns an ID (or uses `data.id`) and runs `SET` with the serialized entity, then `EXPIRE` when `defaultTTL` is set.
- `update(id, data)` reads the existing value, merges the partial data, and `SET`s it back.
- `delete(id)` runs `DEL` on the key.
- `clear()` runs `KEYS <keyPrefix>*` then `DEL` on the matched keys.

## TTL management

`RedisStore` supports automatic entity expiration through Redis TTL:

```typescript
// Set TTL at store construction (applies to newly created keys)
const store = new RedisStore('User', { url: 'redis://localhost:6379', defaultTTL: 3600 });

// Manage TTL at runtime (operates across the store's keyPrefix namespace)
await store.setTTL(7200); // Set a 2-hour TTL on this store's keys
await store.setTTL(undefined); // PERSIST — remove TTL
const ttl = await store.getTTL(); // Remaining TTL in seconds for the namespace
```

When `defaultTTL` is configured, newly created entity keys receive the TTL automatically via `EXPIRE`.

## Pub/Sub — use `RedisEventBus`, not `RedisStore`

`RedisStore.publishEvent` and `RedisStore.subscribe`
were silent no-ops and now **throw**, pointing callers at `RedisEventBus`. Cross-process event
fan-out is `RuntimeOptions.eventBus` + `RedisEventBus` from `@angriff36/manifest/events/redis`
(see below). There is no auto-construct from env — wire the bus yourself.

## `RedisEventBus` (cross-process fan-out)

```typescript
import { RuntimeEngine } from '@angriff36/manifest';
import { RedisEventBus } from '@angriff36/manifest/events/redis';

const eventBus = new RedisEventBus({
  url: process.env.REDIS_URL, // default redis://localhost:6379
  channel: 'manifest:events', // default
});

const runtime = new RuntimeEngine(ir, context, { eventBus });
await runtime.connectEventBus(); // subscribe this process to remote envelopes
```

`RedisEventBus` implements the same `EventBus` contract as `MemoryEventBus`
(`@angriff36/manifest/events`). Optional peer: `ioredis`. Inject `client` in tests to avoid loading
ioredis. Call `close()` to quit bus-owned connections.

## Outbox delivery

`RedisOutboxStore` is a first-party Redis Streams implementation of the `OutboxStore` contract, exported from `@angriff36/manifest/outbox/redis`. It uses Redis Streams (`XADD` / `XREADGROUP` / `XACK`) with a consumer group so multiple dispatcher workers can drain the outbox in parallel.

```typescript
import { RedisOutboxStore } from '@angriff36/manifest/outbox/redis';

const outboxStore = new RedisOutboxStore({
  url: process.env.REDIS_URL,
  keyPrefix: 'manifest:outbox:', // default
  consumerGroup: 'manifest-dispatcher', // default
});
```

Like `RedisStore`, it loads `ioredis` lazily as an optional peer dependency and throws a clear error if the package is not installed. The `enqueue` method accepts an optional Redis pipeline for batching event persistence alongside state mutations.

The package `exports` map exposes `./outbox` (the `OutboxStore` / `OutboxEntry` contract) plus the concrete stores:

| Outbox store          | Import path                           |
| --------------------- | ------------------------------------- |
| `MemoryOutboxStore`   | `@angriff36/manifest/outbox/memory`   |
| `PostgresOutboxStore` | `@angriff36/manifest/outbox/postgres` |
| `RedisOutboxStore`    | `@angriff36/manifest/outbox/redis`    |

## Wiring in Redis

```typescript
import { RuntimeEngine } from '@angriff36/manifest';
import { RedisStore } from '@angriff36/manifest/stores';
import { RedisOutboxStore } from '@angriff36/manifest/outbox/redis';

const runtime = new RuntimeEngine(
  ir,
  { actorId: session.userId, tenantId: session.tenantId },
  {
    storeProvider: (entityName) =>
      entityName === 'Session'
        ? new RedisStore(entityName, { url: process.env.REDIS_URL, defaultTTL: 3600 })
        : undefined,
    // Durable at-least-once event delivery via the Redis Streams outbox:
    outboxStore: new RedisOutboxStore({ url: process.env.REDIS_URL }),
  },
);
```

## Import paths

| What                                     | Import path                           |
| ---------------------------------------- | ------------------------------------- |
| `RedisStore` class and `RedisConfig`     | `@angriff36/manifest/stores`          |
| `RedisEventBus`                          | `@angriff36/manifest/events/redis`    |
| `MemoryEventBus`                         | `@angriff36/manifest/events`          |
| `RedisOutboxStore`                       | `@angriff36/manifest/outbox/redis`    |
| `MemoryOutboxStore`                      | `@angriff36/manifest/outbox/memory`   |
| `PostgresOutboxStore`                    | `@angriff36/manifest/outbox/postgres` |
| `OutboxStore` / `OutboxEntry` interfaces | `@angriff36/manifest/outbox`          |

## Notes & limitations

<Note>
  `ioredis` is loaded lazily and is optional for `RedisStore`, `RedisOutboxStore`, and
  `RedisEventBus`. If it is not installed, those adapters throw a clear install hint. Prefer TLS
  (`rediss://`) in production. Package pin for the main SDK: `@angriff36/manifest@3.6.41`.
</Note>


---

## turso

URL: https://manifest-b1e8623f.mintlify.app/adapters/turso

---
title: 'Turso / libSQL store adapter'
description: "Persist entity state in Turso's edge-optimized libSQL database with JSON blob rows and optional schema DDL helper."
sidebarTitle: 'Turso'
---

The Turso adapter persists entity state in a [Turso](https://turso.tech/) edge database using libSQL. It implements the `Store<T>` interface and stores each entity instance as a JSON blob in a single table.

## Configuration

`TursoStore` is exported from `@angriff36/manifest/stores` (Node.js only). Optional peer: `@libsql/client`.

```typescript
import { TursoStore, generateTursoSchema } from '@angriff36/manifest/stores';

const store = new TursoStore({
  url: process.env.TURSO_DATABASE_URL!,
  authToken: process.env.TURSO_AUTH_TOKEN, // optional for local SQLite
  tableName: 'entities', // default
});
```

### Options

| Option      | Required | Description                                                              |
| ----------- | -------- | ------------------------------------------------------------------------ |
| `url`       | Yes      | Turso / libSQL URL (e.g. `libsql://…` or a local file URL)               |
| `authToken` | No       | Auth token (optional for local SQLite)                                   |
| `tableName` | No       | Single table for JSON blobs (default `entities`)                         |
| `client`    | No       | Pre-built LibSQL client (tests / custom setups)                          |

There is no `schemaPrefix` option. Use `tableName`.

## Wiring into the runtime

```typescript
import { RuntimeEngine } from '@angriff36/manifest';
import { TursoStore } from '@angriff36/manifest/stores';

const runtime = new RuntimeEngine(
  ir,
  { user: { id: 'user-1' } },
  {
    storeProvider: (entityName) =>
      new TursoStore({
        url: process.env.TURSO_DATABASE_URL!,
        authToken: process.env.TURSO_AUTH_TOKEN,
        tableName: entityName.toLowerCase(),
      }),
  },
);
```

## Schema helper (not a CLI projection)

There is **no** `turso`
projection in `listBuiltinProjections()`. Schema is a JSON-blob table produced by
`generateTursoSchema(tableName)` (or automatically on first use via the store). Apply DDL in
your app — there is no `manifest generate --projection turso`.

```typescript
import { generateTursoSchema } from '@angriff36/manifest/stores';

const ddl = generateTursoSchema('entities');
// CREATE TABLE IF NOT EXISTS "entities" (
// id TEXT PRIMARY KEY,
// data TEXT NOT NULL,
// created_at TEXT DEFAULT (...),
// updated_at TEXT DEFAULT (...)
// );
```

## Edge deployment

- Turso databases are replicated to edge locations for low-latency reads
- Writes go to the primary and propagate to replicas
- Connection pooling is handled by `@libsql/client`

<Note>
  Install the peer: `npm install @libsql/client`. Package pin: `@angriff36/manifest@3.6.41`.
</Note>


---

## workspaces

URL: https://manifest-b1e8623f.mintlify.app/builder/concepts/workspaces

---
title: 'Documents, folders, and cloud projects'
sidebarTitle: 'Workspaces'
description: 'Learn how Builder handles single Manifest documents, local multi-file workspaces, drafts, templates, and cloud projects.'
---

Builder has three authoring scopes. Choose the narrowest one that matches your source layout.

## Single document

A single document is one editor buffer. You can create it from a built-in template, import a `.manifest` or plain-text file, drag a file into the app, or download the current source.

When the browser grants a writable file handle, Builder can save changes back to that file. Permission is always browser-controlled.

## Local folder workspace

**Open Manifest Folder** loads the `.manifest` files in a selected directory tree. Builder creates a logical workspace with:

- normalized relative paths;
- an active source file;
- a combined project compilation across all files;
- dirty-path tracking;
- direct saves back to granted file handles;
- navigation from cross-file diagnostics to the relevant source.

`use` directives resolve against the workspace file map. Builder rejects path traversal shapes such as `.` and `..` and ignores non-Manifest files during workspace discovery.

<Note>
  Folder access depends on the File System Access API. When directory iteration is unavailable,
  Builder can use a `manifest-workspace.txt` index supplied by the selected workspace.
</Note>

## Cloud projects

With Supabase configured and a user signed in, Builder can save named projects, keep revisions, create share links, and synchronize queued offline saves. If the cloud copy changed while you were offline, Builder presents a conflict instead of silently replacing either version.

Cloud features are optional. Local compilation and generation do not depend on them.

## Drafts and templates

Builder keeps recovery drafts in browser storage. On restart, you can restore or discard the pending draft.

User templates are also stored locally, separately from projects and drafts. Template JSON includes a kind and version marker; Builder validates the file before importing it.

## Saving safely

- Save all dirty workspace paths before closing a folder.
- Resolve cloud conflicts explicitly with **Keep mine**, **Use cloud version**, or **Save copy**.
- Treat browser storage as convenience state, not your only source control.
- Keep application regeneration separate from saving Manifest source. They have different ownership rules.


---

## assemble project

URL: https://manifest-b1e8623f.mintlify.app/builder/guides/assemble-project

---
title: 'Assemble a generated application'
sidebarTitle: 'Assemble a project'
description: 'Combine compatible Manifest projections into a project file map, inspect assembly diagnostics, and export only complete output.'
---

The Assemble panel combines generated projection artifacts with Builder-owned project wiring. It operates on an in-memory file map; it does not write into an existing application directory.

## Select compatible projections

Builder groups projections by category. ORM and framework selections are exclusive: selecting two ORMs or two frameworks creates an assembly conflict.

The assembler routes known outputs into conventional directories, merges generated dependency requirements, and detects artifact path collisions.

## Review the assembly report

The report separates:

- conflicts between selected projection families;
- generation errors that block export;
- warnings that require application configuration;
- informational verification checks;
- environment variable names required by selected outputs.

Generated `.env.example` files contain names only. Builder does not write actual environment values.

## Convex application preset

Selecting Convex uses the dedicated Manifest-backed Convex application preset and its assembly verification. Builder does not substitute generic multi-projection output for that preset.

Export is enabled only when the preset is complete. The output contains source and package requirements; it does not select or initialize a Convex deployment.

## Export options

When assembly is complete, you can:

- download the in-memory files as a ZIP for a new project; or
- open them in StackBlitz for temporary inspection.

<Warning>
  A ZIP export is not an ownership-aware update. Do not extract it over an existing application. Use
  the [safe regeneration workflow](/builder/guides/safe-regeneration) for existing projects.
</Warning>

## Generated authentication

Next.js assembly uses a fail-closed custom authentication seam. Implement the generated application's `lib/auth.ts` before runtime use. The generated stub is not anonymous production authentication.


---

## generate projections

URL: https://manifest-b1e8623f.mintlify.app/builder/guides/generate-projections

---
title: 'Generate and preview projections'
sidebarTitle: 'Generate projections'
description: 'Select Manifest projection surfaces in Builder, inspect generated artifacts, and understand generation diagnostics and environment limits.'
---

The Projections panel lists projections registered by the installed `@angriff36/manifest` package. Builder calls the Manifest projection API and displays returned artifacts and diagnostics.

## Preview a projection

1. Compile valid Manifest source.
2. Open **Projections**.
3. Select a projection and one of its advertised surfaces.
4. Review generated files, diagnostics, and any required options.

Builder caches projection generation by IR and request options. A model or option change invalidates the relevant cache entry.

## Projection versus assembly

A projection preview answers, “What does this Manifest surface emit?” Assembly answers, “Can these selected outputs form one coherent project?”

Assembly adds routing, collision checks, package requirements, framework wiring, and application-preset verification. Do not treat a successful projection preview as proof that a complete application is ready.

## Environment-specific tooling

Some Manifest tooling is intentionally unavailable in the browser:

- coverage commands require filesystem paths and a Node project root;
- generated test tooling requires a provider API key and a Node host;
- the MCP stdio server must run in a host process, not a browser tab.

Builder reports those host requirements instead of attempting to polyfill them.

## Projection catalog

The repository's generated `PROJECTIONS.md` is the source-backed catalog of registered projections and representative emitted surfaces. Run this check after changing the Manifest package pin:

```bash
npm run docs:projections:check
```

If it fails because the installed compiler changed, regenerate and review the catalog:

```bash
npm run docs:projections
```


---

## inspect model

URL: https://manifest-b1e8623f.mintlify.app/builder/guides/inspect-model

---
title: 'Inspect a compiled Manifest model'
sidebarTitle: 'Inspect the model'
description: 'Use Builder panels to understand entities, relationships, command flows, diagnostics, metrics, symbols, changes, and IR.'
---

Builder derives its inspection views from compiler output. Fix compiler errors before relying on projection or assembly results.

## Inspection panels

| Panel   | Use it for                                                                |
| ------- | ------------------------------------------------------------------------- |
| Model   | Browse compiled entities and their properties and commands.               |
| Graph   | Explore entity relationships visually.                                    |
| Flow    | Follow command, guard, mutation, and event flow.                          |
| Changes | Compare the current compiled IR with the previous successful compilation. |
| Checks  | Review compiler diagnostics and completeness findings.                    |
| Metrics | Inspect model size and complexity measurements.                           |
| Symbols | Navigate to declarations in the active source or workspace.               |
| IR      | Inspect the exact compiled intermediate representation.                   |

## Navigate from evidence

Use the Symbols panel or command palette to jump to a declaration. Workspace diagnostics include a logical path; selecting one opens the matching file before moving the editor cursor.

## Compare changes

Builder retains the previous successful IR in the current session. The Changes panel summarizes structural differences rather than comparing raw source lines. Use it to answer questions such as:

- Which entities or commands were added or removed?
- Did a property type or requiredness change?
- Which downstream projection surfaces may need regeneration?

The comparison is an authoring aid, not a database migration plan.

## Inspect generated meaning

The IR panel is the clearest boundary between Manifest and Builder. If a rule is absent from IR, Builder cannot make it authoritative by adding UI behavior. Correct the Manifest source or compiler semantics instead.

<Tip>
  When a projection looks wrong, inspect the IR first, then the projection diagnostics. This
  separates a source/compiler issue from a Builder routing or assembly issue.
</Tip>


---

## run commands

URL: https://manifest-b1e8623f.mintlify.app/builder/guides/run-commands

---
title: 'Run Manifest commands in Studio'
sidebarTitle: 'Run commands'
description: "Execute compiled commands in Builder's isolated runtime, provide inputs, and inspect state, policy failures, guard failures, and events."
---

The Run panel creates an isolated runtime session from the current compiled IR. Use it to inspect command behavior while authoring a model.

## Execution context

Studio currently creates the runtime with this fixed authoring context:

```typescript
{
  source: 'ui',
  user: { id: 'studio-user', role: 'admin' }
}
```

The panel lists entity commands and their parameters, guards, policies, and declared events. Enter parameter values and execute the command.

## Outcomes

Builder distinguishes:

- successful execution and resulting state;
- guard failures with the formatted failed condition;
- policy denials with policy name and message;
- general runtime errors;
- a two-second UI timeout.

Successful outcomes include state before and after execution when the runtime can read it, plus emitted event names and payloads.

<Warning>
  The timeout stops waiting in the UI; it does not cancel the underlying runtime promise. The
  command may still finish. Refresh the execution context before running another command after a
  timeout.
</Warning>

## What Run is not

The Studio runtime is not a production environment, database migration tool, seed runner, or deployment verifier. It does not connect to an existing Convex deployment or application database.


---

## safe regeneration

URL: https://manifest-b1e8623f.mintlify.app/builder/guides/safe-regeneration

---
title: 'Safely create or regenerate an application'
sidebarTitle: 'Safe regeneration'
description: 'Use Builder initial and update modes to create owned files, preview changes, detect conflicts, merge package requirements, and roll back failed updates.'
---

Filesystem project generation has two explicit modes. Both produce a machine-readable plan before writing.

## Initial mode

Use initial mode only for a new application directory. It may create the complete generated structure and initial package configuration.

Initial mode:

- refuses a non-empty or initialized target;
- requires `--destructive-override` before overwriting existing paths;
- writes `.builder/ownership.json` after a successful transaction;
- records a SHA-256 digest for each generated file;
- records Builder-owned dependency, development dependency, and script entries.

The ownership manifest is control metadata. Keep it in source control with the generated application.

## Update mode

Update mode requires `.builder/ownership.json`. It compares three states:

1. the previously recorded Builder-owned hash;
2. the current application file;
3. the newly generated desired content.

The resulting plan contains additions, modifications, conflicts, deletions, dependency-change status, and follow-up actions.

| Situation                                                     | Result                                    |
| ------------------------------------------------------------- | ----------------------------------------- |
| New generated path does not exist                             | Addition                                  |
| Owned file still matches its recorded hash                    | Modification when desired content changed |
| Owned file contains an unrecognized edit                      | Conflict; no apply                        |
| New generated path collides with an app-owned file            | Conflict; Builder does not claim it       |
| Previously owned file is no longer generated and is unchanged | Deletion                                  |
| Previously owned stale file was edited                        | Conflict; file is preserved               |

## Transaction behavior

Builder stages desired writes and snapshots all affected target files in a sibling temporary directory. It verifies plan preconditions, applies only conflict-free plans, and restores the snapshot if any write or deletion fails.

If the target changes after planning, apply stops and asks you to create a new plan.

## Protected application state

Builder refuses to generate or delete:

- `.env` and `.env.*` files at any depth;
- `.convex` deployment state;
- `.vercel`, `.netlify`, and `.firebase` deployment identifiers;
- `package-lock.json`, `pnpm-lock.yaml`, `yarn.lock`, `bun.lock`, `bun.lockb`, and `npm-shrinkwrap.json`.

Application-owned routes, UI, authentication wiring, local configuration, and unrelated files are outside the ownership manifest and are never update targets.

## Package merging

`package.json` is merged by owned entry instead of replaced as a whole.

- Application-added dependencies and scripts remain.
- An unchanged Builder-owned requirement can be updated or removed.
- A Builder-owned entry changed by the application becomes a conflict when the new generated requirement differs.
- Unrelated top-level package fields remain.
- Lockfiles change only through an explicitly enabled dependency install after requirements changed.

## Installs and lifecycle scripts

`--install` has no effect unless generated dependency requirements changed. Install commands use `--ignore-scripts` by default. Add `--allow-lifecycle-scripts` only after reviewing the package changes.

## Convex follow-up

When Convex source changes, the plan reports a `convex-deploy` follow-up. Builder does not execute it. Review the generated source and deploy from the application repository using that application's established process.

<Warning>
  Builder never runs Convex deployment, schema deployment, migrations, seeds, resets, table
  clearing, environment-variable changes, or deployment initialization as part of generation.
</Warning>


---

## introduction

URL: https://manifest-b1e8623f.mintlify.app/builder/introduction

---
title: 'Builder: the visual control plane for Manifest applications'
sidebarTitle: 'Introduction'
description: 'Understand how Builder edits Manifest source, inspects compiled models, generates projections, assembles applications, and manages generated-file ownership.'
---

Builder is a browser-based studio and application-generation control plane for [Manifest](https://manifest.build). You edit one or more `.manifest` source files, compile them to Manifest IR, inspect the resulting model, generate projection artifacts, and assemble runnable application packages.

Builder does not replace Manifest. Manifest owns the language, compiler, IR, runtime semantics, and projection implementations. Builder owns the authoring environment, project orchestration, visualization, assembly presets, and generated-application lifecycle.

## What you can do

<CardGroup cols={2}>
  <Card title="Author and compile" icon="code" href="/builder/concepts/workspaces">
    Edit a single document or open a folder of related Manifest source files with live compiler
    diagnostics.
  </Card>
  <Card title="Inspect the model" icon="diagram-project" href="/builder/guides/inspect-model">
    Browse entities, relationships, flows, symbols, metrics, changes, and compiled IR.
  </Card>
  <Card title="Generate projections" icon="layers" href="/builder/guides/generate-projections">
    Preview registered Manifest projections and their emitted source artifacts.
  </Card>
  <Card title="Assemble applications" icon="box" href="/builder/guides/assemble-project">
    Combine compatible projections with Builder-owned project wiring and package metadata.
  </Card>
  <Card title="Regenerate safely" icon="rotate" href="/builder/guides/safe-regeneration">
    Update only recognized Builder-owned files with conflict detection, package merging, and
    rollback.
  </Card>
  <Card title="Run commands" icon="play" href="/builder/guides/run-commands">
    Exercise compiled commands against an isolated Studio runtime and inspect state and events.
  </Card>
</CardGroup>

## The core workflow

```text
.manifest source
      |
      v
Manifest compiler -> IR + diagnostics
      |                    |
      |                    +-> graphs, checks, flows, metrics, symbols
      v
Manifest projections -> generated source artifacts
      |
      v
Builder assembly -> project file map + assembly report
      |
      v
initial build or ownership-aware update
```

<Info>
  Builder can run locally without a backend. Supabase configuration is optional and enables sign-in,
  cloud project synchronization, share links, and the authenticated AI edge function.
</Info>

## Safety boundary

Assembly creates a desired file map. Filesystem project generation is a separate operation with two explicit modes:

- **Initial** creates a new application and records every generated file in `.builder/ownership.json`.
- **Update** requires that ownership manifest, previews the plan, and changes only recognized Builder-owned files.

Builder never implicitly deploys Convex, initializes a deployment, changes environment variables, runs migrations or seeds, or clears application data.

## Next steps

- [Run Builder locally](/builder/quickstart)
- [Learn the workspace model](/builder/concepts/workspaces)
- [Understand safe regeneration](/builder/guides/safe-regeneration)


---

## quickstart

URL: https://manifest-b1e8623f.mintlify.app/builder/quickstart

---
title: 'Run Builder locally'
sidebarTitle: 'Quickstart'
description: 'Install Builder, start the local Vite application, open Manifest source, and inspect the compiled model.'
---

You can use Builder's local authoring, compilation, workspace, inspection, projection, assembly, and runtime features without configuring a cloud backend.

## Prerequisites

- Node.js 22 or later
- npm 10 or later
- A modern Chromium-based browser for folder access through the File System Access API

<Steps>
  <Step title="Clone and install">

```bash
git clone https://github.com/Angriff36/builder.git
cd builder
npm install
```

  </Step>
  <Step title="Start the development server">

```bash
npm run dev
```

Open the local URL printed by Vite, normally `http://localhost:5173`.

  </Step>
  <Step title="Create or open source">

Use **New document** to start from an empty editor, open a `.manifest` file, or choose **Open Manifest Folder** to load a multi-file workspace.

The editor compiles as you work. The header reports compiler state, and diagnostics appear in the editor and Checks panel.

  </Step>
  <Step title="Inspect the compiled model">

Open the right panel and switch between Model, Graph, Flow, Checks, Metrics, Symbols, and IR. Projection and assembly actions remain blocked while compilation has errors.

  </Step>
</Steps>

## Optional cloud configuration

Create a local `.env` file only when you need sign-in, cloud saves, share links, or AI generation:

```bash
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
```

<Warning>
  Do not put an Anthropic API key in a `VITE_*` variable. Builder sends authenticated AI requests
  through the `manifest-ai` Supabase Edge Function so the browser never receives the provider key.
</Warning>

## Repository checks

Before committing Builder changes, run:

```bash
npm run typecheck
npm run lint
npm test
npm run build
npm run docs:projections:check
```

The projection documentation check verifies that `PROJECTIONS.md` matches the installed Manifest compiler and registered projections.


---

## cloud and ai

URL: https://manifest-b1e8623f.mintlify.app/builder/reference/cloud-and-ai

---
title: 'Cloud, storage, and AI boundaries'
sidebarTitle: 'Cloud and AI'
description: 'Understand which Builder features are local, which require Supabase, what is stored in the browser, and how AI credentials remain server-side.'
---

Builder is local-first. The core studio does not require Supabase.

## Local features

Without cloud configuration you can:

- edit and compile Manifest source;
- open browser-granted files and folders;
- use inspection, projection, assembly, and Run panels;
- keep recovery drafts and user templates in browser storage;
- download source and assembled ZIP files.

## Supabase-backed features

Set both `VITE_SUPABASE_URL` and `VITE_SUPABASE_ANON_KEY` to enable the cloud client. Cloud features include:

- authentication;
- saved projects and revisions;
- share links;
- queued save synchronization and conflict handling;
- calls to the `manifest-ai` Edge Function.

Builder creates a stable random device identifier in local storage for synchronization behavior.

## AI request path

The browser sends the authoring intent, current IR, and any repair feedback to the authenticated `manifest-ai` Supabase Edge Function. The Edge Function holds the provider credential.

Returned source is compiled before insertion. In a folder workspace, validation uses the full workspace snapshot with the proposed active-file revision.

<Warning>
  Never expose provider API credentials through `VITE_*` variables. Vite embeds those variables in
  browser assets.
</Warning>

## Data handling checklist

- Review Manifest source before sending it to configured AI services.
- Use repository source control for durable history.
- Do not treat localStorage drafts or templates as backups.
- Keep application `.env` files and deployment identifiers outside generated-file ownership.


---

## panels

URL: https://manifest-b1e8623f.mintlify.app/builder/reference/panels

---
title: 'Builder panel reference'
sidebarTitle: 'Panel reference'
description: "Reference for Builder's model, graph, flow, changes, checks, metrics, symbols, assistant, projections, assemble, run, docs, help, and IR panels."
---

| Panel       | Source                                | Primary purpose                                    |
| ----------- | ------------------------------------- | -------------------------------------------------- |
| Model       | Compiled IR                           | Browse entity structure and commands.              |
| Graph       | Compiled IR                           | Visualize entity relationships.                    |
| Flow        | Compiled IR                           | Inspect command and event flow.                    |
| Changes     | Current and previous IR               | Review structural model changes.                   |
| Checks      | Diagnostics and completeness analysis | Find compiler and model issues.                    |
| Metrics     | Compiled model                        | Measure size and complexity.                       |
| Symbols     | Source index                          | Navigate declarations.                             |
| Assistant   | IR, source, optional cloud AI         | Scaffold or repair Manifest source.                |
| Projections | Manifest projection registry          | Preview individual generated surfaces.             |
| Assemble    | Selected projections and IR           | Build a coherent project file map and report.      |
| Run         | Isolated runtime session              | Exercise commands and inspect outcomes.            |
| Docs        | Generated domain documentation        | Preview and download model documentation.          |
| Help        | Repository `DSL.md`                   | Read the bundled Manifest language reference.      |
| IR          | Compiler output                       | Inspect the canonical intermediate representation. |

## Keyboard navigation

Open the command palette with `Ctrl+K`. The first ten panels also receive `Ctrl+Alt+<number>` shortcuts based on their displayed order. Use `F8` and `Shift+F8` to move between editor problems.

## Availability

Panels that depend on valid IR may show no result or disable generation when compilation has errors. Fix the source diagnostics first; Builder does not generate from the last successful IR while the active source is invalid.


---

## project generation cli

URL: https://manifest-b1e8623f.mintlify.app/builder/reference/project-generation-cli

---
title: 'Project generation CLI reference'
sidebarTitle: 'Generation CLI'
description: "Reference for Builder's Convex project-generation command, modes, dry runs, apply gates, install flags, and exit behavior."
---

Builder exposes the safe Convex application generation entrypoint through npm:

```bash
npm run project:generate:convex -- [options]
```

The command is a dry run unless `--apply` is present.

## Options

| Option                      | Default                                                   | Meaning                                                                    |
| --------------------------- | --------------------------------------------------------- | -------------------------------------------------------------------------- |
| `--mode initial\|update`    | `update`                                                  | Select new-project or ownership-aware update behavior.                     |
| `--ir <path>`               | `C:/projects/Manifest-source/generated/ir/merged.ir.json` | Manifest IR JSON used by the Convex preset.                                |
| `--target <path>`           | `C:/projects/CapsuleX`                                    | Application directory to plan against.                                     |
| `--name <name>`             | `capsule`                                                 | Generated project name.                                                    |
| `--apply`                   | off                                                       | Apply a conflict-free plan.                                                |
| `--destructive-override`    | off                                                       | Permit initial mode to overwrite generated paths in an initialized target. |
| `--install`                 | off                                                       | Install only when generated dependency requirements changed.               |
| `--allow-lifecycle-scripts` | off                                                       | Allow package lifecycle scripts during an explicitly enabled install.      |

## Create a new project

Review the initial plan:

```bash
npm run project:generate:convex -- \
  --mode initial \
  --ir C:/projects/manifest-source/generated/ir/merged.ir.json \
  --target C:/projects/new-application \
  --name new-application
```

After reviewing additions and conflicts, apply it:

```bash
npm run project:generate:convex -- \
  --mode initial \
  --ir C:/projects/manifest-source/generated/ir/merged.ir.json \
  --target C:/projects/new-application \
  --name new-application \
  --apply
```

## Update an owned project

Run the update without `--apply` first:

```bash
npm run project:generate:convex -- \
  --mode update \
  --ir C:/projects/manifest-source/generated/ir/merged.ir.json \
  --target C:/projects/existing-application \
  --name existing-application
```

The JSON output lists `additions`, `modifications`, `conflicts`, `deletions`, `dependencyRequirementsChanged`, and `followUps`. Resolve every conflict and rerun the plan before adding `--apply`.

## Exit behavior

- Invalid mode or incomplete Convex assembly exits with an error before planning.
- A plan with conflicts sets exit code `2` and is not applied.
- A dry run with no conflicts prints the plan and stops.
- Apply writes transactionally, then optionally runs the gated install.


---

## troubleshooting

URL: https://manifest-b1e8623f.mintlify.app/builder/troubleshooting

---
title: 'Troubleshoot Builder'
sidebarTitle: 'Troubleshooting'
description: 'Resolve common Builder workspace, compiler, projection, assembly, regeneration, cloud, and runtime problems.'
---

<AccordionGroup>
  <Accordion title="Open Manifest Folder finds no files">
    Confirm the selected folder contains `.manifest` files. Builder ignores other extensions. In
    browser environments where directory iteration returns no entries, provide a valid
    `manifest-workspace.txt` index with normalized relative file paths.
  </Accordion>
  <Accordion title="A use directive does not resolve">
    Open the containing folder as a workspace instead of importing one file. Check the directive's
    relative path and exact filename casing. Paths containing `.` or `..` traversal segments are
    rejected.
  </Accordion>
  <Accordion title="Projection or assembly buttons are disabled">
    Compile valid source first. Then select at least one projection and resolve exclusive
    ORM/framework conflicts. Convex export also requires every preset verification check to pass.
  </Accordion>
  <Accordion title="Update mode says ownership manifest missing">
    Update mode only works for applications created by initial mode and carrying
    `.builder/ownership.json`. Builder does not guess ownership for an existing untracked
    application because that could overwrite application code.
  </Accordion>
  <Accordion title="An owned file reports a conflict">
    The file's current SHA-256 hash no longer matches the prior ownership record. Preserve the
    application edit, move the customization to an app-owned seam, or reconcile it with the newly
    generated content. Do not edit the ownership hash to bypass review.
  </Accordion>
  <Accordion title="package.json reports package-entry-modified">
    An application changed a dependency or script that Builder previously owned, and the new
    generated requirement differs. Choose a compatible requirement explicitly. Builder will not
    replace the whole package file.
  </Accordion>
  <Accordion title="--install does not run">
    Installs run only when the plan reports `dependencyRequirementsChanged: true` and `--install` is
    present. Lifecycle scripts remain disabled unless you also pass `--allow-lifecycle-scripts`.
  </Accordion>
  <Accordion title="Convex source changed but deployment did not">
    This is expected. Builder reports Convex deployment as a separate follow-up and never executes
    it. Deploy from the application repository after reviewing the source and using the
    application's established deployment process.
  </Accordion>
  <Accordion title="Cloud save or AI is unavailable">
    Confirm both `VITE_SUPABASE_URL` and `VITE_SUPABASE_ANON_KEY` are configured, restart the Vite
    server, and sign in. Local Studio features continue to work without those values.
  </Accordion>
  <Accordion title="A Run command timed out">
    The panel stops waiting after two seconds, but the underlying execution may still finish.
    Refresh the runtime context before running another command. Treat Studio execution as an
    authoring aid, not production transaction evidence.
  </Accordion>
</AccordionGroup>


---

## Customize Node.js versions for the matrix build

URL: https://manifest-b1e8623f.mintlify.app/cli/ci-cd

---
title: 'CI/CD: GitHub Actions and breaking change detection'
description: 'Generate GitHub Actions workflows, detect breaking changes between IR versions, and integrate Manifest validation into your CI pipeline.'
sidebarTitle: 'CI/CD'
---

Manifest provides CI/CD tooling for automated validation, breaking change detection, and regression testing in continuous integration pipelines. Use the workflow generator to bootstrap a GitHub Actions pipeline, and the breaking change detector to gate pull requests on incompatible IR changes.

## GitHub Actions workflow generator

Generate a complete GitHub Actions workflow file with a single command:

```bash
manifest init --ci github
```

This creates `.github/workflows/manifest-ci.yml` with two jobs:

**PR validation** (runs on every pull request):

- `manifest validate` — validates IR JSON against the IR schema
- `manifest scan` — scans the codebase for governance violations
- `npm test` — runs the full test suite

**Conformance regen** (runs on pushes to `main`):

- Auto-regenerates conformance fixtures
- Commits updated fixtures if they have changed
- Uses `[skip ci]` in the commit message to avoid infinite workflow loops

### Options

| Flag                         | Default    | Description                                               |
| ---------------------------- | ---------- | --------------------------------------------------------- |
| `--ci <provider>`            | (required) | CI provider to generate for. Currently supports `github`. |
| `--node-versions <versions>` | `18,20,22` | Comma-separated Node.js versions for matrix builds        |
| `--force`                    | `false`    | Overwrite existing workflow file                          |

The CLI flag default string is still `18,20,22`, but
`@angriff36/manifest` declares `engines.node: ">=20"`. Prefer `--node-versions 20,22` (or newer
LTS) so the generated matrix matches what the package supports.

```bash
# Customize Node.js versions for the matrix build
manifest init --ci github --node-versions 20,22
```
<Note>
  The command is invoked as `manifest init --ci github` — it is a flag on the existing `init`
  command, not a separate subcommand. It does not run the interactive setup; it only generates the
  workflow file.
</Note>

### Generated workflow structure

```yaml
name: Manifest CI
on:
  pull_request:
  push:
    branches: [main]

jobs:
  validate:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [20, 22]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
      - run: npm ci
      - run: npx manifest validate
      - run: npx manifest scan
      - run: npm test

  conformance-regen:
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
      - run: npm ci
      - run: npm run conformance:regen
      - uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: 'chore: regen conformance fixtures [skip ci]'
```

## Generated-code drift gate

`manifest generate --check` regenerates projection output **in memory** and compares it to the committed files without writing anything. It exits non-zero and lists the drifted files (a missing file or any byte difference counts as drift) — the codegen analogue of `prettier --check`. This lets CI assert that committed code equals freshly generated code, so stale checked-in output can never silently diverge from the IR.

```bash
# Fails the job if any committed file differs from a fresh generation
manifest generate ir/ --output generated/ --check
```

Add it as a PR step alongside `validate` and `scan`:

```yaml
codegen-drift:
  runs-on: ubuntu-latest
  steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-node@v4
      with:
        node-version: 20
    - run: npm ci
    - name: Assert generated code is up to date
      run: npx manifest generate ir/ --output generated/ --check
```

When the job fails, the fix is to run `manifest generate` (without `--check`) locally and commit the result. Pair this with `manifest compile`'s idempotent output (unchanged source re-compiles byte-identically) so a clean `compile` + `generate --check` chain proves the whole `.manifest → IR → code` pipeline is committed and current.

## Breaking change detector

Classify changes between two IR versions as `compatible`, `deprecated`, or `breaking`:

```bash
manifest diff breaking old.ir.json new.ir.json
```

The detector is a pure function (`classifyBreakingChanges`) that compares two IR JSON files and produces a report of every change, classified by severity with consumer impact annotations.

### Classification rules

| Element                           | Change                        | Severity                    |
| --------------------------------- | ----------------------------- | --------------------------- |
| Entity added                      | `entity-added`                | compatible                  |
| Entity removed                    | `entity-removed`              | **breaking**                |
| Property added (optional/default) | `property-added`              | compatible                  |
| Property added (no details)       | `property-added`              | **breaking** (conservative) |
| Property removed                  | `property-removed`            | **breaking**                |
| Property type changed             | `property-type-changed`       | **breaking**                |
| Property made optional            | `property-made-optional`      | compatible                  |
| Property made required            | `property-made-required`      | **breaking**                |
| Computed expression changed       | `computed-expression-changed` | deprecated                  |
| Computed property removed         | `computed-removed`            | **breaking**                |
| Relationship removed              | `relationship-removed`        | **breaking**                |
| Relationship kind changed         | `relationship-kind-changed`   | **breaking**                |
| Constraint removed                | `constraint-removed`          | deprecated                  |
| Constraint severity raised        | `constraint-severity-raised`  | compatible                  |
| Command removed                   | `command-removed`             | **breaking**                |
| Command parameter added           | `command-param-added`         | **breaking**                |
| Command parameter removed         | `command-param-removed`       | **breaking**                |
| Command guards changed            | `command-guards-changed`      | deprecated                  |
| Command returns changed           | `command-returns-changed`     | **breaking**                |
| Policy removed                    | `policy-removed`              | **breaking**                |
| Policy expression changed         | `policy-expression-changed`   | deprecated                  |
| Store removed                     | `store-removed`               | **breaking**                |
| Store target changed              | `store-target-changed`        | **breaking**                |
| Event removed                     | `event-removed`               | **breaking**                |
| Event channel changed             | `event-channel-changed`       | **breaking**                |
| Module added                      | `module-added`                | compatible                  |
| Module removed                    | `module-removed`              | **breaking**                |

The conservative default for `property-added` (no details) is `breaking` because adding a required property without a default value breaks existing consumers. Properties added as optional or with defaults are classified as `compatible`.

### Output format

By default, the detector outputs a human-readable report with color-coded severity indicators:

```
BREAKING entities.Order.properties.discount (property-removed)
  Impact: commands, routes, projections

DEPRECATED entities.Task.computed.dueDate (computed-expression-changed)
  Impact: projections
```

### CI mode

Use `--ci` to gate pipelines on unacknowledged breaking changes:

```bash
manifest diff breaking old.ir.json new.ir.json --ci
```

In CI mode, the command exits non-zero when there are breaking changes not listed in the acknowledgments file. This makes it suitable as a PR gate: if the PR introduces breaking changes, the CI job fails until the changes are acknowledged or the IR is updated to be compatible.

### Acknowledgments file

Create an acknowledgments file to track breaking changes that have been reviewed and accepted:

```json
{
  "acknowledged": [
    {
      "path": "entities.Order.properties.discount",
      "category": "property-removed",
      "reason": "Discount feature removed in v2, all consumers updated"
    },
    {
      "path": "entities.Task.commands.estimate.command-removed",
      "category": "command-removed",
      "reason": "Replaced by async estimateV2 command"
    }
  ]
}
```

Reference it with `--ack`:

```bash
manifest diff breaking old.ir.json new.ir.json --ack acknowledgments.json --ci
```

Acknowledged breaking changes are filtered from the report. Only unacknowledged breaking changes cause a non-zero exit in CI mode.

### Options

| Flag                  | Description                                           |
| --------------------- | ----------------------------------------------------- |
| `--json`              | Output structured JSON instead of human-readable text |
| `--ack <path>`        | Path to acknowledgments file for filtering            |
| `--ci`                | Exit non-zero on unacknowledged breaking changes      |
| `-o, --output <path>` | Write output to a file instead of stdout              |

## Complete PR pipeline example

A complete GitHub Actions workflow combining validation, testing, and breaking change detection:

```yaml
name: Manifest CI
on: [pull_request]

jobs:
  validate:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [20, 22]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
      - run: npm ci
      - run: npx manifest validate
      - run: npx manifest scan --strict
      - run: npm test

  breaking-changes:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm ci
      - name: Compare IR against main branch
        run: |
          git stash
          git checkout origin/main -- ir/
          cp -r ir/ old-ir/
          git stash pop || true
          git checkout - -- ir/
          npx manifest diff breaking old-ir/ ir/ --ci --ack acknowledgments.json
```

This pipeline runs validation and tests across three Node.js versions, then checks for breaking changes against the main branch. The `--ci` flag ensures the job fails if the PR introduces unacknowledged breaking changes.

See [CLI overview](/cli/overview) for the full set of CLI commands, [Governance and audit](/cli/governance) for `manifest scan` and `enforce-surface`, and [Configuration](/cli/configuration) for storing default flags in `manifest.config.yaml`.


---

## Interactive setup

URL: https://manifest-b1e8623f.mintlify.app/cli/commands

---
title: 'Manifest CLI command reference'
sidebarTitle: 'Commands'
description: 'Full syntax, options, and usage examples for core Manifest CLI commands: manifest init, compile, generate, build, and validate — with flags and defaults.'
---

Every command reads `manifest.config.yaml` from your project root when it exists. CLI flags always take precedence over config values. Run any command with `--help` to see its options inline.

---

## `manifest init`

Runs an interactive prompt that collects your project settings and writes a `manifest.config.yaml` to the project root. If a config file already exists, the command exits without overwriting it unless you pass `--force`.

```bash
manifest init
```

### Questions asked

| Prompt                                  | Default         |
| --------------------------------------- | --------------- |
| Where are your `.manifest` files?       | `**/*.manifest` |
| Where should IR output go?              | `ir/`           |
| Will you be generating code from IR?    | No              |
| What projection target?                 | `nextjs`        |
| Where should generated code be written? | `app/api`       |

If the target is `nextjs` or `remix`, the generated code output defaults to `app/api`. For any other target it defaults to `generated`.

### Options

<ParamField path="-f, --force" type="boolean" default="false">
  Overwrite an existing `manifest.config.yaml` (or CI workflow file when `--ci` is used). The
  interactive setup requires a TTY; in non-interactive shells it writes a minimal config with
  defaults.
</ParamField>

<ParamField path="--ci" type="string">
  Generate a CI workflow for the given provider (currently `github`) instead of running the
  interactive config setup. See [CI/CD](/cli/ci-cd).
</ParamField>

<ParamField path="--node-versions" type="string">
  Comma-separated Node.js versions for the generated CI matrix (default: `18,20,22`). Only
  meaningful with `--ci`. Prefer `20,22` — package `engines.node` is `>=20`.
</ParamField>

### Examples

```bash
# Interactive setup
manifest init

# Overwrite an existing config
manifest init --force
```

<Warning>
  `manifest init --force` requires an interactive terminal. In headless or non-TTY environments the
  command writes a minimal default config instead of prompting.
</Warning>

---

## `manifest compile [source]`

Parses one or more `.manifest` source files and writes the compiled IR as `.ir.json` files. When no source is provided, the CLI uses the glob pattern from your config (or `**/*.manifest` by default).

```bash
manifest compile [source] [options]
```

`source` can be a single `.manifest` file path, a directory, or omitted to use the config glob.

### Options

<ParamField path="-o, --output" type="string">
  Output directory or file path. The flag itself has **no default** — when omitted, the CLI falls
  back to the config's `output` field if a config file is present, and otherwise writes alongside
  the source / to stdout depending on the input. (The `ir/` value is the documented config-level
  fallback, not a flag default.) When the output is a directory, each source file produces a
  matching `.ir.json` file inside it.
</ParamField>

<ParamField path="-g, --glob" type="string">
  Glob pattern to select multiple source files. Use this when you pass a directory as `source` and
  want a non-default pattern, or when you want to specify patterns without a config file.
</ParamField>

<ParamField path="-d, --diagnostics" type="boolean" default="false">
  Print compiler diagnostics (errors, warnings, and informational messages) to the console after
  each file is compiled.
</ParamField>

<ParamField path="--pretty" type="boolean" default="true">
  Pretty-print the IR JSON output with two-space indentation. Disable for compact output.
</ParamField>

<ParamField path="--merge" type="boolean" default="false">
  Merge multiple source files into a single IR, resolving `use` declarations across files. Produces
  one combined IR rather than one `.ir.json` per source.
</ParamField>

<ParamField path="--entry" type="string...">
  Entry file(s) for merge compilation (repeatable). The merge starts from these files and follows
  their `use` declarations. Auto-detected when omitted. Only meaningful together with `--merge`.
</ParamField>

<ParamField path="--all" type="boolean" default="false">
  Read all settings from `manifest.config.yaml` and use the config-driven flow instead of individual flags.
  When set, the command uses config values for `output`, `glob`, `diagnostics`, and other options.
  This is the preferred workflow for projects with a config file.
</ParamField>

<ParamField path="--fail-on <policy>" type="string" default="block">
  CI exit policy: `block` (errors only), `warn` (errors or warnings), or `never` (report-only).
</ParamField>

<Note>
  **Idempotent output.** When an output `.ir.json` already exists and was produced from
  byte-identical source (matching `provenance.contentHash`), the compiler reuses the prior
  `provenance.compiledAt` (recomputing `irHash` against it) so re-compiling unchanged source is
  byte-identical — no spurious git diff. A fresh `compiledAt` lands only when the source actually
  changes. Applies to single-file and `--merge` compilation.
</Note>

<Tip>
  **Duplicate event names** produce a compile warning (shown with `--diagnostics` and in the LSP):
  two declarations of the same event name collide in the event registry, so one shadows the other.
  Rename one.
</Tip>

### Examples

```bash
# Compile a single file; output goes to the config output dir if set
manifest compile modules/recipe.manifest

# Compile a single file to a specific directory
manifest compile modules/recipe.manifest --output ir/

# Compile all .manifest files matching a glob
manifest compile --glob "modules/**/*.manifest" --output ir/

# Compile and show diagnostics
manifest compile modules/recipe.manifest --output ir/ --diagnostics

# Use all config defaults (finds all .manifest files via config src pattern)
manifest compile
```

---

## `manifest generate <ir>`

Reads one or more compiled IR files and generates code using the specified projection and surface. `<ir>` can be a single `.ir.json` file or a directory; when it is a directory, the CLI processes every `.ir.json` file found inside it.

```bash
manifest generate <ir> [options]
```

### Options

<ParamField path="-p, --projection" type="string" default="nextjs">
  Projection to use for code generation. Accepts **any registered projection name** — dispatch is
  generic via the projection registry, so this is not a fixed list. Registered projections include
  `nextjs`, `routes`, `prisma`, `prisma-store`, `zod`, `drizzle`, `convex`, `openapi`, `kysely`,
  `graphql`, `react-query`, `express`, `hono`, `mermaid`, `jsonschema`, `storybook`, `sveltekit`,
  `pydantic`, `dynamodb`, `dart`, `terraform`, `elasticsearch`, `analytics`, `materialized-views`,
  and more. Adding a new projection means registering it, not editing the CLI.
</ParamField>

<ParamField path="-s, --surface" type="string" default="all">
  Which surface(s) to generate. Valid surface names are projection-specific; pass `all` to emit every surface the projection declares.

**nextjs** (default): `route` · `detail` · `command` · `dispatcher` · `subscribe` · `subscriptionHook` · `sharedRuntime` · `schedule` · `webhook` · `companions` · `ts.types` · `ts.client` · `all` (everything except concrete per-command routes, which are opt-in)

For other projections the CLI default `all` resolves to the projection's declared surface set (e.g. the `prisma` projection emits a single `schema.prisma` artifact). An unknown surface name is reported as a diagnostic, not silently ignored.

</ParamField>

<ParamField path="-o, --output" type="string">
  Output directory for generated files. Falls back to the config's `projections.<name>.output` value (e.g. `projections.nextjs.output`, `projections.prisma.output`) when not provided.
</ParamField>

<ParamField path="--auth" type="string">
  *(nextjs only)* Auth provider name or import path (e.g. `clerk` or `@repo/auth/server`). Falls
  back to `projections.nextjs.options.authImportPath`, then `authProvider`, then the `authProvider`
  default of `none` (no auth wrapper).
</ParamField>

<ParamField path="--database" type="string">
  *(nextjs only)* Database import path used in generated files (e.g. `@/lib/database` or
  `@repo/database`). Falls back to `projections.nextjs.options.databaseImportPath`, then defaults to
  `@/lib/database`.
</ParamField>

<ParamField path="--runtime" type="string">
  *(nextjs only)* Runtime import path (e.g. `@/lib/manifest-runtime` or `@repo/manifest/runtime`).
  Falls back to `projections.nextjs.options.runtimeImportPath`, then defaults to
  `@/lib/manifest-runtime`.
</ParamField>

<ParamField path="--response" type="string">
  *(nextjs only)* Response helpers import path (e.g. `@/lib/manifest-response` or
  `@repo/manifest/response`). Falls back to `projections.nextjs.options.responseImportPath`, then
  defaults to `@/lib/manifest-response`.
</ParamField>

<ParamField path="--check" type="boolean" default="false">
  Drift mode: regenerate code in memory and compare it to the committed files **without writing
  anything**. Exits non-zero and lists the drifted files (a missing file or any byte difference
  counts as drift). Use it in CI to assert that committed code equals freshly generated code — the
  codegen analogue of `prettier --check`. See [CI/CD](/cli/ci-cd).
</ParamField>

<ParamField path="--all" type="boolean" default="false">
  Read all settings from `manifest.config.yaml` and use the config-driven flow instead of individual flags.
  When set, the command uses config values for `output`, `projection`, `surface`, and other options.
  This is the preferred workflow for projects with a config file.
</ParamField>

#### Examples

```bash
# Default: nextjs, all surfaces
manifest generate ir/

# Just the canonical dispatcher route
manifest generate ir/ --surface dispatcher --output app/api

# Prisma schema from compiled IR
manifest generate ir/ --projection prisma --surface prisma.schema --output prisma

# CI drift gate: fail if committed code is stale vs. the IR
manifest generate ir/ --output generated/ --check
```

Projection-specific options (table names, column mappings, decimal precision, indexes, type overrides, foreign-key names for prisma; auth provider, tenant filters, dispatcher mode for nextjs) live in `manifest.config.yaml` under `projections.<name>.options`. See [Configuration](/cli/configuration) and the per-projection guides ([Next.js](/integration/nextjs), [Prisma](/integration/prisma)).

#### Exit codes

`manifest generate` exits **non-zero** when any projection returns a diagnostic with `severity: 'error'`. Example trigger conditions for the Prisma projection: bare `number`-typed properties (`PRISMA_AMBIGUOUS_NUMBER`), unknown `type.name` with no `typeMappings` override (`PRISMA_UNKNOWN_TYPE`), or an unknown surface name (`UNKNOWN_SURFACE`). The failing diagnostic codes are surfaced in stdout/stderr so CI logs are actionable. Info / warning severities log but do not fail the command.

### Examples

```bash
# Generate using all config defaults (config file must exist)
manifest generate ir/recipe.ir.json

# Override the output directory
manifest generate ir/recipe.ir.json --output apps/api/app/api/

# Generate only TypeScript types
manifest generate ir/recipe.ir.json --surface types --output generated/types.ts

# Generate only the client SDK
manifest generate ir/recipe.ir.json --surface client --output generated/client.ts

# Generate from a directory of IR files with workspace imports
manifest generate ir/ \
  --output apps/api/app/api/ \
  --auth @repo/auth/server \
  --database @repo/database \
  --runtime @repo/manifest/runtime \
  --response @repo/manifest/response
```

---

## `manifest build [source]`

Runs compile and generate as a single pipeline. The source `.manifest` files are compiled to IR in `--ir-output`, then code is generated from that IR into `--code-output`. All config defaults apply to both stages.

```bash
manifest build [source] [options]
```

`source` can be a single `.manifest` file, a glob pattern, or omitted to use the config `src` field.

### Options

<ParamField path="-p, --projection" type="string" default="nextjs">
  Projection name for the generate stage. Accepts any registered projection (e.g. `nextjs`,
  `prisma`, `zod`, `drizzle`, `convex`, `openapi`, `kysely`, …) — see [`manifest
  generate`](#manifest-generate-ir) for the full list.
</ParamField>

<ParamField path="-s, --surface" type="string" default="all">
  Surface(s) to generate. Same values as `manifest generate` — projection-specific, with `all`
  emitting every declared surface (nextjs: `route`, `detail`, `command`, `dispatcher`, `types`,
  `client`).
</ParamField>

<ParamField path="--ir-output" type="string">
  Directory to write compiled IR files. Falls back to the config `output` field, then defaults to
  `ir/`.
</ParamField>

<ParamField path="--code-output" type="string">
  Directory to write generated code files. Falls back to `projections.nextjs.output` in config, then
  defaults to `generated/`.
</ParamField>

<ParamField path="-g, --glob" type="string">
  Glob pattern for selecting multiple source files during the compile stage.
</ParamField>

<ParamField path="--auth" type="string">
  Auth provider or import path, passed through to the generate stage.
</ParamField>

<ParamField path="--database" type="string">
  Database import path, passed through to the generate stage.
</ParamField>

<ParamField path="--runtime" type="string">
  Runtime import path, passed through to the generate stage.
</ParamField>

<ParamField path="--response" type="string">
  Response helpers import path, passed through to the generate stage.
</ParamField>

<ParamField path="--all" type="boolean" default="false">
  Read all settings from `manifest.config.yaml` and use the config-driven flow instead of individual flags.
  When set, the command uses config values for compile and generate stages (output, projection, surface, etc.).
  This is the preferred workflow for projects with a config file.
</ParamField>

### Examples

```bash
# Use all config defaults (recommended after manifest init)
manifest build

# Build a single source file with explicit outputs
manifest build modules/recipe.manifest \
  --ir-output ir/ \
  --code-output app/api/

# Override the code output directory for a staging target
manifest build modules/recipe.manifest --code-output apps/staging/app/api/

# Build multiple files via glob with workspace imports
manifest build \
  --glob "modules/**/*.manifest" \
  --ir-output ir/ \
  --code-output apps/api/app/api/ \
  --auth @repo/auth/server \
  --database @repo/database \
  --runtime @repo/manifest/runtime \
  --response @repo/manifest/response
```

---

## `manifest docs [source]`

Generates a static API reference site from one or more Manifest sources. Inputs can be `.manifest` (compiled first) or `.ir.json` files (loaded directly).

```bash
manifest docs [source] [options]
```

`source` can be a single file, a directory, or a glob.

### Options

<ParamField path="-o, --output" type="string" default="docs-site">
  Output directory for generated docs.
</ParamField>

<ParamField path="-f, --format" type="string" default="html">
  Output format: `html` or `markdown`.
</ParamField>

<ParamField path="-t, --title" type="string" default="Manifest API Reference">
  Site title used in generated pages.
</ParamField>

### IR merge workflow (`mergeIrs` behavior)

When `manifest docs` receives multiple inputs, it merges all compiled/loaded IR objects into one aggregate IR for page generation.

Expected merge contract:

1. Initialize all required IR arrays before merging: `modules`, `values`, `entities`, `enums`, `stores`, `events`, `commands`, `policies`.
2. Append each input IR array into the merged array.
3. Propagate `values` exactly like the other top-level arrays.

```ts
const mergedIR: IR = {
  version: '1.0',
  provenance: firstProvenance,
  modules: [],
  values: [],
  entities: [],
  enums: [],
  stores: [],
  events: [],
  commands: [],
  policies: [],
};

for (const ir of inputIRs) {
  mergedIR.modules.push(...ir.modules);
  mergedIR.values.push(...ir.values);
  mergedIR.entities.push(...ir.entities);
  mergedIR.enums.push(...ir.enums);
  mergedIR.stores.push(...ir.stores);
  mergedIR.events.push(...ir.events);
  mergedIR.commands.push(...ir.commands);
  mergedIR.policies.push(...ir.policies);
}
```

<Note>
  `values` is required by the IR v1 schema even when empty. Any merge path that does not initialize
  and populate `values` can produce nonconformant merged IR and break downstream tooling.
</Note>

### Examples

```bash
# Build docs from all manifest/IR files under src
manifest docs src/ --format markdown --output docs-site

# Build docs from a single compiled IR
manifest docs ir/app.ir.json --output docs-site
```

---

## `manifest validate [ir]`

Validates one or more IR JSON files against the IR v1 JSON schema using [Ajv](https://ajv.js.org). When `ir` is omitted, the CLI searches the current directory for all `*.ir.json` files (excluding `node_modules`, `dist`, and `.next`).

```bash
manifest validate [ir] [options]
```

`ir` can be a single `.ir.json` file, a directory of IR files, or omitted to scan the current directory.

### Options

<ParamField path="--schema" type="string">
  Path to a custom JSON schema file. When omitted, the CLI uses the IR v1 schema bundled with the
  installed package automatically.
</ParamField>

<ParamField path="--strict" type="boolean" default="false">
  Alias for `--fail-on warn` (fail the process on warnings).
</ParamField>

<ParamField path="--fail-on <policy>" type="string" default="block">
  CI exit policy: `block` (errors only), `warn` (errors or warnings), or `never` (report-only).
</ParamField>

### Examples

```bash
# Validate all IR files in the ir/ directory
manifest validate ir/

# Validate a specific IR file
manifest validate ir/recipe.ir.json

# Fail on warnings as well as errors
manifest validate ir/ --strict

# Validate against a custom schema
manifest validate ir/ --schema custom-schema.json
```

<Note>
  The bundled schema path is resolved relative to the installed package, so `manifest validate`
  works correctly whether you run it from inside the Manifest repo or from a consumer project.
</Note>

---

## `manifest config`

Inspection surface for `manifest.config.{yaml,yml,ts,js}`. Three subcommands let you prove what Manifest will actually do before regenerating code — the cornerstone of the CI drift workflow described in [Configuration](/cli/configuration).

```bash
manifest config <subcommand> [options]
```

### `manifest config validate`

JSON-schema validates the build config against [`docs/spec/config/manifest.config.schema.json`](https://github.com/Angriff36/Manifest/blob/main/docs/spec/config/manifest.config.schema.json), the same schema referenced via `$schema:` in your config file.

```bash
manifest config validate
manifest config validate --json
```

<ParamField path="--json" type="boolean" default="false">
  Emit structured JSON (`{ configPath, ok, diagnostics }`) instead of human-readable output. Useful for CI pipelines that need machine-parseable failures.
</ParamField>

The command exits non-zero on the first schema violation and prints a dotted-path diagnostic for each error, e.g.:

```text
  - projections.nextjs.options.authProvider: must be equal to one of the allowed values (allowed: clerk, nextauth, custom, none)
  - projections.nextjs.options: unknown property "unknownOption" (additionalProperties: false)
```

### `manifest config print-defaults`

Prints the canonical defaults snapshot — every value Manifest applies when an option is unset. Sourced from `src/manifest/projections/nextjs/defaults.ts` so the projection, the schema, and this command can never disagree.

```bash
manifest config print-defaults
```

Output is key-sorted JSON across six blocks: `nextjs`, `tenantProvider`, `dispatcher`, `concreteCommandRoutes`, `readRoutes`, `routes`. See the [configuration reference](/cli/configuration) for what each key controls.

### `manifest config inspect`

Aliased as `print-effective`. Prints the **effective** configuration — defaults merged with user overrides from your config file. The output is deterministic and key-sorted, making it safe to snapshot in CI.

```bash
manifest config inspect
manifest config inspect --json
```

```json
{
  "configPath": "/work/app/manifest.config.yaml",
  "build": { "output": "ir/", "src": "**/*.manifest" },
  "projections": {
    "nextjs": {
      "output": "generated/",
      "options": {
        "appDir": "apps/api/app/api",
        "authProvider": "clerk",
        "concreteCommandRoutes": {
          "enabled": false,
          "legacyAliasesOnly": true
        },
        "dispatcher": {
          "deriveInstanceId": true,
          "enabled": true,
          "executionMode": "inline",
          "executorImportName": "executeManifestCommand",
          "executorImportPath": "@/lib/manifest-executor",
          "path": "/manifest/[entity]/commands/[command]/route.ts"
        },
        "readRoutes": {
          "directDbReads": true,
          "enabled": true
        },
        "unauthorizedStatus": 401,
        ...
      }
    }
  }
}
```

<Tip>
  Commit the inspect output as a snapshot under VCS (e.g.
  `.manifest/effective-config.snapshot.json`) and gate CI on `git diff --exit-code` against it.
  Drift in either Manifest defaults or your local config will then surface as a diff before it
  changes emitted code.
</Tip>

---

## `manifest watch [source]`

Watches `.manifest` files and rebuilds (compile + generate) on every change. Useful during local development so generated code stays in sync with your source.

```bash
manifest watch [source] [options]
```

### Options

`watch` accepts the same flags as `manifest build` — `-p, --projection`, `-s, --surface`, `--ir-output`, `--code-output`, `-g, --glob`, and the `--auth`/`--database`/`--runtime`/`--response` import-path flags — plus:

<ParamField path="--debounce" type="number" default="300">
  Debounce delay in milliseconds before a detected change triggers a rebuild.
</ParamField>

<ParamField path="--events" type="boolean" default="false">
  Emit structured JSON change events to stdout, useful for piping into other tooling.
</ParamField>

<ParamField path="--clear" type="boolean" default="false">
  Clear the terminal on each rebuild.
</ParamField>

<ParamField path="--all" type="boolean" default="false">
  Read all settings from `manifest.config.yaml` and use the config-driven flow instead of individual flags.
  When set, the command uses config values for compile and generate stages (output, projection, surface, etc.).
  This is the preferred workflow for projects with a config file.
</ParamField>

### Examples

```bash
# Watch a directory and rebuild on change
manifest watch src/

# Watch with a custom debounce and JSON change events
manifest watch src/ --debounce 300 --events
```

---

## `manifest validate-ai [source]`

Validates a `.manifest` or IR file and returns scored diagnostics tuned for AI agents, so an agent can gate on a numeric pass threshold rather than parsing free-form output.

```bash
manifest validate-ai [source] [options]
```

`source` can be a `.manifest` file, an `.ir.json` file, a directory, or a glob.

### Options

<ParamField path="-f, --format" type="string" default="text">
  Output format: `text` or `json`.
</ParamField>

<ParamField path="--schema" type="string">
  Path to a custom JSON schema file.
</ParamField>

<ParamField path="--min-score" type="number" default="100">
  Minimum score required to pass. The command exits non-zero when the score falls below this value.
</ParamField>

<ParamField path="--verbose" type="boolean" default="false">
  Include info-level diagnostics in the output.
</ParamField>

### Diagnostic categories

Every diagnostic carries a `category`: `schema`, `compile`, `semantic`, `structural`, or `domain`. The **`domain`** category surfaces compile-time domain- and reaction-wiring checks against the IR with coded diagnostics (each includes a fix suggestion):

| Code                   | Severity | Meaning                                                                                                                                                                  |
| ---------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `DOMAIN_UNWIRED_FK`    | error    | A `{parent}Id`-style foreign key with no `belongsTo`/`ref` and no command that sets it from `self.id`.                                                                   |
| `DOMAIN_ORPHAN_CREATE` | error    | A child entity with no command or reaction that wires its parent FK.                                                                                                     |
| `REACTION_UNWIRED`     | error    | A reaction whose event is never emitted by a command, or whose payload references can't be satisfied by emitter params, `payload._subject`, or create-result properties. |

Non-error domain findings are reported as `DOMAIN_COMPLETENESS` / `REACTION_COMPLETENESS`. These checks mirror the compiler's domain-completeness semantics (see `docs/spec/semantics.md`) — a half-wired model fails validation.

### Examples

```bash
# Score an IR file as JSON, passing at 80 or above
manifest validate-ai generated.ir.json --format json --min-score 80
```

---

## `manifest fmt [source]`

Formats `.manifest` source files using deterministic whitespace normalization. Pairs well with a pre-commit hook to keep source style consistent.

```bash
manifest fmt [source] [options]
```

`source` can be a file, directory, or glob.

### Options

<ParamField path="--check" type="boolean" default="false">
  Fail if any file would change. When set, writing is disabled — ideal for CI.
</ParamField>

<ParamField path="--write" type="boolean">
  Write formatted output. This is the default when `--check` is not set.
</ParamField>

<ParamField path="-g, --glob" type="string">
  Glob pattern to use when `source` is a directory.
</ParamField>

### Examples

```bash
# Check formatting without writing (CI mode)
manifest fmt src/ --check

# Format and write changes in place
manifest fmt src/ --write
```

---

## `manifest diagram [source]`

Generates Mermaid diagrams from IR — entity-relationship, state, and sequence views — so you can visualize a program's shape.

```bash
manifest diagram [source] [options]
```

`source` can be a `.manifest`, `.ir.json`, directory, or glob.

### Options

<ParamField path="-o, --output" type="string" default="diagrams">
  Output directory for generated diagram files.
</ParamField>

<ParamField path="-t, --type" type="string" default="all">
  Diagram type: `er`, `state`, `sequence`, or `all`.
</ParamField>

<ParamField path="-e, --entity" type="string">
  Filter output to a single entity.
</ParamField>

<ParamField path="--markdown" type="boolean" default="false">
  Wrap output in fenced code blocks for embedding in Markdown.
</ParamField>

### Examples

```bash
# Generate an ER diagram wrapped in Markdown fences
manifest diagram src/app.manifest -t er --markdown
```

---

## `manifest coverage`

Reports command/guard/policy/constraint coverage by correlating compiled IR with conformance and unit test evidence found in the project.

```bash
manifest coverage [options]
```

### Options

<ParamField path="--ir" type="string">
  Path to a compiled IR JSON file.
</ParamField>

<ParamField path="-s, --source" type="string">
  Path to `.manifest` source, compiled on the fly.
</ParamField>

<ParamField path="-r, --root" type="string" default=".">
  Root directory to scan for test evidence.
</ParamField>

<ParamField path="-f, --format" type="string" default="text">
  Output format: `text` or `json`.
</ParamField>

<ParamField path="--min-coverage" type="number">
  Minimum coverage percentage required to pass.
</ParamField>

<ParamField path="--strict" type="boolean" default="false">
  Exit non-zero when coverage falls below `--min-coverage`.
</ParamField>

### Examples

```bash
# Fail CI if coverage drops below 80%
manifest coverage -s src/app.manifest --min-coverage 80 --strict
```

---

## `manifest diff`

`diff` is a parent command for comparing source against IR, comparing two IR files, and classifying breaking changes.

```bash
manifest diff <subcommand> [options]
```

### `manifest diff source-vs-ir <entityName>`

Compares source-manifest parse output against precompiled IR for a single entity.

<ParamField path="--json" type="boolean" />
<ParamField path="--src" type="string">
  Source glob pattern.
</ParamField>
<ParamField path="--ir-root" type="string">
  One or more IR root paths.
</ParamField>

### `manifest diff ir-vs-ir <oldIR> <newIR>`

Compares two IR JSON files and produces a diff report, optionally rendering migration SQL or Prisma steps.

<ParamField path="--json" type="boolean" />
<ParamField path="--sql" type="boolean">
  Render the diff as migration SQL.
</ParamField>
<ParamField path="--prisma" type="boolean">
  Render the diff as Prisma migration steps.
</ParamField>
<ParamField path="-o, --output" type="string">
  Output path.
</ParamField>

```bash
manifest diff ir-vs-ir old.ir.json new.ir.json --sql
```

---

## `manifest diff breaking`

Classifies the changes in an IR diff as compatible, deprecated, or breaking, with consumer impact. Run it as a `diff` subcommand. Use `--ci` to gate pipelines on unacknowledged breaking changes.

```bash
manifest diff breaking <oldIR> <newIR> [options]
```

### Options

<ParamField path="--json" type="boolean" />

<ParamField path="--ack" type="string">
  Path to an acknowledgement file listing breaking changes that have been reviewed and accepted.
</ParamField>

<ParamField path="--ci" type="boolean" default="false">
  Exit non-zero when there are unacknowledged breaking changes.
</ParamField>

<ParamField path="-o, --output" type="string">
  Output path.
</ParamField>

### Examples

```bash
# Fail CI on unacknowledged breaking changes
manifest diff breaking old.ir.json new.ir.json --ci
```

---

## `manifest migrate`

Analyzes an IR diff to produce a database migration plan, with optional SQL/Prisma preview and reversibility checks.

```bash
manifest migrate [options]
```

### Options

<ParamField path="--old-ir" type="string" required>
  Path to the previous IR file.
</ParamField>

<ParamField path="--new-ir" type="string" required>
  Path to the new IR file.
</ParamField>

<ParamField path="--dry-run" type="boolean" default="false">
  Show the plan without applying it.
</ParamField>

<ParamField path="--preview" type="boolean" default="false">
  Show the generated SQL and Prisma steps.
</ParamField>

<ParamField path="--force" type="boolean" default="false">
  Apply despite warnings or unacknowledged breaking changes.
</ParamField>

<ParamField path="--tool" type="string" default="prisma">
  Migration tool: `prisma` or `drizzle`.
</ParamField>

<ParamField path="--no-check-reversibility" type="boolean">
  Skip reversibility validation.
</ParamField>

<ParamField path="--json" type="boolean" />
<ParamField path="-o, --output" type="string">
  Output path.
</ParamField>

### Examples

```bash
# Preview a migration plan between two IR versions
manifest migrate --old-ir old.ir.json --new-ir new.ir.json --preview
```

---

## `manifest changelog <from-ref> [to-ref]`

Generates a Markdown changelog from IR diffs between two Git refs. `to-ref` defaults to `HEAD`.

```bash
manifest changelog <from-ref> [to-ref] [options]
```

### Options

<ParamField path="-s, --source" type="string" default="**/*.manifest">
  Source glob pattern.
</ParamField>

<ParamField path="-o, --output" type="string">
  Output path for the changelog file.
</ParamField>

<ParamField path="-t, --title" type="string">
  Changelog title.
</ParamField>

<ParamField path="--json" type="boolean" />

### Examples

```bash
# Generate a changelog from v0.3.0 to HEAD
manifest changelog v0.3.0 HEAD -o CHANGELOG.md
```


---

## or:

URL: https://manifest-b1e8623f.mintlify.app/cli/configuration

---
title: 'Configure Manifest with manifest.config.yaml'
sidebarTitle: 'Configuration'
description: 'Full reference for manifest.config.yaml: source globs, IR output, projection options (nextjs, routes, prisma, prisma-store), preflight env declarations, hooks, plugins, naming convention, auth providers, and monorepo import paths.'
---

`manifest.config.yaml` is the project-level config file that the CLI loads from your project root before executing any command. You create it by running `manifest init` or by writing it by hand. Every field is optional — the CLI falls back to built-in defaults for any field you omit.

## Config file locations

The CLI searches for a config file in the following order, using the first one it finds:

1. `manifest.config.yaml`
2. `manifest.config.yml`
3. `.manifestrc.yaml`
4. `.manifestrc.yml`

All four files live at your project root (the directory where you run `manifest` commands).

Runtime bindings that contain JavaScript values—store classes, store instances,
factories, and `resolveUser`—belong in `manifest.config.ts` or
`manifest.config.js`. A TypeScript config may also contain a `build` block; its
build values override YAML.

## Durable storage for generated runtime companions

`DATABASE_URL` only supplies connection details. It does not tell Manifest
which entity uses which adapter or table. Put those bindings in
`manifest.config.ts`:

```ts
import { defineConfig } from '@angriff36/manifest/config';
import { PostgresStore } from '@angriff36/manifest/stores';

const connectionString = process.env.DATABASE_URL;
if (!connectionString) throw new Error('DATABASE_URL is required');

export default defineConfig({
  stores: {
    Customer: { implementation: new PostgresStore({ connectionString, tableName: 'customers' }) },
    Invoice: { implementation: new PostgresStore({ connectionString, tableName: 'invoices' }) },
  },
});
```

Then point the Next.js runtime companion at that file:

```yaml
projections:
  nextjs:
    output: generated
    options:
      # Relative to the emitted lib/manifest-runtime.ts file.
      runtimeConfigImport: '../manifest.config'
```

Only `memory` and browser `localStorage` work without this binding. Generated
runtimes containing `postgres`, `supabase`, `durable`, or custom stores fail
clearly when `runtimeConfigImport` is absent. See [Stores](/language/stores#complete-generated-companion-postgresql-setup) for the full source-to-runtime example.

## Option priority

When resolving any value, the CLI checks three sources in this order:

1. **CLI flag** (highest priority) — value passed directly on the command line
2. **Config file** — value from `manifest.config.yaml`
3. **Built-in default** (lowest priority) — fallback hardcoded in the CLI

You can always override a config value for a single run without modifying the file.

## Top-level fields

<ParamField path="$schema" type="string">
  Optional pointer to the config JSON Schema for editor IntelliSense. Manifest does **not** publish a resolvable schema URL, and `manifest config validate` loads the schema bundled with the package — it never fetches a URL. For autocomplete without a dead link, map the bundled schema in `.vscode/settings.json`:

```jsonc
{
  "yaml.schemas": {
    "./node_modules/@angriff36/manifest/docs/spec/config/manifest.config.schema.json": [
      "manifest.config.yaml",
      ".manifestrc.yaml",
    ],
  },
}
```

If you do set `$schema`, point it at a real local path — never a public URL Manifest doesn't host.

</ParamField>

<ParamField path="src" type="string" default="**/*.manifest">
  Glob pattern that selects your `.manifest` source files. Used by `manifest compile` and `manifest build` when no `source` argument is passed on the command line.

```yaml
src: modules/**/*.manifest
```

</ParamField>

<ParamField path="output" type="string" default="ir/">
  Directory where compiled IR JSON files are written. Used by `manifest compile` as the default `--output` value, and by `manifest build` as the default `--ir-output` value.

```yaml
output: ir/
```

</ParamField>

<ParamField path="naming" type="string | object">
  Global identifier-casing convention inherited by projections that map IR names to physical database names (currently the Prisma projection). Opt-in; omit to emit IR names verbatim. A per-projection `projections.<name>.options.naming` overrides this default.

The string shorthand `snake_case` expands to `{ table: snake_case, column: snake_case, pluralizeTables: true }` — e.g. `createdAt` → `@map("created_at")`, `Widget` → `@@map("widgets")`. The object form tunes each axis (`table`: `snake_case` | `camelCase` | `PascalCase` | `preserve`; `column`: `snake_case` | `camelCase` | `preserve`; `pluralizeTables`: boolean, default `true`).

```yaml
naming: snake_case
# or:
naming:
  table: snake_case
  column: snake_case
  pluralizeTables: true
```

Only `@map`/`@@map` attributes are added — Prisma model names and field identifiers stay as the IR name, so relations and indexes are unaffected. Explicit `tableMappings`/`columnMappings` always win and are the escape hatch for irregular plurals.

</ParamField>

<ParamField path="prismaSchema" type="string">
  Path to your Prisma schema file, used by property alignment checks. When omitted, the CLI searches `prisma/schema.prisma`, `schema.prisma`, and `db/schema.prisma` in that order.

```yaml
prismaSchema: prisma/schema.prisma
```

</ParamField>

<ParamField path="hooks" type="object">
  Hook settings: Git pre-commit settings consumed by `manifest install-hooks`, plus optional build lifecycle scripts.

<ParamField path="hooks.skipInCi" type="boolean" default="true">
  Skip running the generated pre-commit hook in CI environments.
</ParamField>
<ParamField path="hooks.provider" type="string" default="husky">
  Git hook manager the pre-commit hook is installed into. Accepted values: `husky`,
  `simple-git-hooks`.
</ParamField>
<ParamField path="hooks.runFmt" type="boolean" default="true">
  Run `manifest fmt` from the generated pre-commit hook.
</ParamField>
<ParamField path="hooks.runValidate" type="boolean" default="true">
  Run `manifest validate` from the generated pre-commit hook.
</ParamField>

<ParamField path="hooks.lifecycle" type="object">
  Build lifecycle scripts (not git hooks). These are shell scripts or Node scripts run at specific points in the build process. Paths are relative to the project root.
</ParamField>

<ParamField path="hooks.lifecycle.beforeCompile" type="array">
  Scripts (paths) run before `manifest compile`. Executes in the order listed. Fails the build if any script exits non-zero.
</ParamField>

<ParamField path="hooks.lifecycle.afterGenerate" type="array">
  Scripts (paths) run after a successful `manifest generate` or `manifest generate --all`. Executes in the order listed. Fails the build if any script exits non-zero.
</ParamField>

```yaml
hooks:
  # Git pre-commit hooks
  provider: husky
  skipInCi: true
  runFmt: true
  runValidate: true
  # Build lifecycle scripts
  lifecycle:
    beforeCompile:
      - scripts/pre-compile.sh
    afterGenerate:
      - scripts/post-generate.js
```

</ParamField>

<ParamField path="plugins" type="array">
  Third-party plugin declarations. At CLI startup any declared plugins are loaded and the CLI commands they contribute are registered, so they appear in `manifest --help` and are dispatchable; `manifest plugins list` reports their real load status and diagnostics. Loading is conditional — a project with no `plugins` declared loads nothing. A plugin that fails to load surfaces a diagnostic and is skipped; it never bricks the CLI. For runtime use (custom store adapters, expression builtins, audit sinks), compose the loaded registries into your runtime options with `pluginRegistriesToRuntimeOptions` from `@angriff36/manifest/plugin-loader` — see [Plugin API](/extensibility/plugin-api). Each entry requires a `module`; `enabled`, `options`, `order`, and `capabilities` are optional.
</ParamField>

<ParamField path="plugins[].module" type="string" required>
  npm package name or relative file path to the plugin module.
</ParamField>
<ParamField path="plugins[].options" type="object">
  Plugin-specific options forwarded to the plugin's `onLoad` hook and to its store-adapter and
  audit-sink factories.
</ParamField>
<ParamField path="plugins[].enabled" type="boolean" default="true">
  Whether the plugin is active.
</ParamField>
<ParamField path="plugins[].order" type="integer">
  Load order for this plugin. Lower numbers load first. Ties break by `module` string (localeCompare). Missing `order` sorts after any explicit order (stable relative to other unordered entries by module). Use this when plugin A depends on plugin B being loaded first.
</ParamField>
<ParamField path="plugins[].capabilities" type="array">
  Well-known capability tags the plugin advertises. Accepted values: `storeAdapter`, `auditSink`, `builtin`, `cliCommand`, `projection`. Unknown tags are kept (host extension) but surface an info diagnostic. Used for plugin discovery and dependency checking.
</ParamField>

```yaml
plugins:
  - module: '@my-org/manifest-plugin-audit'
    enabled: true
    order: 10
    capabilities:
      - auditSink
    options:
      level: strict
  - module: './local-plugins/naming-lint.js'
    order: 20
    capabilities:
      - cliCommand
```

</ParamField>

<ParamField path="env" type="object">
  Structured environment-variable mapping for **preflight validation** — grouped definitions of the variables your stores, auth, adapters, and custom settings require. Consumed by `manifest preflight` to check the environment and to generate `.env.example`.

There is **no** `env(VAR)` config-load inliner. This `env` block
only _declares_ variable metadata for `manifest preflight` / `.env.example`. Inject secrets with
`process.env` in `manifest.config.ts` (see [Durable storage](#durable-storage-for-generated-runtime-companions)).

The block has four optional groups — `stores`, `auth`, `adapters`, `custom` — each a map of an arbitrary key to an `EnvVarDefinition`. Each definition requires `name` and may set `description`, `required` (default `true`), `default`, and `example`.

```yaml
env:
  stores:
    primaryDb:
      name: DATABASE_URL
      description: Postgres connection string
      required: true
      example: postgres://user:pass@localhost:5432/app
  auth:
    clerk:
      name: CLERK_SECRET_KEY
      required: true
  adapters:
    outbox:
      name: OUTBOX_TABLE
      required: false
      default: manifest_outbox
  custom:
    region:
      name: AWS_REGION
      default: us-east-1
```

</ParamField>

## `mergeIntegrity`

<ParamField path="mergeIntegrity" type="object">
  Controls multi-file compilation behavior when duplicate names or cross-module references appear. Only applies when compiling multiple `.manifest` files (e.g. `manifest compile --all` or multi-module repos).
</ParamField>

<ParamField path="mergeIntegrity.onDuplicateEntity" type="string" default="error">
  Policy when multiple files declare an entity with the same name.

  - `error` (default): emit a diagnostic and fail compilation.
  - `lastWins`: silently use the last declaration (file load order wins).

  ```yaml
  mergeIntegrity:
    onDuplicateEntity: lastWins # Allow duplicate entity names; last file wins
  ```
</ParamField>

<ParamField path="mergeIntegrity.onDuplicateCommand" type="string" default="error">
  Policy when multiple files declare a command with the same name (on the same entity or across entities). Accepted values: `error`, `lastWins`. Defaults to `error`.
</ParamField>

<ParamField path="mergeIntegrity.allowCrossModuleRefs" type="boolean" default="true">
  Whether relationships (`hasMany`, `hasOne`, `belongsTo`, `ref`) may target entities defined in other modules (other `.manifest` files). When `false`, cross-module relationships emit a diagnostic and fail compilation. Defaults to `true`.

  ```yaml
  mergeIntegrity:
    allowCrossModuleRefs: false # Block relationships across modules
  ```
</ParamField>

<ParamField path="mergeIntegrity.forbidCycles" type="boolean" default="true">
  Whether circular relationship chains are rejected. When `true`, the compiler emits a diagnostic if a cycle is detected (e.g. `A hasMany B`, `B hasMany A`). This is a **required invariant** — setting it to `false` is rejected at config resolution time. Defaults to `true`.
</ParamField>

<ParamField path="mergeIntegrity" type="object">
  **Why this exists.** In multi-file projects, duplicate names and cross-file references can introduce silent bugs or ambiguous semantics. `mergeIntegrity` makes these conditions explicit and configurable. Use `lastWins` during rapid prototyping with split files; use `error` in production to enforce uniqueness. Use `allowCrossModuleRefs: false` to enforce module boundaries.
</ParamField>

## `validation`

<ParamField path="validation" type="object">
  Preflight validation rules that emit diagnostics when certain structural conditions are detected. These are **compile-time checks only** — they do not affect runtime behavior. Rules are disabled by default; enable the ones you want.
</ParamField>

<ParamField path="validation.rules" type="array">
  List of validation rule names to enable. Accepted values: `missing-policy`, `unused-entity`, `orphan-relationship`.

  <ParamField path="validation.rules" type="array">
  **`missing-policy`** — Emit a diagnostic when an entity has commands but no policies target it **and** `defaultPolicies` is empty. Helps catch missing authorization for command-bearing entities.

  **`unused-entity`** — Emit a diagnostic for "bare declaration" entities: not external, have no store, no entity-scoped commands, and are not referenced by any other entity's relationships. Helps identify dead code.

  **`orphan-relationship`** — Emit a diagnostic for one-sided relationships where an entity has `belongsTo` or `ref` pointing to a parent, but the parent lacks a corresponding `hasMany` or `hasOne` inverse. Helps catch typos and missing inverse declarations.
  </ParamField>

  ```yaml
  validation:
    rules:
      - missing-policy # Flag entities with commands but no policies
      - unused-entity # Flag entities that are never referenced
      - orphan-relationship # Flag one-sided relationships
  ```
</ParamField>

<ParamField path="validation.failOn" type="array">
  Severity levels that should fail compilation. Accepted values: `ok`, `warn`, `block`. When omitted, all severities pass (diagnostics are emitted but compilation continues). Set to `['block']` or `['warn', 'block']` to halt on specific severities.
</ParamField>

## `provenance`

<ParamField path="provenance" type="object">
  Controls IR provenance stamps and lockfile behavior. IR always carries required provenance fields (schema version, compiler version, content hash). This config determines whether timestamps are deterministic and whether a lockfile is written.
</ParamField>

<ParamField path="provenance.stamp" type="boolean" default="true">
  When `false`, skip writing the provenance lockfile. IR is still stamped with required fields (this flag only controls the lockfile write). Defaults to `true`.
</ParamField>

<ParamField path="provenance.fields" type="array" default="['sourceHash', 'generatorVersion', 'irSchemaVersion', 'gitSha']">
  Requested stamp fields. Required IR fields are always stamped regardless of this list. `gitSha` is accepted but not yet written (deferred). Defaults to all four fields.
</ParamField>

<ParamField path="provenance.deterministic" type="boolean" default="false">
  When `true`, use a fixed `compiledAt` timestamp (`1970-01-01T00:00:00.000Z`) instead of wall-clock time. Required for reproducible builds. Defaults to `false`.
</ParamField>

<ParamField path="provenance.lockfile" type="string">
  Path for the provenance lockfile (relative to the project root). When omitted, the default is `.manifest/versions.json`.
</ParamField>

<ParamField path="provenance.failIfStale" type="boolean" default="false">
  When `true` and a lockfile exists: if source content (`contentHash`) matches the lock but the IR hash (`irHash`) differs under deterministic mode, compilation fails. This catches hand-edited IR or non-deterministic compiler behavior. Defaults to `false`.
</ParamField>

```yaml
provenance:
  deterministic: true # Use fixed timestamp for reproducible builds
  lockfile: .manifest/versions.json
  failIfStale: true # Fail if IR drifts despite unchanged sources
```

## `runtime`

<ParamField path="runtime" type="object">
  Central runtime build knobs that affect generated companion factories and execution mode. These settings are consumed by projections (Next.js, Express, Hono, Remix, SvelteKit) and by the generated runtime helpers.
</ParamField>

<ParamField path="runtime.executionMode" type="string" default="inline">
  Dispatcher execution mode for the Next.js projection. Accepted values: `inline`, `externalExecutor`.

  - `inline` (default): Generated handlers call `createManifestRuntime(...)` then `runtime.runCommand(...)` per request.
  - `externalExecutor`: Handlers import a configured executor and delegate — no `createManifestRuntime` or `runtime.runCommand` appears in emitted code.

  See [`projections.nextjs.options.dispatcher`](#projectionsnextjsoptionsdispatcher) for the executor migration guide.
</ParamField>

<ParamField path="runtime.determinism" type="object">
  Determinism settings for the generated runtime factory.

  <ParamField path="runtime.determinism.deterministicMode" type="boolean" default="false">
    When `true`, the factory injects `RuntimeOptions.deterministicMode` (fixed clock, seeded `generateId`). Useful for reproducible tests.
  </ParamField>

  <ParamField path="runtime.determinism.forbidWallClock" type="boolean" default="false">
    When `true`, the factory requires `RuntimeOptions.now` to be provided (no `Date.now` fallback). Enforces explicit time control.
  </ParamField>

  <ParamField path="runtime.determinism.seed" type="number">
    Fixed clock milliseconds when `forbidWallClock` is `true`. Also seeds the `generateId` sequence. Ignored unless `forbidWallClock` is set.
  </ParamField>
</ParamField>

<ParamField path="runtime.stores" type="string">
  Import path for store bindings (`ManifestRuntimeConfig` module). Fans into projection `runtimeConfigImport` when that projection-specific key is unset. Used by Next.js, Express, Hono, Remix, and SvelteKit projections.

  ```yaml
  runtime:
    stores: './manifest.config' # Points to the TS config with store bindings
  ```
</ParamField>

<ParamField path="runtime.defaultContext" type="object">
  Default runtime context merged into `createManifestRuntime` calls (caller-provided context wins on conflict). Useful for global constants like `appVersion` or `region`.
</ParamField>

<ParamField path="runtime.concurrency" type="object">
  Parallel execution settings.

  <ParamField path="runtime.concurrency.maxParallelCommands" type="integer">
    Maps to `RuntimeOptions.maxParallelCommands` on the generated factory. Positive integer only; invalid values are ignored (unlimited parallelism).
  </ParamField>
</ParamField>

## `projections`

The `projections` block configures code generation per target. Each key matches a registered projection name. The keys validated by the config schema are `nextjs`, `routes`, `prisma`, and `prisma-store`; the `nextjs` projection is documented in full below, with `prisma`/`prisma-store` summarized at the end of this section. (Many more projections are registered — zod, drizzle, convex, openapi, kysely, graphql, and others — and are driven entirely by CLI flags; only the four above accept structured config blocks today.)

### `projections.nextjs.output`

<ParamField path="projections.nextjs.output" type="string" default="generated/">
  Directory where the Next.js projection writes generated route files. Used by `manifest generate` and `manifest build` as the default `--output` / `--code-output` value.

```yaml
projections:
  nextjs:
    output: app/api/
```

</ParamField>

### `projections.nextjs.options`

These options control the import paths and filtering behavior written into generated route files.

<ParamField path="projections.nextjs.options.authProvider" type="string" default="none">
  Auth provider name. Used when `authImportPath` is not set. Accepted values: `clerk`, `nextauth`, `custom`, `none`. Defaults to `none` so a basic entity generates runnable code with no auth wrapper — opt into a provider explicitly.

```yaml
options:
  authProvider: clerk
```

</ParamField>

<ParamField path="projections.nextjs.options.authImportPath" type="string">
  Import path for your auth helper. When set, this takes precedence over `authProvider`. Can be a local alias (`@/lib/auth`) or a workspace package (`@repo/auth/server`).

```yaml
options:
  authImportPath: '@repo/auth/server'
```

</ParamField>

<ParamField path="projections.nextjs.options.databaseImportPath" type="string" default="@/lib/database">
  Import path for your database client, injected into generated route files.

```yaml
options:
  databaseImportPath: '@repo/database'
```

</ParamField>

<ParamField path="projections.nextjs.options.runtimeImportPath" type="string" default="@/lib/manifest-runtime">
  Import path for the Manifest runtime helper, injected into generated route files.

```yaml
options:
  runtimeImportPath: '@repo/manifest/runtime'
```

</ParamField>

<ParamField path="projections.nextjs.options.responseImportPath" type="string" default="@/lib/manifest-response">
  Import path for the Manifest response helper, injected into generated route files.

```yaml
options:
  responseImportPath: '@repo/manifest/response'
```

</ParamField>

<ParamField path="projections.nextjs.options.includeTenantFilter" type="boolean" default="false">
  When `true`, generated read routes include a `WHERE tenantId = <tenant>` filter and POST handlers resolve a `tenantId` before executing. Defaults to `false` (single-tenant). Set to `true` for multi-tenant applications.

```yaml
options:
  includeTenantFilter: true
```

</ParamField>

<ParamField path="projections.nextjs.options.includeSoftDeleteFilter" type="boolean" default="false">
  When `true`, generated read routes include a `WHERE deletedAt IS NULL` filter. Defaults to `false`. Set to `true` if your data model uses soft deletes.

```yaml
options:
  includeSoftDeleteFilter: true
```

</ParamField>

<ParamField path="projections.nextjs.options.tenantIdProperty" type="string" default="tenantId">
  Property name used for tenant scoping in generated filters.

```yaml
options:
  tenantIdProperty: tenantId
```

</ParamField>

<ParamField path="projections.nextjs.options.deletedAtProperty" type="string" default="deletedAt">
  Property name used for soft-delete filtering in generated queries.

```yaml
options:
  deletedAtProperty: deletedAt
```

</ParamField>

<ParamField path="projections.nextjs.options.appDir" type="string" default="app/api">
  App Router base directory. Every generated route's `pathHint` is relative to this. The default `app/api` suits a standard (non-monorepo) Next.js project. For a monorepo, set this to e.g. `apps/api/app/api`.

```yaml
options:
  appDir: apps/api/app/api
```

</ParamField>

<ParamField path="projections.nextjs.options.strictMode" type="boolean" default="true">
  Whether generated TypeScript is written to be strict-mode friendly. Leave on unless you have a
  specific compatibility reason to disable it.
</ParamField>

<ParamField path="projections.nextjs.options.includeComments" type="boolean" default="true">
  Whether to emit explanatory comments above generated handlers. Set `false` to slim generated
  files.
</ParamField>

<ParamField path="projections.nextjs.options.indentSize" type="integer" default="2">
  Spaces of indentation in generated code. Must be between 1 and 8.
</ParamField>

### `projections.nextjs.options.tenantProvider`

Replaces the default `userTenantMapping.findUnique` pattern with a project-supplied lookup helper. When set, generated handlers import `functionName` from `importPath` and call it with the auth field named by `lookupKey`.

<ParamField path="projections.nextjs.options.tenantProvider.importPath" type="string" required>
  Module path that exports the tenant lookup helper.
</ParamField>

<ParamField path="projections.nextjs.options.tenantProvider.functionName" type="string" required>
  Named export to call (e.g. `getTenantIdForOrg`).
</ParamField>

<ParamField path="projections.nextjs.options.tenantProvider.lookupKey" type="string" required>
  Which auth-context field is passed as the lookup argument. Accepted values: `orgId`, `userId`.
</ParamField>

```yaml
projections:
  nextjs:
    options:
      tenantProvider:
        importPath: '@my-app/data'
        functionName: 'getTenantIdForOrg'
        lookupKey: 'orgId'
```

### `projections.nextjs.options.dispatcher`

The dispatcher is the canonical Manifest write surface at `POST /api/manifest/[entity]/commands/[command]`. Two execution modes are supported, and either can be selected from config alone — no projection-source edits required.

<ParamField path="projections.nextjs.options.dispatcher.enabled" type="boolean" default="true">
  When `false`, the `nextjs.dispatcher` surface emits no artifact and the CLI surfaces an
  info-diagnostic.
</ParamField>

<ParamField path="projections.nextjs.options.dispatcher.executionMode" type="string" default="inline">
  Where command execution happens.

- `inline` (default, back-compat): handler calls `createManifestRuntime(...)` then `runtime.runCommand(...)` per request.
- `externalExecutor`: handler imports the configured executor and delegates — **no `createManifestRuntime` or `runtime.runCommand` appears in the emitted code.**
  </ParamField>

<ParamField
  path="projections.nextjs.options.dispatcher.executorImportPath"
  type="string"
  default="@/lib/manifest-executor"
>
  Module path for the external executor. Only used when `executionMode` is `externalExecutor`.
</ParamField>

<ParamField
  path="projections.nextjs.options.dispatcher.executorImportName"
  type="string"
  default="executeManifestCommand"
>
  Named export to call on the external executor module.
</ParamField>

<ParamField
  path="projections.nextjs.options.dispatcher.deriveInstanceId"
  type="boolean"
  default="true"
>
  When `true` (default), the dispatcher extracts an `instanceId` from `body.instanceId` or `body.id`
  and forwards it to `runCommand` (inline mode) or the configured executor (`externalExecutor`
  mode). Non-create commands (release, archive, update, …) need this to address the target instance;
  create commands ignore it harmlessly. Override to `false` only with strong reason.
</ParamField>

<ParamField
  path="projections.nextjs.options.dispatcher.path"
  type="string"
  default="/manifest/[entity]/commands/[command]/route.ts"
>
  Dispatcher route path relative to `appDir`. Override only for non-canonical prefixes (e.g.
  `/api/v1/manifest/...`).
</ParamField>

<ParamField path="projections.nextjs.options.unauthorizedStatus" type="integer" default="401">
  HTTP status returned when the auth helper rejects the request OR when it throws (invalid/expired
  token). Must be in the 400–499 range. Auth failures MUST NEVER surface as 500. Override only if
  you standardise on 403 to avoid user-existence leak.
</ParamField>

### `projections.nextjs.options.concreteCommandRoutes`

Policy for the deprecated per-command "concrete" routes (the `nextjs.command` surface). These are **opt-in** in current releases — the canonical dispatcher is the only write surface by default.

<ParamField
  path="projections.nextjs.options.concreteCommandRoutes.enabled"
  type="boolean"
  default="false"
>
  **Default false (opt-in).** When `false`, `nextjs.command` artifacts are suppressed entirely and
  `manifest generate --surface all` does not emit them. Opt in only if you still need per-command
  routes for legacy callers.
</ParamField>

<ParamField
  path="projections.nextjs.options.concreteCommandRoutes.legacyAliasesOnly"
  type="boolean"
  default="true"
>
  When `true` (default) and `enabled` is `true`, generated concrete routes carry the `DEPRECATED
  ALIAS` banner pointing callers at the dispatcher. Set `false` only if you intentionally treat
  per-command routes as a first-class surface.
</ParamField>

### `projections.nextjs.options.readRoutes`

Policy for direct database read routes (`nextjs.route` for list, `nextjs.detail` for single-entity GET). Direct reads bypass the runtime engine for read performance and assume a Prisma-compatible client at `databaseImportPath`.

<ParamField path="projections.nextjs.options.readRoutes.enabled" type="boolean" default="true">
  When `false`, both list and detail read routes are suppressed (info diagnostic only).
</ParamField>

<ParamField
  path="projections.nextjs.options.readRoutes.directDbReads"
  type="boolean"
  default="true"
>
  When `false`, read route handlers are emitted but contain no inline Prisma call — useful for
  projects that route reads through a separate query layer.
</ParamField>

<Note>
**Prisma 7 note.** When `includeTenantFilter` and/or `includeSoftDeleteFilter` are true, the detail route emits `findFirst` (not `findUnique`). The previous template emitted `findUnique({ where: { id, tenantId, deletedAt: null } })`, which fails type-check on Prisma 7+. This is fixed in current releases.
</Note>

### `projections.prisma`

Configuration for the Prisma schema projection (emits `schema.prisma`). All option keys are optional.

<ParamField path="projections.prisma.output" type="string" default="schema.prisma">
  Path hint for the emitted `schema.prisma` artifact.
</ParamField>

<ParamField path="projections.prisma.options" type="object">
  Per-entity overrides for the emitted schema. Keys include `provider` (emits a datasource block plus a `prisma.config.ts` companion; one of `postgresql`, `mysql`, `sqlite`, `sqlserver`, `mongodb`, `cockroachdb`), `urlEnvVar` (default `DATABASE_URL`), `tableMappings`, `columnMappings`, `precision`, `indexes`, `typeMappings`, `foreignKeys`, `dbAttributes`, `fieldAttributes`, and a projection-local `naming` (see the top-level [`naming`](#param-naming) field — explicit `tableMappings`/`columnMappings` always win).

```yaml
projections:
  prisma:
    output: prisma/schema.prisma
    options:
      provider: postgresql
      urlEnvVar: DATABASE_URL
      naming: snake_case
      tableMappings:
        OrderLine: order_lines
```

</ParamField>

### `projections.prisma-store`

Configuration for the Prisma store metadata/registry projection (`GenericPrismaStore`). It emits a model-metadata module and a store-registry module, and extends every `projections.prisma.options` key for consistent naming with `schema.prisma` emission.

<ParamField
  path="projections.prisma-store.options.metadataOutput"
  type="string"
  default="prisma-model-metadata.generated.ts"
>
  Path hint for the emitted `prisma-model-metadata.generated.ts`.
</ParamField>

<ParamField
  path="projections.prisma-store.options.registryOutput"
  type="string"
  default="prisma-store-registry.generated.ts"
>
  Path hint for the emitted `prisma-store-registry.generated.ts`.
</ParamField>

<ParamField path="projections.prisma-store.options.accessorNames" type="object">
  Per-entity Prisma client delegate override (e.g. `{ "OrderLine": "orderLines" }`).
</ParamField>

<ParamField path="projections.prisma-store.options.storeImportPath" type="string">
  Module path for the `GenericPrismaStore` import in generated output.
</ParamField>

<ParamField path="projections.prisma-store.options.metadataImportPath" type="string">
  Module path for the generated metadata import in the registry output.
</ParamField>

```yaml
projections:
  prisma-store:
    options:
      naming: snake_case
      metadataOutput: src/generated/prisma-model-metadata.generated.ts
      registryOutput: src/generated/prisma-store-registry.generated.ts
      accessorNames:
        OrderLine: orderLines
```

## externalExecutor migration

Use `executionMode: externalExecutor` when your app owns Manifest runtime construction (shared executor, custom store wiring, extra context) and wants the dispatcher to be a thin transport adapter.

**Step 1.** Author an executor in your app. Place it where `executorImportPath` points; the call shape below is the contract.

```ts
// apps/api/lib/manifest-executor.ts
import { createManifestRuntime } from './manifest-runtime';

export interface ManifestExecutorCall {
  entityName: string;
  commandName: string;
  input: unknown;
  instanceId?: string;
  context: {
    tenantId?: string;
    orgId?: string;
    actorId?: string;
    requestId?: string;
    source?: string;
    user?: Record<string, unknown>;
  };
}

export async function executeManifestCommand(call: ManifestExecutorCall) {
  const runtime = await createManifestRuntime(call.context);
  return runtime.runCommand(call.commandName, call.input, {
    entityName: call.entityName,
    ...(call.instanceId ? { instanceId: call.instanceId } : {}),
  });
}
```

**Step 2.** Flip config to point at your executor:

```yaml
projections:
  nextjs:
    options:
      dispatcher:
        executionMode: externalExecutor
        executorImportPath: '@my-app/manifest-executor'
        executorImportName: 'executeManifestCommand'
```

**Step 3.** Regenerate. The dispatcher route now imports your executor and delegates to it — no `createManifestRuntime` or `runtime.runCommand` in the emitted code.

**Step 4.** Confirm with the CLI:

```bash
manifest config inspect --json | jq '.projections.nextjs.options.dispatcher'
```

## CI / drift workflow

Downstream repos should commit `manifest.config.yaml` and pin generated output under VCS. The recommended four-step CI gate catches both Manifest default changes and local config drift:

```bash
# 1. Fail on invalid config.
manifest config validate

# 2. Snapshot the effective config (commit the snapshot under VCS).
manifest config inspect --json > .manifest/effective-config.snapshot.json
git diff --exit-code .manifest/effective-config.snapshot.json

# 3. Regenerate code from IR.
manifest build

# 4. Fail on drift between checked-in and just-generated code.
git diff --exit-code
```

This flow guarantees that the effective config has not silently changed and that emitted code matches what the current Manifest + config combination produces.

## Workspace monorepo imports

In a pnpm/yarn workspace (e.g. Turborepo or a custom monorepo), your shared packages are available under a workspace prefix like `@repo/`. Set the import path options to point at those packages instead of local `@/lib/` aliases:

```yaml
projections:
  nextjs:
    output: apps/api/app/api/
    options:
      authImportPath: '@repo/auth/server'
      databaseImportPath: '@repo/database'
      runtimeImportPath: '@repo/manifest/runtime'
      responseImportPath: '@repo/manifest/response'
```

Generated routes will then import from those workspace packages:

```typescript
import { auth } from '@repo/auth/server';
import { database } from '@repo/database';
import { createRuntime } from '@repo/manifest/runtime';
import { manifestSuccessResponse, manifestErrorResponse } from '@repo/manifest/response';
```

## Complete example

The following file shows the major sections with representative values (values shown override the built-in defaults — omit any field to keep its default):

```yaml
# manifest.config.yaml

src: modules/**/*.manifest
output: ir/
naming: snake_case

prismaSchema: prisma/schema.prisma

hooks:
  provider: husky
  runFmt: true
  runValidate: true

plugins:
  - module: '@my-org/manifest-plugin-audit'
    enabled: true

env:
  stores:
    primaryDb:
      name: DATABASE_URL
      required: true
      example: postgres://user:pass@localhost:5432/app
  auth:
    clerk:
      name: CLERK_SECRET_KEY
      required: true

projections:
  nextjs:
    output: app/api/
    options:
      authProvider: clerk
      authImportPath: '@/lib/auth'
      databaseImportPath: '@/lib/database'
      runtimeImportPath: '@/lib/manifest-runtime'
      responseImportPath: '@/lib/manifest-response'
      includeTenantFilter: true
      includeSoftDeleteFilter: true
      tenantIdProperty: tenantId
      deletedAtProperty: deletedAt
  prisma:
    output: prisma/schema.prisma
    options:
      provider: postgresql
  prisma-store:
    options:
      metadataOutput: src/generated/prisma-model-metadata.generated.ts
      registryOutput: src/generated/prisma-store-registry.generated.ts
```

And the equivalent for a workspace monorepo with all options set explicitly:

```yaml
# manifest.config.yaml (monorepo)

src: modules/**/*.manifest
output: ir/

projections:
  nextjs:
    output: apps/api/app/api/
    options:
      authProvider: clerk
      authImportPath: '@repo/auth/server'
      databaseImportPath: '@repo/database'
      runtimeImportPath: '@repo/manifest/runtime'
      responseImportPath: '@repo/manifest/response'
      includeTenantFilter: true
      includeSoftDeleteFilter: false
      tenantIdProperty: tenantId
      deletedAtProperty: deletedAt
```

<Tip>
  After editing `manifest.config.yaml`, run `manifest build` with no flags to confirm the CLI picks
  up your changes. The build summary printed at the end shows the resolved `ir-output` and
  `code-output` paths.
</Tip>

## Environment variables (what actually ships)

Those three mechanisms are **not implemented**. YAML load does not
expand `env(…)` strings, there is no connection-string `stores:` block in build YAML, and the CLI
does not merge `manifest.config.<env>.yaml` from `MANIFEST_ENV`.

**Use instead:**

1. **Preflight declarations** — the `env:` block above (metadata only) with `manifest preflight`
2. **Runtime bindings** — `process.env.DATABASE_URL` (etc.) inside `manifest.config.ts` store
   implementations ([Durable storage](#durable-storage-for-generated-runtime-companions))
3. **Multi-environment** — supply different env vars / deploy secrets per environment, or keep
   separate config files and point the process at the right one yourself (no built-in overlay
   selector)

Do not use these unsupported patterns (they are not wired):

`stores.postgres.url: env(DATABASE_URL)` · `MANIFEST_ENV=staging manifest build` ·
`manifest.config.staging.yaml` overlay merge


---

## Format files in place

URL: https://manifest-b1e8623f.mintlify.app/cli/dev-tools

---
title: 'Developer tools: formatter, watch mode, VS Code, and LSP'
description: 'Keep Manifest source consistent with manifest fmt, rebuild on save with manifest watch, and get rich editing support through the VS Code extension and Language Server Protocol.'
sidebarTitle: 'Dev Tools'
---

Manifest ships a set of developer tools that keep source files consistent, rebuild generated code on every save, and provide rich editing support in your IDE.

## `manifest fmt`

`manifest fmt` normalizes `.manifest` source files using deterministic whitespace rules. It converts CRLF to LF, tabs to two spaces, strips trailing whitespace and trailing blank lines, and ensures a final newline.

```bash
# Format files in place
manifest fmt src/

# Check formatting without writing (exit 1 if any file needs changes)
manifest fmt src/ --check

# Format with a custom glob
manifest fmt --glob "modules/**/*.manifest"
```

### Options

<ParamField path="--check" type="boolean" default="false">
  CI mode. Report files that would change without writing them. Exits non-zero when any file needs
  formatting.
</ParamField>

<ParamField path="--write" type="boolean">
  Write formatted output in place. This is the default when `--check` is not set.
</ParamField>

<ParamField path="-g, --glob" type="string">
  Custom glob pattern when `source` is a directory.
</ParamField>

### What the formatter does

The formatter applies the following transformations deterministically:

- CRLF line endings are normalized to LF
- Tab characters are replaced with two spaces
- Trailing whitespace on each line is removed
- Trailing blank lines at the end of the file are removed
- The file ends with exactly one newline character
- Empty files produce a single newline

The formatter is intentionally minimal. It normalizes whitespace only. It does not reorder properties, normalize brace style, or restructure code.

### Idempotency

Running `manifest fmt` on an already-formatted file produces no diff. This makes it safe to run in a pre-commit hook or CI pipeline without introducing spurious changes.

### CI integration

Use `--check` in CI to enforce formatting standards without modifying files:

```bash
# In your CI pipeline
manifest fmt src/ --check
```

The command exits non-zero when any file would change, giving you an actionable signal.

## `manifest watch`

`manifest watch` monitors `.manifest` files for changes and triggers an incremental compile-and-generate cycle on every save. It uses Node.js `fs.watch` with recursive mode for efficient file system monitoring (with `watchFile` polling fallback for older platforms).

```bash
# Watch a directory and rebuild on change
manifest watch src/

# Watch with JSON change events for downstream tooling
manifest watch src/ --events

# Watch with a custom debounce
manifest watch src/ --debounce 500
```

### Options

`watch` accepts all the same flags as [`manifest build`](/cli/commands) (`-p`, `-s`, `--ir-output`, `--code-output`, `-g`, and the import-path flags) plus:

<ParamField path="--debounce" type="number" default="300">
  Debounce delay in milliseconds. Rapid file changes within this window are coalesced into a single
  rebuild.
</ParamField>

<ParamField path="--events" type="boolean" default="false">
  Emit structured JSON change events to stdout. Useful for piping into other build tools.
</ParamField>

<ParamField path="--clear" type="boolean" default="false">
  Clear the terminal on each rebuild.
</ParamField>

### Structured events

When `--events` is set, `manifest watch` prints one JSON object per line to stdout:

```json
{"type":"ready","timestamp":1717312000000,"files":12,"irOutput":"ir/","codeOutput":"generated/"}
{"type":"change","timestamp":1717312010000,"file":"src/orders.manifest"}
{"type":"build:start","timestamp":1717312010100}
{"type":"build:success","timestamp":1717312010800,"filesCompiled":1,"filesGenerated":3}
{"type":"build:error","timestamp":1717312010800,"error":"Parse error at line 42: unexpected token"}
```

These events are machine-readable, making it straightforward to pipe the output into other tools or build orchestrators.

### Graceful shutdown

The watcher traps `SIGINT` and `SIGTERM` and shuts down cleanly. Build errors do not terminate the watcher; the process continues monitoring and will rebuild on the next change.

## VS Code extension

The Manifest VS Code extension provides syntax highlighting, autocomplete, hover documentation, go-to-definition, and real-time diagnostics. It is available as a bundled extension with an embedded LSP server -- no separate install needed.

Extension id `manifest-lang` (in-repo `0.3.0`) and
`@manifest/lsp-server@0.1.0` are **built in-repo / not published** to npm or the VS Code
Marketplace (see `docs/TODO.md`). Use the VSIX build path below, or the published
`manifest-lsp` bin on `@angriff36/manifest@3.6.41` for editor stdio wiring.

### Installation

Build and install the extension from source:

```bash
# Build the root library first
pnpm run build:lib

# Build and package the extension
cd packages/vscode-extension
pnpm run build
pnpm run package
```

This produces a `.vsix` file you can install via the VS Code Extensions view ("Install from VSIX...") or via the command line:

```bash
code --install-extension manifest-lang-*.vsix
```

For development, use the Extension Development Host:

```bash
code --extensionDevelopmentPath=packages/vscode-extension
```

### Features

**Syntax highlighting.** A TextMate grammar covers all Manifest keywords: entity declarations, property modifiers, control flow constructs, type names, operators, constants, relationship keywords, access control terms, store targets, and severity levels.

**Code snippets.** Fifteen snippets for common patterns: `entity`, `property`, `command`, `policy`, `constraint`, `computed`, `hasMany`, `belongsTo`, `store`, `event`, `module`, `enum`, `guard`, `mutate`, and `emit`.

**Language configuration.** Comment toggling (`//` and `/* */`), bracket matching, auto-closing pairs, and indentation rules.

**Diagnostics.** Real-time error and warning squiggles from the Manifest compiler, debounced at 200ms to avoid noisy feedback during typing. Positions are converted from the Manifest compiler's 1-based coordinates to the LSP protocol's 0-based coordinates.

**Document symbols.** The Outline view shows entities (with nested properties, commands, relationships, policies, and constraints), modules, enums, stores, and events.

**Hover.** Formatted markdown information for entities (property and command counts), properties (type and modifiers), commands (parameters and guard count), computed properties, policies, constraints, enums, and keywords.

**Autocomplete.** All keywords, type names, entity and enum names from the parsed AST, and context variables (`self`, `user`, `context`).

**Go-to-definition.** Intra-file resolution for entity, enum, and module references.

## Language Server Protocol

The Manifest LSP server provides IDE-agnostic language support. It lives in `packages/lsp-server/` and reuses the existing Manifest compiler pipeline (Lexer, Parser, IR Compiler) to produce the artifacts that each LSP feature consumes.

### Capabilities

| Feature              | Description                                                                       |
| -------------------- | --------------------------------------------------------------------------------- |
| **Diagnostics**      | Compilation errors and warnings with 200ms publish debounce                       |
| **Completion**       | Context-aware keyword, type, and symbol completion using brace-depth tracking     |
| **Hover**            | Markdown-formatted documentation for entities, properties, commands, and keywords |
| **Go-to-definition** | Jump to entity, enum, and module declarations                                     |
| **Document symbols** | Hierarchical outline of all declarations in a file                                |

### Architecture

The LSP server consists of these modules:

- **compiler-bridge.ts** -- Orchestrates the full compilation pipeline and returns tokens, AST, and IR for feature consumption
- **position-utils.ts** -- Handles the Manifest lexer's end-position convention (token position is one column past the last character) with conversion utilities between 1-based and 0-based coordinates
- **document-store.ts** -- Manages open document state and invalidation
- **Features** -- Each feature (diagnostics, completion, hover, definition, document-symbols) is a set of pure functions that take compiled artifacts and return LSP protocol objects

The server communicates over stdio and is launched by the editor client. Each feature is stateless: no shared mutable state exists between requests.

### Connecting a custom editor

Prefer the published bin from the main package:
`npx --package @angriff36/manifest manifest-lsp --stdio` (or the path under
`node_modules/@angriff36/manifest/...`). The workspace package `@manifest/lsp-server` is not
on npm.

```json
{
  "languageserver": {
    "manifest": {
      "command": "npx",
      "args": ["--package", "@angriff36/manifest", "manifest-lsp", "--stdio"],
      "filetypes": ["manifest"],
      "rootPatterns": ["manifest.config.yaml", "manifest.config.yml"]
    }
  }
}
```
<Tip>
  The LSP server bundles the Manifest compiler, so no separate compile step is needed. It reads
  `.manifest` files directly from disk and compiles them on every document change.
</Tip>

## More developer commands

Concise reference for the remaining inspection, debugging, and authoring commands. Run any with `--help` for the full flag list.

### `manifest check [source]`

Compile `.manifest` source and validate the generated IR in one step — a fast "does this compile and conform?" gate. Flags mirror `compile` plus `validate`: `-o, --output`, `-g, --glob`, `-d, --diagnostics`, `--pretty`, `--schema <path>`, `--strict` (fail on warnings).

```bash
manifest check modules/recipe.manifest --strict
```

### `manifest preflight`

Validate environment variables against the `env` mapping in `manifest.config.yaml`, or generate a `.env.example`. Consumes the structured `env` block documented in [Configuration](/cli/configuration#param-env).

<ParamField path="-f, --format" type="string" default="text">
  Output format: `text` or `json`.
</ParamField>
<ParamField path="--generate-example" type="boolean">
  Generate `.env.example` instead of checking the environment.
</ParamField>
<ParamField path="-o, --output" type="string" default=".env.example">
  Output path for the generated `.env.example`.
</ParamField>

```bash
manifest preflight
manifest preflight --generate-example
```

### `manifest install-hooks`

Install a Git pre-commit hook that runs `manifest fmt` / `manifest validate` (driven by the [`hooks`](/cli/configuration#param-hooks) config block).

<ParamField path="-f, --force" type="boolean">
  Overwrite an existing hook configuration.
</ParamField>
<ParamField path="--provider" type="string" default="husky">
  Hook provider: `husky` or `simple-git-hooks`.
</ParamField>

```bash
manifest install-hooks --provider husky
```

### `manifest generate-tests [source]` (alias `gen-tests`)

LLM-driven conformance fixture generation: analyzes existing fixtures and emits new `.manifest` sources plus expected IR covering edge cases. Generated fixtures are compiled and validated before being written.

Key flags: `--feature <description>`, `--category <edge-cases|boundary|adversarial|coverage>` (default `edge-cases`), `--count <n>` (default `3`), `--output <path>`, `--api-key <key>` (or `ANTHROPIC_API_KEY`), `--model <model>`, `--temperature <n>`, `--max-retries <n>`, `--dry-run`, `--verbose`.

```bash
manifest generate-tests src/ --category boundary --count 5 --dry-run
```

### `manifest mock <source>`

Start a local HTTP mock server from compiled IR (or a `.manifest` file), backed by the runtime engine with in-memory stores — lets frontend teams develop against a realistic API before the backend ships.

<ParamField path="-p, --port" type="string" default="4000">
  Port number.
</ParamField>
<ParamField path="--host" type="string" default="127.0.0.1">
  Bind host.
</ParamField>
<ParamField path="--cors" type="boolean" default="false">
  Enable CORS headers.
</ParamField>
<ParamField path="--scenario" type="string" default="default">
  Hint mode: `default`, `guard-fail`, or `constraint-fail`.
</ParamField>

```bash
manifest mock ir/app.ir.json --port 4000 --cors
```

### `manifest repl [source]`

Interactive runtime REPL for inspecting IR and exercising guards, policies, and entity state against the runtime.

<ParamField path="--json" type="boolean">
  Machine-readable JSON response mode.
</ParamField>
<ParamField path="--user" type="string">
  User id for runtime context.
</ParamField>
<ParamField path="--tenant" type="string">
  Tenant id for runtime context.
</ParamField>
<ParamField path="--context" type="string">
  Additional runtime context as JSON.
</ParamField>

```bash
manifest repl src/app.manifest --user u_123 --tenant t_1
```

### `manifest harness <manifest>`

Run a fixture-generator-style harness script against compiled IR and report failed steps/assertions.

<ParamField path="-s, --script" type="string" required>
  Path to the harness script JSON.
</ParamField>
<ParamField path="-f, --format" type="string" default="text">
  Output format: `text` or `json`.
</ParamField>

```bash
manifest harness src/app.manifest --script harness.json
```

### `manifest load-test [source]`

Generate k6 or Artillery load-test scripts from IR entities and commands, with realistic data, ramp-up profiles, and SLO thresholds.

Key flags: `-o, --output <path>` (default `load-tests`), `-f, --format <k6|artillery>` (default `k6`), `--base-url <url>`, `--ramp-up <stages>`, `--slo <thresholds>`, `--command <name...>`, `--entity <name...>`, `--profile`, `--timeout <ms>` (default `30000`), `--json`.

```bash
manifest load-test src/app.manifest --format k6 --base-url http://localhost:3000
```

### `manifest routes`

Compile all `.manifest` files and emit the canonical route manifest (the agent-accessible equivalent of the DevTools Route Surface tab).

<ParamField path="-s, --src" type="string">
  Source glob for `.manifest` files.
</ParamField>
<ParamField path="-f, --format" type="string" default="json">
  Output format: `json` or `summary`.
</ParamField>
<ParamField path="-b, --base-path" type="string" default="/api">
  Base path prefix for routes.
</ParamField>

```bash
manifest routes --format summary
```

### `manifest lint-routes`

Scan client directories for hardcoded route strings and fail when violations are found — the enforcement layer for canonical routes.

<ParamField path="-f, --format" type="string" default="text">
  Output format: `text` or `json`.
</ParamField>
<ParamField path="-c, --config" type="string">
  Config file path.
</ParamField>

```bash
manifest lint-routes
```

### `manifest runtime-check <entity> <command>`

Correlate route surface, source manifests, and precompiled IR for a single command to spot drift.

<ParamField path="--route" type="string">
  Optional canonical route path to validate (exact match).
</ParamField>
<ParamField path="--json" type="boolean">
  JSON output.
</ParamField>
<ParamField path="--src" type="string">
  Source manifest glob pattern.
</ParamField>
<ParamField path="--ir-root" type="path...">
  Compiled IR root directory/directories.
</ParamField>

```bash
manifest runtime-check Recipe publish --route /api/recipe/commands/publish
```

### `manifest inspect entity <entityName>`

Inspect a single entity across source manifests and precompiled IR.

<ParamField path="--json" type="boolean">
  JSON output.
</ParamField>
<ParamField path="--src" type="string">
  Source manifest glob pattern.
</ParamField>
<ParamField path="--ir-root" type="path...">
  Compiled IR root directory/directories.
</ParamField>

```bash
manifest inspect entity Recipe
```

### `manifest duplicates`

Summarize duplicate merge reports (`*.merge-report.json`) produced by merge compilation.

<ParamField path="--entity" type="string">
  Filter duplicate entries by entity name/key.
</ParamField>
<ParamField path="--merge-report" type="string">
  Override the merge-report glob pattern.
</ParamField>
<ParamField path="--json" type="boolean">
  JSON output.
</ParamField>

```bash
manifest duplicates --entity Recipe
```

### `manifest cache-status`

Show offline cache guidance — precompiled IR timestamps and restart advice.

<ParamField path="--entity" type="string">
  Optional entity context for guidance text.
</ParamField>
<ParamField path="--command" type="string">
  Optional command context for guidance text.
</ParamField>
<ParamField path="--json" type="boolean">
  JSON output.
</ParamField>
<ParamField path="--ir-root" type="path...">
  Compiled IR root directory/directories.
</ParamField>

```bash
manifest cache-status
```

### `manifest plugins list`

List plugins declared in `manifest.config` (see the [`plugins`](/cli/configuration#param-plugins) config block). Pass `--json` for machine-readable output.

```bash
manifest plugins list
```

## `manifest versions`

IR snapshot versioning with semantic tags, changelog, and integrity verification. Snapshots live under `.manifest-versions/` (override with `--store <path>` on every subcommand).

| Subcommand                       | Purpose                                          | Key flags                                                |
| -------------------------------- | ------------------------------------------------ | -------------------------------------------------------- |
| `versions list`                  | List saved IR versions                           | `--store`, `--json`                                      |
| `versions show <version>`        | Show version metadata (number, tag, or `latest`) | `--store`, `--json`                                      |
| `versions save [source]`         | Compile and save a new IR snapshot               | `--tag <tag>`, `--auto-tag`, `--label <text>`, `--store` |
| `versions diff <from> <to>`      | Compare two saved versions                       | `--breaking`, `--sql`, `--json`, `--store`               |
| `versions changelog [from] [to]` | Generate a changelog between two versions        | `--store`, `--json`                                      |
| `versions tag <version> <tag>`   | Apply a semantic version tag to a saved version  | `--store`                                                |
| `versions rollback <version>`    | Output a previous IR snapshot                    | `-o, --output <path>`, `--store`                         |
| `versions verify [version]`      | Verify IR integrity via SHA-256 comparison       | `--all`, `--json`, `--store`                             |

```bash
# Save a tagged snapshot, then verify integrity
manifest versions save src/app.manifest --tag 1.0.0
manifest versions verify latest

# Diff two versions with breaking-change analysis
manifest versions diff 1.0.0 latest --breaking
```

## Typical workflow

A common development workflow combines all four tools:

1. **Format** your source before committing:

   ```bash
   manifest fmt src/ --check
   ```

2. **Watch** for changes during development:

   ```bash
   manifest watch src/ --events
   ```

3. **Edit** in VS Code with real-time diagnostics, completion, and hover.

4. **Commit** with confidence that formatting and generated code are in sync.

```bash
# Full workflow in a Makefile or package.json script
fmt: manifest fmt src/ --write
dev: manifest watch src/ --events
ci: manifest fmt src/ --check && manifest build
```


---

## Run all five detectors against the current directory

URL: https://manifest-b1e8623f.mintlify.app/cli/governance

---
title: 'Governance & audit commands'
sidebarTitle: 'Governance & audit'
description: "Reference for enforce-surface, integration-check, audit-governance, audit-bypasses, scan, doctor, and emit — Manifest's static enforcement layer for downstream apps."
---

The governance commands are the static enforcement layer that sits beside the runtime. They check that an application codebase only writes through registered Manifest commands, that emitted routes match the canonical dispatcher, that bypasses are registered, and that the package integrates correctly end-to-end. None of these commands execute a Manifest program — they read the registries, the IR, and your source tree.

<Note>
  These commands are available in current releases. `enforce-surface` is the strictest
  registry-vs-app check; `integration-check` is the recommended pre-deploy umbrella for
  downstream consumers.
</Note>

---

## `manifest enforce-surface`

Detect duplicate or bypass write paths in application code against a registry of canonical Manifest commands. Fails the build when an agent or contributor has invented a direct database write that should have gone through a registered command.

```bash
manifest enforce-surface --root <path> --commands-registry <path> [options]
```

### Required options

<ParamField path="--root" type="string" required>
  Repository or application root to scan.
</ParamField>

<ParamField path="--commands-registry" type="string" required>
  Path to `commands.json` emitted from Manifest IR (see `manifest emit registries`).
</ParamField>

### Optional flags

<ParamField path="--entities-registry" type="string">
  Path to `entities.json` emitted from Manifest IR.
</ParamField>

<ParamField path="--bypass-registry" type="string">
  Path to `bypasses.json` listing approved exceptions.
</ParamField>

<ParamField path="--write-receiver <name>" type="string" default="prisma">
  ORM client identifier that direct-write detectors match on (e.g., `prisma`, `kysely`, `drizzle`). Default is `prisma`.
</ParamField>

<ParamField path="--include" type="glob...">
  Additional include globs (repeatable).
</ParamField>

<ParamField path="--exclude" type="glob...">
  Exclude globs — generated files, build output, fixtures, etc.
</ParamField>

<ParamField path="--strict" type="boolean" default="false">
  Exit non-zero on any error finding.
</ParamField>

<ParamField path="-f, --format" type="string" default="text">
  Output format: `text` or `json`.
</ParamField>

### Example

```bash
manifest enforce-surface \
  --root apps/web \
  --commands-registry manifest-registry/commands.json \
  --bypass-registry manifest-registry/bypasses.json \
  --exclude "**/*.test.ts" "**/generated/**" \
  --strict
```

<Tip>
  `enforce-surface` honors `--include`/`--exclude` correctly and scans `.js` and
  `.jsx` in addition to TypeScript.
</Tip>

---

## `manifest integration-check`

End-to-end validation that a downstream repo is correctly integrated with the Manifest governance contract. Runs governance audit + bypass audit + dispatcher presence + runtime smoke (audit and outbox adapters) + package shape in one command. Exit code is `0` only if every section passes.

```bash
manifest integration-check [options]
```

### Options

<ParamField path="--root" type="string">
  Downstream repo root (defaults to cwd).
</ParamField>

<ParamField path="--commands-registry" type="string">
  Path to a commands registry JSON.
</ParamField>

<ParamField path="--bypass-registry" type="string">
  Path to a bypass registry JSON.
</ParamField>

<ParamField path="--format" type="string" default="text">
  Output format: `text` or `json`.
</ParamField>

<ParamField path="--strict" type="boolean" default="false">
  Treat warnings as failures.
</ParamField>

<ParamField path="--skip-runtime-smoke" type="boolean" default="false">
  Skip the in-memory `RuntimeEngine` smoke test.
</ParamField>

<ParamField path="--skip-package-shape" type="boolean" default="false">
  Skip the package-shape check.
</ParamField>

<ParamField path="--skip-tarball" type="boolean" default="false">
  Skip the `npm pack --dry-run` sub-step in package-shape.
</ParamField>

<ParamField path="--package-root" type="string">
  Override the `@angriff36/manifest` package root for the package-shape check.
</ParamField>

### Example

```bash
manifest integration-check \
  --root . \
  --commands-registry manifest-registry/commands.json \
  --bypass-registry manifest-registry/bypasses.json \
  --strict
```

---

## `manifest audit-governance`

Run all five governance detectors in one pass and aggregate findings. This is the umbrella for the individual detectors.

```bash
manifest audit-governance [options]
```

### Detectors

| Detector            | Catches                                                          |
| ------------------- | ---------------------------------------------------------------- |
| `direct-writes`     | Direct database writes that bypass `RuntimeEngine.runCommand`    |
| `event-fabrication` | Manually constructed event payloads that should come from `emit` |
| `route-drift`       | Routes that don't match the canonical dispatcher pattern         |
| `missing-tests`     | Registered commands that have no test coverage                   |
| `bypass-violations` | Bypass entries that no longer match their referenced files       |

### Options

<ParamField path="-r, --root" type="string" default=".">
  Root directory to audit.
</ParamField>

<ParamField path="--only" type="string">
  Comma-separated detector names to run (default: all five).
</ParamField>

<ParamField path="--commands-registry" type="string">
  Path to `commands.json` (enables the `missing-tests` detector).
</ParamField>

<ParamField path="--bypass-registry" type="string">
  Path to `bypasses.json` (enables the `bypass-violations` detector).
</ParamField>

<ParamField path="--write-receiver <name>" type="string" default="prisma">
  ORM client identifier that direct-write detectors match on (e.g., `prisma`, `kysely`, `drizzle`). Default is `prisma`.
</ParamField>

<ParamField path="--strict" type="boolean" default="false">
  Exit non-zero on any error finding.
</ParamField>

<ParamField path="-f, --format" type="string" default="text">
  Output format: `text` or `json`.
</ParamField>

### Example

```bash
# Run all five detectors against the current directory
manifest audit-governance --strict

# Run only the route-drift detector
manifest audit-governance --only route-drift

# Run with both registries enabled
manifest audit-governance \
  --commands-registry manifest-registry/commands.json \
  --bypass-registry manifest-registry/bypasses.json \
  --strict
```

<Note>
  `manifest audit-constitution` is a deprecated alias for `audit-governance`. It still works but
  emits a deprecation warning. Switch to `audit-governance` in new scripts.
</Note>

---

## `manifest audit-bypasses`

Validate an approved-bypass registry against the canonical schema (`docs/spec/registry/bypasses.schema.json`). Reports missing-file references as errors and expired review dates as warnings (or errors under `--strict-expiry`).

```bash
manifest audit-bypasses --registry <path> [options]
```

### Options

<ParamField path="--registry" type="string">
  Path to bypass registry JSON file.
</ParamField>

<ParamField path="-r, --root" type="string" default=".">
  Root directory used to resolve bypass paths.
</ParamField>

<ParamField path="--strict-expiry" type="boolean" default="false">
  Treat expired `reviewBy` dates as errors instead of warnings.
</ParamField>

<ParamField path="-f, --format" type="string" default="text">
  Output format: `text` or `json`.
</ParamField>

### Example

```bash
manifest audit-bypasses --registry manifest-registry/bypasses.json --strict-expiry
```

---

## `manifest scan [source]`

Scan `.manifest` source files for configuration issues _before_ runtime. The primary goal: "If scan passes, the code works."

Checks include:

- **Policy coverage** — every command has at least one policy
- **Store consistency** — all `store` targets are recognized
- **Route context** — generated routes pass required user context
- **Property alignment** — Manifest properties match Prisma schema (when configured)

```bash
manifest scan [source] [options]
```

### Options

<ParamField path="-g, --glob" type="string" default="**/*.manifest">
  Glob pattern for manifest files.
</ParamField>

<ParamField path="-f, --format" type="string" default="text">
  Output format: `text` or `json`.
</ParamField>

<ParamField path="--strict" type="boolean" default="false">
  Fail on warnings.
</ParamField>

### Example

```bash
# Scan all .manifest files under modules/
manifest scan modules/ --strict
```

---

## `manifest doctor`

Run ranked offline diagnostics for source / IR / route drift and duplicate merges. Useful as a single "what's wrong?" command.

```bash
manifest doctor [options]
```

### Options

<ParamField path="--entity" type="string">
  Optional entity to focus the diagnosis on.
</ParamField>

<ParamField path="--command" type="string">
  Optional command to focus the diagnosis on.
</ParamField>

<ParamField path="--route" type="string">
  Optional route path for route-surface correlation.
</ParamField>

<ParamField path="--json" type="boolean" default="false">
  Output JSON instead of text.
</ParamField>

<ParamField path="--src" type="string">
  Source manifest glob pattern.
</ParamField>

<ParamField path="--ir-root" type="path...">
  Compiled IR root directory or directories.
</ParamField>

### Example

```bash
manifest doctor --entity Recipe --command publish
```

---

## `manifest audit-routes`

Audit generated or hand-written routes for Manifest boundary compliance:

- Writes should execute via `runtime.runCommand`
- Direct reads should include the expected tenant, location, and soft-delete filters

```bash
manifest audit-routes [options]
```

### Options

<ParamField path="-r, --root" type="string" default=".">
  Root directory to audit.
</ParamField>

<ParamField path="-f, --format" type="string" default="text">
  Output format: `text` or `json`.
</ParamField>

<ParamField path="--strict" type="boolean" default="false">
  Fail on warnings and enforce ownership rules as errors.
</ParamField>

<ParamField path="--tenant-field" type="string" default="tenantId">
  Tenant scope field name.
</ParamField>

<ParamField path="--deleted-field" type="string" default="deletedAt">
  Soft-delete field name.
</ParamField>

<ParamField path="--location-field" type="string" default="locationId">
  Location scope field name.
</ParamField>

<ParamField path="--commands-manifest" type="string">
  Path to commands manifest JSON (enables ownership rules).
</ParamField>

<ParamField path="--exemptions" type="string">
  Path to exemptions registry JSON.
</ParamField>

---

## `manifest emit registries`

Emit machine-readable `commands.json` and `entities.json` registries from a compiled IR. These registries are inputs to `enforce-surface`, `audit-governance`, and `integration-check`.

```bash
manifest emit registries [options]
```

### Options

<ParamField path="--ir" type="string">
  Path to a compiled IR JSON file.
</ParamField>

<ParamField path="--source" type="string">
  Path to a `.manifest` source file to compile and emit from.
</ParamField>

<ParamField path="--out" type="string" default="manifest-registry">
  Output directory for the emitted registries.
</ParamField>

<ParamField path="--no-validate" type="boolean">
  Skip JSON-schema validation of the emitted output.
</ParamField>

<ParamField path="--no-pretty" type="boolean">
  Emit compact JSON (no indentation).
</ParamField>

### Example

```bash
# Emit registries from a compiled IR
manifest emit registries --ir ir/recipe.ir.json --out manifest-registry/

# Or emit straight from source
manifest emit registries --source modules/recipe.manifest --out manifest-registry/
```

---

## Recommended CI pipeline

A typical pre-deploy check for a downstream consumer:

```bash
# 1. Compile source to IR
manifest build modules/**/*.manifest --code-output app/api/

# 2. Emit registries used by the audit suite
manifest emit registries --ir ir/ --out manifest-registry/

# 3. Run the umbrella check
manifest integration-check \
  --commands-registry manifest-registry/commands.json \
  --bypass-registry manifest-registry/bypasses.json \
  --strict
```

If `integration-check` exits 0, your app is on contract.


---

## One-time setup — asks a few questions and writes manifest.config.yaml

URL: https://manifest-b1e8623f.mintlify.app/cli/overview

---
title: 'Manifest CLI overview and installation'
sidebarTitle: 'Overview'
description: 'The Manifest CLI compiles .manifest files to IR, generates Next.js routes and TypeScript types, and validates IR against the schema. Install via npm.'
---

The `manifest` CLI compiles `.manifest` source files to Intermediate Representation (IR), generates framework-specific code from that IR, and validates the output against the IR schema. You can run each stage independently or chain them together with a single `build` command. A config file lets you store project-level defaults so you rarely need to pass flags by hand.

Package pin: **`@angriff36/manifest@3.6.41`** (`engines.node` ≥ 20).

## Installation

Install the CLI as a development dependency in your project:

```bash
npm install -D @angriff36/manifest
```

If you are working inside the Manifest monorepo, you can run commands through the workspace script instead:

```bash
npm run manifest --help
```

## Two build workflows

### Workflow 1 — interactive setup, then build

Run `manifest init` once to create a `manifest.config.yaml` that captures your project's paths and options. After that, every subsequent build uses those defaults:

```bash
# One-time setup — asks a few questions and writes manifest.config.yaml
manifest init

# Every build after that is just:
manifest build
```

This is the recommended approach for any project you will build repeatedly.

### Workflow 2 — direct commands

Pass all options inline. Useful for one-off runs, CI pipelines, or when you want full control without a config file:

```bash
# Compile a single source file to IR
manifest compile modules/recipe.manifest --output ir/

# Generate Next.js routes from that IR
manifest generate ir/recipe.ir.json --projection nextjs --output app/api/

# Or do both steps at once
manifest build modules/recipe.manifest --projection nextjs --code-output app/api/
```

## Command quick reference

### Build pipeline

| Command                     | Description                                           |
| --------------------------- | ----------------------------------------------------- |
| `manifest init`             | Interactive setup that creates `manifest.config.yaml` |
| `manifest compile [source]` | Compile `.manifest` source files to IR JSON           |
| `manifest generate <ir>`    | Generate code from IR using a projection              |
| `manifest build [source]`   | Compile and generate in one step                      |
| `manifest validate [ir]`    | Validate IR JSON against the IR schema                |

### Governance & audit

| Command                      | Description                                                                                     |
| ---------------------------- | ----------------------------------------------------------------------------------------------- |
| `manifest enforce-surface`   | Detect duplicate/bypass write paths against the canonical dispatcher                            |
| `manifest integration-check` | Umbrella validator for downstream consumers (package shape, dispatcher, surface, runtime smoke) |
| `manifest audit-governance`  | Run the five governance detectors in one pass                                                   |
| `manifest audit-bypasses`    | Validate the bypass registry schema and that referenced paths exist                             |
| `manifest audit-routes`      | Check that emitted routes line up with the canonical dispatcher                                 |
| `manifest scan`              | Scan the codebase for policy coverage, store consistency, route context, and property alignment |
| `manifest doctor`            | Run ranked diagnostics for source/IR/route drift                                                |
| `manifest emit registries`   | Emit `commands.json` and `entities.json` from compiled IR                                       |

See [Governance & audit commands](/cli/governance) for full flag reference.

## Config file priority

When a command resolves an option it checks three sources in order:

1. **CLI flags** — values you pass directly on the command line (highest priority)
2. **`manifest.config.yaml`** — project-level defaults loaded from the project root
3. **Built-in defaults** — fallback values baked into the CLI (lowest priority)

This means you can override any config value for a single run without editing the file. For example:

```bash
# Normally uses config's code-output path; override just for this run
manifest build --code-output apps/staging/app/api/
```

<Tip>
  The CLI also accepts `manifest.config.yml`, `.manifestrc.yaml`, and `.manifestrc.yml` as config
  file names, checked in that order after `manifest.config.yaml`.
</Tip>

## Quick-start with npx

If you don't have Manifest installed yet, you can run the CLI without installing it using npx:

```bash
# Interactive config setup in the current project
npx @angriff36/manifest init

# Overwrite an existing config
npx @angriff36/manifest init --force
```

`manifest init` interactively writes a `manifest.config.yaml` into the current directory; it does not scaffold a project or accept a template. Its only flags are `-f, --force`, `--ci <provider>` (generate a CI workflow instead of a config — see [CI/CD](/cli/ci-cd)), and `--node-versions <versions>` (CI matrix versions, used with `--ci`).


---

## testing

URL: https://manifest-b1e8623f.mintlify.app/cli/testing

---
title: 'Testing tools: harness, mock server, and load testing'
description: 'CLI testing tools for Manifest programs — IR harness scripts, an HTTP mock server backed by RuntimeEngine, seed data, and load-test script generation.'
sidebarTitle: 'Testing'
---

Manifest provides CLI testing tools beyond unit/conformance suites. Verified against
`packages/cli/src/index.ts` and `@angriff36/manifest@3.6.41`.

## IR harness scripts

There is **no** `manifest test`
command group, **no** interactive constraint harness CLI, and **no**
`@angriff36/manifest/testing` export / `ConstraintTestHarness` class. The shipped harness
runner is:

```bash
manifest harness path/to/app.manifest --script path/to/harness.json
manifest harness path/to/app.manifest -s harness.json -f json
```

`harness` runs a JSON script of steps/assertions against compiled IR and reports pass/fail
counts. Explore constraints interactively via `manifest repl` or unit tests against
`RuntimeEngine`.

## Mock HTTP server

`manifest mock` starts a local HTTP server from `.manifest` or `.ir.json`. It uses
**`RuntimeEngine` with in-memory stores** (real command execution), not a fake guard-only stub.

Flags are `-p/--port` (default **`4000`**), `--host`
(default `127.0.0.1`), `--cors`, `--scenario` (`default` \| `guard-fail` \| `constraint-fail`).
There is no `--output` / `--format` / `--seed` on `mock`.

```bash
manifest mock ir/app.ir.json --port 4000 --cors
manifest mock src/app.manifest --scenario guard-fail
```

## Seed / fixture data

There is **no** `generate-fixtures` command. Use
`manifest seed` for deterministic seed files (json / sql / supabase):

```bash
manifest seed src/app.manifest -o seeds/ --profile demo --format json
manifest seed ir/app.ir.json --count 100 --entity Order --seed 42
```

| Option | Notes |
| ------ | ----- |
| `--profile` | `dev` \| `staging` \| `demo` (default record counts) |
| `-f/--format` | `json` \| `sql` \| `supabase` |
| `--count` | Override rows per entity |
| `--entity` | Repeatable entity filter |
| `--seed` | Deterministic PRNG seed |

## Load testing scripts

use **`manifest load-test`**:

```bash
manifest load-test ir/app.ir.json -o load-tests/ -f k6
manifest load-test src/app.manifest --format artillery --base-url http://localhost:3000
```

Options include `--ramp-up`, `--slo`, `--command`, `--entity`, `--profile`, `--timeout`, `--json`.

## Coverage reporter

Command/guard/policy/constraint coverage is `manifest coverage` (export surface
`@angriff36/manifest/coverage` exposes `coverageCommand`, not a `CoverageReporter` class). See
[Snapshot testing](/extensibility/snapshot-testing).

<Note>
  Package pin: `@angriff36/manifest@3.6.41`. Prefer conformance fixtures + `pnpm test` for
  language semantics; these CLI tools are for app/integration workflows.
</Note>


---

## agent sdk

URL: https://manifest-b1e8623f.mintlify.app/extensibility/agent-sdk

---
title: 'AI Agent SDK for LLM-driven Manifest execution'
sidebarTitle: 'Agent SDK'
description: 'Generate Anthropic, OpenAI, and Vercel AI SDK tool definitions from IR, map natural-language intent to commands, and route tool calls through the Manifest runtime engine.'
---

The agent SDK (`@angriff36/manifest/agent-sdk`) wraps the runtime engine with LLM-friendly interfaces: IR introspection, tool-definition generation for Anthropic, OpenAI, and the Vercel AI SDK, keyword-based intent mapping, and a stateful `AgentRuntime` that routes tool calls to built-in introspection or IR commands. It adds no IR-shape or runtime-semantic changes. Verified against `src/manifest/agent-sdk/` and the `./agent-sdk` export on `@angriff36/manifest@3.6.41` (not a separately published npm package).

## Quick start

```ts
import { AgentRuntime } from '@angriff36/manifest/agent-sdk';
import { RuntimeEngine } from '@angriff36/manifest';

const engine = new RuntimeEngine(ir);
const agent = new AgentRuntime(engine, { builtinPrefix: 'manifest', toolNameStrategy: 'snake' });

// Hand tool definitions to the model
const tools = agent.getToolDefinitions('anthropic');

// Route a tool call the model emitted
const result = await agent.executeToolCall({
  name: 'order_placeOrder',
  arguments: { total: 100 },
});
```

Stateless helpers are also exported and can be used without an `AgentRuntime`:

```ts
import {
  toOpenAITools,
  listEntities,
  describeCommand,
  findMatchingCommands,
} from '@angriff36/manifest/agent-sdk';
```

## How it works

`AgentRuntime` wraps a `RuntimeEngine`, reads its IR via `engine.getIR()`, and precomputes the built-in tool names from the configured prefix (default `manifest`). `getToolDefinitions(format)` returns command tools plus built-in tools in the requested format (`anthropic`, `openai`, or `vercel`).

`executeToolCall(call)` routes by name: if the name matches a built-in tool it runs introspection; otherwise it checks the IR command registry (by raw name or mangled `entity_command` name) and, if found, executes through the engine. Unknown names return `{ success: false, code: 'UNKNOWN_TOOL' }`. Results are returned as `AgentToolResult` with a `success` flag, a `code` (such as `SUCCESS`, `UNKNOWN_TOOL`, `MISSING_ARGUMENT`, `ENTITY_NOT_FOUND`, `COMMAND_NOT_FOUND`), a `message`, and optional `data`.

Tool-name mangling (`mangleToolName`): the `snake` strategy lowercases the entity and joins with the command, preserving the command's original case — `Order.placeOrder` becomes `order_placeOrder`. The `dot` strategy leaves the name as `Entity.command`. `parseToolName` reverses the snake form reliably.

Tool descriptions are derived from each command: entity, module, parameters (with `?` marking optional), optional guard/policy hints, and emitted events.

The introspection module exposes `listEntities`, `describeEntity`, `listCommands`, `describeCommand`, `getEntityRelationships`, `getActionableEntities`, plus a standalone `formatExpression` (a reimplementation of the engine's expression formatter so callers can render guard/policy expressions) and `formatIRType`.

The intent mapper (`tokenize`, `findMatchingCommands`) tokenizes natural-language input by splitting on whitespace and camelCase before lowercasing, and scores candidate commands by token overlap.

JSON Schema helpers (`irTypeToJsonSchema`, `irParametersToJsonSchema`, `irValueToJson`) convert IR types and values to a Draft-07 JSON Schema subset for tool parameter schemas.

## Reference

Built-in introspection tool names (prefix is configurable, default `manifest`): `<prefix>_list_entities`, `<prefix>_describe_entity`, `<prefix>_list_commands`, `<prefix>_describe_command`, `<prefix>_execute_command`, `<prefix>_get_instances`, `<prefix>_check_constraints`.

`AgentRuntimeOptions`: `builtinPrefix?: string` (default `manifest`), `toolNameStrategy?: 'snake' | 'dot'` (default `snake`).

Key exports (from `agent-sdk/index.ts`): `AgentRuntime`; `toAnthropicTools`, `toOpenAITools`, `toVercelAITools`, `commandToAnthropicTool`, `commandToOpenAITool`, `commandToVercelTool`, `mangleToolName`, `parseToolName`, `getBuiltinToolNames`; `listEntities`, `describeEntity`, `listCommands`, `describeCommand`, `getEntityRelationships`, `getActionableEntities`, `formatExpression`, `formatIRType`; `findMatchingCommands`, `tokenize`; `irTypeToJsonSchema`, `irParametersToJsonSchema`, `irValueToJson`.

Tool formats: `AnthropicTool` (`{ name, description, input_schema }`), `OpenAITool` (`{ type: 'function', function: { name, description, parameters } }`), `VercelAITools` (record keyed by tool name of `{ description, parameters }`).

## Notes and limitations

<Note>
  The SDK is a wrapper; all execution still flows through `RuntimeEngine.runCommand`, so guards,
  policies, constraints, and tenant gating apply unchanged.
</Note>

`parseToolName` only round-trips reliably under the `snake` strategy. The intent mapper is keyword/token-based scoring, not a semantic matcher. The package re-exports `AgentToolResult` rather than the engine's `CommandResult`, so the agent surface intentionally differs from the raw runtime result type. The SDK is distributed via the `./agent-sdk` package export rather than as a separately published npm package.


---

## Schema validation of compiled IR

URL: https://manifest-b1e8623f.mintlify.app/extensibility/ai-tooling

---
title: 'AI tooling: test generator, IR validator, and natural language transpiler'
description: 'AI-powered CLI tools that generate conformance fixtures, score IR/source for agents, and transpile natural language into Manifest source.'
sidebarTitle: 'AI tooling'
---

Manifest ships LLM-backed CLI tools for generating fixtures and source from prompts, plus agent-oriented validation. Verified against `packages/cli/src/index.ts` and `@angriff36/manifest@3.6.41`.

## AI-assisted conformance test generator

`manifest generate-tests` (alias `gen-tests`) is an
**LLM** fixture generator. Flags are `--feature`, `--category`, `--count`, `--output`,
`--api-key`, `--model`, `--temperature`, `--max-retries`, `--dry-run`, `--verbose` — not
`--per-entity` / `--include-negative` / `--format`. Requires `ANTHROPIC_API_KEY`.

```bash
manifest generate-tests src/app.manifest --count 5 --category edge-cases --dry-run
manifest generate-tests --feature "computed properties" --count 3 --output src/manifest/conformance/fixtures
```

| Option | Default | Description |
| ------ | ------- | ----------- |
| `--feature <text>` | — | Focus description for generation |
| `--category <type>` | `edge-cases` | `edge-cases`, `boundary`, `adversarial`, `coverage` |
| `--count <n>` | `3` | Number of fixtures to generate |
| `--output <path>` | conformance fixtures dir | Where to write fixtures |
| `--api-key` / `--model` / `--temperature` | env / defaults | Anthropic settings |
| `--dry-run` | `false` | Preview without writing |
| `--verbose` | `false` | Iteration logs |

Generated fixtures are compiled/validated before write. They are starting points — review them.

## LLM-oriented IR / source validation

There is **no** `validate-ir`
command and **no** `@angriff36/manifest/ir-validator` export. Use:

```bash
# Schema validation of compiled IR
manifest validate ir/app.ir.json --strict

# Agent-scored diagnostics (text or json)
manifest validate-ai src/app.manifest --format json --min-score 80
```

Automatic “repair IR then continue” is not a shipped public API. Prefer re-emitting source via
`manifest generate-from-prompt` or fixing diagnostics by hand.

## Natural language to Manifest transpiler

The shipped command is the separate CLI entry
`manifest generate-from-prompt <prompt>` (not a flag on `manifest generate`). Requires
`ANTHROPIC_API_KEY`. Options: `--model`, `-o/--output`, `--max-retries`, `--temperature`,
`--api-key`, `--skip-validation`, `--verbose`. There is no `--format ir` flag.

```bash
manifest generate-from-prompt "An order entity with status tracking and manager approval over 1000" -o generated.manifest
manifest generate-from-prompt "A task tracker with assignees" --model claude-sonnet-4-20250514 --verbose
```

Always compile and validate the result:

```bash
manifest compile generated.manifest --diagnostics
manifest validate-ai generated.manifest --min-score 80
```

<Note>
  LLM output is an emitter, not a validator. Review guards, policies, and domain completeness
  before shipping. Package pin: `@angriff36/manifest@3.6.41`.
</Note>


---

## federation

URL: https://manifest-b1e8623f.mintlify.app/extensibility/federation

---
title: 'Federated multi-service runtime'
description: "Connect multiple Manifest runtimes into a federated mesh where services discover each other's entity schemas, invoke cross-service commands, and propagate identity through policy bridge headers."
sidebarTitle: 'Federation'
---

The federation layer enables multiple Manifest runtime instances -- typically running as separate microservices -- to discover each other's entity schemas, invoke commands across service boundaries, and enforce policy-based authorization end-to-end. Federation is transport-agnostic: the default implementation uses HTTP with `fetch`, but the `FederationTransport` interface supports gRPC, message queues, or any custom protocol.

## Service descriptors

Each runtime instance publishes a `ServiceDescriptor` that declares which entities and commands it exposes to the federation mesh. Descriptors are built from compiled IR by the `buildDescriptor` function.

```typescript
import { buildDescriptor } from '@angriff36/manifest/federation';
import ir from './compiled.ir.json';

// buildDescriptor(serviceId, ir, options)
const descriptor = buildDescriptor('inventory-service', ir, {
  endpoint: 'https://inventory.internal',
  // exposeEntities: ['InventoryItem'], // omit to expose all entities with commands
});
```

### ServiceDescriptor schema

| Field           | Type                 | Description                                          |
| --------------- | -------------------- | ---------------------------------------------------- |
| `serviceId`     | `string`             | Unique identifier for this service in the mesh       |
| `displayName`   | `string?`            | Human-readable name                                  |
| `endpoint`      | `string`             | Base URL for reaching this service                   |
| `schemaVersion` | `string`             | IR schema version this service speaks                |
| `entities`      | `ExposedEntity[]`    | Entities this service exposes, with their commands   |
| `health`        | `ServiceHealth?`     | Health status, populated by registry health checks   |
| `auth`          | `ServiceAuthConfig?` | Service-to-service auth scheme for the policy bridge |

The `buildDescriptor` function is pure: identical IR input always produces the same descriptor output, supporting deterministic builds and reproducible deployments.

## Service registry

The `FederationRegistry` is a central discovery layer. Services register their descriptors with the registry, which then provides command lookup, health tracking, and reachable-service filtering.

```typescript
import { FederationRegistry } from '@angriff36/manifest/federation';

const registry = new FederationRegistry();

// Register each service
registry.register(inventoryDescriptor);
registry.register(orderDescriptor);

// Look up which service handles a given command
const target = registry.findCommand('InventoryItem', 'reserve');
// => { service: ServiceDescriptor, command: { name: 'reserve', idempotent, requiredPolicies } }
```

### Health checks

When configured with `healthCheckIntervalMs`, the registry periodically probes each service's health endpoint. Services that fail their health check are marked unreachable and excluded from command lookup until they recover. Health checks use a separate timeout to avoid blocking federation calls.

## Federation client

The `FederationClient` invokes commands on remote services. It resolves the target service via the registry, constructs a `FederationRequest`, and dispatches it through the configured transport.

```typescript
import { FederationClient } from '@angriff36/manifest/federation';

// new FederationClient(registry, options?, transport?)
// The transport defaults to HttpFederationTransport (globalThis.fetch).
const client = new FederationClient(registry, {
  defaultTimeoutMs: 5000, // per-call timeout (default 30000)
  maxRetries: 2, // retry transient failures on idempotent commands (default 0)
  retryDelayMs: 1000, // delay between retries
});

const result = await client.invoke({
  serviceId: 'inventory-service',
  entity: 'InventoryItem',
  command: 'reserve',
  input: { instanceId: 'item-sku-123', quantity: 5 },
  bridge: { actorId: 'user-1', tenantId: 'tenant-1' },
});
```

### Idempotency heuristics

The client uses command-level idempotency heuristics to decide whether a failed request is safe to retry:

| Command contains only       | Idempotent?                 |
| --------------------------- | --------------------------- |
| `mutate`, `persist`         | Yes -- safe to retry        |
| `emit`, `effect`, `publish` | No -- may have side effects |

Commands with `emit`, `effect`, or `publish` actions are conservatively marked non-idempotent. Only commands whose actions are limited to `mutate` and `persist` are retried on transient failure.

### Transport interface

The default transport uses `globalThis.fetch`. Replace it for testing or alternative protocols by implementing the `FederationTransport` interface:

```typescript
interface FederationTransport {
  invoke(
    descriptor: ServiceDescriptor,
    request: FederationRequest,
    options: { timeoutMs: number; authToken?: string },
  ): Promise<FederationResponse>;
}
```

## Policy bridge

The policy bridge propagates caller identity across service boundaries so that policy expressions on the receiving service can evaluate correctly. Identity is encoded as HTTP headers with the `X-Manifest-*` prefix:

| Header              | Source                         | Reconstructed as        |
| ------------------- | ------------------------------ | ----------------------- |
| `X-Manifest-Actor`  | `context.actorId` or `user.id` | `context.actorId`       |
| `X-Manifest-Tenant` | `context.tenantId`             | `context.tenantId`      |
| `X-Manifest-Org`    | `context.orgId`                | `context.orgId`         |
| `X-Manifest-Roles`  | `user.role`                    | `user.role`             |
| `X-Request-Id`      | `context.requestId`            | `context.requestId`     |
| `X-Correlation-Id`  | Generated per invocation       | `context.correlationId` |

The sending side injects these headers into the outgoing request. The receiving side extracts them and reconstructs a `RuntimeContext` so that policy expressions like `user.role == "admin"` evaluate correctly on the downstream service.

```typescript
import {
  buildBridgeFromContext,
  buildBridgeHeaders,
  parseBridgeHeaders,
  contextFromBridgeHeaders,
} from '@angriff36/manifest/federation';

// Sending side: RuntimeContext -> bridge -> X-Manifest-* HTTP headers
const bridge = buildBridgeFromContext(runtimeContext);
const headers = buildBridgeHeaders(bridge);

// Receiving side (in your API route handler): HTTP headers -> bridge -> RuntimeContext
const reconstructed = contextFromBridgeHeaders(parseBridgeHeaders(request.headers));
```

<Warning>
  The policy bridge carries identity but not authorization decisions. Each service evaluates its own
  policies against the reconstructed context. A policy that denies access on service B will deny the
  federated call even if service A authorized it.
</Warning>

## HTTP adapter generation

The federation module includes a code generator that produces typed HTTP client classes for calling remote services. Each exposed command becomes a typed method on the generated client.

```typescript
import { generateHttpAdapter } from '@angriff36/manifest/federation';

const adapter = generateHttpAdapter(inventoryDescriptor);
const client = adapter.create('https://inventory.example.com', {
  authToken: process.env.INVENTORY_API_KEY,
  timeoutMs: 5000,
});

// client now has typed methods for each exposed command
await client.checkAvailability({ instanceId: 'sku-123', quantity: 10 });
```

The generated adapter includes:

- A `create(baseUrl, options?)` method that returns a typed client
- One async method per exposed command on the client
- Typed request and response objects derived from the IR
- Automatic `X-Manifest-*` header injection
- Configurable base URL, auth token, and timeout

## Example: Order service calling Inventory service

A common pattern is an order service that checks inventory availability before confirming an order:

```typescript
import {
  FederationRegistry,
  FederationClient,
  buildDescriptor,
} from '@angriff36/manifest/federation';
import { orderIr } from './ir/order.ir.json';
import { inventoryDescriptor } from './inventory-descriptor';

// 1. Set up the registry
const registry = new FederationRegistry();
registry.register(inventoryDescriptor);

// 2. Create the federation client
const federation = new FederationClient(registry, {
  defaultTimeoutMs: 3000,
  maxRetries: 3,
  retryDelayMs: 1000,
});

// 3. In the order service's "place" command middleware or action,
// call the inventory service to reserve stock
async function placeOrder(orderId: string, sku: string, qty: number) {
  // Check inventory availability
  const check = await federation.invoke({
    serviceId: 'inventory-service',
    entity: 'InventoryItem',
    command: 'checkAvailability',
    input: { instanceId: sku, quantity: qty },
    bridge: { actorId: 'order-service' },
  });

  if (!check.success) {
    return { success: false, error: 'INSUFFICIENT_INVENTORY' };
  }

  // Reserve stock
  const reserve = await federation.invoke({
    serviceId: 'inventory-service',
    entity: 'InventoryItem',
    command: 'reserve',
    input: { instanceId: sku, quantity: qty },
    bridge: { actorId: 'order-service' },
  });

  if (!reserve.success) {
    return { success: false, error: 'RESERVATION_FAILED' };
  }

  // Proceed with order creation on the local runtime
  return localRuntime.runCommand(
    'create',
    { sku, quantity: qty },
    { entityName: 'Order', instanceId: orderId },
  );
}
```

Local `RuntimeEngine.runCommand` is
`runCommand(commandName, input, options?)` — not entity-first. The `placeOrder` example
above uses the corrected shape.

## Importing federation

The federation module is available as a subpath export:

```typescript
// All federation types and functions
import {
  FederationRegistry,
  FederationClient,
  HttpFederationTransport,
  buildDescriptor,
  generateHttpAdapter,
  buildBridgeFromContext,
  contextFromBridgeHeaders,
} from '@angriff36/manifest/federation';
```

<Note>
  The federation module is transport-agnostic by design. The default HTTP transport works out of the
  box with any Manifest service exposed via the generated Next.js or Express routes. For
  message-queue or gRPC-based deployments, implement the `FederationTransport` interface.
</Note>


---

## Generate an ER diagram as Mermaid

URL: https://manifest-b1e8623f.mintlify.app/extensibility/ir-version-control

---
title: 'IR version control with semantic tags and integrity checks'
sidebarTitle: 'IR Version Control'
description: 'Persist IR snapshots with semantic version tags, auto-increment versions from breaking-change analysis, generate changelogs, and verify integrity with SHA-256 hashing.'
---

IR version control persists IR snapshots with semantic version tags, generates changelogs between versions, and verifies integrity via SHA-256 hashing. The pure versioning logic lives in `src/manifest/ir-version-store.ts`; filesystem I/O and the user-facing commands live in the `manifest versions` CLI command group.

## Usage

Versions are managed through the `manifest versions` subcommands against a store directory (default `.manifest-versions`):

```bash
manifest versions save src/app.manifest --auto-tag --label "initial"
manifest versions list
manifest versions show latest
manifest versions diff 1 2 --breaking --sql
manifest versions tag 2 1.1.0
manifest versions rollback 1 -o restored.ir.json
manifest versions verify --all
manifest versions changelog 1 2
```

See the [CLI command reference](/cli/commands) for the full flag set.

## How it works

The store module is pure and deterministic — no I/O — operating on IR and an index structure.

The index (`IRVersionIndex`) holds `storeVersion: 1`, the current highest version number, and an ordered list of `IRVersionMeta` (oldest first). `createVersionIndex`, `addVersionToIndex`, `removeTagFromIndex`, and `tagVersionInIndex` are immutable transforms; tagging a version removes the tag from any other version that previously held it.

`createVersionMeta` extracts metadata from an IR and its provenance: version number, optional semver tag, `irHash`, `contentHash`, `savedAt` (ISO timestamp), `compilerVersion`, `schemaVersion`, and an optional label.

Integrity verification (`verifyIRIntegrity`) recomputes the IR hash with `computeIRHash` and compares it against the stored hash, returning `{ valid, storedIrHash, computedIrHash }`.

Semantic versioning helpers parse and format `major.minor.patch` strings. `autoIncrementSemver` derives the next tag from diff and breaking-change analysis: a breaking change bumps major (resetting minor and patch), any other change bumps minor, no change bumps patch, and the absence of a previous tag yields `0.1.0`.

`resolveVersionRef` resolves a reference to a version number, accepting `latest` (or undefined → current version), a numeric string, or a semver tag.

`generateChangelog` builds a `ChangelogEntry` between two IRs by running the existing `diffIR`, `classifyBreakingChanges`, and `generateMigration` engines, capturing the from/to version numbers and tags alongside the diff, breaking, and migration reports.

## Reference

Public types: `SemanticVersion`, `IRVersionMeta`, `IRVersionIndex`, `SaveVersionOptions` (`tag?`, `autoTag?`, `label?`), `VerifyResult`, `ChangelogEntry`.

Functions: `createVersionIndex`, `addVersionToIndex`, `removeTagFromIndex`, `tagVersionInIndex`, `createVersionMeta`, `verifyIRIntegrity`, `parseSemverTag`, `formatSemver`, `autoIncrementSemver`, `resolveVersionRef`, `generateChangelog`.

CLI subcommands (under `manifest versions`, all accepting `--store <path>`): `list`, `show <version>`, `save [source] [--tag|--auto-tag|--label]`, `diff <from> <to> [--breaking|--sql]`, `changelog [from] [to]`, `tag <version> <tag>`, `rollback <version> [-o]`, `verify [version] [--all]`.

## Notes and limitations

<Note>
  The store module performs no filesystem access; persistence is handled by the CLI command layer.
</Note>

Semver tags must match `major.minor.patch` exactly — prerelease and build-metadata suffixes are not parsed. `autoIncrementSemver` resolves a major bump only from breaking changes and otherwise treats any change as a minor bump, so it does not distinguish deprecations from compatible additions in the resulting version number. Version references resolve against the in-index entries; an unknown reference returns `undefined`.

## IR compression and binary serialization

There is **no** `manifest versions compress|decompress`
and **no** `@angriff36/manifest/compression` export. Binary IR is MessagePack `.mir` via
`manifest pack` / `manifest unpack` and `@angriff36/manifest/binary-ir` (`packIR`, `unpackIR`).

```bash
manifest pack ir/app.ir.json -o app.mir
manifest unpack app.mir -o ir/app.ir.json
```

```typescript
import { packIR, unpackIR, compareSizes } from '@angriff36/manifest/binary-ir';

const buf = packIR(ir);
const restored = unpackIR(buf);
```

## IR entity relationship graph visualizer

```bash
# Generate an ER diagram as Mermaid
manifest diagram ir/app.ir.json --type er --output diagrams/
```

`manifest diagram` options are `-o/--output`,
`-t/--type` (`er` \| `state` \| `sequence` \| `all`), `-e/--entity`, `--markdown`. There is
**no** `--format dot`.

| Diagram type | Content |
| ------------ | ------- |
| `er` | Entity boxes with properties, relationships as labeled edges |
| `state` | State transition diagrams for entities with state fields |
| `sequence` | Command execution sequences showing guard → action → emit flow |

## Automated changelog generation from IR diffs

```bash
# Between saved IR versions
manifest versions changelog 1 2

# Between Git refs (compiles source)
manifest changelog v3.5.0 HEAD -o CHANGELOG.md
```

Top-level `manifest changelog` takes
`<from-ref> [to-ref]` (Git refs), not `--old-ir` / `--new-ir`. IR-file diffs use
`manifest diff ir-vs-ir` / `manifest versions diff`.

The changelog path uses IR structural comparison (diff + breaking-change classification +
migration helpers), not Git commit subjects alone.


---

## mcp server

URL: https://manifest-b1e8623f.mintlify.app/extensibility/mcp-server

---
title: 'Manifest MCP server for AI hosts'
sidebarTitle: 'MCP Server'
description: 'Expose Manifest compilation, execution, validation, and introspection as Model Context Protocol tools and resources over stdio for Claude Desktop, Cursor, and other MCP hosts.'
---

The Manifest MCP server exposes compilation, execution, validation, and introspection as Model Context Protocol tools and resources over stdio, letting MCP-compatible hosts (Claude Desktop, Cursor, and others) consume and reason about Manifest programs as structured context. Verified against `packages/mcp-server/`.

## Configuration

`@manifest/mcp-server` is an **in-repo** package
(`packages/mcp-server`, version `0.1.0`) and is **not published** to npm (404 verified; see
`docs/TODO.md`). The published path is the `manifest-mcp` bin on `@angriff36/manifest` (current
pin **3.6.41**), also re-exported as `@angriff36/manifest/mcp-server`.

```json
{
  "mcpServers": {
    "manifest": {
      "command": "npx",
      "args": ["--package", "@angriff36/manifest", "manifest-mcp"]
    }
  }
}
```

The bin (`packages/mcp-server/bin/manifest-mcp.js`) imports `startServer` from the built output and starts it on a `StdioServerTransport`. The server is created with name `manifest-mcp-server`.

## What it exposes

`startServer()` constructs an `McpServer`, registers the tools and resources, and connects a stdio transport. Registration is orchestrated in `src/server.ts`.

Four tools are registered:

- **compile** — compiles `.manifest` source to IR via `compileToIR` (with `useCache: false`), caches the IR in the session store keyed by the IR's provenance `contentHash`, and returns `{ contentHash, ir, diagnostics, summary }`, where `summary` includes entity/command/policy counts and a `hasErrors` flag. Input: `source` (string), optional `sourcePath`.
- **execute** — looks up a previously compiled IR by `contentHash`, optionally replaces the runtime context, then runs the named command through `RuntimeEngine.runCommand`. Returns a structured result with `success`, `result`, `error`, `deniedBy`, and normalized `guardFailure`, `policyDenial`, `constraintOutcomes`, and `emittedEvents`. If the hash is unknown it returns a failure telling the caller to run compile first. Input: `contentHash`, `commandName`, `input`, optional `entityName`, `instanceId`, and a `context` object (tenantId, orgId, actorId, requestId, source, user).
- **validate** — lightweight check that compiles the source (no caching) and returns `{ valid, diagnostics, errorCount, warningCount }`. `valid` is true only when there are no error diagnostics and IR was produced. Input: `source`, optional `sourcePath`.
- **explain** — references a compiled IR by `contentHash` and formats a chosen `target` (`entity`, `command`, or `policy`) by `name` into human-readable detail, with its own expression/value formatters. Input: `contentHash`, `target` enum, `name`, optional `entityName`.

Three resources are registered (`src/resources/`): `manifest://ir/schema` (the IR JSON Schema), `manifest://ir/{contentHash}` (cached compiled IR via a dynamic `ResourceTemplate`), and `manifest://semantics` (the language semantics reference).

State is held in a `SessionStore` singleton: an in-process cache keyed by `contentHash`, each entry holding the IR plus a pre-warmed `RuntimeEngine`.

## Reference

| Item | Value |
| ---- | ----- |
| Published bin | `manifest-mcp` via `@angriff36/manifest` (npm **3.6.41**) |
| Library export | `@angriff36/manifest/mcp-server` |
| In-repo package name | `@manifest/mcp-server@0.1.0` (workspace only — **not** on npm) |
| Transport | stdio (`StdioServerTransport`) |
| Tools | `compile`, `execute`, `validate`, `explain` |
| Resources | `manifest://ir/schema`, `manifest://ir/{contentHash}`, `manifest://semantics` |
| Deps | `@modelcontextprotocol/sdk`, `zod` |

## Notes and limitations

<Warning>
  The session cache is in-process and lost on restart. `execute` and `explain` require a
  `contentHash` from a prior `compile` call in the same session.
</Warning>

`compile` uses the IR provenance `contentHash` (SHA-256 of source computed by the compiler) as the cache key, so identical source reuses the same entry. `validate` deliberately does not cache, so it cannot be followed by `execute` on the same hash. The session store caps at 50 entries with FIFO eviction (an implementation detail of `session-store.ts`).


---

## plugin api

URL: https://manifest-b1e8623f.mintlify.app/extensibility/plugin-api

---
title: 'Plugin API for extending Manifest tooling and runtime'
sidebarTitle: 'Plugin API'
description: 'Extend Manifest with projections, store adapters, audit sinks, expression builtins, and CLI commands through a stable plugin contract that never touches language semantics.'
---

The Plugin API is a stable contract for extending Manifest tooling and runtime without touching language semantics. Plugins are declared in `manifest.config.yaml`, loaded by the plugin loader, and may contribute projections, store adapters, audit sinks, expression builtins, and CLI commands. It is verified against `src/manifest/plugin-api.ts` and `src/manifest/plugin-loader.ts`.

## Defining a plugin

A plugin is an object matching the `ManifestPlugin` interface, typically built with the `definePlugin` helper:

```ts
import { definePlugin } from '@angriff36/manifest/plugin-api';

export default definePlugin({
  manifest: {
    name: '@acme/manifest-plugin-redis',
    version: '1.0.0',
    pluginApiVersion: '1',
    manifestVersion: '>=1.0.0',
    description: 'Redis store adapter for Manifest',
  },
  storeAdapters: [
    {
      scheme: 'redis',
      createStore(entityName, options) {
        return new RedisStore(entityName, options?.connectionUrl as string);
      },
    },
  ],
  builtins: [
    {
      name: 'slugify',
      purity: 'pure',
      arity: 1,
      fn: (s) => String(s).toLowerCase().replace(/\s+/g, '-'),
    },
  ],
});
```

Plugins are declared in config under the `plugins` key as `PluginDeclaration` entries (`{ module, options?, enabled? }`) and loaded with `loadPlugins(declarations, { manifestVersion })` from `@angriff36/manifest/plugin-loader`.

## How loading works

`PLUGIN_API_VERSION` is `'1'`. `definePlugin` validates at definition time: it requires `manifest`, `manifest.name`, and `manifest.version`, enforces that `manifest.pluginApiVersion` equals `PLUGIN_API_VERSION` exactly, and rejects any store adapter whose `scheme` is a built-in target (`memory`, `localStorage`, `postgres`, `supabase`, `durable`, `mongodb`).

The loader (`loadPlugins`) processes each declaration in order: it skips disabled plugins, resolves the module (absolute, relative `./`/`../`, or npm package via `createRequire`), dynamic-imports it, and validates shape (accepting either a default export or a named `plugin`-shaped object). It then checks Manifest version compatibility against `manifest.manifestVersion` using a minimal SemVer matcher that supports exact, `>=`, `<`, `^`, `~`, and compound ranges; an incompatible plugin produces a warning and is skipped.

For each loaded plugin the loader:

- **Projections** are registered via `registerProjection`; a registration failure becomes a warning and does not abort the load.
- **Store adapters** are collected into a `CompositeStoreProvider`. Schemes colliding with built-in targets are rejected with an error; duplicate schemes across plugins are skipped (first registered wins). The provider lazily creates and caches one store per `scheme:entityName` key.
- **Audit sinks** are collected into a factory map keyed by sink `id`; duplicate ids are skipped.
- **Builtins** are merged into a name→function map. Reserved names (see reference) are rejected with an error; a name already registered by another plugin produces a warning and the duplicate is skipped.
- **CLI commands** are collected for later registration via `registerPluginCliCommands(cliCommands, program)`, which calls each command's `register(program)` against a minimal `CliProgramLike` interface (decoupled from commander.js).
- **onLoad** lifecycle hooks are awaited, receiving a `PluginContext` with `options` and `manifestVersion`.

`loadPlugins` returns `LoadedPluginRegistries` containing the composite store provider, builtins map, audit sink factories, collected CLI commands, the list of loaded plugins, and an array of diagnostics (severity `error`/`warning`/`info`).

## Activation

Declaring a plugin in `manifest.config` is enough — the CLI activates it. At startup the CLI reads the config; if `plugins` are declared it calls `loadPlugins` and registers each plugin's `CliCommandPlugin` into its commander program, so plugin commands appear in `manifest --help` and are dispatchable. Activation is conditional (a project with no `plugins` declared loads nothing) and fail-soft (a plugin that fails to load surfaces a diagnostic and is skipped; the CLI is never bricked, and the same config always yields the same commands). `manifest plugins list` performs a real load and reports each plugin's contributed capabilities plus any diagnostics.

For the **runtime**, activation is explicit rather than automatic: an app maps loaded registries into runtime options with `pluginRegistriesToRuntimeOptions(registries, options?)` from `@angriff36/manifest/plugin-loader`, then spreads the result into `new RuntimeEngine(ir, context, options)`:

```ts
import { loadPlugins, pluginRegistriesToRuntimeOptions } from '@angriff36/manifest/plugin-loader';

const registries = await loadPlugins(config.plugins ?? [], { manifestVersion });
const pluginOptions = await pluginRegistriesToRuntimeOptions(registries, { stores: ir.stores });
const engine = new RuntimeEngine(ir, context, { ...pluginOptions /* ...your other options */ });
```

The returned fragment carries `customBuiltins` (the plugin builtins map, passed straight through), a `storeProvider` (the scheme-keyed composite adapted to the engine's entity-keyed signature — this is why `stores` from the IR is required to resolve each entity's scheme), and an optional `auditSink`. Audit sinks are never guessed: with exactly one registered sink it is selected automatically, but when more than one is registered you must pass `auditSinkId` (otherwise the helper throws). The engine itself does not read config or dynamic-import plugins — that indirection would be nondeterministic inside library code — so wiring the fragment is the app's responsibility.

## Reference

Extension-point interfaces (from `plugin-api.ts`):

- `StoreAdapterPlugin`: `{ scheme: string; createStore(entityName, options?): Store | Promise<Store> }`. `Store<T>` exposes `getAll`, `getById`, `create`, `update`, `delete`, `clear`.
- `AuditSinkPlugin`: `{ id: string; createSink(options?): AuditSink | Promise<AuditSink> }`.
- `BuiltinFunctionPlugin`: `{ name: string; purity: 'pure' | 'time-dependent' | 'random'; arity: number; fn: (...args) => unknown }` (`arity` of `-1` indicates variadic).
- `CliCommandPlugin`: `{ name: string; register(program: CliProgramLike): void }`.
- `ManifestPlugin`: `{ manifest, projections?, storeAdapters?, auditSinks?, builtins?, cliCommands?, onLoad? }`.

Constants: `PLUGIN_API_VERSION = '1'`. `BUILTIN_STORE_TARGETS` = `memory`, `localStorage`, `postgres`, `supabase`, `durable`, `mongodb`. `RESERVED_BUILTIN_NAMES` = `now`, `uuid`, `trim`, `split`, `count`, `startsWith`, `endsWith`, `replace`, `toUpperCase`, `toLowerCase`, `length`, `substring`, `indexOf`, `matches`, `search`, `abs`, `round`, `floor`, `ceil`, `min`, `max`, `between`, `sum`, `avg`, `min_of`, `max_of`, `count_of`, `filter`, `flat_map`, `map`, `unique_of`, `year`, `month`, `day`, `hours`, `minutes`, `seconds`, `dateOf`, `timeOf`, `datetimeOf`, `addDuration`, `durationBetween`, `durationDays`, `durationHours`, `durationMinutes`, `durationSeconds`, `flag`, `hasPermission`, `roleAllows`. (RYANSIGNED 2026-07-22)

You can inspect declared plugins with the [`manifest plugins list`](/cli/commands) CLI command. It performs a real `loadPlugins` and reports each declaration's enabled flag, each loaded plugin's contributed capabilities (projections, store adapters, builtins, audit sinks, CLI commands), and any load diagnostics.

## Notes and limitations

<Note>
  The API has no IR-mutation hook by design: plugins extend tooling and runtime, never language
  semantics.
</Note>

The store provider only resolves a store when a `scheme` argument is supplied; without a scheme it returns `undefined`. The SemVer matcher is intentionally minimal and recognizes only the forms listed above. Version incompatibility is non-fatal (warn-and-skip), as are projection registration failures and duplicate builtin/sink registrations, so a partially-loaded plugin set is possible — inspect the returned `diagnostics` to detect skipped contributions.


---

## realtime subscriptions

URL: https://manifest-b1e8623f.mintlify.app/extensibility/realtime-subscriptions

---
title: 'Real-time entity subscriptions'
description: 'Stream runtime events for realtime entities over SSE with a generated subscription route, a typed auto-reconnecting React hook, and a shared singleton runtime.'
sidebarTitle: 'Realtime Subscriptions'
---

The `realtime` entity modifier marks an entity for live event streaming. When you declare `realtime` inside an entity block, the Next.js projection emits three additional surfaces: an SSE (Server-Sent Events) subscription route, a typed React hook with exponential-backoff reconnect, and a shared singleton runtime accessor that lets command routes and subscriptions observe the same event stream.

## Declaring a realtime entity

Place the `realtime` keyword inside an entity block:

```manifest
entity Task {
  realtime

  property required title: string
  property status: string = "pending"
  property assigneeId: string = ""

  command complete() {
    guard self.assigneeId == user.id or user.role == "admin"
    mutate status = "completed"
    emit TaskCompleted
  }
}
```

- `realtime` is a contextual identifier, not a reserved word — `property realtime: boolean` is still a valid plain property.
- The flag compiles to `realtime: true` on the entity's IR representation.
- **`realtime` has no runtime execution semantics.** It is a projection hint only: identical IR with and without the flag produces identical command results, events, and state.

## Generated surfaces

The Next.js projection exposes three realtime surfaces:

| Surface                   | Output path                            | What it emits                                            |
| ------------------------- | -------------------------------------- | -------------------------------------------------------- |
| `nextjs.subscribe`        | `app/api/{entity}/subscribe/route.ts`  | SSE `GET` route streaming runtime events for the entity  |
| `nextjs.subscriptionHook` | `src/hooks/use{Entity}Subscription.ts` | Typed client-side `EventSource` hook with auto-reconnect |
| `nextjs.sharedRuntime`    | `src/lib/manifest-shared-runtime.ts`   | Module-scoped `getSharedRuntime()` singleton accessor    |

Generate them programmatically:

```typescript
import { NextJsProjection } from '@angriff36/manifest/projections/nextjs';

const projection = new NextJsProjection();

const route = projection.generate(ir, { surface: 'nextjs.subscribe', entity: 'Task' });
const hook = projection.generate(ir, { surface: 'nextjs.subscriptionHook', entity: 'Task' });
const shared = projection.generate(ir, { surface: 'nextjs.sharedRuntime' });
```

Requesting a realtime surface for an entity that is **not** flagged `realtime` produces no artifacts and an `info` diagnostic with code `REALTIME_NOT_ENABLED`. The same applies to `nextjs.sharedRuntime` when no entity in the IR is flagged.

## The SSE subscription route

For each realtime entity, `nextjs.subscribe` generates an endpoint at:

```
GET /api/{entity}/subscribe
```

The route authenticates the request (using your configured auth provider — unauthorized requests are rejected before any stream is opened), obtains the shared runtime, and streams every runtime event whose `subject.entity` matches the entity name. Each SSE message is a `data:` line containing the JSON-serialized runtime event:

```json
{
  "name": "TaskCompleted",
  "channel": "task.completed",
  "payload": { "taskId": "t1" },
  "timestamp": 1765329600000,
  "subject": { "entity": "Task", "command": "complete", "instanceId": "t1" }
}
```

The route unsubscribes from the runtime and closes the stream when the client disconnects (via `request.signal` abort).

## The subscription hook

`nextjs.subscriptionHook` generates a `use{Entity}Subscription` hook — a `"use client"` module that manages the `EventSource` lifecycle:

```typescript
'use client';

import { useTaskSubscription } from '@/hooks/useTaskSubscription';

function TaskFeed() {
  const { connected, lastEvent } = useTaskSubscription({
    onEvent: (event) => {
      console.log(event.name, event.payload);
    },
  });

  if (!connected) return <span className="badge">Reconnecting...</span>;
  return <pre>{JSON.stringify(lastEvent, null, 2)}</pre>;
}
```

### Hook return value

| Field       | Type                                | Description                                  |
| ----------- | ----------------------------------- | -------------------------------------------- |
| `connected` | `boolean`                           | Whether the SSE connection is currently open |
| `lastEvent` | `{Entity}SubscriptionEvent \| null` | The most recently received event             |

### Hook options

| Option                | Type                                         | Default | Description                                |
| --------------------- | -------------------------------------------- | ------- | ------------------------------------------ |
| `onEvent`             | `(event: {Entity}SubscriptionEvent) => void` | —       | Callback fired for every event received    |
| `initialRetryDelayMs` | `number`                                     | `1000`  | Initial reconnect delay after a disconnect |
| `maxRetryDelayMs`     | `number`                                     | `30000` | Reconnect delay ceiling                    |

### Auto-reconnect behavior

When the connection drops, the hook closes the `EventSource` and schedules a reconnect. The delay starts at `initialRetryDelayMs` and **doubles on each failed attempt** up to `maxRetryDelayMs`. A successful reconnect resets the delay. While disconnected, `connected` is `false` and `lastEvent` retains its last value.

## The shared runtime

The event stream is **per-engine-instance and in-memory**. For a subscription to observe events emitted by command execution, the SSE route and the command routes must share one engine. `nextjs.sharedRuntime` generates that singleton:

```typescript
import { getSharedRuntime } from '@/lib/manifest-shared-runtime';

const runtime = await getSharedRuntime();
```

When any entity in the IR is flagged `realtime`, the generated dispatcher and per-command routes (inline execution mode) automatically switch from constructing a per-request runtime to calling `getSharedRuntime()` and setting per-request context via `runtime.replaceContext(...)`.

`externalExecutor` mode is unaffected — the executor owns runtime construction and should call `getSharedRuntime()` itself when realtime is in play.

### Single-instance vs. multi-instance delivery

In-memory, the shared engine's stream is **single-instance**: a subscriber only observes events emitted by commands that executed in its own server process. On a long-lived, single-instance Node server that is all you need.

To fan events across processes (serverless, multiple replicas), configure an `eventBus` in the runtime factory's `RuntimeOptions`. When one is present, the generated `getSharedRuntime()` singleton calls `runtime.connectEventBus()` **once at initialization**, subscribing the process to remote events so events published by commands on other instances reach this stream's local subscribers. Manifest ships two `EventBus` implementations: an in-process `MemoryEventBus` (`@angriff36/manifest/events`, single node / tests) and a `RedisEventBus` (`@angriff36/manifest/events/redis`) for cross-process fan-out.

When **no** bus is configured, the singleton emits a single `console.warn` at init noting that delivery is single-instance only, and continues. For a deployment that advertises multi-instance realtime, set the `realtime.requireEventBus` option to make the generated singleton **throw at init** instead — so a missing bus fails loud at startup rather than silently degrading:

```typescript
const shared = projection.generate(ir, {
  surface: 'nextjs.sharedRuntime',
  options: { realtime: { requireEventBus: true } },
});
```

<Note>
  The default companion runtime factory does not inject a bus — supply the `eventBus` through the
  runtime's `RuntimeOptions` (a custom runtime factory or your app's runtime wiring). See
  `docs/spec/semantics.md` § "Realtime Entities" and `docs/spec/adapters.md` § "Event Bus".
</Note>

## Runtime `subscribe()` contract

The reference runtime exposes `subscribe(entityName, listener)` — a convenience over `onEvent` that exists regardless of any entity's `realtime` flag:

- The listener receives only events whose `subject.entity === entityName`.
- Events **without** a `subject.entity` are not delivered (use `onEvent` for the unfiltered firehose).
- The return value is an unsubscribe function; after calling it the listener receives no further events.
- Multiple subscribers are independent; listener errors are swallowed and never affect command execution.

```typescript
const unsubscribe = runtime.subscribe('Task', (event) => {
  console.log(event.name, event.subject?.instanceId);
});

// later
unsubscribe();
```

## IR representation

The `realtime` modifier adds a boolean field to the entity IR:

```json
{
  "name": "Task",
  "realtime": true,
  "properties": [...],
  "commands": [...]
}
```

Projections inspect `entity.realtime === true` to decide whether to generate subscription artifacts. Entities without the modifier produce no realtime artifacts.

<Note>
  The `realtime` modifier is a declaration only. The SSE transport is implemented by the projection
  target (the generated Next.js route). The runtime engine itself is transport-agnostic.
</Note>


---

## runtime middleware

URL: https://manifest-b1e8623f.mintlify.app/extensibility/runtime-middleware

---
title: 'Runtime middleware pipeline'
description: 'Compose cross-cutting concerns — logging, auth enrichment, short-circuiting — into the command execution lifecycle via deterministic, ordered middleware hooks.'
sidebarTitle: 'Runtime Middleware'
---

The runtime middleware pipeline inserts composable hooks into the command execution lifecycle. Middleware runs at four deterministic points: before policies, before guards, before actions, and after events. Each hook receives a `MiddlewareContext` and can either enrich the evaluation context, inspect intermediate state, or short-circuit execution with a custom result.

Middleware is optional and fully backward compatible. If you do not configure any middleware, the runtime behaves exactly as before.

## Middleware hooks

Each middleware declares which lifecycle hooks it participates in via a `hooks` array. The runtime invokes the handler once per matching hook during command execution.

| Hook            | When it runs                                            | Typical use                                           |
| --------------- | ------------------------------------------------------- | ----------------------------------------------------- |
| `before-policy` | After evaluation context is built, before policy checks | Inject identity, enrich `context.*`                   |
| `before-guard`  | After policies and constraints pass, before guards      | Audit the incoming state, enforce domain invariants   |
| `before-action` | After guards pass, before actions execute               | Validate action preconditions, request-scoped logging |
| `after-emit`    | After all events are emitted and reactions cascade      | Event auditing, metric collection, post-hoc analysis  |

Hooks execute in a deterministic order that matches the command lifecycle documented in [Commands](/language/commands):

1. Build evaluation context
2. Evaluate rate limits (no middleware hook — returns `rateLimitExceeded` immediately if exceeded)
3. **`before-policy`** middleware
4. Evaluate policies
5. Evaluate command constraints
6. **`before-guard`** middleware
7. Evaluate guards
8. **`before-action`** middleware
9. Execute actions
10. Emit declared events
11. **`after-emit`** middleware
12. Return `CommandResult`

## The `MiddlewareContext` object

Every middleware handler receives a `MiddlewareContext` with the following fields:

| Field            | Type                      | Description                                              |
| ---------------- | ------------------------- | -------------------------------------------------------- |
| `command`        | `string`                  | Name of the command being executed                       |
| `entityName`     | `string`                  | Entity the command belongs to                            |
| `instanceId`     | `string \| undefined`     | Target instance ID, if provided                          |
| `hook`           | `MiddlewareHook`          | Which lifecycle hook triggered this call                 |
| `input`          | `Record<string, unknown>` | Command parameters and runtime options                   |
| `evalContext`    | `Record<string, unknown>` | Current evaluation context (`self`, `user`, `context.*`) |
| `runtimeContext` | `RuntimeContext`          | The full runtime context object                          |
| `emittedEvents`  | `EmittedEvent[]`          | Events emitted so far (populated only at `after-emit`)   |

## The `MiddlewareResult` object

A middleware handler returns a `MiddlewareResult` to control what happens next:

| Field          | Type                      | Description                                                |
| -------------- | ------------------------- | ---------------------------------------------------------- |
| `shortCircuit` | `boolean`                 | If `true`, halt execution and return the provided `result` |
| `result`       | `CommandResult`           | The result to return when short-circuiting                 |
| `contextPatch` | `Record<string, unknown>` | Key-value pairs merged into the evaluation context         |

### Context patching

Use `contextPatch` to inject values into the evaluation context that guard and action expressions can reference. This is how you enrich `context.*` with external data without modifying the IR.

```typescript
const tenantMiddleware: Middleware = {
  hooks: ['before-policy'],
  async handler(ctx) {
    const tenant = await fetchTenant(ctx.runtimeContext.tenantId);
    return {
      contextPatch: {
        'context.tenantPlan': tenant.plan,
        'context.tenantRegion': tenant.region,
      },
    };
  },
};
```

### Short-circuiting

Return `{ shortCircuit: true, result }` to halt the command pipeline immediately. No later middleware, policies, guards, or actions run. The provided `CommandResult` is returned to the caller as-is.

```typescript
const maintenanceMiddleware: Middleware = {
  hooks: ['before-policy'],
  async handler(ctx) {
    if (await isInMaintenanceMode(ctx.entityName)) {
      return {
        shortCircuit: true,
        result: {
          success: false,
          error: 'SERVICE_UNAVAILABLE',
          emittedEvents: [],
        },
      };
    }
    return {};
  },
};
```

## Registration and ordering

Middleware is configured via the `middleware` field on `RuntimeOptions`. Middleware runs in the order it appears in the array. If two middleware both handle the same hook, the first-registered middleware runs first.

```typescript
import { RuntimeEngine, Middleware } from '@angriff36/manifest';
import { ir } from './compiled.ir.json';

const loggingMiddleware: Middleware = {
  hooks: ['before-policy', 'after-emit'],
  async handler(ctx) {
    if (ctx.hook === 'before-policy') {
      console.log(`[START] ${ctx.entityName}.${ctx.command}`, ctx.input);
    } else {
      console.log(`[END] ${ctx.entityName}.${ctx.command}`, {
        events: ctx.emittedEvents.length,
      });
    }
    return {};
  },
};

const runtime = new RuntimeEngine(
  ir,
  {},
  { middleware: [loggingMiddleware] },
);
```

`middleware` is a `RuntimeOptions`
field — put it in the **options** (3rd) arg of `new RuntimeEngine(ir, context,
options)`, not in `context`. In `context` it typechecks (`RuntimeContext` has
an open `[key: string]` index signature, `runtime-engine.ts:109`) but the
engine only reads `this.options.middleware` (`runtime-engine.ts:2285`), so the
pipeline silently never runs.

<Note>
  The ordering guarantee is strict: middleware at index 0 always runs before middleware at index 1
  for the same hook. This makes the pipeline deterministic and testable.
</Note>

## Example: logging middleware

A simple middleware that logs command start and end with timing:

```typescript
const timedLogging: Middleware = {
  hooks: ['before-policy', 'after-emit'],
  async handler(ctx) {
    if (ctx.hook === 'before-policy') {
      ctx.input.__startTime = Date.now();
      return {};
    }
    const duration = Date.now() - (ctx.input.__startTime as number);
    console.log(
      JSON.stringify({
        entity: ctx.entityName,
        command: ctx.command,
        instanceId: ctx.instanceId,
        durationMs: duration,
        eventsEmitted: ctx.emittedEvents.length,
      }),
    );
    return {};
  },
};
```

## Example: auth enrichment middleware

Inject caller identity and role data into the evaluation context so that guard expressions can reference them:

```typescript
const authEnrichment: Middleware = {
  hooks: ['before-policy'],
  async handler(ctx) {
    const userId = ctx.runtimeContext.actorId;
    if (!userId) return {};

    const user = await lookupUser(userId);
    return {
      contextPatch: {
        'context.userRole': user.role,
        'context.userDepartment': user.department,
        'context.isUserActive': user.active,
      },
    };
  },
};
```

## Example: conditional short-circuit

Block all mutations on entities that are in an archived state:

```typescript
const archiveGuard: Middleware = {
  hooks: ['before-guard'],
  async handler(ctx) {
    if (!ctx.instanceId) return {};
    const instance = ctx.evalContext.self;
    if (instance?.archived === true) {
      return {
        shortCircuit: true,
        result: {
          success: false,
          error: 'ENTITY_ARCHIVED',
          emittedEvents: [],
        },
      };
    }
    return {};
  },
};
```

## Type reference

```typescript
type MiddlewareHook = 'before-policy' | 'before-guard' | 'before-action' | 'after-emit';

interface MiddlewareContext {
  command: string;
  evalContext: Record<string, unknown>;
  runtimeContext: RuntimeContext;
  entityName: string;
  instanceId?: string;
  hook: MiddlewareHook;
  input: Record<string, unknown>;
  emittedEvents: EmittedEvent[];
}

interface MiddlewareResult {
  shortCircuit?: boolean;
  result?: CommandResult;
  contextPatch?: Record<string, unknown>;
}

interface Middleware {
  hooks: MiddlewareHook[];
  handler: (ctx: MiddlewareContext) => Promise<MiddlewareResult>;
}
```

All middleware types are exported from `@angriff36/manifest` and are available for library consumers.


---

## Start with a compiled IR

URL: https://manifest-b1e8623f.mintlify.app/extensibility/runtime-tooling

---
title: 'Runtime tooling: REPL, time-travel debugger, profiler'
description: 'Interactive developer tools for inspecting and debugging Manifest programs — an interactive REPL, a time-travel debugger for stepping through command execution, and a performance profiler.'
sidebarTitle: 'Runtime tooling'
---

Manifest ships with three interactive developer tools for inspecting, debugging, and profiling runtime execution. All three are designed for development and testing — they are not intended for production use.

## Runtime REPL

The interactive REPL (`manifest repl`) provides a command-line interface for loading IR, creating instances, running commands, and inspecting state — all without writing test code.

### Starting the REPL

```bash
# Start with a compiled IR
manifest repl ir/app.ir.json

# Start with a source file (compiled on the fly)
manifest repl src/app.manifest
```

### Available commands

Verified against `packages/cli/src/commands/repl.ts`.
The REPL loads source at startup (`manifest repl <path>`); there is no `load` / `create` /
`reset`. `run` takes **two** positional args (`run <entity> <command> [json]`), not
`Entity.command` dotted form. `eval` is stubbed (“not yet implemented”).

| Command | Description |
| ------- | ----------- |
| `help` | Show available commands |
| `list` | List entities (or instances when scoped) |
| `inspect` / `show` / `info` | Inspect IR / entity / runtime info |
| `get <Entity> <id>` | Retrieve an instance |
| `run <entity> <command> [json]` | Execute a command via `runCommand` |
| `cmds` | List commands |
| `eval <expression>` | Stub — expression eval not implemented yet |
| `set` | Set REPL context (e.g. tenant) |
| `policies` | Show policies |
| `events` | Show `runtime.getEventLog()` |
| `clear` | Clear console / event log helpers |
| `reload` | Recompile and reload the startup manifest |
| `json` | Toggle JSON output mode |
| `exit` | Exit the REPL |

### Usage example

```
$ manifest repl src/app.manifest
manifest> run Order addItem {"amount": 29.99}
✓ Command Order.addItem succeeded

manifest> get Order order-1
{ id: "order-1", status: "pending", total: 29.99 }

manifest> events
Event Log (1 events):
  OrderUpdated …
```

<Note>
  The REPL uses an in-memory store by default. State is lost when you exit. For persistent
  debugging, consider the snapshot testing infrastructure instead.
</Note>

## Time-travel debugger

The time-travel debugger records every state mutation during command execution and lets you step forward and backward through the execution timeline. It wraps `RuntimeEngine` and captures a snapshot before and after each action.

### Enabling time-travel

Shipped debug API is `CommandTraceRecorder` from
`@angriff36/manifest/debug`. Its `runCommand` matches
`RuntimeEngine.runCommand(commandName, input, options?)` — not entity-first.
There is no exported `TimeTravelDebugger` class.

```typescript
import { RuntimeEngine } from '@angriff36/manifest';
import { CommandTraceRecorder } from '@angriff36/manifest/debug';

const runtime = new RuntimeEngine(ir, {}, { /* runtime options */ });
const tracer = new CommandTraceRecorder(runtime);

const result = await tracer.runCommand('complete', {}, {
  entityName: 'Order',
  instanceId: 'order-1',
});

const steps = tracer.getSteps();
// [{ index: 0, label: 'before', snapshot: { ... } }, { index: 1, label: 'after', ... }]
```

### Stepping through execution

`CommandTraceRecorder` exposes `getSteps()` and
`restoreToStep(stepIndex)` (via `RuntimeEngine.restore`). There is no `getStateAt` /
`stepForward` / `stepBack` / `diff` API.

```typescript
const steps = tracer.getSteps();
await tracer.restoreToStep(0); // restore pre-command snapshot
```

### Timeline entries

Each `CommandTraceStep` has `index`, `label`, and
`snapshot` (`SerializedRuntimeSnapshot`).

<Warning>
  Trace snapshots capture store state. For large datasets or high-frequency command execution,
  this can consume significant memory. Use it in development and testing only.
</Warning>

## Performance profiler

The runtime profiler measures execution time for each phase of command execution, helping you identify slow guards, expensive computed properties, or bottlenecked actions.

### Running the profiler

There is no `@angriff36/manifest/profiler` export /
`RuntimeProfiler` class. Profile timing via your own harness around
`runtime.runCommand(commandName, input, options?)` (Kitchen FlameGraph UI is separate).

```typescript
const result = await runtime.runCommand('complete', {}, {
  entityName: 'Order',
  instanceId: 'order-1',
});
```

### Profile report shape

No shipped `ProfileReport` type
or runtime profiler package. Use host-level timing around `runCommand` if needed.

<Note>
  Kitchen's FlameGraph profiler tab is a UI surface, not a public `@angriff36/manifest/profiler`
  API.
</Note>


---

## Report coverage for a compiled IR

URL: https://manifest-b1e8623f.mintlify.app/extensibility/snapshot-testing

---
title: 'Snapshot testing for Manifest projections'
sidebarTitle: 'Snapshot Testing'
description: 'Snapshot generated code across all built-in projections so any change to a generator surfaces as a reviewable diff, with timestamp stabilization and a determinism check.'
---

Snapshot tests compile a representative Manifest program to IR, run every built-in projection against it, and snapshot the generated artifacts. Any change to generated code surfaces as a snapshot diff in review. Verified against `src/manifest/projections/snapshot.test.ts` and `src/manifest/projections/__snapshots__/snapshot.test.ts.snap`.

## Usage

The tests run as part of the normal vitest suite. To update snapshots after an intentional change to a generator:

```bash
npx vitest -u src/manifest/projections/snapshot.test.ts
```

## How it works

The test file builds a fixed IR fixture in `snapshotIR()` — a small but representative program with two entities (`Task`, `User`), a computed property, relationships (`belongsTo`/`hasMany`), commands with guards and actions, events with channels, a policy, constraints, and a `durable` store for both entities so the ORM projections (Prisma, Drizzle) emit output. The IR provenance is hardcoded (`contentHash: 'snapshot-fixture-hash'`, fixed `compiledAt`) so the input is deterministic.

The suite enumerates projections via `listBuiltinProjections()` and asserts there are exactly **29** built-in projections.

`snapshot.test.ts` uses
 **29**) is a deliberate tripwire: it fails when the built-in projection set changes, forcing a conscious snapshot update. Only ISO-millisecond timestamps are stabilized; any other non-deterministic output in a generator would make snapshots flaky and must be handled in the generator itself, since the runtime determinism assertion would also catch it.

## Command and guard coverage reporter

The coverage reporter correlates compiled IR with conformance and unit test evidence to produce a coverage report. It answers the question: "Which commands, guards, policies, and constraints have test coverage?"

### Usage

```bash
# Report coverage for a compiled IR
manifest coverage --ir ir/app.ir.json

# Fail CI if coverage drops below a threshold
manifest coverage --ir ir/app.ir.json --min-coverage 80 --strict

# JSON output for CI integration
manifest coverage --ir ir/app.ir.json --format json
```

### Coverage categories

| Category    | What it checks                                            |
| ----------- | --------------------------------------------------------- |
| Commands    | Each declared command has at least one test case          |
| Guards      | Each guard expression is exercised (true and false paths) |
| Policies    | Each policy rule has a test that evaluates it             |
| Constraints | Each constraint expression is tested for pass and fail    |

### Programmatic API

`@angriff36/manifest/coverage` exports
`coverageCommand` (and coverage result types), **not** a `CoverageReporter` class.

```typescript
import { coverageCommand } from '@angriff36/manifest/coverage';

const report = await coverageCommand({
  ir: 'ir/app.ir.json',
  root: '.',
  format: 'json',
  minCoverage: 80,
  strict: true,
});
console.log(report.overall.percentage);
```

## Property-based testing

In-repo property tests use **fast-check** in
`src/manifest/runtime-guard-properties.test.ts` (guard evaluation properties). There is no
dedicated `property-based` vitest grep title and no documented `FC_SEED` env contract on this
page. Run the file via the normal suite (`pnpm test`) or target that test file explicitly.

Invariants covered there include guard failure indexing / short-circuit behavior under random
inputs — not a full “random IR program” generator as previously implied.

---

## faq

URL: https://manifest-b1e8623f.mintlify.app/faq

---
title: 'Frequently asked questions about Manifest'
description: "Answers to the most common questions about Manifest's design, architecture, integration patterns, runtime semantics, and vNext features."
sidebarTitle: 'FAQ'
---

Manifest is an IR-first domain-specific language for encoding business rules, authorization policies, and event-driven workflows. The questions below address the most common points of confusion — from how Manifest compares to ORMs, to how the runtime evaluates guards and policies, to what's coming in vNext.

<AccordionGroup>

  <Accordion title="What is Manifest and who is it for?">
    Manifest is a domain-specific language for defining business rules and workflows with declarative specifications. It is **IR-first**: the Intermediate Representation (IR) is the single source of truth for program semantics, not the generated TypeScript or route code.

    The primary consumers are **AI agents** that emit, validate, and reason about Manifest programs. Secondary consumers are developers who want:
    - Declarative business rules that compile to full-stack implementations
    - Deterministic execution — identical IR plus identical context always produces identical results
    - Guard-based authorization that cannot be bypassed
    - Event-driven architectures with guaranteed ordering

  </Accordion>

  <Accordion title="What makes Manifest different from ORMs like Prisma?">
    Prisma and Manifest solve different problems. Prisma gives you a type-safe query API over a database schema. Manifest gives you an executable contract over your business rules.

    | Prisma | Manifest |
    |--------|----------|
    | Database schema + type-safe queries | Business rules + execution semantics |
    | Queries are imperative (`findMany`, `update`) | Commands are declarative (`create Recipe { ... }`) |
    | No built-in authorization | Guards and policies are first-class |
    | No event system | Events are first-class with ordering guarantees |
    | Generated code is source | IR is source; generated code is derivative |

    You can use Prisma as a storage adapter behind Manifest — it is not a replacement.

  </Accordion>

  <Accordion title="What are the two integration patterns?">
    Manifest supports two ways to integrate with your application:

    1. **Projections** — Generate platform-specific code (Next.js App Router routes, Express controllers, TypeScript types, client SDKs) directly from IR. Use this when your mutation flow is standard runtime execution and you want convention-over-configuration.

    2. **Embedded Runtime** — Write application handlers that call `RuntimeEngine.runCommand` directly. Use this when you need custom orchestration, custom event handling pipelines, or framework-specific behavior.

    | Use projections when | Use embedded runtime when |
    |---------------------|--------------------------|
    | You want generated route/controller code | You need custom orchestration around commands |
    | Your mutation flow is standard | You need custom event handling pipelines |
    | You want convention-over-configuration | You need framework-specific behavior |

  </Accordion>

  <Accordion title="Can I use Manifest with my existing database?">
    Yes. Manifest does not require a specific database. You can connect any storage backend by implementing the `Store` interface and passing it through `storeProvider` in `RuntimeOptions`:

    ```typescript
    interface Store<T extends EntityInstance = EntityInstance> {
      getAll(): Promise<T[]>;
      getById(id: string): Promise<T | undefined>;
      create(data: Partial<T>): Promise<T>;
      update(id: string, data: Partial<T>): Promise<T | undefined>;
      delete(id: string): Promise<boolean>;
      clear(): Promise<void>;
    }
    ```

    Pass your store implementation when constructing `RuntimeEngine`:

    ```typescript
    const runtime = new RuntimeEngine(
      ir,
      { actorId: 'user-123' },
      {
        storeProvider: (entityName) => {
          if (entityName === 'Recipe') return new MyRecipeStore();
          return undefined;
        },
      },
    );
    ```

    Returning `undefined` uses built-in behavior only for `memory` and browser
    `localStorage`. Durable targets fail until they are bound. Generated
    companions can load bindings from `manifest.config.ts`; see the
    [complete PostgreSQL setup](/language/stores#complete-generated-companion-postgresql-setup).

  </Accordion>

  <Accordion title="What storage targets are supported?">
    The runtime **must** support `memory`. The following targets **may** be supported depending on your environment:

    - `localStorage` (browser)
    - `postgres` (Node.js) — requires a `storeProvider` supplying the adapter; the runtime does not auto-instantiate it
    - `supabase` (Node.js / Edge) — requires a `storeProvider` supplying the adapter; the runtime does not auto-instantiate it
    - Custom stores via the `Store` interface and `storeProvider`

    Unsupported targets emit diagnostics — there is no silent fallback unless you explicitly configure one.

  </Accordion>

  <Accordion title="Does Manifest handle authentication?">
    No. Manifest handles **authorization** (policies and guards), not authentication.

    Authentication is the responsibility of your application layer — Clerk, Auth0, NextAuth, and similar providers all work. You derive identity from your auth context and inject authoritative values (for example `userId`, `tenantId`, `user.role`) into the runtime context before command execution.

    <Warning>
      Never read `id`, `userId`, `tenantId`, or `orgId` from `request.json()`. Always derive these values from your auth context and inject them into the runtime. Client-supplied identity values cannot be trusted.
    </Warning>

  </Accordion>

  <Accordion title="What is the execution order for commands?">
    Manifest commands execute in a fixed, deterministic order defined in the language specification (`docs/spec/semantics.md`):

    > Full command path:
    > 1. **Build evaluation context** — apply parameter defaults; fail closed on missing required parameters (`MISSING_REQUIRED_PARAMETER`) before later gates
    > 2. **Rate-limit check** — per-user, per-tenant, or global limits declared on the command; stop with `rateLimitDenial` if exceeded
    > 3. **Policy check** — evaluated for `execute` or `all` scope (policy-level rate limits run before each policy expression)
    > 4. **Command-level constraint validation** — severity `block` / `warn` / `ok`; unauthorized `block` failures halt execution
    > 5. **Guard evaluation** — declaration order; halts on the first falsey result
    > 6. **Action execution** — actions run in order
    > 7. **Event emission** — events emitted in declaration order
    > 8. **Return** — `CommandResult` is returned to the caller
    >
    > This order is not configurable. Tenant / idempotency gates (when configured) run before evaluation. Entity-level constraints and transitions are enforced on create/update mutations, not as a substitute for step 4.

  </Accordion>

  <Accordion title="What happens when a guard fails?">
    Execution halts immediately. There is no auto-repair, no fallback, and no permissive default.

    The runtime returns a `CommandResult` with `success: false` and a `guardFailure` object that includes the failing guard's index, expression, and resolved values at the time of failure. Always inspect `result.guardFailure`, not a `result.diagnostics` field — `CommandResult` does not have a `diagnostics` field.

    ```typescript
    const result = await runtime.runCommand('create', input, { entityName: 'Todo' });

    if (!result.success && result.guardFailure) {
      console.error('Guard failed:', {
        index: result.guardFailure.index,
        expression: result.guardFailure.formatted,
        resolved: result.guardFailure.resolved
      });
    }
    ```

  </Accordion>

  <Accordion title="What is the difference between read, execute, and all policy scopes?">
    Policy scope controls when a policy is enforced during command processing:

    - **`read`** — Enforced at the runtime read gate: `getInstance` / `getAllInstances`. A denied read fails closed (`getInstance` returns `undefined`, `getAllInstances` omits the row). Note that generated projections may bypass the engine and query storage directly; those reads are not gated.
    - **`execute`** — Enforced during command execution. Use this for policies that must block mutations.
    - **`all`** — Enforced during command execution AND at the read gate.

    `read` policies are enforced only when reads go through the engine's `getInstance` / `getAllInstances`, not on the store and not inside `runCommand` (the internal execution read path is un-gated so command results are unchanged).

  </Accordion>

  <Accordion title="What are constraint severity levels?">
    Constraints have three severity levels that control whether a violation blocks execution:

    - **`block`** — The constraint must pass for the command to proceed. This is the default.
    - **`warn`** — Execution continues even if the constraint fails. The violation is recorded in `result.constraintOutcomes`.
    - **`ok`** — Informational only. Never blocks execution.

    ```typescript
    // Check warning-severity constraint outcomes after a successful command
    const warnings = result.constraintOutcomes?.filter(o => !o.passed && o.severity === 'warn');
    if (warnings?.length) {
      console.warn('Constraint warnings:', warnings);
    }
    ```

    If a constraint is not blocking execution when you expect it to, verify that its severity is set to `block`.

  </Accordion>

  <Accordion title="Are read operations (GET) validated by the runtime?">
    Reads that go through the engine are gated. `read` (and `all`) policies are enforced at a central read gate in `getInstance` / `getAllInstances`: a denied read fails closed (`getInstance` returns `undefined`, `getAllInstances` omits the denied rows).

    However, generated projections (for example, Next.js GET route handlers) may bypass the engine entirely and query storage directly; those reads are not gated by the runtime. Mutations — commands executed via `runtime.runCommand()` — go through the full guard, policy, constraint, and event pipeline, and their internal reads are deliberately un-gated so command results stay deterministic.

  </Accordion>

  <Accordion title="What are vNext features?">
    vNext features are implemented in the IR and runtime and are ready for adoption today. They are not behind a flag. The current vNext features are:

    - **Command-level constraints** — Constraints attached to individual commands, not just entities
    - **Entity concurrency controls** — Optimistic concurrency on entity mutations
    - **Policy-based authorization** — `execute` and `all` scoped policies enforced at command execution; `read` (and `all`) scoped policies enforced at the runtime read gate (`getInstance` / `getAllInstances`, fail-closed)
    - **IR caching** — Compiled IR can be cached to disk to avoid recompilation on every request

    > **Note:** Constraint severity/outcomes (`block`/`warn`/`ok`) are **fully implemented** — not vNext. See the severity accordion above for usage.

    See the vNext spec and migration guide for adoption instructions.

  </Accordion>

</AccordionGroup>

<Note>
  Looking for troubleshooting help instead? See the [Troubleshooting guide](/troubleshooting) for
  specific error messages and fixes.
</Note>


---

## index

URL: https://manifest-b1e8623f.mintlify.app/index

---
title: 'Manifest: Declarative Domain Modeling for Developers'
description: 'Manifest is a domain-specific language that compiles business rules, entities, and commands into a portable runtime. Define once, run anywhere.'
---

Manifest is a declarative language for expressing your application's domain model — its entities, commands, guards, policies, and events — in a format that compiles to an executable Intermediate Representation (IR). The runtime enforces your rules deterministically on every execution, making business logic auditable, testable, and portable across platforms.

<CardGroup cols={2}>
  <Card title="Quick Start" icon="bolt" href="/quickstart">
    Install Manifest and run your first command in under 5 minutes.
  </Card>
  <Card title="Language Reference" icon="book" href="/language/entities">
    Learn entities, commands, guards, policies, events, and stores.
  </Card>
  <Card title="Integration Guide" icon="plug" href="/integration/overview">
    Use projections or embed the runtime directly in your app.
  </Card>
  <Card title="CLI Reference" icon="terminal" href="/cli/overview">
    Compile, generate, build, and validate from the command line.
  </Card>
</CardGroup>

## How Manifest works

Manifest programs are `.manifest` source files. The compiler transforms them into IR — a JSON contract that the runtime executes. You can generate platform-specific code (like Next.js API routes) from that IR, or embed the runtime directly in your application handlers.

<Steps>
  <Step title="Install the package">
    Add `@angriff36/manifest` to your project with npm or pnpm.
  </Step>
  <Step title="Write a .manifest file">
    Define entities, commands, guards, and policies in the Manifest language.
  </Step>
  <Step title="Compile to IR">
    Use the CLI or the `compileToIR` function to produce an IR JSON file.
  </Step>
  <Step title="Run commands">
    Instantiate `RuntimeEngine` with your IR and call `runCommand` from your handlers.
  </Step>
</Steps>

## Key capabilities

<CardGroup cols={2}>
  <Card title="Deterministic execution" icon="lock">
    Identical IR plus identical runtime context always produces identical results. No surprises in
    production.
  </Card>
  <Card title="Guard-based authorization" icon="shield">
    Guards and policies are first-class language constructs, not bolt-on middleware. They cannot be
    bypassed.
  </Card>
  <Card title="Event emission" icon="satellite-dish">
    Commands declare the events they emit. The runtime logs every emission with payload, channel,
    and timestamp.
  </Card>
  <Card title="Platform projections" icon="code">

    Generate platform artifacts from IR (Next.js routes,
    Prisma/Drizzle, Zod, React Query, OpenAPI, Convex, and other registered projections). There is
    no single packaged "client SDK" product — choose the projection(s) you need.
  </Card>
</CardGroup>


---

## Example: only if you use the Redis outbox

URL: https://manifest-b1e8623f.mintlify.app/installation

---
title: 'Installing @angriff36/manifest: setup and configuration'
sidebarTitle: 'Installation'
description: 'Add @angriff36/manifest to any Node.js 20+ project with npm or pnpm, configure TypeScript, and optionally install the CLI and Supabase peer dependency.'
---

Manifest is published as `@angriff36/manifest` to the public npm registry. Before installing, make sure your project meets the minimum requirements:

**Node.js 20 or later** (`package.json` `engines.node` is `>=20`) and npm or pnpm.

<Note>
  The package is fully self-contained: the compiler, runtime, CLI, MCP server, and LSP server all
  ship inside `@angriff36/manifest`, and their runtime dependencies are declared and installed
  automatically — no manual dependency installs are needed.
</Note>

## Install the package

<CodeGroup>

```bash npm
npm install @angriff36/manifest
```

```bash pnpm
pnpm add @angriff36/manifest
```

</CodeGroup>

<Note>
  `@angriff36/manifest` resolves from the public npm registry — no GitHub Packages authentication,
  personal access token, or custom `.npmrc` registry entry is required. `npm install` / `pnpm add`
  works out of the box.
</Note>

## Package exports

The package ships multiple named entry points. Import only what you need — each export is independent.

| Import path                              | What it provides                                          |
| ---------------------------------------- | --------------------------------------------------------- |
| `@angriff36/manifest`                    | `RuntimeEngine` — the main command execution engine       |
| `@angriff36/manifest/runtime-engine`     | Full runtime module: `RuntimeEngine`, stores, event types |
| `@angriff36/manifest/binary-ir`          | Binary IR pack/unpack (MessagePack encoding)              |
| `@angriff36/manifest/compiler`           | The high-level `compiler` orchestrator                    |
| `@angriff36/manifest/ir-compiler`        | `compileToIR` — parse source and produce IR v1            |
| `@angriff36/manifest/ir`                 | IR data structure types                                   |
| `@angriff36/manifest/stores`             | Node.js store adapters                                    |
| `@angriff36/manifest/projections/nextjs` | Next.js code generation projection                        |
| `@angriff36/manifest/projections/routes` | Route generation projection                               |
| `@angriff36/manifest/registry/emit`      | Event registry emit helpers                               |
| `@angriff36/manifest/audit`              | `AuditSink` interface                                     |
| `@angriff36/manifest/audit/memory`       | `MemoryAuditSink` for development and tests               |
| `@angriff36/manifest/audit/postgres`     | `PostgresAuditSink` for production                        |
| `@angriff36/manifest/outbox`             | `OutboxStore` interface                                   |
| `@angriff36/manifest/outbox/memory`      | `MemoryOutboxStore` for development and tests             |
| `@angriff36/manifest/outbox/postgres`    | `PostgresOutboxStore` for production                      |

The most common imports for getting started are:

```typescript
import { RuntimeEngine } from '@angriff36/manifest';
import { compileToIR } from '@angriff36/manifest/ir-compiler';
```

## TypeScript setup

The package ships declaration files for every export. No `@types/` package is needed. Your `tsconfig.json` must target **ES2020 or later** and enable `moduleResolution: "bundler"` or `"node16"`/`"nodenext"` to resolve the package's `exports` map correctly.

```json
{
  "compilerOptions": {
    "target": "ES2020",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "strict": true
  }
}
```

<Note>
  The package is published as ESM (`"type": "module"`). If your project uses CommonJS, configure
  your bundler or TypeScript to handle ESM interop, or use a bundler like Vite or esbuild that
  handles this automatically.
</Note>

## Optional peer dependencies

Store and platform adapters that require external services are declared as **optional peer dependencies**. Install only the ones you use — everything else works without them:

| Package                 | Needed for            |
| ----------------------- | --------------------- |
| `@supabase/supabase-js` | Supabase store target |
| `ioredis`               | Redis outbox store    |
| `mongodb`               | MongoDB store adapter |
| `@libsql/client`        | Turso store adapter   |

`@assemblyscript/loader` is **not** an optional peer of `@angriff36/manifest`. It appears only as a repo `devDependency` for WASM evaluator tests; consumers do not install it for normal runtime use. Optional peers in `package.json` are exactly: `@supabase/supabase-js`, `ioredis`, `mongodb`, `@libsql/client`.

```bash
# Example: only if you use the Redis outbox
npm install ioredis
```

If an optional peer is not installed, the corresponding adapter fails with a clear error when used; all other store targets continue to work normally.

## Bundled binaries

Installing `@angriff36/manifest` registers three binaries, all working out of the box with no extra installs:

| Binary         | What it is                                             |
| -------------- | ------------------------------------------------------ |
| `manifest`     | The CLI — compile, generate, validate, scan            |
| `manifest-mcp` | Model Context Protocol server for AI agents            |
| `manifest-lsp` | Language Server Protocol server for editor integration |

```bash
# Verify the CLI is available
npx manifest --help
```

The CLI provides four main commands:

```bash
# Interactive project setup — creates manifest.config.yaml
manifest init

# Compile .manifest source to IR
manifest compile modules/recipe.manifest --output ir/

# Generate code from IR using a projection
manifest generate ir/recipe.ir.json --projection nextjs --output app/api/

# Compile + generate in one step (recommended after manifest init)
manifest build modules/recipe.manifest
```

The `manifest` CLI ships **inside** `@angriff36/manifest` — there is no separate CLI package to install. Once `@angriff36/manifest` is a dependency, the `manifest` binary is available via `npx manifest` or your package manager's script runner.

<Tip>
  Run `manifest init` once at the start of a project. It creates `manifest.config.yaml` with your
  framework, auth provider, import paths, and output directory. After that, `manifest build` reads
  those defaults and you only need to pass flags when overriding them.
</Tip>

## Verifying your install

Run the following to confirm the package resolves correctly and the CLI responds:

```bash
# Check CLI
npx manifest --help

# Smoke-test the compiler from a script
node --input-type=module <<'EOF'
import { compileToIR } from '@angriff36/manifest/ir-compiler';
const { ir, diagnostics } = await compileToIR('entity Ping { property id: string }');
console.log('IR version:', ir?.version ?? 'compiled OK');
EOF
```

## Windows note

On Windows with pnpm, the `manifest` shim has historically exited with code `0` and no output on some setups. If you hit this, invoke the CLI directly:

```bash
pnpm exec manifest --help
# or run the bundled binary directly:
node .\node_modules\@angriff36\manifest\packages\cli\dist\index.js --help
```

If you are on a current release this should not occur; keeping `@angriff36/manifest` up to date is the simplest fix.


---

## dx proof kit

URL: https://manifest-b1e8623f.mintlify.app/integration/dx-proof-kit

---
title: 'DX Proof Kit'
sidebarTitle: 'DX Proof Kit'
description: 'Emit capability catalogs, proof registries, and integration guards from compiled Manifest programs — plus optional Convex test harness helpers.'
---

> **Verified (2026-07-16):** Stable subpaths `@angriff36/manifest/proof-kit` and
> `@angriff36/manifest/proof-kit/convex-test` ship in **`@angriff36/manifest@3.6.13`**
> (`package.json` SoT). Exact pin required (no `^`). Full guide:
> [`docs/guides/dx-proof-kit.md`](https://github.com/Angriff36/Manifest/blob/main/docs/guides/dx-proof-kit.md).

Prove that generated applications stay complete — without scraping TypeScript or
maintaining parallel entity inventories by hand.

## Purpose

The DX Proof Kit turns the compiled program into:

1. A **capability catalog** (entities, commands, hooks, events, reactions, proof status)
2. A **proof registry** with CI validation (`runtime_proven` requires a real test file)
3. An **integration guard** engine (file + line violations for feature-root bypasses)
4. Optional **Convex test harness** helpers (apps inject `convex-test`)

## Smallest valid example

```typescript
import {
  emitCapabilityCatalog,
  emitProofRegistry,
  validateProofRegistry,
} from '@angriff36/manifest/proof-kit';

const catalog = emitCapabilityCatalog(ir, {
  entityFilter: ['IngredientDemand', 'PurchaseNeed'],
  versions: {
    manifestVersion: '3.6.13',
    projection: 'convex',
    preset: { id: 'convex-application', version: '1.3.4' },
  },
});

const registry = emitProofRegistry(ir, {
  entityFilter: ['IngredientDemand', 'PurchaseNeed'],
  versions: catalog.versions,
  testBindings: [
    {
      proofId: 'IngredientDemandConfirmed->PurchaseNeed.create',
      structuralTest: 'tests/event-reaction-projection.test.ts',
      runtimeTest: 'tests/proofs/ingredient-demand-confirm.runtime.test.ts',
    },
  ],
});

const issues = validateProofRegistry(registry, {
  rootDir: process.cwd(),
  catalog,
  installedManifestVersion: '3.6.13',
});
```

## Observable effect

- Committed `capability-catalog.json` / `proof-registry.json` stay aligned with IR
- CI fails on version drift, missing runtime tests, or handwritten `runtime_proven`
- Feature guards report exact file + line for raw Convex / table bypasses

## Package surface

| Subpath | Role |
| --- | --- |
| `@angriff36/manifest/proof-kit` | Catalog, registry, validator, guard — **no** `convex-test` required |
| `@angriff36/manifest/proof-kit/convex-test` | Optional harness; app injects `convexTest` |

Optional peers: `convex` (`>=1.32.0`), `convex-test` (`>=0.0.43`). Install
`convex-test` as a **devDependency** in the application (not required to import
the core proof-kit).

## Use this when / Do not use this when

**Use when** you ship a Convex Manifest app and need CI to prove reactions and
commands stay wired and tested.

**Do not use when** you need to change language semantics — update the spec and
conformance first. The proof kit only observes the compiled program.

Manifest does **not** own product decisions (for example unresolved receipt→stock).
Those stay `blocked_by_product_decision` or `intentionally_unavailable` in the
application registry.

## Related

- [Embedded Runtime](/integration/embedded-runtime)
- [Projections](/integration/projections)
- Ownership boundary:
  [`docs/internal/plans/2026-07-16-dx-proof-kit-boundary.md`](https://github.com/Angriff36/Manifest/blob/main/docs/internal/plans/2026-07-16-dx-proof-kit-boundary.md)
- Stable exports: [`docs/spec/sdk-stability.md`](https://github.com/Angriff36/Manifest/blob/main/docs/spec/sdk-stability.md)

## Diagnostics

| Code | Meaning |
| --- | --- |
| `RUNTIME_PROOF_MISSING_TEST` | `runtime_proven` but test file missing |
| `HANDWRITTEN_RUNTIME_CLAIM` | `runtime_proven` without a `runtimeTest` path |
| `VERSION_MISMATCH` | Registry version ≠ installed `@angriff36/manifest` |
| `PRESET_MISMATCH` | Registry preset ≠ app `manifestPreset` |


---

## embedded runtime

URL: https://manifest-b1e8623f.mintlify.app/integration/embedded-runtime

---
title: 'Embed the Manifest runtime in your handlers'
sidebarTitle: 'Embedded Runtime'
description: 'Instantiate RuntimeEngine in your own handlers to control command orchestration, event routing, and response shaping with full Manifest semantics enforced.'
---

`runCommand(commandName, input, options?)` matches
`RuntimeEngine` in `@angriff36/manifest` **3.6.41**. Execution order includes rate limits before
policies.

The Embedded Runtime pattern gives you direct access to `RuntimeEngine` in your own handlers. Use it when you need custom orchestration around command execution, a bespoke event-handling pipeline, or precise control over how `CommandResult` maps to your API response format.

## When to use this pattern

Reach for the Embedded Runtime when:

- You need to compose multiple commands in a single request.
- You want to publish emitted events to a queue, outbox, or tracing system using your own logic.
- You need custom response shaping that does not match generated route conventions.
- You are writing a framework adapter for a platform that does not have a Manifest projection yet.

For straightforward CRUD routes in Next.js, consider the [Projections pattern](/integration/projections) instead — it generates the boilerplate for you.

## Minimal flow

1. Compile your `.manifest` source to IR, or load a pre-built IR artifact.
2. Construct `RuntimeEngine` with the IR and a runtime context object.
3. Call `runCommand` for each mutation.
4. Inspect `CommandResult` and handle emitted events in your application code.

## Instantiating RuntimeEngine

```typescript
import { compileToIR } from '@angriff36/manifest/ir-compiler';
import { RuntimeEngine } from '@angriff36/manifest';

const source = await fs.readFile('invoice.manifest', 'utf-8');
const { ir, diagnostics } = await compileToIR(source);

if (!ir) {
  throw new Error('Compilation failed');
}

const runtime = new RuntimeEngine(ir, {
  user: { id: 'user-1', role: 'admin' },
  requestId: 'req-abc',
});
```

The second argument is the **runtime context**. If your guards or policies reference `user` or `context` bindings, you must supply those fields here. Omitting required context is a valid execution failure — the runtime will not inject permissive defaults.

## Calling runCommand

```typescript
const result = await runtime.runCommand(
  'approve',
  { reason: 'validated' },
  { entityName: 'Invoice', instanceId: 'inv-1' },
);
```

`runCommand` takes three arguments:

1. **Command name** — the name of the command as declared in your `.manifest` file.
2. **Input** — the command's input parameters as a plain object.
3. **Options** — an object with `entityName` and `instanceId`.

The runtime executes in this fixed order on every call:

1. Build evaluation context (`self`, `this`, params, runtime context)
2. Evaluate rate limits — per-user/per-tenant/global; returns `rateLimitExceeded` immediately if exceeded
3. Evaluate applicable policies — denies halt execution immediately
4. Evaluate command constraints — `block`-severity violations halt execution
5. Evaluate guards in declared order — the first falsey guard halts execution
6. Execute actions in declared order (`mutate`, `emit`, `compute`)
7. Emit declared events
8. Return `CommandResult`

## Handling CommandResult

`CommandResult` has a `success` boolean. When `success` is `false`, inspect the failure reason:

```typescript
const result = await runtime.runCommand(
  'approve',
  { reason: 'validated' },
  { entityName: 'Invoice', instanceId: 'inv-1' },
);

if (result.success) {
  // Command executed; access emitted events
  console.log('Emitted events:', result.emittedEvents);
} else {
  // Surface diagnostics — do not alter or suppress them.
  if (result.guardFailure) {
    // Guard failed: result.guardFailure contains the failing guard index and expression
    console.error('Guard failed:', result.guardFailure);
  }

  if (result.policyDenial) {
    // A policy denied execution
    console.error('Policy denied:', result.policyDenial);
  }

  console.error('Error:', result.error);
}
```

<Note>
  Diagnostics explain failures; they never compensate for them. Do not catch `CommandResult`
  failures and silently succeed — surface the failure to the caller with an appropriate HTTP status
  code.
</Note>

## Handling events with onEvent

Use `onEvent` to register a listener before calling `runCommand`. The listener receives events in emission order as the command executes.

```typescript
const unsubscribe = runtime.onEvent((event) => {
  console.log(event.name, event.channel, event.payload);

  // Publish to a queue, write to an outbox, send a webhook, etc.
});

try {
  await runtime.runCommand('approve', {}, { entityName: 'Invoice', instanceId: 'inv-1' });
} finally {
  unsubscribe();
}
```

`onEvent` returns an unsubscribe function. Call it in a `finally` block to avoid listener leaks across requests.

## Request identity hardening

When `runCommand` is called from an HTTP handler, **strip all client-supplied identity fields from the request body** before passing input to the runtime. A client can supply arbitrary values for `id`, `userId`, `tenantId`, `orgId`, or `user`. Passing those values directly enables parameter tampering and identity spoofing.

Derive identity exclusively from:

- Your auth provider (e.g., Clerk's `auth()`, NextAuth's `getServerSession`)
- URL path parameters

Then inject those authoritative values into the command input:

```typescript
export async function POST(request: NextRequest, { params }: { params: { id: string } }) {
  // 1. Resolve identity from trusted server-side sources
  const { userId } = await auth();
  if (!userId) {
    return Response.json({ error: 'Unauthorized' }, { status: 401 });
  }
  const entityId = params.id; // from the URL path

  // 2. Parse the body
  const body = await request.json();

  // 3. Strip any identity fields the client may have sent
  const {
    id: _id,
    userId: _userId,
    tenantId: _tenantId,
    orgId: _orgId,
    user: _user,
    ...safeInput
  } = body;

  // 4. Build runtime with authoritative context
  const runtime = new RuntimeEngine(ir, {
    user: { id: userId, role: 'admin' },
  });

  // 5. Execute with safe input + injected identity
  const result = await runtime.runCommand(
    'approve',
    { ...safeInput, entityId },
    { entityName: 'Invoice', instanceId: entityId },
  );

  if (!result.success) {
    return Response.json({ error: result.error }, { status: 422 });
  }

  return Response.json({ success: true, events: result.emittedEvents });
}
```

<Warning>
  Preserving non-identity business fields (such as `stationId` or `reason`) from the request body is
  correct and intended. Only strip the identity fields listed above.
</Warning>

## Deterministic options for testing

In tests, pass `generateId` and `now` to `RuntimeEngine` to make IDs and timestamps deterministic. This is required for stable conformance-style assertions.

```typescript
let idCounter = 0;

const runtime = new RuntimeEngine(
  ir,
  {
    user: { id: 'test-user', role: 'admin' },
  },
  {
    generateId: () => `test-id-${++idCounter}`,
    now: () => new Date('2026-01-01T00:00:00Z'),
  },
);

const result = await runtime.runCommand(
  'create',
  { title: 'Test item' },
  { entityName: 'Todo', instanceId: 'test-id-1' },
);
// result.instance.id is always 'test-id-1' — stable across test runs
```

## What not to do

- **Do not bypass `runCommand` for mutations.** Calling your database directly from a handler that is supposed to enforce Manifest guards, policies, or events silently removes that enforcement.
- **Do not reorder or short-circuit the execution pipeline.** The spec-required order is: rate-limit → policies → constraints → guards → actions → emits. Skipping any step silently removes the enforcement it provides.
- **Do not inject permissive context defaults to force success.** If a guard requires `user.role` and you have not provided it, the failure is correct. Surface it.
- **Do not suppress diagnostics.** Diagnostics are the primary debugging signal. Swallowing them leaves callers without actionable error information.


---

## manifest.config.yaml

URL: https://manifest-b1e8623f.mintlify.app/integration/nextjs

---
title: 'Use Manifest with Next.js App Router'
sidebarTitle: 'Next.js'
description: 'End-to-end guide: install Manifest, write a .manifest file, generate App Router routes, add Clerk auth, enable tenant filtering, and handle emitted events.'
---

This guide walks you through adding Manifest to an existing Next.js App Router project. By the end, you will have a `.manifest` domain model, generated route handlers for reads and writes, and Clerk auth wired into the identity pipeline.

## Prerequisites

- Next.js 13+ with the App Router
-
- An existing Next.js project (or `npx create-next-app@latest`)

**Node.js 20 or later** (`package.json` `engines.node` is `>=20`). Package pin `@angriff36/manifest` **3.6.41**.

## Workflow

<Steps>
  <Step title="Install the package">
    Add `@angriff36/manifest` to your project:

    ```bash
    npm install @angriff36/manifest
    ```

  </Step>

  <Step title="Write your .manifest file">
    Create a file describing your domain model. This example models a `Recipe` entity with a `publishRecipe` command (`publish` itself is a reserved word in Manifest and cannot be used as a command name):

    ```manifest
    entity Recipe {
      property required id: string
      property required name: string
      property category: string?
      property status: string = "draft"
      property authorId: string

      command publishRecipe() {
        guard self.status == "draft"
        guard user.id == self.authorId
        mutate status = "published"
        emit RecipePublished
      }

      policy canPublish execute: user.role in ["author", "editor"]
    }

    store Recipe in memory

    event RecipePublished: "kitchen.recipe.published" {
      recipeId: string
      authorId: string
    }
    ```

    Save this as `recipe.manifest` in your project root.

  </Step>

  <Step title="Generate routes">
    Use the projection CLI to generate the route files. Run these commands from your project root:

    ```bash
    # Compile the .manifest file to IR
    manifest compile recipe.manifest --output ir/

    # Generate all surfaces from the compiled IR
    manifest generate ir/recipe.ir.json \
      --auth clerk \
      --output .

    # Or generate specific surfaces individually
    manifest generate ir/recipe.ir.json --surface types --output .
    manifest generate ir/recipe.ir.json --surface client --output .
    ```

    `--output` is a directory; each artifact carries its own path relative to it. With the default surface (`all`), you get:

    | File | What it is |
    |------|------------|
    | `app/api/recipe/list/route.ts` | GET list route for `Recipe` |
    | `app/api/recipe/[id]/route.ts` | GET detail route for `Recipe` |
    | `app/api/manifest/[entity]/commands/[command]/route.ts` | The **command dispatcher** — the single canonical write route for every command |
    | `src/types/manifest-generated.ts` | TypeScript types |
    | `src/lib/manifest-client.ts` | Client SDK |

    <Note>
      Per-command concrete routes (e.g. `app/api/recipe/publish-recipe/route.ts`) are **not** generated by default — all writes go through the dispatcher. Opt in with `concreteCommandRoutes.enabled: true` in `manifest.config.yaml` if you need them for legacy callers.
    </Note>

    <Note>
      For repeatable builds, declare your sources and projections in `manifest.config.yaml` and run `manifest compile --all` followed by `manifest generate --all` — see the monorepo section below.
    </Note>

  </Step>

  <Step title="Review the generated read route">
    The generated GET handler uses a direct database query — it does not call `RuntimeEngine`. This is intentional: read policies are not enforced at the query level by default, and direct queries are more efficient.

    ```typescript
    // app/api/recipe/list/route.ts — auto-generated, illustrative (trimmed)
    import type { NextRequest } from "next/server";
    import { database } from "@/lib/database";
    import { manifestErrorResponse, manifestSuccessResponse } from "@/lib/manifest-response";
    import { auth } from "@clerk/nextjs";

    export async function GET(request: NextRequest) {
      try {
        const { orgId, userId } = await auth();
        if (!(userId && orgId)) {
          return manifestErrorResponse({ error: "Unauthorized", diagnostics: [] }, 401);
        }

        // Direct Prisma query — bypasses runtime deliberately
        const recipes = await database.recipe.findMany({
          orderBy: {
            id: "desc",
          },
        });

        return manifestSuccessResponse({ recipes });
      } catch (error) {
        console.error("Error fetching recipes:", error);
        return manifestErrorResponse(
          { error: "Internal server error", diagnostics: [] },
          500,
        );
      }
    }
    ```

    Tenant and soft-delete `where` filters are **opt-in** (`includeTenantFilter` and `includeSoftDeleteFilter` both default to `false`) — see the multi-tenant section below for how to enable them.

  </Step>

  <Step title="Review the generated command dispatcher">
    All writes flow through **one** generated route — the dispatcher at `app/api/manifest/[entity]/commands/[command]/route.ts`. It resolves the entity and command from the URL at request time and **must** call `RuntimeEngine.runCommand`: guards, policies, constraints, and event emissions only run through the runtime.

    Call it with the entity and command names from your `.manifest` file (both segments are case-sensitive):

    ```bash
    curl -X POST http://localhost:3000/api/manifest/Recipe/commands/publishRecipe \
      -H "Content-Type: application/json" \
      -d '{ "id": "recipe-1" }'
    ```

    Identity comes from Clerk on the server — the dispatcher builds the runtime context from the authenticated `userId`, never from the request body. The body's `instanceId` (or `id`) selects which instance the command runs against.

    ```typescript
    // app/api/manifest/[entity]/commands/[command]/route.ts — auto-generated, illustrative (trimmed)
    import type { NextRequest } from "next/server";
    import { manifestErrorResponse, manifestSuccessResponse, normalizeCommandResult } from "@/lib/manifest-response";
    import { createManifestRuntime } from "@/lib/manifest-runtime";
    import { auth } from "@clerk/nextjs";

    interface DispatcherContext {
      params: Promise<{ entity: string; command: string }>;
    }

    export async function POST(request: NextRequest, ctx: DispatcherContext) {
      try {
        const { orgId, userId } = await auth();
        if (!(userId && orgId)) {
          return manifestErrorResponse({ error: "Unauthorized", diagnostics: [] }, 401);
        }

        const body = await request.json();
        const { entity, command } = await ctx.params;

        if (!entity || !command) {
          return manifestErrorResponse("Missing entity or command in route", 400);
        }

        const instanceId = typeof body?.instanceId === "string"
          ? body.instanceId
          : typeof body?.id === "string"
            ? body.id
            : undefined;

        const runtime = await createManifestRuntime({
          actorId: userId,
          requestId: request.headers.get("x-request-id") ?? undefined,
          source: "route",
          user: { id: userId, tenantId: "__no_tenant__" },
        });

        const result = await runtime.runCommand(command, body, {
          entityName: entity,
          instanceId,
        });

        const normalized = normalizeCommandResult(entity, command, result);

        if (!normalized.success) {
          const firstDiagnostic = normalized.diagnostics?.[0];
          const status = firstDiagnostic?.kind === "policy_denial" ? 403
            : firstDiagnostic?.kind === "guard_failure" ? 422
            : firstDiagnostic?.kind === "constraint_block" ? 422
            : 400;
          return manifestErrorResponse({ error: normalized.error, diagnostics: normalized.diagnostics }, status);
        }

        return manifestSuccessResponse({ data: normalized.data, events: normalized.events, diagnostics: normalized.diagnostics });
      } catch (error) {
        // ... auth errors map to 401; everything else to 500
      }
    }
    ```

    Runtime construction is delegated to `createManifestRuntime` from `@/lib/manifest-runtime` — a module the generator emits for you (the `companions` surface, on by default) that embeds the compiled IR and constructs the `RuntimeEngine` with the given context. Regenerate it after every compile, or set `emitCompanions: false` and maintain your own — for example to enrich the `user` object with fields your guards and policies read (e.g. `role`).

  </Step>

  <Step title="Wire up Clerk auth">
    If you chose `--auth clerk`, install the Clerk SDK and configure your middleware:

    ```bash
    npm install @clerk/nextjs
    ```

    Add `clerkMiddleware` to `middleware.ts`:

    ```typescript
    import { clerkMiddleware } from '@clerk/nextjs/server';

    export default clerkMiddleware();

    export const config = {
      matcher: ['/((?!_next|[^?]*\\.(?:html?|css|js(?!on)|jpe?g|webp|png|gif|svg|ttf|woff2?|ico|csv|docx?|xlsx?|zip|webmanifest)).*)', '/(api|trpc)(.*)'],
    };
    ```

    Set your environment variables in `.env.local`:

    ```bash
    NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_...
    CLERK_SECRET_KEY=sk_...
    ```

    The generated routes call `auth()` imported from `@clerk/nextjs` to resolve `userId` (and `orgId`). Use the `authImportPath` option to import it from somewhere else (e.g. `@clerk/nextjs/server`). No additional configuration is needed in the route files.

  </Step>
</Steps>

## Multi-tenant setup

Tenant filtering is **opt-in**: `includeTenantFilter` defaults to `false`, so generated routes contain no tenant logic until you enable it. Turn it on in `manifest.config.yaml` (use `tenantIdProperty` to filter on a different column, e.g. `orgId`):

```yaml
# manifest.config.yaml
projections:
  nextjs:
    output: .
    options:
      authProvider: clerk
      includeTenantFilter: true
      tenantIdProperty: tenantId
      tenantProvider:
        importPath: '@/lib/tenant'
        functionName: 'getTenantIdForOrg'
        lookupKey: 'orgId' # or "userId"
```

Then regenerate with `manifest generate --all`.

When enabled, every generated route resolves the tenant through your `tenantProvider` function before touching data:

```typescript
// Emitted into generated routes when includeTenantFilter is true
const tenantId = await getTenantIdForOrg(orgId);

if (!tenantId) {
  return manifestErrorResponse({ error: 'Tenant not found', diagnostics: [] }, 400);
}

const recipes = await database.recipe.findMany({
  where: { tenantId },
  orderBy: { id: 'desc' },
});
```

You implement the provider function (shape: `(id: string) => Promise<string | null>`) — for example, backed by a `userTenantMapping` table keyed by `userId` if you set `lookupKey: "userId"`.

## Workspace / monorepo setup

In a monorepo, it is common to compile the IR once and share it across packages. A typical layout:

```
apps/
  web/ # Next.js app — consumes generated routes and client SDK
packages/
  domain/ # recipe.manifest, build script, generated IR artifact
  api-types/ # generated ts.types output, shared across apps
```

In `packages/domain`, declare the sources and projection targets in `manifest.config.yaml` instead of writing a build script by hand:

```yaml
# packages/domain/manifest.config.yaml
src: '*.manifest'
output: 'ir/'

projections:
  nextjs:
    output: '../../apps/web'
    options:
      authProvider: clerk
      # Redirect shared artifacts into the types package
      # (path is relative to the projection output above).
      paths:
        typesFile: '../../packages/api-types/src/recipe.ts'
```

Then the whole pipeline is two config-driven commands:

```bash
cd packages/domain
manifest compile --all # compiles every source into ir/merged.ir.json
manifest generate --all # runs every projection declared in the config
```

Routes land in `apps/web/app/api/`, the dispatcher in `apps/web/app/api/manifest/[entity]/commands/[command]/route.ts`, and types in `packages/api-types/src/recipe.ts`.

In `apps/web`, import the compiled IR directly:

```typescript
// apps/web/lib/manifest-ir.ts
import ir from '@repo/domain/ir/merged.ir.json';
export { ir };
```

## Listening to events in route handlers

Register an `onEvent` listener before calling `runCommand` to tap into emitted events. This is useful for publishing to an outbox or triggering webhooks:

```typescript
const runtime = new RuntimeEngine(ir, {
  user: { id: userId, role: 'author' },
});

const unsubscribe = runtime.onEvent((event) => {
  console.log(event.name, event.channel, event.payload);
  // Enqueue to your outbox, publish to Pub/Sub, etc.
});

try {
  const result = await runtime.runCommand(
    'publishRecipe',
    {},
    { entityName: 'Recipe', instanceId: params.id },
  );

  if (!result.success) {
    return manifestErrorResponse(result.error ?? 'Command failed', 422);
  }

  return manifestSuccessResponse({ events: result.emittedEvents });
} finally {
  unsubscribe();
}
```

## Realtime subscriptions (SSE)

Entities flagged with the `realtime` modifier unlock three additional surfaces: an SSE subscription route (`nextjs.subscribe`), a typed auto-reconnecting React hook (`nextjs.subscriptionHook`), and a shared singleton runtime accessor (`nextjs.sharedRuntime`) that lets command routes and subscriptions observe the same event stream:

```manifest
entity Recipe {
  realtime
  property required id: string
  // ...
}
```

```typescript
const projection = new NextJsProjection();
const sseRoute = projection.generate(ir, { surface: 'nextjs.subscribe', entity: 'Recipe' });
const hook = projection.generate(ir, { surface: 'nextjs.subscriptionHook', entity: 'Recipe' });
const shared = projection.generate(ir, { surface: 'nextjs.sharedRuntime' });
```

When any entity is realtime, generated command routes (inline mode) automatically use the shared runtime via `getSharedRuntime()` instead of constructing one per request, so subscribers see events emitted by commands. This requires a long-lived, single-instance server. See [Realtime Subscriptions](/extensibility/realtime-subscriptions) for the full contract.

## Common issues

<AccordionGroup>
  <Accordion title='"Entity not found in IR"'>
    Entity names are case-sensitive. Verify that the name you pass to `runCommand` exactly matches the entity name declared in your `.manifest` file.
  </Accordion>

<Accordion title="Guard fails even with the correct user">
  Guards reference `user.*` from the runtime context, not from the request body. The generated
  `@/lib/manifest-runtime` forwards the context it receives; if your guards read fields the
  dispatcher's auth step doesn't supply (e.g. `role`), enrich the context — either in a custom
  runtime module (`emitCompanions: false`) or by adjusting your auth provider wiring. If the guard
  references `user.role` and you did not supply it, the failure is correct behavior.
</Accordion>

<Accordion title="Generated route imports are wrong">
  The generated code uses import paths like `@/lib/database`, `@/lib/manifest-response`, and
  `@/lib/manifest-runtime`. The generator emits these companion modules itself (the `companions`
  surface, included in `--surface all`), so a fresh generate compiles without hand-written files —
  this error usually means the companions were generated into a different directory than your `@/*`
  alias points at, or generation ran with `emitCompanions: false`. Custom `databaseImportPath`,
  `responseImportPath`, and `runtimeImportPath` options relocate both the imports and the emitted
  modules; paths pointing at npm packages are left to you.
</Accordion>

<Accordion title="Routes don't match my react-query hooks">
  Both projections derive each entity's default URL segment from its name, and the casing must
  agree. Pass the same `routeCasing` option to both generators: `'lowercase'` (default — `PrepTask`
  → `preptask`), `'kebab-case'` (`prep-task`), `'snake_case'` (`prep_task`), or `'preserve'`
  (`PrepTask`). Explicit `routeSegments` / `entityRoutes` overrides still take precedence. Use
  `dateSerialization: 'iso-string'` (default `'date'`) when `date`/`datetime` fields cross the wire
  as ISO-8601 strings rather than `Date` instances.
</Accordion>

  <Accordion title="Tenant filtering returns no results">
    Tenant filtering is off by default (`includeTenantFilter: false`). If you enabled it and get no results, confirm that your database rows have the tenant column populated and that your `tenantProvider` function returns the correct value. To rule the filter out during development, remove `includeTenantFilter: true` and regenerate.
  </Accordion>
</AccordionGroup>


---

## overview

URL: https://manifest-b1e8623f.mintlify.app/integration/overview

---
title: 'Integrating Manifest into your application'
sidebarTitle: 'Integration overview'
description: 'Choose between Projections and the Embedded Runtime to integrate Manifest. Learn when to use each pattern and how to keep identity fields safe.'
---

Manifest gives you two distinct integration patterns: **Projections**, which generate platform-specific code from your compiled IR, and the **Embedded Runtime**, which lets you drive command execution directly inside your own handlers. Most production applications use both.

This page explains the boundary between them, how to decide which to reach for first, and a security rule that applies to both.

## The two patterns

<Tabs>
  <Tab title="Projections">
    Projections are a **code-generation tool**, not a change to language semantics. They read your compiled Intermediate Representation (IR) and emit artifacts—Next.js App Router routes, TypeScript types, and a client SDK—that you can drop straight into your project.

    ```bash
    # Compile + generate all surfaces for your .manifest file
    manifest build recipe.manifest --projection nextjs --code-output app/api/

    # Or use the CLI to generate individual surfaces from compiled IR
    manifest generate ir/recipe.ir.json --surface route --output app/api/
    manifest generate ir/recipe.ir.json --surface command --output app/api/
    ```

    > The Next.js projection registers **twelve** surfaces
    > (`nextjs.route`, `nextjs.detail`, `nextjs.command`, `nextjs.dispatcher`,
    > `nextjs.subscribe`, `nextjs.subscriptionHook`, `nextjs.sharedRuntime`, `nextjs.schedule`,
    > `nextjs.webhook`, `nextjs.companions`, `ts.types`, `ts.client`). Default `--surface all`
    > writes go through the **command dispatcher** (`nextjs.dispatcher` →
    > `app/api/manifest/[entity]/commands/[command]/route.ts`); per-command concrete routes
    > (`nextjs.command`) are opt-in via `concreteCommandRoutes.enabled`. Other registered
    > projections (`prisma`, `convex`, `zod`, …) are selected with `-p` / `manifest.config.yaml`.

    **Read operations** in generated routes use direct database queries and intentionally bypass the runtime for performance. **Write operations** always flow through `RuntimeEngine.runCommand` so that guards, policies, constraints, and event emissions run in the order the spec requires.

  </Tab>
  <Tab title="Embedded Runtime">
    The Embedded Runtime lets you instantiate `RuntimeEngine` directly in your own handlers and call `runCommand` yourself. You control request parsing, response shaping, event routing, and any custom side-effect pipeline.

    ```typescript
    import { RuntimeEngine } from '@angriff36/manifest';

    const runtime = new RuntimeEngine(ir, {
      user: { id: 'user-1', role: 'admin' },
      requestId: 'req-1',
    });

    const result = await runtime.runCommand(
      'approve',
      { reason: 'validated' },
      { entityName: 'Invoice', instanceId: 'inv-1' }
    );

    if (!result.success) {
      // result.error, result.guardFailure, result.policyDenial
      // Surface these in your response — do not alter execution semantics.
    }
    ```

    The runtime enforces the full execution order on every `runCommand` call:

    1. Build evaluation context (`self`, `this`, params, runtime context)
    2. Evaluate rate limits — returns `rateLimitExceeded` immediately if exceeded
    3. Evaluate applicable policies
    4. Evaluate command constraints — `block`-severity violations halt execution
    5. Evaluate guards in declared order — halts on the first failure
    6. Execute actions in declared order
    7. Emit declared events
    8. Return `CommandResult`

  </Tab>
</Tabs>

## Decision guide

Use **Projections** when:

- You want generated route code that follows Next.js App Router conventions without writing boilerplate by hand.
- Your mutation flow can be expressed as a named Manifest command and you are comfortable with the generated handler structure.

Use the **Embedded Runtime** when:

- You need custom orchestration around command execution — for example, composing multiple commands, injecting computed context, or conditionally routing results.
- You need a custom event-handling pipeline: publishing to a queue, writing to an outbox, or triggering side effects in a specific order.

<Note>
  These patterns are not mutually exclusive. A common setup uses generated routes for
  straightforward CRUD entities and the Embedded Runtime for commands that require complex
  orchestration.
</Note>

## Security: never trust client-supplied identity fields

This rule applies regardless of which pattern you use.

When a command is executed from an HTTP handler, the request body is **untrusted**. A client can supply any value for `id`, `userId`, `tenantId`, `orgId`, or `user`. If those values flow unmodified into `runCommand`, a caller can act on behalf of another user or mutate a resource they do not own.

**Always strip client-supplied identity fields and inject authoritative values from your auth context and URL path parameters before calling `runCommand`.**

```typescript
// Correct: derive identity from trusted server-side sources
const { userId } = await auth(); // from your auth provider
const { id: entityId } = params; // from the URL path param

const body = await request.json();

// Strip any identity fields the client may have sent
const {
  id: _id,
  userId: _userId,
  tenantId: _tenantId,
  orgId: _orgId,
  user: _user,
  ...safeInput
} = body;

// Inject authoritative identity before execution
const result = await runtime.runCommand(
  'update',
  { ...safeInput, userId, entityId },
  { entityName: 'Invoice', instanceId: entityId },
);
```

<Warning>
  Do not pass `await request.json()` directly to `runCommand` without stripping identity fields.
  This is the most common source of privilege-escalation bugs in Manifest-backed APIs.
</Warning>

## Next steps

<CardGroup cols={2}>
  <Card title="Projections" icon="wand-sparkles" href="/integration/projections">
    Learn how to use the projection API programmatically and understand the four generated surfaces.
  </Card>
  <Card title="Embedded Runtime" icon="cpu" href="/integration/embedded-runtime">
    Instantiate RuntimeEngine, handle CommandResult variants, and wire up event handlers.
  </Card>
  <Card title="Next.js guide" icon="triangle" href="/integration/nextjs">
    End-to-end walkthrough: install, write a `.manifest` file, generate routes, and add auth.
  </Card>
</CardGroup>


---

## Single source file

URL: https://manifest-b1e8623f.mintlify.app/integration/prisma

---
title: 'Prisma schema projection'
description: "Generate a Prisma schema from Manifest IR. Compile-time only, app-agnostic, with full @relation wiring, decimal defaults for money, and structured diagnostics for everything the projection can't safely emit."
sidebarTitle: 'Prisma'
---

The **Prisma projection** reads compiled Manifest IR and emits a `schema.prisma` artifact. It is compile-time only — it does not connect to a database, run migrations, or know anything about any specific application. All relational concepts (table names, column names, decimal precision, composite indexes, foreign-key field names) live in projection config; Manifest core stays backend-neutral.

The projection ships **inside the main `@angriff36/manifest` package** — no separate install needed. Running `manifest generate -p prisma` works out of the box.

<Note>
  An earlier pre-release explored shipping the projection as `@manifest/projection-prisma`. That
  package was never published. Since v1.0.0 the projection is bundled in `@angriff36/manifest` with
  the same internal separation (`src/manifest/projections/prisma/`) as the Next.js and routes
  projections.
</Note>

For programmatic use, import from the main package:

```ts
import { PrismaProjection } from '@angriff36/manifest/projections/prisma';
```

## Quickstart

A minimal `.manifest` source:

```manifest widget.manifest
entity Widget {
  property required id: string
  property required name: string
  property required qty: int
  property required unitCost: money
}

store Widget in durable
```

A minimal `manifest.config.yaml`:

```yaml manifest.config.yaml
projections:
  prisma:
    output: generated/
    options:
      provider: postgresql
```

Compile and generate:

```bash
manifest compile widget.manifest
manifest generate ir/ -p prisma -s prisma.schema -o generated/
```

Emitted `generated/schema.prisma`:

```prisma
// Auto-generated by @angriff36/manifest
// DO NOT EDIT — regenerate with the projection.

datasource db {
  provider = "postgresql"
}

generator client {
  provider = "prisma-client-js"
}

model Widget {
  id String @id
  name String
  qty Int
  unitCost Decimal @db.Decimal(12, 2)
}
```

Emitted `generated/prisma.config.ts` (Prisma 7+ — URL lives here, not in `schema.prisma`):

```typescript
// Auto-generated by @angriff36/manifest
// Prisma 7+: connection URL lives here, not in schema.prisma.
import { defineConfig } from 'prisma/config';

export default defineConfig({
  datasources: { db: { url: process.env.DATABASE_URL } },
});
```

## What gets emitted, what gets skipped

| IR shape                                                               | Projection behavior                                        |
| ---------------------------------------------------------------------- | ---------------------------------------------------------- |
| `entity X { ... }` + `store X in durable` (or `postgres` / `supabase`) | Emits a Prisma `model`                                     |
| `entity X { ... }` + `store X in memory` (or `localStorage`)           | **Skipped** with `PRISMA_SKIPPED_NON_DURABLE` info         |
| `entity X { ... }` with no `store` declaration                         | **Skipped** with `PRISMA_SKIPPED_NO_STORE` info            |
| `external entity X { ... }` (sets `external: true` in IR)              | **Skipped** with `PRISMA_SKIPPED_EXTERNAL` info            |
| `property` entries                                                     | Emitted as Prisma fields                                   |
| `computed` entries                                                     | **Never emitted** — derived values must not become columns |

Entities are iterated in IR source order; the projection does not re-sort.

A property literally named `id` becomes `@id` **unless**
the entity declares a composite `key [...]`, which emits `@@id([...])` and suppresses
`PRISMA_NO_ID_PROPERTY`. That diagnostic fires only when there is **neither** an `id` property
**nor** a composite key (`src/manifest/projections/prisma/generator.ts`).

## Type mapping

The IR carries `type.name` as an arbitrary string (Manifest's type vocabulary is open). The projection holds the mapping table:

| IR `type.name`                | Prisma scalar                                 |
| ----------------------------- | --------------------------------------------- |
| `string`, `text`, `uuid`      | `String`                                      |
| `boolean`, `bool`             | `Boolean`                                     |
| `int`                         | `Int`                                         |
| `bigint`                      | `BigInt`                                      |
| `float`                       | `Float`                                       |
| `decimal`, `money`            | `Decimal` (with default `@db.Decimal(12, 2)`) |
| `date`, `datetime`, `timestamp` | `DateTime`                                    |
| `time`                        | `DateTime`                                    |
| `duration`                    | `Float`                                       |
| `json`                        | `Json`                                        |
| `bytes`                       | `Bytes`                                       |
| anything else                 | **diagnostic** (`PRISMA_UNKNOWN_TYPE`)        |

The default mapping does **not** include `number`. That is deliberate.

## The `number` ambiguity rule

Manifest's `number` type does not distinguish integers from real numbers from money. Silently mapping it to `Float` is the class of silent rounding bug this project exists to prevent — especially for currency. So the projection refuses to guess:

```manifest
entity Order {
  property required id: string
  property required total: number // ← ambiguous
}
```

The projection emits an error diagnostic:

```
PRISMA_AMBIGUOUS_NUMBER (Order):
  Property 'Order.total' is typed 'number', which is ambiguous
  (Manifest does not distinguish integers from real numbers from money).
  Pick a precise type in the .manifest source: 'int' or 'bigint' for
  counts and ids, 'float' for measurements where rounding is acceptable,
  'money' or 'decimal' for currency and other exact-decimal values.
  Or supply a 'typeMappings.Order.total' override.
```

The ambiguous column is **skipped** in the output. Fix the source:

```manifest
entity Order {
  property required id: string
  property required total: money // ← exact-decimal, picks up @db.Decimal(12, 2)
}
```

<Note>
  The escape hatch is the projection config's `typeMappings` — supply an explicit Prisma scalar
  there and the diagnostic disappears. Useful when migrating a legacy `.manifest` file that already
  used `number` for a field whose precision intent is recorded elsewhere.
</Note>

## Money and decimal precision

`money` and `decimal` both resolve to Prisma `Decimal`. Either gets a default `@db.Decimal(12, 2)` — the conservative money default (up to 9,999,999,999.99 with cent precision).

```manifest
entity Order {
  property required total: money // → Decimal @db.Decimal(12, 2)
  property required fxRate: decimal // → Decimal @db.Decimal(12, 2)
}
```

Override per-property via `precision`:

```yaml
projections:
  prisma:
    options:
      precision:
        Order:
          fxRate: { precision: 18, scale: 8 } # → @db.Decimal(18, 8)
```

The rule is keyed on the **resolved Prisma scalar**, not the IR type name. A property routed to `Decimal` via `typeMappings` picks up the same default precision.

## Relationships

All four IR relationship kinds emit real Prisma `@relation`:

| IR                  | Emitted Prisma                                                           | Notes                                     |
| ------------------- | ------------------------------------------------------------------------ | ----------------------------------------- |
| `hasMany name: T`   | `name T[]`                                                               | Parent side of 1:N                        |
| `hasOne name: T`    | `name T?`                                                                | Parent side of 1:1                        |
| `belongsTo name: T` | `nameId String` + `name T @relation(fields: [nameId], references: [id])` | Child side; FK type follows target's `id` |
| `ref name: T`       | same shape as `belongsTo`                                                | "Loose" — no missing-backside warning     |

### 1:1 vs 1:N

The projection detects the cardinality from the opposite side:

```manifest
entity User {
  property required id: string
  hasOne profile: Profile // 1:1 — declares opposite as hasOne
}

entity Profile {
  property required id: string
  belongsTo user: User // child side → FK gets @unique
}
```

Emits:

```prisma
model User {
  id String @id
  profile Profile?
}

model Profile {
  id String @id
  userId String @unique // ← @unique because opposite is hasOne
  user User @relation(fields: [userId], references: [id])
}
```

For 1:N (parent side is `hasMany`), the FK is **not** `@unique`.

### Foreign key field name

Default is `${relationshipName}Id` (camelCase). Override at IR level via the `foreignKey` annotation, or at projection-config level via `foreignKeys`:

```yaml
projections:
  prisma:
    options:
      foreignKeys:
        Book:
          author: writerId # → Book.writerId instead of Book.authorId
```

The FK type follows the target entity's `id` property type. A `string` PK gets a `String` FK; an `int` PK gets an `Int` FK. No type mismatches.

### Foreign key columns honor `columnMappings`

FK fields are virtual properties from the consumer's perspective; the existing `columnMappings` knob applies:

```yaml
projections:
  prisma:
    options:
      columnMappings:
        Book:
          authorId: author_id # → authorId String @map("author_id")
```

### `belongsTo` alongside an explicit FK property

If your `.manifest` source declares the FK property explicitly **and** declares the relationship, the projection deduplicates — it does NOT emit a second column with the same name:

```manifest
entity Book {
  property required id: string
  property required authorId: string // ← explicit FK column
  belongsTo author: Author // ← relationship using that column
}
```

Emits:

```prisma
model Book {
  id String @id
  authorId String // ← from the property declaration
  author Author @relation(fields: [authorId], references: [id])
}
```

The relation step sees that `authorId` already exists as a property and reuses it. The FK column line is **not** re-emitted; the `@relation(fields: [authorId], ...)` clause still wires it.

If you supply both an explicit FK property **and** a `foreignKeys` config override that points the relationship at a different column name, the override wins for the relation field — but the explicit property still emits as its own column, so you'll end up with both columns in the model. Pick one approach per relationship.

### Self-relations

A one-sided self-relation triggers `PRISMA_RELATION_MISSING_BACKSIDE`:

```manifest
entity Tree {
  property required id: string
  belongsTo parent: Tree // no `children` declared → warning
}
```

A two-sided self-relation (`parent` + `children` on the same entity) is the textbook case where Prisma requires `@relation("name")` to disambiguate which `belongsTo` matches which `hasMany`. The projection refuses to invent names and emits `PRISMA_RELATION_AMBIGUOUS` for both sides — hand-add the relation names after generation, or refactor to a single relation:

```prisma
model Tree {
  id String @id
  parentId String?
  parent Tree? @relation("TreeHierarchy", fields: [parentId], references: [id])
  children Tree[] @relation("TreeHierarchy")
}
```

### MongoDB provider

When `provider: mongodb`, Prisma's Mongo connector requires the `@id` column to map to `_id` with ObjectId semantics. The projection applies the canonical pattern automatically:

```yaml
projections:
  prisma:
    options:
      provider: mongodb
```

```manifest
entity Order {
  property required id: string
  property required total: money
}
store Order in durable
```

Emits:

```prisma
model Order {
  id String @id @default(auto()) @map("_id") @db.ObjectId
  total Decimal @db.Decimal(12, 2)
}
```

The Mongo-specific attributes are auto-applied only when you have NOT supplied your own `columnMappings.<Entity>.id` or a literal `defaultValue` on the `id` property. If you want a non-ObjectId pattern (e.g. plain string ULIDs), override via `typeMappings.<Entity>.id` to something other than `String` — the projection will skip the ObjectId attribute but still emit `@map("_id")`.

## Configuration options

All per-property options use the nested `Record<EntityName, Record<PropertyName, ...>>` shape. There are no flattened `"Entity.property"` dotted-string keys anywhere in the config.

```yaml
projections:
  prisma:
    # Path hint for the emitted artifact (default: schema.prisma).
    output: generated/schema.prisma

    options:
      # Optional Prisma datasource provider. When set, the projection
      # emits a `datasource` block at the top of the artifact.
      provider: postgresql # one of: postgresql | mysql | sqlite | sqlserver | mongodb | cockroachdb

      # Per-entity table-name override → @@map("...").
      tableMappings:
        Order: orders
        Customer: customers

      # Per-entity, per-property column-name override → @map("...").
      columnMappings:
        Order:
          createdAt: created_at
          updatedAt: updated_at

      # Automatic identifier casing (opt-in). Emits @map/@@map for any name
      # not already in the target case, so you don't hand-write the mappings
      # above for every field. Explicit tableMappings/columnMappings still win.
      naming: snake_case # shorthand: snake_case columns + pluralized snake_case tables
      # Object form:
      # naming:
      # table: snake_case # snake_case | camelCase | PascalCase | preserve
      # column: snake_case # snake_case | camelCase | preserve
      # pluralizeTables: true # Widget → widgets (default true)

      # Per-entity, per-property decimal precision/scale.
      # Applies to any property whose resolved scalar is Decimal.
      precision:
        Order:
          total: { precision: 14, scale: 2 }
          fxRate: { precision: 18, scale: 8 }

      # Per-entity composite/named index definitions → @@index([...]).
      indexes:
        Order:
          - ['customerId', 'createdAt']
          - { fields: ['status'], name: 'order_status_idx' }

      # Per-entity, per-property type override. Bypasses the default
      # IR-type-name → Prisma-scalar mapping. Value is the literal
      # Prisma scalar (e.g. "Int", "BigInt", "Decimal", "DateTime").
      typeMappings:
        Order:
          legacyAmount: Decimal # opt out of PRISMA_AMBIGUOUS_NUMBER for a `number` field

      # Per-entity, per-relationship FK field name override.
      # Overrides default `${relationshipName}Id` and IR `foreignKey` annotation.
      # Value is a plain string (field name) or a structured object with referential actions.
      foreignKeys:
        Order:
          customer: customerRef # → Order.customerRef instead of Order.customerId
          # Structured form — includes referential actions:
          # customer:
          # fields: [customerId]
          # references: [id]
          # onDelete: Cascade
          # onUpdate: NoAction

      # Per-entity, per-property native @db.* attribute (without the @db. prefix).
      # Applied after the Prisma scalar but before @default.
      # Use for types the projection can't infer, or to override the default @db.Decimal(12,2).
      dbAttributes:
        Order:
          amount: Decimal(14, 2) # → @db.Decimal(14, 2)
          externalId: VarChar(36) # → @db.VarChar(36)

      # Per-entity, per-property array of verbatim Prisma field attributes.
      # Appended after type and dbAttributes. Use for @unique, @updatedAt, @default(...), etc.
      fieldAttributes:
        Order:
          code: ['@unique']
          updatedAt: ['@updatedAt', '@default(now())']

      # Environment variable name for the database URL in the emitted prisma.config.ts.
      # Default: DATABASE_URL. Only relevant when `provider` is set.
      urlEnvVar: POSTGRES_CONNECTION_STRING
```

## Automatic casing (the `naming` convention)

By default the projection emits IR names verbatim — to render camelCase IR identifiers as `snake_case` database names you'd hand-write a `columnMappings` entry per field. The `naming` option automates that:

```yaml
projections:
  prisma:
    options:
      naming: snake_case
```

`createdAt` emits `@map("created_at")`, `Widget` emits `@@map("widgets")`. The shorthand `snake_case` expands to `{ table: snake_case, column: snake_case, pluralizeTables: true }`; the object form tunes each axis independently.

**The convention only ever adds `@map`/`@@map`.** The Prisma _model name_ and _field identifiers_ stay as the IR name, so relation `fields`/`references`, `@@id`, `@@unique`, and `@@index` are unaffected — only the physical database name changes, and a `@map`/`@@map` is emitted only when the physical name actually differs (so `id` stays bare):

```prisma
model UserAccount {
  id String @id
  createdAt DateTime @map("created_at")
  displayName String @map("display_name")

  @@map("user_accounts")
}
```

Resolution order per name:

1. explicit `tableMappings` / `columnMappings` (always win — also the **escape hatch** for irregular plurals the built-in pluralizer gets wrong)
2. the `naming` convention
3. the IR name verbatim

A **global** `naming` can also be set once at the top level of `manifest.config.yaml` and is inherited by the Prisma projection; a per-projection `projections.prisma.options.naming` overrides the global default.

## `prisma.config.ts` companion

When `provider` is set, the projection emits **two artifacts** from a single `generate()` call:

| Index          | Path hint          | Content                                                   |
| -------------- | ------------------ | --------------------------------------------------------- |
| `artifacts[0]` | `schema.prisma`    | The full Prisma schema                                    |
| `artifacts[1]` | `prisma.config.ts` | Prisma 7+ config file with `url: process.env.<urlEnvVar>` |

Prisma 7 moved the connection URL out of `schema.prisma` into a `prisma.config.ts` file. The projection handles this automatically so `prisma migrate` works without edits:

```typescript
// prisma.config.ts — emitted automatically when provider is set
import { defineConfig } from 'prisma/config';

export default defineConfig({
  datasources: { db: { url: process.env.DATABASE_URL } },
});
```

Customize the env var name via `urlEnvVar`:

```yaml
projections:
  prisma:
    options:
      provider: postgresql
      urlEnvVar: POSTGRES_CONNECTION_STRING
```

Without `provider`, only `schema.prisma` is emitted (no datasource block, no `prisma.config.ts`). Use this when you manage datasource configuration externally.

## Diagnostics reference

The projection emits structured diagnostics rather than silently doing the wrong thing. Each carries an entity name (where applicable) and a resolution hint. The CLI's `manifest generate` command **exits non-zero** when any error-severity diagnostic is returned, so CI pipelines detect generation failures.

| Code                                        | Severity | Trigger                                                                                                                               | Resolution                                                                                            |
| ------------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------- |
| `PRISMA_AMBIGUOUS_NUMBER`                   | error    | Property typed `number` with no `typeMappings` override                                                                               | Pick `int`/`bigint`/`float`/`money`/`decimal` in source, or supply `typeMappings.<Entity>.<Property>` |
| `PRISMA_UNKNOWN_TYPE`                       | error    | `type.name` not in default mapping and not in `typeMappings`                                                                          | Add a `typeMappings` entry or change the source type                                                  |
| `PRISMA_NO_ID_PROPERTY`                     | info     | Entity has no property named `id`                                                                                                     | Add `property required id: string`                                                                    |
| `PRISMA_SKIPPED_EXTERNAL`                   | info     | Entity marked `external entity`                                                                                                       | Expected — projection intentionally skips external entities                                           |
| `PRISMA_SKIPPED_NO_STORE`                   | info     | Entity has no `store` declaration                                                                                                     | Add `store EntityName in durable` to opt in                                                           |
| `PRISMA_SKIPPED_NON_DURABLE`                | info     | Store target is `memory` or `localStorage`                                                                                            | Flip to `durable` (or `postgres`/`supabase`) to opt in                                                |
| `PRISMA_RELATION_VIA_THROUGH_UNIMPLEMENTED` | info     | `hasMany ... through X` (explicit M2M)                                                                                                | Declare the join entity (`X`) with its own belongsTo relations                                        |
| `PRISMA_RELATION_AMBIGUOUS`                 | info     | Multiple relationships between same entity pair (including two-sided self-relations like `parent` + `children` on the same type)      | Refactor to a single relation, or hand-edit `@relation("name")` after generation                      |
| `PRISMA_RELATION_MISSING_BACKSIDE`          | warning  | One-sided `hasMany` / `hasOne` / `belongsTo` / `ref` — all four emit a full `@relation` that Prisma rejects without the opposite side | Declare the matching relation on the other entity                                                     |
| `PRISMA_RELATION_TARGET_NOT_EMITTED`        | warning  | Relationship targets an entity that is not emitted as a Prisma model (external entity, memory/localStorage store, no store)           | Declare the target as a durable entity, or remove the relationship                                    |

## What the projection deliberately does NOT do

These are not bugs — they're scope boundaries:

- **No database connection.** The projection is pure IR → text. Running migrations is your job (or Prisma's).
- **No `@relation("name")` auto-generation.** Multi-relation pairs require named `@relation` clauses; the projection refuses to invent names. Diagnose, don't guess.
- **No join-entity inference for `through` relationships.** Manifest's `through` annotation declares the join model, but Prisma needs the join entity to be a fully-modeled entity with its own belongsTo declarations. The user provides those.
-

Primary key is still by literal `id` **or** a
composite `key [...]` declaration (`@@id`). There is still no grammar `@id` modifier.

- **No app coupling.** No defaults reference tenant columns, soft-delete columns, auth fields, or any other application-specific concept.

## CLI usage

The projection registers automatically; `manifest generate -p prisma` works out of the box:

```bash
# Single source file
manifest compile orders.manifest
manifest generate ir/ -p prisma -s prisma.schema -o generated/

# One-step build
manifest build orders.manifest -p prisma -s prisma.schema --code-output generated/
```

The CLI's `--projection` flag accepts `prisma` alongside `nextjs`. The surface name `prisma.schema` is the projection's single emission surface.

## Programmatic usage

For build scripts that need direct control:

```typescript
import { PrismaProjection } from '@angriff36/manifest/projections/prisma';
import { compileToIR } from '@angriff36/manifest/ir-compiler';
import fs from 'fs/promises';

const source = await fs.readFile('orders.manifest', 'utf8');
const { ir, diagnostics: compileDiags } = await compileToIR(source);

if (!ir) {
  console.error('Compilation failed:', compileDiags);
  process.exit(1);
}

const projection = new PrismaProjection();
const result = projection.generate(ir, {
  surface: 'prisma.schema',
  options: {
    provider: 'postgresql',
    tableMappings: { Order: 'orders' },
    precision: { Order: { total: { precision: 14, scale: 2 } } },
  },
});

for (const d of result.diagnostics) {
  console[d.severity === 'error' ? 'error' : 'log'](`[${d.code}] ${d.message}`);
}

const errors = result.diagnostics.filter((d) => d.severity === 'error');
if (errors.length > 0) process.exit(1);

for (const artifact of result.artifacts) {
  await fs.writeFile(artifact.pathHint!, artifact.code, 'utf8');
}
```

## Related

- [Stores](/language/stores) — the `durable` store target the projection emits for
- [Entities](/language/entities) — the `external entity` modifier the projection skips
- [Projections overview](/integration/projections) — projection pattern, contract, and the Next.js projection


---

## Whole-project (config-driven) — preferred

URL: https://manifest-b1e8623f.mintlify.app/integration/projections

---
title: 'Generate API routes with Manifest projections'
sidebarTitle: 'Projections'
description: 'Projections are a code-generation tool that reads compiled Manifest IR and emits Next.js routes, TypeScript types, and a client SDK for your application.'
---

A **projection** reads your compiled Intermediate Representation (IR) and emits platform-specific artifacts. Projections are tooling — they do not redefine runtime semantics. Adding or changing a projection never changes what a Manifest program _means_; it only changes what code gets generated from it.

## Available projections

The table above understated the registry. Shipped projections
include (among others) `nextjs`, `prisma`, `prisma-store`, `convex`, `drizzle`, `kysely`,
`zod`, `openapi`, `graphql`, `express`, `hono`, `remix`, `sveltekit`, `mongoose`, and more — see
`manifest generate -p <name>` / the projections registry. Guides for individual targets live
under `/integration/*` and `/projections/*`.

| Projection | CLI identifier | What it generates                                   | Guide                                         |
| ---------- | -------------- | --------------------------------------------------- | --------------------------------------------- |
| Next.js    | `nextjs`       | App Router routes, dispatcher, TS types, client SDK | [Next.js projection](/integration/nextjs)   |
| Prisma     | `prisma`       | A `schema.prisma` file (models, relations, indexes) | [Prisma projection](/integration/prisma)    |
| Mongoose   | `mongoose`     | Mongoose schema and model definitions for MongoDB   | [Mongoose projection](/projections/mongoose)  |

Projections share a single contract — `ProjectionTarget.generate(ir, request)` returns one or more `ProjectionArtifact`s plus structured diagnostics. The CLI's `--projection` flag selects which projection runs; dispatch is generic (the registry resolves the name), so adding a new projection means registering it, not editing a hardcoded dispatch branch.

## What projections produce

The Next.js projection ships four surfaces:

| Surface            | CLI identifier   | What it generates                                                               |
| ------------------ | ---------------- | ------------------------------------------------------------------------------- |
| Entity read routes | `nextjs.route`   | Next.js App Router GET handlers using direct database queries                   |
| Command routes     | `nextjs.command` | POST/PUT/DELETE handlers that call `RuntimeEngine.runCommand`                   |
| TypeScript types   | `ts.types`       | `interface` declarations derived from your IR entity definitions                |
| Client SDK         | `ts.client`      | Typed fetch wrappers for calling your routes from browser or server-client code |

The Prisma projection ships a single surface:

| Surface | CLI identifier  | What it generates                                                                                                                        |
| ------- | --------------- | ---------------------------------------------------------------------------------------------------------------------------------------- |
| Schema  | `prisma.schema` | One `schema.prisma` file with models, `@relation` wiring, `@db.Decimal` precision, and projection-config-driven `@map`/`@@map`/`@@index` |

## Reads vs. writes

This is the most important rule for projections:

- **Read routes (`nextjs.route`) may bypass the runtime.** Generated GET handlers use direct database queries (Prisma by default). That is a **projection** choice for efficiency — those handlers do not call `getInstance` / `getAllInstances`.
-
- **Write routes (`nextjs.command`) MUST use `RuntimeEngine.runCommand`.** Bypassing `runCommand` for mutations bypasses guard evaluation, policy checks, constraint validation, and event emission. The spec requires these to run in a fixed order on every mutation.

Manifest **semantics** do enforce `read` / `all` policies at the
runtime read gate (`getInstance` / `getAllInstances` — fail closed). Generated Next.js read
routes that query the DB directly skip that gate unless you wire reads through the runtime or
re-implement equivalent checks. Do not confuse projection bypass with language semantics.

<Warning>
  Never replace `RuntimeEngine.runCommand` with direct database writes in a command route. Doing so
  silently strips all semantic enforcement from that operation.
</Warning>

## Using the projection CLI

Prefer **config-driven** `--all` after `manifest init` writes `manifest.config.yaml`. Verified in
`packages/cli` @ package **v3.6.41**: `compile --all`, `generate --all`, `watch --all`, and
`build --all` are registered.

```bash
# Whole-project (config-driven) — preferred
manifest compile --all
manifest generate --all
manifest watch --all
# or one shot:
manifest build --all

# Single-file / single-projection (still valid)
manifest build recipe.manifest --projection nextjs --code-output app/api/
manifest generate ir/recipe.ir.json -p nextjs -s nextjs.route --output app/api/
manifest generate ir/recipe.ir.json -p nextjs -s nextjs.command --output app/api/
manifest generate ir/recipe.ir.json -p nextjs -s ts.types --output .
manifest generate ir/recipe.ir.json -p nextjs -s ts.client --output .
```

Do **not** tell teams to hand-call projection helpers in production
pipelines as the default. Use `manifest compile --all` / `generate --all` / `watch --all` /
`build --all` driven by `manifest.config.yaml`. Surface names are registry ids such as
`nextjs.route`, `nextjs.command`, `ts.types`, `ts.client` (not bare `route` / `types` alone
unless the CLI resolves them for a selected `-p`).

## Using the projection API programmatically

`NextJsProjection` exposes the generic
`generate(ir, request: ProjectionRequest)` contract — there are **no** public
`generateRoute` / `generateTypes` / `generateClient` methods. Prefer the CLI `--all` path;
if you must call the API, use `generate` with a `surface` id.

Struck phantom API (do not use): `generateRoute` / `generateTypes` / `generateClient` —
those methods are not on `ProjectionTarget`.

```typescript
import { compileToIR } from '@angriff36/manifest/ir-compiler';
import { NextJsProjection } from '@angriff36/manifest/projections/nextjs';
import fs from 'fs/promises';

const source = await fs.readFile('recipe.manifest', 'utf-8');
const { ir, diagnostics } = await compileToIR(source);

if (diagnostics.some((d) => d.severity === 'error')) {
  for (const d of diagnostics) console.error(d.message);
  process.exit(1);
}

const projection = new NextJsProjection();
const routeResult = projection.generate(ir, {
  surface: 'nextjs.route',
  entity: 'Recipe',
  options: { authProvider: 'clerk', appDir: 'app/api' },
});
for (const artifact of routeResult.artifacts) {
  await fs.writeFile(artifact.pathHint, artifact.code);
}
```

### Configuration options

Pass projection options on `request.options` to `generate`, not as
a third argument to a phantom `generateRoute` method.

```typescript
const result = projection.generate(ir, {
  surface: 'nextjs.route',
  entity: 'Recipe',
  options: {
    authProvider: 'clerk', // 'clerk' | 'nextauth' | 'custom' | 'none'
    authImportPath: '@/lib/auth',
    databaseImportPath: '@/lib/database',
    responseImportPath: '@/lib/manifest-response',
    includeTenantFilter: true,
    includeSoftDeleteFilter: true,
    tenantIdProperty: 'tenantId',
    deletedAtProperty: 'deletedAt',
    appDir: 'app/api',
    strictMode: true,
  },
});
```

## Auth provider examples

Use `generate(ir, { surface, entity, options })` as below.

<CodeGroup>

```typescript Clerk
const result = projection.generate(ir, {
  surface: 'nextjs.route',
  entity: 'Recipe',
  options: {
    authProvider: 'clerk',
    // Generated import: import { auth } from "@clerk/nextjs/server";
  },
});
```

```typescript NextAuth
const result = projection.generate(ir, {
  surface: 'nextjs.route',
  entity: 'Recipe',
  options: {
    authProvider: 'nextauth',
    // Generated import: import { getServerSession } from "next-auth";
  },
});
```

```typescript Custom
const result = projection.generate(ir, {
  surface: 'nextjs.route',
  entity: 'Recipe',
  options: {
    authProvider: 'custom',
    authImportPath: '@/lib/my-auth',
    // Generated import: import { getUser } from "@/lib/my-auth";
  },
});
```

```typescript None
const result = projection.generate(ir, {
  surface: 'nextjs.route',
  entity: 'Recipe',
  options: {
    authProvider: 'none',
    // Generated: const userId = "anonymous";
  },
});
```

</CodeGroup>

## Tenant and soft-delete filtering

```typescript
// Multi-tenant with soft delete (defaults)
const result = projection.generate(ir, {
  surface: 'nextjs.route',
  entity: 'Recipe',
  options: {
    includeTenantFilter: true,
    tenantIdProperty: 'tenantId',
    includeSoftDeleteFilter: true,
    deletedAtProperty: 'deletedAt',
    // Generated WHERE: { tenantId, deletedAt: null }
  },
});

// Single-tenant, no soft delete
const result2 = projection.generate(ir, {
  surface: 'nextjs.route',
  entity: 'Recipe',
  options: {
    includeTenantFilter: false,
    includeSoftDeleteFilter: false,
    // Generated WHERE: { }
  },
});

// Custom property names
const result3 = projection.generate(ir, {
  surface: 'nextjs.route',
  entity: 'Recipe',
  options: {
    tenantIdProperty: 'orgId',
    deletedAtProperty: 'removedAt',
    // Generated WHERE: { orgId, removedAt: null }
  },
});
```

## Per-module output file splitting

When your Manifest source uses the `module` keyword to organize entities into modules (e.g., `module Recipe { … }`), several projections support **per-module path hints** that organize generated files by module instead of flattening all output into a single directory.

**Supported projections:**

| Projection | Module path pattern | Notes |
|-----------|-------------------|-------|
| Zod | `schemas/<module>/schema.ts` | Entity/command file organization when `IREntity.module` is set |
| JSON Schema | `schemas/<module>/<entity>.schema.json` | Shared module-path sanitizer with Zod |
| Pydantic | `models/<module>/models.py` + `models/<module>/commands/` | Entity and command file splitting by module |
| Dart | `lib/models/<module>/` + `lib/commands/<module>/` | Type definitions organized by module |
| Storybook | `stories/<module>/` | Command stories inherit entity module |
| Mermaid | `diagrams/<module>/` | State/sequence diagrams per module; ER diagram stays flat at `diagrams/er-diagram.mmd` |
| Analytics | `analytics/handlers/<module>/` | Per-entity tracking handlers; monolith tracking-plan/events stay flat |
| Express | `routes/<module>/` + `types/<module>/` | Router and type definitions; monolith `manifest-router.ts` / `manifest-types.ts` stay flat |
| Hono | `routes/<module>/` + `types/<module>/` | Same pattern as Express; `src/routes.ts` / `manifest-types.ts` stay flat |

**How it works:**

When an entity has `module: "<ModuleName>"` in the IR, the projection generates files into module-prefixed directories. Entities without a module emit to flat paths (preserving existing behavior).

```manifest
module Inventory {
  entity Product { … }
  entity Category { … }
}

module Orders {
  entity Order { … }
  entity LineItem { … }
}
```

Generated output (Zod example):
```
schemas/
  inventory/
    schema.ts # Product, Category
  orders/
    schema.ts # Order, LineItem
```

<Note>
  Per-module file trees are **optional** — projections emit flat paths when no module is set. Convex and Next.js per-module splitting remains unimplemented (see COMPLIANCE_MATRIX §6).
</Note>

## The projection boundary

Projections handle transport concerns: authentication, HTTP conventions, response shaping, and read-path strategy. They do not own runtime responsibilities.

| Projection owns                       | Runtime owns                    |
| ------------------------------------- | ------------------------------- |
| Reading IR and emitting code          | Building the evaluation context |
| Configuring auth and database imports | Enforcing policies              |
| Choosing the read-path strategy       | Evaluating guards in order      |
| Adapting to framework conventions     | Executing actions               |
|                                       | Emitting declared events        |

Projections are tooling. They cannot introduce new language semantics — only the runtime and its specification can define how Manifest programs behave. If you find a mismatch between what a generated route does and what the Manifest spec requires, the generated code is wrong, not the spec.


---

## introduction

URL: https://manifest-b1e8623f.mintlify.app/introduction

---
title: 'Manifest: a declarative domain language and runtime'
sidebarTitle: 'Introduction'
description: 'Manifest is a declarative domain modeling language that compiles to IR, letting you define entities, commands, guards, and policies in code.'
---

Manifest is a formal language for expressing domain logic — the rules, constraints, and operations that define how your application's data actually behaves. You write `.manifest` source files that describe entities with typed properties, commands that mutate state, guards that gate execution, policies that enforce authorization, and events that notify downstream systems. The compiler transforms that source into a compact Intermediate Representation (IR), and the `RuntimeEngine` executes the IR deterministically — enforcing every guard and policy in strict order, every time.

## The core mental model

Every Manifest program moves through two stages:

1. **Compile** — your `.manifest` source is parsed and transformed to IR v1 JSON. The IR is the canonical contract between the compiler and everything downstream.
2. **Execute** — the `RuntimeEngine` accepts the IR plus a runtime context (user identity, tenant, etc.) and runs commands.

   > Full order is parameters/context → rate-limit → policies →
   > constraints → guards → **approval gate (when declared)** → actions → declared `emits` →
   > `CommandResult` (`semantics.md` / `runtime-engine.ts`).

Because the IR is immutable at runtime, all variability enters through the runtime context. Identical IR plus identical context always produces identical results.

## Key language constructs

| Construct   | What it does                                                                                                    |
| ----------- | --------------------------------------------------------------------------------------------------------------- |
| **Entity**  | Defines a data structure with typed properties, defaults, computed values, and relationships                    |
| **Command** | A named business operation with parameters, guards, actions (`mutate`, `emit`, `compute`), and event emissions  |
| **Guard**   | A boolean expression evaluated in order; execution halts immediately on the first falsey guard                  |
| **Policy**  | An authorization rule scoped to an entity action (`read`, `write`, `execute`, `all`)                            |
| **Event**   | A typed definition with a channel name and payload schema, used for outbox patterns and real-time communication |
| **Store**   | Declares where entity instances persist — `memory` for development, or a database adapter for production        |

## A real example

The following entity models a kitchen prep task. It defines computed properties, guards that restrict who can run each command, and events emitted on state transitions.

```manifest
entity PrepTask {
  property required id: string
  property required name: string
  property assignedTo: string?
  property status: string = "pending"
  property priority: number = 1

  computed isUrgent: boolean = priority >= 3

  command claim(employeeId: string) {
    guard self.status == "pending"
    guard user.role == "cook" or user.role == "chef"
    mutate assignedTo = employeeId
    mutate status = "in_progress"
    emit taskClaimed
  }

  command complete() {
    guard self.status == "in_progress"
    guard self.assignedTo == user.id
    mutate status = "completed"
    emit taskCompleted
  }

  policy canClaim execute: user.role in ["cook", "chef"]
}

store PrepTask in memory

event taskClaimed: "kitchen.task.claimed" {
  taskId: string
  employeeId: string
}

event taskCompleted: "kitchen.task.completed" {
  taskId: string
  completedBy: string
}
```

## How Manifest differs from ORMs

ORMs map database tables to objects and handle queries. Manifest models domain _behavior_ — the rules and operations that govern what can happen to your data, not just its shape. Where an ORM lets you call `update()` freely, Manifest requires that every state change pass through a command with explicit guards and policy checks. The runtime refuses to skip them.

This means your business rules live in one place — the `.manifest` file — rather than scattered across route handlers, service classes, and middleware.

## Two integration patterns

Most applications use one or both of:

- **Projections** — run `manifest build` to auto-generate Next.js API routes directly from your `.manifest` files. Best for standard CRUD workflows.
- **Embedded runtime** — import `RuntimeEngine` and call `runCommand()` directly in your own handlers. Best for complex workflows or when you need full control.

## Where to go next

<CardGroup cols={3}>
  <Card title="Quickstart" icon="bolt" href="/quickstart">
    Install the package and run your first command in under five minutes.
  </Card>
  <Card title="Installation" icon="package" href="/installation">
    Full install options, package exports, TypeScript setup, and CLI reference.
  </Card>
  <Card title="Language reference" icon="book" href="/language/entities">
    Complete specification for entities, commands, guards, policies, events, and stores.
  </Card>
</CardGroup>


---

## advanced entities

URL: https://manifest-b1e8623f.mintlify.app/language/advanced-entities

---
title: 'Advanced entities: inheritance and mixins'
description: 'Extend entity types with single inheritance and mixin composition to build reusable, composable domain models.'
sidebarTitle: 'Advanced entities'
---

Manifest supports two patterns for structuring entity types beyond a flat declaration: single inheritance with `extends` and composition with `mixin`. Both are compile-time features — the runtime sees a flattened entity with no inheritance metadata.

## Entity inheritance (`extends`)

An entity can inherit properties, computed properties, constraints, commands, and policies from a single parent entity using the `extends` keyword:

```manifest
entity BaseEntity {
  property required id: string
  timestamps
}

entity Product extends BaseEntity {
  property required name: string
  property price: number = 0

  command updatePrice(newPrice: number) {
    guard newPrice >= 0
    mutate price = newPrice
  }
}

store Product in memory
```

`Product` inherits the `id` property and `timestamps` from `BaseEntity`. The compiled IR contains a single flattened `Product` entity with all inherited members — there is no runtime polymorphism.

### Overriding inherited members

Child entities can override inherited properties and commands by redeclaring them with the same name. The child's declaration takes precedence.

### Cycle detection

The compiler detects circular inheritance chains (e.g., `A extends B` and `B extends A`) and emits an error diagnostic. Cycles are also caught transitively (A → B → C → A).

<Note>
  Only single inheritance is supported. An entity can extend exactly one parent. For sharing
  behavior across multiple entities, use mixins.
</Note>

## Mixin composition

Mixins inject reusable property and constraint declarations into an entity without creating a parent-child relationship. Declare a mixin with the `mixin` keyword and apply it inside an entity block:

```manifest
mixin Auditable {
  property required createdBy: string
  property required updatedBy: string
}

mixin SoftDeletable {
  property deletedAt: number = 0
  constraint notDeleted: self.deletedAt == 0
}

entity Document {
  mixin Auditable
  mixin SoftDeletable

  property required title: string
  property content: string = ""
}

store Document in memory
```

`Document` receives all properties and constraints from both `Auditable` and `SoftDeletable`. Mixins can be applied to any number of entities.

### Combining extends and mixins

An entity can both extend a parent and apply mixins:

```manifest
entity BaseEntity {
  property required id: string
  timestamps
}

mixin Auditable {
  property required createdBy: string
  property required updatedBy: string
}

mixin SoftDeletable {
  property deletedAt: number = 0
  constraint notDeleted: self.deletedAt == 0
}

entity ArchivedDocument extends BaseEntity {
  mixin Auditable
  mixin SoftDeletable

  property required title: string
}

store ArchivedDocument in memory
```

Composition merge order is **parent → mixins → own**; **own wins** on name conflict (`src/manifest/entity-composition.ts`). Mixin–mixin name clashes are **not** a dedicated compile error — colliding inherited members can both be prepended unless an own declaration overrides them. Prefer disjoint mixin surfaces.

## IR representation

Members are flattened into the entity for runtime/projections, **and** IR still records lineage: `IREntity.parent` (from `extends`) and `IREntity.mixins` (string array) per `docs/spec/ir/ir-v1.schema.json`. Runtime execution uses the flattened member lists; it does not re-walk inheritance.


---

## approvals

URL: https://manifest-b1e8623f.mintlify.app/language/approvals

---
title: 'Approvals: multi-stage approval workflows'
description: 'Gate command execution behind multi-stage approval chains. Each stage has its own policy expression and required count. Commands block until all applicable stages are granted.'
sidebarTitle: 'Approvals'
---

An approval declaration attaches a multi-stage approval gate to a command. When the runtime reaches the approval step, it checks whether every applicable stage has been granted. If any stage is still pending, the command blocks and returns an `approvalRequired` result instead of executing actions.

## Declaring an approval

Approvals live inside an entity block. Each approval references a command by name and declares one or more stages:

```manifest
entity PurchaseOrder {
  property required id: string
  property amount: number = 0
  property status: string = "draft"

  command submit() {
    guard self.status == "draft"
    mutate status = "pending_approval"
    emit OrderSubmitted
  }

  approval submitApproval {
    command: submit
    stages {
      manager {
        policy: user.role == "manager" or user.role == "admin"
        required: 1
      }
      director {
        policy: user.role == "director" or user.role == "admin"
        required: 1
        when: self.amount > 10000
      }
    }
    timeout: 72
    on_timeout: cancel
    emit ApprovalRequested
    emit ApprovalGranted
  }
}
```

### Stage fields

Each stage inside the `stages` block has three fields:

| Field      | Required | Description                                                                                                              |
| ---------- | -------- | ------------------------------------------------------------------------------------------------------------------------ |
| `policy`   | Yes      | Boolean expression evaluated against the approver's user context. If the expression is falsey, the approval is rejected. |
| `required` | Yes      | Number of distinct approvals needed for this stage. Typically `1`.                                                       |
| `when`     | No       | Boolean expression evaluated against the entity instance. If falsey, the stage is skipped entirely.                      |

## Execution gate order

The approval gate sits between guards and actions in the command execution pipeline. See [Commands](/language/commands) for the full pipeline description.

<Steps>
  <Step title="Check rate limit">
    If the command declares a `rateLimit`, check whether the caller has exceeded the configured
    window. If the limit is exceeded, execution stops immediately with a `rateLimitDenial` result.
  </Step>
  <Step title="Evaluate policies">
    Check every applicable policy. If any policy denies, execution stops immediately with a
    `policyDenial` result.
  </Step>
  <Step title="Evaluate constraints">
    Evaluate any `block` constraints declared inside the command. If any fail without an authorized
    override, execution stops with `constraintOutcomes` in the result.
  </Step>
  <Step title="Evaluate guards">
    Evaluate each guard expression in declaration order. Execution halts on the first falsey result
    and returns a `guardFailure` with the failing guard's index and expression.
  </Step>
  <Step title="Check approval gate">
    Find matching approval declarations for the command. Evaluate each stage's `when` condition to
    determine which stages apply. If any applicable stage is not fully granted, block with
    `approvalRequired` instead of executing actions.
  </Step>
  <Step title="Execute actions">
    Run each action (`mutate`, `emit`, `compute`, `publish`, `persist`, `effect`) in declaration
    order.
  </Step>
  <Step title="Return CommandResult">
    Return the `CommandResult` with `success: true` and all emitted events.
  </Step>
</Steps>

<Note>
  The approval gate is strictly opt-in. Commands without a matching approval declaration execute
  identically to before — the gate is never evaluated. The `approvals` field is optional on the IR
  entity and is omitted entirely when empty.
</Note>

## Conditional stages with `when`

Use the `when` expression on a stage to make it conditional. The expression is evaluated against the current entity instance at the time the command runs. When the condition is falsey, the stage is excluded from the approval chain for that invocation.

In the PurchaseOrder example above, the `director` stage only activates when `self.amount > 10000`. Orders under $10,000 need only the `manager` stage:

```manifest fragment
stages {
  manager {
    policy: user.role == "manager" or user.role == "admin"
    required: 1
  }
  director {
    policy: user.role == "director" or user.role == "admin"
    required: 1
    when: self.amount > 10000
  }
}
```

This enables a single approval declaration to handle different business rules depending on the entity's state — for example, requiring an additional director sign-off only for high-value orders.

## ApprovalRequiredInfo in CommandResult

When a command is blocked by the approval gate, the `CommandResult` includes an `approvalRequired` field describing the current state:

```typescript
interface ApprovalRequiredInfo {
  approvalName: string;
  pendingStages: string[];
  requestKey: string;
}
```

Signature is `runCommand(commandName, input, options?)`.
`entityName` / `instanceId` go in options; command params go in `input`.

```typescript
const result = await runtime.runCommand(
  'submit',
  {},
  { entityName: 'PurchaseOrder', instanceId: 'po-001' },
);

// result.success === false
// result.approvalRequired === {
// approvalName: "submitApproval",
// pendingStages: ["manager", "director"],
// requestKey: "po-001-manager-director"
// }
```

Callers inspect `result.approvalRequired` to determine which stages still need approval and which have already been granted. This is the same structure returned by `getApprovalRequest()`.

## Runtime API

The runtime engine exposes four methods for managing the approval lifecycle, plus a query method.

### `requestApproval(entity, instanceId, approvalName)`

Creates a pending approval request. Returns the `ApprovalRequestState` with all applicable stages set to `pending`.

```typescript
const state = runtime.requestApproval('PurchaseOrder', 'order-123', 'submitApproval');
```

### `approveStage(entity, instanceId, approvalName, stage, userId)`

Evaluates the stage's policy expression against the current runtime user context. If the policy passes, the stage is marked as granted. If all applicable stages are granted, subsequent command invocations proceed past the approval gate.

```typescript
const result = runtime.approveStage(
  'PurchaseOrder',
  'order-123',
  'submitApproval',
  'manager',
  'user-mgr-001',
);
```

<Warning>
  `approveStage` evaluates the stage policy expression against the approver's user context — not the
  original requester's context. If the policy is falsey, the approval is rejected with an error.
  This means the approver must satisfy the policy they are approving, which prevents privilege
  escalation through the approval API.
</Warning>

### `denyApproval(entity, instanceId, approvalName, deniedBy, reason?)`

Marks the approval request as denied. Subsequent command invocations create a new pending request rather than reusing the denied one.

```typescript
runtime.denyApproval(
  'PurchaseOrder',
  'order-123',
  'submitApproval',
  'user-mgr-001',
  'Amount exceeds quarterly budget',
);
```

### `expireApprovals(now?)`

Expires all pending approval requests whose timeout has elapsed. Timeout is declared in hours via `timeout:` in the approval block and converted to milliseconds internally.

```typescript
runtime.expireApprovals();
```

<Note>
  `expireApprovals()` must be called explicitly — for example, from a cron job or scheduled task.
  The runtime does not automatically expire approvals on a timer.
</Note>

### `getApprovalRequest(entity, instanceId, approvalName)`

Returns the current `ApprovalRequestState` for inspection. This is useful for UI flows that need to display which stages have been granted or are still pending.

```typescript
const state = runtime.getApprovalRequest('PurchaseOrder', 'order-123', 'submitApproval');
```

## Timeout behavior

When an approval declaration includes a `timeout` (specified in hours), the `on_timeout` field determines what happens when the approval window expires:

| `on_timeout` value | Behavior                                                                                 |
| ------------------ | ---------------------------------------------------------------------------------------- |
| `cancel`           | The approval request becomes `expired`. The next invocation creates a fresh request. |
| `escalate { … }`   | Open author-defined routing: required `to` (any expression), `status`, and `timeout` (`hours` / `none` / `reset`). Runtime stores the evaluated `to` as opaque `escalatedTo` metadata — Manifest does not interpret person vs team vs queue. |
| (omitted)          | The approval remains pending indefinitely until explicitly granted, denied, or expired.  |

> **Escalate timeout behavior (2026-07-15):** `on_timeout: escalate` is supported as an open block
> that routes to an author-defined target: `escalate { to: <expr>, status: pending|granted|denied|expired, timeout: <hours|none|reset> }`.
> The runtime stores the evaluated `to` as opaque `escalatedTo` metadata — Manifest does not interpret person
> vs team vs queue. A bare `on_timeout: escalate` (without the block) fails with
> `APPROVAL_ONTIMEOUT_ESCALATE_INCOMPLETE` — see fixture `103-approval-escalate-unsupported`. Full
> example: fixture `111-approval-escalate`.

Denied and expired approvals are reset on the next command attempt — the runtime creates a new pending request rather than reusing the terminal state. This means a denied approval does not permanently block the command; it only blocks the current attempt.

## Lifecycle events

Approval declarations can include `emit` entries for lifecycle transitions:

```manifest fragment
approval submitApproval {
  command: submit
  stages { /* ... */ }
  timeout: 72
  on_timeout: cancel
  emit ApprovalRequested
  emit ApprovalGranted
}
```

These events fire at the corresponding lifecycle transitions (request created, fully granted), giving external systems a way to react to approval state changes without polling. See [Events](/language/events) for declaring event schemas.

## Compile-time validation

The IR compiler validates approval declarations and produces error diagnostics for:

- **Non-existent command reference**: The `command:` field must name a command that exists on the same entity.
- **Empty stages**: An approval must declare at least one stage.
- **Duplicate stage names**: Each stage name within an approval must be unique.

These checks run at compile time, so invalid approvals surface as build errors rather than runtime failures.

## Complete example

```manifest
entity PurchaseOrder {
  property required id: string
  property amount: number = 0
  property status: string = "draft"
  property approvedBy: string = ""

  command submit() {
    guard self.status == "draft"
    mutate status = "pending_approval"
    emit OrderSubmitted
  }

  command finalize() {
    guard self.status == "pending_approval"
    mutate status = "approved"
    mutate approvedBy = user.id
    emit OrderFinalized
  }

  approval submitApproval {
    command: submit
    stages {
      manager {
        policy: user.role == "manager" or user.role == "admin"
        required: 1
      }
      director {
        policy: user.role == "director" or user.role == "admin"
        required: 1
        when: self.amount > 10000
      }
    }
    timeout: 72
    on_timeout: cancel
    emit ApprovalRequested
    emit ApprovalGranted
  }
}
```

```typescript
import { RuntimeEngine } from '@angriff36/manifest';
import { ir } from './compiled.ir.json';

const runtime = new RuntimeEngine(ir, {
  user: { id: 'requester-1', role: 'employee' },
});

// Submit the order -- blocks at the approval gate

const result = await runtime.runCommand(
  'submit',
  {},
  { entityName: 'PurchaseOrder', instanceId: 'po-001' },
);

console.log(result.success); // false
console.log(result.approvalRequired); // { pendingStages: ["manager", "director"] }

// Manager approves the first stage
runtime.approveStage('PurchaseOrder', 'po-001', 'submitApproval', 'manager', 'user-mgr-1');

// Director approves the second stage (required because amount > 10000)
runtime.approveStage('PurchaseOrder', 'po-001', 'submitApproval', 'director', 'user-dir-1');

// Re-submit -- now passes the approval gate

const final = await runtime.runCommand(
  'submit',
  {},
  { entityName: 'PurchaseOrder', instanceId: 'po-001' },
);

console.log(final.success); // true
```

Both `runCommand` calls above use
`runCommand(commandName, input, options)` — not the phantom entity-first form.

See [Commands](/language/commands) for the full execution pipeline, [Guards and policies](/language/guards-policies) for the gates that precede approvals, [Events](/language/events) for declaring event schemas used in approval lifecycle notifications, and [Roles](/language/roles) for role-based authorization in policy expressions.


---

## async commands

URL: https://manifest-b1e8623f.mintlify.app/language/async-commands

---
title: 'Async commands: background execution with job queues'
description: 'Defer command action execution to a background worker queue with the async keyword. Policies, constraints, and guards are validated synchronously before enqueuing.'
sidebarTitle: 'Async commands'
---

Async commands defer action execution to a background worker queue. The `async` keyword prefix tells the runtime to validate the command synchronously (policies, constraints, guards) and then enqueue it for later execution rather than running actions immediately. The caller receives a job ID and a `pending` status without waiting for the work to complete.

## Declaring an async command

Add the `async` keyword before `command`:

```manifest
entity Order {
  property required id: string
  property status: string = "pending"
  property processedAt: number = 0

  async command processOrder(amount: number) {
    guard amount > 0
    mutate status = "processing"
    mutate processedAt = now()
    emit OrderProcessing
  }
}
```

The `async` modifier is available at program-level, module-level, and entity-level command declarations.

## How async commands execute

<Steps>
  <Step title="Synchronous validation">
    The runtime evaluates policies, constraints, and guards synchronously — exactly as it would for a non-async command. If any check fails, the command fails immediately with the normal `policyDenial`, `constraintOutcomes`, or `guardFailure` result. No job is enqueued.
  </Step>
  <Step title="Enqueue job">
    If validation passes, the runtime creates a `JobRecord` and enqueues it via the configured `JobQueue` adapter.
  </Step>
  <Step title="Return immediately">
    The caller receives a result with `{ jobId, status: 'pending', enqueuedAt }`. Actions have not yet executed.
  </Step>
  <Step title="Worker executes">
    When the job is drained (via `drainJobs()` or a production queue consumer), the runtime re-enters the command with `context.source === 'job'`, bypassing the async branch and executing all actions normally.
  </Step>
</Steps>

<Note>
  Fail-fast validation is the key design decision: policies, constraints, and guards are checked
  synchronously so that invalid commands are rejected immediately. Only valid commands are enqueued.
  This means callers get instant feedback on authorization and precondition failures without waiting
  for a background worker.
</Note>

## Return value

```typescript
// Incorrect flat shape (do not use):
// { success: true, jobId, status: "pending", enqueuedAt }
```

Async enqueue still returns a normal `CommandResult`. The job metadata is nested under `result`:

```typescript
{
  success: true,
  result: { jobId: "job-abc123", status: "pending", enqueuedAt: 1748131200000 },
  emittedEvents: []
}
```

Read `result.result.jobId` / `result.result.status`. The `jobId` correlates with the synthesized completion/failure events.

## Auto-synthesized events

The compiler automatically creates two events for each async command:

| Event                    | When emitted                        |
| ------------------------ | ----------------------------------- |
| `{CommandName}Completed` | After the job executes successfully |
| `{CommandName}Failed`    | If the job execution fails          |

Synthesized events use channel `jobs.<commandName>` (not a domain-style channel), and payloads include timestamps:

```manifest
// Compiler-synthesized (illustrative):
// event ProcessOrderCompleted: "jobs.processOrder" {
// jobId: string; result: any; completedAt: number
// }
// event ProcessOrderFailed: "jobs.processOrder" {
// jobId: string; error: string; failedAt: number
// }
```

Names are `{commandName}Completed` / `{commandName}Failed`, appended to the IR `events` array.

<Warning>
  If you declare an event with the same name as a synthesized event, the compiler produces an error
  diagnostic. Rename your event to avoid the collision.
</Warning>

## JobQueue adapter

Configure a job queue in `RuntimeOptions` to enable async command execution:

```typescript
import { RuntimeEngine, MemoryJobQueue } from '@angriff36/manifest';

const jobQueue = new MemoryJobQueue();

Constructor is `(ir, context, options)`. Put `user` in context and `jobQueue` in options:

```typescript
const runtime = new RuntimeEngine(
  ir,
  { user: { id: 'system', role: 'worker' } },
  { jobQueue },
);
```

The `JobQueue` interface defines three methods:

`enqueue` returns `Promise<void>` (the runtime allocates `jobId` before enqueue). Optional second arg is an opaque transaction handle:

```typescript
interface JobQueue {
  enqueue(job: JobRecord, tx?: unknown): Promise<void>;
  drainPending(): Promise<JobRecord[]>;
  updateStatus(jobId: string, status: string, error?: string): Promise<void>;
}
```

A `JobRecord` carries:

```typescript
interface JobRecord {
  jobId: string;
  commandName: string;
  entityName?: string;
  instanceId?: string;
  input: Record<string, unknown>;
  correlationId?: string;
  causationId?: string;
  enqueuedAt: number;
  status: 'pending' | 'running' | 'completed' | 'failed';
}
```

<Warning>
  `MemoryJobQueue` is for testing and development only. It stores jobs in memory and they are lost
  on process restart. Production deployments should use a durable queue — the shipped
  `PostgresJobQueue` (below), or your own `JobQueue` backed by Redis, SQS, Google Cloud Tasks, or
  similar. If no `jobQueue` is configured and an async command is invoked, the runtime returns a
  `MISSING_JOB_QUEUE` error result.
</Warning>

## Production: durable queue + drain worker

`PostgresJobQueue` is a durable `JobQueue` adapter, and `runJobWorker` is the
shipped drain loop — you no longer hand-write either. Apply the companion
schema (`src/manifest/jobs/stores/postgres.sql`) once, then:

```typescript
import { RuntimeEngine } from '@angriff36/manifest';
import { PostgresJobQueue } from '@angriff36/manifest/jobs/postgres';
import { runJobWorker } from '@angriff36/manifest/jobs/worker';
import { Pool } from 'pg';

const pool = new Pool({ connectionString: process.env.DATABASE_URL });
const runtime = new RuntimeEngine(ir, context, {
  jobQueue: new PostgresJobQueue({ pool }),
});

// Long-lived worker: each tick drains pending jobs, runs their actions, and
// records completed/failed status. Stop it on shutdown.
const worker = runJobWorker(runtime);
await worker.stop();
```

`PostgresJobQueue.drainPending` claims pending rows with `FOR UPDATE SKIP
LOCKED` and flips them to `running` in one statement, so multiple worker
replicas drain disjoint jobs. For a cron/serverless deployment that wants a
single pass instead of a loop, call `drainJobsOnce(runtime)`.

## Deterministic testing with drainJobs()

The `drainJobs()` method processes pending jobs in FIFO order, making async commands fully testable without real message queues:

`runCommand(commandName, input, options?)` — not
entity-first. Command params belong in `input`; `entityName` / `instanceId` in `options`.

```typescript
import { RuntimeEngine, MemoryJobQueue } from '@angriff36/manifest';
import { ir } from './compiled.ir.json';

const jobQueue = new MemoryJobQueue();

const runtime = new RuntimeEngine(
  ir,
  { user: { id: 'system', role: 'worker' } },
  { jobQueue },
);

// Seed an order instance
runtime.createInstance('Order', { id: 'order-1', status: 'pending' });

// Invoke the async command
const result = await runtime.runCommand(
  'processOrder',
  { amount: 50 },
  { entityName: 'Order', instanceId: 'order-1' },
);

console.log(result.success); // true

console.log(result.result.status); // "pending"
console.log(result.result.jobId); // runtime-allocated id

// The instance has NOT changed yet
const before = runtime.getInstance('Order', 'order-1');
console.log(before.status); // "pending"

// Drain the job queue — executes the enqueued command
await runtime.drainJobs();

// Now the instance reflects the async command's mutations
const after = runtime.getInstance('Order', 'order-1');
console.log(after.status); // "processing"
console.log(after.processedAt); // <timestamp>
```

`drainJobs()` is the primary testing surface for async commands. It processes all pending jobs in order, emitting synthesized completion/failure events as appropriate.

## Re-entry guard

When a job is drained, the runtime re-enters the command with `context.source === 'job'`. The async branch detects this and executes actions normally instead of enqueuing again. This prevents infinite enqueue loops:

1. First call (`context.source` is not `'job'`) -- validate and enqueue.
2. Second call during drain (`context.source === 'job'`) -- skip the async branch, execute actions.

You can also use `context.source` in your own guard expressions if you need to enforce different behavior for job-initiated versus user-initiated calls.

## Complete example

```manifest
entity Order {
  property required id: string
  property amount: number = 0
  property status: string = "pending"
  property processedAt: number = 0

  command submit(amount: number) {
    guard amount > 0
    mutate amount = amount
    mutate status = "submitted"
    emit OrderSubmitted
  }

  async command processOrder() {
    guard self.status == "submitted"
    mutate status = "processing"
    mutate processedAt = now()
    emit OrderProcessing
  }
}
```

Same `runCommand(commandName, input, options?)` shape.

```typescript
import { RuntimeEngine, MemoryJobQueue } from '@angriff36/manifest';
import { ir } from './compiled.ir.json';

const jobQueue = new MemoryJobQueue();

const runtime = new RuntimeEngine(
  ir,
  { user: { id: 'system', role: 'worker' } },
  { jobQueue },
);

// Submit the order synchronously
runtime.createInstance('Order', { id: 'order-42', status: 'pending' });
await runtime.runCommand(
  'submit',
  { amount: 250 },
  { entityName: 'Order', instanceId: 'order-42' },
);

// Process the order asynchronously
const result = await runtime.runCommand(
  'processOrder',
  {},
  { entityName: 'Order', instanceId: 'order-42' },
);

console.log(result.result.status); // "pending"
console.log(result.result.jobId); // defined

// Execute the background job
await runtime.drainJobs();

const order = runtime.getInstance('Order', 'order-42');
console.log(order.status); // "processing"
```

See [Commands](/language/commands) for the synchronous execution pipeline, [Events](/language/events) for event declaration syntax, and [Reactions](/language/reactions) for declarative event-driven command dispatch that can chain with async commands.


---

## commands

URL: https://manifest-b1e8623f.mintlify.app/language/commands

---
title: 'Commands: business operations and mutations'
description: 'Declare named operations on entities that enforce guards and policies, execute ordered actions, emit events, and return a structured CommandResult.'
sidebarTitle: 'Commands'
---

A command is a named operation bound to an entity. When you call `runCommand`, the runtime executes the command in a strict, deterministic order:

parameters/context → rate-limit → policies → constraints → guards →
approval gate (when declared) → actions → declared `emits` → return. Nothing in that order can be
rearranged or skipped.

## Declaring a command

Commands live inside an entity block. A command takes an optional parameter list and a body of guards and actions.

```manifest
entity Counter {
  property value: number = 0

  command increment() {
    mutate value = value + 1
    emit CounterIncremented
  }

  command setValue(newValue: number) {
    guard newValue >= 0
    mutate value = newValue
    emit CounterSet
  }
}
```

Parameters are typed with the same types as properties: `string`, `number`, `boolean`, `timestamp`. Parameters are available by name in all guard and action expressions.

## Execution order

The runtime executes every command in exactly this order:

<Steps>
  <Step title="Process parameters">
    Apply each declared parameter's `defaultValue` to any omitted argument, then fail closed with a
    `parameterFailure` (`MISSING_REQUIRED_PARAMETER`) when a `required` parameter with no default is
    absent. An explicit `undefined` is treated as absent; `null` counts as supplied.
  </Step>
  <Step title="Build evaluation context">
    Bind `self` and `this` to the current entity instance, inject command parameters (with defaults
    already applied), and attach the runtime context (`user`, `context.*`).
  </Step>
  <Step title="Check rate limit">
    If the command declares a `rateLimit`, check whether the caller has exceeded the configured
    window. If the limit is exceeded, execution stops immediately with a `rateLimitDenial` result.
  </Step>
  <Step title="Evaluate policies">

    Evaluate the command's effective policy list only. Entity
    `default policy` values are expanded into `IRCommand.policies` at compile time; when a command
    already has an explicit IR `policies` array, defaults are **not** merged. If any policy denies,
    execution stops with a `policyDenial` result. (Source has no command-body policy clause —
    per-command stricter checks use guards.)
  </Step>
  <Step title="Evaluate command constraints">
    Evaluate any `block` constraints declared inside the command. If any fail without an authorized
    override, execution stops with `constraintOutcomes` in the result.
  </Step>
  <Step title="Evaluate guards">
    Evaluate each guard expression in declaration order. Execution halts on the first falsey result
    and returns a `guardFailure` with the failing guard's index and expression.
  </Step>
  <Step title="Check approval gate (when declared)">
    **Added (2026-07-15):** If the entity declares an approval for this command and applicable
    stages are not fully granted, execution stops with `approvalRequired` instead of running
    actions. See [Approvals](/language/approvals).
  </Step>
  <Step title="Execute actions">
    Run each action (`mutate`, `emit`, `compute`, `publish`, `persist`, `effect`) in declaration
    order.
  </Step>
  <Step title="Emit declared events">
    Emit the events listed in the `emits` declaration (if any) in order, attaching payload and
    metadata.
  </Step>
  <Step title="Return CommandResult">
    Return the `CommandResult` with `success: true`, the last action result, and all emitted events.
  </Step>
</Steps>

## Actions

Actions are the statements inside a command body that produce effects.

### `mutate`

Assigns a value to an entity property. The expression is evaluated against the current context and stored on the instance.

```manifest
command complete() {
  guard not self.completed
  mutate completed = true
  mutate completedAt = now()
}
```

### `emit`

Emits an event and optionally attaches an inline payload. Also produces an action result with the evaluated value.

```manifest
command increment() {
  mutate value = value + 1
  emit CounterIncremented
}
```

### `compute`

Calculates a value **without mutating** entity state. `compute <name> = <expr>` binds `<name>` as a command-scoped local into the evaluation scope, available to later actions and to event-payload expressions in the same command. The binding is never persisted and never appears in `getInstance`/`getAllInstances`. Bare `compute <expr>` just returns the value. To persist a field, use `mutate`.

The compiler emits a `COMPUTE_USED_AS_MUTATE` warning when a `compute` target matches a declared property — the common sign of a mis-used `mutate`.

```manifest
command addItem(amount: number) {
  compute newQuantity = quantity + amount // ephemeral local, not persisted
  mutate quantity = newQuantity // persists
}
```

### `publish`

External delivery of the **named** event `target` (e.g. `publish OrderShipped`). Requires `RuntimeOptions.outboxStore`: when it is absent the command **fails closed** with a `MISSING_OUTBOX_STORE` error and persists nothing. When configured, the named event is delivered in-process (result + listeners + reactions) AND durably enqueued to the outbox, joining the command transaction under a `TransactionProvider`. The compiler errors `EMIT_ACTION_UNKNOWN_EVENT` if `target` is missing or undeclared. In deterministic mode, throws `ManifestEffectBoundaryError`.

### `persist`

Explicitly flushes the command's pending working-copy changes to the store at its point in the action sequence, then clears the pending change set. It does not open or close a transaction. Under a `TransactionProvider` the flush joins the command transaction (atomic-on-failure); without one it is an immediate, non-reversible write. Multiple `persist` actions are permitted. In deterministic mode, throws `ManifestEffectBoundaryError`.

### `effect`

Invokes the host `RuntimeOptions.effectHandler` with the evaluated value and command context (`{ name?, value, commandName, entityName?, instanceId?, context }`); the handler's result becomes the action result. An optional `effect <name> = <expr>` form names the effect. When no handler is configured the command **fails closed** with a `MISSING_EFFECT_HANDLER` error. In deterministic mode, throws `ManifestEffectBoundaryError`.

<Warning>
`publish` and `effect` are fail-closed adapter actions: without `outboxStore` / `effectHandler` respectively the command returns `{ success: false }` (it does not silently no-op). `emit` is in-process and always available. When `deterministicMode` is `true`, calling `publish`, `effect`, or `persist` throws `ManifestEffectBoundaryError` — this is by design to enforce the effect boundary contract.
</Warning>

## The `emits` keyword

You can declare events at the command level using the `emits` keyword outside the action body. This serves as a declaration that the command emits a specific event after all actions succeed.

```manifest
entity Order {
  property status: string = "pending"

  command complete() {
    guard self.status == "in_progress"
    mutate status = "completed"
    emit OrderCompleted
  }
}

event OrderCompleted: "order.completed" {
  orderId: string
  completedAt: number
}
```

See [Events](/language/events) for how to declare event schemas.

## Command constraints (vNext)

Commands can declare their own `constraint` blocks that are evaluated after policies but before guards. The same severity levels (`ok`, `warn`, `block`) apply:

```manifest
entity Order {
  property status: string = "pending"
  property amount: number = 0

  command updateStatus(newStatus: string) {
    constraint notCancelled: self.status != "cancelled" "Cannot update a cancelled order"
    constraint amountVerified:warn self.amount >= 100 "Orders under $100 require manual approval"

    mutate status = newStatus
    emit OrderStatusChanged
  }
}
```

A failing `block` constraint stops execution and surfaces `constraintOutcomes` in the `CommandResult`. A `warn` constraint produces an outcome entry but does not stop execution.

## CommandResult shape

Every call to `runCommand` returns a `CommandResult`:

```typescript
interface CommandResult {
  success: boolean;
  error?: string;

  // Present when a policy denied execution
  policyDenial?: {
    policyName: string;
    expression: string;
  };

  // Present when a guard halted execution
  guardFailure?: {
    index: number; // 1-based
    expression: string;
    resolved: { expression: string; value: unknown }[];
  };

  // Present when constraints were evaluated
  constraintOutcomes?: ConstraintOutcome[];

  // Present when optimistic concurrency detected a conflict
  concurrencyConflict?: ConcurrencyConflict;

  // All events emitted during this run
  emittedEvents: EmittedEvent[];

  //
  result?: unknown; // last action value
}
```

The field is `result?: unknown`. There is no `lastActionResult` on
`CommandResult` in `runtime-engine.ts` @ v3.6.41.

## Calling `runCommand` from TypeScript

Real signature is
`runCommand(commandName, input, options?)` — not entity-first
`runCommand(Entity, cmd, args)`. Pass `entityName` / `instanceId` in `options`.

```typescript
import { RuntimeEngine } from '@angriff36/manifest';
import { ir } from './compiled.ir.json';

const runtime = new RuntimeEngine(ir, {
  user: { id: 'user-123', role: 'admin' },
});

const result = await runtime.runCommand(
  'updateStatus',
  { newStatus: 'processing' },
  { entityName: 'Order', instanceId: 'order-abc' },
);

if (!result.success) {
  if (result.policyDenial) {
    console.error('Denied by policy:', result.policyDenial.policyName);
  } else if (result.guardFailure) {
    console.error(`Guard ${result.guardFailure.index} failed:`, result.guardFailure.formatted);
  } else if (result.constraintOutcomes) {
    const blocked = result.constraintOutcomes.filter((o) => o.severity === 'block' && !o.passed);
    console.error(
      'Blocked by constraints:',
      blocked.map((o) => o.code),
    );
  }
  process.exit(1);
}

console.log('Events:', result.emittedEvents);
```

## Retry policies

Commands can declare retry policies for transient failures with configurable backoff strategies. When a command fails (guard failure excluded — guards fail fast), the runtime retries according to the declared policy.

```manifest fragment
entity Notification {
  command sendEmail(recipient: string, body: string) {
    retry {
      maxAttempts: 3
      backoff: exponential
      delay: 1000
      jitter: true
    }
    // command body
  }
}
```

### Retry configuration

| Field         | Description                                                                   |
| ------------- | ----------------------------------------------------------------------------- |
| `maxAttempts` | Maximum number of retry attempts                                              |
| `backoff`     | Strategy: `fixed`, `exponential`, or `linear`                                 |
| `delay`       | Base delay in milliseconds (compiles to IR `delayMs`)                         |
| `jitter`      | Add random jitter to prevent thundering herd (default: `false`)               |
| `retryOn`     | One or more error codes that trigger retry (repeat the field per code)        |

| `maxDelay` | Upper bound on each computed delay (ms) |

> **Note:** Delays are clamped with `min(delay, maxDelay)`. Source field `maxDelay` compiles to IR `maxDelayMs`.
> See `runtime-retry.ts` `computeRetryDelays` and fixture `72-command-retry-policy.manifest`.

 (use `delay` instead)

### Backoff strategies

| Strategy      | Behavior                                                      |
| ------------- | ------------------------------------------------------------- |
| `fixed`       | Constant delay between retries (e.g., 1000ms, 1000ms, 1000ms) |
| `exponential` | Doubles each time (e.g., 1000ms, 2000ms, 4000ms)              |
| `linear`      | Increases by `delay` each time (e.g., 1000ms, 2000ms, 3000ms) |

### Retry events

No `{Command}RetryAttempted` / `{Command}RetryExhausted` events exist
in the reference runtime. Retry status is returned on `CommandResult.retry`
(`{ attempts, exhausted, lastErrorCode?, delaysMs }`).

<Note>
  Retry policies only apply when the failed attempt's derived error code appears in `retryOn`.
  Guard failures, policy denials, and blocking constraint outcomes are never retried — they
  represent intentional business rule enforcement, not transient errors.
</Note>

## Rate limiting

Rate limiting constrains how frequently a command can be invoked per user, tenant, or globally.
When the limit is exceeded, the command returns a `rateLimitDenial` on `CommandResult`.

```manifest fragment
entity ApiKey {
  property required id: string

  command makeRequest() {
    rateLimit {
      windowMs: 60000
      maxRequests: 100
      scope: user
    }
    // command body
  }
}
```

### Rate limit configuration

| Field         | Description                                                |
| ------------- | ---------------------------------------------------------- |
| `windowMs`    | Time window in milliseconds                                |
| `maxRequests` | Maximum allowed requests within the window                 |
| `scope`       | `user`, `tenant`, or `global` (IR enum; not `user.id`)     |

IR `IRRateLimit` requires `maxRequests`, `windowMs`, and
`scope: "user" | "tenant" | "global"`. Optional `burstAllowance` exists. There is no `strategy`
field on the rate-limit block.

When the rate limit is exceeded, the `CommandResult` includes:

```typescript
{
  success: false,
  rateLimitDenial: {
    scope: 'user', // 'user' | 'tenant' | 'global'
    scopeKey: 'user:user-123',
    limit: 100,
    windowMs: 60000,
    retryAfterMs: 45000,
  },
}
```

Field name is `rateLimitDenial`, not `rateLimitExceeded`
(`runtime-engine.ts` `CommandResult`).

<Warning>
  Rate limiting is enforced by the runtime engine before policies/guards run. It is not a
  network-level rate limiter — it operates within the Manifest runtime context. For HTTP-level rate
  limiting, configure your API gateway or middleware. The reference limiter is in-memory only
  (no durable adapter).
</Warning>

<Note>
  Call shape: `runCommand(commandName, input, { entityName?, instanceId?, ... })`. `instanceId` and
  `entityName` belong in **options**, not in the command parameter object. Command parameters are
  only the named values declared in the command signature.
</Note>


---

## computed caching

URL: https://manifest-b1e8623f.mintlify.app/language/computed-caching

---
title: 'Computed property caching: request, session, and TTL'
sidebarTitle: 'Computed caching'
description: 'Memoize computed properties with request, session, or TTL cache strategies, with dependency-driven staleness surfaced on every read.'
---

A computed property can declare a cache strategy so its value is memoized rather than recomputed on every read. Three strategies are available: per-command (`request`), per-engine-lifetime (`session`), and time-bounded (`ttl`). For how computed properties work in general, see [Computed properties](/language/computed-properties).

Strategies match runtime cache keys / staleness marking / `evaluateComputedProperty` `{ value, stale, cached }` surface in `runtime-engine.ts`. Package pin **3.6.41**.

## Syntax

The cache modifier follows the computed property's expression:

```manifest
entity Product {
  property required price: number
  property quantity: number = 1
  property taxRate: number = 0

  computed subtotal: number = price * quantity cache request
  computed total: number = subtotal * (1 + taxRate / 100) cache session
  computed margin: number = subtotal * 0.3 cache ttl 300
  computed label: string = "Product"
}

store Product in memory
```

`cache request` and `cache session` are bare strategies. `cache ttl 300` requires a numeric argument interpreted as a TTL in seconds. A computed property with no `cache` clause (like `label`) is recomputed on every read.

## Behavior

`cache`, `request`, `session`, and `ttl` are reserved keywords. The parser runs after the expression: it consumes `cache`, then expects one of `request`, `session`, or `ttl`. For `ttl` it reads a following `NUMBER` token (throwing if absent) and stores `{ strategy: 'ttl', ttlSeconds: <n> }`; the other two store `{ strategy: 'request' }` or `{ strategy: 'session' }`. The cache config is attached to the computed property and carried through to the IR.

The runtime engine implements the strategies with two caches keyed by `entityName:instanceId:propertyName`:

- **`request`**: stored in a request-scoped cache that is cleared per command execution. The value survives repeated reads within a single command but not across commands.
- **`session`**: stored in the long-lived cache for the lifetime of the engine instance.
- **`ttl`**: stored in the same long-lived cache, but on read the entry is only returned when `getNow() - computedAt < ttlSeconds * 1000`; once expired the entry is deleted and the value is recomputed.

On a cache hit the engine returns `{ value, stale, cached: true }`; on a miss it evaluates the expression, stores the result (when a strategy is configured), and returns `cached: false`. Cache entries are not silently wrong when inputs change: when a mutation touches a property that a cached computed depends on, the engine marks the matching cache entries `stale` in both the session/TTL and request caches, and propagates staleness transitively to computed properties that depend on other computed properties. The stale flag is surfaced on the read result rather than forcing an immediate recompute. Because `ttl` uses `getNow()`, expiry honors the deterministic clock supplied via the `now` runtime option.

Caching is a runtime evaluation concern; the cache strategy is metadata carried on the computed property in the IR. There is no dedicated database or schema projection for it.

<Note>
  The `request` cache is cleared on each command boundary, so it is genuinely scoped to a single
  command's evaluation rather than to an HTTP-request abstraction. `session` persists for the
  lifetime of the engine instance — there is no eviction beyond staleness marking. Dependency-driven
  staleness sets a flag rather than evicting; consumers decide whether to act on `stale: true`. A
  `ttl` clause without a numeric argument is a parse error.
</Note>


---

## computed properties

URL: https://manifest-b1e8623f.mintlify.app/language/computed-properties

---
title: 'Computed properties: derived values'
description: 'Declare derived values with the computed keyword. Manifest evaluates them against the current instance automatically and prevents infinite recursion.'
sidebarTitle: 'Computed properties'
---

A computed property is a read-only value derived from an expression rather than stored directly. You declare it with the `computed` keyword inside an entity block. The runtime evaluates it on demand, using the current instance fields and any declared dependencies.

## Syntax

```manifest
computed <name>: <type> = <expression>
```

The expression can reference `self`, `this`, any property on the instance, other computed properties, and the standard built-ins (`now()`, `uuid()`).

```manifest
entity Item {
  property required price: number
  property quantity: number = 1
  property discountPercent: number = 0

  computed total: number = price * quantity
  computed discountedTotal: number = total * (1 - discountPercent / 100)
  computed hasDiscount: boolean = discountPercent > 0
  computed isExpensive: boolean = total > 100
}
```

## Evaluation context

When the runtime evaluates a computed property, the context contains:

| Binding                       | Value                                                 |
| ----------------------------- | ----------------------------------------------------- |
| `self` / `this`               | The current entity instance                           |
| All property values           | Direct field access by name                           |
| Any dependencies listed in IR | Other computed properties that have already resolved  |
| `user`                        | The current user object (if the runtime provides one) |
| `context`                     | The runtime context object                            |

```manifest
entity Author {
  property required name: string
  hasMany books: Book

  computed bookCount: number = self.books.length
}
```

Relationship traversal (`self.books`) is fully supported in computed expressions. The runtime resolves the relationship synchronously before evaluating the expression.

## Chaining computed properties

A computed property can reference another computed property on the same entity. The runtime resolves them in dependency order:

```manifest
entity WorkflowStep {
  property status: string = "pending"
  property executedAt: number = 0

  computed isPending: boolean = self.status == "pending"
  computed isInProgress: boolean = self.status == "in_progress"
  computed isCompleted: boolean = self.status == "completed"
  computed canStart: boolean = self.isPending
  computed canComplete: boolean = self.isInProgress
}
```

`canStart` depends on `isPending`, which depends on `status`. The runtime evaluates in bottom-up order.

## When computed properties re-evaluate

Without a `cache` clause, values are recomputed on each access. Optional `cache request` / `cache session` / `cache ttl <seconds>` strategies are documented in [Computed caching](/language/computed-caching).

They reflect the state of the instance at the moment of access (or the cached entry when a strategy applies). This means:

- After a `mutate` action inside a command, any subsequent access to a computed property reflects the updated field values.
- Computed properties based on `now()` return a different value on each evaluation if the time source advances.

## Infinite recursion prevention

The runtime detects dependency cycles. If evaluating a computed property would cause infinite recursion, the runtime returns `undefined` for that property instead of throwing. This is defined behavior, not an error condition.

```manifest
entity Circular {
  // If A depends on B and B depends on A, both evaluate to undefined
  computed a: number = self.b + 1
  computed b: number = self.a + 1
}
```

<Warning>
  A computed property that returns `undefined` due to a cycle will silently produce `undefined` in
  guard and action expressions. If you rely on a computed value being non-null, ensure there are no
  cycles in the dependency graph.
</Warning>

## Using computed properties in guards and actions

Computed properties are first-class values in the evaluation context. You can reference them in guards, policies, and action expressions:

```manifest
entity Task {
  property required title: string
  property required status: string = "todo"
  property required assigneeId: string = ""
  property required createdAt: number = 0

  property priority: number = 0

  computed isOverdue: boolean = (now() - self.createdAt) > 86400000 and self.status != "done"
  computed assignedUser: string = self.assigneeId != "" ? self.assigneeId : "Unassigned"

  computed isHighPriority: boolean = self.priority >= 3

  command updateStatus(newStatus: string) {
    guard newStatus != null and newStatus != ""
    guard newStatus == "todo" or newStatus == "in-progress" or newStatus == "done"
    mutate status = newStatus
    emit TaskStatusUpdated
  }

  command assignTask(userId: string) {
    guard userId != null and userId != ""
    guard self.assigneeId == "" or user.role == "admin"
    mutate assigneeId = userId
    emit TaskAssigned
  }
}
```

Guards like `self.assigneeId == ""` reference the raw property. A guard referencing `self.isHighPriority` would evaluate the computed property first and use the result.

## Computed properties in the TypeScript output

When you use the Next.js projection or TypeScript types surface, computed properties appear as read-only fields on the generated interface. They are not persisted in the store — they are derived each time the instance is loaded.

```typescript
interface Task {
  title: string;
  status: string;
  assigneeId: string;
  createdAt: number;
  priority: number;
  // Computed — derived at read time
  readonly isOverdue: boolean;
  readonly assignedUser: string;
  readonly isHighPriority: boolean;
}
```


---

## constraints

URL: https://manifest-b1e8623f.mintlify.app/language/constraints

---
title: 'Constraint built-ins: regex, range, and boundary checks'
sidebarTitle: 'Constraint built-ins'
description: 'Validate string formats and numeric ranges declaratively with the matches, between, min, max, and length built-ins inside Manifest constraints and guards.'
---

Manifest ships built-in functions for the two most common validation needs — matching a string against a pattern and bounding a number or length — so you can express format and range rules declaratively inside constraints and command guards rather than writing custom comparison logic. These built-ins return booleans (or numbers) and slot into the existing constraint and guard evaluation paths unchanged. For how constraints attach to entities and commands and how severity works, see [Entities](/language/entities).

## Regex matching with `matches()`

The `matches(value, pattern)` built-in tests a string value against a regular expression, enabling declarative format validation — emails, phone numbers, postal codes. Invalid literal patterns are caught at compile time; the match itself is enforced at runtime.

```manifest
entity ContactInfo {
  property required id: string
  property email: string = ""
  property phone: string = ""
  property zipCode: string = ""

  constraint validEmail: matches(self.email, "^[^@]+@[^@]+\\.[^@]+$") "Must be a valid email format"
  constraint validPhone: matches(self.phone, "^\\d{3}-\\d{3}-\\d{4}$") "Phone must be in XXX-XXX-XXXX format"
  constraint validZip: matches(self.zipCode, "^\\d{5}$") "Zip code must be 5 digits"
}

store ContactInfo in memory
```

The first argument is the string to test (typically a property via `self.`), the second is the pattern as a string literal. Backslashes are escaped within the string literal (`\\d`, `\\.`). The trailing quoted string is the constraint's failure message.

Regex constraints are validated in two places. At **compile time**, when the callee is `matches`, there are at least two arguments, and the second is a string literal, the compiler attempts `new RegExp(...)`; if construction throws it emits an error diagnostic (`Invalid regex pattern in matches(): "..."`) at the call's source position. Patterns that are not literal strings are not validated at compile time because there is nothing constant to test. At **runtime**, the built-in returns `false` unless both arguments are strings, then evaluates `new RegExp(pattern).test(s)`; a pattern that fails to compile at runtime returns `false` rather than throwing. Because `matches` returns a boolean, it composes with the constraint machinery the same way any other boolean expression does.

A `matches` constraint with a literal pattern is the natural source for a Zod `.regex()` refinement and a database `CHECK` constraint in projections that consume the IR's constraint expressions.

<Note>
  The runtime uses the JavaScript `RegExp` engine, so pattern syntax and semantics are exactly those
  of ECMAScript regular expressions, with no flags applied. A non-string value or non-string pattern
  makes `matches` return `false` — so a constraint over a null or non-string field registers as a
  failed match rather than being skipped.
</Note>

## Range and boundary checks

The `min`, `max`, `between`, and `length` built-ins express numeric range and string-length validation inside constraints and guards.

```manifest
entity Product {
  property required id: string
  property name: string = ""
  property price: number = 0
  property quantity: number = 0
  property discount: number = 0

  constraint priceRange: between(self.price, 0.01, 99999.99) "Price must be between 0.01 and 99999.99"
  // Was incorrect: min(self.quantity, 0) / max(self.discount, 100) — see correction below
  constraint quantityMin: self.quantity >= 0 "Quantity must be at least 0"
  constraint discountMax: self.discount <= 100 "Discount cannot exceed 100%"
  constraint nameMinLength: length(self.name) >= 1 "Name must not be empty"
  constraint nameMaxLength: length(self.name) <= 255 "Name must be at most 255 characters"

  command setPrice(newPrice: number) {
    guard newPrice > 0
    mutate price = newPrice
    emit PriceChanged
  }

  command applyDiscount(pct: number) {
    guard between(pct, 0, 100)
    mutate discount = pct
    emit DiscountApplied
  }
}

event PriceChanged: "product.price.changed" {
}

event DiscountApplied: "product.discount.applied" {
}

store Product in memory
```

`between(value, low, high)` is inclusive on both ends. `length(value)` returns a string's length, which is then compared with ordinary operators (`>= 1`, `<= 255`).

The semantics are:

- `between(value, low, high)` returns `true` only when all three are numbers and `value >= low && value <= high`; otherwise `false`.
- `min(...args)` and `max(...args)` are variadic numeric reducers (`Math.min` / `Math.max`), or `undefined` if none are numeric — **not** boolean boundary predicates.
- `length(value)` returns the length of a string (the same `length` built-in used for strings generally).

In a guard, a falsey result halts command execution in order. In a constraint, pass/fail uses truthiness of the expression result (plus `failWhen` / severity via `constraintExpressionPasses`). A static analysis module also extracts numeric range and length bounds from these expressions and exposes converters for SQL `CHECK`, Zod chains, and OpenAPI `minimum`/`maximum`/`minLength`/`maxLength`.

Do **not** write `constraint x: min(self.n, 0)` expecting a lower-bound check. `min`/`max` return numbers; constraint polarity is `!!result` (unless `failWhen`). `min(5, 0)` → `0` → **fails**; `min(-1, 0)` → `-1` → **passes**. Use comparisons (`self.quantity >= 0`) or `between(...)` for bounds.


---

## entities

URL: https://manifest-b1e8623f.mintlify.app/language/entities

---
title: 'Entities: data structures and behavior'
description: 'Define typed data structures with properties, computed values, relationships, constraints, concurrency controls, and state transitions in the Manifest language.'
sidebarTitle: 'Entities'
---

An entity is the primary building block in Manifest. It combines a typed data schema with behavioral constraints, commands, and computed properties into a single declaration. The compiler turns entities into IR that the runtime can instantiate, query, and mutate.

## Basic declaration

Use the `entity` keyword followed by a name and a block. Property declarations go inside the block.

```manifest
entity Product {
  property required name: string
  property price: number = 0
  property inStock: boolean = true
  property description: string = ""
}
```

### External entities

Prefix the declaration with `external` to mark an entity as owned by another system or file. The compiler records `external: true` on the IR, and persistence projections (Prisma, Drizzle, Kysely, Convex, prisma-store) skip it — they emit no table or model for it, only honoring relationships that point at it. Use this to reference data managed outside the current program.

```manifest
external entity ImportedUser {
  property required id: string
  property email: string
}
```

`external` is a contextual modifier rather than a reserved word, so a property may still be named `external` (`property external: string`).

## Properties

Every property has a type.

`timestamp` **is** a valid property type — it is a normative alias for `datetime` (`docs/spec/semantics.md:281,286-289`). Runtimes MUST treat `timestamp` identically to `datetime`. Common scalar types include `string`, `number`, `boolean`, plus date/time primitives `datetime` / `timestamp` / `date` / `time` / `duration`, `decimal` / `money`, collections, enums, and value-object names. When a property is omitted at create time, the runtime applies the property's `defaultValue` if present, otherwise the **type default** (for example `""`, `0`, `false`) — not `undefined`. An explicit `required` modifier is not satisfied by a zero-filled type default alone (`E_REQUIRED`). See [Types](/language/types).

### Modifiers

Place modifiers between the `property` keyword and the name:

| Modifier     | Meaning                                                                                                                                                                                                                                                                                                                                                                 |
| ------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `required`   | The caller must supply a value. The runtime rejects a create (`E_REQUIRED`) when the property has no supplied value, `defaultValue`, `autoNow`, auto-managed field, or creating-command action; a zero-filled type default does not satisfy it.                                                                                                                         |
| `optional`   | Explicitly marks the property as not required. Equivalent to the default behavior.                                                                                                                                                                                                                                                                                      |
| `readonly`   | Immutable after creation. The runtime rejects an update that changes the value (`E_READONLY`); writes during the creating command and same-value writes are allowed.                                                                                                                                                                                                    |
| `unique`     | Values must be unique within the entity collection. The runtime rejects a create/update whose non-null value another instance already holds (`E_UNIQUE`, scanned within the active tenant scope); adapters may additionally emit a database unique constraint.                                                                                                          |
| `encrypted`  | Declares the property for field-level encryption. The runtime encrypts at store-write boundaries and decrypts on read. Requires an `EncryptionProvider` in runtime options.                                                                                                                                                                                             |
| `masked`     | Applies dynamic data masking to the property value on read. Write `masked` alone for the default `redact` strategy, or `masked(strategy, ...numbers)` to pick a strategy with optional numeric parameters. Strategies are exactly: `redact`, `partial`, `email`, `phone`, `last4`. Add a trailing `unmask when <expr>` clause to bypass masking for authorized readers. |
| `searchable` | Declares the property for full-text search indexing (string-only). Adds a `search(text, query)` built-in with AND-semantics whole-word matching. Projections emit platform-specific full-text indexes (e.g., Prisma `@@fulltext`, Drizzle GIN tsvector).                                                                                                                |
| `private`    | Prevents inclusion in public API projections. The property is omitted from generated client types and API response shapes, and the reference runtime strips every `private` property (with or without `masked`) from `getInstance`/`getAllInstances`. Execution paths (guards, constraints, computed properties) use the raw read path and still see the real value.    |
| `indexed`    | Signals to adapters that the property should be indexed for faster lookups. Projections emit platform-specific index declarations.                                                                                                                                                                                                                                      |

```manifest
entity User {
  property required id: string
  property required email: string
  property optional displayName: string = ""
  property readonly createdAt: number = 0
}
```

### Default values

Any property can carry a literal default. When you create an instance and omit that property, the runtime applies the default. Explicitly supplying a value — including an empty string `""` — skips the default entirely.

```manifest
entity Task {
  property status: string = "pending"
  property priority: number = 1
  property completed: boolean = false
}
```

<Note>
  Defaults apply only when the property is **omitted** from the creation payload. An empty string
  `""` counts as a provided value and overrides the default.
</Note>

## Relationships

Relationships declare connections between entities. The runtime resolves them synchronously from the current store context.

| Keyword     | Cardinality      | Returns                                                          |
| ----------- | ---------------- | ---------------------------------------------------------------- |
| `hasMany`   | One-to-many      | Array of related instances (never `null`; empty array when none) |
| `hasOne`    | One-to-one       | Single related instance or `null`                                |
| `belongsTo` | Many-to-one      | Single related instance or `null`                                |
| `ref`       | Simple reference | Single related instance or `null`                                |

```manifest
entity Author {
  property required name: string
  hasMany books: Book
  computed bookCount: number = self.books.length
}

entity Book {
  property required title: string
  property year: number = 2024
  belongsTo author: Author
}
```

For `belongsTo` and `ref`, the foreign key property on the source instance holds the target ID. For `hasMany` and `hasOne`, the runtime queries the target entity for instances whose foreign key matches the current instance's ID.

<Tip>
  Accessing a relationship on a non-existent instance returns `null` for singular relationships and
  `[]` for `hasMany`. Circular relationships are handled gracefully.
</Tip>

## Constraints

Constraints are boolean expressions that validate entity state. You can attach them at the entity level (evaluated whenever an instance is created or mutated) or at the command level (evaluated before that command runs — see [Commands](/language/commands)).

```manifest
entity Product {
  property required name: string
  property price: number = 0

  constraint pricePositive: self.price > 0 "Price must be positive"
  constraint nameNotEmpty: self.name != "" "Name cannot be empty"
}
```

### Constraint severity

Each constraint has a severity that controls what happens when it evaluates to `false`:

| Severity | Behavior                                                                                                                                                                              |
| -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `ok`     | Informational only. Does not halt execution. The runtime forces `passed = true` — an `ok` constraint never appears as a failure. The expression is still evaluated for observability. |
| `warn`   | Produces a `ConstraintOutcome` with the real result but does **not** halt execution.                                                                                                  |
| `block`  | Halts execution when the expression evaluates to `false` (default).                                                                                                                   |

```manifest
entity Task {
  property status: string = "pending"
  property priority: number = 1
  property dueDate: number = 0

  constraint okInfo {
    expression: priority > 0
    severity: ok
    message: "Priority is positive"
  }

  constraint warnHighPriority {
    expression: priority <= 5
    severity: warn
    message: "Priority is higher than recommended"
  }

  constraint blockStatus {
    expression: status in ["pending", "in_progress", "completed", "cancelled"]
    severity: block
    message: "Status must be one of the allowed values"
  }
}
```

The long-form block syntax also supports `messageTemplate` and `details` for structured diagnostics:

```manifest
constraint amountLimit:block self.amount > 10000 {
  messageTemplate: "Amount {currentAmount} exceeds limit of {maxAmount}"
  details: {
    maxAmount: 10000
    currentAmount: self.amount
    excessAmount: self.amount - 10000
  }
}
```

### Overrideable constraints

Mark a `block` constraint `overrideable` to allow authorized bypass at runtime. Pair it with an `overridePolicy` to control who can authorize the override.

```manifest
entity FinancialRecord {
  property required amount: number = 0
  property required status: string = "pending"

  command process() {
    constraint overrideable requiresApproval {
      expression: self.status == "approved" or self.amount <= 500
      severity: block
      message: "Must be approved before processing"
      overridePolicy: canApproveLargeAmounts
    }

    mutate status = "processed"
    emit RecordProcessed
  }
}

policy canApproveLargeAmounts override:
  user.role == "manager" or user.role == "admin"
  "Only managers and admins can approve large amounts"
```

When a caller supplies an `OverrideRequest` matching the constraint code, the runtime checks the policy, marks the outcome `overridden: true`, and emits a synthetic `OverrideApplied` event.

## Composite keys and foreign key structure

### Composite primary keys

Declare `key [field1, field2]` inside an entity to define a composite primary key. The listed fields together form the entity's identity — no single `id` field is needed.

```manifest
entity Order {
  key [tenantId, id]
  property required tenantId: string
  property required id: string
  property total: number = 0
}
```

When the Prisma projection sees a `key`, it emits `@@id([tenantId, id])` instead of a single `@id` field. The reference runtime also honors `entity.key`: it derives a deterministic canonical identity string from the key tuple (each component percent-encodes `%` and `|`, joined with `|`) and persists each instance under that string, so a composite-key entity is addressable by it through `getInstance`/`updateInstance`/`deleteInstance` and `runCommand`'s `instanceId`. Instances differing in any key component are distinct even when they share another column's value.

### Alternate keys

Declare `unique [field1, field2]` to define a non-PK uniqueness constraint (an alternate key). You can declare multiple.

```manifest
entity AdminChatParticipant {
  key [tenantId, id]
  unique [tenantId, threadId, userId]
  property required tenantId: string
  property required id: string
  property required threadId: string
  property required userId: string
}
```

The Prisma projection emits `@@unique([...])` for each group, and Drizzle emits the equivalent constraint. Convex, Kysely, OpenAPI, and Zod do not emit uniqueness constraints for alternate keys, and the reference runtime does not enforce alternate-key uniqueness.

### Composite foreign keys

For `belongsTo` and `ref` relationships, declare the FK columns explicitly when your relationship spans multiple columns:

```manifest
// Single-column FK using the shorthand (backward-compatible)
belongsTo author: Author with authorId

// Composite FK — explicit fields and references
belongsTo thread: AdminChatThread fields [tenantId, threadId] references [tenantId, id] onDelete cascade

// Cross-order composite FK
belongsTo proposal: Proposal fields [proposalId, tenantId] references [id, tenantId] onDelete cascade
```

`fields` names properties on the current entity; `references` names the corresponding columns on the target. If `references` is omitted, `[id]` is assumed.

**Runtime note**: The Manifest runtime resolves composite foreign keys. A `belongsTo`/`ref` with `fields.length > 1` (and the inverse `hasMany`/`hasOne`) selects the target row where every mapped `fields`/`references` column is equal, so it picks the exact row even when several targets share a first-column value. Local FK columns are paired with target columns via `references`; when `references` is absent or mismatched, the target entity's declared `key` columns are paired positionally, else the field names are assumed to match. If any local FK column is unset the relationship resolves to `null`/`[]`.

### Referential actions

Add `onDelete` and/or `onUpdate` to any `belongsTo` or `ref` to declare referential integrity behavior. Valid values: `cascade`, `restrict`, `setNull`, `setDefault`, `noAction`.

```manifest
belongsTo account: Account with tenantId onDelete restrict
belongsTo item: LineItem fields [tenantId, itemId] references [tenantId, id] onDelete cascade onUpdate restrict
```

When no action is declared, the projection emits nothing and the target platform uses its own default.

Referential actions **are** enforced in the reference runtime via `ReferentialActionApplier` (`src/manifest/runtime-referential-actions.ts`, wired at `runtime-engine.ts:1245-1270`). The runtime applies cascade/restrict/setNull/setDefault semantics on `deleteInstance()` based on the IR relationship configuration. Projection adapters also emit referential-integrity constraints (for example Prisma `onDelete`/`onUpdate`).

## Concurrency controls (vNext)

Entities can declare optimistic concurrency controls using `versionProperty` and `versionAtProperty`. The runtime compares the caller-supplied version against the stored value on every mutation.

```manifest
entity Document {
  property required id: string = ""
  property title: string = ""
  property content: string = ""

  versionProperty version: number
  versionAtProperty versionAt: number

  command update(newTitle: string, currentVersion: number) {
    guard self.version == currentVersion
    mutate title = newTitle
    emit DocumentUpdated
  }
}
```

If the caller's version does not match, the runtime returns a `ConcurrencyConflict` and does not apply any mutations:

```typescript
{
  entityType: "Document",
  entityId: "doc-1",
  expectedVersion: 2,
  actualVersion: 3,
  conflictCode: "VERSION_MISMATCH"
}
```

## State transitions (vNext)

Declare allowed state changes with the `transition` keyword. The runtime validates every mutation of the property against the rules before applying it.

```manifest
entity Document {
  property required id: string
  property status: string = "draft"

  transition status from "draft" to ["review", "archived"]
  transition status from "review" to ["published", "draft"]
  transition status from "published" to ["archived"]

  command submit() {
    mutate status = "review"
  }

  command approve() {
    mutate status = "published"
  }

  command archive() {
    mutate status = "archived"
  }
}
```

<Note>
  Transition validation runs **before** entity constraint validation. If the current value has no
  matching `from` rule, the transition is unconstrained from that state. Properties not mentioned in
  any transition rule are always unconstrained.
</Note>

## Default policies (vNext)

Attach a `default policy` to an entity to apply it to every command that does not declare its own policy list:

```manifest
entity Task {
  property required title: string
  property status: string = "pending"

  default policy RequireAuth execute: user.id != null "Authentication required"

  command complete() {
    guard self.assigneeId == user.id or user.role == "admin"
    mutate status = "completed"
    emit TaskCompleted
  }

  command reassign(newAssigneeId: string) {
    guard user.role == "admin" or user.role == "manager"
    mutate assigneeId = newAssigneeId
  }
}
```

Both `complete` and `reassign` inherit `RequireAuth` because neither declares an explicit policy. See [Guards and Policies](/language/guards-policies) for the full inheritance rules.

## Cross-entity constraints

Constraints can reference properties on related entities through relationship traversal. The runtime resolves the relationship chain and evaluates the constraint against the related instance's properties.

```manifest
entity Customer {
  property required id: string
  property status: string = "active"
}

entity Order {
  property required id: string
  property required customerId: string
  property amount: number = 0

  belongsTo customer: Customer with customerId

  constraint customerActive: self.customer.status == "active"
    "Customer must be active to place orders"
}
```

The runtime enriches resolved relationship instances with `_entity` metadata, enabling arbitrary-depth chaining (e.g., `self.order.customer.address.city`). When a relationship resolves to `null` (missing or nonexistent target), any member access on it evaluates to `null`, which typically causes the constraint to fail.

<Note>
  Cross-entity constraints work because the runtime resolves relationships synchronously from the
  current store context. No parser or IR compiler changes are needed -- the existing infrastructure
  handles nested member access syntax and compilation.
</Note>

## Pagination

**Not shipped.** `pagination-api` remains in the no-implementation / backlog inventory (`docs/FEATURE-LIST.md` Category 3). The reference runtime's `getAllInstances` has no limit/offset/cursor API, and the Next.js projection does not emit paginated list query parameters as described above. **Documentation gap — not verified against shipped Manifest.** Paginate in the host application or store query layer if needed.


---

## events

URL: https://manifest-b1e8623f.mintlify.app/language/events

---
title: 'Events: typed outbox and pub/sub primitives'
description: 'Declare typed events with channels and payload schemas. Commands emit them via the emits keyword. The runtime logs each emission with full metadata.'
sidebarTitle: 'Events'
---

Events in Manifest are first-class declarations. You define a schema, assign a channel, and commands reference the event by name using the `emit` action. Every emission is recorded in the runtime's event log with a payload, timestamp, and workflow metadata.

## Declaring an event

Use the `event` keyword followed by a name, a channel string, and an optional payload schema block:

```manifest
event TaskCompleted: "task.completed" {
  taskId: string
  completedBy: string
}

event CounterIncremented: "counter.incremented" {
  counterId: string
}
```

The channel (`"task.completed"`) is a free-form string. It appears on every `EmittedEvent` and can be used by subscribers to route messages.

Payload fields are typed with `string`, `number`, `boolean`, or `timestamp`. They serve as the schema contract — the runtime populates the payload from command input and the last action result.

## Emitting events from commands

Use the `emit` action inside a command body to emit an event:

```manifest
entity Counter {
  property value: number = 0

  command increment() {
    mutate value = value + 1
    emit CounterIncremented
  }

  command reset() {
    mutate value = 0
    emit CounterReset
  }
}

event CounterIncremented: "counter.incremented" {
  counterId: string
}

event CounterReset: "counter.reset" {
  counterId: string
}
```

`emit` / `publish` **actions** run in action order during the
actions phase (not after). Separately, a command's declared `emits` list is processed after
actions succeed (`semantics.md` steps 6–7). Multiple emissions keep declaration / action order
within that phase.

## EmittedEvent shape

Every emitted event is recorded as an `EmittedEvent` object in `CommandResult.emittedEvents`:

```typescript
interface EmittedEvent {
  name: string; // Event name, e.g. "TaskCompleted"
  channel: string; // Channel string, e.g. "task.completed"
  payload: object; // Command input + last action result
  timestamp: number; // Milliseconds since epoch (from runtime time source)
  emitIndex: number; // Zero-based index within this runCommand invocation (vNext)

  // Workflow metadata (vNext — present when supplied in command options)
  correlationId?: string;
  causationId?: string;
}
```

The `payload` object contains the command's input parameters and the last action result. This gives event consumers enough context to reconstruct what triggered the emission.

## Workflow metadata (vNext)

Pass `correlationId` and `causationId` in the command options to propagate identifiers across a multi-step workflow. The runtime attaches them to every event emitted during that invocation.

Signature is `runCommand(commandName, input, options?)` — three
arguments max. `entityName`, `instanceId`, `correlationId`, and `causationId` all go in `options`.

```typescript
const result = await runtime.runCommand(
  'complete',
  {},
  {
    entityName: 'Order',
    instanceId: 'order-123',
    correlationId: 'workflow-abc',
    causationId: 'step-2',
  },
);

// result.emittedEvents[0].correlationId === "workflow-abc"
// result.emittedEvents[0].causationId === "step-2"
```

`emitIndex` is a per-command zero-based counter. It resets to `0` at the start of each `runCommand` call. It is not a global sequence — cross-command ordering is the caller's responsibility.

<Note>
  Given identical IR, identical runtime context, identical input, and identical options, emitted
  events will always have identical `emitIndex` values. This is a determinism guarantee.
</Note>

## Subscribing to events

Use `runtime.onEvent(listener)` to register a listener. The listener receives **every** emitted event — filter by `event.name` inside the callback. It returns an unsubscribe function.

```typescript
import { RuntimeEngine } from '@angriff36/manifest';

const runtime = new RuntimeEngine(ir, {
  user: { id: 'worker-1', role: 'system' },
});

const unsubscribe = runtime.onEvent((event) => {
  if (event.name === 'TaskCompleted') {
    console.log('Task completed:', event.payload);
    sendNotification(event.payload.completedBy);
  }
  if (event.name === 'OrderCreated') {
    console.log('New order on channel:', event.channel);
    console.log('Correlation:', event.correlationId);
  }
});

// Call unsubscribe() when the listener is no longer needed.
```

Listeners fire synchronously during `runCommand`, after all actions and before `CommandResult` is returned.

## Complete example

```manifest
entity Order {
  property required id: string
  property status: string = "pending"
  property total: number = 0

  command addItem(amount: number) {
    mutate total = self.total + amount
    emit OrderUpdated
  }

  command complete() {
    guard self.status == "in_progress"
    mutate status = "completed"
    emit OrderCompleted
  }
}

store Order in memory

event OrderUpdated: "order.updated" {
  orderId: string
  total: number
}

event OrderCompleted: "order.completed" {
  orderId: string
  finalTotal: number
}
```

Same `runCommand(commandName, input, options)` shape — not entity-first.

```typescript
const runtime = new RuntimeEngine(ir);

runtime.onEvent((event) => {
  if (event.name === 'OrderCompleted') {
    console.log(`Order ${event.payload.orderId} completed.`);
  }
});

const result = await runtime.runCommand('complete', {}, {
  entityName: 'Order',
  instanceId: 'order-001',
});

console.log(result.emittedEvents);
// [
// {
// name: "OrderCompleted",
// channel: "order.completed",
// payload: { /* command input + last action result */ },
// timestamp: 1748131200000,
// emitIndex: 0
// }
// ]
```

## OverrideApplied event (vNext)

When an authorized constraint override is applied, the runtime synthesizes and emits an `OverrideApplied` event. This event is not declared in your `.manifest` source — it is injected by the runtime automatically:

```typescript
{
  name: "OverrideApplied",
  channel: "system",
  payload: {
    constraintCode: "requiresApproval",
    reason: "Emergency processing authorized by finance lead",
    authorizedBy: "user-999",
    timestamp: 1748131200000,
    commandName: "process",
    entityName: "FinancialRecord",
    instanceId: "rec-42"
  }
}
```

This event appears in `CommandResult.emittedEvents` alongside any command-declared events.

## Webhook inbound triggers

Webhooks declare inbound HTTP endpoints that trigger commands when an external system sends a request. The reference runtime verifies the HMAC signature (when declared), deduplicates by idempotency key, evaluates the payload transform, and dispatches the command — every step fail-closed.

```manifest
entity Order {
  property required id: string
  property status: string = "pending"
  property providerRef: string = ""

  command handlePayment(providerRef: string, status: string) {
    guard status == "completed"
    mutate providerRef = providerRef
    mutate status = "paid"
    emit PaymentReceived
  }
}

webhook paymentCallback "/webhooks/payment" run Order.handlePayment
  idempotencyHeader: "X-Idempotency-Key"
  signature {
    algorithm: "hmac-sha256"
    header: "X-Signature"
    secret: "context.webhookSecret"
  }
```

### Webhook configuration

A webhook is declared as `webhook <name> "<path>" run [Entity.]<command>`, where the path string is the generated route and `run` names the command to invoke. The declaration may be followed by optional colon clauses:

| Field                | Description                                                                                                                   |
| -------------------- | ----------------------------------------------------------------------------------------------------------------------------- |
| `method:`            | HTTP method as a string (e.g. `method: "PUT"`); defaults to `POST` when omitted                                               |
| `idempotencyHeader:` | Optional header name carrying an idempotency key for deduplication                                                            |
| `transform:`         | Optional `{ <param>: <expr>, ... }` block mapping request data onto command parameters                                        |
| `signature { ... }`  | Optional verification block with `algorithm` (`hmac-sha256` or `hmac-sha512`), `header`, and `secret` (a context path string) |

<Note>
  When a `signature` block is declared, the reference runtime verifies the HMAC over the exact
  received request bytes before the command is dispatched, comparing with `crypto.timingSafeEqual`.
  The provided signature may be bare hex or a `sha256=`/`sha512=`-prefixed hex (GitHub convention).
  A missing or invalid signature is rejected `401` and no command runs; an unresolvable `secret`
  context path or an unsupported `algorithm` is a fail-closed `500`. A webhook with **no**
  `signature` block is unauthenticated by design — declare one to require authentication. The full
  request pipeline (matching, idempotency, transform, dispatch statuses) is specified in
  `docs/spec/semantics.md` § "Webhooks (Inbound HTTP Triggers)".
</Note>

## Event sourcing store

The `event-sourced` store target persists entity state as an append-only event log rather than overwriting entity records. Each command produces events that are appended to the log, and entity state is reconstructed by replaying events from the beginning.

```manifest
store Order in event-sourced {
  snapshotInterval: 100
  exposeEventLog: true
}
```

The event-sourced store supports:

- **Append-only writes** — events are never modified or deleted
- **Snapshotting** — periodic snapshots (default: every 100 events) avoid replaying the full log
- **Replay** — entity state is reconstructed by replaying events from the last snapshot (or from beginning)
- **getEventLog()** — when `exposeEventLog: true`, returns the append-only log for an entity

<Warning>
  Event sourcing is a storage strategy, not a runtime behavior change. Commands execute identically
  regardless of whether the backing store is memory, postgres, or event-sourced. The difference is
  how state is persisted and recovered.
</Warning>

<Callout type="info">
**Implemented (2026-07-22)**: `EventSourcedStore` ships in the reference runtime (`stores/event-sourced.ts:37-140`, auto-wired at `runtime-engine.ts:1324-1327`). See [Event Sourced Store](/adapters/event-sourced-store) for full documentation.
</Callout>


---

## expressions

URL: https://manifest-b1e8623f.mintlify.app/language/expressions

---
title: 'Expression built-ins: strings, numbers, dates, and aggregates'
sidebarTitle: 'Expression built-ins'
description: "Manifest's deterministic, side-effect-free expression library — string and numeric helpers, UTC date components, and collection aggregates with mapper and predicate lambdas."
---

Manifest's expression language ships a library of deterministic, side-effect-free built-in functions: string and numeric helpers, UTC date components, and aggregate operations over collections. The aggregates accept optional mapper or predicate lambdas, letting computed properties summarize related entities declaratively. All functions are side-effect-free and produce identical results given identical input and runtime context.

## Scalar and string built-ins

String and numeric helpers can be used anywhere expressions are evaluated — `compute` bindings, guards, computed properties, and constraints:

```manifest
command testStringOps(input: string) {
  guard input != ""
  compute cleaned = trim(input)
  compute partCount = count(split(input, ","))
  compute hasPrefix = startsWith(input, "hello")
  compute hasSuffix = endsWith(input, "world")
  compute swapped = replace(input, "old", "new")
  compute upper = toUpperCase(input)
  compute lower = toLowerCase(input)
  compute charCount = length(input)
  compute alsoCharCount = input.length
  compute slice = substring(input, 0, 5)
  compute foundAt = indexOf(input, "world")
}
```

`input.length` is sugar for `length(input)` on strings and arrays (same for `self.tags.length`). Prefer either form; they evaluate the same.

Numeric helpers `abs`, `round`, `floor`, `ceil`, `min`, and `max` and the array helper `sum` appear in the same fixture, and the date components `year`/`month`/`day`/`hours`/`minutes`/`seconds` operate on a millisecond timestamp.

String helpers wrap the corresponding `String.prototype` methods (returning the input unchanged for non-strings), numeric helpers delegate to `Math`, and `min`/`max` are variadic over numeric arguments. The date components use UTC methods so results are timezone-independent — see [Types](/language/types) for the full list.

> **Note:** Date conversion / duration builtins also exist (`dateOf`, `timeOf`, `datetimeOf`, `addDuration`, `durationBetween`, `durationDays` / `Hours` / `Minutes` / `Seconds`) — see `docs/spec/builtins.md`.

## Aggregate built-ins with lambdas

The aggregates reduce a collection — typically a `hasMany` relationship reached via `self.` — to a single value, optionally transforming or filtering each element with a lambda:

```manifest
entity Order {
  property required customerName: string
  property status: string = "pending"

  hasMany lineItems: LineItem

  computed totalAmount: number = sum(self.lineItems, (item) => item.price * item.quantity)
  computed itemCount: number = count_of(self.lineItems)
  computed averagePrice: number = avg(self.lineItems, (item) => item.price)
  computed cheapestItem: number = min_of(self.lineItems, (item) => item.price)
  computed mostExpensiveItem: number = max_of(self.lineItems, (item) => item.price)
  computed highValueCount: number = count_of(self.lineItems, (item) => item.price * item.quantity > 50)
}

entity LineItem {
  property required price: number
  property quantity: number = 1

  belongsTo order: Order

  computed lineTotal: number = price * quantity
}

store Order in memory
store LineItem in memory
```

For `sum`, `avg`, `min_of`, and `max_of` the lambda maps each element to a number; for `count_of` the lambda is a predicate selecting which elements to count.

The behavior with a lambda is:

- `sum` adds each mapped number.
- `avg` averages the mapped numbers (`0` for an empty collection).
- `min_of` / `max_of` return the smallest / largest mapped number (`undefined` for an empty collection).
- `count_of` returns the total length without a predicate, or the count of elements for which the predicate is truthy.

Without a lambda, `sum` and `avg` operate on the raw numeric elements and `count_of` returns the collection length. Each aggregate guards against a non-array first argument: `sum` returns its argument unchanged, `avg` returns `0`, `count_of` returns `0`, `min_of`/`max_of` return `undefined`, and `filter`/`map` return an empty array (or the array unchanged when no callback is supplied).

Because aggregating over a `hasMany` relationship requires resolving related instances asynchronously, the lambda-bearing branches return promises that the evaluator awaits. The element mapper is invoked once per element and its result awaited, keeping evaluation deterministic for a fixed runtime context.

<Note>
  Two distinct count helpers exist: `count(array)` (a plain length used above in `count(split(input,
  ","))`) and `count_of(collection, predicate?)` (the aggregate). Likewise `min`/`max` are variadic
  over scalar arguments, which is different from `min_of`/`max_of`, which reduce a collection. Using
  an aggregate on a non-array yields the safe fallbacks listed above rather than an error.
</Note>

## Standard library

The Manifest standard library (`stdlib`) extends the built-in expression functions with additional utilities for common operations. These functions are available automatically — no import is required.

### Full-text search

The `search(text, query)` function performs full-text matching against a `searchable` property:

```manifest
entity Article {
  property required title: string
  property searchable content: string

  constraint relevant: search(self.content, "manifest dsl")
}
```

The `searchable` modifier on string properties signals to projections that a full-text index should be created (e.g., Prisma `@@fulltext`, Drizzle GIN index). The `search()` function is the runtime counterpart that evaluates the match.

### Feature flags in expressions

The `flag(name)` function resolves a feature flag value at expression evaluation time:

```manifest
constraint betaEnabled: flag("new-pricing") == true

guard flag("max-order-limit") == false or self.total < flag("max-order-limit")
```

`flag()` supports boolean, string, number, and object flag values. See [Feature flags](/language/feature-flags) for the full feature flag system.

### Extended built-ins

Additional built-in functions available in the standard library:

| Function  | Signature                   | Description                              |
| --------- | --------------------------- | ---------------------------------------- |
| `between` | `between(value, low, high)` | Returns `true` if `low <= value <= high` |
| `matches` | `matches(text, pattern)`    | Tests `text` against a regex `pattern`   |
| `search`  | `search(text, query)`       | Full-text search match                   |
| `flag`    | `flag(name)`                | Resolve a feature flag value             |

## Custom expression functions

There is **no** `registerBuiltin` export. Custom functions are injected via `RuntimeOptions.customBuiltins` (a `Map<string, Function>`), typically collected by the plugin loader from `BuiltinFunctionPlugin` entries (`docs/spec/builtins.md` § Custom Expression Functions). Core builtins always win name collisions.

### Registering a custom function

```typescript
import { RuntimeEngine } from '@angriff36/manifest';
import { definePlugin } from '@angriff36/manifest/plugin-api';

// Plugin form (preferred for packages):
export default definePlugin({
  manifest: {
    name: '@acme/custom-functions',
    version: '1.0.0',
    pluginApiVersion: '1',
    manifestVersion: '>=1.0.0',
  },
  builtins: [
    {
      name: 'discount',
      purity: 'pure',
      arity: 2,
      fn: (price: unknown, tier: unknown) => {
        if (typeof price !== 'number') return price;
        if (tier === 'premium') return price * 0.8;
        if (tier === 'standard') return price * 0.9;
        return price;
      },
    },
  ],
});

// Direct RuntimeOptions form (tests / hosts):
const customBuiltins = new Map<string, (...args: unknown[]) => unknown>();
customBuiltins.set('discount', (price, tier) => {
  /* ... */
});
const runtime = new RuntimeEngine(ir, {}, { customBuiltins });
```

Once wired, the function is available in Manifest expressions:

```manifest
entity Order {
  property total: number = 100
  property tier: string = "standard"

  computed discountedTotal: number = discount(self.total, self.tier)
}
```

### Constraints on custom functions

<Warning>
  Custom functions are not deterministic by default — they can reference external state, make
  network calls, or produce different results for the same inputs. If you use custom functions in
  guards or constraints, you are responsible for ensuring they produce consistent results within a
  single `runCommand` invocation. Violating this breaks the Manifest determinism guarantee.
</Warning>

For this reason, custom functions used in expressions should be pure functions with no side effects. If external data is needed, inject it through the runtime context (`user`, `context.*`) rather than calling out from inside a custom function.


---

## feature flags

URL: https://manifest-b1e8623f.mintlify.app/language/feature-flags

---
title: 'Feature flags: runtime-resolved gating'
sidebarTitle: 'Feature flags'
description: 'Gate commands and computed properties on named feature flags with the flag() built-in, resolved through a runtime-supplied provider and off by default.'
---

The `flag(name)` built-in resolves a named feature flag through a runtime-provided provider, letting guards and computed properties gate behavior on flags declared in the program but evaluated against external flag state. When no provider is configured, `flag` returns `false`, so features are off by default.

## Syntax

`flag(name)` is used in guards and computed properties. The single argument is the flag name as a string literal, and `flag(...)` composes with other expressions:

```manifest
entity FeatureGatedEntity {
  property required id: string
  property status: string = "inactive"

  command activate() {
    guard flag("new-activation-flow")
    mutate status = "active"
  }

  command deactivate() {
    guard flag("allow-deactivation") and self.status == "active"
    mutate status = "inactive"
  }
}

entity FlagInspector {
  property required id: string
  property flagValue: string = ""

  computed isNewUIEnabled: boolean = flag("new-ui")
}

store ViewerGatedEntity in memory
store FlagInspector in memory
```

Store name must match the entity (`ViewerGatedEntity`), not a non-existent `FeatureGatedEntity`.

## Behavior

The `flag` built-in returns `false` if the argument is not a string. Otherwise, if a `flagProvider` runtime option is set, it delegates to that provider — `this.options.flagProvider(name)` — and returns whatever the provider returns. When no provider is configured, it returns `false`.

The provider is typed as `flagProvider?: (name: string) => unknown`. Its return value is passed through unchanged, so a flag can resolve to a boolean, string, number, or object depending on the provider — the engine does not coerce it. This is the documented integration point for wiring an external system such as LaunchDarkly:

```typescript
flagProvider: (name) => launchDarklyClient.variation(name, false);
```

Because resolution happens entirely through the runtime-supplied provider and the IR carries only the `flag("name")` call, the same compiled program yields different gating behavior in different runtime contexts without any IR change — consistent with Manifest's rule that variability enters through runtime context, not by editing the IR. In a guard, a falsey `flag(...)` result halts command execution at that guard in order, the same as any other guard expression.

Feature flags are a runtime expression concern, not a schema construct, so there is no dedicated projection mapping; the `flag("name")` call is carried in the IR like any other expression.

<Warning>
  The conformance fixture `66-feature-flags.manifest` has an expected `.ir.json` but **no**
  `results.json`. The IR shape of `flag(...)` is conformance-locked, but the runtime gating behavior
  described above is verified only against the engine source and unit-level reasoning, not against a
  recorded conformance results fixture. Treat the runtime semantics as confirmed in code but not yet
  pinned by an executable conformance result.
</Warning>

<Note>
  The safe default is off: with no `flagProvider`, every `flag(...)` returns `false`, so guards
  depending on a flag deny by default. Provider return values are not validated or coerced by the
  engine, so a provider that returns a non-boolean passes that value straight into the surrounding
  expression.
</Note>


---

## guards policies

URL: https://manifest-b1e8623f.mintlify.app/language/guards-policies

---
title: 'Guards and policies: access control'
description: 'Use guards for precondition checks and policies for authorization. Both are boolean expressions evaluated in strict order before any action runs.'
sidebarTitle: 'Guards & Policies'
---

Manifest separates authorization into two distinct mechanisms: **policies**, which control who is allowed to execute a command at all, and **guards**, which verify that the current state satisfies the preconditions for the command to proceed. Both are evaluated before any action runs, and neither can be bypassed.

## Guards

A guard is a boolean expression declared inside a command body. Guards are evaluated in declaration order. Execution halts on the first expression that evaluates to `false` — no later guards run, and no actions execute.

```manifest
entity Task {
  property completed: boolean = false
  property priority: number = 1

  command complete() {
    guard not self.completed
    mutate completed = true
    emit TaskCompleted
  }

  command setPriority(level: number) {
    guard level >= 1 and level <= 5
    mutate priority = level
  }
}
```

### Guard diagnostics

When a guard fails, the runtime returns a `guardFailure` in the `CommandResult` with:

- `index` — 1-based position of the failing guard
- `expression` — the source expression as a string
- `resolved` — array of `{ expression, value }` pairs showing what each sub-expression evaluated to

This gives you enough information to explain the failure to the caller without any manual debugging.

```typescript
if (result.guardFailure) {
  console.error(`Guard ${result.guardFailure.index} failed: ${result.guardFailure.expression}`);
  for (const r of result.guardFailure.resolved) {
    console.error(` ${r.expression} => ${r.value}`);
  }
}
```

### Available bindings in guards

Guard expressions evaluate in the command's evaluation context. You have access to:

| Binding             | Value                                                             |
| ------------------- | ----------------------------------------------------------------- |
| `self` / `this`     | The current entity instance (or `null` when no instance is bound) |
| `user`              | The current user object, or `null` when unauthenticated           |
| `context`           | The runtime context object                                        |
| `context.tenantId`  | Active tenant ID (if provided by caller)                          |
| `context.orgId`     | Active organization ID (if provided)                              |
| `context.actorId`   | Acting user ID (if provided)                                      |
| `context.requestId` | Caller-supplied request ID                                        |
| Command params      | Named parameters from the command signature                       |
| `now()`             | Current time as milliseconds since epoch                          |
| `uuid()`            | A new globally unique identifier                                  |

<Warning>
  If a guard references `user` and no user is provided to the runtime, the guard correctly fails.
  This is not a bug — it is the expected behavior. The runtime does not inject a permissive default
  `user` to make demos work.
</Warning>

### Operators in guard expressions

Guards support the full expression language:

| Category    | Operators                        |
| ----------- | -------------------------------- |
| Arithmetic  | `+`, `-`, `*`, `/`, `%`          |
| Comparison  | `==`, `!=`, `<`, `>`, `<=`, `>=` |
| Logical     | `and`, `or`, `not`, `!`          |
| Membership  | `in`, `contains`                 |
| Conditional | `? :` (ternary)                  |

`==` uses loose equality (`undefined == null` is `true`). Use `!=` for strict inequality. Note: `is not` parses as `x is (not y)` (a type check combined with a logical negation), not as a single inequality operator.

## Policies

A policy is a named boolean expression with a **scope** that controls when it is checked. Policies are declared at entity level (inside an entity block) or at module level (outside any entity block).

```manifest
policy adminOnly execute: user.role == "admin" "Only administrators can execute commands"
```

The expression after the scope keyword is evaluated before any command runs. If it returns `false`, the runtime returns a `policyDenial` result and no further execution occurs.

### Policy scopes

| Scope      | When enforced                                                |
| ---------- | ------------------------------------------------------------ |
| `execute`  | Before any command execution                                 |
| `read`     | At the runtime read gate — `getInstance` / `getAllInstances` |
| `write`    | On entity write operations (not enforced by default runtime) |
| `all`      | Both command execution and the read gate                     |
| `override` | Before authorizing a constraint override (vNext)             |

The runtime enforces `execute` and `all` scopes before command execution. `read` (and `all`) scopes are **also** enforced at a single central read gate in `getInstance` / `getAllInstances`: a denied read fails closed — `getInstance` returns `undefined` (no existence leak) and `getAllInstances` omits the denied rows. A read policy referencing `self.*` is evaluated per row; a context-only policy is evaluated once per call. The internal execution read path (guards, actions, computed properties) is un-gated, so read policies never change command results. `write` and `delete` scopes are still enforced only at command execution (via `command.policies`), not on the store — they are otherwise available for custom adapter or projection logic.

### Binding a policy to an entity

A policy applies to all commands on a specific entity when you reference it inside the entity block or use the entity name in a standalone policy:

```manifest
entity Document {
  property required title: string
  property isPublic: boolean = false

  command makePublic() {
    mutate isPublic = true
    emit DocumentPublished
  }
}

policy adminOnly execute: user.role == "admin" "Only admins can execute"
```

In this example, `adminOnly` is a module-level policy. Use `default policy` inside an entity block to apply a policy to every command on that entity.

### Per-command access restrictions

Policies are entity-level or module-level — there is no inline policy syntax inside a command body
(**confirmed 2026-07-15**; command-body policy clause remains an Appendix E backlog item). To
enforce stricter access on a single command, use a guard inside that command:

```manifest
entity InventoryItem {
  property stock: number = 0

  default policy KitchenRoles execute: user.role in ["kitchen_staff", "kitchen_lead", "manager", "admin"]

  command consume(quantity: number) {
    // Inherits KitchenRoles policy
    guard quantity > 0
    mutate stock = self.stock - quantity
  }

  command adjust(delta: number) {
    // Per-command stricter access: add a guard rather than an inline policy
    guard user.role in ["kitchen_lead", "manager", "admin"]
    mutate stock = self.stock + delta
  }
}
```

<Note>
  Guards produce a `guardFailure` result; policies produce a `policyDenial` result. Use guards for
  state-based preconditions and additional per-command role checks; use policies for entity-wide
  authorization.
</Note>

## Default policies (vNext)

Use `default policy` on an entity to apply a policy to every command
on that entity. There is **no** source syntax for a command-body / per-command `policies` clause
(Appendix E backlog). Per-command stricter checks use **guards**. Spec/IR may carry
`IRCommand.policies`, but the compiler today only expands entity `default policy` names into that
array.

```manifest
entity Task {
  property required title: string
  property status: string = "pending"
  property assigneeId: string

  default policy RequireAuth execute: user.id != null "Authentication required"

  command complete() {
    guard self.assigneeId == user.id or user.role == "admin"
    mutate status = "completed"
    emit TaskCompleted
  }

  command reassign(newAssigneeId: string) {
    guard user.role == "admin" or user.role == "manager"
    mutate assigneeId = newAssigneeId
  }
}
```

Both `complete` and `reassign` inherit `RequireAuth`. Add a guard inside any command for additional per-command access checks.

### Inheritance rules

1. If the entity declares a `default policy`, every command on that entity inherits it (expanded into IR at compile time).
2. No `.manifest` syntax currently declares per-command policies; use guards for stricter per-command checks.
3. If no default policy applies, no policy check runs (the scanner will warn).

The IR compiler expands default policies into each command's effective policy list at compile time. The runtime always evaluates an explicit, expanded list — it does not perform inheritance at runtime.

## Override policies (vNext)

The `override` scope is a special policy used to authorize constraint bypasses. When a constraint is marked `overrideable`, the runtime evaluates the referenced override policy before granting the bypass:

```manifest
entity FinancialRecord {
  command process() {
    constraint overrideable requiresApproval {
      expression: self.status == "approved" or self.amount <= 500
      severity: block
      message: "Must be approved before processing"
      overridePolicy: canApproveLargeAmounts
    }

    mutate status = "processed"
    emit RecordProcessed
  }
}

policy canApproveLargeAmounts override:
  user.role == "manager" or user.role == "admin"
  "Only managers and admins can approve large amounts"
```

If the override policy passes, the constraint outcome is marked `overridden: true`. Both override paths are audited: the auto-policy path emits the same synthetic `OverrideApplied` event the explicit-request path emits, with `authorizedBy` derived from the acting user in context (falling back to `policy:<name>` when no user is present) and the event `reason` recording the authorizing policy. Attempts to override a constraint that is not marked `overrideable` are always rejected.

## Default-deny enforcement

When the scanner detects a command with no policy coverage (neither explicit nor inherited from entity defaults), it emits a warning. In strict environments, this warning can be promoted to an error:

```bash
manifest scan --strict
```

The `--strict` flag causes the scanner to exit non-zero when any command lacks policy coverage. This enforces a default-deny posture: every command must explicitly declare who is authorized to run it, or inherit a default policy from its entity.

<Warning>
  A command with no policies is **not blocked** by the runtime — it executes normally. The
  default-deny enforcement is a static analysis check (via `manifest scan`), not a runtime gate. To
  enforce it in CI, add `manifest scan --strict` to your pipeline.
</Warning>

## Roles and permission inheritance

For fine-grained authorization based on role hierarchies (e.g., Admin → Manager → User with inherited permissions), see [Roles and permissions](/language/roles). Roles integrate with policies through the `user.role` binding and the `hasPermission()` / `roleAllows()` builtins.


---

## manifest files

URL: https://manifest-b1e8623f.mintlify.app/language/manifest-files

---
title: 'Manifest files: anatomy, syntax, and best practices'
description: 'The complete reference for .manifest source files — what a file contains, the declaration grammar, types, expressions, ordering rules, what is not allowed, and house-style best practices.'
sidebarTitle: 'Manifest files (.manifest)'
---

A `.manifest` file is the human-authored source of a Manifest program. The compiler reads it through a lexer → parser → AST → IR-compiler pipeline and produces an **Intermediate Representation (IR)** — a JSON document that is the single source of truth for program semantics. Everything else (TypeScript, Prisma schemas, Convex functions, React Query hooks, and the other [projections](/projections/additional-projections)) is a _view_ generated from that IR.

This page is the canonical reference for the file format itself: what you can put in a `.manifest` file, what you cannot, how the pieces are ordered, and the conventions that keep programs deterministic. For deep dives on individual constructs, follow the cross-links into the rest of the Language section.

<Note>
  The grammar described here is the one the parser actually accepts (`src/manifest/lexer.ts`,
  `src/manifest/parser.ts`). When this page and your intuition disagree, the parser wins — and the
  conformance fixtures in `src/manifest/conformance/` are the executable proof.
</Note>

## Anatomy of a file

A file is a flat sequence of **top-level declarations**. There is no required wrapper, no `package` header, and no semicolons. Whitespace and newlines are insignificant except as token separators.

```manifest
// 1. Cross-file imports first (optional)
use "./shared/money.manifest"

// 2. Any number of declarations, in any order
enum Status { draft, published, archived }

entity Invoice {
  property required id: string
  property status: Status = draft
  property total: decimal(12, 2) = 0

  computed isPayable: boolean = total > 0 and status == published

  command release() {
    guard self.total > 0
    mutate status = published
    emit invoicePublished
  }
}

store Invoice in postgres { table: "invoices" }
event invoicePublished: "billing.invoice.published" { invoiceId: string }
```

The only hard ordering rule at the top level is that **every `use` declaration must come before all other declarations** (see [Ordering rules](#ordering-rules)). Beyond that, declarations may appear in any order; the IR preserves the order it encountered them.

### Comments

```manifest
// line comment

/*
  block comment
*/
```

### Identifiers

Identifiers match `[A-Za-z_][A-Za-z0-9_]*`. Entity, enum, and module names are conventionally `PascalCase`; properties, commands, and policies are `camelCase`. [Reserved words](#reserved-words) cannot be used as declaration names, but they _are_ allowed as member-access keys and object-literal keys (so `self.from` or `{ to: ... }` parse fine).

### Literals

| Kind    | Examples                                                                                                    |
| ------- | ----------------------------------------------------------------------------------------------------------- |
| String  | `"hello"`, `'hello'`, `` `hello` `` (all three quote styles; backticks are plain strings, not interpolated) |
| Number  | `42`, `3.14`                                                                                                |
| Boolean | `true`, `false`                                                                                             |
| Null    | `null`                                                                                                      |

## Top-level declarations

Every construct the parser accepts at the top level:

| Keyword                                  | Purpose                                            | Reference                                                          |
| ---------------------------------------- | -------------------------------------------------- | ------------------------------------------------------------------ |
| `use`                                    | Import another `.manifest` file (must be first)    | [Modules](/language/modules)                                       |
| `module`                                 | Namespace grouping of declarations                 | [Modules](/language/modules)                                       |
| `entity`                                 | A typed data structure with behavior               | [Entities](/language/entities)                                     |
| `enum`                                   | A named set of values                              | [Types](/language/types)                                           |
| `value`                                  | A value object (properties only)                   | [Types](/language/types)                                           |
| `tenant`                                 | Declare the tenant property (max one per program)  | [Tenancy](/language/tenancy)                                       |
| `command` / `async command`              | A standalone operation                             | [Commands](/language/commands) · [Async](/language/async-commands) |
| `policy`                                 | A standalone authorization rule                    | [Guards & policies](/language/guards-policies)                     |
| `store`                                  | Bind an entity to a persistence target             | [Stores](/language/stores)                                         |
| `event`                                  | Declare a publishable event                        | [Events](/language/events)                                         |
| `on`                                     | A reaction that runs a command when an event fires | [Reactions](/language/reactions)                                   |
| `saga`                                   | A multi-step workflow with compensation            | [Workflows](/language/workflows)                                   |
| `role`                                   | A role in the authorization hierarchy              | [Roles](/language/roles)                                           |
| `webhook`                                | An inbound webhook bound to a command              | [Events](/language/events)                                         |
| `schedule`                               | A cron/interval-triggered command                  | [Workflows](/language/workflows)                                   |
| `flow` / `effect` / `expose` / `compose` | **EXPERIMENTAL — NOT IMPLEMENTED** (parser accepts, emits warning, not lowered to IR; compile succeeds but has no runtime effect; see fixture 94-unlowered-constructs.manifest) | — |

A `module` block accepts a subset of these: entities, enums, commands, policies, stores, events, reactions, sagas, roles, webhooks, and schedules.

## Entities

The entity is the core construct. Its header can declare inheritance and mixins, and its body holds the rest in any order.

```manifest fragment
entity PurchaseOrder extends Auditable mixin Timestamped {
  // properties
  property required id: string
  property amount: money(12, 2) = 0
  property private internalNote: string = ""

  // computed (a.k.a. derived) values — recomputed, never stored as input
  computed isLarge: boolean = amount > 10000

  // relationships
  hasMany lines: PurchaseLine
  belongsTo vendor: Vendor

  // constraints
  constraint nonNegative: amount >= 0 "Amount cannot be negative"

  // authorization
  policy canApprove execute: user.role == "manager"
  default policy openAccess all: true

  // behavior & commands
  command approve() {
    guard self.amount > 0
    mutate status = "approved"
    emit poApproved
  }

  // concurrency, state machine, lifecycle flags
  versionProperty version: number
  transition status from "draft" to ["approved", "rejected"]
  timestamps
}
```

Sub-blocks an entity may contain: `property`, `computed`/`derived`, relationships (`hasMany`, `hasOne`, `belongsTo`, `ref`), `command`/`async command`, `constraint`, `policy` (and a `policies { … }` block / `default policy …`), `store … in …`, `versionProperty`/`versionAtProperty`, `key [...]` / `unique [...]`, `transition`, `approval`, the `timestamps` flag, and the bare `realtime` flag. See [Advanced entities](/language/advanced-entities) for `extends`, `mixin`, keys, and transitions.

Entity `behavior` blocks and bare `on Event { … }` inside an entity are **rejected at compile time** (hard error; conformance fixture 110). They have no IR representation. Use top-level reactions (`on Event run Entity.command`) or command actions instead (`docs/spec/semantics.md` § Entities).

<Warning>
  Events declared *inside* an entity are dropped with a warning. Declare `event` at the top level or
  inside a `module`.
</Warning>

### External entities

Prefix an entity with `external` to mark it as one owned by another system or file. The compiler sets `external: true` on the IR, and persistence projections (Prisma, Drizzle, Kysely, Convex, prisma-store) **skip** it rather than emitting a table or model for it — useful for referencing a table managed elsewhere.

```manifest
external entity ImportedUser {
  property required id: string
  property email: string
}
```

`external` is a contextual modifier, not a reserved word, so `property external: string` still parses as an ordinary property.

## Types

| Category      | Type names                                             |
| ------------- | ------------------------------------------------------ |
| Primitives    | `string`, `number`, `boolean`, `any`, `void`           |
| Collections   | `list`, `map`, `array<T>`, and the `T[]` postfix sugar |
| Exact decimal | `decimal(precision, scale)`, `money(precision, scale)` |
| Date/time     | `date`, `time`, `datetime`, `duration`                 |
| User-defined  | enum names, `value` object names                       |
| Nullable      | any type followed by `?` (e.g. `string?`, `Money?`)    |

<Note>
  `date`, `time`, `datetime`, and `duration` are **not reserved words** — the parser accepts any
  token as a type name, so they work as types and the corresponding date/time
  [builtins](#built-in-functions) operate on them. See [Date/time types](/language/types).
</Note>

## Property modifiers

Modifiers are written **before** the property name:

```manifest fragment
property required unique email: string
property private apiKey: string
property masked(last4) cardNumber: string unmask when user.role == "admin"
```

Reserved modifiers: `required`, `unique`, `indexed`, `private`, `readonly`, `optional`, `searchable`, `encrypted`. A default value uses `= <expr>`.

The contextual `masked` modifier accepts a strategy — `masked(redact)`, `masked(partial)`, `masked(email)`, `masked(phone)`, or `masked(last4)` — and an optional trailing `unmask when <expr>` clause. The `unmask when` clause is a compile error without `masked`.

<Warning>

</Warning>

Omitting `required` means the property is **not** required at create time (type / property defaults may apply). The `optional` modifier is a **projection hint** with no independent runtime enforcement (`docs/spec/semantics.md` § Properties). Nullability is separate: append `?` to the type (for example `string?`). Do not conflate "not required" with "nullable."

## Expressions

Expressions appear in guards, computed properties, constraints, policies, defaults, and command actions.

### Operators

Symbolic: `+ - * / %`, `== != < > <= >=`, `&& || !`, the ternary `? :`, and member/access operators `.` `?.` `[]`. Word operators: `and`, `or`, `not`, `is`, `in`, `contains`. Precedence, lowest to highest: ternary → `or` → `and` → equality (`== != is in contains`) → comparison (`< > <= >=`) → additive → multiplicative → unary (`!`, `-`, `not`) → postfix (`.`, `?.`, calls, index) → primary. Lambdas are written `(a, b) => expr` and are used by aggregate builtins.

### Binding namespaces

Expressions may reference exactly four namespaces. These are spec-guaranteed bindings — guards must reference them rather than relying on inferred state.

| Binding    | Meaning                                             |
| ---------- | --------------------------------------------------- |
| `self.`    | The current entity instance (e.g. `self.status`)    |
| `this.`    | Alias for the current instance                      |
| `user.`    | The calling principal (e.g. `user.role`, `user.id`) |
| `context.` | Runtime context supplied at execution time          |

<Note>
  Prefer `self.` over `this.` in new code. `self` is a reserved binding; `this` works only because
  it happens to be an ordinary identifier, so `self` is the spec-blessed form.
</Note>

### Built-in functions

The runtime provides a fixed set of builtins (`getBuiltins`, `src/manifest/runtime-engine.ts`). Core builtins always win over a plugin of the same name, so these names are effectively reserved in expressions:

- **General:** `now`, `uuid`
- **Strings:** `trim`, `split`, `count`, `startsWith`, `endsWith`, `replace`, `toUpperCase`, `toLowerCase`, `length`, `substring`, `indexOf`, `matches`, `search`
- **Math:** `abs`, `round`, `floor`, `ceil`, `min`, `max`, `between`
- **Aggregates:** `sum`, `avg`, `min_of`, `max_of`, `count_of`, `filter`, `map`, `flat_map`, `unique_of`
- **Date/time:** `year`, `month`, `day`, `hours`, `minutes`, `seconds`, `dateOf`, `timeOf`, `datetimeOf`, `addDuration`, `durationBetween`, `durationDays`, `durationHours`, `durationMinutes`, `durationSeconds`
- **Feature flags:** `flag`
- **RBAC:** `hasPermission`, `roleAllows`

See [Expressions](/language/expressions) for usage and examples.

## Commands and execution order

```manifest
entity Account {
  property required id: string
  property balance: decimal(12, 2) = 0

  command transfer(amount: decimal(12, 2), optional memo: string = "") {
    retry { maxAttempts: 3 backoff: fixed delay: 1000 }
    rateLimit { maxRequests: 10 windowMs: 60000 scope: user }
    guard self.balance >= amount
    constraint amountPositive: amount > 0 "Amount must be positive"
    mutate balance = self.balance - amount
    emit transferred
  }
}

event transferred: "account.transfer.completed" { accountId: string }
```

At runtime, a command executes in a **fixed order** that cannot be reconfigured:

Per `docs/spec/semantics.md` § Commands:
1. Build evaluation context (parameter defaults / required-parameter fail-closed)
2. Rate-limit (when declared)
3. Policies
4. **Command-level constraints**
5. Guards
6. Actions
7. Emits
8. Return

Guards run **after** command constraints, not before.

<Steps>
  <Step title="Evaluation context">Parameter defaults apply; missing required parameters fail closed.</Step>
  <Step title="Rate-limit">When declared on the command.</Step>
  <Step title="Policies">Authorization rules are evaluated.</Step>
  <Step title="Constraints">Command-level constraints run with severity (`ok`/`warn`/`block`).</Step>
  <Step title="Guards">
    Guards run in declaration order; execution halts on the first falsey guard.
  </Step>
  <Step title="Actions">Mutations and other effects apply.</Step>
  <Step title="Emits">Declared events publish.</Step>
  <Step title="Return">The result is produced.</Step>
</Steps>

This ordering is part of the language contract. Diagnostics explain failures (which guard, which expression, resolved values) but never change the order or auto-repair a failing program.

## Other declarations at a glance

```manifest fragment
enum Priority { low, normal = "Normal", urgent(2) } // optional label and ordinal

value Money { // value objects hold properties only
  property amount: decimal(12, 2)
  property currency: string
}

policy adminOnly execute: user.role == "admin" // standalone policy

store Order in supabase { table: "orders" } // persistence binding

event orderPlaced: "shop.order.placed" { orderId: string }

on orderPlaced run Inventory.reserve resolve payload._subject.id // reaction

role manager extends staff { allow approve deny delete } // role hierarchy

saga Checkout { // workflow with compensation
  step charge { command: Payment.charge compensate: Payment.refund }
  step ship { command: Fulfillment.ship compensate: Fulfillment.cancel }
  on_failure: "compensate"
  emit checkoutFailed
}

webhook stripe "/hooks/stripe" run Payment.record // inbound webhook
  method: "POST"
  idempotencyHeader: "Stripe-Idempotency-Key"

schedule nightlyClose cron "0 0 * * *" run Ledger.close // scheduled command
```

## Ordering rules

- **`use` first.** All `use` declarations must precede every other top-level declaration. A `use` that appears later is an error.
- **`use` paths** must be relative (`./` or `../`) and end in `.manifest`.
- **One `tenant`** declaration per program.
- **Command execution order** is fixed (policies → **command constraints** → guards → actions → emits → return, after evaluation-context / rate-limit gates); guards halt on the first falsey result.
- **No required ordering** among an entity's sub-blocks, or among top-level declarations other than `use`. IR preserves encounter order.

## What is not allowed

The parser rejects these outright — they are common mistakes, not features:

- **No entity generics.** `entity Paginated<T>` is invalid; type parameters exist only inside property types like `array<T>`.
- **No `import` statements.** Cross-file composition is `use "./file.manifest"` only. There is no `import { X } from "…"`.
- **No `policy` clause inside a command body.** Declare authorization at the entity level (`policy … execute: …`) or as a top-level `policy`.
- **Reserved words as identifiers.** A keyword used as a declaration name produces _"Reserved word 'X' cannot be used as an identifier."_
- **`value` blocks** may contain properties only — no commands, computed, or constraints.
- **Events inside entities** are dropped with a warning.
- **`unmask when` without `masked`** is a compile error.
- **`async`** must be followed by `command`.
- Structural mistakes — missing colons, unclosed braces, a constraint block without an `expression`, a composite `references [...]` without an explicit `fields [...]`, or a saga/approval missing its required command/stage — all surface as parse errors via `Expected X, got Y`.

## Best practices

The language is designed around a small number of [house-style](https://github.com/Angriff36/Manifest) principles. Following them keeps programs deterministic and reviewable.

- **Determinism over convenience.** Identical IR plus identical runtime context must produce identical results. Avoid leaning on ambient state; pass variability through `context.`.
- **Explicitness over inference.** Guards and policies should reference the spec-guaranteed bindings (`self.`, `user.`, `context.`) rather than implicit values. Prefer `self.` to `this.`.
- **Let constraints carry intent.** Give every constraint a message and an appropriate severity (`block` to stop, `warn` to flag, `ok` to annotate). Use `code` and `messageTemplate` when downstream tools consume the failure.
- **Keep authorization at the entity boundary.** Name your policies (`policy canApprove execute: …`) so denials are legible, and use `default policy` for the baseline.
- **Name events in past tense** on a stable channel string (`"billing.invoice.published"`); reactions and webhooks bind to those names.
- **Model money and quantities with `decimal`/`money`**, never `number`, so scale is explicit and exact.
- **Split large programs with `module` and `use`.** Group related entities, keep files focused, and let `use` wire shared value objects and enums.
- **Treat the IR as the contract.** Don't hand-edit generated projections to "fix" behavior — change the `.manifest` source and regenerate. Generated code must not diverge from IR semantics.

## A complete example

```manifest
use "./shared/money.manifest"

module billing {
  enum InvoiceStatus { draft, sent = "Sent", paid(2), cancelled(3) }

  entity Invoice {
    property required id: string
    property status: InvoiceStatus = draft
    property total: money(12, 2) = 0
    property private notes: string = ""

    computed isOverdue: boolean = status == sent and now() > dueDate
    property dueDate: datetime

    hasMany lines: InvoiceLine

    constraint positiveTotal: total >= 0 "Total cannot be negative"

    policy canSend execute: user.role == "billing" or user.role == "admin"
    default policy openAccess all: true

    command send() {
      guard self.total > 0
      guard self.status == draft
      mutate status = sent
      emit invoiceSent { invoiceId: self.id }
    }

    versionProperty version: number
    transition status from "draft" to ["sent", "cancelled"]
    timestamps
  }

  event invoiceSent: "billing.invoice.sent" { invoiceId: string }

  on invoiceSent run Notifier.email resolve self.invoiceId
}

store Invoice in postgres { table: "invoices" }
```

Compile it with `manifest compile`, inspect the IR, and project it to any target — see [Projections](/integration/projections) and the [CLI reference](/cli/commands).


---

## Compile a multi-file project with merging

URL: https://manifest-b1e8623f.mintlify.app/language/modules

---
title: 'Modules: multi-file projects'
description: 'Organize Manifest programs across multiple files using use declarations, with cycle detection, topological sort, and IR merging.'
sidebarTitle: 'Modules'
---

Manifest supports multi-file projects through `use` declarations. The module resolver discovers all referenced files, detects cycles, sorts them topologically, and merges the compiled IR into a single deterministic output. This lets you split large programs into focused files without sacrificing whole-program validation.

## Use declarations

The `use` keyword includes another `.manifest` file. Use declarations must appear at the top of the file, before any entity, event, or store declarations:

```manifest
// app.manifest
use "./shared/types.manifest"
use "./domains/order.manifest"
use "./domains/product.manifest"
```

The resolver walks `use` declarations recursively to discover the full file graph. A `use` path must be relative and end in `.manifest` — absolute paths and extensions other than `.manifest` produce a parse error.

`use` is the only cross-file mechanism. There is no `import` keyword — every declaration in a `use`d file is brought into the merged program, namespaced by its source file path (see [Namespace isolation](#namespace-isolation)).

## Compilation pipeline

<Steps>
  <Step title="Resolution">
    Walk all `use` declarations starting from the entry file. Build a complete dependency graph
    using BFS discovery.
  </Step>
  <Step title="Cycle detection">
    Detect circular dependencies (A uses B uses A) using DFS with grey/black coloring. Circular
    dependencies produce a compile error diagnostic listing the cycle path.
  </Step>
  <Step title="Topological sort">
    Kahn's algorithm determines the compilation order — dependencies compile before dependents. Ties
    are broken alphabetically for deterministic output.
  </Step>
  <Step title="Per-file compilation">
    Each file is parsed and compiled to IR independently, producing one IR per file.
  </Step>
  <Step title="Cross-file validation">
    The merged IR is validated for cross-file reference integrity: entity types referenced in
    properties must exist, enum members used as defaults must be declared, and event names
    referenced in `emit` must be defined somewhere in the project.
  </Step>
  <Step title="IR merging">
    All per-file IRs are merged into a single output with sorted arrays for determinism. A `sources`
    array in the IR provenance tracks which files contributed to the merged output.
  </Step>
</Steps>

## CLI flags

Use the `--merge` and `--entry` flags to compile multi-file projects:

```bash
# Compile a multi-file project with merging
manifest compile --merge --entry src/app.manifest

# Compile with explicit output path
manifest compile --merge --entry src/app.manifest --output ir/

# Let the compiler auto-detect entry files (files not referenced by any other)
manifest compile --merge src/
```

| Flag             | Description                                                                                        |
| ---------------- | -------------------------------------------------------------------------------------------------- |
| `--merge`        | Enable multi-file compilation and merge all discovered files into a single IR output               |
| `--entry <path>` | Specify the entry point for module resolution. When omitted, the compiler auto-detects root files. |

## Cross-file naming

Multi-file merge does **not** path-prefix entity/enum/event names. Duplicate top-level names across files are a **compile error** (`Duplicate entity` / enum — see `src/manifest/multi-compiler.test.ts`). `module` blocks are a logical grouping only and do not change runtime identity (`docs/spec/semantics.md` § Modules). Keep declaration names unique across the merged project.

## Cross-file validation

After merging, the compiler validates cross-file references and produces error diagnostics with file paths and line numbers:

- Entity types referenced in properties must exist in the merged IR
- Enum members used as default values must be declared in a `use`d file
- Event names referenced in `emit` actions must be declared somewhere in the project
- Store declarations must reference entities that exist in the merged IR

Invalid cross-file references produce compile error diagnostics rather than runtime failures.

## ResolverHost abstraction

The module resolver uses a `ResolverHost` abstraction for file system access. In production, this reads from disk. In tests, you can provide an in-memory host to test resolution without touching the file system:

```typescript
import { ModuleResolver } from '@angriff36/manifest/module-resolver';

const resolver = new ModuleResolver({
  readFile(path) {
    // Return file contents from an in-memory map
    return mockFiles.get(path);
  },
});
```

This makes the resolver testable in isolation and allows the multi-compiler to work in environments without direct file system access.

## Complete example

A multi-file project with shared types, domain entities, and an entry point:

```manifest
// shared/types.manifest
enum Status { draft, submitted, approved, rejected }
enum Priority { low, medium, high }
```

```manifest
// domains/order.manifest
use "../shared/types.manifest"

entity Order {
  property required id: string
  property status: Status = draft
  property priority: Priority = medium
  property amount: number = 0

  command submit() {
    guard self.status == draft
    mutate status = submitted
    emit OrderSubmitted
  }
}

event OrderSubmitted: "order.submitted" {
  orderId: string
}

store Order in memory
```

```manifest
// app.manifest
use "./domains/order.manifest"

// Additional top-level declarations can go here
```

Compile the project:

```bash
manifest compile --merge --entry src/app.manifest --output project.ir.json
```

The resulting `project.ir.json` contains all entities, events, enums, and stores from every discovered file, merged into a single deterministic IR with provenance tracking.

See [Entities](/language/entities) for entity declarations, [Events](/language/events) for event schemas, and [CLI overview](/cli/overview) for the full set of compile options.


---

## reactions

URL: https://manifest-b1e8623f.mintlify.app/language/reactions

---
title: 'Reactions: declarative event-driven command dispatch'
description: 'Automatically dispatch commands when events are emitted using on Event run Command syntax, with resolve expressions, parameter mappings, and cascade protection.'
sidebarTitle: 'Reactions'
---

Reactions connect events to commands declaratively. When an event is emitted, the runtime evaluates matching reactions in declaration order and dispatches the target command with resolved parameters. This creates reactive workflows without manual event wiring.

## Declaring a reaction

Use the `on` keyword followed by an event name, `run`, and a target command reference:

```manifest
event OrderSubmitted: "order.submitted" {
  orderId: string
  amount: number
}

entity Order {
  property required id: string

  command submit(orderId: string, amount: number) {
    emit OrderSubmitted { orderId: orderId, amount: amount }
  }
}

entity OrderProcessor {
  property required id: string
  property status: string = "idle"

  command processOrder(orderId: string, amount: number) {
    guard amount > 0
    mutate status = "processing"
  }
}

on OrderSubmitted run OrderProcessor.processOrder
  resolve "processor-1"
  params {
    orderId: payload.orderId
    amount: payload.amount
  }
```

The `resolve` expression evaluates to the ID of the target entity instance. The `params` block maps values from the event payload into the command's parameters. `resolve` is always required on a single-target reaction; `params` is optional and may be omitted when the target command takes no arguments.

## Resolve expressions

The `resolve` clause provides the ID of the entity instance that receives the dispatched command. It evaluates to a single value — the instance's identifier:

```manifest fragment
on TaskAssigned run Notification.sendAssigned
  resolve payload.assigneeId
  params {
    taskId: payload.taskId
  }
```

The runtime always dispatches `runCommand` with the resolved `instanceId` (`docs/spec/semantics.md` § Reactions). There is **no** silent skip when the instance is missing — the target command still runs; `self`/`this` are unbound when lookup fails, so mutates that need an instance do not persist. Seed target instances (or use a `create` target command) before relying on a reaction.

Use a literal string when the target instance is fixed at design time (a singleton handler, a system processor, etc.):

```manifest fragment
on OrderSubmitted run OrderProcessor.processOrder
  resolve "processor-1"
  params {
    orderId: payload.orderId
    amount: payload.amount
  }
```

Use `payload._subject.id` to target the same entity instance that emitted the event, or `payload.<field>` when the target ID is carried explicitly in the event payload.

## Parameter mappings

The `params` block maps event payload fields to command parameters. Each entry is an expression evaluated in the event's enriched context:

```manifest fragment
params {
  orderId: payload.orderId // Direct field reference
  total: payload.amount * 1.1 // Expression with arithmetic
  currency: "USD" // Literal value
}
```

Inside a `params` block, `payload.*` references the event payload fields. The expression language supports the full set of operators: arithmetic, comparison, logical, conditional, and function calls.

## Follow-on emit payloads

A reaction only sees the fields the triggering event actually carries. By default an `emit` carries the command's input plus the last action result — so a follow-on reaction reading `payload.<field>` for anything else resolves `undefined`. To pass real values down a reaction chain, declare them explicitly with an `emit Event { field: expr }` block. The fields are computed **at emit time**, in the emitting command's context:

```manifest
entity ScheduleShift {
  property required id: string
  property scheduleId: string = ""

  command assign(scheduleId: string) {
    mutate scheduleId = scheduleId
    emit ScheduleShiftCreated { scheduleId: self.scheduleId }
  }
}

event ScheduleShiftCreated: "scheduleShift.created" {}

// The follow-on reaction now reads a real value, not undefined
on ScheduleShiftCreated run Schedule.syncShiftCount
  resolve self.scheduleId
```

Each payload field is `name: <expr>`, where the expression is evaluated in the emitting command's context (`self.*`, command inputs). The declared fields are merged into the emitted event's `payload`, where a follow-on reaction's `resolve`, `params`, `fanOut where`, and `count` predicates can read them as `self.<field>` / `payload.<field>`. This closes the gap consumers otherwise papered over with hand-written after-emit middleware.

## Fan-out reactions (1:N cascades)

A single-target reaction dispatches on ONE resolved instance. For a **1:N cascade** — run a command on _every_ related row, e.g. "when a gig is cancelled, unassign every staff member assigned to it" — use the `fanOut` form. It replaces `resolve` with a collection match:

```manifest
entity Gig {
  property required id: string
  property status: string = "active"
  hasMany staff: GigStaff

  command cancel() {
    mutate status = "cancelled"
    emit GigCancelled
  }
}

entity GigStaff {
  property required id: string
  property gigId: string = ""
  property status: string = "assigned"
  belongsTo gig: Gig

  command unassign(reason: string) {
    mutate status = "unassigned"
  }
}

store Gig in memory
store GigStaff in memory

event GigCancelled: "gig.cancelled" {}

on GigCancelled fanOut GigStaff where gigId = self.id
  run unassign
  params {
    reason: "gig cancelled"
  }
```

This dispatches `GigStaff.unassign` on **every** `GigStaff` row where `row.gigId == self.id` (the source instance's id, read from the event payload). Each match runs the target command through the normal pipeline — its own guards, policies, actions, and emits fire — exactly like a hand-written after-emit handler, but declared.

- `where <field> = <sourceExpr>` selects the collection; `<field>` is a property on the matched entity (an indexed or foreign-key field is preferred — the Convex projection reads it via `withIndex`, falling back to a filter otherwise).
- `<sourceExpr>` is evaluated against the event payload (`self.id` / `payload.*`), so it sees the source entity's id and any declared `emit Event { field }` values.
- `params` maps the same way as a single-target reaction and is shared across all matches. Inside fan-out, `self` refers to the matched row from the source entity; `target` refers to the target entity instance.
- The reaction depth limit and `correlationId`/`causationId` propagation apply per dispatched command.

Fan-out is the declarative replacement for the common "query children by FK, loop, dispatch" middleware pattern: cancel every line item, create a shipment per line, release every reservation, deactivate every child account, close every tip pool in a period, etc.

## Foreach-create (cross-entity fan-out)

Same-entity `fanOut … run unassign` updates each matched row in place. **Foreach-create** is the cross-entity form: match one collection, then `run Target.create` (or another target-entity command) **without** binding the matched row as `instanceId` / `docId`. Each match allocates a new target row; use matched-row fields in `params` to seed it (`self` is the fan-out source row).

Shipped in Manifest **3.6.36+** (conformance fixture `112-fanout-foreach-create`).

```manifest fragment
on OrderPlaced fanOut LineItem where orderId = payload.orderId
  run Shipment.create
  params {
    lineItemId: self.id
    quantity: self.quantity
  }
```

This is how you declare “for each matching source row, create a child” — not a separate `foreach` keyword. The keywords are still `fanOut` + `run Target.create`.

## Natural-key upsert (`match … else create`)

**Shipped in Manifest 3.6.38+** — natural-key lookups for single-target and fan-out reactions.

Instead of resolving by `instanceId`, match the target entity by one or more field values (natural key). If a match is found, run the command on that instance; if zero rows match and `else create` is set, run the command without `instanceId` (allocate a new row / createVia path).

### Single-target: lookup or allocate

```manifest fragment
event ShoppingCartSubmitted: "cart.submitted" {
  cartId: string
  userEmail: string
}

on ShoppingCartSubmitted run Order.create
  match orderId = payload.cartId
  else create
  params {
    orderId: payload.cartId
    userEmail: payload.userEmail
  }
```

- If an `Order` with `orderId == cartId` exists → run `Order.create` on that row (update path, passing `instanceId`).
- If no match → run `Order.create` without `instanceId` (allocate / create a new row).
- If multiple matches → pick the lexicographically smallest `id` (deterministic tiebreak).
- Soft-deleted rows (`deletedAt != null`) are excluded from the match.

### Fan-out: per-row lookup or allocate

Fan-out can also use `match … else create`. When a source row matches, the runtime looks up the target entity by the match predicates:

```manifest fragment
event OrderPlaced: "order.placed" {
  orderId: string
  items: array
}

on OrderPlaced fanOut LineItem where orderId = payload.orderId
  match sku = self.sku
  else create
  run InventoryAdjustment.allocate
  params {
    sku: self.sku
    orderId: payload.orderId
  }
```

For each `LineItem` where `orderId == payload.orderId`:
- If an `InventoryAdjustment` with `sku == self.sku` exists → run `allocate` on that row.
- If no match → run `allocate` without `instanceId` (allocate new row).

### Syntax

```
on <Event> run <Entity>.<command>
  match <field> = <expression>, ...
  [else create]
  params { ... }
```

- `match` is mutually exclusive with `resolve`. Use `match` when you have natural-key predicates; use `resolve` when you have a direct `instanceId`.
- Predicates are **pure equality**, ANDed together. All must hold for a row to match.
- `else create` is optional. Without it, zero matches = no-op. With it, zero matches = run command without `instanceId`.
- For fan-out, `match` applies to `targetEntity`, not the fan-out source collection. Source rows are selected by the `fanOut … where …` clause; `match` then looks up each selected row's target.

## Aggregate count expressions

A reaction often needs to **recompute a stored count on a parent** after a child event — "how many children reference this parent?" — the pattern behind hand-written "count children where the foreign key equals the parent, then patch the parent" after-emit middleware. The `count(...)` aggregate makes that declarative. It is an expression, so it can appear anywhere a reaction param value does:

```manifest
entity Schedule {
  property required id: string
  property shiftCount: number = 0

  command syncShiftCount(shiftCount: number) {
    mutate shiftCount = shiftCount
  }
}

entity ScheduleShift {
  property required id: string
  property scheduleId: string = ""
  belongsTo schedule: Schedule

  command assign(scheduleId: string) {
    mutate scheduleId = scheduleId
    emit ScheduleShiftCreated { scheduleId: self.scheduleId }
  }
}

event ScheduleShiftCreated: "scheduleShift.created" {}

on ScheduleShiftCreated run Schedule.syncShiftCount
  resolve self.scheduleId
  params {
    shiftCount: count(ScheduleShift where scheduleId == self.scheduleId)
  }
```

When a `ScheduleShift` is assigned, the reaction resolves its parent `Schedule` (`self.scheduleId` is the event payload value), counts `ScheduleShift` rows whose `scheduleId` equals that value, and stores the result via `syncShiftCount`.

### Syntax

```
count(<Entity> where <field> == <value>, <field2> == <value2>, ...)
```

- `<Entity>` is the counted collection. Predicate values (`<value>`) are expressions evaluated in the reaction's context — the event payload, just like other params (`self.*` / `payload.*`).
- Predicates are **pure equality**, ANDed together. A second predicate adds a filter — e.g. counting only in-progress tasks:

```manifest fragment
params {
  currentTaskCount: count(PrepTask where stationId == self.stationId, status == "in_progress")
}
```

- `count` is a **contextual** operator, not a reserved word: it is recognized only in the `count(<Entity> where ...)` shape, so a property can still be named `count`.
- At least one predicate (the foreign-key match) is required. There is **no** group-by, join, multi-hop traversal, or arbitrary SQL — `count` only. Scope is deliberately narrow.

### Projection notes

The runtime evaluates `count` by scanning the entity's collection and counting rows matching every predicate (deterministic — count is order-independent). The Convex projection reads via the counted entity's `by_<field>` index on the foreign-key predicate when one exists (the `belongsTo`/`ref` FK or an `indexed` field), applying any remaining predicates — plus the entity's tenant and soft-delete filters — as post-read `.filter`s, then `.length`. With no indexed predicate it falls back to a table scan and emits an `info` diagnostic.

### What this does NOT replace

`count(...)` covers the **per-parent** recompute (one parent's count, updated by the child event that points at it). It does not cover:

- **Tenant-wide reconcile** (recompute _every_ parent on each event) or **dual-target updates** (a reassignment that moves a child from parent A to parent B needs both updated). These are multi-step / conditional fan-out — model them as sagas, actions, or keep the hand middleware. They are intentional scope boundaries, not gaps in `count`.

## Reaction scopes

Reactions can be declared at three levels:

| Scope   | Location                      | Behavior                              |
| ------- | ----------------------------- | ------------------------------------- |
| Program | Top-level in `.manifest` file | Global — fires for any matching event |
| Module  | Inside a `module` block       | Scoped to the module                  |
| Entity  | Inside an `entity` block (`on Event run …` / `fanOut`) | Collected into IR with an `entity` field |

Entity-scoped reactions must use the `on Event run …` / `fanOut` form. Bare `behavior` / `on Event { … }` blocks inside an entity are a hard compile error (conformance fixture 110; `docs/spec/semantics.md` § Entities).

```manifest
event OrderSubmitted: "order.submitted" {
  orderId: string
}

event OrderFailed: "order.failed" {}
event OrderReprocessed: "order.reprocessed" {}

entity OrderProcessor {
  property required id: string
  property status: string = "idle"

  command processOrder(orderId: string) {
    mutate status = "processing"
  }
}

entity Order {
  property required id: string
  property status: string = "pending"

  command submit(orderId: string) {
    mutate status = "submitted"
    emit OrderSubmitted { orderId: orderId }
  }

  command fail() {
    mutate status = "failed"
    emit OrderFailed
  }

  command reprocess() {
    mutate status = "pending"
    emit OrderReprocessed
  }

  // Entity-level reaction -- fires within the entity's context
  on OrderFailed run Order.reprocess
    resolve payload._subject.id
}

// Program-level reaction -- fires globally
on OrderSubmitted run OrderProcessor.processOrder
  resolve "processor-1"
  params {
    orderId: payload.orderId
  }
```

Reactions at all three scopes are collected into the IR's `reactions` array. Module-scoped reactions carry a `module` field; entity-scoped reactions carry an `entity` field. The runtime evaluates all matching reactions regardless of scope.

## Cascading reactions and depth limit

When a reaction's target command emits its own events, those events can trigger further reactions. This creates cascading reaction chains:

```
OrderSubmitted
  -> OrderProcessor.processOrder
    -> OrderProcessing
      -> Warehouse.prepareShipment
```

The runtime enforces a maximum reaction depth (`MAX_REACTION_DEPTH = 10`). If a chain exceeds this limit, the runtime throws a `ManifestReactionDepthError` and stops processing further reactions. This prevents infinite loops from circular reaction chains.

<Warning>
  Design your reaction chains to terminate naturally. If event A triggers command B which emits
  event C which triggers command D which emits event A, the runtime will halt after 10 iterations
  with a `ManifestReactionDepthError`. This is a safety net, not a design pattern — circular
  reaction chains indicate a modeling problem.
</Warning>

## correlationId and causationId propagation

Reactions propagate workflow metadata through the chain:

- **`correlationId`** stays the same across the entire workflow. It is set on the initial event and carried through every subsequent reaction.
-

`causationId` on each reaction-triggered command is the triggering event's **name** (`emitted.name`), not an emit/instance ID (`docs/spec/semantics.md` § Reactions).

This enables cross-reaction tracing for debugging distributed workflows. See [Events](/language/events) for how `correlationId` and `causationId` are attached to emitted events.

## Deterministic ordering

Reactions execute in declaration order. If multiple reactions match the same event, they are dispatched in the order they appear in the source file. This guarantees deterministic behavior: given identical IR, identical runtime context, and identical input, the reaction chain always produces the same result.

The runtime evaluates matching reactions synchronously within the same command execution turn — reactions do not queue asynchronously. This means the full reaction chain completes before the original `CommandResult` is returned to the caller.

## Complete example

```manifest
event OrderSubmitted: "order.submitted" {
  orderId: string
  amount: number
  customerId: string
}

event InvoiceCreated: "invoice.created" {
  invoiceId: string
  orderId: string
  total: number
}

event ShipmentPrepared: "shipment.prepared" {}

entity Order {
  property required id: string
  property status: string = "pending"

  command submit(orderId: string, amount: number, customerId: string) {
    mutate status = "submitted"
    emit OrderSubmitted { orderId: orderId, amount: amount, customerId: customerId }
  }
}

entity Invoice {
  property required id: string
  property orderId: string = ""
  property total: number = 0
  property status: string = "draft"
  belongsTo order: Order

  command createFromOrder(orderId: string, total: number) {
    guard total > 0
    mutate orderId = orderId
    mutate total = total
    mutate status = "issued"
    emit InvoiceCreated
  }
}

entity Shipping {
  property required id: string
  property orderId: string = ""
  property status: string = "pending"
  belongsTo order: Order

  command prepareShipment(orderId: string) {
    mutate orderId = orderId
    mutate status = "preparing"
    emit ShipmentPrepared
  }
}

// Reaction chain: OrderSubmitted -> create invoice
on OrderSubmitted run Invoice.createFromOrder
  resolve "invoice-auto"
  params {
    orderId: payload.orderId
    total: payload.amount
  }

// Reaction chain: InvoiceCreated -> prepare shipment
on InvoiceCreated run Shipping.prepareShipment
  resolve "shipping-auto"
  params {
    orderId: payload.orderId
  }
```

`runCommand(commandName, input, options?)` — not
entity-first. `amount` is command input; `entityName` / `instanceId` go in options.
Do **not** wrap fenced code in `` — Mintlify MDX treats the fence as broken and
parses `{…}` as JSX (`Could not parse expression with acorn`), which blocks deploy.

```typescript
import { RuntimeEngine } from '@angriff36/manifest';
import { ir } from './compiled.ir.json';

const runtime = new RuntimeEngine(ir, {
  user: { id: 'system', role: 'worker' },
});

// Seed instances
runtime.createInstance('Invoice', { id: 'invoice-auto' });
runtime.createInstance('Shipping', { id: 'shipping-auto' });

// Emitting OrderSubmitted triggers the reaction chain
const result = await runtime.runCommand(
  'submit',
  { orderId: 'order-001', amount: 250, customerId: 'customer-123' },
  { entityName: 'Order', instanceId: 'order-001' },
);

// The reaction chain fires automatically:
// OrderSubmitted -> Invoice.createFromOrder -> InvoiceCreated -> Shipping.prepareShipment
console.log(result.emittedEvents.length); // Events from all reactions
```

See [Events](/language/events) for declaring event schemas and [Commands](/language/commands) for the command execution pipeline. For async reactions that defer execution to a background queue, see [Async commands](/language/async-commands).


---

## roles

URL: https://manifest-b1e8623f.mintlify.app/language/roles

---
title: 'Roles: hierarchy and permission inheritance'
description: 'Define role hierarchies with extends, allow/deny permissions, and transitive inheritance. Deny rules are absolute and always win over allow.'
sidebarTitle: 'Roles'
---

Roles define named permission sets with single-parent inheritance. A role that extends a parent inherits all its permissions transitively. Use roles with the [policy](/language/guards-policies) system to control command authorization based on the user's assigned role.

## Declaring a role

Use the `role` keyword at the program or module level with a block body of `allow` and `deny` declarations:

```manifest
role User {
  allow read
}

role Manager extends User {
  allow write
  allow execute
}

role Admin extends Manager {
  allow delete
  allow override
  deny impersonate
}
```

Each `allow` or `deny` line names an action (or optionally a target) that the role permits or forbids. The `extends` keyword establishes a single-parent inheritance chain.

## Permission inheritance

Roles inherit permissions transitively through the `extends` chain. The compiler walks the chain from root to leaf, unioning all `allow` permissions along the way:

| Role                      | Inherited from | Effective permissions                  |
| ------------------------- | -------------- | -------------------------------------- |
| `User`                    | (none)         | read                                   |
| `Manager` (extends User)  | User           | read, write, execute                   |
| `Admin` (extends Manager) | Manager, User  | read, write, execute, delete, override |

`resolveRoleGraph()` precomputes an `effectivePermissions: IRRolePermission[]` (allow entries remaining after transitive deny). Runtime checks scan that array (no inheritance walk at execution time).

### Compile-time validation

The compiler validates the role graph and produces error diagnostics for:

- **Duplicate role names**: Each role name must be unique within the program or module.
- **Unknown parent**: A role that extends a name not declared as a role produces a compile error.
- **Circular inheritance**: If the `extends` chain forms a cycle (A extends B extends A), the compiler detects it via DFS coloring and produces an error diagnostic listing the cycle path.

## Deny-is-absolute semantics

If any role in the inheritance chain denies an action, the denial takes precedence over all `allow` rules regardless of where they appear:

```manifest
role User {
  allow read
}

role Support extends User {
  allow write
  allow execute
  deny delete // Support CANNOT delete, even if a parent allows it
}
```

Deny rules are applied after the full inheritance union is computed. This means:

1. Walk the inheritance chain and union all `allow` permissions.
2. Walk the inheritance chain and collect all `deny` permissions.
3. Filter allows against denies → `effectivePermissions: IRRolePermission[]`.

A `deny` at any level is absolute. There is no mechanism to override a deny with a subsequent allow. This is by design — it prevents privilege escalation through role chain manipulation.

<Warning>
  Deny is not a suggestion; it is a hard rule. If `User` denies `impersonate`, then `Admin extends
  User` cannot re-allow `impersonate`. The deny from `User` will always win. Structure your role
  hierarchy so that denials only appear at the appropriate level.
</Warning>

## Unknown roles default to deny

If a user's role does not match any declared role, the runtime returns `false` for all permission checks. There are no permissive defaults — an unrecognized role has no permissions. This is consistent with Manifest's default-deny posture. See [Guards and policies](/language/guards-policies) for the broader authorization model.

<Warning>
  Role matching is **exact and case-sensitive**. `'Admin'` and `'admin'` are treated as different
  roles. A caller passing the wrong casing silently gets `false` with no error — the runtime treats
  it as an unknown role, not a near-miss. Ensure that the role strings your identity system produces
  exactly match the names declared in the `.manifest` file.
</Warning>

## Using roles in policies

Reference roles in policy expressions via the `user.role` binding and the built-in permission functions. Authorization is declared at the entity level with named `policy <name> execute:` rules (or a `default policy`); command bodies do not accept a `policy` clause:

```manifest
entity Document {
  default policy RequireAuth execute: user.id != null "Authentication required"

  policy CanApprove execute: roleAllows(user.role, "approve") or user.role == "admin"
  policy CanDelete execute: hasPermission("delete")

  command approve() {
    mutate status = "approved"
    emit DocumentApproved
  }

  command removeDocument() {
    mutate status = "deleted"
    emit DocumentDeleted
  }
}
```

## Runtime builtins

Two built-in functions are registered on the runtime engine when roles are present in the IR:

Per `docs/spec/builtins.md` and conformance fixture `71-role-hierarchy.manifest`:

| Function | Signature | Returns |
| --- | --- | --- |
| `hasPermission` | `hasPermission(action, target?)` | `true` if `context.user.role`'s effective permissions grant `action` (optional `target`) |
| `roleAllows` | `roleAllows(roleName, action, target?)` | same check for an explicit role name |

`effectivePermissions` is an `IRRolePermission[]` (not a `Record<string, boolean>`). Lookups scan that array. Passing a user object as the first argument of `hasPermission` always returns `false` (action must be a string).

### `hasPermission(action, target?)`

Uses the bound runtime `user.role`. Returns `false` if there is no role string or the role is undeclared.

```manifest
guard hasPermission("delete")
guard hasPermission("execute", "CreateReport")
```

### `roleAllows(roleName, action, target?)`

Checks whether a named role allows a specific action, without requiring a user object.

```manifest
policy CanExecute execute: roleAllows(user.role, "execute") or user.role == "admin"
```

## Context-sensitive `role` keyword

The `role` keyword is context-sensitive, not a globally reserved keyword. It is emitted as an identifier rather than a reserved token, so existing programs that use `role` as a property name continue to parse correctly:

```manifest
entity UserProfile {
  property role: string = "user" // property named "role"

  command setRole(newRole: string) {
    mutate role = newRole // mutate "role" property
  }
}
```

The parser only treats `role` as a declaration keyword at the top level of a program or module (when followed by an identifier and a block). Inside entity blocks, `role` is an ordinary identifier.

<Note>
  The `role` keyword is only treated as a role declaration at the top level of a program or module.
  Inside entity blocks, `role` is an ordinary identifier. This prevents the keyword from breaking
  existing code that uses `role` as a property name.
</Note>

## IR representation

Roles are stored in the IR as an optional `roles` array on the root `IR` object (and on `IRModule` for module-scoped roles). Each `IRRole` contains:

IR shape (`src/manifest/ir.ts`):

```typescript
interface IRRolePermission {
  action: string;
  target?: string;
}

interface IRRole {
  name: string;
  module?: string;
  parent?: string; // from `extends` in source
  allow: IRRolePermission[];
  deny: IRRolePermission[];
  effectivePermissions: IRRolePermission[]; // post inheritance + deny
}
```

The runtime reads `effectivePermissions` directly and never re-computes it.

## Complete example

```manifest
role User {
  allow read
}

role Manager extends User {
  allow write
  allow execute
}

role Admin extends Manager {
  allow delete
  allow override
  deny impersonate
}

entity Document {
  property required id: string
  property title: string = ""
  property status: string = "draft"

  default policy RequireAuth execute: user.id != null "Authentication required"

  policy CanRead execute: hasPermission("read")
  policy CanWrite execute: hasPermission("write")
  policy CanApprove execute: roleAllows(user.role, "execute")
  policy CanDelete execute: hasPermission("delete")

  command view() {
    emit DocumentViewed
  }

  command edit(newTitle: string) {
    mutate title = newTitle
    emit DocumentEdited
  }

  command approve() {
    guard self.status == "draft"
    mutate status = "approved"
    emit DocumentApproved
  }

  command removeDocument() {
    mutate status = "deleted"
    emit DocumentDeleted
  }
}
```

`RuntimeEngine.runCommand(commandName, input, options?)` —
not entity-first. Pass `entityName` / `instanceId` in `options`.

```typescript
import { RuntimeEngine } from '@angriff36/manifest';
import { ir } from './compiled.ir.json';

// Admin user
const adminRuntime = new RuntimeEngine(ir, {
  user: { id: 'admin-1', role: 'Admin' },
});

const result = await adminRuntime.runCommand('removeDocument', {}, {
  entityName: 'Document',
  instanceId: 'doc-42',
});
console.log(result.success); // true

// Manager user
const mgrRuntime = new RuntimeEngine(ir, {
  user: { id: 'mgr-1', role: 'Manager' },
});

const mgrResult = await mgrRuntime.runCommand('removeDocument', {}, {
  entityName: 'Document',
  instanceId: 'doc-42',
});
console.log(mgrResult.success); // false (Manager has no "delete" permission — removeDocument requires it)

// Unknown role
const unknownRuntime = new RuntimeEngine(ir, {
  user: { id: 'guest-1', role: 'Guest' },
});

const unknownResult = await unknownRuntime.runCommand('view', {}, {
  entityName: 'Document',
  instanceId: 'doc-42',
});
console.log(unknownResult.success); // false (unknown role defaults to deny)
```

See [Guards and policies](/language/guards-policies) for the full policy system, [Commands](/language/commands) for the execution pipeline, and [Approvals](/language/approvals) for multi-stage approval workflows that integrate with role-based policies.


---

## manifest.config.yaml

URL: https://manifest-b1e8623f.mintlify.app/language/stores

---
title: 'Stores: persistence targets for entities'
description: "Declare where each entity's instances are persisted using the store keyword. Choose from memory, postgres, or supabase, or plug in a custom adapter."
sidebarTitle: 'Stores'
---

A store declaration tells the runtime where to persist instances of a given entity. Every entity that participates in runtime command execution needs a corresponding store. The store target determines which adapter the runtime uses to read and write instances.

## Syntax

```manifest
store EntityName in <target>
```

`EntityName` must match an entity declared elsewhere in the same `.manifest` file (or module). The `<target>` is one of the built-in keywords or a custom identifier resolved at runtime configuration time.

## Built-in targets

### `memory`

Stores instances in a plain JavaScript `Map` keyed by instance ID. Data does not survive process restarts. This is the only target that is required to be supported by all conforming runtimes.

```manifest
entity Product {
  property required name: string
  property price: number = 0
}

store Product in memory
```

Use `memory` for:

- Local development and rapid iteration
- Unit and conformance tests
- Demos and prototyping

### `durable`

The **backend-neutral semantic target**. Declares that the entity is persisted in _some_ durable store; the specific technology (Prisma, raw SQL, supabase-js, a custom adapter, …) is chosen by the consumer at runtime via a `storeProvider`, and at compile time via projection config.

```manifest
store Order in durable
```

Use `durable` when you want the entity to participate in storage projections without coupling the `.manifest` source to a specific backend. The [Prisma projection](/integration/prisma) treats `durable`, `postgres`, and `supabase` identically — all three are schema-emission targets. The runtime engine rejects `durable` at command-execution time unless a custom `storeProvider` is bound, with a clear "no store adapter supplied" diagnostic.

<Note>
  `durable` is the recommended target for new code. The legacy `postgres` and `supabase` targets
  continue to work; nothing was removed. Storage projections emit for all three.
</Note>

### `postgres`

Delegates to a `PostgresStore` adapter. The adapter handles SQL reads and writes against a Postgres-compatible database. You must configure the adapter when constructing the `RuntimeEngine`.

```manifest
store Order in postgres
```

<Note>
  `postgres` is a target identifier, not a built-in adapter included in the core runtime. You must
  supply a Postgres adapter via `RuntimeEngine` options. See [Custom
  Stores](/adapters/custom-stores) for the adapter contract.
</Note>

### `supabase`

Delegates to a `SupabaseStore` adapter. Similar to `postgres`, but typed for the Supabase client. Requires adapter configuration at runtime.

```manifest
store User in supabase
```

<Note>
  `supabase` is also an adapter target, not a built-in. Provide a Supabase-backed store via
  `RuntimeEngine` options.
</Note>

## Multiple entities, multiple stores

Each entity gets its own `store` declaration. Entities in the same program can use different targets:

```manifest
entity Author {
  property required name: string
  hasMany books: Book
}

entity Book {
  property required title: string
  belongsTo author: Author
}

store Author in memory
store Book in memory
```

```manifest
entity User {
  property required email: string
  property required role: string = "member"
}

entity AuditLog {
  property required action: string
  property required actorId: string
  property createdAt: number = 0
}

store User in postgres
store AuditLog in postgres
```

## Using a custom store adapter

When you declare `store EntityName in postgres` (or any non-`memory` target), the `RuntimeEngine` must receive a `storeProvider` function that returns the appropriate adapter for that entity name:

```typescript
import { RuntimeEngine } from '@angriff36/manifest';

class PostgresOrderStore implements Store<Order> {
  async getAll() {
    return await db.order.findMany();
  }

  async getById(id: string) {
    return (await db.order.findUnique({ where: { id } })) ?? undefined;
  }

  async create(data: Partial<Order>) {
    return await db.order.create({ data });
  }

  async update(id: string, data: Partial<Order>) {
    return await db.order.update({ where: { id }, data });
  }

  async delete(id: string) {
    await db.order.delete({ where: { id } });
    return true;
  }

  async clear() {
    await db.order.deleteMany({});
  }
}

const runtime = new RuntimeEngine(
  ir,
  { user: { id: 'user-1', role: 'admin' } },
  {
    storeProvider: (entityName) => {
      if (entityName === 'Order') return new PostgresOrderStore();
      return undefined;
    },
  },
);
```

When `storeProvider` returns `undefined`, the runtime uses a built-in store only
for a declared `memory` or browser `localStorage` target. Durable targets never
fall back to memory: `postgres`, `supabase`, `durable`, and custom targets fail
and identify the unbound entity.

## Complete generated-companion PostgreSQL setup

A database URL is necessary, but it is not the entire storage configuration.
Bind every durable entity to a store and table:

```env
DATABASE_URL=postgresql://user:password@localhost:5432/acme
```

```ts
// manifest.config.ts
import { defineConfig } from '@angriff36/manifest/config';
import { PostgresStore } from '@angriff36/manifest/stores';

const connectionString = process.env.DATABASE_URL;
if (!connectionString) throw new Error('DATABASE_URL is required');

export default defineConfig({
  stores: {
    Customer: { implementation: new PostgresStore({ connectionString, tableName: 'customers' }) },
    Invoice: { implementation: new PostgresStore({ connectionString, tableName: 'invoices' }) },
  },
});
```

```yaml
# manifest.config.yaml
projections:
  nextjs:
    output: generated
    options:
      # Relative to the emitted lib/manifest-runtime.ts companion.
      runtimeConfigImport: '../manifest.config'
```

The generated factory imports the TypeScript config, builds the
`storeProvider`, and passes it to `RuntimeEngine`. Without
`runtimeConfigImport`, a generated factory containing durable stores throws a
clear error. Memory and browser localStorage targets need no extra binding.

<Tip>
  See [Custom Stores](/adapters/custom-stores) for the full `Store` interface contract and a guide
  to implementing production-ready adapters.
</Tip>

## Store and `localStorage`

The `localStorage` target is available for browser environments. It is auto-instantiated by the runtime — no `storeProvider` is required. Instances are persisted in the browser's `localStorage` under a key derived from the entity name.

```manifest
store Preferences in localStorage
```

## What the runtime does when a store is missing

If an entity has no store declaration, the runtime uses memory. This is distinct
from declaring a durable target without binding it: an explicitly durable store
fails rather than silently changing persistence.

<Warning>
  Entities without an explicit store declaration have no persistence guarantee. In production,
  always declare an explicit store target to make your persistence intent clear and to enable
  adapter injection.
</Warning>


---

## tenancy

URL: https://manifest-b1e8623f.mintlify.app/language/tenancy

---
title: 'Tenant isolation: scoping reads and writes to a tenant'
sidebarTitle: 'Tenant isolation'
description: 'Scope entity reads and writes to a tenant value drawn from the runtime context with a single tenant declaration that fails closed when no tenant resolves.'
---

A top-level `tenant` declaration scopes entity reads and writes to a tenant value extracted from the runtime context. Tenant-scoped programs fail closed: a command invoked without a resolvable tenant value is rejected with `MISSING_TENANT_CONTEXT`.

## Syntax

The declaration is a single top-level construct — at most one per program. The parser rejects more than one tenant declaration:

```manifest
tenant tenantId : string from context.tenantId

entity Invoice {
  property required id: string
  property required amount: number
  property required description: string

  command createInvoice(amount: number, description: string) {
    mutate amount = amount
    mutate description = description
  }

  store in memory
}
```

The syntax is `tenant <property> : <type> from <context_path>`.

## Behavior

The compiler emits an `IRTenant` record with `property`, `type`, and `contextPath` fields onto `IR.tenant`. The field is only present when a `tenant` declaration exists, so programs without tenancy compile identically to before. The runtime engine reads `IR.tenant` and applies tenant scoping:

- **`resolveTenantValue()`** walks the configured `contextPath` (for example `context.tenantId`) against the active runtime context and returns the tenant value, or `undefined` when the IR has no tenant declaration or the context lacks the value.
- The **tenant gate** in `runCommand()` activates when **either** the `requireTenantContext` runtime option is set **or** the IR declares a `tenant`. When active and no tenant value resolves — any falsy value (`undefined`, empty string, `null`) counts as missing — the command fails with `MISSING_TENANT_CONTEXT: tenant-scoped command invoked without context.tenantId`, classified as outcome `missing_tenant_context`.
- On create, the resolved tenant value is auto-written into the entity's tenant property.
- **`getAllInstances()`** filters results to instances whose tenant property equals the active tenant value.
- **`getInstance()`** returns `undefined` when an instance's tenant property does not match the active tenant value, preventing cross-tenant reads.

The Prisma projection adds a tenant discriminator column and an index per model, and emits PostgreSQL row-level-security policy statements as comments for consumers to apply manually.

## Reference

- Source keyword: `tenant`; syntax `tenant <property> : <type> from <context_path>`.
- IR field: `IR.tenant?: IRTenant` with `{ property, type, contextPath }`.
- Runtime option: `requireTenantContext?: boolean` — independent of the IR-level declaration; can enforce tenant context without a `tenant` block.
- Failure diagnostic: `MISSING_TENANT_CONTEXT`, classified as outcome `missing_tenant_context`.

<Warning>
  Tenant filtering is enforced by the reference runtime engine's read paths (`getAllInstances`,
  `getInstance`); it is **not** enforced by the database unless the emitted RLS policies are
  applied. The Prisma RLS statements ship as comments, not executed migrations.
</Warning>

<Note>
  The runtime gate is fail-closed by design — a missing or empty tenant value is treated as an error
  rather than a permissive default. The `requireTenantContext` option and the IR `tenant`
  declaration are orthogonal; either activates the gate.
</Note>


---

## timestamps

URL: https://manifest-b1e8623f.mintlify.app/language/timestamps

---
title: 'Automatic timestamp fields'
sidebarTitle: 'Timestamps'
description: 'The timestamps modifier auto-injects read-only createdAt and updatedAt fields and populates them from the deterministic runtime clock on create and update.'
---

The `timestamps` entity modifier auto-injects read-only `createdAt` and `updatedAt` properties and populates them at runtime, removing the boilerplate of declaring and maintaining audit timestamps by hand.

Matches `docs/spec/semantics.md` / IR `timestamps` flag / runtime `getNow()` create+update stamping / Prisma `@default(now())` + `@updatedAt`. Package pin `@angriff36/manifest` **3.6.41**.

## Syntax

The modifier is a bare keyword placed inside an entity body. No property declarations for `createdAt` or `updatedAt` are needed:

```manifest
entity Article {
  property required id: string
  property required title: string
  property body: string = ""
  timestamps
}

store Article in memory
```

## Behavior

When an entity declares `timestamps`, the IR compiler sets `IREntity.timestamps: true` and injects two properties — `createdAt` and `updatedAt`, both of type `datetime` with the `readonly` modifier — unless the entity already declares a property of that name. The injection is idempotent and skips names that are already present.

The runtime engine acts on the flag:

- On instance **creation**, when `timestamps` is set, both `createdAt` and `updatedAt` are assigned the value of `getNow()`.
- On instance **update**, when `timestamps` is set, `updatedAt` is reassigned to `getNow()` while `createdAt` is left untouched.

`getNow()` honors the deterministic clock supplied through the `now` runtime option, so tests can pin timestamps to a fixed value. The Prisma projection translates the flag into native column attributes: `@default(now())` on `createdAt` and `@updatedAt` on `updatedAt`.

## Manual `now()` / `today()` defaults

The `timestamps` modifier is the zero-boilerplate path for the `createdAt`/`updatedAt` pair. For a single timestamp field outside that pair, declare it explicitly with a `now()` (or `today()`) default:

```manifest
property createdAt: datetime = now()
```

This call-expression default lowers to the `IRProperty.autoNow` flag (recognized: `now()`/`today()`, called with no arguments). The runtime stamps the field with the current time on create when the caller does not supply a value, and the Prisma projection emits `@default(now())`.

<Note>
  Before this lowering existed, a `= now()` default compiled to **no** default and was null-filled
  at runtime, breaking non-null store columns (the `createdAt must not be null` class of bug). The
  compiler also folds negative numeric literal defaults like `= -1` into a real static default, and
  emits a **warning** for an unsupported call-expression default such as `= uuid()` (which would
  otherwise be silently dropped). See [Types](/language/types) for the full `autoNow` and
  diagnostics behavior.
</Note>

## Reference

- Source keyword: `timestamps`.
- IR field: `IREntity.timestamps?: boolean`.
- Injected properties: `createdAt`, `updatedAt`, type `datetime`, modifier `readonly`.
- Runtime population: `getNow()` on create (both fields) and update (`updatedAt` only).
- Prisma attributes: `@default(now())`, `@updatedAt`.

<Note>
  The injected fields carry the `readonly` modifier, so they cannot be mutated through commands. If
  a program manually declares `createdAt` or `updatedAt`, the auto-injection defers entirely to the
  manual declaration and does not add or override anything for that name. Population is driven by
  the runtime engine's create/update paths — stores or code paths that write instances without going
  through `createInstance`/`updateInstance` will not receive automatic timestamps.
</Note>


---

## types

URL: https://manifest-b1e8623f.mintlify.app/language/types

---
title: 'Types: enums, decimal/money, value objects, datetime, arrays'
sidebarTitle: 'Types'
description: "Reference for Manifest's richer property types — enums, decimal and money, value objects, datetime with UTC built-ins, and arrays — including what each one enforces at runtime."
---

`timestamp` **is** a valid property type — it is a normative alias for `datetime` (`docs/spec/semantics.md:281,286-289`). Runtimes MUST treat `timestamp` identically to `datetime`. Common scalars include `string`, `number`, `boolean`, plus date/time primitives `datetime` / `timestamp` / `date` / `time` / `duration`, `decimal` / `money`, collections, enums, and value-object names. Type names are open strings, so an undeclared custom name can still compile as a type reference. This page documents what each richer type actually provides — and what it does not.

## Enums

A first-class `enum` declaration defines a closed, named set of values that properties can reference. Each member may carry an optional display label and an optional numeric ordinal, giving entities a typed vocabulary for status fields, priorities, and other fixed value sets.

Enums are top-level declarations:

```manifest
enum Status {
  draft
  published = "Published"
  archived(2)
}

entity Article {
  property required title: string
  property status: Status = draft
  property priority: Priority
}

enum Priority {
  low = "Low Priority"
  medium = "Medium Priority"
  high = "High Priority"
}

store Article in memory
```

A member written as `name` is a plain value; `name = "Label"` attaches a display label; `name(ordinal)` attaches a numeric ordinal. The three forms can be mixed freely within one enum. An enum may be referenced as a property type before or after its own declaration in the file, and a property default such as `= draft` refers to a member by name.

The IR carries the full enum definition — a `name`, optional `module`, and a `values` array where each value records its `name` and, only when present, a `label` and an `ordinal`. Absent metadata is omitted rather than defaulted, so the IR stays minimal. This representation is the input downstream projections use to emit database enum columns and TypeScript union types.

<Warning>
  The IR carries only `name`, `label`, and `ordinal` per member. There is **no** enum-specific
  reference checker in the compiler and **no** transition-constraint machinery on enum members —
  both are aspirational, not present in the source. Because type names are not a closed set,
  referencing an undeclared enum name as a property type is not rejected at compile time by the enum
  machinery itself.
</Warning>

## Decimal and money

The `decimal` and `money` types represent high-precision numbers with optional precision and scale parameters, intended for monetary amounts and other values where binary floating point is unacceptable.

Both types accept an optional `(precision, scale)` parameter list and can otherwise be used like any scalar:

```manifest
entity Invoice {
  property required description: string
  property amount: decimal(10, 2) = 0
  property tax: money(12, 4) = 0
  property total: decimal = 0
  property optionalFee: money?
}

store Invoice in memory
```

`decimal(10, 2)` declares 10 total digits with 2 fractional digits. `money(12, 4)` is the same shape with higher precision. Both types are valid without parameters (`decimal`, `total`), in which case no precision or scale is recorded. The `?` suffix makes the property nullable (`money?`).

The parser special-cases a `(` immediately after the type name only when the name is `decimal` or `money` — any other type name followed by `(` is not treated as parameterized. When present, the precision and scale are stored as a `params` object on the emitted `IRType`; unparameterized types produce no `params`. This `params` object is carried on the `IRType` but is not currently consumed by any shipped projection — the Prisma and Drizzle projections read precision and scale from a separate `options.precision` configuration object, not from `IRType.params`. A downstream consumer could read `params` to emit a Postgres `NUMERIC` column or a `Decimal.js` field, but no built-in projection does so today.

<Warning>
  `decimal` and `money` do **not** introduce runtime arithmetic or validation of their own. The
  runtime engine treats their values as ordinary numbers — there is no decimal arithmetic library
  wired into the evaluator, and precision/scale are metadata carried for projection use, **not
  enforced at runtime or at compile time**. Any high-precision guarantee depends on a downstream
  projection rather than the reference runtime.
</Warning>

## Value objects

A `value` declaration defines a reusable composite type that embeds inline in entity properties rather than living in its own table. Value objects group related fields — money, addresses — into a single named shape that several entities can share.

Value objects are top-level declarations containing only property declarations:

```manifest
value Money {
  property amount: decimal
  property currency: string
}

value Address {
  property street: string
  property city: string
  property country: string
}

entity Product {
  property required id: string
  property name: string
  property price: Money
  property billingAddress: Address
}

entity Order {
  property required id: string
  property total: Money
  property shippingAddress: Address
}

store Product in memory
store Order in memory
```

An entity references a value object by using its name as a property type (`price: Money`). The same value object can be embedded in multiple entities.

The `value` token is context-sensitive — it is emitted as an identifier rather than a reserved keyword, so `property value: number` and `mutate value = 1` continue to parse without reserved-word errors. Every compiled program carries a top-level `values` array (empty when none are declared), and a property whose type name matches a declared value object is identified by checking that name against `ir.values`. The code generator emits a TypeScript interface for each value object, and the Prisma projection emits a matching property as a `Json` (JSONB) column rather than a foreign-key relationship or a separate table.

<Note>
  Value objects are described as "immutable by design," but that is a design statement, not a
  runtime-enforced property: the reference runtime does not freeze or reject mutation of embedded
  value data, and there is no immutability check in the compiler or engine. Value object bodies are
  restricted to properties — relationships, commands, and other members are rejected by the parser.
</Note>

## Datetime (and date / time / duration)

Four date/time primitive type names have fixed runtime representations and write-time validation (`docs/spec/semantics.md` § Date/Time Types):

| Type | Representation |
| --- | --- |
| `datetime` | finite number, epoch ms UTC |
| `duration` | finite number, milliseconds (may be negative) |
| `date` | string `"YYYY-MM-DD"` (valid calendar date) |
| `time` | string `"HH:MM:SS"` (`00:00:00`–`23:59:59`) |

Malformed values produce blocking outcomes `E_TYPE_DATETIME` / `E_TYPE_DURATION` / `E_TYPE_DATE` / `E_TYPE_TIME`. `timestamp` is a normative alias for `datetime` (same representation and validation; prefer `datetime` in new docs). There is no `interval` type name. The most common way `datetime` properties appear is via the `timestamps` modifier, which injects `createdAt` and `updatedAt`:

```manifest
entity Article {
  property required title: string
  timestamps
}
```

Because type names are open strings, a property may also be declared `property scheduledFor: datetime` and will compile, but no date-specific parsing or validation is attached to such a declaration.

### `now()` / `today()` defaults (autoNow)

A `datetime` property can default to the current time with a `now()` (or `today()`) call default:

```manifest
entity Article {
  property required id: string
  property createdAt: datetime = now()
}
```

A call-expression default cannot be stored as a static IR value, so the compiler lowers a recognized `now()`/`today()` default (called with no arguments) to the `IRProperty.autoNow` flag. The runtime stamps the field on create when the caller does not supply a value, and the Prisma projection emits `@default(now())`. Negative numeric literal defaults such as `= -1` are folded to a real static default instead of being dropped.

<Warning>
  Before `autoNow` existed, a `= now()` default compiled to **no** default and the field was
  null-filled at runtime, breaking non-null store columns. An **unsupported** call-expression
  default — e.g. `= uuid()` — would still be silently dropped, so the compiler now emits a
  **warning** for it (listing the supported call defaults `now()`, `today()`). Separately, a
  `create` command that leaves a non-null, default-less property unset is **warned** about when the
  runtime would null-fill that field — the types that fill with `null` rather than a zero value:
  `datetime`, `date`, `time`, `enum`, and custom/value-object types. Zero-filling types (`string`,
  `number`, `boolean`, `list`/`array`, `map`) are not flagged, nor are runtime/store-managed fields
  (`id`, composite keys, relationship FKs, the tenant property, version fields, auto timestamps).
  Both are warnings (not hard errors, since the runtime merges caller input on create) and surface
  in `manifest compile` and the LSP.
</Warning>

Date components and conversions are available through expression built-ins. Component extractors operate on a numeric millisecond timestamp:

```manifest
entity DateUtils {
  property required id: string
  property baseTs: number = 0

  computed extractedYear: number = year(self.baseTs)
  computed extractedMonth: number = month(self.baseTs)
  computed extractedDay: number = day(self.baseTs)
  computed extractedHours: number = hours(self.baseTs)
  computed extractedMinutes: number = minutes(self.baseTs)
  computed extractedSeconds: number = seconds(self.baseTs)
}
```

The date built-ins all operate on a number interpreted as milliseconds since the epoch and return a UTC component:

- `year(ts)` returns `getUTCFullYear()`.
- `month(ts)` returns `getUTCMonth() + 1` (so January is 1, not 0).
- `day(ts)` returns `getUTCDate()`.
- `hours(ts)`, `minutes(ts)`, `seconds(ts)` return the corresponding UTC components.

Each returns the input unchanged when it is not a number. UTC methods are used deliberately so results are timezone-independent and deterministic.

In addition to the six component extractors, `docs/spec/builtins.md` ships conversion and duration helpers: `dateOf`, `timeOf`, `datetimeOf`, `addDuration`, `durationBetween`, `durationDays`, `durationHours`, `durationMinutes`, `durationSeconds`. There is still no `interval` type and no general calendar arithmetic library beyond these builtins.

## Arrays

Array properties hold multiple scalar values in a single field, distinct from relationships (which model collections of entities). They are declared with either postfix `[]` sugar or explicit `array<T>` generic syntax.

```manifest
entity TaggedDocument {
  property required id: string
  property tags: string[] = []
  property scores: array<number> = []

  constraint noEmptyTags: self.tags.length > 0
  constraint hasTags: self.tags.contains("published")
}

store TaggedDocument in memory
```

`string[]` and `array<string>` are equivalent. An empty array literal `[]` is a valid default. Constraints can reach into array values with member access such as `self.tags.length` and method-style calls like `self.tags.contains("published")`.

Both forms normalize to an `array` type carrying a `generic` element type in the AST and IR. Array element types may themselves carry parameters — because the inner type goes through the same type parser, the generic element preserves details like decimal precision where applicable. The normalized `array` IR type is the structural input projections consume to emit PostgreSQL array/JSONB columns or Zod array schemas.

<Note>
  The `.length` and `.contains(...)` helpers are expression-level operations rather than dedicated
  array constraint primitives. The two constraints above are independently evaluated rather than
  short-circuited, so both can register failures. Array properties are scalar-valued collections and
  are deliberately separate from `hasMany` relationships, which model entity collections.
</Note>

## Maps / Records

The `map<V>` type declares a key-value dictionary property where all values are of type `V`. Keys are always strings. The form `map<string, V>` is accepted as sugar for `map<V>` (same IR). Other key types are rejected.

```manifest
entity Product {
  property required id: string
  property name: string
  property metadata: map<string> = {}
  property translations: map<string, string> = {}
  property featureFlags: map<boolean> = {}
}

store Product in memory
```

The type parameter is the value type (`map<V>` or `map<string, V>`). An empty map literal `{}` is a valid default. Value types can be any scalar type — `string`, `number`, `boolean`, or a custom type name.

Access map values in expressions using member access:

```manifest fragment
constraint hasEnglishName: self.translations["en"] != ""

computed flagEnabled: self.featureFlags["beta-feature"] == true
```

<Note>
  Map properties are persisted as JSON objects in projections (e.g., Prisma `Json` column, Drizzle
  `jsonb`). The runtime does not enforce key or value types beyond what JavaScript provides — type
  safety is a projection concern.
</Note>


---

## workflows

URL: https://manifest-b1e8623f.mintlify.app/language/workflows

---
title: 'Workflows: saga orchestration and scheduled commands'
description: 'Orchestrate multi-step distributed workflows with saga compensation, and trigger commands on time-based schedules using cron expressions or fixed intervals.'
sidebarTitle: 'Workflows'
---

Manifest provides two workflow primitives for coordinating execution across time and services: **saga orchestration** for distributed transactions with compensation, and **scheduled commands** for time-based triggers.

## Saga orchestration

A saga declares a sequence of steps that execute across entities or services. Each step names a `command` and an optional `compensate` command for rollback. If any step fails, the saga runs compensate for all previously completed steps in reverse order.

### Declaring a saga

Use the `saga` keyword at the program or module level. Each `step` is a block naming its `command` and optional `compensate` as `Entity.command` references. The `on_failure` clause takes a single identifier — `compensate` (the default) or `abort` — and `emit` declares the saga's lifecycle events:

```manifest
event SagaStarted: "saga.started" { sagaName: string }
event SagaCompleted: "saga.completed" { sagaName: string }
event SagaFailed: "saga.failed" { sagaName: string }
event SagaStepCompleted: "saga.step.completed" { sagaName: string }

saga ProcessOrder {
  step reserveInventory {
    command: Inventory.reserve
    compensate: Inventory.release
  }

  step chargePayment {
    command: Payment.charge
    compensate: Payment.refund
  }

  step shipOrder {
    command: Shipping.dispatch
  }

  on_failure: "compensate"

  emit SagaStarted
  emit SagaCompleted
  emit SagaFailed
  emit SagaStepCompleted
}
```

List the fixed lifecycle event names you want emitted (and declare matching `event`s). A single custom `emit OrderProcessingFailed` does **not** substitute for `SagaFailed` / `SagaCompleted`. Start with `runtime.runSaga('ProcessOrder', stepInputs)`.

### Saga lifecycle

<Steps>
  <Step title="Start">
     The saga begins when the host calls `runSaga(name, stepInputs)`. Steps then run
    in declaration order via `runCommand`.
  </Step>
  <Step title="Execute steps">
    Each step's command runs in sequence. If the command succeeds, the saga advances to the next
    step.
  </Step>
  <Step title="On failure — compensate">
    If any step's command fails, the saga enters compensation mode. It runs each completed step's
    compensate command in reverse order (last completed first).
  </Step>
  <Step title="Complete or failed">
    After all steps complete (or all compensations run), the saga emits a completion or failure
    event.
  </Step>
</Steps>

### Best-effort compensation

Compensation is best-effort. If a compensate command itself fails, the saga logs the failure and continues compensating remaining steps. This ensures partial rollback rather than aborting the compensation process entirely.

### Saga lifecycle events

Lifecycle event names are **fixed** (`SagaStarted`, `SagaStepCompleted`, `SagaCompleted`, `SagaFailed`) — not `{SagaName}…`. The runtime emits a lifecycle event **only if** that name appears in the saga's `emit` list **and** a matching `event` is declared in the IR (`emitSagaLifecycle`). There is **no** `SagaStepCompensated` lifecycle emission. Invoke with `runtime.runSaga(name, stepInputs)`.

```manifest
event SagaStarted: "saga.started" { sagaName: string }
event SagaCompleted: "saga.completed" { sagaName: string }
event SagaFailed: "saga.failed" { sagaName: string }
event SagaStepCompleted: "saga.step.completed" { sagaName: string }

entity Order {
  property required id: string
  command charge() {
    mutate charged = true
  }
  command refund() {
    mutate charged = false
  }
  store in memory
}

saga ProcessOrder {
  step chargePayment {
    command: Order.charge
    compensate: Order.refund
  }
  on_failure: "compensate"
  emit SagaStarted
  emit SagaCompleted
  emit SagaFailed
  emit SagaStepCompleted
}
```

<Note>
  Declared saga lifecycle events carry `correlationId` / `causationId` like other emissions when
  present on the emitted record.
</Note>

## Scheduled commands

Scheduled commands trigger entity commands on a time-based schedule using cron expressions, fixed intervals, or periodic timers.

### Declaring a scheduled command

The accepted duration units for `every`/`interval` are `ms`, `s`, `m`, `h`, `d`, `week`, `weeks` only. Long forms like `minutes`/`hours`/`seconds` are NOT accepted and will throw an error. **Important:** Entity-embedded schedules (inside `entity { … }` blocks) are silently dropped from the IR and do not execute. Only top-level single-line schedules are supported — see the corrected example below.

Use the `schedule` keyword at the top level of a `.manifest` file:

```manifest
entity Report {
  property required id: string
  property lastRunAt: number = 0
  property status: string = "idle"

  command generate() {
    mutate status = "running"
    emit ReportGenerationStarted
  }

  command checkHealth() {
    mutate lastRunAt = now()
  }

  command purgeExpired() {
    mutate status = "cleaned"
  }
}

// Top-level schedules (single-line syntax required)
schedule generateDaily cron "0 6 * * *" run Report.generate
schedule healthCheck interval "5m" run Report.checkHealth
schedule cleanup every 24 h run Report.purgeExpired
```

### Trigger types

| Type       | Syntax                             | Description                                                      |
| ---------- | --------------------------------- | ---------------------------------------------------------------- |
| `cron`     | `cron "0 6 * * *"`                | Standard 5-field cron expression (minute hour day month weekday) |
| `interval` | `interval <ms>` \| `interval "<dur>"` | Fixed interval in milliseconds or duration string (e.g., `"5m"`, `"1h"`) |
| `every`    | `every <n> <unit>`                | Human-readable period where `<unit>` is `s`/`m`/`h`/`d`/`week`/`weeks` |

### Schedule resolution

Schedules are resolved at compile time and emitted to the IR as `IRSchedule` objects. The `RuntimeEngine` class has no built-in timer, but the package ships an optional worker (`startScheduleWorker` from `@angriff36/manifest/schedule-worker`) that drives schedules on a tick loop and sweeps approval expiry; projection targets also consume the metadata:

- **Next.js projection**: Generates a cron route per schedule (`/api/cron/[name]/route.ts`) plus a `vercel.json` registering them, so the platform invokes them on schedule.
- **Express/Hono projections**: Emit route handlers intended for external scheduler invocation.
- **Infrastructure projections** (Terraform): Generate CloudWatch Event rules or similar scheduled resources.

### Inline parameters

Scheduled commands can pass static parameters to the target command:

```manifest
schedule warmCache cron "0 */6 * * *" run Cache.refresh(keys: "all")

entity Cache {
  command refresh(keys: string) {
    // ...
  }
}
```

<Warning>
  The `RuntimeEngine` class has no built-in timer. On Vercel, the generated cron routes and
  `vercel.json` fire schedules automatically; on other hosts, run `startScheduleWorker(runtime)`
  from `@angriff36/manifest/schedule-worker`, or invoke `runSchedule(name)` from your own scheduler.
  Cron triggers are matched in UTC to the minute.
</Warning>

## Combining sagas and schedules

Schedules invoke an entity **command** (`run <command>`). They do **not** call `runSaga` automatically. To start a saga from a schedule, the scheduled command (or a reaction it triggers) must call into host code that invokes `runtime.runSaga(...)`, or model the workflow as ordinary commands/reactions. Emitting an event alone does not start a saga.


---

## additional projections

URL: https://manifest-b1e8623f.mintlify.app/projections/additional-projections

---
title: 'Additional projections'
sidebarTitle: 'More projections'
description: 'Kysely, Terraform, materialized views, analytics, Elasticsearch, Storybook, and Python Client SDK projections for specialized infrastructure and tooling targets.'
---

Beyond the primary framework and ORM projections, Manifest ships several specialized projections for infrastructure, analytics, search, and developer tooling targets. Each follows the same `ProjectionTarget` interface and deterministic output contract.

Names `kysely`, `terraform`, `materialized-views`,
`analytics`, `elasticsearch`, `storybook`, and `pydantic` (client via `pydantic.client`) are
registered in `src/manifest/projections/builtins.ts`. Invoke with
`manifest generate -p <name>`. Package pin: see `package.json` SoT
( **3.6.41** in the 2026-07-22 checkout).

### Kysely

The registered surface name is `kysely.types` (not `kysely.schema`). The projection generates TypeScript type definitions for the [Kysely](https://kysely.dev) type-safe SQL query builder.

The projection is registered under the name `kysely` and emits one surface: `kysely.types` (content type `typescript`, default output `kysely.types.ts`).

**Key characteristics:**

- IR types map to Kysely/TypeScript types with `Generated<T>` for ID columns and columns with `defaultValue`, and `ColumnType<SelectType, InsertType, UpdateType>` for Date/datetime columns.
- `belongsTo`/`ref` relationships auto-generate FK columns (skipped if an FK property already exists). `hasMany`/`hasOne` are expressed through Kysely's relation API, not as columns.
- Bare `number` produces a `KYSELY_AMBIGUOUS_NUMBER` diagnostic and the column is skipped, requiring an explicit `int`/`bigint`/`float`/`decimal`/`money` type.
- Skips non-SQL store targets (`memory`, `localStorage`, `mongodb`, `dynamodb`, `eventSourced`).
- Supports `dialect` (`postgresql`, `mysql`, `sqlite`), `tableMappings`, `columnMappings` (applied to property and synthesized FK keys — 2026-07-22), `typeMappings`, and `emitFactory` options.

```ts
import { getProjection } from '@angriff36/manifest/projections';

const projection = getProjection('kysely');
const result = projection.generate(ir, {
  surface: 'kysely.types',
  options: { dialect: 'postgresql' },
});
```

### Terraform

The registered surface name is `terraform.hcl` (not `terraform.main`). The projection generates HCL resource definitions from Manifest IR. It supports three cloud providers: **AWS** (emits `aws_db_instance`, `aws_s3_bucket`, `aws_sns_topic`), **GCP** (emits `google_sql_database_instance`, `google_storage_bucket`, `google_pubsub_topic`), and **Supabase** (emits `supabase_project` with documented table schemas).

The projection is registered under the name `terraform` and emits one surface: `terraform.hcl` (content type `hcl`). It supports both single-file (`main.tf`) and multi-file (`database.tf`, `storage.tf`, `messaging.tf`) output modes.

**Key characteristics:**

- Walks `ir.stores` to find entities with persistent store targets (`postgres`, `supabase`) for database resources.
- Walks `ir.events` to emit pub/sub topic definitions.
- Accepts `storageBuckets` in options for S3/GCS bucket definitions.
- Skips non-persistent targets (`memory`, `localStorage`, `durable`) with diagnostic codes.
- Emits proper HCL with primary keys, unique constraints, `NOT NULL`, and encryption settings.

```ts
const projection = getProjection('terraform');
const result = projection.generate(ir, {
  surface: 'terraform.hcl',
  options: { provider: 'aws', outputMode: 'multi-file' },
});
```

### Materialized View SQL

The materialized views projection generates PostgreSQL `CREATE MATERIALIZED VIEW` DDL from Manifest IR entities. It supports three refresh strategies: **on-demand** (emits `REFRESH MATERIALIZED VIEW`), **scheduled** (emits `SELECT cron.schedule(...)` for pg_cron), and **trigger-based** (emits `CREATE TRIGGER ... AFTER UPDATE OF col ... EXECUTE FUNCTION`).

The projection is registered under the name `materialized-views` and emits one surface: `materialized-views.ddl` (content type `sql`).

**Key characteristics:**

- Includes an expression-to-SQL translator that converts `IRExpression` trees to PostgreSQL: literals, identifiers, binary operations (`===` maps to `=`), function calls (`SUM`, `COUNT`), and conditionals (`CASE WHEN`).
- Supports schema-qualified view names and `CREATE [UNIQUE] INDEX` statements (btree, hash, gin, gist).
- All relational concepts (view names, column aliases, refresh strategies, index definitions) enter via projection options, never into the IR.
- Deterministic output for a given IR + options pair.

```ts
const projection = getProjection('materialized-views');
const result = projection.generate(ir, {
  surface: 'materialized-views.ddl',
  options: {
    views: [
      {
        name: 'order_summary',
        source: 'Order',
        strategy: 'scheduled',
        schedule: { cron: '0 * * * *' },
        columns: [{ expression: 'SUM(total)', alias: 'revenue' }],
      },
    ],
  },
});
```

### Analytics Event Schema

The analytics projection generates typed event schemas from Manifest IR for Segment, Amplitude, Mixpanel, or Snowplow. It produces three surfaces:

- `analytics.tracking-plan` — A JSON tracking plan document following the Segment Tracking Plan spec format, with events derived from command emits and standalone event declarations.
- `analytics.events` — A TypeScript module with typed property interfaces per event, `AnalyticsEvents` constants, `AnalyticsEventMap` type mapping, and provider-specific `track()` functions.
- `analytics.handlers` — TypeScript command handler stubs that inject typed `track()` calls after command execution.

The projection is registered under the name `analytics` and supports the `provider` option (`'segment'` | `'amplitude'` | `'mixpanel'` | `'snowplow'`, default `'segment'`). Events are deduplicated by name across command emits and standalone declarations. All output is deterministic with sorted iteration.

```ts
const projection = getProjection('analytics');
const result = projection.generate(ir, {
  surface: 'analytics.tracking-plan',
  options: { provider: 'segment' },
});
```

### Elasticsearch / OpenSearch

The Elasticsearch projection generates index mappings, templates, ingest pipelines, an outbox-driven indexer worker, and a typed search client from Manifest IR entities stored in Elasticsearch. It emits five surfaces:

- `elasticsearch.mapping` — Per-entity ES index mappings with proper field types, settings, and analyzers.
- `elasticsearch.indexTemplate` — Composable index templates with index patterns and priority.
- `elasticsearch.ingestPipeline` — Ingest pipeline processor configurations.
- `elasticsearch.indexer` — TypeScript worker that consumes outbox entries and bulk-indexes entity state changes with retry logic and exponential backoff.
- `elasticsearch.client` — TypeScript search query builders with `multi_match` full-text search and typed document interfaces.

The projection is registered under the name `elasticsearch`. It maps Manifest types to ES field types (string to `keyword`/`text`, int to `integer`, float to `scaled_float`, etc.). Bare `number` produces an `ELASTICSEARCH_AMBIGUOUS_NUMBER` diagnostic. Computed properties are excluded from index mappings by default. Multi-tenant support automatically adds `tenantId` as a required `keyword` field when tenant context is configured.

```ts
const projection = getProjection('elasticsearch');
const mapping = projection.generate(ir, { surface: 'elasticsearch.mapping' });
const indexer = projection.generate(ir, { surface: 'elasticsearch.indexer' });
```

### Storybook

The Storybook projection generates Component Story Format (CSF3) stories from Manifest IR entities and commands. It emits three surfaces:

- `storybook.entity` — Default stories for each entity with all properties mapped to Storybook controls (text, number, boolean, date, select for enums). Computed properties are rendered as `control: false`. Private properties are excluded.
- `storybook.command` — Interaction stories with `GuardsPass` and `GuardFails` scenarios using play functions. Guard expressions are analyzed heuristically to produce meaningful pass/fail args.
- `storybook.all` — Combined entity and command stories in a single artifact.

The projection is registered under the name `storybook`. It supports `componentImportPattern` (with `{Entity}` and `{Command}` placeholders), `titlePrefix`, `includeGuardScenarios`, and `includeConstraintStories` options. Constraint violation stories are generated for entities with constraints.

```ts
const projection = getProjection('storybook');
const stories = projection.generate(ir, {
  surface: 'storybook.all',
  options: { titlePrefix: 'Manifest/' },
});
```

### Python Client SDK

The Python Client SDK is delivered through the `pydantic.client` surface of the [Pydantic projection](/projections/python-pydantic). It generates a `ManifestClient` class with `httpx.AsyncClient` integration, entity query methods (`list_<entity>s`, `get_<entity>`), typed command invocation methods, optional bearer token auth, and module-level convenience functions. The generated code targets Python 3.10+.

```ts
const projection = getProjection('pydantic');
const client = projection.generate(ir, {
  surface: 'pydantic.client',
  options: { clientBaseUrl: 'https://api.example.com', emitConvenienceFunctions: true },
});
```


---

## one surface

URL: https://manifest-b1e8623f.mintlify.app/projections/convex

---
title: 'Convex backend projection'
sidebarTitle: 'Convex'
description: 'Generate a complete Convex backend from Manifest IR — defineSchema/defineTable validators, reactive queries, governed mutations, crons, HTTP webhooks, and saga orchestrators, all standalone with no runtime dependency.'
---

The Convex projection emits a complete, self-contained Convex backend from compiled Manifest IR. It started as a schema-only projection (`convex.schema`, shipped in 2.9.0) and now spans eight surfaces covering schema, reads, governed writes, schedules, webhooks, sagas, computed properties, and React clients. Everything it emits is standalone Convex code with no Manifest runtime dependency, and typechecks against `convex@1.41`. Like the [Prisma](/integration/prisma) and [Drizzle](/projections/drizzle) projections, all Convex-specific interpretation (table names, validators, references, indexes) lives in the projection and its options — never in Manifest core grammar or IR.

## What it generates

The projection registers under the name `convex` and declares eight surfaces:

| Surface            | Path hint                                      | Emits                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| ------------------ | ---------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `convex.schema`    | `convex/schema.ts` (configurable via `output`) | `defineSchema` / `defineTable` with `convex/values` validators, enum unions, `v.id` references, and indexes. Optionally a system events table.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `convex.queries`   | `convex/queries.ts`                            | Reactive reads over `ctx.db`: `list<Entity>`, `get<Entity>` (by Convex `_id`), and `list<Entity>By<Field…>` over `.withIndex` for **every** schema index — indexed/tenant/reference fields _and_ composite `options.indexes` entries (`list<Entity>By<A>And<B>`). `list`/`get` are tenant-scoped and soft-delete-filtered by default (see [Read filtering](#read-filtering)). When the events table is emitted, adds `listRecentEvents` plus `listEventsByType` / `listEventsByEntity` / `listEventsByEntityId`.                                                                                                                                                                                                                                                                                                             |
| `convex.mutations` | `convex/mutations.ts`                          | One governed `mutation` per IR command. Runs policies → guards → constraints (runtime order, fail-closed), applies `mutate` actions, appends an event row, and fires matched reactions. Honors G7 `emit Event { field: expr }`: declared fields populate the event-row and reaction payloads from the post-action instance.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `convex.crons`     | `convex/crons.ts`                              | IR schedules → `cronJobs()` (`crons.cron` / `crons.interval`) referencing the command mutations.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `convex.http`      | `convex/http.ts`                               | IR webhooks → `httpRouter` / `httpAction` routes that read the request body, map `transform` params, and `ctx.runMutation` the command.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `convex.sagas`     | `convex/sagas.ts`                              | IR sagas → orchestrator `action`s that run each step via `ctx.runMutation`, tracking completed steps and compensating in reverse on failure (`onFailure: compensate`) or rethrowing (`abort`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `convex.computed`  | `convex/computed.ts`                           | Computed property definitions: `isFieldComputed()` helpers and `get<Field>()` selectors for each entity's computed properties. (Shipped 3.6.17+)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `convex.react`     | `convex/react.ts`                              | React hook wrappers: `useQuery<Entity>`, `useQuery<Entity>By<Field>`, `useMutation<Entity,Command>`. Emits only when `convex.react` is enabled via options. (Shipped 3.6.19+)                                                                                                                                                                                                                                                                                                                   |

Only persistent entities are projected. Entities marked `external`, entities with no `store` declaration, and entities whose store target is `memory` / `localStorage` are skipped; `durable` / `postgres` / `supabase` targets are emission targets. `computed` properties are never emitted as fields, and the IR `id` is dropped because Convex's built-in document `_id` is identity.

## Usage

```ts
import { compileToIR } from '@angriff36/manifest/ir-compiler';
import { getProjection } from '@angriff36/manifest/projections';

const { ir } = await compileToIR(source);

const projection = getProjection('convex');
const result = projection?.generate(ir, {
  surface: 'convex.schema',
  options: {
    referenceMode: 'convexId',
    tableMappings: { CateringEvent: 'events' },
  },
});

for (const artifact of result?.artifacts ?? []) {
  // write artifact.code at artifact.pathHint
}
```

`ConvexProjection` can also be imported directly from `@angriff36/manifest/projections/convex`. Through the CLI it is dispatched by registry name. The CLI accepts either the full surface id (`convex.schema`) or its short suffix (`schema`), and `--surface all` walks every surface in one run:

```bash
# one surface
manifest generate ir/app.ir.json --projection convex --surface convex.schema --output convex

# every surface (schema, queries, mutations, crons, http, sagas, computed, react)
manifest generate ir/app.ir.json --projection convex --surface all --output convex
```

Each artifact is written at its `pathHint` (e.g. `convex/schema.ts`, `convex/queries.ts`) relative to `--output`.

## Type mapping & behavior

The IR `type.name` string maps to a `convex/values` validator through `DEFAULT_TYPE_MAPPING` in `type-mapping.ts`:

| Manifest type                                | Convex validator | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| -------------------------------------------- | ---------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `string`, `text`, `uuid`                     | `v.string()`     |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `boolean`, `bool`                            | `v.boolean()`    |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `int`, `bigint`, `float`, `decimal`, `money` | `v.number()`     | Convex Float64. Matches Manifest runtime semantics, which treat every numeric type as an ordinary JS number — decimal/money precision and integer width are projection metadata, not runtime-enforced. Storing them as `v.int64()` (bigint) or `v.string()` (text) would diverge from the runtime and break generated guard/mutation arithmetic (mixed bigint/number throws; string operands concatenate or compare lexically). Opt into `v.int64()` / `v.string()` per property via `typeMappings` where lossless transport is genuinely required. |
| `date`, `datetime`, `time`, `duration`       | `v.number()`     | Epoch milliseconds, the Convex-idiomatic timestamp. Override to `v.string()` for ISO-8601.                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `json`                                       | `v.any()`        |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `bytes`                                      | `v.bytes()`      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |

As with Prisma and Drizzle, bare `number` is intentionally absent — it is ambiguous between integers, reals, and money. The projection emits a hard `CONVEX_AMBIGUOUS_NUMBER` diagnostic and skips the field; an unknown type with no override emits a hard `CONVEX_UNKNOWN_TYPE` diagnostic. There is no silent fallback.

Validators compose: enums become `v.union(v.literal("a"), v.literal("b"))`, `array<T>` becomes `v.array(<element>)`, nullable properties union with `v.null()`, and non-required properties wrap in `v.optional(...)` (enum + nullable flatten into a single union). For `belongsTo` / `ref` relationships in the default `convexId` mode, a property that backs the relationship is retyped from its declared scalar to `v.id("<targetTable>")`, preserving its own optional/nullable. Indexes are emitted for `indexed` properties (`by_<field>`), the tenant column, and every reference, plus any consumer-supplied composite/named indexes. Referential actions (`onDelete` / `onUpdate`) emit a deferred-info diagnostic — Convex has no schema-level cascade.

## Options

The options object is `ConvexProjectionOptions` in `convex/options.ts`, normalized through `normalizeOptions`. Per-entity and per-property options use the nested `Record<EntityName, Record<PropertyName, X>>` shape — there are no dotted-string `"Entity.property"` keys anywhere on this surface.

| Option                    | Type                                               | Default                                         | Purpose                                                                                                                                                                   |
| ------------------------- | -------------------------------------------------- | ----------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `output`                  | `string`                                           | `"convex/schema.ts"`                            | Path hint for the emitted schema artifact (flows to `ProjectionArtifact.pathHint`).                                                                                       |
| `tableMappings`           | `Record<EntityName, string>`                       | `{}`                                            | Per-entity table-name override; always wins over the naming convention.                                                                                                   |
| `typeMappings`            | `Record<EntityName, Record<PropertyName, string>>` | `{}`                                            | Per-property validator override; the value is the literal Convex validator expression, e.g. `"v.number()"` or `"v.int64()"`.                                              |
| `indexes`                 | `Record<EntityName, IndexEntry[]>`                 | `{}`                                            | Per-entity composite/named indexes. Each entry is a `string[]` (composite) or `{ fields, name? }`, becoming a `.index("name", [...])` call.                               |
| `references`              | `Record<EntityName, Record<string, string>>`       | `{}`                                            | Per-relationship foreign-key field-name override (default is the non-tenant `foreignKey.fields` column, or `${relationshipName}Id`).                                      |
| `referenceMode`           | `'convexId' \| 'stringId'`                         | `'convexId'`                                    | How `belongsTo` / `ref` references are represented. `'convexId'` → `v.id("<targetTable>")`; `'stringId'` → `v.string()` for app-level string ids (UUIDs).                 |
| `naming`                  | `NamingConventionInput`                            | `{ table: 'camelCase', pluralizeTables: true }` | Table-name casing/pluralization. The Convex-idiomatic default is applied when omitted; `tableMappings` is the escape hatch for irregular plurals.                         |
| `emitEventsTable`         | `boolean`                                          | `true`                                          | Emit a system events table (the reactive event log governed mutations append to).                                                                                         |
| `eventsTable`             | `string`                                           | `"manifestEvents"`                              | Name of the system events table. If the resolved name collides with an entity table it is deterministically suffixed (with a `CONVEX_EVENTS_TABLE_COLLISION` diagnostic). |
| `policyMode`              | `'enforce' \| 'skip'`                              | `'enforce'`                                     | Authorization-policy enforcement in generated mutations — see below.                                                                                                      |
| `includeTenantFilter`     | `boolean`                                          | `true`                                          | Scope `list<Entity>` / `get<Entity>` reads to the current tenant. Field-aware — see [Read filtering](#read-filtering).                                                    |
| `includeSoftDeleteFilter` | `boolean`                                          | `true`                                          | Exclude soft-deleted rows from generated reads. Field-aware.                                                                                                              |
| `tenantIdProperty`        | `string`                                           | IR `tenant.property`                            | Override the tenant property name used for read scoping.                                                                                                                  |
| `deletedAtProperty`       | `string`                                           | `"deletedAt"`                                   | Soft-delete property name; a read drops rows whose value is non-null.                                                                                                     |
| `authContextImport`       | `string`                                           | unset                                           | Module specifier of an author-owned auth-context module — see [Auth context seam](#auth-context-seam).                                                                    |

### Auth context seam (`authContextImport`)

By default (unset), generated functions read identity from an inline `(ctx as any).auth` bag — a shape the real Convex runtime **never populates** (`ctx.auth` only exposes `getUserIdentity()`), so role/tenant reads evaluate against `undefined` and fail closed until you wire auth by hand. Setting `authContextImport` (e.g. `"./lib/authContext"`) routes all identity reads through your module's exported `getAuthContext(ctx)` — which should derive role and tenant from the real authenticated identity (e.g. a Clerk/Auth0 JWT via `ctx.auth.getUserIdentity()`).

With the seam active, two write-side protections also turn on for tenant-scoped entities:

- **`create` mutations derive the tenant server-side.** The tenant column is filled from the auth context and removed from the mutation's client arguments (a create action targeting it is overridden, with a `CONVEX_TENANT_SERVER_DERIVED` diagnostic).
- **Instance mutations verify document tenancy.** A command against a document from another tenant throws the same "not found" error as a missing document (no cross-tenant existence oracle).

Contract: `getAuthContext(ctx)` returns (or resolves to) an object carrying at least `role` and the tenant property. Crons, sagas, and webhooks invoke the same governed mutations, so the module must also handle identity-less system calls (e.g. return a system context).

### Read filtering

Convex defaults `includeTenantFilter` and
`includeSoftDeleteFilter` to **`true`**. Next.js defaults both to **`false`** — they do **not**
mirror each other. Both Convex filters are _field-aware_: a clause is emitted only for entities
that actually declare the column, so entities that don't follow the tenancy / soft-delete
conventions get plain unfiltered reads.

<Note>
The tenant id for `list<Entity>` and `get<Entity>` is derived from the authenticated identity (via `getAuthContext(ctx)` when `authContextImport` is set, otherwise the legacy inline `ctx.auth.<tenantProp>` read), **never from a client argument** — so an un-scoped `list<Entity>()` cannot return rows across tenants (it fails closed: with no auth wired, it matches no tenant and returns nothing rather than everything). `get<Entity>` returns `null` on a tenant mismatch or a soft-deleted row. `list<Entity>By<Field…>` reads additionally drop soft-deleted rows and apply the auth-derived tenant filter unless the index already constrains the tenant column. Turn either filter off with `includeTenantFilter` / `includeSoftDeleteFilter`.
</Note>

### policyMode

`policyMode` (added in 2.10.x) controls whether generated mutations enforce authorization policies. Its accepted values are exactly `'enforce'` and `'skip'`, defaulting to `'enforce'`.

<Note>
  With `policyMode: 'skip'`, the role/policy (authorization) checks are omitted from generated
  mutations, while guards and constraints (state validation) are still enforced. When skipped, the
  `ROLE_PERMISSIONS` map and `checkRole` helper are not emitted, so there is no dead code. This is
  intended for dev/demo backends that have no auth context configured, so the generated files don't
  need hand-editing. Production builds should keep the default `'enforce'`.
</Note>

## Notes & limitations

<Note>
Mutation governance is rendered **fail-closed**: each policy, guard, and constraint is rendered from IR via a pure expression resolver, and any expression the resolver cannot fully map emits a hard `CONVEX_UNRESOLVED_{POLICY,GUARD,CONSTRAINT}` diagnostic together with a denying `throw` — never a silent pass. Roles become a `ROLE_PERMISSIONS` map plus a `checkRole()` helper (with an `all` wildcard). Reactions that create tenant-scoped targets automatically thread the source `tenantId` (2.10.3) unless an explicit reaction param sets it.
</Note>

<Note>
  The `convex.http` surface generates `httpRouter` / `httpAction` webhook routes that read the
  request body and invoke the command mutation, but does **not** verify `IRWebhook.signature` (HMAC
  sha256/sha512) — generated Convex `httpAction`s have no signature verification. It also does not
  implement `idempotencyHeader` deduplication: duplicate webhook deliveries re-run the command
  mutation. For verified, deduplicated webhook handling, use the reference webhook handler
  (`@angriff36/manifest/webhooks`) with a Next.js, Express, or Hono projection instead.
</Note>

### G7 emit payloads (`emit Event { field: expr }`)

A command may declare explicit event-payload fields (G7). The projection evaluates each declared field against the **post-action instance** and populates both the emitted event row's `payload` and the shared reaction `payload` with them — mirroring the reference runtime (`runtime-engine.ts` emit-time payload construction). This matters for reactions on **mutate** commands: the runtime's base event payload is `{ ...input, result }`, where `input` is only the command's parameters, so an entity-owned field the reaction needs (`payload.invoiceId`, `payload.amount`) or a computed field (`payload.total = self.qty * self.price`) is `undefined` unless declared. Declaring it via G7 makes the reaction resolve instead of silently no-op'ing — which is what lets a cross-entity cascade (e.g. "when a payment is processed, apply it to its invoice") be authored as a reaction rather than hand-written as after-emit middleware. Create commands evaluate against the built `doc`; non-create commands introduce a post-patch `{ ...doc, ...updates }` instance. It is strictly opt-in: commands with no `emitPayloads` generate byte-identical output. Unresolved field expressions emit a `CONVEX_UNRESOLVED_EMIT_PAYLOAD` warning and are omitted (non-governance, so no denying throw).

This projection's behavior is taken directly from `src/manifest/projections/convex/generator.ts`, `options.ts`, `type-mapping.ts`, `functions.ts`, and `orchestration.ts`. The schema surface shipped in 2.9.0; the five functions/orchestration surfaces, `listRecentEvents`, the create-mutation completeness fix, and `policyMode` landed across the 2.10.x releases. Schema↔query index parity (every schema index — single-field, reference FK in both reference modes, and composite — has a matching read), the events-table indexed reads, and default tenant + soft-delete read filtering landed in a later 2.10.x release.

## Rate limiting

Command- and policy-level `rateLimit` is implemented via a sliding-window bucket table. When any command or write/execute/delete policy declares `rateLimit`, the projection emits:

**Schema table** (`commandRateLimitBuckets` by default):

```ts
commandRateLimitBuckets: defineTable({
  scopeKey: v.string(), // "user:user-123", "tenant:org-abc", or "global"
  timestamps: v.array(v.number()), // Window timestamps
  windowStart: v.number(), // When the current window began
})
  .index("by_scopeKey", ["scopeKey"])
```

**Helper function** (`__consumeCommandRateLimit`):

- Checks existing bucket for the scope key
- Filters timestamps outside the rolling window
- Throws if `timestamps.length >= maxRequests + burstAllowance`
- Appends current timestamp and updates/inserts bucket
- Returns `retryAfterMs` on denial

The check runs **before policies/guards** inside each mutation (commands) or **before each policy expression** (policies), so idempotent replays that skip the runner do not consume additional tokens.

**Scope key prefixes**:
- Command rateLimit: `cmd:<entity>:<command>:<scope>`
- Policy rateLimit: `policy:<policyName>:<scope>`

User/tenant-scoped rate limits require `authContextImport` — the scope key derives from the authenticated identity (`getAuthContext(ctx)`). Without auth, these scopes fail closed.

<Note>
  Read/`all` policy `rateLimit` is **not supported** — Convex queries cannot mutate durable buckets. Declaring rateLimit on a read policy emits `CONVEX_UNSUPPORTED_READ_POLICY_RATE_LIMIT` (error diagnostic).
</Note>

## Unsupported features (REJECTED_LOUD)

The following Manifest language features are **not supported** in the Convex projection and emit compile-time error diagnostics (`CONVEX_UNSUPPORTED_*`):

| Feature | Diagnostic | Why it's unsupported |
|---------|-----------|---------------------|
| **Command retry** | `CONVEX_UNSUPPORTED_RETRY` | Convex mutations cannot honor per-attempt rollback + backoff sleep. Use platform optimistic concurrency (`versionProperty`) or caller-side retry instead. |
| **Entity approvals** | `CONVEX_UNSUPPORTED_APPROVAL` | No stage-state tracking or pre-command gate in Convex mutations. Approval workflows must be implemented at the application level. |
| **Read/`all` policy rateLimit** | `CONVEX_UNSUPPORTED_READ_POLICY_RATE_LIMIT` | Convex queries (`ctx.db.query`) cannot mutate durable buckets. Rate limiting is only supported on commands and write/execute/delete policies. |
| **Action kinds: `effect` / `publish` / `persist`** | `CONVEX_UNSUPPORTED_ACTION_KIND` | No mutation lowering for adapter actions. These require runtime adapters (`outboxStore`, `effectHandler`) not available in Convex. |
| **`async` commands** | `CONVEX_UNSUPPORTED_ASYNC_COMMAND` | No job queue or worker drain mechanism in Convex projection. |

These are **loud rejections** — the projection emits a hard diagnostic and skips the feature rather than silently ignoring it. This prevents "parsed but ignored" surprises and forces you to either remove the unsupported feature or restructure your model.

<Note>
  Partial capabilities exist for some features:
  - **Realtime**: Convex queries are already reactive via `useQuery` — no SSE artifact needed (unlike Next.js). This is `CONVEX_PARTIAL_REALTIME` (info diagnostic).
  - **Computed cache**: Computed helpers stay pure; Manifest's `request`/`session`/`ttl` cache strategies are not lowered. This is `CONVEX_PARTIAL_COMPUTED_CACHE` (info diagnostic).
</Note>


---

## drizzle

URL: https://manifest-b1e8623f.mintlify.app/projections/drizzle

---
title: 'Drizzle ORM schema projection'
sidebarTitle: 'Drizzle'
description: 'Generate a TypeScript-first Drizzle ORM schema from Manifest IR — compile-time, app-agnostic, with pgTable / mysqlTable / sqliteTable column builders and structured diagnostics.'
---

The Drizzle projection emits a TypeScript-first Drizzle ORM schema from compiled Manifest IR, compatible with Drizzle Kit. Use it when your persistence layer is Drizzle rather than Prisma; it mirrors the [Prisma projection's](/integration/prisma) compile-time, app-agnostic contract but targets Drizzle's `pgTable` / `mysqlTable` / `sqliteTable` column builders.

Registry name `drizzle`, surface `drizzle.schema`,
CLI `manifest generate … -p drizzle`. Package pin **3.6.41**.

## What it generates

The projection registers under the name `drizzle` and declares a single surface, `drizzle.schema`. A `generate` call returns one artifact (`id: 'drizzle.schema'`, content type `typescript`, written at the configured `output` path, default `schema.ts`).

The emitted file contains a table definition per persistent entity built with the dialect's table function, the dialect-appropriate imports, any `relations()` exports for entities that declare relationships, and index definitions. Relationships of kind `belongsTo` / `ref` emit a foreign-key column; `hasMany` / `hasOne` do not emit a column and are expressed through Drizzle's `relations()` API instead.

## Usage

```ts
import { compileToIR } from '@angriff36/manifest/ir-compiler';
import { getProjection } from '@angriff36/manifest/projections';

const { ir } = await compileToIR(source);

const projection = getProjection('drizzle');
const result = projection?.generate(ir, {
  surface: 'drizzle.schema',
  options: {
    dialect: 'postgresql',
    tableMappings: { Widget: 'widgets' },
  },
});

for (const artifact of result?.artifacts ?? []) {
  // write artifact.code at artifact.pathHint
}
```

`DrizzleProjection` can also be imported directly from `@angriff36/manifest/projections/drizzle`. Through the CLI it is dispatched by name:

```bash
manifest generate ir/widget.ir.json --projection drizzle --surface drizzle.schema --output src/db/schema.ts
```

## Type mapping & behavior

The IR `type.name` string maps to a Drizzle column builder through `DEFAULT_TYPE_MAPPING` in `type-mapping.ts`. The table covers `string` (`varchar(255)`), `text`, `uuid`, `boolean`/`bool`, `int` (`integer`), `bigint`, `float` (`real`), `decimal` and `money` (both `numeric`), `date`, `datetime` (`timestamp`), `json` (`jsonb`), and `bytes` (`bytea`). A type that is not in the table and not overridden produces a hard diagnostic and the column is skipped.

As with Prisma, bare `number` is intentionally absent. The projection emits a `DRIZZLE_AMBIGUOUS_NUMBER` diagnostic and skips the column rather than guessing; authors must pick `int`, `bigint`, `float`, `decimal`, or `money`. Numeric/decimal columns receive a default `(12, 2)` precision and scale unless overridden through the `precision` option.

The `dialect` option selects the column set and imports: `postgresql` uses `pgTable` from `drizzle-orm/pg-core`, `mysql` uses `mysqlTable` from `drizzle-orm/mysql-core`, and `sqlite` uses `sqliteTable` from `drizzle-orm/sqlite-core`. The projection emits diagnostics for relationship shapes it cannot wire on its own, including many-to-many relationships (which it asks you to model as a join entity with two `belongsTo` relations) and ambiguous multi-relation pairs.

## Options

The options object is `DrizzleProjectionOptions` in `drizzle/options.ts`, normalized through `normalizeOptions`. `dialect` (default `postgresql`) selects the SQL flavor, `schemaExportName` (default `schema`) is used for imports, and `output` (default `schema.ts`) is the artifact path hint.

Per-property options use the nested `Record<EntityName, Record<PropertyName, X>>` shape. `tableMappings` overrides the table name; `columnMappings` overrides a column name; `precision` sets `{ precision, scale }` on numeric columns; `indexes` defines composite or named indexes (`string[]` or `{ fields, name }`); `typeMappings` overrides the builder with a literal Drizzle builder name (e.g. `"integer"`, `"timestamp"`); and `foreignKeys` overrides the FK column name (string form) or supplies a full `{ fields, references, onDelete, onUpdate }` definition (object form), defaulting the FK column to `${relationshipName}Id`.

## Notes & limitations

<Note>
  Only persistent entities are emitted. Entities marked `external`, entities with no `store`
  declaration, and entities with a non-durable store target are skipped with info diagnostics
  (`DRIZZLE_SKIPPED_EXTERNAL`, `DRIZZLE_SKIPPED_NO_STORE`, `DRIZZLE_SKIPPED_NON_DURABLE`).
</Note>

This projection's behavior is taken directly from `src/manifest/projections/drizzle/generator.ts`, `type-mapping.ts`, and `options.ts`. Like Prisma, an entity missing a usable key (no `id` property and no `key [...]` declaration) produces a diagnostic and is skipped.


---

## express

URL: https://manifest-b1e8623f.mintlify.app/projections/express

---
title: 'Express & Fastify router projection'
sidebarTitle: 'Express'
description: 'Generate standalone Express or Fastify router modules from Manifest IR — typed handlers that read entities directly and dispatch writes through the runtime, with auth and optional Zod validation wired in.'
---

The Express projection generates standalone router modules — for either Express or Fastify — from compiled Manifest IR. Use it when you are building a Node.js HTTP service and want typed route handlers that read entities directly and dispatch writes through the Manifest runtime, with auth middleware and optional Zod validation already wired in.

The projection is registered under the name `express` and lives at `src/manifest/projections/express/generator.ts`. Despite the name it covers both frameworks via the `framework` option.

## What it generates

The projection exposes six surfaces.

`express.router` emits a complete router module (path hint `routes/manifest-router.ts`, or `routes/<entity>.ts` when an `entity` is supplied). For Express this is an exported `createManifestRouter(): Router`; for Fastify it is an exported `async manifestRoutes(fastify)` plugin. Each entity gets a `GET /{entity}/list` route, a `GET /{entity}/:id` route, and one `POST /{entity}/{command-kebab}` route per entity-scoped command. Read routes call `runtime.list(...)` / `runtime.get(...)` directly; command routes extract an `instanceId` from `body.instanceId ?? body.id` and call the companion facade `runCommand(entity, command, { params, instanceId }, context?)`, which adapts to `RuntimeEngine.runCommand(command, params ?? {}, { entityName, instanceId })`. Do not call `RuntimeEngine` with the 4-arg entity-first form.

`express.entity` emits a single-entity router; with no `entity` set it emits one artifact per entity. `express.types` emits TypeScript request/response interfaces (one `interface` per entity, with computed properties as `readonly`, plus a `<Entity><Command>Params` interface for each command that has parameters). `express.all` emits the router and the types together. `express.companions` emits a companion facade module with `runCommand(entity, command, { params, instanceId }, context?)` that adapts to `RuntimeEngine.runCommand`. `express.webhooks` emits webhook handlers for inbound IR webhooks (HTTP → command dispatch). An unknown surface returns an `UNKNOWN_SURFACE` error; a missing named entity yields an `EXPRESS_ENTITY_NOT_FOUND` warning.

Command handlers map runtime error codes to HTTP status: `GUARD_FAILED` to 403, `CONSTRAINT_VIOLATION` to 422, `CONCURRENCY_CONFLICT` to 409, `NOT_FOUND` to 404, and anything else to 500. When `includeComments` is on, each command handler is preceded by a JSDoc block listing its guards (with index and rendered expression), constraint and policy counts, and emitted events.

## Usage

```ts
import { getProjection } from '@angriff36/manifest/projections';

const projection = getProjection('express');

// Complete Express router
const router = projection.generate(ir, { surface: 'express.router' });

// Fastify plugin plus types
const fastify = projection.generate(ir, {
  surface: 'express.all',
  options: { framework: 'fastify', validationImportPath: './schemas' },
});
```

## Type mapping & behavior

`irTypeToTs` maps IR types to TypeScript: `string` to `string`; `number`/`integer`/`int` to `number`; `boolean`/`bool` to `boolean`; `date`/`datetime`/`uuid`/`email`/`url`/`uri`/`decimal` to `string`; `any` to `unknown`; `object` to `Record<string, unknown>`. Generic `array<T>` becomes `T[]`, `map<V>` becomes `Record<string, V>`, and `record` becomes `Record<string, unknown>`. Unknown type names fall back to `unknown`, and nullable types append ` | null`.

Entity interface fields are optional unless the property has the `required` modifier. Command params interface fields are optional unless the parameter is `required`. Entity route segments are the lower-cased entity name; command route segments are kebab-cased. When `validationImportPath` is set, command handlers import `<Entity><Command>ParamsSchema` and run `safeParse` on the body, returning a 400 `VALIDATION_ERROR` on failure before dispatch.

## Options

`ExpressProjectionOptions` accepts `framework` (`'express' | 'fastify'`, default `express`), `authImportPath` (default `./middleware/auth`), `authMiddlewareName` (default `requireAuth`), `runtimeImportPath` (default `./lib/manifest-runtime`), `runtimeFactoryName` (default `createManifestRuntime`), `validationImportPath` (default unset — no validation emitted), `basePath` (default `/api`), `includeTenantContext` (default `true`), `tenantIdProperty` (default `tenantId`), `emitTypes` (default `true`), `emitHeader` (default `true`), `publicReads` (default `false`), `includeComments` (default `true`).

## Notes & limitations

<Note>
  The generated module imports the auth middleware and runtime factory from the configured paths; it
  does not define them. Read routes (`list`/`get`) bypass the runtime engine and call the runtime's
  read methods directly, while all writes go through `runtime.runCommand`. The `basePath` option is
  normalized but the entity route paths in the generated handlers are not prefixed with it, so the
  prefix is applied where you mount the router, not inside the generated route strings.
</Note>

There is no dedicated CLI command for this projection. Emit Express/Fastify output by invoking `getProjection('express')` programmatically or by running `manifest build <ir> -p express` (any projection dispatches via `-p`).



---

## flutter

URL: https://manifest-b1e8623f.mintlify.app/projections/flutter

---
title: 'Flutter / Dart model projection'
sidebarTitle: 'Flutter'
description: 'Generate Dart model classes with fromJson/toJson serialization, copyWith patterns, constraint validation, and Riverpod or Provider state management hooks from Manifest IR.'
---

The Flutter projection generates type-safe Dart model classes from compiled Manifest IR. Use it when you are building a Flutter or Dart application and want Manifest entities represented as first-class Dart types with JSON serialization, equality, `copyWith`, constraint validation, and optional state management integration via Riverpod or Provider. The projection follows the same compile-time, app-agnostic contract as the [Pydantic projection](/projections/python-pydantic) but targets Dart conventions.

The projection is registered under the name `dart` and lives at `src/manifest/projections/dart/generator.ts`.

## What it generates

The projection exposes six surfaces.

`dart.entity` emits a single Dart model class for one entity. The class includes `fromJson`/`toJson` factory methods, a `copyWith` method for immutable updates, `==`/`hashCode` overrides for value equality, and a `validate()` method that checks Manifest constraints against property values.

`dart.command` emits a Dart class for command parameters and an optional return type class. Both include JSON serialization support.

`dart.models` emits all entity and command models in a single library file with barrel exports.

`dart.client` emits a Dio-based async HTTP client class with CRUD methods (`list`, `get`, `delete`) for each entity and typed command invocation methods.

Registry name is **`dart`** (not `flutter`). Client
networking is **Dio** only — there is no Python `httpx` dependency. CLI:
`manifest generate … -p dart`.

`dart.providers` emits state management hooks supporting Riverpod (`Provider`, `FutureProvider`, `FutureProvider.family`) or classic Provider (`ChangeNotifier`). Choose the pattern via the `stateManagement` option.

`dart.package` emits a complete Flutter package: models, client, providers, a `pubspec.yaml`, and a `README.md`.

## Usage

```ts
import { getProjection } from '@angriff36/manifest/projections';

const projection = getProjection('dart');

// Generate a single entity model
const entity = projection.generate(ir, {
  surface: 'dart.entity',
  entity: 'Order',
});

// Generate all models in one file
const models = projection.generate(ir, { surface: 'dart.models' });

// Generate a complete Flutter package
const pkg = projection.generate(ir, {
  surface: 'dart.package',
  options: {
    stateManagement: 'riverpod',
    clientBaseUrl: 'https://api.example.com',
    packageName: 'manifest_models',
  },
});
```

Through the CLI:

```bash
manifest generate ir/widget.ir.json --projection dart --surface dart.models --output lib/models/manifest_models.dart
```

## Type mapping

The projection maps Manifest IR types to Dart types:

| Manifest type      | Dart type        |
| ------------------ | ---------------- |
| `string`           | `String`         |
| `int`              | `int`            |
| `number` / `float` | `double`         |
| `boolean` / `bool` | `bool`           |
| `datetime`         | `DateTime`       |
| `uuid`             | `String`         |
| `decimal`          | `String`         |
| `array<T>`         | `List<T>`        |
| `map<V>`           | `Map<String, V>` |

Nullable types use Dart's `T?` syntax with null-checked JSON parsing. Enum types generate a Dart `enum` class with a `fromString` lookup factory. Computed properties are emitted as getter stubs annotated with a comment indicating they must be evaluated server-side.

## Constraint validation

Manifest constraints are mapped to `validate()` methods on generated entity classes. The projection uses the shared `constraint-analysis.ts` utilities:

- Numeric range constraints (`min`, `max`) become `if (field < min || field > max)` checks.
- String length constraints (`minLength`, `maxLength`) become `if (field.length < min || field.length > max)` checks.
- Pattern / regex constraints become `if (!RegExp(pattern).hasMatch(field))` checks.

The `validate()` method returns a list of violation descriptions, or an empty list when all constraints pass.

## Options

`DartProjectionOptions` accepts `stateManagement` (`'riverpod'` | `'provider'` | `'none'`, default `'riverpod'`), `clientBaseUrl` (default `'http://localhost:3000'`), `clientClassName` (default `'ManifestClient'`), `packageName` (default `'manifest_models'`), `emitPackageFiles` (default `false`), and `customImports` (additional import statements to include in generated files).

## Generated code example

For an entity `Order` with properties `id`, `total`, and `status`:

```dart
class Order {
  final String id;
  final double total;
  final String status;

  const Order({
    required this.id,
    required this.total,
    required this.status,
  });

  factory Order.fromJson(Map<String, dynamic> json) => Order(
    id: json['id'] as String,
    total: (json['total'] as num).toDouble(),
    status: json['status'] as String,
  });

  Map<String, dynamic> toJson() => {
    'id': id,
    'total': total,
    'status': status,
  };

  Order copyWith({String? id, double? total, String? status}) => Order(
    id: id ?? this.id,
    total: total ?? this.total,
    status: status ?? this.status,
  );

  List<String> validate() {
    final violations = <String>[];
    if (total < 0) violations.add('total must be >= 0');
    return violations;
  }

  @override
  bool operator ==(Object other) =>
    identical(this, other) ||
    other is Order && id == other.id && total == other.total && status == other.status;

  @override
  int get hashCode => Object.hash(id, total, status);
}
```

## Notes & limitations

<Note>
  The projection generates syntactically valid Dart code with `camelCase` field names and
  `PascalCase` class names. The Dio HTTP client is the default networking layer for the
  `dart.client` surface. Computed properties are emitted as getter stubs only — they carry a comment
  noting that the value must be computed server-side, since Manifest computed properties are
  evaluated by the runtime engine, not by the client.
</Note>

The `dart.providers` surface generates Riverpod or Provider state management hooks that wrap the HTTP client methods. The generated providers handle loading, error, and data states following the conventions of the chosen state management library.


---

## graphql

URL: https://manifest-b1e8623f.mintlify.app/projections/graphql

---
title: 'GraphQL schema projection'
sidebarTitle: 'GraphQL'
description: 'Generate a GraphQL SDL schema and matching resolver stubs from Manifest IR — entity reads become queries, commands become mutations, and events become subscriptions.'
---

The GraphQL projection generates a GraphQL SDL schema and matching resolver stubs from compiled Manifest IR. Entity reads become queries, commands become mutations, and events become subscriptions. Use it to expose a Manifest domain through a GraphQL server while keeping write semantics routed through the runtime.

## What it generates

The projection registers under the name `graphql` and declares two surfaces, `graphql.schema` and `graphql.resolvers`. The `graphql.schema` surface emits an SDL document (`id: 'graphql.schema'`, content type `graphql`, written at `schema.graphql`); the `graphql.resolvers` surface emits TypeScript resolver stubs (`id: 'graphql.resolvers'`, content type `typescript`, written at `resolvers.ts`).

The SDL contains entity object types, a `type Query` with per-entity read fields, a `type Mutation` with one field per command, a `type Subscription` derived from IR events, `input` types for command parameters, `enum` definitions from IR enums, and `scalar` declarations for any custom scalars detected (such as `DateTime`, `UUID`, `JSON`). The resolver stubs wire queries to direct database reads and mutations to the Manifest runtime.

## Usage

```ts
import { compileToIR } from '@angriff36/manifest/ir-compiler';
import { getProjection } from '@angriff36/manifest/projections';

const { ir } = await compileToIR(source);

const projection = getProjection('graphql');

const schema = projection?.generate(ir, { surface: 'graphql.schema' });
const resolvers = projection?.generate(ir, {
  surface: 'graphql.resolvers',
  options: {
    runtimeImportPath: '@repo/manifest/runtime',
    databaseImportPath: '@repo/database',
  },
});
```

`GraphQLProjection` can also be imported directly from `@angriff36/manifest/projections/graphql`, or dispatched through the CLI by name:

```bash
manifest generate ir/recipe.ir.json --projection graphql --surface graphql.schema --output schema.graphql
```

## Type mapping & behavior

IR types map to GraphQL types in `irTypeToGraphQL`. `string` maps to `String`, `number` to `Float`, `integer`/`int` to `Int`, `boolean`/`bool` to `Boolean`, `date` to `Date`, `datetime` to `DateTime`, `uuid` to `UUID`, and `email` / `url` / `uri` to `String`. `array` (with a generic) becomes a GraphQL list with non-null inner elements unless the inner type is nullable, and `map` / `record` / `object` become `JSON`. Unknown types fall back to `String`. Types such as `DateTime`, `UUID`, `JSON`, `Date`, `Email`, and `URL` are declared as custom scalars when used.

Mutations are mapped from commands, with non-null markers placed on required parameters in the generated input types. Resolver stubs for queries call the database client directly (for example `database.<table>.findUnique`).

Generated mutation stubs call a **phantom** 3-arg form
`runtime.runCommand(entity, command, input)` on whatever `createManifestRuntime(context)` returns.
That is **not** `RuntimeEngine.runCommand(commandName, input, options?)`. Treat resolvers as
scaffolding: either implement a companion facade that adapts the entity-first call, or rewrite
stubs to the real engine signature (`src/manifest/projections/graphql/generator.ts`).

## Options

The options object is `GraphQLProjectionOptions` in `graphql/types.ts`. All boolean toggles default to true: `includeAuthDirectives` (emit `@auth(requires: ...)` on fields/types that carry policies), `includeSubscriptions` (subscription types from events), `includeGuardDescriptions` and `includeConstraintDescriptions` (fold guard/constraint info into descriptions), `includeResolverStubs` (emit resolver stubs alongside the SDL), `includeComputedProperties`, `includeInputTypes` (input types for command parameters), and `includeEnums`.

`runtimeImportPath` (default `@/lib/manifest-runtime`) and `databaseImportPath` (default `@/lib/database`) set the imports used in resolver stubs. `customScalars` supplies extra scalar definitions beyond the auto-detected set.

## Notes & limitations

<Warning>
  The resolver stubs are stubs — the query resolvers inline a direct database lookup and the
  mutation resolvers are scaffolding that you complete against your runtime; they are not a finished
  server.
</Warning>

This projection's behavior is taken directly from `src/manifest/projections/graphql/generator.ts` and `types.ts`.


---

## health

URL: https://manifest-b1e8623f.mintlify.app/projections/health

---
title: 'Health check projection'
sidebarTitle: 'Health'
description: 'Generate Manifest health-check handlers from IR — framework-agnostic core plus Next.js and Express wrappers for liveness/readiness probes.'
---

Created: 2026-07-15. Edit 2026-07-22: honest scaffolding (`stub: true`). Later same day: live probes via injectable `HealthProbes` / `configureHealthProbes`.

The health projection generates runtime health-check handlers from compiled Manifest IR. Use it for Kubernetes liveness/readiness probes or load-balancer checks that return structured JSON (`status`, `timestamp`, `checks`).

The projection is registered under the name `health` (`HealthCheckProjection` in `src/manifest/projections/health/generator.ts`, wired via `src/manifest/projections/builtins.ts`). There is **no** dedicated `@angriff36/manifest/projections/health` package export — retrieve it with `getProjection('health')` from `@angriff36/manifest/projections`.

## What it generates

Three surfaces:

| Surface | Default path hint | Artifact |
| ------- | ----------------- | -------- |
| `health.handler` | `src/lib/manifest-health-handler.ts` | Framework-agnostic `runHealthCheck()` plus typed `HealthReport` / `ComponentHealth` |
| `health.nextjs` | `app/api/manifest/health/route.ts` | Next.js App Router `GET` that imports the handler and maps status → HTTP |
| `health.express` | `src/middleware/manifest-health.ts` | Express `manifestHealthHandler` middleware |

An unknown surface returns diagnostic `UNKNOWN_SURFACE` (error).

`health.handler` bakes IR provenance (`contentHash`, optional `irHash`, `compilerVersion`, `schemaVersion`, `compiledAt`) into `MANIFEST_IR_META`. When enabled, it emits:

- **IR check** — without probes: healthy + baked provenance (`stub: true`). With `getLiveContentHash`: compares to baked `contentHash`.
- **Store checks** — one async function per unique `ir.stores[].target`. `memory` / `localStorage` always healthy. Other targets use `probes.checkStore(target)` when provided; otherwise scaffolding (`stub: true`).
- **Outbox check** — only for `postgres` / `supabase` stores. With `getOutboxDepth`: live depth; otherwise scaffolding.

Wire probes with `configureHealthProbes({ … })` or `runHealthCheck(probes)`.

Aggregation in `runHealthCheck()`: IR failure → overall `unhealthy`; all stores unhealthy → `unhealthy`; some stores unhealthy → `degraded`; outbox unhealthy while otherwise healthy → `degraded`.

`health.nextjs` / `health.express` import `runHealthCheck` from `handlerImportPath` and respond with `healthyStatus` (default 200) when `status === 'healthy'`, otherwise `unhealthyStatus` (default 503) for both `degraded` and `unhealthy`.

## Usage

```typescript health-generate.ts
import { getProjection } from '@angriff36/manifest/projections';

const projection = getProjection('health');

const handler = projection.generate(ir, { surface: 'health.handler' });
const nextjs = projection.generate(ir, { surface: 'health.nextjs' });
const express = projection.generate(ir, {
  surface: 'health.express',
  options: { handlerImportPath: './lib/manifest-health-handler' },
});
```

CLI: `manifest generate <ir> -p health` resolves via the projection registry (same path as other non-Next.js projections). Emit each surface you need; wrappers require the handler module at `handlerImportPath`.

## Options

`HealthCheckProjectionOptions` / `HEALTH_DEFAULTS` in `src/manifest/projections/health/types.ts`:

| Option | Default |
| ------ | ------- |
| `nextjsPathHint` | `app/api/manifest/health/route.ts` |
| `expressPathHint` | `src/middleware/manifest-health.ts` |
| `handlerPathHint` | `src/lib/manifest-health-handler.ts` |
| `handlerImportPath` | `@/lib/manifest-health-handler` |
| `healthyStatus` | `200` |
| `unhealthyStatus` | `503` |
| `includeIRCheck` | `true` |
| `includeStoreChecks` | `true` |
| `includeOutboxCheck` | `true` |

## Notes & limitations

<Note>
  Without `HealthProbes`, non-memory store / outbox / IR checks stay scaffolding (`details.stub:
  true`). HTTP 200 without probes is not proof of store/outbox connectivity. Hosts supply live I/O
  through the probe hooks. Next.js and Express wrappers do not emit the core handler — generate
  `health.handler` at `handlerImportPath`.
</Note>

Projections are tooling, not runtime semantics; this does not change command execution order or IR meaning.


---

## hono

URL: https://manifest-b1e8623f.mintlify.app/projections/hono

---
title: 'Hono edge router projection'
sidebarTitle: 'Hono'
description: 'Generate edge-runtime-optimized Hono route handlers from Manifest IR — a single router file with zero Node.js dependencies for Cloudflare Workers, Vercel Edge, or Deno Deploy.'
---

The Hono projection generates edge-runtime-optimized route handlers from compiled Manifest IR using the [Hono](https://hono.dev) framework. Use it when you are deploying to Cloudflare Workers, Vercel Edge, or Deno Deploy and need a single router file with zero Node.js dependencies. It mirrors the [Express projection's](/projections/express) structure but emits Hono idioms — `c.get('user')` for auth context, `c.req.json()` for bodies, and `c.json(data, status)` for responses.

The projection is registered under the name `hono` and lives at `src/manifest/projections/hono/generator.ts`.

## What it generates

The projection exposes six surfaces.

`hono.router` emits a complete app module (path hint `src/routes.ts`, or `routes/<entity>.ts` when an `entity` is supplied). It imports `Hono`, the configured auth middleware, and the runtime factory, declares a typed `Env` (binding `Variables.user`), constructs `const app = new Hono<Env>()`, registers routes per entity, and ends with `export default app` for direct edge deployment. Each entity gets `GET /{entity}/list`, `GET /{entity}/:id`, and one `POST /{entity}/{command-kebab}` per entity-scoped command.

`hono.entity` emits a single-entity router (one artifact per entity when no `entity` is set). `hono.types` emits TypeScript types — an `Env` type, an `AuthUser` interface, one `interface` per entity (computed properties as `readonly`), and a `<Entity><Command>Params` interface per parameterized command. `hono.all` emits the router and the types together. `hono.companions` emits a companion facade module with `runCommand(entity, command, { params, instanceId }, context?)` that adapts to `RuntimeEngine.runCommand`. `hono.webhooks` emits webhook handlers for inbound IR webhooks (HTTP → command dispatch). An unknown surface returns an `UNKNOWN_SURFACE` error; a missing named entity yields a `HONO_ENTITY_NOT_FOUND` warning.

Read handlers call `runtime.list(...)` / `runtime.get(...)` directly, reading the tenant id from `c.get('user')` when tenant context is enabled. Command handlers parse the body with `c.req.json()`, derive an `instanceId` from `body.instanceId ?? body.id`, and call the companion facade `runCommand(entity, command, { params, instanceId }, context?)`, which adapts to `RuntimeEngine.runCommand(command, params ?? {}, { entityName, instanceId })`. Do not call `RuntimeEngine` with the 4-arg entity-first form.

Runtime error codes map to HTTP status the same way as the Express projection: `GUARD_FAILED` to 403, `CONSTRAINT_VIOLATION` to 422, `CONCURRENCY_CONFLICT` to 409, `NOT_FOUND` to 404, otherwise 500.

## Usage

```ts
import { getProjection } from '@angriff36/manifest/projections';

const projection = getProjection('hono');

const router = projection.generate(ir, { surface: 'hono.router' });

const all = projection.generate(ir, {
  surface: 'hono.all',
  options: { validationImportPath: './schemas', publicReads: true },
});
```

## Type mapping & behavior

`irTypeToTs` is identical to the Express projection: `string` to `string`; `number`/`integer`/`int` to `number`; `boolean`/`bool` to `boolean`; `date`/`datetime`/`uuid`/`email`/`url`/`uri`/`decimal` to `string`; `any` to `unknown`; `object` to `Record<string, unknown>`; `array<T>` to `T[]`; `map<V>` to `Record<string, V>`; `record` to `Record<string, unknown>`; unknown names to `unknown`; nullable appends ` | null`.

Entity interface fields are optional unless the property has the `required` modifier; command params fields are optional unless the parameter is `required`. Route segments use the lower-cased entity name and the kebab-cased command name. When `validationImportPath` is set, command handlers import `<Entity><Command>ParamsSchema` and `safeParse` the parsed body, returning a 400 `VALIDATION_ERROR` before dispatch. When `includeComments` is on, each command handler is preceded by a JSDoc block describing guards, constraint/policy counts, and emitted events.

## Options

`HonoProjectionOptions` accepts `authImportPath` (default `./middleware/auth`), `authMiddlewareName` (default `requireAuth`), `runtimeImportPath` (default `./lib/manifest-runtime`), `runtimeFactoryName` (default `createManifestRuntime`), `validationImportPath` (default unset — no validation emitted), `basePath` (default `/api`), `includeTenantContext` (default `true`), `tenantIdProperty` (default `tenantId`), `emitTypes` (default `true`), `emitHeader` (default `true`), `publicReads` (default `false`), `includeComments` (default `true`). Note that, unlike the Express projection, there is no `framework` option — the target is always Hono.

## Notes & limitations

<Note>
  The generated app imports its auth middleware and runtime factory from the configured paths rather
  than defining them, and uses Hono's `c.get('user')` convention (set by that middleware) rather
  than an Express-style `req.user`. As with the Express projection, the `basePath` option is
  normalized but not applied to the generated route strings; apply the prefix where you mount or
  `route()` the app. Read routes bypass the runtime engine; all writes go through
  `runtime.runCommand`.
</Note>

There is no dedicated CLI command for this projection. Emit Hono output by invoking `getProjection('hono')` programmatically or by running `manifest build <ir> -p hono` (any projection dispatches via `-p`).



---

## json schema

URL: https://manifest-b1e8623f.mintlify.app/projections/json-schema

---
title: 'JSON Schema projection'
sidebarTitle: 'JSON Schema'
description: 'Generate language-neutral JSON Schema documents (draft-07, 2019-09, or 2020-12) from Manifest IR entity definitions for cross-language validation contracts.'
---

The JSON Schema projection generates JSON Schema documents (draft-07, 2019-09, or 2020-12) from Manifest IR entity definitions. Use it when you need a language-neutral validation contract for your domain objects — for request validation at an API gateway, payload validation in non-TypeScript services, or publishing entity shapes to external consumers.

The projection is registered under the name `jsonschema` and lives at `src/manifest/projections/jsonschema/generator.ts`. It produces one schema document per entity.

## What it generates

The projection exposes two surfaces.

`jsonschema.entity` generates a single entity schema and requires the `entity` field on the request. If `entity` is omitted it falls back to generating all entities; if the named entity is not found it returns an `ENTITY_NOT_FOUND` error diagnostic.

`jsonschema.schemas` generates one schema artifact per entity, sorted alphabetically by entity name for deterministic output.

Each artifact is JSON content (path hint `schemas/<EntityName>.schema.json`, artifact id `jsonschema.entity.<EntityName>`) containing a `$schema` URI for the chosen draft, a `title` set to the entity name, `type: "object"`, a `properties` map, and — by default — `additionalProperties: false`. A `required` array is emitted only when at least one property carries the `required` modifier. When a `baseUri` option is set, each document also gets an `$id` of `<baseUri>/<EntityName>.schema.json`. An unrecognized surface returns an `UNKNOWN_SURFACE` error diagnostic.

## Usage

```ts
import { getProjection } from '@angriff36/manifest/projections';

const projection = getProjection('jsonschema');

// All entities as separate schema files
const all = projection.generate(ir, { surface: 'jsonschema.schemas' });

// A single entity, draft 2020-12, with $id references
const one = projection.generate(ir, {
  surface: 'jsonschema.entity',
  entity: 'Recipe',
  options: { draft: '2020-12', baseUri: 'https://example.com/schemas' },
});
```

## Type mapping & behavior

Scalar IR types map as follows: `string`/`text` to `{ type: "string" }`; `number`/`float`/`decimal` to `number`; `int`/`integer`/`bigint` to `integer`; `boolean`/`bool` to `boolean`; `date` to a string with `format: "date"`; `datetime` to `date-time`; `uuid` to `uuid`; `email` to `email`; `url`/`uri` to `uri`; `bytes` to a string with `format: "byte"`; `object` to an object with open `additionalProperties`; and `json`/`any` to an empty (unconstrained) schema. Generic `array<T>` becomes an array with mapped `items`; `map<V>` becomes an object with mapped `additionalProperties`; `record` becomes an object with open `additionalProperties`. An unknown type name produces a `string` schema plus an `UNKNOWN_TYPE` warning diagnostic.

A type name that matches an entry in `ir.enums` is emitted as `{ type: "string", enum: [...] }` using the enum value names. Nullable types are expressed as an array-form union (for example `type: ["string", "null"]`); a nullable type with no base `type` is left unchanged.

Property defaults are emitted as `default`, and the `readonly` modifier sets `readOnly: true`. Static constraint bounds are extracted via `analyzeConstraints()` from `src/manifest/constraint-analysis.ts`: numeric ranges populate `minimum`/`maximum`, length constraints populate `minLength`/`maxLength`, and the first pattern constraint per property populates `pattern`. Computed properties (when included) are emitted as `readOnly` with a `description` of `Computed: <expression>`.

## Options

`JsonSchemaProjectionOptions` accepts `draft` (`'draft-07' | '2019-09' | '2020-12'`, default `draft-07`), `includeComputed` (default `true`), `strictAdditionalProperties` (default `true`), and `baseUri` (default unset). The two boolean options default to `true` unless explicitly set to `false`.

## Notes & limitations

<Note>
  The projection is self-contained and adds no new dependencies; it relies only on existing IR types
  and the shared constraint-analysis utility. Pattern constraints are collapsed to a single
  `pattern` keyword — when a property has multiple pattern constraints, only the first is emitted.
  The nullable handling uses the array-type-union form across all three drafts rather than
  draft-specific idioms such as `oneOf`.
</Note>

There is

Use the registry CLI:
`manifest generate <ir> -p jsonschema -s jsonschema.schemas --output .`
(or `jsonschema.entity` with `--entity`). Programmatic `getProjection('jsonschema')` still works.


---

## llm context

URL: https://manifest-b1e8623f.mintlify.app/projections/llm-context

---
title: 'LLM context projection'
sidebarTitle: 'LLM Context'
description: "Generate a structured manifest-context.json document from Manifest IR, optimized for injecting a full domain model into an AI agent's context window."
---

The LLM Context projection generates a structured `manifest-context.json` document from compiled Manifest IR, optimized for injection into an AI agent's context window. Use it when an agent needs to understand a full domain model — entities, commands, policies, constraints, relationships, enums, events, and stores — in a single context load rather than reasoning over raw `.manifest` source or the full IR.

The projection is registered under the name `llm-context` and lives at `src/manifest/projections/llm-context/generator.ts`.

## What it generates

The projection exposes three surfaces, each emitting a single JSON artifact.

`llm-context.full` builds the complete context document (path hint `manifest-context.json`, artifact id `llm-context-full`). The document carries `$schema: "manifest-context/v1"` and these sections: `meta` (generation timestamp plus compiler version, schema version, and content hash from IR provenance), `domain` (counts of entities, commands, policies, constraints, enums, and events; the module list; and a `multiTenant` flag), `entities`, `commands`, `policies`, `constraints`, and `relationships`. Optional sections `enums`, `events`, `stores`, and the raw `ir` are appended according to options.

`llm-context.summary` builds the same document with `includeRawIR` and `includeExpressions` forced off — guard, action, constraint, and computed-property expressions are replaced with `"[omitted]"` and the raw IR is excluded (path hint `manifest-context-summary.json`).

`llm-context.ir` emits the raw IR object as pretty-printed JSON with no transformation (path hint `manifest-ir.json`). An unrecognized surface returns a warning diagnostic listing the available surfaces.

## Usage

```ts
import { getProjection } from '@angriff36/manifest/projections';

const projection = getProjection('llm-context');

// Full context for agent injection
const full = projection.generate(ir, { surface: 'llm-context.full' });

// Lightweight summary (no raw IR, no expressions)
const summary = projection.generate(ir, { surface: 'llm-context.summary' });

// Trim further with options
const lean = projection.generate(ir, {
  surface: 'llm-context.full',
  options: { includeRawIR: false, includeStores: false },
});
```

## Type mapping & behavior

IR types are rendered to human-readable strings by `formatIRType`: the type name, an optional `<Generic>` suffix, and a trailing ` | null` when nullable (for example `array<string>`, `number | null`). Expressions (guards, actions, constraint and policy bodies, computed-property bodies) are rendered to source-like strings by a standalone `formatExpression` — literals, identifiers, member access, binary/unary operators, calls, conditionals, arrays, objects, and lambdas are all supported. Property and parameter default values are converted to plain JSON values.

For entities, a property is reported `required` when it carries the `required` modifier; constraint `severity` defaults to `"block"` when unset. For commands, a parameter is reported `required` only when it is required and has no default value. Each entity's `commands` field lists the names of commands scoped to it. When `includeExpressions` is false (as in summary mode), every expression field becomes `"[omitted]"`.

## Options

`LlmContextProjectionOptions` accepts six boolean flags, all defaulting to `true`: `includeRawIR`, `includeExpressions`, `includeEnums`, `includeEvents`, `includeStores`, and `emitHeader`. The summary surface overrides `includeRawIR` and `includeExpressions` to `false` regardless of what is passed.

## Notes & limitations

<Warning>
  The `emitHeader` option is declared and defaulted to `true`, but the generator emits pure JSON in
  all three modes and does not write a header comment — the option has no observable effect on
  output. As of the deterministic-generation change, no surface emits a generation timestamp: identical IR produces byte-for-byte identical output on every run (`meta.generatedAt` was removed).
</Warning>

There is

Use the registry CLI:
`manifest generate <ir> -p llm-context -s llm-context.full --output .`
(also `llm-context.summary`, `llm-context.ir`). Programmatic `getProjection('llm-context')` still works.


---

## mermaid

URL: https://manifest-b1e8623f.mintlify.app/projections/mermaid

---
title: 'Mermaid diagram projection'
sidebarTitle: 'Mermaid'
description: 'Generate deterministic Mermaid diagrams from Manifest IR — entity-relationship, state machine, and command sequence diagrams for documentation and design review.'
---

The Mermaid projection generates [Mermaid](https://mermaid.js.org) diagrams from compiled Manifest IR. Use it to visualize a domain model — entity relationships, state machines, and command execution flows — for documentation, design review, or onboarding. Diagrams are deterministic: identical IR always yields identical output.

Registry name `mermaid`; dedicated CLI
`manifest diagram` exists (`packages/cli/src/commands/diagram.ts`). Also
`manifest generate -p mermaid`. Package pin **3.6.41**.

The projection is registered under the name `mermaid` and lives at `src/manifest/projections/mermaid/generator.ts`. A dedicated CLI wrapper, `manifest diagram`, is provided at `packages/cli/src/commands/diagram.ts`.

## What it generates

The generator emits exactly three diagram families across four surfaces.

`mermaid.er` produces a single `erDiagram` from `ir.entities` and their relationships. Each entity becomes a block; when `includeProperties` is on (the default) each property is rendered as a typed attribute, annotated `"PK"` when the property is `required` or `"nullable"` when the type is nullable. Relationships are rendered as cardinality edges, deduplicated per `(source, target, name)`. With no entities, an `NO_ENTITIES` info diagnostic is returned.

`mermaid.state` produces one `stateDiagram-v2` per entity that has `transitions`. The generator derives the initial state from the default value of the property that backs the transitions, emits `[*] -->` into it, marks states with no outgoing transitions as terminal (`--> [*]`), and renders each transition (sorted for determinism). Entities without transitions yield `NO_TRANSITIONS` / `NO_STATE_ENTITIES` diagnostics; an unknown entity filter yields `ENTITY_NOT_FOUND`.

`mermaid.sequence` produces one `sequenceDiagram` per entity-scoped command, showing the execution flow: the client invoking the command, a `Note` for policies, a `Note` plus per-guard messages for guards, action steps, event emissions to an `EventBus` participant (with channel when known), and the return type. Guard expression labels longer than 60 characters are truncated.

`mermaid.all` runs the ER, state, and sequence generators and concatenates their artifacts. An unknown surface returns an `UNKNOWN_SURFACE` error diagnostic. Artifacts carry `contentType: "mermaid"` and `.mmd` path hints (for example `diagrams/er-diagram.mmd`, `diagrams/state-<entity>.mmd`, `diagrams/sequence-<entity>-<command>.mmd`).

## Usage

Programmatically through the registry:

```ts
import { getProjection } from '@angriff36/manifest/projections';

const projection = getProjection('mermaid');
const result = projection.generate(ir, {
  surface: 'mermaid.all',
  options: { markdown: true, includeProperties: true },
});
```

Or via the CLI, which accepts a `.manifest` source file, a precompiled `.ir.json`, a directory, or a glob, merges all inputs into one IR, and writes `.mmd` files to the output directory:

```bash
manifest diagram ./schema.manifest --type er --output diagrams
manifest diagram ./src --type all --markdown
manifest diagram ./schema.manifest --type state --entity Order
```

The CLI flags are `-o, --output <path>` (default `diagrams`), `-t, --type <er|state|sequence|all>` (default `all`), `-e, --entity <name>`, and `--markdown`. The `--type` value is mapped to the corresponding `mermaid.*` surface.

## Type mapping & behavior

ER attribute types are mapped by `irTypeToMermaidType`: `string`, `number`, `boolean`, `date`, `datetime`, `decimal`, and `any` map to their lowercase names; any other type name passes through unchanged. Relationship kinds map to cardinality notation: `hasMany` to `||--o{`, `hasOne` to `||--||`, `belongsTo` and `ref` to `}o--||`, with `||--||` as the fallback. Entity and state names are sanitized to `[A-Za-z0-9_]`. Quotes inside labels are escaped to `#quot;`. When `markdown` is enabled, each diagram is wrapped in a fenced ` ```mermaid ` block.

## Options

`MermaidProjectionOptions` accepts `markdown` (default `false`), `includeProperties` (default `true`, applies to ER diagrams), and `entity` (a filter for state and sequence diagrams). When a request supplies a top-level `entity` field, it is merged into the options as the `entity` filter.

## Notes & limitations

<Note>
  Only the three diagram families above exist — there are no class, flowchart, or gantt outputs.
  State diagrams require entity `transitions` to be present in the IR; entities without them are
  silently skipped (with an info diagnostic). Sequence diagrams are emitted only for entity-scoped
  commands. The CLI merges multiple input files by concatenating their `entities`, `stores`,
  `events`, `commands`, and `policies`, taking provenance from the first file — it does not
  deduplicate across files, so overlapping definitions can appear more than once.
</Note>


---

## mongoose

URL: https://manifest-b1e8623f.mintlify.app/projections/mongoose

---
title: 'Mongoose schema projection'
sidebarTitle: 'Mongoose'
description: 'Generate Mongoose schema and model definitions from Manifest IR for MongoDB-backed entities.'
---

The Mongoose projection generates TypeScript Mongoose schema and model definitions from compiled Manifest IR. Only entities with `store … in mongodb` are emitted — entities with other store targets are skipped with info diagnostics.

Registry name `mongoose`, surface `mongoose.schema`, CLI `manifest generate -p mongoose`. Package pin **3.6.41**.

## What it generates

The projection registers under the name `mongoose` and declares a single surface, `mongoose.schema`. A `generate` call returns one artifact (`id: 'mongoose.schema'`, content type `typescript`, default output `schema.ts`).

Generated artifacts include:

- **Schema definitions** — `new Schema({ ... })` with field types and validators
- **Model exports** — `model('EntityName', EntitySchema, 'collection-name')` for each entity
- **FK fields** — `Schema.Types.ObjectId` with `ref` for `belongsTo`/`ref` relationships when the FK property doesn't already exist
- **Timestamp support** — Optional `{ timestamps: true }` schema option for `createdAt`/`updatedAt`

## Type mapping

IR types map to Mongoose schema types via a built-in type table. Bare `number` is **intentionally omitted** — Manifest's `number` is ambiguous between integers, reals, and currency. Use precise types:

| IR Type | Mongoose SchemaType | Notes |
| ------- | ------------------- | ----- |
| `string`, `text`, `uuid`, `email`, `url` | `String` | String family |
| `boolean`, `bool` | `Boolean` | Boolean values |
| `int`, `integer` | `Number` | Integer counts and IDs |
| `float` | `Number` | Real numbers where rounding is acceptable |
| `decimal`, `money` | `Schema.Types.Decimal128` | Financial accuracy |
| `bigint` | `Schema.Types.BigInt` | Large integers (MongoDB 6.0+) |
| `date`, `datetime`, `timestamp` | `Date` | Temporal values |
| `json`, `object`, `any` | `Schema.Types.Mixed` | Unstructured data |
| `bytes` | `Buffer` | Binary data |
| `array<T>` | `[...]` | Array fields (generic preserves inner type) |

Using bare `number` without a `typeMappings` override produces a **MONGOOSE_AMBIGUOUS_NUMBER** error diagnostic — the field is skipped until you pick a precise type.

## Usage

```ts
import { compileToIR } from '@angriff36/manifest/ir-compiler';
import { getProjection } from '@angriff36/manifest/projections';

const { ir } = await compileToIR(source);

const projection = getProjection('mongoose');
const result = projection?.generate(ir, {
  surface: 'mongoose.schema',
  options: {
    collectionMappings: { Product: 'products' },
    timestamps: true,
  },
});

const schema = result?.artifacts[0]?.code; // TypeScript code
```

Invoke via CLI:

```bash
manifest generate ir/recipe.ir.json --projection mongoose --surface mongoose.schema --output schema.ts
```

## Options

The options object conforms to `MongooseProjectionOptions` in `projections/mongoose/options.ts`.

| Option | Type | Default | Description |
| ------ | ---- | ------- | ----------- |
| `output` | `string` | `'schema.ts'` | Output file path hint |
| `collectionMappings` | `Record<string, string>` | `{}` | Map IR entity names to MongoDB collection names |
| `fieldMappings` | `Record<string, Record<string, string>>` | `{}` | Rename fields per entity (`{ Entity: { property: 'field_name' } }`) |
| `typeMappings` | `Record<string, Record<string, string>>` | `{}` | Override Mongoose type per property (`{ Entity: { property: 'Schema.Types.Mixed' } }`) |
| `includeValidation` | `boolean` | `true` | Emit `required`/`unique` validators from IR modifiers |
| `timestamps` | `boolean` | `false` | Add `{ timestamps: true }` to each schema |

## Relationship handling

For `belongsTo` and `ref` relationships, the projection emits an ObjectId foreign key field when the FK property doesn't already exist on the entity:

```ts
// IR: entity Book { belongsTo Author }
// Generated:
authorId: { type: Schema.Types.ObjectId, ref: 'Author' }
```

If you've already declared the FK property explicitly (e.g., `authorId: string`), the projection uses your field definition instead of synthesizing one.

## Diagnostics

The projection emits diagnostics for:

| Code | Severity | Condition |
| ---- | -------- | --------- |
| `MONGOOSE_AMBIGUOUS_NUMBER` | `error` | Property uses bare `number` type without `typeMappings` override |
| `MONGOOSE_UNKNOWN_TYPE` | `error` | IR type has no default Mongoose mapping and no override |
| `MONGOOSE_SKIPPED_NO_STORE` | `info` | Entity has no `store` declaration |
| `MONGOOSE_SKIPPED_INCOMPATIBLE` | `info` | Entity's store target isn't `mongodb` |
| `MONGOOSE_SKIPPED_EXTERNAL` | `info` | Entity is marked `external` |

Error diagnostics (ambiguous/unknown types) skip the affected field — the schema still generates for other valid properties.

<Warning>
  Computed properties are **never** emitted as persisted fields. Mongoose schemas represent persisted MongoDB state only; computed values exist at the Manifest runtime level.
</Warning>


---

## openapi

URL: https://manifest-b1e8623f.mintlify.app/projections/openapi

---
title: 'OpenAPI specification projection'
sidebarTitle: 'OpenAPI'
description: 'Generate a complete OpenAPI 3.1.0 specification from Manifest IR — entity reads, command writes, JSON Schema bodies, security schemes, and constraint error responses.'
---

The OpenAPI projection generates a complete OpenAPI 3.1.0 specification from compiled Manifest IR — entity read operations, command write operations, JSON Schema-typed bodies, security schemes, and constraint error responses. Use it to feed OpenAPI toolchains for SDK generation, mock servers, or API documentation.

Registry name `openapi`, surface `openapi.spec`,
CLI `manifest generate … -p openapi`. Package pin **3.6.41**.

## What it generates

The projection registers under the name `openapi` and declares a single surface, `openapi.spec`. A `generate` call returns one artifact (`id: 'openapi.spec'`, content type `json`, written at `openapi.json`).

The spec includes GET list and GET detail operations per entity (with path parameters on the detail route), POST operations for commands with typed request and response bodies, entity schemas in `components.schemas` (carrying required, optional, readonly, and computed properties), write schemas that exclude read-only properties, command-specific request schemas, and constraint error response schemas (`ConstraintErrorResponse`, `GuardFailureResponse`, `ConcurrencyConflictResponse`). Operation IDs are derived from entity and command names (for example `listRecipes`, `getRecipe`, `recipeCreate`), operations are tagged by entity, and guard/constraint information is folded into operation descriptions. Output is deterministic — entities and commands are sorted.

## Usage

```ts
import { compileToIR } from '@angriff36/manifest/ir-compiler';
import { getProjection } from '@angriff36/manifest/projections';

const { ir } = await compileToIR(source);

const projection = getProjection('openapi');
const result = projection?.generate(ir, {
  surface: 'openapi.spec',
  options: {
    basePath: '/api',
    info: { title: 'Recipe API', version: '1.0.0' },
    servers: [{ url: 'https://api.example.com' }],
  },
});

const spec = result?.artifacts[0]?.code; // JSON string
```

`OpenApiProjection` can also be imported directly from `@angriff36/manifest/projections/openapi`, or dispatched through the CLI by name:

```bash
manifest generate ir/recipe.ir.json --projection openapi --surface openapi.spec --output openapi.json
```

## Type mapping & behavior

IR types map to JSON Schema in `irTypeToJsonSchema`. `string`, `number`, `integer`/`int`, and `boolean`/`bool` map to their JSON Schema equivalents; `date` and `datetime` become `string` with `date` / `date-time` formats; `uuid`, `email`, and `url`/`uri` become `string` with `uuid`, `email`, and `uri` formats. `array` (with a generic) becomes a typed array, `map` becomes an object with typed `additionalProperties`, and `record` / `object` become open objects. Unknown types fall back to `string`.

Note that unlike the [Prisma](/integration/prisma) and [Drizzle](/projections/drizzle) projections, OpenAPI maps bare `number` directly to a JSON Schema `number` — there is no ambiguity diagnostic here, because JSON Schema's numeric type is itself unconstrained between integer and real.

Security is derived from entity policies when `includePolicySecurity` is enabled, and global security plus the scheme catalog come from the options. Constraint and guard metadata is surfaced in descriptions and as the error response schemas described above.

## Options

The options object is `OpenApiProjectionOptions` in `openapi/types.ts`. `basePath` (default `/api`) prefixes all routes. `info` overrides the title, version, description, contact, and license; the title defaults to a value derived from IR modules or `Manifest API`, and the version defaults to the IR provenance `schemaVersion` or `1.0.0`. `servers` lists server URLs (with optional variables), `securitySchemes` defines the named schemes in `components` (`apiKey`, `http`, `oauth2`, `openIdConnect`), and `security` sets global requirements.

Boolean toggles default to true: `includeAuth` (auth on routes), `includeTenant` (tenant context), `includeConstraintErrors` (the error response schemas), and `includePolicySecurity` (per-operation security derived from policies).

## Command path styles

The `commandPathStyle` option controls how command write operations appear in the OpenAPI paths:

| Style | Command write path | Description |
| ----- | ----------------- | ----------- |
| `dispatcher` (canonical) | `POST {basePath}/manifest/{entity}/commands/{command}` | Standard dispatcher layout used across HTTP projections |
| `legacy` | `POST {basePath}/{entity}/{command-kebab}` | Older flattened path style |
| `both` (default) | Both paths emitted; legacy marked `deprecated: true` | Eases migration — emit both styles during transition |

The default is `both` for compatibility. Use `dispatcher` for new projects to align with the canonical command path across projections.

```ts
const result = projection.generate(ir, {
  surface: 'openapi.spec',
  options: {
    commandPathStyle: 'dispatcher', // canonical only
    basePath: '/api',
  },
});
```

When `commandPathStyle: 'both'`, the legacy path includes a deprecation notice in the operation description pointing to the canonical dispatcher path.

## Notes & limitations

<Note>
  Earlier feature notes list `includeAuth`, `includeConstraintErrors`, and the
  `info`/`servers`/`securitySchemes`/`security` options; the source additionally exposes
  `includeTenant` and `includePolicySecurity`, documented above. The `email`
  type maps to `string` with `format: email`, and `url`/`uri` are also handled even though older
  notes do not mention them.
</Note>


---

## python pydantic

URL: https://manifest-b1e8623f.mintlify.app/projections/python-pydantic

---
title: 'Python Pydantic model projection'
sidebarTitle: 'Pydantic'
description: 'Generate Pydantic v2 BaseModel classes with validators, computed fields, and an async httpx client SDK from Manifest IR — targeting Python 3.10+.'
---

The Pydantic projection generates Python Pydantic v2 model classes from compiled Manifest IR. Use it when you are building a Python backend, data pipeline, or client library and want Manifest entities represented as typed Pydantic models with validation, serialization, and optional async client methods. The projection follows the same compile-time, app-agnostic contract as the [Drizzle projection](/projections/drizzle) but targets Python conventions.

The projection is registered under the name `pydantic` and lives at `src/manifest/projections/pydantic/generator.ts`.

## What it generates

The projection exposes four surfaces.

`pydantic.entity` emits a Pydantic `BaseModel` class for a single entity. Each property becomes a typed field, constraints are mapped to `@field_validator` decorators, computed properties are emitted as `@computed_field` with `@property` decorators, and optional properties use `| None = None` syntax.

`pydantic.command` emits a `BaseModel` class for command parameters with typed fields matching the command's input schema.

`pydantic.models` emits all entity models, command models, and enum classes in a single Python module with a `__all__` export list.

`pydantic.client` emits a complete async client SDK: a `ManifestClient` class with `async with` context manager support, `httpx.AsyncClient` integration with optional bearer token auth, entity query methods (`list_<entity>s`, `get_<entity>`), typed command invocation methods, and module-level convenience functions.

## Usage

```ts
import { getProjection } from '@angriff36/manifest/projections';

const projection = getProjection('pydantic');

// Generate a single entity model
const entity = projection.generate(ir, {
  surface: 'pydantic.entity',
  entity: 'Order',
});

// Generate all models in one file
const models = projection.generate(ir, { surface: 'pydantic.models' });

// Generate the async client SDK
const client = projection.generate(ir, {
  surface: 'pydantic.client',
  options: { clientBaseUrl: 'https://api.example.com' },
});
```

Through the CLI:

```bash
manifest generate ir/widget.ir.json --projection pydantic --surface pydantic.models --output src/models.py
```

## Type mapping

The projection maps Manifest IR types to Python types:

| Manifest type      | Python type    |
| ------------------ | -------------- |
| `string`           | `str`          |
| `int` / `integer`  | `int`          |
| `number` / `float` | `float`        |
| `boolean` / `bool` | `bool`         |
| `datetime`         | `datetime`     |
| `uuid`             | `UUID`         |
| `decimal`          | `Decimal`      |
| `email`            | `EmailStr`     |
| `array<T>`         | `list[T]`      |
| `map<V>`           | `dict[str, V]` |

Optional properties use Python 3.10+ union syntax (`str | None = None`). Enum types generate a `str`-based enum class with string values.

## Constraint validators

Manifest constraints are mapped to Pydantic `@field_validator` decorators:

- Numeric range constraints (`min`, `max`) become validators with `if value < min or value > max: raise ValueError(...)`.
- String length constraints (`minLength`, `maxLength`) become validators with `if len(value) < min or len(value) > max: raise ValueError(...)`.
- Pattern / regex constraints become validators with `if not re.match(pattern, value): raise ValueError(...)`.

Constraint severity levels from the IR are preserved as validator metadata but do not alter the validation behavior — all constraints produce `ValueError` on failure, matching Pydantic's validation model.

## Options

`PydanticProjectionOptions` accepts `customImports` (additional import statements), `emitJsonSchema` (default `false` — adds `model_json_schema()` export), `emitComputedFields` (default `true` — includes `@computed_field` properties), `clientBaseUrl` (default `'http://localhost:3000'`), `clientClassName` (default `'ManifestClient'`), and `emitConvenienceFunctions` (default `true` — generates module-level async functions that create an implicit client).

## Generated code example

For an entity `Order` with properties `id`, `total`, `status`, and a constraint on `total`:

```python
from __future__ import annotations
from pydantic import BaseModel, field_validator
from decimal import Decimal
from typing import Optional

class Order(BaseModel):
    id: str
    total: Decimal
    status: str
    notes: str | None = None

    @field_validator("total")
    @classmethod
    def validate_total(cls, v: Decimal) -> Decimal:
        if v < 0:
            raise ValueError("total must be >= 0")
        return v

    class Config:
        from_attributes = True
```

The generator still emits the legacy nested
`class Config:` / `from_attributes = True` form (`src/manifest/projections/pydantic/generator.ts`).
That is what ships today — not Pydantic v2 `model_config = ConfigDict(...)`. Treat the sample as
projection output, not as a Pydantic-v2 style guide.

The `pydantic.client` surface generates an async client:

```python
import httpx

class ManifestClient:
    def __init__(self, base_url: str = "http://localhost:3000", api_key: str | None = None):
        self._client = httpx.AsyncClient(
            base_url=base_url,
            headers={"Authorization": f"Bearer {api_key}"} if api_key else {},
        )

    async def list_orders(self) -> list[Order]:
        response = await self._client.get("/api/Order/list")
        response.raise_for_status()
        return [Order(**item) for item in response.json()]

    async def get_order(self, id: str) -> Order:
        response = await self._client.get(f"/api/Order/{id}")
        response.raise_for_status()
        return Order(**response.json())

    async def close(self) -> None:
        await self._client.aclose()

    async def __aenter__(self) -> ManifestClient:
        return self

    async def __aexit__(self, *args) -> None:
        await self.close()
```

## Notes & limitations

<Note>
  Generated code targets Python 3.10+ and uses modern union syntax (`str | None`) rather than
  `Optional[str]`. The `httpx` package is required for the `pydantic.client` surface and is not
  included as a dependency of Manifest. Enum classes inherit from `str` for JSON serialization
  compatibility. Computed properties are generated with `@computed_field` and `@property`
  decorators; their values are placeholders that must be computed by the Manifest runtime on the
  server side.
</Note>


---

## react query

URL: https://manifest-b1e8623f.mintlify.app/projections/react-query

---
title: 'React Query hooks projection'
sidebarTitle: 'React Query'
description: 'Generate typed TanStack Query hooks and a QueryClient provider from Manifest IR, with automatic cache invalidation against your generated API surfaces.'
---

The React Query projection generates typed [TanStack Query](https://tanstack.com/query) hooks and a `QueryClient` provider from compiled Manifest IR. Reach for it when you are building a React front end against the [Next.js](/integration/nextjs), [Express](/projections/express), or [Hono](/projections/hono) API surfaces and want deterministic, typed data-fetching hooks with automatic cache invalidation rather than hand-written `useQuery`/`useMutation` calls.

The projection is registered under the name `react-query` and lives at `src/manifest/projections/react-query/generator.ts`.

## What it generates

The projection exposes two surfaces.

`react-query.hooks` emits a single self-contained TypeScript module (path hint `src/hooks/manifest-hooks.ts`). It inlines an `interface` for every entity in the IR, an input interface for each command that has parameters, a `queryKeys` factory object, an internal `apiFetch<T>` helper, one list hook and one detail hook per entity, and one mutation hook per entity-scoped command. List hooks (`use<Entity>List`) wrap `GET {apiBasePath}/{entity}/list`; detail hooks (`use<Entity>Detail`) wrap `GET {apiBasePath}/{entity}/{id}` and are gated on a truthy `id`. Mutation hooks (`use<Entity><Command>`) issue `POST {dispatcherBasePath}/{entity}/commands/{command-kebab}` and, on success, call `queryClient.invalidateQueries` for the parent entity before delegating to any caller-supplied `onSuccess`.

`react-query.provider` emits a `'use client'` component module (path hint `src/providers/manifest-query-provider.tsx`) exporting `ManifestQueryProvider`. It constructs a `QueryClient` with `staleTime` set from options, `refetchOnWindowFocus: false`, and — when `errorBoundaryIntegration` is enabled — `throwOnError: true` on both queries and mutations.

A command with no associated entity is skipped and reported with an `ORPHAN_COMMAND` warning diagnostic. An unrecognized surface returns an `UNKNOWN_SURFACE` error diagnostic.

## Usage

```ts
import { getProjection } from '@angriff36/manifest/projections';

const projection = getProjection('react-query');
const result = projection.generate(ir, {
  surface: 'react-query.hooks',
  options: { apiBasePath: '/api', defaultStaleTime: 60_000 },
});

for (const artifact of result.artifacts) {
  console.log(artifact.pathHint, artifact.code);
}
```

Swap the surface to `react-query.provider` for the `QueryClient` setup module.

## Type mapping & behavior

IR property and parameter types are mapped to TypeScript by `irTypeToTsType`: `string` to `string`, `number`/`int`/`integer`/`decimal`/`bigint`/`float` to `number`, `boolean` to `boolean`, `any` to `unknown`, and `void` to `void`. Array/list properties emit a real element type — `array<string>` becomes `string[]` (falling back to `unknown[]` when the element type is absent), and a union element parenthesizes (`(A | B)[]`) — rather than leaking the bare `array` token. `date`/`datetime` map to `Date` by default, or to `string` when `dateSerialization: 'iso-string'` is set (see Options). Any other IR type name passes through unchanged. A nullable IR type yields `T | null`.

Entity properties are marked optional (`?`) when the property carries the `optional` modifier, has a default value, or is nullable. Command input fields are optional when the parameter is not required, in which case the TypeScript type also gains ` | undefined`. Query keys use the lower-camel-cased entity name (for example `queryKeys.userProfile.all`). The mutation return type is taken from the command's `returns` type, defaulting to `unknown`.

## Options

`ReactQueryProjectionOptions` accepts `apiBasePath` (default `/api`), `dispatcherBasePath` (default `/api/manifest`), `optimisticUpdates` (default `true`), `errorBoundaryIntegration` (default `true`), `typesImportPath` (default `@/types/manifest-generated`), and `defaultStaleTime` in milliseconds (default `30000`). All are optional.

Two further options keep the hooks aligned with the route surface they call:

- `routeCasing` (default `'lowercase'`; also `'kebab-case'`, `'snake_case'`, `'preserve'`) sets the casing of the default entity URL segment in fetch paths when no per-entity `entityRoutes` override is given. Explicit `entityRoutes` overrides still take precedence.
- `dateSerialization` (default `'date'`, or `'iso-string'`) controls whether `date`/`datetime` props in the inlined types are `Date` or `string`. Non-breaking.

<Warning>
  `routeCasing` **must match the [Next.js projection's](/integration/nextjs) `routeCasing`** so the
  hooks call the routes that actually exist. `'lowercase'` flattens `PrepTask` → `preptask`;
  `'kebab-case'` → `prep-task`, etc.
</Warning>

```ts
const result = projection.generate(ir, {
  surface: 'react-query.hooks',
  options: { routeCasing: 'kebab-case', dateSerialization: 'iso-string' },
});
```

## Notes & limitations

<Warning>
  Two declared options are currently inert. `optimisticUpdates` is accepted and normalized but is
  never read during generation — the generated mutation hooks use a cache-invalidation strategy
  (`invalidateQueries`) only, with no optimistic update helpers emitted, despite the module's header
  comment mentioning them. `typesImportPath` is likewise accepted but unused; entity and
  command-input types are inlined directly into the hooks file rather than imported, so the hooks
  module is fully self-contained.
</Warning>

There is

`manifest generate` dispatches **any** registered
projection via `-p` / `--projection` (non-`nextjs` names walk the registry). Example:
`manifest generate ir/recipe.ir.json -p react-query -s react-query.hooks --output .`
Programmatic `getProjection('react-query')` still works. Package pin **3.6.41**.


---

## remix

URL: https://manifest-b1e8623f.mintlify.app/projections/remix

---
title: 'Remix / React Router v7 projection'
sidebarTitle: 'Remix'
description: 'Generate Remix loader and action functions, route modules, and type-safe API surfaces from Manifest IR — supporting Remix v2 and React Router v7 file-based routing conventions.'
---

The Remix projection generates route modules from compiled Manifest IR that follow Remix conventions: `loader` functions for entity reads, `action` functions for command dispatch, and TypeScript type definitions for your data. Use it when you are building a Remix or React Router v7 application and want Manifest entities wired directly into the data layer of your routes. The projection mirrors the [Express projection's](/projections/express) handler structure but targets Remix's `loader`/`action` exports instead of Express middleware.

The projection is registered under the name `remix` and lives at `src/manifest/projections/remix/generator.ts`.

## What it generates

The projection exposes six surfaces.

`remix.list` emits a route module with a `loader` function that queries entities with optional auth checks, tenant filtering, and soft-delete filtering. The generated code uses Prisma-style queries for list reads and returns data via Remix's `json` helper.

`remix.detail` emits a route module with a `loader` function that loads a single entity instance by ID. The route parameter is extracted from Remix's `params` object.

`remix.command` emits a route module with an `action` function that dispatches a command through the Manifest runtime via `runtime.runCommand`. The handler extracts `instanceId` from the request body, parses command parameters, and returns structured responses using `manifestSuccessResponse` and `manifestErrorResponse` helpers.

`remix.types` emits TypeScript interfaces for all entities and command parameter types. Computed properties are emitted as `readonly` fields. Each entity gets one `interface`, and each parameterized command gets a `<Entity><Command>Params` interface.

`remix.client` emits response helper utilities: `manifestSuccessResponse`, `manifestErrorResponse`, and `normalizeCommandResult`. These map Manifest `CommandResult` outcomes to Remix `Response` objects with proper HTTP status codes. `remix.companions` emits a companion facade module with `runCommand(entity, command, { params, instanceId }, context?)` that adapts to `RuntimeEngine.runCommand`.

Command handlers map diagnostic kinds to HTTP status as documented under Options below. Optional
`ErrorBoundary` / `useRouteError` exports are gated by `includeErrorBoundary` (default **`true`**).

## Usage

```ts
import { getProjection } from '@angriff36/manifest/projections';

const projection = getProjection('remix');

// Generate all list routes
const listRoutes = projection.generate(ir, { surface: 'remix.list' });

// Generate a command route for a specific entity
const commandRoute = projection.generate(ir, {
  surface: 'remix.command',
  entity: 'Order',
});

// Generate TypeScript types
const types = projection.generate(ir, { surface: 'remix.types' });
```

Through the CLI:

```bash
manifest generate ir/widget.ir.json --projection remix --surface remix.types --output src/types/manifest.ts
```

## Auth integration

The projection supports four auth providers, configured via the `authProvider` option:

- **`remix-auth`** (default) — Imports from the configured `authImportPath`, uses `authenticator.isAuthenticated()` for session checks.
- **`clerk`** — Imports from `@clerk/remix`, uses `getAuth()` to retrieve the current user session.
- **`custom`** — Imports a `requireUser(request)` function from the configured `authImportPath`.
- **`none`** — No auth checks in generated code; suitable for public APIs or when auth is handled at a higher layer.

Default `authProvider` is **`remix-auth`** (`REMIX_DEFAULTS`). There is **no** `unauthorizedRedirect`
option — unauthorized responses use `unauthorizedStatus` (default `401`) without a configured redirect
path.

## Generated file structure

For an entity named `Order` with a `placeOrder` command, the projection emits:

```
app/routes/
  orders/
    index.tsx          # remix.list — GET /orders
    $id.tsx            # remix.detail — GET /orders/:id
    place-order.tsx    # remix.command — POST /orders/place-order
app/types/
  manifest.ts          # remix.types — entity & command interfaces
app/utils/
  manifest-client.ts   # remix.client — response helpers
```

Route segment names are derived from the lower-cased entity name and kebab-cased command names, following Remix file-based routing conventions.

## Type mapping

The projection maps Manifest IR types to TypeScript: `string` to `string`; `number`/`integer`/`int` to `number`; `boolean`/`bool` to `boolean`; `date`/`datetime`/`uuid`/`email`/`url`/`uri`/`decimal` to `string`; `any` to `unknown`; `object` to `Record<string, unknown>`. Generic `array<T>` becomes `T[]` and `map<V>` becomes `Record<string, V>`. Nullable types append ` | null`.

## Options

`RemixProjectionOptions` (from `REMIX_DEFAULTS` in `src/manifest/projections/remix/generator.ts`)
defaults: `authProvider` **`'remix-auth'`**; `includeTenantFilter` **`true`**;
`includeSoftDeleteFilter` **`true`**; `includeErrorBoundary` **`true`**; `remixVersion` `'v2'`;
`unauthorizedStatus` `401`; `authImportPath: '~/utils/auth.server'`;
`runtimeImportPath: '~/utils/manifest-runtime'`; `routesDir: 'app/routes'`; `emitCompanions: true`.
There is **no** `unauthorizedRedirect`, `publicReads`, `basePath`, or `runtimeFactoryName` option.

Generated Remix actions map diagnostic **kinds**: `policy_denial` → 403, `guard_failure` → **422**,
`constraint_block` → 422, else 400.

## Notes & limitations

<Note>
  The generated modules import auth and runtime factory from the configured paths; they do not
  define those modules. Read routes (`list`/`detail`) bypass the runtime engine and call the
  database directly. All writes go through
  `runtime.runCommand(commandName, body, { entityName, instanceId })`, matching
  `RuntimeEngine` (not an entity-first signature).
</Note>

The projection supports Remix v2 and React Router v7 file conventions via `remixVersion`.
Tenant and soft-delete filtering default **on** (`includeTenantFilter` /
`includeSoftDeleteFilter`); set them to `false` to disable.


---

## sveltekit

URL: https://manifest-b1e8623f.mintlify.app/projections/sveltekit

---
title: 'SvelteKit server routes projection'
sidebarTitle: 'SvelteKit'
description: 'Generate SvelteKit +server.ts route handlers, +page.server.ts load functions, and typed API surfaces from Manifest IR — with Lucia, Auth.js, or custom auth integration.'
---

The SvelteKit projection generates server routes and load functions from compiled Manifest IR that follow SvelteKit conventions. Use it when you are building a SvelteKit application and want Manifest entities wired into your server-side routes, form actions, and page data. The projection mirrors the [Next.js projection's](/integration/nextjs) read/write split but targets SvelteKit's `+server.ts` and `+page.server.ts` file conventions.

The projection is registered under the name `sveltekit` and lives at `src/manifest/projections/sveltekit/generator.ts`.

## What it generates

The projection exposes six surfaces.

`sveltekit.server` emits a `+server.ts` file with `GET` and `POST` `RequestHandler` exports. The `GET` handler lists entity instances (with optional auth checks, tenant filtering, and soft-delete filtering). The `POST` handler dispatches a command through generated call sites that currently emit entity-first `runtime.runCommand(entity, command, { params, instanceId })` — see Notes; that is not the `RuntimeEngine` signature.

`sveltekit.load` emits a `+page.server.ts` file with a `load: PageServerLoad` function and an `actions: Actions` export. The `load` function fetches entity data for page rendering. Form actions decode `event.request.formData()`, extract `instanceId` from form fields, and invoke commands. SvelteKit's `./$types` provides type-safe `PageData`.

`sveltekit.command` emits a per-command `+server.ts` route under `commands/[command]/+server.ts`. This is a legacy alias for the command-specific route pattern.

`sveltekit.types` emits entity and command type interfaces plus `ManifestActionResult` and `ManifestDiagnostic` utility interfaces. Computed properties are emitted as `readonly` fields.

`sveltekit.client` emits a `$lib/manifest-client.ts` module with an `invokeManifestCommand()` helper function for client-side command invocation. `sveltekit.companions` emits a companion facade module with `runCommand(entity, command, { params, instanceId }, context?)` that adapts to `RuntimeEngine.runCommand`.

## Usage

```ts
import { getProjection } from '@angriff36/manifest/projections';

const projection = getProjection('sveltekit');

// Generate server route handlers
const server = projection.generate(ir, { surface: 'sveltekit.server' });

// Generate page load functions and form actions
const load = projection.generate(ir, { surface: 'sveltekit.load' });

// Generate type definitions
const types = projection.generate(ir, { surface: 'sveltekit.types' });

// Generate the client helper
const client = projection.generate(ir, { surface: 'sveltekit.client' });
```

Through the CLI:

```bash
manifest generate ir/widget.ir.json --projection sveltekit --surface sveltekit.types --output src/lib/manifest-types.ts
```

## Auth integration

The projection supports four auth providers, configured via the `authProvider` option:

- **`lucia`** (default) — Reads `event.locals.session` for the current user session, following the Lucia auth convention.
- **`auth-js`** — Uses `getServerSession(event)` from NextAuth/SvelteKit integration.
- **`custom`** — Imports a `requireUser(event)` function from the configured `authImportPath`.
- **`none`** — No auth checks in generated code.

Tenant filtering is activated via the **`includeTenantFilter`** option (default **`true`**). When
enabled, read queries include a `tenantId` predicate. Soft-delete filtering adds `deletedAt: null`
to read queries when `includeSoftDeleteFilter` is enabled (also default **`true`** per
`SVELTEKIT_DEFAULTS`).

## Generated file structure

For an entity named `Task` with an `updateStatus` command:

```
src/routes/
  tasks/
    +server.ts         # sveltekit.server — GET /tasks, POST /tasks
    +page.server.ts    # sveltekit.load — load + actions
    commands/
      update-status/
        +server.ts     # sveltekit.command — POST /tasks/update-status
src/lib/
  manifest-client.ts   # sveltekit.client — invokeManifestCommand()
src/lib/
  manifest-types.ts    # sveltekit.types — interfaces
```

## Type mapping

The projection maps Manifest IR types to TypeScript using `irTypeToTs`: `string` to `string`; `number`/`integer`/`int` to `number`; `boolean`/`bool` to `boolean`; `date`/`datetime`/`uuid`/`email`/`url`/`uri`/`decimal` to `string`; `any` to `unknown`; `object` to `Record<string, unknown>`. Generic `array<T>` becomes `T[]` and `map<V>` becomes `Record<string, V>`. Nullable types append ` | null`.

## Options

`SvelteKitProjectionOptions` defaults from `SVELTEKIT_DEFAULTS`: `authProvider` `'lucia'`;
`authImportPath` `$lib/server/auth`; `runtimeImportPath` `$lib/server/manifest-runtime`;
`runtimeFactoryName` `createManifestRuntime`; `databaseImportPath` `$lib/server/database`;
`routesDir` `src/routes`; `includeTenantFilter` **`true`**; `includeSoftDeleteFilter` **`true`**;
`emitFormActions` / `emitTypeImports` / `emitCompanions` **`true`**. There is **no** `basePath`,
`publicReads`, or `dispatcherImportPath` option. `validationImportPath` exists and is optional.

## Notes & limitations

Generated SvelteKit handlers call a **phantom** entity-first form
`runtime.runCommand(entityName, commandName, { params, instanceId })`. The default companion from
`generateRuntimeFactoryModule` returns a raw `RuntimeEngine`, whose API is
`runCommand(commandName, input, { entityName, instanceId })` — **not** entity-first. Unlike
Express/Hono, SvelteKit does **not** emit an adapting facade. Treat generated write call sites as
scaffolding that must be aligned with the engine (or a hand-written facade) before use. Reads may
still bypass the runtime via `database.<entity>.findMany()` when `databaseImportPath` is set.

Form actions decode `event.request.formData()` and extract `instanceId` from form fields. The `+page.server.ts` returns typed `data` through SvelteKit's generated `./$types`, providing full type safety from server to client. The output is deterministic: the same IR and options always produce the same generated code.


---

## wiring

URL: https://manifest-b1e8623f.mintlify.app/projections/wiring

---
title: 'Product wiring projection'
sidebarTitle: 'Product wiring'
description: 'Machine-readable command wiring contracts and safe bindings so apps wire UI to Manifest without guessing — not a UI generator.'
---

The **wiring** projection emits a deterministic product-wiring contract and TypeScript binding helpers from compiled IR. Use it so applications and agents connect real UI to Manifest commands without inventing types, lifecycle states, or client-side identity fields.

Registry name `wiring`; CLI
`manifest generate -p wiring --surface all`, plus `manifest wiring-inspect` /
`manifest wiring-remediate`. Package pin **3.6.41**.

This is **not** a UI generator. It does not create pages, forms, buttons, layouts, or copy. Your app still owns presentation.

## Generate

```bash
manifest generate -p wiring --surface all
```

Default artifacts:

- `src/generated/manifest-wiring-contract.json`
- `src/generated/manifest-wiring-bindings.ts`

## What the contract answers

For every entity command:

- Exact parameter types (including `string[]` vs `string`)
- Required vs optional
- Enum / finite values when statically known
- Numeric and non-empty constraints when statically known
- Which fields are **server-owned** (`from context.*`) vs client-owned
- Dispatcher route identity
- Proven lifecycle transitions (from `transition` + literal `mutate`)
- List/detail invalidation hints after success
- Structured error states to handle

Unprovable metadata is omitted — never guessed.

## Trusted server-owned inputs

```manifest
command markCompleted(completedBy: string from context.actorId) {
  mutate completedBy = completedBy
}
```

- Client input types omit `completedBy`
- Runtime strips spoofed client values and injects `context.actorId`
- Missing trusted context fails with `MISSING_TRUSTED_CONTEXT`

Ownership is declared explicitly. Naming heuristics are not used.

## Automatic consumer inspection

Manifest does **not** design the interface. It proves whether application code correctly consumes declared capabilities.

```bash
manifest wiring-inspect \
  --contract src/generated/manifest-wiring-contract.json \
  --root apps/app \
  --root apps/api \
  --strict
```

Primary coverage comes from source inspection (generated client calls, `executeCommand`, server actions → `runManifestCommand`, API route chains, imported helpers). Explicit registries are overrides for `backend-only` / `deferred` / accepted ambiguous cases.

Default CI fails on stale consumers and proven contract mismatches. Unwired capabilities fail only with `--strict-coverage`. Ambiguous findings never fail by default.

Repo-wide scans are supported: Capsule-Pro-scale roots complete in a few seconds with indexed import resolution and parallel loading. Include filters are optional for debugging, not required for the gate.

See the full reference in the repo: `docs/projections/wiring.md`.

## Automatic remediation

Manifest does **not** design the UI. When the correct integration is derivable from Manifest truth and an existing product surface, Manifest plans and applies a deterministic repair:

```bash
manifest wiring-remediate --contract src/generated/manifest-wiring-contract.json \
  --root apps/app --root apps/api --mode one-defect
```

Modes: `plan`, `dry-run`, `apply`, `one-defect`. Auto-fixable findings are patched via AST-aware edits and re-inspected. Ambiguous product placement is never auto-edited. `add-required-input` applies only when a unique proven in-scope value source exists (never invented defaults). Failed verification discards the patch.

## Boundary

| Manifest                                     | Application                                    |
| -------------------------------------------- | ---------------------------------------------- |
| Command truth, types, ownership, constraints | Layout, visuals, labels                        |
| Safe bindings + invalidation metadata        | Button vs menu vs gesture                      |
| Coverage truth + deterministic wiring repair | Explicit consumer registry / product placement |

Full reference: [docs/projections/wiring.md](https://github.com/Angriff36/Manifest/blob/main/docs/projections/wiring.md) in the repository.


---

## zod

URL: https://manifest-b1e8623f.mintlify.app/projections/zod

---
title: 'Zod validation schema projection'
sidebarTitle: 'Zod'
description: 'Generate Zod validation schemas from Manifest IR — one z.object() per entity and command, with constraint-derived refinements and type coercions.'
---

The Zod projection generates Zod validation schemas from compiled Manifest IR — one `z.object()` per entity and per command parameter set, with constraint-derived refinements and type coercions. Use it for runtime validation in any TypeScript environment without coupling to a specific framework.

## What it generates

The projection registers under the name `zod` and declares three surfaces, `['zod.entity', 'zod.command', 'zod.schemas']`.

`zod.entity` emits a schema file per entity (`id: zod.entity.<Entity>`, content type `typescript`, written at `schemas/<Entity>.schema.ts`). `zod.command` emits a schema file per command (`id: zod.command.<command>`, written at `schemas/<command>.schema.ts`). `zod.schemas` emits a single combined module containing all schemas (`id: 'zod.schemas'`, written at `schemas/manifest-schemas.ts`).

Each entity schema is a `z.object()` with constraint refinements applied. When enabled, the projection also emits an `<Entity>ComputedSchema` that extends the base schema with the entity's computed properties, and `export type X = z.infer<typeof XSchema>` type aliases.

## Usage

```ts
import { compileToIR } from '@angriff36/manifest/ir-compiler';
import { getProjection } from '@angriff36/manifest/projections';

const { ir } = await compileToIR(source);

const projection = getProjection('zod');
const result = projection?.generate(ir, {
  surface: 'zod.schemas',
  options: { emitTypes: true, zodImportPath: 'zod' },
});

const code = result?.artifacts[0]?.code;
```

`ZodProjection` can also be imported directly from `@angriff36/manifest/projections/zod`, or dispatched through the CLI by name:

```bash
manifest generate ir/recipe.ir.json --projection zod --surface zod.schemas --output src/schemas/manifest-schemas.ts
```

## Type mapping & behavior

Same base map, plus: `timestamp` aliases `datetime`
(`z.coerce.date()`); `time` → regex-validated `z.string()`; `duration` → `z.number()`;
`list<T>` aliases `array<T>`; IR enum type names resolve to `z.enum([...])` from `ir.enums`
(member **names** as string literals). Value objects inline as `z.object({...})`. Truly unknown
types still emit `ZOD_UNKNOWN_TYPE` → `z.unknown()` (`src/manifest/projections/zod/generator.ts`).

Note that, unlike [Prisma](/integration/prisma) and [Drizzle](/projections/drizzle), the Zod projection maps bare `number` to `z.number()` without an ambiguity diagnostic — Zod has no distinct integer-versus-decimal concern at this layer. Constraints on an entity are analyzed (via `src/manifest/constraint-analysis.ts`) and emitted as `.min()` / `.max()` refinement chains.

## Options

The options object is `ZodProjectionOptions` in `zod/types.ts`. All four options default to enabled or a sensible string: `emitTypes` (default true) emits `z.infer` type aliases, `emitComputedSchemas` (default true) emits the `<Entity>ComputedSchema` variants for entities that have computed properties, `zodImportPath` (default `zod`) sets the import source, and `emitHeader` (default true) prepends a generation-metadata comment.

## Notes & limitations

<Note>
  Computed-property schemas are emitted only when both `emitComputedSchemas` is enabled and the
  entity actually declares computed properties.
</Note>

The behavior documented here comes directly from `src/manifest/projections/zod/generator.ts` and `types.ts`.


---

## Interactive project setup (creates manifest.config.yaml)

URL: https://manifest-b1e8623f.mintlify.app/quickstart

---
title: 'Quickstart: install Manifest and run your first command'
sidebarTitle: 'Quickstart'
description: 'Install @angriff36/manifest, write your first .manifest file, compile it to IR, and execute a command with RuntimeEngine in under five minutes.'
---

This guide walks you from a blank project to a running Manifest program. You will install the package, write a `.manifest` source file, compile it to IR, and call `RuntimeEngine.runCommand()` to see the result. All code examples use real APIs from the package — nothing is invented.

<Steps>

<Step title="Install the package">

`package.json` `engines.node` is `>=20`. Use Node.js 20 or later.

<CodeGroup>

```bash npm
npm install @angriff36/manifest
```

```bash pnpm
pnpm add @angriff36/manifest
```

</CodeGroup>

</Step>

<Step title="Write your first .manifest file">

Create `my-program.manifest` in your project root. This entity models a simple to-do item with two commands and an authorization policy.

```manifest
entity Todo {
  property id: string
  property title: string = ""
  property completed: boolean = false
  property createdAt: number = 0

  command create(newTitle: string) {
    guard newTitle != null and newTitle != ""
    guard length(newTitle) < 100

    mutate title = newTitle
    mutate completed = false
    mutate createdAt = now()
  }

  command complete() {
    guard self.completed == false

    mutate completed = true
    emit TodoCompleted { todoId: self.id }
  }

  policy AdminRead read: user.role == "admin"
  policy Authenticated execute: user.id != null "Authentication required"
}
```

Each `command` defines a named business operation. The `guard` lines gate execution — if any guard evaluates to `false`, the command stops immediately and reports which guard failed. `mutate` assigns a bare property name; expressions read the current instance through `self.*`. The `policy` lines are entity-level authorization rules — each has a name, a scope (`read`, `write`, `execute`, `all`, or `override`), an expression, and an optional denial message — and they are enforced before guards even run.

</Step>

<Step title="Compile the source to IR">

Use the `compileToIR` export to parse the source and produce an IR object. Always check diagnostics before proceeding — compilation errors are surfaced here, not thrown as exceptions.

```typescript
import { compileToIR } from '@angriff36/manifest/ir-compiler';
import { readFileSync } from 'fs';

const source = readFileSync('my-program.manifest', 'utf-8');

const { ir, diagnostics } = await compileToIR(source);

if (diagnostics.some((d) => d.severity === 'error')) {
  console.error('Compilation errors:');
  diagnostics.forEach((d) => console.error(` ${d.message}`));
  process.exit(1);
}

console.log('Compiled IR:', JSON.stringify(ir, null, 2));
```

You can also compile inline source directly — useful for tests and prototyping:

```typescript
import { compileToIR } from '@angriff36/manifest/ir-compiler';

const source = `
  entity Todo {
    property id: string
    property title: string
  }
`;

const { ir, diagnostics } = await compileToIR(source);
```

<Note>
  The IR is plain JSON. You can serialize it to disk, cache it, or pass it to any downstream
  consumer — including the `RuntimeEngine`, projections, and conformance tooling.
</Note>

</Step>

<Step title="Execute a command with RuntimeEngine">

Import `RuntimeEngine` from the package root and construct it with your compiled IR and a runtime context. `user.*` bindings in guards and policies resolve from `context.user` (an object with at least an `id`; `role` and other keys are up to you), and `context.tenantId` scopes tenant-aware commands.

```typescript
import { RuntimeEngine } from '@angriff36/manifest';

const runtime = new RuntimeEngine(ir, {
  user: { id: 'user-123', role: 'admin' },
  tenantId: 'tenant-456',
});

// Run the "create" command on the Todo entity.
// Signature: runCommand(commandName, input, { entityName })
const createResult = await runtime.runCommand(
  'create',
  { newTitle: 'Learn Manifest' },
  { entityName: 'Todo' },
);

if (!createResult.success) {
  console.error('Failed:', createResult.guardFailure ?? createResult.error);
  process.exit(1);
}

console.log('Created:', createResult.instance);
```

When the command succeeds, `createResult.instance` contains the resulting entity state. When it fails — because a guard evaluated to `false` or a policy denied access — inspect `createResult.guardFailure` (and `policyDenial`, `constraintOutcomes`, etc.) to see exactly which check failed and what values were resolved. `CommandResult` has no `diagnostics` field.

</Step>

<Step title="Chain a second command and inspect events">

Use the instance returned by `create` to run `complete`. Check `completeResult.emittedEvents` to see the `TodoCompleted` event that the command emitted.

```typescript
const completeResult = await runtime.runCommand(
  'complete',
  {},
  { entityName: 'Todo', instanceId: createResult.instance.id },
);

if (completeResult.success) {
  console.log('Events:', completeResult.emittedEvents);
  // Output: Events: [{ name: 'TodoCompleted', channel: 'TodoCompleted',
  // payload: { todoId: '...', ... }, subject: { entity: 'Todo', ... }, ... }]
}
```

You can also subscribe to events as they are emitted. `runtime.onEvent(listener)` receives every event (filter by `event.name` yourself); `runtime.subscribe(entityName, listener)` receives only events whose subject is that entity. Both return an unsubscribe function.

```typescript
const unsubscribe = runtime.onEvent((event) => {
  if (event.name !== 'TodoCompleted') return;
  console.log('Todo completed:', event.payload.todoId);

  // Trigger side effects
  sendEmail(event.payload.todoId);
  updateAnalytics(event.payload.todoId);
});
```

</Step>

</Steps>

## Running the CLI instead

If you prefer a command-line workflow, use the `manifest` CLI that ships with the package:

```bash
# Interactive project setup (creates manifest.config.yaml)
manifest init

# Config-driven whole project (preferred after init)
manifest compile --all
manifest generate --all
# or: manifest build --all / manifest watch --all

# Single-file compile / one-shot build still work
manifest compile my-program.manifest --output ir/
manifest build my-program.manifest --projection nextjs --code-output app/api/
```

<Tip>
  Run `manifest init` once to create a `manifest.config.yaml`. After that, prefer
  `manifest build --all` (or `compile --all` + `generate --all`) so every configured projection
  stays in sync — verified CLI flags @ v3.6.41.
</Tip>

## Troubleshooting

`runCommand`'s first argument is the **command** name. Pass the
entity as `options.entityName` (case-sensitive match to the `entity` declaration).

**"Guard failed but no detail"** — Check that you aren't silently catching errors before inspecting `result.guardFailure`. The runtime populates it for every guard failure, including the guard index, the formatted expression, and resolved sub-expression values.

**Compilation errors after a small change** — Conformance tests treat compilation output as executable semantics. If something stops compiling, you've changed language meaning. Review your source against the language specification.

## Next steps

- **[Installation](/installation)** — full install options, all package exports, TypeScript configuration, and CLI setup
- **[Language reference](/language/entities)** — complete specification for entities, commands, guards, policies, events, and stores
- **[Integration patterns](/integration/overview)** — how to use projections and the embedded runtime together in a real application


---

## troubleshooting

URL: https://manifest-b1e8623f.mintlify.app/troubleshooting

---
title: 'Troubleshoot common Manifest issues'
description: "Diagnose and fix the most common errors you'll encounter when compiling, running, or generating code with the Manifest SDK and CLI."
sidebarTitle: 'Troubleshooting'
---

The issues below cover the most common problems reported when working with Manifest — from CLI configuration mismatches to runtime guard inspection and Windows-specific path bugs. Each entry describes what you see, why it happens, and how to fix it.

<AccordionGroup>

  <Accordion title='"Entity not found in IR"'>
    **What you see**

    An error like `Entity 'todo' not found` or `Entity X not found` when compiling or running a command.

    **Why it happens**

    Entity names in Manifest are case-sensitive. If your source defines `entity Todo` but your code references `'todo'` or `'TODO'`, the lookup fails.

    **How to fix it**

    Check the exact casing of the entity name in your `.manifest` source file. Then confirm the entity is present in the compiled IR:

    ```typescript
    // Print all entity names from the compiled IR
    console.log(result.ir.entities.map(e => e.name));
    ```

    Make sure the name you pass to `runCommand` matches exactly.

  </Accordion>

  <Accordion title='"Guard failed but no diagnostic"'>
    **What you see**

    A command returns `success: false` but you cannot find any diagnostic information. Accessing `result.diagnostics` returns `undefined`.

    **Why it happens**

    `CommandResult` does not have a `diagnostics` field. Guard failure details are returned in `result.guardFailure`. Policy denial details are in `result.policyDenial`. General errors are in `result.error`. Inspecting the wrong field produces nothing.

    **How to fix it**

    Check the correct fields on `CommandResult`:

    ```typescript
    const result = await runtime.runCommand('create', input, { entityName: 'Todo' });

    if (!result.success) {
      if (result.guardFailure) {
        console.error('Guard failed:', {
          index: result.guardFailure.index,
          expression: result.guardFailure.formatted,
          resolved: result.guardFailure.resolved
        });
      } else if (result.policyDenial) {
        console.error('Policy denied:', {
          policy: result.policyDenial.policyName,
          message: result.policyDenial.message
        });
      } else {
        console.error('Error:', result.error);
      }
      return;
    }

    console.log('Success:', result.result);
    ```

    <Note>
      The runtime does not emit events like `runtime.on('guardFailed')`. All diagnostic information is returned in the `CommandResult` object returned by `runCommand`.
    </Note>

  </Accordion>

  <Accordion title='"No .manifest files found"'>
    **What you see**

    Running `manifest compile` or `manifest build` prints `No .manifest files found` and exits without producing any output.

    **Why it happens**

    The CLI looks for `.manifest` files using either the `src` glob in `manifest.config.yaml` or a default discovery pattern. If neither is configured and no path is passed explicitly, no files are found.

    **How to fix it**

    Pass the source path directly:

    ```bash
    manifest compile path/to/your/file.manifest
    ```

    Or set the `src` glob in your config file:

    ```yaml
    # manifest.config.yaml
    src: "modules/**/*.manifest"
    output: "ir/"
    ```

    Then run without arguments:

    ```bash
    manifest compile
    ```

  </Accordion>

  <Accordion title='"Cannot find module" errors during manifest compile'>
    **What you see**

    The compile command starts and then fails with:

    ```text
    Cannot find module .../dist/manifest/parser imported from .../dist/manifest/ir-compiler.js
    ```

    **Why it happens**

    You are on a build where ESM relative imports were emitted without `.js` file extensions. Node's ESM resolver requires explicit extensions and cannot fall back to bare specifiers.

    **How to fix it**

    Upgrade to a version where the runtime ESM imports use explicit `.js` extensions (`./parser.js`, `./lexer.js`, `./ir-cache.js`, and so on). Then verify the fix:

    ```bash
    pnpm exec manifest compile
    ```

    Expected result: compile runs across discovered manifests without module-resolution errors.

    If you are working inside the Manifest repo itself, make sure you have built the library before running the CLI:

    ```bash
    npm run build
    pnpm exec manifest compile
    ```

  </Accordion>

  <Accordion title="Windows: manifest exits 0 with no output">
    **What you see**

    On Windows, any `manifest` CLI command exits with code `0` and prints nothing — no help text, no error, no output.

    **Why it happens**

    The CLI uses direct-run detection to avoid double-execution when invoked through pnpm shims. Before version 0.3.10, the detection compared normalized (but not resolved) paths. On Windows, the pnpm shim path and the `.pnpm` real target path differ, so the comparison always failed — the CLI detected itself as a passthrough and exited silently.

    This is fixed in **0.3.10**. The CLI now compares normalized **realpaths** and performs a case-insensitive comparison on Windows.

    **How to fix it**

    Upgrade to the latest release (current package pin for this audit: **3.6.41**; the Windows shim fix has shipped since `0.3.10`):

    ```bash
    pnpm add @angriff36/manifest@latest
    ```

    > **Note (2026-07-15):** `@angriff36/manifest` requires **Node.js >= 20** (`package.json` engines). Historical `0.3.x` version numbers below describe when CLI bugs were fixed; they are not current release pins.

    If you cannot upgrade immediately, use one of these workarounds to verify the CLI is reachable:

    ```bash
    pnpm exec manifest --help
    node .\node_modules\@angriff36\manifest\packages\cli\dist\index.js --help
    .\node_modules\.bin\manifest.cmd --help
    ```

    Each of these should print the Manifest help text. Use whichever form works in your environment until you can upgrade.

    <Warning>
      `manifest init --force` is interactive and requires terminal input. In non-interactive or headless shells it may wait for prompts or exit without rewriting the config file. Run it in an interactive terminal.
    </Warning>

  </Accordion>

  <Accordion title='TypeError: Cannot read properties of undefined (reading "output") in manifest compile'>
    **What you see**

    Running `pnpm exec manifest compile` fails around line 48 of `packages/cli/dist/index.js` with:

    ```text
    TypeError: Cannot read properties of undefined (reading 'output')
    ```

    **Why it happens**

    You are on a build where Commander argument declarations were duplicated — the same argument was registered in both `.command(...)` and `.argument(...)`. The duplicate caused the options object to be `undefined` when the action handler ran.

    **How to fix it**

    Upgrade to a version where command signatures no longer duplicate arguments and action handlers use safe option defaults. Verify the fix by checking the command shapes:

    ```bash
    pnpm exec manifest --help
    ```

    Expected command shapes after the fix:

    ```text
    compile [options] [source]
    generate [options] <ir>
    build [options] [source]
    validate [options] [ir]
    ```

    If `compile` still shows the argument listed twice, you are on an older build. Upgrade the package and rebuild.

  </Accordion>

  <Accordion title="Constraint is not blocking execution">
    **What you see**

    A constraint you defined is violated, but the command succeeds and the entity is persisted anyway.

    **Why it happens**

    The constraint's severity level controls whether a violation halts execution. Only `block` severity stops the command. If the severity is `warn` or `ok`, a violation is recorded but execution continues.

    **How to fix it**

    Check the severity declaration in your `.manifest` source. If you want the constraint to halt execution, make sure it uses the default severity (`block`) or explicitly sets it:

    ```manifest
    constraint titleNotEmpty {
      severity: block
      expression: this.title is not empty
    }
    ```

    After a successful command, check `result.constraintOutcomes` filtered by `severity: 'warn'` to see any warning-level violations that were recorded but did not block:

    ```typescript
    const warnings = result.constraintOutcomes?.filter(o => !o.passed && o.severity === 'warn');
    if (warnings?.length) {
      console.warn('Constraint warnings:', warnings);
    }
    ```

  </Accordion>

  <Accordion title="Read policies are not being enforced">
    **What you see**

    You defined a `policy ReadOnly read: ...` rule on an entity, but the policy is never enforced — reads succeed regardless of the policy condition.

    **Why it happens**

    `read`-scoped policies ARE enforced by the runtime — but only at the engine's read gate (`getInstance` / `getAllInstances`). If your reads bypass the engine — for example generated Next.js GET routes that query the database directly — they are not gated. The policy is enforced only when a read flows through `runtime.getInstance` / `runtime.getAllInstances`.

    **How to fix it**

    Route reads through the engine's `getInstance` / `getAllInstances` to have `read` (or `all`) policies enforced (a denied read returns `undefined` / omits the row). If you instead want a policy enforced during command execution, use the `execute` or `all` scope. Policies are entity-level declarations with the form `policy Name scope: expression`:

    ```manifest
    entity Resource {
      property required title: string
      property createdBy: string = ""

      policy AdminOnly execute: user.role == "admin"
      policy OwnerAll all: user.id == self.createdBy

      command update(newTitle: string) {
        mutate title = newTitle
      }
    }

    store Resource in memory
    ```

    `execute` is enforced during `runCommand`. `all` is enforced during command execution AND at the read gate. `read` is enforced at the read gate only.

  </Accordion>

  <Accordion title="Generated Next.js routes don't work">
    **What you see**

    Running the generated route handler returns a 500 error, a module-not-found error, or silently fails with no output.

    **Why it happens**

    Generated routes import helper modules from paths configured during `manifest init` (or passed via CLI flags). If those files don't exist at the expected paths — for example `@/lib/manifest-runtime`, `@/lib/database`, or `@/lib/manifest-response` — the route fails at import time.

    **How to fix it**

    Verify all three lib files exist in your project:

    - `lib/manifest-runtime.ts` — wraps `RuntimeEngine`
    - `lib/database.ts` — exports your database client
    - `lib/manifest-response.ts` — exports `manifestSuccessResponse` and `manifestErrorResponse`

    If `manifest-response.ts` is missing, create it:

    ```typescript
    // lib/manifest-response.ts
    export function manifestSuccessResponse(data: unknown, status = 200) {
      return Response.json(data, { status });
    }

    export function manifestErrorResponse(
      message: string | object,
      status = 400
    ) {
      return Response.json(
        typeof message === 'string' ? { error: message } : message,
        { status }
      );
    }
    ```

    For workspace monorepos, confirm the import paths in `manifest.config.yaml` match your actual package names (for example `@repo/manifest/runtime` instead of `@/lib/manifest-runtime`).

    You can also verify the entity exists in the compiled IR before regenerating:

    ```bash
    pnpm exec manifest compile modules/recipe.manifest
    cat ir/recipe.ir.json | grep -o '"name":"[^"]*"'
    ```

  </Accordion>

  <Accordion title='"Unsupported storage target" error'>
    **What you see**

    The runtime throws or returns an error like `Store not supported` or `Unsupported storage target`.

    **Why it happens**

    The runtime only supports built-in targets that are explicitly configured. If you reference a target that has not been set up — or pass an unrecognized string — the runtime emits a diagnostic instead of silently falling back to memory.

    **How to fix it**

    `memory` and browser `localStorage` are built in. `postgres`, `supabase`,
    `durable`, and custom targets require a provider; a database URL alone is
    not a provider.

    For custom databases, implement the `Store` interface and pass it via `storeProvider`:

    ```typescript
    import { RuntimeEngine } from '@angriff36/manifest';

    const runtime = new RuntimeEngine(
      ir,
      { actorId: 'user-123' },
      {
        storeProvider: (entityName) => {
          if (entityName === 'Todo') return new MyCustomTodoStore();
          return undefined;
        },
      },
    );
    ```

    Returning `undefined` uses built-in behavior only for `memory` and browser
    `localStorage`. Generated companions load providers from
    `manifest.config.ts` through `runtimeConfigImport`. See the
    [complete setup](/language/stores#complete-generated-companion-postgresql-setup).

  </Accordion>

  <Accordion title='manifest validate reports "Missing required field: metadata" or "Schema not found"'>
    **What you see**

    Running `manifest validate` fails with either:

    ```text
    Missing required field: metadata
    ```

    or:

    ```text
    Schema not found at docs/spec/ir/ir-v1.schema.json
    ```

    **Why it happens**

    You are running a stale global CLI binary (pre-0.3.23). The old `validate` command resolved the schema relative to `process.cwd()`, which only works inside the Manifest repo itself — consumer projects don't have that path. The old validator also ran hardcoded validation against a `metadata` field that does not exist in the IR spec. The IR itself is valid; the CLI was wrong.

    **How to fix it**

    Stop using the global binary and use `pnpm exec manifest` instead, which resolves from the installed package:

    ```bash
    pnpm exec manifest validate path/to/output.ir.json
    ```

    If you are on an old version, upgrade to the latest release:

    ```bash
    pnpm add @angriff36/manifest@latest
    pnpm exec manifest validate path/to/output.ir.json
    ```

    Since `0.3.23`, the schema is bundled inside the package and resolved relative to the CLI binary, not the working directory. Do not copy schema files, modify provenance, or create symlinks as workarounds.

    <Tip>
      Always use `pnpm exec manifest` (or `npx manifest`) rather than a globally installed binary. Run `pnpm exec manifest --version` to confirm the version matches the installed package.
    </Tip>

  </Accordion>

</AccordionGroup>

<Note>
  If your issue is not listed here, check the [FAQ](/faq) for answers about Manifest's design and
  semantics. To report a new issue, open a GitHub issue and include your `.manifest` source, the
  compiled IR, and any diagnostic output.
</Note>


---

## Just the companions

URL: https://manifest-b1e8623f.mintlify.app/whats-new

---
title: "What's new in 3.0"
sidebarTitle: "What's new (3.0)"
description: 'Manifest 3.0 makes generated backends runnable with zero hand-written glue: emitted companions, shipped outbox/job/schedule workers, an atomic transaction boundary, coherent routes and clients, webhooks, and a runtime event bus. Includes breaking changes.'
---

> **Note (2026-07-15, updated 2026-07-22):** This page is a **3.0 release note**, not the
> current package pin. Repo / npm `@angriff36/manifest` is **v3.6.41** at verification
> time. Prefer `manifest compile --all` / `generate --all` / `build --all` /
> `watch --all` for config-driven projects (those flags remain valid).
>
> **3.6.36+ language:** cross-entity fan-out foreach-create —
> `on Event fanOut Source where … run Target.create` — is documented under
> [Reactions → Foreach-create](/language/reactions#foreach-create-cross-entity-fan-out).
>
> **3.6.38–3.6.41 language:** natural-key upsert (`match … else create`) —
> single-target lookup-or-allocate and fan-out per-row lookup-or-allocate reactions —
> is documented under [Reactions → Natural-key upsert](/language/reactions#natural-key-upsert-match-else-create).
> Also includes Convex identity-filter fix for `db.get`, else-create allocate, and soft-delete exclusion.

Manifest 3.0 closes the gap between "code was generated" and "the backend actually
runs." Where earlier releases emitted types and route bodies but left you to
hand-write the runtime factory, response helpers, drain loops, and worker wiring,
3.0 ships those pieces — and makes the generated routes, clients, and schemas agree
on one contract.

<Note>
  This release contains **breaking changes** to generated output (zod schema names, the routes
  projection's default paths, hono/express `basePath`, and the react-query command envelope). See
  [Breaking changes](#breaking-changes) before upgrading.
</Note>

## Docs are now gated against the compiler

CI does run `pnpm run docs:check` (metadata + links +
spec integrity + snippets) via `.github/workflows/ci.yml`. Scope and limits:

- **`check-doc-snippets.mjs`** compiles ` ```manifest ` blocks with the real
  compiler (`compileToIR` via jiti) for `mintlify/**` plus selected `docs/`
  trees — **not** every snippet, and **not** all docs. ` ```manifest fragment `
  blocks are skipped; ` ```manifest invalid ` blocks must fail to compile.
  `docs/internal` and `docs/spec` are out of snippet scope. TypeScript /
  runtime API examples are **not** typechecked.
- **`check-doc-links.mjs`** resolves internal markdown link *paths* only
  (anchors and external URLs are ignored).
- **`check-doc-metadata.mjs`** requires Mintlify `title` frontmatter and the
  codedocs AUTO-GENERATED banner — not full frontmatter on every page, and not
  YAML frontmatter on `docs/spec/`.
- **`check-spec-integrity.mjs`** enforces plaintext headers (`Last updated`,
  `Status`, `Authority`) and relative links under `docs/spec/*.md`.

So the gate keeps standalone Manifest source examples honest for compile
errors; it does **not** guarantee that everything you read matches the
compiler or runtime API.

## Generated companions: zero hand-written files

The five server projections — **Next.js, Hono, Express, Remix, and SvelteKit** —
now emit the modules their generated route code imports, so a fresh `generate`
produces code that compiles with no hand-written files. Emission is on by default
(`emitCompanions: true`).

For the Next.js projection the companions surface writes:

- **Runtime factory** — `createManifestRuntime` (default `@/lib/manifest-runtime`), which embeds the compiled IR and constructs a `RuntimeEngine` with your `RuntimeOptions`.
- **HTTP envelope helpers** — `manifestSuccessResponse`, `manifestErrorResponse`, and `normalizeCommandResult` (default `@/lib/manifest-response`).
- **Database client**, **auth stub**, and **tenant lookup helper** companions when the generated routes need them.

Generate them explicitly, or as part of a normal build:

```bash
# Just the companions
manifest generate ir/app.ir.json --projection nextjs --surface companions --output app/api/

# Or the whole surface set (companions included)
manifest generate ir/app.ir.json --projection nextjs --surface all --output app/api/
```

To keep your own implementations instead, set `emitCompanions: false` — the
projection then emits nothing and leaves your hand-written modules untouched.

## Drains, jobs, cron, and idempotency ship as workers

You no longer hand-write the loops that drive durable delivery and async work.

- **Outbox drain** — `runOutboxWorker(store, deliver)` (a polling worker) and `drainOutboxOnce(store, deliver)` (one pass) from `@angriff36/manifest/outbox/worker` own the claim/deliver/mark loop; pass your publish function as `deliver`. A Redis-backed outbox store is available at `@angriff36/manifest/outbox/redis`.
- **Async job queue** — `PostgresJobQueue` (`@angriff36/manifest/jobs/postgres`) is a durable `JobQueue`; `runJobWorker` / `drainJobsOnce` (`@angriff36/manifest/jobs/worker`) drain and execute enqueued async commands. Apply `src/manifest/jobs/stores/postgres.sql` once.
- **Schedules** — a real 5-field cron matcher backs `startScheduleWorker` and `runSchedulesOnce` (`@angriff36/manifest/schedule-worker`). On Vercel, the Next.js projection emits one cron route per **cron-kind** schedule plus maintenance routes for approval expiry and async-job draining, and a `vercel.json` that registers them — cron-kind schedules fire with no extra setup. `interval` and `every` schedule kinds are not emitted as Vercel cron routes; use `startScheduleWorker` from `@angriff36/manifest/schedule-worker` for those.
- **Idempotency** — `MemoryIdempotencyStore` (`@angriff36/manifest/idempotency/memory`) and `PostgresIdempotencyStore` (`@angriff36/manifest/idempotency/postgres`) record command results keyed by an `idempotencyKey` so retried commands do not double-execute.

See [Async commands](/language/async-commands) and the [Outbox adapter](/adapters/outbox).

## Atomic transaction boundary

Set a `transactionProvider` in `RuntimeOptions` and the engine wraps each command
attempt — the command body, its outbox enqueue, and its idempotency record — in a
single `withTransaction` call. Those writes now commit or roll back together; a
failed outbox enqueue rolls the mutation back and returns `OUTBOX_ENQUEUE_FAILED`
instead of silently persisting state without a durable event. `PostgresTransactionProvider`
ships at `@angriff36/manifest/transactions/postgres`.

```typescript
import { Pool } from 'pg';
import { RuntimeEngine } from '@angriff36/manifest';
import { PostgresOutboxStore } from '@angriff36/manifest/outbox/postgres';
import { PostgresTransactionProvider } from '@angriff36/manifest/transactions/postgres';

const pool = new Pool({ connectionString: process.env.DATABASE_URL });

const runtime = new RuntimeEngine(ir, context, {
  outboxStore: new PostgresOutboxStore({ pool }),
  transactionProvider: new PostgresTransactionProvider({ pool }),
});
```

Without a provider, the prior default behavior is unchanged: mutation and outbox
enqueue run on separate connections and the gap is logged, not enforced.

## Wiring, config, exports, and CLI

- **One shared route contract.** Routes, clients, zod schemas, and hono/express handlers now derive their paths, envelope keys, and schema names from a single contract module, so a client generated from the same `appDir` as its routes cannot target a stale prefix.
- **Plugins load.** Plugins declared in config now register their CLI commands, and a runtime seam threads plugin registries into `RuntimeOptions`. The config schema covers all projection targets.
- **New CLI subcommands** are registered: `analyze`, `seed`, `profile`, `pack`, `unpack`, and `generate-from-prompt`, alongside `manifest build --all` and `manifest generate --all` for config-driven, whole-project builds.
- **New export subpaths** back the features above: `@angriff36/manifest/outbox/worker`, `/jobs/postgres`, `/jobs/worker`, `/schedule-worker`, `/idempotency/memory`, `/idempotency/postgres`, `/transactions/postgres`, `/webhooks`, `/events`, `/events/redis`, `/outbox/redis`, and `/federation`.

## Route and client coherence

The projections that make up an HTTP surface now agree on one contract:

- The **routes** projection emits dispatcher-style command paths by default.
- **ts.client** emits typed command callers with configurable base paths.
- **react-query** command hooks call the dispatcher path using the raw command name, resolved through the shared contract.
- **zod** command-params schema names are now entity-prefixed so hono/express imports resolve against them.

## Webhooks execute

`handleWebhookRequest` (`@angriff36/manifest/webhooks`) turns an inbound HTTP
request into a Manifest command execution. It is **fail-closed**: HMAC signature
verification, idempotency, and the payload transform all run before any command
executes, and a missing or invalid signature is rejected. The Next.js, Hono, and
Express projections emit webhook routes (`--surface webhook`).

## Realtime and the event bus

The runtime now publishes committed events to a connected `EventBus` **after**
the transaction commits. `connectEventBus` wires a bus onto a `RuntimeEngine`;
`EventBus` and a Redis-backed implementation ship at `@angriff36/manifest/events`
and `@angriff36/manifest/events/redis`. The Next.js shared runtime wires the bus
automatically, and `realtime.requireEventBus` makes a missing bus a hard error
rather than a silent single-instance fallback (`--surface realtime`). See
[Realtime subscriptions](/extensibility/realtime-subscriptions).

## Breaking changes

<Warning>
  These change the shape of **generated code**. Regenerate all surfaces together (`manifest generate
  --all` or `manifest build --all`) so routes, clients, and schemas stay on the same contract, and
  review the diff before committing.
</Warning>

- **Zod schema names are entity-prefixed.** The zod projection now emits `${Entity}${Command}ParamsSchema` (e.g. `RecipeCreateParamsSchema`) instead of the un-prefixed `${Command}ParamsSchema` (e.g. `CreateParamsSchema`). Update any code importing the old names. This is what lets hono/express imports resolve against the emitted schemas.
- **The routes projection emits dispatcher paths by default.** Generated command paths now default to the dispatcher layout. If you depended on the previous default path shape, set your client and server to the same surface or update your callers.
- **hono/express `basePath` now applies.** The `basePath` option (default `/api`) is now prefixed onto emitted routes; previously it was effectively ignored. Routes generated with a non-default `basePath` will move.
- **react-query command envelope/paths changed.** Command hooks now target the dispatcher path with the raw command name via the shared contract; regenerate client and server together.

## Upgrading

1. Regenerate every surface from one build: `manifest build --all` (or `manifest generate --all`).
2. Review the diff, paying attention to zod schema imports, route paths, and `basePath`.
3. If you had hand-written a runtime factory or `manifest-response` helper, delete it and let the companions surface emit it — unless you intentionally keep `emitCompanions: false`.
4. To close the outbox transactional gap, configure a `transactionProvider`.


---
