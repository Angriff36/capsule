# Fixes: Event Closeout Photo Evidence

## 2026-07-22

- Prettier cannot infer a `.manifest` parser, so supported TSX/Markdown files were formatted separately.
- Vite listened on IPv6 `::1:7811`; the temporary harness used the documented `http://localhost:7811` URL.
- Playwright verification clicked visible category-card labels and scoped badge assertions to each uploaded photo card.
