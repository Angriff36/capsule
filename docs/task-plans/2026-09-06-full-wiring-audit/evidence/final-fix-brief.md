# Task 8 final review fix wave

Read this first: these are the complete final code review findings, with the requirements to implement. Work only in C:/projects/capsule-release-20260905, branch fix/full-wiring-audit-20260906, base cb4b3de. No deployment, production write, main merge, or external repository change. Do not dispatch subagents. Root owns documentation; own the source/tests listed below. Read AGENTS.md, DESIGN.md and its applicable references, Convex generated guidelines before implementation. Generated files may only change via owned manifest:regen/codegen. Do not weaken guards or invent workflow restrictions. The approved no-fake-data spec and AC031-041 authorize focused behavior regressions. Follow TDD, report genuine RED/GREEN evidence, self-review, run covering tests/types, commit scoped work. Root runs final full gate after temporary-workspace cleanup; do not spend another full gate on the known scratch-root cap failure.

Final gpt-6-astra review of 9eccdbc..4652173 REJECTS three Important items:

1. convex/lib/clientOutreach.ts:29 accepts unrelated/deleted/missing client IDs before reuse/create. Generated mutations.ts:3043 also fails to resolve relationship. Validate real live same-tenant client before either path, preserve generated sales authorization. Do not require active status or otherwise forbid legitimate later outreach. Cover foreign/deleted/missing and existing/reused/new valid tasks, plus existing role denial.
2. src/features/clients/proposalPdfProjection.ts:15,78 falls back to live values for absent/malformed published snapshots, and ProposalsPage.tsx:703 drops source discriminator, reporting ordinary PDF download. Expose nonblocking visible fallback provenance, distinguishing absent legacy snapshots from malformed snapshots. Preserve export usability, immutable valid snapshots, and ordinary draft behavior. Test production projection and caller feedback behavior, not duplicate mock implementations.
3. ProposalCreateForm.tsx:187 returns for empty selection; visible No template option leaves hidden template sections, service-line identity, and tax mode. Reset/detach template-owned configuration when deliberately removing a template, while preserving appropriate operator-entered values. Cover restrictive-template -> No template -> submit, including retained manual pricing input and removed owned fee, all-sections compatibility and detached template tax behavior. Do not silently discard manual edits.

Complete minor findings also to address in this same wave:

4. DESIGN.md:371: Body never falls below 15px and supporting metadata never below 13px. New explanatory body copy EventMenuTab.tsx:490 and MessageInboxPage.tsx:618 uses 13px text-sm. Use established 15px body token, no visual language changes.
5. Add mounted useSavedViews caller-only projection regression (broader query returns manager-visible/shared reports but personal list-view adapter must select current owner/page). Existing backend two-owner defaults proof stays.
6. Add actual readMaterializationReceipt helper cross-tenant exact/head lookup proof. Existing materialization-receipt-privacy.runtime.test.ts:173 raw tenant filter is not this proof. Existing auth-denial/replay/rollback stays.

This is the single final fix dispatch, so fully self-review the complete change and failure/edge cases. Preserve the prior three-round proposal fixes (controlled draft persistence, offered-draft warning, manual tax override, fee ownership, revision loading). Helpful existing reports: task-6-report.md, task-7-report.md in this directory. Read only the relevant parts; no need to reread entire plan/history. Report file: final-fix-report.md in this directory. Full report must include changes, exact covering test commands/output, genuine RED/GREEN evidence, self-review, concerns, commit IDs. Return only status, commits, one-line tests, concerns and report path (<15 lines). Do not stage root documentation or push; root handles final branch push.
