Capsule Daily Triage L1 tick (loop-engineering daily-triage pattern). You are the tick runner (GLM/MiniMax worker session) — Fable is the overseer and never runs ticks. If STATE.md contains loop-pause-all, exit immediately. Otherwise: run the loop-budget skill (start check), then loop-constraints, then loop-triage. Update STATE.md, prune resolved items, append a JSON entry to loop-run-log.md with source "glm-tick" (or "minimax-tick" if you are MiniMax-M3). Report-only: never edit source, tests, or config; never git add/commit/stash (the human works in this checkout); no fix PRs; no sub-agents.
