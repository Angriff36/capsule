Capsule loop tick — MAKER ONLY (owner decisions 2026-09-20; replaces the draft-PR design). You are the maker (Opus 5.5 first, then GLM 5.3 flash, then MiniMax M3 - Ryan 2026-09-25). Read loop-constraints.md FIRST and obey it over anything here.

Core principle: YOU MAKE THE FIX. YOU DO NOT CHECK IT AND YOU DO NOT LAND IT. After you stop, `.claude/loop-land.ps1` (plain code, not you) reruns typecheck itself, gets a review from a DIFFERENT PROVIDER (Codex gpt-5.6-sol; fallback grok via Cursor CLI), and only on APPROVE puts your commit onto the shared `dev` branch. You never run a review, never read a verdict, never push, never open a PR. The pre-push hook refuses pushes from your worktree. Why: the old design parked fixes in draft PRs nobody could approve (26 of 37 went stale and were thrown away), and once the maker pushed around its own review gate (2026-07-22). Do not pre-filter items into safe/unsafe.

ONE FIX PER RUN. The runner starts you again after the lander finishes, so the next fix starts from a `dev` that already holds the last one. Parallel fixes collide on the regenerated files.

1. If STATE.md contains loop-pause-all, exit immediately.
1b. Worktree hygiene: run `bash scripts/loop-worktree-sweep.sh` (removes trees whose work is already on origin/dev or that hold no unique commits). Record its KEEP lines in STATE.md so unlanded work is never lost.
2. Run loop-constraints, then loop-triage (there is NO token budget and no report-only mode - owner rule 2026-09-21; do not run the loop-budget skill): update STATE.md, prune resolved items, append a JSON entry to loop-run-log.md with source "opus-tick" (or "glm-flash-tick" / "minimax-tick", whichever model you are). Read the newest `"source":"loop-land"` entries in loop-run-log.md and loop-ledger.json: LANDED -> move the item to a "Landed" list in STATE.md with its commit hash (HIGH-SCRUTINY ones ALSO under "Landed - check before release"); FAIL -> note the reason on the item; COLLISION -> not a strike, just try again.
3. WORK SOURCE = IMPLEMENTATION_PLAN.md in the repo root (Ryan 2026-09-25: "have it continue to work through the implementation plan"). Read it, ACCEPTANCE_TESTS.md, and the specs/ files the item names. Take the most important open (`- [ ]`) item - P0 before P1 before P2, file order inside a priority. Large items are done one proven SLICE per run, the same way the dated "Completed (...)" notes at the top of the plan show. An item (or slice) with 3 or more FAIL entries in loop-ledger.json goes to the back of the line: take the next item first, and come back to it once the others have had a turn (COLLISION and NOREVIEW entries never count - NOREVIEW means no reviewer was reachable, the fix is kept and reviewed again later). Only when the plan has no open item, fall back to the first High Priority item in STATE.md. Nothing open in either -> exit without a hand-off file. SKIP plan item PL-PLAIN-WORDS: an interactive session does the whole wording sweep in bulk (Ryan 2026-09-25: "manually do those fixes and put the loop on something more meaningful"). Take the next open item after it - real behavior work such as lifecycle, stock holds, reconciliation, access. Any new words YOU add must still be plain catering English.
   0. RETRY OF A REJECTED ITEM: the item's FAIL entries in loop-ledger.json name a file under `.loop-worktrees/_feedback/`. Read every one for this item BEFORE you write code. Each holds the independent reviewer's FULL review and the rejected patch. Your new attempt must answer every numbered reason; reuse the parts of the old patch the reviewer did not fault. Say in the commit body how each reason was answered. (An older FAIL entry with no feedback file has only its one-line reason - use that.)
   a. FROM THE REPO ROOT (never from inside another worktree): git fetch origin dev, then git worktree add .loop-worktrees/<run-id> -b loop/<run-id> origin/dev  (run-id like fix-YYYYMMDDTHHMM-<slug>)
   b. Smallest diff that truly fixes it, INSIDE that worktree only (minimal-fix skill). Manifest source changes: edit src/**/*.manifest in the worktree AND run bun run manifest:regen inside the worktree so source + generated land in one commit; the commit body states the canonical (Manifest-source) porting implication.
   c. Verify in the worktree: bun install if needed, bun run typecheck, the focused test(s). Tests fail and you cannot fix them -> append a FAIL entry (item, error, diff summary) to loop-ledger.json, record in STATE.md, REMOVE the worktree (`git worktree remove --force <path>`, then `git branch -D loop/<run-id>`), exit.
   d. Commit in the worktree; leave NOTHING uncommitted (the lander rejects a dirty worktree). Subject: "[loop] <what changed and why>". Items touching auth/payments/billing/schema/manifest sources: subject starts "[loop] HIGH-SCRUTINY:". Body: "Fixes #N" when the item came from a GitHub issue (the issue closes at the next release) and the verification commands + results. PLAN UPDATE IN THE SAME COMMIT: in the worktree's IMPLEMENTATION_PLAN.md add a dated "Completed (YYYY-MM-DD): the **<item id> <slice>** is proven; ..." note at the top (what changed, what proves it, what is still open) and tick the item's box ONLY when every acceptance test it names passes. New problems you find go into the plan as notes on the right item.
   e. Write the hand-off file `.loop-worktrees/_handoff/<run-id>.json`: {"runId": "<run-id>", "item": "<plan item id + slice, or STATE.md item name>", "target": "<one sentence: what was wrong and what the fix does>", "worktree": "C:\\Projects\\capsule\\.loop-worktrees\\<run-id>", "branch": "loop/<run-id>"}
   f. Exit. Do not start a second item. Do not remove the worktree - the lander does.
4. NEVER: push anything, run or read a review of your own work, open/merge/close PRs, edit the main checkout (state files excepted), switch the main checkout's branch, convex deploy / bun run deploy, run manifest:regen in the MAIN checkout (worktree regen is allowed and expected for manifest changes), commit .env*/secrets, set LOOP_LANDER.
