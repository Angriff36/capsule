Capsule loop tick — L2 STANDING, queue-drain mode (human decisions 2026-07-19 + 2026-07-21). You are the tick runner (GLM/MiniMax worker session); Fable never runs ticks. Read loop-constraints.md FIRST and obey it over anything here.

Core principle: THE PR GATE IS THE SAFETY BOUNDARY. Every change is worktree-isolated, test-verified, cross-model reviewed (reviewer must be a different model than the diff's author), and ships only as a draft PR the human approves. Therefore: do not pre-filter items into safe/unsafe, and do not stop after one fix.

1. If STATE.md contains loop-pause-all, exit immediately.
2. Run the loop-budget start check, then loop-constraints, then loop-triage: update STATE.md, prune resolved items, append a JSON entry to loop-run-log.md with source "glm-tick" (or "minimax-tick").
3. Fix flow — DRAIN THE QUEUE. For each High Priority item in order (skip an item only if loop-ledger.json shows 3 prior failures -> escalate it in STATE.md instead):
   a. git worktree add .loop-worktrees/<run-id> -b loop/<run-id> main  (run-id like fix-YYYYMMDDTHHMM-<slug>; one worktree per logical fix)
   b. Smallest diff that truly fixes it, INSIDE that worktree only (minimal-fix skill). Manifest source changes: edit src/**/*.manifest in the worktree AND run bun run manifest:regen inside the worktree so source + generated land in one PR; PR body states the canonical (Manifest-source) porting implication.
   c. Verify in the worktree: bun install if needed, bun run typecheck, the focused test(s).
   d. Review gate (cross-model; reviewer must be a different model than the diff's author): git diff main | codex exec -s read-only -c model="gpt-5.6-sol" "Review this diff against the fix target. Find reasons to REJECT: wrong scope, unrelated edits, secrets, hand-edited generated files, disabled tests, symptom-fixes. Also: ensure the change does NOT add tedium for app users via guardrails/policies that barely matter (catering app, not a bank) - changes should REDUCE user tedium, not multiply policy denials; flag any new guard/policy/approval/validation that blocks a reasonable user action without a proportionate real-world reason. Verdict: APPROVE or REJECT with reasons."
      If Codex is unavailable (quota/outage/CLI error), run the SAME review prompt through a cross-model alternate instead: grok via Cursor CLI (PowerShell: agent -p --trust --model cursor-grok-4.5-high-fast "<prompt + diff>") or a fresh Fable/Claude review pass. Any frontier model that did not author the diff is eligible. No verdict from any eligible reviewer -> treat as REJECT and escalate in STATE.md; never push unreviewed.
   e. Tests fail or REJECT -> append a failure entry (item, error) to loop-ledger.json, record in STATE.md, move to the NEXT item. Leave the worktree in place.
   f. APPROVE -> commit in the worktree, git push origin loop/<run-id>, gh pr create --draft with verification evidence (commands + results + the reviewing model and its verdict) in the body. Items touching auth/payments/billing/schema/manifest sources: prefix the PR title "HIGH-SCRUTINY:" and say why. Record PR URL in STATE.md and a success entry in loop-ledger.json.
   g. Repeat from (a) for the next item. Stop only when: no actionable items remain, OR the budget gate trips (80% of daily cap -> report-only), OR loop-pause-all appears.
4. NEVER: push main, merge, mark PRs ready, close PRs, edit the main checkout (state files excepted), convex deploy / bun run deploy, run manifest:regen in the MAIN checkout (worktree regen is allowed and expected for manifest changes), commit .env*/secrets.
