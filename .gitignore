node_modules/
node_modules.old*/
.convex/
.env
.env.local
.env.production
.env.*.local
dist/
.DS_Store
*.log
coverage/
.artifacts/
.pw-verify/
graphify-out/

# Editor / IDE local state
.cursor/
.sonarlint/
.vscode/
.scannerwork/
*.tsbuildinfo
package-lock.json
npm-shrinkwrap.json
yarn.lock
pnpm-lock.yaml

generated/ir/
.tmp/

# Claude Code local (per-machine) settings
.claude/settings.local.json

# Loop fix-attempt worktrees (loop-worktree tool)
.loop-worktrees/

# AboardAI board data lives in .aboardai/ (features, kanban). Do NOT move that
# tree under .local/ — agents did that once and emptied the Kanban. .local/ is
# only for personal scratch; .aboardai/ stays at the repo root.
.claude/scheduled_tasks.json
.claude/scheduled_tasks.lock
.claude/loop-tick.log

# Local kitchen reference (photos/CSVs; may include large binaries)
work/

# Local reproducible tool / harness output (not part of CI checkout)
output/


# Agent transcript workspaces - never commit (can contain env dumps/secrets)
.aboardai/

# Agent transcript workspaces - never commit (can contain env dumps/secrets)
.aboardai/
.vercel

.playwright-mcp/
