// GENERATED by the Manifest → Convex projection. DO NOT EDIT.
// 575 mutation(s); roles: 22; policies: 389.

import { mutation, type MutationCtx } from "./_generated/server";
import { v } from "convex/values";
import type { Doc } from "./_generated/dataModel";
import { getAuthContext } from "./lib/authContext";
import { encrypt, decrypt } from "./lib/encryption";

async function __encryptDoc(ctx: any, entity: string, fields: readonly string[], doc: Record<string, any>): Promise<Record<string, any>> {
  const out = { ...doc };
  for (const property of fields) {
    const value = out[property];
    if (value == null) continue;
    const { ciphertext, keyId } = await encrypt(String(value), { ctx, entity, property });
    out[property] = JSON.stringify({ v: 1, kid: keyId, ct: ciphertext });
  }
  return out;
}

async function __decryptDoc(ctx: any, entity: string, fields: readonly string[], doc: any): Promise<any> {
  if (!doc) return doc;
  const out = { ...doc } as Record<string, any>;
  for (const property of fields) {
    const raw = out[property];
    if (typeof raw !== "string") continue;
    let envelope: any;
    try { envelope = JSON.parse(raw); } catch { continue; }
    const looksEncrypted = envelope && typeof envelope === "object" && "v" in envelope && "kid" in envelope && "ct" in envelope;
    if (!looksEncrypted) continue;
    if (envelope.v !== 1) throw new Error(`Unsupported Manifest encryption envelope version: ${String(envelope.v)}`);
    out[property] = await decrypt(envelope.ct, envelope.kid, { ctx, entity, property });
  }
  return out;
}

async function __resolveRelation(
  ctx: any,
  table: string,
  localValues: readonly unknown[],
  targetFields: readonly string[],
  tenantField?: string,
  tenantValue?: unknown,
): Promise<any | null> {
  if (localValues.some((value) => value == null)) return null;
  const identityIndex = targetFields.indexOf("id");
  let target: any | null;
  if (identityIndex >= 0) {
    target = await ctx.db.get(localValues[identityIndex] as any);
  } else {
    target = await ctx.db
      .query(table)
      .filter((q: any) =>
        q.and(...targetFields.map((field, index) => q.eq(q.field(field), localValues[index]))),
      )
      .first();
  }
  if (!target) return null;
  for (let index = 0; index < targetFields.length; index += 1) {
    const field = targetFields[index] === "id" ? "_id" : targetFields[index];
    if (target[field] !== localValues[index]) return null;
  }
  if (tenantField && target[tenantField] !== tenantValue) return null;
  return target;
}

async function __getCommandIdempotency(ctx: MutationCtx, key: string): Promise<any | undefined> {
  const row = await ctx.db
    .query("commandIdempotencyKeys")
    .withIndex("by_key", (q) => q.eq("key", key))
    .first();
  return row?.result;
}

async function __setCommandIdempotency(
  ctx: MutationCtx,
  key: string,
  command: string,
  result: unknown,
): Promise<void> {
  const existing = await ctx.db
    .query("commandIdempotencyKeys")
    .withIndex("by_key", (q) => q.eq("key", key))
    .first();
  if (existing !== null) return;
  await ctx.db.insert("commandIdempotencyKeys", {
    key,
    command,
    result,
    createdAt: Date.now(),
  });
}

// Role hierarchy from IR (effective permissions after inheritance).
const ROLE_PERMISSIONS: Record<string, { action: string; target?: string }[]> = {
  "admin": [
    {
      "action": "adminAccess"
    },
    {
      "action": "eventAccess"
    },
    {
      "action": "eventManageAccess"
    },
    {
      "action": "financeAccess"
    },
    {
      "action": "financeManageAccess"
    },
    {
      "action": "importAccess"
    },
    {
      "action": "inventoryAccess"
    },
    {
      "action": "inventoryManageAccess"
    },
    {
      "action": "kitchenAccess"
    },
    {
      "action": "kitchenLeadAccess"
    },
    {
      "action": "kitchenManageAccess"
    },
    {
      "action": "logisticsAccess"
    },
    {
      "action": "logisticsManageAccess"
    },
    {
      "action": "manageAccess"
    },
    {
      "action": "procurementAccess"
    },
    {
      "action": "salesAccess"
    },
    {
      "action": "salesManageAccess"
    },
    {
      "action": "staffAccess"
    },
    {
      "action": "workforceAccess"
    },
    {
      "action": "workforceManageAccess"
    }
  ],
  "driver": [
    {
      "action": "logisticsAccess"
    },
    {
      "action": "staffAccess"
    }
  ],
  "event_manager": [
    {
      "action": "eventAccess"
    },
    {
      "action": "eventManageAccess"
    },
    {
      "action": "importAccess"
    },
    {
      "action": "manageAccess"
    },
    {
      "action": "staffAccess"
    }
  ],
  "event_staff": [
    {
      "action": "eventAccess"
    },
    {
      "action": "staffAccess"
    }
  ],
  "finance_manager": [
    {
      "action": "financeAccess"
    },
    {
      "action": "financeManageAccess"
    },
    {
      "action": "importAccess"
    },
    {
      "action": "manageAccess"
    },
    {
      "action": "staffAccess"
    }
  ],
  "finance_staff": [
    {
      "action": "financeAccess"
    },
    {
      "action": "staffAccess"
    }
  ],
  "inventory_manager": [
    {
      "action": "importAccess"
    },
    {
      "action": "inventoryAccess"
    },
    {
      "action": "inventoryManageAccess"
    },
    {
      "action": "manageAccess"
    },
    {
      "action": "procurementAccess"
    },
    {
      "action": "staffAccess"
    }
  ],
  "inventory_staff": [
    {
      "action": "inventoryAccess"
    },
    {
      "action": "staffAccess"
    }
  ],
  "kitchen_lead": [
    {
      "action": "kitchenAccess"
    },
    {
      "action": "kitchenLeadAccess"
    },
    {
      "action": "staffAccess"
    }
  ],
  "kitchen_manager": [
    {
      "action": "importAccess"
    },
    {
      "action": "kitchenAccess"
    },
    {
      "action": "kitchenLeadAccess"
    },
    {
      "action": "kitchenManageAccess"
    },
    {
      "action": "manageAccess"
    },
    {
      "action": "staffAccess"
    }
  ],
  "kitchen_staff": [
    {
      "action": "kitchenAccess"
    },
    {
      "action": "staffAccess"
    }
  ],
  "logistics_manager": [
    {
      "action": "importAccess"
    },
    {
      "action": "logisticsAccess"
    },
    {
      "action": "logisticsManageAccess"
    },
    {
      "action": "manageAccess"
    },
    {
      "action": "staffAccess"
    }
  ],
  "logistics_staff": [
    {
      "action": "logisticsAccess"
    },
    {
      "action": "staffAccess"
    }
  ],
  "manager": [
    {
      "action": "importAccess"
    },
    {
      "action": "manageAccess"
    },
    {
      "action": "staffAccess"
    }
  ],
  "owner": [
    {
      "action": "adminAccess"
    },
    {
      "action": "eventAccess"
    },
    {
      "action": "eventManageAccess"
    },
    {
      "action": "financeAccess"
    },
    {
      "action": "financeManageAccess"
    },
    {
      "action": "importAccess"
    },
    {
      "action": "inventoryAccess"
    },
    {
      "action": "inventoryManageAccess"
    },
    {
      "action": "kitchenAccess"
    },
    {
      "action": "kitchenLeadAccess"
    },
    {
      "action": "kitchenManageAccess"
    },
    {
      "action": "logisticsAccess"
    },
    {
      "action": "logisticsManageAccess"
    },
    {
      "action": "manageAccess"
    },
    {
      "action": "procurementAccess"
    },
    {
      "action": "salesAccess"
    },
    {
      "action": "salesManageAccess"
    },
    {
      "action": "staffAccess"
    },
    {
      "action": "workforceAccess"
    },
    {
      "action": "workforceManageAccess"
    }
  ],
  "procurement_staff": [
    {
      "action": "inventoryAccess"
    },
    {
      "action": "procurementAccess"
    },
    {
      "action": "staffAccess"
    }
  ],
  "sales_manager": [
    {
      "action": "importAccess"
    },
    {
      "action": "manageAccess"
    },
    {
      "action": "salesAccess"
    },
    {
      "action": "salesManageAccess"
    },
    {
      "action": "staffAccess"
    }
  ],
  "sales_staff": [
    {
      "action": "salesAccess"
    },
    {
      "action": "staffAccess"
    }
  ],
  "staff": [
    {
      "action": "staffAccess"
    }
  ],
  "system": [
    {
      "action": "adminAccess"
    },
    {
      "action": "eventAccess"
    },
    {
      "action": "eventManageAccess"
    },
    {
      "action": "financeAccess"
    },
    {
      "action": "financeManageAccess"
    },
    {
      "action": "importAccess"
    },
    {
      "action": "inventoryAccess"
    },
    {
      "action": "inventoryManageAccess"
    },
    {
      "action": "kitchenAccess"
    },
    {
      "action": "kitchenLeadAccess"
    },
    {
      "action": "kitchenManageAccess"
    },
    {
      "action": "logisticsAccess"
    },
    {
      "action": "logisticsManageAccess"
    },
    {
      "action": "manageAccess"
    },
    {
      "action": "procurementAccess"
    },
    {
      "action": "salesAccess"
    },
    {
      "action": "salesManageAccess"
    },
    {
      "action": "staffAccess"
    },
    {
      "action": "workforceAccess"
    },
    {
      "action": "workforceManageAccess"
    }
  ],
  "workforce_manager": [
    {
      "action": "importAccess"
    },
    {
      "action": "manageAccess"
    },
    {
      "action": "staffAccess"
    },
    {
      "action": "workforceAccess"
    },
    {
      "action": "workforceManageAccess"
    }
  ],
  "workforce_staff": [
    {
      "action": "staffAccess"
    },
    {
      "action": "workforceAccess"
    }
  ]
};

function checkRole(userOrRole: unknown, action: unknown, target?: unknown): boolean {
  let userRole: unknown;
  let disabledCapabilities: unknown;
  if (typeof userOrRole === "string") {
    userRole = userOrRole;
  } else if (userOrRole !== null && typeof userOrRole === "object") {
    const auth = userOrRole as { role?: unknown; disabledCapabilities?: unknown };
    userRole = auth.role;
    disabledCapabilities = auth.disabledCapabilities;
  } else {
    return false;
  }
  if (typeof userRole !== "string" || typeof action !== "string") return false;
  if (__orgCapabilityDeniesAction(action, disabledCapabilities)) return false;
  const perms = ROLE_PERMISSIONS[userRole];
  const requestedTarget = typeof target === "string" ? target : undefined;
  return perms ? perms.some((permission) =>
    (permission.action === action || permission.action === "all") &&
    (permission.target === undefined || permission.target === requestedTarget)
  ) : false;
}

function __orgCapabilityDeniesAction(action: string, disabled: unknown): boolean {
  if (!Array.isArray(disabled) || disabled.length === 0) return false;
  const capability = __orgCapabilityForAction(action);
  if (capability === null) return false;
  return disabled.some((entry) => entry === capability);
}

function __orgCapabilityForAction(action: string): string | null {
  if (action === "staffAccess" || action === "manageAccess" || action === "adminAccess") return null;
  if (action.startsWith("kitchen")) return "kitchen";
  if (action.startsWith("inventory")) return "inventory";
  if (action.startsWith("procurement")) return "procurement";
  if (action.startsWith("event")) return "events";
  if (action.startsWith("sales")) return "sales";
  if (action.startsWith("logistics")) return "logistics";
  if (action.startsWith("workforce")) return "workforce";
  if (action.startsWith("finance")) return "finance";
  return null;
}

async function __runAnnouncementPost(ctx: MutationCtx, { docId, title, body, category, expiresAt, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Announcement not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Announcement not found");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may read announcements");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may write announcements through commands");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may execute announcement commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!(checkRole(user, "manageAccess"))) throw new Error("Guard 1 failed");
    if (!((((title).trim()).length > 0))) throw new Error("Announcement title is required");
    if (!((((body).trim()).length > 0))) throw new Error("Announcement body is required");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      title: title,
      body: body,
      category: category,
      expiresAt: expiresAt,
      postedById: user.id,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, announcementId: docId, tenantId: __after.tenantId, _subject: { entity: "Announcement", command: "post", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "AnnouncementPosted", entity: "Announcement", entityId: docId, payload: { announcementId: docId, tenantId: __after.tenantId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Announcement_post = mutation({
  args: {
    docId: v.id("announcements"),
    title: v.string(),
    body: v.string(),
    category: v.any(),
    expiresAt: v.number(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runAnnouncementPost(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Announcement_post", __result);
    }
    return __result;
  },
});

export const Announcement_createViaPost = mutation({
  args: {
    title: v.string(),
    body: v.string(),
    category: v.any(),
    expiresAt: v.number(),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { title, body, category, expiresAt } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      createdAt: Date.now(),
      updatedAt: Date.now(),
      body: args.body,
      category: args.category,
      expiresAt: args.expiresAt,
      title: args.title
    };
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may read announcements");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may write announcements through commands");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may execute announcement commands");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!(checkRole(user, "manageAccess"))) throw new Error("Guard 1 failed");
    if (!((((title).trim()).length > 0))) throw new Error("Announcement title is required");
    if (!((((body).trim()).length > 0))) throw new Error("Announcement body is required");
    const doc: Record<string, any> = {
      ...__draft,
      title: title,
      body: body,
      category: category,
      expiresAt: expiresAt,
      postedById: user.id,
      version: 1,
    };
    const docId = await ctx.db.insert("announcements", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, announcementId: docId, tenantId: doc.tenantId, _subject: { entity: "Announcement", command: "post", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "AnnouncementPosted", entity: "Announcement", entityId: docId, payload: { announcementId: docId, tenantId: doc.tenantId }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Announcement_createViaPost", __result);
    }
    return __result;
  },
});

async function __runAnnouncementRemove(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Announcement not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Announcement not found");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may read announcements");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may write announcements through commands");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may execute announcement commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!(checkRole(user, "manageAccess"))) throw new Error("Guard 1 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      deletedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, announcementId: docId, tenantId: __after.tenantId, _subject: { entity: "Announcement", command: "remove", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "AnnouncementRemoved", entity: "Announcement", entityId: docId, payload: { announcementId: docId, tenantId: __after.tenantId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Announcement_remove = mutation({
  args: {
    docId: v.id("announcements"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runAnnouncementRemove(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Announcement_remove", __result);
    }
    return __result;
  },
});

async function __runAnnouncementDismissalDismiss(ctx: MutationCtx, { docId, announcementId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("AnnouncementDismissal not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("AnnouncementDismissal not found");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may read announcement dismissals");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may write their own announcement dismissals through commands");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may dismiss announcements for themselves");
    if (!((user.id != null))) throw new Error("Guard 0 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      announcementId: announcementId,
      authSubjectId: user.id,
      dismissedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, announcementDismissalId: docId, tenantId: __after.tenantId, announcementId: announcementId, authSubjectId: __after.authSubjectId, _subject: { entity: "AnnouncementDismissal", command: "dismiss", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "AnnouncementDismissed", entity: "AnnouncementDismissal", entityId: docId, payload: { announcementDismissalId: docId, tenantId: __after.tenantId, announcementId: announcementId, authSubjectId: __after.authSubjectId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const AnnouncementDismissal_dismiss = mutation({
  args: {
    docId: v.id("announcementDismissals"),
    announcementId: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runAnnouncementDismissalDismiss(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "AnnouncementDismissal_dismiss", __result);
    }
    return __result;
  },
});

export const AnnouncementDismissal_createViaDismiss = mutation({
  args: {
    announcementId: v.string(),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { announcementId } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      createdAt: Date.now(),
      updatedAt: Date.now(),
      announcementId: args.announcementId
    };
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may read announcement dismissals");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may write their own announcement dismissals through commands");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may dismiss announcements for themselves");
    if (!((user.id != null))) throw new Error("Guard 0 failed");
    const doc: Record<string, any> = {
      ...__draft,
      announcementId: announcementId,
      authSubjectId: user.id,
      dismissedAt: Date.now(),
      version: 1,
    };
    const docId = await ctx.db.insert("announcementDismissals", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, announcementDismissalId: docId, tenantId: doc.tenantId, announcementId: doc.announcementId, authSubjectId: doc.authSubjectId, _subject: { entity: "AnnouncementDismissal", command: "dismiss", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "AnnouncementDismissed", entity: "AnnouncementDismissal", entityId: docId, payload: { announcementDismissalId: docId, tenantId: doc.tenantId, announcementId: doc.announcementId, authSubjectId: doc.authSubjectId }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "AnnouncementDismissal_createViaDismiss", __result);
    }
    return __result;
  },
});

async function __runAttachmentAttach(ctx: MutationCtx, { docId, parentType, parentId, fileName, contentType, fileSize, storageId, evidenceType, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Attachment not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Attachment not found");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may read attachments");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may write attachments through commands");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may execute attachment commands");
    if (!((((parentId).trim()).length > 0))) throw new Error("Parent record is required");
    if (!((((fileName).trim()).length > 0))) throw new Error("File name is required");
    if (!((((storageId).trim()).length > 0))) throw new Error("Stored file reference is required");
    if (!((fileSize >= 0))) throw new Error("File size cannot be negative");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      parentType: parentType,
      parentId: parentId,
      fileName: fileName,
      contentType: contentType,
      fileSize: fileSize,
      storageId: storageId,
      evidenceType: evidenceType,
      uploadedById: user.id,
      uploadedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, attachmentId: docId, tenantId: __after.tenantId, parentType: parentType, parentId: parentId, fileName: fileName, _subject: { entity: "Attachment", command: "attach", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "AttachmentAdded", entity: "Attachment", entityId: docId, payload: { attachmentId: docId, tenantId: __after.tenantId, parentType: parentType, parentId: parentId, fileName: fileName }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Attachment_attach = mutation({
  args: {
    docId: v.id("attachments"),
    parentType: v.any(),
    parentId: v.string(),
    fileName: v.string(),
    contentType: v.string(),
    fileSize: v.number(),
    storageId: v.string(),
    evidenceType: v.optional(v.any()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runAttachmentAttach(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Attachment_attach", __result);
    }
    return __result;
  },
});

export const Attachment_createViaAttach = mutation({
  args: {
    parentType: v.any(),
    parentId: v.string(),
    fileName: v.string(),
    contentType: v.string(),
    fileSize: v.number(),
    storageId: v.string(),
    evidenceType: v.optional(v.any()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { parentType, parentId, fileName, contentType, fileSize, storageId, evidenceType } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      createdAt: Date.now(),
      updatedAt: Date.now(),
      contentType: args.contentType,
      evidenceType: args.evidenceType,
      fileName: args.fileName,
      fileSize: args.fileSize,
      parentId: args.parentId,
      parentType: args.parentType,
      storageId: args.storageId
    };
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may read attachments");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may write attachments through commands");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may execute attachment commands");
    if (!((((parentId).trim()).length > 0))) throw new Error("Parent record is required");
    if (!((((fileName).trim()).length > 0))) throw new Error("File name is required");
    if (!((((storageId).trim()).length > 0))) throw new Error("Stored file reference is required");
    if (!((fileSize >= 0))) throw new Error("File size cannot be negative");
    const doc: Record<string, any> = {
      ...__draft,
      parentType: parentType,
      parentId: parentId,
      fileName: fileName,
      contentType: contentType,
      fileSize: fileSize,
      storageId: storageId,
      evidenceType: evidenceType,
      uploadedById: user.id,
      uploadedAt: Date.now(),
      version: 1,
    };
    const docId = await ctx.db.insert("attachments", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, attachmentId: docId, tenantId: doc.tenantId, parentType: doc.parentType, parentId: doc.parentId, fileName: doc.fileName, _subject: { entity: "Attachment", command: "attach", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "AttachmentAdded", entity: "Attachment", entityId: docId, payload: { attachmentId: docId, tenantId: doc.tenantId, parentType: doc.parentType, parentId: doc.parentId, fileName: doc.fileName }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Attachment_createViaAttach", __result);
    }
    return __result;
  },
});

async function __runAttachmentRemove(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Attachment not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Attachment not found");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may read attachments");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may write attachments through commands");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may execute attachment commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      deletedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, attachmentId: docId, tenantId: __after.tenantId, _subject: { entity: "Attachment", command: "remove", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "AttachmentRemoved", entity: "Attachment", entityId: docId, payload: { attachmentId: docId, tenantId: __after.tenantId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Attachment_remove = mutation({
  args: {
    docId: v.id("attachments"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runAttachmentRemove(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Attachment_remove", __result);
    }
    return __result;
  },
});

async function __runAttachmentSetSurveySelection(ctx: MutationCtx, { docId, included, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Attachment not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Attachment not found");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may read attachments");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may write attachments through commands");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may execute attachment commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      inFeedbackSurvey: included,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    return { ...doc, ...updates };
}

export const Attachment_setSurveySelection = mutation({
  args: {
    docId: v.id("attachments"),
    included: v.boolean(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runAttachmentSetSurveySelection(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Attachment_setSurveySelection", __result);
    }
    return __result;
  },
});

async function __runAvailabilityWindowDeclare(ctx: MutationCtx, { docId, personId, startsAt, endsAt, kind, notes, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("AvailabilityWindow not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("AvailabilityWindow not found");
    const doc = await __decryptDoc(ctx, "AvailabilityWindow", ["notes"], __storedDoc) as Record<string, any>;
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may read availability windows");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may write availability through commands");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may execute availability commands");
    if (!((doc.declaredAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.status === "active"))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((user.id != null))) throw new Error("Guard 3 failed");
    if (!(((personId === user.id) || checkRole(user, "workforceManageAccess")))) throw new Error("Guard 4 failed");
    if (!((personId === doc.personId))) throw new Error("Declare personId must match the seeded person reference");
    if (!((endsAt > startsAt))) throw new Error("Availability end must be after its start");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      personId: personId,
      startsAt: startsAt,
      endsAt: endsAt,
      kind: kind,
      notes: ((notes != null) ? notes : doc.notes),
      declaredAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "AvailabilityWindow", ["notes"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, availabilityWindowId: docId, tenantId: __after.tenantId, personId: personId, startsAt: startsAt, endsAt: endsAt, kind: kind, status: "active", _subject: { entity: "AvailabilityWindow", command: "declare", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "AvailabilityDeclared", entity: "AvailabilityWindow", entityId: docId, payload: { availabilityWindowId: docId, tenantId: __after.tenantId, personId: personId, startsAt: startsAt, endsAt: endsAt, kind: kind, status: "active" }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const AvailabilityWindow_declare = mutation({
  args: {
    docId: v.id("availabilityWindows"),
    personId: v.string(),
    startsAt: v.number(),
    endsAt: v.number(),
    kind: v.any(),
    notes: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runAvailabilityWindowDeclare(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "AvailabilityWindow_declare", __result);
    }
    return __result;
  },
});

export const AvailabilityWindow_createViaDeclare = mutation({
  args: {
    personId: v.string(),
    startsAt: v.number(),
    endsAt: v.number(),
    kind: v.any(),
    notes: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { personId, startsAt, endsAt, kind, notes } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      status: "active",
      createdAt: Date.now(),
      updatedAt: Date.now(),
      endsAt: args.endsAt,
      kind: args.kind,
      notes: args.notes,
      personId: args.personId,
      startsAt: args.startsAt
    };
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may read availability windows");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may write availability through commands");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may execute availability commands");
    if (!((__draft.declaredAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.status === "active"))) throw new Error("Guard 1 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((user.id != null))) throw new Error("Guard 3 failed");
    if (!(((personId === user.id) || checkRole(user, "workforceManageAccess")))) throw new Error("Guard 4 failed");
    if (!((personId === __draft.personId))) throw new Error("Declare personId must match the seeded person reference");
    if (!((endsAt > startsAt))) throw new Error("Availability end must be after its start");
    const doc: Record<string, any> = {
      ...__draft,
      personId: personId,
      startsAt: startsAt,
      endsAt: endsAt,
      kind: kind,
      notes: ((notes != null) ? notes : __draft.notes),
      declaredAt: Date.now(),
      version: 1,
    };
    const __storedDoc = await __encryptDoc(ctx, "AvailabilityWindow", ["notes"], doc);
    const docId = await ctx.db.insert("availabilityWindows", __storedDoc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, availabilityWindowId: docId, tenantId: doc.tenantId, personId: doc.personId, startsAt: doc.startsAt, endsAt: doc.endsAt, kind: doc.kind, status: "active", _subject: { entity: "AvailabilityWindow", command: "declare", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "AvailabilityDeclared", entity: "AvailabilityWindow", entityId: docId, payload: { availabilityWindowId: docId, tenantId: doc.tenantId, personId: doc.personId, startsAt: doc.startsAt, endsAt: doc.endsAt, kind: doc.kind, status: "active" }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "AvailabilityWindow_createViaDeclare", __result);
    }
    return __result;
  },
});

async function __runAvailabilityWindowWithdraw(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("AvailabilityWindow not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("AvailabilityWindow not found");
    const doc = await __decryptDoc(ctx, "AvailabilityWindow", ["notes"], __storedDoc) as Record<string, any>;
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may read availability windows");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may write availability through commands");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may execute availability commands");
    if (!((doc.status === "active"))) throw new Error("Guard 0 failed");
    if (!((doc.declaredAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((user.id != null))) throw new Error("Guard 3 failed");
    if (!(((doc.personId === user.id) || checkRole(user, "workforceManageAccess")))) throw new Error("Guard 4 failed");
    const previousStatus = doc.status;
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "withdrawn";
        const __allowed: Record<string, string[]> = { "active": ["withdrawn"], "withdrawn": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "withdrawn",
      withdrawnAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "AvailabilityWindow", ["notes"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, availabilityWindowId: docId, tenantId: __after.tenantId, personId: __after.personId, startsAt: __after.startsAt, endsAt: __after.endsAt, previousStatus: previousStatus, status: "withdrawn", _subject: { entity: "AvailabilityWindow", command: "withdraw", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "AvailabilityWithdrawn", entity: "AvailabilityWindow", entityId: docId, payload: { availabilityWindowId: docId, tenantId: __after.tenantId, personId: __after.personId, startsAt: __after.startsAt, endsAt: __after.endsAt, previousStatus: previousStatus, status: "withdrawn" }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const AvailabilityWindow_withdraw = mutation({
  args: {
    docId: v.id("availabilityWindows"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runAvailabilityWindowWithdraw(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "AvailabilityWindow_withdraw", __result);
    }
    return __result;
  },
});

async function __runCandidateAdvance(ctx: MutationCtx, { docId, toStage, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Candidate not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Candidate not found");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Workforce managers may read candidates");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Workforce managers may write candidates through commands");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Workforce managers may execute candidate commands");
    if (!((doc.appliedAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((user.id != null))) throw new Error("Guard 2 failed");
    if (!(((toStage !== "hired") && (toStage !== "rejected")))) throw new Error("Use Hire or Reject to reach a terminal stage");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      stage: toStage,
      stageChangedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, candidateId: docId, tenantId: __after.tenantId, toStage: toStage, _subject: { entity: "Candidate", command: "advance", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "CandidateStageChanged", entity: "Candidate", entityId: docId, payload: { candidateId: docId, tenantId: __after.tenantId, toStage: toStage }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Candidate_advance = mutation({
  args: {
    docId: v.id("candidates"),
    toStage: v.any(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runCandidateAdvance(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Candidate_advance", __result);
    }
    return __result;
  },
});

async function __runCandidateApply(ctx: MutationCtx, { docId, fullName, email, phone, roleAppliedFor, sourceSystem, externalCandidateId, rawSourceData, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Candidate not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Candidate not found");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Workforce managers may read candidates");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Workforce managers may write candidates through commands");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Workforce managers may execute candidate commands");
    if (!((doc.appliedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((user.id != null))) throw new Error("Guard 2 failed");
    if (!((((fullName).trim()).length > 0))) throw new Error("Candidate name is required");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      fullName: fullName,
      email: email,
      phone: phone,
      roleAppliedFor: ((roleAppliedFor != null) ? roleAppliedFor : "staff"),
      sourceSystem: sourceSystem,
      externalCandidateId: externalCandidateId,
      rawSourceData: rawSourceData,
      appliedAt: Date.now(),
      stageChangedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, candidateId: docId, tenantId: __after.tenantId, fullName: fullName, roleAppliedFor: ((roleAppliedFor != null) ? roleAppliedFor : "staff"), _subject: { entity: "Candidate", command: "apply", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "CandidateApplied", entity: "Candidate", entityId: docId, payload: { candidateId: docId, tenantId: __after.tenantId, fullName: fullName, roleAppliedFor: ((roleAppliedFor != null) ? roleAppliedFor : "staff") }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Candidate_apply = mutation({
  args: {
    docId: v.id("candidates"),
    fullName: v.string(),
    email: v.optional(v.string()),
    phone: v.optional(v.string()),
    roleAppliedFor: v.optional(v.any()),
    sourceSystem: v.optional(v.string()),
    externalCandidateId: v.optional(v.string()),
    rawSourceData: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runCandidateApply(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Candidate_apply", __result);
    }
    return __result;
  },
});

export const Candidate_createViaApply = mutation({
  args: {
    fullName: v.string(),
    email: v.optional(v.string()),
    phone: v.optional(v.string()),
    roleAppliedFor: v.optional(v.any()),
    sourceSystem: v.optional(v.string()),
    externalCandidateId: v.optional(v.string()),
    rawSourceData: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { fullName, email, phone, roleAppliedFor, sourceSystem, externalCandidateId, rawSourceData } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      roleAppliedFor: args.roleAppliedFor !== undefined ? args.roleAppliedFor : "staff",
      stage: "application",
      createdAt: Date.now(),
      updatedAt: Date.now(),
      email: args.email,
      externalCandidateId: args.externalCandidateId,
      fullName: args.fullName,
      phone: args.phone,
      rawSourceData: args.rawSourceData,
      sourceSystem: args.sourceSystem
    };
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Workforce managers may read candidates");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Workforce managers may write candidates through commands");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Workforce managers may execute candidate commands");
    if (!((__draft.appliedAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((user.id != null))) throw new Error("Guard 2 failed");
    if (!((((fullName).trim()).length > 0))) throw new Error("Candidate name is required");
    const doc: Record<string, any> = {
      ...__draft,
      fullName: fullName,
      email: email,
      phone: phone,
      roleAppliedFor: ((roleAppliedFor != null) ? roleAppliedFor : "staff"),
      sourceSystem: sourceSystem,
      externalCandidateId: externalCandidateId,
      rawSourceData: rawSourceData,
      appliedAt: Date.now(),
      stageChangedAt: Date.now(),
      version: 1,
    };
    const docId = await ctx.db.insert("candidates", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, candidateId: docId, tenantId: doc.tenantId, fullName: doc.fullName, roleAppliedFor: ((doc.roleAppliedFor != null) ? doc.roleAppliedFor : "staff"), _subject: { entity: "Candidate", command: "apply", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "CandidateApplied", entity: "Candidate", entityId: docId, payload: { candidateId: docId, tenantId: doc.tenantId, fullName: doc.fullName, roleAppliedFor: ((doc.roleAppliedFor != null) ? doc.roleAppliedFor : "staff") }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Candidate_createViaApply", __result);
    }
    return __result;
  },
});

async function __runCandidateHire(ctx: MutationCtx, { docId, hiredPersonId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Candidate not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Candidate not found");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Workforce managers may read candidates");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Workforce managers may write candidates through commands");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Workforce managers may execute candidate commands");
    if (!((doc.appliedAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.stage !== "hired"))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((user.id != null))) throw new Error("Guard 3 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      stage: "hired",
      hiredPersonId: hiredPersonId,
      stageChangedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, candidateId: docId, tenantId: __after.tenantId, _subject: { entity: "Candidate", command: "hire", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "CandidateHired", entity: "Candidate", entityId: docId, payload: { candidateId: docId, tenantId: __after.tenantId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Candidate_hire = mutation({
  args: {
    docId: v.id("candidates"),
    hiredPersonId: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runCandidateHire(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Candidate_hire", __result);
    }
    return __result;
  },
});

async function __runCandidateReject(ctx: MutationCtx, { docId, reason, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Candidate not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Candidate not found");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Workforce managers may read candidates");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Workforce managers may write candidates through commands");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Workforce managers may execute candidate commands");
    if (!((doc.appliedAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.stage !== "rejected"))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((user.id != null))) throw new Error("Guard 3 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      stage: "rejected",
      rejectionReason: reason,
      stageChangedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, candidateId: docId, tenantId: __after.tenantId, _subject: { entity: "Candidate", command: "reject", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "CandidateRejected", entity: "Candidate", entityId: docId, payload: { candidateId: docId, tenantId: __after.tenantId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Candidate_reject = mutation({
  args: {
    docId: v.id("candidates"),
    reason: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runCandidateReject(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Candidate_reject", __result);
    }
    return __result;
  },
});

async function __runClientArchive(ctx: MutationCtx, { docId, reason, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("Client not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("Client not found");
    const doc = await __decryptDoc(ctx, "Client", ["email","phone","addressLine1","addressLine2","city","region","postalCode","countryCode","taxId"], __storedDoc) as Record<string, any>;
    if (!((checkRole(user, "salesAccess") || checkRole(user, "financeAccess")))) throw new Error("Sales and finance may read client accounts for CRM and billing");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may write client accounts through commands");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may execute client commands");
    if (!((doc.status === "active"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(checkRole(user, "salesManageAccess"))) throw new Error("Guard 2 failed");
    if (!((((reason).trim()).length > 0))) throw new Error("Archive reason is required");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "archived";
        const __allowed: Record<string, string[]> = { "active": ["archived"], "archived": ["active"] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "archived",
      archivedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "Client", ["email","phone","addressLine1","addressLine2","city","region","postalCode","countryCode","taxId"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, clientId: docId, tenantId: __after.tenantId, reason: reason, _subject: { entity: "Client", command: "archive", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ClientArchived", entity: "Client", entityId: docId, payload: { clientId: docId, tenantId: __after.tenantId, reason: reason }, createdAt: Date.now() });
    const __ret: Record<string, any> = { ...doc, ...updates };
    delete (__ret as any).taxId;
    return __ret;
}

export const Client_archive = mutation({
  args: {
    docId: v.id("clients"),
    reason: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runClientArchive(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Client_archive", __result);
    }
    return __result;
  },
});

async function __runClientAssignOwner(ctx: MutationCtx, { docId, assignedToId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("Client not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("Client not found");
    const doc = await __decryptDoc(ctx, "Client", ["email","phone","addressLine1","addressLine2","city","region","postalCode","countryCode","taxId"], __storedDoc) as Record<string, any>;
    if (!((checkRole(user, "salesAccess") || checkRole(user, "financeAccess")))) throw new Error("Sales and finance may read client accounts for CRM and billing");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may write client accounts through commands");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may execute client commands");
    if (!((doc.status === "active"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      assignedToId: assignedToId,
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "Client", ["email","phone","addressLine1","addressLine2","city","region","postalCode","countryCode","taxId"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, clientId: docId, tenantId: __after.tenantId, assignedToId: assignedToId, _subject: { entity: "Client", command: "assignOwner", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ClientOwnerAssigned", entity: "Client", entityId: docId, payload: { clientId: docId, tenantId: __after.tenantId, assignedToId: assignedToId }, createdAt: Date.now() });
    const __ret: Record<string, any> = { ...doc, ...updates };
    delete (__ret as any).taxId;
    return __ret;
}

export const Client_assignOwner = mutation({
  args: {
    docId: v.id("clients"),
    assignedToId: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runClientAssignOwner(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Client_assignOwner", __result);
    }
    return __result;
  },
});

async function __runClientChangeBillingProfile(ctx: MutationCtx, { docId, paymentTermsDays, taxExempt, taxId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("Client not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("Client not found");
    const doc = await __decryptDoc(ctx, "Client", ["email","phone","addressLine1","addressLine2","city","region","postalCode","countryCode","taxId"], __storedDoc) as Record<string, any>;
    if (!((checkRole(user, "salesAccess") || checkRole(user, "financeAccess")))) throw new Error("Sales and finance may read client accounts for CRM and billing");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may write client accounts through commands");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may execute client commands");
    if (!((doc.status === "active"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(checkRole(user, "salesManageAccess"))) throw new Error("Guard 2 failed");
    if (!(((paymentTermsDays >= 0) && (paymentTermsDays <= 365)))) throw new Error("Payment terms must be between 0 and 365 days");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      paymentTermsDays: paymentTermsDays,
      taxExempt: taxExempt,
      taxId: taxId,
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "Client", ["email","phone","addressLine1","addressLine2","city","region","postalCode","countryCode","taxId"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, clientId: docId, tenantId: __after.tenantId, paymentTermsDays: paymentTermsDays, taxExempt: taxExempt, _subject: { entity: "Client", command: "changeBillingProfile", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ClientBillingProfileChanged", entity: "Client", entityId: docId, payload: { clientId: docId, tenantId: __after.tenantId, paymentTermsDays: paymentTermsDays, taxExempt: taxExempt }, createdAt: Date.now() });
    const __ret: Record<string, any> = { ...doc, ...updates };
    delete (__ret as any).taxId;
    return __ret;
}

export const Client_changeBillingProfile = mutation({
  args: {
    docId: v.id("clients"),
    paymentTermsDays: v.any(),
    taxExempt: v.boolean(),
    taxId: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runClientChangeBillingProfile(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Client_changeBillingProfile", __result);
    }
    return __result;
  },
});

async function __runClientChangeContact(ctx: MutationCtx, { docId, email, phone, website, addressLine1, addressLine2, city, region, postalCode, countryCode, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("Client not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("Client not found");
    const doc = await __decryptDoc(ctx, "Client", ["email","phone","addressLine1","addressLine2","city","region","postalCode","countryCode","taxId"], __storedDoc) as Record<string, any>;
    if (!((checkRole(user, "salesAccess") || checkRole(user, "financeAccess")))) throw new Error("Sales and finance may read client accounts for CRM and billing");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may write client accounts through commands");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may execute client commands");
    if (!((doc.status === "active"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(((countryCode == null) || (((countryCode).trim()).length === 2)))) throw new Error("Country code must contain two characters");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      email: email,
      phone: phone,
      website: website,
      addressLine1: addressLine1,
      addressLine2: addressLine2,
      city: city,
      region: region,
      postalCode: postalCode,
      countryCode: countryCode,
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "Client", ["email","phone","addressLine1","addressLine2","city","region","postalCode","countryCode","taxId"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, clientId: docId, tenantId: __after.tenantId, _subject: { entity: "Client", command: "changeContact", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ClientContactChanged", entity: "Client", entityId: docId, payload: { clientId: docId, tenantId: __after.tenantId }, createdAt: Date.now() });
    const __ret: Record<string, any> = { ...doc, ...updates };
    delete (__ret as any).taxId;
    return __ret;
}

export const Client_changeContact = mutation({
  args: {
    docId: v.id("clients"),
    email: v.optional(v.string()),
    phone: v.optional(v.string()),
    website: v.optional(v.string()),
    addressLine1: v.optional(v.string()),
    addressLine2: v.optional(v.string()),
    city: v.optional(v.string()),
    region: v.optional(v.string()),
    postalCode: v.optional(v.string()),
    countryCode: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runClientChangeContact(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Client_changeContact", __result);
    }
    return __result;
  },
});

async function __runClientMarkMerged(ctx: MutationCtx, { docId, clientId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("Client not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("Client not found");
    const doc = await __decryptDoc(ctx, "Client", ["email","phone","addressLine1","addressLine2","city","region","postalCode","countryCode","taxId"], __storedDoc) as Record<string, any>;
    const __rel_clientMergeAuthorization = await __resolveRelation(ctx, "clientMerges", [__auth.tenantId, doc.clientMergeAuthorizationId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_mergedIntoClient = await __resolveRelation(ctx, "clients", [__auth.tenantId, doc.mergedIntoClientId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).clientMergeAuthorizationId;
      ((doc as any) as any).clientMergeAuthorization = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    {
      const __fk = ((doc as any) as any).mergedIntoClientId;
      ((doc as any) as any).mergedIntoClient = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!((checkRole(user, "salesAccess") || checkRole(user, "financeAccess")))) throw new Error("Sales and finance may read client accounts for CRM and billing");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may write client accounts through commands");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may execute client commands");
    if (!((doc.status === "active"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(checkRole(user, "adminAccess"))) throw new Error("Guard 2 failed");
    if (!((__rel_clientMergeAuthorization != null))) throw new Error("Guard 3 failed");
    if (!((__rel_clientMergeAuthorization.deletedAt == null))) throw new Error("Guard 4 failed");
    if (!((__rel_clientMergeAuthorization.mergedAt != null))) throw new Error("Guard 5 failed");
    if (!((__rel_mergedIntoClient != null))) throw new Error("Guard 6 failed");
    if (!((__rel_mergedIntoClient.status === "active"))) throw new Error("Guard 7 failed");
    if (!((__rel_mergedIntoClient.deletedAt == null))) throw new Error("Guard 8 failed");
    if (!((__rel_clientMergeAuthorization.duplicateClientId === clientId))) throw new Error("Merge authorization does not belong to this duplicate");
    if (!((__rel_clientMergeAuthorization.primaryClientId === doc.mergedIntoClientId))) throw new Error("Merge authorization does not match the primary client");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "archived";
        const __allowed: Record<string, string[]> = { "active": ["archived"], "archived": ["active"] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "archived",
      archivedAt: Date.now(),
      mergedAt: Date.now(),
      deletedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "Client", ["email","phone","addressLine1","addressLine2","city","region","postalCode","countryCode","taxId"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, duplicateClientId: docId, primaryClientId: __after.mergedIntoClientId, tenantId: __after.tenantId, _subject: { entity: "Client", command: "markMerged", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ClientMerged", entity: "Client", entityId: docId, payload: { duplicateClientId: docId, primaryClientId: __after.mergedIntoClientId, tenantId: __after.tenantId }, createdAt: Date.now() });
    const __ret: Record<string, any> = { ...doc, ...updates };
    delete (__ret as any).taxId;
    return __ret;
}

export const Client_markMerged = mutation({
  args: {
    docId: v.id("clients"),
    clientId: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runClientMarkMerged(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Client_markMerged", __result);
    }
    return __result;
  },
});

async function __runClientReactivate(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("Client not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("Client not found");
    const doc = await __decryptDoc(ctx, "Client", ["email","phone","addressLine1","addressLine2","city","region","postalCode","countryCode","taxId"], __storedDoc) as Record<string, any>;
    if (!((checkRole(user, "salesAccess") || checkRole(user, "financeAccess")))) throw new Error("Sales and finance may read client accounts for CRM and billing");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may write client accounts through commands");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may execute client commands");
    if (!((doc.status === "archived"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(checkRole(user, "salesManageAccess"))) throw new Error("Guard 2 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "active";
        const __allowed: Record<string, string[]> = { "active": ["archived"], "archived": ["active"] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "active",
      archivedAt: null,
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "Client", ["email","phone","addressLine1","addressLine2","city","region","postalCode","countryCode","taxId"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, clientId: docId, tenantId: __after.tenantId, _subject: { entity: "Client", command: "reactivate", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ClientReactivated", entity: "Client", entityId: docId, payload: { clientId: docId, tenantId: __after.tenantId }, createdAt: Date.now() });
    const __ret: Record<string, any> = { ...doc, ...updates };
    delete (__ret as any).taxId;
    return __ret;
}

export const Client_reactivate = mutation({
  args: {
    docId: v.id("clients"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runClientReactivate(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Client_reactivate", __result);
    }
    return __result;
  },
});

async function __runClientRegister(ctx: MutationCtx, { docId, clientType, companyName, givenName, familyName, email, phone, website, addressLine1, addressLine2, city, region, postalCode, countryCode, taxId, taxExempt, paymentTermsDays, notes, assignedToId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("Client not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("Client not found");
    const doc = await __decryptDoc(ctx, "Client", ["email","phone","addressLine1","addressLine2","city","region","postalCode","countryCode","taxId"], __storedDoc) as Record<string, any>;
    if (!((checkRole(user, "salesAccess") || checkRole(user, "financeAccess")))) throw new Error("Sales and finance may read client accounts for CRM and billing");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may write client accounts through commands");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may execute client commands");
    if (!((doc.registeredAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(((((clientType === "company") && (companyName != null)) && (((companyName).trim()).length > 0)) || (((clientType === "person") && (givenName != null)) && (((givenName).trim()).length > 0))))) throw new Error("Company clients require a company name; person clients require a given name");
    if (!(((paymentTermsDays == null) || ((paymentTermsDays >= 0) && (paymentTermsDays <= 365))))) throw new Error("Payment terms must be between 0 and 365 days");
    if (!(((countryCode == null) || (((countryCode).trim()).length === 2)))) throw new Error("Country code must contain two characters");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      clientType: clientType,
      companyName: companyName,
      givenName: givenName,
      familyName: familyName,
      email: email,
      phone: phone,
      website: website,
      addressLine1: addressLine1,
      addressLine2: addressLine2,
      city: city,
      region: region,
      postalCode: postalCode,
      countryCode: countryCode,
      taxId: taxId,
      taxExempt: ((taxExempt != null) ? taxExempt : false),
      paymentTermsDays: ((paymentTermsDays != null) ? paymentTermsDays : 30),
      notes: notes,
      assignedToId: assignedToId,
      registeredAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "Client", ["email","phone","addressLine1","addressLine2","city","region","postalCode","countryCode","taxId"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, clientId: docId, tenantId: __after.tenantId, clientType: clientType, _subject: { entity: "Client", command: "register", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ClientRegistered", entity: "Client", entityId: docId, payload: { clientId: docId, tenantId: __after.tenantId, clientType: clientType }, createdAt: Date.now() });
    const __ret: Record<string, any> = { ...doc, ...updates };
    delete (__ret as any).taxId;
    return __ret;
}

export const Client_register = mutation({
  args: {
    docId: v.id("clients"),
    clientType: v.any(),
    companyName: v.optional(v.string()),
    givenName: v.optional(v.string()),
    familyName: v.optional(v.string()),
    email: v.optional(v.string()),
    phone: v.optional(v.string()),
    website: v.optional(v.string()),
    addressLine1: v.optional(v.string()),
    addressLine2: v.optional(v.string()),
    city: v.optional(v.string()),
    region: v.optional(v.string()),
    postalCode: v.optional(v.string()),
    countryCode: v.optional(v.string()),
    taxId: v.optional(v.string()),
    taxExempt: v.optional(v.boolean()),
    paymentTermsDays: v.optional(v.any()),
    notes: v.optional(v.string()),
    assignedToId: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runClientRegister(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Client_register", __result);
    }
    return __result;
  },
});

export const Client_createViaRegister = mutation({
  args: {
    clientType: v.any(),
    companyName: v.optional(v.string()),
    givenName: v.optional(v.string()),
    familyName: v.optional(v.string()),
    email: v.optional(v.string()),
    phone: v.optional(v.string()),
    website: v.optional(v.string()),
    addressLine1: v.optional(v.string()),
    addressLine2: v.optional(v.string()),
    city: v.optional(v.string()),
    region: v.optional(v.string()),
    postalCode: v.optional(v.string()),
    countryCode: v.optional(v.string()),
    taxId: v.optional(v.string()),
    taxExempt: v.optional(v.boolean()),
    paymentTermsDays: v.optional(v.any()),
    notes: v.optional(v.string()),
    assignedToId: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { clientType, companyName, givenName, familyName, email, phone, website, addressLine1, addressLine2, city, region, postalCode, countryCode, taxId, taxExempt, paymentTermsDays, notes, assignedToId } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      taxExempt: args.taxExempt !== undefined ? args.taxExempt : false,
      paymentTermsDays: args.paymentTermsDays !== undefined ? args.paymentTermsDays : 30,
      status: "active",
      createdAt: Date.now(),
      updatedAt: Date.now(),
      addressLine1: args.addressLine1,
      addressLine2: args.addressLine2,
      assignedToId: args.assignedToId,
      city: args.city,
      clientType: args.clientType,
      companyName: args.companyName,
      countryCode: args.countryCode,
      email: args.email,
      familyName: args.familyName,
      givenName: args.givenName,
      notes: args.notes,
      phone: args.phone,
      postalCode: args.postalCode,
      region: args.region,
      taxId: args.taxId,
      website: args.website
    };
    if (!((checkRole(user, "salesAccess") || checkRole(user, "financeAccess")))) throw new Error("Sales and finance may read client accounts for CRM and billing");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may write client accounts through commands");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may execute client commands");
    if (!((__draft.registeredAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(((((clientType === "company") && (companyName != null)) && (((companyName).trim()).length > 0)) || (((clientType === "person") && (givenName != null)) && (((givenName).trim()).length > 0))))) throw new Error("Company clients require a company name; person clients require a given name");
    if (!(((paymentTermsDays == null) || ((paymentTermsDays >= 0) && (paymentTermsDays <= 365))))) throw new Error("Payment terms must be between 0 and 365 days");
    if (!(((countryCode == null) || (((countryCode).trim()).length === 2)))) throw new Error("Country code must contain two characters");
    const doc: Record<string, any> = {
      ...__draft,
      clientType: clientType,
      companyName: companyName,
      givenName: givenName,
      familyName: familyName,
      email: email,
      phone: phone,
      website: website,
      addressLine1: addressLine1,
      addressLine2: addressLine2,
      city: city,
      region: region,
      postalCode: postalCode,
      countryCode: countryCode,
      taxId: taxId,
      taxExempt: ((taxExempt != null) ? taxExempt : false),
      paymentTermsDays: ((paymentTermsDays != null) ? paymentTermsDays : 30),
      notes: notes,
      assignedToId: assignedToId,
      registeredAt: Date.now(),
      version: 1,
    };
    const __storedDoc = await __encryptDoc(ctx, "Client", ["email","phone","addressLine1","addressLine2","city","region","postalCode","countryCode","taxId"], doc);
    const docId = await ctx.db.insert("clients", __storedDoc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, clientId: docId, tenantId: doc.tenantId, clientType: doc.clientType, _subject: { entity: "Client", command: "register", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ClientRegistered", entity: "Client", entityId: docId, payload: { clientId: docId, tenantId: doc.tenantId, clientType: doc.clientType }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Client_createViaRegister", __result);
    }
    return __result;
  },
});

async function __runClientStageClientMerge(ctx: MutationCtx, { docId, clientMergeId, primaryClientId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("Client not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("Client not found");
    const doc = await __decryptDoc(ctx, "Client", ["email","phone","addressLine1","addressLine2","city","region","postalCode","countryCode","taxId"], __storedDoc) as Record<string, any>;
    if (!((checkRole(user, "salesAccess") || checkRole(user, "financeAccess")))) throw new Error("Sales and finance may read client accounts for CRM and billing");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may write client accounts through commands");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may execute client commands");
    if (!((doc.status === "active"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(checkRole(user, "adminAccess"))) throw new Error("Guard 2 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      clientMergeAuthorizationId: clientMergeId,
      mergedIntoClientId: primaryClientId,
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "Client", ["email","phone","addressLine1","addressLine2","city","region","postalCode","countryCode","taxId"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, duplicateClientId: docId, clientMergeId: clientMergeId, tenantId: __after.tenantId, _subject: { entity: "Client", command: "stageClientMerge", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ClientMergeStaged", entity: "Client", entityId: docId, payload: { duplicateClientId: docId, clientMergeId: clientMergeId, tenantId: __after.tenantId }, createdAt: Date.now() });
    // Reactions
    const reactionTarget0 = payload.duplicateClientId;
    if (reactionTarget0) await __runClientMarkMerged(ctx, { docId: reactionTarget0, clientId: payload.duplicateClientId } as any);
    const __ret: Record<string, any> = { ...doc, ...updates };
    delete (__ret as any).taxId;
    return __ret;
}

export const Client_stageClientMerge = mutation({
  args: {
    docId: v.id("clients"),
    clientMergeId: v.string(),
    primaryClientId: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runClientStageClientMerge(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Client_stageClientMerge", __result);
    }
    return __result;
  },
});

async function __runClientCommunicationRecord(ctx: MutationCtx, { docId, clientContactId, eventId, occurredAt, medium, summary, authorName, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ClientCommunication not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ClientCommunication not found");
    const __rel_clientContact = await __resolveRelation(ctx, "clientContacts", [__auth.tenantId, doc.clientContactId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_event = await __resolveRelation(ctx, "events", [__auth.tenantId, doc.eventId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).clientContactId;
      ((doc as any) as any).clientContact = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    {
      const __fk = ((doc as any) as any).eventId;
      ((doc as any) as any).event = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may read client communication history");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may record client communication through commands");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may record client communication");
    if (!((doc.recordedAt == null))) throw new Error("Guard 0 failed");
    if (!(((doc.clientContactId == null) || (__rel_clientContact != null)))) throw new Error("Guard 1 failed");
    if (!(((doc.eventId == null) || (__rel_event != null)))) throw new Error("Guard 2 failed");
    if (!((((clientContactId != null) && (eventId == null)) || ((clientContactId == null) && (eventId != null))))) throw new Error("Choose one contact or event");
    if (!((((summary).trim()).length > 0))) throw new Error("Communication summary is required");
    if (!((((authorName).trim()).length > 0))) throw new Error("Communication author is required");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      clientContactId: clientContactId,
      eventId: eventId,
      occurredAt: occurredAt,
      medium: medium,
      summary: summary,
      authorId: user.id,
      authorName: authorName,
      recordedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, clientCommunicationId: docId, tenantId: __after.tenantId, clientContactId: clientContactId, eventId: eventId, occurredAt: occurredAt, medium: medium, authorId: user.id, _subject: { entity: "ClientCommunication", command: "record", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ClientCommunicationRecorded", entity: "ClientCommunication", entityId: docId, payload: { clientCommunicationId: docId, tenantId: __after.tenantId, clientContactId: clientContactId, eventId: eventId, occurredAt: occurredAt, medium: medium, authorId: user.id }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ClientCommunication_record = mutation({
  args: {
    docId: v.id("clientCommunications"),
    clientContactId: v.optional(v.string()),
    eventId: v.optional(v.string()),
    occurredAt: v.number(),
    medium: v.any(),
    summary: v.string(),
    authorName: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runClientCommunicationRecord(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ClientCommunication_record", __result);
    }
    return __result;
  },
});

export const ClientCommunication_createViaRecord = mutation({
  args: {
    clientContactId: v.optional(v.string()),
    eventId: v.optional(v.string()),
    occurredAt: v.number(),
    medium: v.any(),
    summary: v.string(),
    authorName: v.string(),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { clientContactId, eventId, occurredAt, medium, summary, authorName } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      createdAt: Date.now(),
      updatedAt: Date.now(),
      authorName: args.authorName,
      clientContactId: args.clientContactId,
      eventId: args.eventId,
      medium: args.medium,
      occurredAt: args.occurredAt,
      summary: args.summary
    };
    const __rel_clientContact = await __resolveRelation(ctx, "clientContacts", [__auth.tenantId, __draft.clientContactId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_event = await __resolveRelation(ctx, "events", [__auth.tenantId, __draft.eventId], ["tenantId","id"], "tenantId", __auth.tenantId);
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may read client communication history");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may record client communication through commands");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may record client communication");
    if (!((__draft.recordedAt == null))) throw new Error("Guard 0 failed");
    if (!(((__draft.clientContactId == null) || (__rel_clientContact != null)))) throw new Error("Guard 1 failed");
    if (!(((__draft.eventId == null) || (__rel_event != null)))) throw new Error("Guard 2 failed");
    if (!((((clientContactId != null) && (eventId == null)) || ((clientContactId == null) && (eventId != null))))) throw new Error("Choose one contact or event");
    if (!((((summary).trim()).length > 0))) throw new Error("Communication summary is required");
    if (!((((authorName).trim()).length > 0))) throw new Error("Communication author is required");
    const doc: Record<string, any> = {
      ...__draft,
      clientContactId: clientContactId,
      eventId: eventId,
      occurredAt: occurredAt,
      medium: medium,
      summary: summary,
      authorId: user.id,
      authorName: authorName,
      recordedAt: Date.now(),
      version: 1,
    };
    const docId = await ctx.db.insert("clientCommunications", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, clientCommunicationId: docId, tenantId: doc.tenantId, clientContactId: doc.clientContactId, eventId: doc.eventId, occurredAt: doc.occurredAt, medium: doc.medium, authorId: user.id, _subject: { entity: "ClientCommunication", command: "record", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ClientCommunicationRecorded", entity: "ClientCommunication", entityId: docId, payload: { clientCommunicationId: docId, tenantId: doc.tenantId, clientContactId: doc.clientContactId, eventId: doc.eventId, occurredAt: doc.occurredAt, medium: doc.medium, authorId: user.id }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ClientCommunication_createViaRecord", __result);
    }
    return __result;
  },
});

async function __runClientContactAdd(ctx: MutationCtx, { docId, clientId, givenName, familyName, title, email, phone, mobile, isPrimary, isBillingContact, notes, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("ClientContact not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("ClientContact not found");
    const doc = await __decryptDoc(ctx, "ClientContact", ["email","phone","mobile"], __storedDoc) as Record<string, any>;
    const __rel_client = await __resolveRelation(ctx, "clients", [__auth.tenantId, doc.clientId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).clientId;
      ((doc as any) as any).client = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may read client contacts");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may write client contacts through commands");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may execute client contact commands");
    if (!((doc.addedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.status === "active"))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((__rel_client != null))) throw new Error("Guard 3 failed");
    if (!((clientId === doc.clientId))) throw new Error("Add clientId must match the seeded client reference");
    if (!((((givenName).trim()).length > 0))) throw new Error("Contact given name is required");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      clientId: clientId,
      givenName: givenName,
      familyName: familyName,
      title: title,
      email: email,
      phone: phone,
      mobile: mobile,
      isPrimary: ((isPrimary != null) ? isPrimary : false),
      isBillingContact: ((isBillingContact != null) ? isBillingContact : false),
      notes: notes,
      addedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "ClientContact", ["email","phone","mobile"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, clientContactId: docId, tenantId: __after.tenantId, clientId: clientId, isPrimary: ((isPrimary != null) ? isPrimary : false), isBillingContact: ((isBillingContact != null) ? isBillingContact : false), _subject: { entity: "ClientContact", command: "add", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ClientContactAdded", entity: "ClientContact", entityId: docId, payload: { clientContactId: docId, tenantId: __after.tenantId, clientId: clientId, isPrimary: ((isPrimary != null) ? isPrimary : false), isBillingContact: ((isBillingContact != null) ? isBillingContact : false) }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ClientContact_add = mutation({
  args: {
    docId: v.id("clientContacts"),
    clientId: v.string(),
    givenName: v.string(),
    familyName: v.optional(v.string()),
    title: v.optional(v.string()),
    email: v.optional(v.string()),
    phone: v.optional(v.string()),
    mobile: v.optional(v.string()),
    isPrimary: v.optional(v.boolean()),
    isBillingContact: v.optional(v.boolean()),
    notes: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runClientContactAdd(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ClientContact_add", __result);
    }
    return __result;
  },
});

export const ClientContact_createViaAdd = mutation({
  args: {
    clientId: v.string(),
    givenName: v.string(),
    familyName: v.optional(v.string()),
    title: v.optional(v.string()),
    email: v.optional(v.string()),
    phone: v.optional(v.string()),
    mobile: v.optional(v.string()),
    isPrimary: v.optional(v.boolean()),
    isBillingContact: v.optional(v.boolean()),
    notes: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { clientId, givenName, familyName, title, email, phone, mobile, isPrimary, isBillingContact, notes } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      isPrimary: args.isPrimary !== undefined ? args.isPrimary : false,
      isBillingContact: args.isBillingContact !== undefined ? args.isBillingContact : false,
      status: "active",
      createdAt: Date.now(),
      updatedAt: Date.now(),
      clientId: args.clientId,
      email: args.email,
      familyName: args.familyName,
      givenName: args.givenName,
      mobile: args.mobile,
      notes: args.notes,
      phone: args.phone,
      title: args.title
    };
    const __rel_client = await __resolveRelation(ctx, "clients", [__auth.tenantId, __draft.clientId], ["tenantId","id"], "tenantId", __auth.tenantId);
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may read client contacts");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may write client contacts through commands");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may execute client contact commands");
    if (!((__draft.addedAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.status === "active"))) throw new Error("Guard 1 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((__rel_client != null))) throw new Error("Guard 3 failed");
    if (!((clientId === __draft.clientId))) throw new Error("Add clientId must match the seeded client reference");
    if (!((((givenName).trim()).length > 0))) throw new Error("Contact given name is required");
    const doc: Record<string, any> = {
      ...__draft,
      clientId: clientId,
      givenName: givenName,
      familyName: familyName,
      title: title,
      email: email,
      phone: phone,
      mobile: mobile,
      isPrimary: ((isPrimary != null) ? isPrimary : false),
      isBillingContact: ((isBillingContact != null) ? isBillingContact : false),
      notes: notes,
      addedAt: Date.now(),
      version: 1,
    };
    const __storedDoc = await __encryptDoc(ctx, "ClientContact", ["email","phone","mobile"], doc);
    const docId = await ctx.db.insert("clientContacts", __storedDoc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, clientContactId: docId, tenantId: doc.tenantId, clientId: doc.clientId, isPrimary: ((doc.isPrimary != null) ? doc.isPrimary : false), isBillingContact: ((doc.isBillingContact != null) ? doc.isBillingContact : false), _subject: { entity: "ClientContact", command: "add", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ClientContactAdded", entity: "ClientContact", entityId: docId, payload: { clientContactId: docId, tenantId: doc.tenantId, clientId: doc.clientId, isPrimary: ((doc.isPrimary != null) ? doc.isPrimary : false), isBillingContact: ((doc.isBillingContact != null) ? doc.isBillingContact : false) }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ClientContact_createViaAdd", __result);
    }
    return __result;
  },
});

async function __runClientContactReassignClient(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("ClientContact not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("ClientContact not found");
    const doc = await __decryptDoc(ctx, "ClientContact", ["email","phone","mobile"], __storedDoc) as Record<string, any>;
    const __rel_clientMergeAuthorization = await __resolveRelation(ctx, "clientMerges", [__auth.tenantId, doc.clientMergeAuthorizationId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_mergeTargetClient = await __resolveRelation(ctx, "clients", [__auth.tenantId, doc.mergeTargetClientId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).clientMergeAuthorizationId;
      ((doc as any) as any).clientMergeAuthorization = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    {
      const __fk = ((doc as any) as any).mergeTargetClientId;
      ((doc as any) as any).mergeTargetClient = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may read client contacts");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may write client contacts through commands");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may execute client contact commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!(checkRole(user, "adminAccess"))) throw new Error("Guard 1 failed");
    if (!((__rel_clientMergeAuthorization != null))) throw new Error("Guard 2 failed");
    if (!((__rel_clientMergeAuthorization.deletedAt == null))) throw new Error("Guard 3 failed");
    if (!((__rel_clientMergeAuthorization.mergedAt != null))) throw new Error("Guard 4 failed");
    if (!((__rel_mergeTargetClient != null))) throw new Error("Guard 5 failed");
    if (!((__rel_mergeTargetClient.status === "active"))) throw new Error("Guard 6 failed");
    if (!((__rel_mergeTargetClient.deletedAt == null))) throw new Error("Guard 7 failed");
    if (!((__rel_clientMergeAuthorization.duplicateClientId === doc.clientId))) throw new Error("Merge authorization does not match the contact client");
    if (!((__rel_clientMergeAuthorization.primaryClientId === doc.mergeTargetClientId))) throw new Error("Merge authorization does not match the primary client");
    const previousClientId = doc.clientId;
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      clientId: doc.mergeTargetClientId,
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "ClientContact", ["email","phone","mobile"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, clientContactId: docId, tenantId: __after.tenantId, previousClientId: previousClientId, clientId: __after.mergeTargetClientId, _subject: { entity: "ClientContact", command: "reassignClient", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ClientContactReassigned", entity: "ClientContact", entityId: docId, payload: { clientContactId: docId, tenantId: __after.tenantId, previousClientId: previousClientId, clientId: __after.mergeTargetClientId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ClientContact_reassignClient = mutation({
  args: {
    docId: v.id("clientContacts"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runClientContactReassignClient(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ClientContact_reassignClient", __result);
    }
    return __result;
  },
});

async function __runClientContactRemove(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("ClientContact not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("ClientContact not found");
    const doc = await __decryptDoc(ctx, "ClientContact", ["email","phone","mobile"], __storedDoc) as Record<string, any>;
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may read client contacts");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may write client contacts through commands");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may execute client contact commands");
    if (!((doc.status === "active"))) throw new Error("Guard 0 failed");
    if (!((doc.addedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "removed";
        const __allowed: Record<string, string[]> = { "active": ["removed"], "removed": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "removed",
      removedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "ClientContact", ["email","phone","mobile"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, clientContactId: docId, tenantId: __after.tenantId, clientId: __after.clientId, _subject: { entity: "ClientContact", command: "remove", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ClientContactRemoved", entity: "ClientContact", entityId: docId, payload: { clientContactId: docId, tenantId: __after.tenantId, clientId: __after.clientId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ClientContact_remove = mutation({
  args: {
    docId: v.id("clientContacts"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runClientContactRemove(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ClientContact_remove", __result);
    }
    return __result;
  },
});

async function __runClientContactSetPrimary(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("ClientContact not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("ClientContact not found");
    const doc = await __decryptDoc(ctx, "ClientContact", ["email","phone","mobile"], __storedDoc) as Record<string, any>;
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may read client contacts");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may write client contacts through commands");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may execute client contact commands");
    if (!((doc.status === "active"))) throw new Error("Guard 0 failed");
    if (!((doc.addedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      isPrimary: true,
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "ClientContact", ["email","phone","mobile"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, clientContactId: docId, tenantId: __after.tenantId, clientId: __after.clientId, _subject: { entity: "ClientContact", command: "setPrimary", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ClientContactPrimarySet", entity: "ClientContact", entityId: docId, payload: { clientContactId: docId, tenantId: __after.tenantId, clientId: __after.clientId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ClientContact_setPrimary = mutation({
  args: {
    docId: v.id("clientContacts"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runClientContactSetPrimary(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ClientContact_setPrimary", __result);
    }
    return __result;
  },
});

async function __runClientContactStageClientMerge(ctx: MutationCtx, { docId, clientMergeId, clientId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("ClientContact not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("ClientContact not found");
    const doc = await __decryptDoc(ctx, "ClientContact", ["email","phone","mobile"], __storedDoc) as Record<string, any>;
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may read client contacts");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may write client contacts through commands");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may execute client contact commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!(checkRole(user, "adminAccess"))) throw new Error("Guard 1 failed");
    if (!((clientId !== doc.clientId))) throw new Error("Contact already belongs to this client");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      clientMergeAuthorizationId: clientMergeId,
      mergeTargetClientId: clientId,
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "ClientContact", ["email","phone","mobile"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, clientContactId: docId, tenantId: __after.tenantId, _subject: { entity: "ClientContact", command: "stageClientMerge", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ClientContactMergeStaged", entity: "ClientContact", entityId: docId, payload: { clientContactId: docId, tenantId: __after.tenantId }, createdAt: Date.now() });
    // Reactions
    const reactionTarget0 = payload.clientContactId;
    if (reactionTarget0) await __runClientContactReassignClient(ctx, { docId: reactionTarget0 } as any);
    return { ...doc, ...updates };
}

export const ClientContact_stageClientMerge = mutation({
  args: {
    docId: v.id("clientContacts"),
    clientMergeId: v.string(),
    clientId: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runClientContactStageClientMerge(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ClientContact_stageClientMerge", __result);
    }
    return __result;
  },
});

async function __runClientContactUpdateDetails(ctx: MutationCtx, { docId, givenName, familyName, title, email, phone, mobile, isBillingContact, notes, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("ClientContact not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("ClientContact not found");
    const doc = await __decryptDoc(ctx, "ClientContact", ["email","phone","mobile"], __storedDoc) as Record<string, any>;
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may read client contacts");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may write client contacts through commands");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may execute client contact commands");
    if (!((doc.status === "active"))) throw new Error("Guard 0 failed");
    if (!((doc.addedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((((givenName).trim()).length > 0))) throw new Error("Contact given name is required");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      givenName: givenName,
      familyName: ((familyName != null) ? familyName : doc.familyName),
      title: ((title != null) ? title : doc.title),
      email: ((email != null) ? email : doc.email),
      phone: ((phone != null) ? phone : doc.phone),
      mobile: ((mobile != null) ? mobile : doc.mobile),
      isBillingContact: ((isBillingContact != null) ? isBillingContact : doc.isBillingContact),
      notes: ((notes != null) ? notes : doc.notes),
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "ClientContact", ["email","phone","mobile"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, clientContactId: docId, tenantId: __after.tenantId, clientId: __after.clientId, _subject: { entity: "ClientContact", command: "updateDetails", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ClientContactDetailsUpdated", entity: "ClientContact", entityId: docId, payload: { clientContactId: docId, tenantId: __after.tenantId, clientId: __after.clientId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ClientContact_updateDetails = mutation({
  args: {
    docId: v.id("clientContacts"),
    givenName: v.string(),
    familyName: v.optional(v.string()),
    title: v.optional(v.string()),
    email: v.optional(v.string()),
    phone: v.optional(v.string()),
    mobile: v.optional(v.string()),
    isBillingContact: v.optional(v.boolean()),
    notes: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runClientContactUpdateDetails(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ClientContact_updateDetails", __result);
    }
    return __result;
  },
});

async function __runClientMergeMerge(ctx: MutationCtx, { docId, primaryClientId, duplicateClientId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ClientMerge not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ClientMerge not found");
    const __rel_primaryClient = await __resolveRelation(ctx, "clients", [__auth.tenantId, doc.primaryClientId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_duplicateClient = await __resolveRelation(ctx, "clients", [__auth.tenantId, doc.duplicateClientId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).primaryClientId;
      ((doc as any) as any).primaryClient = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    {
      const __fk = ((doc as any) as any).duplicateClientId;
      ((doc as any) as any).duplicateClient = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may review client merge history");
    if (!(checkRole(user, "adminAccess"))) throw new Error("Administrators may consolidate client accounts and their financial history");
    if (!(checkRole(user, "adminAccess"))) throw new Error("Administrators may consolidate client accounts and their financial history");
    if (!((doc.mergedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(checkRole(user, "adminAccess"))) throw new Error("Guard 2 failed");
    if (!((__rel_primaryClient != null))) throw new Error("Guard 3 failed");
    if (!((__rel_duplicateClient != null))) throw new Error("Guard 4 failed");
    if (!((__rel_primaryClient.status === "active"))) throw new Error("Guard 5 failed");
    if (!((__rel_primaryClient.deletedAt == null))) throw new Error("Guard 6 failed");
    if (!((__rel_duplicateClient.status === "active"))) throw new Error("Guard 7 failed");
    if (!((__rel_duplicateClient.deletedAt == null))) throw new Error("Guard 8 failed");
    if (!((primaryClientId === doc.primaryClientId))) throw new Error("Primary client must match the seeded account");
    if (!((duplicateClientId === doc.duplicateClientId))) throw new Error("Duplicate client must match the seeded account");
    if (!((primaryClientId !== duplicateClientId))) throw new Error("Choose two different clients");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      primaryClientId: primaryClientId,
      duplicateClientId: duplicateClientId,
      mergedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, clientMergeId: docId, tenantId: __after.tenantId, primaryClientId: primaryClientId, duplicateClientId: duplicateClientId, _subject: { entity: "ClientMerge", command: "merge", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ClientMergeCompleted", entity: "ClientMerge", entityId: docId, payload: { clientMergeId: docId, tenantId: __after.tenantId, primaryClientId: primaryClientId, duplicateClientId: duplicateClientId }, createdAt: Date.now() });
    // Reactions
    const __fanId0 = payload.duplicateClientId;
    const __fanDoc0 = __fanId0 != null && __fanId0 !== "" ? await ctx.db.get(__fanId0 as any) : null;
    const fanRows0 = __fanDoc0 != null && (__fanDoc0 as any).deletedAt == null ? [__fanDoc0] : [];
    for (const __row of fanRows0) {
      const target = __row;
      await __runClientStageClientMerge(ctx, { docId: (__row as any)._id, clientMergeId: payload.clientMergeId, primaryClientId: payload.primaryClientId } as any);
    }
    const fanRows1 = (await ctx.db.query("clientContacts").withIndex("by_clientId", (q) => q.eq("clientId", payload.duplicateClientId)).collect()).filter((d) => (d as any).deletedAt == null);
    for (const __row of fanRows1) {
      const target = __row;
      await __runClientContactStageClientMerge(ctx, { docId: (__row as any)._id, clientMergeId: payload.clientMergeId, clientId: payload.primaryClientId } as any);
    }
    const fanRows2 = (await ctx.db.query("contracts").withIndex("by_clientId", (q) => q.eq("clientId", payload.duplicateClientId)).collect()).filter((d) => (d as any).deletedAt == null);
    for (const __row of fanRows2) {
      const target = __row;
      await __runContractStageClientMerge(ctx, { docId: (__row as any)._id, clientMergeId: payload.clientMergeId, clientId: payload.primaryClientId } as any);
    }
    const fanRows3 = (await ctx.db.query("creditMemos").withIndex("by_clientId", (q) => q.eq("clientId", payload.duplicateClientId)).collect()).filter((d) => (d as any).deletedAt == null);
    for (const __row of fanRows3) {
      const target = __row;
      await __runCreditMemoStageClientMerge(ctx, { docId: (__row as any)._id, clientMergeId: payload.clientMergeId, clientId: payload.primaryClientId } as any);
    }
    const fanRows4 = (await ctx.db.query("events").withIndex("by_clientId", (q) => q.eq("clientId", payload.duplicateClientId)).collect()).filter((d) => (d as any).deletedAt == null);
    for (const __row of fanRows4) {
      const target = __row;
      await __runEventStageClientMerge(ctx, { docId: (__row as any)._id, clientMergeId: payload.clientMergeId, clientId: payload.primaryClientId } as any);
    }
    const fanRows5 = (await ctx.db.query("invoices").withIndex("by_clientId", (q) => q.eq("clientId", payload.duplicateClientId)).collect()).filter((d) => (d as any).deletedAt == null);
    for (const __row of fanRows5) {
      const target = __row;
      await __runInvoiceStageClientMerge(ctx, { docId: (__row as any)._id, clientMergeId: payload.clientMergeId, clientId: payload.primaryClientId } as any);
    }
    const fanRows6 = (await ctx.db.query("payments").withIndex("by_clientId", (q) => q.eq("clientId", payload.duplicateClientId)).collect()).filter((d) => (d as any).deletedAt == null);
    for (const __row of fanRows6) {
      const target = __row;
      await __runPaymentStageClientMerge(ctx, { docId: (__row as any)._id, clientMergeId: payload.clientMergeId, clientId: payload.primaryClientId } as any);
    }
    const fanRows7 = (await ctx.db.query("paymentMethods").withIndex("by_clientId", (q) => q.eq("clientId", payload.duplicateClientId)).collect()).filter((d) => (d as any).deletedAt == null);
    for (const __row of fanRows7) {
      const target = __row;
      await __runPaymentMethodStageClientMerge(ctx, { docId: (__row as any)._id, clientMergeId: payload.clientMergeId, clientId: payload.primaryClientId } as any);
    }
    const fanRows8 = (await ctx.db.query("proposals").withIndex("by_clientId", (q) => q.eq("clientId", payload.duplicateClientId)).collect()).filter((d) => (d as any).deletedAt == null);
    for (const __row of fanRows8) {
      const target = __row;
      await __runProposalStageClientMerge(ctx, { docId: (__row as any)._id, clientMergeId: payload.clientMergeId, clientId: payload.primaryClientId } as any);
    }
    return { ...doc, ...updates };
}

export const ClientMerge_merge = mutation({
  args: {
    docId: v.id("clientMerges"),
    primaryClientId: v.string(),
    duplicateClientId: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runClientMergeMerge(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ClientMerge_merge", __result);
    }
    return __result;
  },
});

export const ClientMerge_createViaMerge = mutation({
  args: {
    primaryClientId: v.string(),
    duplicateClientId: v.string(),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { primaryClientId, duplicateClientId } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      createdAt: Date.now(),
      updatedAt: Date.now(),
      duplicateClientId: args.duplicateClientId,
      primaryClientId: args.primaryClientId
    };
    const __rel_primaryClient = await __resolveRelation(ctx, "clients", [__auth.tenantId, __draft.primaryClientId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_duplicateClient = await __resolveRelation(ctx, "clients", [__auth.tenantId, __draft.duplicateClientId], ["tenantId","id"], "tenantId", __auth.tenantId);
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may review client merge history");
    if (!(checkRole(user, "adminAccess"))) throw new Error("Administrators may consolidate client accounts and their financial history");
    if (!(checkRole(user, "adminAccess"))) throw new Error("Administrators may consolidate client accounts and their financial history");
    if (!((__draft.mergedAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(checkRole(user, "adminAccess"))) throw new Error("Guard 2 failed");
    if (!((__rel_primaryClient != null))) throw new Error("Guard 3 failed");
    if (!((__rel_duplicateClient != null))) throw new Error("Guard 4 failed");
    if (!((__rel_primaryClient.status === "active"))) throw new Error("Guard 5 failed");
    if (!((__rel_primaryClient.deletedAt == null))) throw new Error("Guard 6 failed");
    if (!((__rel_duplicateClient.status === "active"))) throw new Error("Guard 7 failed");
    if (!((__rel_duplicateClient.deletedAt == null))) throw new Error("Guard 8 failed");
    if (!((primaryClientId === __draft.primaryClientId))) throw new Error("Primary client must match the seeded account");
    if (!((duplicateClientId === __draft.duplicateClientId))) throw new Error("Duplicate client must match the seeded account");
    if (!((primaryClientId !== duplicateClientId))) throw new Error("Choose two different clients");
    const doc: Record<string, any> = {
      ...__draft,
      primaryClientId: primaryClientId,
      duplicateClientId: duplicateClientId,
      mergedAt: Date.now(),
      version: 1,
    };
    const docId = await ctx.db.insert("clientMerges", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, clientMergeId: docId, tenantId: doc.tenantId, primaryClientId: doc.primaryClientId, duplicateClientId: doc.duplicateClientId, _subject: { entity: "ClientMerge", command: "merge", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ClientMergeCompleted", entity: "ClientMerge", entityId: docId, payload: { clientMergeId: docId, tenantId: doc.tenantId, primaryClientId: doc.primaryClientId, duplicateClientId: doc.duplicateClientId }, createdAt: Date.now() });
    // Reactions
    const __fanId0 = payload.duplicateClientId;
    const __fanDoc0 = __fanId0 != null && __fanId0 !== "" ? await ctx.db.get(__fanId0 as any) : null;
    const fanRows0 = __fanDoc0 != null && (__fanDoc0 as any).deletedAt == null ? [__fanDoc0] : [];
    for (const __row of fanRows0) {
      const target = __row;
      await __runClientStageClientMerge(ctx, { docId: (__row as any)._id, clientMergeId: payload.clientMergeId, primaryClientId: payload.primaryClientId } as any);
    }
    const fanRows1 = (await ctx.db.query("clientContacts").withIndex("by_clientId", (q) => q.eq("clientId", payload.duplicateClientId)).collect()).filter((d) => (d as any).deletedAt == null);
    for (const __row of fanRows1) {
      const target = __row;
      await __runClientContactStageClientMerge(ctx, { docId: (__row as any)._id, clientMergeId: payload.clientMergeId, clientId: payload.primaryClientId } as any);
    }
    const fanRows2 = (await ctx.db.query("contracts").withIndex("by_clientId", (q) => q.eq("clientId", payload.duplicateClientId)).collect()).filter((d) => (d as any).deletedAt == null);
    for (const __row of fanRows2) {
      const target = __row;
      await __runContractStageClientMerge(ctx, { docId: (__row as any)._id, clientMergeId: payload.clientMergeId, clientId: payload.primaryClientId } as any);
    }
    const fanRows3 = (await ctx.db.query("creditMemos").withIndex("by_clientId", (q) => q.eq("clientId", payload.duplicateClientId)).collect()).filter((d) => (d as any).deletedAt == null);
    for (const __row of fanRows3) {
      const target = __row;
      await __runCreditMemoStageClientMerge(ctx, { docId: (__row as any)._id, clientMergeId: payload.clientMergeId, clientId: payload.primaryClientId } as any);
    }
    const fanRows4 = (await ctx.db.query("events").withIndex("by_clientId", (q) => q.eq("clientId", payload.duplicateClientId)).collect()).filter((d) => (d as any).deletedAt == null);
    for (const __row of fanRows4) {
      const target = __row;
      await __runEventStageClientMerge(ctx, { docId: (__row as any)._id, clientMergeId: payload.clientMergeId, clientId: payload.primaryClientId } as any);
    }
    const fanRows5 = (await ctx.db.query("invoices").withIndex("by_clientId", (q) => q.eq("clientId", payload.duplicateClientId)).collect()).filter((d) => (d as any).deletedAt == null);
    for (const __row of fanRows5) {
      const target = __row;
      await __runInvoiceStageClientMerge(ctx, { docId: (__row as any)._id, clientMergeId: payload.clientMergeId, clientId: payload.primaryClientId } as any);
    }
    const fanRows6 = (await ctx.db.query("payments").withIndex("by_clientId", (q) => q.eq("clientId", payload.duplicateClientId)).collect()).filter((d) => (d as any).deletedAt == null);
    for (const __row of fanRows6) {
      const target = __row;
      await __runPaymentStageClientMerge(ctx, { docId: (__row as any)._id, clientMergeId: payload.clientMergeId, clientId: payload.primaryClientId } as any);
    }
    const fanRows7 = (await ctx.db.query("paymentMethods").withIndex("by_clientId", (q) => q.eq("clientId", payload.duplicateClientId)).collect()).filter((d) => (d as any).deletedAt == null);
    for (const __row of fanRows7) {
      const target = __row;
      await __runPaymentMethodStageClientMerge(ctx, { docId: (__row as any)._id, clientMergeId: payload.clientMergeId, clientId: payload.primaryClientId } as any);
    }
    const fanRows8 = (await ctx.db.query("proposals").withIndex("by_clientId", (q) => q.eq("clientId", payload.duplicateClientId)).collect()).filter((d) => (d as any).deletedAt == null);
    for (const __row of fanRows8) {
      const target = __row;
      await __runProposalStageClientMerge(ctx, { docId: (__row as any)._id, clientMergeId: payload.clientMergeId, clientId: payload.primaryClientId } as any);
    }
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ClientMerge_createViaMerge", __result);
    }
    return __result;
  },
});

async function __runClientOutreachTaskComplete(ctx: MutationCtx, { docId, note, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ClientOutreachTask not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ClientOutreachTask not found");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may read client follow-up reminders");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may write client follow-up reminders through commands");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may execute client follow-up commands");
    if (!((doc.status === "open"))) throw new Error("Guard 0 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "completed";
        const __allowed: Record<string, string[]> = { "open": ["completed", "dismissed"], "completed": [], "dismissed": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "completed",
      resolvedAt: Date.now(),
      resolutionNote: note,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, reminderId: docId, tenantId: __after.tenantId, clientId: __after.clientId, _subject: { entity: "ClientOutreachTask", command: "complete", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ClientOutreachTaskCompleted", entity: "ClientOutreachTask", entityId: docId, payload: { reminderId: docId, tenantId: __after.tenantId, clientId: __after.clientId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ClientOutreachTask_complete = mutation({
  args: {
    docId: v.id("clientOutreachTasks"),
    note: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runClientOutreachTaskComplete(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ClientOutreachTask_complete", __result);
    }
    return __result;
  },
});

async function __runClientOutreachTaskDismiss(ctx: MutationCtx, { docId, note, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ClientOutreachTask not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ClientOutreachTask not found");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may read client follow-up reminders");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may write client follow-up reminders through commands");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may execute client follow-up commands");
    if (!((doc.status === "open"))) throw new Error("Guard 0 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "dismissed";
        const __allowed: Record<string, string[]> = { "open": ["completed", "dismissed"], "completed": [], "dismissed": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "dismissed",
      resolvedAt: Date.now(),
      resolutionNote: note,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, reminderId: docId, tenantId: __after.tenantId, clientId: __after.clientId, _subject: { entity: "ClientOutreachTask", command: "dismiss", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ClientOutreachTaskDismissed", entity: "ClientOutreachTask", entityId: docId, payload: { reminderId: docId, tenantId: __after.tenantId, clientId: __after.clientId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ClientOutreachTask_dismiss = mutation({
  args: {
    docId: v.id("clientOutreachTasks"),
    note: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runClientOutreachTaskDismiss(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ClientOutreachTask_dismiss", __result);
    }
    return __result;
  },
});

async function __runClientOutreachTaskOpen(ctx: MutationCtx, { docId, clientId, reason, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ClientOutreachTask not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ClientOutreachTask not found");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may read client follow-up reminders");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may write client follow-up reminders through commands");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may execute client follow-up commands");
    if (!((doc.openedAt == null))) throw new Error("Guard 0 failed");
    if (!((((reason).trim()).length > 0))) throw new Error("Follow-up reason is required");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "open";
        const __allowed: Record<string, string[]> = { "open": ["completed", "dismissed"], "completed": [], "dismissed": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      clientId: clientId,
      reason: reason,
      status: "open",
      openedById: user.id,
      openedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, reminderId: docId, tenantId: __after.tenantId, clientId: clientId, _subject: { entity: "ClientOutreachTask", command: "open", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ClientOutreachTaskOpened", entity: "ClientOutreachTask", entityId: docId, payload: { reminderId: docId, tenantId: __after.tenantId, clientId: clientId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ClientOutreachTask_open = mutation({
  args: {
    docId: v.id("clientOutreachTasks"),
    clientId: v.string(),
    reason: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runClientOutreachTaskOpen(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ClientOutreachTask_open", __result);
    }
    return __result;
  },
});

export const ClientOutreachTask_createViaOpen = mutation({
  args: {
    clientId: v.string(),
    reason: v.string(),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { clientId, reason } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      createdAt: Date.now(),
      updatedAt: Date.now(),
      status: "open",
      clientId: args.clientId,
      reason: args.reason
    };
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may read client follow-up reminders");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may write client follow-up reminders through commands");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may execute client follow-up commands");
    if (!((__draft.openedAt == null))) throw new Error("Guard 0 failed");
    if (!((((reason).trim()).length > 0))) throw new Error("Follow-up reason is required");
    const doc: Record<string, any> = {
      ...__draft,
      clientId: clientId,
      reason: reason,
      status: "open",
      openedById: user.id,
      openedAt: Date.now(),
      version: 1,
    };
    const docId = await ctx.db.insert("clientOutreachTasks", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, reminderId: docId, tenantId: doc.tenantId, clientId: doc.clientId, _subject: { entity: "ClientOutreachTask", command: "open", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ClientOutreachTaskOpened", entity: "ClientOutreachTask", entityId: docId, payload: { reminderId: docId, tenantId: doc.tenantId, clientId: doc.clientId }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ClientOutreachTask_createViaOpen", __result);
    }
    return __result;
  },
});

async function __runComponentDraft(ctx: MutationCtx, { docId, name, yieldQuantity, yieldUnit, servesPerYield, batchMultiplier, category, cuisine, description, instructions, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Component not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Component not found");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may read components");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write components through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute component commands");
    if (!((doc.draftedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((((name).trim()).length > 0))) throw new Error("Component name is required");
    if (!((yieldQuantity > 0))) throw new Error("Component yield quantity must be positive");
    if (!(((servesPerYield == null) || (servesPerYield > 0)))) throw new Error("Serves per yield must be a positive integer");
    if (!(((batchMultiplier == null) || (batchMultiplier > 0)))) throw new Error("Component batch multiplier must be positive");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      name: name,
      yieldQuantity: yieldQuantity,
      yieldUnit: yieldUnit,
      servesPerYield: ((servesPerYield != null) ? servesPerYield : 1),
      batchMultiplier: ((batchMultiplier != null) ? batchMultiplier : 1),
      category: category,
      cuisine: cuisine,
      description: description,
      instructions: instructions,
      versionNumber: 1,
      draftedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, componentId: docId, tenantId: __after.tenantId, name: name, yieldQuantity: yieldQuantity, yieldUnit: yieldUnit, servesPerYield: ((servesPerYield != null) ? servesPerYield : 1), batchMultiplier: ((batchMultiplier != null) ? batchMultiplier : 1), versionNumber: 1, _subject: { entity: "Component", command: "draft", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ComponentDrafted", entity: "Component", entityId: docId, payload: { componentId: docId, tenantId: __after.tenantId, name: name, yieldQuantity: yieldQuantity, yieldUnit: yieldUnit, servesPerYield: ((servesPerYield != null) ? servesPerYield : 1), batchMultiplier: ((batchMultiplier != null) ? batchMultiplier : 1), versionNumber: 1 }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Component_draft = mutation({
  args: {
    docId: v.id("components"),
    name: v.string(),
    yieldQuantity: v.number(),
    yieldUnit: v.any(),
    servesPerYield: v.optional(v.number()),
    batchMultiplier: v.optional(v.number()),
    category: v.optional(v.string()),
    cuisine: v.optional(v.string()),
    description: v.optional(v.string()),
    instructions: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runComponentDraft(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Component_draft", __result);
    }
    return __result;
  },
});

export const Component_createViaDraft = mutation({
  args: {
    name: v.string(),
    yieldQuantity: v.number(),
    yieldUnit: v.any(),
    servesPerYield: v.optional(v.number()),
    batchMultiplier: v.optional(v.number()),
    category: v.optional(v.string()),
    cuisine: v.optional(v.string()),
    description: v.optional(v.string()),
    instructions: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { name, yieldQuantity, yieldUnit, servesPerYield, batchMultiplier, category, cuisine, description, instructions } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      servesPerYield: args.servesPerYield !== undefined ? args.servesPerYield : 1,
      batchMultiplier: args.batchMultiplier !== undefined ? args.batchMultiplier : 1,
      status: "draft",
      createdAt: Date.now(),
      updatedAt: Date.now(),
      category: args.category,
      cuisine: args.cuisine,
      description: args.description,
      instructions: args.instructions,
      name: args.name,
      yieldQuantity: args.yieldQuantity,
      yieldUnit: args.yieldUnit
    };
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may read components");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write components through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute component commands");
    if (!((__draft.draftedAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((((name).trim()).length > 0))) throw new Error("Component name is required");
    if (!((yieldQuantity > 0))) throw new Error("Component yield quantity must be positive");
    if (!(((servesPerYield == null) || (servesPerYield > 0)))) throw new Error("Serves per yield must be a positive integer");
    if (!(((batchMultiplier == null) || (batchMultiplier > 0)))) throw new Error("Component batch multiplier must be positive");
    const doc: Record<string, any> = {
      ...__draft,
      name: name,
      yieldQuantity: yieldQuantity,
      yieldUnit: yieldUnit,
      servesPerYield: ((servesPerYield != null) ? servesPerYield : 1),
      batchMultiplier: ((batchMultiplier != null) ? batchMultiplier : 1),
      category: category,
      cuisine: cuisine,
      description: description,
      instructions: instructions,
      versionNumber: 1,
      draftedAt: Date.now(),
      version: 1,
    };
    const docId = await ctx.db.insert("components", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, componentId: docId, tenantId: doc.tenantId, name: doc.name, yieldQuantity: doc.yieldQuantity, yieldUnit: doc.yieldUnit, servesPerYield: ((doc.servesPerYield != null) ? doc.servesPerYield : 1), batchMultiplier: ((doc.batchMultiplier != null) ? doc.batchMultiplier : 1), versionNumber: 1, _subject: { entity: "Component", command: "draft", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ComponentDrafted", entity: "Component", entityId: docId, payload: { componentId: docId, tenantId: doc.tenantId, name: doc.name, yieldQuantity: doc.yieldQuantity, yieldUnit: doc.yieldUnit, servesPerYield: ((doc.servesPerYield != null) ? doc.servesPerYield : 1), batchMultiplier: ((doc.batchMultiplier != null) ? doc.batchMultiplier : 1), versionNumber: 1 }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Component_createViaDraft", __result);
    }
    return __result;
  },
});

async function __runComponentPublishVersion(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Component not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Component not found");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may read components");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write components through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute component commands");
    if (!((doc.status === "draft"))) throw new Error("Guard 0 failed");
    if (!((doc.draftedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!(checkRole(user, "kitchenManageAccess"))) throw new Error("Guard 3 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "published";
        const __allowed: Record<string, string[]> = { "draft": ["published", "retired"], "published": ["draft", "retired"], "retired": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "published",
      publishedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, componentId: docId, tenantId: __after.tenantId, versionNumber: __after.versionNumber, yieldQuantity: __after.yieldQuantity, yieldUnit: __after.yieldUnit, batchMultiplier: __after.batchMultiplier, _subject: { entity: "Component", command: "publishVersion", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ComponentVersionPublished", entity: "Component", entityId: docId, payload: { componentId: docId, tenantId: __after.tenantId, versionNumber: __after.versionNumber, yieldQuantity: __after.yieldQuantity, yieldUnit: __after.yieldUnit, batchMultiplier: __after.batchMultiplier }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Component_publishVersion = mutation({
  args: {
    docId: v.id("components"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runComponentPublishVersion(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Component_publishVersion", __result);
    }
    return __result;
  },
});

async function __runComponentPurge(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Component not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Component not found");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may read components");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write components through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute component commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!(checkRole(user, "kitchenManageAccess"))) throw new Error("Guard 1 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "retired";
        const __allowed: Record<string, string[]> = { "draft": ["published", "retired"], "published": ["draft", "retired"], "retired": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "retired",
      retiredAt: ((doc.retiredAt != null) ? doc.retiredAt : Date.now()),
      retirementReason: "Deleted",
      deletedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, componentId: docId, tenantId: __after.tenantId, reason: "Deleted", _subject: { entity: "Component", command: "purge", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ComponentPurged", entity: "Component", entityId: docId, payload: { componentId: docId, tenantId: __after.tenantId, reason: "Deleted" }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Component_purge = mutation({
  args: {
    docId: v.id("components"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runComponentPurge(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Component_purge", __result);
    }
    return __result;
  },
});

async function __runComponentRetire(ctx: MutationCtx, { docId, reason, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Component not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Component not found");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may read components");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write components through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute component commands");
    if (!(((doc.status === "draft") || (doc.status === "published")))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(checkRole(user, "kitchenManageAccess"))) throw new Error("Guard 2 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "retired";
        const __allowed: Record<string, string[]> = { "draft": ["published", "retired"], "published": ["draft", "retired"], "retired": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "retired",
      retiredAt: Date.now(),
      retirementReason: ((reason != null) ? reason : "Deleted"),
      deletedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, componentId: docId, tenantId: __after.tenantId, reason: ((reason != null) ? reason : "Deleted"), _subject: { entity: "Component", command: "retire", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ComponentRetired", entity: "Component", entityId: docId, payload: { componentId: docId, tenantId: __after.tenantId, reason: ((reason != null) ? reason : "Deleted") }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Component_retire = mutation({
  args: {
    docId: v.id("components"),
    reason: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runComponentRetire(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Component_retire", __result);
    }
    return __result;
  },
});

async function __runComponentRetract(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Component not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Component not found");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may read components");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write components through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute component commands");
    if (!((doc.status === "published"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(checkRole(user, "kitchenManageAccess"))) throw new Error("Guard 2 failed");
    const nextVersion = (doc.versionNumber + 1);
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "draft";
        const __allowed: Record<string, string[]> = { "draft": ["published", "retired"], "published": ["draft", "retired"], "retired": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "draft",
      publishedAt: null,
      versionNumber: nextVersion,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, componentId: docId, tenantId: __after.tenantId, versionNumber: nextVersion, _subject: { entity: "Component", command: "retract", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ComponentVersionRetracted", entity: "Component", entityId: docId, payload: { componentId: docId, tenantId: __after.tenantId, versionNumber: nextVersion }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Component_retract = mutation({
  args: {
    docId: v.id("components"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runComponentRetract(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Component_retract", __result);
    }
    return __result;
  },
});

async function __runComponentReviseDraft(ctx: MutationCtx, { docId, name, yieldQuantity, yieldUnit, batchMultiplier, servesPerYield, category, cuisine, description, instructions, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Component not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Component not found");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may read components");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write components through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute component commands");
    if (!((doc.status === "draft"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((((name).trim()).length > 0))) throw new Error("Component name is required");
    if (!((yieldQuantity > 0))) throw new Error("Component yield quantity must be positive");
    if (!(((servesPerYield == null) || (servesPerYield > 0)))) throw new Error("Serves per yield must be a positive integer");
    if (!((batchMultiplier > 0))) throw new Error("Component batch multiplier must be positive");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      name: name,
      yieldQuantity: yieldQuantity,
      yieldUnit: yieldUnit,
      servesPerYield: ((servesPerYield != null) ? servesPerYield : doc.servesPerYield),
      batchMultiplier: batchMultiplier,
      category: category,
      cuisine: cuisine,
      description: description,
      instructions: instructions,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, componentId: docId, tenantId: __after.tenantId, name: name, yieldQuantity: yieldQuantity, yieldUnit: yieldUnit, servesPerYield: ((servesPerYield != null) ? servesPerYield : __after.servesPerYield), batchMultiplier: batchMultiplier, versionNumber: __after.versionNumber, _subject: { entity: "Component", command: "reviseDraft", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ComponentDraftRevised", entity: "Component", entityId: docId, payload: { componentId: docId, tenantId: __after.tenantId, name: name, yieldQuantity: yieldQuantity, yieldUnit: yieldUnit, servesPerYield: ((servesPerYield != null) ? servesPerYield : __after.servesPerYield), batchMultiplier: batchMultiplier, versionNumber: __after.versionNumber }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Component_reviseDraft = mutation({
  args: {
    docId: v.id("components"),
    name: v.string(),
    yieldQuantity: v.number(),
    yieldUnit: v.any(),
    batchMultiplier: v.number(),
    servesPerYield: v.optional(v.number()),
    category: v.optional(v.string()),
    cuisine: v.optional(v.string()),
    description: v.optional(v.string()),
    instructions: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runComponentReviseDraft(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Component_reviseDraft", __result);
    }
    return __result;
  },
});

async function __runComponentSetServesPerYield(ctx: MutationCtx, { docId, servesPerYield, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Component not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Component not found");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may read components");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write components through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute component commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!((servesPerYield > 0))) throw new Error("Serves per yield must be a positive integer");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      servesPerYield: servesPerYield,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, componentId: docId, tenantId: __after.tenantId, servesPerYield: servesPerYield, _subject: { entity: "Component", command: "setServesPerYield", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ComponentServesPerYieldSet", entity: "Component", entityId: docId, payload: { componentId: docId, tenantId: __after.tenantId, servesPerYield: servesPerYield }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Component_setServesPerYield = mutation({
  args: {
    docId: v.id("components"),
    servesPerYield: v.number(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runComponentSetServesPerYield(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Component_setServesPerYield", __result);
    }
    return __result;
  },
});

async function __runComponentImportApproveReview(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ComponentImport not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ComponentImport not found");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may read component imports");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write component imports through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute component import commands");
    if (!((doc.status === "reviewing"))) throw new Error("Guard 0 failed");
    if (!((doc.reviewStartedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((doc.parsedLineCount > 0))) throw new Error("Guard 3 failed");
    if (!((doc.resolvedLineCount === doc.parsedLineCount))) throw new Error("Guard 4 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "ready";
        const __allowed: Record<string, string[]> = { "uploaded": ["parsed", "failed", "cancelled"], "parsed": ["reviewing", "failed", "cancelled"], "reviewing": ["ready", "failed", "cancelled"], "ready": ["finalizing", "reviewing", "failed", "cancelled"], "finalizing": ["completed", "failed", "reviewing", "cancelled"], "completed": [], "failed": ["reviewing", "cancelled"], "cancelled": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "ready",
      readyAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, componentImportId: docId, tenantId: __after.tenantId, parsedLineCount: __after.parsedLineCount, _subject: { entity: "ComponentImport", command: "approveReview", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ComponentImportReviewApproved", entity: "ComponentImport", entityId: docId, payload: { componentImportId: docId, tenantId: __after.tenantId, parsedLineCount: __after.parsedLineCount }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ComponentImport_approveReview = mutation({
  args: {
    docId: v.id("componentImports"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runComponentImportApproveReview(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ComponentImport_approveReview", __result);
    }
    return __result;
  },
});

async function __runComponentImportBeginFinalization(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ComponentImport not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ComponentImport not found");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may read component imports");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write component imports through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute component import commands");
    if (!((doc.status === "ready"))) throw new Error("Guard 0 failed");
    if (!((doc.readyAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "finalizing";
        const __allowed: Record<string, string[]> = { "uploaded": ["parsed", "failed", "cancelled"], "parsed": ["reviewing", "failed", "cancelled"], "reviewing": ["ready", "failed", "cancelled"], "ready": ["finalizing", "reviewing", "failed", "cancelled"], "finalizing": ["completed", "failed", "reviewing", "cancelled"], "completed": [], "failed": ["reviewing", "cancelled"], "cancelled": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "finalizing",
      finalizationStartedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, componentImportId: docId, tenantId: __after.tenantId, _subject: { entity: "ComponentImport", command: "beginFinalization", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ComponentImportFinalizationStarted", entity: "ComponentImport", entityId: docId, payload: { componentImportId: docId, tenantId: __after.tenantId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ComponentImport_beginFinalization = mutation({
  args: {
    docId: v.id("componentImports"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runComponentImportBeginFinalization(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ComponentImport_beginFinalization", __result);
    }
    return __result;
  },
});

async function __runComponentImportBeginReview(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ComponentImport not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ComponentImport not found");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may read component imports");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write component imports through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute component import commands");
    if (!((doc.status === "parsed"))) throw new Error("Guard 0 failed");
    if (!((doc.parsedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "reviewing";
        const __allowed: Record<string, string[]> = { "uploaded": ["parsed", "failed", "cancelled"], "parsed": ["reviewing", "failed", "cancelled"], "reviewing": ["ready", "failed", "cancelled"], "ready": ["finalizing", "reviewing", "failed", "cancelled"], "finalizing": ["completed", "failed", "reviewing", "cancelled"], "completed": [], "failed": ["reviewing", "cancelled"], "cancelled": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "reviewing",
      reviewStartedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, componentImportId: docId, tenantId: __after.tenantId, _subject: { entity: "ComponentImport", command: "beginReview", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ComponentImportReviewStarted", entity: "ComponentImport", entityId: docId, payload: { componentImportId: docId, tenantId: __after.tenantId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ComponentImport_beginReview = mutation({
  args: {
    docId: v.id("componentImports"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runComponentImportBeginReview(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ComponentImport_beginReview", __result);
    }
    return __result;
  },
});

async function __runComponentImportCancel(ctx: MutationCtx, { docId, reason, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ComponentImport not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ComponentImport not found");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may read component imports");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write component imports through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute component import commands");
    if (!((doc.status !== "completed"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((((reason).trim()).length > 0))) throw new Error("Cancellation reason is required");
    const previousStatus = doc.status;
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "cancelled";
        const __allowed: Record<string, string[]> = { "uploaded": ["parsed", "failed", "cancelled"], "parsed": ["reviewing", "failed", "cancelled"], "reviewing": ["ready", "failed", "cancelled"], "ready": ["finalizing", "reviewing", "failed", "cancelled"], "finalizing": ["completed", "failed", "reviewing", "cancelled"], "completed": [], "failed": ["reviewing", "cancelled"], "cancelled": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "cancelled",
      cancelledAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, componentImportId: docId, tenantId: __after.tenantId, previousStatus: previousStatus, reason: reason, _subject: { entity: "ComponentImport", command: "cancel", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ComponentImportCancelled", entity: "ComponentImport", entityId: docId, payload: { componentImportId: docId, tenantId: __after.tenantId, previousStatus: previousStatus, reason: reason }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ComponentImport_cancel = mutation({
  args: {
    docId: v.id("componentImports"),
    reason: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runComponentImportCancel(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ComponentImport_cancel", __result);
    }
    return __result;
  },
});

async function __runComponentImportComplete(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ComponentImport not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ComponentImport not found");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may read component imports");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write component imports through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute component import commands");
    if (!((doc.status === "finalizing"))) throw new Error("Guard 0 failed");
    if (!((doc.finalizationStartedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.resultingComponentId != null))) throw new Error("Guard 2 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 3 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "completed";
        const __allowed: Record<string, string[]> = { "uploaded": ["parsed", "failed", "cancelled"], "parsed": ["reviewing", "failed", "cancelled"], "reviewing": ["ready", "failed", "cancelled"], "ready": ["finalizing", "reviewing", "failed", "cancelled"], "finalizing": ["completed", "failed", "reviewing", "cancelled"], "completed": [], "failed": ["reviewing", "cancelled"], "cancelled": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "completed",
      completedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, componentImportId: docId, tenantId: __after.tenantId, resultingComponentId: __after.resultingComponentId, _subject: { entity: "ComponentImport", command: "complete", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ComponentImportCompleted", entity: "ComponentImport", entityId: docId, payload: { componentImportId: docId, tenantId: __after.tenantId, resultingComponentId: __after.resultingComponentId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ComponentImport_complete = mutation({
  args: {
    docId: v.id("componentImports"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runComponentImportComplete(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ComponentImport_complete", __result);
    }
    return __result;
  },
});

async function __runComponentImportMarkFailed(ctx: MutationCtx, { docId, failureDetail, duringParsing, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ComponentImport not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ComponentImport not found");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may read component imports");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write component imports through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute component import commands");
    if (!((doc.status !== "completed"))) throw new Error("Guard 0 failed");
    if (!((doc.status !== "cancelled"))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((((failureDetail).trim()).length > 0))) throw new Error("Failure detail is required");
    const previousStatus = doc.status;
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "failed";
        const __allowed: Record<string, string[]> = { "uploaded": ["parsed", "failed", "cancelled"], "parsed": ["reviewing", "failed", "cancelled"], "reviewing": ["ready", "failed", "cancelled"], "ready": ["finalizing", "reviewing", "failed", "cancelled"], "finalizing": ["completed", "failed", "reviewing", "cancelled"], "completed": [], "failed": ["reviewing", "cancelled"], "cancelled": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "failed",
      failedAt: Date.now(),
      parsingFailureDetail: ((duringParsing === true) ? failureDetail : doc.parsingFailureDetail),
      finalizationFailureDetail: ((duringParsing === true) ? doc.finalizationFailureDetail : failureDetail),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, componentImportId: docId, tenantId: __after.tenantId, previousStatus: previousStatus, failureDetail: failureDetail, _subject: { entity: "ComponentImport", command: "markFailed", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ComponentImportFailed", entity: "ComponentImport", entityId: docId, payload: { componentImportId: docId, tenantId: __after.tenantId, previousStatus: previousStatus, failureDetail: failureDetail }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ComponentImport_markFailed = mutation({
  args: {
    docId: v.id("componentImports"),
    failureDetail: v.string(),
    duringParsing: v.optional(v.boolean()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runComponentImportMarkFailed(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ComponentImport_markFailed", __result);
    }
    return __result;
  },
});

async function __runComponentImportRecordComponent(ctx: MutationCtx, { docId, resultingComponentId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ComponentImport not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ComponentImport not found");
    const __rel_resultingComponent = await __resolveRelation(ctx, "components", [__auth.tenantId, doc.resultingComponentId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).resultingComponentId;
      ((doc as any) as any).resultingComponent = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may read component imports");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write component imports through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute component import commands");
    if (!((doc.status === "finalizing"))) throw new Error("Guard 0 failed");
    if (!((doc.finalizationStartedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((__rel_resultingComponent != null))) throw new Error("Guard 3 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      resultingComponentId: resultingComponentId,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, componentImportId: docId, tenantId: __after.tenantId, resultingComponentId: resultingComponentId, _subject: { entity: "ComponentImport", command: "recordComponent", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ComponentImportComponentRecorded", entity: "ComponentImport", entityId: docId, payload: { componentImportId: docId, tenantId: __after.tenantId, resultingComponentId: resultingComponentId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ComponentImport_recordComponent = mutation({
  args: {
    docId: v.id("componentImports"),
    resultingComponentId: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runComponentImportRecordComponent(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ComponentImport_recordComponent", __result);
    }
    return __result;
  },
});

async function __runComponentImportRecordParse(ctx: MutationCtx, { docId, parsedName, parsedLineCount, parsedDescription, parsedCategory, parsedCuisine, parsedInstructions, parsedYieldQuantity, parsedYieldUnit, parsedBatchMultiplier, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ComponentImport not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ComponentImport not found");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may read component imports");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write component imports through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute component import commands");
    if (!((doc.status === "uploaded"))) throw new Error("Guard 0 failed");
    if (!((doc.uploadedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((((parsedName).trim()).length > 0))) throw new Error("Parsed component name is required");
    if (!((parsedLineCount >= 0))) throw new Error("Parsed line count cannot be negative");
    if (!(((parsedYieldQuantity == null) || (parsedYieldQuantity > 0)))) throw new Error("Parsed yield quantity must be positive when present");
    if (!(((parsedBatchMultiplier == null) || (parsedBatchMultiplier > 0)))) throw new Error("Parsed batch multiplier must be positive when present");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "parsed";
        const __allowed: Record<string, string[]> = { "uploaded": ["parsed", "failed", "cancelled"], "parsed": ["reviewing", "failed", "cancelled"], "reviewing": ["ready", "failed", "cancelled"], "ready": ["finalizing", "reviewing", "failed", "cancelled"], "finalizing": ["completed", "failed", "reviewing", "cancelled"], "completed": [], "failed": ["reviewing", "cancelled"], "cancelled": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      parsedName: parsedName,
      parsedDescription: parsedDescription,
      parsedCategory: parsedCategory,
      parsedCuisine: parsedCuisine,
      parsedInstructions: parsedInstructions,
      parsedYieldQuantity: parsedYieldQuantity,
      parsedYieldUnit: parsedYieldUnit,
      parsedBatchMultiplier: ((parsedBatchMultiplier != null) ? parsedBatchMultiplier : 1),
      parsedLineCount: parsedLineCount,
      resolvedLineCount: 0,
      status: "parsed",
      parsedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, componentImportId: docId, tenantId: __after.tenantId, parsedName: parsedName, parsedLineCount: parsedLineCount, _subject: { entity: "ComponentImport", command: "recordParse", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ComponentImportParsed", entity: "ComponentImport", entityId: docId, payload: { componentImportId: docId, tenantId: __after.tenantId, parsedName: parsedName, parsedLineCount: parsedLineCount }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ComponentImport_recordParse = mutation({
  args: {
    docId: v.id("componentImports"),
    parsedName: v.string(),
    parsedLineCount: v.number(),
    parsedDescription: v.optional(v.string()),
    parsedCategory: v.optional(v.string()),
    parsedCuisine: v.optional(v.string()),
    parsedInstructions: v.optional(v.string()),
    parsedYieldQuantity: v.optional(v.number()),
    parsedYieldUnit: v.optional(v.any()),
    parsedBatchMultiplier: v.optional(v.number()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runComponentImportRecordParse(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ComponentImport_recordParse", __result);
    }
    return __result;
  },
});

async function __runComponentImportRecordResolutionProgress(ctx: MutationCtx, { docId, resolvedLineCount, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ComponentImport not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ComponentImport not found");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may read component imports");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write component imports through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute component import commands");
    if (!((doc.status === "reviewing"))) throw new Error("Guard 0 failed");
    if (!((doc.reviewStartedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((resolvedLineCount >= 0))) throw new Error("Resolved line count cannot be negative");
    if (!((resolvedLineCount <= doc.parsedLineCount))) throw new Error("Resolved line count cannot exceed parsed line count");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      resolvedLineCount: resolvedLineCount,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, componentImportId: docId, tenantId: __after.tenantId, resolvedLineCount: resolvedLineCount, parsedLineCount: __after.parsedLineCount, _subject: { entity: "ComponentImport", command: "recordResolutionProgress", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ComponentImportResolutionProgressRecorded", entity: "ComponentImport", entityId: docId, payload: { componentImportId: docId, tenantId: __after.tenantId, resolvedLineCount: resolvedLineCount, parsedLineCount: __after.parsedLineCount }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ComponentImport_recordResolutionProgress = mutation({
  args: {
    docId: v.id("componentImports"),
    resolvedLineCount: v.number(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runComponentImportRecordResolutionProgress(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ComponentImport_recordResolutionProgress", __result);
    }
    return __result;
  },
});

async function __runComponentImportResumeReview(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ComponentImport not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ComponentImport not found");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may read component imports");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write component imports through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute component import commands");
    if (!(((doc.status === "failed") || (doc.status === "finalizing")))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "reviewing";
        const __allowed: Record<string, string[]> = { "uploaded": ["parsed", "failed", "cancelled"], "parsed": ["reviewing", "failed", "cancelled"], "reviewing": ["ready", "failed", "cancelled"], "ready": ["finalizing", "reviewing", "failed", "cancelled"], "finalizing": ["completed", "failed", "reviewing", "cancelled"], "completed": [], "failed": ["reviewing", "cancelled"], "cancelled": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "reviewing",
      failedAt: null,
      finalizationStartedAt: null,
      finalizationFailureDetail: null,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, componentImportId: docId, tenantId: __after.tenantId, _subject: { entity: "ComponentImport", command: "resumeReview", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ComponentImportReviewResumed", entity: "ComponentImport", entityId: docId, payload: { componentImportId: docId, tenantId: __after.tenantId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ComponentImport_resumeReview = mutation({
  args: {
    docId: v.id("componentImports"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runComponentImportResumeReview(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ComponentImport_resumeReview", __result);
    }
    return __result;
  },
});

async function __runComponentImportUpload(ctx: MutationCtx, { docId, sourceKind, rawSourceText, sourceByteCount, sourceFingerprint, sourceFilename, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ComponentImport not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ComponentImport not found");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may read component imports");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write component imports through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute component import commands");
    if (!((doc.uploadedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((((rawSourceText).trim()).length > 0))) throw new Error("Import source content is required");
    if (!((sourceByteCount >= 0))) throw new Error("Source byte count cannot be negative");
    if (!((((sourceFingerprint).trim()).length > 0))) throw new Error("Source fingerprint is required");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "uploaded";
        const __allowed: Record<string, string[]> = { "uploaded": ["parsed", "failed", "cancelled"], "parsed": ["reviewing", "failed", "cancelled"], "reviewing": ["ready", "failed", "cancelled"], "ready": ["finalizing", "reviewing", "failed", "cancelled"], "finalizing": ["completed", "failed", "reviewing", "cancelled"], "completed": [], "failed": ["reviewing", "cancelled"], "cancelled": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      sourceKind: sourceKind,
      sourceFilename: sourceFilename,
      rawSourceText: rawSourceText,
      sourceByteCount: sourceByteCount,
      sourceFingerprint: sourceFingerprint,
      status: "uploaded",
      uploadedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, componentImportId: docId, tenantId: __after.tenantId, sourceKind: sourceKind, sourceByteCount: sourceByteCount, sourceFingerprint: sourceFingerprint, _subject: { entity: "ComponentImport", command: "upload", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ComponentImportUploaded", entity: "ComponentImport", entityId: docId, payload: { componentImportId: docId, tenantId: __after.tenantId, sourceKind: sourceKind, sourceByteCount: sourceByteCount, sourceFingerprint: sourceFingerprint }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ComponentImport_upload = mutation({
  args: {
    docId: v.id("componentImports"),
    sourceKind: v.any(),
    rawSourceText: v.string(),
    sourceByteCount: v.number(),
    sourceFingerprint: v.string(),
    sourceFilename: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runComponentImportUpload(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ComponentImport_upload", __result);
    }
    return __result;
  },
});

export const ComponentImport_createViaUpload = mutation({
  args: {
    sourceKind: v.any(),
    rawSourceText: v.string(),
    sourceByteCount: v.number(),
    sourceFingerprint: v.string(),
    sourceFilename: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { sourceKind, rawSourceText, sourceByteCount, sourceFingerprint, sourceFilename } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      parsedLineCount: 0,
      resolvedLineCount: 0,
      createdAt: Date.now(),
      updatedAt: Date.now(),
      status: "uploaded",
      rawSourceText: args.rawSourceText,
      sourceByteCount: args.sourceByteCount,
      sourceFilename: args.sourceFilename,
      sourceFingerprint: args.sourceFingerprint,
      sourceKind: args.sourceKind
    };
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may read component imports");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write component imports through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute component import commands");
    if (!((__draft.uploadedAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((((rawSourceText).trim()).length > 0))) throw new Error("Import source content is required");
    if (!((sourceByteCount >= 0))) throw new Error("Source byte count cannot be negative");
    if (!((((sourceFingerprint).trim()).length > 0))) throw new Error("Source fingerprint is required");
    const doc: Record<string, any> = {
      ...__draft,
      sourceKind: sourceKind,
      sourceFilename: sourceFilename,
      rawSourceText: rawSourceText,
      sourceByteCount: sourceByteCount,
      sourceFingerprint: sourceFingerprint,
      status: "uploaded",
      uploadedAt: Date.now(),
      version: 1,
    };
    const docId = await ctx.db.insert("componentImports", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, componentImportId: docId, tenantId: doc.tenantId, sourceKind: doc.sourceKind, sourceByteCount: doc.sourceByteCount, sourceFingerprint: doc.sourceFingerprint, _subject: { entity: "ComponentImport", command: "upload", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ComponentImportUploaded", entity: "ComponentImport", entityId: docId, payload: { componentImportId: docId, tenantId: doc.tenantId, sourceKind: doc.sourceKind, sourceByteCount: doc.sourceByteCount, sourceFingerprint: doc.sourceFingerprint }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ComponentImport_createViaUpload", __result);
    }
    return __result;
  },
});

async function __runComponentImportLineAttachCreatedIngredient(ctx: MutationCtx, { docId, matchedIngredientId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ComponentImportLine not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ComponentImportLine not found");
    const __rel_matchedIngredient = await __resolveRelation(ctx, "ingredients", [__auth.tenantId, doc.matchedIngredientId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).matchedIngredientId;
      ((doc as any) as any).matchedIngredient = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may read component import lines");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write component import lines through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute component import line commands");
    if (!((doc.matchStatus === "confirmed_new"))) throw new Error("Guard 0 failed");
    if (!((doc.resolvedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!(((__rel_matchedIngredient != null) && (__rel_matchedIngredient.status === "active")))) throw new Error("Guard 3 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      matchedIngredientId: matchedIngredientId,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, componentImportLineId: docId, tenantId: __after.tenantId, importId: __after.importId, matchedIngredientId: matchedIngredientId, _subject: { entity: "ComponentImportLine", command: "attachCreatedIngredient", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ComponentImportLineCreatedIngredientAttached", entity: "ComponentImportLine", entityId: docId, payload: { componentImportLineId: docId, tenantId: __after.tenantId, importId: __after.importId, matchedIngredientId: matchedIngredientId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ComponentImportLine_attachCreatedIngredient = mutation({
  args: {
    docId: v.id("componentImportLines"),
    matchedIngredientId: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runComponentImportLineAttachCreatedIngredient(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ComponentImportLine_attachCreatedIngredient", __result);
    }
    return __result;
  },
});

async function __runComponentImportLineConfirmExisting(ctx: MutationCtx, { docId, matchedIngredientId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ComponentImportLine not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ComponentImportLine not found");
    const __rel_matchedIngredient = await __resolveRelation(ctx, "ingredients", [__auth.tenantId, doc.matchedIngredientId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).matchedIngredientId;
      ((doc as any) as any).matchedIngredient = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may read component import lines");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write component import lines through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute component import line commands");
    if (!((doc.resolvedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(((__rel_matchedIngredient != null) && (__rel_matchedIngredient.status === "active")))) throw new Error("Guard 2 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      matchStatus: "confirmed_existing",
      matchedIngredientId: matchedIngredientId,
      resolvedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, componentImportLineId: docId, tenantId: __after.tenantId, importId: __after.importId, matchedIngredientId: matchedIngredientId, _subject: { entity: "ComponentImportLine", command: "confirmExisting", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ComponentImportLineExistingConfirmed", entity: "ComponentImportLine", entityId: docId, payload: { componentImportLineId: docId, tenantId: __after.tenantId, importId: __after.importId, matchedIngredientId: matchedIngredientId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ComponentImportLine_confirmExisting = mutation({
  args: {
    docId: v.id("componentImportLines"),
    matchedIngredientId: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runComponentImportLineConfirmExisting(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ComponentImportLine_confirmExisting", __result);
    }
    return __result;
  },
});

async function __runComponentImportLineConfirmNew(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ComponentImportLine not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ComponentImportLine not found");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may read component import lines");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write component import lines through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute component import line commands");
    if (!((doc.resolvedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      matchStatus: "confirmed_new",
      matchedIngredientId: null,
      resolvedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, componentImportLineId: docId, tenantId: __after.tenantId, importId: __after.importId, _subject: { entity: "ComponentImportLine", command: "confirmNew", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ComponentImportLineNewConfirmed", entity: "ComponentImportLine", entityId: docId, payload: { componentImportLineId: docId, tenantId: __after.tenantId, importId: __after.importId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ComponentImportLine_confirmNew = mutation({
  args: {
    docId: v.id("componentImportLines"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runComponentImportLineConfirmNew(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ComponentImportLine_confirmNew", __result);
    }
    return __result;
  },
});

async function __runComponentImportLineDiscard(ctx: MutationCtx, { docId, reason, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ComponentImportLine not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ComponentImportLine not found");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may read component import lines");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write component import lines through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute component import line commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!((((reason).trim()).length > 0))) throw new Error("Discard reason is required");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      deletedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, componentImportLineId: docId, tenantId: __after.tenantId, importId: __after.importId, reason: reason, _subject: { entity: "ComponentImportLine", command: "discard", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ComponentImportLineDiscarded", entity: "ComponentImportLine", entityId: docId, payload: { componentImportLineId: docId, tenantId: __after.tenantId, importId: __after.importId, reason: reason }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ComponentImportLine_discard = mutation({
  args: {
    docId: v.id("componentImportLines"),
    reason: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runComponentImportLineDiscard(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ComponentImportLine_discard", __result);
    }
    return __result;
  },
});

async function __runComponentImportLineMarkNew(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ComponentImportLine not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ComponentImportLine not found");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may read component import lines");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write component import lines through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute component import line commands");
    if (!((doc.resolvedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      matchStatus: "new",
      matchedIngredientId: null,
      possibleMatchIngredientIds: [],
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, componentImportLineId: docId, tenantId: __after.tenantId, importId: __after.importId, _subject: { entity: "ComponentImportLine", command: "markNew", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ComponentImportLineMarkedNew", entity: "ComponentImportLine", entityId: docId, payload: { componentImportLineId: docId, tenantId: __after.tenantId, importId: __after.importId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ComponentImportLine_markNew = mutation({
  args: {
    docId: v.id("componentImportLines"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runComponentImportLineMarkNew(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ComponentImportLine_markNew", __result);
    }
    return __result;
  },
});

async function __runComponentImportLineResetResolution(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ComponentImportLine not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ComponentImportLine not found");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may read component import lines");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write component import lines through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute component import line commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      matchStatus: "unresolved",
      matchedIngredientId: null,
      possibleMatchIngredientIds: [],
      resolvedAt: null,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, componentImportLineId: docId, tenantId: __after.tenantId, importId: __after.importId, _subject: { entity: "ComponentImportLine", command: "resetResolution", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ComponentImportLineResolutionReset", entity: "ComponentImportLine", entityId: docId, payload: { componentImportLineId: docId, tenantId: __after.tenantId, importId: __after.importId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ComponentImportLine_resetResolution = mutation({
  args: {
    docId: v.id("componentImportLines"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runComponentImportLineResetResolution(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ComponentImportLine_resetResolution", __result);
    }
    return __result;
  },
});

async function __runComponentImportLineStage(ctx: MutationCtx, { docId, importId, sourceOrder, sourceLine, parsedQuantity, parsedUnit, parsedIngredientName, preparationNote, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ComponentImportLine not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ComponentImportLine not found");
    const __rel_import = await __resolveRelation(ctx, "componentImports", [__auth.tenantId, doc.importId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).importId;
      ((doc as any) as any).import = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may read component import lines");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write component import lines through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute component import line commands");
    if (!((doc.resolvedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((__rel_import != null))) throw new Error("Guard 2 failed");
    if (!((importId === doc.importId))) throw new Error("Stage importId must match the seeded import reference");
    if (!((((sourceLine).trim()).length > 0))) throw new Error("Source line is required");
    if (!((sourceOrder >= 0))) throw new Error("Source order cannot be negative");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      importId: importId,
      sourceOrder: sourceOrder,
      sourceLine: sourceLine,
      parsedQuantity: parsedQuantity,
      parsedUnit: parsedUnit,
      parsedIngredientName: parsedIngredientName,
      preparationNote: preparationNote,
      matchStatus: "unresolved",
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, componentImportLineId: docId, tenantId: __after.tenantId, importId: importId, sourceOrder: sourceOrder, _subject: { entity: "ComponentImportLine", command: "stage", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ComponentImportLineStaged", entity: "ComponentImportLine", entityId: docId, payload: { componentImportLineId: docId, tenantId: __after.tenantId, importId: importId, sourceOrder: sourceOrder }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ComponentImportLine_stage = mutation({
  args: {
    docId: v.id("componentImportLines"),
    importId: v.string(),
    sourceOrder: v.number(),
    sourceLine: v.string(),
    parsedQuantity: v.optional(v.number()),
    parsedUnit: v.optional(v.any()),
    parsedIngredientName: v.optional(v.string()),
    preparationNote: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runComponentImportLineStage(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ComponentImportLine_stage", __result);
    }
    return __result;
  },
});

export const ComponentImportLine_createViaStage = mutation({
  args: {
    importId: v.string(),
    sourceOrder: v.number(),
    sourceLine: v.string(),
    parsedQuantity: v.optional(v.number()),
    parsedUnit: v.optional(v.any()),
    parsedIngredientName: v.optional(v.string()),
    preparationNote: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { importId, sourceOrder, sourceLine, parsedQuantity, parsedUnit, parsedIngredientName, preparationNote } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      possibleMatchIngredientIds: [],
      createdAt: Date.now(),
      updatedAt: Date.now(),
      importId: args.importId,
      parsedIngredientName: args.parsedIngredientName,
      parsedQuantity: args.parsedQuantity,
      parsedUnit: args.parsedUnit,
      preparationNote: args.preparationNote,
      sourceLine: args.sourceLine,
      sourceOrder: args.sourceOrder
    };
    const __rel_import = await __resolveRelation(ctx, "componentImports", [__auth.tenantId, __draft.importId], ["tenantId","id"], "tenantId", __auth.tenantId);
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may read component import lines");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write component import lines through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute component import line commands");
    if (!((__draft.resolvedAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((__rel_import != null))) throw new Error("Guard 2 failed");
    if (!((importId === __draft.importId))) throw new Error("Stage importId must match the seeded import reference");
    if (!((((sourceLine).trim()).length > 0))) throw new Error("Source line is required");
    if (!((sourceOrder >= 0))) throw new Error("Source order cannot be negative");
    const doc: Record<string, any> = {
      ...__draft,
      importId: importId,
      sourceOrder: sourceOrder,
      sourceLine: sourceLine,
      parsedQuantity: parsedQuantity,
      parsedUnit: parsedUnit,
      parsedIngredientName: parsedIngredientName,
      preparationNote: preparationNote,
      matchStatus: "unresolved",
      version: 1,
    };
    const docId = await ctx.db.insert("componentImportLines", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, componentImportLineId: docId, tenantId: doc.tenantId, importId: doc.importId, sourceOrder: doc.sourceOrder, _subject: { entity: "ComponentImportLine", command: "stage", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ComponentImportLineStaged", entity: "ComponentImportLine", entityId: docId, payload: { componentImportLineId: docId, tenantId: doc.tenantId, importId: doc.importId, sourceOrder: doc.sourceOrder }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ComponentImportLine_createViaStage", __result);
    }
    return __result;
  },
});

async function __runComponentImportLineSuggestExactMatch(ctx: MutationCtx, { docId, matchedIngredientId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ComponentImportLine not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ComponentImportLine not found");
    const __rel_matchedIngredient = await __resolveRelation(ctx, "ingredients", [__auth.tenantId, doc.matchedIngredientId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).matchedIngredientId;
      ((doc as any) as any).matchedIngredient = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may read component import lines");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write component import lines through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute component import line commands");
    if (!((doc.resolvedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(((__rel_matchedIngredient != null) && (__rel_matchedIngredient.status === "active")))) throw new Error("Guard 2 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      matchStatus: "exact",
      matchedIngredientId: matchedIngredientId,
      possibleMatchIngredientIds: [],
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, componentImportLineId: docId, tenantId: __after.tenantId, importId: __after.importId, matchedIngredientId: matchedIngredientId, _subject: { entity: "ComponentImportLine", command: "suggestExactMatch", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ComponentImportLineExactMatchSuggested", entity: "ComponentImportLine", entityId: docId, payload: { componentImportLineId: docId, tenantId: __after.tenantId, importId: __after.importId, matchedIngredientId: matchedIngredientId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ComponentImportLine_suggestExactMatch = mutation({
  args: {
    docId: v.id("componentImportLines"),
    matchedIngredientId: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runComponentImportLineSuggestExactMatch(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ComponentImportLine_suggestExactMatch", __result);
    }
    return __result;
  },
});

async function __runComponentImportLineSuggestPossibleMatches(ctx: MutationCtx, { docId, possibleMatchIngredientIds, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ComponentImportLine not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ComponentImportLine not found");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may read component import lines");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write component import lines through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute component import line commands");
    if (!((doc.resolvedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      matchStatus: "possible",
      matchedIngredientId: null,
      possibleMatchIngredientIds: possibleMatchIngredientIds,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, componentImportLineId: docId, tenantId: __after.tenantId, importId: __after.importId, candidateCount: (possibleMatchIngredientIds).length, _subject: { entity: "ComponentImportLine", command: "suggestPossibleMatches", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ComponentImportLinePossibleMatchesSuggested", entity: "ComponentImportLine", entityId: docId, payload: { componentImportLineId: docId, tenantId: __after.tenantId, importId: __after.importId, candidateCount: (possibleMatchIngredientIds).length }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ComponentImportLine_suggestPossibleMatches = mutation({
  args: {
    docId: v.id("componentImportLines"),
    possibleMatchIngredientIds: v.array(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runComponentImportLineSuggestPossibleMatches(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ComponentImportLine_suggestPossibleMatches", __result);
    }
    return __result;
  },
});

async function __runComponentIngredientAdd(ctx: MutationCtx, { docId, componentId, ingredientId, quantity, unit, sortOrder, wasteFactor, prepNotes, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ComponentIngredient not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ComponentIngredient not found");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may read component ingredient lines");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write component ingredient lines through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute component ingredient line commands");
    if (!((doc.addedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((quantity > 0))) throw new Error("Component ingredient quantity must be positive");
    if (!(((wasteFactor == null) || (wasteFactor > 0)))) throw new Error("Waste factor must be greater than 0");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      componentId: componentId,
      ingredientId: ingredientId,
      quantity: quantity,
      unit: unit,
      wasteFactor: ((wasteFactor != null) ? wasteFactor : 1),
      sortOrder: ((sortOrder != null) ? sortOrder : 0),
      prepNotes: prepNotes,
      addedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, componentIngredientId: docId, tenantId: __after.tenantId, componentId: componentId, ingredientId: ingredientId, quantity: quantity, unit: unit, wasteFactor: ((wasteFactor != null) ? wasteFactor : 1), _subject: { entity: "ComponentIngredient", command: "add", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ComponentIngredientAdded", entity: "ComponentIngredient", entityId: docId, payload: { componentIngredientId: docId, tenantId: __after.tenantId, componentId: componentId, ingredientId: ingredientId, quantity: quantity, unit: unit, wasteFactor: ((wasteFactor != null) ? wasteFactor : 1) }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ComponentIngredient_add = mutation({
  args: {
    docId: v.id("componentIngredients"),
    componentId: v.string(),
    ingredientId: v.string(),
    quantity: v.number(),
    unit: v.any(),
    sortOrder: v.optional(v.any()),
    wasteFactor: v.optional(v.number()),
    prepNotes: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runComponentIngredientAdd(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ComponentIngredient_add", __result);
    }
    return __result;
  },
});

export const ComponentIngredient_createViaAdd = mutation({
  args: {
    componentId: v.string(),
    ingredientId: v.string(),
    quantity: v.number(),
    unit: v.any(),
    sortOrder: v.optional(v.any()),
    wasteFactor: v.optional(v.number()),
    prepNotes: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { componentId, ingredientId, quantity, unit, sortOrder, wasteFactor, prepNotes } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      sortOrder: args.sortOrder !== undefined ? args.sortOrder : 0,
      wasteFactor: args.wasteFactor !== undefined ? args.wasteFactor : 1,
      createdAt: Date.now(),
      updatedAt: Date.now(),
      componentId: args.componentId,
      ingredientId: args.ingredientId,
      prepNotes: args.prepNotes,
      quantity: args.quantity,
      unit: args.unit
    };
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may read component ingredient lines");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write component ingredient lines through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute component ingredient line commands");
    if (!((__draft.addedAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((quantity > 0))) throw new Error("Component ingredient quantity must be positive");
    if (!(((wasteFactor == null) || (wasteFactor > 0)))) throw new Error("Waste factor must be greater than 0");
    const doc: Record<string, any> = {
      ...__draft,
      componentId: componentId,
      ingredientId: ingredientId,
      quantity: quantity,
      unit: unit,
      wasteFactor: ((wasteFactor != null) ? wasteFactor : 1),
      sortOrder: ((sortOrder != null) ? sortOrder : 0),
      prepNotes: prepNotes,
      addedAt: Date.now(),
      version: 1,
    };
    const docId = await ctx.db.insert("componentIngredients", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, componentIngredientId: docId, tenantId: doc.tenantId, componentId: doc.componentId, ingredientId: doc.ingredientId, quantity: doc.quantity, unit: doc.unit, wasteFactor: ((doc.wasteFactor != null) ? doc.wasteFactor : 1), _subject: { entity: "ComponentIngredient", command: "add", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ComponentIngredientAdded", entity: "ComponentIngredient", entityId: docId, payload: { componentIngredientId: docId, tenantId: doc.tenantId, componentId: doc.componentId, ingredientId: doc.ingredientId, quantity: doc.quantity, unit: doc.unit, wasteFactor: ((doc.wasteFactor != null) ? doc.wasteFactor : 1) }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ComponentIngredient_createViaAdd", __result);
    }
    return __result;
  },
});

async function __runComponentIngredientAdjustQuantity(ctx: MutationCtx, { docId, quantity, unit, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ComponentIngredient not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ComponentIngredient not found");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may read component ingredient lines");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write component ingredient lines through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute component ingredient line commands");
    if (!((doc.addedAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((quantity > 0))) throw new Error("Component ingredient quantity must be positive");
    const previousQuantity = doc.quantity;
    const previousUnit = doc.unit;
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      quantity: quantity,
      unit: unit,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, componentIngredientId: docId, tenantId: __after.tenantId, componentId: __after.componentId, ingredientId: __after.ingredientId, previousQuantity: previousQuantity, previousUnit: previousUnit, quantity: quantity, unit: unit, _subject: { entity: "ComponentIngredient", command: "adjustQuantity", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ComponentIngredientQuantityAdjusted", entity: "ComponentIngredient", entityId: docId, payload: { componentIngredientId: docId, tenantId: __after.tenantId, componentId: __after.componentId, ingredientId: __after.ingredientId, previousQuantity: previousQuantity, previousUnit: previousUnit, quantity: quantity, unit: unit }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ComponentIngredient_adjustQuantity = mutation({
  args: {
    docId: v.id("componentIngredients"),
    quantity: v.number(),
    unit: v.any(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runComponentIngredientAdjustQuantity(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ComponentIngredient_adjustQuantity", __result);
    }
    return __result;
  },
});

async function __runComponentIngredientRemove(ctx: MutationCtx, { docId, reason, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ComponentIngredient not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ComponentIngredient not found");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may read component ingredient lines");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write component ingredient lines through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute component ingredient line commands");
    if (!((doc.addedAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((((reason).trim()).length > 0))) throw new Error("Removal reason is required");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      deletedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, componentIngredientId: docId, tenantId: __after.tenantId, componentId: __after.componentId, ingredientId: __after.ingredientId, quantity: __after.quantity, unit: __after.unit, reason: reason, _subject: { entity: "ComponentIngredient", command: "remove", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ComponentIngredientRemoved", entity: "ComponentIngredient", entityId: docId, payload: { componentIngredientId: docId, tenantId: __after.tenantId, componentId: __after.componentId, ingredientId: __after.ingredientId, quantity: __after.quantity, unit: __after.unit, reason: reason }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ComponentIngredient_remove = mutation({
  args: {
    docId: v.id("componentIngredients"),
    reason: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runComponentIngredientRemove(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ComponentIngredient_remove", __result);
    }
    return __result;
  },
});

async function __runComponentIngredientSetWasteFactor(ctx: MutationCtx, { docId, wasteFactor, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ComponentIngredient not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ComponentIngredient not found");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may read component ingredient lines");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write component ingredient lines through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute component ingredient line commands");
    if (!((doc.addedAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((wasteFactor > 0))) throw new Error("Waste factor must be greater than 0");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      wasteFactor: wasteFactor,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, componentIngredientId: docId, tenantId: __after.tenantId, componentId: __after.componentId, ingredientId: __after.ingredientId, wasteFactor: wasteFactor, _subject: { entity: "ComponentIngredient", command: "setWasteFactor", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ComponentIngredientWasteFactorSet", entity: "ComponentIngredient", entityId: docId, payload: { componentIngredientId: docId, tenantId: __after.tenantId, componentId: __after.componentId, ingredientId: __after.ingredientId, wasteFactor: wasteFactor }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ComponentIngredient_setWasteFactor = mutation({
  args: {
    docId: v.id("componentIngredients"),
    wasteFactor: v.number(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runComponentIngredientSetWasteFactor(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ComponentIngredient_setWasteFactor", __result);
    }
    return __result;
  },
});

async function __runComponentSnapshotCapture(ctx: MutationCtx, { docId, componentId, versionNumber, capturedByName, changeSummary, snapshot, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ComponentSnapshot not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ComponentSnapshot not found");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may read component version history");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write component snapshots through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute component snapshot commands");
    if (!((doc.capturedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((user.id != null))) throw new Error("Guard 2 failed");
    if (!((((snapshot).trim()).length > 0))) throw new Error("Snapshot payload is required");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      componentId: componentId,
      versionNumber: versionNumber,
      capturedByName: capturedByName,
      capturedByAuthSubjectId: user.id,
      changeSummary: changeSummary,
      snapshot: snapshot,
      capturedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, snapshotId: docId, tenantId: __after.tenantId, componentId: componentId, versionNumber: versionNumber, _subject: { entity: "ComponentSnapshot", command: "capture", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ComponentSnapshotCaptured", entity: "ComponentSnapshot", entityId: docId, payload: { snapshotId: docId, tenantId: __after.tenantId, componentId: componentId, versionNumber: versionNumber }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ComponentSnapshot_capture = mutation({
  args: {
    docId: v.id("componentSnapshots"),
    componentId: v.string(),
    versionNumber: v.number(),
    capturedByName: v.string(),
    changeSummary: v.string(),
    snapshot: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runComponentSnapshotCapture(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ComponentSnapshot_capture", __result);
    }
    return __result;
  },
});

export const ComponentSnapshot_createViaCapture = mutation({
  args: {
    componentId: v.string(),
    versionNumber: v.number(),
    capturedByName: v.string(),
    changeSummary: v.string(),
    snapshot: v.string(),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { componentId, versionNumber, capturedByName, changeSummary, snapshot } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      createdAt: Date.now(),
      updatedAt: Date.now(),
      capturedByName: args.capturedByName,
      changeSummary: args.changeSummary,
      componentId: args.componentId,
      snapshot: args.snapshot,
      versionNumber: args.versionNumber
    };
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may read component version history");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write component snapshots through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute component snapshot commands");
    if (!((__draft.capturedAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((user.id != null))) throw new Error("Guard 2 failed");
    if (!((((snapshot).trim()).length > 0))) throw new Error("Snapshot payload is required");
    const doc: Record<string, any> = {
      ...__draft,
      componentId: componentId,
      versionNumber: versionNumber,
      capturedByName: capturedByName,
      capturedByAuthSubjectId: user.id,
      changeSummary: changeSummary,
      snapshot: snapshot,
      capturedAt: Date.now(),
      version: 1,
    };
    const docId = await ctx.db.insert("componentSnapshots", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, snapshotId: docId, tenantId: doc.tenantId, componentId: doc.componentId, versionNumber: doc.versionNumber, _subject: { entity: "ComponentSnapshot", command: "capture", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ComponentSnapshotCaptured", entity: "ComponentSnapshot", entityId: docId, payload: { snapshotId: docId, tenantId: doc.tenantId, componentId: doc.componentId, versionNumber: doc.versionNumber }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ComponentSnapshot_createViaCapture", __result);
    }
    return __result;
  },
});

async function __runComponentStepAdd(ctx: MutationCtx, { docId, componentId, instruction, sortOrder, durationMinutes, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ComponentStep not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ComponentStep not found");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may read component steps");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write component steps through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute component step commands");
    if (!((doc.addedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((((instruction).trim()).length > 0))) throw new Error("Component step instruction is required");
    if (!(((durationMinutes == null) || (durationMinutes >= 0)))) throw new Error("Component step duration cannot be negative");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      componentId: componentId,
      instruction: instruction,
      sortOrder: ((sortOrder != null) ? sortOrder : 0),
      durationMinutes: durationMinutes,
      addedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, componentStepId: docId, tenantId: __after.tenantId, componentId: componentId, instruction: instruction, sortOrder: ((sortOrder != null) ? sortOrder : 0), _subject: { entity: "ComponentStep", command: "add", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ComponentStepAdded", entity: "ComponentStep", entityId: docId, payload: { componentStepId: docId, tenantId: __after.tenantId, componentId: componentId, instruction: instruction, sortOrder: ((sortOrder != null) ? sortOrder : 0) }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ComponentStep_add = mutation({
  args: {
    docId: v.id("componentSteps"),
    componentId: v.string(),
    instruction: v.string(),
    sortOrder: v.optional(v.any()),
    durationMinutes: v.optional(v.any()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runComponentStepAdd(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ComponentStep_add", __result);
    }
    return __result;
  },
});

export const ComponentStep_createViaAdd = mutation({
  args: {
    componentId: v.string(),
    instruction: v.string(),
    sortOrder: v.optional(v.any()),
    durationMinutes: v.optional(v.any()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { componentId, instruction, sortOrder, durationMinutes } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      sortOrder: args.sortOrder !== undefined ? args.sortOrder : 0,
      createdAt: Date.now(),
      updatedAt: Date.now(),
      componentId: args.componentId,
      durationMinutes: args.durationMinutes,
      instruction: args.instruction
    };
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may read component steps");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write component steps through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute component step commands");
    if (!((__draft.addedAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((((instruction).trim()).length > 0))) throw new Error("Component step instruction is required");
    if (!(((durationMinutes == null) || (durationMinutes >= 0)))) throw new Error("Component step duration cannot be negative");
    const doc: Record<string, any> = {
      ...__draft,
      componentId: componentId,
      instruction: instruction,
      sortOrder: ((sortOrder != null) ? sortOrder : 0),
      durationMinutes: durationMinutes,
      addedAt: Date.now(),
      version: 1,
    };
    const docId = await ctx.db.insert("componentSteps", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, componentStepId: docId, tenantId: doc.tenantId, componentId: doc.componentId, instruction: doc.instruction, sortOrder: ((doc.sortOrder != null) ? doc.sortOrder : 0), _subject: { entity: "ComponentStep", command: "add", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ComponentStepAdded", entity: "ComponentStep", entityId: docId, payload: { componentStepId: docId, tenantId: doc.tenantId, componentId: doc.componentId, instruction: doc.instruction, sortOrder: ((doc.sortOrder != null) ? doc.sortOrder : 0) }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ComponentStep_createViaAdd", __result);
    }
    return __result;
  },
});

async function __runComponentStepRemove(ctx: MutationCtx, { docId, reason, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ComponentStep not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ComponentStep not found");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may read component steps");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write component steps through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute component step commands");
    if (!((doc.addedAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((((reason).trim()).length > 0))) throw new Error("Removal reason is required");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      deletedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, componentStepId: docId, tenantId: __after.tenantId, componentId: __after.componentId, reason: reason, _subject: { entity: "ComponentStep", command: "remove", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ComponentStepRemoved", entity: "ComponentStep", entityId: docId, payload: { componentStepId: docId, tenantId: __after.tenantId, componentId: __after.componentId, reason: reason }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ComponentStep_remove = mutation({
  args: {
    docId: v.id("componentSteps"),
    reason: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runComponentStepRemove(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ComponentStep_remove", __result);
    }
    return __result;
  },
});

async function __runComponentStepRevise(ctx: MutationCtx, { docId, instruction, sortOrder, durationMinutes, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ComponentStep not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ComponentStep not found");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may read component steps");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write component steps through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute component step commands");
    if (!((doc.addedAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((((instruction).trim()).length > 0))) throw new Error("Component step instruction is required");
    if (!(((durationMinutes == null) || (durationMinutes >= 0)))) throw new Error("Component step duration cannot be negative");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      instruction: instruction,
      sortOrder: ((sortOrder != null) ? sortOrder : doc.sortOrder),
      durationMinutes: durationMinutes,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, componentStepId: docId, tenantId: __after.tenantId, componentId: __after.componentId, instruction: instruction, sortOrder: ((sortOrder != null) ? sortOrder : __after.sortOrder), _subject: { entity: "ComponentStep", command: "revise", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ComponentStepRevised", entity: "ComponentStep", entityId: docId, payload: { componentStepId: docId, tenantId: __after.tenantId, componentId: __after.componentId, instruction: instruction, sortOrder: ((sortOrder != null) ? sortOrder : __after.sortOrder) }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ComponentStep_revise = mutation({
  args: {
    docId: v.id("componentSteps"),
    instruction: v.string(),
    sortOrder: v.optional(v.any()),
    durationMinutes: v.optional(v.any()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runComponentStepRevise(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ComponentStep_revise", __result);
    }
    return __result;
  },
});

async function __runContractDraft(ctx: MutationCtx, { docId, eventId, clientId, title, contractNumber, documentUrl, expiresAt, notes, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Contract not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Contract not found");
    const __rel_event = await __resolveRelation(ctx, "events", [__auth.tenantId, doc.eventId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_client = await __resolveRelation(ctx, "clients", [__auth.tenantId, doc.clientId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).eventId;
      ((doc as any) as any).event = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    {
      const __fk = ((doc as any) as any).clientId;
      ((doc as any) as any).client = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may read contracts");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may write contracts through commands");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may execute contract commands");
    if (!((doc.draftedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.status === "draft"))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((__rel_event != null))) throw new Error("Guard 3 failed");
    if (!((__rel_client != null))) throw new Error("Guard 4 failed");
    if (!((eventId === doc.eventId))) throw new Error("Draft eventId must match the seeded event reference");
    if (!((clientId === doc.clientId))) throw new Error("Draft clientId must match the seeded client reference");
    if (!((((title).trim()).length > 0))) throw new Error("Contract title is required");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      eventId: eventId,
      clientId: clientId,
      title: title,
      contractNumber: contractNumber,
      documentUrl: documentUrl,
      expiresAt: expiresAt,
      notes: notes,
      draftedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, contractId: docId, tenantId: __after.tenantId, eventId: eventId, clientId: clientId, title: title, _subject: { entity: "Contract", command: "draft", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ContractDrafted", entity: "Contract", entityId: docId, payload: { contractId: docId, tenantId: __after.tenantId, eventId: eventId, clientId: clientId, title: title }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Contract_draft = mutation({
  args: {
    docId: v.id("contracts"),
    eventId: v.string(),
    clientId: v.string(),
    title: v.string(),
    contractNumber: v.optional(v.string()),
    documentUrl: v.optional(v.string()),
    expiresAt: v.optional(v.number()),
    notes: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runContractDraft(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Contract_draft", __result);
    }
    return __result;
  },
});

export const Contract_createViaDraft = mutation({
  args: {
    eventId: v.string(),
    clientId: v.string(),
    title: v.string(),
    contractNumber: v.optional(v.string()),
    documentUrl: v.optional(v.string()),
    expiresAt: v.optional(v.number()),
    notes: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { eventId, clientId, title, contractNumber, documentUrl, expiresAt, notes } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      status: "draft",
      createdAt: Date.now(),
      updatedAt: Date.now(),
      clientId: args.clientId,
      contractNumber: args.contractNumber,
      documentUrl: args.documentUrl,
      eventId: args.eventId,
      expiresAt: args.expiresAt,
      notes: args.notes,
      title: args.title
    };
    const __rel_event = await __resolveRelation(ctx, "events", [__auth.tenantId, __draft.eventId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_client = await __resolveRelation(ctx, "clients", [__auth.tenantId, __draft.clientId], ["tenantId","id"], "tenantId", __auth.tenantId);
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may read contracts");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may write contracts through commands");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may execute contract commands");
    if (!((__draft.draftedAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.status === "draft"))) throw new Error("Guard 1 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((__rel_event != null))) throw new Error("Guard 3 failed");
    if (!((__rel_client != null))) throw new Error("Guard 4 failed");
    if (!((eventId === __draft.eventId))) throw new Error("Draft eventId must match the seeded event reference");
    if (!((clientId === __draft.clientId))) throw new Error("Draft clientId must match the seeded client reference");
    if (!((((title).trim()).length > 0))) throw new Error("Contract title is required");
    const doc: Record<string, any> = {
      ...__draft,
      eventId: eventId,
      clientId: clientId,
      title: title,
      contractNumber: contractNumber,
      documentUrl: documentUrl,
      expiresAt: expiresAt,
      notes: notes,
      draftedAt: Date.now(),
      version: 1,
    };
    const docId = await ctx.db.insert("contracts", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, contractId: docId, tenantId: doc.tenantId, eventId: doc.eventId, clientId: doc.clientId, title: doc.title, _subject: { entity: "Contract", command: "draft", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ContractDrafted", entity: "Contract", entityId: docId, payload: { contractId: docId, tenantId: doc.tenantId, eventId: doc.eventId, clientId: doc.clientId, title: doc.title }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Contract_createViaDraft", __result);
    }
    return __result;
  },
});

async function __runContractExpire(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Contract not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Contract not found");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may read contracts");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may write contracts through commands");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may execute contract commands");
    if (!(((doc.status === "sent") || (doc.status === "viewed")))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "expired";
        const __allowed: Record<string, string[]> = { "draft": ["sent"], "sent": ["viewed", "expired", "voided"], "viewed": ["signed", "expired", "voided"], "signed": [], "expired": [], "voided": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "expired",
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, contractId: docId, tenantId: __after.tenantId, eventId: __after.eventId, clientId: __after.clientId, _subject: { entity: "Contract", command: "expire", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ContractExpired", entity: "Contract", entityId: docId, payload: { contractId: docId, tenantId: __after.tenantId, eventId: __after.eventId, clientId: __after.clientId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Contract_expire = mutation({
  args: {
    docId: v.id("contracts"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runContractExpire(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Contract_expire", __result);
    }
    return __result;
  },
});

async function __runContractMarkViewed(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Contract not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Contract not found");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may read contracts");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may write contracts through commands");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may execute contract commands");
    if (!((doc.status === "sent"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "viewed";
        const __allowed: Record<string, string[]> = { "draft": ["sent"], "sent": ["viewed", "expired", "voided"], "viewed": ["signed", "expired", "voided"], "signed": [], "expired": [], "voided": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "viewed",
      viewedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, contractId: docId, tenantId: __after.tenantId, eventId: __after.eventId, clientId: __after.clientId, _subject: { entity: "Contract", command: "markViewed", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ContractViewed", entity: "Contract", entityId: docId, payload: { contractId: docId, tenantId: __after.tenantId, eventId: __after.eventId, clientId: __after.clientId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Contract_markViewed = mutation({
  args: {
    docId: v.id("contracts"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runContractMarkViewed(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Contract_markViewed", __result);
    }
    return __result;
  },
});

async function __runContractMarkVoided(ctx: MutationCtx, { docId, reason, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Contract not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Contract not found");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may read contracts");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may write contracts through commands");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may execute contract commands");
    if (!(((doc.status === "sent") || (doc.status === "viewed")))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((((reason).trim()).length > 0))) throw new Error("Void reason is required");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "voided";
        const __allowed: Record<string, string[]> = { "draft": ["sent"], "sent": ["viewed", "expired", "voided"], "viewed": ["signed", "expired", "voided"], "signed": [], "expired": [], "voided": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "voided",
      voidReason: reason,
      voidedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, contractId: docId, tenantId: __after.tenantId, eventId: __after.eventId, clientId: __after.clientId, reason: reason, _subject: { entity: "Contract", command: "markVoided", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ContractVoided", entity: "Contract", entityId: docId, payload: { contractId: docId, tenantId: __after.tenantId, eventId: __after.eventId, clientId: __after.clientId, reason: reason }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Contract_markVoided = mutation({
  args: {
    docId: v.id("contracts"),
    reason: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runContractMarkVoided(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Contract_markVoided", __result);
    }
    return __result;
  },
});

async function __runContractReassignClient(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Contract not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Contract not found");
    const __rel_clientMergeAuthorization = await __resolveRelation(ctx, "clientMerges", [__auth.tenantId, doc.clientMergeAuthorizationId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_mergeTargetClient = await __resolveRelation(ctx, "clients", [__auth.tenantId, doc.mergeTargetClientId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).clientMergeAuthorizationId;
      ((doc as any) as any).clientMergeAuthorization = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    {
      const __fk = ((doc as any) as any).mergeTargetClientId;
      ((doc as any) as any).mergeTargetClient = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may read contracts");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may write contracts through commands");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may execute contract commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!(checkRole(user, "adminAccess"))) throw new Error("Guard 1 failed");
    if (!((__rel_clientMergeAuthorization != null))) throw new Error("Guard 2 failed");
    if (!((__rel_clientMergeAuthorization.deletedAt == null))) throw new Error("Guard 3 failed");
    if (!((__rel_clientMergeAuthorization.mergedAt != null))) throw new Error("Guard 4 failed");
    if (!((__rel_mergeTargetClient != null))) throw new Error("Guard 5 failed");
    if (!((__rel_mergeTargetClient.status === "active"))) throw new Error("Guard 6 failed");
    if (!((__rel_mergeTargetClient.deletedAt == null))) throw new Error("Guard 7 failed");
    if (!((__rel_clientMergeAuthorization.duplicateClientId === doc.clientId))) throw new Error("Merge authorization does not match the contract client");
    if (!((__rel_clientMergeAuthorization.primaryClientId === doc.mergeTargetClientId))) throw new Error("Merge authorization does not match the primary client");
    const previousClientId = doc.clientId;
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      clientId: doc.mergeTargetClientId,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, contractId: docId, tenantId: __after.tenantId, previousClientId: previousClientId, clientId: __after.mergeTargetClientId, _subject: { entity: "Contract", command: "reassignClient", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ContractClientReassigned", entity: "Contract", entityId: docId, payload: { contractId: docId, tenantId: __after.tenantId, previousClientId: previousClientId, clientId: __after.mergeTargetClientId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Contract_reassignClient = mutation({
  args: {
    docId: v.id("contracts"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runContractReassignClient(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Contract_reassignClient", __result);
    }
    return __result;
  },
});

async function __runContractSend(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Contract not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Contract not found");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may read contracts");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may write contracts through commands");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may execute contract commands");
    if (!((doc.status === "draft"))) throw new Error("Guard 0 failed");
    if (!((doc.draftedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((((doc.title).trim()).length > 0))) throw new Error("Contract title is required before send");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "sent";
        const __allowed: Record<string, string[]> = { "draft": ["sent"], "sent": ["viewed", "expired", "voided"], "viewed": ["signed", "expired", "voided"], "signed": [], "expired": [], "voided": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "sent",
      sentAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, contractId: docId, tenantId: __after.tenantId, eventId: __after.eventId, clientId: __after.clientId, _subject: { entity: "Contract", command: "send", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ContractSent", entity: "Contract", entityId: docId, payload: { contractId: docId, tenantId: __after.tenantId, eventId: __after.eventId, clientId: __after.clientId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Contract_send = mutation({
  args: {
    docId: v.id("contracts"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runContractSend(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Contract_send", __result);
    }
    return __result;
  },
});

async function __runContractSign(ctx: MutationCtx, { docId, signedBy, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Contract not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Contract not found");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may read contracts");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may write contracts through commands");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may execute contract commands");
    if (!((doc.status === "viewed"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((((signedBy).trim()).length > 0))) throw new Error("Signer name is required");
    const signedAtValue = Date.now();
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "signed";
        const __allowed: Record<string, string[]> = { "draft": ["sent"], "sent": ["viewed", "expired", "voided"], "viewed": ["signed", "expired", "voided"], "signed": [], "expired": [], "voided": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "signed",
      signedBy: signedBy,
      signedAt: signedAtValue,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, contractId: docId, tenantId: __after.tenantId, eventId: __after.eventId, clientId: __after.clientId, signedBy: signedBy, signedAt: signedAtValue, _subject: { entity: "Contract", command: "sign", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ContractSigned", entity: "Contract", entityId: docId, payload: { contractId: docId, tenantId: __after.tenantId, eventId: __after.eventId, clientId: __after.clientId, signedBy: signedBy, signedAt: signedAtValue }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Contract_sign = mutation({
  args: {
    docId: v.id("contracts"),
    signedBy: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runContractSign(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Contract_sign", __result);
    }
    return __result;
  },
});

async function __runContractStageClientMerge(ctx: MutationCtx, { docId, clientMergeId, clientId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Contract not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Contract not found");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may read contracts");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may write contracts through commands");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may execute contract commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!(checkRole(user, "adminAccess"))) throw new Error("Guard 1 failed");
    if (!((clientId !== doc.clientId))) throw new Error("Contract already belongs to this client");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      clientMergeAuthorizationId: clientMergeId,
      mergeTargetClientId: clientId,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, contractId: docId, tenantId: __after.tenantId, _subject: { entity: "Contract", command: "stageClientMerge", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ContractClientMergeStaged", entity: "Contract", entityId: docId, payload: { contractId: docId, tenantId: __after.tenantId }, createdAt: Date.now() });
    // Reactions
    const reactionTarget0 = payload.contractId;
    if (reactionTarget0) await __runContractReassignClient(ctx, { docId: reactionTarget0 } as any);
    return { ...doc, ...updates };
}

export const Contract_stageClientMerge = mutation({
  args: {
    docId: v.id("contracts"),
    clientMergeId: v.string(),
    clientId: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runContractStageClientMerge(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Contract_stageClientMerge", __result);
    }
    return __result;
  },
});

async function __runCorrectiveActionClose(ctx: MutationCtx, { docId, resolutionNotes, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("CorrectiveAction not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("CorrectiveAction not found");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "kitchenAccess")))) throw new Error("Event and kitchen staff may read corrective actions");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "kitchenAccess")))) throw new Error("Event and kitchen staff may write corrective actions through commands");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "kitchenAccess")))) throw new Error("Event and kitchen staff may execute corrective action commands");
    if (!((doc.status === "open"))) throw new Error("Guard 0 failed");
    if (!((doc.openedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((user.id != null))) throw new Error("Guard 3 failed");
    if (!((((resolutionNotes).trim()).length > 0))) throw new Error("Resolution notes are required");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "closed";
        const __allowed: Record<string, string[]> = { "open": ["closed"], "closed": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      resolutionNotes: resolutionNotes,
      closedById: user.id,
      status: "closed",
      closedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, correctiveActionId: docId, tenantId: __after.tenantId, incidentId: __after.incidentId, eventId: __after.eventId, closedById: user.id, status: "closed", resolutionNotes: resolutionNotes, _subject: { entity: "CorrectiveAction", command: "close", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "CorrectiveActionClosed", entity: "CorrectiveAction", entityId: docId, payload: { correctiveActionId: docId, tenantId: __after.tenantId, incidentId: __after.incidentId, eventId: __after.eventId, closedById: user.id, status: "closed", resolutionNotes: resolutionNotes }, createdAt: Date.now() });
    // Reactions
    const reactionTarget0 = payload.incidentId;
    if (reactionTarget0) await __runIncidentClearCorrectiveActionLock(ctx, { docId: reactionTarget0 } as any);
    return { ...doc, ...updates };
}

export const CorrectiveAction_close = mutation({
  args: {
    docId: v.id("correctiveActions"),
    resolutionNotes: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runCorrectiveActionClose(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "CorrectiveAction_close", __result);
    }
    return __result;
  },
});

async function __runCorrectiveActionOpen(ctx: MutationCtx, { docId, incidentId, eventId, description, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("CorrectiveAction not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("CorrectiveAction not found");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "kitchenAccess")))) throw new Error("Event and kitchen staff may read corrective actions");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "kitchenAccess")))) throw new Error("Event and kitchen staff may write corrective actions through commands");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "kitchenAccess")))) throw new Error("Event and kitchen staff may execute corrective action commands");
    if (!((doc.openedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.status === "open"))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((user.id != null))) throw new Error("Guard 3 failed");
    if (!((incidentId === doc.incidentId))) throw new Error("Open incidentId must match the seeded incident reference");
    if (!((eventId === doc.eventId))) throw new Error("Open eventId must match the seeded event reference");
    if (!((((description).trim()).length > 0))) throw new Error("Corrective action description is required");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      incidentId: incidentId,
      eventId: eventId,
      description: description,
      openedById: user.id,
      openedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, correctiveActionId: docId, tenantId: __after.tenantId, incidentId: incidentId, eventId: eventId, openedById: user.id, status: "open", _subject: { entity: "CorrectiveAction", command: "open", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "CorrectiveActionOpened", entity: "CorrectiveAction", entityId: docId, payload: { correctiveActionId: docId, tenantId: __after.tenantId, incidentId: incidentId, eventId: eventId, openedById: user.id, status: "open" }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const CorrectiveAction_open = mutation({
  args: {
    docId: v.id("correctiveActions"),
    incidentId: v.string(),
    eventId: v.string(),
    description: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runCorrectiveActionOpen(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "CorrectiveAction_open", __result);
    }
    return __result;
  },
});

export const CorrectiveAction_createViaOpen = mutation({
  args: {
    incidentId: v.string(),
    eventId: v.string(),
    description: v.string(),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { incidentId, eventId, description } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      status: "open",
      createdAt: Date.now(),
      updatedAt: Date.now(),
      description: args.description,
      eventId: args.eventId,
      incidentId: args.incidentId
    };
    if (!((checkRole(user, "eventAccess") || checkRole(user, "kitchenAccess")))) throw new Error("Event and kitchen staff may read corrective actions");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "kitchenAccess")))) throw new Error("Event and kitchen staff may write corrective actions through commands");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "kitchenAccess")))) throw new Error("Event and kitchen staff may execute corrective action commands");
    if (!((__draft.openedAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.status === "open"))) throw new Error("Guard 1 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((user.id != null))) throw new Error("Guard 3 failed");
    if (!((incidentId === __draft.incidentId))) throw new Error("Open incidentId must match the seeded incident reference");
    if (!((eventId === __draft.eventId))) throw new Error("Open eventId must match the seeded event reference");
    if (!((((description).trim()).length > 0))) throw new Error("Corrective action description is required");
    const doc: Record<string, any> = {
      ...__draft,
      incidentId: incidentId,
      eventId: eventId,
      description: description,
      openedById: user.id,
      openedAt: Date.now(),
      version: 1,
    };
    const docId = await ctx.db.insert("correctiveActions", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, correctiveActionId: docId, tenantId: doc.tenantId, incidentId: doc.incidentId, eventId: doc.eventId, openedById: user.id, status: "open", _subject: { entity: "CorrectiveAction", command: "open", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "CorrectiveActionOpened", entity: "CorrectiveAction", entityId: docId, payload: { correctiveActionId: docId, tenantId: doc.tenantId, incidentId: doc.incidentId, eventId: doc.eventId, openedById: user.id, status: "open" }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "CorrectiveAction_createViaOpen", __result);
    }
    return __result;
  },
});

async function __runCreditMemoIssue(ctx: MutationCtx, { docId, sourceInvoiceId, clientId, creditMemoNumber, amount, reason, disposition, targetInvoiceId, eventId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("CreditMemo not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("CreditMemo not found");
    const __rel_sourceInvoice = await __resolveRelation(ctx, "invoices", [__auth.tenantId, doc.sourceInvoiceId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_client = await __resolveRelation(ctx, "clients", [__auth.tenantId, doc.clientId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_targetInvoice = await __resolveRelation(ctx, "invoices", [__auth.tenantId, doc.targetInvoiceId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).sourceInvoiceId;
      ((doc as any) as any).sourceInvoice = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    {
      const __fk = ((doc as any) as any).clientId;
      ((doc as any) as any).client = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    {
      const __fk = ((doc as any) as any).targetInvoiceId;
      ((doc as any) as any).targetInvoice = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may read credit memos");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may write credit memos through commands");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may execute credit memo commands");
    if (!((doc.issuedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.status === "draft"))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!(((__rel_sourceInvoice != null) && (__rel_sourceInvoice.deletedAt == null)))) throw new Error("Guard 3 failed");
    if (!((__rel_client != null))) throw new Error("Guard 4 failed");
    if (!((sourceInvoiceId === doc.sourceInvoiceId))) throw new Error("Source invoice must match the seeded invoice reference");
    if (!((clientId === doc.clientId))) throw new Error("Client must match the seeded client reference");
    if (!((__rel_sourceInvoice.clientId === clientId))) throw new Error("Credit memo client must match the paid source invoice");
    if (!((__rel_sourceInvoice.status === "paid"))) throw new Error("Credit memos may only be issued against paid invoices");
    if (!((((creditMemoNumber).trim()).length > 0))) throw new Error("Credit memo number is required");
    if (!((amount > 0))) throw new Error("Credit memo amount must be positive");
    if (!((amount <= __rel_sourceInvoice.amountPaid))) throw new Error("Credit memo amount cannot exceed the amount paid");
    if (!((((reason).trim()).length > 0))) throw new Error("Credit memo reason is required");
    if (!(((disposition !== "apply_to_balance") || (targetInvoiceId != null)))) throw new Error("Choose an open invoice when applying the credit now");
    if (!(((disposition !== "carry_forward") || (targetInvoiceId == null)))) throw new Error("Carry-forward credit cannot select a target invoice");
    if (!(((targetInvoiceId == null) || (__rel_targetInvoice != null)))) throw new Error("Selected target invoice was not found");
    if (!(((targetInvoiceId == null) || (targetInvoiceId !== sourceInvoiceId)))) throw new Error("Credit must be applied to a different invoice");
    if (!(((targetInvoiceId == null) || (__rel_targetInvoice.clientId === clientId)))) throw new Error("Target invoice must belong to the same client");
    if (!((((((targetInvoiceId == null) || (__rel_targetInvoice.status === "sent")) || (__rel_targetInvoice.status === "viewed")) || (__rel_targetInvoice.status === "overdue")) || (__rel_targetInvoice.status === "partial")))) throw new Error("Credit can only be applied to an open invoice");
    if (!(((targetInvoiceId == null) || (amount <= __rel_targetInvoice.amountDue)))) throw new Error("Credit memo amount cannot exceed the target invoice balance");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = String(((targetInvoiceId == null) ? "available" : "applied"));
        const __allowed: Record<string, string[]> = { "draft": ["available", "applied"], "available": ["applied"], "applied": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      sourceInvoiceId: sourceInvoiceId,
      clientId: clientId,
      targetInvoiceId: targetInvoiceId,
      eventId: ((eventId != null) ? eventId : doc.eventId),
      creditMemoNumber: creditMemoNumber,
      amount: amount,
      remainingAmount: ((targetInvoiceId == null) ? amount : 0),
      reason: reason,
      disposition: disposition,
      status: ((targetInvoiceId == null) ? "available" : "applied"),
      issuedAt: Date.now(),
      appliedAt: ((targetInvoiceId == null) ? null : Date.now()),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, creditMemoId: docId, tenantId: __after.tenantId, sourceInvoiceId: sourceInvoiceId, targetInvoiceId: targetInvoiceId, clientId: clientId, eventId: ((eventId != null) ? eventId : __after.eventId), creditMemoNumber: creditMemoNumber, amount: amount, remainingAmount: ((targetInvoiceId == null) ? amount : 0), disposition: disposition, _subject: { entity: "CreditMemo", command: "issue", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "CreditMemoIssued", entity: "CreditMemo", entityId: docId, payload: { creditMemoId: docId, tenantId: __after.tenantId, sourceInvoiceId: sourceInvoiceId, targetInvoiceId: targetInvoiceId, clientId: clientId, eventId: ((eventId != null) ? eventId : __after.eventId), creditMemoNumber: creditMemoNumber, amount: amount, remainingAmount: ((targetInvoiceId == null) ? amount : 0), disposition: disposition }, createdAt: Date.now() });
    // Reactions
    const reactionTarget0 = payload.sourceInvoiceId;
    if (reactionTarget0) await __runInvoiceRecordCreditMemo(ctx, { docId: reactionTarget0, creditAmount: payload.amount, creditMemoId: payload.creditMemoId } as any);
    const __fanId1 = payload.targetInvoiceId;
    const __fanDoc1 = __fanId1 != null && __fanId1 !== "" ? await ctx.db.get(__fanId1 as any) : null;
    const fanRows1 = __fanDoc1 != null && (__fanDoc1 as any).deletedAt == null ? [__fanDoc1] : [];
    for (const __row of fanRows1) {
      const target = __row;
      await __runInvoiceApplyCredit(ctx, { docId: (__row as any)._id, creditAmount: payload.amount, creditMemoId: payload.creditMemoId } as any);
    }
    return { ...doc, ...updates };
}

export const CreditMemo_issue = mutation({
  args: {
    docId: v.id("creditMemos"),
    sourceInvoiceId: v.string(),
    clientId: v.string(),
    creditMemoNumber: v.string(),
    amount: v.number(),
    reason: v.string(),
    disposition: v.any(),
    targetInvoiceId: v.optional(v.string()),
    eventId: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runCreditMemoIssue(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "CreditMemo_issue", __result);
    }
    return __result;
  },
});

export const CreditMemo_createViaIssue = mutation({
  args: {
    sourceInvoiceId: v.string(),
    clientId: v.string(),
    creditMemoNumber: v.string(),
    amount: v.number(),
    reason: v.string(),
    disposition: v.any(),
    targetInvoiceId: v.optional(v.string()),
    eventId: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { sourceInvoiceId, clientId, creditMemoNumber, amount, reason, disposition, targetInvoiceId, eventId } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      createdAt: Date.now(),
      updatedAt: Date.now(),
      status: "draft",
      amount: args.amount,
      clientId: args.clientId,
      creditMemoNumber: args.creditMemoNumber,
      disposition: args.disposition,
      eventId: args.eventId,
      reason: args.reason,
      sourceInvoiceId: args.sourceInvoiceId,
      targetInvoiceId: args.targetInvoiceId
    };
    const __rel_sourceInvoice = await __resolveRelation(ctx, "invoices", [__auth.tenantId, __draft.sourceInvoiceId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_client = await __resolveRelation(ctx, "clients", [__auth.tenantId, __draft.clientId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_targetInvoice = await __resolveRelation(ctx, "invoices", [__auth.tenantId, __draft.targetInvoiceId], ["tenantId","id"], "tenantId", __auth.tenantId);
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may read credit memos");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may write credit memos through commands");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may execute credit memo commands");
    if (!((__draft.issuedAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.status === "draft"))) throw new Error("Guard 1 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!(((__rel_sourceInvoice != null) && (__rel_sourceInvoice.deletedAt == null)))) throw new Error("Guard 3 failed");
    if (!((__rel_client != null))) throw new Error("Guard 4 failed");
    if (!((sourceInvoiceId === __draft.sourceInvoiceId))) throw new Error("Source invoice must match the seeded invoice reference");
    if (!((clientId === __draft.clientId))) throw new Error("Client must match the seeded client reference");
    if (!((__rel_sourceInvoice.clientId === clientId))) throw new Error("Credit memo client must match the paid source invoice");
    if (!((__rel_sourceInvoice.status === "paid"))) throw new Error("Credit memos may only be issued against paid invoices");
    if (!((((creditMemoNumber).trim()).length > 0))) throw new Error("Credit memo number is required");
    if (!((amount > 0))) throw new Error("Credit memo amount must be positive");
    if (!((amount <= __rel_sourceInvoice.amountPaid))) throw new Error("Credit memo amount cannot exceed the amount paid");
    if (!((((reason).trim()).length > 0))) throw new Error("Credit memo reason is required");
    if (!(((disposition !== "apply_to_balance") || (targetInvoiceId != null)))) throw new Error("Choose an open invoice when applying the credit now");
    if (!(((disposition !== "carry_forward") || (targetInvoiceId == null)))) throw new Error("Carry-forward credit cannot select a target invoice");
    if (!(((targetInvoiceId == null) || (__rel_targetInvoice != null)))) throw new Error("Selected target invoice was not found");
    if (!(((targetInvoiceId == null) || (targetInvoiceId !== sourceInvoiceId)))) throw new Error("Credit must be applied to a different invoice");
    if (!(((targetInvoiceId == null) || (__rel_targetInvoice.clientId === clientId)))) throw new Error("Target invoice must belong to the same client");
    if (!((((((targetInvoiceId == null) || (__rel_targetInvoice.status === "sent")) || (__rel_targetInvoice.status === "viewed")) || (__rel_targetInvoice.status === "overdue")) || (__rel_targetInvoice.status === "partial")))) throw new Error("Credit can only be applied to an open invoice");
    if (!(((targetInvoiceId == null) || (amount <= __rel_targetInvoice.amountDue)))) throw new Error("Credit memo amount cannot exceed the target invoice balance");
    const doc: Record<string, any> = {
      ...__draft,
      sourceInvoiceId: sourceInvoiceId,
      clientId: clientId,
      targetInvoiceId: targetInvoiceId,
      eventId: ((eventId != null) ? eventId : __draft.eventId),
      creditMemoNumber: creditMemoNumber,
      amount: amount,
      remainingAmount: ((targetInvoiceId == null) ? amount : 0),
      reason: reason,
      disposition: disposition,
      status: ((targetInvoiceId == null) ? "available" : "applied"),
      issuedAt: Date.now(),
      appliedAt: ((targetInvoiceId == null) ? null : Date.now()),
      version: 1,
    };
    const docId = await ctx.db.insert("creditMemos", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, creditMemoId: docId, tenantId: doc.tenantId, sourceInvoiceId: doc.sourceInvoiceId, targetInvoiceId: doc.targetInvoiceId, clientId: doc.clientId, eventId: ((doc.eventId != null) ? doc.eventId : doc.eventId), creditMemoNumber: doc.creditMemoNumber, amount: doc.amount, remainingAmount: ((doc.targetInvoiceId == null) ? doc.amount : 0), disposition: doc.disposition, _subject: { entity: "CreditMemo", command: "issue", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "CreditMemoIssued", entity: "CreditMemo", entityId: docId, payload: { creditMemoId: docId, tenantId: doc.tenantId, sourceInvoiceId: doc.sourceInvoiceId, targetInvoiceId: doc.targetInvoiceId, clientId: doc.clientId, eventId: ((doc.eventId != null) ? doc.eventId : doc.eventId), creditMemoNumber: doc.creditMemoNumber, amount: doc.amount, remainingAmount: ((doc.targetInvoiceId == null) ? doc.amount : 0), disposition: doc.disposition }, createdAt: Date.now() });
    // Reactions
    const reactionTarget0 = payload.sourceInvoiceId;
    if (reactionTarget0) await __runInvoiceRecordCreditMemo(ctx, { docId: reactionTarget0, creditAmount: payload.amount, creditMemoId: payload.creditMemoId } as any);
    const __fanId1 = payload.targetInvoiceId;
    const __fanDoc1 = __fanId1 != null && __fanId1 !== "" ? await ctx.db.get(__fanId1 as any) : null;
    const fanRows1 = __fanDoc1 != null && (__fanDoc1 as any).deletedAt == null ? [__fanDoc1] : [];
    for (const __row of fanRows1) {
      const target = __row;
      await __runInvoiceApplyCredit(ctx, { docId: (__row as any)._id, creditAmount: payload.amount, creditMemoId: payload.creditMemoId } as any);
    }
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "CreditMemo_createViaIssue", __result);
    }
    return __result;
  },
});

async function __runCreditMemoReassignClient(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("CreditMemo not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("CreditMemo not found");
    const __rel_clientMergeAuthorization = await __resolveRelation(ctx, "clientMerges", [__auth.tenantId, doc.clientMergeAuthorizationId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_mergeTargetClient = await __resolveRelation(ctx, "clients", [__auth.tenantId, doc.mergeTargetClientId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).clientMergeAuthorizationId;
      ((doc as any) as any).clientMergeAuthorization = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    {
      const __fk = ((doc as any) as any).mergeTargetClientId;
      ((doc as any) as any).mergeTargetClient = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may read credit memos");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may write credit memos through commands");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may execute credit memo commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!(checkRole(user, "adminAccess"))) throw new Error("Guard 1 failed");
    if (!((__rel_clientMergeAuthorization != null))) throw new Error("Guard 2 failed");
    if (!((__rel_clientMergeAuthorization.deletedAt == null))) throw new Error("Guard 3 failed");
    if (!((__rel_clientMergeAuthorization.mergedAt != null))) throw new Error("Guard 4 failed");
    if (!((__rel_mergeTargetClient != null))) throw new Error("Guard 5 failed");
    if (!((__rel_mergeTargetClient.status === "active"))) throw new Error("Guard 6 failed");
    if (!((__rel_mergeTargetClient.deletedAt == null))) throw new Error("Guard 7 failed");
    if (!((__rel_clientMergeAuthorization.duplicateClientId === doc.clientId))) throw new Error("Merge authorization does not match the credit-memo client");
    if (!((__rel_clientMergeAuthorization.primaryClientId === doc.mergeTargetClientId))) throw new Error("Merge authorization does not match the primary client");
    const previousClientId = doc.clientId;
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      clientId: doc.mergeTargetClientId,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, creditMemoId: docId, tenantId: __after.tenantId, previousClientId: previousClientId, clientId: __after.mergeTargetClientId, _subject: { entity: "CreditMemo", command: "reassignClient", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "CreditMemoClientReassigned", entity: "CreditMemo", entityId: docId, payload: { creditMemoId: docId, tenantId: __after.tenantId, previousClientId: previousClientId, clientId: __after.mergeTargetClientId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const CreditMemo_reassignClient = mutation({
  args: {
    docId: v.id("creditMemos"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runCreditMemoReassignClient(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "CreditMemo_reassignClient", __result);
    }
    return __result;
  },
});

async function __runCreditMemoStageClientMerge(ctx: MutationCtx, { docId, clientMergeId, clientId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("CreditMemo not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("CreditMemo not found");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may read credit memos");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may write credit memos through commands");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may execute credit memo commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!(checkRole(user, "adminAccess"))) throw new Error("Guard 1 failed");
    if (!((clientId !== doc.clientId))) throw new Error("Credit memo already belongs to this client");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      clientMergeAuthorizationId: clientMergeId,
      mergeTargetClientId: clientId,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, creditMemoId: docId, tenantId: __after.tenantId, _subject: { entity: "CreditMemo", command: "stageClientMerge", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "CreditMemoClientMergeStaged", entity: "CreditMemo", entityId: docId, payload: { creditMemoId: docId, tenantId: __after.tenantId }, createdAt: Date.now() });
    // Reactions
    const reactionTarget0 = payload.creditMemoId;
    if (reactionTarget0) await __runCreditMemoReassignClient(ctx, { docId: reactionTarget0 } as any);
    return { ...doc, ...updates };
}

export const CreditMemo_stageClientMerge = mutation({
  args: {
    docId: v.id("creditMemos"),
    clientMergeId: v.string(),
    clientId: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runCreditMemoStageClientMerge(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "CreditMemo_stageClientMerge", __result);
    }
    return __result;
  },
});

async function __runCutoverDecisionCreate(ctx: MutationCtx, args: any) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc: Record<string, any> = {
      tenantId: __auth.tenantId,
      tppReadOnlyAt: args.tppReadOnlyAt,
      status: "not_started",
      decidedAt: Date.now(),
      decidedBy: user.id,
      reason: args.reason,
      rollbackPlan: args.rollbackPlan,
      businessApproved: ((args.businessApproved != null) ? args.businessApproved : false)
    };
    if (!((user.id != null))) throw new Error("Authenticated staff may read cutover decisions");
    if (!((user.id != null))) throw new Error("Authenticated staff may write cutover decisions");
    if (!((user.id != null))) throw new Error("Authenticated staff may execute cutover decision commands");
    if (!(checkRole(user, "adminAccess"))) throw new Error("Guard 0 failed");
    if (!((((args.reason).trim()).length > 0))) throw new Error("Reason is required to open a cutover decision");
    const _id = await ctx.db.insert("cutoverDecisions", doc as any);
    return { _id, ...doc };

}

export const CutoverDecision_create = mutation({
  args: {
    tppReadOnlyAt: v.optional(v.union(v.number(), v.null())),
    reason: v.string(),
    rollbackPlan: v.string(),
    businessApproved: v.optional(v.boolean()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runCutoverDecisionCreate(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "CutoverDecision_create", __result);
    }
    return __result;
  },
});

async function __runCutoverDecisionExecute(ctx: MutationCtx, { docId, decision, reason }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("CutoverDecision not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("CutoverDecision not found");
    if (!((user.id != null))) throw new Error("Authenticated staff may read cutover decisions");
    if (!((user.id != null))) throw new Error("Authenticated staff may write cutover decisions");
    if (!((user.id != null))) throw new Error("Authenticated staff may execute cutover decision commands");
    if (!(checkRole(user, "adminAccess"))) throw new Error("Guard 0 failed");
    const updates = {
      status: decision,
      reason: reason,
      decidedAt: Date.now(),
      decidedBy: user.id
    };
    await ctx.db.patch(docId, updates as any);
    return { ...doc, ...updates };
}

export const CutoverDecision_execute = mutation({
  args: {
    docId: v.id("cutoverDecisions"),
    decision: v.any(),
    reason: v.string(),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runCutoverDecisionExecute(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "CutoverDecision_execute", __result);
    }
    return __result;
  },
});

async function __runCutoverDecisionRecordApprovals(ctx: MutationCtx, { docId, businessApproved, rollbackPlan }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("CutoverDecision not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("CutoverDecision not found");
    if (!((user.id != null))) throw new Error("Authenticated staff may read cutover decisions");
    if (!((user.id != null))) throw new Error("Authenticated staff may write cutover decisions");
    if (!((user.id != null))) throw new Error("Authenticated staff may execute cutover decision commands");
    if (!(checkRole(user, "adminAccess"))) throw new Error("Guard 0 failed");
    const updates = {
      businessApproved: businessApproved,
      rollbackPlan: rollbackPlan,
      decidedAt: Date.now(),
      decidedBy: user.id
    };
    await ctx.db.patch(docId, updates as any);
    return { ...doc, ...updates };
}

export const CutoverDecision_recordApprovals = mutation({
  args: {
    docId: v.id("cutoverDecisions"),
    businessApproved: v.boolean(),
    rollbackPlan: v.string(),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runCutoverDecisionRecordApprovals(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "CutoverDecision_recordApprovals", __result);
    }
    return __result;
  },
});

async function __runCutoverDecisionRollback(ctx: MutationCtx, { docId, reason }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("CutoverDecision not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("CutoverDecision not found");
    if (!((user.id != null))) throw new Error("Authenticated staff may read cutover decisions");
    if (!((user.id != null))) throw new Error("Authenticated staff may write cutover decisions");
    if (!((user.id != null))) throw new Error("Authenticated staff may execute cutover decision commands");
    if (!((doc.status === "go"))) throw new Error("Guard 0 failed");
    if (!(checkRole(user, "adminAccess"))) throw new Error("Guard 1 failed");
    const updates = {
      status: "rolled_back",
      reason: reason,
      decidedAt: Date.now(),
      decidedBy: user.id
    };
    await ctx.db.patch(docId, updates as any);
    return { ...doc, ...updates };
}

export const CutoverDecision_rollback = mutation({
  args: {
    docId: v.id("cutoverDecisions"),
    reason: v.string(),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runCutoverDecisionRollback(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "CutoverDecision_rollback", __result);
    }
    return __result;
  },
});

async function __runCutoverDecisionSetTppReadOnly(ctx: MutationCtx, { docId, reason }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("CutoverDecision not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("CutoverDecision not found");
    if (!((user.id != null))) throw new Error("Authenticated staff may read cutover decisions");
    if (!((user.id != null))) throw new Error("Authenticated staff may write cutover decisions");
    if (!((user.id != null))) throw new Error("Authenticated staff may execute cutover decision commands");
    if (!((doc.status === "go"))) throw new Error("Guard 0 failed");
    if (!(checkRole(user, "adminAccess"))) throw new Error("Guard 1 failed");
    const updates = {
      tppReadOnlyAt: Date.now()
    };
    await ctx.db.patch(docId, updates as any);
    return { ...doc, ...updates };
}

export const CutoverDecision_setTppReadOnly = mutation({
  args: {
    docId: v.id("cutoverDecisions"),
    reason: v.string(),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runCutoverDecisionSetTppReadOnly(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "CutoverDecision_setTppReadOnly", __result);
    }
    return __result;
  },
});

async function __runDeliveryCancel(ctx: MutationCtx, { docId, reason, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("Delivery not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("Delivery not found");
    const doc = await __decryptDoc(ctx, "Delivery", ["notes"], __storedDoc) as Record<string, any>;
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may read deliveries");
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may write deliveries through commands");
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may execute delivery commands");
    if (!(((doc.status === "scheduled") || (doc.status === "in_transit")))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(checkRole(user, "logisticsManageAccess"))) throw new Error("Guard 2 failed");
    if (!((((reason).trim()).length > 0))) throw new Error("Cancellation reason is required");
    const previousStatus = doc.status;
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "cancelled";
        const __allowed: Record<string, string[]> = { "scheduled": ["in_transit", "failed", "cancelled"], "in_transit": ["delivered", "failed", "cancelled"], "delivered": [], "failed": [], "cancelled": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "cancelled",
      cancelledAt: Date.now(),
      cancellationReason: reason,
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "Delivery", ["notes"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, deliveryId: docId, tenantId: __after.tenantId, packListId: __after.packListId, eventId: __after.eventId, driverId: __after.driverId, previousStatus: previousStatus, status: "cancelled", reason: reason, _subject: { entity: "Delivery", command: "cancel", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "DeliveryCancelled", entity: "Delivery", entityId: docId, payload: { deliveryId: docId, tenantId: __after.tenantId, packListId: __after.packListId, eventId: __after.eventId, driverId: __after.driverId, previousStatus: previousStatus, status: "cancelled", reason: reason }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Delivery_cancel = mutation({
  args: {
    docId: v.id("deliveries"),
    reason: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runDeliveryCancel(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Delivery_cancel", __result);
    }
    return __result;
  },
});

async function __runDeliveryConfirmDelivery(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("Delivery not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("Delivery not found");
    const doc = await __decryptDoc(ctx, "Delivery", ["notes"], __storedDoc) as Record<string, any>;
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may read deliveries");
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may write deliveries through commands");
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may execute delivery commands");
    if (!((doc.status === "in_transit"))) throw new Error("Guard 0 failed");
    if (!((doc.departedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((user.id != null))) throw new Error("Guard 3 failed");
    if (!(((doc.driverId === user.id) || checkRole(user, "logisticsManageAccess")))) throw new Error("Guard 4 failed");
    const previousStatus = doc.status;
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "delivered";
        const __allowed: Record<string, string[]> = { "scheduled": ["in_transit", "failed", "cancelled"], "in_transit": ["delivered", "failed", "cancelled"], "delivered": [], "failed": [], "cancelled": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "delivered",
      deliveredAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "Delivery", ["notes"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, deliveryId: docId, tenantId: __after.tenantId, packListId: __after.packListId, eventId: __after.eventId, driverId: __after.driverId, previousStatus: previousStatus, status: "delivered", deliveredAt: __after.deliveredAt, _subject: { entity: "Delivery", command: "confirmDelivery", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "DeliveryConfirmed", entity: "Delivery", entityId: docId, payload: { deliveryId: docId, tenantId: __after.tenantId, packListId: __after.packListId, eventId: __after.eventId, driverId: __after.driverId, previousStatus: previousStatus, status: "delivered", deliveredAt: __after.deliveredAt }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Delivery_confirmDelivery = mutation({
  args: {
    docId: v.id("deliveries"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runDeliveryConfirmDelivery(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Delivery_confirmDelivery", __result);
    }
    return __result;
  },
});

async function __runDeliveryMarkFailed(ctx: MutationCtx, { docId, reason, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("Delivery not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("Delivery not found");
    const doc = await __decryptDoc(ctx, "Delivery", ["notes"], __storedDoc) as Record<string, any>;
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may read deliveries");
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may write deliveries through commands");
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may execute delivery commands");
    if (!(((doc.status === "scheduled") || (doc.status === "in_transit")))) throw new Error("Guard 0 failed");
    if (!((doc.scheduledAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!(checkRole(user, "logisticsManageAccess"))) throw new Error("Guard 3 failed");
    if (!((((reason).trim()).length > 0))) throw new Error("Failure reason is required");
    const previousStatus = doc.status;
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "failed";
        const __allowed: Record<string, string[]> = { "scheduled": ["in_transit", "failed", "cancelled"], "in_transit": ["delivered", "failed", "cancelled"], "delivered": [], "failed": [], "cancelled": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "failed",
      failedAt: Date.now(),
      failureReason: reason,
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "Delivery", ["notes"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, deliveryId: docId, tenantId: __after.tenantId, packListId: __after.packListId, eventId: __after.eventId, driverId: __after.driverId, previousStatus: previousStatus, status: "failed", reason: reason, _subject: { entity: "Delivery", command: "markFailed", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "DeliveryFailed", entity: "Delivery", entityId: docId, payload: { deliveryId: docId, tenantId: __after.tenantId, packListId: __after.packListId, eventId: __after.eventId, driverId: __after.driverId, previousStatus: previousStatus, status: "failed", reason: reason }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Delivery_markFailed = mutation({
  args: {
    docId: v.id("deliveries"),
    reason: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runDeliveryMarkFailed(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Delivery_markFailed", __result);
    }
    return __result;
  },
});

async function __runDeliverySchedule(ctx: MutationCtx, { docId, packListId, eventId, destination, windowStartsAt, windowEndsAt, driverId, notes, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("Delivery not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("Delivery not found");
    const doc = await __decryptDoc(ctx, "Delivery", ["notes"], __storedDoc) as Record<string, any>;
    const __rel_packList = await __resolveRelation(ctx, "packLists", [__auth.tenantId, doc.packListId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_event = await __resolveRelation(ctx, "events", [__auth.tenantId, doc.eventId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).packListId;
      ((doc as any) as any).packList = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    {
      const __fk = ((doc as any) as any).eventId;
      ((doc as any) as any).event = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may read deliveries");
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may write deliveries through commands");
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may execute delivery commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.status === "scheduled"))) throw new Error("Guard 1 failed");
    if (!(((__rel_packList != null) && (((__rel_packList.status === "packed") || (__rel_packList.status === "loaded")) || (__rel_packList.status === "dispatched"))))) throw new Error("Guard 2 failed");
    if (!((__rel_event != null))) throw new Error("Guard 3 failed");
    if (!(((doc.scheduledAt == null) || (doc.packListId === packListId)))) throw new Error("Guard 4 failed");
    if (!((packListId === doc.packListId))) throw new Error("Schedule packListId must match the seeded pack list reference");
    if (!((eventId === doc.eventId))) throw new Error("Schedule eventId must match the seeded event reference");
    if (!((((driverId == null) || (doc.driverId == null)) || (driverId === doc.driverId)))) throw new Error("Schedule driverId must match the seeded driver reference when provided");
    if (!(((((destination).trim()).length > 0) || (((doc.destination).trim()).length > 0)))) throw new Error("Delivery destination is required");
    if (!(((doc.scheduledAt != null) || (windowEndsAt > windowStartsAt)))) throw new Error("Delivery window end must be after its start");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      packListId: packListId,
      eventId: eventId,
      destination: ((((doc.destination).trim()).length > 0) ? doc.destination : destination),
      windowStartsAt: ((doc.windowStartsAt != null) ? doc.windowStartsAt : windowStartsAt),
      windowEndsAt: ((doc.windowEndsAt != null) ? doc.windowEndsAt : windowEndsAt),
      driverId: ((driverId != null) ? driverId : doc.driverId),
      notes: ((notes != null) ? notes : doc.notes),
      scheduledAt: ((doc.scheduledAt != null) ? doc.scheduledAt : Date.now()),
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "Delivery", ["notes"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, deliveryId: docId, tenantId: __after.tenantId, packListId: packListId, eventId: eventId, driverId: ((driverId != null) ? driverId : __after.driverId), destination: ((((__after.destination).trim()).length > 0) ? __after.destination : destination), windowStartsAt: ((__after.windowStartsAt != null) ? __after.windowStartsAt : windowStartsAt), windowEndsAt: ((__after.windowEndsAt != null) ? __after.windowEndsAt : windowEndsAt), status: "scheduled", _subject: { entity: "Delivery", command: "schedule", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "DeliveryScheduled", entity: "Delivery", entityId: docId, payload: { deliveryId: docId, tenantId: __after.tenantId, packListId: packListId, eventId: eventId, driverId: ((driverId != null) ? driverId : __after.driverId), destination: ((((__after.destination).trim()).length > 0) ? __after.destination : destination), windowStartsAt: ((__after.windowStartsAt != null) ? __after.windowStartsAt : windowStartsAt), windowEndsAt: ((__after.windowEndsAt != null) ? __after.windowEndsAt : windowEndsAt), status: "scheduled" }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Delivery_schedule = mutation({
  args: {
    docId: v.id("deliveries"),
    packListId: v.string(),
    eventId: v.string(),
    destination: v.string(),
    windowStartsAt: v.number(),
    windowEndsAt: v.number(),
    driverId: v.optional(v.string()),
    notes: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runDeliverySchedule(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Delivery_schedule", __result);
    }
    return __result;
  },
});

export const Delivery_createViaSchedule = mutation({
  args: {
    packListId: v.string(),
    eventId: v.string(),
    destination: v.string(),
    windowStartsAt: v.number(),
    windowEndsAt: v.number(),
    driverId: v.optional(v.string()),
    notes: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { packListId, eventId, destination, windowStartsAt, windowEndsAt, driverId, notes } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      status: "scheduled",
      createdAt: Date.now(),
      updatedAt: Date.now(),
      destination: args.destination,
      driverId: args.driverId,
      eventId: args.eventId,
      notes: args.notes,
      packListId: args.packListId,
      windowEndsAt: args.windowEndsAt,
      windowStartsAt: args.windowStartsAt
    };
    const __rel_packList = await __resolveRelation(ctx, "packLists", [__auth.tenantId, __draft.packListId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_event = await __resolveRelation(ctx, "events", [__auth.tenantId, __draft.eventId], ["tenantId","id"], "tenantId", __auth.tenantId);
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may read deliveries");
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may write deliveries through commands");
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may execute delivery commands");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.status === "scheduled"))) throw new Error("Guard 1 failed");
    if (!(((__rel_packList != null) && (((__rel_packList.status === "packed") || (__rel_packList.status === "loaded")) || (__rel_packList.status === "dispatched"))))) throw new Error("Guard 2 failed");
    if (!((__rel_event != null))) throw new Error("Guard 3 failed");
    if (!(((__draft.scheduledAt == null) || (__draft.packListId === packListId)))) throw new Error("Guard 4 failed");
    if (!((packListId === __draft.packListId))) throw new Error("Schedule packListId must match the seeded pack list reference");
    if (!((eventId === __draft.eventId))) throw new Error("Schedule eventId must match the seeded event reference");
    if (!((((driverId == null) || (__draft.driverId == null)) || (driverId === __draft.driverId)))) throw new Error("Schedule driverId must match the seeded driver reference when provided");
    if (!(((((destination).trim()).length > 0) || (((__draft.destination).trim()).length > 0)))) throw new Error("Delivery destination is required");
    if (!(((__draft.scheduledAt != null) || (windowEndsAt > windowStartsAt)))) throw new Error("Delivery window end must be after its start");
    const doc: Record<string, any> = {
      ...__draft,
      packListId: packListId,
      eventId: eventId,
      destination: ((((__draft.destination).trim()).length > 0) ? __draft.destination : destination),
      windowStartsAt: ((__draft.windowStartsAt != null) ? __draft.windowStartsAt : windowStartsAt),
      windowEndsAt: ((__draft.windowEndsAt != null) ? __draft.windowEndsAt : windowEndsAt),
      driverId: ((driverId != null) ? driverId : __draft.driverId),
      notes: ((notes != null) ? notes : __draft.notes),
      scheduledAt: ((__draft.scheduledAt != null) ? __draft.scheduledAt : Date.now()),
      version: 1,
    };
    const __storedDoc = await __encryptDoc(ctx, "Delivery", ["notes"], doc);
    const docId = await ctx.db.insert("deliveries", __storedDoc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, deliveryId: docId, tenantId: doc.tenantId, packListId: doc.packListId, eventId: doc.eventId, driverId: ((doc.driverId != null) ? doc.driverId : doc.driverId), destination: ((((doc.destination).trim()).length > 0) ? doc.destination : doc.destination), windowStartsAt: ((doc.windowStartsAt != null) ? doc.windowStartsAt : doc.windowStartsAt), windowEndsAt: ((doc.windowEndsAt != null) ? doc.windowEndsAt : doc.windowEndsAt), status: "scheduled", _subject: { entity: "Delivery", command: "schedule", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "DeliveryScheduled", entity: "Delivery", entityId: docId, payload: { deliveryId: docId, tenantId: doc.tenantId, packListId: doc.packListId, eventId: doc.eventId, driverId: ((doc.driverId != null) ? doc.driverId : doc.driverId), destination: ((((doc.destination).trim()).length > 0) ? doc.destination : doc.destination), windowStartsAt: ((doc.windowStartsAt != null) ? doc.windowStartsAt : doc.windowStartsAt), windowEndsAt: ((doc.windowEndsAt != null) ? doc.windowEndsAt : doc.windowEndsAt), status: "scheduled" }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Delivery_createViaSchedule", __result);
    }
    return __result;
  },
});

async function __runDeliveryStartTransit(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("Delivery not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("Delivery not found");
    const doc = await __decryptDoc(ctx, "Delivery", ["notes"], __storedDoc) as Record<string, any>;
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may read deliveries");
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may write deliveries through commands");
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may execute delivery commands");
    if (!((doc.status === "scheduled"))) throw new Error("Guard 0 failed");
    if (!((doc.scheduledAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.driverId != null))) throw new Error("Guard 2 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 3 failed");
    if (!((user.id != null))) throw new Error("Guard 4 failed");
    if (!(((doc.driverId === user.id) || checkRole(user, "logisticsManageAccess")))) throw new Error("Guard 5 failed");
    const previousStatus = doc.status;
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "in_transit";
        const __allowed: Record<string, string[]> = { "scheduled": ["in_transit", "failed", "cancelled"], "in_transit": ["delivered", "failed", "cancelled"], "delivered": [], "failed": [], "cancelled": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "in_transit",
      departedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "Delivery", ["notes"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, deliveryId: docId, tenantId: __after.tenantId, packListId: __after.packListId, eventId: __after.eventId, driverId: __after.driverId, previousStatus: previousStatus, status: "in_transit", departedAt: __after.departedAt, _subject: { entity: "Delivery", command: "startTransit", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "DeliveryTransitStarted", entity: "Delivery", entityId: docId, payload: { deliveryId: docId, tenantId: __after.tenantId, packListId: __after.packListId, eventId: __after.eventId, driverId: __after.driverId, previousStatus: previousStatus, status: "in_transit", departedAt: __after.departedAt }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Delivery_startTransit = mutation({
  args: {
    docId: v.id("deliveries"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runDeliveryStartTransit(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Delivery_startTransit", __result);
    }
    return __result;
  },
});

async function __runDishClassifyAllergens(ctx: MutationCtx, { docId, allergenSummary, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Dish not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Dish not found");
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "salesAccess")))) throw new Error("Kitchen and sales staff may read dishes");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write dishes through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute dish commands");
    if (!((doc.status === "active"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      allergenSummary: allergenSummary,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, dishId: docId, tenantId: __after.tenantId, allergenSummary: allergenSummary, _subject: { entity: "Dish", command: "classifyAllergens", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "DishAllergensClassified", entity: "Dish", entityId: docId, payload: { dishId: docId, tenantId: __after.tenantId, allergenSummary: allergenSummary }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Dish_classifyAllergens = mutation({
  args: {
    docId: v.id("dishes"),
    allergenSummary: v.array(v.any()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runDishClassifyAllergens(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Dish_classifyAllergens", __result);
    }
    return __result;
  },
});

async function __runDishClearPrimaryImage(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Dish not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Dish not found");
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "salesAccess")))) throw new Error("Kitchen and sales staff may read dishes");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write dishes through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute dish commands");
    if (!((doc.status === "active"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      primaryImageStorageId: null,
      primaryImageFileName: null,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, dishId: docId, tenantId: __after.tenantId, _subject: { entity: "Dish", command: "clearPrimaryImage", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "DishPrimaryImageCleared", entity: "Dish", entityId: docId, payload: { dishId: docId, tenantId: __after.tenantId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Dish_clearPrimaryImage = mutation({
  args: {
    docId: v.id("dishes"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runDishClearPrimaryImage(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Dish_clearPrimaryImage", __result);
    }
    return __result;
  },
});

async function __runDishIntroduce(ctx: MutationCtx, { docId, name, portionSize, portionUnit, description, category, course, serviceStyle, dietaryTags, allergenSummary, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Dish not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Dish not found");
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "salesAccess")))) throw new Error("Kitchen and sales staff may read dishes");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write dishes through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute dish commands");
    if (!((doc.introducedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((((name).trim()).length > 0))) throw new Error("Dish name is required");
    if (!((portionSize > 0))) throw new Error("Dish portion size must be positive");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      name: name,
      portionSize: portionSize,
      portionUnit: portionUnit,
      description: description,
      category: category,
      course: course,
      serviceStyle: serviceStyle,
      dietaryTags: ((dietaryTags != null) ? dietaryTags : []),
      allergenSummary: ((allergenSummary != null) ? allergenSummary : []),
      introducedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, dishId: docId, tenantId: __after.tenantId, name: name, portionSize: portionSize, portionUnit: portionUnit, allergenSummary: ((allergenSummary != null) ? allergenSummary : []), _subject: { entity: "Dish", command: "introduce", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "DishIntroduced", entity: "Dish", entityId: docId, payload: { dishId: docId, tenantId: __after.tenantId, name: name, portionSize: portionSize, portionUnit: portionUnit, allergenSummary: ((allergenSummary != null) ? allergenSummary : []) }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Dish_introduce = mutation({
  args: {
    docId: v.id("dishes"),
    name: v.string(),
    portionSize: v.number(),
    portionUnit: v.any(),
    description: v.optional(v.string()),
    category: v.optional(v.string()),
    course: v.optional(v.string()),
    serviceStyle: v.optional(v.string()),
    dietaryTags: v.optional(v.array(v.string())),
    allergenSummary: v.optional(v.array(v.any())),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runDishIntroduce(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Dish_introduce", __result);
    }
    return __result;
  },
});

export const Dish_createViaIntroduce = mutation({
  args: {
    name: v.string(),
    portionSize: v.number(),
    portionUnit: v.any(),
    description: v.optional(v.string()),
    category: v.optional(v.string()),
    course: v.optional(v.string()),
    serviceStyle: v.optional(v.string()),
    dietaryTags: v.optional(v.array(v.string())),
    allergenSummary: v.optional(v.array(v.any())),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { name, portionSize, portionUnit, description, category, course, serviceStyle, dietaryTags, allergenSummary } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      dietaryTags: args.dietaryTags !== undefined ? args.dietaryTags : [],
      allergenSummary: args.allergenSummary !== undefined ? args.allergenSummary : [],
      status: "active",
      editionNumber: 1,
      createdAt: Date.now(),
      updatedAt: Date.now(),
      category: args.category,
      course: args.course,
      description: args.description,
      name: args.name,
      portionSize: args.portionSize,
      portionUnit: args.portionUnit,
      serviceStyle: args.serviceStyle
    };
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "salesAccess")))) throw new Error("Kitchen and sales staff may read dishes");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write dishes through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute dish commands");
    if (!((__draft.introducedAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((((name).trim()).length > 0))) throw new Error("Dish name is required");
    if (!((portionSize > 0))) throw new Error("Dish portion size must be positive");
    const doc: Record<string, any> = {
      ...__draft,
      name: name,
      portionSize: portionSize,
      portionUnit: portionUnit,
      description: description,
      category: category,
      course: course,
      serviceStyle: serviceStyle,
      dietaryTags: ((dietaryTags != null) ? dietaryTags : []),
      allergenSummary: ((allergenSummary != null) ? allergenSummary : []),
      introducedAt: Date.now(),
      version: 1,
    };
    const docId = await ctx.db.insert("dishes", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, dishId: docId, tenantId: doc.tenantId, name: doc.name, portionSize: doc.portionSize, portionUnit: doc.portionUnit, allergenSummary: ((doc.allergenSummary != null) ? doc.allergenSummary : []), _subject: { entity: "Dish", command: "introduce", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "DishIntroduced", entity: "Dish", entityId: docId, payload: { dishId: docId, tenantId: doc.tenantId, name: doc.name, portionSize: doc.portionSize, portionUnit: doc.portionUnit, allergenSummary: ((doc.allergenSummary != null) ? doc.allergenSummary : []) }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Dish_createViaIntroduce", __result);
    }
    return __result;
  },
});

async function __runDishLinkAsEdition(ctx: MutationCtx, { docId, sourceDishId, editionNumber, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Dish not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Dish not found");
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "salesAccess")))) throw new Error("Kitchen and sales staff may read dishes");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write dishes through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute dish commands");
    if (!((doc.introducedAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((doc.mergedIntoDishId == null))) throw new Error("Guard 2 failed");
    if (!((sourceDishId !== doc.id))) throw new Error("A dish cannot be an edition of itself");
    if (!(((editionNumber == null) || (editionNumber > 0)))) throw new Error("Edition number must be positive");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      canonicalDishId: sourceDishId,
      editionNumber: ((editionNumber != null) ? editionNumber : doc.editionNumber),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, dishId: docId, tenantId: __after.tenantId, sourceDishId: sourceDishId, name: __after.name, editionNumber: ((editionNumber != null) ? editionNumber : __after.editionNumber), _subject: { entity: "Dish", command: "linkAsEdition", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "DishEditionCreated", entity: "Dish", entityId: docId, payload: { dishId: docId, tenantId: __after.tenantId, sourceDishId: sourceDishId, name: __after.name, editionNumber: ((editionNumber != null) ? editionNumber : __after.editionNumber) }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Dish_linkAsEdition = mutation({
  args: {
    docId: v.id("dishes"),
    sourceDishId: v.string(),
    editionNumber: v.optional(v.number()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runDishLinkAsEdition(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Dish_linkAsEdition", __result);
    }
    return __result;
  },
});

async function __runDishMergeInto(ctx: MutationCtx, { docId, targetDishId, reason, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Dish not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Dish not found");
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "salesAccess")))) throw new Error("Kitchen and sales staff may read dishes");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write dishes through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute dish commands");
    if (!((doc.status === "active"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((doc.mergedIntoDishId == null))) throw new Error("Guard 2 failed");
    if (!((targetDishId !== doc.id))) throw new Error("Cannot merge a dish into itself");
    if (!((((reason).trim()).length > 0))) throw new Error("Merge reason is required");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "retired";
        const __allowed: Record<string, string[]> = { "active": ["retired"], "retired": ["active"] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      mergedIntoDishId: targetDishId,
      status: "retired",
      retiredAt: Date.now(),
      retirementReason: reason,
      deletedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, dishId: docId, tenantId: __after.tenantId, targetDishId: targetDishId, reason: reason, _subject: { entity: "Dish", command: "mergeInto", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "DishMerged", entity: "Dish", entityId: docId, payload: { dishId: docId, tenantId: __after.tenantId, targetDishId: targetDishId, reason: reason }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Dish_mergeInto = mutation({
  args: {
    docId: v.id("dishes"),
    targetDishId: v.string(),
    reason: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runDishMergeInto(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Dish_mergeInto", __result);
    }
    return __result;
  },
});

async function __runDishPurge(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Dish not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Dish not found");
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "salesAccess")))) throw new Error("Kitchen and sales staff may read dishes");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write dishes through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute dish commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Guard 1 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "retired";
        const __allowed: Record<string, string[]> = { "active": ["retired"], "retired": ["active"] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "retired",
      retiredAt: ((doc.retiredAt != null) ? doc.retiredAt : Date.now()),
      retirementReason: "Deleted",
      deletedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, dishId: docId, tenantId: __after.tenantId, reason: "Deleted", _subject: { entity: "Dish", command: "purge", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "DishPurged", entity: "Dish", entityId: docId, payload: { dishId: docId, tenantId: __after.tenantId, reason: "Deleted" }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Dish_purge = mutation({
  args: {
    docId: v.id("dishes"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runDishPurge(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Dish_purge", __result);
    }
    return __result;
  },
});

async function __runDishReinstate(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Dish not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Dish not found");
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "salesAccess")))) throw new Error("Kitchen and sales staff may read dishes");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write dishes through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute dish commands");
    if (!((doc.status === "retired"))) throw new Error("Guard 0 failed");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Guard 1 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "active";
        const __allowed: Record<string, string[]> = { "active": ["retired"], "retired": ["active"] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "active",
      retiredAt: null,
      retirementReason: null,
      deletedAt: null,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, dishId: docId, tenantId: __after.tenantId, _subject: { entity: "Dish", command: "reinstate", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "DishReinstated", entity: "Dish", entityId: docId, payload: { dishId: docId, tenantId: __after.tenantId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Dish_reinstate = mutation({
  args: {
    docId: v.id("dishes"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runDishReinstate(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Dish_reinstate", __result);
    }
    return __result;
  },
});

async function __runDishRetire(ctx: MutationCtx, { docId, reason, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Dish not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Dish not found");
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "salesAccess")))) throw new Error("Kitchen and sales staff may read dishes");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write dishes through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute dish commands");
    if (!((doc.status === "active"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Guard 2 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "retired";
        const __allowed: Record<string, string[]> = { "active": ["retired"], "retired": ["active"] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "retired",
      retiredAt: Date.now(),
      retirementReason: ((reason != null) ? reason : "Deleted"),
      deletedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, dishId: docId, tenantId: __after.tenantId, reason: ((reason != null) ? reason : "Deleted"), _subject: { entity: "Dish", command: "retire", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "DishRetired", entity: "Dish", entityId: docId, payload: { dishId: docId, tenantId: __after.tenantId, reason: ((reason != null) ? reason : "Deleted") }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Dish_retire = mutation({
  args: {
    docId: v.id("dishes"),
    reason: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runDishRetire(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Dish_retire", __result);
    }
    return __result;
  },
});

async function __runDishReviseDetails(ctx: MutationCtx, { docId, name, description, category, course, serviceStyle, dietaryTags, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Dish not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Dish not found");
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "salesAccess")))) throw new Error("Kitchen and sales staff may read dishes");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write dishes through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute dish commands");
    if (!((doc.status === "active"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((((name).trim()).length > 0))) throw new Error("Dish name is required");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      name: name,
      description: description,
      category: category,
      course: course,
      serviceStyle: serviceStyle,
      dietaryTags: ((dietaryTags != null) ? dietaryTags : doc.dietaryTags),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, dishId: docId, tenantId: __after.tenantId, name: name, _subject: { entity: "Dish", command: "reviseDetails", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "DishDetailsRevised", entity: "Dish", entityId: docId, payload: { dishId: docId, tenantId: __after.tenantId, name: name }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Dish_reviseDetails = mutation({
  args: {
    docId: v.id("dishes"),
    name: v.string(),
    description: v.optional(v.string()),
    category: v.optional(v.string()),
    course: v.optional(v.string()),
    serviceStyle: v.optional(v.string()),
    dietaryTags: v.optional(v.array(v.string())),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runDishReviseDetails(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Dish_reviseDetails", __result);
    }
    return __result;
  },
});

async function __runDishSetPrimaryImage(ctx: MutationCtx, { docId, storageId, fileName, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Dish not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Dish not found");
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "salesAccess")))) throw new Error("Kitchen and sales staff may read dishes");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write dishes through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute dish commands");
    if (!((doc.status === "active"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((((storageId).trim()).length > 0))) throw new Error("Primary image storage id is required");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      primaryImageStorageId: storageId,
      primaryImageFileName: fileName,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, dishId: docId, tenantId: __after.tenantId, storageId: storageId, _subject: { entity: "Dish", command: "setPrimaryImage", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "DishPrimaryImageSet", entity: "Dish", entityId: docId, payload: { dishId: docId, tenantId: __after.tenantId, storageId: storageId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Dish_setPrimaryImage = mutation({
  args: {
    docId: v.id("dishes"),
    storageId: v.string(),
    fileName: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runDishSetPrimaryImage(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Dish_setPrimaryImage", __result);
    }
    return __result;
  },
});

async function __runDishUpdatePortioning(ctx: MutationCtx, { docId, portionSize, portionUnit, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Dish not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Dish not found");
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "salesAccess")))) throw new Error("Kitchen and sales staff may read dishes");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write dishes through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute dish commands");
    if (!((doc.status === "active"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((portionSize > 0))) throw new Error("Dish portion size must be positive");
    const previousPortionSize = doc.portionSize;
    const previousPortionUnit = doc.portionUnit;
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      portionSize: portionSize,
      portionUnit: portionUnit,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, dishId: docId, tenantId: __after.tenantId, previousPortionSize: previousPortionSize, previousPortionUnit: previousPortionUnit, portionSize: portionSize, portionUnit: portionUnit, _subject: { entity: "Dish", command: "updatePortioning", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "DishPortioningUpdated", entity: "Dish", entityId: docId, payload: { dishId: docId, tenantId: __after.tenantId, previousPortionSize: previousPortionSize, previousPortionUnit: previousPortionUnit, portionSize: portionSize, portionUnit: portionUnit }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Dish_updatePortioning = mutation({
  args: {
    docId: v.id("dishes"),
    portionSize: v.number(),
    portionUnit: v.any(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runDishUpdatePortioning(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Dish_updatePortioning", __result);
    }
    return __result;
  },
});

async function __runDishComponentAttach(ctx: MutationCtx, { docId, dishId, componentId, yieldQuantity, batchMultiplier, sortOrder, role, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("DishComponent not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("DishComponent not found");
    const __rel_dish = await __resolveRelation(ctx, "dishes", [__auth.tenantId, doc.dishId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).dishId;
      ((doc as any) as any).dish = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may read dish component composition");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write dish component composition through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute dish component composition commands");
    if (!((doc.attachedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(((__rel_dish != null) && (__rel_dish.status === "active")))) throw new Error("Guard 2 failed");
    if (!((dishId === doc.dishId))) throw new Error("Attach dishId must match the seeded dish reference");
    if (!((yieldQuantity > 0))) throw new Error("Attached component yield must be positive");
    if (!(((batchMultiplier == null) || (batchMultiplier > 0)))) throw new Error("Attached component batch multiplier must be positive");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      dishId: dishId,
      componentId: componentId,
      yieldQuantity: yieldQuantity,
      batchMultiplier: ((batchMultiplier != null) ? batchMultiplier : 1),
      sortOrder: ((sortOrder != null) ? sortOrder : 0),
      role: role,
      attachedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, dishComponentId: docId, tenantId: __after.tenantId, dishId: dishId, componentId: componentId, yieldQuantity: yieldQuantity, batchMultiplier: ((batchMultiplier != null) ? batchMultiplier : 1), sortOrder: ((sortOrder != null) ? sortOrder : 0), role: role, _subject: { entity: "DishComponent", command: "attach", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "DishComponentAttached", entity: "DishComponent", entityId: docId, payload: { dishComponentId: docId, tenantId: __after.tenantId, dishId: dishId, componentId: componentId, yieldQuantity: yieldQuantity, batchMultiplier: ((batchMultiplier != null) ? batchMultiplier : 1), sortOrder: ((sortOrder != null) ? sortOrder : 0), role: role }, createdAt: Date.now() });
    // Reactions
    const fanRows0 = (await ctx.db.query("eventDishes").withIndex("by_dishId", (q) => q.eq("dishId", payload.dishId)).collect()).filter((d) => (d as any).deletedAt == null);
    for (const __row of fanRows0) {
      const target = __row;
      const __fanMatch0_raw = await ctx.db.query("eventDishComponentSeeds").withIndex("by_eventDishId", (q) => q.eq("eventDishId", (__row as any)._id)).collect();
      const __fanMatch0_rows = __fanMatch0_raw.filter((d) => (d as any).eventDishId === (__row as any)._id && (d as any).componentId === payload.componentId && (d as any).deletedAt == null).sort((a, b) => String((a as any)._id).localeCompare(String((b as any)._id)));
      const __fanMatch0_id = __fanMatch0_rows.length > 0 ? (__fanMatch0_rows[0] as any)._id : null;
      if (__fanMatch0_id) {
        await __runEventDishComponentSeedSeed(ctx, { docId: __fanMatch0_id, eventId: __row.eventId, eventDishId: (__row as any)._id, dishId: payload.dishId, componentId: payload.componentId, servings: __row.quantityServings, yieldQuantity: payload.yieldQuantity, batchMultiplier: payload.batchMultiplier, purchasingWeekStart: __row.purchasingWeekStart } as any);
      } else {
        const __elseArgs: Record<string, any> = { eventId: __row.eventId, eventDishId: (__row as any)._id, dishId: payload.dishId, componentId: payload.componentId, servings: __row.quantityServings, yieldQuantity: payload.yieldQuantity, batchMultiplier: payload.batchMultiplier, purchasingWeekStart: __row.purchasingWeekStart };
        const __elseDoc: Record<string, any> = {
          tenantId: __auth.tenantId,
          servings: 0,
          yieldQuantity: 1,
          batchMultiplier: 1,
          createdAt: Date.now(),
          updatedAt: Date.now(),
          version: 0,
        };
        for (const __k of ["deletedAt","eventId","eventDishId","dishId","componentId","servings","yieldQuantity","batchMultiplier","purchasingWeekStart","seededAt","createdAt","updatedAt"] as string[]) {
          if (__elseArgs[__k] !== undefined) __elseDoc[__k] = __elseArgs[__k];
        }
        const __elseId = await ctx.db.insert("eventDishComponentSeeds", __elseDoc as any);
        await __runEventDishComponentSeedSeed(ctx, { docId: __elseId, ...__elseArgs } as any);
      }
    }
    return { ...doc, ...updates };
}

export const DishComponent_attach = mutation({
  args: {
    docId: v.id("dishComponents"),
    dishId: v.string(),
    componentId: v.string(),
    yieldQuantity: v.number(),
    batchMultiplier: v.optional(v.number()),
    sortOrder: v.optional(v.any()),
    role: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runDishComponentAttach(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "DishComponent_attach", __result);
    }
    return __result;
  },
});

export const DishComponent_createViaAttach = mutation({
  args: {
    dishId: v.string(),
    componentId: v.string(),
    yieldQuantity: v.number(),
    batchMultiplier: v.optional(v.number()),
    sortOrder: v.optional(v.any()),
    role: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { dishId, componentId, yieldQuantity, batchMultiplier, sortOrder, role } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      sortOrder: args.sortOrder !== undefined ? args.sortOrder : 0,
      batchMultiplier: args.batchMultiplier !== undefined ? args.batchMultiplier : 1,
      createdAt: Date.now(),
      updatedAt: Date.now(),
      componentId: args.componentId,
      dishId: args.dishId,
      role: args.role,
      yieldQuantity: args.yieldQuantity
    };
    const __rel_dish = await __resolveRelation(ctx, "dishes", [__auth.tenantId, __draft.dishId], ["tenantId","id"], "tenantId", __auth.tenantId);
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may read dish component composition");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write dish component composition through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute dish component composition commands");
    if (!((__draft.attachedAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(((__rel_dish != null) && (__rel_dish.status === "active")))) throw new Error("Guard 2 failed");
    if (!((dishId === __draft.dishId))) throw new Error("Attach dishId must match the seeded dish reference");
    if (!((yieldQuantity > 0))) throw new Error("Attached component yield must be positive");
    if (!(((batchMultiplier == null) || (batchMultiplier > 0)))) throw new Error("Attached component batch multiplier must be positive");
    const doc: Record<string, any> = {
      ...__draft,
      dishId: dishId,
      componentId: componentId,
      yieldQuantity: yieldQuantity,
      batchMultiplier: ((batchMultiplier != null) ? batchMultiplier : 1),
      sortOrder: ((sortOrder != null) ? sortOrder : 0),
      role: role,
      attachedAt: Date.now(),
      version: 1,
    };
    const docId = await ctx.db.insert("dishComponents", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, dishComponentId: docId, tenantId: doc.tenantId, dishId: doc.dishId, componentId: doc.componentId, yieldQuantity: doc.yieldQuantity, batchMultiplier: ((doc.batchMultiplier != null) ? doc.batchMultiplier : 1), sortOrder: ((doc.sortOrder != null) ? doc.sortOrder : 0), role: doc.role, _subject: { entity: "DishComponent", command: "attach", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "DishComponentAttached", entity: "DishComponent", entityId: docId, payload: { dishComponentId: docId, tenantId: doc.tenantId, dishId: doc.dishId, componentId: doc.componentId, yieldQuantity: doc.yieldQuantity, batchMultiplier: ((doc.batchMultiplier != null) ? doc.batchMultiplier : 1), sortOrder: ((doc.sortOrder != null) ? doc.sortOrder : 0), role: doc.role }, createdAt: Date.now() });
    // Reactions
    const fanRows0 = (await ctx.db.query("eventDishes").withIndex("by_dishId", (q) => q.eq("dishId", payload.dishId)).collect()).filter((d) => (d as any).deletedAt == null);
    for (const __row of fanRows0) {
      const target = __row;
      const __fanMatch0_raw = await ctx.db.query("eventDishComponentSeeds").withIndex("by_eventDishId", (q) => q.eq("eventDishId", (__row as any)._id)).collect();
      const __fanMatch0_rows = __fanMatch0_raw.filter((d) => (d as any).eventDishId === (__row as any)._id && (d as any).componentId === payload.componentId && (d as any).deletedAt == null).sort((a, b) => String((a as any)._id).localeCompare(String((b as any)._id)));
      const __fanMatch0_id = __fanMatch0_rows.length > 0 ? (__fanMatch0_rows[0] as any)._id : null;
      if (__fanMatch0_id) {
        await __runEventDishComponentSeedSeed(ctx, { docId: __fanMatch0_id, eventId: __row.eventId, eventDishId: (__row as any)._id, dishId: payload.dishId, componentId: payload.componentId, servings: __row.quantityServings, yieldQuantity: payload.yieldQuantity, batchMultiplier: payload.batchMultiplier, purchasingWeekStart: __row.purchasingWeekStart } as any);
      } else {
        const __elseArgs: Record<string, any> = { eventId: __row.eventId, eventDishId: (__row as any)._id, dishId: payload.dishId, componentId: payload.componentId, servings: __row.quantityServings, yieldQuantity: payload.yieldQuantity, batchMultiplier: payload.batchMultiplier, purchasingWeekStart: __row.purchasingWeekStart };
        const __elseDoc: Record<string, any> = {
          tenantId: __auth.tenantId,
          servings: 0,
          yieldQuantity: 1,
          batchMultiplier: 1,
          createdAt: Date.now(),
          updatedAt: Date.now(),
          version: 0,
        };
        for (const __k of ["deletedAt","eventId","eventDishId","dishId","componentId","servings","yieldQuantity","batchMultiplier","purchasingWeekStart","seededAt","createdAt","updatedAt"] as string[]) {
          if (__elseArgs[__k] !== undefined) __elseDoc[__k] = __elseArgs[__k];
        }
        const __elseId = await ctx.db.insert("eventDishComponentSeeds", __elseDoc as any);
        await __runEventDishComponentSeedSeed(ctx, { docId: __elseId, ...__elseArgs } as any);
      }
    }
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "DishComponent_createViaAttach", __result);
    }
    return __result;
  },
});

async function __runDishComponentDetach(ctx: MutationCtx, { docId, reason, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("DishComponent not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("DishComponent not found");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may read dish component composition");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write dish component composition through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute dish component composition commands");
    if (!((doc.attachedAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((((reason).trim()).length > 0))) throw new Error("Detach reason is required");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      deletedAt: Date.now(),
      removedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, dishComponentId: docId, tenantId: __after.tenantId, dishId: __after.dishId, componentId: __after.componentId, reason: reason, _subject: { entity: "DishComponent", command: "detach", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "DishComponentDetached", entity: "DishComponent", entityId: docId, payload: { dishComponentId: docId, tenantId: __after.tenantId, dishId: __after.dishId, componentId: __after.componentId, reason: reason }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const DishComponent_detach = mutation({
  args: {
    docId: v.id("dishComponents"),
    reason: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runDishComponentDetach(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "DishComponent_detach", __result);
    }
    return __result;
  },
});

async function __runDishContainerDefine(ctx: MutationCtx, { docId, dishId, name, serviceMethod, servingsPerContainer, baseQuantity, unit, equipmentNotes, handlingNotes, sortOrder, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("DishContainer not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("DishContainer not found");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may read dish containers");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write dish containers through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute dish container commands");
    if (!((doc.addedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((((name).trim()).length > 0))) throw new Error("Container name is required");
    if (!((servingsPerContainer >= 1))) throw new Error("Servings per container must be at least 1");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      dishId: dishId,
      name: name,
      serviceMethod: serviceMethod,
      servingsPerContainer: servingsPerContainer,
      baseQuantity: ((baseQuantity != null) ? baseQuantity : 0),
      unit: ((unit != null) ? unit : "each"),
      equipmentNotes: equipmentNotes,
      handlingNotes: handlingNotes,
      sortOrder: ((sortOrder != null) ? sortOrder : 0),
      addedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, dishContainerId: docId, tenantId: __after.tenantId, dishId: dishId, name: name, serviceMethod: serviceMethod, _subject: { entity: "DishContainer", command: "define", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "DishContainerDefined", entity: "DishContainer", entityId: docId, payload: { dishContainerId: docId, tenantId: __after.tenantId, dishId: dishId, name: name, serviceMethod: serviceMethod }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const DishContainer_define = mutation({
  args: {
    docId: v.id("dishContainers"),
    dishId: v.string(),
    name: v.string(),
    serviceMethod: v.any(),
    servingsPerContainer: v.number(),
    baseQuantity: v.optional(v.number()),
    unit: v.optional(v.any()),
    equipmentNotes: v.optional(v.string()),
    handlingNotes: v.optional(v.string()),
    sortOrder: v.optional(v.number()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runDishContainerDefine(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "DishContainer_define", __result);
    }
    return __result;
  },
});

export const DishContainer_createViaDefine = mutation({
  args: {
    dishId: v.string(),
    name: v.string(),
    serviceMethod: v.any(),
    servingsPerContainer: v.number(),
    baseQuantity: v.optional(v.number()),
    unit: v.optional(v.any()),
    equipmentNotes: v.optional(v.string()),
    handlingNotes: v.optional(v.string()),
    sortOrder: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { dishId, name, serviceMethod, servingsPerContainer, baseQuantity, unit, equipmentNotes, handlingNotes, sortOrder } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      baseQuantity: args.baseQuantity !== undefined ? args.baseQuantity : 0,
      unit: args.unit !== undefined ? args.unit : "each",
      sortOrder: args.sortOrder !== undefined ? args.sortOrder : 0,
      status: "active",
      createdAt: Date.now(),
      updatedAt: Date.now(),
      dishId: args.dishId,
      equipmentNotes: args.equipmentNotes,
      handlingNotes: args.handlingNotes,
      name: args.name,
      serviceMethod: args.serviceMethod,
      servingsPerContainer: args.servingsPerContainer
    };
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may read dish containers");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write dish containers through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute dish container commands");
    if (!((__draft.addedAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((((name).trim()).length > 0))) throw new Error("Container name is required");
    if (!((servingsPerContainer >= 1))) throw new Error("Servings per container must be at least 1");
    const doc: Record<string, any> = {
      ...__draft,
      dishId: dishId,
      name: name,
      serviceMethod: serviceMethod,
      servingsPerContainer: servingsPerContainer,
      baseQuantity: ((baseQuantity != null) ? baseQuantity : 0),
      unit: ((unit != null) ? unit : "each"),
      equipmentNotes: equipmentNotes,
      handlingNotes: handlingNotes,
      sortOrder: ((sortOrder != null) ? sortOrder : 0),
      addedAt: Date.now(),
      version: 1,
    };
    const docId = await ctx.db.insert("dishContainers", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, dishContainerId: docId, tenantId: doc.tenantId, dishId: doc.dishId, name: doc.name, serviceMethod: doc.serviceMethod, _subject: { entity: "DishContainer", command: "define", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "DishContainerDefined", entity: "DishContainer", entityId: docId, payload: { dishContainerId: docId, tenantId: doc.tenantId, dishId: doc.dishId, name: doc.name, serviceMethod: doc.serviceMethod }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "DishContainer_createViaDefine", __result);
    }
    return __result;
  },
});

async function __runDishContainerReinstate(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("DishContainer not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("DishContainer not found");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may read dish containers");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write dish containers through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute dish container commands");
    if (!((doc.status === "retired"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "active";
        const __allowed: Record<string, string[]> = { "active": ["retired"], "retired": ["active"] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "active",
      retiredAt: null,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, dishContainerId: docId, tenantId: __after.tenantId, dishId: __after.dishId, _subject: { entity: "DishContainer", command: "reinstate", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "DishContainerReinstated", entity: "DishContainer", entityId: docId, payload: { dishContainerId: docId, tenantId: __after.tenantId, dishId: __after.dishId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const DishContainer_reinstate = mutation({
  args: {
    docId: v.id("dishContainers"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runDishContainerReinstate(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "DishContainer_reinstate", __result);
    }
    return __result;
  },
});

async function __runDishContainerRetire(ctx: MutationCtx, { docId, reason, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("DishContainer not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("DishContainer not found");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may read dish containers");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write dish containers through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute dish container commands");
    if (!((doc.status === "active"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "retired";
        const __allowed: Record<string, string[]> = { "active": ["retired"], "retired": ["active"] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "retired",
      retiredAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, dishContainerId: docId, tenantId: __after.tenantId, dishId: __after.dishId, _subject: { entity: "DishContainer", command: "retire", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "DishContainerRetired", entity: "DishContainer", entityId: docId, payload: { dishContainerId: docId, tenantId: __after.tenantId, dishId: __after.dishId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const DishContainer_retire = mutation({
  args: {
    docId: v.id("dishContainers"),
    reason: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runDishContainerRetire(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "DishContainer_retire", __result);
    }
    return __result;
  },
});

async function __runDishContainerRevise(ctx: MutationCtx, { docId, name, serviceMethod, servingsPerContainer, baseQuantity, unit, equipmentNotes, handlingNotes, sortOrder, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("DishContainer not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("DishContainer not found");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may read dish containers");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write dish containers through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute dish container commands");
    if (!((doc.addedAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((((name).trim()).length > 0))) throw new Error("Container name is required");
    if (!((servingsPerContainer >= 1))) throw new Error("Servings per container must be at least 1");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      name: name,
      serviceMethod: serviceMethod,
      servingsPerContainer: servingsPerContainer,
      baseQuantity: ((baseQuantity != null) ? baseQuantity : 0),
      unit: ((unit != null) ? unit : "each"),
      equipmentNotes: equipmentNotes,
      handlingNotes: handlingNotes,
      sortOrder: ((sortOrder != null) ? sortOrder : 0),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, dishContainerId: docId, tenantId: __after.tenantId, dishId: __after.dishId, name: name, serviceMethod: serviceMethod, _subject: { entity: "DishContainer", command: "revise", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "DishContainerRevised", entity: "DishContainer", entityId: docId, payload: { dishContainerId: docId, tenantId: __after.tenantId, dishId: __after.dishId, name: name, serviceMethod: serviceMethod }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const DishContainer_revise = mutation({
  args: {
    docId: v.id("dishContainers"),
    name: v.string(),
    serviceMethod: v.any(),
    servingsPerContainer: v.number(),
    baseQuantity: v.optional(v.number()),
    unit: v.optional(v.any()),
    equipmentNotes: v.optional(v.string()),
    handlingNotes: v.optional(v.string()),
    sortOrder: v.optional(v.number()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runDishContainerRevise(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "DishContainer_revise", __result);
    }
    return __result;
  },
});

async function __runDishIngredientAdd(ctx: MutationCtx, { docId, dishId, ingredientId, quantity, unit, sortOrder, wasteFactor, prepNotes, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("DishIngredient not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("DishIngredient not found");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may read dish ingredient lines");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write dish ingredient lines through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute dish ingredient line commands");
    if (!((doc.addedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((quantity > 0))) throw new Error("Dish ingredient quantity must be positive");
    if (!(((wasteFactor == null) || (wasteFactor > 0)))) throw new Error("Waste factor must be greater than 0");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      dishId: dishId,
      ingredientId: ingredientId,
      quantity: quantity,
      unit: unit,
      wasteFactor: ((wasteFactor != null) ? wasteFactor : 1),
      sortOrder: ((sortOrder != null) ? sortOrder : 0),
      prepNotes: prepNotes,
      addedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, dishIngredientId: docId, tenantId: __after.tenantId, dishId: dishId, ingredientId: ingredientId, quantity: quantity, unit: unit, wasteFactor: ((wasteFactor != null) ? wasteFactor : 1), _subject: { entity: "DishIngredient", command: "add", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "DishIngredientAdded", entity: "DishIngredient", entityId: docId, payload: { dishIngredientId: docId, tenantId: __after.tenantId, dishId: dishId, ingredientId: ingredientId, quantity: quantity, unit: unit, wasteFactor: ((wasteFactor != null) ? wasteFactor : 1) }, createdAt: Date.now() });
    // Reactions
    const fanRows0 = (await ctx.db.query("eventDishes").withIndex("by_dishId", (q) => q.eq("dishId", payload.dishId)).collect()).filter((d) => (d as any).deletedAt == null);
    for (const __row of fanRows0) {
      const target = __row;
      const __fanMatch0_raw = await ctx.db.query("eventIngredientContributions").withIndex("by_eventDishId", (q) => q.eq("eventDishId", (__row as any)._id)).collect();
      const __fanMatch0_rows = __fanMatch0_raw.filter((d) => (d as any).eventDishId === (__row as any)._id && (d as any).componentId === null && (d as any).ingredientId === payload.ingredientId && (d as any).unit === payload.unit && (d as any).deletedAt == null).sort((a, b) => String((a as any)._id).localeCompare(String((b as any)._id)));
      const __fanMatch0_id = __fanMatch0_rows.length > 0 ? (__fanMatch0_rows[0] as any)._id : null;
      if (__fanMatch0_id) {
        await __runEventIngredientContributionRecord(ctx, { docId: __fanMatch0_id, eventId: __row.eventId, eventDishId: (__row as any)._id, dishId: payload.dishId, componentId: null, ingredientId: payload.ingredientId, quantity: (payload.quantity * __row.quantityServings), unit: payload.unit, servings: __row.quantityServings, purchasingWeekStart: __row.purchasingWeekStart } as any);
      } else {
        const __elseArgs: Record<string, any> = { eventId: __row.eventId, eventDishId: (__row as any)._id, dishId: payload.dishId, componentId: null, ingredientId: payload.ingredientId, quantity: (payload.quantity * __row.quantityServings), unit: payload.unit, servings: __row.quantityServings, purchasingWeekStart: __row.purchasingWeekStart };
        const __elseDoc: Record<string, any> = {
          tenantId: __auth.tenantId,
          quantity: 0,
          unit: "each",
          servings: 0,
          createdAt: Date.now(),
          updatedAt: Date.now(),
          version: 0,
        };
        for (const __k of ["deletedAt","eventId","eventDishId","dishId","componentId","ingredientId","quantity","unit","servings","purchasingWeekStart","recordedAt","createdAt","updatedAt"] as string[]) {
          if (__elseArgs[__k] !== undefined) __elseDoc[__k] = __elseArgs[__k];
        }
        const __elseId = await ctx.db.insert("eventIngredientContributions", __elseDoc as any);
        await __runEventIngredientContributionRecord(ctx, { docId: __elseId, ...__elseArgs } as any);
      }
    }
    return { ...doc, ...updates };
}

export const DishIngredient_add = mutation({
  args: {
    docId: v.id("dishIngredients"),
    dishId: v.string(),
    ingredientId: v.string(),
    quantity: v.number(),
    unit: v.any(),
    sortOrder: v.optional(v.any()),
    wasteFactor: v.optional(v.number()),
    prepNotes: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runDishIngredientAdd(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "DishIngredient_add", __result);
    }
    return __result;
  },
});

export const DishIngredient_createViaAdd = mutation({
  args: {
    dishId: v.string(),
    ingredientId: v.string(),
    quantity: v.number(),
    unit: v.any(),
    sortOrder: v.optional(v.any()),
    wasteFactor: v.optional(v.number()),
    prepNotes: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { dishId, ingredientId, quantity, unit, sortOrder, wasteFactor, prepNotes } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      sortOrder: args.sortOrder !== undefined ? args.sortOrder : 0,
      wasteFactor: args.wasteFactor !== undefined ? args.wasteFactor : 1,
      createdAt: Date.now(),
      updatedAt: Date.now(),
      dishId: args.dishId,
      ingredientId: args.ingredientId,
      prepNotes: args.prepNotes,
      quantity: args.quantity,
      unit: args.unit
    };
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may read dish ingredient lines");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write dish ingredient lines through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute dish ingredient line commands");
    if (!((__draft.addedAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((quantity > 0))) throw new Error("Dish ingredient quantity must be positive");
    if (!(((wasteFactor == null) || (wasteFactor > 0)))) throw new Error("Waste factor must be greater than 0");
    const doc: Record<string, any> = {
      ...__draft,
      dishId: dishId,
      ingredientId: ingredientId,
      quantity: quantity,
      unit: unit,
      wasteFactor: ((wasteFactor != null) ? wasteFactor : 1),
      sortOrder: ((sortOrder != null) ? sortOrder : 0),
      prepNotes: prepNotes,
      addedAt: Date.now(),
      version: 1,
    };
    const docId = await ctx.db.insert("dishIngredients", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, dishIngredientId: docId, tenantId: doc.tenantId, dishId: doc.dishId, ingredientId: doc.ingredientId, quantity: doc.quantity, unit: doc.unit, wasteFactor: ((doc.wasteFactor != null) ? doc.wasteFactor : 1), _subject: { entity: "DishIngredient", command: "add", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "DishIngredientAdded", entity: "DishIngredient", entityId: docId, payload: { dishIngredientId: docId, tenantId: doc.tenantId, dishId: doc.dishId, ingredientId: doc.ingredientId, quantity: doc.quantity, unit: doc.unit, wasteFactor: ((doc.wasteFactor != null) ? doc.wasteFactor : 1) }, createdAt: Date.now() });
    // Reactions
    const fanRows0 = (await ctx.db.query("eventDishes").withIndex("by_dishId", (q) => q.eq("dishId", payload.dishId)).collect()).filter((d) => (d as any).deletedAt == null);
    for (const __row of fanRows0) {
      const target = __row;
      const __fanMatch0_raw = await ctx.db.query("eventIngredientContributions").withIndex("by_eventDishId", (q) => q.eq("eventDishId", (__row as any)._id)).collect();
      const __fanMatch0_rows = __fanMatch0_raw.filter((d) => (d as any).eventDishId === (__row as any)._id && (d as any).componentId === null && (d as any).ingredientId === payload.ingredientId && (d as any).unit === payload.unit && (d as any).deletedAt == null).sort((a, b) => String((a as any)._id).localeCompare(String((b as any)._id)));
      const __fanMatch0_id = __fanMatch0_rows.length > 0 ? (__fanMatch0_rows[0] as any)._id : null;
      if (__fanMatch0_id) {
        await __runEventIngredientContributionRecord(ctx, { docId: __fanMatch0_id, eventId: __row.eventId, eventDishId: (__row as any)._id, dishId: payload.dishId, componentId: null, ingredientId: payload.ingredientId, quantity: (payload.quantity * __row.quantityServings), unit: payload.unit, servings: __row.quantityServings, purchasingWeekStart: __row.purchasingWeekStart } as any);
      } else {
        const __elseArgs: Record<string, any> = { eventId: __row.eventId, eventDishId: (__row as any)._id, dishId: payload.dishId, componentId: null, ingredientId: payload.ingredientId, quantity: (payload.quantity * __row.quantityServings), unit: payload.unit, servings: __row.quantityServings, purchasingWeekStart: __row.purchasingWeekStart };
        const __elseDoc: Record<string, any> = {
          tenantId: __auth.tenantId,
          quantity: 0,
          unit: "each",
          servings: 0,
          createdAt: Date.now(),
          updatedAt: Date.now(),
          version: 0,
        };
        for (const __k of ["deletedAt","eventId","eventDishId","dishId","componentId","ingredientId","quantity","unit","servings","purchasingWeekStart","recordedAt","createdAt","updatedAt"] as string[]) {
          if (__elseArgs[__k] !== undefined) __elseDoc[__k] = __elseArgs[__k];
        }
        const __elseId = await ctx.db.insert("eventIngredientContributions", __elseDoc as any);
        await __runEventIngredientContributionRecord(ctx, { docId: __elseId, ...__elseArgs } as any);
      }
    }
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "DishIngredient_createViaAdd", __result);
    }
    return __result;
  },
});

async function __runDishIngredientAdjustQuantity(ctx: MutationCtx, { docId, quantity, unit, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("DishIngredient not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("DishIngredient not found");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may read dish ingredient lines");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write dish ingredient lines through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute dish ingredient line commands");
    if (!((doc.addedAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((quantity > 0))) throw new Error("Dish ingredient quantity must be positive");
    const previousQuantity = doc.quantity;
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      quantity: quantity,
      unit: unit,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, dishIngredientId: docId, tenantId: __after.tenantId, dishId: __after.dishId, ingredientId: __after.ingredientId, previousQuantity: previousQuantity, quantity: quantity, unit: unit, _subject: { entity: "DishIngredient", command: "adjustQuantity", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "DishIngredientQuantityAdjusted", entity: "DishIngredient", entityId: docId, payload: { dishIngredientId: docId, tenantId: __after.tenantId, dishId: __after.dishId, ingredientId: __after.ingredientId, previousQuantity: previousQuantity, quantity: quantity, unit: unit }, createdAt: Date.now() });
    // Reactions
    const fanRows0 = (await ctx.db.query("eventDishes").withIndex("by_dishId", (q) => q.eq("dishId", payload.dishId)).collect()).filter((d) => (d as any).deletedAt == null);
    for (const __row of fanRows0) {
      const target = __row;
      const __fanMatch0_raw = await ctx.db.query("eventIngredientContributions").withIndex("by_eventDishId", (q) => q.eq("eventDishId", (__row as any)._id)).collect();
      const __fanMatch0_rows = __fanMatch0_raw.filter((d) => (d as any).eventDishId === (__row as any)._id && (d as any).componentId === null && (d as any).ingredientId === payload.ingredientId && (d as any).unit === payload.unit && (d as any).deletedAt == null).sort((a, b) => String((a as any)._id).localeCompare(String((b as any)._id)));
      const __fanMatch0_id = __fanMatch0_rows.length > 0 ? (__fanMatch0_rows[0] as any)._id : null;
      if (__fanMatch0_id) {
        await __runEventIngredientContributionRecord(ctx, { docId: __fanMatch0_id, eventId: __row.eventId, eventDishId: (__row as any)._id, dishId: payload.dishId, componentId: null, ingredientId: payload.ingredientId, quantity: (payload.quantity * __row.quantityServings), unit: payload.unit, servings: __row.quantityServings, purchasingWeekStart: __row.purchasingWeekStart } as any);
      } else {
        const __elseArgs: Record<string, any> = { eventId: __row.eventId, eventDishId: (__row as any)._id, dishId: payload.dishId, componentId: null, ingredientId: payload.ingredientId, quantity: (payload.quantity * __row.quantityServings), unit: payload.unit, servings: __row.quantityServings, purchasingWeekStart: __row.purchasingWeekStart };
        const __elseDoc: Record<string, any> = {
          tenantId: __auth.tenantId,
          quantity: 0,
          unit: "each",
          servings: 0,
          createdAt: Date.now(),
          updatedAt: Date.now(),
          version: 0,
        };
        for (const __k of ["deletedAt","eventId","eventDishId","dishId","componentId","ingredientId","quantity","unit","servings","purchasingWeekStart","recordedAt","createdAt","updatedAt"] as string[]) {
          if (__elseArgs[__k] !== undefined) __elseDoc[__k] = __elseArgs[__k];
        }
        const __elseId = await ctx.db.insert("eventIngredientContributions", __elseDoc as any);
        await __runEventIngredientContributionRecord(ctx, { docId: __elseId, ...__elseArgs } as any);
      }
    }
    return { ...doc, ...updates };
}

export const DishIngredient_adjustQuantity = mutation({
  args: {
    docId: v.id("dishIngredients"),
    quantity: v.number(),
    unit: v.any(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runDishIngredientAdjustQuantity(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "DishIngredient_adjustQuantity", __result);
    }
    return __result;
  },
});

async function __runDishIngredientRemove(ctx: MutationCtx, { docId, reason, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("DishIngredient not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("DishIngredient not found");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may read dish ingredient lines");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write dish ingredient lines through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute dish ingredient line commands");
    if (!((doc.addedAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((((reason).trim()).length > 0))) throw new Error("Removal reason is required");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      deletedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, dishIngredientId: docId, tenantId: __after.tenantId, dishId: __after.dishId, ingredientId: __after.ingredientId, reason: reason, _subject: { entity: "DishIngredient", command: "remove", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "DishIngredientRemoved", entity: "DishIngredient", entityId: docId, payload: { dishIngredientId: docId, tenantId: __after.tenantId, dishId: __after.dishId, ingredientId: __after.ingredientId, reason: reason }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const DishIngredient_remove = mutation({
  args: {
    docId: v.id("dishIngredients"),
    reason: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runDishIngredientRemove(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "DishIngredient_remove", __result);
    }
    return __result;
  },
});

async function __runDishTaskAdd(ctx: MutationCtx, { docId, dishId, name, category, taskType, defaultQuantity, defaultUnit, station, sortOrder, componentId, ingredientId, instructions, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("DishTask not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("DishTask not found");
    const __rel_dish = await __resolveRelation(ctx, "dishes", [__auth.tenantId, doc.dishId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).dishId;
      ((doc as any) as any).dish = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may read dish task templates");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write dish task templates through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute dish task template commands");
    if (!((doc.status === "active"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(((__rel_dish != null) && (__rel_dish.status === "active")))) throw new Error("Guard 2 failed");
    if (!((dishId === doc.dishId))) throw new Error("Dish task dishId must match the seeded Dish reference");
    if (!((((name).trim()).length > 0))) throw new Error("Dish task name is required");
    if (!(((defaultQuantity == null) || (defaultQuantity > 0)))) throw new Error("Dish task quantity must be positive");
    if (!(((sortOrder == null) || (sortOrder >= 0)))) throw new Error("Dish task sort order cannot be negative");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      dishId: dishId,
      activeDishId: dishId,
      name: name,
      category: ((category != null) ? category : "finish_at_event"),
      taskType: ((taskType != null) ? taskType : "manual"),
      defaultQuantity: defaultQuantity,
      defaultUnit: defaultUnit,
      station: station,
      sortOrder: ((sortOrder != null) ? sortOrder : 0),
      componentId: componentId,
      ingredientId: ingredientId,
      instructions: instructions,
      activeAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, dishTaskId: docId, tenantId: __after.tenantId, dishId: dishId, name: name, category: ((category != null) ? category : "finish_at_event"), taskType: ((taskType != null) ? taskType : "manual"), componentId: componentId, ingredientId: ingredientId, defaultQuantity: defaultQuantity, defaultUnit: defaultUnit, sortOrder: ((sortOrder != null) ? sortOrder : 0), _subject: { entity: "DishTask", command: "add", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "DishTaskAdded", entity: "DishTask", entityId: docId, payload: { dishTaskId: docId, tenantId: __after.tenantId, dishId: dishId, name: name, category: ((category != null) ? category : "finish_at_event"), taskType: ((taskType != null) ? taskType : "manual"), componentId: componentId, ingredientId: ingredientId, defaultQuantity: defaultQuantity, defaultUnit: defaultUnit, sortOrder: ((sortOrder != null) ? sortOrder : 0) }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const DishTask_add = mutation({
  args: {
    docId: v.id("dishTasks"),
    dishId: v.string(),
    name: v.string(),
    category: v.optional(v.string()),
    taskType: v.optional(v.string()),
    defaultQuantity: v.optional(v.number()),
    defaultUnit: v.optional(v.any()),
    station: v.optional(v.string()),
    sortOrder: v.optional(v.any()),
    componentId: v.optional(v.string()),
    ingredientId: v.optional(v.string()),
    instructions: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runDishTaskAdd(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "DishTask_add", __result);
    }
    return __result;
  },
});

export const DishTask_createViaAdd = mutation({
  args: {
    dishId: v.string(),
    name: v.string(),
    category: v.optional(v.string()),
    taskType: v.optional(v.string()),
    defaultQuantity: v.optional(v.number()),
    defaultUnit: v.optional(v.any()),
    station: v.optional(v.string()),
    sortOrder: v.optional(v.any()),
    componentId: v.optional(v.string()),
    ingredientId: v.optional(v.string()),
    instructions: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { dishId, name, category, taskType, defaultQuantity, defaultUnit, station, sortOrder, componentId, ingredientId, instructions } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      category: args.category !== undefined ? args.category : "finish_at_event",
      taskType: args.taskType !== undefined ? args.taskType : "manual",
      sortOrder: args.sortOrder !== undefined ? args.sortOrder : 0,
      status: "active",
      createdAt: Date.now(),
      updatedAt: Date.now(),
      componentId: args.componentId,
      defaultQuantity: args.defaultQuantity,
      defaultUnit: args.defaultUnit,
      dishId: args.dishId,
      ingredientId: args.ingredientId,
      instructions: args.instructions,
      name: args.name,
      station: args.station
    };
    const __rel_dish = await __resolveRelation(ctx, "dishes", [__auth.tenantId, __draft.dishId], ["tenantId","id"], "tenantId", __auth.tenantId);
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may read dish task templates");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write dish task templates through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute dish task template commands");
    if (!((__draft.status === "active"))) throw new Error("Guard 0 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(((__rel_dish != null) && (__rel_dish.status === "active")))) throw new Error("Guard 2 failed");
    if (!((dishId === __draft.dishId))) throw new Error("Dish task dishId must match the seeded Dish reference");
    if (!((((name).trim()).length > 0))) throw new Error("Dish task name is required");
    if (!(((defaultQuantity == null) || (defaultQuantity > 0)))) throw new Error("Dish task quantity must be positive");
    if (!(((sortOrder == null) || (sortOrder >= 0)))) throw new Error("Dish task sort order cannot be negative");
    const doc: Record<string, any> = {
      ...__draft,
      dishId: dishId,
      activeDishId: dishId,
      name: name,
      category: ((category != null) ? category : "finish_at_event"),
      taskType: ((taskType != null) ? taskType : "manual"),
      defaultQuantity: defaultQuantity,
      defaultUnit: defaultUnit,
      station: station,
      sortOrder: ((sortOrder != null) ? sortOrder : 0),
      componentId: componentId,
      ingredientId: ingredientId,
      instructions: instructions,
      activeAt: Date.now(),
      version: 1,
    };
    const docId = await ctx.db.insert("dishTasks", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, dishTaskId: docId, tenantId: doc.tenantId, dishId: doc.dishId, name: doc.name, category: ((doc.category != null) ? doc.category : "finish_at_event"), taskType: ((doc.taskType != null) ? doc.taskType : "manual"), componentId: doc.componentId, ingredientId: doc.ingredientId, defaultQuantity: doc.defaultQuantity, defaultUnit: doc.defaultUnit, sortOrder: ((doc.sortOrder != null) ? doc.sortOrder : 0), _subject: { entity: "DishTask", command: "add", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "DishTaskAdded", entity: "DishTask", entityId: docId, payload: { dishTaskId: docId, tenantId: doc.tenantId, dishId: doc.dishId, name: doc.name, category: ((doc.category != null) ? doc.category : "finish_at_event"), taskType: ((doc.taskType != null) ? doc.taskType : "manual"), componentId: doc.componentId, ingredientId: doc.ingredientId, defaultQuantity: doc.defaultQuantity, defaultUnit: doc.defaultUnit, sortOrder: ((doc.sortOrder != null) ? doc.sortOrder : 0) }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "DishTask_createViaAdd", __result);
    }
    return __result;
  },
});

async function __runDishTaskBackfillActiveKey(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("DishTask not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("DishTask not found");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may read dish task templates");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write dish task templates through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute dish task template commands");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      activeDishId: ((((doc.deletedAt == null) && (doc.status === "active")) && (doc.activeDishId == null)) ? doc.dishId : doc.activeDishId),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    return { ...doc, ...updates };
}

export const DishTask_backfillActiveKey = mutation({
  args: {
    docId: v.id("dishTasks"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runDishTaskBackfillActiveKey(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "DishTask_backfillActiveKey", __result);
    }
    return __result;
  },
});

async function __runDishTaskRetire(ctx: MutationCtx, { docId, reason, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("DishTask not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("DishTask not found");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may read dish task templates");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write dish task templates through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute dish task template commands");
    if (!((doc.status === "active"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((((reason).trim()).length > 0))) throw new Error("Dish task retirement reason is required");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "retired";
        const __allowed: Record<string, string[]> = { "active": ["retired"], "retired": ["active"] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      retiredAt: Date.now(),
      retirementReason: reason,
      status: "retired",
      activeDishId: null,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, dishTaskId: docId, tenantId: __after.tenantId, dishId: __after.dishId, reason: reason, _subject: { entity: "DishTask", command: "retire", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "DishTaskRetired", entity: "DishTask", entityId: docId, payload: { dishTaskId: docId, tenantId: __after.tenantId, dishId: __after.dishId, reason: reason }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const DishTask_retire = mutation({
  args: {
    docId: v.id("dishTasks"),
    reason: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runDishTaskRetire(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "DishTask_retire", __result);
    }
    return __result;
  },
});

async function __runDishTaskRevise(ctx: MutationCtx, { docId, name, category, taskType, defaultQuantity, defaultUnit, station, sortOrder, componentId, ingredientId, instructions, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("DishTask not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("DishTask not found");
    const __rel_dish = await __resolveRelation(ctx, "dishes", [__auth.tenantId, doc.dishId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).dishId;
      ((doc as any) as any).dish = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may read dish task templates");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write dish task templates through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute dish task template commands");
    if (!((doc.status === "active"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(((__rel_dish != null) && (__rel_dish.status === "active")))) throw new Error("Guard 2 failed");
    if (!((((name).trim()).length > 0))) throw new Error("Dish task name is required");
    if (!(((defaultQuantity == null) || (defaultQuantity > 0)))) throw new Error("Dish task quantity must be positive");
    if (!(((sortOrder == null) || (sortOrder >= 0)))) throw new Error("Dish task sort order cannot be negative");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      name: name,
      category: ((category != null) ? category : doc.category),
      taskType: ((taskType != null) ? taskType : doc.taskType),
      defaultQuantity: defaultQuantity,
      defaultUnit: defaultUnit,
      station: station,
      sortOrder: ((sortOrder != null) ? sortOrder : doc.sortOrder),
      componentId: componentId,
      ingredientId: ingredientId,
      instructions: instructions,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, dishTaskId: docId, tenantId: __after.tenantId, dishId: __after.dishId, name: name, category: __after.category, taskType: __after.taskType, _subject: { entity: "DishTask", command: "revise", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "DishTaskRevised", entity: "DishTask", entityId: docId, payload: { dishTaskId: docId, tenantId: __after.tenantId, dishId: __after.dishId, name: name, category: __after.category, taskType: __after.taskType }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const DishTask_revise = mutation({
  args: {
    docId: v.id("dishTasks"),
    name: v.string(),
    category: v.optional(v.string()),
    taskType: v.optional(v.string()),
    defaultQuantity: v.optional(v.number()),
    defaultUnit: v.optional(v.any()),
    station: v.optional(v.string()),
    sortOrder: v.optional(v.any()),
    componentId: v.optional(v.string()),
    ingredientId: v.optional(v.string()),
    instructions: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runDishTaskRevise(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "DishTask_revise", __result);
    }
    return __result;
  },
});

async function __runEmailNotificationSubscriptionConfigure(ctx: MutationCtx, { docId, eventUpdates, invoiceReminders, lowStockAlerts, shiftChanges, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("EmailNotificationSubscription not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("EmailNotificationSubscription not found");
    if (!(((doc.ownerId == null) || (doc.ownerId === user.id)))) throw new Error("Users may read unowned creation drafts or their own email subscriptions");
    if (!(((user.id != null) && ((doc.ownerId == null) || (doc.ownerId === user.id))))) throw new Error("Users may write only their own email subscriptions");
    if (!(((user.id != null) && ((doc.ownerId == null) || (doc.ownerId === user.id))))) throw new Error("Users may configure only their own email subscriptions");
    if (!((doc.configuredAt == null))) throw new Error("Guard 0 failed");
    if (!((user.id != null))) throw new Error("Guard 1 failed");
    if (!((doc.ownerId == null))) throw new Error("Guard 2 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      ownerId: user.id,
      eventUpdates: eventUpdates,
      invoiceReminders: invoiceReminders,
      lowStockAlerts: lowStockAlerts,
      shiftChanges: shiftChanges,
      configuredAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, emailNotificationSubscriptionId: docId, tenantId: __after.tenantId, ownerId: user.id, eventUpdates: eventUpdates, invoiceReminders: invoiceReminders, lowStockAlerts: lowStockAlerts, shiftChanges: shiftChanges, _subject: { entity: "EmailNotificationSubscription", command: "configure", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EmailNotificationSubscriptionConfigured", entity: "EmailNotificationSubscription", entityId: docId, payload: { emailNotificationSubscriptionId: docId, tenantId: __after.tenantId, ownerId: user.id, eventUpdates: eventUpdates, invoiceReminders: invoiceReminders, lowStockAlerts: lowStockAlerts, shiftChanges: shiftChanges }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const EmailNotificationSubscription_configure = mutation({
  args: {
    docId: v.id("emailNotificationSubscriptions"),
    eventUpdates: v.boolean(),
    invoiceReminders: v.boolean(),
    lowStockAlerts: v.boolean(),
    shiftChanges: v.boolean(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runEmailNotificationSubscriptionConfigure(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "EmailNotificationSubscription_configure", __result);
    }
    return __result;
  },
});

export const EmailNotificationSubscription_createViaConfigure = mutation({
  args: {
    eventUpdates: v.boolean(),
    invoiceReminders: v.boolean(),
    lowStockAlerts: v.boolean(),
    shiftChanges: v.boolean(),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { eventUpdates, invoiceReminders, lowStockAlerts, shiftChanges } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      createdAt: Date.now(),
      updatedAt: Date.now(),
      eventUpdates: args.eventUpdates,
      invoiceReminders: args.invoiceReminders,
      lowStockAlerts: args.lowStockAlerts,
      shiftChanges: args.shiftChanges
    };
    if (!(((__draft.ownerId == null) || (__draft.ownerId === user.id)))) throw new Error("Users may read unowned creation drafts or their own email subscriptions");
    if (!(((user.id != null) && ((__draft.ownerId == null) || (__draft.ownerId === user.id))))) throw new Error("Users may write only their own email subscriptions");
    if (!(((user.id != null) && ((__draft.ownerId == null) || (__draft.ownerId === user.id))))) throw new Error("Users may configure only their own email subscriptions");
    if (!((__draft.configuredAt == null))) throw new Error("Guard 0 failed");
    if (!((user.id != null))) throw new Error("Guard 1 failed");
    if (!((__draft.ownerId == null))) throw new Error("Guard 2 failed");
    const doc: Record<string, any> = {
      ...__draft,
      ownerId: user.id,
      eventUpdates: eventUpdates,
      invoiceReminders: invoiceReminders,
      lowStockAlerts: lowStockAlerts,
      shiftChanges: shiftChanges,
      configuredAt: Date.now(),
      version: 1,
    };
    const docId = await ctx.db.insert("emailNotificationSubscriptions", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, emailNotificationSubscriptionId: docId, tenantId: doc.tenantId, ownerId: user.id, eventUpdates: doc.eventUpdates, invoiceReminders: doc.invoiceReminders, lowStockAlerts: doc.lowStockAlerts, shiftChanges: doc.shiftChanges, _subject: { entity: "EmailNotificationSubscription", command: "configure", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EmailNotificationSubscriptionConfigured", entity: "EmailNotificationSubscription", entityId: docId, payload: { emailNotificationSubscriptionId: docId, tenantId: doc.tenantId, ownerId: user.id, eventUpdates: doc.eventUpdates, invoiceReminders: doc.invoiceReminders, lowStockAlerts: doc.lowStockAlerts, shiftChanges: doc.shiftChanges }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "EmailNotificationSubscription_createViaConfigure", __result);
    }
    return __result;
  },
});

async function __runEmailNotificationSubscriptionUpdateSubscriptions(ctx: MutationCtx, { docId, eventUpdates, invoiceReminders, lowStockAlerts, shiftChanges, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("EmailNotificationSubscription not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("EmailNotificationSubscription not found");
    if (!(((doc.ownerId == null) || (doc.ownerId === user.id)))) throw new Error("Users may read unowned creation drafts or their own email subscriptions");
    if (!(((user.id != null) && ((doc.ownerId == null) || (doc.ownerId === user.id))))) throw new Error("Users may write only their own email subscriptions");
    if (!(((user.id != null) && ((doc.ownerId == null) || (doc.ownerId === user.id))))) throw new Error("Users may configure only their own email subscriptions");
    if (!((doc.configuredAt != null))) throw new Error("Guard 0 failed");
    if (!((user.id != null))) throw new Error("Guard 1 failed");
    if (!((doc.ownerId === user.id))) throw new Error("Guard 2 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      eventUpdates: eventUpdates,
      invoiceReminders: invoiceReminders,
      lowStockAlerts: lowStockAlerts,
      shiftChanges: shiftChanges,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, emailNotificationSubscriptionId: docId, tenantId: __after.tenantId, ownerId: user.id, eventUpdates: eventUpdates, invoiceReminders: invoiceReminders, lowStockAlerts: lowStockAlerts, shiftChanges: shiftChanges, _subject: { entity: "EmailNotificationSubscription", command: "updateSubscriptions", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EmailNotificationSubscriptionsUpdated", entity: "EmailNotificationSubscription", entityId: docId, payload: { emailNotificationSubscriptionId: docId, tenantId: __after.tenantId, ownerId: user.id, eventUpdates: eventUpdates, invoiceReminders: invoiceReminders, lowStockAlerts: lowStockAlerts, shiftChanges: shiftChanges }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const EmailNotificationSubscription_updateSubscriptions = mutation({
  args: {
    docId: v.id("emailNotificationSubscriptions"),
    eventUpdates: v.boolean(),
    invoiceReminders: v.boolean(),
    lowStockAlerts: v.boolean(),
    shiftChanges: v.boolean(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runEmailNotificationSubscriptionUpdateSubscriptions(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "EmailNotificationSubscription_updateSubscriptions", __result);
    }
    return __result;
  },
});

async function __runEquipmentReactivate(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Equipment not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Equipment not found");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "logisticsAccess")))) throw new Error("Inventory or logistics staff may read equipment");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "logisticsAccess")))) throw new Error("Inventory or logistics staff may write equipment through commands");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "logisticsAccess")))) throw new Error("Inventory or logistics staff may execute equipment commands");
    if (!((doc.status === "retired"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((checkRole(user, "inventoryManageAccess") || checkRole(user, "logisticsManageAccess")))) throw new Error("Guard 2 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "active";
        const __allowed: Record<string, string[]> = { "active": ["retired"], "retired": ["active"] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "active",
      retiredAt: null,
      retirementReason: null,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, equipmentId: docId, tenantId: __after.tenantId, _subject: { entity: "Equipment", command: "reactivate", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EquipmentReactivated", entity: "Equipment", entityId: docId, payload: { equipmentId: docId, tenantId: __after.tenantId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Equipment_reactivate = mutation({
  args: {
    docId: v.id("equipments"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runEquipmentReactivate(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Equipment_reactivate", __result);
    }
    return __result;
  },
});

async function __runEquipmentRecount(ctx: MutationCtx, { docId, actualQuantity, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Equipment not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Equipment not found");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "logisticsAccess")))) throw new Error("Inventory or logistics staff may read equipment");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "logisticsAccess")))) throw new Error("Inventory or logistics staff may write equipment through commands");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "logisticsAccess")))) throw new Error("Inventory or logistics staff may execute equipment commands");
    if (!((doc.registeredAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((actualQuantity >= 0))) throw new Error("Recount quantity cannot be negative");
    const previousQuantity = doc.quantity;
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      quantity: actualQuantity,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, equipmentId: docId, tenantId: __after.tenantId, previousQuantity: previousQuantity, quantity: actualQuantity, _subject: { entity: "Equipment", command: "recount", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EquipmentRecounted", entity: "Equipment", entityId: docId, payload: { equipmentId: docId, tenantId: __after.tenantId, previousQuantity: previousQuantity, quantity: actualQuantity }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Equipment_recount = mutation({
  args: {
    docId: v.id("equipments"),
    actualQuantity: v.number(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runEquipmentRecount(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Equipment_recount", __result);
    }
    return __result;
  },
});

async function __runEquipmentRegister(ctx: MutationCtx, { docId, name, assetTag, category, ownership, quantity, purchaseValue, condition, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Equipment not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Equipment not found");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "logisticsAccess")))) throw new Error("Inventory or logistics staff may read equipment");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "logisticsAccess")))) throw new Error("Inventory or logistics staff may write equipment through commands");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "logisticsAccess")))) throw new Error("Inventory or logistics staff may execute equipment commands");
    if (!((doc.registeredAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((((name).trim()).length > 0))) throw new Error("Equipment name is required");
    if (!((((assetTag).trim()).length > 0))) throw new Error("Asset tag is required");
    if (!((((category).trim()).length > 0))) throw new Error("Category is required");
    if (!(((quantity == null) || (quantity > 0)))) throw new Error("Quantity must be positive");
    if (!(((purchaseValue == null) || (purchaseValue >= 0)))) throw new Error("Purchase value cannot be negative");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      name: name,
      assetTag: assetTag,
      category: category,
      ownership: ownership,
      quantity: ((quantity != null) ? quantity : 1),
      purchaseValue: ((purchaseValue != null) ? purchaseValue : 0),
      condition: ((condition != null) ? condition : "good"),
      registeredAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, equipmentId: docId, tenantId: __after.tenantId, name: name, assetTag: assetTag, category: category, ownership: ownership, _subject: { entity: "Equipment", command: "register", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EquipmentRegistered", entity: "Equipment", entityId: docId, payload: { equipmentId: docId, tenantId: __after.tenantId, name: name, assetTag: assetTag, category: category, ownership: ownership }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Equipment_register = mutation({
  args: {
    docId: v.id("equipments"),
    name: v.string(),
    assetTag: v.string(),
    category: v.string(),
    ownership: v.any(),
    quantity: v.optional(v.number()),
    purchaseValue: v.optional(v.number()),
    condition: v.optional(v.any()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runEquipmentRegister(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Equipment_register", __result);
    }
    return __result;
  },
});

export const Equipment_createViaRegister = mutation({
  args: {
    name: v.string(),
    assetTag: v.string(),
    category: v.string(),
    ownership: v.any(),
    quantity: v.optional(v.number()),
    purchaseValue: v.optional(v.number()),
    condition: v.optional(v.any()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { name, assetTag, category, ownership, quantity, purchaseValue, condition } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      quantity: args.quantity !== undefined ? args.quantity : 1,
      purchaseValue: args.purchaseValue !== undefined ? args.purchaseValue : 0,
      condition: args.condition !== undefined ? args.condition : "good",
      status: "active",
      createdAt: Date.now(),
      updatedAt: Date.now(),
      assetTag: args.assetTag,
      category: args.category,
      name: args.name,
      ownership: args.ownership
    };
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "logisticsAccess")))) throw new Error("Inventory or logistics staff may read equipment");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "logisticsAccess")))) throw new Error("Inventory or logistics staff may write equipment through commands");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "logisticsAccess")))) throw new Error("Inventory or logistics staff may execute equipment commands");
    if (!((__draft.registeredAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((((name).trim()).length > 0))) throw new Error("Equipment name is required");
    if (!((((assetTag).trim()).length > 0))) throw new Error("Asset tag is required");
    if (!((((category).trim()).length > 0))) throw new Error("Category is required");
    if (!(((quantity == null) || (quantity > 0)))) throw new Error("Quantity must be positive");
    if (!(((purchaseValue == null) || (purchaseValue >= 0)))) throw new Error("Purchase value cannot be negative");
    const doc: Record<string, any> = {
      ...__draft,
      name: name,
      assetTag: assetTag,
      category: category,
      ownership: ownership,
      quantity: ((quantity != null) ? quantity : 1),
      purchaseValue: ((purchaseValue != null) ? purchaseValue : 0),
      condition: ((condition != null) ? condition : "good"),
      registeredAt: Date.now(),
      version: 1,
    };
    const docId = await ctx.db.insert("equipments", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, equipmentId: docId, tenantId: doc.tenantId, name: doc.name, assetTag: doc.assetTag, category: doc.category, ownership: doc.ownership, _subject: { entity: "Equipment", command: "register", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EquipmentRegistered", entity: "Equipment", entityId: docId, payload: { equipmentId: docId, tenantId: doc.tenantId, name: doc.name, assetTag: doc.assetTag, category: doc.category, ownership: doc.ownership }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Equipment_createViaRegister", __result);
    }
    return __result;
  },
});

async function __runEquipmentRetire(ctx: MutationCtx, { docId, reason, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Equipment not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Equipment not found");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "logisticsAccess")))) throw new Error("Inventory or logistics staff may read equipment");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "logisticsAccess")))) throw new Error("Inventory or logistics staff may write equipment through commands");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "logisticsAccess")))) throw new Error("Inventory or logistics staff may execute equipment commands");
    if (!((doc.status === "active"))) throw new Error("Guard 0 failed");
    if (!((doc.registeredAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((checkRole(user, "inventoryManageAccess") || checkRole(user, "logisticsManageAccess")))) throw new Error("Guard 3 failed");
    if (!((((reason).trim()).length > 0))) throw new Error("Retirement reason is required");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "retired";
        const __allowed: Record<string, string[]> = { "active": ["retired"], "retired": ["active"] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "retired",
      retiredAt: Date.now(),
      retirementReason: reason,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, equipmentId: docId, tenantId: __after.tenantId, reason: reason, _subject: { entity: "Equipment", command: "retire", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EquipmentRetired", entity: "Equipment", entityId: docId, payload: { equipmentId: docId, tenantId: __after.tenantId, reason: reason }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Equipment_retire = mutation({
  args: {
    docId: v.id("equipments"),
    reason: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runEquipmentRetire(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Equipment_retire", __result);
    }
    return __result;
  },
});

async function __runEquipmentReviseDetails(ctx: MutationCtx, { docId, name, category, ownership, purchaseValue, homeLocation, currentLocation, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Equipment not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Equipment not found");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "logisticsAccess")))) throw new Error("Inventory or logistics staff may read equipment");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "logisticsAccess")))) throw new Error("Inventory or logistics staff may write equipment through commands");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "logisticsAccess")))) throw new Error("Inventory or logistics staff may execute equipment commands");
    if (!((doc.registeredAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.status === "active"))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((((name).trim()).length > 0))) throw new Error("Equipment name is required");
    if (!((((category).trim()).length > 0))) throw new Error("Category is required");
    if (!(((purchaseValue == null) || (purchaseValue >= 0)))) throw new Error("Purchase value cannot be negative");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      name: name,
      category: category,
      ownership: ((ownership != null) ? ownership : doc.ownership),
      purchaseValue: ((purchaseValue != null) ? purchaseValue : doc.purchaseValue),
      homeLocation: ((homeLocation != null) ? homeLocation : doc.homeLocation),
      currentLocation: ((currentLocation != null) ? currentLocation : doc.currentLocation),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, equipmentId: docId, tenantId: __after.tenantId, name: name, category: category, _subject: { entity: "Equipment", command: "reviseDetails", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EquipmentDetailsRevised", entity: "Equipment", entityId: docId, payload: { equipmentId: docId, tenantId: __after.tenantId, name: name, category: category }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Equipment_reviseDetails = mutation({
  args: {
    docId: v.id("equipments"),
    name: v.string(),
    category: v.string(),
    ownership: v.optional(v.any()),
    purchaseValue: v.optional(v.number()),
    homeLocation: v.optional(v.string()),
    currentLocation: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runEquipmentReviseDetails(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Equipment_reviseDetails", __result);
    }
    return __result;
  },
});

async function __runEquipmentUpdateCondition(ctx: MutationCtx, { docId, condition, note, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Equipment not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Equipment not found");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "logisticsAccess")))) throw new Error("Inventory or logistics staff may read equipment");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "logisticsAccess")))) throw new Error("Inventory or logistics staff may write equipment through commands");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "logisticsAccess")))) throw new Error("Inventory or logistics staff may execute equipment commands");
    if (!((doc.registeredAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    const previousCondition = doc.condition;
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      condition: condition,
      conditionNote: note,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, equipmentId: docId, tenantId: __after.tenantId, previousCondition: previousCondition, condition: condition, note: note, _subject: { entity: "Equipment", command: "updateCondition", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EquipmentConditionUpdated", entity: "Equipment", entityId: docId, payload: { equipmentId: docId, tenantId: __after.tenantId, previousCondition: previousCondition, condition: condition, note: note }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Equipment_updateCondition = mutation({
  args: {
    docId: v.id("equipments"),
    condition: v.any(),
    note: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runEquipmentUpdateCondition(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Equipment_updateCondition", __result);
    }
    return __result;
  },
});

async function __runEquipmentMaintenanceTaskApplyService(ctx: MutationCtx, { docId, completedAt, nextDueAt, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("EquipmentMaintenanceTask not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("EquipmentMaintenanceTask not found");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "logisticsAccess")))) throw new Error("Inventory or logistics staff may read equipment maintenance");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "logisticsAccess")))) throw new Error("Inventory or logistics staff may update equipment maintenance");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "logisticsAccess")))) throw new Error("Inventory or logistics staff may execute equipment maintenance commands");
    if (!((doc.scheduledAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((nextDueAt > completedAt))) throw new Error("Next maintenance due date must be after the completed service");
    const isLatestService = ((doc.lastServicedAt == null) || (completedAt >= doc.lastServicedAt));
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      lastServicedAt: (isLatestService ? completedAt : doc.lastServicedAt),
      nextDueAt: (isLatestService ? nextDueAt : doc.nextDueAt),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, maintenanceTaskId: docId, equipmentId: __after.equipmentId, tenantId: __after.tenantId, completedAt: completedAt, nextDueAt: (isLatestService ? nextDueAt : __after.nextDueAt), _subject: { entity: "EquipmentMaintenanceTask", command: "applyService", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EquipmentMaintenanceAdvanced", entity: "EquipmentMaintenanceTask", entityId: docId, payload: { maintenanceTaskId: docId, equipmentId: __after.equipmentId, tenantId: __after.tenantId, completedAt: completedAt, nextDueAt: (isLatestService ? nextDueAt : __after.nextDueAt) }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const EquipmentMaintenanceTask_applyService = mutation({
  args: {
    docId: v.id("equipmentMaintenanceTasks"),
    completedAt: v.number(),
    nextDueAt: v.number(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runEquipmentMaintenanceTaskApplyService(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "EquipmentMaintenanceTask_applyService", __result);
    }
    return __result;
  },
});

async function __runEquipmentMaintenanceTaskSchedule(ctx: MutationCtx, { docId, equipmentId, taskName, intervalDays, nextDueAt, instructions, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("EquipmentMaintenanceTask not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("EquipmentMaintenanceTask not found");
    const __rel_equipment = await __resolveRelation(ctx, "equipments", [__auth.tenantId, doc.equipmentId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).equipmentId;
      ((doc as any) as any).equipment = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "logisticsAccess")))) throw new Error("Inventory or logistics staff may read equipment maintenance");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "logisticsAccess")))) throw new Error("Inventory or logistics staff may update equipment maintenance");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "logisticsAccess")))) throw new Error("Inventory or logistics staff may execute equipment maintenance commands");
    if (!((doc.scheduledAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((__rel_equipment != null))) throw new Error("Guard 2 failed");
    if (!((equipmentId === doc.equipmentId))) throw new Error("Maintenance equipment must match the selected catalog item");
    if (!((((taskName).trim()).length > 0))) throw new Error("Maintenance task is required");
    if (!((intervalDays > 0))) throw new Error("Recurring interval must be at least one day");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      taskName: taskName,
      intervalDays: intervalDays,
      nextDueAt: nextDueAt,
      instructions: instructions,
      scheduledAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, maintenanceTaskId: docId, equipmentId: equipmentId, tenantId: __after.tenantId, taskName: taskName, intervalDays: intervalDays, nextDueAt: nextDueAt, _subject: { entity: "EquipmentMaintenanceTask", command: "schedule", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EquipmentMaintenanceScheduled", entity: "EquipmentMaintenanceTask", entityId: docId, payload: { maintenanceTaskId: docId, equipmentId: equipmentId, tenantId: __after.tenantId, taskName: taskName, intervalDays: intervalDays, nextDueAt: nextDueAt }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const EquipmentMaintenanceTask_schedule = mutation({
  args: {
    docId: v.id("equipmentMaintenanceTasks"),
    equipmentId: v.string(),
    taskName: v.string(),
    intervalDays: v.number(),
    nextDueAt: v.number(),
    instructions: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runEquipmentMaintenanceTaskSchedule(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "EquipmentMaintenanceTask_schedule", __result);
    }
    return __result;
  },
});

export const EquipmentMaintenanceTask_createViaSchedule = mutation({
  args: {
    equipmentId: v.string(),
    taskName: v.string(),
    intervalDays: v.number(),
    nextDueAt: v.number(),
    instructions: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { equipmentId, taskName, intervalDays, nextDueAt, instructions } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      createdAt: Date.now(),
      updatedAt: Date.now(),
      equipmentId: args.equipmentId,
      instructions: args.instructions,
      intervalDays: args.intervalDays,
      nextDueAt: args.nextDueAt,
      taskName: args.taskName
    };
    const __rel_equipment = await __resolveRelation(ctx, "equipments", [__auth.tenantId, __draft.equipmentId], ["tenantId","id"], "tenantId", __auth.tenantId);
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "logisticsAccess")))) throw new Error("Inventory or logistics staff may read equipment maintenance");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "logisticsAccess")))) throw new Error("Inventory or logistics staff may update equipment maintenance");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "logisticsAccess")))) throw new Error("Inventory or logistics staff may execute equipment maintenance commands");
    if (!((__draft.scheduledAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((__rel_equipment != null))) throw new Error("Guard 2 failed");
    if (!((equipmentId === __draft.equipmentId))) throw new Error("Maintenance equipment must match the selected catalog item");
    if (!((((taskName).trim()).length > 0))) throw new Error("Maintenance task is required");
    if (!((intervalDays > 0))) throw new Error("Recurring interval must be at least one day");
    const doc: Record<string, any> = {
      ...__draft,
      taskName: taskName,
      intervalDays: intervalDays,
      nextDueAt: nextDueAt,
      instructions: instructions,
      scheduledAt: Date.now(),
      version: 1,
    };
    const docId = await ctx.db.insert("equipmentMaintenanceTasks", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, maintenanceTaskId: docId, equipmentId: doc.equipmentId, tenantId: doc.tenantId, taskName: doc.taskName, intervalDays: doc.intervalDays, nextDueAt: doc.nextDueAt, _subject: { entity: "EquipmentMaintenanceTask", command: "schedule", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EquipmentMaintenanceScheduled", entity: "EquipmentMaintenanceTask", entityId: docId, payload: { maintenanceTaskId: docId, equipmentId: doc.equipmentId, tenantId: doc.tenantId, taskName: doc.taskName, intervalDays: doc.intervalDays, nextDueAt: doc.nextDueAt }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "EquipmentMaintenanceTask_createViaSchedule", __result);
    }
    return __result;
  },
});

async function __runEquipmentReservationCancel(ctx: MutationCtx, { docId, reason, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("EquipmentReservation not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("EquipmentReservation not found");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "logisticsAccess")))) throw new Error("Inventory or logistics staff may read equipment reservations");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "logisticsAccess")))) throw new Error("Inventory or logistics staff may update equipment handoffs");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "logisticsAccess")))) throw new Error("Inventory or logistics staff may execute equipment handoff commands");
    if (!((doc.status === "reserved"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "cancelled";
        const __allowed: Record<string, string[]> = { "reserved": ["checked_out", "cancelled"], "checked_out": ["returned"] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "cancelled",
      cancellationReason: reason,
      cancelledAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, equipmentReservationId: docId, equipmentId: __after.equipmentId, eventId: __after.eventId, tenantId: __after.tenantId, reason: reason, _subject: { entity: "EquipmentReservation", command: "cancel", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EquipmentReservationCancelled", entity: "EquipmentReservation", entityId: docId, payload: { equipmentReservationId: docId, equipmentId: __after.equipmentId, eventId: __after.eventId, tenantId: __after.tenantId, reason: reason }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const EquipmentReservation_cancel = mutation({
  args: {
    docId: v.id("equipmentReservations"),
    reason: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runEquipmentReservationCancel(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "EquipmentReservation_cancel", __result);
    }
    return __result;
  },
});

async function __runEquipmentReservationCheckOut(ctx: MutationCtx, { docId, condition, note, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("EquipmentReservation not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("EquipmentReservation not found");
    const __rel_equipment = await __resolveRelation(ctx, "equipments", [__auth.tenantId, doc.equipmentId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).equipmentId;
      ((doc as any) as any).equipment = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "logisticsAccess")))) throw new Error("Inventory or logistics staff may read equipment reservations");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "logisticsAccess")))) throw new Error("Inventory or logistics staff may update equipment handoffs");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "logisticsAccess")))) throw new Error("Inventory or logistics staff may execute equipment handoff commands");
    if (!((doc.status === "reserved"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(((__rel_equipment != null) && (__rel_equipment.condition !== "out_of_service")))) throw new Error("Equipment marked out of service cannot be checked out");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "checked_out";
        const __allowed: Record<string, string[]> = { "reserved": ["checked_out", "cancelled"], "checked_out": ["returned"] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "checked_out",
      checkoutCondition: condition,
      checkoutNote: note,
      checkedOutAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, equipmentReservationId: docId, equipmentId: __after.equipmentId, eventId: __after.eventId, tenantId: __after.tenantId, condition: condition, note: note, _subject: { entity: "EquipmentReservation", command: "checkOut", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EquipmentCheckedOut", entity: "EquipmentReservation", entityId: docId, payload: { equipmentReservationId: docId, equipmentId: __after.equipmentId, eventId: __after.eventId, tenantId: __after.tenantId, condition: condition, note: note }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const EquipmentReservation_checkOut = mutation({
  args: {
    docId: v.id("equipmentReservations"),
    condition: v.any(),
    note: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runEquipmentReservationCheckOut(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "EquipmentReservation_checkOut", __result);
    }
    return __result;
  },
});

async function __runEquipmentReservationMarkReturned(ctx: MutationCtx, { docId, condition, note, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("EquipmentReservation not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("EquipmentReservation not found");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "logisticsAccess")))) throw new Error("Inventory or logistics staff may read equipment reservations");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "logisticsAccess")))) throw new Error("Inventory or logistics staff may update equipment handoffs");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "logisticsAccess")))) throw new Error("Inventory or logistics staff may execute equipment handoff commands");
    if (!((doc.status === "checked_out"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "returned";
        const __allowed: Record<string, string[]> = { "reserved": ["checked_out", "cancelled"], "checked_out": ["returned"] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "returned",
      returnCondition: condition,
      returnNote: note,
      returnedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, equipmentReservationId: docId, equipmentId: __after.equipmentId, eventId: __after.eventId, tenantId: __after.tenantId, condition: condition, note: note, _subject: { entity: "EquipmentReservation", command: "markReturned", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EquipmentReturned", entity: "EquipmentReservation", entityId: docId, payload: { equipmentReservationId: docId, equipmentId: __after.equipmentId, eventId: __after.eventId, tenantId: __after.tenantId, condition: condition, note: note }, createdAt: Date.now() });
    // Reactions
    const __fanId0 = payload.equipmentId;
    const __fanDoc0 = __fanId0 != null && __fanId0 !== "" ? await ctx.db.get(__fanId0 as any) : null;
    const fanRows0 = __fanDoc0 != null && (__fanDoc0 as any).deletedAt == null ? [__fanDoc0] : [];
    for (const __row of fanRows0) {
      const target = __row;
      await __runEquipmentUpdateCondition(ctx, { docId: (__row as any)._id, condition: payload.condition, note: payload.note } as any);
    }
    return { ...doc, ...updates };
}

export const EquipmentReservation_markReturned = mutation({
  args: {
    docId: v.id("equipmentReservations"),
    condition: v.any(),
    note: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runEquipmentReservationMarkReturned(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "EquipmentReservation_markReturned", __result);
    }
    return __result;
  },
});

async function __runEquipmentServiceEntryRecord(ctx: MutationCtx, { docId, maintenanceTaskId, equipmentId, technician, cost, completedAt, nextDueAt, notes, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("EquipmentServiceEntry not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("EquipmentServiceEntry not found");
    const __rel_maintenanceTask = await __resolveRelation(ctx, "equipmentMaintenanceTasks", [__auth.tenantId, doc.maintenanceTaskId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_equipment = await __resolveRelation(ctx, "equipments", [__auth.tenantId, doc.equipmentId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).maintenanceTaskId;
      ((doc as any) as any).maintenanceTask = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    {
      const __fk = ((doc as any) as any).equipmentId;
      ((doc as any) as any).equipment = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "logisticsAccess")))) throw new Error("Inventory or logistics staff may read equipment service history");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "logisticsAccess")))) throw new Error("Inventory or logistics staff may record equipment service");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "logisticsAccess")))) throw new Error("Inventory or logistics staff may execute equipment service commands");
    if (!((doc.loggedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((__rel_maintenanceTask != null))) throw new Error("Guard 2 failed");
    if (!((__rel_equipment != null))) throw new Error("Guard 3 failed");
    if (!((maintenanceTaskId === doc.maintenanceTaskId))) throw new Error("Service entry must match the selected maintenance task");
    if (!(((equipmentId === doc.equipmentId) && (__rel_maintenanceTask.equipmentId === equipmentId)))) throw new Error("Service entry equipment must match its maintenance task");
    if (!((((technician).trim()).length > 0))) throw new Error("Technician is required");
    if (!((cost >= 0))) throw new Error("Service cost cannot be negative");
    if (!((completedAt <= Date.now()))) throw new Error("Service completion cannot be in the future");
    if (!((nextDueAt > completedAt))) throw new Error("Next maintenance due date must be after the completed service");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      technician: technician,
      cost: cost,
      notes: notes,
      completedAt: completedAt,
      nextDueAt: nextDueAt,
      loggedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, serviceEntryId: docId, maintenanceTaskId: maintenanceTaskId, equipmentId: equipmentId, tenantId: __after.tenantId, technician: technician, cost: cost, notes: notes, completedAt: completedAt, nextDueAt: nextDueAt, _subject: { entity: "EquipmentServiceEntry", command: "record", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EquipmentServiceRecorded", entity: "EquipmentServiceEntry", entityId: docId, payload: { serviceEntryId: docId, maintenanceTaskId: maintenanceTaskId, equipmentId: equipmentId, tenantId: __after.tenantId, technician: technician, cost: cost, notes: notes, completedAt: completedAt, nextDueAt: nextDueAt }, createdAt: Date.now() });
    // Reactions
    const reactionTarget0 = payload.maintenanceTaskId;
    if (reactionTarget0) await __runEquipmentMaintenanceTaskApplyService(ctx, { docId: reactionTarget0, completedAt: payload.completedAt, nextDueAt: payload.nextDueAt } as any);
    return { ...doc, ...updates };
}

export const EquipmentServiceEntry_record = mutation({
  args: {
    docId: v.id("equipmentServiceEntries"),
    maintenanceTaskId: v.string(),
    equipmentId: v.string(),
    technician: v.string(),
    cost: v.number(),
    completedAt: v.number(),
    nextDueAt: v.number(),
    notes: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runEquipmentServiceEntryRecord(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "EquipmentServiceEntry_record", __result);
    }
    return __result;
  },
});

export const EquipmentServiceEntry_createViaRecord = mutation({
  args: {
    maintenanceTaskId: v.string(),
    equipmentId: v.string(),
    technician: v.string(),
    cost: v.number(),
    completedAt: v.number(),
    nextDueAt: v.number(),
    notes: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { maintenanceTaskId, equipmentId, technician, cost, completedAt, nextDueAt, notes } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      createdAt: Date.now(),
      updatedAt: Date.now(),
      completedAt: args.completedAt,
      cost: args.cost,
      equipmentId: args.equipmentId,
      maintenanceTaskId: args.maintenanceTaskId,
      nextDueAt: args.nextDueAt,
      notes: args.notes,
      technician: args.technician
    };
    const __rel_maintenanceTask = await __resolveRelation(ctx, "equipmentMaintenanceTasks", [__auth.tenantId, __draft.maintenanceTaskId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_equipment = await __resolveRelation(ctx, "equipments", [__auth.tenantId, __draft.equipmentId], ["tenantId","id"], "tenantId", __auth.tenantId);
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "logisticsAccess")))) throw new Error("Inventory or logistics staff may read equipment service history");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "logisticsAccess")))) throw new Error("Inventory or logistics staff may record equipment service");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "logisticsAccess")))) throw new Error("Inventory or logistics staff may execute equipment service commands");
    if (!((__draft.loggedAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((__rel_maintenanceTask != null))) throw new Error("Guard 2 failed");
    if (!((__rel_equipment != null))) throw new Error("Guard 3 failed");
    if (!((maintenanceTaskId === __draft.maintenanceTaskId))) throw new Error("Service entry must match the selected maintenance task");
    if (!(((equipmentId === __draft.equipmentId) && (__rel_maintenanceTask.equipmentId === equipmentId)))) throw new Error("Service entry equipment must match its maintenance task");
    if (!((((technician).trim()).length > 0))) throw new Error("Technician is required");
    if (!((cost >= 0))) throw new Error("Service cost cannot be negative");
    if (!((completedAt <= Date.now()))) throw new Error("Service completion cannot be in the future");
    if (!((nextDueAt > completedAt))) throw new Error("Next maintenance due date must be after the completed service");
    const doc: Record<string, any> = {
      ...__draft,
      technician: technician,
      cost: cost,
      notes: notes,
      completedAt: completedAt,
      nextDueAt: nextDueAt,
      loggedAt: Date.now(),
      version: 1,
    };
    const docId = await ctx.db.insert("equipmentServiceEntries", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, serviceEntryId: docId, maintenanceTaskId: doc.maintenanceTaskId, equipmentId: doc.equipmentId, tenantId: doc.tenantId, technician: doc.technician, cost: doc.cost, notes: doc.notes, completedAt: doc.completedAt, nextDueAt: doc.nextDueAt, _subject: { entity: "EquipmentServiceEntry", command: "record", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EquipmentServiceRecorded", entity: "EquipmentServiceEntry", entityId: docId, payload: { serviceEntryId: docId, maintenanceTaskId: doc.maintenanceTaskId, equipmentId: doc.equipmentId, tenantId: doc.tenantId, technician: doc.technician, cost: doc.cost, notes: doc.notes, completedAt: doc.completedAt, nextDueAt: doc.nextDueAt }, createdAt: Date.now() });
    // Reactions
    const reactionTarget0 = payload.maintenanceTaskId;
    if (reactionTarget0) await __runEquipmentMaintenanceTaskApplyService(ctx, { docId: reactionTarget0, completedAt: payload.completedAt, nextDueAt: payload.nextDueAt } as any);
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "EquipmentServiceEntry_createViaRecord", __result);
    }
    return __result;
  },
});

async function __runEventApprove(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("Event not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("Event not found");
    const doc = await __decryptDoc(ctx, "Event", ["primaryContactName","primaryContactEmail","primaryContactPhone"], __storedDoc) as Record<string, any>;
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may read events");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may write events through commands");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may execute event commands");
    if (!((doc.stage === "pending_approval"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(checkRole(user, "eventManageAccess"))) throw new Error("Guard 2 failed");
    {
      const __cur = doc.stage;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "approved";
        const __allowed: Record<string, string[]> = { "quote": ["planning", "cancelled"], "planning": ["pending_approval", "cancelled"], "pending_approval": ["planning", "approved", "cancelled"], "approved": ["sales_lock", "cancelled"], "sales_lock": ["executing", "cancelled"], "executing": ["final", "cancelled"], "final": ["completed", "cancelled"], "completed": ["closed_out"], "cancelled": [], "closed_out": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'stage'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      stage: "approved",
      approvedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "Event", ["primaryContactName","primaryContactEmail","primaryContactPhone"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, eventId: docId, tenantId: __after.tenantId, clientId: __after.clientId, expectedHeadcount: __after.expectedHeadcount, startsAt: __after.startsAt, endsAt: __after.endsAt, quotedPrice: __after.quotedPrice, assignedToId: __after.assignedToId, _subject: { entity: "Event", command: "approve", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EventApproved", entity: "Event", entityId: docId, payload: { eventId: docId, tenantId: __after.tenantId, clientId: __after.clientId, expectedHeadcount: __after.expectedHeadcount, startsAt: __after.startsAt, endsAt: __after.endsAt, quotedPrice: __after.quotedPrice, assignedToId: __after.assignedToId }, createdAt: Date.now() });
    // Reactions
    const __fanId0 = payload.assignedToId;
    const __fanDoc0 = __fanId0 != null && __fanId0 !== "" ? await ctx.db.get(__fanId0 as any) : null;
    const fanRows0 = __fanDoc0 != null && (__fanDoc0 as any).deletedAt == null ? [__fanDoc0] : [];
    for (const __row of fanRows0) {
      const target = __row;
      const __fanMatch0_raw = await ctx.db.query("eventAssignments").withIndex("by_eventId", (q) => q.eq("eventId", payload.eventId)).collect();
      const __fanMatch0_rows = __fanMatch0_raw.filter((d) => (d as any).eventId === payload.eventId && (d as any).personId === (__row as any)._id && (d as any).deletedAt == null).sort((a, b) => String((a as any)._id).localeCompare(String((b as any)._id)));
      const __fanMatch0_id = __fanMatch0_rows.length > 0 ? (__fanMatch0_rows[0] as any)._id : null;
      if (__fanMatch0_id) {
        await __runEventAssignmentAssign(ctx, { docId: __fanMatch0_id, eventId: payload.eventId, personId: (__row as any)._id, role: "event_lead", startsAt: payload.startsAt, endsAt: payload.endsAt } as any);
      } else {
        const __elseArgs: Record<string, any> = { eventId: payload.eventId, personId: (__row as any)._id, role: "event_lead", startsAt: payload.startsAt, endsAt: payload.endsAt };
        const __elseDoc: Record<string, any> = {
          tenantId: __auth.tenantId,
          role: "",
          status: "assigned",
          createdAt: Date.now(),
          updatedAt: Date.now(),
          version: 0,
        };
        for (const __k of ["deletedAt","eventId","personId","role","startsAt","endsAt","notes","status","assignedAt","confirmedAt","checkedInAt","checkedOutAt","noShowAt","unassignedAt","createdAt","updatedAt"] as string[]) {
          if (__elseArgs[__k] !== undefined) __elseDoc[__k] = __elseArgs[__k];
        }
        const __elseId = await ctx.db.insert("eventAssignments", __elseDoc as any);
        await __runEventAssignmentAssign(ctx, { docId: __elseId, ...__elseArgs } as any);
      }
    }
    const fanRows1 = (await ctx.db.query("ingredientDemands").withIndex("by_eventId", (q) => q.eq("eventId", payload.eventId)).collect()).filter((d) => (d as any).deletedAt == null);
    for (const __row of fanRows1) {
      const target = __row;
      await __runIngredientDemandEnsurePurchaseEligible(ctx, { docId: (__row as any)._id } as any);
    }
    const __match2_raw = await ctx.db.query("invoices").withIndex("by_eventId", (q) => q.eq("eventId", payload.eventId)).collect();
    const __match2_rows = __match2_raw.filter((d) => (d as any).eventId === payload.eventId && (d as any).deletedAt == null).sort((a, b) => String((a as any)._id).localeCompare(String((b as any)._id)));
    const __match2_id = __match2_rows.length > 0 ? (__match2_rows[0] as any)._id : null;
    if (__match2_id) {
      await __runInvoiceIssue(ctx, { docId: __match2_id, clientId: payload.clientId, eventId: payload.eventId, invoiceNumber: payload.eventId, subtotal: payload.quotedPrice, taxAmount: 0, discountAmount: 0, total: payload.quotedPrice } as any);
    } else {
      const __elseArgs: Record<string, any> = { clientId: payload.clientId, eventId: payload.eventId, invoiceNumber: payload.eventId, subtotal: payload.quotedPrice, taxAmount: 0, discountAmount: 0, total: payload.quotedPrice };
      const __elseDoc: Record<string, any> = {
        tenantId: __auth.tenantId,
        subtotal: 0,
        taxAmount: 0,
        discountAmount: 0,
        total: 0,
        amountPaid: 0,
        amountDue: 0,
        paymentTermsDays: 30,
        status: "draft",
        createdAt: Date.now(),
        updatedAt: Date.now(),
        version: 0,
      };
      for (const __k of ["deletedAt","clientId","clientMergeAuthorizationId","mergeTargetClientId","eventId","invoiceNumber","subtotal","taxAmount","discountAmount","total","amountPaid","amountDue","amountCredited","creditMemoAmount","lineItems","taxBreakdown","paymentTermsDays","depositAmount","depositPaidAt","balanceReminderLeadDays","balanceReminderSentAt","dueDate","notes","status","issuedAt","sentAt","viewedAt","overdueSince","paidAt","voidedAt","voidReason","writtenOffAt","writeOffReason","currencyCode","exchangeRate","createdAt","updatedAt"] as string[]) {
        if (__elseArgs[__k] !== undefined) __elseDoc[__k] = __elseArgs[__k];
      }
      const __elseId = await ctx.db.insert("invoices", __elseDoc as any);
      await __runInvoiceIssue(ctx, { docId: __elseId, ...__elseArgs } as any);
    }
    const __match3_raw = await ctx.db.query("packLists").withIndex("by_eventId", (q) => q.eq("eventId", payload.eventId)).collect();
    const __match3_rows = __match3_raw.filter((d) => (d as any).eventId === payload.eventId && (d as any).activeEventId === payload.eventId && (d as any).deletedAt == null).sort((a, b) => String((a as any)._id).localeCompare(String((b as any)._id)));
    const __match3_id = __match3_rows.length > 0 ? (__match3_rows[0] as any)._id : null;
    if (__match3_id) {
      await __runPackListOpen(ctx, { docId: __match3_id, eventId: payload.eventId, name: "Event pack list" } as any);
    } else {
      const __elseArgs: Record<string, any> = { eventId: payload.eventId, name: "Event pack list" };
      const __elseDoc: Record<string, any> = {
        tenantId: __auth.tenantId,
        name: "",
        status: "draft",
        createdAt: Date.now(),
        updatedAt: Date.now(),
        version: 0,
      };
      for (const __k of ["deletedAt","eventId","activeEventId","name","purpose","notes","status","openedAt","packingStartedAt","packedAt","loadedAt","dispatchedAt","cancelledAt","cancellationReason","createdAt","updatedAt"] as string[]) {
        if (__elseArgs[__k] !== undefined) __elseDoc[__k] = __elseArgs[__k];
      }
      const __elseId = await ctx.db.insert("packLists", __elseDoc as any);
      await __runPackListOpen(ctx, { docId: __elseId, ...__elseArgs } as any);
    }
    const fanRows4 = (await ctx.db.query("eventDishComponentSeeds").filter((q) => q.eq(q.field("eventId"), payload.eventId)).collect()).filter((d) => (d as any).deletedAt == null);
    for (const __row of fanRows4) {
      const target = __row;
      const __fanMatch4_raw = await ctx.db.query("productionBatches").withIndex("by_eventId", (q) => q.eq("eventId", payload.eventId)).collect();
      const __fanMatch4_rows = __fanMatch4_raw.filter((d) => (d as any).eventId === payload.eventId && (d as any).componentId === __row.componentId && (d as any).deletedAt == null).sort((a, b) => String((a as any)._id).localeCompare(String((b as any)._id)));
      const __fanMatch4_id = __fanMatch4_rows.length > 0 ? (__fanMatch4_rows[0] as any)._id : null;
      if (__fanMatch4_id) {
        await __runProductionBatchPlan(ctx, { docId: __fanMatch4_id, componentId: __row.componentId, plannedYield: __row.servings, yieldUnit: "portion", eventId: payload.eventId } as any);
      } else {
        const __elseArgs: Record<string, any> = { componentId: __row.componentId, plannedYield: __row.servings, yieldUnit: "portion", eventId: payload.eventId };
        const __elseDoc: Record<string, any> = {
          tenantId: __auth.tenantId,
          plannedYield: 0,
          yieldUnit: "portion",
          status: "planned",
          createdAt: Date.now(),
          updatedAt: Date.now(),
          version: 0,
        };
        for (const __k of ["deletedAt","componentId","eventId","plannedYield","actualYield","yieldUnit","notes","status","plannedAt","startedAt","completedAt","cancelledAt","cancellationReason","createdAt","updatedAt"] as string[]) {
          if (__elseArgs[__k] !== undefined) __elseDoc[__k] = __elseArgs[__k];
        }
        const __elseId = await ctx.db.insert("productionBatches", __elseDoc as any);
        await __runProductionBatchPlan(ctx, { docId: __elseId, ...__elseArgs } as any);
      }
    }
    const fanRows5 = (await ctx.db.query("ingredientDemands").withIndex("by_purchaseEligibleEventId", (q) => q.eq("purchaseEligibleEventId", payload.eventId)).collect()).filter((d) => (d as any).deletedAt == null);
    for (const __row of fanRows5) {
      const target = __row;
      await __runPurchaseNeedCreate(ctx, { eventId: __row.eventId, ingredientDemandId: (__row as any)._id, ingredientId: __row.ingredientId, requiredQuantity: __row.requiredQuantity, unit: __row.unit, purchasingWeekStart: __row.purchasingWeekStart, preferredVendorId: __row.preferredVendorId } as any);
    }
    return { ...doc, ...updates };
}

export const Event_approve = mutation({
  args: {
    docId: v.id("events"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runEventApprove(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Event_approve", __result);
    }
    return __result;
  },
});

async function __runEventAssignOwner(ctx: MutationCtx, { docId, assignedToId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("Event not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("Event not found");
    const doc = await __decryptDoc(ctx, "Event", ["primaryContactName","primaryContactEmail","primaryContactPhone"], __storedDoc) as Record<string, any>;
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may read events");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may write events through commands");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may execute event commands");
    if (!((((doc.stage === "planning") || (doc.stage === "pending_approval")) || (doc.stage === "approved")))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      assignedToId: assignedToId,
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "Event", ["primaryContactName","primaryContactEmail","primaryContactPhone"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, eventId: docId, tenantId: __after.tenantId, assignedToId: assignedToId, startsAt: __after.startsAt, endsAt: __after.endsAt, _subject: { entity: "Event", command: "assignOwner", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EventOwnerAssigned", entity: "Event", entityId: docId, payload: { eventId: docId, tenantId: __after.tenantId, assignedToId: assignedToId, startsAt: __after.startsAt, endsAt: __after.endsAt }, createdAt: Date.now() });
    // Reactions
    const __fanId0 = payload.assignedToId;
    const __fanDoc0 = __fanId0 != null && __fanId0 !== "" ? await ctx.db.get(__fanId0 as any) : null;
    const fanRows0 = __fanDoc0 != null && (__fanDoc0 as any).deletedAt == null ? [__fanDoc0] : [];
    for (const __row of fanRows0) {
      const target = __row;
      const __fanMatch0_raw = await ctx.db.query("eventAssignments").withIndex("by_eventId", (q) => q.eq("eventId", payload.eventId)).collect();
      const __fanMatch0_rows = __fanMatch0_raw.filter((d) => (d as any).eventId === payload.eventId && (d as any).personId === (__row as any)._id && (d as any).deletedAt == null).sort((a, b) => String((a as any)._id).localeCompare(String((b as any)._id)));
      const __fanMatch0_id = __fanMatch0_rows.length > 0 ? (__fanMatch0_rows[0] as any)._id : null;
      if (__fanMatch0_id) {
        await __runEventAssignmentAssign(ctx, { docId: __fanMatch0_id, eventId: payload.eventId, personId: (__row as any)._id, role: "event_lead", startsAt: payload.startsAt, endsAt: payload.endsAt } as any);
      } else {
        const __elseArgs: Record<string, any> = { eventId: payload.eventId, personId: (__row as any)._id, role: "event_lead", startsAt: payload.startsAt, endsAt: payload.endsAt };
        const __elseDoc: Record<string, any> = {
          tenantId: __auth.tenantId,
          role: "",
          status: "assigned",
          createdAt: Date.now(),
          updatedAt: Date.now(),
          version: 0,
        };
        for (const __k of ["deletedAt","eventId","personId","role","startsAt","endsAt","notes","status","assignedAt","confirmedAt","checkedInAt","checkedOutAt","noShowAt","unassignedAt","createdAt","updatedAt"] as string[]) {
          if (__elseArgs[__k] !== undefined) __elseDoc[__k] = __elseArgs[__k];
        }
        const __elseId = await ctx.db.insert("eventAssignments", __elseDoc as any);
        await __runEventAssignmentAssign(ctx, { docId: __elseId, ...__elseArgs } as any);
      }
    }
    return { ...doc, ...updates };
}

export const Event_assignOwner = mutation({
  args: {
    docId: v.id("events"),
    assignedToId: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runEventAssignOwner(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Event_assignOwner", __result);
    }
    return __result;
  },
});

async function __runEventBeginExecution(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("Event not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("Event not found");
    const doc = await __decryptDoc(ctx, "Event", ["primaryContactName","primaryContactEmail","primaryContactPhone"], __storedDoc) as Record<string, any>;
    (doc as any).prepTasks = await ctx.db.query("prepTasks").withIndex("by_eventId", (q: any) => q.eq("eventId", docId)).collect();
    (doc as any).packLists = await ctx.db.query("packLists").withIndex("by_eventId", (q: any) => q.eq("eventId", docId)).collect();
    (doc as any).deliveries = await ctx.db.query("deliveries").withIndex("by_eventId", (q: any) => q.eq("eventId", docId)).collect();
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may read events");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may write events through commands");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may execute event commands");
    if (!(((doc.stage === "approved") || (doc.stage === "sales_lock")))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Guard 2 failed");
    if (!((((((doc.prepTasks) ?? []).filter((t: Doc<"prepTasks">) => (((t.status !== "completed") && (t.status !== "cancelled")))).length === 0) && (((doc.packLists) ?? []).filter((p: Doc<"packLists">) => ((((p.status !== "dispatched") && (p.status !== "cancelled")) && (p.status !== "draft")))).length === 0)) && (((doc.deliveries) ?? []).filter((d: Doc<"deliveries">) => ((((d.status !== "delivered") && (d.status !== "cancelled")) && (d.status !== "failed")))).length === 0)))) throw new Error("Guard 3 failed");
    {
      const __cur = doc.stage;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "executing";
        const __allowed: Record<string, string[]> = { "quote": ["planning", "cancelled"], "planning": ["pending_approval", "cancelled"], "pending_approval": ["planning", "approved", "cancelled"], "approved": ["sales_lock", "cancelled"], "sales_lock": ["executing", "cancelled"], "executing": ["final", "cancelled"], "final": ["completed", "cancelled"], "completed": ["closed_out"], "cancelled": [], "closed_out": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'stage'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      stage: "executing",
      executionStartedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "Event", ["primaryContactName","primaryContactEmail","primaryContactPhone"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, eventId: docId, tenantId: __after.tenantId, _subject: { entity: "Event", command: "beginExecution", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EventExecutionStarted", entity: "Event", entityId: docId, payload: { eventId: docId, tenantId: __after.tenantId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Event_beginExecution = mutation({
  args: {
    docId: v.id("events"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runEventBeginExecution(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Event_beginExecution", __result);
    }
    return __result;
  },
});

async function __runEventCancel(ctx: MutationCtx, { docId, reason, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("Event not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("Event not found");
    const doc = await __decryptDoc(ctx, "Event", ["primaryContactName","primaryContactEmail","primaryContactPhone"], __storedDoc) as Record<string, any>;
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may read events");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may write events through commands");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may execute event commands");
    if (!(((((doc.stage === "planning") || (doc.stage === "pending_approval")) || (doc.stage === "approved")) || (doc.stage === "executing")))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(checkRole(user, "eventManageAccess"))) throw new Error("Guard 2 failed");
    if (!((((reason).trim()).length > 0))) throw new Error("Cancellation reason is required");
    {
      const __cur = doc.stage;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "cancelled";
        const __allowed: Record<string, string[]> = { "quote": ["planning", "cancelled"], "planning": ["pending_approval", "cancelled"], "pending_approval": ["planning", "approved", "cancelled"], "approved": ["sales_lock", "cancelled"], "sales_lock": ["executing", "cancelled"], "executing": ["final", "cancelled"], "final": ["completed", "cancelled"], "completed": ["closed_out"], "cancelled": [], "closed_out": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'stage'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      stage: "cancelled",
      cancellationReason: reason,
      cancelledAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "Event", ["primaryContactName","primaryContactEmail","primaryContactPhone"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, eventId: docId, tenantId: __after.tenantId, clientId: __after.clientId, reason: reason, _subject: { entity: "Event", command: "cancel", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EventCancelled", entity: "Event", entityId: docId, payload: { eventId: docId, tenantId: __after.tenantId, clientId: __after.clientId, reason: reason }, createdAt: Date.now() });
    // Reactions
    const fanRows0 = (await ctx.db.query("deliveries").withIndex("by_eventId", (q) => q.eq("eventId", payload.eventId)).collect()).filter((d) => (d as any).deletedAt == null);
    for (const __row of fanRows0) {
      const target = __row;
      await __runDeliveryCancel(ctx, { docId: (__row as any)._id, reason: payload.reason } as any);
    }
    const fanRows1 = (await ctx.db.query("inventoryReservations").withIndex("by_eventId", (q) => q.eq("eventId", payload.eventId)).collect()).filter((d) => (d as any).deletedAt == null);
    for (const __row of fanRows1) {
      const target = __row;
      await __runInventoryReservationRelease(ctx, { docId: (__row as any)._id, reason: payload.reason } as any);
    }
    const fanRows2 = (await ctx.db.query("invoices").withIndex("by_eventId", (q) => q.eq("eventId", payload.eventId)).collect()).filter((d) => (d as any).deletedAt == null);
    for (const __row of fanRows2) {
      const target = __row;
      await __runInvoiceMarkVoided(ctx, { docId: (__row as any)._id, reason: payload.reason } as any);
    }
    const fanRows3 = (await ctx.db.query("packLists").withIndex("by_eventId", (q) => q.eq("eventId", payload.eventId)).collect()).filter((d) => (d as any).deletedAt == null);
    for (const __row of fanRows3) {
      const target = __row;
      await __runPackListCancel(ctx, { docId: (__row as any)._id, reason: payload.reason } as any);
    }
    const fanRows4 = (await ctx.db.query("prepTasks").withIndex("by_eventId", (q) => q.eq("eventId", payload.eventId)).collect()).filter((d) => (d as any).deletedAt == null);
    for (const __row of fanRows4) {
      const target = __row;
      await __runPrepTaskStandDown(ctx, { docId: (__row as any)._id, reason: payload.reason } as any);
    }
    const fanRows5 = (await ctx.db.query("purchaseNeeds").withIndex("by_eventId", (q) => q.eq("eventId", payload.eventId)).collect()).filter((d) => (d as any).deletedAt == null);
    for (const __row of fanRows5) {
      const target = __row;
      await __runPurchaseNeedCancel(ctx, { docId: (__row as any)._id, reason: payload.reason } as any);
    }
    return { ...doc, ...updates };
}

export const Event_cancel = mutation({
  args: {
    docId: v.id("events"),
    reason: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runEventCancel(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Event_cancel", __result);
    }
    return __result;
  },
});

async function __runEventChangeHeadcount(ctx: MutationCtx, { docId, newHeadcount, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("Event not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("Event not found");
    const doc = await __decryptDoc(ctx, "Event", ["primaryContactName","primaryContactEmail","primaryContactPhone"], __storedDoc) as Record<string, any>;
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may read events");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may write events through commands");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may execute event commands");
    if (!(((((doc.stage === "planning") || (doc.stage === "pending_approval")) || (doc.stage === "approved")) || (doc.stage === "executing")))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(((newHeadcount >= 1) && (newHeadcount <= 100000)))) throw new Error("Headcount must be between 1 and 100000");
    const previousHeadcount = doc.expectedHeadcount;
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      expectedHeadcount: newHeadcount,
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "Event", ["primaryContactName","primaryContactEmail","primaryContactPhone"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, eventId: docId, tenantId: __after.tenantId, previousHeadcount: previousHeadcount, newHeadcount: newHeadcount, _subject: { entity: "Event", command: "changeHeadcount", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EventHeadcountChanged", entity: "Event", entityId: docId, payload: { eventId: docId, tenantId: __after.tenantId, previousHeadcount: previousHeadcount, newHeadcount: newHeadcount }, createdAt: Date.now() });
    // Reactions
    const fanRows0 = (await ctx.db.query("eventDishes").withIndex("by_eventId", (q) => q.eq("eventId", payload.eventId)).collect()).filter((d) => (d as any).deletedAt == null);
    for (const __row of fanRows0) {
      const target = __row;
      await __runEventDishAdjustServings(ctx, { docId: (__row as any)._id, quantityServings: payload.newHeadcount } as any);
    }
    return { ...doc, ...updates };
}

export const Event_changeHeadcount = mutation({
  args: {
    docId: v.id("events"),
    newHeadcount: v.any(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runEventChangeHeadcount(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Event_changeHeadcount", __result);
    }
    return __result;
  },
});

async function __runEventChangePricing(ctx: MutationCtx, { docId, budgetAmount, quotedPrice, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("Event not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("Event not found");
    const doc = await __decryptDoc(ctx, "Event", ["primaryContactName","primaryContactEmail","primaryContactPhone"], __storedDoc) as Record<string, any>;
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may read events");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may write events through commands");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may execute event commands");
    if (!((((doc.stage === "planning") || (doc.stage === "pending_approval")) || (doc.stage === "approved")))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((checkRole(user, "eventManageAccess") || checkRole(user, "salesManageAccess")))) throw new Error("Guard 2 failed");
    if (!(((budgetAmount >= 0) && (quotedPrice >= 0)))) throw new Error("Event pricing cannot be negative");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      budgetAmount: budgetAmount,
      quotedPrice: quotedPrice,
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "Event", ["primaryContactName","primaryContactEmail","primaryContactPhone"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, eventId: docId, tenantId: __after.tenantId, budgetAmount: budgetAmount, quotedPrice: quotedPrice, _subject: { entity: "Event", command: "changePricing", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EventPricingChanged", entity: "Event", entityId: docId, payload: { eventId: docId, tenantId: __after.tenantId, budgetAmount: budgetAmount, quotedPrice: quotedPrice }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Event_changePricing = mutation({
  args: {
    docId: v.id("events"),
    budgetAmount: v.number(),
    quotedPrice: v.number(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runEventChangePricing(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Event_changePricing", __result);
    }
    return __result;
  },
});

async function __runEventChangePrimaryContact(ctx: MutationCtx, { docId, primaryContactName, primaryContactEmail, primaryContactPhone, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("Event not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("Event not found");
    const doc = await __decryptDoc(ctx, "Event", ["primaryContactName","primaryContactEmail","primaryContactPhone"], __storedDoc) as Record<string, any>;
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may read events");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may write events through commands");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may execute event commands");
    if (!((((doc.stage === "planning") || (doc.stage === "pending_approval")) || (doc.stage === "approved")))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((((primaryContactName).trim()).length > 0))) throw new Error("Primary contact name is required");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      primaryContactName: primaryContactName,
      primaryContactEmail: primaryContactEmail,
      primaryContactPhone: primaryContactPhone,
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "Event", ["primaryContactName","primaryContactEmail","primaryContactPhone"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, eventId: docId, tenantId: __after.tenantId, _subject: { entity: "Event", command: "changePrimaryContact", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EventPrimaryContactChanged", entity: "Event", entityId: docId, payload: { eventId: docId, tenantId: __after.tenantId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Event_changePrimaryContact = mutation({
  args: {
    docId: v.id("events"),
    primaryContactName: v.string(),
    primaryContactEmail: v.optional(v.string()),
    primaryContactPhone: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runEventChangePrimaryContact(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Event_changePrimaryContact", __result);
    }
    return __result;
  },
});

async function __runEventChangeRequirements(ctx: MutationCtx, { docId, accessibilityNeeds, serviceRequirements, operationalRequirements, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("Event not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("Event not found");
    const doc = await __decryptDoc(ctx, "Event", ["primaryContactName","primaryContactEmail","primaryContactPhone"], __storedDoc) as Record<string, any>;
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may read events");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may write events through commands");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may execute event commands");
    if (!((((doc.stage === "planning") || (doc.stage === "pending_approval")) || (doc.stage === "approved")))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      accessibilityNeeds: ((accessibilityNeeds != null) ? accessibilityNeeds : []),
      serviceRequirements: serviceRequirements,
      operationalRequirements: operationalRequirements,
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "Event", ["primaryContactName","primaryContactEmail","primaryContactPhone"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, eventId: docId, tenantId: __after.tenantId, accessibilityNeeds: ((accessibilityNeeds != null) ? accessibilityNeeds : []), serviceRequirements: serviceRequirements, operationalRequirements: operationalRequirements, _subject: { entity: "Event", command: "changeRequirements", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EventRequirementsChanged", entity: "Event", entityId: docId, payload: { eventId: docId, tenantId: __after.tenantId, accessibilityNeeds: ((accessibilityNeeds != null) ? accessibilityNeeds : []), serviceRequirements: serviceRequirements, operationalRequirements: operationalRequirements }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Event_changeRequirements = mutation({
  args: {
    docId: v.id("events"),
    accessibilityNeeds: v.optional(v.array(v.string())),
    serviceRequirements: v.optional(v.string()),
    operationalRequirements: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runEventChangeRequirements(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Event_changeRequirements", __result);
    }
    return __result;
  },
});

async function __runEventChangeVenue(ctx: MutationCtx, { docId, venueId, venueName, venueAddress, venueCapacity, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("Event not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("Event not found");
    const doc = await __decryptDoc(ctx, "Event", ["primaryContactName","primaryContactEmail","primaryContactPhone"], __storedDoc) as Record<string, any>;
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may read events");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may write events through commands");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may execute event commands");
    if (!((((doc.stage === "planning") || (doc.stage === "pending_approval")) || (doc.stage === "approved")))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(((venueCapacity == null) || (venueCapacity >= 0)))) throw new Error("Venue capacity cannot be negative");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      venueId: venueId,
      venueName: venueName,
      venueAddress: venueAddress,
      venueCapacity: venueCapacity,
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "Event", ["primaryContactName","primaryContactEmail","primaryContactPhone"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, eventId: docId, tenantId: __after.tenantId, venueId: venueId, venueName: venueName, venueAddress: venueAddress, venueCapacity: venueCapacity, _subject: { entity: "Event", command: "changeVenue", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EventVenueChanged", entity: "Event", entityId: docId, payload: { eventId: docId, tenantId: __after.tenantId, venueId: venueId, venueName: venueName, venueAddress: venueAddress, venueCapacity: venueCapacity }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Event_changeVenue = mutation({
  args: {
    docId: v.id("events"),
    venueId: v.optional(v.string()),
    venueName: v.optional(v.string()),
    venueAddress: v.optional(v.string()),
    venueCapacity: v.optional(v.any()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runEventChangeVenue(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Event_changeVenue", __result);
    }
    return __result;
  },
});

async function __runEventCloseOut(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("Event not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("Event not found");
    const doc = await __decryptDoc(ctx, "Event", ["primaryContactName","primaryContactEmail","primaryContactPhone"], __storedDoc) as Record<string, any>;
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may read events");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may write events through commands");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may execute event commands");
    if (!((doc.stage === "completed"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(checkRole(user, "eventManageAccess"))) throw new Error("Guard 2 failed");
    {
      const __cur = doc.stage;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "closed_out";
        const __allowed: Record<string, string[]> = { "quote": ["planning", "cancelled"], "planning": ["pending_approval", "cancelled"], "pending_approval": ["planning", "approved", "cancelled"], "approved": ["sales_lock", "cancelled"], "sales_lock": ["executing", "cancelled"], "executing": ["final", "cancelled"], "final": ["completed", "cancelled"], "completed": ["closed_out"], "cancelled": [], "closed_out": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'stage'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      stage: "closed_out",
      closedOutAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "Event", ["primaryContactName","primaryContactEmail","primaryContactPhone"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, eventId: docId, tenantId: __after.tenantId, clientId: __after.clientId, expectedHeadcount: __after.expectedHeadcount, quotedPrice: __after.quotedPrice, budgetAmount: __after.budgetAmount, _subject: { entity: "Event", command: "closeOut", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EventClosedOut", entity: "Event", entityId: docId, payload: { eventId: docId, tenantId: __after.tenantId, clientId: __after.clientId, expectedHeadcount: __after.expectedHeadcount, quotedPrice: __after.quotedPrice, budgetAmount: __after.budgetAmount }, createdAt: Date.now() });
    // Reactions
    const __match0_raw = await ctx.db.query("eventCloseouts").withIndex("by_eventId", (q) => q.eq("eventId", payload.eventId)).collect();
    const __match0_rows = __match0_raw.filter((d) => (d as any).eventId === payload.eventId && (d as any).deletedAt == null).sort((a, b) => String((a as any)._id).localeCompare(String((b as any)._id)));
    const __match0_id = __match0_rows.length > 0 ? (__match0_rows[0] as any)._id : null;
    if (__match0_id) {
      await __runEventCloseoutCapture(ctx, { docId: __match0_id, eventId: payload.eventId, actualRevenue: 0, budgetedRevenue: ((payload.quotedPrice != null) ? payload.quotedPrice : 0), revenueVariance: ((payload.quotedPrice != null) ? payload.quotedPrice : 0), actualIngredientCost: 0, actualWasteCost: 0, actualLaborCost: 0, actualVendorCost: 0, budgetedCost: ((payload.budgetAmount != null) ? payload.budgetAmount : 0), totalActualCost: 0, costVariance: ((payload.budgetAmount != null) ? payload.budgetAmount : 0), grossProfit: 0, expectedHeadcount: payload.expectedHeadcount, actualHeadcount: 0 } as any);
    } else {
      const __elseArgs: Record<string, any> = { eventId: payload.eventId, actualRevenue: 0, budgetedRevenue: ((payload.quotedPrice != null) ? payload.quotedPrice : 0), revenueVariance: ((payload.quotedPrice != null) ? payload.quotedPrice : 0), actualIngredientCost: 0, actualWasteCost: 0, actualLaborCost: 0, actualVendorCost: 0, budgetedCost: ((payload.budgetAmount != null) ? payload.budgetAmount : 0), totalActualCost: 0, costVariance: ((payload.budgetAmount != null) ? payload.budgetAmount : 0), grossProfit: 0, expectedHeadcount: payload.expectedHeadcount, actualHeadcount: 0 };
      const __elseDoc: Record<string, any> = {
        tenantId: __auth.tenantId,
        actualRevenue: 0,
        budgetedRevenue: 0,
        revenueVariance: 0,
        actualIngredientCost: 0,
        actualWasteCost: 0,
        actualLaborCost: 0,
        actualVendorCost: 0,
        budgetedCost: 0,
        totalActualCost: 0,
        costVariance: 0,
        grossProfit: 0,
        expectedHeadcount: 0,
        actualHeadcount: 0,
        status: "draft",
        createdAt: Date.now(),
        updatedAt: Date.now(),
        version: 0,
      };
      for (const __k of ["deletedAt","eventId","actualRevenue","budgetedRevenue","revenueVariance","actualIngredientCost","actualWasteCost","actualLaborCost","actualVendorCost","budgetedCost","totalActualCost","costVariance","grossProfit","expectedHeadcount","actualHeadcount","unresolvedIssues","performanceNotes","notes","status","capturedAt","finalizedAt","createdAt","updatedAt"] as string[]) {
        if (__elseArgs[__k] !== undefined) __elseDoc[__k] = __elseArgs[__k];
      }
      const __elseId = await ctx.db.insert("eventCloseouts", __elseDoc as any);
      await __runEventCloseoutCapture(ctx, { docId: __elseId, ...__elseArgs } as any);
    }
    return { ...doc, ...updates };
}

export const Event_closeOut = mutation({
  args: {
    docId: v.id("events"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runEventCloseOut(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Event_closeOut", __result);
    }
    return __result;
  },
});

async function __runEventComplete(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("Event not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("Event not found");
    const doc = await __decryptDoc(ctx, "Event", ["primaryContactName","primaryContactEmail","primaryContactPhone"], __storedDoc) as Record<string, any>;
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may read events");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may write events through commands");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may execute event commands");
    if (!((doc.stage === "final"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(checkRole(user, "eventManageAccess"))) throw new Error("Guard 2 failed");
    {
      const __cur = doc.stage;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "completed";
        const __allowed: Record<string, string[]> = { "quote": ["planning", "cancelled"], "planning": ["pending_approval", "cancelled"], "pending_approval": ["planning", "approved", "cancelled"], "approved": ["sales_lock", "cancelled"], "sales_lock": ["executing", "cancelled"], "executing": ["final", "cancelled"], "final": ["completed", "cancelled"], "completed": ["closed_out"], "cancelled": [], "closed_out": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'stage'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      stage: "completed",
      completedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "Event", ["primaryContactName","primaryContactEmail","primaryContactPhone"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, eventId: docId, tenantId: __after.tenantId, _subject: { entity: "Event", command: "complete", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EventCompleted", entity: "Event", entityId: docId, payload: { eventId: docId, tenantId: __after.tenantId }, createdAt: Date.now() });
    // Reactions
    const fanRows0 = (await ctx.db.query("inventoryReservations").withIndex("by_eventId", (q) => q.eq("eventId", payload.eventId)).collect()).filter((d) => (d as any).deletedAt == null);
    for (const __row of fanRows0) {
      const target = __row;
      await __runInventoryReservationRelease(ctx, { docId: (__row as any)._id, reason: "Event completed" } as any);
    }
    return { ...doc, ...updates };
}

export const Event_complete = mutation({
  args: {
    docId: v.id("events"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runEventComplete(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Event_complete", __result);
    }
    return __result;
  },
});

async function __runEventConfigureRecurrence(ctx: MutationCtx, { docId, frequency, endCondition, recurrenceEndsAt, occurrenceLimit, nextStartsAt, seriesId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("Event not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("Event not found");
    const doc = await __decryptDoc(ctx, "Event", ["primaryContactName","primaryContactEmail","primaryContactPhone"], __storedDoc) as Record<string, any>;
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may read events");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may write events through commands");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may execute event commands");
    if (!((((doc.stage === "planning") || (doc.stage === "pending_approval")) || (doc.stage === "approved")))) throw new Error("Guard 0 failed");
    if (!((doc.plannedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.startsAt != null))) throw new Error("Guard 2 failed");
    if (!((doc.endsAt != null))) throw new Error("Guard 3 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 4 failed");
    if (!((doc.recurrenceTemplateEventId == null))) throw new Error("Guard 5 failed");
    if (!((((seriesId).trim()).length > 0))) throw new Error("Recurring series id is required");
    if (!((nextStartsAt > doc.startsAt))) throw new Error("The first recurring instance must start after the source event");
    if (!((((((endCondition === "on_date") && (recurrenceEndsAt != null)) && (recurrenceEndsAt >= nextStartsAt)) && (occurrenceLimit == null)) || (((((endCondition === "after_occurrences") && (recurrenceEndsAt == null)) && (occurrenceLimit != null)) && (occurrenceLimit >= 2)) && (occurrenceLimit <= 1000))))) throw new Error("Choose an end date that includes another event or a total occurrence count from 2 to 1000");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      recurrenceFrequency: frequency,
      recurrenceEndCondition: endCondition,
      recurrenceEndsAt: recurrenceEndsAt,
      recurrenceOccurrenceLimit: occurrenceLimit,
      recurrenceAnchorStartsAt: doc.startsAt,
      recurrenceNextStartsAt: nextStartsAt,
      recurrenceGeneratedCount: 1,
      recurrenceSeriesId: seriesId,
      recurrenceActive: true,
      recurrenceConfiguredAt: Date.now(),
      recurrenceStoppedAt: null,
      recurrenceCompletedAt: null,
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "Event", ["primaryContactName","primaryContactEmail","primaryContactPhone"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, eventId: docId, tenantId: __after.tenantId, frequency: frequency, endCondition: endCondition, recurrenceEndsAt: recurrenceEndsAt, occurrenceLimit: occurrenceLimit, nextStartsAt: nextStartsAt, _subject: { entity: "Event", command: "configureRecurrence", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EventRecurrenceConfigured", entity: "Event", entityId: docId, payload: { eventId: docId, tenantId: __after.tenantId, frequency: frequency, endCondition: endCondition, recurrenceEndsAt: recurrenceEndsAt, occurrenceLimit: occurrenceLimit, nextStartsAt: nextStartsAt }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Event_configureRecurrence = mutation({
  args: {
    docId: v.id("events"),
    frequency: v.any(),
    endCondition: v.any(),
    recurrenceEndsAt: v.optional(v.number()),
    occurrenceLimit: v.optional(v.number()),
    nextStartsAt: v.number(),
    seriesId: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runEventConfigureRecurrence(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Event_configureRecurrence", __result);
    }
    return __result;
  },
});

async function __runEventConfirmSalesLock(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("Event not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("Event not found");
    const doc = await __decryptDoc(ctx, "Event", ["primaryContactName","primaryContactEmail","primaryContactPhone"], __storedDoc) as Record<string, any>;
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may read events");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may write events through commands");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may execute event commands");
    if (!((doc.stage === "sales_lock"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Guard 2 failed");
    {
      const __cur = doc.stage;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "executing";
        const __allowed: Record<string, string[]> = { "quote": ["planning", "cancelled"], "planning": ["pending_approval", "cancelled"], "pending_approval": ["planning", "approved", "cancelled"], "approved": ["sales_lock", "cancelled"], "sales_lock": ["executing", "cancelled"], "executing": ["final", "cancelled"], "final": ["completed", "cancelled"], "completed": ["closed_out"], "cancelled": [], "closed_out": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'stage'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      stage: "executing",
      executionStartedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "Event", ["primaryContactName","primaryContactEmail","primaryContactPhone"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, eventId: docId, tenantId: __after.tenantId, clientId: __after.clientId, startsAt: __after.startsAt, _subject: { entity: "Event", command: "confirmSalesLock", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EventSalesLockConfirmed", entity: "Event", entityId: docId, payload: { eventId: docId, tenantId: __after.tenantId, clientId: __after.clientId, startsAt: __after.startsAt }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Event_confirmSalesLock = mutation({
  args: {
    docId: v.id("events"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runEventConfirmSalesLock(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Event_confirmSalesLock", __result);
    }
    return __result;
  },
});

async function __runEventFinalizeEvent(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("Event not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("Event not found");
    const doc = await __decryptDoc(ctx, "Event", ["primaryContactName","primaryContactEmail","primaryContactPhone"], __storedDoc) as Record<string, any>;
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may read events");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may write events through commands");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may execute event commands");
    if (!((doc.stage === "executing"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(checkRole(user, "eventManageAccess"))) throw new Error("Guard 2 failed");
    {
      const __cur = doc.stage;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "final";
        const __allowed: Record<string, string[]> = { "quote": ["planning", "cancelled"], "planning": ["pending_approval", "cancelled"], "pending_approval": ["planning", "approved", "cancelled"], "approved": ["sales_lock", "cancelled"], "sales_lock": ["executing", "cancelled"], "executing": ["final", "cancelled"], "final": ["completed", "cancelled"], "completed": ["closed_out"], "cancelled": [], "closed_out": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'stage'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      stage: "final",
      finalizedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "Event", ["primaryContactName","primaryContactEmail","primaryContactPhone"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, eventId: docId, tenantId: __after.tenantId, clientId: __after.clientId, expectedHeadcount: __after.expectedHeadcount, quotedPrice: __after.quotedPrice, _subject: { entity: "Event", command: "finalizeEvent", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EventFinalized", entity: "Event", entityId: docId, payload: { eventId: docId, tenantId: __after.tenantId, clientId: __after.clientId, expectedHeadcount: __after.expectedHeadcount, quotedPrice: __after.quotedPrice }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Event_finalizeEvent = mutation({
  args: {
    docId: v.id("events"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runEventFinalizeEvent(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Event_finalizeEvent", __result);
    }
    return __result;
  },
});

async function __runEventLockForSales(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("Event not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("Event not found");
    const doc = await __decryptDoc(ctx, "Event", ["primaryContactName","primaryContactEmail","primaryContactPhone"], __storedDoc) as Record<string, any>;
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may read events");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may write events through commands");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may execute event commands");
    if (!((doc.stage === "approved"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Guard 2 failed");
    if (!((doc.clientId != null))) throw new Error("Guard 3 failed");
    if (!((doc.plannedAt != null))) throw new Error("Guard 4 failed");
    if (!((doc.startsAt != null))) throw new Error("Guard 5 failed");
    if (!((doc.endsAt != null))) throw new Error("Guard 6 failed");
    if (!((doc.expectedHeadcount > 0))) throw new Error("Guard 7 failed");
    {
      const __cur = doc.stage;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "sales_lock";
        const __allowed: Record<string, string[]> = { "quote": ["planning", "cancelled"], "planning": ["pending_approval", "cancelled"], "pending_approval": ["planning", "approved", "cancelled"], "approved": ["sales_lock", "cancelled"], "sales_lock": ["executing", "cancelled"], "executing": ["final", "cancelled"], "final": ["completed", "cancelled"], "completed": ["closed_out"], "cancelled": [], "closed_out": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'stage'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      stage: "sales_lock",
      salesLockedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "Event", ["primaryContactName","primaryContactEmail","primaryContactPhone"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, eventId: docId, tenantId: __after.tenantId, clientId: __after.clientId, expectedHeadcount: __after.expectedHeadcount, quotedPrice: __after.quotedPrice, startsAt: __after.startsAt, endsAt: __after.endsAt, _subject: { entity: "Event", command: "lockForSales", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EventSalesLocked", entity: "Event", entityId: docId, payload: { eventId: docId, tenantId: __after.tenantId, clientId: __after.clientId, expectedHeadcount: __after.expectedHeadcount, quotedPrice: __after.quotedPrice, startsAt: __after.startsAt, endsAt: __after.endsAt }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Event_lockForSales = mutation({
  args: {
    docId: v.id("events"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runEventLockForSales(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Event_lockForSales", __result);
    }
    return __result;
  },
});

async function __runEventPlanEngagement(ctx: MutationCtx, { docId, clientId, title, eventType, startsAt, endsAt, expectedHeadcount, primaryContactName, budgetAmount, quotedPrice, serviceStyleId, occasionId, venueId, venueName, venueAddress, venueCapacity, primaryContactEmail, primaryContactPhone, accessibilityNeeds, serviceRequirements, operationalRequirements, assignedToId, referralSourceId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("Event not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("Event not found");
    const doc = await __decryptDoc(ctx, "Event", ["primaryContactName","primaryContactEmail","primaryContactPhone"], __storedDoc) as Record<string, any>;
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may read events");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may write events through commands");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may execute event commands");
    if (!((doc.stage === "planning"))) throw new Error("Guard 0 failed");
    if (!((doc.plannedAt == null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((((title).trim()).length > 0))) throw new Error("Event title is required");
    if (!((((eventType).trim()).length > 0))) throw new Error("Event type is required");
    if (!((endsAt > startsAt))) throw new Error("Event end must be after its start");
    if (!(((expectedHeadcount >= 1) && (expectedHeadcount <= 100000)))) throw new Error("Headcount must be between 1 and 100000");
    if (!(((venueCapacity == null) || (venueCapacity >= 0)))) throw new Error("Venue capacity cannot be negative");
    if (!((((primaryContactName).trim()).length > 0))) throw new Error("Primary contact name is required");
    if (!(((budgetAmount >= 0) && (quotedPrice >= 0)))) throw new Error("Event pricing cannot be negative");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      clientId: clientId,
      title: title,
      eventType: eventType,
      startsAt: startsAt,
      endsAt: endsAt,
      purchasingWeekStart: startsAt,
      serviceStyleId: serviceStyleId,
      occasionId: occasionId,
      venueId: venueId,
      venueName: venueName,
      venueAddress: venueAddress,
      venueCapacity: venueCapacity,
      expectedHeadcount: expectedHeadcount,
      primaryContactName: primaryContactName,
      primaryContactEmail: primaryContactEmail,
      primaryContactPhone: primaryContactPhone,
      accessibilityNeeds: ((accessibilityNeeds != null) ? accessibilityNeeds : []),
      serviceRequirements: serviceRequirements,
      operationalRequirements: operationalRequirements,
      budgetAmount: budgetAmount,
      quotedPrice: quotedPrice,
      assignedToId: assignedToId,
      referralSourceId: referralSourceId,
      plannedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "Event", ["primaryContactName","primaryContactEmail","primaryContactPhone"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, eventId: docId, tenantId: __after.tenantId, clientId: clientId, serviceStyleId: serviceStyleId, venueId: venueId, venueCapacity: venueCapacity, startsAt: startsAt, endsAt: endsAt, expectedHeadcount: expectedHeadcount, _subject: { entity: "Event", command: "planEngagement", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EventPlanned", entity: "Event", entityId: docId, payload: { eventId: docId, tenantId: __after.tenantId, clientId: clientId, serviceStyleId: serviceStyleId, venueId: venueId, venueCapacity: venueCapacity, startsAt: startsAt, endsAt: endsAt, expectedHeadcount: expectedHeadcount }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Event_planEngagement = mutation({
  args: {
    docId: v.id("events"),
    clientId: v.string(),
    title: v.string(),
    eventType: v.string(),
    startsAt: v.number(),
    endsAt: v.number(),
    expectedHeadcount: v.any(),
    primaryContactName: v.string(),
    budgetAmount: v.number(),
    quotedPrice: v.number(),
    serviceStyleId: v.optional(v.string()),
    occasionId: v.optional(v.string()),
    venueId: v.optional(v.string()),
    venueName: v.optional(v.string()),
    venueAddress: v.optional(v.string()),
    venueCapacity: v.optional(v.any()),
    primaryContactEmail: v.optional(v.string()),
    primaryContactPhone: v.optional(v.string()),
    accessibilityNeeds: v.optional(v.array(v.string())),
    serviceRequirements: v.optional(v.string()),
    operationalRequirements: v.optional(v.string()),
    assignedToId: v.optional(v.string()),
    referralSourceId: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runEventPlanEngagement(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Event_planEngagement", __result);
    }
    return __result;
  },
});

export const Event_createViaPlanEngagement = mutation({
  args: {
    clientId: v.string(),
    title: v.string(),
    eventType: v.string(),
    startsAt: v.number(),
    endsAt: v.number(),
    expectedHeadcount: v.any(),
    primaryContactName: v.string(),
    budgetAmount: v.number(),
    quotedPrice: v.number(),
    serviceStyleId: v.optional(v.string()),
    occasionId: v.optional(v.string()),
    venueId: v.optional(v.string()),
    venueName: v.optional(v.string()),
    venueAddress: v.optional(v.string()),
    venueCapacity: v.optional(v.any()),
    primaryContactEmail: v.optional(v.string()),
    primaryContactPhone: v.optional(v.string()),
    accessibilityNeeds: v.optional(v.array(v.string())),
    serviceRequirements: v.optional(v.string()),
    operationalRequirements: v.optional(v.string()),
    assignedToId: v.optional(v.string()),
    referralSourceId: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { clientId, title, eventType, startsAt, endsAt, expectedHeadcount, primaryContactName, budgetAmount, quotedPrice, serviceStyleId, occasionId, venueId, venueName, venueAddress, venueCapacity, primaryContactEmail, primaryContactPhone, accessibilityNeeds, serviceRequirements, operationalRequirements, assignedToId, referralSourceId } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      accessibilityNeeds: args.accessibilityNeeds !== undefined ? args.accessibilityNeeds : [],
      stage: "planning",
      recurrenceActive: false,
      createdAt: Date.now(),
      updatedAt: Date.now(),
      assignedToId: args.assignedToId,
      budgetAmount: args.budgetAmount,
      clientId: args.clientId,
      endsAt: args.endsAt,
      eventType: args.eventType,
      expectedHeadcount: args.expectedHeadcount,
      occasionId: args.occasionId,
      operationalRequirements: args.operationalRequirements,
      primaryContactEmail: args.primaryContactEmail,
      primaryContactName: args.primaryContactName,
      primaryContactPhone: args.primaryContactPhone,
      quotedPrice: args.quotedPrice,
      referralSourceId: args.referralSourceId,
      serviceRequirements: args.serviceRequirements,
      serviceStyleId: args.serviceStyleId,
      startsAt: args.startsAt,
      title: args.title,
      venueAddress: args.venueAddress,
      venueCapacity: args.venueCapacity,
      venueId: args.venueId,
      venueName: args.venueName
    };
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may read events");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may write events through commands");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may execute event commands");
    if (!((__draft.stage === "planning"))) throw new Error("Guard 0 failed");
    if (!((__draft.plannedAt == null))) throw new Error("Guard 1 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((((title).trim()).length > 0))) throw new Error("Event title is required");
    if (!((((eventType).trim()).length > 0))) throw new Error("Event type is required");
    if (!((endsAt > startsAt))) throw new Error("Event end must be after its start");
    if (!(((expectedHeadcount >= 1) && (expectedHeadcount <= 100000)))) throw new Error("Headcount must be between 1 and 100000");
    if (!(((venueCapacity == null) || (venueCapacity >= 0)))) throw new Error("Venue capacity cannot be negative");
    if (!((((primaryContactName).trim()).length > 0))) throw new Error("Primary contact name is required");
    if (!(((budgetAmount >= 0) && (quotedPrice >= 0)))) throw new Error("Event pricing cannot be negative");
    const doc: Record<string, any> = {
      ...__draft,
      clientId: clientId,
      title: title,
      eventType: eventType,
      startsAt: startsAt,
      endsAt: endsAt,
      purchasingWeekStart: startsAt,
      serviceStyleId: serviceStyleId,
      occasionId: occasionId,
      venueId: venueId,
      venueName: venueName,
      venueAddress: venueAddress,
      venueCapacity: venueCapacity,
      expectedHeadcount: expectedHeadcount,
      primaryContactName: primaryContactName,
      primaryContactEmail: primaryContactEmail,
      primaryContactPhone: primaryContactPhone,
      accessibilityNeeds: ((accessibilityNeeds != null) ? accessibilityNeeds : []),
      serviceRequirements: serviceRequirements,
      operationalRequirements: operationalRequirements,
      budgetAmount: budgetAmount,
      quotedPrice: quotedPrice,
      assignedToId: assignedToId,
      referralSourceId: referralSourceId,
      plannedAt: Date.now(),
      version: 1,
    };
    const __storedDoc = await __encryptDoc(ctx, "Event", ["primaryContactName","primaryContactEmail","primaryContactPhone"], doc);
    const docId = await ctx.db.insert("events", __storedDoc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, eventId: docId, tenantId: doc.tenantId, clientId: doc.clientId, serviceStyleId: doc.serviceStyleId, venueId: doc.venueId, venueCapacity: doc.venueCapacity, startsAt: doc.startsAt, endsAt: doc.endsAt, expectedHeadcount: doc.expectedHeadcount, _subject: { entity: "Event", command: "planEngagement", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EventPlanned", entity: "Event", entityId: docId, payload: { eventId: docId, tenantId: doc.tenantId, clientId: doc.clientId, serviceStyleId: doc.serviceStyleId, venueId: doc.venueId, venueCapacity: doc.venueCapacity, startsAt: doc.startsAt, endsAt: doc.endsAt, expectedHeadcount: doc.expectedHeadcount }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Event_createViaPlanEngagement", __result);
    }
    return __result;
  },
});

async function __runEventReassignClient(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("Event not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("Event not found");
    const doc = await __decryptDoc(ctx, "Event", ["primaryContactName","primaryContactEmail","primaryContactPhone"], __storedDoc) as Record<string, any>;
    const __rel_clientMergeAuthorization = await __resolveRelation(ctx, "clientMerges", [__auth.tenantId, doc.clientMergeAuthorizationId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_mergeTargetClient = await __resolveRelation(ctx, "clients", [__auth.tenantId, doc.mergeTargetClientId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).clientMergeAuthorizationId;
      ((doc as any) as any).clientMergeAuthorization = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    {
      const __fk = ((doc as any) as any).mergeTargetClientId;
      ((doc as any) as any).mergeTargetClient = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may read events");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may write events through commands");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may execute event commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!(checkRole(user, "adminAccess"))) throw new Error("Guard 1 failed");
    if (!((__rel_clientMergeAuthorization != null))) throw new Error("Guard 2 failed");
    if (!((__rel_clientMergeAuthorization.deletedAt == null))) throw new Error("Guard 3 failed");
    if (!((__rel_clientMergeAuthorization.mergedAt != null))) throw new Error("Guard 4 failed");
    if (!((__rel_mergeTargetClient != null))) throw new Error("Guard 5 failed");
    if (!((__rel_mergeTargetClient.status === "active"))) throw new Error("Guard 6 failed");
    if (!((__rel_mergeTargetClient.deletedAt == null))) throw new Error("Guard 7 failed");
    if (!((__rel_clientMergeAuthorization.duplicateClientId === doc.clientId))) throw new Error("Merge authorization does not match the event client");
    if (!((__rel_clientMergeAuthorization.primaryClientId === doc.mergeTargetClientId))) throw new Error("Merge authorization does not match the primary client");
    const previousClientId = doc.clientId;
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      clientId: doc.mergeTargetClientId,
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "Event", ["primaryContactName","primaryContactEmail","primaryContactPhone"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, eventId: docId, tenantId: __after.tenantId, previousClientId: previousClientId, clientId: __after.mergeTargetClientId, _subject: { entity: "Event", command: "reassignClient", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EventClientReassigned", entity: "Event", entityId: docId, payload: { eventId: docId, tenantId: __after.tenantId, previousClientId: previousClientId, clientId: __after.mergeTargetClientId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Event_reassignClient = mutation({
  args: {
    docId: v.id("events"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runEventReassignClient(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Event_reassignClient", __result);
    }
    return __result;
  },
});

async function __runEventReschedule(ctx: MutationCtx, { docId, startsAt, endsAt, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("Event not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("Event not found");
    const doc = await __decryptDoc(ctx, "Event", ["primaryContactName","primaryContactEmail","primaryContactPhone"], __storedDoc) as Record<string, any>;
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may read events");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may write events through commands");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may execute event commands");
    if (!((((doc.stage === "planning") || (doc.stage === "pending_approval")) || (doc.stage === "approved")))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((endsAt > startsAt))) throw new Error("Event end must be after its start");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      startsAt: startsAt,
      endsAt: endsAt,
      purchasingWeekStart: startsAt,
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "Event", ["primaryContactName","primaryContactEmail","primaryContactPhone"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, eventId: docId, tenantId: __after.tenantId, startsAt: startsAt, endsAt: endsAt, _subject: { entity: "Event", command: "reschedule", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EventScheduleChanged", entity: "Event", entityId: docId, payload: { eventId: docId, tenantId: __after.tenantId, startsAt: startsAt, endsAt: endsAt }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Event_reschedule = mutation({
  args: {
    docId: v.id("events"),
    startsAt: v.number(),
    endsAt: v.number(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runEventReschedule(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Event_reschedule", __result);
    }
    return __result;
  },
});

async function __runEventReturnToPlanning(ctx: MutationCtx, { docId, reason, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("Event not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("Event not found");
    const doc = await __decryptDoc(ctx, "Event", ["primaryContactName","primaryContactEmail","primaryContactPhone"], __storedDoc) as Record<string, any>;
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may read events");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may write events through commands");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may execute event commands");
    if (!(((doc.stage === "pending_approval") || (doc.stage === "approved")))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(checkRole(user, "eventManageAccess"))) throw new Error("Guard 2 failed");
    if (!((((reason).trim()).length > 0))) throw new Error("Return reason is required");
    {
      const __cur = doc.stage;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "planning";
        const __allowed: Record<string, string[]> = { "quote": ["planning", "cancelled"], "planning": ["pending_approval", "cancelled"], "pending_approval": ["planning", "approved", "cancelled"], "approved": ["sales_lock", "cancelled"], "sales_lock": ["executing", "cancelled"], "executing": ["final", "cancelled"], "final": ["completed", "cancelled"], "completed": ["closed_out"], "cancelled": [], "closed_out": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'stage'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      stage: "planning",
      approvedAt: null,
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "Event", ["primaryContactName","primaryContactEmail","primaryContactPhone"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, eventId: docId, tenantId: __after.tenantId, reason: reason, _subject: { entity: "Event", command: "returnToPlanning", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EventReturnedToPlanning", entity: "Event", entityId: docId, payload: { eventId: docId, tenantId: __after.tenantId, reason: reason }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Event_returnToPlanning = mutation({
  args: {
    docId: v.id("events"),
    reason: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runEventReturnToPlanning(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Event_returnToPlanning", __result);
    }
    return __result;
  },
});

async function __runEventStageClientMerge(ctx: MutationCtx, { docId, clientMergeId, clientId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("Event not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("Event not found");
    const doc = await __decryptDoc(ctx, "Event", ["primaryContactName","primaryContactEmail","primaryContactPhone"], __storedDoc) as Record<string, any>;
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may read events");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may write events through commands");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may execute event commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!(checkRole(user, "adminAccess"))) throw new Error("Guard 1 failed");
    if (!((clientId !== doc.clientId))) throw new Error("Event already belongs to this client");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      clientMergeAuthorizationId: clientMergeId,
      mergeTargetClientId: clientId,
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "Event", ["primaryContactName","primaryContactEmail","primaryContactPhone"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, eventId: docId, tenantId: __after.tenantId, _subject: { entity: "Event", command: "stageClientMerge", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EventClientMergeStaged", entity: "Event", entityId: docId, payload: { eventId: docId, tenantId: __after.tenantId }, createdAt: Date.now() });
    // Reactions
    const reactionTarget0 = payload.eventId;
    if (reactionTarget0) await __runEventReassignClient(ctx, { docId: reactionTarget0 } as any);
    return { ...doc, ...updates };
}

export const Event_stageClientMerge = mutation({
  args: {
    docId: v.id("events"),
    clientMergeId: v.string(),
    clientId: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runEventStageClientMerge(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Event_stageClientMerge", __result);
    }
    return __result;
  },
});

async function __runEventStopRecurrence(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("Event not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("Event not found");
    const doc = await __decryptDoc(ctx, "Event", ["primaryContactName","primaryContactEmail","primaryContactPhone"], __storedDoc) as Record<string, any>;
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may read events");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may write events through commands");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may execute event commands");
    if (!((doc.recurrenceActive === true))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      recurrenceActive: false,
      recurrenceNextStartsAt: null,
      recurrenceStoppedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "Event", ["primaryContactName","primaryContactEmail","primaryContactPhone"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, eventId: docId, tenantId: __after.tenantId, generatedCount: __after.recurrenceGeneratedCount, _subject: { entity: "Event", command: "stopRecurrence", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EventRecurrenceStopped", entity: "Event", entityId: docId, payload: { eventId: docId, tenantId: __after.tenantId, generatedCount: __after.recurrenceGeneratedCount }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Event_stopRecurrence = mutation({
  args: {
    docId: v.id("events"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runEventStopRecurrence(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Event_stopRecurrence", __result);
    }
    return __result;
  },
});

async function __runEventSubmitForApproval(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("Event not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("Event not found");
    const doc = await __decryptDoc(ctx, "Event", ["primaryContactName","primaryContactEmail","primaryContactPhone"], __storedDoc) as Record<string, any>;
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may read events");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may write events through commands");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may execute event commands");
    if (!((doc.stage === "planning"))) throw new Error("Guard 0 failed");
    if (!((doc.plannedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    {
      const __cur = doc.stage;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "pending_approval";
        const __allowed: Record<string, string[]> = { "quote": ["planning", "cancelled"], "planning": ["pending_approval", "cancelled"], "pending_approval": ["planning", "approved", "cancelled"], "approved": ["sales_lock", "cancelled"], "sales_lock": ["executing", "cancelled"], "executing": ["final", "cancelled"], "final": ["completed", "cancelled"], "completed": ["closed_out"], "cancelled": [], "closed_out": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'stage'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      stage: "pending_approval",
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "Event", ["primaryContactName","primaryContactEmail","primaryContactPhone"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, eventId: docId, tenantId: __after.tenantId, _subject: { entity: "Event", command: "submitForApproval", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EventSubmittedForApproval", entity: "Event", entityId: docId, payload: { eventId: docId, tenantId: __after.tenantId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Event_submitForApproval = mutation({
  args: {
    docId: v.id("events"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runEventSubmitForApproval(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Event_submitForApproval", __result);
    }
    return __result;
  },
});

async function __runEventAllergenCheckRecord(ctx: MutationCtx, { docId, eventId, result, eventDishId, dishId, flaggedAllergens, notes, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("EventAllergenCheck not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("EventAllergenCheck not found");
    const __rel_event = await __resolveRelation(ctx, "events", [__auth.tenantId, doc.eventId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).eventId;
      ((doc as any) as any).event = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!((checkRole(user, "eventAccess") || checkRole(user, "kitchenAccess")))) throw new Error("Event and kitchen staff may read event allergen checks");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "kitchenAccess")))) throw new Error("Event and kitchen staff may write event allergen checks through commands");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "kitchenAccess")))) throw new Error("Event and kitchen staff may execute event allergen check commands");
    if (!((doc.status === "pending"))) throw new Error("Guard 0 failed");
    if (!((doc.checkedAt == null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((__rel_event != null))) throw new Error("Guard 3 failed");
    if (!((user.id != null))) throw new Error("Guard 4 failed");
    if (!((eventId === doc.eventId))) throw new Error("Record eventId must match the seeded event reference");
    if (!((((eventDishId == null) || (doc.eventDishId == null)) || (eventDishId === doc.eventDishId)))) throw new Error("Record eventDishId must match the seeded event dish reference when provided");
    if (!((((dishId == null) || (doc.dishId == null)) || (dishId === doc.dishId)))) throw new Error("Record dishId must match the seeded dish reference when provided");
    if (!((((result === "pass") && ((flaggedAllergens == null) || ((flaggedAllergens).length === 0))) || (((result === "flagged") && (flaggedAllergens != null)) && ((flaggedAllergens).length > 0))))) throw new Error("Pass requires no flagged allergens; flagged requires at least one");
    const nextFlags = ((flaggedAllergens != null) ? flaggedAllergens : []);
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "recorded";
        const __allowed: Record<string, string[]> = { "pending": ["recorded"], "recorded": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      eventId: eventId,
      eventDishId: ((eventDishId != null) ? eventDishId : doc.eventDishId),
      dishId: ((dishId != null) ? dishId : doc.dishId),
      result: result,
      flaggedAllergens: nextFlags,
      notes: ((notes != null) ? notes : doc.notes),
      checkedById: user.personId,
      status: "recorded",
      checkedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, allergenCheckId: docId, tenantId: __after.tenantId, eventId: eventId, eventDishId: ((eventDishId != null) ? eventDishId : __after.eventDishId), dishId: ((dishId != null) ? dishId : __after.dishId), result: result, flaggedAllergens: nextFlags, checkedById: user.personId, status: "recorded", _subject: { entity: "EventAllergenCheck", command: "record", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EventAllergenCheckRecorded", entity: "EventAllergenCheck", entityId: docId, payload: { allergenCheckId: docId, tenantId: __after.tenantId, eventId: eventId, eventDishId: ((eventDishId != null) ? eventDishId : __after.eventDishId), dishId: ((dishId != null) ? dishId : __after.dishId), result: result, flaggedAllergens: nextFlags, checkedById: user.personId, status: "recorded" }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const EventAllergenCheck_record = mutation({
  args: {
    docId: v.id("eventAllergenChecks"),
    eventId: v.string(),
    result: v.any(),
    eventDishId: v.optional(v.string()),
    dishId: v.optional(v.string()),
    flaggedAllergens: v.optional(v.array(v.any())),
    notes: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runEventAllergenCheckRecord(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "EventAllergenCheck_record", __result);
    }
    return __result;
  },
});

export const EventAllergenCheck_createViaRecord = mutation({
  args: {
    eventId: v.string(),
    result: v.any(),
    eventDishId: v.optional(v.string()),
    dishId: v.optional(v.string()),
    flaggedAllergens: v.optional(v.array(v.any())),
    notes: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { eventId, result, eventDishId, dishId, flaggedAllergens, notes } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      flaggedAllergens: args.flaggedAllergens !== undefined ? args.flaggedAllergens : [],
      createdAt: Date.now(),
      updatedAt: Date.now(),
      status: "pending",
      dishId: args.dishId,
      eventDishId: args.eventDishId,
      eventId: args.eventId,
      notes: args.notes,
      result: args.result
    };
    const __rel_event = await __resolveRelation(ctx, "events", [__auth.tenantId, __draft.eventId], ["tenantId","id"], "tenantId", __auth.tenantId);
    if (!((checkRole(user, "eventAccess") || checkRole(user, "kitchenAccess")))) throw new Error("Event and kitchen staff may read event allergen checks");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "kitchenAccess")))) throw new Error("Event and kitchen staff may write event allergen checks through commands");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "kitchenAccess")))) throw new Error("Event and kitchen staff may execute event allergen check commands");
    if (!((__draft.status === "pending"))) throw new Error("Guard 0 failed");
    if (!((__draft.checkedAt == null))) throw new Error("Guard 1 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((__rel_event != null))) throw new Error("Guard 3 failed");
    if (!((user.id != null))) throw new Error("Guard 4 failed");
    if (!((eventId === __draft.eventId))) throw new Error("Record eventId must match the seeded event reference");
    if (!((((eventDishId == null) || (__draft.eventDishId == null)) || (eventDishId === __draft.eventDishId)))) throw new Error("Record eventDishId must match the seeded event dish reference when provided");
    if (!((((dishId == null) || (__draft.dishId == null)) || (dishId === __draft.dishId)))) throw new Error("Record dishId must match the seeded dish reference when provided");
    if (!((((result === "pass") && ((flaggedAllergens == null) || ((flaggedAllergens).length === 0))) || (((result === "flagged") && (flaggedAllergens != null)) && ((flaggedAllergens).length > 0))))) throw new Error("Pass requires no flagged allergens; flagged requires at least one");
    const doc: Record<string, any> = {
      ...__draft,
      eventId: eventId,
      eventDishId: ((eventDishId != null) ? eventDishId : __draft.eventDishId),
      dishId: ((dishId != null) ? dishId : __draft.dishId),
      result: result,
      flaggedAllergens: __draft.nextFlags,
      notes: ((notes != null) ? notes : __draft.notes),
      checkedById: user.personId,
      status: "recorded",
      checkedAt: Date.now(),
      version: 1,
    };
    const docId = await ctx.db.insert("eventAllergenChecks", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, allergenCheckId: docId, tenantId: doc.tenantId, eventId: doc.eventId, eventDishId: ((doc.eventDishId != null) ? doc.eventDishId : doc.eventDishId), dishId: ((doc.dishId != null) ? doc.dishId : doc.dishId), result: doc.result, flaggedAllergens: doc.nextFlags, checkedById: user.personId, status: "recorded", _subject: { entity: "EventAllergenCheck", command: "record", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EventAllergenCheckRecorded", entity: "EventAllergenCheck", entityId: docId, payload: { allergenCheckId: docId, tenantId: doc.tenantId, eventId: doc.eventId, eventDishId: ((doc.eventDishId != null) ? doc.eventDishId : doc.eventDishId), dishId: ((doc.dishId != null) ? doc.dishId : doc.dishId), result: doc.result, flaggedAllergens: doc.nextFlags, checkedById: user.personId, status: "recorded" }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "EventAllergenCheck_createViaRecord", __result);
    }
    return __result;
  },
});

async function __runEventAssignmentAssign(ctx: MutationCtx, { docId, eventId, personId, role, startsAt, endsAt, notes, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("EventAssignment not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("EventAssignment not found");
    const doc = await __decryptDoc(ctx, "EventAssignment", ["notes"], __storedDoc) as Record<string, any>;
    const __rel_event = await __resolveRelation(ctx, "events", [__auth.tenantId, doc.eventId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_person = await __resolveRelation(ctx, "people", [__auth.tenantId, doc.personId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).eventId;
      ((doc as any) as any).event = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    {
      const __fk = ((doc as any) as any).personId;
      ((doc as any) as any).person = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may read event assignments");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may write event assignments through commands");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may execute event assignment commands");
    if (!((doc.assignedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.status === "assigned"))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((__rel_event != null))) throw new Error("Guard 3 failed");
    if (!(((__rel_person != null) && (__rel_person.status === "active")))) throw new Error("Guard 4 failed");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Guard 5 failed");
    if (!((eventId === doc.eventId))) throw new Error("Assign eventId must match the seeded event reference");
    if (!((personId === doc.personId))) throw new Error("Assign personId must match the seeded person reference");
    if (!((((role).trim()).length > 0))) throw new Error("Assignment role is required");
    if (!((((startsAt == null) || (endsAt == null)) || (endsAt > startsAt)))) throw new Error("Assignment shift end must be after its start");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      eventId: eventId,
      personId: personId,
      role: role,
      startsAt: ((startsAt != null) ? startsAt : doc.startsAt),
      endsAt: ((endsAt != null) ? endsAt : doc.endsAt),
      notes: ((notes != null) ? notes : doc.notes),
      assignedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "EventAssignment", ["notes"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, eventAssignmentId: docId, tenantId: __after.tenantId, eventId: eventId, personId: personId, role: role, startsAt: ((startsAt != null) ? startsAt : __after.startsAt), endsAt: ((endsAt != null) ? endsAt : __after.endsAt), status: "assigned", _subject: { entity: "EventAssignment", command: "assign", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EventAssignmentAssigned", entity: "EventAssignment", entityId: docId, payload: { eventAssignmentId: docId, tenantId: __after.tenantId, eventId: eventId, personId: personId, role: role, startsAt: ((startsAt != null) ? startsAt : __after.startsAt), endsAt: ((endsAt != null) ? endsAt : __after.endsAt), status: "assigned" }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const EventAssignment_assign = mutation({
  args: {
    docId: v.id("eventAssignments"),
    eventId: v.string(),
    personId: v.string(),
    role: v.string(),
    startsAt: v.optional(v.number()),
    endsAt: v.optional(v.number()),
    notes: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runEventAssignmentAssign(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "EventAssignment_assign", __result);
    }
    return __result;
  },
});

export const EventAssignment_createViaAssign = mutation({
  args: {
    eventId: v.string(),
    personId: v.string(),
    role: v.string(),
    startsAt: v.optional(v.number()),
    endsAt: v.optional(v.number()),
    notes: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { eventId, personId, role, startsAt, endsAt, notes } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      status: "assigned",
      createdAt: Date.now(),
      updatedAt: Date.now(),
      endsAt: args.endsAt,
      eventId: args.eventId,
      notes: args.notes,
      personId: args.personId,
      role: args.role,
      startsAt: args.startsAt
    };
    const __rel_event = await __resolveRelation(ctx, "events", [__auth.tenantId, __draft.eventId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_person = await __resolveRelation(ctx, "people", [__auth.tenantId, __draft.personId], ["tenantId","id"], "tenantId", __auth.tenantId);
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may read event assignments");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may write event assignments through commands");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may execute event assignment commands");
    if (!((__draft.assignedAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.status === "assigned"))) throw new Error("Guard 1 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((__rel_event != null))) throw new Error("Guard 3 failed");
    if (!(((__rel_person != null) && (__rel_person.status === "active")))) throw new Error("Guard 4 failed");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Guard 5 failed");
    if (!((eventId === __draft.eventId))) throw new Error("Assign eventId must match the seeded event reference");
    if (!((personId === __draft.personId))) throw new Error("Assign personId must match the seeded person reference");
    if (!((((role).trim()).length > 0))) throw new Error("Assignment role is required");
    if (!((((startsAt == null) || (endsAt == null)) || (endsAt > startsAt)))) throw new Error("Assignment shift end must be after its start");
    const doc: Record<string, any> = {
      ...__draft,
      eventId: eventId,
      personId: personId,
      role: role,
      startsAt: ((startsAt != null) ? startsAt : __draft.startsAt),
      endsAt: ((endsAt != null) ? endsAt : __draft.endsAt),
      notes: ((notes != null) ? notes : __draft.notes),
      assignedAt: Date.now(),
      version: 1,
    };
    const __storedDoc = await __encryptDoc(ctx, "EventAssignment", ["notes"], doc);
    const docId = await ctx.db.insert("eventAssignments", __storedDoc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, eventAssignmentId: docId, tenantId: doc.tenantId, eventId: doc.eventId, personId: doc.personId, role: doc.role, startsAt: ((doc.startsAt != null) ? doc.startsAt : doc.startsAt), endsAt: ((doc.endsAt != null) ? doc.endsAt : doc.endsAt), status: "assigned", _subject: { entity: "EventAssignment", command: "assign", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EventAssignmentAssigned", entity: "EventAssignment", entityId: docId, payload: { eventAssignmentId: docId, tenantId: doc.tenantId, eventId: doc.eventId, personId: doc.personId, role: doc.role, startsAt: ((doc.startsAt != null) ? doc.startsAt : doc.startsAt), endsAt: ((doc.endsAt != null) ? doc.endsAt : doc.endsAt), status: "assigned" }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "EventAssignment_createViaAssign", __result);
    }
    return __result;
  },
});

async function __runEventAssignmentCheckIn(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("EventAssignment not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("EventAssignment not found");
    const doc = await __decryptDoc(ctx, "EventAssignment", ["notes"], __storedDoc) as Record<string, any>;
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may read event assignments");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may write event assignments through commands");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may execute event assignment commands");
    if (!(((doc.status === "assigned") || (doc.status === "confirmed")))) throw new Error("Guard 0 failed");
    if (!((doc.assignedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((user.id != null))) throw new Error("Guard 3 failed");
    if (!(((doc.personId === user.id) || checkRole(user, "workforceManageAccess")))) throw new Error("Guard 4 failed");
    const previousStatus = doc.status;
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "checked_in";
        const __allowed: Record<string, string[]> = { "assigned": ["confirmed", "checked_in", "no_show", "unassigned"], "confirmed": ["checked_in", "no_show", "unassigned"], "checked_in": ["checked_out"], "checked_out": [], "no_show": [], "unassigned": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "checked_in",
      checkedInAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "EventAssignment", ["notes"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, eventAssignmentId: docId, tenantId: __after.tenantId, eventId: __after.eventId, personId: __after.personId, role: __after.role, previousStatus: previousStatus, status: "checked_in", _subject: { entity: "EventAssignment", command: "checkIn", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EventAssignmentCheckedIn", entity: "EventAssignment", entityId: docId, payload: { eventAssignmentId: docId, tenantId: __after.tenantId, eventId: __after.eventId, personId: __after.personId, role: __after.role, previousStatus: previousStatus, status: "checked_in" }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const EventAssignment_checkIn = mutation({
  args: {
    docId: v.id("eventAssignments"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runEventAssignmentCheckIn(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "EventAssignment_checkIn", __result);
    }
    return __result;
  },
});

async function __runEventAssignmentCheckOut(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("EventAssignment not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("EventAssignment not found");
    const doc = await __decryptDoc(ctx, "EventAssignment", ["notes"], __storedDoc) as Record<string, any>;
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may read event assignments");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may write event assignments through commands");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may execute event assignment commands");
    if (!((doc.status === "checked_in"))) throw new Error("Guard 0 failed");
    if (!((doc.checkedInAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((user.id != null))) throw new Error("Guard 3 failed");
    if (!(((doc.personId === user.id) || checkRole(user, "workforceManageAccess")))) throw new Error("Guard 4 failed");
    const previousStatus = doc.status;
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "checked_out";
        const __allowed: Record<string, string[]> = { "assigned": ["confirmed", "checked_in", "no_show", "unassigned"], "confirmed": ["checked_in", "no_show", "unassigned"], "checked_in": ["checked_out"], "checked_out": [], "no_show": [], "unassigned": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "checked_out",
      checkedOutAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "EventAssignment", ["notes"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, eventAssignmentId: docId, tenantId: __after.tenantId, eventId: __after.eventId, personId: __after.personId, role: __after.role, previousStatus: previousStatus, status: "checked_out", _subject: { entity: "EventAssignment", command: "checkOut", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EventAssignmentCheckedOut", entity: "EventAssignment", entityId: docId, payload: { eventAssignmentId: docId, tenantId: __after.tenantId, eventId: __after.eventId, personId: __after.personId, role: __after.role, previousStatus: previousStatus, status: "checked_out" }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const EventAssignment_checkOut = mutation({
  args: {
    docId: v.id("eventAssignments"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runEventAssignmentCheckOut(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "EventAssignment_checkOut", __result);
    }
    return __result;
  },
});

async function __runEventAssignmentConfirm(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("EventAssignment not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("EventAssignment not found");
    const doc = await __decryptDoc(ctx, "EventAssignment", ["notes"], __storedDoc) as Record<string, any>;
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may read event assignments");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may write event assignments through commands");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may execute event assignment commands");
    if (!((doc.status === "assigned"))) throw new Error("Guard 0 failed");
    if (!((doc.assignedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((user.id != null))) throw new Error("Guard 3 failed");
    if (!(((doc.personId === user.id) || checkRole(user, "workforceManageAccess")))) throw new Error("Guard 4 failed");
    const previousStatus = doc.status;
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "confirmed";
        const __allowed: Record<string, string[]> = { "assigned": ["confirmed", "checked_in", "no_show", "unassigned"], "confirmed": ["checked_in", "no_show", "unassigned"], "checked_in": ["checked_out"], "checked_out": [], "no_show": [], "unassigned": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "confirmed",
      confirmedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "EventAssignment", ["notes"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, eventAssignmentId: docId, tenantId: __after.tenantId, eventId: __after.eventId, personId: __after.personId, role: __after.role, previousStatus: previousStatus, status: "confirmed", _subject: { entity: "EventAssignment", command: "confirm", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EventAssignmentConfirmed", entity: "EventAssignment", entityId: docId, payload: { eventAssignmentId: docId, tenantId: __after.tenantId, eventId: __after.eventId, personId: __after.personId, role: __after.role, previousStatus: previousStatus, status: "confirmed" }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const EventAssignment_confirm = mutation({
  args: {
    docId: v.id("eventAssignments"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runEventAssignmentConfirm(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "EventAssignment_confirm", __result);
    }
    return __result;
  },
});

async function __runEventAssignmentMarkNoShow(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("EventAssignment not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("EventAssignment not found");
    const doc = await __decryptDoc(ctx, "EventAssignment", ["notes"], __storedDoc) as Record<string, any>;
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may read event assignments");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may write event assignments through commands");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may execute event assignment commands");
    if (!(((doc.status === "assigned") || (doc.status === "confirmed")))) throw new Error("Guard 0 failed");
    if (!((doc.assignedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Guard 3 failed");
    const previousStatus = doc.status;
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "no_show";
        const __allowed: Record<string, string[]> = { "assigned": ["confirmed", "checked_in", "no_show", "unassigned"], "confirmed": ["checked_in", "no_show", "unassigned"], "checked_in": ["checked_out"], "checked_out": [], "no_show": [], "unassigned": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "no_show",
      noShowAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "EventAssignment", ["notes"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, eventAssignmentId: docId, tenantId: __after.tenantId, eventId: __after.eventId, personId: __after.personId, role: __after.role, previousStatus: previousStatus, status: "no_show", _subject: { entity: "EventAssignment", command: "markNoShow", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EventAssignmentNoShowMarked", entity: "EventAssignment", entityId: docId, payload: { eventAssignmentId: docId, tenantId: __after.tenantId, eventId: __after.eventId, personId: __after.personId, role: __after.role, previousStatus: previousStatus, status: "no_show" }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const EventAssignment_markNoShow = mutation({
  args: {
    docId: v.id("eventAssignments"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runEventAssignmentMarkNoShow(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "EventAssignment_markNoShow", __result);
    }
    return __result;
  },
});

async function __runEventAssignmentUnassign(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("EventAssignment not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("EventAssignment not found");
    const doc = await __decryptDoc(ctx, "EventAssignment", ["notes"], __storedDoc) as Record<string, any>;
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may read event assignments");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may write event assignments through commands");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may execute event assignment commands");
    if (!(((doc.status === "assigned") || (doc.status === "confirmed")))) throw new Error("Guard 0 failed");
    if (!((doc.assignedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Guard 3 failed");
    const previousStatus = doc.status;
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "unassigned";
        const __allowed: Record<string, string[]> = { "assigned": ["confirmed", "checked_in", "no_show", "unassigned"], "confirmed": ["checked_in", "no_show", "unassigned"], "checked_in": ["checked_out"], "checked_out": [], "no_show": [], "unassigned": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "unassigned",
      unassignedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "EventAssignment", ["notes"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, eventAssignmentId: docId, tenantId: __after.tenantId, eventId: __after.eventId, personId: __after.personId, role: __after.role, previousStatus: previousStatus, status: "unassigned", _subject: { entity: "EventAssignment", command: "unassign", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EventAssignmentUnassigned", entity: "EventAssignment", entityId: docId, payload: { eventAssignmentId: docId, tenantId: __after.tenantId, eventId: __after.eventId, personId: __after.personId, role: __after.role, previousStatus: previousStatus, status: "unassigned" }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const EventAssignment_unassign = mutation({
  args: {
    docId: v.id("eventAssignments"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runEventAssignmentUnassign(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "EventAssignment_unassign", __result);
    }
    return __result;
  },
});

async function __runEventCloseoutCapture(ctx: MutationCtx, { docId, eventId, actualRevenue, budgetedRevenue, revenueVariance, actualIngredientCost, actualWasteCost, actualLaborCost, actualVendorCost, budgetedCost, totalActualCost, costVariance, grossProfit, expectedHeadcount, actualHeadcount, unresolvedIssues, performanceNotes, notes, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("EventCloseout not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("EventCloseout not found");
    const __rel_event = await __resolveRelation(ctx, "events", [__auth.tenantId, doc.eventId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).eventId;
      ((doc as any) as any).event = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!((checkRole(user, "financeAccess") || checkRole(user, "eventManageAccess")))) throw new Error("Finance staff and event coordinators may read event closeouts");
    if (!((checkRole(user, "financeAccess") || checkRole(user, "eventManageAccess")))) throw new Error("Finance staff and event coordinators may write event closeouts through commands");
    if (!((checkRole(user, "financeAccess") || checkRole(user, "eventManageAccess")))) throw new Error("Finance staff and event coordinators may execute event closeout commands");
    if (!((doc.status === "draft"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((__rel_event != null))) throw new Error("Guard 2 failed");
    if (!((__rel_event.stage === "closed_out"))) throw new Error("Guard 3 failed");
    if (!((eventId === doc.eventId))) throw new Error("Capture eventId must match the seeded event reference");
    if (!(((((((((actualRevenue >= 0) && (budgetedRevenue >= 0)) && (actualIngredientCost >= 0)) && (actualWasteCost >= 0)) && (actualLaborCost >= 0)) && (actualVendorCost >= 0)) && (budgetedCost >= 0)) && (totalActualCost >= 0)))) throw new Error("Closeout money amounts cannot be negative");
    if (!((totalActualCost === (((actualIngredientCost + actualWasteCost) + actualLaborCost) + actualVendorCost)))) throw new Error("Total actual cost must equal ingredient plus waste plus labor plus vendor costs");
    if (!((revenueVariance === (budgetedRevenue - actualRevenue)))) throw new Error("Revenue variance must equal budgeted revenue minus actual revenue");
    if (!((costVariance === (budgetedCost - totalActualCost)))) throw new Error("Cost variance must equal budgeted cost minus total actual cost");
    if (!((grossProfit === (actualRevenue - totalActualCost)))) throw new Error("Gross profit must equal actual revenue minus total actual cost");
    if (!(((expectedHeadcount >= 0) && (actualHeadcount >= 0)))) throw new Error("Headcount values cannot be negative");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      eventId: eventId,
      actualRevenue: actualRevenue,
      budgetedRevenue: budgetedRevenue,
      revenueVariance: revenueVariance,
      actualIngredientCost: actualIngredientCost,
      actualWasteCost: actualWasteCost,
      actualLaborCost: actualLaborCost,
      actualVendorCost: actualVendorCost,
      budgetedCost: budgetedCost,
      totalActualCost: totalActualCost,
      costVariance: costVariance,
      grossProfit: grossProfit,
      expectedHeadcount: expectedHeadcount,
      actualHeadcount: actualHeadcount,
      unresolvedIssues: unresolvedIssues,
      performanceNotes: performanceNotes,
      notes: notes,
      capturedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, closeoutId: docId, tenantId: __after.tenantId, eventId: eventId, actualRevenue: actualRevenue, totalActualCost: totalActualCost, grossProfit: grossProfit, actualHeadcount: actualHeadcount, status: "draft", _subject: { entity: "EventCloseout", command: "capture", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EventCloseoutCaptured", entity: "EventCloseout", entityId: docId, payload: { closeoutId: docId, tenantId: __after.tenantId, eventId: eventId, actualRevenue: actualRevenue, totalActualCost: totalActualCost, grossProfit: grossProfit, actualHeadcount: actualHeadcount, status: "draft" }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const EventCloseout_capture = mutation({
  args: {
    docId: v.id("eventCloseouts"),
    eventId: v.string(),
    actualRevenue: v.number(),
    budgetedRevenue: v.number(),
    revenueVariance: v.number(),
    actualIngredientCost: v.number(),
    actualWasteCost: v.number(),
    actualLaborCost: v.number(),
    actualVendorCost: v.number(),
    budgetedCost: v.number(),
    totalActualCost: v.number(),
    costVariance: v.number(),
    grossProfit: v.number(),
    expectedHeadcount: v.any(),
    actualHeadcount: v.any(),
    unresolvedIssues: v.optional(v.string()),
    performanceNotes: v.optional(v.string()),
    notes: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runEventCloseoutCapture(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "EventCloseout_capture", __result);
    }
    return __result;
  },
});

export const EventCloseout_createViaCapture = mutation({
  args: {
    eventId: v.string(),
    actualRevenue: v.number(),
    budgetedRevenue: v.number(),
    revenueVariance: v.number(),
    actualIngredientCost: v.number(),
    actualWasteCost: v.number(),
    actualLaborCost: v.number(),
    actualVendorCost: v.number(),
    budgetedCost: v.number(),
    totalActualCost: v.number(),
    costVariance: v.number(),
    grossProfit: v.number(),
    expectedHeadcount: v.any(),
    actualHeadcount: v.any(),
    unresolvedIssues: v.optional(v.string()),
    performanceNotes: v.optional(v.string()),
    notes: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { eventId, actualRevenue, budgetedRevenue, revenueVariance, actualIngredientCost, actualWasteCost, actualLaborCost, actualVendorCost, budgetedCost, totalActualCost, costVariance, grossProfit, expectedHeadcount, actualHeadcount, unresolvedIssues, performanceNotes, notes } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      status: "draft",
      createdAt: Date.now(),
      updatedAt: Date.now(),
      actualHeadcount: args.actualHeadcount,
      actualIngredientCost: args.actualIngredientCost,
      actualLaborCost: args.actualLaborCost,
      actualRevenue: args.actualRevenue,
      actualVendorCost: args.actualVendorCost,
      actualWasteCost: args.actualWasteCost,
      budgetedCost: args.budgetedCost,
      budgetedRevenue: args.budgetedRevenue,
      costVariance: args.costVariance,
      eventId: args.eventId,
      expectedHeadcount: args.expectedHeadcount,
      grossProfit: args.grossProfit,
      notes: args.notes,
      performanceNotes: args.performanceNotes,
      revenueVariance: args.revenueVariance,
      totalActualCost: args.totalActualCost,
      unresolvedIssues: args.unresolvedIssues
    };
    const __rel_event = await __resolveRelation(ctx, "events", [__auth.tenantId, __draft.eventId], ["tenantId","id"], "tenantId", __auth.tenantId);
    if (!((checkRole(user, "financeAccess") || checkRole(user, "eventManageAccess")))) throw new Error("Finance staff and event coordinators may read event closeouts");
    if (!((checkRole(user, "financeAccess") || checkRole(user, "eventManageAccess")))) throw new Error("Finance staff and event coordinators may write event closeouts through commands");
    if (!((checkRole(user, "financeAccess") || checkRole(user, "eventManageAccess")))) throw new Error("Finance staff and event coordinators may execute event closeout commands");
    if (!((__draft.status === "draft"))) throw new Error("Guard 0 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((__rel_event != null))) throw new Error("Guard 2 failed");
    if (!((__rel_event.stage === "closed_out"))) throw new Error("Guard 3 failed");
    if (!((eventId === __draft.eventId))) throw new Error("Capture eventId must match the seeded event reference");
    if (!(((((((((actualRevenue >= 0) && (budgetedRevenue >= 0)) && (actualIngredientCost >= 0)) && (actualWasteCost >= 0)) && (actualLaborCost >= 0)) && (actualVendorCost >= 0)) && (budgetedCost >= 0)) && (totalActualCost >= 0)))) throw new Error("Closeout money amounts cannot be negative");
    if (!((totalActualCost === (((actualIngredientCost + actualWasteCost) + actualLaborCost) + actualVendorCost)))) throw new Error("Total actual cost must equal ingredient plus waste plus labor plus vendor costs");
    if (!((revenueVariance === (budgetedRevenue - actualRevenue)))) throw new Error("Revenue variance must equal budgeted revenue minus actual revenue");
    if (!((costVariance === (budgetedCost - totalActualCost)))) throw new Error("Cost variance must equal budgeted cost minus total actual cost");
    if (!((grossProfit === (actualRevenue - totalActualCost)))) throw new Error("Gross profit must equal actual revenue minus total actual cost");
    if (!(((expectedHeadcount >= 0) && (actualHeadcount >= 0)))) throw new Error("Headcount values cannot be negative");
    const doc: Record<string, any> = {
      ...__draft,
      eventId: eventId,
      actualRevenue: actualRevenue,
      budgetedRevenue: budgetedRevenue,
      revenueVariance: revenueVariance,
      actualIngredientCost: actualIngredientCost,
      actualWasteCost: actualWasteCost,
      actualLaborCost: actualLaborCost,
      actualVendorCost: actualVendorCost,
      budgetedCost: budgetedCost,
      totalActualCost: totalActualCost,
      costVariance: costVariance,
      grossProfit: grossProfit,
      expectedHeadcount: expectedHeadcount,
      actualHeadcount: actualHeadcount,
      unresolvedIssues: unresolvedIssues,
      performanceNotes: performanceNotes,
      notes: notes,
      capturedAt: Date.now(),
      version: 1,
    };
    const docId = await ctx.db.insert("eventCloseouts", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, closeoutId: docId, tenantId: doc.tenantId, eventId: doc.eventId, actualRevenue: doc.actualRevenue, totalActualCost: doc.totalActualCost, grossProfit: doc.grossProfit, actualHeadcount: doc.actualHeadcount, status: "draft", _subject: { entity: "EventCloseout", command: "capture", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EventCloseoutCaptured", entity: "EventCloseout", entityId: docId, payload: { closeoutId: docId, tenantId: doc.tenantId, eventId: doc.eventId, actualRevenue: doc.actualRevenue, totalActualCost: doc.totalActualCost, grossProfit: doc.grossProfit, actualHeadcount: doc.actualHeadcount, status: "draft" }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "EventCloseout_createViaCapture", __result);
    }
    return __result;
  },
});

async function __runEventCloseoutFinalize(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("EventCloseout not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("EventCloseout not found");
    if (!((checkRole(user, "financeAccess") || checkRole(user, "eventManageAccess")))) throw new Error("Finance staff and event coordinators may read event closeouts");
    if (!((checkRole(user, "financeAccess") || checkRole(user, "eventManageAccess")))) throw new Error("Finance staff and event coordinators may write event closeouts through commands");
    if (!((checkRole(user, "financeAccess") || checkRole(user, "eventManageAccess")))) throw new Error("Finance staff and event coordinators may execute event closeout commands");
    if (!((doc.status === "draft"))) throw new Error("Guard 0 failed");
    if (!((doc.capturedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!(checkRole(user, "financeManageAccess"))) throw new Error("Guard 3 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "finalized";
        const __allowed: Record<string, string[]> = { "draft": ["finalized"], "finalized": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "finalized",
      finalizedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, closeoutId: docId, tenantId: __after.tenantId, eventId: __after.eventId, actualRevenue: __after.actualRevenue, totalActualCost: __after.totalActualCost, grossProfit: __after.grossProfit, revenueVariance: __after.revenueVariance, costVariance: __after.costVariance, status: "finalized", _subject: { entity: "EventCloseout", command: "finalize", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EventCloseoutFinalized", entity: "EventCloseout", entityId: docId, payload: { closeoutId: docId, tenantId: __after.tenantId, eventId: __after.eventId, actualRevenue: __after.actualRevenue, totalActualCost: __after.totalActualCost, grossProfit: __after.grossProfit, revenueVariance: __after.revenueVariance, costVariance: __after.costVariance, status: "finalized" }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const EventCloseout_finalize = mutation({
  args: {
    docId: v.id("eventCloseouts"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runEventCloseoutFinalize(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "EventCloseout_finalize", __result);
    }
    return __result;
  },
});

async function __runEventDishAddToEvent(ctx: MutationCtx, { docId, eventId, dishId, quantityServings, headcountOverride, course, serviceStyle, specialInstructions, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("EventDish not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("EventDish not found");
    const __rel_event = await __resolveRelation(ctx, "events", [__auth.tenantId, doc.eventId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).eventId;
      ((doc as any) as any).event = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!(checkRole(user, "staffAccess"))) throw new Error("Employed staff may read event dishes");
    if (!(checkRole(user, "manageAccess"))) throw new Error("Managers may write event dishes through commands");
    if (!(checkRole(user, "manageAccess"))) throw new Error("Managers may execute event dish commands");
    if (!((doc.addedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(((__rel_event != null) && ((((__rel_event.stage === "planning") || (__rel_event.stage === "pending_approval")) || (__rel_event.stage === "approved")) || (__rel_event.stage === "executing"))))) throw new Error("Guard 2 failed");
    if (!((eventId === doc.eventId))) throw new Error("addToEvent eventId must match the seeded event reference");
    if (!((quantityServings >= 0))) throw new Error("Event dish servings cannot be negative");
    if (!(((headcountOverride == null) || (headcountOverride >= 0)))) throw new Error("Headcount override cannot be negative");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      eventId: eventId,
      dishId: dishId,
      quantityServings: quantityServings,
      headcountOverride: ((headcountOverride != null) ? headcountOverride : 0),
      purchasingWeekStart: __rel_event.purchasingWeekStart,
      course: course,
      serviceStyle: serviceStyle,
      specialInstructions: specialInstructions,
      addedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const __rel_event__post = await __resolveRelation(ctx, "events", [__auth.tenantId, __after.eventId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, eventDishId: docId, tenantId: __after.tenantId, eventId: eventId, dishId: dishId, quantityServings: quantityServings, headcountOverride: ((headcountOverride != null) ? headcountOverride : 0), purchasingWeekStart: __rel_event__post.purchasingWeekStart, course: course, serviceStyle: serviceStyle, _subject: { entity: "EventDish", command: "addToEvent", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EventDishAdded", entity: "EventDish", entityId: docId, payload: { eventDishId: docId, tenantId: __after.tenantId, eventId: eventId, dishId: dishId, quantityServings: quantityServings, headcountOverride: ((headcountOverride != null) ? headcountOverride : 0), purchasingWeekStart: __rel_event__post.purchasingWeekStart, course: course, serviceStyle: serviceStyle }, createdAt: Date.now() });
    // Reactions
    const fanRows0 = (await ctx.db.query("dishTasks").withIndex("by_dishId", (q) => q.eq("dishId", payload.dishId)).collect()).filter((d) => (d as any).deletedAt == null);
    for (const __row of fanRows0) {
      const target = __row;
      await __runDishTaskBackfillActiveKey(ctx, { docId: (__row as any)._id } as any);
    }
    const fanRows1 = (await ctx.db.query("dishComponents").withIndex("by_dishId", (q) => q.eq("dishId", payload.dishId)).collect()).filter((d) => (d as any).deletedAt == null);
    for (const __row of fanRows1) {
      const target = __row;
      const __fanMatch1_raw = await ctx.db.query("eventDishComponentSeeds").withIndex("by_eventDishId", (q) => q.eq("eventDishId", payload.eventDishId)).collect();
      const __fanMatch1_rows = __fanMatch1_raw.filter((d) => (d as any).eventDishId === payload.eventDishId && (d as any).componentId === __row.componentId && (d as any).deletedAt == null).sort((a, b) => String((a as any)._id).localeCompare(String((b as any)._id)));
      const __fanMatch1_id = __fanMatch1_rows.length > 0 ? (__fanMatch1_rows[0] as any)._id : null;
      if (__fanMatch1_id) {
        await __runEventDishComponentSeedSeed(ctx, { docId: __fanMatch1_id, eventId: payload.eventId, eventDishId: payload.eventDishId, dishId: payload.dishId, componentId: __row.componentId, servings: payload.quantityServings, yieldQuantity: __row.yieldQuantity, batchMultiplier: __row.batchMultiplier, purchasingWeekStart: payload.purchasingWeekStart } as any);
      } else {
        const __elseArgs: Record<string, any> = { eventId: payload.eventId, eventDishId: payload.eventDishId, dishId: payload.dishId, componentId: __row.componentId, servings: payload.quantityServings, yieldQuantity: __row.yieldQuantity, batchMultiplier: __row.batchMultiplier, purchasingWeekStart: payload.purchasingWeekStart };
        const __elseDoc: Record<string, any> = {
          tenantId: __auth.tenantId,
          servings: 0,
          yieldQuantity: 1,
          batchMultiplier: 1,
          createdAt: Date.now(),
          updatedAt: Date.now(),
          version: 0,
        };
        for (const __k of ["deletedAt","eventId","eventDishId","dishId","componentId","servings","yieldQuantity","batchMultiplier","purchasingWeekStart","seededAt","createdAt","updatedAt"] as string[]) {
          if (__elseArgs[__k] !== undefined) __elseDoc[__k] = __elseArgs[__k];
        }
        const __elseId = await ctx.db.insert("eventDishComponentSeeds", __elseDoc as any);
        await __runEventDishComponentSeedSeed(ctx, { docId: __elseId, ...__elseArgs } as any);
      }
    }
    const fanRows2 = (await ctx.db.query("dishIngredients").withIndex("by_dishId", (q) => q.eq("dishId", payload.dishId)).collect()).filter((d) => (d as any).deletedAt == null);
    for (const __row of fanRows2) {
      const target = __row;
      const __fanMatch2_raw = await ctx.db.query("eventIngredientContributions").withIndex("by_eventDishId", (q) => q.eq("eventDishId", payload.eventDishId)).collect();
      const __fanMatch2_rows = __fanMatch2_raw.filter((d) => (d as any).eventDishId === payload.eventDishId && (d as any).componentId === null && (d as any).ingredientId === __row.ingredientId && (d as any).unit === __row.unit && (d as any).deletedAt == null).sort((a, b) => String((a as any)._id).localeCompare(String((b as any)._id)));
      const __fanMatch2_id = __fanMatch2_rows.length > 0 ? (__fanMatch2_rows[0] as any)._id : null;
      if (__fanMatch2_id) {
        await __runEventIngredientContributionRecord(ctx, { docId: __fanMatch2_id, eventId: payload.eventId, eventDishId: payload.eventDishId, dishId: payload.dishId, componentId: null, ingredientId: __row.ingredientId, quantity: (__row.quantity * payload.quantityServings), unit: __row.unit, servings: payload.quantityServings, purchasingWeekStart: payload.purchasingWeekStart } as any);
      } else {
        const __elseArgs: Record<string, any> = { eventId: payload.eventId, eventDishId: payload.eventDishId, dishId: payload.dishId, componentId: null, ingredientId: __row.ingredientId, quantity: (__row.quantity * payload.quantityServings), unit: __row.unit, servings: payload.quantityServings, purchasingWeekStart: payload.purchasingWeekStart };
        const __elseDoc: Record<string, any> = {
          tenantId: __auth.tenantId,
          quantity: 0,
          unit: "each",
          servings: 0,
          createdAt: Date.now(),
          updatedAt: Date.now(),
          version: 0,
        };
        for (const __k of ["deletedAt","eventId","eventDishId","dishId","componentId","ingredientId","quantity","unit","servings","purchasingWeekStart","recordedAt","createdAt","updatedAt"] as string[]) {
          if (__elseArgs[__k] !== undefined) __elseDoc[__k] = __elseArgs[__k];
        }
        const __elseId = await ctx.db.insert("eventIngredientContributions", __elseDoc as any);
        await __runEventIngredientContributionRecord(ctx, { docId: __elseId, ...__elseArgs } as any);
      }
    }
    const fanRows3 = (await ctx.db.query("dishTasks").withIndex("by_activeDishId", (q) => q.eq("activeDishId", payload.dishId)).collect()).filter((d) => (d as any).deletedAt == null);
    for (const __row of fanRows3) {
      const target = __row;
      const __fanMatch3_raw = await ctx.db.query("prepTasks").withIndex("by_eventDishId", (q) => q.eq("eventDishId", payload.eventDishId)).collect();
      const __fanMatch3_rows = __fanMatch3_raw.filter((d) => (d as any).eventDishId === payload.eventDishId && (d as any).dishTaskId === (__row as any)._id && (d as any).deletedAt == null).sort((a, b) => String((a as any)._id).localeCompare(String((b as any)._id)));
      const __fanMatch3_id = __fanMatch3_rows.length > 0 ? (__fanMatch3_rows[0] as any)._id : null;
      if (__fanMatch3_id) {
        await __runPrepTaskOpen(ctx, { docId: __fanMatch3_id, eventDishId: payload.eventDishId, eventId: payload.eventId, name: __row.name, quantity: (((__row.defaultQuantity != null) && (__row.defaultQuantity > 0)) ? (__row.defaultQuantity * payload.quantityServings) : ((payload.quantityServings > 0) ? payload.quantityServings : 1)), unit: ((__row.defaultUnit != null) ? __row.defaultUnit : "portion"), dishTaskId: (__row as any)._id, dishId: payload.dishId, category: __row.category, taskType: __row.taskType, componentId: __row.componentId, ingredientId: __row.ingredientId, specialInstructions: __row.instructions } as any);
      } else {
        const __elseArgs: Record<string, any> = { eventDishId: payload.eventDishId, eventId: payload.eventId, name: __row.name, quantity: (((__row.defaultQuantity != null) && (__row.defaultQuantity > 0)) ? (__row.defaultQuantity * payload.quantityServings) : ((payload.quantityServings > 0) ? payload.quantityServings : 1)), unit: ((__row.defaultUnit != null) ? __row.defaultUnit : "portion"), dishTaskId: (__row as any)._id, dishId: payload.dishId, category: __row.category, taskType: __row.taskType, componentId: __row.componentId, ingredientId: __row.ingredientId, specialInstructions: __row.instructions };
        const __elseDoc: Record<string, any> = {
          tenantId: __auth.tenantId,
          name: "",
          category: "finish_at_event",
          taskType: "manual",
          isGenerated: true,
          quantity: 0,
          unit: "each",
          status: "pending",
          createdAt: Date.now(),
          updatedAt: Date.now(),
          version: 0,
        };
        for (const __k of ["deletedAt","eventDishId","eventId","name","dishTaskId","ingredientId","ingredientDemandId","dishId","componentId","category","taskType","specialInstructions","isGenerated","quantity","completedQuantity","unit","station","dueAt","assignedToId","notes","status","claimedAt","startedAt","completedAt","cancelledAt","cancellationReason","blockedAt","blockReason","createdAt","updatedAt"] as string[]) {
          if (__elseArgs[__k] !== undefined) __elseDoc[__k] = __elseArgs[__k];
        }
        const __elseId = await ctx.db.insert("prepTasks", __elseDoc as any);
        await __runPrepTaskOpen(ctx, { docId: __elseId, ...__elseArgs } as any);
      }
    }
    return { ...doc, ...updates };
}

export const EventDish_addToEvent = mutation({
  args: {
    docId: v.id("eventDishes"),
    eventId: v.string(),
    dishId: v.string(),
    quantityServings: v.any(),
    headcountOverride: v.optional(v.number()),
    course: v.optional(v.string()),
    serviceStyle: v.optional(v.string()),
    specialInstructions: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runEventDishAddToEvent(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "EventDish_addToEvent", __result);
    }
    return __result;
  },
});

export const EventDish_createViaAddToEvent = mutation({
  args: {
    eventId: v.string(),
    dishId: v.string(),
    quantityServings: v.any(),
    headcountOverride: v.optional(v.number()),
    course: v.optional(v.string()),
    serviceStyle: v.optional(v.string()),
    specialInstructions: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { eventId, dishId, quantityServings, headcountOverride, course, serviceStyle, specialInstructions } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      headcountOverride: args.headcountOverride !== undefined ? args.headcountOverride : 0,
      createdAt: Date.now(),
      updatedAt: Date.now(),
      course: args.course,
      dishId: args.dishId,
      eventId: args.eventId,
      quantityServings: args.quantityServings,
      serviceStyle: args.serviceStyle,
      specialInstructions: args.specialInstructions
    };
    const __rel_event = await __resolveRelation(ctx, "events", [__auth.tenantId, __draft.eventId], ["tenantId","id"], "tenantId", __auth.tenantId);
    if (!(checkRole(user, "staffAccess"))) throw new Error("Employed staff may read event dishes");
    if (!(checkRole(user, "manageAccess"))) throw new Error("Managers may write event dishes through commands");
    if (!(checkRole(user, "manageAccess"))) throw new Error("Managers may execute event dish commands");
    if (!((__draft.addedAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(((__rel_event != null) && ((((__rel_event.stage === "planning") || (__rel_event.stage === "pending_approval")) || (__rel_event.stage === "approved")) || (__rel_event.stage === "executing"))))) throw new Error("Guard 2 failed");
    if (!((eventId === __draft.eventId))) throw new Error("addToEvent eventId must match the seeded event reference");
    if (!((quantityServings >= 0))) throw new Error("Event dish servings cannot be negative");
    if (!(((headcountOverride == null) || (headcountOverride >= 0)))) throw new Error("Headcount override cannot be negative");
    const doc: Record<string, any> = {
      ...__draft,
      eventId: eventId,
      dishId: dishId,
      quantityServings: quantityServings,
      headcountOverride: ((headcountOverride != null) ? headcountOverride : 0),
      purchasingWeekStart: __rel_event.purchasingWeekStart,
      course: course,
      serviceStyle: serviceStyle,
      specialInstructions: specialInstructions,
      addedAt: Date.now(),
      version: 1,
    };
    const docId = await ctx.db.insert("eventDishes", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, eventDishId: docId, tenantId: doc.tenantId, eventId: doc.eventId, dishId: doc.dishId, quantityServings: doc.quantityServings, headcountOverride: ((doc.headcountOverride != null) ? doc.headcountOverride : 0), purchasingWeekStart: __rel_event.purchasingWeekStart, course: doc.course, serviceStyle: doc.serviceStyle, _subject: { entity: "EventDish", command: "addToEvent", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EventDishAdded", entity: "EventDish", entityId: docId, payload: { eventDishId: docId, tenantId: doc.tenantId, eventId: doc.eventId, dishId: doc.dishId, quantityServings: doc.quantityServings, headcountOverride: ((doc.headcountOverride != null) ? doc.headcountOverride : 0), purchasingWeekStart: __rel_event.purchasingWeekStart, course: doc.course, serviceStyle: doc.serviceStyle }, createdAt: Date.now() });
    // Reactions
    const fanRows0 = (await ctx.db.query("dishTasks").withIndex("by_dishId", (q) => q.eq("dishId", payload.dishId)).collect()).filter((d) => (d as any).deletedAt == null);
    for (const __row of fanRows0) {
      const target = __row;
      await __runDishTaskBackfillActiveKey(ctx, { docId: (__row as any)._id } as any);
    }
    const fanRows1 = (await ctx.db.query("dishComponents").withIndex("by_dishId", (q) => q.eq("dishId", payload.dishId)).collect()).filter((d) => (d as any).deletedAt == null);
    for (const __row of fanRows1) {
      const target = __row;
      const __fanMatch1_raw = await ctx.db.query("eventDishComponentSeeds").withIndex("by_eventDishId", (q) => q.eq("eventDishId", payload.eventDishId)).collect();
      const __fanMatch1_rows = __fanMatch1_raw.filter((d) => (d as any).eventDishId === payload.eventDishId && (d as any).componentId === __row.componentId && (d as any).deletedAt == null).sort((a, b) => String((a as any)._id).localeCompare(String((b as any)._id)));
      const __fanMatch1_id = __fanMatch1_rows.length > 0 ? (__fanMatch1_rows[0] as any)._id : null;
      if (__fanMatch1_id) {
        await __runEventDishComponentSeedSeed(ctx, { docId: __fanMatch1_id, eventId: payload.eventId, eventDishId: payload.eventDishId, dishId: payload.dishId, componentId: __row.componentId, servings: payload.quantityServings, yieldQuantity: __row.yieldQuantity, batchMultiplier: __row.batchMultiplier, purchasingWeekStart: payload.purchasingWeekStart } as any);
      } else {
        const __elseArgs: Record<string, any> = { eventId: payload.eventId, eventDishId: payload.eventDishId, dishId: payload.dishId, componentId: __row.componentId, servings: payload.quantityServings, yieldQuantity: __row.yieldQuantity, batchMultiplier: __row.batchMultiplier, purchasingWeekStart: payload.purchasingWeekStart };
        const __elseDoc: Record<string, any> = {
          tenantId: __auth.tenantId,
          servings: 0,
          yieldQuantity: 1,
          batchMultiplier: 1,
          createdAt: Date.now(),
          updatedAt: Date.now(),
          version: 0,
        };
        for (const __k of ["deletedAt","eventId","eventDishId","dishId","componentId","servings","yieldQuantity","batchMultiplier","purchasingWeekStart","seededAt","createdAt","updatedAt"] as string[]) {
          if (__elseArgs[__k] !== undefined) __elseDoc[__k] = __elseArgs[__k];
        }
        const __elseId = await ctx.db.insert("eventDishComponentSeeds", __elseDoc as any);
        await __runEventDishComponentSeedSeed(ctx, { docId: __elseId, ...__elseArgs } as any);
      }
    }
    const fanRows2 = (await ctx.db.query("dishIngredients").withIndex("by_dishId", (q) => q.eq("dishId", payload.dishId)).collect()).filter((d) => (d as any).deletedAt == null);
    for (const __row of fanRows2) {
      const target = __row;
      const __fanMatch2_raw = await ctx.db.query("eventIngredientContributions").withIndex("by_eventDishId", (q) => q.eq("eventDishId", payload.eventDishId)).collect();
      const __fanMatch2_rows = __fanMatch2_raw.filter((d) => (d as any).eventDishId === payload.eventDishId && (d as any).componentId === null && (d as any).ingredientId === __row.ingredientId && (d as any).unit === __row.unit && (d as any).deletedAt == null).sort((a, b) => String((a as any)._id).localeCompare(String((b as any)._id)));
      const __fanMatch2_id = __fanMatch2_rows.length > 0 ? (__fanMatch2_rows[0] as any)._id : null;
      if (__fanMatch2_id) {
        await __runEventIngredientContributionRecord(ctx, { docId: __fanMatch2_id, eventId: payload.eventId, eventDishId: payload.eventDishId, dishId: payload.dishId, componentId: null, ingredientId: __row.ingredientId, quantity: (__row.quantity * payload.quantityServings), unit: __row.unit, servings: payload.quantityServings, purchasingWeekStart: payload.purchasingWeekStart } as any);
      } else {
        const __elseArgs: Record<string, any> = { eventId: payload.eventId, eventDishId: payload.eventDishId, dishId: payload.dishId, componentId: null, ingredientId: __row.ingredientId, quantity: (__row.quantity * payload.quantityServings), unit: __row.unit, servings: payload.quantityServings, purchasingWeekStart: payload.purchasingWeekStart };
        const __elseDoc: Record<string, any> = {
          tenantId: __auth.tenantId,
          quantity: 0,
          unit: "each",
          servings: 0,
          createdAt: Date.now(),
          updatedAt: Date.now(),
          version: 0,
        };
        for (const __k of ["deletedAt","eventId","eventDishId","dishId","componentId","ingredientId","quantity","unit","servings","purchasingWeekStart","recordedAt","createdAt","updatedAt"] as string[]) {
          if (__elseArgs[__k] !== undefined) __elseDoc[__k] = __elseArgs[__k];
        }
        const __elseId = await ctx.db.insert("eventIngredientContributions", __elseDoc as any);
        await __runEventIngredientContributionRecord(ctx, { docId: __elseId, ...__elseArgs } as any);
      }
    }
    const fanRows3 = (await ctx.db.query("dishTasks").withIndex("by_activeDishId", (q) => q.eq("activeDishId", payload.dishId)).collect()).filter((d) => (d as any).deletedAt == null);
    for (const __row of fanRows3) {
      const target = __row;
      const __fanMatch3_raw = await ctx.db.query("prepTasks").withIndex("by_eventDishId", (q) => q.eq("eventDishId", payload.eventDishId)).collect();
      const __fanMatch3_rows = __fanMatch3_raw.filter((d) => (d as any).eventDishId === payload.eventDishId && (d as any).dishTaskId === (__row as any)._id && (d as any).deletedAt == null).sort((a, b) => String((a as any)._id).localeCompare(String((b as any)._id)));
      const __fanMatch3_id = __fanMatch3_rows.length > 0 ? (__fanMatch3_rows[0] as any)._id : null;
      if (__fanMatch3_id) {
        await __runPrepTaskOpen(ctx, { docId: __fanMatch3_id, eventDishId: payload.eventDishId, eventId: payload.eventId, name: __row.name, quantity: (((__row.defaultQuantity != null) && (__row.defaultQuantity > 0)) ? (__row.defaultQuantity * payload.quantityServings) : ((payload.quantityServings > 0) ? payload.quantityServings : 1)), unit: ((__row.defaultUnit != null) ? __row.defaultUnit : "portion"), dishTaskId: (__row as any)._id, dishId: payload.dishId, category: __row.category, taskType: __row.taskType, componentId: __row.componentId, ingredientId: __row.ingredientId, specialInstructions: __row.instructions } as any);
      } else {
        const __elseArgs: Record<string, any> = { eventDishId: payload.eventDishId, eventId: payload.eventId, name: __row.name, quantity: (((__row.defaultQuantity != null) && (__row.defaultQuantity > 0)) ? (__row.defaultQuantity * payload.quantityServings) : ((payload.quantityServings > 0) ? payload.quantityServings : 1)), unit: ((__row.defaultUnit != null) ? __row.defaultUnit : "portion"), dishTaskId: (__row as any)._id, dishId: payload.dishId, category: __row.category, taskType: __row.taskType, componentId: __row.componentId, ingredientId: __row.ingredientId, specialInstructions: __row.instructions };
        const __elseDoc: Record<string, any> = {
          tenantId: __auth.tenantId,
          name: "",
          category: "finish_at_event",
          taskType: "manual",
          isGenerated: true,
          quantity: 0,
          unit: "each",
          status: "pending",
          createdAt: Date.now(),
          updatedAt: Date.now(),
          version: 0,
        };
        for (const __k of ["deletedAt","eventDishId","eventId","name","dishTaskId","ingredientId","ingredientDemandId","dishId","componentId","category","taskType","specialInstructions","isGenerated","quantity","completedQuantity","unit","station","dueAt","assignedToId","notes","status","claimedAt","startedAt","completedAt","cancelledAt","cancellationReason","blockedAt","blockReason","createdAt","updatedAt"] as string[]) {
          if (__elseArgs[__k] !== undefined) __elseDoc[__k] = __elseArgs[__k];
        }
        const __elseId = await ctx.db.insert("prepTasks", __elseDoc as any);
        await __runPrepTaskOpen(ctx, { docId: __elseId, ...__elseArgs } as any);
      }
    }
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "EventDish_createViaAddToEvent", __result);
    }
    return __result;
  },
});

async function __runEventDishAdjustServings(ctx: MutationCtx, { docId, quantityServings, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("EventDish not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("EventDish not found");
    const __rel_event = await __resolveRelation(ctx, "events", [__auth.tenantId, doc.eventId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).eventId;
      ((doc as any) as any).event = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!(checkRole(user, "staffAccess"))) throw new Error("Employed staff may read event dishes");
    if (!(checkRole(user, "manageAccess"))) throw new Error("Managers may write event dishes through commands");
    if (!(checkRole(user, "manageAccess"))) throw new Error("Managers may execute event dish commands");
    if (!((doc.addedAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(((__rel_event != null) && ((((((__rel_event.stage === "planning") || (__rel_event.stage === "pending_approval")) || (__rel_event.stage === "approved")) || (__rel_event.stage === "executing")) || (__rel_event.stage === "completed")) || (__rel_event.stage === "closed_out"))))) throw new Error("Guard 2 failed");
    if (!((quantityServings >= 0))) throw new Error("Event dish servings cannot be negative");
    const previousQuantityServings = doc.quantityServings;
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      quantityServings: quantityServings,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, eventDishId: docId, tenantId: __after.tenantId, eventId: __after.eventId, dishId: __after.dishId, previousQuantityServings: previousQuantityServings, quantityServings: quantityServings, _subject: { entity: "EventDish", command: "adjustServings", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EventDishServingsAdjusted", entity: "EventDish", entityId: docId, payload: { eventDishId: docId, tenantId: __after.tenantId, eventId: __after.eventId, dishId: __after.dishId, previousQuantityServings: previousQuantityServings, quantityServings: quantityServings }, createdAt: Date.now() });
    // Reactions
    const fanRows0 = (await ctx.db.query("eventIngredientContributions").withIndex("by_eventDishId", (q) => q.eq("eventDishId", payload.eventDishId)).collect()).filter((d) => (d as any).deletedAt == null);
    for (const __row of fanRows0) {
      const target = __row;
      await __runEventIngredientContributionRevise(ctx, { docId: (__row as any)._id, quantity: ((__row.quantity / __row.servings) * payload.quantityServings), servings: payload.quantityServings } as any);
    }
    return { ...doc, ...updates };
}

export const EventDish_adjustServings = mutation({
  args: {
    docId: v.id("eventDishes"),
    quantityServings: v.any(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runEventDishAdjustServings(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "EventDish_adjustServings", __result);
    }
    return __result;
  },
});

async function __runEventDishChangeCourse(ctx: MutationCtx, { docId, course, serviceStyle, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("EventDish not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("EventDish not found");
    const __rel_event = await __resolveRelation(ctx, "events", [__auth.tenantId, doc.eventId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).eventId;
      ((doc as any) as any).event = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!(checkRole(user, "staffAccess"))) throw new Error("Employed staff may read event dishes");
    if (!(checkRole(user, "manageAccess"))) throw new Error("Managers may write event dishes through commands");
    if (!(checkRole(user, "manageAccess"))) throw new Error("Managers may execute event dish commands");
    if (!((doc.addedAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(((__rel_event != null) && ((((__rel_event.stage === "planning") || (__rel_event.stage === "pending_approval")) || (__rel_event.stage === "approved")) || (__rel_event.stage === "executing"))))) throw new Error("Guard 2 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      course: course,
      serviceStyle: serviceStyle,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, eventDishId: docId, tenantId: __after.tenantId, eventId: __after.eventId, dishId: __after.dishId, course: course, serviceStyle: serviceStyle, _subject: { entity: "EventDish", command: "changeCourse", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EventDishCourseChanged", entity: "EventDish", entityId: docId, payload: { eventDishId: docId, tenantId: __after.tenantId, eventId: __after.eventId, dishId: __after.dishId, course: course, serviceStyle: serviceStyle }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const EventDish_changeCourse = mutation({
  args: {
    docId: v.id("eventDishes"),
    course: v.optional(v.string()),
    serviceStyle: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runEventDishChangeCourse(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "EventDish_changeCourse", __result);
    }
    return __result;
  },
});

async function __runEventDishConfirmFromProposal(ctx: MutationCtx, { docId, eventId, dishId, quantityServings, course, serviceStyle, specialInstructions, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("EventDish not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("EventDish not found");
    const __rel_event = await __resolveRelation(ctx, "events", [__auth.tenantId, doc.eventId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).eventId;
      ((doc as any) as any).event = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!(checkRole(user, "staffAccess"))) throw new Error("Employed staff may read event dishes");
    if (!(checkRole(user, "manageAccess"))) throw new Error("Managers may write event dishes through commands");
    if (!(checkRole(user, "manageAccess"))) throw new Error("Managers may execute event dish commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!(((__rel_event != null) && ((((__rel_event.stage === "planning") || (__rel_event.stage === "pending_approval")) || (__rel_event.stage === "approved")) || (__rel_event.stage === "executing"))))) throw new Error("Guard 1 failed");
    if (!((eventId === doc.eventId))) throw new Error("confirmFromProposal eventId must match the seeded event reference");
    if (!((quantityServings >= 0))) throw new Error("Event dish servings cannot be negative");
    const wasExistingLine = (doc.addedAt != null);
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      eventId: eventId,
      dishId: dishId,
      quantityServings: ((doc.addedAt != null) ? doc.quantityServings : quantityServings),
      purchasingWeekStart: __rel_event.purchasingWeekStart,
      course: ((doc.addedAt != null) ? doc.course : course),
      serviceStyle: ((doc.addedAt != null) ? doc.serviceStyle : serviceStyle),
      specialInstructions: ((doc.addedAt != null) ? doc.specialInstructions : specialInstructions),
      addedAt: ((doc.addedAt != null) ? doc.addedAt : Date.now()),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const __rel_event__post = await __resolveRelation(ctx, "events", [__auth.tenantId, __after.eventId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, eventDishId: docId, tenantId: __after.tenantId, eventId: eventId, dishId: dishId, seedDishId: (wasExistingLine ? null : dishId), quantityServings: __after.quantityServings, purchasingWeekStart: __rel_event__post.purchasingWeekStart, course: __after.course, serviceStyle: __after.serviceStyle, _subject: { entity: "EventDish", command: "confirmFromProposal", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EventDishConfirmedFromProposal", entity: "EventDish", entityId: docId, payload: { eventDishId: docId, tenantId: __after.tenantId, eventId: eventId, dishId: dishId, seedDishId: (wasExistingLine ? null : dishId), quantityServings: __after.quantityServings, purchasingWeekStart: __rel_event__post.purchasingWeekStart, course: __after.course, serviceStyle: __after.serviceStyle }, createdAt: Date.now() });
    // Reactions
    const fanRows0 = (await ctx.db.query("dishComponents").withIndex("by_dishId", (q) => q.eq("dishId", payload.seedDishId)).collect()).filter((d) => (d as any).deletedAt == null);
    for (const __row of fanRows0) {
      const target = __row;
      const __fanMatch0_raw = await ctx.db.query("eventDishComponentSeeds").withIndex("by_eventDishId", (q) => q.eq("eventDishId", payload.eventDishId)).collect();
      const __fanMatch0_rows = __fanMatch0_raw.filter((d) => (d as any).eventDishId === payload.eventDishId && (d as any).componentId === __row.componentId && (d as any).deletedAt == null).sort((a, b) => String((a as any)._id).localeCompare(String((b as any)._id)));
      const __fanMatch0_id = __fanMatch0_rows.length > 0 ? (__fanMatch0_rows[0] as any)._id : null;
      if (__fanMatch0_id) {
        await __runEventDishComponentSeedSeed(ctx, { docId: __fanMatch0_id, eventId: payload.eventId, eventDishId: payload.eventDishId, dishId: payload.dishId, componentId: __row.componentId, servings: payload.quantityServings, yieldQuantity: __row.yieldQuantity, batchMultiplier: __row.batchMultiplier, purchasingWeekStart: payload.purchasingWeekStart } as any);
      } else {
        const __elseArgs: Record<string, any> = { eventId: payload.eventId, eventDishId: payload.eventDishId, dishId: payload.dishId, componentId: __row.componentId, servings: payload.quantityServings, yieldQuantity: __row.yieldQuantity, batchMultiplier: __row.batchMultiplier, purchasingWeekStart: payload.purchasingWeekStart };
        const __elseDoc: Record<string, any> = {
          tenantId: __auth.tenantId,
          servings: 0,
          yieldQuantity: 1,
          batchMultiplier: 1,
          createdAt: Date.now(),
          updatedAt: Date.now(),
          version: 0,
        };
        for (const __k of ["deletedAt","eventId","eventDishId","dishId","componentId","servings","yieldQuantity","batchMultiplier","purchasingWeekStart","seededAt","createdAt","updatedAt"] as string[]) {
          if (__elseArgs[__k] !== undefined) __elseDoc[__k] = __elseArgs[__k];
        }
        const __elseId = await ctx.db.insert("eventDishComponentSeeds", __elseDoc as any);
        await __runEventDishComponentSeedSeed(ctx, { docId: __elseId, ...__elseArgs } as any);
      }
    }
    const fanRows1 = (await ctx.db.query("dishIngredients").withIndex("by_dishId", (q) => q.eq("dishId", payload.seedDishId)).collect()).filter((d) => (d as any).deletedAt == null);
    for (const __row of fanRows1) {
      const target = __row;
      const __fanMatch1_raw = await ctx.db.query("eventIngredientContributions").withIndex("by_eventDishId", (q) => q.eq("eventDishId", payload.eventDishId)).collect();
      const __fanMatch1_rows = __fanMatch1_raw.filter((d) => (d as any).eventDishId === payload.eventDishId && (d as any).componentId === null && (d as any).ingredientId === __row.ingredientId && (d as any).unit === __row.unit && (d as any).deletedAt == null).sort((a, b) => String((a as any)._id).localeCompare(String((b as any)._id)));
      const __fanMatch1_id = __fanMatch1_rows.length > 0 ? (__fanMatch1_rows[0] as any)._id : null;
      if (__fanMatch1_id) {
        await __runEventIngredientContributionRecord(ctx, { docId: __fanMatch1_id, eventId: payload.eventId, eventDishId: payload.eventDishId, dishId: payload.seedDishId, componentId: null, ingredientId: __row.ingredientId, quantity: (__row.quantity * payload.quantityServings), unit: __row.unit, servings: payload.quantityServings, purchasingWeekStart: payload.purchasingWeekStart } as any);
      } else {
        const __elseArgs: Record<string, any> = { eventId: payload.eventId, eventDishId: payload.eventDishId, dishId: payload.seedDishId, componentId: null, ingredientId: __row.ingredientId, quantity: (__row.quantity * payload.quantityServings), unit: __row.unit, servings: payload.quantityServings, purchasingWeekStart: payload.purchasingWeekStart };
        const __elseDoc: Record<string, any> = {
          tenantId: __auth.tenantId,
          quantity: 0,
          unit: "each",
          servings: 0,
          createdAt: Date.now(),
          updatedAt: Date.now(),
          version: 0,
        };
        for (const __k of ["deletedAt","eventId","eventDishId","dishId","componentId","ingredientId","quantity","unit","servings","purchasingWeekStart","recordedAt","createdAt","updatedAt"] as string[]) {
          if (__elseArgs[__k] !== undefined) __elseDoc[__k] = __elseArgs[__k];
        }
        const __elseId = await ctx.db.insert("eventIngredientContributions", __elseDoc as any);
        await __runEventIngredientContributionRecord(ctx, { docId: __elseId, ...__elseArgs } as any);
      }
    }
    return { ...doc, ...updates };
}

export const EventDish_confirmFromProposal = mutation({
  args: {
    docId: v.id("eventDishes"),
    eventId: v.string(),
    dishId: v.string(),
    quantityServings: v.any(),
    course: v.optional(v.string()),
    serviceStyle: v.optional(v.string()),
    specialInstructions: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runEventDishConfirmFromProposal(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "EventDish_confirmFromProposal", __result);
    }
    return __result;
  },
});

async function __runEventDishRemove(ctx: MutationCtx, { docId, reason, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("EventDish not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("EventDish not found");
    const __rel_event = await __resolveRelation(ctx, "events", [__auth.tenantId, doc.eventId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).eventId;
      ((doc as any) as any).event = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!(checkRole(user, "staffAccess"))) throw new Error("Employed staff may read event dishes");
    if (!(checkRole(user, "manageAccess"))) throw new Error("Managers may write event dishes through commands");
    if (!(checkRole(user, "manageAccess"))) throw new Error("Managers may execute event dish commands");
    if (!((doc.addedAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(((__rel_event != null) && ((((__rel_event.stage === "planning") || (__rel_event.stage === "pending_approval")) || (__rel_event.stage === "approved")) || (__rel_event.stage === "executing"))))) throw new Error("Guard 2 failed");
    if (!((((reason).trim()).length > 0))) throw new Error("Removal reason is required");
    const previousQuantityServings = doc.quantityServings;
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      deletedAt: Date.now(),
      removedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, eventDishId: docId, tenantId: __after.tenantId, eventId: __after.eventId, dishId: __after.dishId, previousQuantityServings: previousQuantityServings, reason: reason, _subject: { entity: "EventDish", command: "remove", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EventDishRemoved", entity: "EventDish", entityId: docId, payload: { eventDishId: docId, tenantId: __after.tenantId, eventId: __after.eventId, dishId: __after.dishId, previousQuantityServings: previousQuantityServings, reason: reason }, createdAt: Date.now() });
    // Reactions
    const fanRows0 = (await ctx.db.query("eventIngredientContributions").withIndex("by_eventDishId", (q) => q.eq("eventDishId", payload.eventDishId)).collect()).filter((d) => (d as any).deletedAt == null);
    for (const __row of fanRows0) {
      const target = __row;
      await __runEventIngredientContributionRetire(ctx, { docId: (__row as any)._id, reason: payload.reason } as any);
    }
    const fanRows1 = (await ctx.db.query("prepTasks").withIndex("by_eventDishId", (q) => q.eq("eventDishId", payload.eventDishId)).collect()).filter((d) => (d as any).deletedAt == null);
    for (const __row of fanRows1) {
      const target = __row;
      await __runPrepTaskStandDown(ctx, { docId: (__row as any)._id, reason: payload.reason } as any);
    }
    return { ...doc, ...updates };
}

export const EventDish_remove = mutation({
  args: {
    docId: v.id("eventDishes"),
    reason: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runEventDishRemove(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "EventDish_remove", __result);
    }
    return __result;
  },
});

async function __runEventDishRequestContainerPack(ctx: MutationCtx, { docId, packListId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("EventDish not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("EventDish not found");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Employed staff may read event dishes");
    if (!(checkRole(user, "manageAccess"))) throw new Error("Managers may write event dishes through commands");
    if (!(checkRole(user, "manageAccess"))) throw new Error("Managers may execute event dish commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, eventDishId: docId, tenantId: __after.tenantId, eventId: __after.eventId, dishId: __after.dishId, packListId: packListId, quantityServings: __after.quantityServings, _subject: { entity: "EventDish", command: "requestContainerPack", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EventDishContainerPackRequested", entity: "EventDish", entityId: docId, payload: { eventDishId: docId, tenantId: __after.tenantId, eventId: __after.eventId, dishId: __after.dishId, packListId: packListId, quantityServings: __after.quantityServings }, createdAt: Date.now() });
    // Reactions
    const fanRows0 = (await ctx.db.query("dishContainers").withIndex("by_dishId", (q) => q.eq("dishId", payload.dishId)).collect()).filter((d) => (d as any).deletedAt == null);
    for (const __row of fanRows0) {
      const target = __row;
      const __fanMatch0_raw = await ctx.db.query("packListItems").withIndex("by_packListId", (q) => q.eq("packListId", payload.packListId)).collect();
      const __fanMatch0_rows = __fanMatch0_raw.filter((d) => (d as any).packListId === payload.packListId && (d as any).dishContainerId === (__row as any)._id && (d as any).deletedAt == null).sort((a, b) => String((a as any)._id).localeCompare(String((b as any)._id)));
      const __fanMatch0_id = __fanMatch0_rows.length > 0 ? (__fanMatch0_rows[0] as any)._id : null;
      if (__fanMatch0_id) {
        await __runPackListItemAddItem(ctx, { docId: __fanMatch0_id, packListId: payload.packListId, dishContainerId: (__row as any)._id, description: __row.name, requiredQuantity: (Math.ceil((payload.quantityServings / __row.servingsPerContainer)) + __row.baseQuantity), unit: __row.unit, dishId: payload.dishId } as any);
      } else {
        const __elseArgs: Record<string, any> = { packListId: payload.packListId, dishContainerId: (__row as any)._id, description: __row.name, requiredQuantity: (Math.ceil((payload.quantityServings / __row.servingsPerContainer)) + __row.baseQuantity), unit: __row.unit, dishId: payload.dishId };
        const __elseDoc: Record<string, any> = {
          tenantId: __auth.tenantId,
          description: "",
          requiredQuantity: 0,
          packedQuantity: 0,
          unit: "each",
          status: "pending",
          createdAt: Date.now(),
          updatedAt: Date.now(),
          version: 0,
        };
        for (const __k of ["deletedAt","packListId","description","dishId","dishContainerId","productionBatchId","requiredQuantity","packedQuantity","unit","status","listedAt","packedAt","missingAt","createdAt","updatedAt"] as string[]) {
          if (__elseArgs[__k] !== undefined) __elseDoc[__k] = __elseArgs[__k];
        }
        const __elseId = await ctx.db.insert("packListItems", __elseDoc as any);
        await __runPackListItemAddItem(ctx, { docId: __elseId, ...__elseArgs } as any);
      }
    }
    return { ...doc, ...updates };
}

export const EventDish_requestContainerPack = mutation({
  args: {
    docId: v.id("eventDishes"),
    packListId: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runEventDishRequestContainerPack(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "EventDish_requestContainerPack", __result);
    }
    return __result;
  },
});

async function __runEventDishSetHeadcountOverride(ctx: MutationCtx, { docId, headcountOverride, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("EventDish not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("EventDish not found");
    const __rel_event = await __resolveRelation(ctx, "events", [__auth.tenantId, doc.eventId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).eventId;
      ((doc as any) as any).event = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!(checkRole(user, "staffAccess"))) throw new Error("Employed staff may read event dishes");
    if (!(checkRole(user, "manageAccess"))) throw new Error("Managers may write event dishes through commands");
    if (!(checkRole(user, "manageAccess"))) throw new Error("Managers may execute event dish commands");
    if (!((doc.addedAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(((__rel_event != null) && ((((((__rel_event.stage === "planning") || (__rel_event.stage === "pending_approval")) || (__rel_event.stage === "approved")) || (__rel_event.stage === "executing")) || (__rel_event.stage === "completed")) || (__rel_event.stage === "closed_out"))))) throw new Error("Guard 2 failed");
    if (!((headcountOverride >= 0))) throw new Error("Headcount override cannot be negative");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      headcountOverride: headcountOverride,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, eventDishId: docId, tenantId: __after.tenantId, eventId: __after.eventId, dishId: __after.dishId, headcountOverride: headcountOverride, _subject: { entity: "EventDish", command: "setHeadcountOverride", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EventDishHeadcountOverrideSet", entity: "EventDish", entityId: docId, payload: { eventDishId: docId, tenantId: __after.tenantId, eventId: __after.eventId, dishId: __after.dishId, headcountOverride: headcountOverride }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const EventDish_setHeadcountOverride = mutation({
  args: {
    docId: v.id("eventDishes"),
    headcountOverride: v.number(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runEventDishSetHeadcountOverride(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "EventDish_setHeadcountOverride", __result);
    }
    return __result;
  },
});

async function __runEventDishUpdateInstructions(ctx: MutationCtx, { docId, specialInstructions, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("EventDish not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("EventDish not found");
    const __rel_event = await __resolveRelation(ctx, "events", [__auth.tenantId, doc.eventId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).eventId;
      ((doc as any) as any).event = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!(checkRole(user, "staffAccess"))) throw new Error("Employed staff may read event dishes");
    if (!(checkRole(user, "manageAccess"))) throw new Error("Managers may write event dishes through commands");
    if (!(checkRole(user, "manageAccess"))) throw new Error("Managers may execute event dish commands");
    if (!((doc.addedAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(((__rel_event != null) && ((((__rel_event.stage === "planning") || (__rel_event.stage === "pending_approval")) || (__rel_event.stage === "approved")) || (__rel_event.stage === "executing"))))) throw new Error("Guard 2 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      specialInstructions: specialInstructions,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, eventDishId: docId, tenantId: __after.tenantId, eventId: __after.eventId, dishId: __after.dishId, _subject: { entity: "EventDish", command: "updateInstructions", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EventDishInstructionsUpdated", entity: "EventDish", entityId: docId, payload: { eventDishId: docId, tenantId: __after.tenantId, eventId: __after.eventId, dishId: __after.dishId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const EventDish_updateInstructions = mutation({
  args: {
    docId: v.id("eventDishes"),
    specialInstructions: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runEventDishUpdateInstructions(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "EventDish_updateInstructions", __result);
    }
    return __result;
  },
});

async function __runEventDishComponentSeedSeed(ctx: MutationCtx, { docId, eventId, eventDishId, dishId, componentId, servings, yieldQuantity, batchMultiplier, purchasingWeekStart, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("EventDishComponentSeed not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("EventDishComponentSeed not found");
    if (!((doc.seededAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      eventId: eventId,
      eventDishId: eventDishId,
      dishId: dishId,
      componentId: componentId,
      servings: servings,
      yieldQuantity: yieldQuantity,
      batchMultiplier: batchMultiplier,
      purchasingWeekStart: purchasingWeekStart,
      seededAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, seedId: docId, tenantId: __after.tenantId, eventId: eventId, eventDishId: eventDishId, dishId: dishId, componentId: componentId, servings: servings, yieldQuantity: yieldQuantity, batchMultiplier: batchMultiplier, purchasingWeekStart: purchasingWeekStart, _subject: { entity: "EventDishComponentSeed", command: "seed", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EventDishComponentSeeded", entity: "EventDishComponentSeed", entityId: docId, payload: { seedId: docId, tenantId: __after.tenantId, eventId: eventId, eventDishId: eventDishId, dishId: dishId, componentId: componentId, servings: servings, yieldQuantity: yieldQuantity, batchMultiplier: batchMultiplier, purchasingWeekStart: purchasingWeekStart }, createdAt: Date.now() });
    // Reactions
    const fanRows0 = (await ctx.db.query("componentIngredients").withIndex("by_componentId", (q) => q.eq("componentId", payload.componentId)).collect()).filter((d) => (d as any).deletedAt == null);
    for (const __row of fanRows0) {
      const target = __row;
      const __fanMatch0_raw = await ctx.db.query("eventIngredientContributions").withIndex("by_eventDishId", (q) => q.eq("eventDishId", payload.eventDishId)).collect();
      const __fanMatch0_rows = __fanMatch0_raw.filter((d) => (d as any).eventDishId === payload.eventDishId && (d as any).componentId === payload.componentId && (d as any).ingredientId === __row.ingredientId && (d as any).unit === __row.unit && (d as any).deletedAt == null).sort((a, b) => String((a as any)._id).localeCompare(String((b as any)._id)));
      const __fanMatch0_id = __fanMatch0_rows.length > 0 ? (__fanMatch0_rows[0] as any)._id : null;
      if (__fanMatch0_id) {
        await __runEventIngredientContributionRecord(ctx, { docId: __fanMatch0_id, eventId: payload.eventId, eventDishId: payload.eventDishId, dishId: payload.dishId, componentId: payload.componentId, ingredientId: __row.ingredientId, quantity: (((__row.quantity * payload.batchMultiplier) * payload.servings) / payload.yieldQuantity), unit: __row.unit, servings: payload.servings, purchasingWeekStart: payload.purchasingWeekStart } as any);
      } else {
        const __elseArgs: Record<string, any> = { eventId: payload.eventId, eventDishId: payload.eventDishId, dishId: payload.dishId, componentId: payload.componentId, ingredientId: __row.ingredientId, quantity: (((__row.quantity * payload.batchMultiplier) * payload.servings) / payload.yieldQuantity), unit: __row.unit, servings: payload.servings, purchasingWeekStart: payload.purchasingWeekStart };
        const __elseDoc: Record<string, any> = {
          tenantId: __auth.tenantId,
          quantity: 0,
          unit: "each",
          servings: 0,
          createdAt: Date.now(),
          updatedAt: Date.now(),
          version: 0,
        };
        for (const __k of ["deletedAt","eventId","eventDishId","dishId","componentId","ingredientId","quantity","unit","servings","purchasingWeekStart","recordedAt","createdAt","updatedAt"] as string[]) {
          if (__elseArgs[__k] !== undefined) __elseDoc[__k] = __elseArgs[__k];
        }
        const __elseId = await ctx.db.insert("eventIngredientContributions", __elseDoc as any);
        await __runEventIngredientContributionRecord(ctx, { docId: __elseId, ...__elseArgs } as any);
      }
    }
    return { ...doc, ...updates };
}

export const EventDishComponentSeed_seed = mutation({
  args: {
    docId: v.id("eventDishComponentSeeds"),
    eventId: v.string(),
    eventDishId: v.string(),
    dishId: v.string(),
    componentId: v.string(),
    servings: v.number(),
    yieldQuantity: v.number(),
    batchMultiplier: v.number(),
    purchasingWeekStart: v.optional(v.number()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runEventDishComponentSeedSeed(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "EventDishComponentSeed_seed", __result);
    }
    return __result;
  },
});

export const EventDishComponentSeed_createViaSeed = mutation({
  args: {
    eventId: v.string(),
    eventDishId: v.string(),
    dishId: v.string(),
    componentId: v.string(),
    servings: v.number(),
    yieldQuantity: v.number(),
    batchMultiplier: v.number(),
    purchasingWeekStart: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const { eventId, eventDishId, dishId, componentId, servings, yieldQuantity, batchMultiplier, purchasingWeekStart } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      createdAt: Date.now(),
      updatedAt: Date.now(),
      batchMultiplier: args.batchMultiplier,
      componentId: args.componentId,
      dishId: args.dishId,
      eventDishId: args.eventDishId,
      eventId: args.eventId,
      purchasingWeekStart: args.purchasingWeekStart,
      servings: args.servings,
      yieldQuantity: args.yieldQuantity
    };
    if (!((__draft.seededAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 1 failed");
    const doc: Record<string, any> = {
      ...__draft,
      eventId: eventId,
      eventDishId: eventDishId,
      dishId: dishId,
      componentId: componentId,
      servings: servings,
      yieldQuantity: yieldQuantity,
      batchMultiplier: batchMultiplier,
      purchasingWeekStart: purchasingWeekStart,
      seededAt: Date.now(),
      version: 1,
    };
    const docId = await ctx.db.insert("eventDishComponentSeeds", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, seedId: docId, tenantId: doc.tenantId, eventId: doc.eventId, eventDishId: doc.eventDishId, dishId: doc.dishId, componentId: doc.componentId, servings: doc.servings, yieldQuantity: doc.yieldQuantity, batchMultiplier: doc.batchMultiplier, purchasingWeekStart: doc.purchasingWeekStart, _subject: { entity: "EventDishComponentSeed", command: "seed", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EventDishComponentSeeded", entity: "EventDishComponentSeed", entityId: docId, payload: { seedId: docId, tenantId: doc.tenantId, eventId: doc.eventId, eventDishId: doc.eventDishId, dishId: doc.dishId, componentId: doc.componentId, servings: doc.servings, yieldQuantity: doc.yieldQuantity, batchMultiplier: doc.batchMultiplier, purchasingWeekStart: doc.purchasingWeekStart }, createdAt: Date.now() });
    // Reactions
    const fanRows0 = (await ctx.db.query("componentIngredients").withIndex("by_componentId", (q) => q.eq("componentId", payload.componentId)).collect()).filter((d) => (d as any).deletedAt == null);
    for (const __row of fanRows0) {
      const target = __row;
      const __fanMatch0_raw = await ctx.db.query("eventIngredientContributions").withIndex("by_eventDishId", (q) => q.eq("eventDishId", payload.eventDishId)).collect();
      const __fanMatch0_rows = __fanMatch0_raw.filter((d) => (d as any).eventDishId === payload.eventDishId && (d as any).componentId === payload.componentId && (d as any).ingredientId === __row.ingredientId && (d as any).unit === __row.unit && (d as any).deletedAt == null).sort((a, b) => String((a as any)._id).localeCompare(String((b as any)._id)));
      const __fanMatch0_id = __fanMatch0_rows.length > 0 ? (__fanMatch0_rows[0] as any)._id : null;
      if (__fanMatch0_id) {
        await __runEventIngredientContributionRecord(ctx, { docId: __fanMatch0_id, eventId: payload.eventId, eventDishId: payload.eventDishId, dishId: payload.dishId, componentId: payload.componentId, ingredientId: __row.ingredientId, quantity: (((__row.quantity * payload.batchMultiplier) * payload.servings) / payload.yieldQuantity), unit: __row.unit, servings: payload.servings, purchasingWeekStart: payload.purchasingWeekStart } as any);
      } else {
        const __elseArgs: Record<string, any> = { eventId: payload.eventId, eventDishId: payload.eventDishId, dishId: payload.dishId, componentId: payload.componentId, ingredientId: __row.ingredientId, quantity: (((__row.quantity * payload.batchMultiplier) * payload.servings) / payload.yieldQuantity), unit: __row.unit, servings: payload.servings, purchasingWeekStart: payload.purchasingWeekStart };
        const __elseDoc: Record<string, any> = {
          tenantId: __auth.tenantId,
          quantity: 0,
          unit: "each",
          servings: 0,
          createdAt: Date.now(),
          updatedAt: Date.now(),
          version: 0,
        };
        for (const __k of ["deletedAt","eventId","eventDishId","dishId","componentId","ingredientId","quantity","unit","servings","purchasingWeekStart","recordedAt","createdAt","updatedAt"] as string[]) {
          if (__elseArgs[__k] !== undefined) __elseDoc[__k] = __elseArgs[__k];
        }
        const __elseId = await ctx.db.insert("eventIngredientContributions", __elseDoc as any);
        await __runEventIngredientContributionRecord(ctx, { docId: __elseId, ...__elseArgs } as any);
      }
    }
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "EventDishComponentSeed_createViaSeed", __result);
    }
    return __result;
  },
});

async function __runEventGuestAssignTable(ctx: MutationCtx, { docId, tableAssignment, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("EventGuest not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("EventGuest not found");
    const doc = await __decryptDoc(ctx, "EventGuest", ["email","phone"], __storedDoc) as Record<string, any>;
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may read guest attendance");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may write guest attendance through commands");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may execute guest attendance commands");
    if (!((doc.rsvpStatus !== "declined"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((((tableAssignment).trim()).length > 0))) throw new Error("Table assignment is required");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      tableAssignment: tableAssignment,
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "EventGuest", ["email","phone"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, guestId: docId, tenantId: __after.tenantId, eventId: __after.eventId, tableAssignment: tableAssignment, _subject: { entity: "EventGuest", command: "assignTable", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EventGuestTableAssigned", entity: "EventGuest", entityId: docId, payload: { guestId: docId, tenantId: __after.tenantId, eventId: __after.eventId, tableAssignment: tableAssignment }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const EventGuest_assignTable = mutation({
  args: {
    docId: v.id("eventGuests"),
    tableAssignment: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runEventGuestAssignTable(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "EventGuest_assignTable", __result);
    }
    return __result;
  },
});

async function __runEventGuestCheckIn(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("EventGuest not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("EventGuest not found");
    const doc = await __decryptDoc(ctx, "EventGuest", ["email","phone"], __storedDoc) as Record<string, any>;
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may read guest attendance");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may write guest attendance through commands");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may execute guest attendance commands");
    if (!((doc.rsvpStatus === "confirmed"))) throw new Error("Guard 0 failed");
    if (!((doc.checkedInAt == null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    const checkInTime = Date.now();
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      checkedInAt: checkInTime,
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "EventGuest", ["email","phone"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, guestId: docId, tenantId: __after.tenantId, eventId: __after.eventId, checkedInAt: checkInTime, _subject: { entity: "EventGuest", command: "checkIn", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EventGuestCheckedIn", entity: "EventGuest", entityId: docId, payload: { guestId: docId, tenantId: __after.tenantId, eventId: __after.eventId, checkedInAt: checkInTime }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const EventGuest_checkIn = mutation({
  args: {
    docId: v.id("eventGuests"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runEventGuestCheckIn(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "EventGuest_checkIn", __result);
    }
    return __result;
  },
});

async function __runEventGuestInvite(ctx: MutationCtx, { docId, eventId, name, email, phone, dietaryRestrictions, allergenRestrictions, accessibilityNeeds, specialMealRequired, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("EventGuest not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("EventGuest not found");
    const doc = await __decryptDoc(ctx, "EventGuest", ["email","phone"], __storedDoc) as Record<string, any>;
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may read guest attendance");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may write guest attendance through commands");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may execute guest attendance commands");
    if (!((doc.invitedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((((name).trim()).length > 0))) throw new Error("Guest name is required");
    {
      const __cur = doc.rsvpStatus;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "pending";
        const __allowed: Record<string, string[]> = { "pending": ["confirmed", "declined"], "confirmed": ["confirmed", "declined"], "declined": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'rsvpStatus'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      eventId: eventId,
      name: name,
      email: email,
      phone: phone,
      dietaryRestrictions: ((dietaryRestrictions != null) ? dietaryRestrictions : []),
      allergenRestrictions: ((allergenRestrictions != null) ? allergenRestrictions : []),
      accessibilityNeeds: ((accessibilityNeeds != null) ? accessibilityNeeds : []),
      specialMealRequired: ((specialMealRequired != null) ? specialMealRequired : false),
      rsvpStatus: "pending",
      invitedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "EventGuest", ["email","phone"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, guestId: docId, tenantId: __after.tenantId, eventId: eventId, name: name, _subject: { entity: "EventGuest", command: "invite", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EventGuestInvited", entity: "EventGuest", entityId: docId, payload: { guestId: docId, tenantId: __after.tenantId, eventId: eventId, name: name }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const EventGuest_invite = mutation({
  args: {
    docId: v.id("eventGuests"),
    eventId: v.string(),
    name: v.string(),
    email: v.optional(v.string()),
    phone: v.optional(v.string()),
    dietaryRestrictions: v.optional(v.array(v.string())),
    allergenRestrictions: v.optional(v.array(v.string())),
    accessibilityNeeds: v.optional(v.array(v.string())),
    specialMealRequired: v.optional(v.boolean()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runEventGuestInvite(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "EventGuest_invite", __result);
    }
    return __result;
  },
});

export const EventGuest_createViaInvite = mutation({
  args: {
    eventId: v.string(),
    name: v.string(),
    email: v.optional(v.string()),
    phone: v.optional(v.string()),
    dietaryRestrictions: v.optional(v.array(v.string())),
    allergenRestrictions: v.optional(v.array(v.string())),
    accessibilityNeeds: v.optional(v.array(v.string())),
    specialMealRequired: v.optional(v.boolean()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { eventId, name, email, phone, dietaryRestrictions, allergenRestrictions, accessibilityNeeds, specialMealRequired } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      dietaryRestrictions: args.dietaryRestrictions !== undefined ? args.dietaryRestrictions : [],
      allergenRestrictions: args.allergenRestrictions !== undefined ? args.allergenRestrictions : [],
      accessibilityNeeds: args.accessibilityNeeds !== undefined ? args.accessibilityNeeds : [],
      specialMealRequired: args.specialMealRequired !== undefined ? args.specialMealRequired : false,
      createdAt: Date.now(),
      updatedAt: Date.now(),
      rsvpStatus: "pending",
      email: args.email,
      eventId: args.eventId,
      name: args.name,
      phone: args.phone
    };
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may read guest attendance");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may write guest attendance through commands");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may execute guest attendance commands");
    if (!((__draft.invitedAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((((name).trim()).length > 0))) throw new Error("Guest name is required");
    const doc: Record<string, any> = {
      ...__draft,
      eventId: eventId,
      name: name,
      email: email,
      phone: phone,
      dietaryRestrictions: ((dietaryRestrictions != null) ? dietaryRestrictions : []),
      allergenRestrictions: ((allergenRestrictions != null) ? allergenRestrictions : []),
      accessibilityNeeds: ((accessibilityNeeds != null) ? accessibilityNeeds : []),
      specialMealRequired: ((specialMealRequired != null) ? specialMealRequired : false),
      rsvpStatus: "pending",
      invitedAt: Date.now(),
      version: 1,
    };
    const __storedDoc = await __encryptDoc(ctx, "EventGuest", ["email","phone"], doc);
    const docId = await ctx.db.insert("eventGuests", __storedDoc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, guestId: docId, tenantId: doc.tenantId, eventId: doc.eventId, name: doc.name, _subject: { entity: "EventGuest", command: "invite", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EventGuestInvited", entity: "EventGuest", entityId: docId, payload: { guestId: docId, tenantId: doc.tenantId, eventId: doc.eventId, name: doc.name }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "EventGuest_createViaInvite", __result);
    }
    return __result;
  },
});

async function __runEventGuestRsvpConfirm(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("EventGuest not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("EventGuest not found");
    const doc = await __decryptDoc(ctx, "EventGuest", ["email","phone"], __storedDoc) as Record<string, any>;
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may read guest attendance");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may write guest attendance through commands");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may execute guest attendance commands");
    if (!(((doc.rsvpStatus === "pending") || (doc.rsvpStatus === "confirmed")))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    {
      const __cur = doc.rsvpStatus;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "confirmed";
        const __allowed: Record<string, string[]> = { "pending": ["confirmed", "declined"], "confirmed": ["confirmed", "declined"], "declined": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'rsvpStatus'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      rsvpStatus: "confirmed",
      respondedAt: Date.now(),
      declineReason: null,
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "EventGuest", ["email","phone"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, guestId: docId, tenantId: __after.tenantId, eventId: __after.eventId, _subject: { entity: "EventGuest", command: "rsvpConfirm", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EventGuestRsvpConfirmed", entity: "EventGuest", entityId: docId, payload: { guestId: docId, tenantId: __after.tenantId, eventId: __after.eventId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const EventGuest_rsvpConfirm = mutation({
  args: {
    docId: v.id("eventGuests"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runEventGuestRsvpConfirm(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "EventGuest_rsvpConfirm", __result);
    }
    return __result;
  },
});

async function __runEventGuestRsvpDecline(ctx: MutationCtx, { docId, reason, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("EventGuest not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("EventGuest not found");
    const doc = await __decryptDoc(ctx, "EventGuest", ["email","phone"], __storedDoc) as Record<string, any>;
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may read guest attendance");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may write guest attendance through commands");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may execute guest attendance commands");
    if (!(((doc.rsvpStatus === "pending") || (doc.rsvpStatus === "confirmed")))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    {
      const __cur = doc.rsvpStatus;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "declined";
        const __allowed: Record<string, string[]> = { "pending": ["confirmed", "declined"], "confirmed": ["confirmed", "declined"], "declined": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'rsvpStatus'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      rsvpStatus: "declined",
      respondedAt: Date.now(),
      declineReason: reason,
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "EventGuest", ["email","phone"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, guestId: docId, tenantId: __after.tenantId, eventId: __after.eventId, reason: reason, _subject: { entity: "EventGuest", command: "rsvpDecline", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EventGuestRsvpDeclined", entity: "EventGuest", entityId: docId, payload: { guestId: docId, tenantId: __after.tenantId, eventId: __after.eventId, reason: reason }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const EventGuest_rsvpDecline = mutation({
  args: {
    docId: v.id("eventGuests"),
    reason: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runEventGuestRsvpDecline(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "EventGuest_rsvpDecline", __result);
    }
    return __result;
  },
});

async function __runEventGuestWithdraw(ctx: MutationCtx, { docId, reason, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("EventGuest not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("EventGuest not found");
    const doc = await __decryptDoc(ctx, "EventGuest", ["email","phone"], __storedDoc) as Record<string, any>;
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may read guest attendance");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may write guest attendance through commands");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may execute guest attendance commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!((((reason).trim()).length > 0))) throw new Error("Withdrawal reason is required");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      deletedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "EventGuest", ["email","phone"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, guestId: docId, tenantId: __after.tenantId, eventId: __after.eventId, reason: reason, _subject: { entity: "EventGuest", command: "withdraw", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EventGuestWithdrawn", entity: "EventGuest", entityId: docId, payload: { guestId: docId, tenantId: __after.tenantId, eventId: __after.eventId, reason: reason }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const EventGuest_withdraw = mutation({
  args: {
    docId: v.id("eventGuests"),
    reason: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runEventGuestWithdraw(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "EventGuest_withdraw", __result);
    }
    return __result;
  },
});

async function __runEventIngredientContributionRecord(ctx: MutationCtx, { docId, eventId, eventDishId, dishId, ingredientId, quantity, unit, servings, componentId, purchasingWeekStart, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("EventIngredientContribution not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("EventIngredientContribution not found");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) throw new Error("Inventory and managers may read event ingredient contributions");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) throw new Error("Inventory and managers may write event ingredient contributions");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) throw new Error("Inventory and managers may execute event ingredient contributions");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!((quantity > 0))) throw new Error("Contribution quantity must be positive");
    if (!((servings >= 0))) throw new Error("Contribution servings cannot be negative");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      eventId: eventId,
      eventDishId: eventDishId,
      dishId: dishId,
      componentId: componentId,
      ingredientId: ingredientId,
      quantity: quantity,
      unit: unit,
      servings: servings,
      purchasingWeekStart: purchasingWeekStart,
      recordedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, contributionId: docId, tenantId: __after.tenantId, eventId: eventId, eventDishId: eventDishId, ingredientId: ingredientId, quantity: quantity, unit: unit, purchasingWeekStart: purchasingWeekStart, _subject: { entity: "EventIngredientContribution", command: "record", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EventIngredientContributionRecorded", entity: "EventIngredientContribution", entityId: docId, payload: { contributionId: docId, tenantId: __after.tenantId, eventId: eventId, eventDishId: eventDishId, ingredientId: ingredientId, quantity: quantity, unit: unit, purchasingWeekStart: purchasingWeekStart }, createdAt: Date.now() });
    // Reactions
    const __agg0_rows = await ctx.db.query("eventIngredientContributions").withIndex("by_eventId", (q) => q.eq("eventId", payload.eventId)).collect();
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    const __agg0_rowsf = __agg0_rows.filter((d) => (d as any).ingredientId === payload.ingredientId).filter((d) => (d as any).deletedAt == null).filter((d) => (d as any).tenantId === __tenant);
    const __agg0 = __agg0_rowsf.reduce((acc, d) => { const n = Number((d as any).quantity); return acc + (Number.isFinite(n) ? n : 0); }, 0);
    const __match0_raw = await ctx.db.query("ingredientDemands").withIndex("by_eventId", (q) => q.eq("eventId", payload.eventId)).collect();
    const __match0_rows = __match0_raw.filter((d) => (d as any).eventId === payload.eventId && (d as any).ingredientId === payload.ingredientId && (d as any).deletedAt == null).sort((a, b) => String((a as any)._id).localeCompare(String((b as any)._id)));
    const __match0_id = __match0_rows.length > 0 ? (__match0_rows[0] as any)._id : null;
    if (__match0_id) {
      await __runIngredientDemandSyncFromContributions(ctx, { docId: __match0_id, eventId: payload.eventId, ingredientId: payload.ingredientId, requiredQuantity: __agg0, unit: payload.unit, purchasingWeekStart: payload.purchasingWeekStart } as any);
    } else {
      const __elseArgs: Record<string, any> = { eventId: payload.eventId, ingredientId: payload.ingredientId, requiredQuantity: __agg0, unit: payload.unit, purchasingWeekStart: payload.purchasingWeekStart };
      const __elseDoc: Record<string, any> = {
        tenantId: __auth.tenantId,
        requiredQuantity: 0,
        unit: "each",
        status: "pending",
        createdAt: Date.now(),
        updatedAt: Date.now(),
        version: 0,
      };
      for (const __k of ["deletedAt","eventId","ingredientId","preferredVendorId","requiredQuantity","unit","servings","dishId","sourceComponentLineQuantity","sourceBatchMultiplier","sourceYieldQuantity","status","purchaseEligibleEventId","purchasingWeekStart","calculatedAt","confirmedAt","fulfilledAt","supersededAt","supersedeReason","lastRecalculationReason","createdAt","updatedAt"] as string[]) {
        if (__elseArgs[__k] !== undefined) __elseDoc[__k] = __elseArgs[__k];
      }
      const __elseId = await ctx.db.insert("ingredientDemands", __elseDoc as any);
      await __runIngredientDemandSyncFromContributions(ctx, { docId: __elseId, ...__elseArgs } as any);
    }
    return { ...doc, ...updates };
}

export const EventIngredientContribution_record = mutation({
  args: {
    docId: v.id("eventIngredientContributions"),
    eventId: v.string(),
    eventDishId: v.string(),
    dishId: v.string(),
    ingredientId: v.string(),
    quantity: v.number(),
    unit: v.any(),
    servings: v.number(),
    componentId: v.optional(v.string()),
    purchasingWeekStart: v.optional(v.number()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runEventIngredientContributionRecord(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "EventIngredientContribution_record", __result);
    }
    return __result;
  },
});

export const EventIngredientContribution_createViaRecord = mutation({
  args: {
    eventId: v.string(),
    eventDishId: v.string(),
    dishId: v.string(),
    ingredientId: v.string(),
    quantity: v.number(),
    unit: v.any(),
    servings: v.number(),
    componentId: v.optional(v.string()),
    purchasingWeekStart: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { eventId, eventDishId, dishId, ingredientId, quantity, unit, servings, componentId, purchasingWeekStart } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      createdAt: Date.now(),
      updatedAt: Date.now(),
      componentId: args.componentId,
      dishId: args.dishId,
      eventDishId: args.eventDishId,
      eventId: args.eventId,
      ingredientId: args.ingredientId,
      purchasingWeekStart: args.purchasingWeekStart,
      quantity: args.quantity,
      servings: args.servings,
      unit: args.unit
    };
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) throw new Error("Inventory and managers may read event ingredient contributions");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) throw new Error("Inventory and managers may write event ingredient contributions");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) throw new Error("Inventory and managers may execute event ingredient contributions");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!((quantity > 0))) throw new Error("Contribution quantity must be positive");
    if (!((servings >= 0))) throw new Error("Contribution servings cannot be negative");
    const doc: Record<string, any> = {
      ...__draft,
      eventId: eventId,
      eventDishId: eventDishId,
      dishId: dishId,
      componentId: componentId,
      ingredientId: ingredientId,
      quantity: quantity,
      unit: unit,
      servings: servings,
      purchasingWeekStart: purchasingWeekStart,
      recordedAt: Date.now(),
      version: 1,
    };
    const docId = await ctx.db.insert("eventIngredientContributions", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, contributionId: docId, tenantId: doc.tenantId, eventId: doc.eventId, eventDishId: doc.eventDishId, ingredientId: doc.ingredientId, quantity: doc.quantity, unit: doc.unit, purchasingWeekStart: doc.purchasingWeekStart, _subject: { entity: "EventIngredientContribution", command: "record", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EventIngredientContributionRecorded", entity: "EventIngredientContribution", entityId: docId, payload: { contributionId: docId, tenantId: doc.tenantId, eventId: doc.eventId, eventDishId: doc.eventDishId, ingredientId: doc.ingredientId, quantity: doc.quantity, unit: doc.unit, purchasingWeekStart: doc.purchasingWeekStart }, createdAt: Date.now() });
    // Reactions
    const __agg0_rows = await ctx.db.query("eventIngredientContributions").withIndex("by_eventId", (q) => q.eq("eventId", payload.eventId)).collect();
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    const __agg0_rowsf = __agg0_rows.filter((d) => (d as any).ingredientId === payload.ingredientId).filter((d) => (d as any).deletedAt == null).filter((d) => (d as any).tenantId === __tenant);
    const __agg0 = __agg0_rowsf.reduce((acc, d) => { const n = Number((d as any).quantity); return acc + (Number.isFinite(n) ? n : 0); }, 0);
    const __match0_raw = await ctx.db.query("ingredientDemands").withIndex("by_eventId", (q) => q.eq("eventId", payload.eventId)).collect();
    const __match0_rows = __match0_raw.filter((d) => (d as any).eventId === payload.eventId && (d as any).ingredientId === payload.ingredientId && (d as any).deletedAt == null).sort((a, b) => String((a as any)._id).localeCompare(String((b as any)._id)));
    const __match0_id = __match0_rows.length > 0 ? (__match0_rows[0] as any)._id : null;
    if (__match0_id) {
      await __runIngredientDemandSyncFromContributions(ctx, { docId: __match0_id, eventId: payload.eventId, ingredientId: payload.ingredientId, requiredQuantity: __agg0, unit: payload.unit, purchasingWeekStart: payload.purchasingWeekStart } as any);
    } else {
      const __elseArgs: Record<string, any> = { eventId: payload.eventId, ingredientId: payload.ingredientId, requiredQuantity: __agg0, unit: payload.unit, purchasingWeekStart: payload.purchasingWeekStart };
      const __elseDoc: Record<string, any> = {
        tenantId: __auth.tenantId,
        requiredQuantity: 0,
        unit: "each",
        status: "pending",
        createdAt: Date.now(),
        updatedAt: Date.now(),
        version: 0,
      };
      for (const __k of ["deletedAt","eventId","ingredientId","preferredVendorId","requiredQuantity","unit","servings","dishId","sourceComponentLineQuantity","sourceBatchMultiplier","sourceYieldQuantity","status","purchaseEligibleEventId","purchasingWeekStart","calculatedAt","confirmedAt","fulfilledAt","supersededAt","supersedeReason","lastRecalculationReason","createdAt","updatedAt"] as string[]) {
        if (__elseArgs[__k] !== undefined) __elseDoc[__k] = __elseArgs[__k];
      }
      const __elseId = await ctx.db.insert("ingredientDemands", __elseDoc as any);
      await __runIngredientDemandSyncFromContributions(ctx, { docId: __elseId, ...__elseArgs } as any);
    }
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "EventIngredientContribution_createViaRecord", __result);
    }
    return __result;
  },
});

async function __runEventIngredientContributionRetire(ctx: MutationCtx, { docId, reason, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("EventIngredientContribution not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("EventIngredientContribution not found");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) throw new Error("Inventory and managers may read event ingredient contributions");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) throw new Error("Inventory and managers may write event ingredient contributions");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) throw new Error("Inventory and managers may execute event ingredient contributions");
    if (!((doc.recordedAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((((reason).trim()).length > 0))) throw new Error("Retire reason is required");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      deletedAt: Date.now(),
      quantity: 0,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, contributionId: docId, tenantId: __after.tenantId, eventId: __after.eventId, eventDishId: __after.eventDishId, ingredientId: __after.ingredientId, quantity: 0, unit: __after.unit, purchasingWeekStart: __after.purchasingWeekStart, _subject: { entity: "EventIngredientContribution", command: "retire", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EventIngredientContributionRecorded", entity: "EventIngredientContribution", entityId: docId, payload: { contributionId: docId, tenantId: __after.tenantId, eventId: __after.eventId, eventDishId: __after.eventDishId, ingredientId: __after.ingredientId, quantity: 0, unit: __after.unit, purchasingWeekStart: __after.purchasingWeekStart }, createdAt: Date.now() });
    // Reactions
    const __agg0_rows = await ctx.db.query("eventIngredientContributions").withIndex("by_eventId", (q) => q.eq("eventId", payload.eventId)).collect();
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    const __agg0_rowsf = __agg0_rows.filter((d) => (d as any).ingredientId === payload.ingredientId).filter((d) => (d as any).deletedAt == null).filter((d) => (d as any).tenantId === __tenant);
    const __agg0 = __agg0_rowsf.reduce((acc, d) => { const n = Number((d as any).quantity); return acc + (Number.isFinite(n) ? n : 0); }, 0);
    const __match0_raw = await ctx.db.query("ingredientDemands").withIndex("by_eventId", (q) => q.eq("eventId", payload.eventId)).collect();
    const __match0_rows = __match0_raw.filter((d) => (d as any).eventId === payload.eventId && (d as any).ingredientId === payload.ingredientId && (d as any).deletedAt == null).sort((a, b) => String((a as any)._id).localeCompare(String((b as any)._id)));
    const __match0_id = __match0_rows.length > 0 ? (__match0_rows[0] as any)._id : null;
    if (__match0_id) {
      await __runIngredientDemandSyncFromContributions(ctx, { docId: __match0_id, eventId: payload.eventId, ingredientId: payload.ingredientId, requiredQuantity: __agg0, unit: payload.unit, purchasingWeekStart: payload.purchasingWeekStart } as any);
    } else {
      const __elseArgs: Record<string, any> = { eventId: payload.eventId, ingredientId: payload.ingredientId, requiredQuantity: __agg0, unit: payload.unit, purchasingWeekStart: payload.purchasingWeekStart };
      const __elseDoc: Record<string, any> = {
        tenantId: __auth.tenantId,
        requiredQuantity: 0,
        unit: "each",
        status: "pending",
        createdAt: Date.now(),
        updatedAt: Date.now(),
        version: 0,
      };
      for (const __k of ["deletedAt","eventId","ingredientId","preferredVendorId","requiredQuantity","unit","servings","dishId","sourceComponentLineQuantity","sourceBatchMultiplier","sourceYieldQuantity","status","purchaseEligibleEventId","purchasingWeekStart","calculatedAt","confirmedAt","fulfilledAt","supersededAt","supersedeReason","lastRecalculationReason","createdAt","updatedAt"] as string[]) {
        if (__elseArgs[__k] !== undefined) __elseDoc[__k] = __elseArgs[__k];
      }
      const __elseId = await ctx.db.insert("ingredientDemands", __elseDoc as any);
      await __runIngredientDemandSyncFromContributions(ctx, { docId: __elseId, ...__elseArgs } as any);
    }
    return { ...doc, ...updates };
}

export const EventIngredientContribution_retire = mutation({
  args: {
    docId: v.id("eventIngredientContributions"),
    reason: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runEventIngredientContributionRetire(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "EventIngredientContribution_retire", __result);
    }
    return __result;
  },
});

async function __runEventIngredientContributionRevise(ctx: MutationCtx, { docId, quantity, servings, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("EventIngredientContribution not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("EventIngredientContribution not found");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) throw new Error("Inventory and managers may read event ingredient contributions");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) throw new Error("Inventory and managers may write event ingredient contributions");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) throw new Error("Inventory and managers may execute event ingredient contributions");
    if (!((doc.recordedAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((quantity > 0))) throw new Error("Contribution quantity must be positive");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      quantity: quantity,
      servings: servings,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, contributionId: docId, tenantId: __after.tenantId, eventId: __after.eventId, eventDishId: __after.eventDishId, ingredientId: __after.ingredientId, quantity: quantity, unit: __after.unit, purchasingWeekStart: __after.purchasingWeekStart, _subject: { entity: "EventIngredientContribution", command: "revise", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EventIngredientContributionRecorded", entity: "EventIngredientContribution", entityId: docId, payload: { contributionId: docId, tenantId: __after.tenantId, eventId: __after.eventId, eventDishId: __after.eventDishId, ingredientId: __after.ingredientId, quantity: quantity, unit: __after.unit, purchasingWeekStart: __after.purchasingWeekStart }, createdAt: Date.now() });
    // Reactions
    const __agg0_rows = await ctx.db.query("eventIngredientContributions").withIndex("by_eventId", (q) => q.eq("eventId", payload.eventId)).collect();
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    const __agg0_rowsf = __agg0_rows.filter((d) => (d as any).ingredientId === payload.ingredientId).filter((d) => (d as any).deletedAt == null).filter((d) => (d as any).tenantId === __tenant);
    const __agg0 = __agg0_rowsf.reduce((acc, d) => { const n = Number((d as any).quantity); return acc + (Number.isFinite(n) ? n : 0); }, 0);
    const __match0_raw = await ctx.db.query("ingredientDemands").withIndex("by_eventId", (q) => q.eq("eventId", payload.eventId)).collect();
    const __match0_rows = __match0_raw.filter((d) => (d as any).eventId === payload.eventId && (d as any).ingredientId === payload.ingredientId && (d as any).deletedAt == null).sort((a, b) => String((a as any)._id).localeCompare(String((b as any)._id)));
    const __match0_id = __match0_rows.length > 0 ? (__match0_rows[0] as any)._id : null;
    if (__match0_id) {
      await __runIngredientDemandSyncFromContributions(ctx, { docId: __match0_id, eventId: payload.eventId, ingredientId: payload.ingredientId, requiredQuantity: __agg0, unit: payload.unit, purchasingWeekStart: payload.purchasingWeekStart } as any);
    } else {
      const __elseArgs: Record<string, any> = { eventId: payload.eventId, ingredientId: payload.ingredientId, requiredQuantity: __agg0, unit: payload.unit, purchasingWeekStart: payload.purchasingWeekStart };
      const __elseDoc: Record<string, any> = {
        tenantId: __auth.tenantId,
        requiredQuantity: 0,
        unit: "each",
        status: "pending",
        createdAt: Date.now(),
        updatedAt: Date.now(),
        version: 0,
      };
      for (const __k of ["deletedAt","eventId","ingredientId","preferredVendorId","requiredQuantity","unit","servings","dishId","sourceComponentLineQuantity","sourceBatchMultiplier","sourceYieldQuantity","status","purchaseEligibleEventId","purchasingWeekStart","calculatedAt","confirmedAt","fulfilledAt","supersededAt","supersedeReason","lastRecalculationReason","createdAt","updatedAt"] as string[]) {
        if (__elseArgs[__k] !== undefined) __elseDoc[__k] = __elseArgs[__k];
      }
      const __elseId = await ctx.db.insert("ingredientDemands", __elseDoc as any);
      await __runIngredientDemandSyncFromContributions(ctx, { docId: __elseId, ...__elseArgs } as any);
    }
    return { ...doc, ...updates };
}

export const EventIngredientContribution_revise = mutation({
  args: {
    docId: v.id("eventIngredientContributions"),
    quantity: v.number(),
    servings: v.number(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runEventIngredientContributionRevise(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "EventIngredientContribution_revise", __result);
    }
    return __result;
  },
});

async function __runEventLayoutSectionAdd(ctx: MutationCtx, { docId, eventId, type, instructions, sortOrder, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("EventLayoutSection not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("EventLayoutSection not found");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may read event layout sections");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may write event layout sections through commands");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may execute event layout section commands");
    if (!((doc.addedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((((type).trim()).length > 0))) throw new Error("Layout section type is required");
    if (!(((sortOrder == null) || (sortOrder >= 0)))) throw new Error("Layout section sort order cannot be negative");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      eventId: eventId,
      type: type,
      instructions: instructions,
      sortOrder: ((sortOrder != null) ? sortOrder : 0),
      addedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, sectionId: docId, tenantId: __after.tenantId, eventId: eventId, type: type, sortOrder: ((sortOrder != null) ? sortOrder : 0), _subject: { entity: "EventLayoutSection", command: "add", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EventLayoutSectionAdded", entity: "EventLayoutSection", entityId: docId, payload: { sectionId: docId, tenantId: __after.tenantId, eventId: eventId, type: type, sortOrder: ((sortOrder != null) ? sortOrder : 0) }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const EventLayoutSection_add = mutation({
  args: {
    docId: v.id("eventLayoutSections"),
    eventId: v.string(),
    type: v.string(),
    instructions: v.optional(v.string()),
    sortOrder: v.optional(v.any()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runEventLayoutSectionAdd(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "EventLayoutSection_add", __result);
    }
    return __result;
  },
});

export const EventLayoutSection_createViaAdd = mutation({
  args: {
    eventId: v.string(),
    type: v.string(),
    instructions: v.optional(v.string()),
    sortOrder: v.optional(v.any()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { eventId, type, instructions, sortOrder } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      sortOrder: args.sortOrder !== undefined ? args.sortOrder : 0,
      createdAt: Date.now(),
      updatedAt: Date.now(),
      eventId: args.eventId,
      instructions: args.instructions,
      type: args.type
    };
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may read event layout sections");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may write event layout sections through commands");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may execute event layout section commands");
    if (!((__draft.addedAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((((type).trim()).length > 0))) throw new Error("Layout section type is required");
    if (!(((sortOrder == null) || (sortOrder >= 0)))) throw new Error("Layout section sort order cannot be negative");
    const doc: Record<string, any> = {
      ...__draft,
      eventId: eventId,
      type: type,
      instructions: instructions,
      sortOrder: ((sortOrder != null) ? sortOrder : 0),
      addedAt: Date.now(),
      version: 1,
    };
    const docId = await ctx.db.insert("eventLayoutSections", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, sectionId: docId, tenantId: doc.tenantId, eventId: doc.eventId, type: doc.type, sortOrder: ((doc.sortOrder != null) ? doc.sortOrder : 0), _subject: { entity: "EventLayoutSection", command: "add", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EventLayoutSectionAdded", entity: "EventLayoutSection", entityId: docId, payload: { sectionId: docId, tenantId: doc.tenantId, eventId: doc.eventId, type: doc.type, sortOrder: ((doc.sortOrder != null) ? doc.sortOrder : 0) }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "EventLayoutSection_createViaAdd", __result);
    }
    return __result;
  },
});

async function __runEventLayoutSectionRemove(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("EventLayoutSection not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("EventLayoutSection not found");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may read event layout sections");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may write event layout sections through commands");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may execute event layout section commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      deletedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, sectionId: docId, tenantId: __after.tenantId, eventId: __after.eventId, _subject: { entity: "EventLayoutSection", command: "remove", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EventLayoutSectionRemoved", entity: "EventLayoutSection", entityId: docId, payload: { sectionId: docId, tenantId: __after.tenantId, eventId: __after.eventId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const EventLayoutSection_remove = mutation({
  args: {
    docId: v.id("eventLayoutSections"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runEventLayoutSectionRemove(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "EventLayoutSection_remove", __result);
    }
    return __result;
  },
});

async function __runEventLayoutSectionUpdate(ctx: MutationCtx, { docId, type, instructions, sortOrder, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("EventLayoutSection not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("EventLayoutSection not found");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may read event layout sections");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may write event layout sections through commands");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may execute event layout section commands");
    if (!((doc.addedAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(((type == null) || (((type).trim()).length > 0)))) throw new Error("Layout section type cannot be blanked");
    if (!(((sortOrder == null) || (sortOrder >= 0)))) throw new Error("Layout section sort order cannot be negative");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      type: ((type != null) ? type : doc.type),
      instructions: ((instructions != null) ? instructions : doc.instructions),
      sortOrder: ((sortOrder != null) ? sortOrder : doc.sortOrder),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, sectionId: docId, tenantId: __after.tenantId, eventId: __after.eventId, _subject: { entity: "EventLayoutSection", command: "update", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EventLayoutSectionUpdated", entity: "EventLayoutSection", entityId: docId, payload: { sectionId: docId, tenantId: __after.tenantId, eventId: __after.eventId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const EventLayoutSection_update = mutation({
  args: {
    docId: v.id("eventLayoutSections"),
    type: v.optional(v.string()),
    instructions: v.optional(v.string()),
    sortOrder: v.optional(v.any()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runEventLayoutSectionUpdate(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "EventLayoutSection_update", __result);
    }
    return __result;
  },
});

async function __runEventStaffNeedCancel(ctx: MutationCtx, { docId, reason, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("EventStaffNeed not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("EventStaffNeed not found");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may read open staff needs");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may write staff needs through commands");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may execute staff need commands");
    if (!(((doc.status === "open") || (doc.status === "claimed")))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Guard 2 failed");
    if (!((((reason).trim()).length > 0))) throw new Error("Cancel reason is required");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "cancelled";
        const __allowed: Record<string, string[]> = { "open": ["claimed", "filled", "cancelled"], "claimed": ["filled", "open", "cancelled"], "filled": [], "cancelled": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "cancelled",
      cancelledAt: Date.now(),
      notes: reason,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, staffNeedId: docId, tenantId: __after.tenantId, eventId: __after.eventId, reason: reason, _subject: { entity: "EventStaffNeed", command: "cancel", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EventStaffNeedCancelled", entity: "EventStaffNeed", entityId: docId, payload: { staffNeedId: docId, tenantId: __after.tenantId, eventId: __after.eventId, reason: reason }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const EventStaffNeed_cancel = mutation({
  args: {
    docId: v.id("eventStaffNeeds"),
    reason: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runEventStaffNeedCancel(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "EventStaffNeed_cancel", __result);
    }
    return __result;
  },
});

async function __runEventStaffNeedClaim(ctx: MutationCtx, { docId, personId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("EventStaffNeed not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("EventStaffNeed not found");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may read open staff needs");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may write staff needs through commands");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may execute staff need commands");
    if (!((doc.status === "open"))) throw new Error("Guard 0 failed");
    if (!((doc.postedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "claimed";
        const __allowed: Record<string, string[]> = { "open": ["claimed", "filled", "cancelled"], "claimed": ["filled", "open", "cancelled"], "filled": [], "cancelled": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      claimedByPersonId: personId,
      status: "claimed",
      claimedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, staffNeedId: docId, tenantId: __after.tenantId, eventId: __after.eventId, personId: personId, _subject: { entity: "EventStaffNeed", command: "claim", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EventStaffNeedClaimed", entity: "EventStaffNeed", entityId: docId, payload: { staffNeedId: docId, tenantId: __after.tenantId, eventId: __after.eventId, personId: personId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const EventStaffNeed_claim = mutation({
  args: {
    docId: v.id("eventStaffNeeds"),
    personId: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runEventStaffNeedClaim(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "EventStaffNeed_claim", __result);
    }
    return __result;
  },
});

async function __runEventStaffNeedFill(ctx: MutationCtx, { docId, personId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("EventStaffNeed not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("EventStaffNeed not found");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may read open staff needs");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may write staff needs through commands");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may execute staff need commands");
    if (!(((doc.status === "open") || (doc.status === "claimed")))) throw new Error("Guard 0 failed");
    if (!((doc.postedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Guard 3 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "filled";
        const __allowed: Record<string, string[]> = { "open": ["claimed", "filled", "cancelled"], "claimed": ["filled", "open", "cancelled"], "filled": [], "cancelled": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      filledByPersonId: personId,
      claimedByPersonId: personId,
      status: "filled",
      filledAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, staffNeedId: docId, tenantId: __after.tenantId, eventId: __after.eventId, personId: personId, _subject: { entity: "EventStaffNeed", command: "fill", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EventStaffNeedFilled", entity: "EventStaffNeed", entityId: docId, payload: { staffNeedId: docId, tenantId: __after.tenantId, eventId: __after.eventId, personId: personId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const EventStaffNeed_fill = mutation({
  args: {
    docId: v.id("eventStaffNeeds"),
    personId: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runEventStaffNeedFill(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "EventStaffNeed_fill", __result);
    }
    return __result;
  },
});

async function __runEventStaffNeedPostOpen(ctx: MutationCtx, { docId, eventId, role, description, startsAt, endsAt, notes, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("EventStaffNeed not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("EventStaffNeed not found");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may read open staff needs");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may write staff needs through commands");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may execute staff need commands");
    if (!((doc.postedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Guard 2 failed");
    if (!((((role).trim()).length > 0))) throw new Error("Staff need role is required");
    if (!((((startsAt == null) || (endsAt == null)) || (endsAt > startsAt)))) throw new Error("Staff need end must be after its start");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "open";
        const __allowed: Record<string, string[]> = { "open": ["claimed", "filled", "cancelled"], "claimed": ["filled", "open", "cancelled"], "filled": [], "cancelled": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      eventId: eventId,
      role: role,
      description: description,
      startsAt: startsAt,
      endsAt: endsAt,
      notes: notes,
      status: "open",
      postedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, staffNeedId: docId, tenantId: __after.tenantId, eventId: eventId, role: role, _subject: { entity: "EventStaffNeed", command: "postOpen", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EventStaffNeedPosted", entity: "EventStaffNeed", entityId: docId, payload: { staffNeedId: docId, tenantId: __after.tenantId, eventId: eventId, role: role }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const EventStaffNeed_postOpen = mutation({
  args: {
    docId: v.id("eventStaffNeeds"),
    eventId: v.string(),
    role: v.string(),
    description: v.optional(v.string()),
    startsAt: v.optional(v.number()),
    endsAt: v.optional(v.number()),
    notes: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runEventStaffNeedPostOpen(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "EventStaffNeed_postOpen", __result);
    }
    return __result;
  },
});

export const EventStaffNeed_createViaPostOpen = mutation({
  args: {
    eventId: v.string(),
    role: v.string(),
    description: v.optional(v.string()),
    startsAt: v.optional(v.number()),
    endsAt: v.optional(v.number()),
    notes: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { eventId, role, description, startsAt, endsAt, notes } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      createdAt: Date.now(),
      updatedAt: Date.now(),
      status: "open",
      description: args.description,
      endsAt: args.endsAt,
      eventId: args.eventId,
      notes: args.notes,
      role: args.role,
      startsAt: args.startsAt
    };
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may read open staff needs");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may write staff needs through commands");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may execute staff need commands");
    if (!((__draft.postedAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Guard 2 failed");
    if (!((((role).trim()).length > 0))) throw new Error("Staff need role is required");
    if (!((((startsAt == null) || (endsAt == null)) || (endsAt > startsAt)))) throw new Error("Staff need end must be after its start");
    const doc: Record<string, any> = {
      ...__draft,
      eventId: eventId,
      role: role,
      description: description,
      startsAt: startsAt,
      endsAt: endsAt,
      notes: notes,
      status: "open",
      postedAt: Date.now(),
      version: 1,
    };
    const docId = await ctx.db.insert("eventStaffNeeds", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, staffNeedId: docId, tenantId: doc.tenantId, eventId: doc.eventId, role: doc.role, _subject: { entity: "EventStaffNeed", command: "postOpen", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EventStaffNeedPosted", entity: "EventStaffNeed", entityId: docId, payload: { staffNeedId: docId, tenantId: doc.tenantId, eventId: doc.eventId, role: doc.role }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "EventStaffNeed_createViaPostOpen", __result);
    }
    return __result;
  },
});

async function __runEventStaffNeedReleaseClaim(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("EventStaffNeed not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("EventStaffNeed not found");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may read open staff needs");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may write staff needs through commands");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may execute staff need commands");
    if (!((doc.status === "claimed"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "open";
        const __allowed: Record<string, string[]> = { "open": ["claimed", "filled", "cancelled"], "claimed": ["filled", "open", "cancelled"], "filled": [], "cancelled": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      claimedByPersonId: null,
      claimedAt: null,
      status: "open",
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, staffNeedId: docId, tenantId: __after.tenantId, eventId: __after.eventId, _subject: { entity: "EventStaffNeed", command: "releaseClaim", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EventStaffNeedReleased", entity: "EventStaffNeed", entityId: docId, payload: { staffNeedId: docId, tenantId: __after.tenantId, eventId: __after.eventId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const EventStaffNeed_releaseClaim = mutation({
  args: {
    docId: v.id("eventStaffNeeds"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runEventStaffNeedReleaseClaim(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "EventStaffNeed_releaseClaim", __result);
    }
    return __result;
  },
});

async function __runEventTemplateArchive(ctx: MutationCtx, { docId, reason, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("EventTemplate not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("EventTemplate not found");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may read event templates");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may write event templates through commands");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may execute event template commands");
    if (!((doc.status === "active"))) throw new Error("Guard 0 failed");
    if (!((doc.definedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((((reason).trim()).length > 0))) throw new Error("Archive reason is required");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "archived";
        const __allowed: Record<string, string[]> = { "active": ["archived"], "archived": ["active"] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "archived",
      archivedAt: Date.now(),
      archiveReason: reason,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, templateId: docId, tenantId: __after.tenantId, reason: reason, _subject: { entity: "EventTemplate", command: "archive", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EventTemplateArchived", entity: "EventTemplate", entityId: docId, payload: { templateId: docId, tenantId: __after.tenantId, reason: reason }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const EventTemplate_archive = mutation({
  args: {
    docId: v.id("eventTemplates"),
    reason: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runEventTemplateArchive(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "EventTemplate_archive", __result);
    }
    return __result;
  },
});

async function __runEventTemplateDefine(ctx: MutationCtx, { docId, name, clientType, eventType, defaultHeadcount, menuId, defaultStaffRoles, typicalEquipment, notes, sourceEventId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("EventTemplate not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("EventTemplate not found");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may read event templates");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may write event templates through commands");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may execute event template commands");
    if (!((doc.definedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((((name).trim()).length > 0))) throw new Error("Template name is required");
    if (!((((eventType).trim()).length > 0))) throw new Error("Event type is required");
    if (!(((defaultHeadcount >= 1) && (defaultHeadcount <= 100000)))) throw new Error("Default headcount must be between 1 and 100000");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      name: name,
      clientType: clientType,
      eventType: eventType,
      defaultHeadcount: defaultHeadcount,
      menuId: menuId,
      defaultStaffRoles: ((defaultStaffRoles != null) ? defaultStaffRoles : []),
      typicalEquipment: ((typicalEquipment != null) ? typicalEquipment : []),
      notes: notes,
      sourceEventId: sourceEventId,
      definedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, templateId: docId, tenantId: __after.tenantId, name: name, eventType: eventType, sourceEventId: sourceEventId, _subject: { entity: "EventTemplate", command: "define", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EventTemplateDefined", entity: "EventTemplate", entityId: docId, payload: { templateId: docId, tenantId: __after.tenantId, name: name, eventType: eventType, sourceEventId: sourceEventId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const EventTemplate_define = mutation({
  args: {
    docId: v.id("eventTemplates"),
    name: v.string(),
    clientType: v.any(),
    eventType: v.string(),
    defaultHeadcount: v.number(),
    menuId: v.optional(v.string()),
    defaultStaffRoles: v.optional(v.array(v.string())),
    typicalEquipment: v.optional(v.array(v.string())),
    notes: v.optional(v.string()),
    sourceEventId: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runEventTemplateDefine(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "EventTemplate_define", __result);
    }
    return __result;
  },
});

export const EventTemplate_createViaDefine = mutation({
  args: {
    name: v.string(),
    clientType: v.any(),
    eventType: v.string(),
    defaultHeadcount: v.number(),
    menuId: v.optional(v.string()),
    defaultStaffRoles: v.optional(v.array(v.string())),
    typicalEquipment: v.optional(v.array(v.string())),
    notes: v.optional(v.string()),
    sourceEventId: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { name, clientType, eventType, defaultHeadcount, menuId, defaultStaffRoles, typicalEquipment, notes, sourceEventId } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      defaultStaffRoles: args.defaultStaffRoles !== undefined ? args.defaultStaffRoles : [],
      typicalEquipment: args.typicalEquipment !== undefined ? args.typicalEquipment : [],
      status: "active",
      createdAt: Date.now(),
      updatedAt: Date.now(),
      clientType: args.clientType,
      defaultHeadcount: args.defaultHeadcount,
      eventType: args.eventType,
      menuId: args.menuId,
      name: args.name,
      notes: args.notes,
      sourceEventId: args.sourceEventId
    };
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may read event templates");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may write event templates through commands");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may execute event template commands");
    if (!((__draft.definedAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((((name).trim()).length > 0))) throw new Error("Template name is required");
    if (!((((eventType).trim()).length > 0))) throw new Error("Event type is required");
    if (!(((defaultHeadcount >= 1) && (defaultHeadcount <= 100000)))) throw new Error("Default headcount must be between 1 and 100000");
    const doc: Record<string, any> = {
      ...__draft,
      name: name,
      clientType: clientType,
      eventType: eventType,
      defaultHeadcount: defaultHeadcount,
      menuId: menuId,
      defaultStaffRoles: ((defaultStaffRoles != null) ? defaultStaffRoles : []),
      typicalEquipment: ((typicalEquipment != null) ? typicalEquipment : []),
      notes: notes,
      sourceEventId: sourceEventId,
      definedAt: Date.now(),
      version: 1,
    };
    const docId = await ctx.db.insert("eventTemplates", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, templateId: docId, tenantId: doc.tenantId, name: doc.name, eventType: doc.eventType, sourceEventId: doc.sourceEventId, _subject: { entity: "EventTemplate", command: "define", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EventTemplateDefined", entity: "EventTemplate", entityId: docId, payload: { templateId: docId, tenantId: doc.tenantId, name: doc.name, eventType: doc.eventType, sourceEventId: doc.sourceEventId }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "EventTemplate_createViaDefine", __result);
    }
    return __result;
  },
});

async function __runEventTemplateReactivate(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("EventTemplate not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("EventTemplate not found");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may read event templates");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may write event templates through commands");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may execute event template commands");
    if (!((doc.status === "archived"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "active";
        const __allowed: Record<string, string[]> = { "active": ["archived"], "archived": ["active"] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "active",
      archivedAt: null,
      archiveReason: null,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, templateId: docId, tenantId: __after.tenantId, _subject: { entity: "EventTemplate", command: "reactivate", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EventTemplateReactivated", entity: "EventTemplate", entityId: docId, payload: { templateId: docId, tenantId: __after.tenantId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const EventTemplate_reactivate = mutation({
  args: {
    docId: v.id("eventTemplates"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runEventTemplateReactivate(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "EventTemplate_reactivate", __result);
    }
    return __result;
  },
});

async function __runEventTemplateRevise(ctx: MutationCtx, { docId, name, clientType, eventType, defaultHeadcount, menuId, defaultStaffRoles, typicalEquipment, notes, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("EventTemplate not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("EventTemplate not found");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may read event templates");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may write event templates through commands");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may execute event template commands");
    if (!((doc.definedAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.status === "active"))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((((name).trim()).length > 0))) throw new Error("Template name is required");
    if (!((((eventType).trim()).length > 0))) throw new Error("Event type is required");
    if (!(((defaultHeadcount >= 1) && (defaultHeadcount <= 100000)))) throw new Error("Default headcount must be between 1 and 100000");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      name: name,
      clientType: clientType,
      eventType: eventType,
      defaultHeadcount: defaultHeadcount,
      menuId: menuId,
      defaultStaffRoles: ((defaultStaffRoles != null) ? defaultStaffRoles : []),
      typicalEquipment: ((typicalEquipment != null) ? typicalEquipment : []),
      notes: notes,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, templateId: docId, tenantId: __after.tenantId, name: name, eventType: eventType, _subject: { entity: "EventTemplate", command: "revise", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EventTemplateRevised", entity: "EventTemplate", entityId: docId, payload: { templateId: docId, tenantId: __after.tenantId, name: name, eventType: eventType }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const EventTemplate_revise = mutation({
  args: {
    docId: v.id("eventTemplates"),
    name: v.string(),
    clientType: v.any(),
    eventType: v.string(),
    defaultHeadcount: v.number(),
    menuId: v.optional(v.string()),
    defaultStaffRoles: v.optional(v.array(v.string())),
    typicalEquipment: v.optional(v.array(v.string())),
    notes: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runEventTemplateRevise(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "EventTemplate_revise", __result);
    }
    return __result;
  },
});

async function __runEventTimelineActivityAdjust(ctx: MutationCtx, { docId, name, startsAt, endsAt, responsibleParty, notes, category, siteNotes, sortOrder, assigneeTeams, assigneePersonIds, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("EventTimelineActivity not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("EventTimelineActivity not found");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may read the event timeline");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may write the event timeline through commands");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may execute event timeline commands");
    if (!((doc.scheduledAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(((name == null) || (((name).trim()).length > 0)))) throw new Error("Activity name cannot be blanked");
    if (!(((sortOrder == null) || (sortOrder >= 0)))) throw new Error("Timeline activity sort order cannot be negative");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      name: ((name != null) ? name : doc.name),
      startsAt: ((startsAt != null) ? startsAt : doc.startsAt),
      endsAt: ((endsAt != null) ? endsAt : doc.endsAt),
      responsibleParty: ((responsibleParty != null) ? responsibleParty : doc.responsibleParty),
      notes: ((notes != null) ? notes : doc.notes),
      category: ((category != null) ? category : doc.category),
      siteNotes: ((siteNotes != null) ? siteNotes : doc.siteNotes),
      sortOrder: ((sortOrder != null) ? sortOrder : doc.sortOrder),
      assigneeTeams: ((assigneeTeams != null) ? assigneeTeams : doc.assigneeTeams),
      assigneePersonIds: ((assigneePersonIds != null) ? assigneePersonIds : doc.assigneePersonIds),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, activityId: docId, tenantId: __after.tenantId, eventId: __after.eventId, _subject: { entity: "EventTimelineActivity", command: "adjust", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EventTimelineActivityAdjusted", entity: "EventTimelineActivity", entityId: docId, payload: { activityId: docId, tenantId: __after.tenantId, eventId: __after.eventId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const EventTimelineActivity_adjust = mutation({
  args: {
    docId: v.id("eventTimelineActivities"),
    name: v.optional(v.string()),
    startsAt: v.optional(v.number()),
    endsAt: v.optional(v.number()),
    responsibleParty: v.optional(v.string()),
    notes: v.optional(v.string()),
    category: v.optional(v.string()),
    siteNotes: v.optional(v.string()),
    sortOrder: v.optional(v.any()),
    assigneeTeams: v.optional(v.array(v.string())),
    assigneePersonIds: v.optional(v.array(v.string())),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runEventTimelineActivityAdjust(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "EventTimelineActivity_adjust", __result);
    }
    return __result;
  },
});

async function __runEventTimelineActivityRemove(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("EventTimelineActivity not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("EventTimelineActivity not found");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may read the event timeline");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may write the event timeline through commands");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may execute event timeline commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      deletedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, activityId: docId, tenantId: __after.tenantId, eventId: __after.eventId, _subject: { entity: "EventTimelineActivity", command: "remove", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EventTimelineActivityRemoved", entity: "EventTimelineActivity", entityId: docId, payload: { activityId: docId, tenantId: __after.tenantId, eventId: __after.eventId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const EventTimelineActivity_remove = mutation({
  args: {
    docId: v.id("eventTimelineActivities"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runEventTimelineActivityRemove(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "EventTimelineActivity_remove", __result);
    }
    return __result;
  },
});

async function __runEventTimelineActivitySchedule(ctx: MutationCtx, { docId, eventId, name, startsAt, endsAt, responsibleParty, notes, category, siteNotes, sortOrder, assigneeTeams, assigneePersonIds, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("EventTimelineActivity not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("EventTimelineActivity not found");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may read the event timeline");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may write the event timeline through commands");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may execute event timeline commands");
    if (!((doc.scheduledAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((((name).trim()).length > 0))) throw new Error("Activity name is required");
    if (!(((endsAt == null) || (endsAt >= startsAt)))) throw new Error("Activity end must not precede its start");
    if (!(((sortOrder == null) || (sortOrder >= 0)))) throw new Error("Timeline activity sort order cannot be negative");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      eventId: eventId,
      name: name,
      startsAt: startsAt,
      endsAt: endsAt,
      responsibleParty: responsibleParty,
      notes: notes,
      category: category,
      siteNotes: siteNotes,
      sortOrder: ((sortOrder != null) ? sortOrder : 0),
      assigneeTeams: ((assigneeTeams != null) ? assigneeTeams : []),
      assigneePersonIds: ((assigneePersonIds != null) ? assigneePersonIds : []),
      scheduledAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, activityId: docId, tenantId: __after.tenantId, eventId: eventId, name: name, startsAt: startsAt, _subject: { entity: "EventTimelineActivity", command: "schedule", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EventTimelineActivityScheduled", entity: "EventTimelineActivity", entityId: docId, payload: { activityId: docId, tenantId: __after.tenantId, eventId: eventId, name: name, startsAt: startsAt }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const EventTimelineActivity_schedule = mutation({
  args: {
    docId: v.id("eventTimelineActivities"),
    eventId: v.string(),
    name: v.string(),
    startsAt: v.number(),
    endsAt: v.optional(v.number()),
    responsibleParty: v.optional(v.string()),
    notes: v.optional(v.string()),
    category: v.optional(v.string()),
    siteNotes: v.optional(v.string()),
    sortOrder: v.optional(v.any()),
    assigneeTeams: v.optional(v.array(v.string())),
    assigneePersonIds: v.optional(v.array(v.string())),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runEventTimelineActivitySchedule(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "EventTimelineActivity_schedule", __result);
    }
    return __result;
  },
});

export const EventTimelineActivity_createViaSchedule = mutation({
  args: {
    eventId: v.string(),
    name: v.string(),
    startsAt: v.number(),
    endsAt: v.optional(v.number()),
    responsibleParty: v.optional(v.string()),
    notes: v.optional(v.string()),
    category: v.optional(v.string()),
    siteNotes: v.optional(v.string()),
    sortOrder: v.optional(v.any()),
    assigneeTeams: v.optional(v.array(v.string())),
    assigneePersonIds: v.optional(v.array(v.string())),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { eventId, name, startsAt, endsAt, responsibleParty, notes, category, siteNotes, sortOrder, assigneeTeams, assigneePersonIds } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      assigneeTeams: args.assigneeTeams !== undefined ? args.assigneeTeams : [],
      assigneePersonIds: args.assigneePersonIds !== undefined ? args.assigneePersonIds : [],
      sortOrder: args.sortOrder !== undefined ? args.sortOrder : 0,
      createdAt: Date.now(),
      updatedAt: Date.now(),
      category: args.category,
      endsAt: args.endsAt,
      eventId: args.eventId,
      name: args.name,
      notes: args.notes,
      responsibleParty: args.responsibleParty,
      siteNotes: args.siteNotes,
      startsAt: args.startsAt
    };
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may read the event timeline");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may write the event timeline through commands");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may execute event timeline commands");
    if (!((__draft.scheduledAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((((name).trim()).length > 0))) throw new Error("Activity name is required");
    if (!(((endsAt == null) || (endsAt >= startsAt)))) throw new Error("Activity end must not precede its start");
    if (!(((sortOrder == null) || (sortOrder >= 0)))) throw new Error("Timeline activity sort order cannot be negative");
    const doc: Record<string, any> = {
      ...__draft,
      eventId: eventId,
      name: name,
      startsAt: startsAt,
      endsAt: endsAt,
      responsibleParty: responsibleParty,
      notes: notes,
      category: category,
      siteNotes: siteNotes,
      sortOrder: ((sortOrder != null) ? sortOrder : 0),
      assigneeTeams: ((assigneeTeams != null) ? assigneeTeams : []),
      assigneePersonIds: ((assigneePersonIds != null) ? assigneePersonIds : []),
      scheduledAt: Date.now(),
      version: 1,
    };
    const docId = await ctx.db.insert("eventTimelineActivities", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, activityId: docId, tenantId: doc.tenantId, eventId: doc.eventId, name: doc.name, startsAt: doc.startsAt, _subject: { entity: "EventTimelineActivity", command: "schedule", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EventTimelineActivityScheduled", entity: "EventTimelineActivity", entityId: docId, payload: { activityId: docId, tenantId: doc.tenantId, eventId: doc.eventId, name: doc.name, startsAt: doc.startsAt }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "EventTimelineActivity_createViaSchedule", __result);
    }
    return __result;
  },
});

async function __runEventTimelineCommentPost(ctx: MutationCtx, { docId, eventId, authorPersonId, authorName, body, activityId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("EventTimelineComment not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("EventTimelineComment not found");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may read timeline comments");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may write timeline comments through commands");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may execute timeline comment commands");
    if (!((doc.postedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((user.id != null))) throw new Error("Guard 2 failed");
    if (!((((body).trim()).length > 0))) throw new Error("Comment text is required");
    if (!((((authorName).trim()).length > 0))) throw new Error("Author name is required");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      eventId: eventId,
      activityId: activityId,
      authorPersonId: authorPersonId,
      authorName: authorName,
      authorAuthSubjectId: user.id,
      body: body,
      postedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, commentId: docId, tenantId: __after.tenantId, eventId: eventId, authorPersonId: authorPersonId, _subject: { entity: "EventTimelineComment", command: "post", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EventTimelineCommentPosted", entity: "EventTimelineComment", entityId: docId, payload: { commentId: docId, tenantId: __after.tenantId, eventId: eventId, authorPersonId: authorPersonId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const EventTimelineComment_post = mutation({
  args: {
    docId: v.id("eventTimelineComments"),
    eventId: v.string(),
    authorPersonId: v.string(),
    authorName: v.string(),
    body: v.string(),
    activityId: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runEventTimelineCommentPost(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "EventTimelineComment_post", __result);
    }
    return __result;
  },
});

export const EventTimelineComment_createViaPost = mutation({
  args: {
    eventId: v.string(),
    authorPersonId: v.string(),
    authorName: v.string(),
    body: v.string(),
    activityId: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { eventId, authorPersonId, authorName, body, activityId } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      createdAt: Date.now(),
      updatedAt: Date.now(),
      activityId: args.activityId,
      authorName: args.authorName,
      authorPersonId: args.authorPersonId,
      body: args.body,
      eventId: args.eventId
    };
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may read timeline comments");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may write timeline comments through commands");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may execute timeline comment commands");
    if (!((__draft.postedAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((user.id != null))) throw new Error("Guard 2 failed");
    if (!((((body).trim()).length > 0))) throw new Error("Comment text is required");
    if (!((((authorName).trim()).length > 0))) throw new Error("Author name is required");
    const doc: Record<string, any> = {
      ...__draft,
      eventId: eventId,
      activityId: activityId,
      authorPersonId: authorPersonId,
      authorName: authorName,
      authorAuthSubjectId: user.id,
      body: body,
      postedAt: Date.now(),
      version: 1,
    };
    const docId = await ctx.db.insert("eventTimelineComments", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, commentId: docId, tenantId: doc.tenantId, eventId: doc.eventId, authorPersonId: doc.authorPersonId, _subject: { entity: "EventTimelineComment", command: "post", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EventTimelineCommentPosted", entity: "EventTimelineComment", entityId: docId, payload: { commentId: docId, tenantId: doc.tenantId, eventId: doc.eventId, authorPersonId: doc.authorPersonId }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "EventTimelineComment_createViaPost", __result);
    }
    return __result;
  },
});

async function __runEventTimelineCommentRemove(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("EventTimelineComment not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("EventTimelineComment not found");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may read timeline comments");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may write timeline comments through commands");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may execute timeline comment commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!(((doc.authorAuthSubjectId === user.id) || checkRole(user, "adminAccess")))) throw new Error("Guard 1 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      deletedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, commentId: docId, tenantId: __after.tenantId, eventId: __after.eventId, _subject: { entity: "EventTimelineComment", command: "remove", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "EventTimelineCommentRemoved", entity: "EventTimelineComment", entityId: docId, payload: { commentId: docId, tenantId: __after.tenantId, eventId: __after.eventId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const EventTimelineComment_remove = mutation({
  args: {
    docId: v.id("eventTimelineComments"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runEventTimelineCommentRemove(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "EventTimelineComment_remove", __result);
    }
    return __result;
  },
});

async function __runExternalRecordLinkDiscard(ctx: MutationCtx, { docId, reason, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ExternalRecordLink not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ExternalRecordLink not found");
    if (!(checkRole(user, "importAccess"))) throw new Error("Staff may read external record links");
    if (!(checkRole(user, "importAccess"))) throw new Error("Staff may write external record links through commands");
    if (!(checkRole(user, "importAccess"))) throw new Error("Staff may execute external record link commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!((((reason).trim()).length > 0))) throw new Error("Discard reason is required");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      deletedAt: Date.now(),
      effectiveEndDate: ((doc.effectiveEndDate != null) ? doc.effectiveEndDate : Date.now()),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, externalRecordLinkId: docId, tenantId: __after.tenantId, reason: reason, _subject: { entity: "ExternalRecordLink", command: "discard", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ExternalRecordDiscarded", entity: "ExternalRecordLink", entityId: docId, payload: { externalRecordLinkId: docId, tenantId: __after.tenantId, reason: reason }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ExternalRecordLink_discard = mutation({
  args: {
    docId: v.id("externalRecordLinks"),
    reason: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runExternalRecordLinkDiscard(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ExternalRecordLink_discard", __result);
    }
    return __result;
  },
});

export const ExternalRecordLink_createViaDiscard = mutation({
  args: {
    reason: v.string(),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { reason } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      sourceSystem: "tpp_legacy",
      recordType: "",
      externalId: "",
      capsuleEntity: "contact",
      capsuleId: "",
      verified: false,
      conflictStatus: "pending_conflict",
      createdAt: Date.now(),
      updatedAt: Date.now()
    };
    if (!(checkRole(user, "importAccess"))) throw new Error("Staff may read external record links");
    if (!(checkRole(user, "importAccess"))) throw new Error("Staff may write external record links through commands");
    if (!(checkRole(user, "importAccess"))) throw new Error("Staff may execute external record link commands");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!((((reason).trim()).length > 0))) throw new Error("Discard reason is required");
    const doc: Record<string, any> = {
      ...__draft,
      deletedAt: Date.now(),
      effectiveEndDate: ((__draft.effectiveEndDate != null) ? __draft.effectiveEndDate : Date.now()),
      version: 1,
    };
    const docId = await ctx.db.insert("externalRecordLinks", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, externalRecordLinkId: docId, tenantId: doc.tenantId, reason: doc.reason, _subject: { entity: "ExternalRecordLink", command: "discard", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ExternalRecordDiscarded", entity: "ExternalRecordLink", entityId: docId, payload: { externalRecordLinkId: docId, tenantId: doc.tenantId, reason: doc.reason }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ExternalRecordLink_createViaDiscard", __result);
    }
    return __result;
  },
});

async function __runExternalRecordLinkLink(ctx: MutationCtx, { docId, sourceSystem, recordType, externalId, capsuleEntity, capsuleId, verified, verifiedByUserId, sourceImportRunId, effectiveStartDate, effectiveEndDate, rawSourceData, metadata, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ExternalRecordLink not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ExternalRecordLink not found");
    if (!(checkRole(user, "importAccess"))) throw new Error("Staff may read external record links");
    if (!(checkRole(user, "importAccess"))) throw new Error("Staff may write external record links through commands");
    if (!(checkRole(user, "importAccess"))) throw new Error("Staff may execute external record link commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.createdAt == null))) throw new Error("Guard 1 failed");
    if (!((sourceSystem != null))) throw new Error("Source system is required");
    if (!((((externalId).trim()).length > 0))) throw new Error("External ID is required");
    if (!((((capsuleId).trim()).length > 0))) throw new Error("Capsule ID is required");
    if (!(((verified === false) || ((verifiedByUserId != null) && (((verifiedByUserId).trim()).length > 0))))) throw new Error("Verified status requires a verifying user");
    if (!((((effectiveStartDate == null) || (effectiveEndDate == null)) || (effectiveStartDate <= effectiveEndDate)))) throw new Error("Effective start date must be before or equal to end date");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      sourceSystem: sourceSystem,
      recordType: ((recordType != null) ? recordType : ""),
      externalId: externalId,
      capsuleEntity: capsuleEntity,
      capsuleId: capsuleId,
      verified: ((verified != null) ? verified : false),
      verifiedByUserId: verifiedByUserId,
      lastVerifiedAt: (((verified != null) && verified) ? Date.now() : null),
      sourceImportRunId: sourceImportRunId,
      effectiveStartDate: effectiveStartDate,
      effectiveEndDate: effectiveEndDate,
      rawSourceData: rawSourceData,
      metadata: metadata,
      conflictStatus: "pending_conflict",
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, externalRecordLinkId: docId, tenantId: __after.tenantId, sourceSystem: sourceSystem, recordType: ((recordType != null) ? recordType : ""), externalId: externalId, capsuleEntity: capsuleEntity, capsuleId: capsuleId, verified: ((verified != null) ? verified : false), sourceImportRunId: sourceImportRunId, _subject: { entity: "ExternalRecordLink", command: "link", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ExternalRecordLinked", entity: "ExternalRecordLink", entityId: docId, payload: { externalRecordLinkId: docId, tenantId: __after.tenantId, sourceSystem: sourceSystem, recordType: ((recordType != null) ? recordType : ""), externalId: externalId, capsuleEntity: capsuleEntity, capsuleId: capsuleId, verified: ((verified != null) ? verified : false), sourceImportRunId: sourceImportRunId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ExternalRecordLink_link = mutation({
  args: {
    docId: v.id("externalRecordLinks"),
    sourceSystem: v.any(),
    recordType: v.string(),
    externalId: v.string(),
    capsuleEntity: v.any(),
    capsuleId: v.string(),
    verified: v.optional(v.boolean()),
    verifiedByUserId: v.optional(v.string()),
    sourceImportRunId: v.optional(v.string()),
    effectiveStartDate: v.optional(v.number()),
    effectiveEndDate: v.optional(v.number()),
    rawSourceData: v.optional(v.string()),
    metadata: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runExternalRecordLinkLink(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ExternalRecordLink_link", __result);
    }
    return __result;
  },
});

async function __runExternalRecordLinkResolveConflict(ctx: MutationCtx, { docId, conflictStatus, resolvedByUserId, resolutionNote, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ExternalRecordLink not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ExternalRecordLink not found");
    if (!(checkRole(user, "importAccess"))) throw new Error("Staff may read external record links");
    if (!(checkRole(user, "importAccess"))) throw new Error("Staff may write external record links through commands");
    if (!(checkRole(user, "importAccess"))) throw new Error("Staff may execute external record link commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.createdAt != null))) throw new Error("Guard 1 failed");
    if (!((((resolvedByUserId).trim()).length > 0))) throw new Error("Resolving user is required");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      conflictStatus: conflictStatus,
      resolvedByUserId: resolvedByUserId,
      resolutionNote: resolutionNote,
      lastVerifiedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, externalRecordLinkId: docId, tenantId: __after.tenantId, conflictStatus: conflictStatus, resolvedByUserId: resolvedByUserId, _subject: { entity: "ExternalRecordLink", command: "resolveConflict", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ExternalRecordConflictResolved", entity: "ExternalRecordLink", entityId: docId, payload: { externalRecordLinkId: docId, tenantId: __after.tenantId, conflictStatus: conflictStatus, resolvedByUserId: resolvedByUserId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ExternalRecordLink_resolveConflict = mutation({
  args: {
    docId: v.id("externalRecordLinks"),
    conflictStatus: v.any(),
    resolvedByUserId: v.string(),
    resolutionNote: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runExternalRecordLinkResolveConflict(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ExternalRecordLink_resolveConflict", __result);
    }
    return __result;
  },
});

async function __runExternalRecordLinkRetire(ctx: MutationCtx, { docId, effectiveEndDate, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ExternalRecordLink not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ExternalRecordLink not found");
    if (!(checkRole(user, "importAccess"))) throw new Error("Staff may read external record links");
    if (!(checkRole(user, "importAccess"))) throw new Error("Staff may write external record links through commands");
    if (!(checkRole(user, "importAccess"))) throw new Error("Staff may execute external record link commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.createdAt != null))) throw new Error("Guard 1 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      effectiveEndDate: ((effectiveEndDate != null) ? effectiveEndDate : Date.now()),
      conflictStatus: "superseded",
      verified: false,
      lastVerifiedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, externalRecordLinkId: docId, tenantId: __after.tenantId, effectiveEndDate: ((effectiveEndDate != null) ? effectiveEndDate : Date.now()), _subject: { entity: "ExternalRecordLink", command: "retire", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ExternalRecordRetired", entity: "ExternalRecordLink", entityId: docId, payload: { externalRecordLinkId: docId, tenantId: __after.tenantId, effectiveEndDate: ((effectiveEndDate != null) ? effectiveEndDate : Date.now()) }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ExternalRecordLink_retire = mutation({
  args: {
    docId: v.id("externalRecordLinks"),
    effectiveEndDate: v.optional(v.number()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runExternalRecordLinkRetire(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ExternalRecordLink_retire", __result);
    }
    return __result;
  },
});

async function __runExternalRecordLinkUnlinkExternalRecord(ctx: MutationCtx, { docId, reason, unlinkedByUserId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ExternalRecordLink not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ExternalRecordLink not found");
    if (!(checkRole(user, "importAccess"))) throw new Error("Staff may read external record links");
    if (!(checkRole(user, "importAccess"))) throw new Error("Staff may write external record links through commands");
    if (!(checkRole(user, "importAccess"))) throw new Error("Staff may execute external record link commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.createdAt != null))) throw new Error("Guard 1 failed");
    if (!((((reason).trim()).length > 0))) throw new Error("Unlink reason is required");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      deletedAt: Date.now(),
      effectiveEndDate: ((doc.effectiveEndDate != null) ? doc.effectiveEndDate : Date.now()),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, externalRecordLinkId: docId, tenantId: __after.tenantId, reason: reason, unlinkedByUserId: ((unlinkedByUserId != null) ? unlinkedByUserId : ""), capsuleEntity: __after.capsuleEntity, capsuleId: __after.capsuleId, externalId: __after.externalId, sourceSystem: __after.sourceSystem, _subject: { entity: "ExternalRecordLink", command: "unlinkExternalRecord", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ExternalRecordUnlinked", entity: "ExternalRecordLink", entityId: docId, payload: { externalRecordLinkId: docId, tenantId: __after.tenantId, reason: reason, unlinkedByUserId: ((unlinkedByUserId != null) ? unlinkedByUserId : ""), capsuleEntity: __after.capsuleEntity, capsuleId: __after.capsuleId, externalId: __after.externalId, sourceSystem: __after.sourceSystem }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ExternalRecordLink_unlinkExternalRecord = mutation({
  args: {
    docId: v.id("externalRecordLinks"),
    reason: v.string(),
    unlinkedByUserId: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runExternalRecordLinkUnlinkExternalRecord(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ExternalRecordLink_unlinkExternalRecord", __result);
    }
    return __result;
  },
});

async function __runExternalRecordLinkUpdateCapsuleId(ctx: MutationCtx, { docId, capsuleId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ExternalRecordLink not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ExternalRecordLink not found");
    if (!(checkRole(user, "importAccess"))) throw new Error("Staff may read external record links");
    if (!(checkRole(user, "importAccess"))) throw new Error("Staff may write external record links through commands");
    if (!(checkRole(user, "importAccess"))) throw new Error("Staff may execute external record link commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.createdAt != null))) throw new Error("Guard 1 failed");
    if (!((((capsuleId).trim()).length > 0))) throw new Error("Capsule ID is required");
    const previousCapsuleId = doc.capsuleId;
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      capsuleId: capsuleId,
      lastVerifiedAt: Date.now(),
      verified: false,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, externalRecordLinkId: docId, tenantId: __after.tenantId, previousCapsuleId: previousCapsuleId, capsuleId: capsuleId, capsuleEntity: __after.capsuleEntity, _subject: { entity: "ExternalRecordLink", command: "updateCapsuleId", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ExternalRecordCapsuleIdUpdated", entity: "ExternalRecordLink", entityId: docId, payload: { externalRecordLinkId: docId, tenantId: __after.tenantId, previousCapsuleId: previousCapsuleId, capsuleId: capsuleId, capsuleEntity: __after.capsuleEntity }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ExternalRecordLink_updateCapsuleId = mutation({
  args: {
    docId: v.id("externalRecordLinks"),
    capsuleId: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runExternalRecordLinkUpdateCapsuleId(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ExternalRecordLink_updateCapsuleId", __result);
    }
    return __result;
  },
});

async function __runExternalRecordLinkVerifyLink(ctx: MutationCtx, { docId, verifiedByUserId, verified, metadata, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ExternalRecordLink not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ExternalRecordLink not found");
    if (!(checkRole(user, "importAccess"))) throw new Error("Staff may read external record links");
    if (!(checkRole(user, "importAccess"))) throw new Error("Staff may write external record links through commands");
    if (!(checkRole(user, "importAccess"))) throw new Error("Staff may execute external record link commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.createdAt != null))) throw new Error("Guard 1 failed");
    if (!((((verifiedByUserId).trim()).length > 0))) throw new Error("Verifying user is required");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      verified: ((verified != null) ? verified : true),
      verifiedByUserId: verifiedByUserId,
      lastVerifiedAt: Date.now(),
      metadata: ((metadata != null) ? metadata : doc.metadata),
      conflictStatus: (((verified != null) && verified) ? "resolved" : doc.conflictStatus),
      resolvedByUserId: (((verified != null) && verified) ? verifiedByUserId : doc.resolvedByUserId),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, externalRecordLinkId: docId, tenantId: __after.tenantId, verified: ((verified != null) ? verified : true), verifiedByUserId: verifiedByUserId, capsuleEntity: __after.capsuleEntity, capsuleId: __after.capsuleId, _subject: { entity: "ExternalRecordLink", command: "verifyLink", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ExternalRecordVerified", entity: "ExternalRecordLink", entityId: docId, payload: { externalRecordLinkId: docId, tenantId: __after.tenantId, verified: ((verified != null) ? verified : true), verifiedByUserId: verifiedByUserId, capsuleEntity: __after.capsuleEntity, capsuleId: __after.capsuleId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ExternalRecordLink_verifyLink = mutation({
  args: {
    docId: v.id("externalRecordLinks"),
    verifiedByUserId: v.string(),
    verified: v.optional(v.boolean()),
    metadata: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runExternalRecordLinkVerifyLink(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ExternalRecordLink_verifyLink", __result);
    }
    return __result;
  },
});

async function __runImportDatasetActivate(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ImportDataset not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ImportDataset not found");
    if (!(checkRole(user, "importAccess"))) throw new Error("Staff may read import datasets");
    if (!(checkRole(user, "importAccess"))) throw new Error("Staff may write import datasets");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.createdAt != null))) throw new Error("Guard 1 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      active: true,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, importDatasetId: docId, tenantId: __after.tenantId, datasetCategory: __after.datasetCategory, _subject: { entity: "ImportDataset", command: "activate", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ImportDatasetActivated", entity: "ImportDataset", entityId: docId, payload: { importDatasetId: docId, tenantId: __after.tenantId, datasetCategory: __after.datasetCategory }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ImportDataset_activate = mutation({
  args: {
    docId: v.id("importDatasets"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runImportDatasetActivate(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ImportDataset_activate", __result);
    }
    return __result;
  },
});

async function __runImportDatasetDeactivate(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ImportDataset not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ImportDataset not found");
    if (!(checkRole(user, "importAccess"))) throw new Error("Staff may read import datasets");
    if (!(checkRole(user, "importAccess"))) throw new Error("Staff may write import datasets");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.createdAt != null))) throw new Error("Guard 1 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      active: false,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, importDatasetId: docId, tenantId: __after.tenantId, datasetCategory: __after.datasetCategory, _subject: { entity: "ImportDataset", command: "deactivate", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ImportDatasetDeactivated", entity: "ImportDataset", entityId: docId, payload: { importDatasetId: docId, tenantId: __after.tenantId, datasetCategory: __after.datasetCategory }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ImportDataset_deactivate = mutation({
  args: {
    docId: v.id("importDatasets"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runImportDatasetDeactivate(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ImportDataset_deactivate", __result);
    }
    return __result;
  },
});

async function __runImportDatasetRecordLastImport(ctx: MutationCtx, { docId, importRunId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ImportDataset not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ImportDataset not found");
    if (!(checkRole(user, "importAccess"))) throw new Error("Staff may read import datasets");
    if (!(checkRole(user, "importAccess"))) throw new Error("Staff may write import datasets");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.createdAt != null))) throw new Error("Guard 1 failed");
    if (!((((importRunId).trim()).length > 0))) throw new Error("Import run ID is required");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      lastImportRunId: importRunId,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, importDatasetId: docId, tenantId: __after.tenantId, datasetCategory: __after.datasetCategory, importRunId: importRunId, _subject: { entity: "ImportDataset", command: "recordLastImport", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ImportDatasetLastImportRecorded", entity: "ImportDataset", entityId: docId, payload: { importDatasetId: docId, tenantId: __after.tenantId, datasetCategory: __after.datasetCategory, importRunId: importRunId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ImportDataset_recordLastImport = mutation({
  args: {
    docId: v.id("importDatasets"),
    importRunId: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runImportDatasetRecordLastImport(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ImportDataset_recordLastImport", __result);
    }
    return __result;
  },
});

async function __runImportDatasetRegister(ctx: MutationCtx, { docId, datasetCategory, targetEntity, config, name, description, schemaVersion, importOrder, dependsOn, uniqueKeyFields, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ImportDataset not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ImportDataset not found");
    if (!(checkRole(user, "importAccess"))) throw new Error("Staff may read import datasets");
    if (!(checkRole(user, "importAccess"))) throw new Error("Staff may write import datasets");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.createdAt == null))) throw new Error("Guard 1 failed");
    if (!((datasetCategory != null))) throw new Error("Dataset category is required");
    if (!((((config).trim()).length > 0))) throw new Error("Dataset config is required");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      datasetCategory: datasetCategory,
      targetEntity: targetEntity,
      config: config,
      name: name,
      description: description,
      schemaVersion: schemaVersion,
      importOrder: ((importOrder != null) ? importOrder : 1),
      dependsOn: ((dependsOn != null) ? dependsOn : "[]"),
      uniqueKeyFields: ((uniqueKeyFields != null) ? uniqueKeyFields : "[]"),
      active: true,
      fieldMappingCount: 0,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, importDatasetId: docId, tenantId: __after.tenantId, datasetCategory: datasetCategory, targetEntity: targetEntity, name: ((name != null) ? name : ""), _subject: { entity: "ImportDataset", command: "register", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ImportDatasetRegistered", entity: "ImportDataset", entityId: docId, payload: { importDatasetId: docId, tenantId: __after.tenantId, datasetCategory: datasetCategory, targetEntity: targetEntity, name: ((name != null) ? name : "") }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ImportDataset_register = mutation({
  args: {
    docId: v.id("importDatasets"),
    datasetCategory: v.any(),
    targetEntity: v.any(),
    config: v.string(),
    name: v.optional(v.string()),
    description: v.optional(v.string()),
    schemaVersion: v.optional(v.string()),
    importOrder: v.optional(v.number()),
    dependsOn: v.optional(v.string()),
    uniqueKeyFields: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runImportDatasetRegister(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ImportDataset_register", __result);
    }
    return __result;
  },
});

async function __runImportDatasetUpdateConfig(ctx: MutationCtx, { docId, config, fieldMappingCount, name, description, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ImportDataset not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ImportDataset not found");
    if (!(checkRole(user, "importAccess"))) throw new Error("Staff may read import datasets");
    if (!(checkRole(user, "importAccess"))) throw new Error("Staff may write import datasets");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.createdAt != null))) throw new Error("Guard 1 failed");
    if (!((((config).trim()).length > 0))) throw new Error("Dataset config is required");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      config: config,
      fieldMappingCount: ((fieldMappingCount != null) ? fieldMappingCount : doc.fieldMappingCount),
      name: ((name != null) ? name : doc.name),
      description: ((description != null) ? description : doc.description),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, importDatasetId: docId, tenantId: __after.tenantId, datasetCategory: __after.datasetCategory, fieldMappingCount: ((fieldMappingCount != null) ? fieldMappingCount : __after.fieldMappingCount), _subject: { entity: "ImportDataset", command: "updateConfig", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ImportDatasetConfigUpdated", entity: "ImportDataset", entityId: docId, payload: { importDatasetId: docId, tenantId: __after.tenantId, datasetCategory: __after.datasetCategory, fieldMappingCount: ((fieldMappingCount != null) ? fieldMappingCount : __after.fieldMappingCount) }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ImportDataset_updateConfig = mutation({
  args: {
    docId: v.id("importDatasets"),
    config: v.string(),
    fieldMappingCount: v.optional(v.any()),
    name: v.optional(v.string()),
    description: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runImportDatasetUpdateConfig(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ImportDataset_updateConfig", __result);
    }
    return __result;
  },
});

async function __runImportRunApproveReview(ctx: MutationCtx, { docId, finalRecordCounts, actorId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ImportRun not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ImportRun not found");
    if (!(checkRole(user, "importAccess"))) throw new Error("Staff may read import runs");
    if (!(checkRole(user, "importAccess"))) throw new Error("Staff may write import runs through commands");
    if (!(checkRole(user, "importAccess"))) throw new Error("Staff may execute import run commands");
    if (!((doc.status === "reviewing"))) throw new Error("Guard 0 failed");
    if (!((doc.reviewStartedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((finalRecordCounts != null))) throw new Error("Final record counts are required");
    if (!(((actorId == null) || (((actorId).trim()).length > 0)))) throw new Error("Actor id must be non-empty when present");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "committing";
        const __allowed: Record<string, string[]> = { "started": ["parsing", "failed"], "parsing": ["validating", "failed"], "validating": ["reviewing", "failed"], "reviewing": ["committing", "failed"], "committing": ["completed", "failed"], "completed": ["reverted"], "failed": [], "reverted": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      recordCounts: finalRecordCounts,
      actorId: ((actorId != null) ? actorId : doc.actorId),
      reviewApprovedAt: Date.now(),
      status: "committing",
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, importRunId: docId, tenantId: __after.tenantId, finalRecordCounts: finalRecordCounts, actorId: ((actorId != null) ? actorId : __after.actorId), _subject: { entity: "ImportRun", command: "approveReview", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ImportRunReviewApproved", entity: "ImportRun", entityId: docId, payload: { importRunId: docId, tenantId: __after.tenantId, finalRecordCounts: finalRecordCounts, actorId: ((actorId != null) ? actorId : __after.actorId) }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ImportRun_approveReview = mutation({
  args: {
    docId: v.id("importRuns"),
    finalRecordCounts: v.string(),
    actorId: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runImportRunApproveReview(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ImportRun_approveReview", __result);
    }
    return __result;
  },
});

async function __runImportRunBeginReview(ctx: MutationCtx, { docId, actorId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ImportRun not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ImportRun not found");
    if (!(checkRole(user, "importAccess"))) throw new Error("Staff may read import runs");
    if (!(checkRole(user, "importAccess"))) throw new Error("Staff may write import runs through commands");
    if (!(checkRole(user, "importAccess"))) throw new Error("Staff may execute import run commands");
    if (!((doc.status === "validating"))) throw new Error("Guard 0 failed");
    if (!((doc.validatedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!(((actorId == null) || (((actorId).trim()).length > 0)))) throw new Error("Actor id must be non-empty when present");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "reviewing";
        const __allowed: Record<string, string[]> = { "started": ["parsing", "failed"], "parsing": ["validating", "failed"], "validating": ["reviewing", "failed"], "reviewing": ["committing", "failed"], "committing": ["completed", "failed"], "completed": ["reverted"], "failed": [], "reverted": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      actorId: ((actorId != null) ? actorId : doc.actorId),
      reviewStartedAt: Date.now(),
      status: "reviewing",
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, importRunId: docId, tenantId: __after.tenantId, actorId: ((actorId != null) ? actorId : __after.actorId), _subject: { entity: "ImportRun", command: "beginReview", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ImportRunReviewStarted", entity: "ImportRun", entityId: docId, payload: { importRunId: docId, tenantId: __after.tenantId, actorId: ((actorId != null) ? actorId : __after.actorId) }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ImportRun_beginReview = mutation({
  args: {
    docId: v.id("importRuns"),
    actorId: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runImportRunBeginReview(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ImportRun_beginReview", __result);
    }
    return __result;
  },
});

async function __runImportRunCommit(ctx: MutationCtx, { docId, actorId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ImportRun not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ImportRun not found");
    if (!(checkRole(user, "importAccess"))) throw new Error("Staff may read import runs");
    if (!(checkRole(user, "importAccess"))) throw new Error("Staff may write import runs through commands");
    if (!(checkRole(user, "importAccess"))) throw new Error("Staff may execute import run commands");
    if (!((doc.status === "committing"))) throw new Error("Guard 0 failed");
    if (!((doc.reviewApprovedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!(((actorId == null) || (((actorId).trim()).length > 0)))) throw new Error("Actor id must be non-empty when present");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "completed";
        const __allowed: Record<string, string[]> = { "started": ["parsing", "failed"], "parsing": ["validating", "failed"], "validating": ["reviewing", "failed"], "reviewing": ["committing", "failed"], "committing": ["completed", "failed"], "completed": ["reverted"], "failed": [], "reverted": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      actorId: ((actorId != null) ? actorId : doc.actorId),
      commitStartedAt: Date.now(),
      completionTime: Date.now(),
      endTime: Date.now(),
      status: "completed",
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, importRunId: docId, tenantId: __after.tenantId, recordCounts: __after.recordCounts, actorId: ((actorId != null) ? actorId : __after.actorId), _subject: { entity: "ImportRun", command: "commit", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ImportRunCommitted", entity: "ImportRun", entityId: docId, payload: { importRunId: docId, tenantId: __after.tenantId, recordCounts: __after.recordCounts, actorId: ((actorId != null) ? actorId : __after.actorId) }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ImportRun_commit = mutation({
  args: {
    docId: v.id("importRuns"),
    actorId: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runImportRunCommit(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ImportRun_commit", __result);
    }
    return __result;
  },
});

async function __runImportRunMarkFailed(ctx: MutationCtx, { docId, failureDetails, actorId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ImportRun not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ImportRun not found");
    if (!(checkRole(user, "importAccess"))) throw new Error("Staff may read import runs");
    if (!(checkRole(user, "importAccess"))) throw new Error("Staff may write import runs through commands");
    if (!(checkRole(user, "importAccess"))) throw new Error("Staff may execute import run commands");
    if (!((doc.status !== "completed"))) throw new Error("Guard 0 failed");
    if (!((doc.status !== "reverted"))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((((failureDetails).trim()).length > 0))) throw new Error("Failure details are required");
    if (!(((actorId == null) || (((actorId).trim()).length > 0)))) throw new Error("Actor id must be non-empty when present");
    const previousStatus = doc.status;
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "failed";
        const __allowed: Record<string, string[]> = { "started": ["parsing", "failed"], "parsing": ["validating", "failed"], "validating": ["reviewing", "failed"], "reviewing": ["committing", "failed"], "committing": ["completed", "failed"], "completed": ["reverted"], "failed": [], "reverted": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      failureDetails: failureDetails,
      actorId: ((actorId != null) ? actorId : doc.actorId),
      endTime: Date.now(),
      status: "failed",
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, importRunId: docId, tenantId: __after.tenantId, previousStatus: previousStatus, failureDetails: failureDetails, actorId: ((actorId != null) ? actorId : __after.actorId), _subject: { entity: "ImportRun", command: "markFailed", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ImportRunFailed", entity: "ImportRun", entityId: docId, payload: { importRunId: docId, tenantId: __after.tenantId, previousStatus: previousStatus, failureDetails: failureDetails, actorId: ((actorId != null) ? actorId : __after.actorId) }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ImportRun_markFailed = mutation({
  args: {
    docId: v.id("importRuns"),
    failureDetails: v.string(),
    actorId: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runImportRunMarkFailed(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ImportRun_markFailed", __result);
    }
    return __result;
  },
});

async function __runImportRunRecordParse(ctx: MutationCtx, { docId, recordCounts, actorId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ImportRun not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ImportRun not found");
    if (!(checkRole(user, "importAccess"))) throw new Error("Staff may read import runs");
    if (!(checkRole(user, "importAccess"))) throw new Error("Staff may write import runs through commands");
    if (!(checkRole(user, "importAccess"))) throw new Error("Staff may execute import run commands");
    if (!((doc.status === "started"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((recordCounts != null))) throw new Error("Record counts are required");
    if (!(((actorId == null) || (((actorId).trim()).length > 0)))) throw new Error("Actor id must be non-empty when present");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "parsing";
        const __allowed: Record<string, string[]> = { "started": ["parsing", "failed"], "parsing": ["validating", "failed"], "validating": ["reviewing", "failed"], "reviewing": ["committing", "failed"], "committing": ["completed", "failed"], "completed": ["reverted"], "failed": [], "reverted": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      recordCounts: recordCounts,
      actorId: ((actorId != null) ? actorId : doc.actorId),
      parsedAt: Date.now(),
      status: "parsing",
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, importRunId: docId, tenantId: __after.tenantId, recordCounts: recordCounts, actorId: ((actorId != null) ? actorId : __after.actorId), _subject: { entity: "ImportRun", command: "recordParse", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ImportRunParsed", entity: "ImportRun", entityId: docId, payload: { importRunId: docId, tenantId: __after.tenantId, recordCounts: recordCounts, actorId: ((actorId != null) ? actorId : __after.actorId) }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ImportRun_recordParse = mutation({
  args: {
    docId: v.id("importRuns"),
    recordCounts: v.string(),
    actorId: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runImportRunRecordParse(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ImportRun_recordParse", __result);
    }
    return __result;
  },
});

async function __runImportRunRevert(ctx: MutationCtx, { docId, actorId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ImportRun not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ImportRun not found");
    if (!(checkRole(user, "importAccess"))) throw new Error("Staff may read import runs");
    if (!(checkRole(user, "importAccess"))) throw new Error("Staff may write import runs through commands");
    if (!(checkRole(user, "importAccess"))) throw new Error("Staff may execute import run commands");
    if (!((doc.status === "completed"))) throw new Error("Guard 0 failed");
    if (!((doc.completionTime != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!(((actorId == null) || (((actorId).trim()).length > 0)))) throw new Error("Actor id must be non-empty when present");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "reverted";
        const __allowed: Record<string, string[]> = { "started": ["parsing", "failed"], "parsing": ["validating", "failed"], "validating": ["reviewing", "failed"], "reviewing": ["committing", "failed"], "committing": ["completed", "failed"], "completed": ["reverted"], "failed": [], "reverted": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      actorId: ((actorId != null) ? actorId : doc.actorId),
      revertedAt: Date.now(),
      endTime: Date.now(),
      status: "reverted",
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, importRunId: docId, tenantId: __after.tenantId, actorId: ((actorId != null) ? actorId : __after.actorId), _subject: { entity: "ImportRun", command: "revert", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ImportRunReverted", entity: "ImportRun", entityId: docId, payload: { importRunId: docId, tenantId: __after.tenantId, actorId: ((actorId != null) ? actorId : __after.actorId) }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ImportRun_revert = mutation({
  args: {
    docId: v.id("importRuns"),
    actorId: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runImportRunRevert(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ImportRun_revert", __result);
    }
    return __result;
  },
});

async function __runImportRunStart(ctx: MutationCtx, { docId, sourceSystem, datasetType, actorId, checksum, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ImportRun not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ImportRun not found");
    if (!(checkRole(user, "importAccess"))) throw new Error("Staff may read import runs");
    if (!(checkRole(user, "importAccess"))) throw new Error("Staff may write import runs through commands");
    if (!(checkRole(user, "importAccess"))) throw new Error("Staff may execute import run commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.status === "started"))) throw new Error("Guard 1 failed");
    if (!(((actorId == null) || (((actorId).trim()).length > 0)))) throw new Error("Actor id must be non-empty when present");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "started";
        const __allowed: Record<string, string[]> = { "started": ["parsing", "failed"], "parsing": ["validating", "failed"], "validating": ["reviewing", "failed"], "reviewing": ["committing", "failed"], "committing": ["completed", "failed"], "completed": ["reverted"], "failed": [], "reverted": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      sourceSystem: sourceSystem,
      datasetType: datasetType,
      actorId: ((actorId != null) ? actorId : ""),
      checksum: checksum,
      startTime: Date.now(),
      status: "started",
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, importRunId: docId, tenantId: __after.tenantId, sourceSystem: sourceSystem, datasetType: datasetType, actorId: ((actorId != null) ? actorId : ""), _subject: { entity: "ImportRun", command: "start", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ImportRunStarted", entity: "ImportRun", entityId: docId, payload: { importRunId: docId, tenantId: __after.tenantId, sourceSystem: sourceSystem, datasetType: datasetType, actorId: ((actorId != null) ? actorId : "") }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ImportRun_start = mutation({
  args: {
    docId: v.id("importRuns"),
    sourceSystem: v.any(),
    datasetType: v.any(),
    actorId: v.optional(v.string()),
    checksum: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runImportRunStart(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ImportRun_start", __result);
    }
    return __result;
  },
});

async function __runImportRunValidate(ctx: MutationCtx, { docId, actorId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ImportRun not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ImportRun not found");
    if (!(checkRole(user, "importAccess"))) throw new Error("Staff may read import runs");
    if (!(checkRole(user, "importAccess"))) throw new Error("Staff may write import runs through commands");
    if (!(checkRole(user, "importAccess"))) throw new Error("Staff may execute import run commands");
    if (!((doc.status === "parsing"))) throw new Error("Guard 0 failed");
    if (!((doc.parsedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!(((actorId == null) || (((actorId).trim()).length > 0)))) throw new Error("Actor id must be non-empty when present");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "validating";
        const __allowed: Record<string, string[]> = { "started": ["parsing", "failed"], "parsing": ["validating", "failed"], "validating": ["reviewing", "failed"], "reviewing": ["committing", "failed"], "committing": ["completed", "failed"], "completed": ["reverted"], "failed": [], "reverted": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      actorId: ((actorId != null) ? actorId : doc.actorId),
      validatedAt: Date.now(),
      status: "validating",
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, importRunId: docId, tenantId: __after.tenantId, actorId: ((actorId != null) ? actorId : __after.actorId), _subject: { entity: "ImportRun", command: "validate", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ImportRunValidated", entity: "ImportRun", entityId: docId, payload: { importRunId: docId, tenantId: __after.tenantId, actorId: ((actorId != null) ? actorId : __after.actorId) }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ImportRun_validate = mutation({
  args: {
    docId: v.id("importRuns"),
    actorId: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runImportRunValidate(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ImportRun_validate", __result);
    }
    return __result;
  },
});

async function __runIncidentBeginInvestigation(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Incident not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Incident not found");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "kitchenAccess")))) throw new Error("Event and kitchen staff may read incidents");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "kitchenAccess")))) throw new Error("Event and kitchen staff may write incidents through commands");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "kitchenAccess")))) throw new Error("Event and kitchen staff may execute incident commands");
    if (!((doc.status === "open"))) throw new Error("Guard 0 failed");
    if (!((doc.reportedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    const previousStatus = doc.status;
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "investigating";
        const __allowed: Record<string, string[]> = { "open": ["investigating", "resolved", "dismissed"], "investigating": ["resolved", "dismissed"], "resolved": [], "dismissed": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "investigating",
      investigatingAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, incidentId: docId, tenantId: __after.tenantId, eventId: __after.eventId, severity: __after.severity, previousStatus: previousStatus, status: "investigating", _subject: { entity: "Incident", command: "beginInvestigation", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "IncidentInvestigationStarted", entity: "Incident", entityId: docId, payload: { incidentId: docId, tenantId: __after.tenantId, eventId: __after.eventId, severity: __after.severity, previousStatus: previousStatus, status: "investigating" }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Incident_beginInvestigation = mutation({
  args: {
    docId: v.id("incidents"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runIncidentBeginInvestigation(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Incident_beginInvestigation", __result);
    }
    return __result;
  },
});

async function __runIncidentClearCorrectiveActionLock(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Incident not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Incident not found");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "kitchenAccess")))) throw new Error("Event and kitchen staff may read incidents");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "kitchenAccess")))) throw new Error("Event and kitchen staff may write incidents through commands");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "kitchenAccess")))) throw new Error("Event and kitchen staff may execute incident commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      correctiveActionRequired: false,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, incidentId: docId, tenantId: __after.tenantId, eventId: __after.eventId, severity: __after.severity, status: __after.status, _subject: { entity: "Incident", command: "clearCorrectiveActionLock", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "IncidentCorrectiveActionCleared", entity: "Incident", entityId: docId, payload: { incidentId: docId, tenantId: __after.tenantId, eventId: __after.eventId, severity: __after.severity, status: __after.status }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Incident_clearCorrectiveActionLock = mutation({
  args: {
    docId: v.id("incidents"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runIncidentClearCorrectiveActionLock(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Incident_clearCorrectiveActionLock", __result);
    }
    return __result;
  },
});

async function __runIncidentDismiss(ctx: MutationCtx, { docId, reason, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Incident not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Incident not found");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "kitchenAccess")))) throw new Error("Event and kitchen staff may read incidents");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "kitchenAccess")))) throw new Error("Event and kitchen staff may write incidents through commands");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "kitchenAccess")))) throw new Error("Event and kitchen staff may execute incident commands");
    if (!(((doc.status === "open") || (doc.status === "investigating")))) throw new Error("Guard 0 failed");
    if (!((doc.reportedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((checkRole(user, "eventManageAccess") || checkRole(user, "kitchenManageAccess")))) throw new Error("Guard 3 failed");
    if (!((doc.correctiveActionRequired !== true))) throw new Error("Allergen incidents stay locked until the corrective action is closed");
    if (!((((reason).trim()).length > 0))) throw new Error("Dismissal reason is required");
    const previousStatus = doc.status;
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "dismissed";
        const __allowed: Record<string, string[]> = { "open": ["investigating", "resolved", "dismissed"], "investigating": ["resolved", "dismissed"], "resolved": [], "dismissed": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      dismissalReason: reason,
      status: "dismissed",
      dismissedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, incidentId: docId, tenantId: __after.tenantId, eventId: __after.eventId, severity: __after.severity, previousStatus: previousStatus, status: "dismissed", reason: reason, _subject: { entity: "Incident", command: "dismiss", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "IncidentDismissed", entity: "Incident", entityId: docId, payload: { incidentId: docId, tenantId: __after.tenantId, eventId: __after.eventId, severity: __after.severity, previousStatus: previousStatus, status: "dismissed", reason: reason }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Incident_dismiss = mutation({
  args: {
    docId: v.id("incidents"),
    reason: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runIncidentDismiss(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Incident_dismiss", __result);
    }
    return __result;
  },
});

async function __runIncidentMarkResolved(ctx: MutationCtx, { docId, resolution, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Incident not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Incident not found");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "kitchenAccess")))) throw new Error("Event and kitchen staff may read incidents");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "kitchenAccess")))) throw new Error("Event and kitchen staff may write incidents through commands");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "kitchenAccess")))) throw new Error("Event and kitchen staff may execute incident commands");
    if (!(((doc.status === "open") || (doc.status === "investigating")))) throw new Error("Guard 0 failed");
    if (!((doc.reportedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((doc.correctiveActionRequired !== true))) throw new Error("Allergen incidents stay locked until the corrective action is closed");
    if (!((((resolution).trim()).length > 0))) throw new Error("Resolution is required");
    const previousStatus = doc.status;
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "resolved";
        const __allowed: Record<string, string[]> = { "open": ["investigating", "resolved", "dismissed"], "investigating": ["resolved", "dismissed"], "resolved": [], "dismissed": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      resolution: resolution,
      status: "resolved",
      resolvedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, incidentId: docId, tenantId: __after.tenantId, eventId: __after.eventId, severity: __after.severity, previousStatus: previousStatus, status: "resolved", resolution: resolution, _subject: { entity: "Incident", command: "markResolved", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "IncidentResolved", entity: "Incident", entityId: docId, payload: { incidentId: docId, tenantId: __after.tenantId, eventId: __after.eventId, severity: __after.severity, previousStatus: previousStatus, status: "resolved", resolution: resolution }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Incident_markResolved = mutation({
  args: {
    docId: v.id("incidents"),
    resolution: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runIncidentMarkResolved(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Incident_markResolved", __result);
    }
    return __result;
  },
});

async function __runIncidentReport(ctx: MutationCtx, { docId, eventId, severity, category, description, prepTaskId, deliveryId, shiftId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Incident not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Incident not found");
    const __rel_event = await __resolveRelation(ctx, "events", [__auth.tenantId, doc.eventId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).eventId;
      ((doc as any) as any).event = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!((checkRole(user, "eventAccess") || checkRole(user, "kitchenAccess")))) throw new Error("Event and kitchen staff may read incidents");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "kitchenAccess")))) throw new Error("Event and kitchen staff may write incidents through commands");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "kitchenAccess")))) throw new Error("Event and kitchen staff may execute incident commands");
    if (!((doc.reportedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.status === "open"))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((__rel_event != null))) throw new Error("Guard 3 failed");
    if (!((user.id != null))) throw new Error("Guard 4 failed");
    if (!((eventId === doc.eventId))) throw new Error("Report eventId must match the seeded event reference");
    if (!((((prepTaskId == null) || (doc.prepTaskId == null)) || (prepTaskId === doc.prepTaskId)))) throw new Error("Report prepTaskId must match the seeded prep task reference when provided");
    if (!((((deliveryId == null) || (doc.deliveryId == null)) || (deliveryId === doc.deliveryId)))) throw new Error("Report deliveryId must match the seeded delivery reference when provided");
    if (!((((shiftId == null) || (doc.shiftId == null)) || (shiftId === doc.shiftId)))) throw new Error("Report shiftId must match the seeded shift reference when provided");
    if (!((((description).trim()).length > 0))) throw new Error("Incident description is required");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      eventId: eventId,
      prepTaskId: ((prepTaskId != null) ? prepTaskId : doc.prepTaskId),
      deliveryId: ((deliveryId != null) ? deliveryId : doc.deliveryId),
      shiftId: ((shiftId != null) ? shiftId : doc.shiftId),
      severity: severity,
      category: category,
      description: description,
      correctiveActionRequired: (category === "allergen"),
      reportedById: user.id,
      reportedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, incidentId: docId, tenantId: __after.tenantId, eventId: eventId, severity: severity, category: category, reportedById: user.id, status: "open", _subject: { entity: "Incident", command: "report", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "IncidentReported", entity: "Incident", entityId: docId, payload: { incidentId: docId, tenantId: __after.tenantId, eventId: eventId, severity: severity, category: category, reportedById: user.id, status: "open" }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Incident_report = mutation({
  args: {
    docId: v.id("incidents"),
    eventId: v.string(),
    severity: v.any(),
    category: v.any(),
    description: v.string(),
    prepTaskId: v.optional(v.string()),
    deliveryId: v.optional(v.string()),
    shiftId: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runIncidentReport(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Incident_report", __result);
    }
    return __result;
  },
});

export const Incident_createViaReport = mutation({
  args: {
    eventId: v.string(),
    severity: v.any(),
    category: v.any(),
    description: v.string(),
    prepTaskId: v.optional(v.string()),
    deliveryId: v.optional(v.string()),
    shiftId: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { eventId, severity, category, description, prepTaskId, deliveryId, shiftId } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      status: "open",
      createdAt: Date.now(),
      updatedAt: Date.now(),
      category: args.category,
      deliveryId: args.deliveryId,
      description: args.description,
      eventId: args.eventId,
      prepTaskId: args.prepTaskId,
      severity: args.severity,
      shiftId: args.shiftId
    };
    const __rel_event = await __resolveRelation(ctx, "events", [__auth.tenantId, __draft.eventId], ["tenantId","id"], "tenantId", __auth.tenantId);
    if (!((checkRole(user, "eventAccess") || checkRole(user, "kitchenAccess")))) throw new Error("Event and kitchen staff may read incidents");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "kitchenAccess")))) throw new Error("Event and kitchen staff may write incidents through commands");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "kitchenAccess")))) throw new Error("Event and kitchen staff may execute incident commands");
    if (!((__draft.reportedAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.status === "open"))) throw new Error("Guard 1 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((__rel_event != null))) throw new Error("Guard 3 failed");
    if (!((user.id != null))) throw new Error("Guard 4 failed");
    if (!((eventId === __draft.eventId))) throw new Error("Report eventId must match the seeded event reference");
    if (!((((prepTaskId == null) || (__draft.prepTaskId == null)) || (prepTaskId === __draft.prepTaskId)))) throw new Error("Report prepTaskId must match the seeded prep task reference when provided");
    if (!((((deliveryId == null) || (__draft.deliveryId == null)) || (deliveryId === __draft.deliveryId)))) throw new Error("Report deliveryId must match the seeded delivery reference when provided");
    if (!((((shiftId == null) || (__draft.shiftId == null)) || (shiftId === __draft.shiftId)))) throw new Error("Report shiftId must match the seeded shift reference when provided");
    if (!((((description).trim()).length > 0))) throw new Error("Incident description is required");
    const doc: Record<string, any> = {
      ...__draft,
      eventId: eventId,
      prepTaskId: ((prepTaskId != null) ? prepTaskId : __draft.prepTaskId),
      deliveryId: ((deliveryId != null) ? deliveryId : __draft.deliveryId),
      shiftId: ((shiftId != null) ? shiftId : __draft.shiftId),
      severity: severity,
      category: category,
      description: description,
      correctiveActionRequired: (category === "allergen"),
      reportedById: user.id,
      reportedAt: Date.now(),
      version: 1,
    };
    const docId = await ctx.db.insert("incidents", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, incidentId: docId, tenantId: doc.tenantId, eventId: doc.eventId, severity: doc.severity, category: doc.category, reportedById: user.id, status: "open", _subject: { entity: "Incident", command: "report", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "IncidentReported", entity: "Incident", entityId: docId, payload: { incidentId: docId, tenantId: doc.tenantId, eventId: doc.eventId, severity: doc.severity, category: doc.category, reportedById: user.id, status: "open" }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Incident_createViaReport", __result);
    }
    return __result;
  },
});

async function __runIngredientClassifyAllergens(ctx: MutationCtx, { docId, allergens, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Ingredient not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Ingredient not found");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may read ingredients");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write ingredients through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute ingredient commands");
    if (!((doc.status === "active"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      allergens: allergens,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, ingredientId: docId, tenantId: __after.tenantId, allergens: allergens, _subject: { entity: "Ingredient", command: "classifyAllergens", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "IngredientAllergensClassified", entity: "Ingredient", entityId: docId, payload: { ingredientId: docId, tenantId: __after.tenantId, allergens: allergens }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Ingredient_classifyAllergens = mutation({
  args: {
    docId: v.id("ingredients"),
    allergens: v.array(v.any()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runIngredientClassifyAllergens(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Ingredient_classifyAllergens", __result);
    }
    return __result;
  },
});

async function __runIngredientConfigureSubstitutes(ctx: MutationCtx, { docId, substituteIngredientIds, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Ingredient not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Ingredient not found");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may read ingredients");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write ingredients through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute ingredient commands");
    if (!((doc.introducedAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      substituteIngredientIds: substituteIngredientIds,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    return { ...doc, ...updates };
}

export const Ingredient_configureSubstitutes = mutation({
  args: {
    docId: v.id("ingredients"),
    substituteIngredientIds: v.array(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runIngredientConfigureSubstitutes(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Ingredient_configureSubstitutes", __result);
    }
    return __result;
  },
});

async function __runIngredientDiscontinue(ctx: MutationCtx, { docId, reason, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Ingredient not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Ingredient not found");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may read ingredients");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write ingredients through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute ingredient commands");
    if (!((doc.status === "active"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(checkRole(user, "kitchenManageAccess"))) throw new Error("Guard 2 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "discontinued";
        const __allowed: Record<string, string[]> = { "active": ["discontinued"], "discontinued": ["active"] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "discontinued",
      discontinuedAt: Date.now(),
      discontinuationReason: ((reason != null) ? reason : "Deleted"),
      deletedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, ingredientId: docId, tenantId: __after.tenantId, reason: ((reason != null) ? reason : "Deleted"), _subject: { entity: "Ingredient", command: "discontinue", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "IngredientDiscontinued", entity: "Ingredient", entityId: docId, payload: { ingredientId: docId, tenantId: __after.tenantId, reason: ((reason != null) ? reason : "Deleted") }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Ingredient_discontinue = mutation({
  args: {
    docId: v.id("ingredients"),
    reason: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runIngredientDiscontinue(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Ingredient_discontinue", __result);
    }
    return __result;
  },
});

async function __runIngredientIntroduce(ctx: MutationCtx, { docId, name, unit, costPerUnit, allergens, category, preferredVendorId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Ingredient not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Ingredient not found");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may read ingredients");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write ingredients through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute ingredient commands");
    if (!((doc.introducedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((((name).trim()).length > 0))) throw new Error("Ingredient name is required");
    if (!((costPerUnit >= 0))) throw new Error("Ingredient cost per unit cannot be negative");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      name: name,
      unit: unit,
      costPerUnit: costPerUnit,
      allergens: ((allergens != null) ? allergens : []),
      category: category,
      preferredVendorId: preferredVendorId,
      introducedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, ingredientId: docId, tenantId: __after.tenantId, name: name, unit: unit, costPerUnit: costPerUnit, allergens: ((allergens != null) ? allergens : []), _subject: { entity: "Ingredient", command: "introduce", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "IngredientIntroduced", entity: "Ingredient", entityId: docId, payload: { ingredientId: docId, tenantId: __after.tenantId, name: name, unit: unit, costPerUnit: costPerUnit, allergens: ((allergens != null) ? allergens : []) }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Ingredient_introduce = mutation({
  args: {
    docId: v.id("ingredients"),
    name: v.string(),
    unit: v.any(),
    costPerUnit: v.number(),
    allergens: v.optional(v.array(v.any())),
    category: v.optional(v.string()),
    preferredVendorId: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runIngredientIntroduce(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Ingredient_introduce", __result);
    }
    return __result;
  },
});

export const Ingredient_createViaIntroduce = mutation({
  args: {
    name: v.string(),
    unit: v.any(),
    costPerUnit: v.number(),
    allergens: v.optional(v.array(v.any())),
    category: v.optional(v.string()),
    preferredVendorId: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { name, unit, costPerUnit, allergens, category, preferredVendorId } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      allergens: args.allergens !== undefined ? args.allergens : [],
      substituteIngredientIds: [],
      preferredVendorIds: [],
      status: "active",
      editionNumber: 1,
      createdAt: Date.now(),
      updatedAt: Date.now(),
      category: args.category,
      costPerUnit: args.costPerUnit,
      name: args.name,
      preferredVendorId: args.preferredVendorId,
      unit: args.unit
    };
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may read ingredients");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write ingredients through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute ingredient commands");
    if (!((__draft.introducedAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((((name).trim()).length > 0))) throw new Error("Ingredient name is required");
    if (!((costPerUnit >= 0))) throw new Error("Ingredient cost per unit cannot be negative");
    const doc: Record<string, any> = {
      ...__draft,
      name: name,
      unit: unit,
      costPerUnit: costPerUnit,
      allergens: ((allergens != null) ? allergens : []),
      category: category,
      preferredVendorId: preferredVendorId,
      introducedAt: Date.now(),
      version: 1,
    };
    const docId = await ctx.db.insert("ingredients", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, ingredientId: docId, tenantId: doc.tenantId, name: doc.name, unit: doc.unit, costPerUnit: doc.costPerUnit, allergens: ((doc.allergens != null) ? doc.allergens : []), _subject: { entity: "Ingredient", command: "introduce", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "IngredientIntroduced", entity: "Ingredient", entityId: docId, payload: { ingredientId: docId, tenantId: doc.tenantId, name: doc.name, unit: doc.unit, costPerUnit: doc.costPerUnit, allergens: ((doc.allergens != null) ? doc.allergens : []) }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Ingredient_createViaIntroduce", __result);
    }
    return __result;
  },
});

async function __runIngredientLinkAsEdition(ctx: MutationCtx, { docId, sourceIngredientId, editionNumber, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Ingredient not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Ingredient not found");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may read ingredients");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write ingredients through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute ingredient commands");
    if (!((doc.introducedAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((doc.mergedIntoIngredientId == null))) throw new Error("Guard 2 failed");
    if (!((sourceIngredientId !== doc.id))) throw new Error("An ingredient cannot be an edition of itself");
    if (!(((editionNumber == null) || (editionNumber > 0)))) throw new Error("Edition number must be positive");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      canonicalIngredientId: sourceIngredientId,
      editionNumber: ((editionNumber != null) ? editionNumber : doc.editionNumber),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, ingredientId: docId, tenantId: __after.tenantId, sourceIngredientId: sourceIngredientId, name: __after.name, editionNumber: ((editionNumber != null) ? editionNumber : __after.editionNumber), _subject: { entity: "Ingredient", command: "linkAsEdition", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "IngredientEditionCreated", entity: "Ingredient", entityId: docId, payload: { ingredientId: docId, tenantId: __after.tenantId, sourceIngredientId: sourceIngredientId, name: __after.name, editionNumber: ((editionNumber != null) ? editionNumber : __after.editionNumber) }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Ingredient_linkAsEdition = mutation({
  args: {
    docId: v.id("ingredients"),
    sourceIngredientId: v.string(),
    editionNumber: v.optional(v.number()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runIngredientLinkAsEdition(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Ingredient_linkAsEdition", __result);
    }
    return __result;
  },
});

async function __runIngredientMergeInto(ctx: MutationCtx, { docId, targetIngredientId, reason, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Ingredient not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Ingredient not found");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may read ingredients");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write ingredients through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute ingredient commands");
    if (!((doc.status === "active"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((doc.mergedIntoIngredientId == null))) throw new Error("Guard 2 failed");
    if (!((targetIngredientId !== doc.id))) throw new Error("Cannot merge an ingredient into itself");
    if (!((((reason).trim()).length > 0))) throw new Error("Merge reason is required");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "discontinued";
        const __allowed: Record<string, string[]> = { "active": ["discontinued"], "discontinued": ["active"] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      mergedIntoIngredientId: targetIngredientId,
      status: "discontinued",
      discontinuedAt: Date.now(),
      discontinuationReason: reason,
      deletedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, ingredientId: docId, tenantId: __after.tenantId, targetIngredientId: targetIngredientId, reason: reason, _subject: { entity: "Ingredient", command: "mergeInto", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "IngredientMerged", entity: "Ingredient", entityId: docId, payload: { ingredientId: docId, tenantId: __after.tenantId, targetIngredientId: targetIngredientId, reason: reason }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Ingredient_mergeInto = mutation({
  args: {
    docId: v.id("ingredients"),
    targetIngredientId: v.string(),
    reason: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runIngredientMergeInto(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Ingredient_mergeInto", __result);
    }
    return __result;
  },
});

async function __runIngredientPurge(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Ingredient not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Ingredient not found");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may read ingredients");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write ingredients through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute ingredient commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!(checkRole(user, "kitchenManageAccess"))) throw new Error("Guard 1 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "discontinued";
        const __allowed: Record<string, string[]> = { "active": ["discontinued"], "discontinued": ["active"] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "discontinued",
      discontinuedAt: ((doc.discontinuedAt != null) ? doc.discontinuedAt : Date.now()),
      discontinuationReason: "Deleted",
      deletedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, ingredientId: docId, tenantId: __after.tenantId, reason: "Deleted", _subject: { entity: "Ingredient", command: "purge", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "IngredientPurged", entity: "Ingredient", entityId: docId, payload: { ingredientId: docId, tenantId: __after.tenantId, reason: "Deleted" }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Ingredient_purge = mutation({
  args: {
    docId: v.id("ingredients"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runIngredientPurge(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Ingredient_purge", __result);
    }
    return __result;
  },
});

async function __runIngredientReinstate(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Ingredient not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Ingredient not found");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may read ingredients");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write ingredients through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute ingredient commands");
    if (!((doc.status === "discontinued"))) throw new Error("Guard 0 failed");
    if (!(checkRole(user, "kitchenManageAccess"))) throw new Error("Guard 1 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "active";
        const __allowed: Record<string, string[]> = { "active": ["discontinued"], "discontinued": ["active"] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "active",
      discontinuedAt: null,
      discontinuationReason: null,
      deletedAt: null,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, ingredientId: docId, tenantId: __after.tenantId, _subject: { entity: "Ingredient", command: "reinstate", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "IngredientReinstated", entity: "Ingredient", entityId: docId, payload: { ingredientId: docId, tenantId: __after.tenantId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Ingredient_reinstate = mutation({
  args: {
    docId: v.id("ingredients"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runIngredientReinstate(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Ingredient_reinstate", __result);
    }
    return __result;
  },
});

async function __runIngredientSetNutrition(ctx: MutationCtx, { docId, caloriesPerUnit, proteinGramsPerUnit, carbsGramsPerUnit, fatGramsPerUnit, fiberGramsPerUnit, sugarGramsPerUnit, sodiumMgPerUnit, calciumMgPerUnit, ironMgPerUnit, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Ingredient not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Ingredient not found");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may read ingredients");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write ingredients through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute ingredient commands");
    if (!((doc.status === "active"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(((caloriesPerUnit == null) || (caloriesPerUnit >= 0)))) throw new Error("Calories cannot be negative");
    if (!(((proteinGramsPerUnit == null) || (proteinGramsPerUnit >= 0)))) throw new Error("Protein cannot be negative");
    if (!(((carbsGramsPerUnit == null) || (carbsGramsPerUnit >= 0)))) throw new Error("Carbohydrates cannot be negative");
    if (!(((fatGramsPerUnit == null) || (fatGramsPerUnit >= 0)))) throw new Error("Fat cannot be negative");
    if (!(((fiberGramsPerUnit == null) || (fiberGramsPerUnit >= 0)))) throw new Error("Fiber cannot be negative");
    if (!(((sugarGramsPerUnit == null) || (sugarGramsPerUnit >= 0)))) throw new Error("Sugar cannot be negative");
    if (!(((sodiumMgPerUnit == null) || (sodiumMgPerUnit >= 0)))) throw new Error("Sodium cannot be negative");
    if (!(((calciumMgPerUnit == null) || (calciumMgPerUnit >= 0)))) throw new Error("Calcium cannot be negative");
    if (!(((ironMgPerUnit == null) || (ironMgPerUnit >= 0)))) throw new Error("Iron cannot be negative");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      caloriesPerUnit: caloriesPerUnit,
      proteinGramsPerUnit: proteinGramsPerUnit,
      carbsGramsPerUnit: carbsGramsPerUnit,
      fatGramsPerUnit: fatGramsPerUnit,
      fiberGramsPerUnit: fiberGramsPerUnit,
      sugarGramsPerUnit: sugarGramsPerUnit,
      sodiumMgPerUnit: sodiumMgPerUnit,
      calciumMgPerUnit: calciumMgPerUnit,
      ironMgPerUnit: ironMgPerUnit,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, ingredientId: docId, tenantId: __after.tenantId, _subject: { entity: "Ingredient", command: "setNutrition", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "IngredientNutritionSet", entity: "Ingredient", entityId: docId, payload: { ingredientId: docId, tenantId: __after.tenantId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Ingredient_setNutrition = mutation({
  args: {
    docId: v.id("ingredients"),
    caloriesPerUnit: v.optional(v.number()),
    proteinGramsPerUnit: v.optional(v.number()),
    carbsGramsPerUnit: v.optional(v.number()),
    fatGramsPerUnit: v.optional(v.number()),
    fiberGramsPerUnit: v.optional(v.number()),
    sugarGramsPerUnit: v.optional(v.number()),
    sodiumMgPerUnit: v.optional(v.number()),
    calciumMgPerUnit: v.optional(v.number()),
    ironMgPerUnit: v.optional(v.number()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runIngredientSetNutrition(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Ingredient_setNutrition", __result);
    }
    return __result;
  },
});

async function __runIngredientSetPreferredVendor(ctx: MutationCtx, { docId, preferredVendorId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Ingredient not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Ingredient not found");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may read ingredients");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write ingredients through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute ingredient commands");
    if (!((doc.introducedAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      preferredVendorId: preferredVendorId,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    return { ...doc, ...updates };
}

export const Ingredient_setPreferredVendor = mutation({
  args: {
    docId: v.id("ingredients"),
    preferredVendorId: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runIngredientSetPreferredVendor(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Ingredient_setPreferredVendor", __result);
    }
    return __result;
  },
});

async function __runIngredientSetPreferredVendors(ctx: MutationCtx, { docId, preferredVendorIds, preferredVendorId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Ingredient not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Ingredient not found");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may read ingredients");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write ingredients through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute ingredient commands");
    if (!((doc.introducedAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      preferredVendorIds: preferredVendorIds,
      preferredVendorId: preferredVendorId,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    return { ...doc, ...updates };
}

export const Ingredient_setPreferredVendors = mutation({
  args: {
    docId: v.id("ingredients"),
    preferredVendorIds: v.array(v.string()),
    preferredVendorId: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runIngredientSetPreferredVendors(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Ingredient_setPreferredVendors", __result);
    }
    return __result;
  },
});

async function __runIngredientUpdateCosting(ctx: MutationCtx, { docId, costPerUnit, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Ingredient not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Ingredient not found");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may read ingredients");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write ingredients through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute ingredient commands");
    if (!((doc.status === "active"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(checkRole(user, "kitchenManageAccess"))) throw new Error("Guard 2 failed");
    if (!((costPerUnit >= 0))) throw new Error("Ingredient cost per unit cannot be negative");
    const previousCostPerUnit = doc.costPerUnit;
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      costPerUnit: costPerUnit,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, ingredientId: docId, tenantId: __after.tenantId, previousCostPerUnit: previousCostPerUnit, costPerUnit: costPerUnit, unit: __after.unit, _subject: { entity: "Ingredient", command: "updateCosting", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "IngredientCostingUpdated", entity: "Ingredient", entityId: docId, payload: { ingredientId: docId, tenantId: __after.tenantId, previousCostPerUnit: previousCostPerUnit, costPerUnit: costPerUnit, unit: __after.unit }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Ingredient_updateCosting = mutation({
  args: {
    docId: v.id("ingredients"),
    costPerUnit: v.number(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runIngredientUpdateCosting(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Ingredient_updateCosting", __result);
    }
    return __result;
  },
});

async function __runIngredientUpdateDetails(ctx: MutationCtx, { docId, name, unit, category, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Ingredient not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Ingredient not found");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may read ingredients");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write ingredients through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute ingredient commands");
    if (!((doc.status === "active"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((((name).trim()).length > 0))) throw new Error("Ingredient name is required");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      name: name,
      unit: unit,
      category: category,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, ingredientId: docId, tenantId: __after.tenantId, name: name, unit: unit, _subject: { entity: "Ingredient", command: "updateDetails", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "IngredientDetailsUpdated", entity: "Ingredient", entityId: docId, payload: { ingredientId: docId, tenantId: __after.tenantId, name: name, unit: unit }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Ingredient_updateDetails = mutation({
  args: {
    docId: v.id("ingredients"),
    name: v.string(),
    unit: v.any(),
    category: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runIngredientUpdateDetails(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Ingredient_updateDetails", __result);
    }
    return __result;
  },
});

async function __runIngredientDemandCalculate(ctx: MutationCtx, { docId, eventId, ingredientId, requiredQuantity, unit, servings, dishId, sourceComponentLineQuantity, sourceBatchMultiplier, sourceYieldQuantity, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("IngredientDemand not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("IngredientDemand not found");
    const __rel_event = await __resolveRelation(ctx, "events", [__auth.tenantId, doc.eventId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_ingredient = await __resolveRelation(ctx, "ingredients", [__auth.tenantId, doc.ingredientId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).eventId;
      ((doc as any) as any).event = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    {
      const __fk = ((doc as any) as any).ingredientId;
      ((doc as any) as any).ingredient = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) throw new Error("Inventory staff and event managers may read ingredient demand");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) throw new Error("Inventory staff and event managers may write ingredient demand through commands");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) throw new Error("Inventory staff and event managers may execute ingredient demand commands");
    if (!((doc.calculatedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.status === "pending"))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((__rel_event != null))) throw new Error("Guard 3 failed");
    if (!(((__rel_ingredient != null) && (__rel_ingredient.status === "active")))) throw new Error("Guard 4 failed");
    if (!((eventId === doc.eventId))) throw new Error("Calculate eventId must match the seeded event reference");
    if (!((ingredientId === doc.ingredientId))) throw new Error("Calculate ingredientId must match the seeded ingredient reference");
    if (!((requiredQuantity > 0))) throw new Error("Required quantity must be positive");
    if (!(((servings == null) || (servings >= 0)))) throw new Error("Servings cannot be negative");
    if (!(((sourceComponentLineQuantity == null) || (sourceComponentLineQuantity >= 0)))) throw new Error("Source component line quantity cannot be negative");
    if (!(((sourceBatchMultiplier == null) || (sourceBatchMultiplier >= 0)))) throw new Error("Source batch multiplier cannot be negative");
    if (!(((sourceYieldQuantity == null) || (sourceYieldQuantity > 0)))) throw new Error("Source yield quantity must be positive when provided");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "calculated";
        const __allowed: Record<string, string[]> = { "pending": ["calculated"], "calculated": ["confirmed", "superseded"], "confirmed": ["fulfilled", "superseded"], "fulfilled": [], "superseded": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      eventId: eventId,
      ingredientId: ingredientId,
      preferredVendorId: __rel_ingredient.preferredVendorId,
      requiredQuantity: requiredQuantity,
      unit: unit,
      servings: ((servings != null) ? servings : doc.servings),
      dishId: ((dishId != null) ? dishId : doc.dishId),
      sourceComponentLineQuantity: ((sourceComponentLineQuantity != null) ? sourceComponentLineQuantity : doc.sourceComponentLineQuantity),
      sourceBatchMultiplier: ((sourceBatchMultiplier != null) ? sourceBatchMultiplier : doc.sourceBatchMultiplier),
      sourceYieldQuantity: ((sourceYieldQuantity != null) ? sourceYieldQuantity : doc.sourceYieldQuantity),
      status: "calculated",
      calculatedAt: Date.now(),
      purchaseEligibleEventId: eventId,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, ingredientDemandId: docId, tenantId: __after.tenantId, eventId: eventId, ingredientId: ingredientId, dishId: ((dishId != null) ? dishId : __after.dishId), requiredQuantity: requiredQuantity, unit: unit, servings: ((servings != null) ? servings : __after.servings), _subject: { entity: "IngredientDemand", command: "calculate", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "IngredientDemandCalculated", entity: "IngredientDemand", entityId: docId, payload: { ingredientDemandId: docId, tenantId: __after.tenantId, eventId: eventId, ingredientId: ingredientId, dishId: ((dishId != null) ? dishId : __after.dishId), requiredQuantity: requiredQuantity, unit: unit, servings: ((servings != null) ? servings : __after.servings) }, createdAt: Date.now() });
    // Reactions
    const fanRows0 = (await ctx.db.query("purchaseNeeds").withIndex("by_ingredientDemandId", (q) => q.eq("ingredientDemandId", payload.ingredientDemandId)).collect()).filter((d) => (d as any).deletedAt == null);
    for (const __row of fanRows0) {
      const target = __row;
      await __runPurchaseNeedReviseRequired(ctx, { docId: (__row as any)._id, requiredQuantity: payload.requiredQuantity } as any);
    }
    return { ...doc, ...updates };
}

export const IngredientDemand_calculate = mutation({
  args: {
    docId: v.id("ingredientDemands"),
    eventId: v.string(),
    ingredientId: v.string(),
    requiredQuantity: v.number(),
    unit: v.any(),
    servings: v.optional(v.number()),
    dishId: v.optional(v.string()),
    sourceComponentLineQuantity: v.optional(v.number()),
    sourceBatchMultiplier: v.optional(v.number()),
    sourceYieldQuantity: v.optional(v.number()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runIngredientDemandCalculate(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "IngredientDemand_calculate", __result);
    }
    return __result;
  },
});

export const IngredientDemand_createViaCalculate = mutation({
  args: {
    eventId: v.string(),
    ingredientId: v.string(),
    requiredQuantity: v.number(),
    unit: v.any(),
    servings: v.optional(v.number()),
    dishId: v.optional(v.string()),
    sourceComponentLineQuantity: v.optional(v.number()),
    sourceBatchMultiplier: v.optional(v.number()),
    sourceYieldQuantity: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { eventId, ingredientId, requiredQuantity, unit, servings, dishId, sourceComponentLineQuantity, sourceBatchMultiplier, sourceYieldQuantity } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      createdAt: Date.now(),
      updatedAt: Date.now(),
      status: "pending",
      dishId: args.dishId,
      eventId: args.eventId,
      ingredientId: args.ingredientId,
      requiredQuantity: args.requiredQuantity,
      servings: args.servings,
      sourceBatchMultiplier: args.sourceBatchMultiplier,
      sourceComponentLineQuantity: args.sourceComponentLineQuantity,
      sourceYieldQuantity: args.sourceYieldQuantity,
      unit: args.unit
    };
    const __rel_event = await __resolveRelation(ctx, "events", [__auth.tenantId, __draft.eventId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_ingredient = await __resolveRelation(ctx, "ingredients", [__auth.tenantId, __draft.ingredientId], ["tenantId","id"], "tenantId", __auth.tenantId);
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) throw new Error("Inventory staff and event managers may read ingredient demand");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) throw new Error("Inventory staff and event managers may write ingredient demand through commands");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) throw new Error("Inventory staff and event managers may execute ingredient demand commands");
    if (!((__draft.calculatedAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.status === "pending"))) throw new Error("Guard 1 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((__rel_event != null))) throw new Error("Guard 3 failed");
    if (!(((__rel_ingredient != null) && (__rel_ingredient.status === "active")))) throw new Error("Guard 4 failed");
    if (!((eventId === __draft.eventId))) throw new Error("Calculate eventId must match the seeded event reference");
    if (!((ingredientId === __draft.ingredientId))) throw new Error("Calculate ingredientId must match the seeded ingredient reference");
    if (!((requiredQuantity > 0))) throw new Error("Required quantity must be positive");
    if (!(((servings == null) || (servings >= 0)))) throw new Error("Servings cannot be negative");
    if (!(((sourceComponentLineQuantity == null) || (sourceComponentLineQuantity >= 0)))) throw new Error("Source component line quantity cannot be negative");
    if (!(((sourceBatchMultiplier == null) || (sourceBatchMultiplier >= 0)))) throw new Error("Source batch multiplier cannot be negative");
    if (!(((sourceYieldQuantity == null) || (sourceYieldQuantity > 0)))) throw new Error("Source yield quantity must be positive when provided");
    const doc: Record<string, any> = {
      ...__draft,
      eventId: eventId,
      ingredientId: ingredientId,
      preferredVendorId: __rel_ingredient.preferredVendorId,
      requiredQuantity: requiredQuantity,
      unit: unit,
      servings: ((servings != null) ? servings : __draft.servings),
      dishId: ((dishId != null) ? dishId : __draft.dishId),
      sourceComponentLineQuantity: ((sourceComponentLineQuantity != null) ? sourceComponentLineQuantity : __draft.sourceComponentLineQuantity),
      sourceBatchMultiplier: ((sourceBatchMultiplier != null) ? sourceBatchMultiplier : __draft.sourceBatchMultiplier),
      sourceYieldQuantity: ((sourceYieldQuantity != null) ? sourceYieldQuantity : __draft.sourceYieldQuantity),
      status: "calculated",
      calculatedAt: Date.now(),
      purchaseEligibleEventId: eventId,
      version: 1,
    };
    const docId = await ctx.db.insert("ingredientDemands", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, ingredientDemandId: docId, tenantId: doc.tenantId, eventId: doc.eventId, ingredientId: doc.ingredientId, dishId: ((doc.dishId != null) ? doc.dishId : doc.dishId), requiredQuantity: doc.requiredQuantity, unit: doc.unit, servings: ((doc.servings != null) ? doc.servings : doc.servings), _subject: { entity: "IngredientDemand", command: "calculate", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "IngredientDemandCalculated", entity: "IngredientDemand", entityId: docId, payload: { ingredientDemandId: docId, tenantId: doc.tenantId, eventId: doc.eventId, ingredientId: doc.ingredientId, dishId: ((doc.dishId != null) ? doc.dishId : doc.dishId), requiredQuantity: doc.requiredQuantity, unit: doc.unit, servings: ((doc.servings != null) ? doc.servings : doc.servings) }, createdAt: Date.now() });
    // Reactions
    const fanRows0 = (await ctx.db.query("purchaseNeeds").withIndex("by_ingredientDemandId", (q) => q.eq("ingredientDemandId", payload.ingredientDemandId)).collect()).filter((d) => (d as any).deletedAt == null);
    for (const __row of fanRows0) {
      const target = __row;
      await __runPurchaseNeedReviseRequired(ctx, { docId: (__row as any)._id, requiredQuantity: payload.requiredQuantity } as any);
    }
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "IngredientDemand_createViaCalculate", __result);
    }
    return __result;
  },
});

async function __runIngredientDemandConfirm(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("IngredientDemand not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("IngredientDemand not found");
    const __rel_ingredient = await __resolveRelation(ctx, "ingredients", [__auth.tenantId, doc.ingredientId], ["tenantId","id"], "tenantId", __auth.tenantId);
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) throw new Error("Inventory staff and event managers may read ingredient demand");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) throw new Error("Inventory staff and event managers may write ingredient demand through commands");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) throw new Error("Inventory staff and event managers may execute ingredient demand commands");
    if (!((doc.status === "calculated"))) throw new Error("Guard 0 failed");
    if (!((doc.calculatedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "confirmed";
        const __allowed: Record<string, string[]> = { "pending": ["calculated"], "calculated": ["confirmed", "superseded"], "confirmed": ["fulfilled", "superseded"], "fulfilled": [], "superseded": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "confirmed",
      confirmedAt: Date.now(),
      purchaseEligibleEventId: null,
      preferredVendorId: __rel_ingredient.preferredVendorId,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, ingredientDemandId: docId, tenantId: __after.tenantId, eventId: __after.eventId, ingredientId: __after.ingredientId, preferredVendorId: __after.preferredVendorId, requiredQuantity: __after.requiredQuantity, unit: __after.unit, _subject: { entity: "IngredientDemand", command: "confirm", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "IngredientDemandConfirmed", entity: "IngredientDemand", entityId: docId, payload: { ingredientDemandId: docId, tenantId: __after.tenantId, eventId: __after.eventId, ingredientId: __after.ingredientId, preferredVendorId: __after.preferredVendorId, requiredQuantity: __after.requiredQuantity, unit: __after.unit }, createdAt: Date.now() });
    // Reactions
    await __runPurchaseNeedCreate(ctx, { tenantId: payload.tenantId, eventId: payload.eventId, ingredientDemandId: payload.ingredientDemandId, ingredientId: payload.ingredientId, preferredVendorId: payload.preferredVendorId, requiredQuantity: payload.requiredQuantity, unit: payload.unit } as any);
    return { ...doc, ...updates };
}

export const IngredientDemand_confirm = mutation({
  args: {
    docId: v.id("ingredientDemands"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runIngredientDemandConfirm(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "IngredientDemand_confirm", __result);
    }
    return __result;
  },
});

async function __runIngredientDemandEnsurePurchaseEligible(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("IngredientDemand not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("IngredientDemand not found");
    const __rel_ingredient = await __resolveRelation(ctx, "ingredients", [__auth.tenantId, doc.ingredientId], ["tenantId","id"], "tenantId", __auth.tenantId);
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) throw new Error("Inventory staff and event managers may read ingredient demand");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) throw new Error("Inventory staff and event managers may write ingredient demand through commands");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) throw new Error("Inventory staff and event managers may execute ingredient demand commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      purchaseEligibleEventId: (((doc.status === "calculated") && (doc.purchaseEligibleEventId == null)) ? doc.eventId : doc.purchaseEligibleEventId),
      preferredVendorId: __rel_ingredient.preferredVendorId,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    return { ...doc, ...updates };
}

export const IngredientDemand_ensurePurchaseEligible = mutation({
  args: {
    docId: v.id("ingredientDemands"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runIngredientDemandEnsurePurchaseEligible(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "IngredientDemand_ensurePurchaseEligible", __result);
    }
    return __result;
  },
});

async function __runIngredientDemandFulfill(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("IngredientDemand not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("IngredientDemand not found");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) throw new Error("Inventory staff and event managers may read ingredient demand");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) throw new Error("Inventory staff and event managers may write ingredient demand through commands");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) throw new Error("Inventory staff and event managers may execute ingredient demand commands");
    if (!((doc.status === "confirmed"))) throw new Error("Guard 0 failed");
    if (!((doc.confirmedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "fulfilled";
        const __allowed: Record<string, string[]> = { "pending": ["calculated"], "calculated": ["confirmed", "superseded"], "confirmed": ["fulfilled", "superseded"], "fulfilled": [], "superseded": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "fulfilled",
      fulfilledAt: Date.now(),
      purchaseEligibleEventId: null,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, ingredientDemandId: docId, tenantId: __after.tenantId, eventId: __after.eventId, ingredientId: __after.ingredientId, requiredQuantity: __after.requiredQuantity, unit: __after.unit, _subject: { entity: "IngredientDemand", command: "fulfill", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "IngredientDemandFulfilled", entity: "IngredientDemand", entityId: docId, payload: { ingredientDemandId: docId, tenantId: __after.tenantId, eventId: __after.eventId, ingredientId: __after.ingredientId, requiredQuantity: __after.requiredQuantity, unit: __after.unit }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const IngredientDemand_fulfill = mutation({
  args: {
    docId: v.id("ingredientDemands"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runIngredientDemandFulfill(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "IngredientDemand_fulfill", __result);
    }
    return __result;
  },
});

async function __runIngredientDemandMarkReleased(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("IngredientDemand not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("IngredientDemand not found");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) throw new Error("Inventory staff and event managers may read ingredient demand");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) throw new Error("Inventory staff and event managers may write ingredient demand through commands");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) throw new Error("Inventory staff and event managers may execute ingredient demand commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!(((doc.status === "calculated") || (doc.status === "confirmed")))) throw new Error("Guard 1 failed");
    if (!((doc.calculatedAt != null))) throw new Error("Guard 2 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = String(((doc.status === "calculated") ? "confirmed" : doc.status));
        const __allowed: Record<string, string[]> = { "pending": ["calculated"], "calculated": ["confirmed", "superseded"], "confirmed": ["fulfilled", "superseded"], "fulfilled": [], "superseded": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: ((doc.status === "calculated") ? "confirmed" : doc.status),
      confirmedAt: ((doc.status === "calculated") ? Date.now() : doc.confirmedAt),
      purchaseEligibleEventId: null,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, ingredientDemandId: docId, tenantId: __after.tenantId, eventId: __after.eventId, ingredientId: __after.ingredientId, requiredQuantity: __after.requiredQuantity, unit: __after.unit, _subject: { entity: "IngredientDemand", command: "markReleased", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "IngredientDemandReleased", entity: "IngredientDemand", entityId: docId, payload: { ingredientDemandId: docId, tenantId: __after.tenantId, eventId: __after.eventId, ingredientId: __after.ingredientId, requiredQuantity: __after.requiredQuantity, unit: __after.unit }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const IngredientDemand_markReleased = mutation({
  args: {
    docId: v.id("ingredientDemands"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runIngredientDemandMarkReleased(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "IngredientDemand_markReleased", __result);
    }
    return __result;
  },
});

async function __runIngredientDemandRecalculate(ctx: MutationCtx, { docId, newQuantity, reason, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("IngredientDemand not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("IngredientDemand not found");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) throw new Error("Inventory staff and event managers may read ingredient demand");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) throw new Error("Inventory staff and event managers may write ingredient demand through commands");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) throw new Error("Inventory staff and event managers may execute ingredient demand commands");
    if (!(((doc.status === "calculated") || (doc.status === "confirmed")))) throw new Error("Guard 0 failed");
    if (!((doc.calculatedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((newQuantity > 0))) throw new Error("Recalculated quantity must be positive");
    if (!((((reason).trim()).length > 0))) throw new Error("Recalculation reason is required");
    const previousQuantity = doc.requiredQuantity;
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      requiredQuantity: newQuantity,
      lastRecalculationReason: reason,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, ingredientDemandId: docId, tenantId: __after.tenantId, eventId: __after.eventId, ingredientId: __after.ingredientId, previousQuantity: previousQuantity, requiredQuantity: newQuantity, unit: __after.unit, reason: reason, _subject: { entity: "IngredientDemand", command: "recalculate", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "IngredientDemandRecalculated", entity: "IngredientDemand", entityId: docId, payload: { ingredientDemandId: docId, tenantId: __after.tenantId, eventId: __after.eventId, ingredientId: __after.ingredientId, previousQuantity: previousQuantity, requiredQuantity: newQuantity, unit: __after.unit, reason: reason }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const IngredientDemand_recalculate = mutation({
  args: {
    docId: v.id("ingredientDemands"),
    newQuantity: v.number(),
    reason: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runIngredientDemandRecalculate(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "IngredientDemand_recalculate", __result);
    }
    return __result;
  },
});

async function __runIngredientDemandSupersede(ctx: MutationCtx, { docId, reason, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("IngredientDemand not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("IngredientDemand not found");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) throw new Error("Inventory staff and event managers may read ingredient demand");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) throw new Error("Inventory staff and event managers may write ingredient demand through commands");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) throw new Error("Inventory staff and event managers may execute ingredient demand commands");
    if (!(((doc.status === "calculated") || (doc.status === "confirmed")))) throw new Error("Guard 0 failed");
    if (!((doc.calculatedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!(checkRole(user, "inventoryManageAccess"))) throw new Error("Guard 3 failed");
    if (!((((reason).trim()).length > 0))) throw new Error("Supersede reason is required");
    const previousQuantity = doc.requiredQuantity;
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "superseded";
        const __allowed: Record<string, string[]> = { "pending": ["calculated"], "calculated": ["confirmed", "superseded"], "confirmed": ["fulfilled", "superseded"], "fulfilled": [], "superseded": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "superseded",
      supersededAt: Date.now(),
      supersedeReason: reason,
      purchaseEligibleEventId: null,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, ingredientDemandId: docId, tenantId: __after.tenantId, eventId: __after.eventId, ingredientId: __after.ingredientId, previousQuantity: previousQuantity, requiredQuantity: __after.requiredQuantity, unit: __after.unit, reason: reason, _subject: { entity: "IngredientDemand", command: "supersede", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "IngredientDemandSuperseded", entity: "IngredientDemand", entityId: docId, payload: { ingredientDemandId: docId, tenantId: __after.tenantId, eventId: __after.eventId, ingredientId: __after.ingredientId, previousQuantity: previousQuantity, requiredQuantity: __after.requiredQuantity, unit: __after.unit, reason: reason }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const IngredientDemand_supersede = mutation({
  args: {
    docId: v.id("ingredientDemands"),
    reason: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runIngredientDemandSupersede(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "IngredientDemand_supersede", __result);
    }
    return __result;
  },
});

async function __runIngredientDemandSyncFromContributions(ctx: MutationCtx, { docId, eventId, ingredientId, requiredQuantity, unit, purchasingWeekStart, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("IngredientDemand not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("IngredientDemand not found");
    const __rel_ingredient = await __resolveRelation(ctx, "ingredients", [__auth.tenantId, doc.ingredientId], ["tenantId","id"], "tenantId", __auth.tenantId);
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) throw new Error("Inventory staff and event managers may read ingredient demand");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) throw new Error("Inventory staff and event managers may write ingredient demand through commands");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) throw new Error("Inventory staff and event managers may execute ingredient demand commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!((((doc.status === "pending") || (doc.status === "calculated")) || (doc.status === "confirmed")))) throw new Error("Guard 1 failed");
    const nextQuantity = Math.max(0.0001, requiredQuantity);
    const previousStatus = doc.status;
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = String(((previousStatus === "pending") ? "calculated" : previousStatus));
        const __allowed: Record<string, string[]> = { "pending": ["calculated"], "calculated": ["confirmed", "superseded"], "confirmed": ["fulfilled", "superseded"], "fulfilled": [], "superseded": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      eventId: eventId,
      ingredientId: ingredientId,
      preferredVendorId: __rel_ingredient.preferredVendorId,
      requiredQuantity: nextQuantity,
      unit: unit,
      purchasingWeekStart: ((purchasingWeekStart != null) ? purchasingWeekStart : doc.purchasingWeekStart),
      status: ((previousStatus === "pending") ? "calculated" : previousStatus),
      calculatedAt: ((doc.calculatedAt == null) ? Date.now() : doc.calculatedAt),
      purchaseEligibleEventId: (((previousStatus === "pending") || (previousStatus === "calculated")) ? eventId : doc.purchaseEligibleEventId),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, ingredientDemandId: docId, tenantId: __after.tenantId, eventId: eventId, ingredientId: ingredientId, dishId: __after.dishId, requiredQuantity: nextQuantity, unit: unit, servings: __after.servings, _subject: { entity: "IngredientDemand", command: "syncFromContributions", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "IngredientDemandCalculated", entity: "IngredientDemand", entityId: docId, payload: { ingredientDemandId: docId, tenantId: __after.tenantId, eventId: eventId, ingredientId: ingredientId, dishId: __after.dishId, requiredQuantity: nextQuantity, unit: unit, servings: __after.servings }, createdAt: Date.now() });
    // Reactions
    const fanRows0 = (await ctx.db.query("purchaseNeeds").withIndex("by_ingredientDemandId", (q) => q.eq("ingredientDemandId", payload.ingredientDemandId)).collect()).filter((d) => (d as any).deletedAt == null);
    for (const __row of fanRows0) {
      const target = __row;
      await __runPurchaseNeedReviseRequired(ctx, { docId: (__row as any)._id, requiredQuantity: payload.requiredQuantity } as any);
    }
    return { ...doc, ...updates };
}

export const IngredientDemand_syncFromContributions = mutation({
  args: {
    docId: v.id("ingredientDemands"),
    eventId: v.string(),
    ingredientId: v.string(),
    requiredQuantity: v.number(),
    unit: v.any(),
    purchasingWeekStart: v.optional(v.number()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runIngredientDemandSyncFromContributions(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "IngredientDemand_syncFromContributions", __result);
    }
    return __result;
  },
});

async function __runIngredientPriceObservationRecord(ctx: MutationCtx, { docId, ingredientId, vendorId, vendorOrderId, vendorOrderLineId, receiptQuantity, cumulativeReceivedQuantity, unit, unitPrice, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("IngredientPriceObservation not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("IngredientPriceObservation not found");
    if (!(((checkRole(user, "kitchenAccess") || checkRole(user, "procurementAccess")) || checkRole(user, "manageAccess")))) throw new Error("Kitchen, procurement, and managers may read confirmed ingredient prices");
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may record confirmed ingredient prices");
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may execute confirmed ingredient price commands");
    if (!(((doc.observedAt == null) || ((((((((doc.ingredientId === ingredientId) && (doc.vendorId === vendorId)) && (doc.vendorOrderId === vendorOrderId)) && (doc.vendorOrderLineId === vendorOrderLineId)) && (doc.receiptQuantity === receiptQuantity)) && (doc.cumulativeReceivedQuantity === cumulativeReceivedQuantity)) && (doc.unit === unit)) && (doc.unitPrice === unitPrice))))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((receiptQuantity > 0))) throw new Error("Price observation receipt quantity must be positive");
    if (!((cumulativeReceivedQuantity > 0))) throw new Error("Price observation cumulative quantity must be positive");
    if (!((unitPrice >= 0))) throw new Error("Price observation unit price cannot be negative");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      ingredientId: ingredientId,
      vendorId: vendorId,
      vendorOrderId: vendorOrderId,
      vendorOrderLineId: vendorOrderLineId,
      receiptQuantity: receiptQuantity,
      cumulativeReceivedQuantity: cumulativeReceivedQuantity,
      unit: unit,
      unitPrice: unitPrice,
      observedAt: ((doc.observedAt == null) ? Date.now() : doc.observedAt),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    return { ...doc, ...updates };
}

export const IngredientPriceObservation_record = mutation({
  args: {
    docId: v.id("ingredientPriceObservations"),
    ingredientId: v.string(),
    vendorId: v.string(),
    vendorOrderId: v.string(),
    vendorOrderLineId: v.string(),
    receiptQuantity: v.number(),
    cumulativeReceivedQuantity: v.number(),
    unit: v.any(),
    unitPrice: v.number(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runIngredientPriceObservationRecord(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "IngredientPriceObservation_record", __result);
    }
    return __result;
  },
});

export const IngredientPriceObservation_createViaRecord = mutation({
  args: {
    ingredientId: v.string(),
    vendorId: v.string(),
    vendorOrderId: v.string(),
    vendorOrderLineId: v.string(),
    receiptQuantity: v.number(),
    cumulativeReceivedQuantity: v.number(),
    unit: v.any(),
    unitPrice: v.number(),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { ingredientId, vendorId, vendorOrderId, vendorOrderLineId, receiptQuantity, cumulativeReceivedQuantity, unit, unitPrice } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      createdAt: Date.now(),
      updatedAt: Date.now(),
      cumulativeReceivedQuantity: args.cumulativeReceivedQuantity,
      ingredientId: args.ingredientId,
      receiptQuantity: args.receiptQuantity,
      unit: args.unit,
      unitPrice: args.unitPrice,
      vendorId: args.vendorId,
      vendorOrderId: args.vendorOrderId,
      vendorOrderLineId: args.vendorOrderLineId
    };
    if (!(((checkRole(user, "kitchenAccess") || checkRole(user, "procurementAccess")) || checkRole(user, "manageAccess")))) throw new Error("Kitchen, procurement, and managers may read confirmed ingredient prices");
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may record confirmed ingredient prices");
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may execute confirmed ingredient price commands");
    if (!(((__draft.observedAt == null) || ((((((((__draft.ingredientId === ingredientId) && (__draft.vendorId === vendorId)) && (__draft.vendorOrderId === vendorOrderId)) && (__draft.vendorOrderLineId === vendorOrderLineId)) && (__draft.receiptQuantity === receiptQuantity)) && (__draft.cumulativeReceivedQuantity === cumulativeReceivedQuantity)) && (__draft.unit === unit)) && (__draft.unitPrice === unitPrice))))) throw new Error("Guard 0 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((receiptQuantity > 0))) throw new Error("Price observation receipt quantity must be positive");
    if (!((cumulativeReceivedQuantity > 0))) throw new Error("Price observation cumulative quantity must be positive");
    if (!((unitPrice >= 0))) throw new Error("Price observation unit price cannot be negative");
    const doc: Record<string, any> = {
      ...__draft,
      ingredientId: ingredientId,
      vendorId: vendorId,
      vendorOrderId: vendorOrderId,
      vendorOrderLineId: vendorOrderLineId,
      receiptQuantity: receiptQuantity,
      cumulativeReceivedQuantity: cumulativeReceivedQuantity,
      unit: unit,
      unitPrice: unitPrice,
      observedAt: ((__draft.observedAt == null) ? Date.now() : __draft.observedAt),
      version: 1,
    };
    const docId = await ctx.db.insert("ingredientPriceObservations", doc as any);
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "IngredientPriceObservation_createViaRecord", __result);
    }
    return __result;
  },
});

async function __runIntegrationConnectionAuthorize(ctx: MutationCtx, { docId, provider, externalAccountId, displayName, scopes, credentialRef, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("IntegrationConnection not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("IntegrationConnection not found");
    if (!(checkRole(user, "manageAccess"))) throw new Error("Managers may read integration connections");
    if (!(checkRole(user, "adminAccess"))) throw new Error("Admins may write integration connections through commands");
    if (!(checkRole(user, "adminAccess"))) throw new Error("Admins may execute integration connection commands");
    if (!((doc.connectedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((user.id != null))) throw new Error("Guard 2 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "pending";
        const __allowed: Record<string, string[]> = { "disconnected": ["pending", "connected", "error", "revoked"], "pending": ["connected", "error", "disconnected", "revoked"], "connected": ["connected", "error", "revoked", "pending"], "error": ["pending", "connected", "error", "revoked", "disconnected"], "revoked": ["pending", "disconnected"] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      provider: provider,
      externalAccountId: externalAccountId,
      displayName: displayName,
      scopes: scopes,
      credentialRef: credentialRef,
      status: "pending",
      connectedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, connectionId: docId, tenantId: __after.tenantId, provider: provider, externalAccountId: externalAccountId, _subject: { entity: "IntegrationConnection", command: "authorize", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "IntegrationConnectionStarted", entity: "IntegrationConnection", entityId: docId, payload: { connectionId: docId, tenantId: __after.tenantId, provider: provider, externalAccountId: externalAccountId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const IntegrationConnection_authorize = mutation({
  args: {
    docId: v.id("integrationConnections"),
    provider: v.any(),
    externalAccountId: v.optional(v.string()),
    displayName: v.optional(v.string()),
    scopes: v.optional(v.string()),
    credentialRef: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runIntegrationConnectionAuthorize(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "IntegrationConnection_authorize", __result);
    }
    return __result;
  },
});

export const IntegrationConnection_createViaAuthorize = mutation({
  args: {
    provider: v.any(),
    externalAccountId: v.optional(v.string()),
    displayName: v.optional(v.string()),
    scopes: v.optional(v.string()),
    credentialRef: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { provider, externalAccountId, displayName, scopes, credentialRef } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      chargesEnabled: false,
      payoutsEnabled: false,
      createdAt: Date.now(),
      updatedAt: Date.now(),
      status: "disconnected",
      credentialRef: args.credentialRef,
      displayName: args.displayName,
      externalAccountId: args.externalAccountId,
      provider: args.provider,
      scopes: args.scopes
    };
    if (!(checkRole(user, "manageAccess"))) throw new Error("Managers may read integration connections");
    if (!(checkRole(user, "adminAccess"))) throw new Error("Admins may write integration connections through commands");
    if (!(checkRole(user, "adminAccess"))) throw new Error("Admins may execute integration connection commands");
    if (!((__draft.connectedAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((user.id != null))) throw new Error("Guard 2 failed");
    const doc: Record<string, any> = {
      ...__draft,
      provider: provider,
      externalAccountId: externalAccountId,
      displayName: displayName,
      scopes: scopes,
      credentialRef: credentialRef,
      status: "pending",
      connectedAt: Date.now(),
      version: 1,
    };
    const docId = await ctx.db.insert("integrationConnections", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, connectionId: docId, tenantId: doc.tenantId, provider: doc.provider, externalAccountId: doc.externalAccountId, _subject: { entity: "IntegrationConnection", command: "authorize", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "IntegrationConnectionStarted", entity: "IntegrationConnection", entityId: docId, payload: { connectionId: docId, tenantId: doc.tenantId, provider: doc.provider, externalAccountId: doc.externalAccountId }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "IntegrationConnection_createViaAuthorize", __result);
    }
    return __result;
  },
});

async function __runIntegrationConnectionDisconnect(ctx: MutationCtx, { docId, reason, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("IntegrationConnection not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("IntegrationConnection not found");
    if (!(checkRole(user, "manageAccess"))) throw new Error("Managers may read integration connections");
    if (!(checkRole(user, "adminAccess"))) throw new Error("Admins may write integration connections through commands");
    if (!(checkRole(user, "adminAccess"))) throw new Error("Admins may execute integration connection commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!((user.id != null))) throw new Error("Guard 1 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "revoked";
        const __allowed: Record<string, string[]> = { "disconnected": ["pending", "connected", "error", "revoked"], "pending": ["connected", "error", "disconnected", "revoked"], "connected": ["connected", "error", "revoked", "pending"], "error": ["pending", "connected", "error", "revoked", "disconnected"], "revoked": ["pending", "disconnected"] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "revoked",
      chargesEnabled: false,
      payoutsEnabled: false,
      lastErrorMessage: reason,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, connectionId: docId, tenantId: __after.tenantId, provider: __after.provider, _subject: { entity: "IntegrationConnection", command: "disconnect", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "IntegrationConnectionRevoked", entity: "IntegrationConnection", entityId: docId, payload: { connectionId: docId, tenantId: __after.tenantId, provider: __after.provider }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const IntegrationConnection_disconnect = mutation({
  args: {
    docId: v.id("integrationConnections"),
    reason: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runIntegrationConnectionDisconnect(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "IntegrationConnection_disconnect", __result);
    }
    return __result;
  },
});

async function __runIntegrationConnectionMarkConnected(ctx: MutationCtx, { docId, externalAccountId, displayName, scopes, chargesEnabled, payoutsEnabled, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("IntegrationConnection not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("IntegrationConnection not found");
    if (!(checkRole(user, "manageAccess"))) throw new Error("Managers may read integration connections");
    if (!(checkRole(user, "adminAccess"))) throw new Error("Admins may write integration connections through commands");
    if (!(checkRole(user, "adminAccess"))) throw new Error("Admins may execute integration connection commands");
    if (!((doc.connectedAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((user.id != null))) throw new Error("Guard 2 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "connected";
        const __allowed: Record<string, string[]> = { "disconnected": ["pending", "connected", "error", "revoked"], "pending": ["connected", "error", "disconnected", "revoked"], "connected": ["connected", "error", "revoked", "pending"], "error": ["pending", "connected", "error", "revoked", "disconnected"], "revoked": ["pending", "disconnected"] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      externalAccountId: externalAccountId,
      displayName: displayName,
      scopes: scopes,
      chargesEnabled: chargesEnabled,
      payoutsEnabled: payoutsEnabled,
      status: "connected",
      lastErrorMessage: null,
      lastErrorAt: null,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, connectionId: docId, tenantId: __after.tenantId, provider: __after.provider, externalAccountId: externalAccountId, chargesEnabled: chargesEnabled, _subject: { entity: "IntegrationConnection", command: "markConnected", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "IntegrationConnectionEstablished", entity: "IntegrationConnection", entityId: docId, payload: { connectionId: docId, tenantId: __after.tenantId, provider: __after.provider, externalAccountId: externalAccountId, chargesEnabled: chargesEnabled }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const IntegrationConnection_markConnected = mutation({
  args: {
    docId: v.id("integrationConnections"),
    externalAccountId: v.optional(v.string()),
    displayName: v.optional(v.string()),
    scopes: v.optional(v.string()),
    chargesEnabled: v.boolean(),
    payoutsEnabled: v.boolean(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runIntegrationConnectionMarkConnected(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "IntegrationConnection_markConnected", __result);
    }
    return __result;
  },
});

async function __runIntegrationConnectionReauthorize(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("IntegrationConnection not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("IntegrationConnection not found");
    if (!(checkRole(user, "manageAccess"))) throw new Error("Managers may read integration connections");
    if (!(checkRole(user, "adminAccess"))) throw new Error("Admins may write integration connections through commands");
    if (!(checkRole(user, "adminAccess"))) throw new Error("Admins may execute integration connection commands");
    if (!((doc.connectedAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((user.id != null))) throw new Error("Guard 2 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "pending";
        const __allowed: Record<string, string[]> = { "disconnected": ["pending", "connected", "error", "revoked"], "pending": ["connected", "error", "disconnected", "revoked"], "connected": ["connected", "error", "revoked", "pending"], "error": ["pending", "connected", "error", "revoked", "disconnected"], "revoked": ["pending", "disconnected"] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "pending",
      lastErrorMessage: null,
      lastErrorAt: null,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, connectionId: docId, tenantId: __after.tenantId, provider: __after.provider, _subject: { entity: "IntegrationConnection", command: "reauthorize", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "IntegrationConnectionReauthorized", entity: "IntegrationConnection", entityId: docId, payload: { connectionId: docId, tenantId: __after.tenantId, provider: __after.provider }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const IntegrationConnection_reauthorize = mutation({
  args: {
    docId: v.id("integrationConnections"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runIntegrationConnectionReauthorize(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "IntegrationConnection_reauthorize", __result);
    }
    return __result;
  },
});

async function __runIntegrationConnectionRecordFailure(ctx: MutationCtx, { docId, reason, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("IntegrationConnection not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("IntegrationConnection not found");
    if (!(checkRole(user, "manageAccess"))) throw new Error("Managers may read integration connections");
    if (!(checkRole(user, "adminAccess"))) throw new Error("Admins may write integration connections through commands");
    if (!(checkRole(user, "adminAccess"))) throw new Error("Admins may execute integration connection commands");
    if (!((doc.connectedAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((((reason).trim()).length > 0))) throw new Error("Failure reason is required");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "error";
        const __allowed: Record<string, string[]> = { "disconnected": ["pending", "connected", "error", "revoked"], "pending": ["connected", "error", "disconnected", "revoked"], "connected": ["connected", "error", "revoked", "pending"], "error": ["pending", "connected", "error", "revoked", "disconnected"], "revoked": ["pending", "disconnected"] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "error",
      lastErrorMessage: reason,
      lastErrorAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, connectionId: docId, tenantId: __after.tenantId, provider: __after.provider, reason: reason, _subject: { entity: "IntegrationConnection", command: "recordFailure", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "IntegrationConnectionFailed", entity: "IntegrationConnection", entityId: docId, payload: { connectionId: docId, tenantId: __after.tenantId, provider: __after.provider, reason: reason }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const IntegrationConnection_recordFailure = mutation({
  args: {
    docId: v.id("integrationConnections"),
    reason: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runIntegrationConnectionRecordFailure(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "IntegrationConnection_recordFailure", __result);
    }
    return __result;
  },
});

async function __runIntegrationConnectionRecordSyncSuccess(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("IntegrationConnection not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("IntegrationConnection not found");
    if (!(checkRole(user, "manageAccess"))) throw new Error("Managers may read integration connections");
    if (!(checkRole(user, "adminAccess"))) throw new Error("Admins may write integration connections through commands");
    if (!(checkRole(user, "adminAccess"))) throw new Error("Admins may execute integration connection commands");
    if (!((doc.connectedAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "connected";
        const __allowed: Record<string, string[]> = { "disconnected": ["pending", "connected", "error", "revoked"], "pending": ["connected", "error", "disconnected", "revoked"], "connected": ["connected", "error", "revoked", "pending"], "error": ["pending", "connected", "error", "revoked", "disconnected"], "revoked": ["pending", "disconnected"] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "connected",
      lastSuccessfulSyncAt: Date.now(),
      lastErrorMessage: null,
      lastErrorAt: null,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, connectionId: docId, tenantId: __after.tenantId, provider: __after.provider, _subject: { entity: "IntegrationConnection", command: "recordSyncSuccess", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "IntegrationConnectionSynced", entity: "IntegrationConnection", entityId: docId, payload: { connectionId: docId, tenantId: __after.tenantId, provider: __after.provider }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const IntegrationConnection_recordSyncSuccess = mutation({
  args: {
    docId: v.id("integrationConnections"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runIntegrationConnectionRecordSyncSuccess(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "IntegrationConnection_recordSyncSuccess", __result);
    }
    return __result;
  },
});

async function __runInterviewRecordOutcome(ctx: MutationCtx, { docId, outcome, notes, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Interview not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Interview not found");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Workforce managers may read interviews");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Workforce managers may write interviews through commands");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Workforce managers may execute interview commands");
    if (!((doc.outcome === "pending"))) throw new Error("Guard 0 failed");
    if (!((doc.bookedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((user.id != null))) throw new Error("Guard 3 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      outcome: outcome,
      notes: notes,
      conductedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, interviewId: docId, tenantId: __after.tenantId, candidateId: __after.candidateId, outcome: outcome, _subject: { entity: "Interview", command: "recordOutcome", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "InterviewOutcomeRecorded", entity: "Interview", entityId: docId, payload: { interviewId: docId, tenantId: __after.tenantId, candidateId: __after.candidateId, outcome: outcome }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Interview_recordOutcome = mutation({
  args: {
    docId: v.id("interviews"),
    outcome: v.any(),
    notes: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runInterviewRecordOutcome(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Interview_recordOutcome", __result);
    }
    return __result;
  },
});

async function __runInterviewSchedule(ctx: MutationCtx, { docId, candidateId, scheduledFor, interviewerPersonId, sourceSystem, externalInterviewId, rawSourceData, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Interview not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Interview not found");
    const __rel_candidate = await __resolveRelation(ctx, "candidates", [__auth.tenantId, doc.candidateId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).candidateId;
      ((doc as any) as any).candidate = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Workforce managers may read interviews");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Workforce managers may write interviews through commands");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Workforce managers may execute interview commands");
    if (!((doc.bookedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((__rel_candidate != null))) throw new Error("Guard 2 failed");
    if (!((user.id != null))) throw new Error("Guard 3 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      candidateId: candidateId,
      scheduledFor: scheduledFor,
      interviewerPersonId: interviewerPersonId,
      sourceSystem: sourceSystem,
      externalInterviewId: externalInterviewId,
      rawSourceData: rawSourceData,
      bookedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, interviewId: docId, tenantId: __after.tenantId, candidateId: candidateId, _subject: { entity: "Interview", command: "schedule", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "InterviewScheduled", entity: "Interview", entityId: docId, payload: { interviewId: docId, tenantId: __after.tenantId, candidateId: candidateId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Interview_schedule = mutation({
  args: {
    docId: v.id("interviews"),
    candidateId: v.string(),
    scheduledFor: v.optional(v.number()),
    interviewerPersonId: v.optional(v.string()),
    sourceSystem: v.optional(v.string()),
    externalInterviewId: v.optional(v.string()),
    rawSourceData: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runInterviewSchedule(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Interview_schedule", __result);
    }
    return __result;
  },
});

export const Interview_createViaSchedule = mutation({
  args: {
    candidateId: v.string(),
    scheduledFor: v.optional(v.number()),
    interviewerPersonId: v.optional(v.string()),
    sourceSystem: v.optional(v.string()),
    externalInterviewId: v.optional(v.string()),
    rawSourceData: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { candidateId, scheduledFor, interviewerPersonId, sourceSystem, externalInterviewId, rawSourceData } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      outcome: "pending",
      createdAt: Date.now(),
      updatedAt: Date.now(),
      candidateId: args.candidateId,
      externalInterviewId: args.externalInterviewId,
      interviewerPersonId: args.interviewerPersonId,
      rawSourceData: args.rawSourceData,
      scheduledFor: args.scheduledFor,
      sourceSystem: args.sourceSystem
    };
    const __rel_candidate = await __resolveRelation(ctx, "candidates", [__auth.tenantId, __draft.candidateId], ["tenantId","id"], "tenantId", __auth.tenantId);
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Workforce managers may read interviews");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Workforce managers may write interviews through commands");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Workforce managers may execute interview commands");
    if (!((__draft.bookedAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((__rel_candidate != null))) throw new Error("Guard 2 failed");
    if (!((user.id != null))) throw new Error("Guard 3 failed");
    const doc: Record<string, any> = {
      ...__draft,
      candidateId: candidateId,
      scheduledFor: scheduledFor,
      interviewerPersonId: interviewerPersonId,
      sourceSystem: sourceSystem,
      externalInterviewId: externalInterviewId,
      rawSourceData: rawSourceData,
      bookedAt: Date.now(),
      version: 1,
    };
    const docId = await ctx.db.insert("interviews", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, interviewId: docId, tenantId: doc.tenantId, candidateId: doc.candidateId, _subject: { entity: "Interview", command: "schedule", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "InterviewScheduled", entity: "Interview", entityId: docId, payload: { interviewId: docId, tenantId: doc.tenantId, candidateId: doc.candidateId }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Interview_createViaSchedule", __result);
    }
    return __result;
  },
});

async function __runInventoryItemAdjustQuantity(ctx: MutationCtx, { docId, delta, reason, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("InventoryItem not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("InventoryItem not found");
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may read stock items");
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may write stock items through commands");
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may execute stock item commands");
    if (!((doc.stockedAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((((reason).trim()).length > 0))) throw new Error("Adjustment reason is required");
    if (!(((doc.quantityOnHand + delta) >= 0))) throw new Error("Quantity on hand cannot be negative");
    const previousQuantity = doc.quantityOnHand;
    const nextQuantity = (doc.quantityOnHand + delta);
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      quantityOnHand: nextQuantity,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, inventoryItemId: docId, tenantId: __after.tenantId, actorId: user.id, ingredientId: __after.ingredientId, locationId: __after.locationId, previousQuantity: previousQuantity, quantityOnHand: nextQuantity, delta: delta, unit: __after.unit, reason: reason, _subject: { entity: "InventoryItem", command: "adjustQuantity", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "InventoryQuantityAdjusted", entity: "InventoryItem", entityId: docId, payload: { inventoryItemId: docId, tenantId: __after.tenantId, actorId: user.id, ingredientId: __after.ingredientId, locationId: __after.locationId, previousQuantity: previousQuantity, quantityOnHand: nextQuantity, delta: delta, unit: __after.unit, reason: reason }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const InventoryItem_adjustQuantity = mutation({
  args: {
    docId: v.id("inventoryItems"),
    delta: v.number(),
    reason: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runInventoryItemAdjustQuantity(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "InventoryItem_adjustQuantity", __result);
    }
    return __result;
  },
});

async function __runInventoryItemOpen(ctx: MutationCtx, { docId, ingredientId, locationId, unit, quantityOnHand, parLevel, reorderThreshold, unitCost, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("InventoryItem not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("InventoryItem not found");
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may read stock items");
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may write stock items through commands");
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may execute stock item commands");
    if (!((doc.stockedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((ingredientId === doc.ingredientId))) throw new Error("Open ingredientId must match the seeded ingredient reference");
    if (!((locationId === doc.locationId))) throw new Error("Open locationId must match the seeded location reference");
    if (!(((quantityOnHand == null) || (quantityOnHand >= 0)))) throw new Error("Quantity on hand cannot be negative");
    if (!(((parLevel == null) || (parLevel >= 0)))) throw new Error("Par level cannot be negative");
    if (!(((reorderThreshold == null) || (reorderThreshold >= 0)))) throw new Error("Reorder threshold cannot be negative");
    if (!(((unitCost == null) || (unitCost >= 0)))) throw new Error("Unit cost cannot be negative");
    const previousQuantity = doc.quantityOnHand;
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      ingredientId: ingredientId,
      locationId: locationId,
      unit: unit,
      quantityOnHand: ((quantityOnHand != null) ? quantityOnHand : 0),
      parLevel: ((parLevel != null) ? parLevel : 0),
      reorderThreshold: ((reorderThreshold != null) ? reorderThreshold : 0),
      unitCost: ((unitCost != null) ? unitCost : 0),
      stockedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, inventoryItemId: docId, tenantId: __after.tenantId, actorId: user.id, ingredientId: ingredientId, locationId: locationId, previousQuantity: previousQuantity, quantityOnHand: ((quantityOnHand != null) ? quantityOnHand : 0), unit: unit, unitCost: ((unitCost != null) ? unitCost : 0), _subject: { entity: "InventoryItem", command: "open", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "InventoryItemOpened", entity: "InventoryItem", entityId: docId, payload: { inventoryItemId: docId, tenantId: __after.tenantId, actorId: user.id, ingredientId: ingredientId, locationId: locationId, previousQuantity: previousQuantity, quantityOnHand: ((quantityOnHand != null) ? quantityOnHand : 0), unit: unit, unitCost: ((unitCost != null) ? unitCost : 0) }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const InventoryItem_open = mutation({
  args: {
    docId: v.id("inventoryItems"),
    ingredientId: v.string(),
    locationId: v.string(),
    unit: v.any(),
    quantityOnHand: v.optional(v.number()),
    parLevel: v.optional(v.number()),
    reorderThreshold: v.optional(v.number()),
    unitCost: v.optional(v.number()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runInventoryItemOpen(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "InventoryItem_open", __result);
    }
    return __result;
  },
});

export const InventoryItem_createViaOpen = mutation({
  args: {
    ingredientId: v.string(),
    locationId: v.string(),
    unit: v.any(),
    quantityOnHand: v.optional(v.number()),
    parLevel: v.optional(v.number()),
    reorderThreshold: v.optional(v.number()),
    unitCost: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { ingredientId, locationId, unit, quantityOnHand, parLevel, reorderThreshold, unitCost } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      quantityOnHand: args.quantityOnHand !== undefined ? args.quantityOnHand : 0,
      parLevel: args.parLevel !== undefined ? args.parLevel : 0,
      reorderThreshold: args.reorderThreshold !== undefined ? args.reorderThreshold : 0,
      unitCost: args.unitCost !== undefined ? args.unitCost : 0,
      createdAt: Date.now(),
      updatedAt: Date.now(),
      ingredientId: args.ingredientId,
      locationId: args.locationId,
      unit: args.unit
    };
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may read stock items");
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may write stock items through commands");
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may execute stock item commands");
    if (!((__draft.stockedAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((ingredientId === __draft.ingredientId))) throw new Error("Open ingredientId must match the seeded ingredient reference");
    if (!((locationId === __draft.locationId))) throw new Error("Open locationId must match the seeded location reference");
    if (!(((quantityOnHand == null) || (quantityOnHand >= 0)))) throw new Error("Quantity on hand cannot be negative");
    if (!(((parLevel == null) || (parLevel >= 0)))) throw new Error("Par level cannot be negative");
    if (!(((reorderThreshold == null) || (reorderThreshold >= 0)))) throw new Error("Reorder threshold cannot be negative");
    if (!(((unitCost == null) || (unitCost >= 0)))) throw new Error("Unit cost cannot be negative");
    const doc: Record<string, any> = {
      ...__draft,
      ingredientId: ingredientId,
      locationId: locationId,
      unit: unit,
      quantityOnHand: ((quantityOnHand != null) ? quantityOnHand : 0),
      parLevel: ((parLevel != null) ? parLevel : 0),
      reorderThreshold: ((reorderThreshold != null) ? reorderThreshold : 0),
      unitCost: ((unitCost != null) ? unitCost : 0),
      stockedAt: Date.now(),
      version: 1,
    };
    const docId = await ctx.db.insert("inventoryItems", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, inventoryItemId: docId, tenantId: doc.tenantId, actorId: user.id, ingredientId: doc.ingredientId, locationId: doc.locationId, previousQuantity: doc.previousQuantity, quantityOnHand: ((doc.quantityOnHand != null) ? doc.quantityOnHand : 0), unit: doc.unit, unitCost: ((doc.unitCost != null) ? doc.unitCost : 0), _subject: { entity: "InventoryItem", command: "open", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "InventoryItemOpened", entity: "InventoryItem", entityId: docId, payload: { inventoryItemId: docId, tenantId: doc.tenantId, actorId: user.id, ingredientId: doc.ingredientId, locationId: doc.locationId, previousQuantity: doc.previousQuantity, quantityOnHand: ((doc.quantityOnHand != null) ? doc.quantityOnHand : 0), unit: doc.unit, unitCost: ((doc.unitCost != null) ? doc.unitCost : 0) }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "InventoryItem_createViaOpen", __result);
    }
    return __result;
  },
});

async function __runInventoryItemReceiveStock(ctx: MutationCtx, { docId, quantity, unitCost, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("InventoryItem not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("InventoryItem not found");
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may read stock items");
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may write stock items through commands");
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may execute stock item commands");
    if (!((doc.stockedAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((quantity > 0))) throw new Error("Received quantity must be positive");
    if (!(((unitCost == null) || (unitCost >= 0)))) throw new Error("Unit cost cannot be negative");
    const previousQuantity = doc.quantityOnHand;
    const nextQuantity = (doc.quantityOnHand + quantity);
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      quantityOnHand: nextQuantity,
      unitCost: ((unitCost != null) ? unitCost : doc.unitCost),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, inventoryItemId: docId, tenantId: __after.tenantId, actorId: user.id, ingredientId: __after.ingredientId, locationId: __after.locationId, previousQuantity: previousQuantity, quantityOnHand: nextQuantity, receivedQuantity: quantity, unit: __after.unit, unitCost: ((unitCost != null) ? unitCost : __after.unitCost), _subject: { entity: "InventoryItem", command: "receiveStock", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "InventoryStockReceived", entity: "InventoryItem", entityId: docId, payload: { inventoryItemId: docId, tenantId: __after.tenantId, actorId: user.id, ingredientId: __after.ingredientId, locationId: __after.locationId, previousQuantity: previousQuantity, quantityOnHand: nextQuantity, receivedQuantity: quantity, unit: __after.unit, unitCost: ((unitCost != null) ? unitCost : __after.unitCost) }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const InventoryItem_receiveStock = mutation({
  args: {
    docId: v.id("inventoryItems"),
    quantity: v.number(),
    unitCost: v.optional(v.number()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runInventoryItemReceiveStock(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "InventoryItem_receiveStock", __result);
    }
    return __result;
  },
});

async function __runInventoryItemRecount(ctx: MutationCtx, { docId, actualQuantity, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("InventoryItem not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("InventoryItem not found");
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may read stock items");
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may write stock items through commands");
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may execute stock item commands");
    if (!((doc.stockedAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((actualQuantity >= 0))) throw new Error("Recount quantity cannot be negative");
    const previousQuantity = doc.quantityOnHand;
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      quantityOnHand: actualQuantity,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, inventoryItemId: docId, tenantId: __after.tenantId, actorId: user.id, ingredientId: __after.ingredientId, locationId: __after.locationId, previousQuantity: previousQuantity, quantityOnHand: actualQuantity, unit: __after.unit, _subject: { entity: "InventoryItem", command: "recount", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "InventoryRecounted", entity: "InventoryItem", entityId: docId, payload: { inventoryItemId: docId, tenantId: __after.tenantId, actorId: user.id, ingredientId: __after.ingredientId, locationId: __after.locationId, previousQuantity: previousQuantity, quantityOnHand: actualQuantity, unit: __after.unit }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const InventoryItem_recount = mutation({
  args: {
    docId: v.id("inventoryItems"),
    actualQuantity: v.number(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runInventoryItemRecount(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "InventoryItem_recount", __result);
    }
    return __result;
  },
});

async function __runInventoryItemRemove(ctx: MutationCtx, { docId, reason, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("InventoryItem not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("InventoryItem not found");
    (doc as any).reservations = await ctx.db.query("inventoryReservations").withIndex("by_inventoryItemId", (q: any) => q.eq("inventoryItemId", docId)).collect();
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may read stock items");
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may write stock items through commands");
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may execute stock item commands");
    if (!((doc.stockedAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(checkRole(user, "inventoryManageAccess"))) throw new Error("Guard 2 failed");
    if (!((((doc.reservations) ?? []).filter((r: Doc<"inventoryReservations">) => ((r.status === "active"))).length === 0))) throw new Error("Guard 3 failed");
    if (!((((reason).trim()).length > 0))) throw new Error("Removal reason is required");
    const previousQuantity = doc.quantityOnHand;
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      deletedAt: Date.now(),
      removedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, inventoryItemId: docId, tenantId: __after.tenantId, ingredientId: __after.ingredientId, locationId: __after.locationId, previousQuantity: previousQuantity, quantityOnHand: __after.quantityOnHand, reason: reason, _subject: { entity: "InventoryItem", command: "remove", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "InventoryItemRemoved", entity: "InventoryItem", entityId: docId, payload: { inventoryItemId: docId, tenantId: __after.tenantId, ingredientId: __after.ingredientId, locationId: __after.locationId, previousQuantity: previousQuantity, quantityOnHand: __after.quantityOnHand, reason: reason }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const InventoryItem_remove = mutation({
  args: {
    docId: v.id("inventoryItems"),
    reason: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runInventoryItemRemove(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "InventoryItem_remove", __result);
    }
    return __result;
  },
});

async function __runInventoryItemSetExpiry(ctx: MutationCtx, { docId, bestBeforeAt, useByAt, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("InventoryItem not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("InventoryItem not found");
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may read stock items");
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may write stock items through commands");
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may execute stock item commands");
    if (!((doc.stockedAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((((bestBeforeAt == null) || (useByAt == null)) || (bestBeforeAt <= useByAt)))) throw new Error("Best-before cannot be after use-by");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      bestBeforeAt: bestBeforeAt,
      useByAt: useByAt,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, inventoryItemId: docId, tenantId: __after.tenantId, ingredientId: __after.ingredientId, locationId: __after.locationId, bestBeforeAt: bestBeforeAt, useByAt: useByAt, _subject: { entity: "InventoryItem", command: "setExpiry", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "InventoryExpiryUpdated", entity: "InventoryItem", entityId: docId, payload: { inventoryItemId: docId, tenantId: __after.tenantId, ingredientId: __after.ingredientId, locationId: __after.locationId, bestBeforeAt: bestBeforeAt, useByAt: useByAt }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const InventoryItem_setExpiry = mutation({
  args: {
    docId: v.id("inventoryItems"),
    bestBeforeAt: v.optional(v.number()),
    useByAt: v.optional(v.number()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runInventoryItemSetExpiry(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "InventoryItem_setExpiry", __result);
    }
    return __result;
  },
});

async function __runInventoryItemTransferIn(ctx: MutationCtx, { docId, quantity, sourceLocationId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("InventoryItem not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("InventoryItem not found");
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may read stock items");
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may write stock items through commands");
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may execute stock item commands");
    if (!((doc.stockedAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((quantity > 0))) throw new Error("Transfer quantity must be positive");
    const previousQuantity = doc.quantityOnHand;
    const nextQuantity = (doc.quantityOnHand + quantity);
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      quantityOnHand: nextQuantity,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, inventoryItemId: docId, tenantId: __after.tenantId, actorId: user.id, ingredientId: __after.ingredientId, locationId: __after.locationId, sourceLocationId: sourceLocationId, previousQuantity: previousQuantity, quantityOnHand: nextQuantity, transferredQuantity: quantity, unit: __after.unit, _subject: { entity: "InventoryItem", command: "transferIn", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "InventoryTransferredIn", entity: "InventoryItem", entityId: docId, payload: { inventoryItemId: docId, tenantId: __after.tenantId, actorId: user.id, ingredientId: __after.ingredientId, locationId: __after.locationId, sourceLocationId: sourceLocationId, previousQuantity: previousQuantity, quantityOnHand: nextQuantity, transferredQuantity: quantity, unit: __after.unit }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const InventoryItem_transferIn = mutation({
  args: {
    docId: v.id("inventoryItems"),
    quantity: v.number(),
    sourceLocationId: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runInventoryItemTransferIn(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "InventoryItem_transferIn", __result);
    }
    return __result;
  },
});

async function __runInventoryItemTransferOut(ctx: MutationCtx, { docId, quantity, destinationLocationId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("InventoryItem not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("InventoryItem not found");
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may read stock items");
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may write stock items through commands");
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may execute stock item commands");
    if (!((doc.stockedAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((quantity > 0))) throw new Error("Transfer quantity must be positive");
    if (!((doc.quantityOnHand >= quantity))) throw new Error("Cannot transfer more than quantity on hand");
    const previousQuantity = doc.quantityOnHand;
    const nextQuantity = (doc.quantityOnHand - quantity);
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      quantityOnHand: nextQuantity,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, inventoryItemId: docId, tenantId: __after.tenantId, actorId: user.id, ingredientId: __after.ingredientId, locationId: __after.locationId, destinationLocationId: destinationLocationId, previousQuantity: previousQuantity, quantityOnHand: nextQuantity, transferredQuantity: quantity, unit: __after.unit, _subject: { entity: "InventoryItem", command: "transferOut", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "InventoryTransferredOut", entity: "InventoryItem", entityId: docId, payload: { inventoryItemId: docId, tenantId: __after.tenantId, actorId: user.id, ingredientId: __after.ingredientId, locationId: __after.locationId, destinationLocationId: destinationLocationId, previousQuantity: previousQuantity, quantityOnHand: nextQuantity, transferredQuantity: quantity, unit: __after.unit }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const InventoryItem_transferOut = mutation({
  args: {
    docId: v.id("inventoryItems"),
    quantity: v.number(),
    destinationLocationId: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runInventoryItemTransferOut(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "InventoryItem_transferOut", __result);
    }
    return __result;
  },
});

async function __runInventoryItemUpdateLevels(ctx: MutationCtx, { docId, parLevel, reorderThreshold, unitCost, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("InventoryItem not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("InventoryItem not found");
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may read stock items");
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may write stock items through commands");
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may execute stock item commands");
    if (!((doc.stockedAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(checkRole(user, "inventoryManageAccess"))) throw new Error("Guard 2 failed");
    if (!((parLevel >= 0))) throw new Error("Par level cannot be negative");
    if (!((reorderThreshold >= 0))) throw new Error("Reorder threshold cannot be negative");
    if (!(((unitCost == null) || (unitCost >= 0)))) throw new Error("Unit cost cannot be negative");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      parLevel: parLevel,
      reorderThreshold: reorderThreshold,
      unitCost: ((unitCost != null) ? unitCost : doc.unitCost),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, inventoryItemId: docId, tenantId: __after.tenantId, ingredientId: __after.ingredientId, locationId: __after.locationId, parLevel: parLevel, reorderThreshold: reorderThreshold, unitCost: ((unitCost != null) ? unitCost : __after.unitCost), _subject: { entity: "InventoryItem", command: "updateLevels", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "InventoryLevelsUpdated", entity: "InventoryItem", entityId: docId, payload: { inventoryItemId: docId, tenantId: __after.tenantId, ingredientId: __after.ingredientId, locationId: __after.locationId, parLevel: parLevel, reorderThreshold: reorderThreshold, unitCost: ((unitCost != null) ? unitCost : __after.unitCost) }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const InventoryItem_updateLevels = mutation({
  args: {
    docId: v.id("inventoryItems"),
    parLevel: v.number(),
    reorderThreshold: v.number(),
    unitCost: v.optional(v.number()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runInventoryItemUpdateLevels(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "InventoryItem_updateLevels", __result);
    }
    return __result;
  },
});

async function __runInventoryLotRecord(ctx: MutationCtx, { docId, supplierLotNumber, vendorOrderLineId, vendorOrderId, vendorId, ingredientId, ingredientDemandId, eventId, locationId, receiptQuantity, cumulativeReceivedQuantity, unit, unitCost, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("InventoryLot not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("InventoryLot not found");
    if (!(((checkRole(user, "inventoryAccess") || checkRole(user, "procurementAccess")) || checkRole(user, "manageAccess")))) throw new Error("Inventory, procurement, and managers may read receipt lots");
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may record receipt lots");
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may execute receipt lot commands");
    if (!(((doc.receivedAt == null) || ((((((((((((doc.supplierLotNumber === supplierLotNumber) && (doc.vendorOrderLineId === vendorOrderLineId)) && (doc.vendorOrderId === vendorOrderId)) && (doc.vendorId === vendorId)) && (doc.ingredientId === ingredientId)) && (doc.ingredientDemandId === ingredientDemandId)) && (doc.eventId === eventId)) && (doc.locationId === locationId)) && (doc.receiptQuantity === receiptQuantity)) && (doc.cumulativeReceivedQuantity === cumulativeReceivedQuantity)) && (doc.unit === unit)) && (doc.unitCost === unitCost))))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((((supplierLotNumber).trim()).length > 0))) throw new Error("Supplier lot number is required for traceability");
    if (!((receiptQuantity > 0))) throw new Error("Inventory lot receipt quantity must be positive");
    if (!((cumulativeReceivedQuantity > 0))) throw new Error("Inventory lot cumulative quantity must be positive");
    if (!((unitCost >= 0))) throw new Error("Inventory lot unit cost cannot be negative");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      supplierLotNumber: supplierLotNumber,
      vendorOrderLineId: vendorOrderLineId,
      vendorOrderId: vendorOrderId,
      vendorId: vendorId,
      ingredientId: ingredientId,
      ingredientDemandId: ingredientDemandId,
      eventId: eventId,
      locationId: locationId,
      receiptQuantity: receiptQuantity,
      cumulativeReceivedQuantity: cumulativeReceivedQuantity,
      unit: unit,
      unitCost: unitCost,
      receivedAt: ((doc.receivedAt == null) ? Date.now() : doc.receivedAt),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    return { ...doc, ...updates };
}

export const InventoryLot_record = mutation({
  args: {
    docId: v.id("inventoryLots"),
    supplierLotNumber: v.string(),
    vendorOrderLineId: v.string(),
    vendorOrderId: v.string(),
    vendorId: v.string(),
    ingredientId: v.string(),
    ingredientDemandId: v.optional(v.string()),
    eventId: v.optional(v.string()),
    locationId: v.string(),
    receiptQuantity: v.number(),
    cumulativeReceivedQuantity: v.number(),
    unit: v.any(),
    unitCost: v.number(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runInventoryLotRecord(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "InventoryLot_record", __result);
    }
    return __result;
  },
});

export const InventoryLot_createViaRecord = mutation({
  args: {
    supplierLotNumber: v.string(),
    vendorOrderLineId: v.string(),
    vendorOrderId: v.string(),
    vendorId: v.string(),
    ingredientId: v.string(),
    ingredientDemandId: v.optional(v.string()),
    eventId: v.optional(v.string()),
    locationId: v.string(),
    receiptQuantity: v.number(),
    cumulativeReceivedQuantity: v.number(),
    unit: v.any(),
    unitCost: v.number(),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { supplierLotNumber, vendorOrderLineId, vendorOrderId, vendorId, ingredientId, ingredientDemandId, eventId, locationId, receiptQuantity, cumulativeReceivedQuantity, unit, unitCost } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      createdAt: Date.now(),
      updatedAt: Date.now(),
      cumulativeReceivedQuantity: args.cumulativeReceivedQuantity,
      eventId: args.eventId,
      ingredientDemandId: args.ingredientDemandId,
      ingredientId: args.ingredientId,
      locationId: args.locationId,
      receiptQuantity: args.receiptQuantity,
      supplierLotNumber: args.supplierLotNumber,
      unit: args.unit,
      unitCost: args.unitCost,
      vendorId: args.vendorId,
      vendorOrderId: args.vendorOrderId,
      vendorOrderLineId: args.vendorOrderLineId
    };
    if (!(((checkRole(user, "inventoryAccess") || checkRole(user, "procurementAccess")) || checkRole(user, "manageAccess")))) throw new Error("Inventory, procurement, and managers may read receipt lots");
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may record receipt lots");
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may execute receipt lot commands");
    if (!(((__draft.receivedAt == null) || ((((((((((((__draft.supplierLotNumber === supplierLotNumber) && (__draft.vendorOrderLineId === vendorOrderLineId)) && (__draft.vendorOrderId === vendorOrderId)) && (__draft.vendorId === vendorId)) && (__draft.ingredientId === ingredientId)) && (__draft.ingredientDemandId === ingredientDemandId)) && (__draft.eventId === eventId)) && (__draft.locationId === locationId)) && (__draft.receiptQuantity === receiptQuantity)) && (__draft.cumulativeReceivedQuantity === cumulativeReceivedQuantity)) && (__draft.unit === unit)) && (__draft.unitCost === unitCost))))) throw new Error("Guard 0 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((((supplierLotNumber).trim()).length > 0))) throw new Error("Supplier lot number is required for traceability");
    if (!((receiptQuantity > 0))) throw new Error("Inventory lot receipt quantity must be positive");
    if (!((cumulativeReceivedQuantity > 0))) throw new Error("Inventory lot cumulative quantity must be positive");
    if (!((unitCost >= 0))) throw new Error("Inventory lot unit cost cannot be negative");
    const doc: Record<string, any> = {
      ...__draft,
      supplierLotNumber: supplierLotNumber,
      vendorOrderLineId: vendorOrderLineId,
      vendorOrderId: vendorOrderId,
      vendorId: vendorId,
      ingredientId: ingredientId,
      ingredientDemandId: ingredientDemandId,
      eventId: eventId,
      locationId: locationId,
      receiptQuantity: receiptQuantity,
      cumulativeReceivedQuantity: cumulativeReceivedQuantity,
      unit: unit,
      unitCost: unitCost,
      receivedAt: ((__draft.receivedAt == null) ? Date.now() : __draft.receivedAt),
      version: 1,
    };
    const docId = await ctx.db.insert("inventoryLots", doc as any);
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "InventoryLot_createViaRecord", __result);
    }
    return __result;
  },
});

async function __runInventoryReservationConsume(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("InventoryReservation not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("InventoryReservation not found");
    const __rel_inventoryItem = await __resolveRelation(ctx, "inventoryItems", [__auth.tenantId, doc.inventoryItemId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).inventoryItemId;
      ((doc as any) as any).inventoryItem = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "eventManageAccess")))) throw new Error("Inventory staff or event managers may read inventory reservations");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "eventManageAccess")))) throw new Error("Inventory staff or event managers may write inventory reservations through commands");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "eventManageAccess")))) throw new Error("Inventory staff or event managers may execute reservation commands");
    if (!((doc.status === "active"))) throw new Error("Guard 0 failed");
    if (!((doc.reservedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((((__rel_inventoryItem == null) || (__rel_inventoryItem.useByAt == null)) || (__rel_inventoryItem.useByAt >= Date.now())))) throw new Error("Guard 3 failed");
    const previousQuantity = doc.quantity;
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "consumed";
        const __allowed: Record<string, string[]> = { "pending": ["active"], "active": ["released", "consumed"], "released": [], "consumed": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "consumed",
      consumedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, inventoryReservationId: docId, tenantId: __after.tenantId, actorId: user.id, inventoryItemId: __after.inventoryItemId, inventoryLotId: __after.inventoryLotId, eventId: __after.eventId, ingredientId: __after.ingredientId, previousQuantity: previousQuantity, quantity: __after.quantity, _subject: { entity: "InventoryReservation", command: "consume", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "InventoryReservationConsumed", entity: "InventoryReservation", entityId: docId, payload: { inventoryReservationId: docId, tenantId: __after.tenantId, actorId: user.id, inventoryItemId: __after.inventoryItemId, inventoryLotId: __after.inventoryLotId, eventId: __after.eventId, ingredientId: __after.ingredientId, previousQuantity: previousQuantity, quantity: __after.quantity }, createdAt: Date.now() });
    // Reactions
    const reactionTarget0 = payload.inventoryItemId;
    if (reactionTarget0) await __runInventoryItemAdjustQuantity(ctx, { docId: reactionTarget0, delta: (0 - payload.quantity), reason: "Reservation consumed" } as any);
    return { ...doc, ...updates };
}

export const InventoryReservation_consume = mutation({
  args: {
    docId: v.id("inventoryReservations"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runInventoryReservationConsume(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "InventoryReservation_consume", __result);
    }
    return __result;
  },
});

async function __runInventoryReservationRelease(ctx: MutationCtx, { docId, reason, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("InventoryReservation not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("InventoryReservation not found");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "eventManageAccess")))) throw new Error("Inventory staff or event managers may read inventory reservations");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "eventManageAccess")))) throw new Error("Inventory staff or event managers may write inventory reservations through commands");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "eventManageAccess")))) throw new Error("Inventory staff or event managers may execute reservation commands");
    if (!((doc.status === "active"))) throw new Error("Guard 0 failed");
    if (!((doc.reservedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((((reason).trim()).length > 0))) throw new Error("Release reason is required");
    const previousQuantity = doc.quantity;
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "released";
        const __allowed: Record<string, string[]> = { "pending": ["active"], "active": ["released", "consumed"], "released": [], "consumed": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "released",
      releasedAt: Date.now(),
      releaseReason: reason,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, inventoryReservationId: docId, tenantId: __after.tenantId, actorId: user.id, inventoryItemId: __after.inventoryItemId, inventoryLotId: __after.inventoryLotId, eventId: __after.eventId, ingredientId: __after.ingredientId, previousQuantity: previousQuantity, quantity: __after.quantity, reason: reason, _subject: { entity: "InventoryReservation", command: "release", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "InventoryReservationReleased", entity: "InventoryReservation", entityId: docId, payload: { inventoryReservationId: docId, tenantId: __after.tenantId, actorId: user.id, inventoryItemId: __after.inventoryItemId, inventoryLotId: __after.inventoryLotId, eventId: __after.eventId, ingredientId: __after.ingredientId, previousQuantity: previousQuantity, quantity: __after.quantity, reason: reason }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const InventoryReservation_release = mutation({
  args: {
    docId: v.id("inventoryReservations"),
    reason: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runInventoryReservationRelease(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "InventoryReservation_release", __result);
    }
    return __result;
  },
});

async function __runInventoryReservationReserve(ctx: MutationCtx, { docId, inventoryItemId, eventId, ingredientId, quantity, inventoryLotId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("InventoryReservation not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("InventoryReservation not found");
    const __rel_inventoryItem = await __resolveRelation(ctx, "inventoryItems", [__auth.tenantId, doc.inventoryItemId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_inventoryLot = await __resolveRelation(ctx, "inventoryLots", [__auth.tenantId, doc.inventoryLotId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_event = await __resolveRelation(ctx, "events", [__auth.tenantId, doc.eventId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).eventId;
      ((doc as any) as any).event = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    {
      const __fk = ((doc as any) as any).inventoryItemId;
      ((doc as any) as any).inventoryItem = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    {
      const __fk = ((doc as any) as any).inventoryLotId;
      ((doc as any) as any).inventoryLot = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "eventManageAccess")))) throw new Error("Inventory staff or event managers may read inventory reservations");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "eventManageAccess")))) throw new Error("Inventory staff or event managers may write inventory reservations through commands");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "eventManageAccess")))) throw new Error("Inventory staff or event managers may execute reservation commands");
    if (!((doc.reservedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.status === "pending"))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!(((__rel_event != null) && ((((__rel_event.stage === "planning") || (__rel_event.stage === "pending_approval")) || (__rel_event.stage === "approved")) || (__rel_event.stage === "executing"))))) throw new Error("Guard 3 failed");
    if (!((((__rel_inventoryItem != null) && (__rel_inventoryItem.stockedAt != null)) && (__rel_inventoryItem.deletedAt == null)))) throw new Error("Guard 4 failed");
    if (!((inventoryItemId === doc.inventoryItemId))) throw new Error("Reserve inventoryItemId must match the seeded stock reference");
    if (!((eventId === doc.eventId))) throw new Error("Reserve eventId must match the seeded event reference");
    if (!((ingredientId === __rel_inventoryItem.ingredientId))) throw new Error("Reserve ingredientId must match the stock ingredient");
    if (!(((inventoryLotId == null) || ((((__rel_inventoryLot != null) && (__rel_inventoryLot.deletedAt == null)) && (__rel_inventoryLot.ingredientId === ingredientId)) && (__rel_inventoryLot.locationId === __rel_inventoryItem.locationId))))) throw new Error("Selected lot must match the reserved ingredient and stock location");
    if (!((quantity > 0))) throw new Error("Reservation quantity must be positive");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "active";
        const __allowed: Record<string, string[]> = { "pending": ["active"], "active": ["released", "consumed"], "released": [], "consumed": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      inventoryItemId: inventoryItemId,
      inventoryLotId: inventoryLotId,
      eventId: eventId,
      ingredientId: ingredientId,
      quantity: quantity,
      status: "active",
      reservedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, inventoryReservationId: docId, tenantId: __after.tenantId, actorId: user.id, inventoryItemId: inventoryItemId, inventoryLotId: inventoryLotId, eventId: eventId, ingredientId: ingredientId, quantity: quantity, _subject: { entity: "InventoryReservation", command: "reserve", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "InventoryReserved", entity: "InventoryReservation", entityId: docId, payload: { inventoryReservationId: docId, tenantId: __after.tenantId, actorId: user.id, inventoryItemId: inventoryItemId, inventoryLotId: inventoryLotId, eventId: eventId, ingredientId: ingredientId, quantity: quantity }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const InventoryReservation_reserve = mutation({
  args: {
    docId: v.id("inventoryReservations"),
    inventoryItemId: v.string(),
    eventId: v.string(),
    ingredientId: v.string(),
    quantity: v.number(),
    inventoryLotId: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runInventoryReservationReserve(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "InventoryReservation_reserve", __result);
    }
    return __result;
  },
});

export const InventoryReservation_createViaReserve = mutation({
  args: {
    inventoryItemId: v.string(),
    eventId: v.string(),
    ingredientId: v.string(),
    quantity: v.number(),
    inventoryLotId: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { inventoryItemId, eventId, ingredientId, quantity, inventoryLotId } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      createdAt: Date.now(),
      updatedAt: Date.now(),
      status: "pending",
      eventId: args.eventId,
      ingredientId: args.ingredientId,
      inventoryItemId: args.inventoryItemId,
      inventoryLotId: args.inventoryLotId,
      quantity: args.quantity
    };
    const __rel_inventoryItem = await __resolveRelation(ctx, "inventoryItems", [__auth.tenantId, __draft.inventoryItemId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_inventoryLot = await __resolveRelation(ctx, "inventoryLots", [__auth.tenantId, __draft.inventoryLotId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_event = await __resolveRelation(ctx, "events", [__auth.tenantId, __draft.eventId], ["tenantId","id"], "tenantId", __auth.tenantId);
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "eventManageAccess")))) throw new Error("Inventory staff or event managers may read inventory reservations");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "eventManageAccess")))) throw new Error("Inventory staff or event managers may write inventory reservations through commands");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "eventManageAccess")))) throw new Error("Inventory staff or event managers may execute reservation commands");
    if (!((__draft.reservedAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.status === "pending"))) throw new Error("Guard 1 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!(((__rel_event != null) && ((((__rel_event.stage === "planning") || (__rel_event.stage === "pending_approval")) || (__rel_event.stage === "approved")) || (__rel_event.stage === "executing"))))) throw new Error("Guard 3 failed");
    if (!((((__rel_inventoryItem != null) && (__rel_inventoryItem.stockedAt != null)) && (__rel_inventoryItem.deletedAt == null)))) throw new Error("Guard 4 failed");
    if (!((inventoryItemId === __draft.inventoryItemId))) throw new Error("Reserve inventoryItemId must match the seeded stock reference");
    if (!((eventId === __draft.eventId))) throw new Error("Reserve eventId must match the seeded event reference");
    if (!((ingredientId === __rel_inventoryItem.ingredientId))) throw new Error("Reserve ingredientId must match the stock ingredient");
    if (!(((inventoryLotId == null) || ((((__rel_inventoryLot != null) && (__rel_inventoryLot.deletedAt == null)) && (__rel_inventoryLot.ingredientId === ingredientId)) && (__rel_inventoryLot.locationId === __rel_inventoryItem.locationId))))) throw new Error("Selected lot must match the reserved ingredient and stock location");
    if (!((quantity > 0))) throw new Error("Reservation quantity must be positive");
    const doc: Record<string, any> = {
      ...__draft,
      inventoryItemId: inventoryItemId,
      inventoryLotId: inventoryLotId,
      eventId: eventId,
      ingredientId: ingredientId,
      quantity: quantity,
      status: "active",
      reservedAt: Date.now(),
      version: 1,
    };
    const docId = await ctx.db.insert("inventoryReservations", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, inventoryReservationId: docId, tenantId: doc.tenantId, actorId: user.id, inventoryItemId: doc.inventoryItemId, inventoryLotId: doc.inventoryLotId, eventId: doc.eventId, ingredientId: doc.ingredientId, quantity: doc.quantity, _subject: { entity: "InventoryReservation", command: "reserve", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "InventoryReserved", entity: "InventoryReservation", entityId: docId, payload: { inventoryReservationId: docId, tenantId: doc.tenantId, actorId: user.id, inventoryItemId: doc.inventoryItemId, inventoryLotId: doc.inventoryLotId, eventId: doc.eventId, ingredientId: doc.ingredientId, quantity: doc.quantity }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "InventoryReservation_createViaReserve", __result);
    }
    return __result;
  },
});

async function __runInvoiceApplyCredit(ctx: MutationCtx, { docId, creditAmount, creditMemoId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Invoice not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Invoice not found");
    if (!((checkRole(user, "financeAccess") || checkRole(user, "manageAccess")))) throw new Error("Finance staff and managers may read invoices");
    if (!((checkRole(user, "financeAccess") || checkRole(user, "manageAccess")))) throw new Error("Finance staff and managers may write invoices through commands");
    if (!((checkRole(user, "financeAccess") || checkRole(user, "manageAccess")))) throw new Error("Finance staff and managers may execute invoice commands");
    if (!(((((doc.status === "sent") || (doc.status === "viewed")) || (doc.status === "overdue")) || (doc.status === "partial")))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((creditAmount > 0))) throw new Error("Applied credit must be positive");
    if (!((creditAmount <= doc.amountDue))) throw new Error("Applied credit cannot exceed the target invoice balance");
    if (!((((creditMemoId).trim()).length > 0))) throw new Error("Credit memo id is required");
    const priorAmountCredited = ((doc.amountCredited != null) ? doc.amountCredited : 0);
    const nextAmountCredited = (priorAmountCredited + creditAmount);
    const nextDue = (doc.amountDue - creditAmount);
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = String(((nextDue === 0) ? "paid" : "partial"));
        const __allowed: Record<string, string[]> = { "draft": ["sent", "voided"], "sent": ["viewed", "overdue", "partial", "paid", "voided"], "viewed": ["overdue", "partial", "paid", "voided"], "overdue": ["partial", "paid", "written_off", "voided"], "partial": ["partial", "paid", "overdue", "written_off"], "paid": ["partial"], "voided": [], "written_off": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      amountCredited: nextAmountCredited,
      amountDue: nextDue,
      status: ((nextDue === 0) ? "paid" : "partial"),
      paidAt: ((nextDue === 0) ? Date.now() : doc.paidAt),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, invoiceId: docId, tenantId: __after.tenantId, clientId: __after.clientId, eventId: __after.eventId, creditMemoId: creditMemoId, creditAmount: creditAmount, amountCredited: nextAmountCredited, amountDue: nextDue, _subject: { entity: "Invoice", command: "applyCredit", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "InvoiceCreditApplied", entity: "Invoice", entityId: docId, payload: { invoiceId: docId, tenantId: __after.tenantId, clientId: __after.clientId, eventId: __after.eventId, creditMemoId: creditMemoId, creditAmount: creditAmount, amountCredited: nextAmountCredited, amountDue: nextDue }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Invoice_applyCredit = mutation({
  args: {
    docId: v.id("invoices"),
    creditAmount: v.number(),
    creditMemoId: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runInvoiceApplyCredit(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Invoice_applyCredit", __result);
    }
    return __result;
  },
});

async function __runInvoiceApplyPayment(ctx: MutationCtx, { docId, paymentAmount, paymentId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Invoice not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Invoice not found");
    if (!((checkRole(user, "financeAccess") || checkRole(user, "manageAccess")))) throw new Error("Finance staff and managers may read invoices");
    if (!((checkRole(user, "financeAccess") || checkRole(user, "manageAccess")))) throw new Error("Finance staff and managers may write invoices through commands");
    if (!((checkRole(user, "financeAccess") || checkRole(user, "manageAccess")))) throw new Error("Finance staff and managers may execute invoice commands");
    if (!(((((doc.status === "sent") || (doc.status === "viewed")) || (doc.status === "overdue")) || (doc.status === "partial")))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((paymentAmount > 0))) throw new Error("Payment amount must be positive");
    if (!((paymentAmount <= doc.amountDue))) throw new Error("Payment amount cannot exceed amount due");
    const nextPaid = (doc.amountPaid + paymentAmount);
    const nextDue = (doc.amountDue - paymentAmount);
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = String(((nextDue === 0) ? "paid" : "partial"));
        const __allowed: Record<string, string[]> = { "draft": ["sent", "voided"], "sent": ["viewed", "overdue", "partial", "paid", "voided"], "viewed": ["overdue", "partial", "paid", "voided"], "overdue": ["partial", "paid", "written_off", "voided"], "partial": ["partial", "paid", "overdue", "written_off"], "paid": ["partial"], "voided": [], "written_off": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      amountPaid: nextPaid,
      amountDue: nextDue,
      status: ((nextDue === 0) ? "paid" : "partial"),
      paidAt: ((nextDue === 0) ? Date.now() : doc.paidAt),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, invoiceId: docId, tenantId: __after.tenantId, clientId: __after.clientId, eventId: __after.eventId, paymentId: paymentId, paymentAmount: paymentAmount, amountPaid: nextPaid, amountDue: nextDue, status: ((nextDue === 0) ? "paid" : "partial"), _subject: { entity: "Invoice", command: "applyPayment", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "InvoicePaymentApplied", entity: "Invoice", entityId: docId, payload: { invoiceId: docId, tenantId: __after.tenantId, clientId: __after.clientId, eventId: __after.eventId, paymentId: paymentId, paymentAmount: paymentAmount, amountPaid: nextPaid, amountDue: nextDue, status: ((nextDue === 0) ? "paid" : "partial") }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Invoice_applyPayment = mutation({
  args: {
    docId: v.id("invoices"),
    paymentAmount: v.number(),
    paymentId: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runInvoiceApplyPayment(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Invoice_applyPayment", __result);
    }
    return __result;
  },
});

async function __runInvoiceIssue(ctx: MutationCtx, { docId, clientId, invoiceNumber, subtotal, taxAmount, discountAmount, total, eventId, paymentTermsDays, dueDate, notes, lineItems, taxBreakdown, currencyCode, exchangeRate, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Invoice not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Invoice not found");
    const __rel_client = await __resolveRelation(ctx, "clients", [__auth.tenantId, doc.clientId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).clientId;
      ((doc as any) as any).client = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!((checkRole(user, "financeAccess") || checkRole(user, "manageAccess")))) throw new Error("Finance staff and managers may read invoices");
    if (!((checkRole(user, "financeAccess") || checkRole(user, "manageAccess")))) throw new Error("Finance staff and managers may write invoices through commands");
    if (!((checkRole(user, "financeAccess") || checkRole(user, "manageAccess")))) throw new Error("Finance staff and managers may execute invoice commands");
    if (!((doc.issuedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.status === "draft"))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((__rel_client != null))) throw new Error("Guard 3 failed");
    if (!((clientId === doc.clientId))) throw new Error("Issue clientId must match the seeded client reference");
    if (!((((eventId == null) || (doc.eventId == null)) || (eventId === doc.eventId)))) throw new Error("Issue eventId must match the seeded event reference when both are set");
    if (!((((invoiceNumber).trim()).length > 0))) throw new Error("Invoice number is required");
    if (!(((((subtotal >= 0) && (taxAmount >= 0)) && (discountAmount >= 0)) && (total >= 0)))) throw new Error("Invoice money amounts cannot be negative");
    if (!((total === ((subtotal + taxAmount) - discountAmount)))) throw new Error("Invoice total must equal subtotal plus tax minus discount");
    if (!(((currencyCode == null) || (((currencyCode).trim()).length === 3)))) throw new Error("Currency code must be a three-letter ISO 4217 code");
    if (!(((exchangeRate == null) || (exchangeRate > 0)))) throw new Error("Exchange rate must be greater than zero");
    const normalizedCurrencyCode = ((currencyCode != null) ? (currencyCode).toUpperCase() : null);
    const resolvedExchangeRate = ((exchangeRate != null) ? exchangeRate : 1);
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      clientId: clientId,
      eventId: ((eventId != null) ? eventId : doc.eventId),
      invoiceNumber: invoiceNumber,
      subtotal: subtotal,
      taxAmount: taxAmount,
      discountAmount: discountAmount,
      total: total,
      amountPaid: 0,
      amountDue: total,
      amountCredited: 0,
      creditMemoAmount: 0,
      lineItems: ((lineItems != null) ? lineItems : []),
      taxBreakdown: ((taxBreakdown != null) ? taxBreakdown : []),
      paymentTermsDays: ((paymentTermsDays != null) ? paymentTermsDays : 30),
      dueDate: dueDate,
      notes: notes,
      currencyCode: normalizedCurrencyCode,
      exchangeRate: resolvedExchangeRate,
      issuedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, invoiceId: docId, tenantId: __after.tenantId, clientId: clientId, eventId: ((eventId != null) ? eventId : __after.eventId), invoiceNumber: invoiceNumber, total: total, amountDue: total, currencyCode: normalizedCurrencyCode, exchangeRate: resolvedExchangeRate, _subject: { entity: "Invoice", command: "issue", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "InvoiceIssued", entity: "Invoice", entityId: docId, payload: { invoiceId: docId, tenantId: __after.tenantId, clientId: clientId, eventId: ((eventId != null) ? eventId : __after.eventId), invoiceNumber: invoiceNumber, total: total, amountDue: total, currencyCode: normalizedCurrencyCode, exchangeRate: resolvedExchangeRate }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Invoice_issue = mutation({
  args: {
    docId: v.id("invoices"),
    clientId: v.string(),
    invoiceNumber: v.string(),
    subtotal: v.number(),
    taxAmount: v.number(),
    discountAmount: v.number(),
    total: v.number(),
    eventId: v.optional(v.string()),
    paymentTermsDays: v.optional(v.any()),
    dueDate: v.optional(v.number()),
    notes: v.optional(v.string()),
    lineItems: v.optional(v.any()),
    taxBreakdown: v.optional(v.any()),
    currencyCode: v.optional(v.string()),
    exchangeRate: v.optional(v.number()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runInvoiceIssue(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Invoice_issue", __result);
    }
    return __result;
  },
});

export const Invoice_createViaIssue = mutation({
  args: {
    clientId: v.string(),
    invoiceNumber: v.string(),
    subtotal: v.number(),
    taxAmount: v.number(),
    discountAmount: v.number(),
    total: v.number(),
    eventId: v.optional(v.string()),
    paymentTermsDays: v.optional(v.any()),
    dueDate: v.optional(v.number()),
    notes: v.optional(v.string()),
    lineItems: v.optional(v.any()),
    taxBreakdown: v.optional(v.any()),
    currencyCode: v.optional(v.string()),
    exchangeRate: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { clientId, invoiceNumber, subtotal, taxAmount, discountAmount, total, eventId, paymentTermsDays, dueDate, notes, lineItems, taxBreakdown, currencyCode, exchangeRate } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      paymentTermsDays: args.paymentTermsDays !== undefined ? args.paymentTermsDays : 30,
      status: "draft",
      createdAt: Date.now(),
      updatedAt: Date.now(),
      clientId: args.clientId,
      currencyCode: args.currencyCode,
      discountAmount: args.discountAmount,
      dueDate: args.dueDate,
      eventId: args.eventId,
      exchangeRate: args.exchangeRate,
      invoiceNumber: args.invoiceNumber,
      lineItems: args.lineItems,
      notes: args.notes,
      subtotal: args.subtotal,
      taxAmount: args.taxAmount,
      taxBreakdown: args.taxBreakdown,
      total: args.total
    };
    const __rel_client = await __resolveRelation(ctx, "clients", [__auth.tenantId, __draft.clientId], ["tenantId","id"], "tenantId", __auth.tenantId);
    if (!((checkRole(user, "financeAccess") || checkRole(user, "manageAccess")))) throw new Error("Finance staff and managers may read invoices");
    if (!((checkRole(user, "financeAccess") || checkRole(user, "manageAccess")))) throw new Error("Finance staff and managers may write invoices through commands");
    if (!((checkRole(user, "financeAccess") || checkRole(user, "manageAccess")))) throw new Error("Finance staff and managers may execute invoice commands");
    if (!((__draft.issuedAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.status === "draft"))) throw new Error("Guard 1 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((__rel_client != null))) throw new Error("Guard 3 failed");
    if (!((clientId === __draft.clientId))) throw new Error("Issue clientId must match the seeded client reference");
    if (!((((eventId == null) || (__draft.eventId == null)) || (eventId === __draft.eventId)))) throw new Error("Issue eventId must match the seeded event reference when both are set");
    if (!((((invoiceNumber).trim()).length > 0))) throw new Error("Invoice number is required");
    if (!(((((subtotal >= 0) && (taxAmount >= 0)) && (discountAmount >= 0)) && (total >= 0)))) throw new Error("Invoice money amounts cannot be negative");
    if (!((total === ((subtotal + taxAmount) - discountAmount)))) throw new Error("Invoice total must equal subtotal plus tax minus discount");
    if (!(((currencyCode == null) || (((currencyCode).trim()).length === 3)))) throw new Error("Currency code must be a three-letter ISO 4217 code");
    if (!(((exchangeRate == null) || (exchangeRate > 0)))) throw new Error("Exchange rate must be greater than zero");
    const doc: Record<string, any> = {
      ...__draft,
      clientId: clientId,
      eventId: ((eventId != null) ? eventId : __draft.eventId),
      invoiceNumber: invoiceNumber,
      subtotal: subtotal,
      taxAmount: taxAmount,
      discountAmount: discountAmount,
      total: total,
      amountPaid: 0,
      amountDue: total,
      amountCredited: 0,
      creditMemoAmount: 0,
      lineItems: ((lineItems != null) ? lineItems : []),
      taxBreakdown: ((taxBreakdown != null) ? taxBreakdown : []),
      paymentTermsDays: ((paymentTermsDays != null) ? paymentTermsDays : 30),
      dueDate: dueDate,
      notes: notes,
      currencyCode: __draft.normalizedCurrencyCode,
      exchangeRate: __draft.resolvedExchangeRate,
      issuedAt: Date.now(),
      version: 1,
    };
    const docId = await ctx.db.insert("invoices", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, invoiceId: docId, tenantId: doc.tenantId, clientId: doc.clientId, eventId: ((doc.eventId != null) ? doc.eventId : doc.eventId), invoiceNumber: doc.invoiceNumber, total: doc.total, amountDue: doc.total, currencyCode: doc.normalizedCurrencyCode, exchangeRate: doc.resolvedExchangeRate, _subject: { entity: "Invoice", command: "issue", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "InvoiceIssued", entity: "Invoice", entityId: docId, payload: { invoiceId: docId, tenantId: doc.tenantId, clientId: doc.clientId, eventId: ((doc.eventId != null) ? doc.eventId : doc.eventId), invoiceNumber: doc.invoiceNumber, total: doc.total, amountDue: doc.total, currencyCode: doc.normalizedCurrencyCode, exchangeRate: doc.resolvedExchangeRate }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Invoice_createViaIssue", __result);
    }
    return __result;
  },
});

async function __runInvoiceMarkDepositPaid(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Invoice not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Invoice not found");
    if (!((checkRole(user, "financeAccess") || checkRole(user, "manageAccess")))) throw new Error("Finance staff and managers may read invoices");
    if (!((checkRole(user, "financeAccess") || checkRole(user, "manageAccess")))) throw new Error("Finance staff and managers may write invoices through commands");
    if (!((checkRole(user, "financeAccess") || checkRole(user, "manageAccess")))) throw new Error("Finance staff and managers may execute invoice commands");
    if (!(((((doc.status === "sent") || (doc.status === "viewed")) || (doc.status === "overdue")) || (doc.status === "partial")))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((doc.depositPaidAt == null))) throw new Error("Guard 2 failed");
    if (!(((doc.depositAmount != null) && (doc.depositAmount > 0)))) throw new Error("No deposit is due on this invoice");
    if (!(((doc.depositAmount != null) && (doc.depositAmount <= doc.amountDue)))) throw new Error("Deposit cannot exceed amount due");
    const nextPaid = (doc.amountPaid + doc.depositAmount);
    const nextDue = (doc.amountDue - doc.depositAmount);
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = String(((nextDue === 0) ? "paid" : "partial"));
        const __allowed: Record<string, string[]> = { "draft": ["sent", "voided"], "sent": ["viewed", "overdue", "partial", "paid", "voided"], "viewed": ["overdue", "partial", "paid", "voided"], "overdue": ["partial", "paid", "written_off", "voided"], "partial": ["partial", "paid", "overdue", "written_off"], "paid": ["partial"], "voided": [], "written_off": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      amountPaid: nextPaid,
      amountDue: nextDue,
      depositPaidAt: Date.now(),
      status: ((nextDue === 0) ? "paid" : "partial"),
      paidAt: ((nextDue === 0) ? Date.now() : doc.paidAt),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, invoiceId: docId, tenantId: __after.tenantId, clientId: __after.clientId, eventId: __after.eventId, depositAmount: __after.depositAmount, amountPaid: nextPaid, amountDue: nextDue, _subject: { entity: "Invoice", command: "markDepositPaid", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "InvoiceDepositPaid", entity: "Invoice", entityId: docId, payload: { invoiceId: docId, tenantId: __after.tenantId, clientId: __after.clientId, eventId: __after.eventId, depositAmount: __after.depositAmount, amountPaid: nextPaid, amountDue: nextDue }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Invoice_markDepositPaid = mutation({
  args: {
    docId: v.id("invoices"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runInvoiceMarkDepositPaid(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Invoice_markDepositPaid", __result);
    }
    return __result;
  },
});

async function __runInvoiceMarkOverdue(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Invoice not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Invoice not found");
    if (!((checkRole(user, "financeAccess") || checkRole(user, "manageAccess")))) throw new Error("Finance staff and managers may read invoices");
    if (!((checkRole(user, "financeAccess") || checkRole(user, "manageAccess")))) throw new Error("Finance staff and managers may write invoices through commands");
    if (!((checkRole(user, "financeAccess") || checkRole(user, "manageAccess")))) throw new Error("Finance staff and managers may execute invoice commands");
    if (!((((doc.status === "sent") || (doc.status === "viewed")) || (doc.status === "partial")))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(((doc.dueDate != null) && (doc.dueDate < Date.now())))) throw new Error("Guard 2 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "overdue";
        const __allowed: Record<string, string[]> = { "draft": ["sent", "voided"], "sent": ["viewed", "overdue", "partial", "paid", "voided"], "viewed": ["overdue", "partial", "paid", "voided"], "overdue": ["partial", "paid", "written_off", "voided"], "partial": ["partial", "paid", "overdue", "written_off"], "paid": ["partial"], "voided": [], "written_off": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "overdue",
      overdueSince: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, invoiceId: docId, tenantId: __after.tenantId, clientId: __after.clientId, eventId: __after.eventId, amountDue: __after.amountDue, _subject: { entity: "Invoice", command: "markOverdue", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "InvoiceMarkedOverdue", entity: "Invoice", entityId: docId, payload: { invoiceId: docId, tenantId: __after.tenantId, clientId: __after.clientId, eventId: __after.eventId, amountDue: __after.amountDue }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Invoice_markOverdue = mutation({
  args: {
    docId: v.id("invoices"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runInvoiceMarkOverdue(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Invoice_markOverdue", __result);
    }
    return __result;
  },
});

async function __runInvoiceMarkViewed(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Invoice not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Invoice not found");
    if (!((checkRole(user, "financeAccess") || checkRole(user, "manageAccess")))) throw new Error("Finance staff and managers may read invoices");
    if (!((checkRole(user, "financeAccess") || checkRole(user, "manageAccess")))) throw new Error("Finance staff and managers may write invoices through commands");
    if (!((checkRole(user, "financeAccess") || checkRole(user, "manageAccess")))) throw new Error("Finance staff and managers may execute invoice commands");
    if (!((doc.status === "sent"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "viewed";
        const __allowed: Record<string, string[]> = { "draft": ["sent", "voided"], "sent": ["viewed", "overdue", "partial", "paid", "voided"], "viewed": ["overdue", "partial", "paid", "voided"], "overdue": ["partial", "paid", "written_off", "voided"], "partial": ["partial", "paid", "overdue", "written_off"], "paid": ["partial"], "voided": [], "written_off": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "viewed",
      viewedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, invoiceId: docId, tenantId: __after.tenantId, clientId: __after.clientId, eventId: __after.eventId, _subject: { entity: "Invoice", command: "markViewed", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "InvoiceViewed", entity: "Invoice", entityId: docId, payload: { invoiceId: docId, tenantId: __after.tenantId, clientId: __after.clientId, eventId: __after.eventId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Invoice_markViewed = mutation({
  args: {
    docId: v.id("invoices"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runInvoiceMarkViewed(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Invoice_markViewed", __result);
    }
    return __result;
  },
});

async function __runInvoiceMarkVoided(ctx: MutationCtx, { docId, reason, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Invoice not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Invoice not found");
    if (!((checkRole(user, "financeAccess") || checkRole(user, "manageAccess")))) throw new Error("Finance staff and managers may read invoices");
    if (!((checkRole(user, "financeAccess") || checkRole(user, "manageAccess")))) throw new Error("Finance staff and managers may write invoices through commands");
    if (!((checkRole(user, "financeAccess") || checkRole(user, "manageAccess")))) throw new Error("Finance staff and managers may execute invoice commands");
    if (!(((((doc.status === "draft") || (doc.status === "sent")) || (doc.status === "viewed")) || (doc.status === "overdue")))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((doc.amountPaid === 0))) throw new Error("Guard 2 failed");
    if (!((((reason).trim()).length > 0))) throw new Error("Void reason is required");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "voided";
        const __allowed: Record<string, string[]> = { "draft": ["sent", "voided"], "sent": ["viewed", "overdue", "partial", "paid", "voided"], "viewed": ["overdue", "partial", "paid", "voided"], "overdue": ["partial", "paid", "written_off", "voided"], "partial": ["partial", "paid", "overdue", "written_off"], "paid": ["partial"], "voided": [], "written_off": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "voided",
      voidReason: reason,
      voidedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, invoiceId: docId, tenantId: __after.tenantId, clientId: __after.clientId, eventId: __after.eventId, reason: reason, _subject: { entity: "Invoice", command: "markVoided", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "InvoiceVoided", entity: "Invoice", entityId: docId, payload: { invoiceId: docId, tenantId: __after.tenantId, clientId: __after.clientId, eventId: __after.eventId, reason: reason }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Invoice_markVoided = mutation({
  args: {
    docId: v.id("invoices"),
    reason: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runInvoiceMarkVoided(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Invoice_markVoided", __result);
    }
    return __result;
  },
});

async function __runInvoiceReassignClient(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Invoice not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Invoice not found");
    const __rel_clientMergeAuthorization = await __resolveRelation(ctx, "clientMerges", [__auth.tenantId, doc.clientMergeAuthorizationId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_mergeTargetClient = await __resolveRelation(ctx, "clients", [__auth.tenantId, doc.mergeTargetClientId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).clientMergeAuthorizationId;
      ((doc as any) as any).clientMergeAuthorization = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    {
      const __fk = ((doc as any) as any).mergeTargetClientId;
      ((doc as any) as any).mergeTargetClient = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!((checkRole(user, "financeAccess") || checkRole(user, "manageAccess")))) throw new Error("Finance staff and managers may read invoices");
    if (!((checkRole(user, "financeAccess") || checkRole(user, "manageAccess")))) throw new Error("Finance staff and managers may write invoices through commands");
    if (!((checkRole(user, "financeAccess") || checkRole(user, "manageAccess")))) throw new Error("Finance staff and managers may execute invoice commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!(checkRole(user, "adminAccess"))) throw new Error("Guard 1 failed");
    if (!((__rel_clientMergeAuthorization != null))) throw new Error("Guard 2 failed");
    if (!((__rel_clientMergeAuthorization.deletedAt == null))) throw new Error("Guard 3 failed");
    if (!((__rel_clientMergeAuthorization.mergedAt != null))) throw new Error("Guard 4 failed");
    if (!((__rel_mergeTargetClient != null))) throw new Error("Guard 5 failed");
    if (!((__rel_mergeTargetClient.status === "active"))) throw new Error("Guard 6 failed");
    if (!((__rel_mergeTargetClient.deletedAt == null))) throw new Error("Guard 7 failed");
    if (!((__rel_clientMergeAuthorization.duplicateClientId === doc.clientId))) throw new Error("Merge authorization does not match the invoice client");
    if (!((__rel_clientMergeAuthorization.primaryClientId === doc.mergeTargetClientId))) throw new Error("Merge authorization does not match the primary client");
    const previousClientId = doc.clientId;
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      clientId: doc.mergeTargetClientId,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, invoiceId: docId, tenantId: __after.tenantId, previousClientId: previousClientId, clientId: __after.mergeTargetClientId, _subject: { entity: "Invoice", command: "reassignClient", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "InvoiceClientReassigned", entity: "Invoice", entityId: docId, payload: { invoiceId: docId, tenantId: __after.tenantId, previousClientId: previousClientId, clientId: __after.mergeTargetClientId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Invoice_reassignClient = mutation({
  args: {
    docId: v.id("invoices"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runInvoiceReassignClient(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Invoice_reassignClient", __result);
    }
    return __result;
  },
});

async function __runInvoiceRecordCreditMemo(ctx: MutationCtx, { docId, creditAmount, creditMemoId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Invoice not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Invoice not found");
    if (!((checkRole(user, "financeAccess") || checkRole(user, "manageAccess")))) throw new Error("Finance staff and managers may read invoices");
    if (!((checkRole(user, "financeAccess") || checkRole(user, "manageAccess")))) throw new Error("Finance staff and managers may write invoices through commands");
    if (!((checkRole(user, "financeAccess") || checkRole(user, "manageAccess")))) throw new Error("Finance staff and managers may execute invoice commands");
    if (!((doc.status === "paid"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((creditAmount > 0))) throw new Error("Credit memo amount must be positive");
    if (!((((creditMemoId).trim()).length > 0))) throw new Error("Credit memo id is required");
    if (!((creditAmount <= (doc.amountPaid - ((doc.creditMemoAmount != null) ? doc.creditMemoAmount : 0))))) throw new Error("Credit memos cannot exceed the amount paid on the source invoice");
    const priorCreditMemoAmount = ((doc.creditMemoAmount != null) ? doc.creditMemoAmount : 0);
    const nextCreditMemoAmount = (priorCreditMemoAmount + creditAmount);
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      creditMemoAmount: nextCreditMemoAmount,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, invoiceId: docId, tenantId: __after.tenantId, clientId: __after.clientId, eventId: __after.eventId, creditMemoId: creditMemoId, creditAmount: creditAmount, creditMemoAmount: nextCreditMemoAmount, _subject: { entity: "Invoice", command: "recordCreditMemo", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "InvoiceCreditMemoRecorded", entity: "Invoice", entityId: docId, payload: { invoiceId: docId, tenantId: __after.tenantId, clientId: __after.clientId, eventId: __after.eventId, creditMemoId: creditMemoId, creditAmount: creditAmount, creditMemoAmount: nextCreditMemoAmount }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Invoice_recordCreditMemo = mutation({
  args: {
    docId: v.id("invoices"),
    creditAmount: v.number(),
    creditMemoId: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runInvoiceRecordCreditMemo(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Invoice_recordCreditMemo", __result);
    }
    return __result;
  },
});

async function __runInvoiceRecordRefund(ctx: MutationCtx, { docId, refundAmount, paymentId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Invoice not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Invoice not found");
    if (!((checkRole(user, "financeAccess") || checkRole(user, "manageAccess")))) throw new Error("Finance staff and managers may read invoices");
    if (!((checkRole(user, "financeAccess") || checkRole(user, "manageAccess")))) throw new Error("Finance staff and managers may write invoices through commands");
    if (!((checkRole(user, "financeAccess") || checkRole(user, "manageAccess")))) throw new Error("Finance staff and managers may execute invoice commands");
    if (!(((doc.status === "paid") || (doc.status === "partial")))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((refundAmount > 0))) throw new Error("Refund amount must be positive");
    if (!((refundAmount <= doc.amountPaid))) throw new Error("Refund amount cannot exceed amount paid");
    if (!((((paymentId).trim()).length > 0))) throw new Error("Payment id is required for refund");
    const nextPaid = (doc.amountPaid - refundAmount);
    const nextDue = (doc.amountDue + refundAmount);
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "partial";
        const __allowed: Record<string, string[]> = { "draft": ["sent", "voided"], "sent": ["viewed", "overdue", "partial", "paid", "voided"], "viewed": ["overdue", "partial", "paid", "voided"], "overdue": ["partial", "paid", "written_off", "voided"], "partial": ["partial", "paid", "overdue", "written_off"], "paid": ["partial"], "voided": [], "written_off": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      amountPaid: nextPaid,
      amountDue: nextDue,
      status: "partial",
      paidAt: null,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, invoiceId: docId, tenantId: __after.tenantId, clientId: __after.clientId, eventId: __after.eventId, paymentId: paymentId, refundAmount: refundAmount, amountPaid: nextPaid, amountDue: nextDue, _subject: { entity: "Invoice", command: "recordRefund", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "InvoiceRefundRecorded", entity: "Invoice", entityId: docId, payload: { invoiceId: docId, tenantId: __after.tenantId, clientId: __after.clientId, eventId: __after.eventId, paymentId: paymentId, refundAmount: refundAmount, amountPaid: nextPaid, amountDue: nextDue }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Invoice_recordRefund = mutation({
  args: {
    docId: v.id("invoices"),
    refundAmount: v.number(),
    paymentId: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runInvoiceRecordRefund(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Invoice_recordRefund", __result);
    }
    return __result;
  },
});

async function __runInvoiceSend(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Invoice not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Invoice not found");
    if (!((checkRole(user, "financeAccess") || checkRole(user, "manageAccess")))) throw new Error("Finance staff and managers may read invoices");
    if (!((checkRole(user, "financeAccess") || checkRole(user, "manageAccess")))) throw new Error("Finance staff and managers may write invoices through commands");
    if (!((checkRole(user, "financeAccess") || checkRole(user, "manageAccess")))) throw new Error("Finance staff and managers may execute invoice commands");
    if (!((doc.status === "draft"))) throw new Error("Guard 0 failed");
    if (!((doc.issuedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((doc.amountDue > 0))) throw new Error("Cannot send an invoice with zero balance");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "sent";
        const __allowed: Record<string, string[]> = { "draft": ["sent", "voided"], "sent": ["viewed", "overdue", "partial", "paid", "voided"], "viewed": ["overdue", "partial", "paid", "voided"], "overdue": ["partial", "paid", "written_off", "voided"], "partial": ["partial", "paid", "overdue", "written_off"], "paid": ["partial"], "voided": [], "written_off": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "sent",
      sentAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, invoiceId: docId, tenantId: __after.tenantId, clientId: __after.clientId, eventId: __after.eventId, invoiceNumber: __after.invoiceNumber, total: __after.total, amountDue: __after.amountDue, _subject: { entity: "Invoice", command: "send", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "InvoiceSent", entity: "Invoice", entityId: docId, payload: { invoiceId: docId, tenantId: __after.tenantId, clientId: __after.clientId, eventId: __after.eventId, invoiceNumber: __after.invoiceNumber, total: __after.total, amountDue: __after.amountDue }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Invoice_send = mutation({
  args: {
    docId: v.id("invoices"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runInvoiceSend(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Invoice_send", __result);
    }
    return __result;
  },
});

async function __runInvoiceSendBalanceReminder(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Invoice not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Invoice not found");
    if (!((checkRole(user, "financeAccess") || checkRole(user, "manageAccess")))) throw new Error("Finance staff and managers may read invoices");
    if (!((checkRole(user, "financeAccess") || checkRole(user, "manageAccess")))) throw new Error("Finance staff and managers may write invoices through commands");
    if (!((checkRole(user, "financeAccess") || checkRole(user, "manageAccess")))) throw new Error("Finance staff and managers may execute invoice commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.balanceReminderSentAt == null))) throw new Error("Guard 1 failed");
    if (!((doc.amountDue > 0))) throw new Error("No outstanding balance to remind about");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      balanceReminderSentAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, invoiceId: docId, tenantId: __after.tenantId, clientId: __after.clientId, eventId: __after.eventId, amountDue: __after.amountDue, _subject: { entity: "Invoice", command: "sendBalanceReminder", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "InvoiceBalanceReminderSent", entity: "Invoice", entityId: docId, payload: { invoiceId: docId, tenantId: __after.tenantId, clientId: __after.clientId, eventId: __after.eventId, amountDue: __after.amountDue }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Invoice_sendBalanceReminder = mutation({
  args: {
    docId: v.id("invoices"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runInvoiceSendBalanceReminder(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Invoice_sendBalanceReminder", __result);
    }
    return __result;
  },
});

async function __runInvoiceSetDeposit(ctx: MutationCtx, { docId, depositAmount, balanceReminderLeadDays, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Invoice not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Invoice not found");
    if (!((checkRole(user, "financeAccess") || checkRole(user, "manageAccess")))) throw new Error("Finance staff and managers may read invoices");
    if (!((checkRole(user, "financeAccess") || checkRole(user, "manageAccess")))) throw new Error("Finance staff and managers may write invoices through commands");
    if (!((checkRole(user, "financeAccess") || checkRole(user, "manageAccess")))) throw new Error("Finance staff and managers may execute invoice commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.depositPaidAt == null))) throw new Error("Guard 1 failed");
    if (!((depositAmount >= 0))) throw new Error("Deposit amount cannot be negative");
    if (!(((doc.total === 0) || (depositAmount <= doc.total)))) throw new Error("Deposit amount cannot exceed the invoice total");
    if (!(((balanceReminderLeadDays == null) || (balanceReminderLeadDays >= 0)))) throw new Error("Reminder lead days cannot be negative");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      depositAmount: depositAmount,
      balanceReminderLeadDays: ((balanceReminderLeadDays != null) ? balanceReminderLeadDays : doc.balanceReminderLeadDays),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, invoiceId: docId, tenantId: __after.tenantId, clientId: __after.clientId, eventId: __after.eventId, depositAmount: depositAmount, balanceReminderLeadDays: ((balanceReminderLeadDays != null) ? balanceReminderLeadDays : __after.balanceReminderLeadDays), _subject: { entity: "Invoice", command: "setDeposit", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "InvoiceDepositSet", entity: "Invoice", entityId: docId, payload: { invoiceId: docId, tenantId: __after.tenantId, clientId: __after.clientId, eventId: __after.eventId, depositAmount: depositAmount, balanceReminderLeadDays: ((balanceReminderLeadDays != null) ? balanceReminderLeadDays : __after.balanceReminderLeadDays) }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Invoice_setDeposit = mutation({
  args: {
    docId: v.id("invoices"),
    depositAmount: v.number(),
    balanceReminderLeadDays: v.optional(v.any()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runInvoiceSetDeposit(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Invoice_setDeposit", __result);
    }
    return __result;
  },
});

async function __runInvoiceStageClientMerge(ctx: MutationCtx, { docId, clientMergeId, clientId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Invoice not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Invoice not found");
    if (!((checkRole(user, "financeAccess") || checkRole(user, "manageAccess")))) throw new Error("Finance staff and managers may read invoices");
    if (!((checkRole(user, "financeAccess") || checkRole(user, "manageAccess")))) throw new Error("Finance staff and managers may write invoices through commands");
    if (!((checkRole(user, "financeAccess") || checkRole(user, "manageAccess")))) throw new Error("Finance staff and managers may execute invoice commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!(checkRole(user, "adminAccess"))) throw new Error("Guard 1 failed");
    if (!((clientId !== doc.clientId))) throw new Error("Invoice already belongs to this client");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      clientMergeAuthorizationId: clientMergeId,
      mergeTargetClientId: clientId,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, invoiceId: docId, tenantId: __after.tenantId, _subject: { entity: "Invoice", command: "stageClientMerge", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "InvoiceClientMergeStaged", entity: "Invoice", entityId: docId, payload: { invoiceId: docId, tenantId: __after.tenantId }, createdAt: Date.now() });
    // Reactions
    const reactionTarget0 = payload.invoiceId;
    if (reactionTarget0) await __runInvoiceReassignClient(ctx, { docId: reactionTarget0 } as any);
    return { ...doc, ...updates };
}

export const Invoice_stageClientMerge = mutation({
  args: {
    docId: v.id("invoices"),
    clientMergeId: v.string(),
    clientId: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runInvoiceStageClientMerge(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Invoice_stageClientMerge", __result);
    }
    return __result;
  },
});

async function __runInvoiceWriteOff(ctx: MutationCtx, { docId, reason, writeOffAmount, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Invoice not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Invoice not found");
    if (!((checkRole(user, "financeAccess") || checkRole(user, "manageAccess")))) throw new Error("Finance staff and managers may read invoices");
    if (!((checkRole(user, "financeAccess") || checkRole(user, "manageAccess")))) throw new Error("Finance staff and managers may write invoices through commands");
    if (!((checkRole(user, "financeAccess") || checkRole(user, "manageAccess")))) throw new Error("Finance staff and managers may execute invoice commands");
    if (!(((doc.status === "overdue") || (doc.status === "partial")))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((((reason).trim()).length > 0))) throw new Error("Write-off reason is required");
    if (!((writeOffAmount > 0))) throw new Error("Write-off amount must be positive");
    if (!((writeOffAmount <= doc.amountDue))) throw new Error("Write-off amount cannot exceed amount due");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "written_off";
        const __allowed: Record<string, string[]> = { "draft": ["sent", "voided"], "sent": ["viewed", "overdue", "partial", "paid", "voided"], "viewed": ["overdue", "partial", "paid", "voided"], "overdue": ["partial", "paid", "written_off", "voided"], "partial": ["partial", "paid", "overdue", "written_off"], "paid": ["partial"], "voided": [], "written_off": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "written_off",
      amountDue: 0,
      writeOffReason: reason,
      writtenOffAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, invoiceId: docId, tenantId: __after.tenantId, clientId: __after.clientId, eventId: __after.eventId, reason: reason, writeOffAmount: writeOffAmount, _subject: { entity: "Invoice", command: "writeOff", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "InvoiceWrittenOff", entity: "Invoice", entityId: docId, payload: { invoiceId: docId, tenantId: __after.tenantId, clientId: __after.clientId, eventId: __after.eventId, reason: reason, writeOffAmount: writeOffAmount }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Invoice_writeOff = mutation({
  args: {
    docId: v.id("invoices"),
    reason: v.string(),
    writeOffAmount: v.number(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runInvoiceWriteOff(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Invoice_writeOff", __result);
    }
    return __result;
  },
});

async function __runLeadCapture(ctx: MutationCtx, { docId, leadType, source, referralSourceId, estimatedValue, companyName, givenName, familyName, email, phone, probability, notes, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("Lead not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("Lead not found");
    const doc = await __decryptDoc(ctx, "Lead", ["email","phone"], __storedDoc) as Record<string, any>;
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may read leads");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may update leads through commands");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may execute lead commands");
    if (!((doc.capturedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(((((leadType === "company") && (companyName != null)) && (((companyName).trim()).length > 0)) || (((leadType === "person") && (givenName != null)) && (((givenName).trim()).length > 0))))) throw new Error("Company leads require a company name; person leads require a given name");
    if (!((((source).trim()).length > 0))) throw new Error("Lead source is required");
    if (!((estimatedValue >= 0))) throw new Error("Lead estimated value cannot be negative");
    if (!(((probability == null) || ((probability >= 0) && (probability <= 100))))) throw new Error("Lead probability must be between 0 and 100");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      leadType: leadType,
      companyName: companyName,
      givenName: givenName,
      familyName: familyName,
      email: email,
      phone: phone,
      source: source,
      referralSourceId: referralSourceId,
      estimatedValue: estimatedValue,
      stage: "new",
      probability: ((probability != null) ? probability : 10),
      notes: notes,
      capturedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "Lead", ["email","phone"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, leadId: docId, tenantId: __after.tenantId, source: source, estimatedValue: estimatedValue, probability: ((probability != null) ? probability : 10), _subject: { entity: "Lead", command: "capture", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "LeadCaptured", entity: "Lead", entityId: docId, payload: { leadId: docId, tenantId: __after.tenantId, source: source, estimatedValue: estimatedValue, probability: ((probability != null) ? probability : 10) }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Lead_capture = mutation({
  args: {
    docId: v.id("leads"),
    leadType: v.any(),
    source: v.string(),
    referralSourceId: v.optional(v.string()),
    estimatedValue: v.number(),
    companyName: v.optional(v.string()),
    givenName: v.optional(v.string()),
    familyName: v.optional(v.string()),
    email: v.optional(v.string()),
    phone: v.optional(v.string()),
    probability: v.optional(v.any()),
    notes: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runLeadCapture(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Lead_capture", __result);
    }
    return __result;
  },
});

export const Lead_createViaCapture = mutation({
  args: {
    leadType: v.any(),
    source: v.string(),
    referralSourceId: v.optional(v.string()),
    estimatedValue: v.number(),
    companyName: v.optional(v.string()),
    givenName: v.optional(v.string()),
    familyName: v.optional(v.string()),
    email: v.optional(v.string()),
    phone: v.optional(v.string()),
    probability: v.optional(v.any()),
    notes: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { leadType, source, referralSourceId, estimatedValue, companyName, givenName, familyName, email, phone, probability, notes } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      probability: args.probability !== undefined ? args.probability : 10,
      createdAt: Date.now(),
      updatedAt: Date.now(),
      companyName: args.companyName,
      email: args.email,
      estimatedValue: args.estimatedValue,
      familyName: args.familyName,
      givenName: args.givenName,
      leadType: args.leadType,
      notes: args.notes,
      phone: args.phone,
      referralSourceId: args.referralSourceId,
      source: args.source
    };
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may read leads");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may update leads through commands");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may execute lead commands");
    if (!((__draft.capturedAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(((((leadType === "company") && (companyName != null)) && (((companyName).trim()).length > 0)) || (((leadType === "person") && (givenName != null)) && (((givenName).trim()).length > 0))))) throw new Error("Company leads require a company name; person leads require a given name");
    if (!((((source).trim()).length > 0))) throw new Error("Lead source is required");
    if (!((estimatedValue >= 0))) throw new Error("Lead estimated value cannot be negative");
    if (!(((probability == null) || ((probability >= 0) && (probability <= 100))))) throw new Error("Lead probability must be between 0 and 100");
    const doc: Record<string, any> = {
      ...__draft,
      leadType: leadType,
      companyName: companyName,
      givenName: givenName,
      familyName: familyName,
      email: email,
      phone: phone,
      source: source,
      referralSourceId: referralSourceId,
      estimatedValue: estimatedValue,
      stage: "new",
      probability: ((probability != null) ? probability : 10),
      notes: notes,
      capturedAt: Date.now(),
      version: 1,
    };
    const __storedDoc = await __encryptDoc(ctx, "Lead", ["email","phone"], doc);
    const docId = await ctx.db.insert("leads", __storedDoc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, leadId: docId, tenantId: doc.tenantId, source: doc.source, estimatedValue: doc.estimatedValue, probability: ((doc.probability != null) ? doc.probability : 10), _subject: { entity: "Lead", command: "capture", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "LeadCaptured", entity: "Lead", entityId: docId, payload: { leadId: docId, tenantId: doc.tenantId, source: doc.source, estimatedValue: doc.estimatedValue, probability: ((doc.probability != null) ? doc.probability : 10) }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Lead_createViaCapture", __result);
    }
    return __result;
  },
});

async function __runLeadConfirmConversion(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("Lead not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("Lead not found");
    const doc = await __decryptDoc(ctx, "Lead", ["email","phone"], __storedDoc) as Record<string, any>;
    const __rel_client = await __resolveRelation(ctx, "clients", [__auth.tenantId, doc.clientId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_clientContact = await __resolveRelation(ctx, "clientContacts", [__auth.tenantId, doc.clientContactId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).clientId;
      ((doc as any) as any).client = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    {
      const __fk = ((doc as any) as any).clientContactId;
      ((doc as any) as any).clientContact = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may read leads");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may update leads through commands");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may execute lead commands");
    if (!((doc.capturedAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((((__rel_client != null) && (__rel_client.status === "active")) && (__rel_client.deletedAt == null)))) throw new Error("Guard 2 failed");
    if (!(((__rel_clientContact == null) || ((__rel_clientContact.clientId === doc.clientId) && (__rel_clientContact.deletedAt == null))))) throw new Error("Lead contact must belong to the converted client");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      convertedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "Lead", ["email","phone"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, leadId: docId, tenantId: __after.tenantId, clientId: __after.clientId, clientContactId: __after.clientContactId, _subject: { entity: "Lead", command: "confirmConversion", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "LeadConverted", entity: "Lead", entityId: docId, payload: { leadId: docId, tenantId: __after.tenantId, clientId: __after.clientId, clientContactId: __after.clientContactId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Lead_confirmConversion = mutation({
  args: {
    docId: v.id("leads"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runLeadConfirmConversion(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Lead_confirmConversion", __result);
    }
    return __result;
  },
});

async function __runLeadConfirmProposalSent(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("Lead not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("Lead not found");
    const doc = await __decryptDoc(ctx, "Lead", ["email","phone"], __storedDoc) as Record<string, any>;
    const __rel_proposal = await __resolveRelation(ctx, "proposals", [__auth.tenantId, doc.proposalId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).proposalId;
      ((doc as any) as any).proposal = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may read leads");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may update leads through commands");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may execute lead commands");
    if (!((doc.capturedAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.convertedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((((__rel_proposal != null) && (__rel_proposal.status === "sent")) && (__rel_proposal.deletedAt == null)))) throw new Error("Guard 3 failed");
    if (!((__rel_proposal.clientId === doc.clientId))) throw new Error("Lead proposal must belong to the converted client");
    const previousStage = doc.stage;
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      stage: "proposalSent",
      proposalLinkedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "Lead", ["email","phone"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, leadId: docId, tenantId: __after.tenantId, clientId: __after.clientId, proposalId: __after.proposalId, previousStage: previousStage, _subject: { entity: "Lead", command: "confirmProposalSent", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "LeadProposalLinked", entity: "Lead", entityId: docId, payload: { leadId: docId, tenantId: __after.tenantId, clientId: __after.clientId, proposalId: __after.proposalId, previousStage: previousStage }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Lead_confirmProposalSent = mutation({
  args: {
    docId: v.id("leads"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runLeadConfirmProposalSent(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Lead_confirmProposalSent", __result);
    }
    return __result;
  },
});

async function __runLeadReviseDetails(ctx: MutationCtx, { docId, leadType, source, referralSourceId, companyName, givenName, familyName, email, phone, notes, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("Lead not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("Lead not found");
    const doc = await __decryptDoc(ctx, "Lead", ["email","phone"], __storedDoc) as Record<string, any>;
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may read leads");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may update leads through commands");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may execute lead commands");
    if (!((doc.capturedAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(((((leadType === "company") && (companyName != null)) && (((companyName).trim()).length > 0)) || (((leadType === "person") && (givenName != null)) && (((givenName).trim()).length > 0))))) throw new Error("Company leads require a company name; person leads require a given name");
    if (!((((source).trim()).length > 0))) throw new Error("Lead source is required");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      leadType: leadType,
      companyName: companyName,
      givenName: givenName,
      familyName: familyName,
      email: email,
      phone: phone,
      source: source,
      referralSourceId: referralSourceId,
      notes: notes,
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "Lead", ["email","phone"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, leadId: docId, tenantId: __after.tenantId, source: source, _subject: { entity: "Lead", command: "reviseDetails", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "LeadDetailsRevised", entity: "Lead", entityId: docId, payload: { leadId: docId, tenantId: __after.tenantId, source: source }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Lead_reviseDetails = mutation({
  args: {
    docId: v.id("leads"),
    leadType: v.any(),
    source: v.string(),
    referralSourceId: v.optional(v.string()),
    companyName: v.optional(v.string()),
    givenName: v.optional(v.string()),
    familyName: v.optional(v.string()),
    email: v.optional(v.string()),
    phone: v.optional(v.string()),
    notes: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runLeadReviseDetails(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Lead_reviseDetails", __result);
    }
    return __result;
  },
});

async function __runLeadStageConversion(ctx: MutationCtx, { docId, clientId, clientContactId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("Lead not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("Lead not found");
    const doc = await __decryptDoc(ctx, "Lead", ["email","phone"], __storedDoc) as Record<string, any>;
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may read leads");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may update leads through commands");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may execute lead commands");
    if (!((doc.capturedAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      clientId: clientId,
      clientContactId: clientContactId,
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "Lead", ["email","phone"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    return { ...doc, ...updates };
}

export const Lead_stageConversion = mutation({
  args: {
    docId: v.id("leads"),
    clientId: v.string(),
    clientContactId: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runLeadStageConversion(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Lead_stageConversion", __result);
    }
    return __result;
  },
});

async function __runLeadStageProposal(ctx: MutationCtx, { docId, proposalId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("Lead not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("Lead not found");
    const doc = await __decryptDoc(ctx, "Lead", ["email","phone"], __storedDoc) as Record<string, any>;
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may read leads");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may update leads through commands");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may execute lead commands");
    if (!((doc.capturedAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.convertedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      proposalId: proposalId,
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "Lead", ["email","phone"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    return { ...doc, ...updates };
}

export const Lead_stageProposal = mutation({
  args: {
    docId: v.id("leads"),
    proposalId: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runLeadStageProposal(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Lead_stageProposal", __result);
    }
    return __result;
  },
});

async function __runLeadUpdatePipeline(ctx: MutationCtx, { docId, stage, estimatedValue, probability, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("Lead not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("Lead not found");
    const doc = await __decryptDoc(ctx, "Lead", ["email","phone"], __storedDoc) as Record<string, any>;
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may read leads");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may update leads through commands");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may execute lead commands");
    if (!((doc.capturedAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((estimatedValue >= 0))) throw new Error("Lead estimated value cannot be negative");
    if (!(((probability >= 0) && (probability <= 100)))) throw new Error("Lead probability must be between 0 and 100");
    const previousStage = doc.stage;
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      stage: stage,
      estimatedValue: estimatedValue,
      probability: probability,
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "Lead", ["email","phone"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, leadId: docId, tenantId: __after.tenantId, previousStage: previousStage, stage: stage, estimatedValue: estimatedValue, probability: probability, _subject: { entity: "Lead", command: "updatePipeline", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "LeadPipelineUpdated", entity: "Lead", entityId: docId, payload: { leadId: docId, tenantId: __after.tenantId, previousStage: previousStage, stage: stage, estimatedValue: estimatedValue, probability: probability }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Lead_updatePipeline = mutation({
  args: {
    docId: v.id("leads"),
    stage: v.any(),
    estimatedValue: v.number(),
    probability: v.any(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runLeadUpdatePipeline(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Lead_updatePipeline", __result);
    }
    return __result;
  },
});

async function __runMenuArchive(ctx: MutationCtx, { docId, reason, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Menu not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Menu not found");
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "salesAccess")))) throw new Error("Kitchen and sales staff may read menus");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write menus through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute menu commands");
    if (!(((doc.status === "draft") || (doc.status === "published")))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(checkRole(user, "kitchenManageAccess"))) throw new Error("Guard 2 failed");
    if (!((((reason).trim()).length > 0))) throw new Error("Archive reason is required");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "archived";
        const __allowed: Record<string, string[]> = { "draft": ["published", "archived"], "published": ["draft", "archived"], "archived": ["draft"] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "archived",
      archivedAt: Date.now(),
      archiveReason: reason,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, menuId: docId, tenantId: __after.tenantId, reason: reason, _subject: { entity: "Menu", command: "archive", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "MenuArchived", entity: "Menu", entityId: docId, payload: { menuId: docId, tenantId: __after.tenantId, reason: reason }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Menu_archive = mutation({
  args: {
    docId: v.id("menus"),
    reason: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runMenuArchive(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Menu_archive", __result);
    }
    return __result;
  },
});

async function __runMenuDraft(ctx: MutationCtx, { docId, name, description, category, isTemplate, basePrice, pricePerPerson, minGuests, maxGuests, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Menu not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Menu not found");
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "salesAccess")))) throw new Error("Kitchen and sales staff may read menus");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write menus through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute menu commands");
    if (!((doc.draftedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((((name).trim()).length > 0))) throw new Error("Menu name is required");
    if (!((((basePrice == null) || (basePrice >= 0)) && ((pricePerPerson == null) || (pricePerPerson >= 0))))) throw new Error("Menu pricing cannot be negative");
    if (!(((((minGuests == null) || (minGuests >= 0)) && ((maxGuests == null) || (maxGuests >= 0))) && ((((minGuests == null) || (maxGuests == null)) || (maxGuests === 0)) || (maxGuests >= minGuests))))) throw new Error("Menu max guests must be zero (unlimited) or at least min guests");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      name: name,
      description: description,
      category: category,
      isTemplate: ((isTemplate != null) ? isTemplate : false),
      basePrice: ((basePrice != null) ? basePrice : 0),
      pricePerPerson: ((pricePerPerson != null) ? pricePerPerson : 0),
      minGuests: ((minGuests != null) ? minGuests : 0),
      maxGuests: ((maxGuests != null) ? maxGuests : 0),
      draftedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, menuId: docId, tenantId: __after.tenantId, name: name, isTemplate: ((isTemplate != null) ? isTemplate : false), _subject: { entity: "Menu", command: "draft", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "MenuDrafted", entity: "Menu", entityId: docId, payload: { menuId: docId, tenantId: __after.tenantId, name: name, isTemplate: ((isTemplate != null) ? isTemplate : false) }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Menu_draft = mutation({
  args: {
    docId: v.id("menus"),
    name: v.string(),
    description: v.optional(v.string()),
    category: v.optional(v.string()),
    isTemplate: v.optional(v.boolean()),
    basePrice: v.optional(v.number()),
    pricePerPerson: v.optional(v.number()),
    minGuests: v.optional(v.any()),
    maxGuests: v.optional(v.any()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runMenuDraft(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Menu_draft", __result);
    }
    return __result;
  },
});

export const Menu_createViaDraft = mutation({
  args: {
    name: v.string(),
    description: v.optional(v.string()),
    category: v.optional(v.string()),
    isTemplate: v.optional(v.boolean()),
    basePrice: v.optional(v.number()),
    pricePerPerson: v.optional(v.number()),
    minGuests: v.optional(v.any()),
    maxGuests: v.optional(v.any()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { name, description, category, isTemplate, basePrice, pricePerPerson, minGuests, maxGuests } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      isTemplate: args.isTemplate !== undefined ? args.isTemplate : false,
      basePrice: args.basePrice !== undefined ? args.basePrice : 0,
      pricePerPerson: args.pricePerPerson !== undefined ? args.pricePerPerson : 0,
      minGuests: args.minGuests !== undefined ? args.minGuests : 0,
      maxGuests: args.maxGuests !== undefined ? args.maxGuests : 0,
      status: "draft",
      createdAt: Date.now(),
      updatedAt: Date.now(),
      category: args.category,
      description: args.description,
      name: args.name
    };
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "salesAccess")))) throw new Error("Kitchen and sales staff may read menus");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write menus through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute menu commands");
    if (!((__draft.draftedAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((((name).trim()).length > 0))) throw new Error("Menu name is required");
    if (!((((basePrice == null) || (basePrice >= 0)) && ((pricePerPerson == null) || (pricePerPerson >= 0))))) throw new Error("Menu pricing cannot be negative");
    if (!(((((minGuests == null) || (minGuests >= 0)) && ((maxGuests == null) || (maxGuests >= 0))) && ((((minGuests == null) || (maxGuests == null)) || (maxGuests === 0)) || (maxGuests >= minGuests))))) throw new Error("Menu max guests must be zero (unlimited) or at least min guests");
    const doc: Record<string, any> = {
      ...__draft,
      name: name,
      description: description,
      category: category,
      isTemplate: ((isTemplate != null) ? isTemplate : false),
      basePrice: ((basePrice != null) ? basePrice : 0),
      pricePerPerson: ((pricePerPerson != null) ? pricePerPerson : 0),
      minGuests: ((minGuests != null) ? minGuests : 0),
      maxGuests: ((maxGuests != null) ? maxGuests : 0),
      draftedAt: Date.now(),
      version: 1,
    };
    const docId = await ctx.db.insert("menus", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, menuId: docId, tenantId: doc.tenantId, name: doc.name, isTemplate: ((doc.isTemplate != null) ? doc.isTemplate : false), _subject: { entity: "Menu", command: "draft", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "MenuDrafted", entity: "Menu", entityId: docId, payload: { menuId: docId, tenantId: doc.tenantId, name: doc.name, isTemplate: ((doc.isTemplate != null) ? doc.isTemplate : false) }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Menu_createViaDraft", __result);
    }
    return __result;
  },
});

async function __runMenuMarkPublished(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Menu not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Menu not found");
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "salesAccess")))) throw new Error("Kitchen and sales staff may read menus");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write menus through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute menu commands");
    if (!((doc.status === "draft"))) throw new Error("Guard 0 failed");
    if (!((doc.draftedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!(checkRole(user, "kitchenManageAccess"))) throw new Error("Guard 3 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "published";
        const __allowed: Record<string, string[]> = { "draft": ["published", "archived"], "published": ["draft", "archived"], "archived": ["draft"] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "published",
      publishedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, menuId: docId, tenantId: __after.tenantId, name: __after.name, _subject: { entity: "Menu", command: "markPublished", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "MenuPublished", entity: "Menu", entityId: docId, payload: { menuId: docId, tenantId: __after.tenantId, name: __after.name }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Menu_markPublished = mutation({
  args: {
    docId: v.id("menus"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runMenuMarkPublished(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Menu_markPublished", __result);
    }
    return __result;
  },
});

async function __runMenuRestore(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Menu not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Menu not found");
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "salesAccess")))) throw new Error("Kitchen and sales staff may read menus");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write menus through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute menu commands");
    if (!((doc.status === "archived"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(checkRole(user, "kitchenManageAccess"))) throw new Error("Guard 2 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "draft";
        const __allowed: Record<string, string[]> = { "draft": ["published", "archived"], "published": ["draft", "archived"], "archived": ["draft"] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "draft",
      archivedAt: null,
      archiveReason: null,
      publishedAt: null,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, menuId: docId, tenantId: __after.tenantId, _subject: { entity: "Menu", command: "restore", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "MenuRestored", entity: "Menu", entityId: docId, payload: { menuId: docId, tenantId: __after.tenantId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Menu_restore = mutation({
  args: {
    docId: v.id("menus"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runMenuRestore(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Menu_restore", __result);
    }
    return __result;
  },
});

async function __runMenuReviseDetails(ctx: MutationCtx, { docId, name, description, category, isTemplate, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Menu not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Menu not found");
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "salesAccess")))) throw new Error("Kitchen and sales staff may read menus");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write menus through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute menu commands");
    if (!((doc.status === "draft"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((((name).trim()).length > 0))) throw new Error("Menu name is required");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      name: name,
      description: description,
      category: category,
      isTemplate: ((isTemplate != null) ? isTemplate : doc.isTemplate),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, menuId: docId, tenantId: __after.tenantId, name: name, _subject: { entity: "Menu", command: "reviseDetails", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "MenuDetailsRevised", entity: "Menu", entityId: docId, payload: { menuId: docId, tenantId: __after.tenantId, name: name }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Menu_reviseDetails = mutation({
  args: {
    docId: v.id("menus"),
    name: v.string(),
    description: v.optional(v.string()),
    category: v.optional(v.string()),
    isTemplate: v.optional(v.boolean()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runMenuReviseDetails(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Menu_reviseDetails", __result);
    }
    return __result;
  },
});

async function __runMenuUnpublish(ctx: MutationCtx, { docId, reason, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Menu not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Menu not found");
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "salesAccess")))) throw new Error("Kitchen and sales staff may read menus");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write menus through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute menu commands");
    if (!((doc.status === "published"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(checkRole(user, "kitchenManageAccess"))) throw new Error("Guard 2 failed");
    if (!((((reason).trim()).length > 0))) throw new Error("Unpublish reason is required");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "draft";
        const __allowed: Record<string, string[]> = { "draft": ["published", "archived"], "published": ["draft", "archived"], "archived": ["draft"] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "draft",
      publishedAt: null,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, menuId: docId, tenantId: __after.tenantId, reason: reason, _subject: { entity: "Menu", command: "unpublish", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "MenuUnpublished", entity: "Menu", entityId: docId, payload: { menuId: docId, tenantId: __after.tenantId, reason: reason }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Menu_unpublish = mutation({
  args: {
    docId: v.id("menus"),
    reason: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runMenuUnpublish(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Menu_unpublish", __result);
    }
    return __result;
  },
});

async function __runMenuUpdatePricing(ctx: MutationCtx, { docId, basePrice, pricePerPerson, minGuests, maxGuests, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Menu not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Menu not found");
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "salesAccess")))) throw new Error("Kitchen and sales staff may read menus");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write menus through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute menu commands");
    if (!(((doc.status === "draft") || (doc.status === "published")))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(checkRole(user, "kitchenManageAccess"))) throw new Error("Guard 2 failed");
    if (!(((basePrice >= 0) && (pricePerPerson >= 0)))) throw new Error("Menu pricing cannot be negative");
    if (!((((minGuests >= 0) && (maxGuests >= 0)) && ((maxGuests === 0) || (maxGuests >= minGuests))))) throw new Error("Menu max guests must be zero (unlimited) or at least min guests");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      basePrice: basePrice,
      pricePerPerson: pricePerPerson,
      minGuests: minGuests,
      maxGuests: maxGuests,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, menuId: docId, tenantId: __after.tenantId, basePrice: basePrice, pricePerPerson: pricePerPerson, minGuests: minGuests, maxGuests: maxGuests, _subject: { entity: "Menu", command: "updatePricing", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "MenuPricingUpdated", entity: "Menu", entityId: docId, payload: { menuId: docId, tenantId: __after.tenantId, basePrice: basePrice, pricePerPerson: pricePerPerson, minGuests: minGuests, maxGuests: maxGuests }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Menu_updatePricing = mutation({
  args: {
    docId: v.id("menus"),
    basePrice: v.number(),
    pricePerPerson: v.number(),
    minGuests: v.any(),
    maxGuests: v.any(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runMenuUpdatePricing(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Menu_updatePricing", __result);
    }
    return __result;
  },
});

async function __runMenuDishAdd(ctx: MutationCtx, { docId, menuId, dishId, sortOrder, sellingPrice, course, serviceStyle, specialInstructions, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("MenuDish not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("MenuDish not found");
    const __rel_menu = await __resolveRelation(ctx, "menus", [__auth.tenantId, doc.menuId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).menuId;
      ((doc as any) as any).menu = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "salesAccess")))) throw new Error("Kitchen and sales staff may read menu dish lines");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write menu dish lines through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute menu dish line commands");
    if (!((doc.addedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(((__rel_menu != null) && (__rel_menu.status === "draft")))) throw new Error("Guard 2 failed");
    if (!((menuId === doc.menuId))) throw new Error("Add menuId must match the seeded menu reference");
    if (!(((sellingPrice == null) || (sellingPrice >= 0)))) throw new Error("Menu dish selling price cannot be negative");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      menuId: menuId,
      dishId: dishId,
      sortOrder: ((sortOrder != null) ? sortOrder : 0),
      sellingPrice: sellingPrice,
      course: course,
      serviceStyle: serviceStyle,
      specialInstructions: specialInstructions,
      addedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, menuDishId: docId, tenantId: __after.tenantId, menuId: menuId, dishId: dishId, sortOrder: ((sortOrder != null) ? sortOrder : 0), sellingPrice: sellingPrice, course: course, serviceStyle: serviceStyle, _subject: { entity: "MenuDish", command: "add", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "MenuDishAdded", entity: "MenuDish", entityId: docId, payload: { menuDishId: docId, tenantId: __after.tenantId, menuId: menuId, dishId: dishId, sortOrder: ((sortOrder != null) ? sortOrder : 0), sellingPrice: sellingPrice, course: course, serviceStyle: serviceStyle }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const MenuDish_add = mutation({
  args: {
    docId: v.id("menuDishes"),
    menuId: v.string(),
    dishId: v.string(),
    sortOrder: v.optional(v.any()),
    sellingPrice: v.optional(v.number()),
    course: v.optional(v.string()),
    serviceStyle: v.optional(v.string()),
    specialInstructions: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runMenuDishAdd(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "MenuDish_add", __result);
    }
    return __result;
  },
});

export const MenuDish_createViaAdd = mutation({
  args: {
    menuId: v.string(),
    dishId: v.string(),
    sortOrder: v.optional(v.any()),
    sellingPrice: v.optional(v.number()),
    course: v.optional(v.string()),
    serviceStyle: v.optional(v.string()),
    specialInstructions: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { menuId, dishId, sortOrder, sellingPrice, course, serviceStyle, specialInstructions } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      sortOrder: args.sortOrder !== undefined ? args.sortOrder : 0,
      createdAt: Date.now(),
      updatedAt: Date.now(),
      course: args.course,
      dishId: args.dishId,
      menuId: args.menuId,
      sellingPrice: args.sellingPrice,
      serviceStyle: args.serviceStyle,
      specialInstructions: args.specialInstructions
    };
    const __rel_menu = await __resolveRelation(ctx, "menus", [__auth.tenantId, __draft.menuId], ["tenantId","id"], "tenantId", __auth.tenantId);
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "salesAccess")))) throw new Error("Kitchen and sales staff may read menu dish lines");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write menu dish lines through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute menu dish line commands");
    if (!((__draft.addedAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(((__rel_menu != null) && (__rel_menu.status === "draft")))) throw new Error("Guard 2 failed");
    if (!((menuId === __draft.menuId))) throw new Error("Add menuId must match the seeded menu reference");
    if (!(((sellingPrice == null) || (sellingPrice >= 0)))) throw new Error("Menu dish selling price cannot be negative");
    const doc: Record<string, any> = {
      ...__draft,
      menuId: menuId,
      dishId: dishId,
      sortOrder: ((sortOrder != null) ? sortOrder : 0),
      sellingPrice: sellingPrice,
      course: course,
      serviceStyle: serviceStyle,
      specialInstructions: specialInstructions,
      addedAt: Date.now(),
      version: 1,
    };
    const docId = await ctx.db.insert("menuDishes", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, menuDishId: docId, tenantId: doc.tenantId, menuId: doc.menuId, dishId: doc.dishId, sortOrder: ((doc.sortOrder != null) ? doc.sortOrder : 0), sellingPrice: doc.sellingPrice, course: doc.course, serviceStyle: doc.serviceStyle, _subject: { entity: "MenuDish", command: "add", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "MenuDishAdded", entity: "MenuDish", entityId: docId, payload: { menuDishId: docId, tenantId: doc.tenantId, menuId: doc.menuId, dishId: doc.dishId, sortOrder: ((doc.sortOrder != null) ? doc.sortOrder : 0), sellingPrice: doc.sellingPrice, course: doc.course, serviceStyle: doc.serviceStyle }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "MenuDish_createViaAdd", __result);
    }
    return __result;
  },
});

async function __runMenuDishRemove(ctx: MutationCtx, { docId, reason, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("MenuDish not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("MenuDish not found");
    const __rel_menu = await __resolveRelation(ctx, "menus", [__auth.tenantId, doc.menuId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).menuId;
      ((doc as any) as any).menu = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "salesAccess")))) throw new Error("Kitchen and sales staff may read menu dish lines");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write menu dish lines through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute menu dish line commands");
    if (!((doc.addedAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(((__rel_menu != null) && ((__rel_menu.status === "draft") || (__rel_menu.status === "published"))))) throw new Error("Guard 2 failed");
    if (!((((reason).trim()).length > 0))) throw new Error("Removal reason is required");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      deletedAt: Date.now(),
      removedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, menuDishId: docId, tenantId: __after.tenantId, menuId: __after.menuId, dishId: __after.dishId, reason: reason, _subject: { entity: "MenuDish", command: "remove", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "MenuDishRemoved", entity: "MenuDish", entityId: docId, payload: { menuDishId: docId, tenantId: __after.tenantId, menuId: __after.menuId, dishId: __after.dishId, reason: reason }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const MenuDish_remove = mutation({
  args: {
    docId: v.id("menuDishes"),
    reason: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runMenuDishRemove(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "MenuDish_remove", __result);
    }
    return __result;
  },
});

async function __runMenuDishUpdateDetails(ctx: MutationCtx, { docId, sortOrder, course, serviceStyle, specialInstructions, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("MenuDish not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("MenuDish not found");
    const __rel_menu = await __resolveRelation(ctx, "menus", [__auth.tenantId, doc.menuId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).menuId;
      ((doc as any) as any).menu = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "salesAccess")))) throw new Error("Kitchen and sales staff may read menu dish lines");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write menu dish lines through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute menu dish line commands");
    if (!((doc.addedAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(((__rel_menu != null) && (__rel_menu.status === "draft")))) throw new Error("Guard 2 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      sortOrder: ((sortOrder != null) ? sortOrder : doc.sortOrder),
      course: course,
      serviceStyle: serviceStyle,
      specialInstructions: specialInstructions,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, menuDishId: docId, tenantId: __after.tenantId, menuId: __after.menuId, dishId: __after.dishId, sortOrder: ((sortOrder != null) ? sortOrder : __after.sortOrder), course: course, serviceStyle: serviceStyle, _subject: { entity: "MenuDish", command: "updateDetails", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "MenuDishDetailsUpdated", entity: "MenuDish", entityId: docId, payload: { menuDishId: docId, tenantId: __after.tenantId, menuId: __after.menuId, dishId: __after.dishId, sortOrder: ((sortOrder != null) ? sortOrder : __after.sortOrder), course: course, serviceStyle: serviceStyle }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const MenuDish_updateDetails = mutation({
  args: {
    docId: v.id("menuDishes"),
    sortOrder: v.optional(v.any()),
    course: v.optional(v.string()),
    serviceStyle: v.optional(v.string()),
    specialInstructions: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runMenuDishUpdateDetails(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "MenuDish_updateDetails", __result);
    }
    return __result;
  },
});

async function __runMenuDishUpdateSellingPrice(ctx: MutationCtx, { docId, sellingPrice, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("MenuDish not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("MenuDish not found");
    const __rel_menu = await __resolveRelation(ctx, "menus", [__auth.tenantId, doc.menuId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).menuId;
      ((doc as any) as any).menu = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "salesAccess")))) throw new Error("Kitchen and sales staff may read menu dish lines");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write menu dish lines through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute menu dish line commands");
    if (!((doc.addedAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(((__rel_menu != null) && ((__rel_menu.status === "draft") || (__rel_menu.status === "published"))))) throw new Error("Guard 2 failed");
    if (!(checkRole(user, "kitchenManageAccess"))) throw new Error("Guard 3 failed");
    if (!((sellingPrice >= 0))) throw new Error("Menu dish selling price cannot be negative");
    const previousSellingPrice = doc.sellingPrice;
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      sellingPrice: sellingPrice,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, menuDishId: docId, tenantId: __after.tenantId, menuId: __after.menuId, dishId: __after.dishId, previousSellingPrice: previousSellingPrice, sellingPrice: sellingPrice, _subject: { entity: "MenuDish", command: "updateSellingPrice", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "MenuDishSellingPriceUpdated", entity: "MenuDish", entityId: docId, payload: { menuDishId: docId, tenantId: __after.tenantId, menuId: __after.menuId, dishId: __after.dishId, previousSellingPrice: previousSellingPrice, sellingPrice: sellingPrice }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const MenuDish_updateSellingPrice = mutation({
  args: {
    docId: v.id("menuDishes"),
    sellingPrice: v.number(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runMenuDishUpdateSellingPrice(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "MenuDish_updateSellingPrice", __result);
    }
    return __result;
  },
});

async function __runMessagePost(ctx: MutationCtx, { docId, threadId, direction, status, bodyText, providerMessageId, senderIdentity, sentAt, rawPayload, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Message not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Message not found");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may read messages");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may write messages through commands");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may execute message commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!((user.id != null))) throw new Error("Guard 1 failed");
    if (!((((bodyText).trim()).length > 0))) throw new Error("Message text is required");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      threadId: threadId,
      direction: direction,
      status: status,
      bodyText: bodyText,
      providerMessageId: providerMessageId,
      senderIdentity: senderIdentity,
      sentAt: sentAt,
      rawPayload: rawPayload,
      authorAuthSubjectId: user.id,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, messageId: docId, tenantId: __after.tenantId, threadId: threadId, direction: direction, providerMessageId: providerMessageId, _subject: { entity: "Message", command: "post", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "MessagePosted", entity: "Message", entityId: docId, payload: { messageId: docId, tenantId: __after.tenantId, threadId: threadId, direction: direction, providerMessageId: providerMessageId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Message_post = mutation({
  args: {
    docId: v.id("messages"),
    threadId: v.string(),
    direction: v.any(),
    status: v.any(),
    bodyText: v.string(),
    providerMessageId: v.optional(v.string()),
    senderIdentity: v.optional(v.string()),
    sentAt: v.optional(v.number()),
    rawPayload: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runMessagePost(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Message_post", __result);
    }
    return __result;
  },
});

export const Message_createViaPost = mutation({
  args: {
    threadId: v.string(),
    direction: v.any(),
    status: v.any(),
    bodyText: v.string(),
    providerMessageId: v.optional(v.string()),
    senderIdentity: v.optional(v.string()),
    sentAt: v.optional(v.number()),
    rawPayload: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { threadId, direction, status, bodyText, providerMessageId, senderIdentity, sentAt, rawPayload } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      createdAt: Date.now(),
      updatedAt: Date.now(),
      status: args.status !== undefined ? args.status : "received",
      bodyText: args.bodyText,
      direction: args.direction,
      providerMessageId: args.providerMessageId,
      rawPayload: args.rawPayload,
      senderIdentity: args.senderIdentity,
      sentAt: args.sentAt,
      threadId: args.threadId
    };
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may read messages");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may write messages through commands");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may execute message commands");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!((user.id != null))) throw new Error("Guard 1 failed");
    if (!((((bodyText).trim()).length > 0))) throw new Error("Message text is required");
    const doc: Record<string, any> = {
      ...__draft,
      threadId: threadId,
      direction: direction,
      status: status,
      bodyText: bodyText,
      providerMessageId: providerMessageId,
      senderIdentity: senderIdentity,
      sentAt: sentAt,
      rawPayload: rawPayload,
      authorAuthSubjectId: user.id,
      version: 1,
    };
    const docId = await ctx.db.insert("messages", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, messageId: docId, tenantId: doc.tenantId, threadId: doc.threadId, direction: doc.direction, providerMessageId: doc.providerMessageId, _subject: { entity: "Message", command: "post", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "MessagePosted", entity: "Message", entityId: docId, payload: { messageId: docId, tenantId: doc.tenantId, threadId: doc.threadId, direction: doc.direction, providerMessageId: doc.providerMessageId }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Message_createViaPost", __result);
    }
    return __result;
  },
});

async function __runMessageSetDelivery(ctx: MutationCtx, { docId, status, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Message not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Message not found");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may read messages");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may write messages through commands");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may execute message commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!((user.id != null))) throw new Error("Guard 1 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: status,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, messageId: docId, tenantId: __after.tenantId, status: status, _subject: { entity: "Message", command: "setDelivery", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "MessageDeliveryUpdated", entity: "Message", entityId: docId, payload: { messageId: docId, tenantId: __after.tenantId, status: status }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Message_setDelivery = mutation({
  args: {
    docId: v.id("messages"),
    status: v.any(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runMessageSetDelivery(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Message_setDelivery", __result);
    }
    return __result;
  },
});

async function __runMessageThreadCreate(ctx: MutationCtx, args: any) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc: Record<string, any> = {
      tenantId: __auth.tenantId,
      deletedAt: args.deletedAt,
      leadId: args.leadId,
      status: args.status ?? "active",
      createdAt: args.createdAt,
      updatedAt: args.updatedAt,
      provider: args.provider,
      providerAccountId: args.providerAccountId,
      providerThreadId: args.providerThreadId,
      subject: args.subject,
      senderIdentity: args.senderIdentity,
      contactId: args.contactId,
      openedByAuthSubjectId: user.id,
      version: 1
    };
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may read message threads");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may write message threads through commands");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may execute message thread commands");
    if (!((args.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!((user.id != null))) throw new Error("Guard 1 failed");
    const _id = await ctx.db.insert("messageThreads", doc as any);
    const payload: Record<string, any> = { _id, id: _id, ...doc, result: { _id, id: _id, ...doc }, threadId: _id, tenantId: doc.tenantId, provider: doc.provider, providerThreadId: doc.providerThreadId, contactId: doc.contactId, _subject: { entity: "MessageThread", command: "create", id: _id } };
    await ctx.db.insert("manifestEvents", { type: "MessageThreadCreated", entity: "MessageThread", entityId: _id, payload: { threadId: _id, tenantId: doc.tenantId, provider: doc.provider, providerThreadId: doc.providerThreadId, contactId: doc.contactId }, createdAt: Date.now() });
    return { _id, ...doc };

}

export const MessageThread_create = mutation({
  args: {
    deletedAt: v.optional(v.union(v.number(), v.null())),
    leadId: v.optional(v.union(v.id("leads"), v.null())),
    status: v.optional(v.union(v.literal("active"), v.literal("non_lead"), v.literal("archived"))),
    createdAt: v.optional(v.number()),
    updatedAt: v.optional(v.number()),
    provider: v.any(),
    providerAccountId: v.optional(v.string()),
    providerThreadId: v.optional(v.string()),
    subject: v.optional(v.string()),
    senderIdentity: v.optional(v.string()),
    contactId: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runMessageThreadCreate(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "MessageThread_create", __result);
    }
    return __result;
  },
});

async function __runMessageThreadLinkContact(ctx: MutationCtx, { docId, contactId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("MessageThread not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("MessageThread not found");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may read message threads");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may write message threads through commands");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may execute message thread commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!((user.id != null))) throw new Error("Guard 1 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      contactId: contactId,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, threadId: docId, tenantId: __after.tenantId, contactId: contactId, _subject: { entity: "MessageThread", command: "linkContact", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "MessageThreadContactLinked", entity: "MessageThread", entityId: docId, payload: { threadId: docId, tenantId: __after.tenantId, contactId: contactId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const MessageThread_linkContact = mutation({
  args: {
    docId: v.id("messageThreads"),
    contactId: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runMessageThreadLinkContact(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "MessageThread_linkContact", __result);
    }
    return __result;
  },
});

async function __runMessageThreadLinkLead(ctx: MutationCtx, { docId, leadId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("MessageThread not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("MessageThread not found");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may read message threads");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may write message threads through commands");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may execute message thread commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!((user.id != null))) throw new Error("Guard 1 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      leadId: leadId,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, threadId: docId, tenantId: __after.tenantId, leadId: leadId, _subject: { entity: "MessageThread", command: "linkLead", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "MessageThreadLeadLinked", entity: "MessageThread", entityId: docId, payload: { threadId: docId, tenantId: __after.tenantId, leadId: leadId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const MessageThread_linkLead = mutation({
  args: {
    docId: v.id("messageThreads"),
    leadId: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runMessageThreadLinkLead(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "MessageThread_linkLead", __result);
    }
    return __result;
  },
});

async function __runMessageThreadSetStatus(ctx: MutationCtx, { docId, status, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("MessageThread not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("MessageThread not found");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may read message threads");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may write message threads through commands");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may execute message thread commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!((user.id != null))) throw new Error("Guard 1 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: status,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, threadId: docId, tenantId: __after.tenantId, status: status, _subject: { entity: "MessageThread", command: "setStatus", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "MessageThreadStatusSet", entity: "MessageThread", entityId: docId, payload: { threadId: docId, tenantId: __after.tenantId, status: status }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const MessageThread_setStatus = mutation({
  args: {
    docId: v.id("messageThreads"),
    status: v.any(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runMessageThreadSetStatus(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "MessageThread_setStatus", __result);
    }
    return __result;
  },
});

async function __runOccasionActivate(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Occasion not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Occasion not found");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may read occasions");
    if (!(checkRole(user, "eventManageAccess"))) throw new Error("Event managers may write occasions through commands");
    if (!(checkRole(user, "eventManageAccess"))) throw new Error("Event managers may execute occasion commands");
    if (!((doc.status === "inactive"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(checkRole(user, "eventManageAccess"))) throw new Error("Guard 2 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "active";
        const __allowed: Record<string, string[]> = { "active": ["inactive"], "inactive": ["active"] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "active",
      deactivatedAt: null,
      deactivationReason: null,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, occasionId: docId, tenantId: __after.tenantId, _subject: { entity: "Occasion", command: "activate", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "OccasionActivated", entity: "Occasion", entityId: docId, payload: { occasionId: docId, tenantId: __after.tenantId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Occasion_activate = mutation({
  args: {
    docId: v.id("occasions"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runOccasionActivate(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Occasion_activate", __result);
    }
    return __result;
  },
});

async function __runOccasionDeactivate(ctx: MutationCtx, { docId, reason, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Occasion not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Occasion not found");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may read occasions");
    if (!(checkRole(user, "eventManageAccess"))) throw new Error("Event managers may write occasions through commands");
    if (!(checkRole(user, "eventManageAccess"))) throw new Error("Event managers may execute occasion commands");
    if (!((doc.status === "active"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(checkRole(user, "eventManageAccess"))) throw new Error("Guard 2 failed");
    if (!((((reason).trim()).length > 0))) throw new Error("Deactivation reason is required");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "inactive";
        const __allowed: Record<string, string[]> = { "active": ["inactive"], "inactive": ["active"] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "inactive",
      deactivatedAt: Date.now(),
      deactivationReason: reason,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, occasionId: docId, tenantId: __after.tenantId, reason: reason, _subject: { entity: "Occasion", command: "deactivate", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "OccasionDeactivated", entity: "Occasion", entityId: docId, payload: { occasionId: docId, tenantId: __after.tenantId, reason: reason }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Occasion_deactivate = mutation({
  args: {
    docId: v.id("occasions"),
    reason: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runOccasionDeactivate(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Occasion_deactivate", __result);
    }
    return __result;
  },
});

async function __runOccasionRegister(ctx: MutationCtx, { docId, name, code, sortOrder, description, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Occasion not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Occasion not found");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may read occasions");
    if (!(checkRole(user, "eventManageAccess"))) throw new Error("Event managers may write occasions through commands");
    if (!(checkRole(user, "eventManageAccess"))) throw new Error("Event managers may execute occasion commands");
    if (!((doc.registeredAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((((name).trim()).length > 0))) throw new Error("Occasion name is required");
    if (!((((code).trim()).length > 0))) throw new Error("Occasion code is required");
    if (!(((sortOrder == null) || (sortOrder >= 0)))) throw new Error("Display order cannot be negative");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "active";
        const __allowed: Record<string, string[]> = { "active": ["inactive"], "inactive": ["active"] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      name: name,
      code: code,
      sortOrder: ((sortOrder != null) ? sortOrder : 0),
      description: description,
      status: "active",
      registeredAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, occasionId: docId, tenantId: __after.tenantId, name: name, code: code, sortOrder: ((sortOrder != null) ? sortOrder : 0), _subject: { entity: "Occasion", command: "register", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "OccasionRegistered", entity: "Occasion", entityId: docId, payload: { occasionId: docId, tenantId: __after.tenantId, name: name, code: code, sortOrder: ((sortOrder != null) ? sortOrder : 0) }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Occasion_register = mutation({
  args: {
    docId: v.id("occasions"),
    name: v.string(),
    code: v.string(),
    sortOrder: v.optional(v.any()),
    description: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runOccasionRegister(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Occasion_register", __result);
    }
    return __result;
  },
});

export const Occasion_createViaRegister = mutation({
  args: {
    name: v.string(),
    code: v.string(),
    sortOrder: v.optional(v.any()),
    description: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { name, code, sortOrder, description } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      sortOrder: args.sortOrder !== undefined ? args.sortOrder : 0,
      createdAt: Date.now(),
      updatedAt: Date.now(),
      status: "active",
      code: args.code,
      description: args.description,
      name: args.name
    };
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may read occasions");
    if (!(checkRole(user, "eventManageAccess"))) throw new Error("Event managers may write occasions through commands");
    if (!(checkRole(user, "eventManageAccess"))) throw new Error("Event managers may execute occasion commands");
    if (!((__draft.registeredAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((((name).trim()).length > 0))) throw new Error("Occasion name is required");
    if (!((((code).trim()).length > 0))) throw new Error("Occasion code is required");
    if (!(((sortOrder == null) || (sortOrder >= 0)))) throw new Error("Display order cannot be negative");
    const doc: Record<string, any> = {
      ...__draft,
      name: name,
      code: code,
      sortOrder: ((sortOrder != null) ? sortOrder : 0),
      description: description,
      status: "active",
      registeredAt: Date.now(),
      version: 1,
    };
    const docId = await ctx.db.insert("occasions", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, occasionId: docId, tenantId: doc.tenantId, name: doc.name, code: doc.code, sortOrder: ((doc.sortOrder != null) ? doc.sortOrder : 0), _subject: { entity: "Occasion", command: "register", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "OccasionRegistered", entity: "Occasion", entityId: docId, payload: { occasionId: docId, tenantId: doc.tenantId, name: doc.name, code: doc.code, sortOrder: ((doc.sortOrder != null) ? doc.sortOrder : 0) }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Occasion_createViaRegister", __result);
    }
    return __result;
  },
});

async function __runOccasionReviseDetails(ctx: MutationCtx, { docId, name, description, sortOrder, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Occasion not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Occasion not found");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may read occasions");
    if (!(checkRole(user, "eventManageAccess"))) throw new Error("Event managers may write occasions through commands");
    if (!(checkRole(user, "eventManageAccess"))) throw new Error("Event managers may execute occasion commands");
    if (!((doc.registeredAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.status === "active"))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((((name).trim()).length > 0))) throw new Error("Occasion name is required");
    if (!(((sortOrder == null) || (sortOrder >= 0)))) throw new Error("Display order cannot be negative");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      name: name,
      description: description,
      sortOrder: ((sortOrder != null) ? sortOrder : doc.sortOrder),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, occasionId: docId, tenantId: __after.tenantId, name: name, sortOrder: ((sortOrder != null) ? sortOrder : __after.sortOrder), _subject: { entity: "Occasion", command: "reviseDetails", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "OccasionDetailsRevised", entity: "Occasion", entityId: docId, payload: { occasionId: docId, tenantId: __after.tenantId, name: name, sortOrder: ((sortOrder != null) ? sortOrder : __after.sortOrder) }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Occasion_reviseDetails = mutation({
  args: {
    docId: v.id("occasions"),
    name: v.string(),
    description: v.optional(v.string()),
    sortOrder: v.optional(v.any()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runOccasionReviseDetails(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Occasion_reviseDetails", __result);
    }
    return __result;
  },
});

async function __runOneOnOneHold(ctx: MutationCtx, { docId, leadPersonId, staffMemberId, meetingDate, agenda, goals, wins, opportunities, decisions, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("OneOnOne not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("OneOnOne not found");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Workforce managers may read one-on-ones");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Workforce managers may write one-on-ones through commands");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Workforce managers may execute one-on-one commands");
    if (!((doc.heldAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((user.id != null))) throw new Error("Guard 2 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      leadPersonId: leadPersonId,
      staffMemberId: staffMemberId,
      meetingDate: meetingDate,
      agenda: ((agenda != null) ? agenda : ""),
      goals: ((goals != null) ? goals : "[]"),
      wins: ((wins != null) ? wins : ""),
      opportunities: ((opportunities != null) ? opportunities : ""),
      decisions: ((decisions != null) ? decisions : "[]"),
      heldAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, oneOnOneId: docId, tenantId: __after.tenantId, leadPersonId: leadPersonId, staffMemberId: staffMemberId, meetingDate: meetingDate, _subject: { entity: "OneOnOne", command: "hold", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "OneOnOneHeld", entity: "OneOnOne", entityId: docId, payload: { oneOnOneId: docId, tenantId: __after.tenantId, leadPersonId: leadPersonId, staffMemberId: staffMemberId, meetingDate: meetingDate }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const OneOnOne_hold = mutation({
  args: {
    docId: v.id("oneOnOnes"),
    leadPersonId: v.string(),
    staffMemberId: v.string(),
    meetingDate: v.number(),
    agenda: v.optional(v.string()),
    goals: v.optional(v.string()),
    wins: v.optional(v.string()),
    opportunities: v.optional(v.string()),
    decisions: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runOneOnOneHold(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "OneOnOne_hold", __result);
    }
    return __result;
  },
});

export const OneOnOne_createViaHold = mutation({
  args: {
    leadPersonId: v.string(),
    staffMemberId: v.string(),
    meetingDate: v.number(),
    agenda: v.optional(v.string()),
    goals: v.optional(v.string()),
    wins: v.optional(v.string()),
    opportunities: v.optional(v.string()),
    decisions: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { leadPersonId, staffMemberId, meetingDate, agenda, goals, wins, opportunities, decisions } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      agenda: args.agenda !== undefined ? args.agenda : "",
      goals: args.goals !== undefined ? args.goals : "[]",
      wins: args.wins !== undefined ? args.wins : "",
      opportunities: args.opportunities !== undefined ? args.opportunities : "",
      decisions: args.decisions !== undefined ? args.decisions : "[]",
      createdAt: Date.now(),
      updatedAt: Date.now(),
      leadPersonId: args.leadPersonId,
      meetingDate: args.meetingDate,
      staffMemberId: args.staffMemberId
    };
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Workforce managers may read one-on-ones");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Workforce managers may write one-on-ones through commands");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Workforce managers may execute one-on-one commands");
    if (!((__draft.heldAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((user.id != null))) throw new Error("Guard 2 failed");
    const doc: Record<string, any> = {
      ...__draft,
      leadPersonId: leadPersonId,
      staffMemberId: staffMemberId,
      meetingDate: meetingDate,
      agenda: ((agenda != null) ? agenda : ""),
      goals: ((goals != null) ? goals : "[]"),
      wins: ((wins != null) ? wins : ""),
      opportunities: ((opportunities != null) ? opportunities : ""),
      decisions: ((decisions != null) ? decisions : "[]"),
      heldAt: Date.now(),
      version: 1,
    };
    const docId = await ctx.db.insert("oneOnOnes", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, oneOnOneId: docId, tenantId: doc.tenantId, leadPersonId: doc.leadPersonId, staffMemberId: doc.staffMemberId, meetingDate: doc.meetingDate, _subject: { entity: "OneOnOne", command: "hold", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "OneOnOneHeld", entity: "OneOnOne", entityId: docId, payload: { oneOnOneId: docId, tenantId: doc.tenantId, leadPersonId: doc.leadPersonId, staffMemberId: doc.staffMemberId, meetingDate: doc.meetingDate }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "OneOnOne_createViaHold", __result);
    }
    return __result;
  },
});

async function __runOneOnOneActionCapture(ctx: MutationCtx, { docId, oneOnOneId, ownerPersonId, description, dueDate, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("OneOnOneAction not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("OneOnOneAction not found");
    const __rel_oneOnOne = await __resolveRelation(ctx, "oneOnOnes", [__auth.tenantId, doc.oneOnOneId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).oneOnOneId;
      ((doc as any) as any).oneOnOne = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Workforce managers may read one-on-one actions");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Workforce managers may write one-on-one actions through commands");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Workforce managers may execute one-on-one action commands");
    if (!((doc.capturedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(((__rel_oneOnOne != null) && (__rel_oneOnOne.heldAt != null)))) throw new Error("Guard 2 failed");
    if (!((user.id != null))) throw new Error("Guard 3 failed");
    if (!((((description).trim()).length > 0))) throw new Error("Action description is required");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      oneOnOneId: oneOnOneId,
      ownerPersonId: ownerPersonId,
      description: description,
      dueDate: dueDate,
      capturedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, actionId: docId, tenantId: __after.tenantId, oneOnOneId: oneOnOneId, ownerPersonId: ownerPersonId, description: description, _subject: { entity: "OneOnOneAction", command: "capture", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "OneOnOneActionCaptured", entity: "OneOnOneAction", entityId: docId, payload: { actionId: docId, tenantId: __after.tenantId, oneOnOneId: oneOnOneId, ownerPersonId: ownerPersonId, description: description }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const OneOnOneAction_capture = mutation({
  args: {
    docId: v.id("oneOnOneActions"),
    oneOnOneId: v.string(),
    ownerPersonId: v.string(),
    description: v.string(),
    dueDate: v.optional(v.number()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runOneOnOneActionCapture(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "OneOnOneAction_capture", __result);
    }
    return __result;
  },
});

export const OneOnOneAction_createViaCapture = mutation({
  args: {
    oneOnOneId: v.string(),
    ownerPersonId: v.string(),
    description: v.string(),
    dueDate: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { oneOnOneId, ownerPersonId, description, dueDate } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      status: "open",
      createdAt: Date.now(),
      updatedAt: Date.now(),
      description: args.description,
      dueDate: args.dueDate,
      oneOnOneId: args.oneOnOneId,
      ownerPersonId: args.ownerPersonId
    };
    const __rel_oneOnOne = await __resolveRelation(ctx, "oneOnOnes", [__auth.tenantId, __draft.oneOnOneId], ["tenantId","id"], "tenantId", __auth.tenantId);
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Workforce managers may read one-on-one actions");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Workforce managers may write one-on-one actions through commands");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Workforce managers may execute one-on-one action commands");
    if (!((__draft.capturedAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(((__rel_oneOnOne != null) && (__rel_oneOnOne.heldAt != null)))) throw new Error("Guard 2 failed");
    if (!((user.id != null))) throw new Error("Guard 3 failed");
    if (!((((description).trim()).length > 0))) throw new Error("Action description is required");
    const doc: Record<string, any> = {
      ...__draft,
      oneOnOneId: oneOnOneId,
      ownerPersonId: ownerPersonId,
      description: description,
      dueDate: dueDate,
      capturedAt: Date.now(),
      version: 1,
    };
    const docId = await ctx.db.insert("oneOnOneActions", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, actionId: docId, tenantId: doc.tenantId, oneOnOneId: doc.oneOnOneId, ownerPersonId: doc.ownerPersonId, description: doc.description, _subject: { entity: "OneOnOneAction", command: "capture", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "OneOnOneActionCaptured", entity: "OneOnOneAction", entityId: docId, payload: { actionId: docId, tenantId: doc.tenantId, oneOnOneId: doc.oneOnOneId, ownerPersonId: doc.ownerPersonId, description: doc.description }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "OneOnOneAction_createViaCapture", __result);
    }
    return __result;
  },
});

async function __runOneOnOneActionClose(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("OneOnOneAction not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("OneOnOneAction not found");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Workforce managers may read one-on-one actions");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Workforce managers may write one-on-one actions through commands");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Workforce managers may execute one-on-one action commands");
    if (!((doc.status === "open"))) throw new Error("Guard 0 failed");
    if (!((doc.capturedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((user.id != null))) throw new Error("Guard 3 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "closed";
        const __allowed: Record<string, string[]> = { "open": ["closed"] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "closed",
      closedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, actionId: docId, tenantId: __after.tenantId, oneOnOneId: __after.oneOnOneId, _subject: { entity: "OneOnOneAction", command: "close", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "OneOnOneActionClosed", entity: "OneOnOneAction", entityId: docId, payload: { actionId: docId, tenantId: __after.tenantId, oneOnOneId: __after.oneOnOneId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const OneOnOneAction_close = mutation({
  args: {
    docId: v.id("oneOnOneActions"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runOneOnOneActionClose(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "OneOnOneAction_close", __result);
    }
    return __result;
  },
});

async function __runOrganizationConfigureBranding(ctx: MutationCtx, { docId, displayName, address, primaryColor, accentColor, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Organization not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Organization not found");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may read organization records");
    if (!(checkRole(user, "manageAccess"))) throw new Error("Managers may write organization records");
    if (!(checkRole(user, "manageAccess"))) throw new Error("Managers may execute organization commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      brandDisplayName: displayName,
      brandAddress: address,
      brandPrimaryColor: primaryColor,
      brandAccentColor: accentColor,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, organizationId: docId, tenantId: __after.tenantId, displayName: displayName, address: address, primaryColor: primaryColor, accentColor: accentColor, _subject: { entity: "Organization", command: "configureBranding", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "OrganizationBrandingConfigured", entity: "Organization", entityId: docId, payload: { organizationId: docId, tenantId: __after.tenantId, displayName: displayName, address: address, primaryColor: primaryColor, accentColor: accentColor }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Organization_configureBranding = mutation({
  args: {
    docId: v.id("organizations"),
    displayName: v.string(),
    address: v.string(),
    primaryColor: v.string(),
    accentColor: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runOrganizationConfigureBranding(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Organization_configureBranding", __result);
    }
    return __result;
  },
});

async function __runOrganizationDeactivate(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Organization not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Organization not found");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may read organization records");
    if (!(checkRole(user, "manageAccess"))) throw new Error("Managers may write organization records");
    if (!(checkRole(user, "manageAccess"))) throw new Error("Managers may execute organization commands");
    if (!(((doc.status === "active") || (doc.status === "suspended")))) throw new Error("Guard 0 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "deactivated";
        const __allowed: Record<string, string[]> = { "active": ["suspended", "deactivated"], "suspended": ["active", "deactivated"], "deactivated": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "deactivated",
      deletedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, organizationId: docId, tenantId: __after.tenantId, _subject: { entity: "Organization", command: "deactivate", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "OrganizationDeactivated", entity: "Organization", entityId: docId, payload: { organizationId: docId, tenantId: __after.tenantId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Organization_deactivate = mutation({
  args: {
    docId: v.id("organizations"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runOrganizationDeactivate(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Organization_deactivate", __result);
    }
    return __result;
  },
});

async function __runOrganizationReactivate(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Organization not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Organization not found");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may read organization records");
    if (!(checkRole(user, "manageAccess"))) throw new Error("Managers may write organization records");
    if (!(checkRole(user, "manageAccess"))) throw new Error("Managers may execute organization commands");
    if (!((doc.status === "suspended"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "active";
        const __allowed: Record<string, string[]> = { "active": ["suspended", "deactivated"], "suspended": ["active", "deactivated"], "deactivated": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "active",
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, organizationId: docId, tenantId: __after.tenantId, _subject: { entity: "Organization", command: "reactivate", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "OrganizationReactivated", entity: "Organization", entityId: docId, payload: { organizationId: docId, tenantId: __after.tenantId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Organization_reactivate = mutation({
  args: {
    docId: v.id("organizations"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runOrganizationReactivate(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Organization_reactivate", __result);
    }
    return __result;
  },
});

async function __runOrganizationRegister(ctx: MutationCtx, { docId, name, brandDisplayName, brandAddress, brandPrimaryColor, brandAccentColor, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Organization not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Organization not found");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may read organization records");
    if (!(checkRole(user, "manageAccess"))) throw new Error("Managers may write organization records");
    if (!(checkRole(user, "manageAccess"))) throw new Error("Managers may execute organization commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!(checkRole(user, "adminAccess"))) throw new Error("Guard 1 failed");
    if (!((((name).trim()).length > 0))) throw new Error("Organization name is required");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      name: name,
      brandDisplayName: brandDisplayName,
      brandAddress: brandAddress,
      brandPrimaryColor: brandPrimaryColor,
      brandAccentColor: brandAccentColor,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, organizationId: docId, tenantId: __after.tenantId, name: name, brandDisplayName: brandDisplayName, brandAddress: brandAddress, brandPrimaryColor: brandPrimaryColor, brandAccentColor: brandAccentColor, _subject: { entity: "Organization", command: "register", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "OrganizationRegistered", entity: "Organization", entityId: docId, payload: { organizationId: docId, tenantId: __after.tenantId, name: name, brandDisplayName: brandDisplayName, brandAddress: brandAddress, brandPrimaryColor: brandPrimaryColor, brandAccentColor: brandAccentColor }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Organization_register = mutation({
  args: {
    docId: v.id("organizations"),
    name: v.string(),
    brandDisplayName: v.optional(v.string()),
    brandAddress: v.optional(v.string()),
    brandPrimaryColor: v.optional(v.string()),
    brandAccentColor: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runOrganizationRegister(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Organization_register", __result);
    }
    return __result;
  },
});

export const Organization_createViaRegister = mutation({
  args: {
    name: v.string(),
    brandDisplayName: v.optional(v.string()),
    brandAddress: v.optional(v.string()),
    brandPrimaryColor: v.optional(v.string()),
    brandAccentColor: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { name, brandDisplayName, brandAddress, brandPrimaryColor, brandAccentColor } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      status: "active",
      defaultExchangeRateScale: 6,
      createdAt: Date.now(),
      updatedAt: Date.now(),
      brandAccentColor: args.brandAccentColor,
      brandAddress: args.brandAddress,
      brandDisplayName: args.brandDisplayName,
      brandPrimaryColor: args.brandPrimaryColor,
      name: args.name
    };
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may read organization records");
    if (!(checkRole(user, "manageAccess"))) throw new Error("Managers may write organization records");
    if (!(checkRole(user, "manageAccess"))) throw new Error("Managers may execute organization commands");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!(checkRole(user, "adminAccess"))) throw new Error("Guard 1 failed");
    if (!((((name).trim()).length > 0))) throw new Error("Organization name is required");
    const doc: Record<string, any> = {
      ...__draft,
      name: name,
      brandDisplayName: brandDisplayName,
      brandAddress: brandAddress,
      brandPrimaryColor: brandPrimaryColor,
      brandAccentColor: brandAccentColor,
      version: 1,
    };
    const docId = await ctx.db.insert("organizations", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, organizationId: docId, tenantId: doc.tenantId, name: doc.name, brandDisplayName: doc.brandDisplayName, brandAddress: doc.brandAddress, brandPrimaryColor: doc.brandPrimaryColor, brandAccentColor: doc.brandAccentColor, _subject: { entity: "Organization", command: "register", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "OrganizationRegistered", entity: "Organization", entityId: docId, payload: { organizationId: docId, tenantId: doc.tenantId, name: doc.name, brandDisplayName: doc.brandDisplayName, brandAddress: doc.brandAddress, brandPrimaryColor: doc.brandPrimaryColor, brandAccentColor: doc.brandAccentColor }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Organization_createViaRegister", __result);
    }
    return __result;
  },
});

async function __runOrganizationRename(ctx: MutationCtx, { docId, name, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Organization not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Organization not found");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may read organization records");
    if (!(checkRole(user, "manageAccess"))) throw new Error("Managers may write organization records");
    if (!(checkRole(user, "manageAccess"))) throw new Error("Managers may execute organization commands");
    if (!((doc.status === "active"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((((name).trim()).length > 0))) throw new Error("Organization name is required");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      name: name,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, organizationId: docId, tenantId: __after.tenantId, name: name, _subject: { entity: "Organization", command: "rename", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "OrganizationRenamed", entity: "Organization", entityId: docId, payload: { organizationId: docId, tenantId: __after.tenantId, name: name }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Organization_rename = mutation({
  args: {
    docId: v.id("organizations"),
    name: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runOrganizationRename(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Organization_rename", __result);
    }
    return __result;
  },
});

async function __runOrganizationSetDefaultCurrency(ctx: MutationCtx, { docId, currencyCode, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Organization not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Organization not found");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may read organization records");
    if (!(checkRole(user, "manageAccess"))) throw new Error("Managers may write organization records");
    if (!(checkRole(user, "manageAccess"))) throw new Error("Managers may execute organization commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!((((currencyCode).trim()).length === 3))) throw new Error("Currency code must be a three-letter ISO 4217 code");
    const normalizedCode = (currencyCode).toUpperCase();
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      defaultCurrencyCode: normalizedCode,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, organizationId: docId, tenantId: __after.tenantId, currencyCode: normalizedCode, _subject: { entity: "Organization", command: "setDefaultCurrency", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "OrganizationDefaultCurrencySet", entity: "Organization", entityId: docId, payload: { organizationId: docId, tenantId: __after.tenantId, currencyCode: normalizedCode }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Organization_setDefaultCurrency = mutation({
  args: {
    docId: v.id("organizations"),
    currencyCode: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runOrganizationSetDefaultCurrency(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Organization_setDefaultCurrency", __result);
    }
    return __result;
  },
});

async function __runOrganizationSuspend(ctx: MutationCtx, { docId, reason, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Organization not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Organization not found");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may read organization records");
    if (!(checkRole(user, "manageAccess"))) throw new Error("Managers may write organization records");
    if (!(checkRole(user, "manageAccess"))) throw new Error("Managers may execute organization commands");
    if (!((doc.status === "active"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "suspended";
        const __allowed: Record<string, string[]> = { "active": ["suspended", "deactivated"], "suspended": ["active", "deactivated"], "deactivated": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "suspended",
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, organizationId: docId, tenantId: __after.tenantId, reason: reason, _subject: { entity: "Organization", command: "suspend", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "OrganizationSuspended", entity: "Organization", entityId: docId, payload: { organizationId: docId, tenantId: __after.tenantId, reason: reason }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Organization_suspend = mutation({
  args: {
    docId: v.id("organizations"),
    reason: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runOrganizationSuspend(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Organization_suspend", __result);
    }
    return __result;
  },
});

async function __runOrganizationCapabilitySettingRegister(ctx: MutationCtx, { docId, capability, enabled, updatedBy, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("OrganizationCapabilitySetting not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("OrganizationCapabilitySetting not found");
    if (!((user.id != null))) throw new Error("Authenticated staff may read capability settings");
    if (!((user.id != null))) throw new Error("Authenticated staff may write capability settings");
    if (!((user.id != null))) throw new Error("Authenticated staff may execute capability setting commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.capability == null))) throw new Error("Guard 1 failed");
    if (!(checkRole(user, "adminAccess"))) throw new Error("Guard 2 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      capability: capability,
      enabled: enabled,
      updatedBy: updatedBy,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    return { ...doc, ...updates };
}

export const OrganizationCapabilitySetting_register = mutation({
  args: {
    docId: v.id("organizationCapabilitySettings"),
    capability: v.any(),
    enabled: v.boolean(),
    updatedBy: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runOrganizationCapabilitySettingRegister(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "OrganizationCapabilitySetting_register", __result);
    }
    return __result;
  },
});

export const OrganizationCapabilitySetting_createViaRegister = mutation({
  args: {
    capability: v.any(),
    enabled: v.boolean(),
    updatedBy: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { capability, enabled, updatedBy } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      createdAt: Date.now(),
      updatedAt: Date.now(),
      capability: args.capability,
      enabled: args.enabled,
      updatedBy: args.updatedBy
    };
    if (!((user.id != null))) throw new Error("Authenticated staff may read capability settings");
    if (!((user.id != null))) throw new Error("Authenticated staff may write capability settings");
    if (!((user.id != null))) throw new Error("Authenticated staff may execute capability setting commands");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.capability == null))) throw new Error("Guard 1 failed");
    if (!(checkRole(user, "adminAccess"))) throw new Error("Guard 2 failed");
    const doc: Record<string, any> = {
      ...__draft,
      capability: capability,
      enabled: enabled,
      updatedBy: updatedBy,
      version: 1,
    };
    const docId = await ctx.db.insert("organizationCapabilitySettings", doc as any);
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "OrganizationCapabilitySetting_createViaRegister", __result);
    }
    return __result;
  },
});

async function __runOrganizationCapabilitySettingSetEnabled(ctx: MutationCtx, { docId, enabled, updatedBy, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("OrganizationCapabilitySetting not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("OrganizationCapabilitySetting not found");
    if (!((user.id != null))) throw new Error("Authenticated staff may read capability settings");
    if (!((user.id != null))) throw new Error("Authenticated staff may write capability settings");
    if (!((user.id != null))) throw new Error("Authenticated staff may execute capability setting commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!(checkRole(user, "adminAccess"))) throw new Error("Guard 1 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      enabled: enabled,
      updatedBy: updatedBy,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    return { ...doc, ...updates };
}

export const OrganizationCapabilitySetting_setEnabled = mutation({
  args: {
    docId: v.id("organizationCapabilitySettings"),
    enabled: v.boolean(),
    updatedBy: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runOrganizationCapabilitySettingSetEnabled(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "OrganizationCapabilitySetting_setEnabled", __result);
    }
    return __result;
  },
});

async function __runPackListCancel(ctx: MutationCtx, { docId, reason, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("PackList not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("PackList not found");
    const doc = await __decryptDoc(ctx, "PackList", ["notes"], __storedDoc) as Record<string, any>;
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may read pack lists");
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may write pack lists through commands");
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may execute pack list commands");
    if (!(((((doc.status === "draft") || (doc.status === "packing")) || (doc.status === "packed")) || (doc.status === "loaded")))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(checkRole(user, "logisticsManageAccess"))) throw new Error("Guard 2 failed");
    if (!((((reason).trim()).length > 0))) throw new Error("Cancellation reason is required");
    const previousStatus = doc.status;
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "cancelled";
        const __allowed: Record<string, string[]> = { "draft": ["packing", "cancelled"], "packing": ["packed", "cancelled"], "packed": ["loaded", "cancelled"], "loaded": ["dispatched", "cancelled"], "dispatched": [], "cancelled": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "cancelled",
      activeEventId: null,
      cancelledAt: Date.now(),
      cancellationReason: reason,
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "PackList", ["notes"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, packListId: docId, tenantId: __after.tenantId, eventId: __after.eventId, name: __after.name, previousStatus: previousStatus, status: "cancelled", reason: reason, _subject: { entity: "PackList", command: "cancel", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "PackListCancelled", entity: "PackList", entityId: docId, payload: { packListId: docId, tenantId: __after.tenantId, eventId: __after.eventId, name: __after.name, previousStatus: previousStatus, status: "cancelled", reason: reason }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const PackList_cancel = mutation({
  args: {
    docId: v.id("packLists"),
    reason: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runPackListCancel(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "PackList_cancel", __result);
    }
    return __result;
  },
});

async function __runPackListDispatch(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("PackList not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("PackList not found");
    const doc = await __decryptDoc(ctx, "PackList", ["notes"], __storedDoc) as Record<string, any>;
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may read pack lists");
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may write pack lists through commands");
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may execute pack list commands");
    if (!((doc.status === "loaded"))) throw new Error("Guard 0 failed");
    if (!((doc.loadedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    const previousStatus = doc.status;
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "dispatched";
        const __allowed: Record<string, string[]> = { "draft": ["packing", "cancelled"], "packing": ["packed", "cancelled"], "packed": ["loaded", "cancelled"], "loaded": ["dispatched", "cancelled"], "dispatched": [], "cancelled": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "dispatched",
      activeEventId: null,
      dispatchedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "PackList", ["notes"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, packListId: docId, tenantId: __after.tenantId, eventId: __after.eventId, name: __after.name, previousStatus: previousStatus, status: "dispatched", dispatchedAt: __after.dispatchedAt, _subject: { entity: "PackList", command: "dispatch", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "PackListDispatched", entity: "PackList", entityId: docId, payload: { packListId: docId, tenantId: __after.tenantId, eventId: __after.eventId, name: __after.name, previousStatus: previousStatus, status: "dispatched", dispatchedAt: __after.dispatchedAt }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const PackList_dispatch = mutation({
  args: {
    docId: v.id("packLists"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runPackListDispatch(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "PackList_dispatch", __result);
    }
    return __result;
  },
});

async function __runPackListMarkLoaded(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("PackList not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("PackList not found");
    const doc = await __decryptDoc(ctx, "PackList", ["notes"], __storedDoc) as Record<string, any>;
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may read pack lists");
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may write pack lists through commands");
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may execute pack list commands");
    if (!((doc.status === "packed"))) throw new Error("Guard 0 failed");
    if (!((doc.packedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    const previousStatus = doc.status;
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "loaded";
        const __allowed: Record<string, string[]> = { "draft": ["packing", "cancelled"], "packing": ["packed", "cancelled"], "packed": ["loaded", "cancelled"], "loaded": ["dispatched", "cancelled"], "dispatched": [], "cancelled": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "loaded",
      loadedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "PackList", ["notes"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, packListId: docId, tenantId: __after.tenantId, eventId: __after.eventId, name: __after.name, previousStatus: previousStatus, status: "loaded", loadedAt: __after.loadedAt, _subject: { entity: "PackList", command: "markLoaded", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "PackListLoaded", entity: "PackList", entityId: docId, payload: { packListId: docId, tenantId: __after.tenantId, eventId: __after.eventId, name: __after.name, previousStatus: previousStatus, status: "loaded", loadedAt: __after.loadedAt }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const PackList_markLoaded = mutation({
  args: {
    docId: v.id("packLists"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runPackListMarkLoaded(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "PackList_markLoaded", __result);
    }
    return __result;
  },
});

async function __runPackListMarkPacked(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("PackList not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("PackList not found");
    const doc = await __decryptDoc(ctx, "PackList", ["notes"], __storedDoc) as Record<string, any>;
    const __rel_event = await __resolveRelation(ctx, "events", [__auth.tenantId, doc.eventId], ["tenantId","id"], "tenantId", __auth.tenantId);
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may read pack lists");
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may write pack lists through commands");
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may execute pack list commands");
    if (!((doc.status === "packing"))) throw new Error("Guard 0 failed");
    if (!((doc.packingStartedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    const previousStatus = doc.status;
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "packed";
        const __allowed: Record<string, string[]> = { "draft": ["packing", "cancelled"], "packing": ["packed", "cancelled"], "packed": ["loaded", "cancelled"], "loaded": ["dispatched", "cancelled"], "dispatched": [], "cancelled": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "packed",
      packedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "PackList", ["notes"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, packListId: docId, tenantId: __after.tenantId, eventId: __after.eventId, name: __after.name, previousStatus: previousStatus, status: "packed", packedAt: __after.packedAt, destination: ((((__rel_event != null) && (__rel_event.venueAddress != null)) && (((__rel_event.venueAddress).trim()).length > 0)) ? __rel_event.venueAddress : ((((__rel_event != null) && (__rel_event.venueName != null)) && (((__rel_event.venueName).trim()).length > 0)) ? __rel_event.venueName : "Event site")), windowStartsAt: ((__rel_event != null) ? __rel_event.startsAt : Date.now()), windowEndsAt: ((__rel_event != null) ? __rel_event.endsAt : Date.now()), _subject: { entity: "PackList", command: "markPacked", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "PackListPacked", entity: "PackList", entityId: docId, payload: { packListId: docId, tenantId: __after.tenantId, eventId: __after.eventId, name: __after.name, previousStatus: previousStatus, status: "packed", packedAt: __after.packedAt, destination: ((((__rel_event != null) && (__rel_event.venueAddress != null)) && (((__rel_event.venueAddress).trim()).length > 0)) ? __rel_event.venueAddress : ((((__rel_event != null) && (__rel_event.venueName != null)) && (((__rel_event.venueName).trim()).length > 0)) ? __rel_event.venueName : "Event site")), windowStartsAt: ((__rel_event != null) ? __rel_event.startsAt : Date.now()), windowEndsAt: ((__rel_event != null) ? __rel_event.endsAt : Date.now()) }, createdAt: Date.now() });
    // Reactions
    const __match0_raw = await ctx.db.query("deliveries").withIndex("by_packListId", (q) => q.eq("packListId", payload.packListId)).collect();
    const __match0_rows = __match0_raw.filter((d) => (d as any).packListId === payload.packListId && (d as any).deletedAt == null).sort((a, b) => String((a as any)._id).localeCompare(String((b as any)._id)));
    const __match0_id = __match0_rows.length > 0 ? (__match0_rows[0] as any)._id : null;
    if (__match0_id) {
      await __runDeliverySchedule(ctx, { docId: __match0_id, packListId: payload.packListId, eventId: payload.eventId, destination: payload.destination, windowStartsAt: payload.windowStartsAt, windowEndsAt: payload.windowEndsAt } as any);
    } else {
      const __elseArgs: Record<string, any> = { packListId: payload.packListId, eventId: payload.eventId, destination: payload.destination, windowStartsAt: payload.windowStartsAt, windowEndsAt: payload.windowEndsAt };
      const __elseDoc: Record<string, any> = {
        tenantId: __auth.tenantId,
        destination: "",
        status: "scheduled",
        createdAt: Date.now(),
        updatedAt: Date.now(),
        version: 0,
      };
      for (const __k of ["deletedAt","packListId","eventId","driverId","vehicleId","destination","windowStartsAt","windowEndsAt","notes","status","scheduledAt","departedAt","deliveredAt","failedAt","failureReason","cancelledAt","cancellationReason","createdAt","updatedAt"] as string[]) {
        if (__elseArgs[__k] !== undefined) __elseDoc[__k] = __elseArgs[__k];
      }
      const __elseId = await ctx.db.insert("deliveries", __elseDoc as any);
      await __runDeliverySchedule(ctx, { docId: __elseId, ...__elseArgs } as any);
    }
    return { ...doc, ...updates };
}

export const PackList_markPacked = mutation({
  args: {
    docId: v.id("packLists"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runPackListMarkPacked(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "PackList_markPacked", __result);
    }
    return __result;
  },
});

async function __runPackListOpen(ctx: MutationCtx, { docId, eventId, name, purpose, notes, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("PackList not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("PackList not found");
    const doc = await __decryptDoc(ctx, "PackList", ["notes"], __storedDoc) as Record<string, any>;
    const __rel_event = await __resolveRelation(ctx, "events", [__auth.tenantId, doc.eventId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).eventId;
      ((doc as any) as any).event = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may read pack lists");
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may write pack lists through commands");
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may execute pack list commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!(((doc.status !== "cancelled") && (doc.status !== "dispatched")))) throw new Error("Guard 1 failed");
    if (!((__rel_event != null))) throw new Error("Guard 2 failed");
    if (!(((doc.openedAt == null) || (doc.eventId === eventId)))) throw new Error("Guard 3 failed");
    if (!((eventId === doc.eventId))) throw new Error("Open eventId must match the seeded event reference");
    if (!(((((name).trim()).length > 0) || (((doc.name).trim()).length > 0)))) throw new Error("Pack list name is required");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      eventId: eventId,
      activeEventId: eventId,
      name: ((((doc.name).trim()).length > 0) ? doc.name : name),
      purpose: ((purpose != null) ? purpose : doc.purpose),
      notes: ((notes != null) ? notes : doc.notes),
      openedAt: ((doc.openedAt != null) ? doc.openedAt : Date.now()),
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "PackList", ["notes"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, packListId: docId, tenantId: __after.tenantId, eventId: eventId, name: ((((__after.name).trim()).length > 0) ? __after.name : name), purpose: ((purpose != null) ? purpose : __after.purpose), status: __after.status, _subject: { entity: "PackList", command: "open", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "PackListOpened", entity: "PackList", entityId: docId, payload: { packListId: docId, tenantId: __after.tenantId, eventId: eventId, name: ((((__after.name).trim()).length > 0) ? __after.name : name), purpose: ((purpose != null) ? purpose : __after.purpose), status: __after.status }, createdAt: Date.now() });
    // Reactions
    const fanRows0 = (await ctx.db.query("eventDishes").withIndex("by_eventId", (q) => q.eq("eventId", payload.eventId)).collect()).filter((d) => (d as any).deletedAt == null);
    for (const __row of fanRows0) {
      const target = __row;
      await __runEventDishRequestContainerPack(ctx, { docId: (__row as any)._id, packListId: payload.packListId } as any);
    }
    return { ...doc, ...updates };
}

export const PackList_open = mutation({
  args: {
    docId: v.id("packLists"),
    eventId: v.string(),
    name: v.string(),
    purpose: v.optional(v.string()),
    notes: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runPackListOpen(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "PackList_open", __result);
    }
    return __result;
  },
});

export const PackList_createViaOpen = mutation({
  args: {
    eventId: v.string(),
    name: v.string(),
    purpose: v.optional(v.string()),
    notes: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { eventId, name, purpose, notes } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      status: "draft",
      createdAt: Date.now(),
      updatedAt: Date.now(),
      eventId: args.eventId,
      name: args.name,
      notes: args.notes,
      purpose: args.purpose
    };
    const __rel_event = await __resolveRelation(ctx, "events", [__auth.tenantId, __draft.eventId], ["tenantId","id"], "tenantId", __auth.tenantId);
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may read pack lists");
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may write pack lists through commands");
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may execute pack list commands");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!(((__draft.status !== "cancelled") && (__draft.status !== "dispatched")))) throw new Error("Guard 1 failed");
    if (!((__rel_event != null))) throw new Error("Guard 2 failed");
    if (!(((__draft.openedAt == null) || (__draft.eventId === eventId)))) throw new Error("Guard 3 failed");
    if (!((eventId === __draft.eventId))) throw new Error("Open eventId must match the seeded event reference");
    if (!(((((name).trim()).length > 0) || (((__draft.name).trim()).length > 0)))) throw new Error("Pack list name is required");
    const doc: Record<string, any> = {
      ...__draft,
      eventId: eventId,
      activeEventId: eventId,
      name: ((((__draft.name).trim()).length > 0) ? __draft.name : name),
      purpose: ((purpose != null) ? purpose : __draft.purpose),
      notes: ((notes != null) ? notes : __draft.notes),
      openedAt: ((__draft.openedAt != null) ? __draft.openedAt : Date.now()),
      version: 1,
    };
    const __storedDoc = await __encryptDoc(ctx, "PackList", ["notes"], doc);
    const docId = await ctx.db.insert("packLists", __storedDoc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, packListId: docId, tenantId: doc.tenantId, eventId: doc.eventId, name: ((((doc.name).trim()).length > 0) ? doc.name : doc.name), purpose: ((doc.purpose != null) ? doc.purpose : doc.purpose), status: doc.status, _subject: { entity: "PackList", command: "open", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "PackListOpened", entity: "PackList", entityId: docId, payload: { packListId: docId, tenantId: doc.tenantId, eventId: doc.eventId, name: ((((doc.name).trim()).length > 0) ? doc.name : doc.name), purpose: ((doc.purpose != null) ? doc.purpose : doc.purpose), status: doc.status }, createdAt: Date.now() });
    // Reactions
    const fanRows0 = (await ctx.db.query("eventDishes").withIndex("by_eventId", (q) => q.eq("eventId", payload.eventId)).collect()).filter((d) => (d as any).deletedAt == null);
    for (const __row of fanRows0) {
      const target = __row;
      await __runEventDishRequestContainerPack(ctx, { docId: (__row as any)._id, packListId: payload.packListId } as any);
    }
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "PackList_createViaOpen", __result);
    }
    return __result;
  },
});

async function __runPackListStartPacking(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("PackList not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("PackList not found");
    const doc = await __decryptDoc(ctx, "PackList", ["notes"], __storedDoc) as Record<string, any>;
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may read pack lists");
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may write pack lists through commands");
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may execute pack list commands");
    if (!((doc.status === "draft"))) throw new Error("Guard 0 failed");
    if (!((doc.openedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    const previousStatus = doc.status;
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "packing";
        const __allowed: Record<string, string[]> = { "draft": ["packing", "cancelled"], "packing": ["packed", "cancelled"], "packed": ["loaded", "cancelled"], "loaded": ["dispatched", "cancelled"], "dispatched": [], "cancelled": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "packing",
      packingStartedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "PackList", ["notes"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, packListId: docId, tenantId: __after.tenantId, eventId: __after.eventId, name: __after.name, previousStatus: previousStatus, status: "packing", _subject: { entity: "PackList", command: "startPacking", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "PackListPackingStarted", entity: "PackList", entityId: docId, payload: { packListId: docId, tenantId: __after.tenantId, eventId: __after.eventId, name: __after.name, previousStatus: previousStatus, status: "packing" }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const PackList_startPacking = mutation({
  args: {
    docId: v.id("packLists"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runPackListStartPacking(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "PackList_startPacking", __result);
    }
    return __result;
  },
});

async function __runPackListItemAddItem(ctx: MutationCtx, { docId, packListId, description, requiredQuantity, unit, dishId, productionBatchId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("PackListItem not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("PackListItem not found");
    const __rel_packList = await __resolveRelation(ctx, "packLists", [__auth.tenantId, doc.packListId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).packListId;
      ((doc as any) as any).packList = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!(checkRole(user, "logisticsAccess"))) throw new Error("Logistics staff may read pack list items");
    if (!(checkRole(user, "logisticsAccess"))) throw new Error("Logistics staff may write pack list items through commands");
    if (!(checkRole(user, "logisticsAccess"))) throw new Error("Logistics staff may execute pack list item commands");
    if (!((doc.listedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.status === "pending"))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((((__rel_packList != null) && (__rel_packList.openedAt != null)) && ((__rel_packList.status === "draft") || (__rel_packList.status === "packing"))))) throw new Error("Guard 3 failed");
    if (!((packListId === doc.packListId))) throw new Error("Add item packListId must match the seeded pack list reference");
    if (!((((dishId == null) || (doc.dishId == null)) || (dishId === doc.dishId)))) throw new Error("Add item dishId must match the seeded dish reference when provided");
    if (!((((productionBatchId == null) || (doc.productionBatchId == null)) || (productionBatchId === doc.productionBatchId)))) throw new Error("Add item productionBatchId must match the seeded batch reference when provided");
    if (!((((description).trim()).length > 0))) throw new Error("Pack item description is required");
    if (!((requiredQuantity > 0))) throw new Error("Required quantity must be positive");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "listed";
        const __allowed: Record<string, string[]> = { "pending": ["listed"], "listed": ["packed", "missing"], "packed": [], "missing": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      packListId: packListId,
      description: description,
      dishId: ((dishId != null) ? dishId : doc.dishId),
      productionBatchId: ((productionBatchId != null) ? productionBatchId : doc.productionBatchId),
      requiredQuantity: requiredQuantity,
      unit: unit,
      status: "listed",
      listedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const __rel_packList__post = await __resolveRelation(ctx, "packLists", [__auth.tenantId, __after.packListId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, packListItemId: docId, tenantId: __after.tenantId, packListId: packListId, eventId: __rel_packList__post.eventId, description: description, dishId: ((dishId != null) ? dishId : __after.dishId), productionBatchId: ((productionBatchId != null) ? productionBatchId : __after.productionBatchId), requiredQuantity: requiredQuantity, unit: unit, status: "listed", _subject: { entity: "PackListItem", command: "addItem", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "PackListItemAdded", entity: "PackListItem", entityId: docId, payload: { packListItemId: docId, tenantId: __after.tenantId, packListId: packListId, eventId: __rel_packList__post.eventId, description: description, dishId: ((dishId != null) ? dishId : __after.dishId), productionBatchId: ((productionBatchId != null) ? productionBatchId : __after.productionBatchId), requiredQuantity: requiredQuantity, unit: unit, status: "listed" }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const PackListItem_addItem = mutation({
  args: {
    docId: v.id("packListItems"),
    packListId: v.string(),
    description: v.string(),
    requiredQuantity: v.number(),
    unit: v.any(),
    dishId: v.optional(v.string()),
    productionBatchId: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runPackListItemAddItem(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "PackListItem_addItem", __result);
    }
    return __result;
  },
});

export const PackListItem_createViaAddItem = mutation({
  args: {
    packListId: v.string(),
    description: v.string(),
    requiredQuantity: v.number(),
    unit: v.any(),
    dishId: v.optional(v.string()),
    productionBatchId: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { packListId, description, requiredQuantity, unit, dishId, productionBatchId } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      packedQuantity: 0,
      createdAt: Date.now(),
      updatedAt: Date.now(),
      status: "pending",
      description: args.description,
      dishId: args.dishId,
      packListId: args.packListId,
      productionBatchId: args.productionBatchId,
      requiredQuantity: args.requiredQuantity,
      unit: args.unit
    };
    const __rel_packList = await __resolveRelation(ctx, "packLists", [__auth.tenantId, __draft.packListId], ["tenantId","id"], "tenantId", __auth.tenantId);
    if (!(checkRole(user, "logisticsAccess"))) throw new Error("Logistics staff may read pack list items");
    if (!(checkRole(user, "logisticsAccess"))) throw new Error("Logistics staff may write pack list items through commands");
    if (!(checkRole(user, "logisticsAccess"))) throw new Error("Logistics staff may execute pack list item commands");
    if (!((__draft.listedAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.status === "pending"))) throw new Error("Guard 1 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((((__rel_packList != null) && (__rel_packList.openedAt != null)) && ((__rel_packList.status === "draft") || (__rel_packList.status === "packing"))))) throw new Error("Guard 3 failed");
    if (!((packListId === __draft.packListId))) throw new Error("Add item packListId must match the seeded pack list reference");
    if (!((((dishId == null) || (__draft.dishId == null)) || (dishId === __draft.dishId)))) throw new Error("Add item dishId must match the seeded dish reference when provided");
    if (!((((productionBatchId == null) || (__draft.productionBatchId == null)) || (productionBatchId === __draft.productionBatchId)))) throw new Error("Add item productionBatchId must match the seeded batch reference when provided");
    if (!((((description).trim()).length > 0))) throw new Error("Pack item description is required");
    if (!((requiredQuantity > 0))) throw new Error("Required quantity must be positive");
    const doc: Record<string, any> = {
      ...__draft,
      packListId: packListId,
      description: description,
      dishId: ((dishId != null) ? dishId : __draft.dishId),
      productionBatchId: ((productionBatchId != null) ? productionBatchId : __draft.productionBatchId),
      requiredQuantity: requiredQuantity,
      unit: unit,
      status: "listed",
      listedAt: Date.now(),
      version: 1,
    };
    const docId = await ctx.db.insert("packListItems", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, packListItemId: docId, tenantId: doc.tenantId, packListId: doc.packListId, eventId: __rel_packList.eventId, description: doc.description, dishId: ((doc.dishId != null) ? doc.dishId : doc.dishId), productionBatchId: ((doc.productionBatchId != null) ? doc.productionBatchId : doc.productionBatchId), requiredQuantity: doc.requiredQuantity, unit: doc.unit, status: "listed", _subject: { entity: "PackListItem", command: "addItem", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "PackListItemAdded", entity: "PackListItem", entityId: docId, payload: { packListItemId: docId, tenantId: doc.tenantId, packListId: doc.packListId, eventId: __rel_packList.eventId, description: doc.description, dishId: ((doc.dishId != null) ? doc.dishId : doc.dishId), productionBatchId: ((doc.productionBatchId != null) ? doc.productionBatchId : doc.productionBatchId), requiredQuantity: doc.requiredQuantity, unit: doc.unit, status: "listed" }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "PackListItem_createViaAddItem", __result);
    }
    return __result;
  },
});

async function __runPackListItemAdjustQuantity(ctx: MutationCtx, { docId, requiredQuantity, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("PackListItem not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("PackListItem not found");
    const __rel_packList = await __resolveRelation(ctx, "packLists", [__auth.tenantId, doc.packListId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).packListId;
      ((doc as any) as any).packList = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!(checkRole(user, "logisticsAccess"))) throw new Error("Logistics staff may read pack list items");
    if (!(checkRole(user, "logisticsAccess"))) throw new Error("Logistics staff may write pack list items through commands");
    if (!(checkRole(user, "logisticsAccess"))) throw new Error("Logistics staff may execute pack list item commands");
    if (!((doc.status === "listed"))) throw new Error("Guard 0 failed");
    if (!((doc.listedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!(((__rel_packList != null) && ((__rel_packList.status === "draft") || (__rel_packList.status === "packing"))))) throw new Error("Guard 3 failed");
    if (!((requiredQuantity > 0))) throw new Error("Required quantity must be positive");
    if (!((requiredQuantity >= doc.packedQuantity))) throw new Error("Required quantity cannot be below packed quantity");
    const previousRequired = doc.requiredQuantity;
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      requiredQuantity: requiredQuantity,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, packListItemId: docId, tenantId: __after.tenantId, packListId: __after.packListId, eventId: __rel_packList.eventId, description: __after.description, previousRequiredQuantity: previousRequired, requiredQuantity: requiredQuantity, packedQuantity: __after.packedQuantity, unit: __after.unit, status: __after.status, _subject: { entity: "PackListItem", command: "adjustQuantity", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "PackListItemQuantityAdjusted", entity: "PackListItem", entityId: docId, payload: { packListItemId: docId, tenantId: __after.tenantId, packListId: __after.packListId, eventId: __rel_packList.eventId, description: __after.description, previousRequiredQuantity: previousRequired, requiredQuantity: requiredQuantity, packedQuantity: __after.packedQuantity, unit: __after.unit, status: __after.status }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const PackListItem_adjustQuantity = mutation({
  args: {
    docId: v.id("packListItems"),
    requiredQuantity: v.number(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runPackListItemAdjustQuantity(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "PackListItem_adjustQuantity", __result);
    }
    return __result;
  },
});

async function __runPackListItemMarkMissing(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("PackListItem not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("PackListItem not found");
    const __rel_packList = await __resolveRelation(ctx, "packLists", [__auth.tenantId, doc.packListId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).packListId;
      ((doc as any) as any).packList = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!(checkRole(user, "logisticsAccess"))) throw new Error("Logistics staff may read pack list items");
    if (!(checkRole(user, "logisticsAccess"))) throw new Error("Logistics staff may write pack list items through commands");
    if (!(checkRole(user, "logisticsAccess"))) throw new Error("Logistics staff may execute pack list item commands");
    if (!((doc.status === "listed"))) throw new Error("Guard 0 failed");
    if (!((doc.listedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!(((__rel_packList != null) && ((((__rel_packList.status === "packing") || (__rel_packList.status === "packed")) || (__rel_packList.status === "loaded")) || (__rel_packList.status === "dispatched"))))) throw new Error("Guard 3 failed");
    const previousStatus = doc.status;
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "missing";
        const __allowed: Record<string, string[]> = { "pending": ["listed"], "listed": ["packed", "missing"], "packed": [], "missing": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "missing",
      missingAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, packListItemId: docId, tenantId: __after.tenantId, packListId: __after.packListId, eventId: __rel_packList.eventId, description: __after.description, previousStatus: previousStatus, status: "missing", requiredQuantity: __after.requiredQuantity, packedQuantity: __after.packedQuantity, unit: __after.unit, _subject: { entity: "PackListItem", command: "markMissing", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "PackListItemMissing", entity: "PackListItem", entityId: docId, payload: { packListItemId: docId, tenantId: __after.tenantId, packListId: __after.packListId, eventId: __rel_packList.eventId, description: __after.description, previousStatus: previousStatus, status: "missing", requiredQuantity: __after.requiredQuantity, packedQuantity: __after.packedQuantity, unit: __after.unit }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const PackListItem_markMissing = mutation({
  args: {
    docId: v.id("packListItems"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runPackListItemMarkMissing(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "PackListItem_markMissing", __result);
    }
    return __result;
  },
});

async function __runPackListItemMarkPacked(ctx: MutationCtx, { docId, packedQuantity, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("PackListItem not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("PackListItem not found");
    const __rel_packList = await __resolveRelation(ctx, "packLists", [__auth.tenantId, doc.packListId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).packListId;
      ((doc as any) as any).packList = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!(checkRole(user, "logisticsAccess"))) throw new Error("Logistics staff may read pack list items");
    if (!(checkRole(user, "logisticsAccess"))) throw new Error("Logistics staff may write pack list items through commands");
    if (!(checkRole(user, "logisticsAccess"))) throw new Error("Logistics staff may execute pack list item commands");
    if (!((doc.status === "listed"))) throw new Error("Guard 0 failed");
    if (!((doc.listedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!(((__rel_packList != null) && ((((__rel_packList.status === "packing") || (__rel_packList.status === "packed")) || (__rel_packList.status === "loaded")) || (__rel_packList.status === "dispatched"))))) throw new Error("Guard 3 failed");
    if (!((packedQuantity > 0))) throw new Error("Packed quantity must be positive");
    if (!((packedQuantity <= doc.requiredQuantity))) throw new Error("Packed quantity cannot exceed required quantity");
    const previousStatus = doc.status;
    const previousPacked = doc.packedQuantity;
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "packed";
        const __allowed: Record<string, string[]> = { "pending": ["listed"], "listed": ["packed", "missing"], "packed": [], "missing": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      packedQuantity: packedQuantity,
      status: "packed",
      packedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, packListItemId: docId, tenantId: __after.tenantId, packListId: __after.packListId, eventId: __rel_packList.eventId, description: __after.description, previousStatus: previousStatus, status: "packed", previousPackedQuantity: previousPacked, packedQuantity: packedQuantity, requiredQuantity: __after.requiredQuantity, unit: __after.unit, _subject: { entity: "PackListItem", command: "markPacked", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "PackListItemPacked", entity: "PackListItem", entityId: docId, payload: { packListItemId: docId, tenantId: __after.tenantId, packListId: __after.packListId, eventId: __rel_packList.eventId, description: __after.description, previousStatus: previousStatus, status: "packed", previousPackedQuantity: previousPacked, packedQuantity: packedQuantity, requiredQuantity: __after.requiredQuantity, unit: __after.unit }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const PackListItem_markPacked = mutation({
  args: {
    docId: v.id("packListItems"),
    packedQuantity: v.number(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runPackListItemMarkPacked(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "PackListItem_markPacked", __result);
    }
    return __result;
  },
});

async function __runPackListTemplateArchive(ctx: MutationCtx, { docId, reason, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("PackListTemplate not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("PackListTemplate not found");
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may read pack list templates");
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may write pack list templates through commands");
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may execute pack list template commands");
    if (!((doc.status === "active"))) throw new Error("Guard 0 failed");
    if (!((doc.definedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "archived";
        const __allowed: Record<string, string[]> = { "active": ["archived"], "archived": ["active"] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "archived",
      archivedAt: Date.now(),
      archiveReason: reason,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, templateId: docId, tenantId: __after.tenantId, reason: reason, _subject: { entity: "PackListTemplate", command: "archive", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "PackListTemplateArchived", entity: "PackListTemplate", entityId: docId, payload: { templateId: docId, tenantId: __after.tenantId, reason: reason }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const PackListTemplate_archive = mutation({
  args: {
    docId: v.id("packListTemplates"),
    reason: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runPackListTemplateArchive(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "PackListTemplate_archive", __result);
    }
    return __result;
  },
});

async function __runPackListTemplateDefine(ctx: MutationCtx, { docId, name, description, items, serviceStyleId, occasionId, guestCountMin, guestCountMax, venueRequirement, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("PackListTemplate not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("PackListTemplate not found");
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may read pack list templates");
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may write pack list templates through commands");
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may execute pack list template commands");
    if (!((doc.definedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((((name).trim()).length > 0))) throw new Error("Template name is required");
    if (!((((guestCountMin == null) || (guestCountMax == null)) || (guestCountMax >= guestCountMin)))) throw new Error("Guest-count band max must be >= min");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      name: name,
      description: description,
      items: ((items != null) ? items : "[]"),
      serviceStyleId: serviceStyleId,
      occasionId: occasionId,
      guestCountMin: guestCountMin,
      guestCountMax: guestCountMax,
      venueRequirement: venueRequirement,
      definedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, templateId: docId, tenantId: __after.tenantId, name: name, _subject: { entity: "PackListTemplate", command: "define", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "PackListTemplateDefined", entity: "PackListTemplate", entityId: docId, payload: { templateId: docId, tenantId: __after.tenantId, name: name }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const PackListTemplate_define = mutation({
  args: {
    docId: v.id("packListTemplates"),
    name: v.string(),
    description: v.optional(v.string()),
    items: v.optional(v.string()),
    serviceStyleId: v.optional(v.string()),
    occasionId: v.optional(v.string()),
    guestCountMin: v.optional(v.number()),
    guestCountMax: v.optional(v.number()),
    venueRequirement: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runPackListTemplateDefine(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "PackListTemplate_define", __result);
    }
    return __result;
  },
});

export const PackListTemplate_createViaDefine = mutation({
  args: {
    name: v.string(),
    description: v.optional(v.string()),
    items: v.optional(v.string()),
    serviceStyleId: v.optional(v.string()),
    occasionId: v.optional(v.string()),
    guestCountMin: v.optional(v.number()),
    guestCountMax: v.optional(v.number()),
    venueRequirement: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { name, description, items, serviceStyleId, occasionId, guestCountMin, guestCountMax, venueRequirement } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      items: args.items !== undefined ? args.items : "[]",
      status: "active",
      createdAt: Date.now(),
      updatedAt: Date.now(),
      description: args.description,
      guestCountMax: args.guestCountMax,
      guestCountMin: args.guestCountMin,
      name: args.name,
      occasionId: args.occasionId,
      serviceStyleId: args.serviceStyleId,
      venueRequirement: args.venueRequirement
    };
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may read pack list templates");
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may write pack list templates through commands");
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may execute pack list template commands");
    if (!((__draft.definedAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((((name).trim()).length > 0))) throw new Error("Template name is required");
    if (!((((guestCountMin == null) || (guestCountMax == null)) || (guestCountMax >= guestCountMin)))) throw new Error("Guest-count band max must be >= min");
    const doc: Record<string, any> = {
      ...__draft,
      name: name,
      description: description,
      items: ((items != null) ? items : "[]"),
      serviceStyleId: serviceStyleId,
      occasionId: occasionId,
      guestCountMin: guestCountMin,
      guestCountMax: guestCountMax,
      venueRequirement: venueRequirement,
      definedAt: Date.now(),
      version: 1,
    };
    const docId = await ctx.db.insert("packListTemplates", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, templateId: docId, tenantId: doc.tenantId, name: doc.name, _subject: { entity: "PackListTemplate", command: "define", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "PackListTemplateDefined", entity: "PackListTemplate", entityId: docId, payload: { templateId: docId, tenantId: doc.tenantId, name: doc.name }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "PackListTemplate_createViaDefine", __result);
    }
    return __result;
  },
});

async function __runPackListTemplateReactivate(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("PackListTemplate not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("PackListTemplate not found");
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may read pack list templates");
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may write pack list templates through commands");
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may execute pack list template commands");
    if (!((doc.status === "archived"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "active";
        const __allowed: Record<string, string[]> = { "active": ["archived"], "archived": ["active"] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "active",
      archivedAt: null,
      archiveReason: null,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, templateId: docId, tenantId: __after.tenantId, _subject: { entity: "PackListTemplate", command: "reactivate", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "PackListTemplateReactivated", entity: "PackListTemplate", entityId: docId, payload: { templateId: docId, tenantId: __after.tenantId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const PackListTemplate_reactivate = mutation({
  args: {
    docId: v.id("packListTemplates"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runPackListTemplateReactivate(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "PackListTemplate_reactivate", __result);
    }
    return __result;
  },
});

async function __runPackListTemplateRevise(ctx: MutationCtx, { docId, name, description, items, serviceStyleId, occasionId, guestCountMin, guestCountMax, venueRequirement, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("PackListTemplate not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("PackListTemplate not found");
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may read pack list templates");
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may write pack list templates through commands");
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may execute pack list template commands");
    if (!((doc.definedAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.status === "active"))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((((name).trim()).length > 0))) throw new Error("Template name is required");
    if (!((((guestCountMin == null) || (guestCountMax == null)) || (guestCountMax >= guestCountMin)))) throw new Error("Guest-count band max must be >= min");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      name: name,
      description: description,
      items: ((items != null) ? items : doc.items),
      serviceStyleId: serviceStyleId,
      occasionId: occasionId,
      guestCountMin: guestCountMin,
      guestCountMax: guestCountMax,
      venueRequirement: venueRequirement,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, templateId: docId, tenantId: __after.tenantId, name: name, _subject: { entity: "PackListTemplate", command: "revise", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "PackListTemplateRevised", entity: "PackListTemplate", entityId: docId, payload: { templateId: docId, tenantId: __after.tenantId, name: name }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const PackListTemplate_revise = mutation({
  args: {
    docId: v.id("packListTemplates"),
    name: v.string(),
    description: v.optional(v.string()),
    items: v.optional(v.string()),
    serviceStyleId: v.optional(v.string()),
    occasionId: v.optional(v.string()),
    guestCountMin: v.optional(v.number()),
    guestCountMax: v.optional(v.number()),
    venueRequirement: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runPackListTemplateRevise(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "PackListTemplate_revise", __result);
    }
    return __result;
  },
});

async function __runPaymentBeginProcessing(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Payment not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Payment not found");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may read payments");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may write payments through commands");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may execute payment commands");
    if (!((doc.status === "pending"))) throw new Error("Guard 0 failed");
    if (!((doc.recordedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "processing";
        const __allowed: Record<string, string[]> = { "pending": ["processing", "completed", "failed"], "processing": ["completed", "failed"], "completed": ["refunded"], "failed": [], "refunded": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "processing",
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, paymentId: docId, tenantId: __after.tenantId, invoiceId: __after.invoiceId, amount: __after.amount, _subject: { entity: "Payment", command: "beginProcessing", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "PaymentProcessingStarted", entity: "Payment", entityId: docId, payload: { paymentId: docId, tenantId: __after.tenantId, invoiceId: __after.invoiceId, amount: __after.amount }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Payment_beginProcessing = mutation({
  args: {
    docId: v.id("payments"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runPaymentBeginProcessing(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Payment_beginProcessing", __result);
    }
    return __result;
  },
});

async function __runPaymentDisputeReconciliation(ctx: MutationCtx, { docId, reason, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Payment not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Payment not found");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may read payments");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may write payments through commands");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may execute payment commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Guard 1 failed");
    if (!((((reason).trim()).length > 0))) throw new Error("Dispute reason is required");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      reconciliationStatus: "disputed",
      reconciliationDetails: reason,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, paymentId: docId, tenantId: __after.tenantId, reason: reason, _subject: { entity: "Payment", command: "disputeReconciliation", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "PaymentReconciliationDisputed", entity: "Payment", entityId: docId, payload: { paymentId: docId, tenantId: __after.tenantId, reason: reason }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Payment_disputeReconciliation = mutation({
  args: {
    docId: v.id("payments"),
    reason: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runPaymentDisputeReconciliation(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Payment_disputeReconciliation", __result);
    }
    return __result;
  },
});

async function __runPaymentFail(ctx: MutationCtx, { docId, reason, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Payment not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Payment not found");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may read payments");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may write payments through commands");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may execute payment commands");
    if (!(((doc.status === "pending") || (doc.status === "processing")))) throw new Error("Guard 0 failed");
    if (!((doc.recordedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((((reason).trim()).length > 0))) throw new Error("Failure reason is required");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "failed";
        const __allowed: Record<string, string[]> = { "pending": ["processing", "completed", "failed"], "processing": ["completed", "failed"], "completed": ["refunded"], "failed": [], "refunded": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "failed",
      failedAt: Date.now(),
      failureReason: reason,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, paymentId: docId, tenantId: __after.tenantId, invoiceId: __after.invoiceId, amount: __after.amount, reason: reason, _subject: { entity: "Payment", command: "fail", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "PaymentFailed", entity: "Payment", entityId: docId, payload: { paymentId: docId, tenantId: __after.tenantId, invoiceId: __after.invoiceId, amount: __after.amount, reason: reason }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Payment_fail = mutation({
  args: {
    docId: v.id("payments"),
    reason: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runPaymentFail(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Payment_fail", __result);
    }
    return __result;
  },
});

async function __runPaymentMarkMatched(ctx: MutationCtx, { docId, source, externalPaymentId, providerTransactionIds, notes, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Payment not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Payment not found");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may read payments");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may write payments through commands");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may execute payment commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Guard 1 failed");
    if (!((((externalPaymentId).trim()).length > 0))) throw new Error("External payment ID is required for matching");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      reconciliationStatus: "matched",
      externalSource: source,
      externalPaymentId: externalPaymentId,
      providerTransactionIds: ((providerTransactionIds != null) ? providerTransactionIds : doc.providerTransactionIds),
      reconciliationDetails: ((notes != null) ? notes : doc.reconciliationDetails),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, paymentId: docId, tenantId: __after.tenantId, source: source, externalPaymentId: externalPaymentId, amount: __after.amount, _subject: { entity: "Payment", command: "markMatched", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "PaymentMatched", entity: "Payment", entityId: docId, payload: { paymentId: docId, tenantId: __after.tenantId, source: source, externalPaymentId: externalPaymentId, amount: __after.amount }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Payment_markMatched = mutation({
  args: {
    docId: v.id("payments"),
    source: v.any(),
    externalPaymentId: v.string(),
    providerTransactionIds: v.optional(v.string()),
    notes: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runPaymentMarkMatched(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Payment_markMatched", __result);
    }
    return __result;
  },
});

async function __runPaymentReassignClient(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Payment not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Payment not found");
    const __rel_clientMergeAuthorization = await __resolveRelation(ctx, "clientMerges", [__auth.tenantId, doc.clientMergeAuthorizationId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_mergeTargetClient = await __resolveRelation(ctx, "clients", [__auth.tenantId, doc.mergeTargetClientId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).clientMergeAuthorizationId;
      ((doc as any) as any).clientMergeAuthorization = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    {
      const __fk = ((doc as any) as any).mergeTargetClientId;
      ((doc as any) as any).mergeTargetClient = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may read payments");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may write payments through commands");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may execute payment commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!(checkRole(user, "adminAccess"))) throw new Error("Guard 1 failed");
    if (!((__rel_clientMergeAuthorization != null))) throw new Error("Guard 2 failed");
    if (!((__rel_clientMergeAuthorization.deletedAt == null))) throw new Error("Guard 3 failed");
    if (!((__rel_clientMergeAuthorization.mergedAt != null))) throw new Error("Guard 4 failed");
    if (!((__rel_mergeTargetClient != null))) throw new Error("Guard 5 failed");
    if (!((__rel_mergeTargetClient.status === "active"))) throw new Error("Guard 6 failed");
    if (!((__rel_mergeTargetClient.deletedAt == null))) throw new Error("Guard 7 failed");
    if (!((__rel_clientMergeAuthorization.duplicateClientId === doc.clientId))) throw new Error("Merge authorization does not match the payment client");
    if (!((__rel_clientMergeAuthorization.primaryClientId === doc.mergeTargetClientId))) throw new Error("Merge authorization does not match the primary client");
    const previousClientId = doc.clientId;
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      clientId: doc.mergeTargetClientId,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, paymentId: docId, tenantId: __after.tenantId, previousClientId: previousClientId, clientId: __after.mergeTargetClientId, _subject: { entity: "Payment", command: "reassignClient", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "PaymentClientReassigned", entity: "Payment", entityId: docId, payload: { paymentId: docId, tenantId: __after.tenantId, previousClientId: previousClientId, clientId: __after.mergeTargetClientId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Payment_reassignClient = mutation({
  args: {
    docId: v.id("payments"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runPaymentReassignClient(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Payment_reassignClient", __result);
    }
    return __result;
  },
});

async function __runPaymentRecord(ctx: MutationCtx, { docId, invoiceId, clientId, amount, method, eventId, paymentMethodId, notes, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Payment not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Payment not found");
    const __rel_invoice = await __resolveRelation(ctx, "invoices", [__auth.tenantId, doc.invoiceId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_client = await __resolveRelation(ctx, "clients", [__auth.tenantId, doc.clientId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).invoiceId;
      ((doc as any) as any).invoice = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    {
      const __fk = ((doc as any) as any).clientId;
      ((doc as any) as any).client = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may read payments");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may write payments through commands");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may execute payment commands");
    if (!((doc.recordedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.status === "pending"))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((__rel_invoice != null))) throw new Error("Guard 3 failed");
    if (!((__rel_client != null))) throw new Error("Guard 4 failed");
    if (!((invoiceId === doc.invoiceId))) throw new Error("Record invoiceId must match the seeded invoice reference");
    if (!((clientId === doc.clientId))) throw new Error("Record clientId must match the seeded client reference");
    if (!((amount > 0))) throw new Error("Payment amount must be positive");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      invoiceId: invoiceId,
      clientId: clientId,
      eventId: ((eventId != null) ? eventId : doc.eventId),
      amount: amount,
      method: method,
      paymentMethodId: paymentMethodId,
      notes: notes,
      recordedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, paymentId: docId, tenantId: __after.tenantId, invoiceId: invoiceId, clientId: clientId, eventId: ((eventId != null) ? eventId : __after.eventId), amount: amount, method: method, _subject: { entity: "Payment", command: "record", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "PaymentRecorded", entity: "Payment", entityId: docId, payload: { paymentId: docId, tenantId: __after.tenantId, invoiceId: invoiceId, clientId: clientId, eventId: ((eventId != null) ? eventId : __after.eventId), amount: amount, method: method }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Payment_record = mutation({
  args: {
    docId: v.id("payments"),
    invoiceId: v.string(),
    clientId: v.string(),
    amount: v.number(),
    method: v.any(),
    eventId: v.optional(v.string()),
    paymentMethodId: v.optional(v.string()),
    notes: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runPaymentRecord(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Payment_record", __result);
    }
    return __result;
  },
});

export const Payment_createViaRecord = mutation({
  args: {
    invoiceId: v.string(),
    clientId: v.string(),
    amount: v.number(),
    method: v.any(),
    eventId: v.optional(v.string()),
    paymentMethodId: v.optional(v.string()),
    notes: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { invoiceId, clientId, amount, method, eventId, paymentMethodId, notes } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      status: "pending",
      reconciliationStatus: "unreconciled",
      createdAt: Date.now(),
      updatedAt: Date.now(),
      amount: args.amount,
      clientId: args.clientId,
      eventId: args.eventId,
      invoiceId: args.invoiceId,
      method: args.method,
      notes: args.notes,
      paymentMethodId: args.paymentMethodId
    };
    const __rel_invoice = await __resolveRelation(ctx, "invoices", [__auth.tenantId, __draft.invoiceId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_client = await __resolveRelation(ctx, "clients", [__auth.tenantId, __draft.clientId], ["tenantId","id"], "tenantId", __auth.tenantId);
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may read payments");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may write payments through commands");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may execute payment commands");
    if (!((__draft.recordedAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.status === "pending"))) throw new Error("Guard 1 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((__rel_invoice != null))) throw new Error("Guard 3 failed");
    if (!((__rel_client != null))) throw new Error("Guard 4 failed");
    if (!((invoiceId === __draft.invoiceId))) throw new Error("Record invoiceId must match the seeded invoice reference");
    if (!((clientId === __draft.clientId))) throw new Error("Record clientId must match the seeded client reference");
    if (!((amount > 0))) throw new Error("Payment amount must be positive");
    const doc: Record<string, any> = {
      ...__draft,
      invoiceId: invoiceId,
      clientId: clientId,
      eventId: ((eventId != null) ? eventId : __draft.eventId),
      amount: amount,
      method: method,
      paymentMethodId: paymentMethodId,
      notes: notes,
      recordedAt: Date.now(),
      version: 1,
    };
    const docId = await ctx.db.insert("payments", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, paymentId: docId, tenantId: doc.tenantId, invoiceId: doc.invoiceId, clientId: doc.clientId, eventId: ((doc.eventId != null) ? doc.eventId : doc.eventId), amount: doc.amount, method: doc.method, _subject: { entity: "Payment", command: "record", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "PaymentRecorded", entity: "Payment", entityId: docId, payload: { paymentId: docId, tenantId: doc.tenantId, invoiceId: doc.invoiceId, clientId: doc.clientId, eventId: ((doc.eventId != null) ? doc.eventId : doc.eventId), amount: doc.amount, method: doc.method }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Payment_createViaRecord", __result);
    }
    return __result;
  },
});

async function __runPaymentRefund(ctx: MutationCtx, { docId, reason, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Payment not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Payment not found");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may read payments");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may write payments through commands");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may execute payment commands");
    if (!((doc.status === "completed"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((((reason).trim()).length > 0))) throw new Error("Refund reason is required");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "refunded";
        const __allowed: Record<string, string[]> = { "pending": ["processing", "completed", "failed"], "processing": ["completed", "failed"], "completed": ["refunded"], "failed": [], "refunded": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "refunded",
      refundedAt: Date.now(),
      refundReason: reason,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, paymentId: docId, tenantId: __after.tenantId, invoiceId: __after.invoiceId, clientId: __after.clientId, amount: __after.amount, reason: reason, _subject: { entity: "Payment", command: "refund", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "PaymentRefunded", entity: "Payment", entityId: docId, payload: { paymentId: docId, tenantId: __after.tenantId, invoiceId: __after.invoiceId, clientId: __after.clientId, amount: __after.amount, reason: reason }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Payment_refund = mutation({
  args: {
    docId: v.id("payments"),
    reason: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runPaymentRefund(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Payment_refund", __result);
    }
    return __result;
  },
});

async function __runPaymentSettle(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Payment not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Payment not found");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may read payments");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may write payments through commands");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may execute payment commands");
    if (!(((doc.status === "pending") || (doc.status === "processing")))) throw new Error("Guard 0 failed");
    if (!((doc.recordedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "completed";
        const __allowed: Record<string, string[]> = { "pending": ["processing", "completed", "failed"], "processing": ["completed", "failed"], "completed": ["refunded"], "failed": [], "refunded": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "completed",
      settledAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, paymentId: docId, tenantId: __after.tenantId, invoiceId: __after.invoiceId, clientId: __after.clientId, eventId: __after.eventId, amount: __after.amount, method: __after.method, _subject: { entity: "Payment", command: "settle", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "PaymentSettled", entity: "Payment", entityId: docId, payload: { paymentId: docId, tenantId: __after.tenantId, invoiceId: __after.invoiceId, clientId: __after.clientId, eventId: __after.eventId, amount: __after.amount, method: __after.method }, createdAt: Date.now() });
    // Reactions
    const reactionTarget0 = payload.invoiceId;
    if (reactionTarget0) await __runInvoiceApplyPayment(ctx, { docId: reactionTarget0, paymentAmount: payload.amount, paymentId: payload.paymentId } as any);
    return { ...doc, ...updates };
}

export const Payment_settle = mutation({
  args: {
    docId: v.id("payments"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runPaymentSettle(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Payment_settle", __result);
    }
    return __result;
  },
});

async function __runPaymentStageClientMerge(ctx: MutationCtx, { docId, clientMergeId, clientId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Payment not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Payment not found");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may read payments");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may write payments through commands");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may execute payment commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!(checkRole(user, "adminAccess"))) throw new Error("Guard 1 failed");
    if (!((clientId !== doc.clientId))) throw new Error("Payment already belongs to this client");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      clientMergeAuthorizationId: clientMergeId,
      mergeTargetClientId: clientId,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, paymentId: docId, tenantId: __after.tenantId, _subject: { entity: "Payment", command: "stageClientMerge", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "PaymentClientMergeStaged", entity: "Payment", entityId: docId, payload: { paymentId: docId, tenantId: __after.tenantId }, createdAt: Date.now() });
    // Reactions
    const reactionTarget0 = payload.paymentId;
    if (reactionTarget0) await __runPaymentReassignClient(ctx, { docId: reactionTarget0 } as any);
    return { ...doc, ...updates };
}

export const Payment_stageClientMerge = mutation({
  args: {
    docId: v.id("payments"),
    clientMergeId: v.string(),
    clientId: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runPaymentStageClientMerge(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Payment_stageClientMerge", __result);
    }
    return __result;
  },
});

async function __runPaymentUpdateProviderTransactionIds(ctx: MutationCtx, { docId, providerTransactionIds, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Payment not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Payment not found");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may read payments");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may write payments through commands");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may execute payment commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Guard 1 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      providerTransactionIds: providerTransactionIds,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, paymentId: docId, tenantId: __after.tenantId, _subject: { entity: "Payment", command: "updateProviderTransactionIds", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "PaymentProviderIdsUpdated", entity: "Payment", entityId: docId, payload: { paymentId: docId, tenantId: __after.tenantId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Payment_updateProviderTransactionIds = mutation({
  args: {
    docId: v.id("payments"),
    providerTransactionIds: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runPaymentUpdateProviderTransactionIds(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Payment_updateProviderTransactionIds", __result);
    }
    return __result;
  },
});

async function __runPaymentVerifyReconciliation(ctx: MutationCtx, { docId, notes, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Payment not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Payment not found");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may read payments");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may write payments through commands");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may execute payment commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Guard 1 failed");
    if (!((doc.reconciliationStatus === "matched"))) throw new Error("Guard 2 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      reconciliationStatus: "verified",
      reconciledAt: Date.now(),
      reconciledByUserId: user.id,
      reconciliationDetails: ((notes != null) ? notes : doc.reconciliationDetails),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, paymentId: docId, tenantId: __after.tenantId, externalPaymentId: ((__after.externalPaymentId != null) ? __after.externalPaymentId : ""), amount: __after.amount, _subject: { entity: "Payment", command: "verifyReconciliation", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "PaymentReconciliationVerified", entity: "Payment", entityId: docId, payload: { paymentId: docId, tenantId: __after.tenantId, externalPaymentId: ((__after.externalPaymentId != null) ? __after.externalPaymentId : ""), amount: __after.amount }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Payment_verifyReconciliation = mutation({
  args: {
    docId: v.id("payments"),
    notes: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runPaymentVerifyReconciliation(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Payment_verifyReconciliation", __result);
    }
    return __result;
  },
});

async function __runPaymentMethodClearDefault(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("PaymentMethod not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("PaymentMethod not found");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may read payment methods");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may write payment methods through commands");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may execute payment method commands");
    if (!((doc.registeredAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.status === "active"))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      isDefault: false,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, paymentMethodId: docId, tenantId: __after.tenantId, clientId: __after.clientId, _subject: { entity: "PaymentMethod", command: "clearDefault", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "PaymentMethodDefaultCleared", entity: "PaymentMethod", entityId: docId, payload: { paymentMethodId: docId, tenantId: __after.tenantId, clientId: __after.clientId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const PaymentMethod_clearDefault = mutation({
  args: {
    docId: v.id("paymentMethods"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runPaymentMethodClearDefault(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "PaymentMethod_clearDefault", __result);
    }
    return __result;
  },
});

async function __runPaymentMethodExpire(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("PaymentMethod not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("PaymentMethod not found");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may read payment methods");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may write payment methods through commands");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may execute payment method commands");
    if (!((doc.status === "active"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "expired";
        const __allowed: Record<string, string[]> = { "active": ["expired", "invalid", "fraudulent", "removed"], "expired": ["active"], "invalid": [], "fraudulent": [], "removed": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "expired",
      expiredAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, paymentMethodId: docId, tenantId: __after.tenantId, clientId: __after.clientId, _subject: { entity: "PaymentMethod", command: "expire", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "PaymentMethodExpired", entity: "PaymentMethod", entityId: docId, payload: { paymentMethodId: docId, tenantId: __after.tenantId, clientId: __after.clientId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const PaymentMethod_expire = mutation({
  args: {
    docId: v.id("paymentMethods"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runPaymentMethodExpire(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "PaymentMethod_expire", __result);
    }
    return __result;
  },
});

async function __runPaymentMethodInvalidate(ctx: MutationCtx, { docId, reason, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("PaymentMethod not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("PaymentMethod not found");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may read payment methods");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may write payment methods through commands");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may execute payment method commands");
    if (!((doc.status === "active"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((((reason).trim()).length > 0))) throw new Error("Invalidation reason is required");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "invalid";
        const __allowed: Record<string, string[]> = { "active": ["expired", "invalid", "fraudulent", "removed"], "expired": ["active"], "invalid": [], "fraudulent": [], "removed": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "invalid",
      invalidatedAt: Date.now(),
      notes: reason,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, paymentMethodId: docId, tenantId: __after.tenantId, clientId: __after.clientId, reason: reason, _subject: { entity: "PaymentMethod", command: "invalidate", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "PaymentMethodInvalidated", entity: "PaymentMethod", entityId: docId, payload: { paymentMethodId: docId, tenantId: __after.tenantId, clientId: __after.clientId, reason: reason }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const PaymentMethod_invalidate = mutation({
  args: {
    docId: v.id("paymentMethods"),
    reason: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runPaymentMethodInvalidate(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "PaymentMethod_invalidate", __result);
    }
    return __result;
  },
});

async function __runPaymentMethodMakeDefault(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("PaymentMethod not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("PaymentMethod not found");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may read payment methods");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may write payment methods through commands");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may execute payment method commands");
    if (!((doc.registeredAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.status === "active"))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      isDefault: true,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, paymentMethodId: docId, tenantId: __after.tenantId, clientId: __after.clientId, _subject: { entity: "PaymentMethod", command: "makeDefault", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "PaymentMethodDefaultSet", entity: "PaymentMethod", entityId: docId, payload: { paymentMethodId: docId, tenantId: __after.tenantId, clientId: __after.clientId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const PaymentMethod_makeDefault = mutation({
  args: {
    docId: v.id("paymentMethods"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runPaymentMethodMakeDefault(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "PaymentMethod_makeDefault", __result);
    }
    return __result;
  },
});

async function __runPaymentMethodReactivate(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("PaymentMethod not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("PaymentMethod not found");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may read payment methods");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may write payment methods through commands");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may execute payment method commands");
    if (!((doc.status === "expired"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "active";
        const __allowed: Record<string, string[]> = { "active": ["expired", "invalid", "fraudulent", "removed"], "expired": ["active"], "invalid": [], "fraudulent": [], "removed": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "active",
      expiredAt: null,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, paymentMethodId: docId, tenantId: __after.tenantId, clientId: __after.clientId, _subject: { entity: "PaymentMethod", command: "reactivate", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "PaymentMethodReactivated", entity: "PaymentMethod", entityId: docId, payload: { paymentMethodId: docId, tenantId: __after.tenantId, clientId: __after.clientId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const PaymentMethod_reactivate = mutation({
  args: {
    docId: v.id("paymentMethods"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runPaymentMethodReactivate(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "PaymentMethod_reactivate", __result);
    }
    return __result;
  },
});

async function __runPaymentMethodReassignClient(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("PaymentMethod not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("PaymentMethod not found");
    const __rel_clientMergeAuthorization = await __resolveRelation(ctx, "clientMerges", [__auth.tenantId, doc.clientMergeAuthorizationId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_mergeTargetClient = await __resolveRelation(ctx, "clients", [__auth.tenantId, doc.mergeTargetClientId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).clientMergeAuthorizationId;
      ((doc as any) as any).clientMergeAuthorization = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    {
      const __fk = ((doc as any) as any).mergeTargetClientId;
      ((doc as any) as any).mergeTargetClient = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may read payment methods");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may write payment methods through commands");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may execute payment method commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!(checkRole(user, "adminAccess"))) throw new Error("Guard 1 failed");
    if (!((__rel_clientMergeAuthorization != null))) throw new Error("Guard 2 failed");
    if (!((__rel_clientMergeAuthorization.deletedAt == null))) throw new Error("Guard 3 failed");
    if (!((__rel_clientMergeAuthorization.mergedAt != null))) throw new Error("Guard 4 failed");
    if (!((__rel_mergeTargetClient != null))) throw new Error("Guard 5 failed");
    if (!((__rel_mergeTargetClient.status === "active"))) throw new Error("Guard 6 failed");
    if (!((__rel_mergeTargetClient.deletedAt == null))) throw new Error("Guard 7 failed");
    if (!((__rel_clientMergeAuthorization.duplicateClientId === doc.clientId))) throw new Error("Merge authorization does not match the payment-method client");
    if (!((__rel_clientMergeAuthorization.primaryClientId === doc.mergeTargetClientId))) throw new Error("Merge authorization does not match the primary client");
    const previousClientId = doc.clientId;
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      clientId: doc.mergeTargetClientId,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, paymentMethodId: docId, tenantId: __after.tenantId, previousClientId: previousClientId, clientId: __after.mergeTargetClientId, _subject: { entity: "PaymentMethod", command: "reassignClient", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "PaymentMethodClientReassigned", entity: "PaymentMethod", entityId: docId, payload: { paymentMethodId: docId, tenantId: __after.tenantId, previousClientId: previousClientId, clientId: __after.mergeTargetClientId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const PaymentMethod_reassignClient = mutation({
  args: {
    docId: v.id("paymentMethods"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runPaymentMethodReassignClient(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "PaymentMethod_reassignClient", __result);
    }
    return __result;
  },
});

async function __runPaymentMethodRegister(ctx: MutationCtx, { docId, clientId, methodType, provider, lastFour, isDefault, notes, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("PaymentMethod not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("PaymentMethod not found");
    const __rel_client = await __resolveRelation(ctx, "clients", [__auth.tenantId, doc.clientId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).clientId;
      ((doc as any) as any).client = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may read payment methods");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may write payment methods through commands");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may execute payment method commands");
    if (!((doc.registeredAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.status === "active"))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((__rel_client != null))) throw new Error("Guard 3 failed");
    if (!((clientId === doc.clientId))) throw new Error("Register clientId must match the seeded client reference");
    if (!(((lastFour == null) || (((lastFour).trim()).length <= 4)))) throw new Error("Last-four hint must be at most four characters");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      clientId: clientId,
      methodType: methodType,
      provider: provider,
      lastFour: lastFour,
      isDefault: ((isDefault != null) ? isDefault : false),
      notes: notes,
      registeredAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, paymentMethodId: docId, tenantId: __after.tenantId, clientId: clientId, methodType: methodType, isDefault: ((isDefault != null) ? isDefault : false), _subject: { entity: "PaymentMethod", command: "register", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "PaymentMethodRegistered", entity: "PaymentMethod", entityId: docId, payload: { paymentMethodId: docId, tenantId: __after.tenantId, clientId: clientId, methodType: methodType, isDefault: ((isDefault != null) ? isDefault : false) }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const PaymentMethod_register = mutation({
  args: {
    docId: v.id("paymentMethods"),
    clientId: v.string(),
    methodType: v.any(),
    provider: v.optional(v.string()),
    lastFour: v.optional(v.string()),
    isDefault: v.optional(v.boolean()),
    notes: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runPaymentMethodRegister(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "PaymentMethod_register", __result);
    }
    return __result;
  },
});

export const PaymentMethod_createViaRegister = mutation({
  args: {
    clientId: v.string(),
    methodType: v.any(),
    provider: v.optional(v.string()),
    lastFour: v.optional(v.string()),
    isDefault: v.optional(v.boolean()),
    notes: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { clientId, methodType, provider, lastFour, isDefault, notes } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      isDefault: args.isDefault !== undefined ? args.isDefault : false,
      status: "active",
      createdAt: Date.now(),
      updatedAt: Date.now(),
      clientId: args.clientId,
      lastFour: args.lastFour,
      methodType: args.methodType,
      notes: args.notes,
      provider: args.provider
    };
    const __rel_client = await __resolveRelation(ctx, "clients", [__auth.tenantId, __draft.clientId], ["tenantId","id"], "tenantId", __auth.tenantId);
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may read payment methods");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may write payment methods through commands");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may execute payment method commands");
    if (!((__draft.registeredAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.status === "active"))) throw new Error("Guard 1 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((__rel_client != null))) throw new Error("Guard 3 failed");
    if (!((clientId === __draft.clientId))) throw new Error("Register clientId must match the seeded client reference");
    if (!(((lastFour == null) || (((lastFour).trim()).length <= 4)))) throw new Error("Last-four hint must be at most four characters");
    const doc: Record<string, any> = {
      ...__draft,
      clientId: clientId,
      methodType: methodType,
      provider: provider,
      lastFour: lastFour,
      isDefault: ((isDefault != null) ? isDefault : false),
      notes: notes,
      registeredAt: Date.now(),
      version: 1,
    };
    const docId = await ctx.db.insert("paymentMethods", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, paymentMethodId: docId, tenantId: doc.tenantId, clientId: doc.clientId, methodType: doc.methodType, isDefault: ((doc.isDefault != null) ? doc.isDefault : false), _subject: { entity: "PaymentMethod", command: "register", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "PaymentMethodRegistered", entity: "PaymentMethod", entityId: docId, payload: { paymentMethodId: docId, tenantId: doc.tenantId, clientId: doc.clientId, methodType: doc.methodType, isDefault: ((doc.isDefault != null) ? doc.isDefault : false) }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "PaymentMethod_createViaRegister", __result);
    }
    return __result;
  },
});

async function __runPaymentMethodRemove(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("PaymentMethod not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("PaymentMethod not found");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may read payment methods");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may write payment methods through commands");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may execute payment method commands");
    if (!((((doc.status === "active") || (doc.status === "expired")) || (doc.status === "invalid")))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "removed";
        const __allowed: Record<string, string[]> = { "active": ["expired", "invalid", "fraudulent", "removed"], "expired": ["active"], "invalid": [], "fraudulent": [], "removed": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "removed",
      removedAt: Date.now(),
      isDefault: false,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, paymentMethodId: docId, tenantId: __after.tenantId, clientId: __after.clientId, _subject: { entity: "PaymentMethod", command: "remove", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "PaymentMethodRemoved", entity: "PaymentMethod", entityId: docId, payload: { paymentMethodId: docId, tenantId: __after.tenantId, clientId: __after.clientId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const PaymentMethod_remove = mutation({
  args: {
    docId: v.id("paymentMethods"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runPaymentMethodRemove(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "PaymentMethod_remove", __result);
    }
    return __result;
  },
});

async function __runPaymentMethodStageClientMerge(ctx: MutationCtx, { docId, clientMergeId, clientId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("PaymentMethod not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("PaymentMethod not found");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may read payment methods");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may write payment methods through commands");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may execute payment method commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!(checkRole(user, "adminAccess"))) throw new Error("Guard 1 failed");
    if (!((clientId !== doc.clientId))) throw new Error("Payment method already belongs to this client");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      clientMergeAuthorizationId: clientMergeId,
      mergeTargetClientId: clientId,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, paymentMethodId: docId, tenantId: __after.tenantId, _subject: { entity: "PaymentMethod", command: "stageClientMerge", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "PaymentMethodClientMergeStaged", entity: "PaymentMethod", entityId: docId, payload: { paymentMethodId: docId, tenantId: __after.tenantId }, createdAt: Date.now() });
    // Reactions
    const reactionTarget0 = payload.paymentMethodId;
    if (reactionTarget0) await __runPaymentMethodReassignClient(ctx, { docId: reactionTarget0 } as any);
    return { ...doc, ...updates };
}

export const PaymentMethod_stageClientMerge = mutation({
  args: {
    docId: v.id("paymentMethods"),
    clientMergeId: v.string(),
    clientId: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runPaymentMethodStageClientMerge(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "PaymentMethod_stageClientMerge", __result);
    }
    return __result;
  },
});

async function __runPayrollInputFinalize(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("PayrollInput not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("PayrollInput not found");
    const doc = await __decryptDoc(ctx, "PayrollInput", ["hourlyRate","overtimeRate","grossAmount","notes"], __storedDoc) as Record<string, any>;
    if (!(checkRole(user, "financeManageAccess"))) throw new Error("Finance managers may read payroll inputs");
    if (!(checkRole(user, "financeManageAccess"))) throw new Error("Finance managers may write payroll inputs through commands");
    if (!(checkRole(user, "financeManageAccess"))) throw new Error("Finance managers may execute payroll input commands");
    if (!((doc.status === "prepared"))) throw new Error("Guard 0 failed");
    if (!((doc.preparedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "finalized";
        const __allowed: Record<string, string[]> = { "draft": ["prepared", "voided"], "prepared": ["finalized", "voided"], "finalized": ["voided"], "voided": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "finalized",
      finalizedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "PayrollInput", ["hourlyRate","overtimeRate","grossAmount","notes"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, payrollInputId: docId, tenantId: __after.tenantId, personId: __after.personId, eventId: __after.eventId, periodStart: __after.periodStart, periodEnd: __after.periodEnd, totalMinutes: __after.totalMinutes, status: "finalized", _subject: { entity: "PayrollInput", command: "finalize", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "PayrollInputFinalized", entity: "PayrollInput", entityId: docId, payload: { payrollInputId: docId, tenantId: __after.tenantId, personId: __after.personId, eventId: __after.eventId, periodStart: __after.periodStart, periodEnd: __after.periodEnd, totalMinutes: __after.totalMinutes, status: "finalized" }, createdAt: Date.now() });
    const __ret: Record<string, any> = { ...doc, ...updates };
    delete (__ret as any).hourlyRate; delete (__ret as any).overtimeRate; delete (__ret as any).grossAmount;
    return __ret;
}

export const PayrollInput_finalize = mutation({
  args: {
    docId: v.id("payrollInputs"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runPayrollInputFinalize(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "PayrollInput_finalize", __result);
    }
    return __result;
  },
});

async function __runPayrollInputMarkVoided(ctx: MutationCtx, { docId, reason, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("PayrollInput not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("PayrollInput not found");
    const doc = await __decryptDoc(ctx, "PayrollInput", ["hourlyRate","overtimeRate","grossAmount","notes"], __storedDoc) as Record<string, any>;
    if (!(checkRole(user, "financeManageAccess"))) throw new Error("Finance managers may read payroll inputs");
    if (!(checkRole(user, "financeManageAccess"))) throw new Error("Finance managers may write payroll inputs through commands");
    if (!(checkRole(user, "financeManageAccess"))) throw new Error("Finance managers may execute payroll input commands");
    if (!((((doc.status === "draft") || (doc.status === "prepared")) || (doc.status === "finalized")))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((((reason).trim()).length > 0))) throw new Error("Void reason is required");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "voided";
        const __allowed: Record<string, string[]> = { "draft": ["prepared", "voided"], "prepared": ["finalized", "voided"], "finalized": ["voided"], "voided": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "voided",
      voidedAt: Date.now(),
      voidReason: reason,
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "PayrollInput", ["hourlyRate","overtimeRate","grossAmount","notes"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, payrollInputId: docId, tenantId: __after.tenantId, personId: __after.personId, eventId: __after.eventId, reason: reason, status: "voided", _subject: { entity: "PayrollInput", command: "markVoided", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "PayrollInputVoided", entity: "PayrollInput", entityId: docId, payload: { payrollInputId: docId, tenantId: __after.tenantId, personId: __after.personId, eventId: __after.eventId, reason: reason, status: "voided" }, createdAt: Date.now() });
    const __ret: Record<string, any> = { ...doc, ...updates };
    delete (__ret as any).hourlyRate; delete (__ret as any).overtimeRate; delete (__ret as any).grossAmount;
    return __ret;
}

export const PayrollInput_markVoided = mutation({
  args: {
    docId: v.id("payrollInputs"),
    reason: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runPayrollInputMarkVoided(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "PayrollInput_markVoided", __result);
    }
    return __result;
  },
});

async function __runPayrollInputPrepare(ctx: MutationCtx, { docId, personId, periodStart, periodEnd, regularMinutes, overtimeMinutes, totalMinutes, eventId, shiftId, hourlyRate, overtimeRate, grossAmount, notes, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("PayrollInput not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("PayrollInput not found");
    const doc = await __decryptDoc(ctx, "PayrollInput", ["hourlyRate","overtimeRate","grossAmount","notes"], __storedDoc) as Record<string, any>;
    const __rel_person = await __resolveRelation(ctx, "people", [__auth.tenantId, doc.personId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).personId;
      ((doc as any) as any).person = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!(checkRole(user, "financeManageAccess"))) throw new Error("Finance managers may read payroll inputs");
    if (!(checkRole(user, "financeManageAccess"))) throw new Error("Finance managers may write payroll inputs through commands");
    if (!(checkRole(user, "financeManageAccess"))) throw new Error("Finance managers may execute payroll input commands");
    if (!((doc.status === "draft"))) throw new Error("Guard 0 failed");
    if (!((doc.preparedAt == null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((__rel_person != null))) throw new Error("Guard 3 failed");
    if (!((personId === doc.personId))) throw new Error("Prepare personId must match the seeded person reference");
    if (!((((eventId == null) || (doc.eventId == null)) || (eventId === doc.eventId)))) throw new Error("Prepare eventId must match the seeded event reference when both are set");
    if (!((((shiftId == null) || (doc.shiftId == null)) || (shiftId === doc.shiftId)))) throw new Error("Prepare shiftId must match the seeded shift reference when both are set");
    if (!((periodEnd >= periodStart))) throw new Error("Payroll period end must be at or after period start");
    if (!((((regularMinutes >= 0) && (overtimeMinutes >= 0)) && (totalMinutes >= 0)))) throw new Error("Payroll minutes cannot be negative");
    if (!((totalMinutes === (regularMinutes + overtimeMinutes)))) throw new Error("Total minutes must equal regular plus overtime");
    if (!(((((hourlyRate == null) || (hourlyRate >= 0)) && ((overtimeRate == null) || (overtimeRate >= 0))) && ((grossAmount == null) || (grossAmount >= 0))))) throw new Error("Payroll rate and amount fields cannot be negative");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "prepared";
        const __allowed: Record<string, string[]> = { "draft": ["prepared", "voided"], "prepared": ["finalized", "voided"], "finalized": ["voided"], "voided": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      personId: personId,
      periodStart: periodStart,
      periodEnd: periodEnd,
      eventId: ((eventId != null) ? eventId : doc.eventId),
      shiftId: ((shiftId != null) ? shiftId : doc.shiftId),
      regularMinutes: regularMinutes,
      overtimeMinutes: overtimeMinutes,
      totalMinutes: totalMinutes,
      hourlyRate: hourlyRate,
      overtimeRate: overtimeRate,
      grossAmount: grossAmount,
      notes: notes,
      status: "prepared",
      preparedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "PayrollInput", ["hourlyRate","overtimeRate","grossAmount","notes"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, payrollInputId: docId, tenantId: __after.tenantId, personId: personId, eventId: ((eventId != null) ? eventId : __after.eventId), periodStart: periodStart, periodEnd: periodEnd, totalMinutes: totalMinutes, status: "prepared", _subject: { entity: "PayrollInput", command: "prepare", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "PayrollInputPrepared", entity: "PayrollInput", entityId: docId, payload: { payrollInputId: docId, tenantId: __after.tenantId, personId: personId, eventId: ((eventId != null) ? eventId : __after.eventId), periodStart: periodStart, periodEnd: periodEnd, totalMinutes: totalMinutes, status: "prepared" }, createdAt: Date.now() });
    const __ret: Record<string, any> = { ...doc, ...updates };
    delete (__ret as any).hourlyRate; delete (__ret as any).overtimeRate; delete (__ret as any).grossAmount;
    return __ret;
}

export const PayrollInput_prepare = mutation({
  args: {
    docId: v.id("payrollInputs"),
    personId: v.string(),
    periodStart: v.number(),
    periodEnd: v.number(),
    regularMinutes: v.any(),
    overtimeMinutes: v.any(),
    totalMinutes: v.any(),
    eventId: v.optional(v.string()),
    shiftId: v.optional(v.string()),
    hourlyRate: v.optional(v.number()),
    overtimeRate: v.optional(v.number()),
    grossAmount: v.optional(v.number()),
    notes: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runPayrollInputPrepare(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "PayrollInput_prepare", __result);
    }
    return __result;
  },
});

export const PayrollInput_createViaPrepare = mutation({
  args: {
    personId: v.string(),
    periodStart: v.number(),
    periodEnd: v.number(),
    regularMinutes: v.any(),
    overtimeMinutes: v.any(),
    totalMinutes: v.any(),
    eventId: v.optional(v.string()),
    shiftId: v.optional(v.string()),
    hourlyRate: v.optional(v.number()),
    overtimeRate: v.optional(v.number()),
    grossAmount: v.optional(v.number()),
    notes: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { personId, periodStart, periodEnd, regularMinutes, overtimeMinutes, totalMinutes, eventId, shiftId, hourlyRate, overtimeRate, grossAmount, notes } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      createdAt: Date.now(),
      updatedAt: Date.now(),
      status: "draft",
      eventId: args.eventId,
      grossAmount: args.grossAmount,
      hourlyRate: args.hourlyRate,
      notes: args.notes,
      overtimeMinutes: args.overtimeMinutes,
      overtimeRate: args.overtimeRate,
      periodEnd: args.periodEnd,
      periodStart: args.periodStart,
      personId: args.personId,
      regularMinutes: args.regularMinutes,
      shiftId: args.shiftId,
      totalMinutes: args.totalMinutes
    };
    const __rel_person = await __resolveRelation(ctx, "people", [__auth.tenantId, __draft.personId], ["tenantId","id"], "tenantId", __auth.tenantId);
    if (!(checkRole(user, "financeManageAccess"))) throw new Error("Finance managers may read payroll inputs");
    if (!(checkRole(user, "financeManageAccess"))) throw new Error("Finance managers may write payroll inputs through commands");
    if (!(checkRole(user, "financeManageAccess"))) throw new Error("Finance managers may execute payroll input commands");
    if (!((__draft.status === "draft"))) throw new Error("Guard 0 failed");
    if (!((__draft.preparedAt == null))) throw new Error("Guard 1 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((__rel_person != null))) throw new Error("Guard 3 failed");
    if (!((personId === __draft.personId))) throw new Error("Prepare personId must match the seeded person reference");
    if (!((((eventId == null) || (__draft.eventId == null)) || (eventId === __draft.eventId)))) throw new Error("Prepare eventId must match the seeded event reference when both are set");
    if (!((((shiftId == null) || (__draft.shiftId == null)) || (shiftId === __draft.shiftId)))) throw new Error("Prepare shiftId must match the seeded shift reference when both are set");
    if (!((periodEnd >= periodStart))) throw new Error("Payroll period end must be at or after period start");
    if (!((((regularMinutes >= 0) && (overtimeMinutes >= 0)) && (totalMinutes >= 0)))) throw new Error("Payroll minutes cannot be negative");
    if (!((totalMinutes === (regularMinutes + overtimeMinutes)))) throw new Error("Total minutes must equal regular plus overtime");
    if (!(((((hourlyRate == null) || (hourlyRate >= 0)) && ((overtimeRate == null) || (overtimeRate >= 0))) && ((grossAmount == null) || (grossAmount >= 0))))) throw new Error("Payroll rate and amount fields cannot be negative");
    const doc: Record<string, any> = {
      ...__draft,
      personId: personId,
      periodStart: periodStart,
      periodEnd: periodEnd,
      eventId: ((eventId != null) ? eventId : __draft.eventId),
      shiftId: ((shiftId != null) ? shiftId : __draft.shiftId),
      regularMinutes: regularMinutes,
      overtimeMinutes: overtimeMinutes,
      totalMinutes: totalMinutes,
      hourlyRate: hourlyRate,
      overtimeRate: overtimeRate,
      grossAmount: grossAmount,
      notes: notes,
      status: "prepared",
      preparedAt: Date.now(),
      version: 1,
    };
    const __storedDoc = await __encryptDoc(ctx, "PayrollInput", ["hourlyRate","overtimeRate","grossAmount","notes"], doc);
    const docId = await ctx.db.insert("payrollInputs", __storedDoc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, payrollInputId: docId, tenantId: doc.tenantId, personId: doc.personId, eventId: ((doc.eventId != null) ? doc.eventId : doc.eventId), periodStart: doc.periodStart, periodEnd: doc.periodEnd, totalMinutes: doc.totalMinutes, status: "prepared", _subject: { entity: "PayrollInput", command: "prepare", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "PayrollInputPrepared", entity: "PayrollInput", entityId: docId, payload: { payrollInputId: docId, tenantId: doc.tenantId, personId: doc.personId, eventId: ((doc.eventId != null) ? doc.eventId : doc.eventId), periodStart: doc.periodStart, periodEnd: doc.periodEnd, totalMinutes: doc.totalMinutes, status: "prepared" }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "PayrollInput_createViaPrepare", __result);
    }
    return __result;
  },
});

async function __runPerformanceReviewRecord(ctx: MutationCtx, { docId, personId, reviewerId, eventId, reviewDate, reliabilityRating, qualityRating, teamworkRating, notes, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("PerformanceReview not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("PerformanceReview not found");
    const doc = await __decryptDoc(ctx, "PerformanceReview", ["notes"], __storedDoc) as Record<string, any>;
    const __rel_person = await __resolveRelation(ctx, "people", [__auth.tenantId, doc.personId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).personId;
      ((doc as any) as any).person = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!(checkRole(user, "manageAccess"))) throw new Error("Managers may read performance reviews");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Workforce managers may write performance reviews through commands");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Workforce managers may execute performance review commands");
    if (!((doc.recordedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(((__rel_person != null) && (__rel_person.status === "active")))) throw new Error("Guard 2 failed");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Guard 3 failed");
    if (!((personId === doc.personId))) throw new Error("Review person must match the selected staff member");
    if (!(((reliabilityRating >= 1) && (reliabilityRating <= 5)))) throw new Error("Reliability rating must be between 1 and 5");
    if (!(((qualityRating >= 1) && (qualityRating <= 5)))) throw new Error("Quality rating must be between 1 and 5");
    if (!(((teamworkRating >= 1) && (teamworkRating <= 5)))) throw new Error("Teamwork rating must be between 1 and 5");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      personId: personId,
      reviewerId: reviewerId,
      eventId: eventId,
      reviewDate: reviewDate,
      reliabilityRating: reliabilityRating,
      qualityRating: qualityRating,
      teamworkRating: teamworkRating,
      notes: notes,
      recordedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "PerformanceReview", ["notes"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, performanceReviewId: docId, tenantId: __after.tenantId, personId: personId, reviewerId: reviewerId, eventId: eventId, reviewDate: reviewDate, reliabilityRating: reliabilityRating, qualityRating: qualityRating, teamworkRating: teamworkRating, _subject: { entity: "PerformanceReview", command: "record", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "PerformanceReviewRecorded", entity: "PerformanceReview", entityId: docId, payload: { performanceReviewId: docId, tenantId: __after.tenantId, personId: personId, reviewerId: reviewerId, eventId: eventId, reviewDate: reviewDate, reliabilityRating: reliabilityRating, qualityRating: qualityRating, teamworkRating: teamworkRating }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const PerformanceReview_record = mutation({
  args: {
    docId: v.id("performanceReviews"),
    personId: v.string(),
    reviewerId: v.string(),
    eventId: v.optional(v.string()),
    reviewDate: v.number(),
    reliabilityRating: v.number(),
    qualityRating: v.number(),
    teamworkRating: v.number(),
    notes: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runPerformanceReviewRecord(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "PerformanceReview_record", __result);
    }
    return __result;
  },
});

export const PerformanceReview_createViaRecord = mutation({
  args: {
    personId: v.string(),
    reviewerId: v.string(),
    eventId: v.optional(v.string()),
    reviewDate: v.number(),
    reliabilityRating: v.number(),
    qualityRating: v.number(),
    teamworkRating: v.number(),
    notes: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { personId, reviewerId, eventId, reviewDate, reliabilityRating, qualityRating, teamworkRating, notes } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      createdAt: Date.now(),
      updatedAt: Date.now(),
      eventId: args.eventId,
      notes: args.notes,
      personId: args.personId,
      qualityRating: args.qualityRating,
      reliabilityRating: args.reliabilityRating,
      reviewDate: args.reviewDate,
      reviewerId: args.reviewerId,
      teamworkRating: args.teamworkRating
    };
    const __rel_person = await __resolveRelation(ctx, "people", [__auth.tenantId, __draft.personId], ["tenantId","id"], "tenantId", __auth.tenantId);
    if (!(checkRole(user, "manageAccess"))) throw new Error("Managers may read performance reviews");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Workforce managers may write performance reviews through commands");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Workforce managers may execute performance review commands");
    if (!((__draft.recordedAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(((__rel_person != null) && (__rel_person.status === "active")))) throw new Error("Guard 2 failed");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Guard 3 failed");
    if (!((personId === __draft.personId))) throw new Error("Review person must match the selected staff member");
    if (!(((reliabilityRating >= 1) && (reliabilityRating <= 5)))) throw new Error("Reliability rating must be between 1 and 5");
    if (!(((qualityRating >= 1) && (qualityRating <= 5)))) throw new Error("Quality rating must be between 1 and 5");
    if (!(((teamworkRating >= 1) && (teamworkRating <= 5)))) throw new Error("Teamwork rating must be between 1 and 5");
    const doc: Record<string, any> = {
      ...__draft,
      personId: personId,
      reviewerId: reviewerId,
      eventId: eventId,
      reviewDate: reviewDate,
      reliabilityRating: reliabilityRating,
      qualityRating: qualityRating,
      teamworkRating: teamworkRating,
      notes: notes,
      recordedAt: Date.now(),
      version: 1,
    };
    const __storedDoc = await __encryptDoc(ctx, "PerformanceReview", ["notes"], doc);
    const docId = await ctx.db.insert("performanceReviews", __storedDoc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, performanceReviewId: docId, tenantId: doc.tenantId, personId: doc.personId, reviewerId: doc.reviewerId, eventId: doc.eventId, reviewDate: doc.reviewDate, reliabilityRating: doc.reliabilityRating, qualityRating: doc.qualityRating, teamworkRating: doc.teamworkRating, _subject: { entity: "PerformanceReview", command: "record", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "PerformanceReviewRecorded", entity: "PerformanceReview", entityId: docId, payload: { performanceReviewId: docId, tenantId: doc.tenantId, personId: doc.personId, reviewerId: doc.reviewerId, eventId: doc.eventId, reviewDate: doc.reviewDate, reliabilityRating: doc.reliabilityRating, qualityRating: doc.qualityRating, teamworkRating: doc.teamworkRating }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "PerformanceReview_createViaRecord", __result);
    }
    return __result;
  },
});

async function __runPersonAssignRole(ctx: MutationCtx, { docId, role, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("Person not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("Person not found");
    const doc = await __decryptDoc(ctx, "Person", ["email","phone"], __storedDoc) as Record<string, any>;
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may read people");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Workforce managers may write people");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Workforce managers may execute people commands");
    if (!((doc.status === "active"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(checkRole(user, "adminAccess"))) throw new Error("Guard 2 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      role: role,
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "Person", ["email","phone"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, personId: docId, tenantId: __after.tenantId, role: role, _subject: { entity: "Person", command: "assignRole", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "PersonRoleAssigned", entity: "Person", entityId: docId, payload: { personId: docId, tenantId: __after.tenantId, role: role }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Person_assignRole = mutation({
  args: {
    docId: v.id("people"),
    role: v.any(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runPersonAssignRole(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Person_assignRole", __result);
    }
    return __result;
  },
});

async function __runPersonCorrectIdentity(ctx: MutationCtx, { docId, givenName, familyName, phone, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("Person not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("Person not found");
    const doc = await __decryptDoc(ctx, "Person", ["email","phone"], __storedDoc) as Record<string, any>;
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may read people");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Workforce managers may write people");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Workforce managers may execute people commands");
    if (!((doc.status === "active"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((((givenName).trim()).length > 0))) throw new Error("First name is required");
    if (!((((familyName).trim()).length > 0))) throw new Error("Last name is required");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      givenName: givenName,
      familyName: familyName,
      phone: phone,
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "Person", ["email","phone"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, personId: docId, tenantId: __after.tenantId, givenName: givenName, familyName: familyName, _subject: { entity: "Person", command: "correctIdentity", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "PersonIdentityCorrected", entity: "Person", entityId: docId, payload: { personId: docId, tenantId: __after.tenantId, givenName: givenName, familyName: familyName }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Person_correctIdentity = mutation({
  args: {
    docId: v.id("people"),
    givenName: v.string(),
    familyName: v.string(),
    phone: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runPersonCorrectIdentity(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Person_correctIdentity", __result);
    }
    return __result;
  },
});

async function __runPersonDeactivate(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("Person not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("Person not found");
    const doc = await __decryptDoc(ctx, "Person", ["email","phone"], __storedDoc) as Record<string, any>;
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may read people");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Workforce managers may write people");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Workforce managers may execute people commands");
    if (!((doc.status === "active"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "inactive";
        const __allowed: Record<string, string[]> = { "active": ["inactive", "terminated"], "inactive": ["active", "terminated"], "terminated": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "inactive",
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "Person", ["email","phone"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, personId: docId, tenantId: __after.tenantId, _subject: { entity: "Person", command: "deactivate", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "PersonDeactivated", entity: "Person", entityId: docId, payload: { personId: docId, tenantId: __after.tenantId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Person_deactivate = mutation({
  args: {
    docId: v.id("people"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runPersonDeactivate(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Person_deactivate", __result);
    }
    return __result;
  },
});

async function __runPersonHire(ctx: MutationCtx, { docId, givenName, familyName, email, phone, role, employmentType, employeeNumber, authSubjectId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("Person not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("Person not found");
    const doc = await __decryptDoc(ctx, "Person", ["email","phone"], __storedDoc) as Record<string, any>;
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may read people");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Workforce managers may write people");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Workforce managers may execute people commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!((((givenName).trim()).length > 0))) throw new Error("First name is required");
    if (!((((familyName).trim()).length > 0))) throw new Error("Last name is required");
    if (!((((email).trim()).length > 0))) throw new Error("Email is required");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      givenName: givenName,
      familyName: familyName,
      email: email,
      phone: phone,
      role: ((role != null) ? role : "staff"),
      employmentType: ((employmentType != null) ? employmentType : "full_time"),
      employeeNumber: employeeNumber,
      authSubjectId: authSubjectId,
      hireDate: Date.now(),
      terminationDate: null,
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "Person", ["email","phone"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, personId: docId, tenantId: __after.tenantId, email: email, role: __after.role, _subject: { entity: "Person", command: "hire", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "PersonHired", entity: "Person", entityId: docId, payload: { personId: docId, tenantId: __after.tenantId, email: email, role: __after.role }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Person_hire = mutation({
  args: {
    docId: v.id("people"),
    givenName: v.string(),
    familyName: v.string(),
    email: v.string(),
    phone: v.optional(v.string()),
    role: v.optional(v.any()),
    employmentType: v.optional(v.any()),
    employeeNumber: v.optional(v.string()),
    authSubjectId: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runPersonHire(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Person_hire", __result);
    }
    return __result;
  },
});

export const Person_createViaHire = mutation({
  args: {
    givenName: v.string(),
    familyName: v.string(),
    email: v.string(),
    phone: v.optional(v.string()),
    role: v.optional(v.any()),
    employmentType: v.optional(v.any()),
    employeeNumber: v.optional(v.string()),
    authSubjectId: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { givenName, familyName, email, phone, role, employmentType, employeeNumber, authSubjectId } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      role: args.role !== undefined ? args.role : "staff",
      employmentType: args.employmentType !== undefined ? args.employmentType : "full_time",
      status: "active",
      createdAt: Date.now(),
      updatedAt: Date.now(),
      authSubjectId: args.authSubjectId,
      email: args.email,
      employeeNumber: args.employeeNumber,
      familyName: args.familyName,
      givenName: args.givenName,
      phone: args.phone
    };
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may read people");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Workforce managers may write people");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Workforce managers may execute people commands");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!((((givenName).trim()).length > 0))) throw new Error("First name is required");
    if (!((((familyName).trim()).length > 0))) throw new Error("Last name is required");
    if (!((((email).trim()).length > 0))) throw new Error("Email is required");
    const doc: Record<string, any> = {
      ...__draft,
      givenName: givenName,
      familyName: familyName,
      email: email,
      phone: phone,
      role: ((role != null) ? role : "staff"),
      employmentType: ((employmentType != null) ? employmentType : "full_time"),
      employeeNumber: employeeNumber,
      authSubjectId: authSubjectId,
      hireDate: Date.now(),
      terminationDate: null,
      version: 1,
    };
    const __storedDoc = await __encryptDoc(ctx, "Person", ["email","phone"], doc);
    const docId = await ctx.db.insert("people", __storedDoc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, personId: docId, tenantId: doc.tenantId, email: doc.email, role: doc.role, _subject: { entity: "Person", command: "hire", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "PersonHired", entity: "Person", entityId: docId, payload: { personId: docId, tenantId: doc.tenantId, email: doc.email, role: doc.role }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Person_createViaHire", __result);
    }
    return __result;
  },
});

async function __runPersonLinkAccount(ctx: MutationCtx, { docId, authSubjectId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("Person not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("Person not found");
    const doc = await __decryptDoc(ctx, "Person", ["email","phone"], __storedDoc) as Record<string, any>;
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may read people");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Workforce managers may write people");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Workforce managers may execute people commands");
    if (!((doc.status === "active"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Guard 2 failed");
    if (!((checkRole(user, "adminAccess") || (!checkRole(doc.role, "adminAccess"))))) throw new Error("Guard 3 failed");
    if (!((((authSubjectId).trim()).length > 0))) throw new Error("Choose an account to link");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      authSubjectId: authSubjectId,
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "Person", ["email","phone"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, personId: docId, tenantId: __after.tenantId, authSubjectId: authSubjectId, _subject: { entity: "Person", command: "linkAccount", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "PersonAccountLinked", entity: "Person", entityId: docId, payload: { personId: docId, tenantId: __after.tenantId, authSubjectId: authSubjectId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Person_linkAccount = mutation({
  args: {
    docId: v.id("people"),
    authSubjectId: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runPersonLinkAccount(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Person_linkAccount", __result);
    }
    return __result;
  },
});

async function __runPersonReactivate(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("Person not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("Person not found");
    const doc = await __decryptDoc(ctx, "Person", ["email","phone"], __storedDoc) as Record<string, any>;
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may read people");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Workforce managers may write people");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Workforce managers may execute people commands");
    if (!((doc.status === "inactive"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "active";
        const __allowed: Record<string, string[]> = { "active": ["inactive", "terminated"], "inactive": ["active", "terminated"], "terminated": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "active",
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "Person", ["email","phone"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, personId: docId, tenantId: __after.tenantId, _subject: { entity: "Person", command: "reactivate", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "PersonReactivated", entity: "Person", entityId: docId, payload: { personId: docId, tenantId: __after.tenantId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Person_reactivate = mutation({
  args: {
    docId: v.id("people"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runPersonReactivate(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Person_reactivate", __result);
    }
    return __result;
  },
});

async function __runPersonSetSmsAlerts(ctx: MutationCtx, { docId, optIn, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("Person not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("Person not found");
    const doc = await __decryptDoc(ctx, "Person", ["email","phone"], __storedDoc) as Record<string, any>;
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may read people");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Workforce managers may write people");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Workforce managers may execute people commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      smsAlertsOptIn: optIn,
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "Person", ["email","phone"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, personId: docId, tenantId: __after.tenantId, optIn: optIn, _subject: { entity: "Person", command: "setSmsAlerts", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "PersonSmsAlertsChanged", entity: "Person", entityId: docId, payload: { personId: docId, tenantId: __after.tenantId, optIn: optIn }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Person_setSmsAlerts = mutation({
  args: {
    docId: v.id("people"),
    optIn: v.boolean(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runPersonSetSmsAlerts(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Person_setSmsAlerts", __result);
    }
    return __result;
  },
});

async function __runPersonTerminate(ctx: MutationCtx, { docId, reason, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("Person not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("Person not found");
    const doc = await __decryptDoc(ctx, "Person", ["email","phone"], __storedDoc) as Record<string, any>;
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may read people");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Workforce managers may write people");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Workforce managers may execute people commands");
    if (!(((doc.status === "active") || (doc.status === "inactive")))) throw new Error("Guard 0 failed");
    if (!(checkRole(user, "adminAccess"))) throw new Error("Guard 1 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "terminated";
        const __allowed: Record<string, string[]> = { "active": ["inactive", "terminated"], "inactive": ["active", "terminated"], "terminated": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "terminated",
      terminationDate: Date.now(),
      deletedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "Person", ["email","phone"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, personId: docId, tenantId: __after.tenantId, reason: reason, _subject: { entity: "Person", command: "terminate", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "PersonTerminated", entity: "Person", entityId: docId, payload: { personId: docId, tenantId: __after.tenantId, reason: reason }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Person_terminate = mutation({
  args: {
    docId: v.id("people"),
    reason: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runPersonTerminate(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Person_terminate", __result);
    }
    return __result;
  },
});

async function __runPersonUnlinkAccount(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("Person not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("Person not found");
    const doc = await __decryptDoc(ctx, "Person", ["email","phone"], __storedDoc) as Record<string, any>;
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may read people");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Workforce managers may write people");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Workforce managers may execute people commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Guard 1 failed");
    if (!((checkRole(user, "adminAccess") || (!checkRole(doc.role, "adminAccess"))))) throw new Error("Guard 2 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      authSubjectId: null,
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "Person", ["email","phone"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, personId: docId, tenantId: __after.tenantId, _subject: { entity: "Person", command: "unlinkAccount", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "PersonAccountUnlinked", entity: "Person", entityId: docId, payload: { personId: docId, tenantId: __after.tenantId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Person_unlinkAccount = mutation({
  args: {
    docId: v.id("people"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runPersonUnlinkAccount(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Person_unlinkAccount", __result);
    }
    return __result;
  },
});

async function __runPrepTaskAssign(ctx: MutationCtx, { docId, personId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("PrepTask not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("PrepTask not found");
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) throw new Error("Kitchen and event managers may read prep tasks");
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) throw new Error("Kitchen and event managers may write prep tasks through commands");
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) throw new Error("Kitchen and event managers may execute prep task commands");
    if (!(((doc.status === "pending") || (doc.status === "claimed")))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((personId != null))) throw new Error("Guard 2 failed");
    if (!((doc.quantity > 0))) throw new Error("Prep quantity must be positive before assign");
    const previousStatus = doc.status;
    const previousAssignee = doc.assignedToId;
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "claimed";
        const __allowed: Record<string, string[]> = { "pending": ["claimed", "blocked", "cancelled"], "claimed": ["in_progress", "pending", "blocked", "cancelled"], "in_progress": ["completed", "blocked", "cancelled"], "blocked": ["pending", "cancelled"], "completed": [], "cancelled": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      assignedToId: personId,
      status: "claimed",
      claimedAt: ((doc.claimedAt != null) ? doc.claimedAt : Date.now()),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, prepTaskId: docId, tenantId: __after.tenantId, eventDishId: __after.eventDishId, eventId: __after.eventId, name: __after.name, assignedToId: personId, previousAssignedToId: previousAssignee, quantity: __after.quantity, unit: __after.unit, previousStatus: previousStatus, status: "claimed", _subject: { entity: "PrepTask", command: "assign", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "PrepTaskAssigned", entity: "PrepTask", entityId: docId, payload: { prepTaskId: docId, tenantId: __after.tenantId, eventDishId: __after.eventDishId, eventId: __after.eventId, name: __after.name, assignedToId: personId, previousAssignedToId: previousAssignee, quantity: __after.quantity, unit: __after.unit, previousStatus: previousStatus, status: "claimed" }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const PrepTask_assign = mutation({
  args: {
    docId: v.id("prepTasks"),
    personId: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runPrepTaskAssign(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "PrepTask_assign", __result);
    }
    return __result;
  },
});

async function __runPrepTaskCancel(ctx: MutationCtx, { docId, reason, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("PrepTask not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("PrepTask not found");
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) throw new Error("Kitchen and event managers may read prep tasks");
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) throw new Error("Kitchen and event managers may write prep tasks through commands");
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) throw new Error("Kitchen and event managers may execute prep task commands");
    if (!(((((doc.status === "pending") || (doc.status === "claimed")) || (doc.status === "in_progress")) || (doc.status === "blocked")))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(checkRole(user, "kitchenLeadAccess"))) throw new Error("Guard 2 failed");
    if (!((((reason).trim()).length > 0))) throw new Error("Cancellation reason is required");
    const previousStatus = doc.status;
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "cancelled";
        const __allowed: Record<string, string[]> = { "pending": ["claimed", "blocked", "cancelled"], "claimed": ["in_progress", "pending", "blocked", "cancelled"], "in_progress": ["completed", "blocked", "cancelled"], "blocked": ["pending", "cancelled"], "completed": [], "cancelled": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "cancelled",
      cancelledAt: Date.now(),
      cancellationReason: reason,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, prepTaskId: docId, tenantId: __after.tenantId, eventDishId: __after.eventDishId, eventId: __after.eventId, name: __after.name, quantity: __after.quantity, unit: __after.unit, previousStatus: previousStatus, status: "cancelled", reason: reason, _subject: { entity: "PrepTask", command: "cancel", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "PrepTaskCancelled", entity: "PrepTask", entityId: docId, payload: { prepTaskId: docId, tenantId: __after.tenantId, eventDishId: __after.eventDishId, eventId: __after.eventId, name: __after.name, quantity: __after.quantity, unit: __after.unit, previousStatus: previousStatus, status: "cancelled", reason: reason }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const PrepTask_cancel = mutation({
  args: {
    docId: v.id("prepTasks"),
    reason: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runPrepTaskCancel(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "PrepTask_cancel", __result);
    }
    return __result;
  },
});

async function __runPrepTaskClaim(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("PrepTask not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("PrepTask not found");
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) throw new Error("Kitchen and event managers may read prep tasks");
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) throw new Error("Kitchen and event managers may write prep tasks through commands");
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) throw new Error("Kitchen and event managers may execute prep task commands");
    if (!((doc.status === "pending"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((user.personId != null))) throw new Error("Guard 2 failed");
    if (!((doc.quantity > 0))) throw new Error("Prep quantity must be positive before claim");
    const previousStatus = doc.status;
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "claimed";
        const __allowed: Record<string, string[]> = { "pending": ["claimed", "blocked", "cancelled"], "claimed": ["in_progress", "pending", "blocked", "cancelled"], "in_progress": ["completed", "blocked", "cancelled"], "blocked": ["pending", "cancelled"], "completed": [], "cancelled": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      assignedToId: user.personId,
      status: "claimed",
      claimedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, prepTaskId: docId, tenantId: __after.tenantId, eventDishId: __after.eventDishId, eventId: __after.eventId, name: __after.name, assignedToId: user.personId, quantity: __after.quantity, unit: __after.unit, previousStatus: previousStatus, status: "claimed", _subject: { entity: "PrepTask", command: "claim", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "PrepTaskClaimed", entity: "PrepTask", entityId: docId, payload: { prepTaskId: docId, tenantId: __after.tenantId, eventDishId: __after.eventDishId, eventId: __after.eventId, name: __after.name, assignedToId: user.personId, quantity: __after.quantity, unit: __after.unit, previousStatus: previousStatus, status: "claimed" }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const PrepTask_claim = mutation({
  args: {
    docId: v.id("prepTasks"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runPrepTaskClaim(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "PrepTask_claim", __result);
    }
    return __result;
  },
});

async function __runPrepTaskComplete(ctx: MutationCtx, { docId, completedQuantity, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("PrepTask not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("PrepTask not found");
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) throw new Error("Kitchen and event managers may read prep tasks");
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) throw new Error("Kitchen and event managers may write prep tasks through commands");
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) throw new Error("Kitchen and event managers may execute prep task commands");
    if (!((doc.status === "in_progress"))) throw new Error("Guard 0 failed");
    if (!((doc.startedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!(((completedQuantity == null) || (completedQuantity >= 0)))) throw new Error("Completed quantity cannot be negative");
    const previousStatus = doc.status;
    const nextCompleted = ((completedQuantity != null) ? completedQuantity : doc.quantity);
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "completed";
        const __allowed: Record<string, string[]> = { "pending": ["claimed", "blocked", "cancelled"], "claimed": ["in_progress", "pending", "blocked", "cancelled"], "in_progress": ["completed", "blocked", "cancelled"], "blocked": ["pending", "cancelled"], "completed": [], "cancelled": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      completedQuantity: nextCompleted,
      status: "completed",
      completedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, prepTaskId: docId, tenantId: __after.tenantId, eventDishId: __after.eventDishId, eventId: __after.eventId, name: __after.name, assignedToId: __after.assignedToId, quantity: __after.quantity, completedQuantity: nextCompleted, unit: __after.unit, previousStatus: previousStatus, status: "completed", _subject: { entity: "PrepTask", command: "complete", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "PrepTaskCompleted", entity: "PrepTask", entityId: docId, payload: { prepTaskId: docId, tenantId: __after.tenantId, eventDishId: __after.eventDishId, eventId: __after.eventId, name: __after.name, assignedToId: __after.assignedToId, quantity: __after.quantity, completedQuantity: nextCompleted, unit: __after.unit, previousStatus: previousStatus, status: "completed" }, createdAt: Date.now() });
    // Reactions
    const fanRows0 = (await ctx.db.query("prepTaskDependencies").withIndex("by_predecessorTaskId", (q) => q.eq("predecessorTaskId", payload.prepTaskId)).collect()).filter((d) => (d as any).deletedAt == null);
    for (const __row of fanRows0) {
      const target = __row;
      await __runPrepTaskDependencySatisfy(ctx, { docId: (__row as any)._id } as any);
    }
    return { ...doc, ...updates };
}

export const PrepTask_complete = mutation({
  args: {
    docId: v.id("prepTasks"),
    completedQuantity: v.optional(v.number()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runPrepTaskComplete(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "PrepTask_complete", __result);
    }
    return __result;
  },
});

async function __runPrepTaskMarkBlocked(ctx: MutationCtx, { docId, reason, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("PrepTask not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("PrepTask not found");
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) throw new Error("Kitchen and event managers may read prep tasks");
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) throw new Error("Kitchen and event managers may write prep tasks through commands");
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) throw new Error("Kitchen and event managers may execute prep task commands");
    if (!((((doc.status === "pending") || (doc.status === "claimed")) || (doc.status === "in_progress")))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(checkRole(user, "kitchenLeadAccess"))) throw new Error("Guard 2 failed");
    if (!((((reason).trim()).length > 0))) throw new Error("Block reason is required");
    const previousStatus = doc.status;
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "blocked";
        const __allowed: Record<string, string[]> = { "pending": ["claimed", "blocked", "cancelled"], "claimed": ["in_progress", "pending", "blocked", "cancelled"], "in_progress": ["completed", "blocked", "cancelled"], "blocked": ["pending", "cancelled"], "completed": [], "cancelled": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "blocked",
      blockedAt: Date.now(),
      blockReason: reason,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, prepTaskId: docId, tenantId: __after.tenantId, eventDishId: __after.eventDishId, eventId: __after.eventId, name: __after.name, quantity: __after.quantity, unit: __after.unit, previousStatus: previousStatus, status: "blocked", reason: reason, _subject: { entity: "PrepTask", command: "markBlocked", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "PrepTaskBlocked", entity: "PrepTask", entityId: docId, payload: { prepTaskId: docId, tenantId: __after.tenantId, eventDishId: __after.eventDishId, eventId: __after.eventId, name: __after.name, quantity: __after.quantity, unit: __after.unit, previousStatus: previousStatus, status: "blocked", reason: reason }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const PrepTask_markBlocked = mutation({
  args: {
    docId: v.id("prepTasks"),
    reason: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runPrepTaskMarkBlocked(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "PrepTask_markBlocked", __result);
    }
    return __result;
  },
});

async function __runPrepTaskOpen(ctx: MutationCtx, { docId, eventDishId, eventId, name, quantity, unit, ingredientId, ingredientDemandId, componentId, dishTaskId, dishId, category, taskType, specialInstructions, isGenerated, station, dueAt, notes, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("PrepTask not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("PrepTask not found");
    const __rel_eventDish = await __resolveRelation(ctx, "eventDishes", [__auth.tenantId, doc.eventDishId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).eventDishId;
      ((doc as any) as any).eventDish = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) throw new Error("Kitchen and event managers may read prep tasks");
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) throw new Error("Kitchen and event managers may write prep tasks through commands");
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) throw new Error("Kitchen and event managers may execute prep task commands");
    if (!((doc.claimedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.status === "pending"))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((doc.eventDishId != null))) throw new Error("Guard 3 failed");
    if (!((doc.eventId != null))) throw new Error("Guard 4 failed");
    if (!((eventDishId === doc.eventDishId))) throw new Error("Open eventDishId must match the seeded event dish reference");
    if (!((eventId === doc.eventId))) throw new Error("Open eventId must match the seeded event reference");
    if (!(((__rel_eventDish == null) || (eventId === __rel_eventDish.eventId)))) throw new Error("PrepTask eventId must match EventDish.eventId");
    if (!((((name).trim()).length > 0))) throw new Error("Prep task name is required");
    if (!((quantity > 0))) throw new Error("Prep quantity must be positive");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      eventDishId: eventDishId,
      eventId: eventId,
      name: name,
      ingredientId: ((ingredientId != null) ? ingredientId : doc.ingredientId),
      ingredientDemandId: ((ingredientDemandId != null) ? ingredientDemandId : doc.ingredientDemandId),
      componentId: ((componentId != null) ? componentId : doc.componentId),
      dishTaskId: ((dishTaskId != null) ? dishTaskId : doc.dishTaskId),
      dishId: ((dishId != null) ? dishId : doc.dishId),
      category: ((category != null) ? category : doc.category),
      taskType: ((taskType != null) ? taskType : doc.taskType),
      specialInstructions: ((specialInstructions != null) ? specialInstructions : doc.specialInstructions),
      isGenerated: ((isGenerated != null) ? isGenerated : doc.isGenerated),
      quantity: quantity,
      unit: unit,
      station: ((station != null) ? station : doc.station),
      dueAt: ((dueAt != null) ? dueAt : doc.dueAt),
      notes: ((notes != null) ? notes : doc.notes),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, prepTaskId: docId, tenantId: __after.tenantId, eventDishId: eventDishId, eventId: eventId, name: name, ingredientId: ((ingredientId != null) ? ingredientId : __after.ingredientId), ingredientDemandId: ((ingredientDemandId != null) ? ingredientDemandId : __after.ingredientDemandId), componentId: ((componentId != null) ? componentId : __after.componentId), dishTaskId: ((dishTaskId != null) ? dishTaskId : __after.dishTaskId), dishId: ((dishId != null) ? dishId : __after.dishId), category: ((category != null) ? category : __after.category), taskType: ((taskType != null) ? taskType : __after.taskType), specialInstructions: ((specialInstructions != null) ? specialInstructions : __after.specialInstructions), isGenerated: ((isGenerated != null) ? isGenerated : __after.isGenerated), quantity: quantity, unit: unit, station: ((station != null) ? station : __after.station), dueAt: ((dueAt != null) ? dueAt : __after.dueAt), status: "pending", _subject: { entity: "PrepTask", command: "open", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "PrepTaskOpened", entity: "PrepTask", entityId: docId, payload: { prepTaskId: docId, tenantId: __after.tenantId, eventDishId: eventDishId, eventId: eventId, name: name, ingredientId: ((ingredientId != null) ? ingredientId : __after.ingredientId), ingredientDemandId: ((ingredientDemandId != null) ? ingredientDemandId : __after.ingredientDemandId), componentId: ((componentId != null) ? componentId : __after.componentId), dishTaskId: ((dishTaskId != null) ? dishTaskId : __after.dishTaskId), dishId: ((dishId != null) ? dishId : __after.dishId), category: ((category != null) ? category : __after.category), taskType: ((taskType != null) ? taskType : __after.taskType), specialInstructions: ((specialInstructions != null) ? specialInstructions : __after.specialInstructions), isGenerated: ((isGenerated != null) ? isGenerated : __after.isGenerated), quantity: quantity, unit: unit, station: ((station != null) ? station : __after.station), dueAt: ((dueAt != null) ? dueAt : __after.dueAt), status: "pending" }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const PrepTask_open = mutation({
  args: {
    docId: v.id("prepTasks"),
    eventDishId: v.string(),
    eventId: v.string(),
    name: v.string(),
    quantity: v.number(),
    unit: v.any(),
    ingredientId: v.optional(v.string()),
    ingredientDemandId: v.optional(v.string()),
    componentId: v.optional(v.string()),
    dishTaskId: v.optional(v.string()),
    dishId: v.optional(v.string()),
    category: v.optional(v.string()),
    taskType: v.optional(v.string()),
    specialInstructions: v.optional(v.string()),
    isGenerated: v.optional(v.boolean()),
    station: v.optional(v.string()),
    dueAt: v.optional(v.number()),
    notes: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runPrepTaskOpen(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "PrepTask_open", __result);
    }
    return __result;
  },
});

export const PrepTask_createViaOpen = mutation({
  args: {
    eventDishId: v.string(),
    eventId: v.string(),
    name: v.string(),
    quantity: v.number(),
    unit: v.any(),
    ingredientId: v.optional(v.string()),
    ingredientDemandId: v.optional(v.string()),
    componentId: v.optional(v.string()),
    dishTaskId: v.optional(v.string()),
    dishId: v.optional(v.string()),
    category: v.optional(v.string()),
    taskType: v.optional(v.string()),
    specialInstructions: v.optional(v.string()),
    isGenerated: v.optional(v.boolean()),
    station: v.optional(v.string()),
    dueAt: v.optional(v.number()),
    notes: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { eventDishId, eventId, name, quantity, unit, ingredientId, ingredientDemandId, componentId, dishTaskId, dishId, category, taskType, specialInstructions, isGenerated, station, dueAt, notes } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      category: args.category !== undefined ? args.category : "finish_at_event",
      taskType: args.taskType !== undefined ? args.taskType : "manual",
      isGenerated: args.isGenerated !== undefined ? args.isGenerated : true,
      status: "pending",
      createdAt: Date.now(),
      updatedAt: Date.now(),
      componentId: args.componentId,
      dishId: args.dishId,
      dishTaskId: args.dishTaskId,
      dueAt: args.dueAt,
      eventDishId: args.eventDishId,
      eventId: args.eventId,
      ingredientDemandId: args.ingredientDemandId,
      ingredientId: args.ingredientId,
      name: args.name,
      notes: args.notes,
      quantity: args.quantity,
      specialInstructions: args.specialInstructions,
      station: args.station,
      unit: args.unit
    };
    const __rel_eventDish = await __resolveRelation(ctx, "eventDishes", [__auth.tenantId, __draft.eventDishId], ["tenantId","id"], "tenantId", __auth.tenantId);
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) throw new Error("Kitchen and event managers may read prep tasks");
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) throw new Error("Kitchen and event managers may write prep tasks through commands");
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) throw new Error("Kitchen and event managers may execute prep task commands");
    if (!((__draft.claimedAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.status === "pending"))) throw new Error("Guard 1 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((__draft.eventDishId != null))) throw new Error("Guard 3 failed");
    if (!((__draft.eventId != null))) throw new Error("Guard 4 failed");
    if (!((eventDishId === __draft.eventDishId))) throw new Error("Open eventDishId must match the seeded event dish reference");
    if (!((eventId === __draft.eventId))) throw new Error("Open eventId must match the seeded event reference");
    if (!(((__rel_eventDish == null) || (eventId === __rel_eventDish.eventId)))) throw new Error("PrepTask eventId must match EventDish.eventId");
    if (!((((name).trim()).length > 0))) throw new Error("Prep task name is required");
    if (!((quantity > 0))) throw new Error("Prep quantity must be positive");
    const doc: Record<string, any> = {
      ...__draft,
      eventDishId: eventDishId,
      eventId: eventId,
      name: name,
      ingredientId: ((ingredientId != null) ? ingredientId : __draft.ingredientId),
      ingredientDemandId: ((ingredientDemandId != null) ? ingredientDemandId : __draft.ingredientDemandId),
      componentId: ((componentId != null) ? componentId : __draft.componentId),
      dishTaskId: ((dishTaskId != null) ? dishTaskId : __draft.dishTaskId),
      dishId: ((dishId != null) ? dishId : __draft.dishId),
      category: ((category != null) ? category : __draft.category),
      taskType: ((taskType != null) ? taskType : __draft.taskType),
      specialInstructions: ((specialInstructions != null) ? specialInstructions : __draft.specialInstructions),
      isGenerated: ((isGenerated != null) ? isGenerated : __draft.isGenerated),
      quantity: quantity,
      unit: unit,
      station: ((station != null) ? station : __draft.station),
      dueAt: ((dueAt != null) ? dueAt : __draft.dueAt),
      notes: ((notes != null) ? notes : __draft.notes),
      version: 1,
    };
    const docId = await ctx.db.insert("prepTasks", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, prepTaskId: docId, tenantId: doc.tenantId, eventDishId: doc.eventDishId, eventId: doc.eventId, name: doc.name, ingredientId: ((doc.ingredientId != null) ? doc.ingredientId : doc.ingredientId), ingredientDemandId: ((doc.ingredientDemandId != null) ? doc.ingredientDemandId : doc.ingredientDemandId), componentId: ((doc.componentId != null) ? doc.componentId : doc.componentId), dishTaskId: ((doc.dishTaskId != null) ? doc.dishTaskId : doc.dishTaskId), dishId: ((doc.dishId != null) ? doc.dishId : doc.dishId), category: ((doc.category != null) ? doc.category : doc.category), taskType: ((doc.taskType != null) ? doc.taskType : doc.taskType), specialInstructions: ((doc.specialInstructions != null) ? doc.specialInstructions : doc.specialInstructions), isGenerated: ((doc.isGenerated != null) ? doc.isGenerated : doc.isGenerated), quantity: doc.quantity, unit: doc.unit, station: ((doc.station != null) ? doc.station : doc.station), dueAt: ((doc.dueAt != null) ? doc.dueAt : doc.dueAt), status: "pending", _subject: { entity: "PrepTask", command: "open", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "PrepTaskOpened", entity: "PrepTask", entityId: docId, payload: { prepTaskId: docId, tenantId: doc.tenantId, eventDishId: doc.eventDishId, eventId: doc.eventId, name: doc.name, ingredientId: ((doc.ingredientId != null) ? doc.ingredientId : doc.ingredientId), ingredientDemandId: ((doc.ingredientDemandId != null) ? doc.ingredientDemandId : doc.ingredientDemandId), componentId: ((doc.componentId != null) ? doc.componentId : doc.componentId), dishTaskId: ((doc.dishTaskId != null) ? doc.dishTaskId : doc.dishTaskId), dishId: ((doc.dishId != null) ? doc.dishId : doc.dishId), category: ((doc.category != null) ? doc.category : doc.category), taskType: ((doc.taskType != null) ? doc.taskType : doc.taskType), specialInstructions: ((doc.specialInstructions != null) ? doc.specialInstructions : doc.specialInstructions), isGenerated: ((doc.isGenerated != null) ? doc.isGenerated : doc.isGenerated), quantity: doc.quantity, unit: doc.unit, station: ((doc.station != null) ? doc.station : doc.station), dueAt: ((doc.dueAt != null) ? doc.dueAt : doc.dueAt), status: "pending" }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "PrepTask_createViaOpen", __result);
    }
    return __result;
  },
});

async function __runPrepTaskRefreshGenerated(ctx: MutationCtx, { docId, quantity, specialInstructions, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("PrepTask not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("PrepTask not found");
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) throw new Error("Kitchen and event managers may read prep tasks");
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) throw new Error("Kitchen and event managers may write prep tasks through commands");
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) throw new Error("Kitchen and event managers may execute prep task commands");
    if (!((doc.status === "pending"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((doc.isGenerated === true))) throw new Error("Guard 2 failed");
    if (!((quantity > 0))) throw new Error("Generated prep task quantity must be positive");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      quantity: quantity,
      specialInstructions: ((specialInstructions != null) ? specialInstructions : doc.specialInstructions),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, prepTaskId: docId, tenantId: __after.tenantId, eventDishId: __after.eventDishId, eventId: __after.eventId, quantity: quantity, unit: __after.unit, specialInstructions: ((specialInstructions != null) ? specialInstructions : __after.specialInstructions), _subject: { entity: "PrepTask", command: "refreshGenerated", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "PrepTaskGeneratedRefreshed", entity: "PrepTask", entityId: docId, payload: { prepTaskId: docId, tenantId: __after.tenantId, eventDishId: __after.eventDishId, eventId: __after.eventId, quantity: quantity, unit: __after.unit, specialInstructions: ((specialInstructions != null) ? specialInstructions : __after.specialInstructions) }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const PrepTask_refreshGenerated = mutation({
  args: {
    docId: v.id("prepTasks"),
    quantity: v.number(),
    specialInstructions: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runPrepTaskRefreshGenerated(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "PrepTask_refreshGenerated", __result);
    }
    return __result;
  },
});

async function __runPrepTaskRelease(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("PrepTask not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("PrepTask not found");
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) throw new Error("Kitchen and event managers may read prep tasks");
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) throw new Error("Kitchen and event managers may write prep tasks through commands");
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) throw new Error("Kitchen and event managers may execute prep task commands");
    if (!((doc.status === "claimed"))) throw new Error("Guard 0 failed");
    if (!((doc.claimedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!(((doc.assignedToId === user.id) || checkRole(user, "kitchenLeadAccess")))) throw new Error("Guard 3 failed");
    const previousStatus = doc.status;
    const previousAssignee = doc.assignedToId;
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "pending";
        const __allowed: Record<string, string[]> = { "pending": ["claimed", "blocked", "cancelled"], "claimed": ["in_progress", "pending", "blocked", "cancelled"], "in_progress": ["completed", "blocked", "cancelled"], "blocked": ["pending", "cancelled"], "completed": [], "cancelled": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      assignedToId: null,
      status: "pending",
      claimedAt: null,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, prepTaskId: docId, tenantId: __after.tenantId, eventDishId: __after.eventDishId, eventId: __after.eventId, name: __after.name, previousAssignedToId: previousAssignee, quantity: __after.quantity, unit: __after.unit, previousStatus: previousStatus, status: "pending", _subject: { entity: "PrepTask", command: "release", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "PrepTaskReleased", entity: "PrepTask", entityId: docId, payload: { prepTaskId: docId, tenantId: __after.tenantId, eventDishId: __after.eventDishId, eventId: __after.eventId, name: __after.name, previousAssignedToId: previousAssignee, quantity: __after.quantity, unit: __after.unit, previousStatus: previousStatus, status: "pending" }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const PrepTask_release = mutation({
  args: {
    docId: v.id("prepTasks"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runPrepTaskRelease(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "PrepTask_release", __result);
    }
    return __result;
  },
});

async function __runPrepTaskRevise(ctx: MutationCtx, { docId, name, quantity, unit, category, taskType, specialInstructions, ingredientId, componentId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("PrepTask not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("PrepTask not found");
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) throw new Error("Kitchen and event managers may read prep tasks");
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) throw new Error("Kitchen and event managers may write prep tasks through commands");
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) throw new Error("Kitchen and event managers may execute prep task commands");
    if (!((doc.status === "pending"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(((name == null) || (((name).trim()).length > 0)))) throw new Error("Prep task name cannot be blank");
    if (!(((quantity == null) || (quantity > 0)))) throw new Error("Prep quantity must be positive");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      name: ((name != null) ? name : doc.name),
      quantity: ((quantity != null) ? quantity : doc.quantity),
      unit: ((unit != null) ? unit : doc.unit),
      category: ((category != null) ? category : doc.category),
      taskType: ((taskType != null) ? taskType : doc.taskType),
      specialInstructions: ((specialInstructions != null) ? specialInstructions : doc.specialInstructions),
      ingredientId: ((ingredientId != null) ? ingredientId : doc.ingredientId),
      componentId: ((componentId != null) ? componentId : doc.componentId),
      isGenerated: false,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, prepTaskId: docId, tenantId: __after.tenantId, eventDishId: __after.eventDishId, eventId: __after.eventId, name: ((name != null) ? name : __after.name), quantity: ((quantity != null) ? quantity : __after.quantity), unit: ((unit != null) ? unit : __after.unit), category: ((category != null) ? category : __after.category), taskType: ((taskType != null) ? taskType : __after.taskType), specialInstructions: ((specialInstructions != null) ? specialInstructions : __after.specialInstructions), _subject: { entity: "PrepTask", command: "revise", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "PrepTaskRevised", entity: "PrepTask", entityId: docId, payload: { prepTaskId: docId, tenantId: __after.tenantId, eventDishId: __after.eventDishId, eventId: __after.eventId, name: ((name != null) ? name : __after.name), quantity: ((quantity != null) ? quantity : __after.quantity), unit: ((unit != null) ? unit : __after.unit), category: ((category != null) ? category : __after.category), taskType: ((taskType != null) ? taskType : __after.taskType), specialInstructions: ((specialInstructions != null) ? specialInstructions : __after.specialInstructions) }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const PrepTask_revise = mutation({
  args: {
    docId: v.id("prepTasks"),
    name: v.optional(v.string()),
    quantity: v.optional(v.number()),
    unit: v.optional(v.any()),
    category: v.optional(v.string()),
    taskType: v.optional(v.string()),
    specialInstructions: v.optional(v.string()),
    ingredientId: v.optional(v.string()),
    componentId: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runPrepTaskRevise(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "PrepTask_revise", __result);
    }
    return __result;
  },
});

async function __runPrepTaskStandDown(ctx: MutationCtx, { docId, reason, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("PrepTask not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("PrepTask not found");
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) throw new Error("Kitchen and event managers may read prep tasks");
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) throw new Error("Kitchen and event managers may write prep tasks through commands");
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) throw new Error("Kitchen and event managers may execute prep task commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!((checkRole(user, "kitchenLeadAccess") || checkRole(user, "eventManageAccess")))) throw new Error("Guard 1 failed");
    if (!((((reason).trim()).length > 0))) throw new Error("Stand-down reason is required");
    const previousStatus = doc.status;
    const alreadySettled = ((doc.status === "completed") || (doc.status === "cancelled"));
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = String((alreadySettled ? doc.status : "cancelled"));
        const __allowed: Record<string, string[]> = { "pending": ["claimed", "blocked", "cancelled"], "claimed": ["in_progress", "pending", "blocked", "cancelled"], "in_progress": ["completed", "blocked", "cancelled"], "blocked": ["pending", "cancelled"], "completed": [], "cancelled": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: (alreadySettled ? doc.status : "cancelled"),
      cancelledAt: (alreadySettled ? doc.cancelledAt : Date.now()),
      cancellationReason: (alreadySettled ? doc.cancellationReason : reason),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, prepTaskId: docId, tenantId: __after.tenantId, eventDishId: __after.eventDishId, eventId: __after.eventId, name: __after.name, quantity: __after.quantity, unit: __after.unit, previousStatus: previousStatus, status: __after.status, reason: reason, _subject: { entity: "PrepTask", command: "standDown", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "PrepTaskCancelled", entity: "PrepTask", entityId: docId, payload: { prepTaskId: docId, tenantId: __after.tenantId, eventDishId: __after.eventDishId, eventId: __after.eventId, name: __after.name, quantity: __after.quantity, unit: __after.unit, previousStatus: previousStatus, status: __after.status, reason: reason }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const PrepTask_standDown = mutation({
  args: {
    docId: v.id("prepTasks"),
    reason: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runPrepTaskStandDown(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "PrepTask_standDown", __result);
    }
    return __result;
  },
});

async function __runPrepTaskStart(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("PrepTask not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("PrepTask not found");
    (doc as any).incomingDependencies = await ctx.db.query("prepTaskDependencies").withIndex("by_dependentTaskId", (q: any) => q.eq("dependentTaskId", docId)).collect();
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) throw new Error("Kitchen and event managers may read prep tasks");
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) throw new Error("Kitchen and event managers may write prep tasks through commands");
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) throw new Error("Kitchen and event managers may execute prep task commands");
    if (!((doc.status === "claimed"))) throw new Error("Guard 0 failed");
    if (!((doc.claimedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.assignedToId != null))) throw new Error("Guard 2 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 3 failed");
    if (!((((doc.incomingDependencies) ?? []).filter((dependency: Doc<"prepTaskDependencies">) => ((dependency.isSatisfied === false))).length === 0))) throw new Error("All predecessor prep tasks must be complete before this task can start");
    const previousStatus = doc.status;
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "in_progress";
        const __allowed: Record<string, string[]> = { "pending": ["claimed", "blocked", "cancelled"], "claimed": ["in_progress", "pending", "blocked", "cancelled"], "in_progress": ["completed", "blocked", "cancelled"], "blocked": ["pending", "cancelled"], "completed": [], "cancelled": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "in_progress",
      startedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, prepTaskId: docId, tenantId: __after.tenantId, eventDishId: __after.eventDishId, eventId: __after.eventId, name: __after.name, assignedToId: __after.assignedToId, quantity: __after.quantity, unit: __after.unit, previousStatus: previousStatus, status: "in_progress", _subject: { entity: "PrepTask", command: "start", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "PrepTaskStarted", entity: "PrepTask", entityId: docId, payload: { prepTaskId: docId, tenantId: __after.tenantId, eventDishId: __after.eventDishId, eventId: __after.eventId, name: __after.name, assignedToId: __after.assignedToId, quantity: __after.quantity, unit: __after.unit, previousStatus: previousStatus, status: "in_progress" }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const PrepTask_start = mutation({
  args: {
    docId: v.id("prepTasks"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runPrepTaskStart(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "PrepTask_start", __result);
    }
    return __result;
  },
});

async function __runPrepTaskUnblock(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("PrepTask not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("PrepTask not found");
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) throw new Error("Kitchen and event managers may read prep tasks");
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) throw new Error("Kitchen and event managers may write prep tasks through commands");
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) throw new Error("Kitchen and event managers may execute prep task commands");
    if (!((doc.status === "blocked"))) throw new Error("Guard 0 failed");
    if (!((doc.blockedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!(checkRole(user, "kitchenLeadAccess"))) throw new Error("Guard 3 failed");
    const previousStatus = doc.status;
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "pending";
        const __allowed: Record<string, string[]> = { "pending": ["claimed", "blocked", "cancelled"], "claimed": ["in_progress", "pending", "blocked", "cancelled"], "in_progress": ["completed", "blocked", "cancelled"], "blocked": ["pending", "cancelled"], "completed": [], "cancelled": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "pending",
      blockedAt: null,
      blockReason: null,
      assignedToId: null,
      claimedAt: null,
      startedAt: null,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, prepTaskId: docId, tenantId: __after.tenantId, eventDishId: __after.eventDishId, eventId: __after.eventId, name: __after.name, quantity: __after.quantity, unit: __after.unit, previousStatus: previousStatus, status: "pending", _subject: { entity: "PrepTask", command: "unblock", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "PrepTaskUnblocked", entity: "PrepTask", entityId: docId, payload: { prepTaskId: docId, tenantId: __after.tenantId, eventDishId: __after.eventDishId, eventId: __after.eventId, name: __after.name, quantity: __after.quantity, unit: __after.unit, previousStatus: previousStatus, status: "pending" }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const PrepTask_unblock = mutation({
  args: {
    docId: v.id("prepTasks"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runPrepTaskUnblock(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "PrepTask_unblock", __result);
    }
    return __result;
  },
});

async function __runPrepTaskCommentEdit(ctx: MutationCtx, { docId, body, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("PrepTaskComment not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("PrepTaskComment not found");
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) throw new Error("Kitchen staff and event managers may read prep task comments");
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) throw new Error("Kitchen staff and event managers may post prep task comments through commands");
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) throw new Error("Kitchen staff and event managers may execute prep task comment commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.postedAt != null))) throw new Error("Guard 1 failed");
    if (!(((doc.authorAuthSubjectId === user.id) || checkRole(user, "kitchenLeadAccess")))) throw new Error("Guard 2 failed");
    if (!((((body).trim()).length > 0))) throw new Error("Comment text is required");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      body: body,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, prepTaskCommentId: docId, tenantId: __after.tenantId, prepTaskId: __after.prepTaskId, eventId: __after.eventId, _subject: { entity: "PrepTaskComment", command: "edit", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "PrepTaskCommentEdited", entity: "PrepTaskComment", entityId: docId, payload: { prepTaskCommentId: docId, tenantId: __after.tenantId, prepTaskId: __after.prepTaskId, eventId: __after.eventId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const PrepTaskComment_edit = mutation({
  args: {
    docId: v.id("prepTaskComments"),
    body: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runPrepTaskCommentEdit(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "PrepTaskComment_edit", __result);
    }
    return __result;
  },
});

async function __runPrepTaskCommentPost(ctx: MutationCtx, { docId, prepTaskId, eventId, body, eventDishId, category, authorPersonId, authorName, taskOwnerAssignedToId, taskOwnerAuthSubjectId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("PrepTaskComment not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("PrepTaskComment not found");
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) throw new Error("Kitchen staff and event managers may read prep task comments");
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) throw new Error("Kitchen staff and event managers may post prep task comments through commands");
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) throw new Error("Kitchen staff and event managers may execute prep task comment commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.postedAt == null))) throw new Error("Guard 1 failed");
    if (!((user.id != null))) throw new Error("Guard 2 failed");
    if (!((((body).trim()).length > 0))) throw new Error("Comment text is required");
    if (!(((authorName == null) || (((authorName).trim()).length > 0)))) throw new Error("Author name cannot be blank");
    if (!(((taskOwnerAuthSubjectId == null) || (taskOwnerAuthSubjectId !== user.id)))) throw new Error("Task owners cannot post comments that notify themselves");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      prepTaskId: prepTaskId,
      eventId: eventId,
      eventDishId: ((eventDishId != null) ? eventDishId : doc.eventDishId),
      taskOwnerAssignedToId: taskOwnerAssignedToId,
      taskOwnerAuthSubjectId: taskOwnerAuthSubjectId,
      authorPersonId: ((authorPersonId != null) ? authorPersonId : doc.authorPersonId),
      authorAuthSubjectId: user.id,
      authorName: ((authorName != null) ? authorName : doc.authorName),
      category: ((category != null) ? category : "note"),
      body: body,
      postedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, prepTaskCommentId: docId, tenantId: __after.tenantId, prepTaskId: prepTaskId, eventId: eventId, eventDishId: ((eventDishId != null) ? eventDishId : __after.eventDishId), taskOwnerAuthSubjectId: taskOwnerAuthSubjectId, authorPersonId: ((authorPersonId != null) ? authorPersonId : __after.authorPersonId), authorName: ((authorName != null) ? authorName : __after.authorName), category: ((category != null) ? category : "note"), body: body, _subject: { entity: "PrepTaskComment", command: "post", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "PrepTaskCommentPosted", entity: "PrepTaskComment", entityId: docId, payload: { prepTaskCommentId: docId, tenantId: __after.tenantId, prepTaskId: prepTaskId, eventId: eventId, eventDishId: ((eventDishId != null) ? eventDishId : __after.eventDishId), taskOwnerAuthSubjectId: taskOwnerAuthSubjectId, authorPersonId: ((authorPersonId != null) ? authorPersonId : __after.authorPersonId), authorName: ((authorName != null) ? authorName : __after.authorName), category: ((category != null) ? category : "note"), body: body }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const PrepTaskComment_post = mutation({
  args: {
    docId: v.id("prepTaskComments"),
    prepTaskId: v.string(),
    eventId: v.string(),
    body: v.string(),
    eventDishId: v.optional(v.string()),
    category: v.optional(v.any()),
    authorPersonId: v.optional(v.string()),
    authorName: v.optional(v.string()),
    taskOwnerAssignedToId: v.optional(v.string()),
    taskOwnerAuthSubjectId: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runPrepTaskCommentPost(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "PrepTaskComment_post", __result);
    }
    return __result;
  },
});

export const PrepTaskComment_createViaPost = mutation({
  args: {
    prepTaskId: v.string(),
    eventId: v.string(),
    body: v.string(),
    eventDishId: v.optional(v.string()),
    category: v.optional(v.any()),
    authorPersonId: v.optional(v.string()),
    authorName: v.optional(v.string()),
    taskOwnerAssignedToId: v.optional(v.string()),
    taskOwnerAuthSubjectId: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { prepTaskId, eventId, body, eventDishId, category, authorPersonId, authorName, taskOwnerAssignedToId, taskOwnerAuthSubjectId } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      category: args.category !== undefined ? args.category : "note",
      createdAt: Date.now(),
      updatedAt: Date.now(),
      authorName: args.authorName,
      authorPersonId: args.authorPersonId,
      body: args.body,
      eventDishId: args.eventDishId,
      eventId: args.eventId,
      prepTaskId: args.prepTaskId,
      taskOwnerAssignedToId: args.taskOwnerAssignedToId,
      taskOwnerAuthSubjectId: args.taskOwnerAuthSubjectId
    };
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) throw new Error("Kitchen staff and event managers may read prep task comments");
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) throw new Error("Kitchen staff and event managers may post prep task comments through commands");
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) throw new Error("Kitchen staff and event managers may execute prep task comment commands");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.postedAt == null))) throw new Error("Guard 1 failed");
    if (!((user.id != null))) throw new Error("Guard 2 failed");
    if (!((((body).trim()).length > 0))) throw new Error("Comment text is required");
    if (!(((authorName == null) || (((authorName).trim()).length > 0)))) throw new Error("Author name cannot be blank");
    if (!(((taskOwnerAuthSubjectId == null) || (taskOwnerAuthSubjectId !== user.id)))) throw new Error("Task owners cannot post comments that notify themselves");
    const doc: Record<string, any> = {
      ...__draft,
      prepTaskId: prepTaskId,
      eventId: eventId,
      eventDishId: ((eventDishId != null) ? eventDishId : __draft.eventDishId),
      taskOwnerAssignedToId: taskOwnerAssignedToId,
      taskOwnerAuthSubjectId: taskOwnerAuthSubjectId,
      authorPersonId: ((authorPersonId != null) ? authorPersonId : __draft.authorPersonId),
      authorAuthSubjectId: user.id,
      authorName: ((authorName != null) ? authorName : __draft.authorName),
      category: ((category != null) ? category : "note"),
      body: body,
      postedAt: Date.now(),
      version: 1,
    };
    const docId = await ctx.db.insert("prepTaskComments", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, prepTaskCommentId: docId, tenantId: doc.tenantId, prepTaskId: doc.prepTaskId, eventId: doc.eventId, eventDishId: ((doc.eventDishId != null) ? doc.eventDishId : doc.eventDishId), taskOwnerAuthSubjectId: doc.taskOwnerAuthSubjectId, authorPersonId: ((doc.authorPersonId != null) ? doc.authorPersonId : doc.authorPersonId), authorName: ((doc.authorName != null) ? doc.authorName : doc.authorName), category: ((doc.category != null) ? doc.category : "note"), body: doc.body, _subject: { entity: "PrepTaskComment", command: "post", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "PrepTaskCommentPosted", entity: "PrepTaskComment", entityId: docId, payload: { prepTaskCommentId: docId, tenantId: doc.tenantId, prepTaskId: doc.prepTaskId, eventId: doc.eventId, eventDishId: ((doc.eventDishId != null) ? doc.eventDishId : doc.eventDishId), taskOwnerAuthSubjectId: doc.taskOwnerAuthSubjectId, authorPersonId: ((doc.authorPersonId != null) ? doc.authorPersonId : doc.authorPersonId), authorName: ((doc.authorName != null) ? doc.authorName : doc.authorName), category: ((doc.category != null) ? doc.category : "note"), body: doc.body }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "PrepTaskComment_createViaPost", __result);
    }
    return __result;
  },
});

async function __runPrepTaskDependencyDeclare(ctx: MutationCtx, { docId, dependentTaskId, predecessorTaskId }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("PrepTaskDependency not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("PrepTaskDependency not found");
    const __rel_dependentTask = await __resolveRelation(ctx, "prepTasks", [__auth.tenantId, doc.dependentTaskId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_predecessorTask = await __resolveRelation(ctx, "prepTasks", [__auth.tenantId, doc.predecessorTaskId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).dependentTaskId;
      ((doc as any) as any).dependentTask = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    {
      const __fk = ((doc as any) as any).predecessorTaskId;
      ((doc as any) as any).predecessorTask = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) throw new Error("Kitchen and event managers may read prep task dependencies");
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) throw new Error("Kitchen and event managers may declare prep task dependencies");
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) throw new Error("Kitchen and event managers may execute prep task dependency commands");
    if (!((__rel_dependentTask != null))) throw new Error("Guard 0 failed");
    if (!((__rel_predecessorTask != null))) throw new Error("Guard 1 failed");
    if (!((__rel_dependentTask.status === "pending"))) throw new Error("Guard 2 failed");
    if (!((dependentTaskId !== predecessorTaskId))) throw new Error("A prep task cannot depend on itself");
    if (!((__rel_dependentTask.eventId === __rel_predecessorTask.eventId))) throw new Error("Predecessor prep tasks must belong to the same event");
    const updates = {
      dependentTaskId: dependentTaskId,
      predecessorTaskId: predecessorTaskId,
      isSatisfied: (__rel_predecessorTask.status === "completed")
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const __rel_predecessorTask__post = await __resolveRelation(ctx, "prepTasks", [__auth.tenantId, __after.predecessorTaskId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, prepTaskDependencyId: docId, tenantId: __after.tenantId, dependentTaskId: dependentTaskId, predecessorTaskId: predecessorTaskId, isSatisfied: (__rel_predecessorTask__post.status === "completed"), _subject: { entity: "PrepTaskDependency", command: "declare", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "PrepTaskDependencyDeclared", entity: "PrepTaskDependency", entityId: docId, payload: { prepTaskDependencyId: docId, tenantId: __after.tenantId, dependentTaskId: dependentTaskId, predecessorTaskId: predecessorTaskId, isSatisfied: (__rel_predecessorTask__post.status === "completed") }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const PrepTaskDependency_declare = mutation({
  args: {
    docId: v.id("prepTaskDependencies"),
    dependentTaskId: v.string(),
    predecessorTaskId: v.string(),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runPrepTaskDependencyDeclare(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "PrepTaskDependency_declare", __result);
    }
    return __result;
  },
});

export const PrepTaskDependency_createViaDeclare = mutation({
  args: {
    dependentTaskId: v.string(),
    predecessorTaskId: v.string(),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { dependentTaskId, predecessorTaskId } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      createdAt: Date.now(),
      updatedAt: Date.now(),
      dependentTaskId: args.dependentTaskId,
      predecessorTaskId: args.predecessorTaskId
    };
    const __rel_dependentTask = await __resolveRelation(ctx, "prepTasks", [__auth.tenantId, __draft.dependentTaskId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_predecessorTask = await __resolveRelation(ctx, "prepTasks", [__auth.tenantId, __draft.predecessorTaskId], ["tenantId","id"], "tenantId", __auth.tenantId);
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) throw new Error("Kitchen and event managers may read prep task dependencies");
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) throw new Error("Kitchen and event managers may declare prep task dependencies");
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) throw new Error("Kitchen and event managers may execute prep task dependency commands");
    if (!((__rel_dependentTask != null))) throw new Error("Guard 0 failed");
    if (!((__rel_predecessorTask != null))) throw new Error("Guard 1 failed");
    if (!((__rel_dependentTask.status === "pending"))) throw new Error("Guard 2 failed");
    if (!((dependentTaskId !== predecessorTaskId))) throw new Error("A prep task cannot depend on itself");
    if (!((__rel_dependentTask.eventId === __rel_predecessorTask.eventId))) throw new Error("Predecessor prep tasks must belong to the same event");
    const doc: Record<string, any> = {
      ...__draft,
      dependentTaskId: dependentTaskId,
      predecessorTaskId: predecessorTaskId,
      isSatisfied: (__rel_predecessorTask.status === "completed"),
    };
    const docId = await ctx.db.insert("prepTaskDependencies", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, prepTaskDependencyId: docId, tenantId: doc.tenantId, dependentTaskId: doc.dependentTaskId, predecessorTaskId: doc.predecessorTaskId, isSatisfied: (__rel_predecessorTask.status === "completed"), _subject: { entity: "PrepTaskDependency", command: "declare", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "PrepTaskDependencyDeclared", entity: "PrepTaskDependency", entityId: docId, payload: { prepTaskDependencyId: docId, tenantId: doc.tenantId, dependentTaskId: doc.dependentTaskId, predecessorTaskId: doc.predecessorTaskId, isSatisfied: (__rel_predecessorTask.status === "completed") }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "PrepTaskDependency_createViaDeclare", __result);
    }
    return __result;
  },
});

async function __runPrepTaskDependencySatisfy(ctx: MutationCtx, { docId }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("PrepTaskDependency not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("PrepTaskDependency not found");
    const __rel_predecessorTask = await __resolveRelation(ctx, "prepTasks", [__auth.tenantId, doc.predecessorTaskId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).predecessorTaskId;
      ((doc as any) as any).predecessorTask = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) throw new Error("Kitchen and event managers may read prep task dependencies");
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) throw new Error("Kitchen and event managers may declare prep task dependencies");
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) throw new Error("Kitchen and event managers may execute prep task dependency commands");
    if (!((doc.isSatisfied === false))) throw new Error("Guard 0 failed");
    if (!((__rel_predecessorTask != null))) throw new Error("Guard 1 failed");
    if (!((__rel_predecessorTask.status === "completed"))) throw new Error("Guard 2 failed");
    const updates = {
      isSatisfied: true
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, prepTaskDependencyId: docId, tenantId: __after.tenantId, dependentTaskId: __after.dependentTaskId, predecessorTaskId: __after.predecessorTaskId, _subject: { entity: "PrepTaskDependency", command: "satisfy", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "PrepTaskDependencySatisfied", entity: "PrepTaskDependency", entityId: docId, payload: { prepTaskDependencyId: docId, tenantId: __after.tenantId, dependentTaskId: __after.dependentTaskId, predecessorTaskId: __after.predecessorTaskId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const PrepTaskDependency_satisfy = mutation({
  args: {
    docId: v.id("prepTaskDependencies"),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runPrepTaskDependencySatisfy(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "PrepTaskDependency_satisfy", __result);
    }
    return __result;
  },
});

async function __runProductionBatchCancel(ctx: MutationCtx, { docId, reason, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ProductionBatch not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ProductionBatch not found");
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) throw new Error("Kitchen staff and managers may read production batches");
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) throw new Error("Kitchen staff and managers may write production batches through commands");
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) throw new Error("Kitchen staff and managers may execute production batch commands");
    if (!(((doc.status === "planned") || (doc.status === "in_progress")))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(checkRole(user, "kitchenLeadAccess"))) throw new Error("Guard 2 failed");
    if (!((((reason).trim()).length > 0))) throw new Error("Cancellation reason is required");
    const previousStatus = doc.status;
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "cancelled";
        const __allowed: Record<string, string[]> = { "planned": ["in_progress", "cancelled"], "in_progress": ["completed", "cancelled"], "completed": [], "cancelled": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "cancelled",
      cancelledAt: Date.now(),
      cancellationReason: reason,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, productionBatchId: docId, tenantId: __after.tenantId, componentId: __after.componentId, eventId: __after.eventId, plannedYield: __after.plannedYield, actualYield: __after.actualYield, yieldUnit: __after.yieldUnit, previousStatus: previousStatus, status: "cancelled", reason: reason, _subject: { entity: "ProductionBatch", command: "cancel", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ProductionBatchCancelled", entity: "ProductionBatch", entityId: docId, payload: { productionBatchId: docId, tenantId: __after.tenantId, componentId: __after.componentId, eventId: __after.eventId, plannedYield: __after.plannedYield, actualYield: __after.actualYield, yieldUnit: __after.yieldUnit, previousStatus: previousStatus, status: "cancelled", reason: reason }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ProductionBatch_cancel = mutation({
  args: {
    docId: v.id("productionBatches"),
    reason: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runProductionBatchCancel(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ProductionBatch_cancel", __result);
    }
    return __result;
  },
});

async function __runProductionBatchComplete(ctx: MutationCtx, { docId, actualYield, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ProductionBatch not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ProductionBatch not found");
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) throw new Error("Kitchen staff and managers may read production batches");
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) throw new Error("Kitchen staff and managers may write production batches through commands");
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) throw new Error("Kitchen staff and managers may execute production batch commands");
    if (!((doc.status === "in_progress"))) throw new Error("Guard 0 failed");
    if (!((doc.startedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((actualYield >= 0))) throw new Error("Actual yield cannot be negative");
    const previousStatus = doc.status;
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "completed";
        const __allowed: Record<string, string[]> = { "planned": ["in_progress", "cancelled"], "in_progress": ["completed", "cancelled"], "completed": [], "cancelled": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      actualYield: actualYield,
      status: "completed",
      completedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, productionBatchId: docId, tenantId: __after.tenantId, componentId: __after.componentId, eventId: __after.eventId, plannedYield: __after.plannedYield, actualYield: actualYield, yieldUnit: __after.yieldUnit, previousStatus: previousStatus, status: "completed", _subject: { entity: "ProductionBatch", command: "complete", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ProductionBatchCompleted", entity: "ProductionBatch", entityId: docId, payload: { productionBatchId: docId, tenantId: __after.tenantId, componentId: __after.componentId, eventId: __after.eventId, plannedYield: __after.plannedYield, actualYield: actualYield, yieldUnit: __after.yieldUnit, previousStatus: previousStatus, status: "completed" }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ProductionBatch_complete = mutation({
  args: {
    docId: v.id("productionBatches"),
    actualYield: v.number(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runProductionBatchComplete(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ProductionBatch_complete", __result);
    }
    return __result;
  },
});

async function __runProductionBatchPlan(ctx: MutationCtx, { docId, componentId, plannedYield, yieldUnit, eventId, notes, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ProductionBatch not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ProductionBatch not found");
    const __rel_component = await __resolveRelation(ctx, "components", [__auth.tenantId, doc.componentId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).componentId;
      ((doc as any) as any).component = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) throw new Error("Kitchen staff and managers may read production batches");
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) throw new Error("Kitchen staff and managers may write production batches through commands");
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) throw new Error("Kitchen staff and managers may execute production batch commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.status === "planned"))) throw new Error("Guard 1 failed");
    if (!(((doc.plannedAt == null) || (doc.componentId === componentId)))) throw new Error("Guard 2 failed");
    if (!(((doc.plannedAt != null) || ((__rel_component != null) && (__rel_component.status === "published"))))) throw new Error("Guard 3 failed");
    if (!((componentId === doc.componentId))) throw new Error("Plan componentId must match the seeded component reference");
    if (!((((eventId == null) || (doc.eventId == null)) || (eventId === doc.eventId)))) throw new Error("Plan eventId must match the seeded event reference when provided");
    if (!(((plannedYield > 0) || (doc.plannedYield > 0)))) throw new Error("Planned yield must be positive");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      componentId: componentId,
      eventId: ((eventId != null) ? eventId : doc.eventId),
      plannedYield: ((doc.plannedAt != null) ? doc.plannedYield : plannedYield),
      yieldUnit: ((doc.plannedAt != null) ? doc.yieldUnit : yieldUnit),
      notes: ((notes != null) ? notes : doc.notes),
      plannedAt: ((doc.plannedAt != null) ? doc.plannedAt : Date.now()),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, productionBatchId: docId, tenantId: __after.tenantId, componentId: componentId, eventId: ((eventId != null) ? eventId : __after.eventId), plannedYield: ((__after.plannedAt != null) ? __after.plannedYield : plannedYield), yieldUnit: ((__after.plannedAt != null) ? __after.yieldUnit : yieldUnit), status: "planned", _subject: { entity: "ProductionBatch", command: "plan", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ProductionBatchPlanned", entity: "ProductionBatch", entityId: docId, payload: { productionBatchId: docId, tenantId: __after.tenantId, componentId: componentId, eventId: ((eventId != null) ? eventId : __after.eventId), plannedYield: ((__after.plannedAt != null) ? __after.plannedYield : plannedYield), yieldUnit: ((__after.plannedAt != null) ? __after.yieldUnit : yieldUnit), status: "planned" }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ProductionBatch_plan = mutation({
  args: {
    docId: v.id("productionBatches"),
    componentId: v.string(),
    plannedYield: v.number(),
    yieldUnit: v.any(),
    eventId: v.optional(v.string()),
    notes: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runProductionBatchPlan(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ProductionBatch_plan", __result);
    }
    return __result;
  },
});

export const ProductionBatch_createViaPlan = mutation({
  args: {
    componentId: v.string(),
    plannedYield: v.number(),
    yieldUnit: v.any(),
    eventId: v.optional(v.string()),
    notes: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { componentId, plannedYield, yieldUnit, eventId, notes } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      status: "planned",
      createdAt: Date.now(),
      updatedAt: Date.now(),
      componentId: args.componentId,
      eventId: args.eventId,
      notes: args.notes,
      plannedYield: args.plannedYield,
      yieldUnit: args.yieldUnit
    };
    const __rel_component = await __resolveRelation(ctx, "components", [__auth.tenantId, __draft.componentId], ["tenantId","id"], "tenantId", __auth.tenantId);
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) throw new Error("Kitchen staff and managers may read production batches");
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) throw new Error("Kitchen staff and managers may write production batches through commands");
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) throw new Error("Kitchen staff and managers may execute production batch commands");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.status === "planned"))) throw new Error("Guard 1 failed");
    if (!(((__draft.plannedAt == null) || (__draft.componentId === componentId)))) throw new Error("Guard 2 failed");
    if (!(((__draft.plannedAt != null) || ((__rel_component != null) && (__rel_component.status === "published"))))) throw new Error("Guard 3 failed");
    if (!((componentId === __draft.componentId))) throw new Error("Plan componentId must match the seeded component reference");
    if (!((((eventId == null) || (__draft.eventId == null)) || (eventId === __draft.eventId)))) throw new Error("Plan eventId must match the seeded event reference when provided");
    if (!(((plannedYield > 0) || (__draft.plannedYield > 0)))) throw new Error("Planned yield must be positive");
    const doc: Record<string, any> = {
      ...__draft,
      componentId: componentId,
      eventId: ((eventId != null) ? eventId : __draft.eventId),
      plannedYield: ((__draft.plannedAt != null) ? __draft.plannedYield : plannedYield),
      yieldUnit: ((__draft.plannedAt != null) ? __draft.yieldUnit : yieldUnit),
      notes: ((notes != null) ? notes : __draft.notes),
      plannedAt: ((__draft.plannedAt != null) ? __draft.plannedAt : Date.now()),
      version: 1,
    };
    const docId = await ctx.db.insert("productionBatches", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, productionBatchId: docId, tenantId: doc.tenantId, componentId: doc.componentId, eventId: ((doc.eventId != null) ? doc.eventId : doc.eventId), plannedYield: ((doc.plannedAt != null) ? doc.plannedYield : doc.plannedYield), yieldUnit: ((doc.plannedAt != null) ? doc.yieldUnit : doc.yieldUnit), status: "planned", _subject: { entity: "ProductionBatch", command: "plan", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ProductionBatchPlanned", entity: "ProductionBatch", entityId: docId, payload: { productionBatchId: docId, tenantId: doc.tenantId, componentId: doc.componentId, eventId: ((doc.eventId != null) ? doc.eventId : doc.eventId), plannedYield: ((doc.plannedAt != null) ? doc.plannedYield : doc.plannedYield), yieldUnit: ((doc.plannedAt != null) ? doc.yieldUnit : doc.yieldUnit), status: "planned" }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ProductionBatch_createViaPlan", __result);
    }
    return __result;
  },
});

async function __runProductionBatchStart(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ProductionBatch not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ProductionBatch not found");
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) throw new Error("Kitchen staff and managers may read production batches");
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) throw new Error("Kitchen staff and managers may write production batches through commands");
    if (!((checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) throw new Error("Kitchen staff and managers may execute production batch commands");
    if (!((doc.status === "planned"))) throw new Error("Guard 0 failed");
    if (!((doc.plannedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    const previousStatus = doc.status;
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "in_progress";
        const __allowed: Record<string, string[]> = { "planned": ["in_progress", "cancelled"], "in_progress": ["completed", "cancelled"], "completed": [], "cancelled": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "in_progress",
      startedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, productionBatchId: docId, tenantId: __after.tenantId, componentId: __after.componentId, eventId: __after.eventId, plannedYield: __after.plannedYield, yieldUnit: __after.yieldUnit, previousStatus: previousStatus, status: "in_progress", _subject: { entity: "ProductionBatch", command: "start", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ProductionBatchStarted", entity: "ProductionBatch", entityId: docId, payload: { productionBatchId: docId, tenantId: __after.tenantId, componentId: __after.componentId, eventId: __after.eventId, plannedYield: __after.plannedYield, yieldUnit: __after.yieldUnit, previousStatus: previousStatus, status: "in_progress" }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ProductionBatch_start = mutation({
  args: {
    docId: v.id("productionBatches"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runProductionBatchStart(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ProductionBatch_start", __result);
    }
    return __result;
  },
});

async function __runProposalAccept(ctx: MutationCtx, { docId, eventId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Proposal not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Proposal not found");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may read proposals");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may write proposals through commands");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may execute proposal commands");
    if (!(((doc.status === "sent") || (doc.status === "viewed")))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(((doc.expiresAt == null) || (doc.expiresAt > Date.now())))) throw new Error("Guard 2 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "accepted";
        const __allowed: Record<string, string[]> = { "draft": ["sent"], "sent": ["viewed", "accepted", "declined", "expired", "superseded"], "viewed": ["accepted", "declined", "expired", "superseded"], "accepted": [], "declined": [], "expired": [], "superseded": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "accepted",
      eventId: ((eventId != null) ? eventId : doc.eventId),
      acceptedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, proposalId: docId, tenantId: __after.tenantId, clientId: __after.clientId, eventId: ((eventId != null) ? eventId : __after.eventId), title: __after.title, eventDate: __after.eventDate, eventType: __after.eventType, guestCount: __after.guestCount, venueName: __after.venueName, venueAddress: __after.venueAddress, subtotal: __after.subtotal, taxAmount: __after.taxAmount, discountAmount: __after.discountAmount, total: __after.total, dishSelectionProposalId: ((__after.eventId != null) ? docId : null), _subject: { entity: "Proposal", command: "accept", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ProposalAccepted", entity: "Proposal", entityId: docId, payload: { proposalId: docId, tenantId: __after.tenantId, clientId: __after.clientId, eventId: ((eventId != null) ? eventId : __after.eventId), title: __after.title, eventDate: __after.eventDate, eventType: __after.eventType, guestCount: __after.guestCount, venueName: __after.venueName, venueAddress: __after.venueAddress, subtotal: __after.subtotal, taxAmount: __after.taxAmount, discountAmount: __after.discountAmount, total: __after.total, dishSelectionProposalId: ((__after.eventId != null) ? docId : null) }, createdAt: Date.now() });
    // Reactions
    const fanRows0 = (await ctx.db.query("proposalDishSelections").withIndex("by_proposalId", (q) => q.eq("proposalId", payload.dishSelectionProposalId)).collect()).filter((d) => (d as any).deletedAt == null);
    for (const __row of fanRows0) {
      const target = __row;
      const __fanMatch0_raw = await ctx.db.query("eventDishes").withIndex("by_eventId", (q) => q.eq("eventId", payload.eventId)).collect();
      const __fanMatch0_rows = __fanMatch0_raw.filter((d) => (d as any).eventId === payload.eventId && (d as any).dishId === __row.dishId && (d as any).deletedAt == null).sort((a, b) => String((a as any)._id).localeCompare(String((b as any)._id)));
      const __fanMatch0_id = __fanMatch0_rows.length > 0 ? (__fanMatch0_rows[0] as any)._id : null;
      if (__fanMatch0_id) {
        await __runEventDishConfirmFromProposal(ctx, { docId: __fanMatch0_id, eventId: payload.eventId, dishId: __row.dishId, quantityServings: __row.quantityServings, course: __row.course, serviceStyle: __row.serviceStyle, specialInstructions: __row.specialInstructions } as any);
      } else {
        const __elseArgs: Record<string, any> = { eventId: payload.eventId, dishId: __row.dishId, quantityServings: __row.quantityServings, course: __row.course, serviceStyle: __row.serviceStyle, specialInstructions: __row.specialInstructions };
        const __elseDoc: Record<string, any> = {
          tenantId: __auth.tenantId,
          quantityServings: 1,
          headcountOverride: 0,
          createdAt: Date.now(),
          updatedAt: Date.now(),
          version: 0,
        };
        for (const __k of ["deletedAt","eventId","dishId","quantityServings","headcountOverride","purchasingWeekStart","course","serviceStyle","specialInstructions","addedAt","removedAt","createdAt","updatedAt"] as string[]) {
          if (__elseArgs[__k] !== undefined) __elseDoc[__k] = __elseArgs[__k];
        }
        const __elseId = await ctx.db.insert("eventDishes", __elseDoc as any);
        await __runEventDishConfirmFromProposal(ctx, { docId: __elseId, ...__elseArgs } as any);
      }
    }
    return { ...doc, ...updates };
}

export const Proposal_accept = mutation({
  args: {
    docId: v.id("proposals"),
    eventId: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runProposalAccept(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Proposal_accept", __result);
    }
    return __result;
  },
});

async function __runProposalDecline(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Proposal not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Proposal not found");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may read proposals");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may write proposals through commands");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may execute proposal commands");
    if (!(((doc.status === "sent") || (doc.status === "viewed")))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "declined";
        const __allowed: Record<string, string[]> = { "draft": ["sent"], "sent": ["viewed", "accepted", "declined", "expired", "superseded"], "viewed": ["accepted", "declined", "expired", "superseded"], "accepted": [], "declined": [], "expired": [], "superseded": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "declined",
      declinedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, proposalId: docId, tenantId: __after.tenantId, clientId: __after.clientId, _subject: { entity: "Proposal", command: "decline", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ProposalDeclined", entity: "Proposal", entityId: docId, payload: { proposalId: docId, tenantId: __after.tenantId, clientId: __after.clientId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Proposal_decline = mutation({
  args: {
    docId: v.id("proposals"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runProposalDecline(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Proposal_decline", __result);
    }
    return __result;
  },
});

async function __runProposalDraft(ctx: MutationCtx, { docId, clientId, title, subtotal, taxAmount, discountAmount, total, proposalNumber, eventDate, eventType, guestCount, venueName, venueAddress, expiresAt, notes, terms, eventId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Proposal not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Proposal not found");
    const __rel_client = await __resolveRelation(ctx, "clients", [__auth.tenantId, doc.clientId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).clientId;
      ((doc as any) as any).client = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may read proposals");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may write proposals through commands");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may execute proposal commands");
    if (!((doc.draftedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.status === "draft"))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((__rel_client != null))) throw new Error("Guard 3 failed");
    if (!((clientId === doc.clientId))) throw new Error("Draft clientId must match the seeded client reference");
    if (!((((title).trim()).length > 0))) throw new Error("Proposal title is required");
    if (!(((guestCount == null) || (guestCount >= 0)))) throw new Error("Guest count cannot be negative");
    if (!(((((subtotal >= 0) && (taxAmount >= 0)) && (discountAmount >= 0)) && (total >= 0)))) throw new Error("Proposal money amounts cannot be negative");
    if (!((total === ((subtotal + taxAmount) - discountAmount)))) throw new Error("Proposal total must equal subtotal plus tax minus discount");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      clientId: clientId,
      title: title,
      proposalNumber: proposalNumber,
      eventDate: eventDate,
      eventType: eventType,
      guestCount: ((guestCount != null) ? guestCount : 0),
      venueName: venueName,
      venueAddress: venueAddress,
      subtotal: subtotal,
      taxAmount: taxAmount,
      discountAmount: discountAmount,
      total: total,
      expiresAt: expiresAt,
      notes: notes,
      terms: terms,
      eventId: eventId,
      draftedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, proposalId: docId, tenantId: __after.tenantId, clientId: clientId, title: title, total: total, _subject: { entity: "Proposal", command: "draft", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ProposalDrafted", entity: "Proposal", entityId: docId, payload: { proposalId: docId, tenantId: __after.tenantId, clientId: clientId, title: title, total: total }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Proposal_draft = mutation({
  args: {
    docId: v.id("proposals"),
    clientId: v.string(),
    title: v.string(),
    subtotal: v.number(),
    taxAmount: v.number(),
    discountAmount: v.number(),
    total: v.number(),
    proposalNumber: v.optional(v.string()),
    eventDate: v.optional(v.number()),
    eventType: v.optional(v.string()),
    guestCount: v.optional(v.any()),
    venueName: v.optional(v.string()),
    venueAddress: v.optional(v.string()),
    expiresAt: v.optional(v.number()),
    notes: v.optional(v.string()),
    terms: v.optional(v.string()),
    eventId: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runProposalDraft(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Proposal_draft", __result);
    }
    return __result;
  },
});

export const Proposal_createViaDraft = mutation({
  args: {
    clientId: v.string(),
    title: v.string(),
    subtotal: v.number(),
    taxAmount: v.number(),
    discountAmount: v.number(),
    total: v.number(),
    proposalNumber: v.optional(v.string()),
    eventDate: v.optional(v.number()),
    eventType: v.optional(v.string()),
    guestCount: v.optional(v.any()),
    venueName: v.optional(v.string()),
    venueAddress: v.optional(v.string()),
    expiresAt: v.optional(v.number()),
    notes: v.optional(v.string()),
    terms: v.optional(v.string()),
    eventId: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { clientId, title, subtotal, taxAmount, discountAmount, total, proposalNumber, eventDate, eventType, guestCount, venueName, venueAddress, expiresAt, notes, terms, eventId } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      guestCount: args.guestCount !== undefined ? args.guestCount : 0,
      status: "draft",
      createdAt: Date.now(),
      updatedAt: Date.now(),
      clientId: args.clientId,
      discountAmount: args.discountAmount,
      eventDate: args.eventDate,
      eventId: args.eventId,
      eventType: args.eventType,
      expiresAt: args.expiresAt,
      notes: args.notes,
      proposalNumber: args.proposalNumber,
      subtotal: args.subtotal,
      taxAmount: args.taxAmount,
      terms: args.terms,
      title: args.title,
      total: args.total,
      venueAddress: args.venueAddress,
      venueName: args.venueName
    };
    const __rel_client = await __resolveRelation(ctx, "clients", [__auth.tenantId, __draft.clientId], ["tenantId","id"], "tenantId", __auth.tenantId);
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may read proposals");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may write proposals through commands");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may execute proposal commands");
    if (!((__draft.draftedAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.status === "draft"))) throw new Error("Guard 1 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((__rel_client != null))) throw new Error("Guard 3 failed");
    if (!((clientId === __draft.clientId))) throw new Error("Draft clientId must match the seeded client reference");
    if (!((((title).trim()).length > 0))) throw new Error("Proposal title is required");
    if (!(((guestCount == null) || (guestCount >= 0)))) throw new Error("Guest count cannot be negative");
    if (!(((((subtotal >= 0) && (taxAmount >= 0)) && (discountAmount >= 0)) && (total >= 0)))) throw new Error("Proposal money amounts cannot be negative");
    if (!((total === ((subtotal + taxAmount) - discountAmount)))) throw new Error("Proposal total must equal subtotal plus tax minus discount");
    const doc: Record<string, any> = {
      ...__draft,
      clientId: clientId,
      title: title,
      proposalNumber: proposalNumber,
      eventDate: eventDate,
      eventType: eventType,
      guestCount: ((guestCount != null) ? guestCount : 0),
      venueName: venueName,
      venueAddress: venueAddress,
      subtotal: subtotal,
      taxAmount: taxAmount,
      discountAmount: discountAmount,
      total: total,
      expiresAt: expiresAt,
      notes: notes,
      terms: terms,
      eventId: eventId,
      draftedAt: Date.now(),
      version: 1,
    };
    const docId = await ctx.db.insert("proposals", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, proposalId: docId, tenantId: doc.tenantId, clientId: doc.clientId, title: doc.title, total: doc.total, _subject: { entity: "Proposal", command: "draft", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ProposalDrafted", entity: "Proposal", entityId: docId, payload: { proposalId: docId, tenantId: doc.tenantId, clientId: doc.clientId, title: doc.title, total: doc.total }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Proposal_createViaDraft", __result);
    }
    return __result;
  },
});

async function __runProposalExpire(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Proposal not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Proposal not found");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may read proposals");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may write proposals through commands");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may execute proposal commands");
    if (!(((doc.status === "sent") || (doc.status === "viewed")))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "expired";
        const __allowed: Record<string, string[]> = { "draft": ["sent"], "sent": ["viewed", "accepted", "declined", "expired", "superseded"], "viewed": ["accepted", "declined", "expired", "superseded"], "accepted": [], "declined": [], "expired": [], "superseded": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "expired",
      expiredAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, proposalId: docId, tenantId: __after.tenantId, clientId: __after.clientId, _subject: { entity: "Proposal", command: "expire", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ProposalExpired", entity: "Proposal", entityId: docId, payload: { proposalId: docId, tenantId: __after.tenantId, clientId: __after.clientId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Proposal_expire = mutation({
  args: {
    docId: v.id("proposals"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runProposalExpire(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Proposal_expire", __result);
    }
    return __result;
  },
});

async function __runProposalMarkViewed(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Proposal not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Proposal not found");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may read proposals");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may write proposals through commands");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may execute proposal commands");
    if (!((doc.status === "sent"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "viewed";
        const __allowed: Record<string, string[]> = { "draft": ["sent"], "sent": ["viewed", "accepted", "declined", "expired", "superseded"], "viewed": ["accepted", "declined", "expired", "superseded"], "accepted": [], "declined": [], "expired": [], "superseded": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "viewed",
      viewedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, proposalId: docId, tenantId: __after.tenantId, clientId: __after.clientId, _subject: { entity: "Proposal", command: "markViewed", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ProposalViewed", entity: "Proposal", entityId: docId, payload: { proposalId: docId, tenantId: __after.tenantId, clientId: __after.clientId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Proposal_markViewed = mutation({
  args: {
    docId: v.id("proposals"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runProposalMarkViewed(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Proposal_markViewed", __result);
    }
    return __result;
  },
});

async function __runProposalReassignClient(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Proposal not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Proposal not found");
    const __rel_clientMergeAuthorization = await __resolveRelation(ctx, "clientMerges", [__auth.tenantId, doc.clientMergeAuthorizationId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_mergeTargetClient = await __resolveRelation(ctx, "clients", [__auth.tenantId, doc.mergeTargetClientId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).clientMergeAuthorizationId;
      ((doc as any) as any).clientMergeAuthorization = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    {
      const __fk = ((doc as any) as any).mergeTargetClientId;
      ((doc as any) as any).mergeTargetClient = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may read proposals");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may write proposals through commands");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may execute proposal commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!(checkRole(user, "adminAccess"))) throw new Error("Guard 1 failed");
    if (!((__rel_clientMergeAuthorization != null))) throw new Error("Guard 2 failed");
    if (!((__rel_clientMergeAuthorization.deletedAt == null))) throw new Error("Guard 3 failed");
    if (!((__rel_clientMergeAuthorization.mergedAt != null))) throw new Error("Guard 4 failed");
    if (!((__rel_mergeTargetClient != null))) throw new Error("Guard 5 failed");
    if (!((__rel_mergeTargetClient.status === "active"))) throw new Error("Guard 6 failed");
    if (!((__rel_mergeTargetClient.deletedAt == null))) throw new Error("Guard 7 failed");
    if (!((__rel_clientMergeAuthorization.duplicateClientId === doc.clientId))) throw new Error("Merge authorization does not match the proposal client");
    if (!((__rel_clientMergeAuthorization.primaryClientId === doc.mergeTargetClientId))) throw new Error("Merge authorization does not match the primary client");
    const previousClientId = doc.clientId;
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      clientId: doc.mergeTargetClientId,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, proposalId: docId, tenantId: __after.tenantId, previousClientId: previousClientId, clientId: __after.mergeTargetClientId, _subject: { entity: "Proposal", command: "reassignClient", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ProposalClientReassigned", entity: "Proposal", entityId: docId, payload: { proposalId: docId, tenantId: __after.tenantId, previousClientId: previousClientId, clientId: __after.mergeTargetClientId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Proposal_reassignClient = mutation({
  args: {
    docId: v.id("proposals"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runProposalReassignClient(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Proposal_reassignClient", __result);
    }
    return __result;
  },
});

async function __runProposalSend(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Proposal not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Proposal not found");
    const __rel_client = await __resolveRelation(ctx, "clients", [__auth.tenantId, doc.clientId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).clientId;
      ((doc as any) as any).client = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may read proposals");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may write proposals through commands");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may execute proposal commands");
    if (!((doc.status === "draft"))) throw new Error("Guard 0 failed");
    if (!((doc.draftedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!(((__rel_client != null) && (__rel_client.status === "active")))) throw new Error("Guard 3 failed");
    if (!((((doc.title).trim()).length > 0))) throw new Error("Proposal title is required before send");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "sent";
        const __allowed: Record<string, string[]> = { "draft": ["sent"], "sent": ["viewed", "accepted", "declined", "expired", "superseded"], "viewed": ["accepted", "declined", "expired", "superseded"], "accepted": [], "declined": [], "expired": [], "superseded": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "sent",
      sentAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, proposalId: docId, tenantId: __after.tenantId, clientId: __after.clientId, title: __after.title, total: __after.total, _subject: { entity: "Proposal", command: "send", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ProposalSent", entity: "Proposal", entityId: docId, payload: { proposalId: docId, tenantId: __after.tenantId, clientId: __after.clientId, title: __after.title, total: __after.total }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Proposal_send = mutation({
  args: {
    docId: v.id("proposals"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runProposalSend(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Proposal_send", __result);
    }
    return __result;
  },
});

async function __runProposalStageClientMerge(ctx: MutationCtx, { docId, clientMergeId, clientId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Proposal not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Proposal not found");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may read proposals");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may write proposals through commands");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may execute proposal commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!(checkRole(user, "adminAccess"))) throw new Error("Guard 1 failed");
    if (!((clientId !== doc.clientId))) throw new Error("Proposal already belongs to this client");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      clientMergeAuthorizationId: clientMergeId,
      mergeTargetClientId: clientId,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, proposalId: docId, tenantId: __after.tenantId, _subject: { entity: "Proposal", command: "stageClientMerge", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ProposalClientMergeStaged", entity: "Proposal", entityId: docId, payload: { proposalId: docId, tenantId: __after.tenantId }, createdAt: Date.now() });
    // Reactions
    const reactionTarget0 = payload.proposalId;
    if (reactionTarget0) await __runProposalReassignClient(ctx, { docId: reactionTarget0 } as any);
    return { ...doc, ...updates };
}

export const Proposal_stageClientMerge = mutation({
  args: {
    docId: v.id("proposals"),
    clientMergeId: v.string(),
    clientId: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runProposalStageClientMerge(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Proposal_stageClientMerge", __result);
    }
    return __result;
  },
});

async function __runProposalSupersede(ctx: MutationCtx, { docId, revisedById, reason, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Proposal not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Proposal not found");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may read proposals");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may write proposals through commands");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may execute proposal commands");
    if (!(((doc.status === "sent") || (doc.status === "viewed")))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((revisedById !== doc.id))) throw new Error("A proposal cannot supersede itself");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "superseded";
        const __allowed: Record<string, string[]> = { "draft": ["sent"], "sent": ["viewed", "accepted", "declined", "expired", "superseded"], "viewed": ["accepted", "declined", "expired", "superseded"], "accepted": [], "declined": [], "expired": [], "superseded": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "superseded",
      supersededAt: Date.now(),
      supersedeReason: reason,
      supersededById: revisedById,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, proposalId: docId, tenantId: __after.tenantId, supersededById: revisedById, reason: reason, supersededAt: Date.now(), _subject: { entity: "Proposal", command: "supersede", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ProposalSuperseded", entity: "Proposal", entityId: docId, payload: { proposalId: docId, tenantId: __after.tenantId, supersededById: revisedById, reason: reason, supersededAt: Date.now() }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Proposal_supersede = mutation({
  args: {
    docId: v.id("proposals"),
    revisedById: v.string(),
    reason: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runProposalSupersede(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Proposal_supersede", __result);
    }
    return __result;
  },
});

async function __runProposalDishSelectionAdjustServings(ctx: MutationCtx, { docId, quantityServings, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ProposalDishSelection not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ProposalDishSelection not found");
    const __rel_proposal = await __resolveRelation(ctx, "proposals", [__auth.tenantId, doc.proposalId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).proposalId;
      ((doc as any) as any).proposal = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may read proposal dish selections");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may write proposal dish selections through commands");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may execute proposal dish selection commands");
    if (!((doc.selectedAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(((__rel_proposal != null) && (((__rel_proposal.status === "draft") || (__rel_proposal.status === "sent")) || (__rel_proposal.status === "viewed"))))) throw new Error("Guard 2 failed");
    if (!((quantityServings > 0))) throw new Error("Selection servings must be positive");
    const previousQuantityServings = doc.quantityServings;
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      quantityServings: quantityServings,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, selectionId: docId, tenantId: __after.tenantId, proposalId: __after.proposalId, dishId: __after.dishId, previousQuantityServings: previousQuantityServings, quantityServings: quantityServings, _subject: { entity: "ProposalDishSelection", command: "adjustServings", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ProposalDishSelectionServingsAdjusted", entity: "ProposalDishSelection", entityId: docId, payload: { selectionId: docId, tenantId: __after.tenantId, proposalId: __after.proposalId, dishId: __after.dishId, previousQuantityServings: previousQuantityServings, quantityServings: quantityServings }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ProposalDishSelection_adjustServings = mutation({
  args: {
    docId: v.id("proposalDishSelections"),
    quantityServings: v.any(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runProposalDishSelectionAdjustServings(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ProposalDishSelection_adjustServings", __result);
    }
    return __result;
  },
});

async function __runProposalDishSelectionRemove(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ProposalDishSelection not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ProposalDishSelection not found");
    const __rel_proposal = await __resolveRelation(ctx, "proposals", [__auth.tenantId, doc.proposalId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).proposalId;
      ((doc as any) as any).proposal = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may read proposal dish selections");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may write proposal dish selections through commands");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may execute proposal dish selection commands");
    if (!((doc.selectedAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(((__rel_proposal != null) && (((__rel_proposal.status === "draft") || (__rel_proposal.status === "sent")) || (__rel_proposal.status === "viewed"))))) throw new Error("Guard 2 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      deletedAt: Date.now(),
      removedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, selectionId: docId, tenantId: __after.tenantId, proposalId: __after.proposalId, dishId: __after.dishId, _subject: { entity: "ProposalDishSelection", command: "remove", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ProposalDishSelectionRemoved", entity: "ProposalDishSelection", entityId: docId, payload: { selectionId: docId, tenantId: __after.tenantId, proposalId: __after.proposalId, dishId: __after.dishId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ProposalDishSelection_remove = mutation({
  args: {
    docId: v.id("proposalDishSelections"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runProposalDishSelectionRemove(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ProposalDishSelection_remove", __result);
    }
    return __result;
  },
});

async function __runProposalDishSelectionSelect(ctx: MutationCtx, { docId, proposalId, menuId, dishId, quantityServings, course, serviceStyle, specialInstructions, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ProposalDishSelection not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ProposalDishSelection not found");
    const __rel_proposal = await __resolveRelation(ctx, "proposals", [__auth.tenantId, doc.proposalId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_menu = await __resolveRelation(ctx, "menus", [__auth.tenantId, doc.menuId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_dish = await __resolveRelation(ctx, "dishes", [__auth.tenantId, doc.dishId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).proposalId;
      ((doc as any) as any).proposal = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    {
      const __fk = ((doc as any) as any).menuId;
      ((doc as any) as any).menu = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    {
      const __fk = ((doc as any) as any).dishId;
      ((doc as any) as any).dish = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may read proposal dish selections");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may write proposal dish selections through commands");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may execute proposal dish selection commands");
    if (!((doc.selectedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(((__rel_proposal != null) && (((__rel_proposal.status === "draft") || (__rel_proposal.status === "sent")) || (__rel_proposal.status === "viewed"))))) throw new Error("Guard 2 failed");
    if (!(((__rel_menu != null) && (__rel_menu.status === "published")))) throw new Error("Guard 3 failed");
    if (!(((__rel_dish != null) && (__rel_dish.status === "active")))) throw new Error("Guard 4 failed");
    if (!((proposalId === doc.proposalId))) throw new Error("Select proposalId must match the seeded proposal reference");
    if (!((quantityServings > 0))) throw new Error("Selection servings must be positive");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      proposalId: proposalId,
      menuId: menuId,
      dishId: dishId,
      quantityServings: quantityServings,
      course: course,
      serviceStyle: serviceStyle,
      specialInstructions: specialInstructions,
      selectedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, selectionId: docId, tenantId: __after.tenantId, proposalId: proposalId, menuId: menuId, dishId: dishId, quantityServings: quantityServings, _subject: { entity: "ProposalDishSelection", command: "select", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ProposalDishSelected", entity: "ProposalDishSelection", entityId: docId, payload: { selectionId: docId, tenantId: __after.tenantId, proposalId: proposalId, menuId: menuId, dishId: dishId, quantityServings: quantityServings }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ProposalDishSelection_select = mutation({
  args: {
    docId: v.id("proposalDishSelections"),
    proposalId: v.string(),
    menuId: v.string(),
    dishId: v.string(),
    quantityServings: v.any(),
    course: v.optional(v.string()),
    serviceStyle: v.optional(v.string()),
    specialInstructions: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runProposalDishSelectionSelect(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ProposalDishSelection_select", __result);
    }
    return __result;
  },
});

export const ProposalDishSelection_createViaSelect = mutation({
  args: {
    proposalId: v.string(),
    menuId: v.string(),
    dishId: v.string(),
    quantityServings: v.any(),
    course: v.optional(v.string()),
    serviceStyle: v.optional(v.string()),
    specialInstructions: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { proposalId, menuId, dishId, quantityServings, course, serviceStyle, specialInstructions } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      createdAt: Date.now(),
      updatedAt: Date.now(),
      course: args.course,
      dishId: args.dishId,
      menuId: args.menuId,
      proposalId: args.proposalId,
      quantityServings: args.quantityServings,
      serviceStyle: args.serviceStyle,
      specialInstructions: args.specialInstructions
    };
    const __rel_proposal = await __resolveRelation(ctx, "proposals", [__auth.tenantId, __draft.proposalId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_menu = await __resolveRelation(ctx, "menus", [__auth.tenantId, __draft.menuId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_dish = await __resolveRelation(ctx, "dishes", [__auth.tenantId, __draft.dishId], ["tenantId","id"], "tenantId", __auth.tenantId);
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may read proposal dish selections");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may write proposal dish selections through commands");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may execute proposal dish selection commands");
    if (!((__draft.selectedAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(((__rel_proposal != null) && (((__rel_proposal.status === "draft") || (__rel_proposal.status === "sent")) || (__rel_proposal.status === "viewed"))))) throw new Error("Guard 2 failed");
    if (!(((__rel_menu != null) && (__rel_menu.status === "published")))) throw new Error("Guard 3 failed");
    if (!(((__rel_dish != null) && (__rel_dish.status === "active")))) throw new Error("Guard 4 failed");
    if (!((proposalId === __draft.proposalId))) throw new Error("Select proposalId must match the seeded proposal reference");
    if (!((quantityServings > 0))) throw new Error("Selection servings must be positive");
    const doc: Record<string, any> = {
      ...__draft,
      proposalId: proposalId,
      menuId: menuId,
      dishId: dishId,
      quantityServings: quantityServings,
      course: course,
      serviceStyle: serviceStyle,
      specialInstructions: specialInstructions,
      selectedAt: Date.now(),
      version: 1,
    };
    const docId = await ctx.db.insert("proposalDishSelections", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, selectionId: docId, tenantId: doc.tenantId, proposalId: doc.proposalId, menuId: doc.menuId, dishId: doc.dishId, quantityServings: doc.quantityServings, _subject: { entity: "ProposalDishSelection", command: "select", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ProposalDishSelected", entity: "ProposalDishSelection", entityId: docId, payload: { selectionId: docId, tenantId: doc.tenantId, proposalId: doc.proposalId, menuId: doc.menuId, dishId: doc.dishId, quantityServings: doc.quantityServings }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ProposalDishSelection_createViaSelect", __result);
    }
    return __result;
  },
});

async function __runProposalEnhancementOffer(ctx: MutationCtx, { docId, proposalId, name, price, description, sortOrder, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ProposalEnhancement not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ProposalEnhancement not found");
    const __rel_proposal = await __resolveRelation(ctx, "proposals", [__auth.tenantId, doc.proposalId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).proposalId;
      ((doc as any) as any).proposal = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may read proposal enhancements");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may write proposal enhancements through commands");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may execute proposal enhancement commands");
    if (!((doc.addedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(((__rel_proposal != null) && (__rel_proposal.status === "draft")))) throw new Error("Guard 2 failed");
    if (!((proposalId === doc.proposalId))) throw new Error("Offer proposalId must match the seeded proposal reference");
    if (!((((name).trim()).length > 0))) throw new Error("Enhancement name is required");
    if (!((price >= 0))) throw new Error("Enhancement price cannot be negative");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      proposalId: proposalId,
      name: name,
      description: description,
      price: price,
      sortOrder: ((sortOrder != null) ? sortOrder : 0),
      addedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, enhancementId: docId, tenantId: __after.tenantId, proposalId: proposalId, name: name, price: price, _subject: { entity: "ProposalEnhancement", command: "offer", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ProposalEnhancementOffered", entity: "ProposalEnhancement", entityId: docId, payload: { enhancementId: docId, tenantId: __after.tenantId, proposalId: proposalId, name: name, price: price }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ProposalEnhancement_offer = mutation({
  args: {
    docId: v.id("proposalEnhancements"),
    proposalId: v.string(),
    name: v.string(),
    price: v.number(),
    description: v.optional(v.string()),
    sortOrder: v.optional(v.number()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runProposalEnhancementOffer(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ProposalEnhancement_offer", __result);
    }
    return __result;
  },
});

export const ProposalEnhancement_createViaOffer = mutation({
  args: {
    proposalId: v.string(),
    name: v.string(),
    price: v.number(),
    description: v.optional(v.string()),
    sortOrder: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { proposalId, name, price, description, sortOrder } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      sortOrder: args.sortOrder !== undefined ? args.sortOrder : 0,
      createdAt: Date.now(),
      updatedAt: Date.now(),
      description: args.description,
      name: args.name,
      price: args.price,
      proposalId: args.proposalId
    };
    const __rel_proposal = await __resolveRelation(ctx, "proposals", [__auth.tenantId, __draft.proposalId], ["tenantId","id"], "tenantId", __auth.tenantId);
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may read proposal enhancements");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may write proposal enhancements through commands");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may execute proposal enhancement commands");
    if (!((__draft.addedAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(((__rel_proposal != null) && (__rel_proposal.status === "draft")))) throw new Error("Guard 2 failed");
    if (!((proposalId === __draft.proposalId))) throw new Error("Offer proposalId must match the seeded proposal reference");
    if (!((((name).trim()).length > 0))) throw new Error("Enhancement name is required");
    if (!((price >= 0))) throw new Error("Enhancement price cannot be negative");
    const doc: Record<string, any> = {
      ...__draft,
      proposalId: proposalId,
      name: name,
      description: description,
      price: price,
      sortOrder: ((sortOrder != null) ? sortOrder : 0),
      addedAt: Date.now(),
      version: 1,
    };
    const docId = await ctx.db.insert("proposalEnhancements", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, enhancementId: docId, tenantId: doc.tenantId, proposalId: doc.proposalId, name: doc.name, price: doc.price, _subject: { entity: "ProposalEnhancement", command: "offer", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ProposalEnhancementOffered", entity: "ProposalEnhancement", entityId: docId, payload: { enhancementId: docId, tenantId: doc.tenantId, proposalId: doc.proposalId, name: doc.name, price: doc.price }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ProposalEnhancement_createViaOffer", __result);
    }
    return __result;
  },
});

async function __runProposalEnhancementRevise(ctx: MutationCtx, { docId, name, price, description, sortOrder, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ProposalEnhancement not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ProposalEnhancement not found");
    const __rel_proposal = await __resolveRelation(ctx, "proposals", [__auth.tenantId, doc.proposalId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).proposalId;
      ((doc as any) as any).proposal = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may read proposal enhancements");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may write proposal enhancements through commands");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may execute proposal enhancement commands");
    if (!((doc.addedAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.removedAt == null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!(((__rel_proposal != null) && (__rel_proposal.status === "draft")))) throw new Error("Guard 3 failed");
    if (!((((name).trim()).length > 0))) throw new Error("Enhancement name is required");
    if (!((price >= 0))) throw new Error("Enhancement price cannot be negative");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      name: name,
      description: description,
      price: price,
      sortOrder: ((sortOrder != null) ? sortOrder : doc.sortOrder),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, enhancementId: docId, tenantId: __after.tenantId, proposalId: __after.proposalId, name: name, price: price, _subject: { entity: "ProposalEnhancement", command: "revise", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ProposalEnhancementRevised", entity: "ProposalEnhancement", entityId: docId, payload: { enhancementId: docId, tenantId: __after.tenantId, proposalId: __after.proposalId, name: name, price: price }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ProposalEnhancement_revise = mutation({
  args: {
    docId: v.id("proposalEnhancements"),
    name: v.string(),
    price: v.number(),
    description: v.optional(v.string()),
    sortOrder: v.optional(v.number()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runProposalEnhancementRevise(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ProposalEnhancement_revise", __result);
    }
    return __result;
  },
});

async function __runProposalEnhancementWithdraw(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ProposalEnhancement not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ProposalEnhancement not found");
    const __rel_proposal = await __resolveRelation(ctx, "proposals", [__auth.tenantId, doc.proposalId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).proposalId;
      ((doc as any) as any).proposal = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may read proposal enhancements");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may write proposal enhancements through commands");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may execute proposal enhancement commands");
    if (!((doc.addedAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.removedAt == null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!(((__rel_proposal != null) && (__rel_proposal.status === "draft")))) throw new Error("Guard 3 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      removedAt: Date.now(),
      deletedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, enhancementId: docId, tenantId: __after.tenantId, proposalId: __after.proposalId, name: __after.name, _subject: { entity: "ProposalEnhancement", command: "withdraw", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ProposalEnhancementWithdrawn", entity: "ProposalEnhancement", entityId: docId, payload: { enhancementId: docId, tenantId: __after.tenantId, proposalId: __after.proposalId, name: __after.name }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ProposalEnhancement_withdraw = mutation({
  args: {
    docId: v.id("proposalEnhancements"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runProposalEnhancementWithdraw(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ProposalEnhancement_withdraw", __result);
    }
    return __result;
  },
});

async function __runProposalLineItemAddLine(ctx: MutationCtx, { docId, proposalId, description, pricingBasis, unitPrice, amount, quantity, unit, sortOrder, notes, menuDishId, overrideReason, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ProposalLineItem not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ProposalLineItem not found");
    const __rel_proposal = await __resolveRelation(ctx, "proposals", [__auth.tenantId, doc.proposalId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).proposalId;
      ((doc as any) as any).proposal = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may read proposal line items");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may write proposal line items through commands");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may execute proposal line item commands");
    if (!((doc.addedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(((__rel_proposal != null) && (__rel_proposal.status === "draft")))) throw new Error("Guard 2 failed");
    if (!((proposalId === doc.proposalId))) throw new Error("Add line proposalId must match the seeded proposal reference");
    if (!((((description).trim()).length > 0))) throw new Error("Line description is required");
    if (!(((unitPrice >= 0) && (amount >= 0)))) throw new Error("Line money cannot be negative");
    if (!(((quantity == null) || (quantity >= 0)))) throw new Error("Line quantity cannot be negative");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      proposalId: proposalId,
      description: description,
      pricingBasis: pricingBasis,
      unitPrice: unitPrice,
      quantity: ((quantity != null) ? quantity : 1),
      unit: unit,
      amount: amount,
      sortOrder: ((sortOrder != null) ? sortOrder : 0),
      notes: notes,
      menuDishId: menuDishId,
      overrideReason: overrideReason,
      addedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, lineItemId: docId, tenantId: __after.tenantId, proposalId: proposalId, description: description, pricingBasis: pricingBasis, unitPrice: unitPrice, amount: amount, _subject: { entity: "ProposalLineItem", command: "addLine", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ProposalLineItemAdded", entity: "ProposalLineItem", entityId: docId, payload: { lineItemId: docId, tenantId: __after.tenantId, proposalId: proposalId, description: description, pricingBasis: pricingBasis, unitPrice: unitPrice, amount: amount }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ProposalLineItem_addLine = mutation({
  args: {
    docId: v.id("proposalLineItems"),
    proposalId: v.string(),
    description: v.string(),
    pricingBasis: v.any(),
    unitPrice: v.number(),
    amount: v.number(),
    quantity: v.optional(v.number()),
    unit: v.optional(v.string()),
    sortOrder: v.optional(v.number()),
    notes: v.optional(v.string()),
    menuDishId: v.optional(v.string()),
    overrideReason: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runProposalLineItemAddLine(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ProposalLineItem_addLine", __result);
    }
    return __result;
  },
});

export const ProposalLineItem_createViaAddLine = mutation({
  args: {
    proposalId: v.string(),
    description: v.string(),
    pricingBasis: v.any(),
    unitPrice: v.number(),
    amount: v.number(),
    quantity: v.optional(v.number()),
    unit: v.optional(v.string()),
    sortOrder: v.optional(v.number()),
    notes: v.optional(v.string()),
    menuDishId: v.optional(v.string()),
    overrideReason: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { proposalId, description, pricingBasis, unitPrice, amount, quantity, unit, sortOrder, notes, menuDishId, overrideReason } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      quantity: args.quantity !== undefined ? args.quantity : 1,
      sortOrder: args.sortOrder !== undefined ? args.sortOrder : 0,
      createdAt: Date.now(),
      updatedAt: Date.now(),
      amount: args.amount,
      description: args.description,
      menuDishId: args.menuDishId,
      notes: args.notes,
      overrideReason: args.overrideReason,
      pricingBasis: args.pricingBasis,
      proposalId: args.proposalId,
      unit: args.unit,
      unitPrice: args.unitPrice
    };
    const __rel_proposal = await __resolveRelation(ctx, "proposals", [__auth.tenantId, __draft.proposalId], ["tenantId","id"], "tenantId", __auth.tenantId);
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may read proposal line items");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may write proposal line items through commands");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may execute proposal line item commands");
    if (!((__draft.addedAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(((__rel_proposal != null) && (__rel_proposal.status === "draft")))) throw new Error("Guard 2 failed");
    if (!((proposalId === __draft.proposalId))) throw new Error("Add line proposalId must match the seeded proposal reference");
    if (!((((description).trim()).length > 0))) throw new Error("Line description is required");
    if (!(((unitPrice >= 0) && (amount >= 0)))) throw new Error("Line money cannot be negative");
    if (!(((quantity == null) || (quantity >= 0)))) throw new Error("Line quantity cannot be negative");
    const doc: Record<string, any> = {
      ...__draft,
      proposalId: proposalId,
      description: description,
      pricingBasis: pricingBasis,
      unitPrice: unitPrice,
      quantity: ((quantity != null) ? quantity : 1),
      unit: unit,
      amount: amount,
      sortOrder: ((sortOrder != null) ? sortOrder : 0),
      notes: notes,
      menuDishId: menuDishId,
      overrideReason: overrideReason,
      addedAt: Date.now(),
      version: 1,
    };
    const docId = await ctx.db.insert("proposalLineItems", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, lineItemId: docId, tenantId: doc.tenantId, proposalId: doc.proposalId, description: doc.description, pricingBasis: doc.pricingBasis, unitPrice: doc.unitPrice, amount: doc.amount, _subject: { entity: "ProposalLineItem", command: "addLine", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ProposalLineItemAdded", entity: "ProposalLineItem", entityId: docId, payload: { lineItemId: docId, tenantId: doc.tenantId, proposalId: doc.proposalId, description: doc.description, pricingBasis: doc.pricingBasis, unitPrice: doc.unitPrice, amount: doc.amount }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ProposalLineItem_createViaAddLine", __result);
    }
    return __result;
  },
});

async function __runProposalLineItemRemoveLine(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ProposalLineItem not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ProposalLineItem not found");
    const __rel_proposal = await __resolveRelation(ctx, "proposals", [__auth.tenantId, doc.proposalId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).proposalId;
      ((doc as any) as any).proposal = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may read proposal line items");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may write proposal line items through commands");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may execute proposal line item commands");
    if (!((doc.addedAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(((__rel_proposal != null) && (__rel_proposal.status === "draft")))) throw new Error("Guard 2 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      deletedAt: Date.now(),
      removedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, lineItemId: docId, tenantId: __after.tenantId, proposalId: __after.proposalId, _subject: { entity: "ProposalLineItem", command: "removeLine", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ProposalLineItemRemoved", entity: "ProposalLineItem", entityId: docId, payload: { lineItemId: docId, tenantId: __after.tenantId, proposalId: __after.proposalId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ProposalLineItem_removeLine = mutation({
  args: {
    docId: v.id("proposalLineItems"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runProposalLineItemRemoveLine(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ProposalLineItem_removeLine", __result);
    }
    return __result;
  },
});

async function __runProposalLineItemReviseLine(ctx: MutationCtx, { docId, description, pricingBasis, unitPrice, amount, quantity, unit, sortOrder, notes, menuDishId, overrideReason, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ProposalLineItem not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ProposalLineItem not found");
    const __rel_proposal = await __resolveRelation(ctx, "proposals", [__auth.tenantId, doc.proposalId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).proposalId;
      ((doc as any) as any).proposal = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may read proposal line items");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may write proposal line items through commands");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may execute proposal line item commands");
    if (!((doc.addedAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(((__rel_proposal != null) && (__rel_proposal.status === "draft")))) throw new Error("Guard 2 failed");
    if (!((((description).trim()).length > 0))) throw new Error("Line description is required");
    if (!(((unitPrice >= 0) && (amount >= 0)))) throw new Error("Line money cannot be negative");
    if (!(((quantity == null) || (quantity >= 0)))) throw new Error("Line quantity cannot be negative");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      description: description,
      pricingBasis: pricingBasis,
      unitPrice: unitPrice,
      quantity: ((quantity != null) ? quantity : doc.quantity),
      unit: ((unit != null) ? unit : doc.unit),
      amount: amount,
      sortOrder: ((sortOrder != null) ? sortOrder : doc.sortOrder),
      notes: notes,
      menuDishId: menuDishId,
      overrideReason: overrideReason,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, lineItemId: docId, tenantId: __after.tenantId, proposalId: __after.proposalId, description: description, pricingBasis: pricingBasis, unitPrice: unitPrice, amount: amount, _subject: { entity: "ProposalLineItem", command: "reviseLine", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ProposalLineItemRevised", entity: "ProposalLineItem", entityId: docId, payload: { lineItemId: docId, tenantId: __after.tenantId, proposalId: __after.proposalId, description: description, pricingBasis: pricingBasis, unitPrice: unitPrice, amount: amount }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ProposalLineItem_reviseLine = mutation({
  args: {
    docId: v.id("proposalLineItems"),
    description: v.string(),
    pricingBasis: v.any(),
    unitPrice: v.number(),
    amount: v.number(),
    quantity: v.optional(v.number()),
    unit: v.optional(v.string()),
    sortOrder: v.optional(v.number()),
    notes: v.optional(v.string()),
    menuDishId: v.optional(v.string()),
    overrideReason: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runProposalLineItemReviseLine(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ProposalLineItem_reviseLine", __result);
    }
    return __result;
  },
});

async function __runProposalRevisionCapture(ctx: MutationCtx, { docId, proposalId, revisionNumber, capturedByName, changeSummary, snapshot, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ProposalRevision not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ProposalRevision not found");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may read proposal revisions");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may write proposal revisions through commands");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may execute proposal revision commands");
    if (!((doc.capturedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((user.id != null))) throw new Error("Guard 2 failed");
    if (!((((snapshot).trim()).length > 0))) throw new Error("Snapshot payload is required");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      proposalId: proposalId,
      revisionNumber: revisionNumber,
      capturedByName: capturedByName,
      capturedByAuthSubjectId: user.id,
      changeSummary: changeSummary,
      snapshot: snapshot,
      capturedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, revisionId: docId, tenantId: __after.tenantId, proposalId: proposalId, revisionNumber: revisionNumber, changeSummary: changeSummary, capturedByName: capturedByName, capturedAt: Date.now(), _subject: { entity: "ProposalRevision", command: "capture", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ProposalRevisionCaptured", entity: "ProposalRevision", entityId: docId, payload: { revisionId: docId, tenantId: __after.tenantId, proposalId: proposalId, revisionNumber: revisionNumber, changeSummary: changeSummary, capturedByName: capturedByName, capturedAt: Date.now() }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ProposalRevision_capture = mutation({
  args: {
    docId: v.id("proposalRevisions"),
    proposalId: v.string(),
    revisionNumber: v.number(),
    capturedByName: v.string(),
    changeSummary: v.string(),
    snapshot: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runProposalRevisionCapture(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ProposalRevision_capture", __result);
    }
    return __result;
  },
});

export const ProposalRevision_createViaCapture = mutation({
  args: {
    proposalId: v.string(),
    revisionNumber: v.number(),
    capturedByName: v.string(),
    changeSummary: v.string(),
    snapshot: v.string(),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { proposalId, revisionNumber, capturedByName, changeSummary, snapshot } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      createdAt: Date.now(),
      updatedAt: Date.now(),
      capturedByName: args.capturedByName,
      changeSummary: args.changeSummary,
      proposalId: args.proposalId,
      revisionNumber: args.revisionNumber,
      snapshot: args.snapshot
    };
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may read proposal revisions");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may write proposal revisions through commands");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may execute proposal revision commands");
    if (!((__draft.capturedAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((user.id != null))) throw new Error("Guard 2 failed");
    if (!((((snapshot).trim()).length > 0))) throw new Error("Snapshot payload is required");
    const doc: Record<string, any> = {
      ...__draft,
      proposalId: proposalId,
      revisionNumber: revisionNumber,
      capturedByName: capturedByName,
      capturedByAuthSubjectId: user.id,
      changeSummary: changeSummary,
      snapshot: snapshot,
      capturedAt: Date.now(),
      version: 1,
    };
    const docId = await ctx.db.insert("proposalRevisions", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, revisionId: docId, tenantId: doc.tenantId, proposalId: doc.proposalId, revisionNumber: doc.revisionNumber, changeSummary: doc.changeSummary, capturedByName: doc.capturedByName, capturedAt: Date.now(), _subject: { entity: "ProposalRevision", command: "capture", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ProposalRevisionCaptured", entity: "ProposalRevision", entityId: docId, payload: { revisionId: docId, tenantId: doc.tenantId, proposalId: doc.proposalId, revisionNumber: doc.revisionNumber, changeSummary: doc.changeSummary, capturedByName: doc.capturedByName, capturedAt: Date.now() }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ProposalRevision_createViaCapture", __result);
    }
    return __result;
  },
});

async function __runProposalTemplateArchive(ctx: MutationCtx, { docId, reason, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ProposalTemplate not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ProposalTemplate not found");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may read proposal templates");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may write proposal templates through commands");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may execute proposal template commands");
    if (!((doc.status === "active"))) throw new Error("Guard 0 failed");
    if (!((doc.definedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((user.id != null))) throw new Error("Guard 3 failed");
    if (!((((reason).trim()).length > 0))) throw new Error("Archive reason is required");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "archived";
        const __allowed: Record<string, string[]> = { "active": ["archived"], "archived": ["active"] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "archived",
      archivedAt: Date.now(),
      archiveReason: reason,
      archivedById: user.id,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, templateId: docId, tenantId: __after.tenantId, reason: reason, _subject: { entity: "ProposalTemplate", command: "archive", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ProposalTemplateArchived", entity: "ProposalTemplate", entityId: docId, payload: { templateId: docId, tenantId: __after.tenantId, reason: reason }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ProposalTemplate_archive = mutation({
  args: {
    docId: v.id("proposalTemplates"),
    reason: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runProposalTemplateArchive(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ProposalTemplate_archive", __result);
    }
    return __result;
  },
});

async function __runProposalTemplateDefine(ctx: MutationCtx, { docId, name, description, visibleSections, defaultTerms, defaultNotes, defaultTaxRate, defaultServiceChargePercent, validityDays, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ProposalTemplate not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ProposalTemplate not found");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may read proposal templates");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may write proposal templates through commands");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may execute proposal template commands");
    if (!((doc.definedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((user.id != null))) throw new Error("Guard 2 failed");
    if (!((((name).trim()).length > 0))) throw new Error("Template name is required");
    if (!(((defaultTaxRate == null) || ((defaultTaxRate >= 0) && (defaultTaxRate <= 1))))) throw new Error("Tax rate must be between 0 and 100%");
    if (!(((defaultServiceChargePercent == null) || ((defaultServiceChargePercent >= 0) && (defaultServiceChargePercent <= 1))))) throw new Error("Service charge must be between 0 and 100%");
    if (!(((validityDays == null) || ((validityDays >= 1) && (validityDays <= 365))))) throw new Error("Validity days must be between 1 and 365");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      name: name,
      description: description,
      visibleSections: ((visibleSections != null) ? visibleSections : []),
      defaultTerms: defaultTerms,
      defaultNotes: defaultNotes,
      defaultTaxRate: defaultTaxRate,
      defaultServiceChargePercent: defaultServiceChargePercent,
      validityDays: validityDays,
      definedAt: Date.now(),
      definedById: user.id,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, templateId: docId, tenantId: __after.tenantId, name: name, visibleSections: ((visibleSections != null) ? visibleSections : []), _subject: { entity: "ProposalTemplate", command: "define", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ProposalTemplateDefined", entity: "ProposalTemplate", entityId: docId, payload: { templateId: docId, tenantId: __after.tenantId, name: name, visibleSections: ((visibleSections != null) ? visibleSections : []) }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ProposalTemplate_define = mutation({
  args: {
    docId: v.id("proposalTemplates"),
    name: v.string(),
    description: v.optional(v.string()),
    visibleSections: v.optional(v.array(v.string())),
    defaultTerms: v.optional(v.string()),
    defaultNotes: v.optional(v.string()),
    defaultTaxRate: v.optional(v.number()),
    defaultServiceChargePercent: v.optional(v.number()),
    validityDays: v.optional(v.number()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runProposalTemplateDefine(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ProposalTemplate_define", __result);
    }
    return __result;
  },
});

export const ProposalTemplate_createViaDefine = mutation({
  args: {
    name: v.string(),
    description: v.optional(v.string()),
    visibleSections: v.optional(v.array(v.string())),
    defaultTerms: v.optional(v.string()),
    defaultNotes: v.optional(v.string()),
    defaultTaxRate: v.optional(v.number()),
    defaultServiceChargePercent: v.optional(v.number()),
    validityDays: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { name, description, visibleSections, defaultTerms, defaultNotes, defaultTaxRate, defaultServiceChargePercent, validityDays } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      visibleSections: args.visibleSections !== undefined ? args.visibleSections : [],
      status: "active",
      createdAt: Date.now(),
      updatedAt: Date.now(),
      defaultNotes: args.defaultNotes,
      defaultServiceChargePercent: args.defaultServiceChargePercent,
      defaultTaxRate: args.defaultTaxRate,
      defaultTerms: args.defaultTerms,
      description: args.description,
      name: args.name,
      validityDays: args.validityDays
    };
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may read proposal templates");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may write proposal templates through commands");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may execute proposal template commands");
    if (!((__draft.definedAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((user.id != null))) throw new Error("Guard 2 failed");
    if (!((((name).trim()).length > 0))) throw new Error("Template name is required");
    if (!(((defaultTaxRate == null) || ((defaultTaxRate >= 0) && (defaultTaxRate <= 1))))) throw new Error("Tax rate must be between 0 and 100%");
    if (!(((defaultServiceChargePercent == null) || ((defaultServiceChargePercent >= 0) && (defaultServiceChargePercent <= 1))))) throw new Error("Service charge must be between 0 and 100%");
    if (!(((validityDays == null) || ((validityDays >= 1) && (validityDays <= 365))))) throw new Error("Validity days must be between 1 and 365");
    const doc: Record<string, any> = {
      ...__draft,
      name: name,
      description: description,
      visibleSections: ((visibleSections != null) ? visibleSections : []),
      defaultTerms: defaultTerms,
      defaultNotes: defaultNotes,
      defaultTaxRate: defaultTaxRate,
      defaultServiceChargePercent: defaultServiceChargePercent,
      validityDays: validityDays,
      definedAt: Date.now(),
      definedById: user.id,
      version: 1,
    };
    const docId = await ctx.db.insert("proposalTemplates", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, templateId: docId, tenantId: doc.tenantId, name: doc.name, visibleSections: ((doc.visibleSections != null) ? doc.visibleSections : []), _subject: { entity: "ProposalTemplate", command: "define", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ProposalTemplateDefined", entity: "ProposalTemplate", entityId: docId, payload: { templateId: docId, tenantId: doc.tenantId, name: doc.name, visibleSections: ((doc.visibleSections != null) ? doc.visibleSections : []) }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ProposalTemplate_createViaDefine", __result);
    }
    return __result;
  },
});

async function __runProposalTemplateReactivate(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ProposalTemplate not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ProposalTemplate not found");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may read proposal templates");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may write proposal templates through commands");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may execute proposal template commands");
    if (!((doc.status === "archived"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((user.id != null))) throw new Error("Guard 2 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "active";
        const __allowed: Record<string, string[]> = { "active": ["archived"], "archived": ["active"] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "active",
      archivedAt: null,
      archiveReason: null,
      reactivatedAt: Date.now(),
      reactivatedById: user.id,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, templateId: docId, tenantId: __after.tenantId, _subject: { entity: "ProposalTemplate", command: "reactivate", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ProposalTemplateReactivated", entity: "ProposalTemplate", entityId: docId, payload: { templateId: docId, tenantId: __after.tenantId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ProposalTemplate_reactivate = mutation({
  args: {
    docId: v.id("proposalTemplates"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runProposalTemplateReactivate(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ProposalTemplate_reactivate", __result);
    }
    return __result;
  },
});

async function __runProposalTemplateRevise(ctx: MutationCtx, { docId, name, description, visibleSections, defaultTerms, defaultNotes, defaultTaxRate, defaultServiceChargePercent, validityDays, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ProposalTemplate not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ProposalTemplate not found");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may read proposal templates");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may write proposal templates through commands");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may execute proposal template commands");
    if (!((doc.definedAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.status === "active"))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((user.id != null))) throw new Error("Guard 3 failed");
    if (!((((name).trim()).length > 0))) throw new Error("Template name is required");
    if (!(((defaultTaxRate == null) || ((defaultTaxRate >= 0) && (defaultTaxRate <= 1))))) throw new Error("Tax rate must be between 0 and 100%");
    if (!(((defaultServiceChargePercent == null) || ((defaultServiceChargePercent >= 0) && (defaultServiceChargePercent <= 1))))) throw new Error("Service charge must be between 0 and 100%");
    if (!(((validityDays == null) || ((validityDays >= 1) && (validityDays <= 365))))) throw new Error("Validity days must be between 1 and 365");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      name: name,
      description: description,
      visibleSections: ((visibleSections != null) ? visibleSections : doc.visibleSections),
      defaultTerms: ((defaultTerms != null) ? defaultTerms : doc.defaultTerms),
      defaultNotes: ((defaultNotes != null) ? defaultNotes : doc.defaultNotes),
      defaultTaxRate: defaultTaxRate,
      defaultServiceChargePercent: defaultServiceChargePercent,
      validityDays: validityDays,
      updatedAt: Date.now(),
      updatedById: user.id,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, templateId: docId, tenantId: __after.tenantId, name: name, visibleSections: ((visibleSections != null) ? visibleSections : __after.visibleSections), _subject: { entity: "ProposalTemplate", command: "revise", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ProposalTemplateRevised", entity: "ProposalTemplate", entityId: docId, payload: { templateId: docId, tenantId: __after.tenantId, name: name, visibleSections: ((visibleSections != null) ? visibleSections : __after.visibleSections) }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ProposalTemplate_revise = mutation({
  args: {
    docId: v.id("proposalTemplates"),
    name: v.string(),
    description: v.optional(v.string()),
    visibleSections: v.optional(v.array(v.string())),
    defaultTerms: v.optional(v.string()),
    defaultNotes: v.optional(v.string()),
    defaultTaxRate: v.optional(v.number()),
    defaultServiceChargePercent: v.optional(v.number()),
    validityDays: v.optional(v.number()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runProposalTemplateRevise(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ProposalTemplate_revise", __result);
    }
    return __result;
  },
});

async function __runPurchaseNeedAssignToDraft(ctx: MutationCtx, { docId, vendorOrderId, vendorOrderLineId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("PurchaseNeed not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("PurchaseNeed not found");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) throw new Error("Inventory staff and managers may read purchase needs");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) throw new Error("Inventory staff and managers may write purchase needs through commands");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) throw new Error("Inventory staff and managers may execute purchase need commands");
    if (!((doc.status === "open"))) throw new Error("Guard 0 failed");
    if (!((doc.openedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!(((doc.vendorOrderId == null) || ((doc.vendorOrderId === vendorOrderId) && (doc.vendorOrderLineId === vendorOrderLineId))))) throw new Error("Guard 3 failed");
    if (!(((doc.vendorOrderLineId == null) || (doc.vendorOrderLineId === vendorOrderLineId)))) throw new Error("Guard 4 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      vendorOrderId: vendorOrderId,
      vendorOrderLineId: vendorOrderLineId,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, purchaseNeedId: docId, tenantId: __after.tenantId, eventId: __after.eventId, ingredientDemandId: __after.ingredientDemandId, ingredientId: __after.ingredientId, vendorOrderId: vendorOrderId, vendorOrderLineId: vendorOrderLineId, status: "open", _subject: { entity: "PurchaseNeed", command: "assignToDraft", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "PurchaseNeedDraftAssigned", entity: "PurchaseNeed", entityId: docId, payload: { purchaseNeedId: docId, tenantId: __after.tenantId, eventId: __after.eventId, ingredientDemandId: __after.ingredientDemandId, ingredientId: __after.ingredientId, vendorOrderId: vendorOrderId, vendorOrderLineId: vendorOrderLineId, status: "open" }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const PurchaseNeed_assignToDraft = mutation({
  args: {
    docId: v.id("purchaseNeeds"),
    vendorOrderId: v.string(),
    vendorOrderLineId: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runPurchaseNeedAssignToDraft(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "PurchaseNeed_assignToDraft", __result);
    }
    return __result;
  },
});

async function __runPurchaseNeedCancel(ctx: MutationCtx, { docId, reason, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("PurchaseNeed not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("PurchaseNeed not found");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) throw new Error("Inventory staff and managers may read purchase needs");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) throw new Error("Inventory staff and managers may write purchase needs through commands");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) throw new Error("Inventory staff and managers may execute purchase need commands");
    if (!(((doc.status === "open") || (doc.status === "ordered")))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(checkRole(user, "inventoryManageAccess"))) throw new Error("Guard 2 failed");
    if (!((((reason).trim()).length > 0))) throw new Error("Cancellation reason is required");
    const previousStatus = doc.status;
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "cancelled";
        const __allowed: Record<string, string[]> = { "open": ["ordered", "cancelled"], "ordered": ["fulfilled", "cancelled"], "fulfilled": [], "cancelled": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "cancelled",
      cancelledAt: Date.now(),
      cancellationReason: reason,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, purchaseNeedId: docId, tenantId: __after.tenantId, eventId: __after.eventId, ingredientDemandId: __after.ingredientDemandId, ingredientId: __after.ingredientId, previousStatus: previousStatus, status: "cancelled", reason: reason, _subject: { entity: "PurchaseNeed", command: "cancel", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "PurchaseNeedCancelled", entity: "PurchaseNeed", entityId: docId, payload: { purchaseNeedId: docId, tenantId: __after.tenantId, eventId: __after.eventId, ingredientDemandId: __after.ingredientDemandId, ingredientId: __after.ingredientId, previousStatus: previousStatus, status: "cancelled", reason: reason }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const PurchaseNeed_cancel = mutation({
  args: {
    docId: v.id("purchaseNeeds"),
    reason: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runPurchaseNeedCancel(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "PurchaseNeed_cancel", __result);
    }
    return __result;
  },
});

async function __runPurchaseNeedCreate(ctx: MutationCtx, args: any) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc: Record<string, any> = {
      tenantId: __auth.tenantId,
      deletedAt: args.deletedAt,
      eventId: args.eventId,
      ingredientDemandId: args.ingredientDemandId,
      ingredientId: args.ingredientId,
      unit: args.unit ?? "each",
      vendorOrderId: args.vendorOrderId,
      vendorOrderLineId: args.vendorOrderLineId,
      status: args.status ?? "open",
      orderedAt: args.orderedAt,
      fulfilledAt: args.fulfilledAt,
      cancelledAt: args.cancelledAt,
      cancellationReason: args.cancellationReason,
      createdAt: args.createdAt,
      updatedAt: args.updatedAt,
      requiredQuantity: args.requiredQuantity,
      purchasingWeekStart: args.purchasingWeekStart,
      preferredVendorId: args.preferredVendorId,
      openedAt: Date.now(),
      version: 1
    };
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) throw new Error("Inventory staff and managers may read purchase needs");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) throw new Error("Inventory staff and managers may write purchase needs through commands");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) throw new Error("Inventory staff and managers may execute purchase need commands");
    if (!((args.openedAt == null))) throw new Error("Guard 0 failed");
    if (!((args.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((args.requiredQuantity > 0))) throw new Error("Required quantity must be positive");
    const _id = await ctx.db.insert("purchaseNeeds", doc as any);
    const payload: Record<string, any> = { _id, id: _id, ...doc, result: { _id, id: _id, ...doc }, purchaseNeedId: _id, tenantId: doc.tenantId, eventId: doc.eventId, ingredientDemandId: doc.ingredientDemandId, ingredientId: doc.ingredientId, preferredVendorId: doc.preferredVendorId, requiredQuantity: doc.requiredQuantity, unit: doc.unit, purchasingWeekStart: doc.purchasingWeekStart, status: "open", _subject: { entity: "PurchaseNeed", command: "create", id: _id } };
    await ctx.db.insert("manifestEvents", { type: "PurchaseNeedOpened", entity: "PurchaseNeed", entityId: _id, payload: { purchaseNeedId: _id, tenantId: doc.tenantId, eventId: doc.eventId, ingredientDemandId: doc.ingredientDemandId, ingredientId: doc.ingredientId, preferredVendorId: doc.preferredVendorId, requiredQuantity: doc.requiredQuantity, unit: doc.unit, purchasingWeekStart: doc.purchasingWeekStart, status: "open" }, createdAt: Date.now() });
    // Reactions
    const reactionTarget0 = payload.ingredientDemandId;
    if (reactionTarget0) await __runIngredientDemandMarkReleased(ctx, { docId: reactionTarget0 } as any);
    const __match1_raw = await ctx.db.query("weeklyPurchasingConfigs").withIndex("by_tenantId", (q) => q.eq("tenantId", payload.tenantId)).collect();
    const __match1_rows = __match1_raw.filter((d) => (d as any).tenantId === payload.tenantId && (d as any).deletedAt == null).sort((a, b) => String((a as any)._id).localeCompare(String((b as any)._id)));
    const __match1_id = __match1_rows.length > 0 ? (__match1_rows[0] as any)._id : null;
    if (__match1_id) await __runWeeklyPurchasingConfigRouteNeed(ctx, { docId: __match1_id, purchaseNeedId: payload.purchaseNeedId, eventId: payload.eventId, ingredientDemandId: payload.ingredientDemandId, ingredientId: payload.ingredientId, requiredQuantity: payload.requiredQuantity, unit: payload.unit, purchasingWeekStart: payload.purchasingWeekStart, preferredVendorId: payload.preferredVendorId } as any);
    return { _id, ...doc };

}

export const PurchaseNeed_create = mutation({
  args: {
    deletedAt: v.optional(v.union(v.number(), v.null())),
    eventId: v.id("events"),
    ingredientDemandId: v.id("ingredientDemands"),
    ingredientId: v.id("ingredients"),
    unit: v.optional(v.union(v.literal("each"), v.literal("gram"), v.literal("kilogram"), v.literal("ounce"), v.literal("pound"), v.literal("milliliter"), v.literal("liter"), v.literal("teaspoon"), v.literal("tablespoon"), v.literal("cup"), v.literal("pint"), v.literal("quart"), v.literal("gallon"), v.literal("portion"), v.literal("serving"), v.literal("batch"), v.literal("melon"), v.literal("bottle"))),
    vendorOrderId: v.optional(v.union(v.id("vendorOrders"), v.null())),
    vendorOrderLineId: v.optional(v.union(v.id("vendorOrderLines"), v.null())),
    status: v.optional(v.union(v.literal("open"), v.literal("ordered"), v.literal("fulfilled"), v.literal("cancelled"))),
    orderedAt: v.optional(v.union(v.number(), v.null())),
    fulfilledAt: v.optional(v.union(v.number(), v.null())),
    cancelledAt: v.optional(v.union(v.number(), v.null())),
    cancellationReason: v.optional(v.union(v.string(), v.null())),
    createdAt: v.optional(v.number()),
    updatedAt: v.optional(v.number()),
    requiredQuantity: v.number(),
    purchasingWeekStart: v.optional(v.number()),
    preferredVendorId: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runPurchaseNeedCreate(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "PurchaseNeed_create", __result);
    }
    return __result;
  },
});

async function __runPurchaseNeedMarkDraftOrdered(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("PurchaseNeed not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("PurchaseNeed not found");
    const __rel_vendorOrder = await __resolveRelation(ctx, "vendorOrders", [__auth.tenantId, doc.vendorOrderId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).vendorOrderId;
      ((doc as any) as any).vendorOrder = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) throw new Error("Inventory staff and managers may read purchase needs");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) throw new Error("Inventory staff and managers may write purchase needs through commands");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) throw new Error("Inventory staff and managers may execute purchase need commands");
    if (!((doc.status === "open"))) throw new Error("Guard 0 failed");
    if (!((doc.openedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!(((doc.vendorOrderId != null) && (doc.vendorOrderLineId != null)))) throw new Error("Guard 3 failed");
    if (!(((__rel_vendorOrder != null) && (__rel_vendorOrder.status === "submitted")))) throw new Error("Guard 4 failed");
    const previousStatus = doc.status;
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "ordered";
        const __allowed: Record<string, string[]> = { "open": ["ordered", "cancelled"], "ordered": ["fulfilled", "cancelled"], "fulfilled": [], "cancelled": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "ordered",
      orderedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, purchaseNeedId: docId, tenantId: __after.tenantId, eventId: __after.eventId, ingredientDemandId: __after.ingredientDemandId, ingredientId: __after.ingredientId, vendorOrderId: __after.vendorOrderId, vendorOrderLineId: __after.vendorOrderLineId, previousStatus: previousStatus, status: "ordered", _subject: { entity: "PurchaseNeed", command: "markDraftOrdered", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "PurchaseNeedOrdered", entity: "PurchaseNeed", entityId: docId, payload: { purchaseNeedId: docId, tenantId: __after.tenantId, eventId: __after.eventId, ingredientDemandId: __after.ingredientDemandId, ingredientId: __after.ingredientId, vendorOrderId: __after.vendorOrderId, vendorOrderLineId: __after.vendorOrderLineId, previousStatus: previousStatus, status: "ordered" }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const PurchaseNeed_markDraftOrdered = mutation({
  args: {
    docId: v.id("purchaseNeeds"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runPurchaseNeedMarkDraftOrdered(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "PurchaseNeed_markDraftOrdered", __result);
    }
    return __result;
  },
});

async function __runPurchaseNeedMarkFulfilled(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("PurchaseNeed not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("PurchaseNeed not found");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) throw new Error("Inventory staff and managers may read purchase needs");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) throw new Error("Inventory staff and managers may write purchase needs through commands");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) throw new Error("Inventory staff and managers may execute purchase need commands");
    if (!((doc.status === "ordered"))) throw new Error("Guard 0 failed");
    if (!((doc.orderedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    const previousStatus = doc.status;
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "fulfilled";
        const __allowed: Record<string, string[]> = { "open": ["ordered", "cancelled"], "ordered": ["fulfilled", "cancelled"], "fulfilled": [], "cancelled": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "fulfilled",
      fulfilledAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, purchaseNeedId: docId, tenantId: __after.tenantId, eventId: __after.eventId, ingredientDemandId: __after.ingredientDemandId, ingredientId: __after.ingredientId, previousStatus: previousStatus, status: "fulfilled", _subject: { entity: "PurchaseNeed", command: "markFulfilled", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "PurchaseNeedFulfilled", entity: "PurchaseNeed", entityId: docId, payload: { purchaseNeedId: docId, tenantId: __after.tenantId, eventId: __after.eventId, ingredientDemandId: __after.ingredientDemandId, ingredientId: __after.ingredientId, previousStatus: previousStatus, status: "fulfilled" }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const PurchaseNeed_markFulfilled = mutation({
  args: {
    docId: v.id("purchaseNeeds"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runPurchaseNeedMarkFulfilled(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "PurchaseNeed_markFulfilled", __result);
    }
    return __result;
  },
});

async function __runPurchaseNeedMarkOrdered(ctx: MutationCtx, { docId, vendorOrderId, vendorOrderLineId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("PurchaseNeed not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("PurchaseNeed not found");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) throw new Error("Inventory staff and managers may read purchase needs");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) throw new Error("Inventory staff and managers may write purchase needs through commands");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) throw new Error("Inventory staff and managers may execute purchase need commands");
    if (!((doc.status === "open"))) throw new Error("Guard 0 failed");
    if (!((doc.openedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    const previousStatus = doc.status;
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "ordered";
        const __allowed: Record<string, string[]> = { "open": ["ordered", "cancelled"], "ordered": ["fulfilled", "cancelled"], "fulfilled": [], "cancelled": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      vendorOrderId: vendorOrderId,
      vendorOrderLineId: vendorOrderLineId,
      status: "ordered",
      orderedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, purchaseNeedId: docId, tenantId: __after.tenantId, eventId: __after.eventId, ingredientDemandId: __after.ingredientDemandId, ingredientId: __after.ingredientId, vendorOrderId: vendorOrderId, vendorOrderLineId: vendorOrderLineId, previousStatus: previousStatus, status: "ordered", _subject: { entity: "PurchaseNeed", command: "markOrdered", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "PurchaseNeedOrdered", entity: "PurchaseNeed", entityId: docId, payload: { purchaseNeedId: docId, tenantId: __after.tenantId, eventId: __after.eventId, ingredientDemandId: __after.ingredientDemandId, ingredientId: __after.ingredientId, vendorOrderId: vendorOrderId, vendorOrderLineId: vendorOrderLineId, previousStatus: previousStatus, status: "ordered" }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const PurchaseNeed_markOrdered = mutation({
  args: {
    docId: v.id("purchaseNeeds"),
    vendorOrderId: v.string(),
    vendorOrderLineId: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runPurchaseNeedMarkOrdered(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "PurchaseNeed_markOrdered", __result);
    }
    return __result;
  },
});

async function __runPurchaseNeedReviseRequired(ctx: MutationCtx, { docId, requiredQuantity, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("PurchaseNeed not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("PurchaseNeed not found");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) throw new Error("Inventory staff and managers may read purchase needs");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) throw new Error("Inventory staff and managers may write purchase needs through commands");
    if (!((checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) throw new Error("Inventory staff and managers may execute purchase need commands");
    if (!((doc.status === "open"))) throw new Error("Guard 0 failed");
    if (!((doc.openedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((requiredQuantity > 0))) throw new Error("Required quantity must be positive");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      requiredQuantity: requiredQuantity,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, purchaseNeedId: docId, tenantId: __after.tenantId, eventId: __after.eventId, ingredientDemandId: __after.ingredientDemandId, ingredientId: __after.ingredientId, preferredVendorId: __after.preferredVendorId, requiredQuantity: requiredQuantity, unit: __after.unit, purchasingWeekStart: __after.purchasingWeekStart, status: "open", _subject: { entity: "PurchaseNeed", command: "reviseRequired", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "PurchaseNeedOpened", entity: "PurchaseNeed", entityId: docId, payload: { purchaseNeedId: docId, tenantId: __after.tenantId, eventId: __after.eventId, ingredientDemandId: __after.ingredientDemandId, ingredientId: __after.ingredientId, preferredVendorId: __after.preferredVendorId, requiredQuantity: requiredQuantity, unit: __after.unit, purchasingWeekStart: __after.purchasingWeekStart, status: "open" }, createdAt: Date.now() });
    // Reactions
    const reactionTarget0 = payload.ingredientDemandId;
    if (reactionTarget0) await __runIngredientDemandMarkReleased(ctx, { docId: reactionTarget0 } as any);
    const __match1_raw = await ctx.db.query("weeklyPurchasingConfigs").withIndex("by_tenantId", (q) => q.eq("tenantId", payload.tenantId)).collect();
    const __match1_rows = __match1_raw.filter((d) => (d as any).tenantId === payload.tenantId && (d as any).deletedAt == null).sort((a, b) => String((a as any)._id).localeCompare(String((b as any)._id)));
    const __match1_id = __match1_rows.length > 0 ? (__match1_rows[0] as any)._id : null;
    if (__match1_id) await __runWeeklyPurchasingConfigRouteNeed(ctx, { docId: __match1_id, purchaseNeedId: payload.purchaseNeedId, eventId: payload.eventId, ingredientDemandId: payload.ingredientDemandId, ingredientId: payload.ingredientId, requiredQuantity: payload.requiredQuantity, unit: payload.unit, purchasingWeekStart: payload.purchasingWeekStart, preferredVendorId: payload.preferredVendorId } as any);
    return { ...doc, ...updates };
}

export const PurchaseNeed_reviseRequired = mutation({
  args: {
    docId: v.id("purchaseNeeds"),
    requiredQuantity: v.number(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runPurchaseNeedReviseRequired(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "PurchaseNeed_reviseRequired", __result);
    }
    return __result;
  },
});

async function __runQualificationExpire(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("Qualification not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("Qualification not found");
    const doc = await __decryptDoc(ctx, "Qualification", ["notes"], __storedDoc) as Record<string, any>;
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may read qualifications");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may write qualifications through commands");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may execute qualification commands");
    if (!((doc.status === "active"))) throw new Error("Guard 0 failed");
    if (!((doc.grantedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.expiresAt != null))) throw new Error("Guard 2 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 3 failed");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Guard 4 failed");
    const previousStatus = doc.status;
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "expired";
        const __allowed: Record<string, string[]> = { "active": ["expired", "revoked"], "expired": [], "revoked": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "expired",
      expiredAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "Qualification", ["notes"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, qualificationId: docId, tenantId: __after.tenantId, personId: __after.personId, name: __after.name, expiresAt: __after.expiresAt, previousStatus: previousStatus, status: "expired", _subject: { entity: "Qualification", command: "expire", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "QualificationExpired", entity: "Qualification", entityId: docId, payload: { qualificationId: docId, tenantId: __after.tenantId, personId: __after.personId, name: __after.name, expiresAt: __after.expiresAt, previousStatus: previousStatus, status: "expired" }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Qualification_expire = mutation({
  args: {
    docId: v.id("qualifications"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runQualificationExpire(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Qualification_expire", __result);
    }
    return __result;
  },
});

async function __runQualificationGrant(ctx: MutationCtx, { docId, personId, name, issuedAt, certificationType, issuingBody, expiresAt, documentRef, notes, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("Qualification not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("Qualification not found");
    const doc = await __decryptDoc(ctx, "Qualification", ["notes"], __storedDoc) as Record<string, any>;
    const __rel_person = await __resolveRelation(ctx, "people", [__auth.tenantId, doc.personId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).personId;
      ((doc as any) as any).person = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may read qualifications");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may write qualifications through commands");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may execute qualification commands");
    if (!((doc.grantedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.status === "active"))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!(((__rel_person != null) && (__rel_person.status === "active")))) throw new Error("Guard 3 failed");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Guard 4 failed");
    if (!((personId === doc.personId))) throw new Error("Grant personId must match the seeded person reference");
    if (!((((name).trim()).length > 0))) throw new Error("Qualification name is required");
    if (!((((certificationType).trim()).length > 0))) throw new Error("Certification type is required");
    if (!((((issuingBody).trim()).length > 0))) throw new Error("Issuing body is required");
    if (!(((expiresAt == null) || (expiresAt > issuedAt)))) throw new Error("Qualification expiry must be after issued date");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      personId: personId,
      name: name,
      certificationType: certificationType,
      issuingBody: issuingBody,
      issuedAt: issuedAt,
      expiresAt: ((expiresAt != null) ? expiresAt : doc.expiresAt),
      documentRef: ((documentRef != null) ? documentRef : doc.documentRef),
      notes: ((notes != null) ? notes : doc.notes),
      grantedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "Qualification", ["notes"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, qualificationId: docId, tenantId: __after.tenantId, personId: personId, name: name, certificationType: certificationType, issuingBody: issuingBody, issuedAt: issuedAt, expiresAt: ((expiresAt != null) ? expiresAt : __after.expiresAt), status: "active", _subject: { entity: "Qualification", command: "grant", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "QualificationGranted", entity: "Qualification", entityId: docId, payload: { qualificationId: docId, tenantId: __after.tenantId, personId: personId, name: name, certificationType: certificationType, issuingBody: issuingBody, issuedAt: issuedAt, expiresAt: ((expiresAt != null) ? expiresAt : __after.expiresAt), status: "active" }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Qualification_grant = mutation({
  args: {
    docId: v.id("qualifications"),
    personId: v.string(),
    name: v.string(),
    issuedAt: v.number(),
    certificationType: v.string(),
    issuingBody: v.string(),
    expiresAt: v.optional(v.number()),
    documentRef: v.optional(v.string()),
    notes: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runQualificationGrant(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Qualification_grant", __result);
    }
    return __result;
  },
});

export const Qualification_createViaGrant = mutation({
  args: {
    personId: v.string(),
    name: v.string(),
    issuedAt: v.number(),
    certificationType: v.string(),
    issuingBody: v.string(),
    expiresAt: v.optional(v.number()),
    documentRef: v.optional(v.string()),
    notes: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { personId, name, issuedAt, certificationType, issuingBody, expiresAt, documentRef, notes } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      status: "active",
      createdAt: Date.now(),
      updatedAt: Date.now(),
      certificationType: args.certificationType,
      documentRef: args.documentRef,
      expiresAt: args.expiresAt,
      issuedAt: args.issuedAt,
      issuingBody: args.issuingBody,
      name: args.name,
      notes: args.notes,
      personId: args.personId
    };
    const __rel_person = await __resolveRelation(ctx, "people", [__auth.tenantId, __draft.personId], ["tenantId","id"], "tenantId", __auth.tenantId);
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may read qualifications");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may write qualifications through commands");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may execute qualification commands");
    if (!((__draft.grantedAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.status === "active"))) throw new Error("Guard 1 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!(((__rel_person != null) && (__rel_person.status === "active")))) throw new Error("Guard 3 failed");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Guard 4 failed");
    if (!((personId === __draft.personId))) throw new Error("Grant personId must match the seeded person reference");
    if (!((((name).trim()).length > 0))) throw new Error("Qualification name is required");
    if (!((((certificationType).trim()).length > 0))) throw new Error("Certification type is required");
    if (!((((issuingBody).trim()).length > 0))) throw new Error("Issuing body is required");
    if (!(((expiresAt == null) || (expiresAt > issuedAt)))) throw new Error("Qualification expiry must be after issued date");
    const doc: Record<string, any> = {
      ...__draft,
      personId: personId,
      name: name,
      certificationType: certificationType,
      issuingBody: issuingBody,
      issuedAt: issuedAt,
      expiresAt: ((expiresAt != null) ? expiresAt : __draft.expiresAt),
      documentRef: ((documentRef != null) ? documentRef : __draft.documentRef),
      notes: ((notes != null) ? notes : __draft.notes),
      grantedAt: Date.now(),
      version: 1,
    };
    const __storedDoc = await __encryptDoc(ctx, "Qualification", ["notes"], doc);
    const docId = await ctx.db.insert("qualifications", __storedDoc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, qualificationId: docId, tenantId: doc.tenantId, personId: doc.personId, name: doc.name, certificationType: doc.certificationType, issuingBody: doc.issuingBody, issuedAt: doc.issuedAt, expiresAt: ((doc.expiresAt != null) ? doc.expiresAt : doc.expiresAt), status: "active", _subject: { entity: "Qualification", command: "grant", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "QualificationGranted", entity: "Qualification", entityId: docId, payload: { qualificationId: docId, tenantId: doc.tenantId, personId: doc.personId, name: doc.name, certificationType: doc.certificationType, issuingBody: doc.issuingBody, issuedAt: doc.issuedAt, expiresAt: ((doc.expiresAt != null) ? doc.expiresAt : doc.expiresAt), status: "active" }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Qualification_createViaGrant", __result);
    }
    return __result;
  },
});

async function __runQualificationRevoke(ctx: MutationCtx, { docId, notes, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("Qualification not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("Qualification not found");
    const doc = await __decryptDoc(ctx, "Qualification", ["notes"], __storedDoc) as Record<string, any>;
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may read qualifications");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may write qualifications through commands");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may execute qualification commands");
    if (!((doc.status === "active"))) throw new Error("Guard 0 failed");
    if (!((doc.grantedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Guard 3 failed");
    const previousStatus = doc.status;
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "revoked";
        const __allowed: Record<string, string[]> = { "active": ["expired", "revoked"], "expired": [], "revoked": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "revoked",
      revokedAt: Date.now(),
      notes: ((notes != null) ? notes : doc.notes),
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "Qualification", ["notes"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, qualificationId: docId, tenantId: __after.tenantId, personId: __after.personId, name: __after.name, previousStatus: previousStatus, status: "revoked", _subject: { entity: "Qualification", command: "revoke", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "QualificationRevoked", entity: "Qualification", entityId: docId, payload: { qualificationId: docId, tenantId: __after.tenantId, personId: __after.personId, name: __after.name, previousStatus: previousStatus, status: "revoked" }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Qualification_revoke = mutation({
  args: {
    docId: v.id("qualifications"),
    notes: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runQualificationRevoke(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Qualification_revoke", __result);
    }
    return __result;
  },
});

async function __runQualityCheckFail(ctx: MutationCtx, { docId, notes, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("QualityCheck not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("QualityCheck not found");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may read quality checks");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write quality checks through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute quality check commands");
    if (!((doc.status === "pending"))) throw new Error("Guard 0 failed");
    if (!((doc.openedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((user.id != null))) throw new Error("Guard 3 failed");
    const previousStatus = doc.status;
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "failed";
        const __allowed: Record<string, string[]> = { "pending": ["passed", "failed"], "passed": ["pending"], "failed": ["pending"] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      result: "fail",
      checkedById: user.personId,
      notes: ((notes != null) ? notes : doc.notes),
      status: "failed",
      completedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, qualityCheckId: docId, tenantId: __after.tenantId, prepTaskId: __after.prepTaskId, productionBatchId: __after.productionBatchId, checkedById: user.personId, result: "fail", previousStatus: previousStatus, status: "failed", notes: ((notes != null) ? notes : __after.notes), _subject: { entity: "QualityCheck", command: "fail", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "QualityCheckFailed", entity: "QualityCheck", entityId: docId, payload: { qualityCheckId: docId, tenantId: __after.tenantId, prepTaskId: __after.prepTaskId, productionBatchId: __after.productionBatchId, checkedById: user.personId, result: "fail", previousStatus: previousStatus, status: "failed", notes: ((notes != null) ? notes : __after.notes) }, createdAt: Date.now() });
    // Reactions
    const reactionTarget0 = payload.prepTaskId;
    if (reactionTarget0) await __runPrepTaskMarkBlocked(ctx, { docId: reactionTarget0, reason: "Quality check failed" } as any);
    return { ...doc, ...updates };
}

export const QualityCheck_fail = mutation({
  args: {
    docId: v.id("qualityChecks"),
    notes: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runQualityCheckFail(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "QualityCheck_fail", __result);
    }
    return __result;
  },
});

async function __runQualityCheckOpen(ctx: MutationCtx, { docId, prepTaskId, productionBatchId, notes, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("QualityCheck not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("QualityCheck not found");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may read quality checks");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write quality checks through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute quality check commands");
    if (!((doc.openedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.status === "pending"))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!(((((prepTaskId != null) || (productionBatchId != null)) || (doc.prepTaskId != null)) || (doc.productionBatchId != null)))) throw new Error("Quality check must reference a PrepTask or ProductionBatch");
    if (!((((prepTaskId == null) || (doc.prepTaskId == null)) || (prepTaskId === doc.prepTaskId)))) throw new Error("Open prepTaskId must match the seeded prep task reference when provided");
    if (!((((productionBatchId == null) || (doc.productionBatchId == null)) || (productionBatchId === doc.productionBatchId)))) throw new Error("Open productionBatchId must match the seeded batch reference when provided");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      prepTaskId: ((prepTaskId != null) ? prepTaskId : doc.prepTaskId),
      productionBatchId: ((productionBatchId != null) ? productionBatchId : doc.productionBatchId),
      notes: ((notes != null) ? notes : doc.notes),
      openedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, qualityCheckId: docId, tenantId: __after.tenantId, prepTaskId: ((prepTaskId != null) ? prepTaskId : __after.prepTaskId), productionBatchId: ((productionBatchId != null) ? productionBatchId : __after.productionBatchId), status: "pending", _subject: { entity: "QualityCheck", command: "open", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "QualityCheckOpened", entity: "QualityCheck", entityId: docId, payload: { qualityCheckId: docId, tenantId: __after.tenantId, prepTaskId: ((prepTaskId != null) ? prepTaskId : __after.prepTaskId), productionBatchId: ((productionBatchId != null) ? productionBatchId : __after.productionBatchId), status: "pending" }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const QualityCheck_open = mutation({
  args: {
    docId: v.id("qualityChecks"),
    prepTaskId: v.optional(v.string()),
    productionBatchId: v.optional(v.string()),
    notes: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runQualityCheckOpen(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "QualityCheck_open", __result);
    }
    return __result;
  },
});

export const QualityCheck_createViaOpen = mutation({
  args: {
    prepTaskId: v.optional(v.string()),
    productionBatchId: v.optional(v.string()),
    notes: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { prepTaskId, productionBatchId, notes } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      status: "pending",
      createdAt: Date.now(),
      updatedAt: Date.now(),
      notes: args.notes,
      prepTaskId: args.prepTaskId,
      productionBatchId: args.productionBatchId
    };
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may read quality checks");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write quality checks through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute quality check commands");
    if (!((__draft.openedAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.status === "pending"))) throw new Error("Guard 1 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!(((((prepTaskId != null) || (productionBatchId != null)) || (__draft.prepTaskId != null)) || (__draft.productionBatchId != null)))) throw new Error("Quality check must reference a PrepTask or ProductionBatch");
    if (!((((prepTaskId == null) || (__draft.prepTaskId == null)) || (prepTaskId === __draft.prepTaskId)))) throw new Error("Open prepTaskId must match the seeded prep task reference when provided");
    if (!((((productionBatchId == null) || (__draft.productionBatchId == null)) || (productionBatchId === __draft.productionBatchId)))) throw new Error("Open productionBatchId must match the seeded batch reference when provided");
    const doc: Record<string, any> = {
      ...__draft,
      prepTaskId: ((prepTaskId != null) ? prepTaskId : __draft.prepTaskId),
      productionBatchId: ((productionBatchId != null) ? productionBatchId : __draft.productionBatchId),
      notes: ((notes != null) ? notes : __draft.notes),
      openedAt: Date.now(),
      version: 1,
    };
    const docId = await ctx.db.insert("qualityChecks", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, qualityCheckId: docId, tenantId: doc.tenantId, prepTaskId: ((doc.prepTaskId != null) ? doc.prepTaskId : doc.prepTaskId), productionBatchId: ((doc.productionBatchId != null) ? doc.productionBatchId : doc.productionBatchId), status: "pending", _subject: { entity: "QualityCheck", command: "open", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "QualityCheckOpened", entity: "QualityCheck", entityId: docId, payload: { qualityCheckId: docId, tenantId: doc.tenantId, prepTaskId: ((doc.prepTaskId != null) ? doc.prepTaskId : doc.prepTaskId), productionBatchId: ((doc.productionBatchId != null) ? doc.productionBatchId : doc.productionBatchId), status: "pending" }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "QualityCheck_createViaOpen", __result);
    }
    return __result;
  },
});

async function __runQualityCheckPass(ctx: MutationCtx, { docId, notes, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("QualityCheck not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("QualityCheck not found");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may read quality checks");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write quality checks through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute quality check commands");
    if (!((doc.status === "pending"))) throw new Error("Guard 0 failed");
    if (!((doc.openedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((user.id != null))) throw new Error("Guard 3 failed");
    const previousStatus = doc.status;
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "passed";
        const __allowed: Record<string, string[]> = { "pending": ["passed", "failed"], "passed": ["pending"], "failed": ["pending"] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      result: "pass",
      checkedById: user.personId,
      notes: ((notes != null) ? notes : doc.notes),
      status: "passed",
      completedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, qualityCheckId: docId, tenantId: __after.tenantId, prepTaskId: __after.prepTaskId, productionBatchId: __after.productionBatchId, checkedById: user.personId, result: "pass", previousStatus: previousStatus, status: "passed", notes: ((notes != null) ? notes : __after.notes), _subject: { entity: "QualityCheck", command: "pass", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "QualityCheckPassed", entity: "QualityCheck", entityId: docId, payload: { qualityCheckId: docId, tenantId: __after.tenantId, prepTaskId: __after.prepTaskId, productionBatchId: __after.productionBatchId, checkedById: user.personId, result: "pass", previousStatus: previousStatus, status: "passed", notes: ((notes != null) ? notes : __after.notes) }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const QualityCheck_pass = mutation({
  args: {
    docId: v.id("qualityChecks"),
    notes: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runQualityCheckPass(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "QualityCheck_pass", __result);
    }
    return __result;
  },
});

async function __runQualityCheckReinspect(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("QualityCheck not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("QualityCheck not found");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may read quality checks");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may write quality checks through commands");
    if (!(checkRole(user, "kitchenAccess"))) throw new Error("Kitchen staff may execute quality check commands");
    if (!(((doc.status === "passed") || (doc.status === "failed")))) throw new Error("Guard 0 failed");
    if (!((doc.completedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((checkRole(user, "kitchenLeadAccess") || checkRole(user, "kitchenManageAccess")))) throw new Error("Guard 3 failed");
    const previousStatus = doc.status;
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "pending";
        const __allowed: Record<string, string[]> = { "pending": ["passed", "failed"], "passed": ["pending"], "failed": ["pending"] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      result: null,
      checkedById: null,
      status: "pending",
      completedAt: null,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, qualityCheckId: docId, tenantId: __after.tenantId, prepTaskId: __after.prepTaskId, productionBatchId: __after.productionBatchId, previousStatus: previousStatus, status: "pending", _subject: { entity: "QualityCheck", command: "reinspect", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "QualityCheckReopened", entity: "QualityCheck", entityId: docId, payload: { qualityCheckId: docId, tenantId: __after.tenantId, prepTaskId: __after.prepTaskId, productionBatchId: __after.productionBatchId, previousStatus: previousStatus, status: "pending" }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const QualityCheck_reinspect = mutation({
  args: {
    docId: v.id("qualityChecks"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runQualityCheckReinspect(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "QualityCheck_reinspect", __result);
    }
    return __result;
  },
});

async function __runQuoteSubmissionComplete(ctx: MutationCtx, { docId, clientId, leadId, eventId, proposalId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("QuoteSubmission not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("QuoteSubmission not found");
    if (!(true)) throw new Error("Anyone may read quote submission status by ID");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Only sales staff may update quote submissions");
    if (!(true)) throw new Error("Anyone may execute quote submission commands (public form)");
    if (!((doc.status === "processing"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "completed",
      completedAt: Date.now(),
      clientId: clientId,
      leadId: leadId,
      eventId: eventId,
      proposalId: proposalId,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, submissionId: docId, tenantId: __after.tenantId, clientId: clientId, leadId: leadId, eventId: eventId, proposalId: proposalId, _subject: { entity: "QuoteSubmission", command: "complete", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "QuoteProcessingCompleted", entity: "QuoteSubmission", entityId: docId, payload: { submissionId: docId, tenantId: __after.tenantId, clientId: clientId, leadId: leadId, eventId: eventId, proposalId: proposalId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const QuoteSubmission_complete = mutation({
  args: {
    docId: v.id("quoteSubmissions"),
    clientId: v.string(),
    leadId: v.string(),
    eventId: v.string(),
    proposalId: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runQuoteSubmissionComplete(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "QuoteSubmission_complete", __result);
    }
    return __result;
  },
});

async function __runQuoteSubmissionCreate(ctx: MutationCtx, args: any) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc: Record<string, any> = {
      tenantId: __auth.tenantId,
      deletedAt: args.deletedAt,
      clientId: args.clientId,
      leadId: args.leadId,
      eventId: args.eventId,
      proposalId: args.proposalId,
      completedAt: args.completedAt,
      errorMessage: args.errorMessage,
      processingErrors: args.processingErrors,
      createdAt: args.createdAt,
      updatedAt: args.updatedAt,
      dedupKey: args.dedupKey,
      clientName: args.clientName,
      email: args.email,
      phone: args.phone,
      eventDate: args.eventDate,
      eventEndTime: args.eventEndTime,
      guestCount: args.guestCount,
      serviceStyleId: args.serviceStyleId,
      occasionId: args.occasionId,
      venueName: args.venueName,
      venueAddress: args.venueAddress,
      menuPreferences: args.menuPreferences,
      dietaryRestrictions: args.dietaryRestrictions,
      notes: args.notes,
      submittedAt: Date.now(),
      consentGrantedAt: Date.now(),
      status: "pending",
      version: 1
    };
    if (!(true)) throw new Error("Anyone may read quote submission status by ID");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Only sales staff may update quote submissions");
    if (!(true)) throw new Error("Anyone may execute quote submission commands (public form)");
    if (!((args.submittedAt == null))) throw new Error("Guard 0 failed");
    if (!((args.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((((args.dedupKey).trim()).length > 0))) throw new Error("Deduplication key is required");
    if (!((((args.clientName).trim()).length > 0))) throw new Error("Client name is required");
    if (!((((args.email).trim()).length > 0))) throw new Error("Email address is required");
    if (!((args.guestCount > 0))) throw new Error("Guest count must be positive");
    const _id = await ctx.db.insert("quoteSubmissions", doc as any);
    const payload: Record<string, any> = { _id, id: _id, ...doc, result: { _id, id: _id, ...doc }, submissionId: _id, tenantId: doc.tenantId, dedupKey: doc.dedupKey, clientName: doc.clientName, email: doc.email, eventDate: doc.eventDate, guestCount: doc.guestCount, _subject: { entity: "QuoteSubmission", command: "create", id: _id } };
    await ctx.db.insert("manifestEvents", { type: "QuoteSubmitted", entity: "QuoteSubmission", entityId: _id, payload: { submissionId: _id, tenantId: doc.tenantId, dedupKey: doc.dedupKey, clientName: doc.clientName, email: doc.email, eventDate: doc.eventDate, guestCount: doc.guestCount }, createdAt: Date.now() });
    return { _id, ...doc };

}

export const QuoteSubmission_create = mutation({
  args: {
    deletedAt: v.optional(v.union(v.number(), v.null())),
    clientId: v.optional(v.union(v.id("clients"), v.null())),
    leadId: v.optional(v.union(v.id("leads"), v.null())),
    eventId: v.optional(v.union(v.id("events"), v.null())),
    proposalId: v.optional(v.union(v.id("proposals"), v.null())),
    completedAt: v.optional(v.union(v.number(), v.null())),
    errorMessage: v.optional(v.union(v.string(), v.null())),
    processingErrors: v.optional(v.union(v.string(), v.null())),
    createdAt: v.optional(v.number()),
    updatedAt: v.optional(v.number()),
    dedupKey: v.string(),
    clientName: v.string(),
    email: v.string(),
    phone: v.string(),
    eventDate: v.number(),
    eventEndTime: v.number(),
    guestCount: v.any(),
    serviceStyleId: v.string(),
    occasionId: v.string(),
    venueName: v.string(),
    venueAddress: v.string(),
    menuPreferences: v.string(),
    dietaryRestrictions: v.string(),
    notes: v.string(),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runQuoteSubmissionCreate(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "QuoteSubmission_create", __result);
    }
    return __result;
  },
});

async function __runQuoteSubmissionFail(ctx: MutationCtx, { docId, errorMessage, processingErrors, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("QuoteSubmission not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("QuoteSubmission not found");
    if (!(true)) throw new Error("Anyone may read quote submission status by ID");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Only sales staff may update quote submissions");
    if (!(true)) throw new Error("Anyone may execute quote submission commands (public form)");
    if (!((doc.status === "processing"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "failed",
      completedAt: Date.now(),
      errorMessage: errorMessage,
      processingErrors: processingErrors,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, submissionId: docId, tenantId: __after.tenantId, errorMessage: errorMessage, _subject: { entity: "QuoteSubmission", command: "fail", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "QuoteProcessingFailed", entity: "QuoteSubmission", entityId: docId, payload: { submissionId: docId, tenantId: __after.tenantId, errorMessage: errorMessage }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const QuoteSubmission_fail = mutation({
  args: {
    docId: v.id("quoteSubmissions"),
    errorMessage: v.string(),
    processingErrors: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runQuoteSubmissionFail(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "QuoteSubmission_fail", __result);
    }
    return __result;
  },
});

async function __runQuoteSubmissionStartProcessing(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("QuoteSubmission not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("QuoteSubmission not found");
    if (!(true)) throw new Error("Anyone may read quote submission status by ID");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Only sales staff may update quote submissions");
    if (!(true)) throw new Error("Anyone may execute quote submission commands (public form)");
    if (!((doc.submittedAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.status === "pending"))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "processing",
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    return { ...doc, ...updates };
}

export const QuoteSubmission_startProcessing = mutation({
  args: {
    docId: v.id("quoteSubmissions"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runQuoteSubmissionStartProcessing(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "QuoteSubmission_startProcessing", __result);
    }
    return __result;
  },
});

async function __runRecurringAvailabilityDeclare(ctx: MutationCtx, { docId, personId, dayOfWeek, startMinute, endMinute, notes, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("RecurringAvailability not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("RecurringAvailability not found");
    const doc = await __decryptDoc(ctx, "RecurringAvailability", ["notes"], __storedDoc) as Record<string, any>;
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may read recurring availability");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may write recurring availability through commands");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may execute recurring availability commands");
    if (!((doc.declaredAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.status === "active"))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((user.id != null))) throw new Error("Guard 3 failed");
    if (!(((personId === user.id) || checkRole(user, "workforceManageAccess")))) throw new Error("Guard 4 failed");
    if (!((personId === doc.personId))) throw new Error("Declare personId must match the seeded person reference");
    if (!(((dayOfWeek >= 0) && (dayOfWeek <= 6)))) throw new Error("Day of week must be 0 (Sunday) through 6 (Saturday)");
    if (!((((startMinute >= 0) && (endMinute <= 1440)) && (endMinute > startMinute)))) throw new Error("Time band must fall within the day and end after it starts");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      personId: personId,
      dayOfWeek: dayOfWeek,
      startMinute: startMinute,
      endMinute: endMinute,
      notes: ((notes != null) ? notes : doc.notes),
      declaredAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "RecurringAvailability", ["notes"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, recurringAvailabilityId: docId, tenantId: __after.tenantId, personId: personId, dayOfWeek: dayOfWeek, startMinute: startMinute, endMinute: endMinute, status: "active", _subject: { entity: "RecurringAvailability", command: "declare", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "RecurringAvailabilityDeclared", entity: "RecurringAvailability", entityId: docId, payload: { recurringAvailabilityId: docId, tenantId: __after.tenantId, personId: personId, dayOfWeek: dayOfWeek, startMinute: startMinute, endMinute: endMinute, status: "active" }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const RecurringAvailability_declare = mutation({
  args: {
    docId: v.id("recurringAvailabilities"),
    personId: v.string(),
    dayOfWeek: v.number(),
    startMinute: v.number(),
    endMinute: v.number(),
    notes: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runRecurringAvailabilityDeclare(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "RecurringAvailability_declare", __result);
    }
    return __result;
  },
});

export const RecurringAvailability_createViaDeclare = mutation({
  args: {
    personId: v.string(),
    dayOfWeek: v.number(),
    startMinute: v.number(),
    endMinute: v.number(),
    notes: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { personId, dayOfWeek, startMinute, endMinute, notes } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      status: "active",
      createdAt: Date.now(),
      updatedAt: Date.now(),
      dayOfWeek: args.dayOfWeek,
      endMinute: args.endMinute,
      notes: args.notes,
      personId: args.personId,
      startMinute: args.startMinute
    };
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may read recurring availability");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may write recurring availability through commands");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may execute recurring availability commands");
    if (!((__draft.declaredAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.status === "active"))) throw new Error("Guard 1 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((user.id != null))) throw new Error("Guard 3 failed");
    if (!(((personId === user.id) || checkRole(user, "workforceManageAccess")))) throw new Error("Guard 4 failed");
    if (!((personId === __draft.personId))) throw new Error("Declare personId must match the seeded person reference");
    if (!(((dayOfWeek >= 0) && (dayOfWeek <= 6)))) throw new Error("Day of week must be 0 (Sunday) through 6 (Saturday)");
    if (!((((startMinute >= 0) && (endMinute <= 1440)) && (endMinute > startMinute)))) throw new Error("Time band must fall within the day and end after it starts");
    const doc: Record<string, any> = {
      ...__draft,
      personId: personId,
      dayOfWeek: dayOfWeek,
      startMinute: startMinute,
      endMinute: endMinute,
      notes: ((notes != null) ? notes : __draft.notes),
      declaredAt: Date.now(),
      version: 1,
    };
    const __storedDoc = await __encryptDoc(ctx, "RecurringAvailability", ["notes"], doc);
    const docId = await ctx.db.insert("recurringAvailabilities", __storedDoc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, recurringAvailabilityId: docId, tenantId: doc.tenantId, personId: doc.personId, dayOfWeek: doc.dayOfWeek, startMinute: doc.startMinute, endMinute: doc.endMinute, status: "active", _subject: { entity: "RecurringAvailability", command: "declare", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "RecurringAvailabilityDeclared", entity: "RecurringAvailability", entityId: docId, payload: { recurringAvailabilityId: docId, tenantId: doc.tenantId, personId: doc.personId, dayOfWeek: doc.dayOfWeek, startMinute: doc.startMinute, endMinute: doc.endMinute, status: "active" }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "RecurringAvailability_createViaDeclare", __result);
    }
    return __result;
  },
});

async function __runRecurringAvailabilityWithdraw(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("RecurringAvailability not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("RecurringAvailability not found");
    const doc = await __decryptDoc(ctx, "RecurringAvailability", ["notes"], __storedDoc) as Record<string, any>;
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may read recurring availability");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may write recurring availability through commands");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may execute recurring availability commands");
    if (!((doc.status === "active"))) throw new Error("Guard 0 failed");
    if (!((doc.declaredAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((user.id != null))) throw new Error("Guard 3 failed");
    if (!(((doc.personId === user.id) || checkRole(user, "workforceManageAccess")))) throw new Error("Guard 4 failed");
    const previousStatus = doc.status;
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "withdrawn";
        const __allowed: Record<string, string[]> = { "active": ["withdrawn"], "withdrawn": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "withdrawn",
      withdrawnAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "RecurringAvailability", ["notes"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, recurringAvailabilityId: docId, tenantId: __after.tenantId, personId: __after.personId, dayOfWeek: __after.dayOfWeek, previousStatus: previousStatus, status: "withdrawn", _subject: { entity: "RecurringAvailability", command: "withdraw", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "RecurringAvailabilityWithdrawn", entity: "RecurringAvailability", entityId: docId, payload: { recurringAvailabilityId: docId, tenantId: __after.tenantId, personId: __after.personId, dayOfWeek: __after.dayOfWeek, previousStatus: previousStatus, status: "withdrawn" }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const RecurringAvailability_withdraw = mutation({
  args: {
    docId: v.id("recurringAvailabilities"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runRecurringAvailabilityWithdraw(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "RecurringAvailability_withdraw", __result);
    }
    return __result;
  },
});

async function __runReferralSourceActivate(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ReferralSource not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ReferralSource not found");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may read referral sources");
    if (!(checkRole(user, "salesManageAccess"))) throw new Error("Sales managers may write referral sources through commands");
    if (!(checkRole(user, "salesManageAccess"))) throw new Error("Sales managers may execute referral source commands");
    if (!((doc.status === "inactive"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(checkRole(user, "salesManageAccess"))) throw new Error("Guard 2 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "active";
        const __allowed: Record<string, string[]> = { "active": ["inactive"], "inactive": ["active"] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "active",
      deactivatedAt: null,
      deactivationReason: null,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, referralSourceId: docId, tenantId: __after.tenantId, _subject: { entity: "ReferralSource", command: "activate", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ReferralSourceActivated", entity: "ReferralSource", entityId: docId, payload: { referralSourceId: docId, tenantId: __after.tenantId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ReferralSource_activate = mutation({
  args: {
    docId: v.id("referralSources"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runReferralSourceActivate(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ReferralSource_activate", __result);
    }
    return __result;
  },
});

async function __runReferralSourceDeactivate(ctx: MutationCtx, { docId, reason, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ReferralSource not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ReferralSource not found");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may read referral sources");
    if (!(checkRole(user, "salesManageAccess"))) throw new Error("Sales managers may write referral sources through commands");
    if (!(checkRole(user, "salesManageAccess"))) throw new Error("Sales managers may execute referral source commands");
    if (!((doc.status === "active"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(checkRole(user, "salesManageAccess"))) throw new Error("Guard 2 failed");
    if (!((((reason).trim()).length > 0))) throw new Error("Deactivation reason is required");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "inactive";
        const __allowed: Record<string, string[]> = { "active": ["inactive"], "inactive": ["active"] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "inactive",
      deactivatedAt: Date.now(),
      deactivationReason: reason,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, referralSourceId: docId, tenantId: __after.tenantId, reason: reason, _subject: { entity: "ReferralSource", command: "deactivate", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ReferralSourceDeactivated", entity: "ReferralSource", entityId: docId, payload: { referralSourceId: docId, tenantId: __after.tenantId, reason: reason }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ReferralSource_deactivate = mutation({
  args: {
    docId: v.id("referralSources"),
    reason: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runReferralSourceDeactivate(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ReferralSource_deactivate", __result);
    }
    return __result;
  },
});

async function __runReferralSourceRegister(ctx: MutationCtx, { docId, name, code, sortOrder, description, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ReferralSource not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ReferralSource not found");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may read referral sources");
    if (!(checkRole(user, "salesManageAccess"))) throw new Error("Sales managers may write referral sources through commands");
    if (!(checkRole(user, "salesManageAccess"))) throw new Error("Sales managers may execute referral source commands");
    if (!((doc.registeredAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((((name).trim()).length > 0))) throw new Error("Referral source name is required");
    if (!((((code).trim()).length > 0))) throw new Error("Referral source code is required");
    if (!(((sortOrder == null) || (sortOrder >= 0)))) throw new Error("Display order cannot be negative");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "active";
        const __allowed: Record<string, string[]> = { "active": ["inactive"], "inactive": ["active"] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      name: name,
      code: code,
      sortOrder: ((sortOrder != null) ? sortOrder : 0),
      description: description,
      status: "active",
      registeredAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, referralSourceId: docId, tenantId: __after.tenantId, name: name, code: code, sortOrder: ((sortOrder != null) ? sortOrder : 0), _subject: { entity: "ReferralSource", command: "register", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ReferralSourceRegistered", entity: "ReferralSource", entityId: docId, payload: { referralSourceId: docId, tenantId: __after.tenantId, name: name, code: code, sortOrder: ((sortOrder != null) ? sortOrder : 0) }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ReferralSource_register = mutation({
  args: {
    docId: v.id("referralSources"),
    name: v.string(),
    code: v.string(),
    sortOrder: v.optional(v.any()),
    description: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runReferralSourceRegister(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ReferralSource_register", __result);
    }
    return __result;
  },
});

export const ReferralSource_createViaRegister = mutation({
  args: {
    name: v.string(),
    code: v.string(),
    sortOrder: v.optional(v.any()),
    description: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { name, code, sortOrder, description } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      sortOrder: args.sortOrder !== undefined ? args.sortOrder : 0,
      createdAt: Date.now(),
      updatedAt: Date.now(),
      status: "active",
      code: args.code,
      description: args.description,
      name: args.name
    };
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may read referral sources");
    if (!(checkRole(user, "salesManageAccess"))) throw new Error("Sales managers may write referral sources through commands");
    if (!(checkRole(user, "salesManageAccess"))) throw new Error("Sales managers may execute referral source commands");
    if (!((__draft.registeredAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((((name).trim()).length > 0))) throw new Error("Referral source name is required");
    if (!((((code).trim()).length > 0))) throw new Error("Referral source code is required");
    if (!(((sortOrder == null) || (sortOrder >= 0)))) throw new Error("Display order cannot be negative");
    const doc: Record<string, any> = {
      ...__draft,
      name: name,
      code: code,
      sortOrder: ((sortOrder != null) ? sortOrder : 0),
      description: description,
      status: "active",
      registeredAt: Date.now(),
      version: 1,
    };
    const docId = await ctx.db.insert("referralSources", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, referralSourceId: docId, tenantId: doc.tenantId, name: doc.name, code: doc.code, sortOrder: ((doc.sortOrder != null) ? doc.sortOrder : 0), _subject: { entity: "ReferralSource", command: "register", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ReferralSourceRegistered", entity: "ReferralSource", entityId: docId, payload: { referralSourceId: docId, tenantId: doc.tenantId, name: doc.name, code: doc.code, sortOrder: ((doc.sortOrder != null) ? doc.sortOrder : 0) }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ReferralSource_createViaRegister", __result);
    }
    return __result;
  },
});

async function __runReferralSourceReviseDetails(ctx: MutationCtx, { docId, name, description, sortOrder, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ReferralSource not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ReferralSource not found");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may read referral sources");
    if (!(checkRole(user, "salesManageAccess"))) throw new Error("Sales managers may write referral sources through commands");
    if (!(checkRole(user, "salesManageAccess"))) throw new Error("Sales managers may execute referral source commands");
    if (!((doc.registeredAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.status === "active"))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((((name).trim()).length > 0))) throw new Error("Referral source name is required");
    if (!(((sortOrder == null) || (sortOrder >= 0)))) throw new Error("Display order cannot be negative");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      name: name,
      description: description,
      sortOrder: ((sortOrder != null) ? sortOrder : doc.sortOrder),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, referralSourceId: docId, tenantId: __after.tenantId, name: name, sortOrder: ((sortOrder != null) ? sortOrder : __after.sortOrder), _subject: { entity: "ReferralSource", command: "reviseDetails", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ReferralSourceDetailsRevised", entity: "ReferralSource", entityId: docId, payload: { referralSourceId: docId, tenantId: __after.tenantId, name: name, sortOrder: ((sortOrder != null) ? sortOrder : __after.sortOrder) }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ReferralSource_reviseDetails = mutation({
  args: {
    docId: v.id("referralSources"),
    name: v.string(),
    description: v.optional(v.string()),
    sortOrder: v.optional(v.any()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runReferralSourceReviseDetails(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ReferralSource_reviseDetails", __result);
    }
    return __result;
  },
});

async function __runRevenueAttributionApply(ctx: MutationCtx, { docId, eventRevenue, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("RevenueAttribution not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("RevenueAttribution not found");
    if (!((checkRole(user, "financeAccess") || checkRole(user, "salesAccess")))) throw new Error("Finance and sales staff may read attributions");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may write attributions");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may execute attribution commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.status === "approved"))) throw new Error("Guard 1 failed");
    if (!((eventRevenue >= 0))) throw new Error("Guard 2 failed");
    const allocation = ((doc.allocationMethod === "percent") ? Math.round(((eventRevenue * doc.percentBasis) / 100)) : doc.fixedAmount);
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "applied";
        const __allowed: Record<string, string[]> = { "draft": ["pending_approval", "approved", "rejected"], "pending_approval": ["approved", "rejected", "draft"], "approved": ["applied"], "rejected": ["draft"], "applied": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      allocatedAmount: allocation,
      appliedAt: Date.now(),
      status: "applied",
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    return { ...doc, ...updates };
}

export const RevenueAttribution_apply = mutation({
  args: {
    docId: v.id("revenueAttributions"),
    eventRevenue: v.number(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runRevenueAttributionApply(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "RevenueAttribution_apply", __result);
    }
    return __result;
  },
});

async function __runRevenueAttributionApprove(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("RevenueAttribution not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("RevenueAttribution not found");
    if (!((checkRole(user, "financeAccess") || checkRole(user, "salesAccess")))) throw new Error("Finance and sales staff may read attributions");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may write attributions");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may execute attribution commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.status === "pending_approval"))) throw new Error("Guard 1 failed");
    if (!((checkRole(user, "financeAccess") || checkRole(user, "salesManageAccess")))) throw new Error("Guard 2 failed");
    if (!((user.id != null))) throw new Error("Guard 3 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "approved";
        const __allowed: Record<string, string[]> = { "draft": ["pending_approval", "approved", "rejected"], "pending_approval": ["approved", "rejected", "draft"], "approved": ["applied"], "rejected": ["draft"], "applied": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "approved",
      approvedById: user.id,
      approvedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    return { ...doc, ...updates };
}

export const RevenueAttribution_approve = mutation({
  args: {
    docId: v.id("revenueAttributions"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runRevenueAttributionApprove(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "RevenueAttribution_approve", __result);
    }
    return __result;
  },
});

async function __runRevenueAttributionCreate(ctx: MutationCtx, args: any) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc: Record<string, any> = {
      tenantId: __auth.tenantId,
      deletedAt: args.deletedAt,
      allocatedAmount: args.allocatedAmount ?? 0,
      approvedById: args.approvedById,
      approvedAt: args.approvedAt,
      rejectionReason: args.rejectionReason,
      notes: args.notes,
      appliedAt: args.appliedAt,
      createdAt: args.createdAt,
      updatedAt: args.updatedAt,
      eventId: args.eventId,
      attributionType: args.attributionType,
      allocationMethod: args.allocationMethod,
      percentBasis: ((args.percentBasis != null) ? args.percentBasis : 0),
      fixedAmount: ((args.fixedAmount != null) ? args.fixedAmount : 0),
      venueId: args.venueId,
      salespersonId: args.salespersonId,
      referralSourceId: args.referralSourceId,
      partnerPersonId: args.partnerPersonId,
      partnerClientId: args.partnerClientId,
      effectiveStartDate: args.effectiveStartDate,
      effectiveEndDate: args.effectiveEndDate,
      reason: args.reason,
      status: "draft",
      requestedById: user.id,
      requestedAt: Date.now(),
      version: 1
    };
    if (!((checkRole(user, "financeAccess") || checkRole(user, "salesAccess")))) throw new Error("Finance and sales staff may read attributions");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may write attributions");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may execute attribution commands");
    if (!((args.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!((user.id != null))) throw new Error("Guard 1 failed");
    const _id = await ctx.db.insert("revenueAttributions", doc as any);
    return { _id, ...doc };

}

export const RevenueAttribution_create = mutation({
  args: {
    deletedAt: v.optional(v.union(v.number(), v.null())),
    allocatedAmount: v.optional(v.number()),
    approvedById: v.optional(v.union(v.id("people"), v.null())),
    approvedAt: v.optional(v.union(v.number(), v.null())),
    rejectionReason: v.optional(v.union(v.string(), v.null())),
    notes: v.optional(v.union(v.string(), v.null())),
    appliedAt: v.optional(v.union(v.number(), v.null())),
    createdAt: v.optional(v.number()),
    updatedAt: v.optional(v.number()),
    eventId: v.string(),
    attributionType: v.any(),
    allocationMethod: v.any(),
    percentBasis: v.optional(v.number()),
    fixedAmount: v.optional(v.number()),
    venueId: v.optional(v.string()),
    salespersonId: v.optional(v.string()),
    referralSourceId: v.optional(v.string()),
    partnerPersonId: v.optional(v.string()),
    partnerClientId: v.optional(v.string()),
    effectiveStartDate: v.optional(v.number()),
    effectiveEndDate: v.optional(v.number()),
    reason: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runRevenueAttributionCreate(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "RevenueAttribution_create", __result);
    }
    return __result;
  },
});

async function __runRevenueAttributionReject(ctx: MutationCtx, { docId, rejectionReason, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("RevenueAttribution not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("RevenueAttribution not found");
    if (!((checkRole(user, "financeAccess") || checkRole(user, "salesAccess")))) throw new Error("Finance and sales staff may read attributions");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may write attributions");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may execute attribution commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.status === "pending_approval"))) throw new Error("Guard 1 failed");
    if (!((checkRole(user, "financeAccess") || checkRole(user, "salesManageAccess")))) throw new Error("Guard 2 failed");
    if (!((((rejectionReason).trim()).length > 0))) throw new Error("Guard 3 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "rejected";
        const __allowed: Record<string, string[]> = { "draft": ["pending_approval", "approved", "rejected"], "pending_approval": ["approved", "rejected", "draft"], "approved": ["applied"], "rejected": ["draft"], "applied": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "rejected",
      rejectionReason: rejectionReason,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    return { ...doc, ...updates };
}

export const RevenueAttribution_reject = mutation({
  args: {
    docId: v.id("revenueAttributions"),
    rejectionReason: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runRevenueAttributionReject(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "RevenueAttribution_reject", __result);
    }
    return __result;
  },
});

async function __runRevenueAttributionRequestApproval(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("RevenueAttribution not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("RevenueAttribution not found");
    if (!((checkRole(user, "financeAccess") || checkRole(user, "salesAccess")))) throw new Error("Finance and sales staff may read attributions");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may write attributions");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may execute attribution commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.status === "draft"))) throw new Error("Guard 1 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "pending_approval";
        const __allowed: Record<string, string[]> = { "draft": ["pending_approval", "approved", "rejected"], "pending_approval": ["approved", "rejected", "draft"], "approved": ["applied"], "rejected": ["draft"], "applied": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "pending_approval",
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    return { ...doc, ...updates };
}

export const RevenueAttribution_requestApproval = mutation({
  args: {
    docId: v.id("revenueAttributions"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runRevenueAttributionRequestApproval(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "RevenueAttribution_requestApproval", __result);
    }
    return __result;
  },
});

async function __runRevenueAttributionUpdate(ctx: MutationCtx, { docId, percentBasis, fixedAmount, effectiveStartDate, effectiveEndDate, reason, notes, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("RevenueAttribution not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("RevenueAttribution not found");
    if (!((checkRole(user, "financeAccess") || checkRole(user, "salesAccess")))) throw new Error("Finance and sales staff may read attributions");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may write attributions");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may execute attribution commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!(((doc.status === "draft") || (doc.status === "rejected")))) throw new Error("Guard 1 failed");
    if (!(((percentBasis == null) || ((percentBasis > 0) && (percentBasis <= 100))))) throw new Error("Guard 2 failed");
    if (!(((fixedAmount == null) || (fixedAmount >= 0)))) throw new Error("Guard 3 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      percentBasis: ((percentBasis != null) ? percentBasis : doc.percentBasis),
      fixedAmount: ((fixedAmount != null) ? fixedAmount : doc.fixedAmount),
      effectiveStartDate: ((effectiveStartDate != null) ? effectiveStartDate : doc.effectiveStartDate),
      effectiveEndDate: ((effectiveEndDate != null) ? effectiveEndDate : doc.effectiveEndDate),
      reason: ((reason != null) ? reason : doc.reason),
      notes: ((notes != null) ? notes : doc.notes),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    return { ...doc, ...updates };
}

export const RevenueAttribution_update = mutation({
  args: {
    docId: v.id("revenueAttributions"),
    percentBasis: v.optional(v.number()),
    fixedAmount: v.optional(v.number()),
    effectiveStartDate: v.optional(v.number()),
    effectiveEndDate: v.optional(v.number()),
    reason: v.optional(v.string()),
    notes: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runRevenueAttributionUpdate(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "RevenueAttribution_update", __result);
    }
    return __result;
  },
});

async function __runRoleScorecardArchive(ctx: MutationCtx, { docId, reason, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("RoleScorecard not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("RoleScorecard not found");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Workforce managers may read role scorecards");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Workforce managers may write role scorecards through commands");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Workforce managers may execute role scorecard commands");
    if (!((doc.status === "active"))) throw new Error("Guard 0 failed");
    if (!((doc.definedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((user.id != null))) throw new Error("Guard 3 failed");
    if (!((((reason).trim()).length > 0))) throw new Error("Archive reason is required");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "archived";
        const __allowed: Record<string, string[]> = { "active": ["archived"], "archived": ["active"] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "archived",
      archivedAt: Date.now(),
      archiveReason: reason,
      effectiveTo: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, scorecardId: docId, tenantId: __after.tenantId, reason: reason, _subject: { entity: "RoleScorecard", command: "archive", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "RoleScorecardArchived", entity: "RoleScorecard", entityId: docId, payload: { scorecardId: docId, tenantId: __after.tenantId, reason: reason }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const RoleScorecard_archive = mutation({
  args: {
    docId: v.id("roleScorecards"),
    reason: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runRoleScorecardArchive(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "RoleScorecard_archive", __result);
    }
    return __result;
  },
});

async function __runRoleScorecardDefine(ctx: MutationCtx, { docId, role, title, expectations, effectiveFrom, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("RoleScorecard not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("RoleScorecard not found");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Workforce managers may read role scorecards");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Workforce managers may write role scorecards through commands");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Workforce managers may execute role scorecard commands");
    if (!((doc.definedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((user.id != null))) throw new Error("Guard 2 failed");
    if (!((((title).trim()).length > 0))) throw new Error("Scorecard title is required");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      role: role,
      title: title,
      expectations: ((expectations != null) ? expectations : "[]"),
      effectiveFrom: effectiveFrom,
      definedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, scorecardId: docId, tenantId: __after.tenantId, role: role, title: title, _subject: { entity: "RoleScorecard", command: "define", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "RoleScorecardDefined", entity: "RoleScorecard", entityId: docId, payload: { scorecardId: docId, tenantId: __after.tenantId, role: role, title: title }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const RoleScorecard_define = mutation({
  args: {
    docId: v.id("roleScorecards"),
    role: v.any(),
    title: v.string(),
    expectations: v.optional(v.string()),
    effectiveFrom: v.optional(v.number()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runRoleScorecardDefine(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "RoleScorecard_define", __result);
    }
    return __result;
  },
});

export const RoleScorecard_createViaDefine = mutation({
  args: {
    role: v.any(),
    title: v.string(),
    expectations: v.optional(v.string()),
    effectiveFrom: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { role, title, expectations, effectiveFrom } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      expectations: args.expectations !== undefined ? args.expectations : "[]",
      status: "active",
      createdAt: Date.now(),
      updatedAt: Date.now(),
      effectiveFrom: args.effectiveFrom,
      role: args.role,
      title: args.title
    };
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Workforce managers may read role scorecards");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Workforce managers may write role scorecards through commands");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Workforce managers may execute role scorecard commands");
    if (!((__draft.definedAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((user.id != null))) throw new Error("Guard 2 failed");
    if (!((((title).trim()).length > 0))) throw new Error("Scorecard title is required");
    const doc: Record<string, any> = {
      ...__draft,
      role: role,
      title: title,
      expectations: ((expectations != null) ? expectations : "[]"),
      effectiveFrom: effectiveFrom,
      definedAt: Date.now(),
      version: 1,
    };
    const docId = await ctx.db.insert("roleScorecards", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, scorecardId: docId, tenantId: doc.tenantId, role: doc.role, title: doc.title, _subject: { entity: "RoleScorecard", command: "define", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "RoleScorecardDefined", entity: "RoleScorecard", entityId: docId, payload: { scorecardId: docId, tenantId: doc.tenantId, role: doc.role, title: doc.title }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "RoleScorecard_createViaDefine", __result);
    }
    return __result;
  },
});

async function __runRoleScorecardReactivate(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("RoleScorecard not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("RoleScorecard not found");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Workforce managers may read role scorecards");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Workforce managers may write role scorecards through commands");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Workforce managers may execute role scorecard commands");
    if (!((doc.status === "archived"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((user.id != null))) throw new Error("Guard 2 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "active";
        const __allowed: Record<string, string[]> = { "active": ["archived"], "archived": ["active"] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "active",
      archivedAt: null,
      archiveReason: null,
      effectiveTo: null,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, scorecardId: docId, tenantId: __after.tenantId, _subject: { entity: "RoleScorecard", command: "reactivate", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "RoleScorecardReactivated", entity: "RoleScorecard", entityId: docId, payload: { scorecardId: docId, tenantId: __after.tenantId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const RoleScorecard_reactivate = mutation({
  args: {
    docId: v.id("roleScorecards"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runRoleScorecardReactivate(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "RoleScorecard_reactivate", __result);
    }
    return __result;
  },
});

async function __runSavedReportDefinitionArchive(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("SavedReportDefinition not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("SavedReportDefinition not found");
    if (!((((doc.ownerId === user.id) || (doc.sharingScope !== "owner_only")) || checkRole(user, "manageAccess")))) throw new Error("Owners and managers may read owner-only reports; team and tenant_wide scopes are staff-readable");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may write saved reports through commands");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may execute saved report commands");
    if (!((doc.status === "active"))) throw new Error("Guard 0 failed");
    if (!((doc.definedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((user.id != null))) throw new Error("Guard 3 failed");
    if (!(((doc.ownerId === user.id) || checkRole(user, "manageAccess")))) throw new Error("Guard 4 failed");
    const previousStatus = doc.status;
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "archived";
        const __allowed: Record<string, string[]> = { "active": ["archived"], "archived": ["active"] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "archived",
      archivedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, savedReportDefinitionId: docId, tenantId: __after.tenantId, ownerId: __after.ownerId, previousStatus: previousStatus, status: "archived", _subject: { entity: "SavedReportDefinition", command: "archive", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "SavedReportArchived", entity: "SavedReportDefinition", entityId: docId, payload: { savedReportDefinitionId: docId, tenantId: __after.tenantId, ownerId: __after.ownerId, previousStatus: previousStatus, status: "archived" }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const SavedReportDefinition_archive = mutation({
  args: {
    docId: v.id("savedReportDefinitions"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runSavedReportDefinitionArchive(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "SavedReportDefinition_archive", __result);
    }
    return __result;
  },
});

async function __runSavedReportDefinitionChangeSharing(ctx: MutationCtx, { docId, sharingScope, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("SavedReportDefinition not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("SavedReportDefinition not found");
    if (!((((doc.ownerId === user.id) || (doc.sharingScope !== "owner_only")) || checkRole(user, "manageAccess")))) throw new Error("Owners and managers may read owner-only reports; team and tenant_wide scopes are staff-readable");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may write saved reports through commands");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may execute saved report commands");
    if (!((doc.status === "active"))) throw new Error("Guard 0 failed");
    if (!((doc.definedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((user.id != null))) throw new Error("Guard 3 failed");
    if (!(((doc.ownerId === user.id) || checkRole(user, "manageAccess")))) throw new Error("Guard 4 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      sharingScope: sharingScope,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, savedReportDefinitionId: docId, tenantId: __after.tenantId, ownerId: __after.ownerId, sharingScope: sharingScope, status: __after.status, _subject: { entity: "SavedReportDefinition", command: "changeSharing", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "SavedReportSharingChanged", entity: "SavedReportDefinition", entityId: docId, payload: { savedReportDefinitionId: docId, tenantId: __after.tenantId, ownerId: __after.ownerId, sharingScope: sharingScope, status: __after.status }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const SavedReportDefinition_changeSharing = mutation({
  args: {
    docId: v.id("savedReportDefinitions"),
    sharingScope: v.any(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runSavedReportDefinitionChangeSharing(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "SavedReportDefinition_changeSharing", __result);
    }
    return __result;
  },
});

async function __runSavedReportDefinitionCreateDefinition(ctx: MutationCtx, { docId, name, subjectArea, chartType, definition, sharingScope, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("SavedReportDefinition not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("SavedReportDefinition not found");
    if (!((((doc.ownerId === user.id) || (doc.sharingScope !== "owner_only")) || checkRole(user, "manageAccess")))) throw new Error("Owners and managers may read owner-only reports; team and tenant_wide scopes are staff-readable");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may write saved reports through commands");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may execute saved report commands");
    if (!((doc.definedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.status === "active"))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((user.id != null))) throw new Error("Guard 3 failed");
    if (!((((name).trim()).length > 0))) throw new Error("Report name is required");
    if (!((((chartType).trim()).length > 0))) throw new Error("Chart type is required");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      ownerId: user.id,
      name: name,
      subjectArea: subjectArea,
      chartType: chartType,
      definition: definition,
      sharingScope: ((sharingScope != null) ? sharingScope : "owner_only"),
      definedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, savedReportDefinitionId: docId, tenantId: __after.tenantId, ownerId: user.id, name: name, subjectArea: subjectArea, chartType: chartType, sharingScope: ((sharingScope != null) ? sharingScope : "owner_only"), status: "active", _subject: { entity: "SavedReportDefinition", command: "createDefinition", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "SavedReportDefinitionCreated", entity: "SavedReportDefinition", entityId: docId, payload: { savedReportDefinitionId: docId, tenantId: __after.tenantId, ownerId: user.id, name: name, subjectArea: subjectArea, chartType: chartType, sharingScope: ((sharingScope != null) ? sharingScope : "owner_only"), status: "active" }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const SavedReportDefinition_createDefinition = mutation({
  args: {
    docId: v.id("savedReportDefinitions"),
    name: v.string(),
    subjectArea: v.any(),
    chartType: v.string(),
    definition: v.any(),
    sharingScope: v.optional(v.any()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runSavedReportDefinitionCreateDefinition(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "SavedReportDefinition_createDefinition", __result);
    }
    return __result;
  },
});

export const SavedReportDefinition_createViaCreateDefinition = mutation({
  args: {
    name: v.string(),
    subjectArea: v.any(),
    chartType: v.string(),
    definition: v.any(),
    sharingScope: v.optional(v.any()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { name, subjectArea, chartType, definition, sharingScope } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      sharingScope: args.sharingScope !== undefined ? args.sharingScope : "owner_only",
      status: "active",
      createdAt: Date.now(),
      updatedAt: Date.now(),
      chartType: args.chartType,
      definition: args.definition,
      name: args.name,
      subjectArea: args.subjectArea
    };
    if (!((((__draft.ownerId === user.id) || (__draft.sharingScope !== "owner_only")) || checkRole(user, "manageAccess")))) throw new Error("Owners and managers may read owner-only reports; team and tenant_wide scopes are staff-readable");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may write saved reports through commands");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may execute saved report commands");
    if (!((__draft.definedAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.status === "active"))) throw new Error("Guard 1 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((user.id != null))) throw new Error("Guard 3 failed");
    if (!((((name).trim()).length > 0))) throw new Error("Report name is required");
    if (!((((chartType).trim()).length > 0))) throw new Error("Chart type is required");
    const doc: Record<string, any> = {
      ...__draft,
      ownerId: user.id,
      name: name,
      subjectArea: subjectArea,
      chartType: chartType,
      definition: definition,
      sharingScope: ((sharingScope != null) ? sharingScope : "owner_only"),
      definedAt: Date.now(),
      version: 1,
    };
    const docId = await ctx.db.insert("savedReportDefinitions", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, savedReportDefinitionId: docId, tenantId: doc.tenantId, ownerId: user.id, name: doc.name, subjectArea: doc.subjectArea, chartType: doc.chartType, sharingScope: ((doc.sharingScope != null) ? doc.sharingScope : "owner_only"), status: "active", _subject: { entity: "SavedReportDefinition", command: "createDefinition", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "SavedReportDefinitionCreated", entity: "SavedReportDefinition", entityId: docId, payload: { savedReportDefinitionId: docId, tenantId: doc.tenantId, ownerId: user.id, name: doc.name, subjectArea: doc.subjectArea, chartType: doc.chartType, sharingScope: ((doc.sharingScope != null) ? doc.sharingScope : "owner_only"), status: "active" }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "SavedReportDefinition_createViaCreateDefinition", __result);
    }
    return __result;
  },
});

async function __runSavedReportDefinitionRename(ctx: MutationCtx, { docId, name, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("SavedReportDefinition not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("SavedReportDefinition not found");
    if (!((((doc.ownerId === user.id) || (doc.sharingScope !== "owner_only")) || checkRole(user, "manageAccess")))) throw new Error("Owners and managers may read owner-only reports; team and tenant_wide scopes are staff-readable");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may write saved reports through commands");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may execute saved report commands");
    if (!((doc.status === "active"))) throw new Error("Guard 0 failed");
    if (!((doc.definedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((user.id != null))) throw new Error("Guard 3 failed");
    if (!(((doc.ownerId === user.id) || checkRole(user, "manageAccess")))) throw new Error("Guard 4 failed");
    if (!((((name).trim()).length > 0))) throw new Error("Report name is required");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      name: name,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, savedReportDefinitionId: docId, tenantId: __after.tenantId, ownerId: __after.ownerId, name: name, status: __after.status, _subject: { entity: "SavedReportDefinition", command: "rename", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "SavedReportRenamed", entity: "SavedReportDefinition", entityId: docId, payload: { savedReportDefinitionId: docId, tenantId: __after.tenantId, ownerId: __after.ownerId, name: name, status: __after.status }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const SavedReportDefinition_rename = mutation({
  args: {
    docId: v.id("savedReportDefinitions"),
    name: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runSavedReportDefinitionRename(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "SavedReportDefinition_rename", __result);
    }
    return __result;
  },
});

async function __runSavedReportDefinitionRestore(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("SavedReportDefinition not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("SavedReportDefinition not found");
    if (!((((doc.ownerId === user.id) || (doc.sharingScope !== "owner_only")) || checkRole(user, "manageAccess")))) throw new Error("Owners and managers may read owner-only reports; team and tenant_wide scopes are staff-readable");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may write saved reports through commands");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may execute saved report commands");
    if (!((doc.status === "archived"))) throw new Error("Guard 0 failed");
    if (!((doc.definedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((user.id != null))) throw new Error("Guard 3 failed");
    if (!(((doc.ownerId === user.id) || checkRole(user, "manageAccess")))) throw new Error("Guard 4 failed");
    const previousStatus = doc.status;
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "active";
        const __allowed: Record<string, string[]> = { "active": ["archived"], "archived": ["active"] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "active",
      restoredAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, savedReportDefinitionId: docId, tenantId: __after.tenantId, ownerId: __after.ownerId, previousStatus: previousStatus, status: "active", _subject: { entity: "SavedReportDefinition", command: "restore", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "SavedReportRestored", entity: "SavedReportDefinition", entityId: docId, payload: { savedReportDefinitionId: docId, tenantId: __after.tenantId, ownerId: __after.ownerId, previousStatus: previousStatus, status: "active" }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const SavedReportDefinition_restore = mutation({
  args: {
    docId: v.id("savedReportDefinitions"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runSavedReportDefinitionRestore(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "SavedReportDefinition_restore", __result);
    }
    return __result;
  },
});

async function __runSavedReportDefinitionUpdateDefinition(ctx: MutationCtx, { docId, chartType, definition, subjectArea, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("SavedReportDefinition not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("SavedReportDefinition not found");
    if (!((((doc.ownerId === user.id) || (doc.sharingScope !== "owner_only")) || checkRole(user, "manageAccess")))) throw new Error("Owners and managers may read owner-only reports; team and tenant_wide scopes are staff-readable");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may write saved reports through commands");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may execute saved report commands");
    if (!((doc.status === "active"))) throw new Error("Guard 0 failed");
    if (!((doc.definedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((user.id != null))) throw new Error("Guard 3 failed");
    if (!(((doc.ownerId === user.id) || checkRole(user, "manageAccess")))) throw new Error("Guard 4 failed");
    if (!(((chartType == null) || (((chartType).trim()).length > 0)))) throw new Error("Chart type cannot be blank");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      chartType: ((chartType != null) ? chartType : doc.chartType),
      definition: ((definition != null) ? definition : doc.definition),
      subjectArea: ((subjectArea != null) ? subjectArea : doc.subjectArea),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, savedReportDefinitionId: docId, tenantId: __after.tenantId, ownerId: __after.ownerId, chartType: __after.chartType, subjectArea: __after.subjectArea, status: __after.status, _subject: { entity: "SavedReportDefinition", command: "updateDefinition", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "SavedReportDefinitionUpdated", entity: "SavedReportDefinition", entityId: docId, payload: { savedReportDefinitionId: docId, tenantId: __after.tenantId, ownerId: __after.ownerId, chartType: __after.chartType, subjectArea: __after.subjectArea, status: __after.status }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const SavedReportDefinition_updateDefinition = mutation({
  args: {
    docId: v.id("savedReportDefinitions"),
    chartType: v.optional(v.string()),
    definition: v.optional(v.any()),
    subjectArea: v.optional(v.any()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runSavedReportDefinitionUpdateDefinition(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "SavedReportDefinition_updateDefinition", __result);
    }
    return __result;
  },
});

async function __runServiceStyleActivate(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ServiceStyle not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ServiceStyle not found");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may read service styles");
    if (!(checkRole(user, "eventManageAccess"))) throw new Error("Event managers may write service styles through commands");
    if (!(checkRole(user, "eventManageAccess"))) throw new Error("Event managers may execute service style commands");
    if (!((doc.status === "inactive"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(checkRole(user, "eventManageAccess"))) throw new Error("Guard 2 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "active";
        const __allowed: Record<string, string[]> = { "active": ["inactive"], "inactive": ["active"] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "active",
      deactivatedAt: null,
      deactivationReason: null,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, serviceStyleId: docId, tenantId: __after.tenantId, _subject: { entity: "ServiceStyle", command: "activate", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ServiceStyleActivated", entity: "ServiceStyle", entityId: docId, payload: { serviceStyleId: docId, tenantId: __after.tenantId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ServiceStyle_activate = mutation({
  args: {
    docId: v.id("serviceStyles"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runServiceStyleActivate(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ServiceStyle_activate", __result);
    }
    return __result;
  },
});

async function __runServiceStyleDeactivate(ctx: MutationCtx, { docId, reason, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ServiceStyle not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ServiceStyle not found");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may read service styles");
    if (!(checkRole(user, "eventManageAccess"))) throw new Error("Event managers may write service styles through commands");
    if (!(checkRole(user, "eventManageAccess"))) throw new Error("Event managers may execute service style commands");
    if (!((doc.status === "active"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(checkRole(user, "eventManageAccess"))) throw new Error("Guard 2 failed");
    if (!((((reason).trim()).length > 0))) throw new Error("Deactivation reason is required");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "inactive";
        const __allowed: Record<string, string[]> = { "active": ["inactive"], "inactive": ["active"] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "inactive",
      deactivatedAt: Date.now(),
      deactivationReason: reason,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, serviceStyleId: docId, tenantId: __after.tenantId, reason: reason, _subject: { entity: "ServiceStyle", command: "deactivate", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ServiceStyleDeactivated", entity: "ServiceStyle", entityId: docId, payload: { serviceStyleId: docId, tenantId: __after.tenantId, reason: reason }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ServiceStyle_deactivate = mutation({
  args: {
    docId: v.id("serviceStyles"),
    reason: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runServiceStyleDeactivate(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ServiceStyle_deactivate", __result);
    }
    return __result;
  },
});

async function __runServiceStyleRegister(ctx: MutationCtx, { docId, name, code, sortOrder, description, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ServiceStyle not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ServiceStyle not found");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may read service styles");
    if (!(checkRole(user, "eventManageAccess"))) throw new Error("Event managers may write service styles through commands");
    if (!(checkRole(user, "eventManageAccess"))) throw new Error("Event managers may execute service style commands");
    if (!((doc.registeredAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((((name).trim()).length > 0))) throw new Error("Service style name is required");
    if (!((((code).trim()).length > 0))) throw new Error("Service style code is required");
    if (!(((sortOrder == null) || (sortOrder >= 0)))) throw new Error("Display order cannot be negative");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "active";
        const __allowed: Record<string, string[]> = { "active": ["inactive"], "inactive": ["active"] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      name: name,
      code: code,
      sortOrder: ((sortOrder != null) ? sortOrder : 0),
      description: description,
      status: "active",
      registeredAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, serviceStyleId: docId, tenantId: __after.tenantId, name: name, code: code, sortOrder: ((sortOrder != null) ? sortOrder : 0), _subject: { entity: "ServiceStyle", command: "register", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ServiceStyleRegistered", entity: "ServiceStyle", entityId: docId, payload: { serviceStyleId: docId, tenantId: __after.tenantId, name: name, code: code, sortOrder: ((sortOrder != null) ? sortOrder : 0) }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ServiceStyle_register = mutation({
  args: {
    docId: v.id("serviceStyles"),
    name: v.string(),
    code: v.string(),
    sortOrder: v.optional(v.any()),
    description: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runServiceStyleRegister(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ServiceStyle_register", __result);
    }
    return __result;
  },
});

export const ServiceStyle_createViaRegister = mutation({
  args: {
    name: v.string(),
    code: v.string(),
    sortOrder: v.optional(v.any()),
    description: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { name, code, sortOrder, description } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      sortOrder: args.sortOrder !== undefined ? args.sortOrder : 0,
      createdAt: Date.now(),
      updatedAt: Date.now(),
      status: "active",
      code: args.code,
      description: args.description,
      name: args.name
    };
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may read service styles");
    if (!(checkRole(user, "eventManageAccess"))) throw new Error("Event managers may write service styles through commands");
    if (!(checkRole(user, "eventManageAccess"))) throw new Error("Event managers may execute service style commands");
    if (!((__draft.registeredAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((((name).trim()).length > 0))) throw new Error("Service style name is required");
    if (!((((code).trim()).length > 0))) throw new Error("Service style code is required");
    if (!(((sortOrder == null) || (sortOrder >= 0)))) throw new Error("Display order cannot be negative");
    const doc: Record<string, any> = {
      ...__draft,
      name: name,
      code: code,
      sortOrder: ((sortOrder != null) ? sortOrder : 0),
      description: description,
      status: "active",
      registeredAt: Date.now(),
      version: 1,
    };
    const docId = await ctx.db.insert("serviceStyles", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, serviceStyleId: docId, tenantId: doc.tenantId, name: doc.name, code: doc.code, sortOrder: ((doc.sortOrder != null) ? doc.sortOrder : 0), _subject: { entity: "ServiceStyle", command: "register", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ServiceStyleRegistered", entity: "ServiceStyle", entityId: docId, payload: { serviceStyleId: docId, tenantId: doc.tenantId, name: doc.name, code: doc.code, sortOrder: ((doc.sortOrder != null) ? doc.sortOrder : 0) }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ServiceStyle_createViaRegister", __result);
    }
    return __result;
  },
});

async function __runServiceStyleReviseDetails(ctx: MutationCtx, { docId, name, description, sortOrder, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ServiceStyle not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ServiceStyle not found");
    if (!((checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) throw new Error("Event and sales staff may read service styles");
    if (!(checkRole(user, "eventManageAccess"))) throw new Error("Event managers may write service styles through commands");
    if (!(checkRole(user, "eventManageAccess"))) throw new Error("Event managers may execute service style commands");
    if (!((doc.registeredAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.status === "active"))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((((name).trim()).length > 0))) throw new Error("Service style name is required");
    if (!(((sortOrder == null) || (sortOrder >= 0)))) throw new Error("Display order cannot be negative");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      name: name,
      description: description,
      sortOrder: ((sortOrder != null) ? sortOrder : doc.sortOrder),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, serviceStyleId: docId, tenantId: __after.tenantId, name: name, sortOrder: ((sortOrder != null) ? sortOrder : __after.sortOrder), _subject: { entity: "ServiceStyle", command: "reviseDetails", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ServiceStyleDetailsRevised", entity: "ServiceStyle", entityId: docId, payload: { serviceStyleId: docId, tenantId: __after.tenantId, name: name, sortOrder: ((sortOrder != null) ? sortOrder : __after.sortOrder) }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ServiceStyle_reviseDetails = mutation({
  args: {
    docId: v.id("serviceStyles"),
    name: v.string(),
    description: v.optional(v.string()),
    sortOrder: v.optional(v.any()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runServiceStyleReviseDetails(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ServiceStyle_reviseDetails", __result);
    }
    return __result;
  },
});

async function __runShareLinkCreate(ctx: MutationCtx, args: any) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc: Record<string, any> = {
      tenantId: __auth.tenantId,
      deletedAt: args.deletedAt,
      revokedAt: args.revokedAt,
      revokedByPersonId: args.revokedByPersonId,
      viewCount: args.viewCount ?? 0,
      firstViewedAt: args.firstViewedAt,
      lastViewedAt: args.lastViewedAt,
      lastViewerIdentity: args.lastViewerIdentity,
      createdAt: args.createdAt,
      updatedAt: args.updatedAt,
      proposalId: args.proposalId,
      proposalRevisionId: args.proposalRevisionId,
      status: "active",
      expiresAt: args.expiresAt,
      createdByPersonId: user.id,
      version: 1
    };
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may read share links");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may write share links through commands");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may execute share link commands");
    if (!((args.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!((user.id != null))) throw new Error("Guard 1 failed");
    if (!(((args.expiresAt == null) || (args.expiresAt > Date.now())))) throw new Error("Share link expiry must be in the future");
    const _id = await ctx.db.insert("shareLinks", doc as any);
    const payload: Record<string, any> = { _id, id: _id, ...doc, result: { _id, id: _id, ...doc }, shareLinkId: _id, tenantId: doc.tenantId, proposalId: doc.proposalId, proposalRevisionId: doc.proposalRevisionId, createdByPersonId: ((user.id != null) ? user.id : ""), expiresAt: doc.expiresAt, _subject: { entity: "ShareLink", command: "create", id: _id } };
    await ctx.db.insert("manifestEvents", { type: "ShareLinkCreated", entity: "ShareLink", entityId: _id, payload: { shareLinkId: _id, tenantId: doc.tenantId, proposalId: doc.proposalId, proposalRevisionId: doc.proposalRevisionId, createdByPersonId: ((user.id != null) ? user.id : ""), expiresAt: doc.expiresAt }, createdAt: Date.now() });
    return { _id, ...doc };

}

export const ShareLink_create = mutation({
  args: {
    deletedAt: v.optional(v.union(v.number(), v.null())),
    revokedAt: v.optional(v.union(v.number(), v.null())),
    revokedByPersonId: v.optional(v.union(v.string(), v.null())),
    viewCount: v.optional(v.number()),
    firstViewedAt: v.optional(v.union(v.number(), v.null())),
    lastViewedAt: v.optional(v.union(v.number(), v.null())),
    lastViewerIdentity: v.optional(v.union(v.string(), v.null())),
    createdAt: v.optional(v.number()),
    updatedAt: v.optional(v.number()),
    proposalId: v.string(),
    proposalRevisionId: v.string(),
    expiresAt: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runShareLinkCreate(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ShareLink_create", __result);
    }
    return __result;
  },
});

async function __runShareLinkRevoke(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ShareLink not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ShareLink not found");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may read share links");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may write share links through commands");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may execute share link commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.status === "active"))) throw new Error("Guard 1 failed");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Guard 2 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "revoked",
      revokedAt: Date.now(),
      revokedByPersonId: user.id,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, shareLinkId: docId, tenantId: __after.tenantId, proposalId: __after.proposalId, revokedByPersonId: ((user.id != null) ? user.id : ""), revokedAt: Date.now(), _subject: { entity: "ShareLink", command: "revoke", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ShareLinkRevoked", entity: "ShareLink", entityId: docId, payload: { shareLinkId: docId, tenantId: __after.tenantId, proposalId: __after.proposalId, revokedByPersonId: ((user.id != null) ? user.id : ""), revokedAt: Date.now() }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ShareLink_revoke = mutation({
  args: {
    docId: v.id("shareLinks"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runShareLinkRevoke(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ShareLink_revoke", __result);
    }
    return __result;
  },
});

async function __runShiftApplyApprovedSwap(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("Shift not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("Shift not found");
    const doc = await __decryptDoc(ctx, "Shift", ["notes"], __storedDoc) as Record<string, any>;
    const __rel_swapAuthorization = await __resolveRelation(ctx, "shiftSwapRequests", [__auth.tenantId, doc.swapAuthorizationId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_swapTargetPerson = await __resolveRelation(ctx, "people", [__auth.tenantId, doc.swapTargetPersonId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_swapTargetQualification = await __resolveRelation(ctx, "qualifications", [__auth.tenantId, doc.swapTargetQualificationId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_swapTargetTrainingCompletion = await __resolveRelation(ctx, "trainingCompletions", [__auth.tenantId, doc.swapTargetTrainingCompletionId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).swapAuthorizationId;
      ((doc as any) as any).swapAuthorization = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    {
      const __fk = ((doc as any) as any).swapTargetPersonId;
      ((doc as any) as any).swapTargetPerson = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    {
      const __fk = ((doc as any) as any).swapTargetQualificationId;
      ((doc as any) as any).swapTargetQualification = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    {
      const __fk = ((doc as any) as any).swapTargetTrainingCompletionId;
      ((doc as any) as any).swapTargetTrainingCompletion = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may read shifts");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may write shifts through commands");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may execute shift commands");
    if (!((doc.status === "scheduled"))) throw new Error("Guard 0 failed");
    if (!((doc.scheduledAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Guard 3 failed");
    if (!((((__rel_swapAuthorization != null) && (__rel_swapAuthorization.deletedAt == null)) && (__rel_swapAuthorization.status === "approved")))) throw new Error("Guard 4 failed");
    if (!((((__rel_swapTargetPerson != null) && (__rel_swapTargetPerson.deletedAt == null)) && (__rel_swapTargetPerson.status === "active")))) throw new Error("Guard 5 failed");
    if (!((__rel_swapTargetPerson.authSubjectId != null))) throw new Error("Guard 6 failed");
    if (!(((doc.swapTargetQualificationId == null) || (((__rel_swapTargetQualification != null) && (__rel_swapTargetQualification.deletedAt == null)) && (__rel_swapTargetQualification.status === "active"))))) throw new Error("Guard 7 failed");
    if (!(((doc.swapTargetTrainingCompletionId == null) || (((__rel_swapTargetTrainingCompletion != null) && (__rel_swapTargetTrainingCompletion.deletedAt == null)) && (__rel_swapTargetTrainingCompletion.recordedAt != null))))) throw new Error("Guard 8 failed");
    if (!((__rel_swapAuthorization.requesterPersonId === doc.personId))) throw new Error("Swap approval does not match the assigned staff member");
    if (!((__rel_swapAuthorization.recipientPersonId === doc.swapTargetPersonId))) throw new Error("Swap approval does not match the recipient");
    if (!((__rel_swapAuthorization.sourceQualificationId === doc.requiredQualificationId))) throw new Error("The shift certification requirement changed after approval");
    if (!((__rel_swapAuthorization.targetQualificationId === doc.swapTargetQualificationId))) throw new Error("Swap approval does not match the replacement certification");
    if (!((__rel_swapAuthorization.targetTrainingCompletionId === doc.swapTargetTrainingCompletionId))) throw new Error("Swap approval does not match the recipient training proof");
    const previousPersonId = doc.personId;
    const shiftSwapRequestId = doc.swapAuthorizationId;
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      personId: doc.swapTargetPersonId,
      requiredQualificationId: doc.swapTargetQualificationId,
      requiredTrainingCompletionId: doc.swapTargetTrainingCompletionId,
      swapAuthorizationId: null,
      swapTargetPersonId: null,
      swapTargetQualificationId: null,
      swapTargetTrainingCompletionId: null,
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "Shift", ["notes"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, shiftId: docId, tenantId: __after.tenantId, shiftSwapRequestId: shiftSwapRequestId, previousPersonId: previousPersonId, personId: __after.personId, requiredQualificationId: __after.requiredQualificationId, requiredTrainingCompletionId: __after.requiredTrainingCompletionId, _subject: { entity: "Shift", command: "applyApprovedSwap", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ShiftSwapped", entity: "Shift", entityId: docId, payload: { shiftId: docId, tenantId: __after.tenantId, shiftSwapRequestId: shiftSwapRequestId, previousPersonId: previousPersonId, personId: __after.personId, requiredQualificationId: __after.requiredQualificationId, requiredTrainingCompletionId: __after.requiredTrainingCompletionId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Shift_applyApprovedSwap = mutation({
  args: {
    docId: v.id("shifts"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runShiftApplyApprovedSwap(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Shift_applyApprovedSwap", __result);
    }
    return __result;
  },
});

async function __runShiftCancel(ctx: MutationCtx, { docId, reason, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("Shift not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("Shift not found");
    const doc = await __decryptDoc(ctx, "Shift", ["notes"], __storedDoc) as Record<string, any>;
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may read shifts");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may write shifts through commands");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may execute shift commands");
    if (!(((doc.status === "scheduled") || (doc.status === "started")))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Guard 2 failed");
    if (!((((reason).trim()).length > 0))) throw new Error("Cancellation reason is required");
    const previousStatus = doc.status;
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "cancelled";
        const __allowed: Record<string, string[]> = { "scheduled": ["started", "cancelled", "no_show"], "started": ["completed", "cancelled", "no_show"], "completed": [], "cancelled": [], "no_show": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "cancelled",
      cancelledAt: Date.now(),
      cancellationReason: reason,
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "Shift", ["notes"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, shiftId: docId, tenantId: __after.tenantId, personId: __after.personId, eventId: __after.eventId, startsAt: __after.startsAt, endsAt: __after.endsAt, previousStatus: previousStatus, status: "cancelled", reason: reason, _subject: { entity: "Shift", command: "cancel", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ShiftCancelled", entity: "Shift", entityId: docId, payload: { shiftId: docId, tenantId: __after.tenantId, personId: __after.personId, eventId: __after.eventId, startsAt: __after.startsAt, endsAt: __after.endsAt, previousStatus: previousStatus, status: "cancelled", reason: reason }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Shift_cancel = mutation({
  args: {
    docId: v.id("shifts"),
    reason: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runShiftCancel(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Shift_cancel", __result);
    }
    return __result;
  },
});

async function __runShiftComplete(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("Shift not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("Shift not found");
    const doc = await __decryptDoc(ctx, "Shift", ["notes"], __storedDoc) as Record<string, any>;
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may read shifts");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may write shifts through commands");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may execute shift commands");
    if (!((doc.status === "started"))) throw new Error("Guard 0 failed");
    if (!((doc.startedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((user.id != null))) throw new Error("Guard 3 failed");
    if (!(((doc.personId === user.id) || checkRole(user, "workforceManageAccess")))) throw new Error("Guard 4 failed");
    const previousStatus = doc.status;
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "completed";
        const __allowed: Record<string, string[]> = { "scheduled": ["started", "cancelled", "no_show"], "started": ["completed", "cancelled", "no_show"], "completed": [], "cancelled": [], "no_show": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "completed",
      completedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "Shift", ["notes"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, shiftId: docId, tenantId: __after.tenantId, personId: __after.personId, eventId: __after.eventId, startsAt: __after.startsAt, endsAt: __after.endsAt, previousStatus: previousStatus, status: "completed", _subject: { entity: "Shift", command: "complete", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ShiftCompleted", entity: "Shift", entityId: docId, payload: { shiftId: docId, tenantId: __after.tenantId, personId: __after.personId, eventId: __after.eventId, startsAt: __after.startsAt, endsAt: __after.endsAt, previousStatus: previousStatus, status: "completed" }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Shift_complete = mutation({
  args: {
    docId: v.id("shifts"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runShiftComplete(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Shift_complete", __result);
    }
    return __result;
  },
});

async function __runShiftMarkNoShow(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("Shift not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("Shift not found");
    const doc = await __decryptDoc(ctx, "Shift", ["notes"], __storedDoc) as Record<string, any>;
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may read shifts");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may write shifts through commands");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may execute shift commands");
    if (!(((doc.status === "scheduled") || (doc.status === "started")))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Guard 2 failed");
    const previousStatus = doc.status;
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "no_show";
        const __allowed: Record<string, string[]> = { "scheduled": ["started", "cancelled", "no_show"], "started": ["completed", "cancelled", "no_show"], "completed": [], "cancelled": [], "no_show": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "no_show",
      noShowAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "Shift", ["notes"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, shiftId: docId, tenantId: __after.tenantId, personId: __after.personId, eventId: __after.eventId, startsAt: __after.startsAt, endsAt: __after.endsAt, previousStatus: previousStatus, status: "no_show", _subject: { entity: "Shift", command: "markNoShow", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ShiftNoShowMarked", entity: "Shift", entityId: docId, payload: { shiftId: docId, tenantId: __after.tenantId, personId: __after.personId, eventId: __after.eventId, startsAt: __after.startsAt, endsAt: __after.endsAt, previousStatus: previousStatus, status: "no_show" }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Shift_markNoShow = mutation({
  args: {
    docId: v.id("shifts"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runShiftMarkNoShow(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Shift_markNoShow", __result);
    }
    return __result;
  },
});

async function __runShiftSchedule(ctx: MutationCtx, { docId, personId, startsAt, endsAt, eventId, role, shiftTypeId, requiredQualificationId, requiredTrainingCompletionId, notes, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("Shift not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("Shift not found");
    const doc = await __decryptDoc(ctx, "Shift", ["notes"], __storedDoc) as Record<string, any>;
    const __rel_person = await __resolveRelation(ctx, "people", [__auth.tenantId, doc.personId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_shiftType = await __resolveRelation(ctx, "shiftTypes", [__auth.tenantId, doc.shiftTypeId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_requiredQualification = await __resolveRelation(ctx, "qualifications", [__auth.tenantId, doc.requiredQualificationId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_requiredTrainingCompletion = await __resolveRelation(ctx, "trainingCompletions", [__auth.tenantId, doc.requiredTrainingCompletionId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).personId;
      ((doc as any) as any).person = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    {
      const __fk = ((doc as any) as any).shiftTypeId;
      ((doc as any) as any).shiftType = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    {
      const __fk = ((doc as any) as any).requiredTrainingCompletionId;
      ((doc as any) as any).requiredTrainingCompletion = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    {
      const __fk = ((doc as any) as any).requiredQualificationId;
      ((doc as any) as any).requiredQualification = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may read shifts");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may write shifts through commands");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may execute shift commands");
    if (!((doc.scheduledAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.status === "scheduled"))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((__rel_person != null))) throw new Error("Guard 3 failed");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Guard 4 failed");
    if (!((personId === doc.personId))) throw new Error("Schedule personId must match the seeded person reference");
    if (!((((eventId == null) || (doc.eventId == null)) || (eventId === doc.eventId)))) throw new Error("Schedule eventId must match the seeded event reference when provided");
    if (!((endsAt > startsAt))) throw new Error("Shift end must be after its start");
    if (!(((shiftTypeId == null) || (((__rel_shiftType != null) && (__rel_shiftType.deletedAt == null)) && (__rel_shiftType.status === "active"))))) throw new Error("Selected shift type must be active");
    if (!(((((shiftTypeId == null) || (__rel_shiftType == null)) || (__rel_shiftType.requiredTrainingModuleId == null)) || (requiredTrainingCompletionId != null)))) throw new Error("This shift type requires completed training before scheduling");
    if (!(((requiredTrainingCompletionId == null) || ((((__rel_requiredTrainingCompletion != null) && (__rel_requiredTrainingCompletion.deletedAt == null)) && (__rel_requiredTrainingCompletion.recordedAt != null)) && (__rel_requiredTrainingCompletion.personId === personId))))) throw new Error("Training completion must be recorded for the selected person");
    if (!(((requiredTrainingCompletionId == null) || ((((__rel_shiftType != null) && (__rel_shiftType.requiredTrainingModuleId != null)) && (__rel_requiredTrainingCompletion != null)) && (__rel_requiredTrainingCompletion.trainingModuleId === __rel_shiftType.requiredTrainingModuleId))))) throw new Error("Training completion must match the module required by the shift type");
    if (!(((requiredQualificationId == null) || ((__rel_requiredQualification != null) && (__rel_requiredQualification.personId === personId))))) throw new Error("Required certification must belong to the selected person");
    if (!(((requiredQualificationId == null) || (((__rel_requiredQualification != null) && (__rel_requiredQualification.deletedAt == null)) && (__rel_requiredQualification.status === "active"))))) throw new Error("Required certification must be active");
    if (!(((requiredQualificationId == null) || ((__rel_requiredQualification != null) && ((__rel_requiredQualification.expiresAt == null) || (__rel_requiredQualification.expiresAt >= endsAt)))))) throw new Error("Required certification must remain valid through the shift");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      personId: personId,
      eventId: ((eventId != null) ? eventId : doc.eventId),
      startsAt: startsAt,
      endsAt: endsAt,
      role: ((role != null) ? role : doc.role),
      shiftTypeId: ((shiftTypeId != null) ? shiftTypeId : doc.shiftTypeId),
      requiredQualificationId: ((requiredQualificationId != null) ? requiredQualificationId : doc.requiredQualificationId),
      requiredTrainingCompletionId: ((requiredTrainingCompletionId != null) ? requiredTrainingCompletionId : doc.requiredTrainingCompletionId),
      notes: ((notes != null) ? notes : doc.notes),
      scheduledAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "Shift", ["notes"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, shiftId: docId, tenantId: __after.tenantId, personId: personId, eventId: ((eventId != null) ? eventId : __after.eventId), startsAt: startsAt, endsAt: endsAt, role: ((role != null) ? role : __after.role), shiftTypeId: ((shiftTypeId != null) ? shiftTypeId : __after.shiftTypeId), requiredQualificationId: ((requiredQualificationId != null) ? requiredQualificationId : __after.requiredQualificationId), requiredTrainingCompletionId: ((requiredTrainingCompletionId != null) ? requiredTrainingCompletionId : __after.requiredTrainingCompletionId), status: "scheduled", _subject: { entity: "Shift", command: "schedule", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ShiftScheduled", entity: "Shift", entityId: docId, payload: { shiftId: docId, tenantId: __after.tenantId, personId: personId, eventId: ((eventId != null) ? eventId : __after.eventId), startsAt: startsAt, endsAt: endsAt, role: ((role != null) ? role : __after.role), shiftTypeId: ((shiftTypeId != null) ? shiftTypeId : __after.shiftTypeId), requiredQualificationId: ((requiredQualificationId != null) ? requiredQualificationId : __after.requiredQualificationId), requiredTrainingCompletionId: ((requiredTrainingCompletionId != null) ? requiredTrainingCompletionId : __after.requiredTrainingCompletionId), status: "scheduled" }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Shift_schedule = mutation({
  args: {
    docId: v.id("shifts"),
    personId: v.string(),
    startsAt: v.number(),
    endsAt: v.number(),
    eventId: v.optional(v.string()),
    role: v.optional(v.string()),
    shiftTypeId: v.optional(v.string()),
    requiredQualificationId: v.optional(v.string()),
    requiredTrainingCompletionId: v.optional(v.string()),
    notes: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runShiftSchedule(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Shift_schedule", __result);
    }
    return __result;
  },
});

export const Shift_createViaSchedule = mutation({
  args: {
    personId: v.string(),
    startsAt: v.number(),
    endsAt: v.number(),
    eventId: v.optional(v.string()),
    role: v.optional(v.string()),
    shiftTypeId: v.optional(v.string()),
    requiredQualificationId: v.optional(v.string()),
    requiredTrainingCompletionId: v.optional(v.string()),
    notes: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { personId, startsAt, endsAt, eventId, role, shiftTypeId, requiredQualificationId, requiredTrainingCompletionId, notes } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      status: "scheduled",
      createdAt: Date.now(),
      updatedAt: Date.now(),
      endsAt: args.endsAt,
      eventId: args.eventId,
      notes: args.notes,
      personId: args.personId,
      requiredQualificationId: args.requiredQualificationId,
      requiredTrainingCompletionId: args.requiredTrainingCompletionId,
      role: args.role,
      shiftTypeId: args.shiftTypeId,
      startsAt: args.startsAt
    };
    const __rel_person = await __resolveRelation(ctx, "people", [__auth.tenantId, __draft.personId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_shiftType = await __resolveRelation(ctx, "shiftTypes", [__auth.tenantId, __draft.shiftTypeId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_requiredQualification = await __resolveRelation(ctx, "qualifications", [__auth.tenantId, __draft.requiredQualificationId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_requiredTrainingCompletion = await __resolveRelation(ctx, "trainingCompletions", [__auth.tenantId, __draft.requiredTrainingCompletionId], ["tenantId","id"], "tenantId", __auth.tenantId);
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may read shifts");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may write shifts through commands");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may execute shift commands");
    if (!((__draft.scheduledAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.status === "scheduled"))) throw new Error("Guard 1 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((__rel_person != null))) throw new Error("Guard 3 failed");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Guard 4 failed");
    if (!((personId === __draft.personId))) throw new Error("Schedule personId must match the seeded person reference");
    if (!((((eventId == null) || (__draft.eventId == null)) || (eventId === __draft.eventId)))) throw new Error("Schedule eventId must match the seeded event reference when provided");
    if (!((endsAt > startsAt))) throw new Error("Shift end must be after its start");
    if (!(((shiftTypeId == null) || (((__rel_shiftType != null) && (__rel_shiftType.deletedAt == null)) && (__rel_shiftType.status === "active"))))) throw new Error("Selected shift type must be active");
    if (!(((((shiftTypeId == null) || (__rel_shiftType == null)) || (__rel_shiftType.requiredTrainingModuleId == null)) || (requiredTrainingCompletionId != null)))) throw new Error("This shift type requires completed training before scheduling");
    if (!(((requiredTrainingCompletionId == null) || ((((__rel_requiredTrainingCompletion != null) && (__rel_requiredTrainingCompletion.deletedAt == null)) && (__rel_requiredTrainingCompletion.recordedAt != null)) && (__rel_requiredTrainingCompletion.personId === personId))))) throw new Error("Training completion must be recorded for the selected person");
    if (!(((requiredTrainingCompletionId == null) || ((((__rel_shiftType != null) && (__rel_shiftType.requiredTrainingModuleId != null)) && (__rel_requiredTrainingCompletion != null)) && (__rel_requiredTrainingCompletion.trainingModuleId === __rel_shiftType.requiredTrainingModuleId))))) throw new Error("Training completion must match the module required by the shift type");
    if (!(((requiredQualificationId == null) || ((__rel_requiredQualification != null) && (__rel_requiredQualification.personId === personId))))) throw new Error("Required certification must belong to the selected person");
    if (!(((requiredQualificationId == null) || (((__rel_requiredQualification != null) && (__rel_requiredQualification.deletedAt == null)) && (__rel_requiredQualification.status === "active"))))) throw new Error("Required certification must be active");
    if (!(((requiredQualificationId == null) || ((__rel_requiredQualification != null) && ((__rel_requiredQualification.expiresAt == null) || (__rel_requiredQualification.expiresAt >= endsAt)))))) throw new Error("Required certification must remain valid through the shift");
    const doc: Record<string, any> = {
      ...__draft,
      personId: personId,
      eventId: ((eventId != null) ? eventId : __draft.eventId),
      startsAt: startsAt,
      endsAt: endsAt,
      role: ((role != null) ? role : __draft.role),
      shiftTypeId: ((shiftTypeId != null) ? shiftTypeId : __draft.shiftTypeId),
      requiredQualificationId: ((requiredQualificationId != null) ? requiredQualificationId : __draft.requiredQualificationId),
      requiredTrainingCompletionId: ((requiredTrainingCompletionId != null) ? requiredTrainingCompletionId : __draft.requiredTrainingCompletionId),
      notes: ((notes != null) ? notes : __draft.notes),
      scheduledAt: Date.now(),
      version: 1,
    };
    const __storedDoc = await __encryptDoc(ctx, "Shift", ["notes"], doc);
    const docId = await ctx.db.insert("shifts", __storedDoc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, shiftId: docId, tenantId: doc.tenantId, personId: doc.personId, eventId: ((doc.eventId != null) ? doc.eventId : doc.eventId), startsAt: doc.startsAt, endsAt: doc.endsAt, role: ((doc.role != null) ? doc.role : doc.role), shiftTypeId: ((doc.shiftTypeId != null) ? doc.shiftTypeId : doc.shiftTypeId), requiredQualificationId: ((doc.requiredQualificationId != null) ? doc.requiredQualificationId : doc.requiredQualificationId), requiredTrainingCompletionId: ((doc.requiredTrainingCompletionId != null) ? doc.requiredTrainingCompletionId : doc.requiredTrainingCompletionId), status: "scheduled", _subject: { entity: "Shift", command: "schedule", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ShiftScheduled", entity: "Shift", entityId: docId, payload: { shiftId: docId, tenantId: doc.tenantId, personId: doc.personId, eventId: ((doc.eventId != null) ? doc.eventId : doc.eventId), startsAt: doc.startsAt, endsAt: doc.endsAt, role: ((doc.role != null) ? doc.role : doc.role), shiftTypeId: ((doc.shiftTypeId != null) ? doc.shiftTypeId : doc.shiftTypeId), requiredQualificationId: ((doc.requiredQualificationId != null) ? doc.requiredQualificationId : doc.requiredQualificationId), requiredTrainingCompletionId: ((doc.requiredTrainingCompletionId != null) ? doc.requiredTrainingCompletionId : doc.requiredTrainingCompletionId), status: "scheduled" }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Shift_createViaSchedule", __result);
    }
    return __result;
  },
});

async function __runShiftStageApprovedSwap(ctx: MutationCtx, { docId, shiftSwapRequestId, requesterPersonId, recipientPersonId, targetQualificationId, targetTrainingCompletionId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("Shift not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("Shift not found");
    const doc = await __decryptDoc(ctx, "Shift", ["notes"], __storedDoc) as Record<string, any>;
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may read shifts");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may write shifts through commands");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may execute shift commands");
    if (!((doc.status === "scheduled"))) throw new Error("Guard 0 failed");
    if (!((doc.scheduledAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((doc.swapAuthorizationId == null))) throw new Error("Guard 3 failed");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Guard 4 failed");
    if (!((doc.personId === requesterPersonId))) throw new Error("This shift is no longer assigned to the swap requester");
    if (!((requesterPersonId !== recipientPersonId))) throw new Error("Choose another staff member for the swap");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      swapAuthorizationId: shiftSwapRequestId,
      swapTargetPersonId: recipientPersonId,
      swapTargetQualificationId: targetQualificationId,
      swapTargetTrainingCompletionId: targetTrainingCompletionId,
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "Shift", ["notes"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, shiftId: docId, tenantId: __after.tenantId, _subject: { entity: "Shift", command: "stageApprovedSwap", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ShiftSwapStaged", entity: "Shift", entityId: docId, payload: { shiftId: docId, tenantId: __after.tenantId }, createdAt: Date.now() });
    // Reactions
    const reactionTarget0 = payload.shiftId;
    if (reactionTarget0) await __runShiftApplyApprovedSwap(ctx, { docId: reactionTarget0 } as any);
    return { ...doc, ...updates };
}

export const Shift_stageApprovedSwap = mutation({
  args: {
    docId: v.id("shifts"),
    shiftSwapRequestId: v.string(),
    requesterPersonId: v.string(),
    recipientPersonId: v.string(),
    targetQualificationId: v.optional(v.string()),
    targetTrainingCompletionId: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runShiftStageApprovedSwap(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Shift_stageApprovedSwap", __result);
    }
    return __result;
  },
});

async function __runShiftStart(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("Shift not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("Shift not found");
    const doc = await __decryptDoc(ctx, "Shift", ["notes"], __storedDoc) as Record<string, any>;
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may read shifts");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may write shifts through commands");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may execute shift commands");
    if (!((doc.status === "scheduled"))) throw new Error("Guard 0 failed");
    if (!((doc.scheduledAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((user.id != null))) throw new Error("Guard 3 failed");
    if (!(((doc.personId === user.id) || checkRole(user, "workforceManageAccess")))) throw new Error("Guard 4 failed");
    const previousStatus = doc.status;
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "started";
        const __allowed: Record<string, string[]> = { "scheduled": ["started", "cancelled", "no_show"], "started": ["completed", "cancelled", "no_show"], "completed": [], "cancelled": [], "no_show": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "started",
      startedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "Shift", ["notes"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, shiftId: docId, tenantId: __after.tenantId, personId: __after.personId, eventId: __after.eventId, startsAt: __after.startsAt, endsAt: __after.endsAt, previousStatus: previousStatus, status: "started", _subject: { entity: "Shift", command: "start", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ShiftStarted", entity: "Shift", entityId: docId, payload: { shiftId: docId, tenantId: __after.tenantId, personId: __after.personId, eventId: __after.eventId, startsAt: __after.startsAt, endsAt: __after.endsAt, previousStatus: previousStatus, status: "started" }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Shift_start = mutation({
  args: {
    docId: v.id("shifts"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runShiftStart(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Shift_start", __result);
    }
    return __result;
  },
});

async function __runShiftSwapRequestAccept(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("ShiftSwapRequest not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("ShiftSwapRequest not found");
    const doc = await __decryptDoc(ctx, "ShiftSwapRequest", ["reason","reviewNote"], __storedDoc) as Record<string, any>;
    const __rel_shift = await __resolveRelation(ctx, "shifts", [__auth.tenantId, doc.shiftId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_recipientPerson = await __resolveRelation(ctx, "people", [__auth.tenantId, doc.recipientPersonId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).recipientPersonId;
      ((doc as any) as any).recipientPerson = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    {
      const __fk = ((doc as any) as any).shiftId;
      ((doc as any) as any).shift = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may read shift swap requests");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may update shift swap requests through commands");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may execute shift swap request commands");
    if (!((doc.status === "pending_recipient"))) throw new Error("Guard 0 failed");
    if (!((doc.requesterConfirmedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((user.id != null))) throw new Error("Guard 3 failed");
    if (!((((__rel_recipientPerson != null) && (__rel_recipientPerson.deletedAt == null)) && (__rel_recipientPerson.status === "active")))) throw new Error("Guard 4 failed");
    if (!(((__rel_recipientPerson.authSubjectId != null) && (__rel_recipientPerson.authSubjectId === user.id)))) throw new Error("Guard 5 failed");
    if (!((((__rel_shift != null) && (__rel_shift.deletedAt == null)) && (__rel_shift.status === "scheduled")))) throw new Error("Guard 6 failed");
    if (!((__rel_shift.personId === doc.requesterPersonId))) throw new Error("This shift is no longer assigned to the requester");
    const previousStatus = doc.status;
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "awaiting_manager";
        const __allowed: Record<string, string[]> = { "pending_recipient": ["awaiting_manager", "declined", "withdrawn"], "awaiting_manager": ["approved", "withdrawn", "rejected"], "approved": [], "declined": [], "withdrawn": [], "rejected": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "awaiting_manager",
      recipientConfirmedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "ShiftSwapRequest", ["reason","reviewNote"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, shiftSwapRequestId: docId, tenantId: __after.tenantId, shiftId: __after.shiftId, requesterPersonId: __after.requesterPersonId, recipientPersonId: __after.recipientPersonId, previousStatus: previousStatus, status: "awaiting_manager", _subject: { entity: "ShiftSwapRequest", command: "accept", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ShiftSwapAccepted", entity: "ShiftSwapRequest", entityId: docId, payload: { shiftSwapRequestId: docId, tenantId: __after.tenantId, shiftId: __after.shiftId, requesterPersonId: __after.requesterPersonId, recipientPersonId: __after.recipientPersonId, previousStatus: previousStatus, status: "awaiting_manager" }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ShiftSwapRequest_accept = mutation({
  args: {
    docId: v.id("shiftSwapRequests"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runShiftSwapRequestAccept(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ShiftSwapRequest_accept", __result);
    }
    return __result;
  },
});

async function __runShiftSwapRequestApprove(ctx: MutationCtx, { docId, reviewNote, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("ShiftSwapRequest not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("ShiftSwapRequest not found");
    const doc = await __decryptDoc(ctx, "ShiftSwapRequest", ["reason","reviewNote"], __storedDoc) as Record<string, any>;
    const __rel_shift = await __resolveRelation(ctx, "shifts", [__auth.tenantId, doc.shiftId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_recipientPerson = await __resolveRelation(ctx, "people", [__auth.tenantId, doc.recipientPersonId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_shiftType = await __resolveRelation(ctx, "shiftTypes", [__auth.tenantId, doc.shiftTypeId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_sourceQualification = await __resolveRelation(ctx, "qualifications", [__auth.tenantId, doc.sourceQualificationId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_targetQualification = await __resolveRelation(ctx, "qualifications", [__auth.tenantId, doc.targetQualificationId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_targetTrainingCompletion = await __resolveRelation(ctx, "trainingCompletions", [__auth.tenantId, doc.targetTrainingCompletionId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).shiftId;
      ((doc as any) as any).shift = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    {
      const __fk = ((doc as any) as any).recipientPersonId;
      ((doc as any) as any).recipientPerson = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    {
      const __fk = ((doc as any) as any).shiftTypeId;
      ((doc as any) as any).shiftType = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    {
      const __fk = ((doc as any) as any).sourceQualificationId;
      ((doc as any) as any).sourceQualification = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    {
      const __fk = ((doc as any) as any).targetQualificationId;
      ((doc as any) as any).targetQualification = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    {
      const __fk = ((doc as any) as any).targetTrainingCompletionId;
      ((doc as any) as any).targetTrainingCompletion = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may read shift swap requests");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may update shift swap requests through commands");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may execute shift swap request commands");
    if (!((doc.status === "awaiting_manager"))) throw new Error("Guard 0 failed");
    if (!(((doc.requesterConfirmedAt != null) && (doc.recipientConfirmedAt != null)))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Guard 3 failed");
    if (!((((__rel_shift != null) && (__rel_shift.deletedAt == null)) && (__rel_shift.status === "scheduled")))) throw new Error("Guard 4 failed");
    if (!((((__rel_recipientPerson != null) && (__rel_recipientPerson.deletedAt == null)) && (__rel_recipientPerson.status === "active")))) throw new Error("Guard 5 failed");
    if (!((__rel_recipientPerson.authSubjectId != null))) throw new Error("Guard 6 failed");
    if (!(((doc.shiftTypeId == null) || (((__rel_shiftType != null) && (__rel_shiftType.deletedAt == null)) && (__rel_shiftType.status === "active"))))) throw new Error("Guard 7 failed");
    if (!(((doc.sourceQualificationId == null) || (((__rel_sourceQualification != null) && (__rel_sourceQualification.deletedAt == null)) && (__rel_sourceQualification.status === "active"))))) throw new Error("Guard 8 failed");
    if (!(((doc.targetQualificationId == null) || (((__rel_targetQualification != null) && (__rel_targetQualification.deletedAt == null)) && (__rel_targetQualification.status === "active"))))) throw new Error("Guard 9 failed");
    if (!(((doc.targetTrainingCompletionId == null) || (((__rel_targetTrainingCompletion != null) && (__rel_targetTrainingCompletion.deletedAt == null)) && (__rel_targetTrainingCompletion.recordedAt != null))))) throw new Error("Guard 10 failed");
    if (!((__rel_shift.personId === doc.requesterPersonId))) throw new Error("This shift is no longer assigned to the requester");
    if (!((doc.shiftTypeId === __rel_shift.shiftTypeId))) throw new Error("The shift type changed after this request was proposed");
    if (!((doc.sourceQualificationId === __rel_shift.requiredQualificationId))) throw new Error("The shift certification requirement changed after this request was proposed");
    if (!(((doc.sourceQualificationId == null) || (doc.targetQualificationId != null)))) throw new Error("The recipient needs a matching active certification");
    if (!(((doc.targetQualificationId == null) || (__rel_targetQualification.personId === doc.recipientPersonId)))) throw new Error("Replacement certification must belong to the recipient");
    if (!(((doc.sourceQualificationId == null) || ((__rel_targetQualification.name === __rel_sourceQualification.name) && (__rel_targetQualification.certificationType === __rel_sourceQualification.certificationType))))) throw new Error("Recipient certification must match the shift requirement");
    if (!((((doc.targetQualificationId == null) || (__rel_targetQualification.expiresAt == null)) || (__rel_targetQualification.expiresAt >= __rel_shift.endsAt)))) throw new Error("Recipient certification must remain valid through the shift");
    if (!((((doc.shiftTypeId == null) || (__rel_shiftType.requiredTrainingModuleId == null)) || (doc.targetTrainingCompletionId != null)))) throw new Error("The recipient needs the training required by this shift type");
    if (!(((doc.targetTrainingCompletionId == null) || (__rel_targetTrainingCompletion.personId === doc.recipientPersonId)))) throw new Error("Training completion must belong to the recipient");
    if (!(((doc.targetTrainingCompletionId == null) || (__rel_targetTrainingCompletion.trainingModuleId === __rel_shiftType.requiredTrainingModuleId)))) throw new Error("Recipient training must match the shift type requirement");
    const previousStatus = doc.status;
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "approved";
        const __allowed: Record<string, string[]> = { "pending_recipient": ["awaiting_manager", "declined", "withdrawn"], "awaiting_manager": ["approved", "withdrawn", "rejected"], "approved": [], "declined": [], "withdrawn": [], "rejected": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "approved",
      managerApprovedAt: Date.now(),
      reviewNote: reviewNote,
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "ShiftSwapRequest", ["reason","reviewNote"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, shiftSwapRequestId: docId, tenantId: __after.tenantId, shiftId: __after.shiftId, requesterPersonId: __after.requesterPersonId, recipientPersonId: __after.recipientPersonId, targetQualificationId: __after.targetQualificationId, targetTrainingCompletionId: __after.targetTrainingCompletionId, previousStatus: previousStatus, status: "approved", _subject: { entity: "ShiftSwapRequest", command: "approve", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ShiftSwapApproved", entity: "ShiftSwapRequest", entityId: docId, payload: { shiftSwapRequestId: docId, tenantId: __after.tenantId, shiftId: __after.shiftId, requesterPersonId: __after.requesterPersonId, recipientPersonId: __after.recipientPersonId, targetQualificationId: __after.targetQualificationId, targetTrainingCompletionId: __after.targetTrainingCompletionId, previousStatus: previousStatus, status: "approved" }, createdAt: Date.now() });
    // Reactions
    const __fanId0 = payload.shiftId;
    const __fanDoc0 = __fanId0 != null && __fanId0 !== "" ? await ctx.db.get(__fanId0 as any) : null;
    const fanRows0 = __fanDoc0 != null && (__fanDoc0 as any).deletedAt == null ? [__fanDoc0] : [];
    for (const __row of fanRows0) {
      const target = __row;
      await __runShiftStageApprovedSwap(ctx, { docId: (__row as any)._id, shiftSwapRequestId: payload.shiftSwapRequestId, requesterPersonId: payload.requesterPersonId, recipientPersonId: payload.recipientPersonId, targetQualificationId: payload.targetQualificationId, targetTrainingCompletionId: payload.targetTrainingCompletionId } as any);
    }
    return { ...doc, ...updates };
}

export const ShiftSwapRequest_approve = mutation({
  args: {
    docId: v.id("shiftSwapRequests"),
    reviewNote: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runShiftSwapRequestApprove(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ShiftSwapRequest_approve", __result);
    }
    return __result;
  },
});

async function __runShiftSwapRequestDecline(ctx: MutationCtx, { docId, reviewNote, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("ShiftSwapRequest not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("ShiftSwapRequest not found");
    const doc = await __decryptDoc(ctx, "ShiftSwapRequest", ["reason","reviewNote"], __storedDoc) as Record<string, any>;
    const __rel_recipientPerson = await __resolveRelation(ctx, "people", [__auth.tenantId, doc.recipientPersonId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).recipientPersonId;
      ((doc as any) as any).recipientPerson = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may read shift swap requests");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may update shift swap requests through commands");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may execute shift swap request commands");
    if (!((doc.status === "pending_recipient"))) throw new Error("Guard 0 failed");
    if (!((doc.requesterConfirmedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((user.id != null))) throw new Error("Guard 3 failed");
    if (!((((__rel_recipientPerson != null) && (__rel_recipientPerson.authSubjectId != null)) && (__rel_recipientPerson.authSubjectId === user.id)))) throw new Error("Guard 4 failed");
    const previousStatus = doc.status;
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "declined";
        const __allowed: Record<string, string[]> = { "pending_recipient": ["awaiting_manager", "declined", "withdrawn"], "awaiting_manager": ["approved", "withdrawn", "rejected"], "approved": [], "declined": [], "withdrawn": [], "rejected": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "declined",
      declinedAt: Date.now(),
      reviewNote: reviewNote,
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "ShiftSwapRequest", ["reason","reviewNote"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, shiftSwapRequestId: docId, tenantId: __after.tenantId, shiftId: __after.shiftId, previousStatus: previousStatus, status: "declined", _subject: { entity: "ShiftSwapRequest", command: "decline", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ShiftSwapDeclined", entity: "ShiftSwapRequest", entityId: docId, payload: { shiftSwapRequestId: docId, tenantId: __after.tenantId, shiftId: __after.shiftId, previousStatus: previousStatus, status: "declined" }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ShiftSwapRequest_decline = mutation({
  args: {
    docId: v.id("shiftSwapRequests"),
    reviewNote: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runShiftSwapRequestDecline(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ShiftSwapRequest_decline", __result);
    }
    return __result;
  },
});

async function __runShiftSwapRequestPropose(ctx: MutationCtx, { docId, shiftId, requesterPersonId, recipientPersonId, shiftTypeId, sourceQualificationId, targetQualificationId, targetTrainingCompletionId, reason, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("ShiftSwapRequest not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("ShiftSwapRequest not found");
    const doc = await __decryptDoc(ctx, "ShiftSwapRequest", ["reason","reviewNote"], __storedDoc) as Record<string, any>;
    const __rel_shift = await __resolveRelation(ctx, "shifts", [__auth.tenantId, doc.shiftId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_requesterPerson = await __resolveRelation(ctx, "people", [__auth.tenantId, doc.requesterPersonId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_recipientPerson = await __resolveRelation(ctx, "people", [__auth.tenantId, doc.recipientPersonId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_shiftType = await __resolveRelation(ctx, "shiftTypes", [__auth.tenantId, doc.shiftTypeId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_sourceQualification = await __resolveRelation(ctx, "qualifications", [__auth.tenantId, doc.sourceQualificationId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_targetQualification = await __resolveRelation(ctx, "qualifications", [__auth.tenantId, doc.targetQualificationId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_targetTrainingCompletion = await __resolveRelation(ctx, "trainingCompletions", [__auth.tenantId, doc.targetTrainingCompletionId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).shiftId;
      ((doc as any) as any).shift = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    {
      const __fk = ((doc as any) as any).requesterPersonId;
      ((doc as any) as any).requesterPerson = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    {
      const __fk = ((doc as any) as any).recipientPersonId;
      ((doc as any) as any).recipientPerson = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    {
      const __fk = ((doc as any) as any).shiftTypeId;
      ((doc as any) as any).shiftType = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    {
      const __fk = ((doc as any) as any).sourceQualificationId;
      ((doc as any) as any).sourceQualification = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    {
      const __fk = ((doc as any) as any).targetQualificationId;
      ((doc as any) as any).targetQualification = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    {
      const __fk = ((doc as any) as any).targetTrainingCompletionId;
      ((doc as any) as any).targetTrainingCompletion = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may read shift swap requests");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may update shift swap requests through commands");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may execute shift swap request commands");
    if (!((doc.requesterConfirmedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.status === "pending_recipient"))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((user.id != null))) throw new Error("Guard 3 failed");
    if (!((((__rel_shift != null) && (__rel_shift.deletedAt == null)) && (__rel_shift.status === "scheduled")))) throw new Error("Guard 4 failed");
    if (!((((__rel_shift.startsAt != null) && (__rel_shift.endsAt != null)) && (__rel_shift.startsAt > Date.now())))) throw new Error("Guard 5 failed");
    if (!((((__rel_requesterPerson != null) && (__rel_requesterPerson.deletedAt == null)) && (__rel_requesterPerson.status === "active")))) throw new Error("Guard 6 failed");
    if (!(((__rel_requesterPerson.authSubjectId != null) && (__rel_requesterPerson.authSubjectId === user.id)))) throw new Error("Guard 7 failed");
    if (!((((__rel_recipientPerson != null) && (__rel_recipientPerson.deletedAt == null)) && (__rel_recipientPerson.status === "active")))) throw new Error("Guard 8 failed");
    if (!((__rel_recipientPerson.authSubjectId != null))) throw new Error("Guard 9 failed");
    if (!(((shiftTypeId == null) || (((__rel_shiftType != null) && (__rel_shiftType.deletedAt == null)) && (__rel_shiftType.status === "active"))))) throw new Error("Guard 10 failed");
    if (!(((sourceQualificationId == null) || (((__rel_sourceQualification != null) && (__rel_sourceQualification.deletedAt == null)) && (__rel_sourceQualification.status === "active"))))) throw new Error("Guard 11 failed");
    if (!(((targetQualificationId == null) || (((__rel_targetQualification != null) && (__rel_targetQualification.deletedAt == null)) && (__rel_targetQualification.status === "active"))))) throw new Error("Guard 12 failed");
    if (!(((targetTrainingCompletionId == null) || (((__rel_targetTrainingCompletion != null) && (__rel_targetTrainingCompletion.deletedAt == null)) && (__rel_targetTrainingCompletion.recordedAt != null))))) throw new Error("Guard 13 failed");
    if (!((shiftId === doc.shiftId))) throw new Error("Swap shift must match the seeded shift reference");
    if (!(((requesterPersonId === doc.requesterPersonId) && (__rel_shift.personId === requesterPersonId)))) throw new Error("Only the assigned staff member may propose this swap");
    if (!((recipientPersonId === doc.recipientPersonId))) throw new Error("Swap recipient must match the seeded person reference");
    if (!((requesterPersonId !== recipientPersonId))) throw new Error("Choose another staff member for the swap");
    if (!((shiftTypeId === __rel_shift.shiftTypeId))) throw new Error("Shift type must match the scheduled shift");
    if (!((sourceQualificationId === __rel_shift.requiredQualificationId))) throw new Error("Source certification must match the scheduled shift");
    if (!(((sourceQualificationId == null) || (__rel_sourceQualification.personId === requesterPersonId)))) throw new Error("Source certification must belong to the assigned staff member");
    if (!(((sourceQualificationId == null) || (targetQualificationId != null)))) throw new Error("The recipient needs a matching active certification");
    if (!(((targetQualificationId == null) || (__rel_targetQualification.personId === recipientPersonId)))) throw new Error("Replacement certification must belong to the recipient");
    if (!(((sourceQualificationId == null) || ((__rel_targetQualification.name === __rel_sourceQualification.name) && (__rel_targetQualification.certificationType === __rel_sourceQualification.certificationType))))) throw new Error("Recipient certification must match the shift requirement");
    if (!((((targetQualificationId == null) || (__rel_targetQualification.expiresAt == null)) || (__rel_targetQualification.expiresAt >= __rel_shift.endsAt)))) throw new Error("Recipient certification must remain valid through the shift");
    if (!((((shiftTypeId == null) || (__rel_shiftType.requiredTrainingModuleId == null)) || (targetTrainingCompletionId != null)))) throw new Error("The recipient needs the training required by this shift type");
    if (!(((targetTrainingCompletionId == null) || (__rel_targetTrainingCompletion.personId === recipientPersonId)))) throw new Error("Training completion must belong to the recipient");
    if (!(((targetTrainingCompletionId == null) || (__rel_targetTrainingCompletion.trainingModuleId === __rel_shiftType.requiredTrainingModuleId)))) throw new Error("Recipient training must match the shift type requirement");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      shiftId: shiftId,
      requesterPersonId: requesterPersonId,
      recipientPersonId: recipientPersonId,
      shiftTypeId: shiftTypeId,
      sourceQualificationId: sourceQualificationId,
      targetQualificationId: targetQualificationId,
      targetTrainingCompletionId: targetTrainingCompletionId,
      reason: reason,
      requesterConfirmedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "ShiftSwapRequest", ["reason","reviewNote"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, shiftSwapRequestId: docId, tenantId: __after.tenantId, shiftId: shiftId, requesterPersonId: requesterPersonId, recipientPersonId: recipientPersonId, status: "pending_recipient", _subject: { entity: "ShiftSwapRequest", command: "propose", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ShiftSwapProposed", entity: "ShiftSwapRequest", entityId: docId, payload: { shiftSwapRequestId: docId, tenantId: __after.tenantId, shiftId: shiftId, requesterPersonId: requesterPersonId, recipientPersonId: recipientPersonId, status: "pending_recipient" }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ShiftSwapRequest_propose = mutation({
  args: {
    docId: v.id("shiftSwapRequests"),
    shiftId: v.string(),
    requesterPersonId: v.string(),
    recipientPersonId: v.string(),
    shiftTypeId: v.optional(v.string()),
    sourceQualificationId: v.optional(v.string()),
    targetQualificationId: v.optional(v.string()),
    targetTrainingCompletionId: v.optional(v.string()),
    reason: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runShiftSwapRequestPropose(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ShiftSwapRequest_propose", __result);
    }
    return __result;
  },
});

export const ShiftSwapRequest_createViaPropose = mutation({
  args: {
    shiftId: v.string(),
    requesterPersonId: v.string(),
    recipientPersonId: v.string(),
    shiftTypeId: v.optional(v.string()),
    sourceQualificationId: v.optional(v.string()),
    targetQualificationId: v.optional(v.string()),
    targetTrainingCompletionId: v.optional(v.string()),
    reason: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { shiftId, requesterPersonId, recipientPersonId, shiftTypeId, sourceQualificationId, targetQualificationId, targetTrainingCompletionId, reason } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      status: "pending_recipient",
      createdAt: Date.now(),
      updatedAt: Date.now(),
      reason: args.reason,
      recipientPersonId: args.recipientPersonId,
      requesterPersonId: args.requesterPersonId,
      shiftId: args.shiftId,
      shiftTypeId: args.shiftTypeId,
      sourceQualificationId: args.sourceQualificationId,
      targetQualificationId: args.targetQualificationId,
      targetTrainingCompletionId: args.targetTrainingCompletionId
    };
    const __rel_shift = await __resolveRelation(ctx, "shifts", [__auth.tenantId, __draft.shiftId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_requesterPerson = await __resolveRelation(ctx, "people", [__auth.tenantId, __draft.requesterPersonId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_recipientPerson = await __resolveRelation(ctx, "people", [__auth.tenantId, __draft.recipientPersonId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_shiftType = await __resolveRelation(ctx, "shiftTypes", [__auth.tenantId, __draft.shiftTypeId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_sourceQualification = await __resolveRelation(ctx, "qualifications", [__auth.tenantId, __draft.sourceQualificationId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_targetQualification = await __resolveRelation(ctx, "qualifications", [__auth.tenantId, __draft.targetQualificationId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_targetTrainingCompletion = await __resolveRelation(ctx, "trainingCompletions", [__auth.tenantId, __draft.targetTrainingCompletionId], ["tenantId","id"], "tenantId", __auth.tenantId);
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may read shift swap requests");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may update shift swap requests through commands");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may execute shift swap request commands");
    if (!((__draft.requesterConfirmedAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.status === "pending_recipient"))) throw new Error("Guard 1 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((user.id != null))) throw new Error("Guard 3 failed");
    if (!((((__rel_shift != null) && (__rel_shift.deletedAt == null)) && (__rel_shift.status === "scheduled")))) throw new Error("Guard 4 failed");
    if (!((((__rel_shift.startsAt != null) && (__rel_shift.endsAt != null)) && (__rel_shift.startsAt > Date.now())))) throw new Error("Guard 5 failed");
    if (!((((__rel_requesterPerson != null) && (__rel_requesterPerson.deletedAt == null)) && (__rel_requesterPerson.status === "active")))) throw new Error("Guard 6 failed");
    if (!(((__rel_requesterPerson.authSubjectId != null) && (__rel_requesterPerson.authSubjectId === user.id)))) throw new Error("Guard 7 failed");
    if (!((((__rel_recipientPerson != null) && (__rel_recipientPerson.deletedAt == null)) && (__rel_recipientPerson.status === "active")))) throw new Error("Guard 8 failed");
    if (!((__rel_recipientPerson.authSubjectId != null))) throw new Error("Guard 9 failed");
    if (!(((shiftTypeId == null) || (((__rel_shiftType != null) && (__rel_shiftType.deletedAt == null)) && (__rel_shiftType.status === "active"))))) throw new Error("Guard 10 failed");
    if (!(((sourceQualificationId == null) || (((__rel_sourceQualification != null) && (__rel_sourceQualification.deletedAt == null)) && (__rel_sourceQualification.status === "active"))))) throw new Error("Guard 11 failed");
    if (!(((targetQualificationId == null) || (((__rel_targetQualification != null) && (__rel_targetQualification.deletedAt == null)) && (__rel_targetQualification.status === "active"))))) throw new Error("Guard 12 failed");
    if (!(((targetTrainingCompletionId == null) || (((__rel_targetTrainingCompletion != null) && (__rel_targetTrainingCompletion.deletedAt == null)) && (__rel_targetTrainingCompletion.recordedAt != null))))) throw new Error("Guard 13 failed");
    if (!((shiftId === __draft.shiftId))) throw new Error("Swap shift must match the seeded shift reference");
    if (!(((requesterPersonId === __draft.requesterPersonId) && (__rel_shift.personId === requesterPersonId)))) throw new Error("Only the assigned staff member may propose this swap");
    if (!((recipientPersonId === __draft.recipientPersonId))) throw new Error("Swap recipient must match the seeded person reference");
    if (!((requesterPersonId !== recipientPersonId))) throw new Error("Choose another staff member for the swap");
    if (!((shiftTypeId === __rel_shift.shiftTypeId))) throw new Error("Shift type must match the scheduled shift");
    if (!((sourceQualificationId === __rel_shift.requiredQualificationId))) throw new Error("Source certification must match the scheduled shift");
    if (!(((sourceQualificationId == null) || (__rel_sourceQualification.personId === requesterPersonId)))) throw new Error("Source certification must belong to the assigned staff member");
    if (!(((sourceQualificationId == null) || (targetQualificationId != null)))) throw new Error("The recipient needs a matching active certification");
    if (!(((targetQualificationId == null) || (__rel_targetQualification.personId === recipientPersonId)))) throw new Error("Replacement certification must belong to the recipient");
    if (!(((sourceQualificationId == null) || ((__rel_targetQualification.name === __rel_sourceQualification.name) && (__rel_targetQualification.certificationType === __rel_sourceQualification.certificationType))))) throw new Error("Recipient certification must match the shift requirement");
    if (!((((targetQualificationId == null) || (__rel_targetQualification.expiresAt == null)) || (__rel_targetQualification.expiresAt >= __rel_shift.endsAt)))) throw new Error("Recipient certification must remain valid through the shift");
    if (!((((shiftTypeId == null) || (__rel_shiftType.requiredTrainingModuleId == null)) || (targetTrainingCompletionId != null)))) throw new Error("The recipient needs the training required by this shift type");
    if (!(((targetTrainingCompletionId == null) || (__rel_targetTrainingCompletion.personId === recipientPersonId)))) throw new Error("Training completion must belong to the recipient");
    if (!(((targetTrainingCompletionId == null) || (__rel_targetTrainingCompletion.trainingModuleId === __rel_shiftType.requiredTrainingModuleId)))) throw new Error("Recipient training must match the shift type requirement");
    const doc: Record<string, any> = {
      ...__draft,
      shiftId: shiftId,
      requesterPersonId: requesterPersonId,
      recipientPersonId: recipientPersonId,
      shiftTypeId: shiftTypeId,
      sourceQualificationId: sourceQualificationId,
      targetQualificationId: targetQualificationId,
      targetTrainingCompletionId: targetTrainingCompletionId,
      reason: reason,
      requesterConfirmedAt: Date.now(),
      version: 1,
    };
    const __storedDoc = await __encryptDoc(ctx, "ShiftSwapRequest", ["reason","reviewNote"], doc);
    const docId = await ctx.db.insert("shiftSwapRequests", __storedDoc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, shiftSwapRequestId: docId, tenantId: doc.tenantId, shiftId: doc.shiftId, requesterPersonId: doc.requesterPersonId, recipientPersonId: doc.recipientPersonId, status: "pending_recipient", _subject: { entity: "ShiftSwapRequest", command: "propose", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ShiftSwapProposed", entity: "ShiftSwapRequest", entityId: docId, payload: { shiftSwapRequestId: docId, tenantId: doc.tenantId, shiftId: doc.shiftId, requesterPersonId: doc.requesterPersonId, recipientPersonId: doc.recipientPersonId, status: "pending_recipient" }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ShiftSwapRequest_createViaPropose", __result);
    }
    return __result;
  },
});

async function __runShiftSwapRequestReject(ctx: MutationCtx, { docId, reviewNote, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("ShiftSwapRequest not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("ShiftSwapRequest not found");
    const doc = await __decryptDoc(ctx, "ShiftSwapRequest", ["reason","reviewNote"], __storedDoc) as Record<string, any>;
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may read shift swap requests");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may update shift swap requests through commands");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may execute shift swap request commands");
    if (!((doc.status === "awaiting_manager"))) throw new Error("Guard 0 failed");
    if (!(((doc.requesterConfirmedAt != null) && (doc.recipientConfirmedAt != null)))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Guard 3 failed");
    const previousStatus = doc.status;
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "rejected";
        const __allowed: Record<string, string[]> = { "pending_recipient": ["awaiting_manager", "declined", "withdrawn"], "awaiting_manager": ["approved", "withdrawn", "rejected"], "approved": [], "declined": [], "withdrawn": [], "rejected": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "rejected",
      rejectedAt: Date.now(),
      reviewNote: reviewNote,
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "ShiftSwapRequest", ["reason","reviewNote"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, shiftSwapRequestId: docId, tenantId: __after.tenantId, shiftId: __after.shiftId, previousStatus: previousStatus, status: "rejected", _subject: { entity: "ShiftSwapRequest", command: "reject", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ShiftSwapRejected", entity: "ShiftSwapRequest", entityId: docId, payload: { shiftSwapRequestId: docId, tenantId: __after.tenantId, shiftId: __after.shiftId, previousStatus: previousStatus, status: "rejected" }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ShiftSwapRequest_reject = mutation({
  args: {
    docId: v.id("shiftSwapRequests"),
    reviewNote: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runShiftSwapRequestReject(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ShiftSwapRequest_reject", __result);
    }
    return __result;
  },
});

async function __runShiftSwapRequestWithdraw(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("ShiftSwapRequest not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("ShiftSwapRequest not found");
    const doc = await __decryptDoc(ctx, "ShiftSwapRequest", ["reason","reviewNote"], __storedDoc) as Record<string, any>;
    const __rel_requesterPerson = await __resolveRelation(ctx, "people", [__auth.tenantId, doc.requesterPersonId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).requesterPersonId;
      ((doc as any) as any).requesterPerson = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may read shift swap requests");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may update shift swap requests through commands");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may execute shift swap request commands");
    if (!(((doc.status === "pending_recipient") || (doc.status === "awaiting_manager")))) throw new Error("Guard 0 failed");
    if (!((doc.requesterConfirmedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((user.id != null))) throw new Error("Guard 3 failed");
    if (!((((__rel_requesterPerson != null) && (__rel_requesterPerson.authSubjectId != null)) && (__rel_requesterPerson.authSubjectId === user.id)))) throw new Error("Guard 4 failed");
    const previousStatus = doc.status;
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "withdrawn";
        const __allowed: Record<string, string[]> = { "pending_recipient": ["awaiting_manager", "declined", "withdrawn"], "awaiting_manager": ["approved", "withdrawn", "rejected"], "approved": [], "declined": [], "withdrawn": [], "rejected": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "withdrawn",
      withdrawnAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "ShiftSwapRequest", ["reason","reviewNote"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, shiftSwapRequestId: docId, tenantId: __after.tenantId, shiftId: __after.shiftId, previousStatus: previousStatus, status: "withdrawn", _subject: { entity: "ShiftSwapRequest", command: "withdraw", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ShiftSwapWithdrawn", entity: "ShiftSwapRequest", entityId: docId, payload: { shiftSwapRequestId: docId, tenantId: __after.tenantId, shiftId: __after.shiftId, previousStatus: previousStatus, status: "withdrawn" }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ShiftSwapRequest_withdraw = mutation({
  args: {
    docId: v.id("shiftSwapRequests"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runShiftSwapRequestWithdraw(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ShiftSwapRequest_withdraw", __result);
    }
    return __result;
  },
});

async function __runShiftTypeDefine(ctx: MutationCtx, { docId, name, description, requiredTrainingModuleId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ShiftType not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ShiftType not found");
    const __rel_requiredTrainingModule = await __resolveRelation(ctx, "trainingModules", [__auth.tenantId, doc.requiredTrainingModuleId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).requiredTrainingModuleId;
      ((doc as any) as any).requiredTrainingModule = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may read shift types");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may write shift types through commands");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may execute shift type commands");
    if (!((doc.definedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Guard 2 failed");
    if (!(((requiredTrainingModuleId == null) || (((__rel_requiredTrainingModule != null) && (__rel_requiredTrainingModule.status === "active")) && (__rel_requiredTrainingModule.deletedAt == null))))) throw new Error("Guard 3 failed");
    if (!((((name).trim()).length > 0))) throw new Error("Shift type name is required");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "active";
        const __allowed: Record<string, string[]> = { "active": ["retired"], "retired": ["active"] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      name: name,
      description: description,
      requiredTrainingModuleId: requiredTrainingModuleId,
      status: "active",
      definedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, shiftTypeId: docId, tenantId: __after.tenantId, name: name, requiredTrainingModuleId: requiredTrainingModuleId, _subject: { entity: "ShiftType", command: "define", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ShiftTypeDefined", entity: "ShiftType", entityId: docId, payload: { shiftTypeId: docId, tenantId: __after.tenantId, name: name, requiredTrainingModuleId: requiredTrainingModuleId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ShiftType_define = mutation({
  args: {
    docId: v.id("shiftTypes"),
    name: v.string(),
    description: v.optional(v.string()),
    requiredTrainingModuleId: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runShiftTypeDefine(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ShiftType_define", __result);
    }
    return __result;
  },
});

export const ShiftType_createViaDefine = mutation({
  args: {
    name: v.string(),
    description: v.optional(v.string()),
    requiredTrainingModuleId: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { name, description, requiredTrainingModuleId } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      createdAt: Date.now(),
      updatedAt: Date.now(),
      status: "active",
      description: args.description,
      name: args.name,
      requiredTrainingModuleId: args.requiredTrainingModuleId
    };
    const __rel_requiredTrainingModule = await __resolveRelation(ctx, "trainingModules", [__auth.tenantId, __draft.requiredTrainingModuleId], ["tenantId","id"], "tenantId", __auth.tenantId);
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may read shift types");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may write shift types through commands");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may execute shift type commands");
    if (!((__draft.definedAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Guard 2 failed");
    if (!(((requiredTrainingModuleId == null) || (((__rel_requiredTrainingModule != null) && (__rel_requiredTrainingModule.status === "active")) && (__rel_requiredTrainingModule.deletedAt == null))))) throw new Error("Guard 3 failed");
    if (!((((name).trim()).length > 0))) throw new Error("Shift type name is required");
    const doc: Record<string, any> = {
      ...__draft,
      name: name,
      description: description,
      requiredTrainingModuleId: requiredTrainingModuleId,
      status: "active",
      definedAt: Date.now(),
      version: 1,
    };
    const docId = await ctx.db.insert("shiftTypes", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, shiftTypeId: docId, tenantId: doc.tenantId, name: doc.name, requiredTrainingModuleId: doc.requiredTrainingModuleId, _subject: { entity: "ShiftType", command: "define", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ShiftTypeDefined", entity: "ShiftType", entityId: docId, payload: { shiftTypeId: docId, tenantId: doc.tenantId, name: doc.name, requiredTrainingModuleId: doc.requiredTrainingModuleId }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ShiftType_createViaDefine", __result);
    }
    return __result;
  },
});

async function __runShiftTypeReactivate(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ShiftType not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ShiftType not found");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may read shift types");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may write shift types through commands");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may execute shift type commands");
    if (!((doc.status === "retired"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Guard 2 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "active";
        const __allowed: Record<string, string[]> = { "active": ["retired"], "retired": ["active"] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "active",
      retiredAt: null,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, shiftTypeId: docId, tenantId: __after.tenantId, name: __after.name, _subject: { entity: "ShiftType", command: "reactivate", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ShiftTypeReactivated", entity: "ShiftType", entityId: docId, payload: { shiftTypeId: docId, tenantId: __after.tenantId, name: __after.name }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ShiftType_reactivate = mutation({
  args: {
    docId: v.id("shiftTypes"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runShiftTypeReactivate(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ShiftType_reactivate", __result);
    }
    return __result;
  },
});

async function __runShiftTypeRetire(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("ShiftType not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("ShiftType not found");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may read shift types");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may write shift types through commands");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may execute shift type commands");
    if (!((doc.status === "active"))) throw new Error("Guard 0 failed");
    if (!((doc.definedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Guard 3 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "retired";
        const __allowed: Record<string, string[]> = { "active": ["retired"], "retired": ["active"] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "retired",
      retiredAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, shiftTypeId: docId, tenantId: __after.tenantId, name: __after.name, _subject: { entity: "ShiftType", command: "retire", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "ShiftTypeRetired", entity: "ShiftType", entityId: docId, payload: { shiftTypeId: docId, tenantId: __after.tenantId, name: __after.name }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const ShiftType_retire = mutation({
  args: {
    docId: v.id("shiftTypes"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runShiftTypeRetire(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "ShiftType_retire", __result);
    }
    return __result;
  },
});

async function __runSignatureRequestComplete(ctx: MutationCtx, { docId, callbackToken, signedArtifactReference, signerIpAddress, signerUserAgent, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("SignatureRequest not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("SignatureRequest not found");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may read signature requests");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may write signature requests through commands");
    if (!((checkRole(user, "salesAccess") || checkRole(user, "clientAccess")))) throw new Error("Sales staff and clients may execute signature request commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.status === "requested"))) throw new Error("Guard 1 failed");
    if (!((doc.requestedAt != null))) throw new Error("Guard 2 failed");
    if (!(((doc.expiresAt == null) || (Date.now() < doc.expiresAt)))) throw new Error("Guard 3 failed");
    if (!((doc.callbackToken === callbackToken))) throw new Error("Guard 4 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "completed";
        const __allowed: Record<string, string[]> = { "requested": ["completed", "expired", "revoked"], "completed": [], "expired": [], "revoked": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "completed",
      completedAt: Date.now(),
      signedArtifactReference: signedArtifactReference,
      signerIpAddress: signerIpAddress,
      signerUserAgent: signerUserAgent,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, signatureRequestId: docId, tenantId: __after.tenantId, proposalRevisionId: __after.proposalRevisionId, proposalId: __after.proposalId, recipientEmail: __after.recipientEmail, recipientName: __after.recipientName, signedAt: Date.now(), signedArtifactReference: signedArtifactReference, _subject: { entity: "SignatureRequest", command: "complete", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "SignatureCompleted", entity: "SignatureRequest", entityId: docId, payload: { signatureRequestId: docId, tenantId: __after.tenantId, proposalRevisionId: __after.proposalRevisionId, proposalId: __after.proposalId, recipientEmail: __after.recipientEmail, recipientName: __after.recipientName, signedAt: Date.now(), signedArtifactReference: signedArtifactReference }, createdAt: Date.now() });
    // Reactions
    const reactionTarget0 = payload.proposalId;
    if (reactionTarget0) await __runProposalAccept(ctx, { docId: reactionTarget0 } as any);
    return { ...doc, ...updates };
}

export const SignatureRequest_complete = mutation({
  args: {
    docId: v.id("signatureRequests"),
    callbackToken: v.string(),
    signedArtifactReference: v.string(),
    signerIpAddress: v.optional(v.string()),
    signerUserAgent: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runSignatureRequestComplete(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "SignatureRequest_complete", __result);
    }
    return __result;
  },
});

async function __runSignatureRequestExpire(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("SignatureRequest not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("SignatureRequest not found");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may read signature requests");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may write signature requests through commands");
    if (!((checkRole(user, "salesAccess") || checkRole(user, "clientAccess")))) throw new Error("Sales staff and clients may execute signature request commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.status === "requested"))) throw new Error("Guard 1 failed");
    if (!((doc.requestedAt != null))) throw new Error("Guard 2 failed");
    if (!(((doc.expiresAt != null) && (Date.now() >= doc.expiresAt)))) throw new Error("Guard 3 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "expired";
        const __allowed: Record<string, string[]> = { "requested": ["completed", "expired", "revoked"], "completed": [], "expired": [], "revoked": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "expired",
      expiredAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, signatureRequestId: docId, tenantId: __after.tenantId, proposalRevisionId: __after.proposalRevisionId, recipientEmail: __after.recipientEmail, expiredAt: Date.now(), _subject: { entity: "SignatureRequest", command: "expire", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "SignatureExpired", entity: "SignatureRequest", entityId: docId, payload: { signatureRequestId: docId, tenantId: __after.tenantId, proposalRevisionId: __after.proposalRevisionId, recipientEmail: __after.recipientEmail, expiredAt: Date.now() }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const SignatureRequest_expire = mutation({
  args: {
    docId: v.id("signatureRequests"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runSignatureRequestExpire(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "SignatureRequest_expire", __result);
    }
    return __result;
  },
});

async function __runSignatureRequestRequestSignature(ctx: MutationCtx, { docId, proposalRevisionId, recipientEmail, recipientName, recipientPersonId, recipientContactId, provider, expiresAt, proposalId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("SignatureRequest not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("SignatureRequest not found");
    const __rel_proposalRevision = await __resolveRelation(ctx, "proposalRevisions", [__auth.tenantId, doc.proposalRevisionId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).proposalRevisionId;
      ((doc as any) as any).proposalRevision = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may read signature requests");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may write signature requests through commands");
    if (!((checkRole(user, "salesAccess") || checkRole(user, "clientAccess")))) throw new Error("Sales staff and clients may execute signature request commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.requestedAt == null))) throw new Error("Guard 1 failed");
    if (!((user.id != null))) throw new Error("Guard 2 failed");
    if (!((user.personId != null))) throw new Error("Guard 3 failed");
    if (!((__rel_proposalRevision != null))) throw new Error("Guard 4 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "requested";
        const __allowed: Record<string, string[]> = { "requested": ["completed", "expired", "revoked"], "completed": [], "expired": [], "revoked": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      proposalRevisionId: proposalRevisionId,
      proposalId: proposalId,
      recipientEmail: recipientEmail,
      recipientName: recipientName,
      recipientPersonId: recipientPersonId,
      recipientContactId: recipientContactId,
      provider: ((provider != null) ? provider : "internal"),
      expiresAt: ((expiresAt != null) ? expiresAt : (Date.now() + ((((30 * 24) * 60) * 60) * 1000))),
      status: "requested",
      requestedAt: Date.now(),
      requestedByPersonId: user.personId,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, signatureRequestId: docId, tenantId: __after.tenantId, proposalRevisionId: proposalRevisionId, recipientEmail: recipientEmail, recipientName: recipientName, provider: ((provider != null) ? provider : "internal"), expiresAt: ((expiresAt != null) ? expiresAt : (Date.now() + ((((30 * 24) * 60) * 60) * 1000))), _subject: { entity: "SignatureRequest", command: "requestSignature", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "SignatureRequested", entity: "SignatureRequest", entityId: docId, payload: { signatureRequestId: docId, tenantId: __after.tenantId, proposalRevisionId: proposalRevisionId, recipientEmail: recipientEmail, recipientName: recipientName, provider: ((provider != null) ? provider : "internal"), expiresAt: ((expiresAt != null) ? expiresAt : (Date.now() + ((((30 * 24) * 60) * 60) * 1000))) }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const SignatureRequest_requestSignature = mutation({
  args: {
    docId: v.id("signatureRequests"),
    proposalRevisionId: v.string(),
    recipientEmail: v.string(),
    recipientName: v.string(),
    recipientPersonId: v.optional(v.string()),
    recipientContactId: v.optional(v.string()),
    provider: v.optional(v.any()),
    expiresAt: v.optional(v.number()),
    proposalId: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runSignatureRequestRequestSignature(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "SignatureRequest_requestSignature", __result);
    }
    return __result;
  },
});

export const SignatureRequest_createViaRequestSignature = mutation({
  args: {
    proposalRevisionId: v.string(),
    recipientEmail: v.string(),
    recipientName: v.string(),
    recipientPersonId: v.optional(v.string()),
    recipientContactId: v.optional(v.string()),
    provider: v.optional(v.any()),
    expiresAt: v.optional(v.number()),
    proposalId: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { proposalRevisionId, recipientEmail, recipientName, recipientPersonId, recipientContactId, provider, expiresAt, proposalId } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      provider: args.provider !== undefined ? args.provider : "internal",
      createdAt: Date.now(),
      updatedAt: Date.now(),
      status: "requested",
      expiresAt: args.expiresAt,
      proposalId: args.proposalId,
      proposalRevisionId: args.proposalRevisionId,
      recipientContactId: args.recipientContactId,
      recipientEmail: args.recipientEmail,
      recipientName: args.recipientName,
      recipientPersonId: args.recipientPersonId
    };
    const __rel_proposalRevision = await __resolveRelation(ctx, "proposalRevisions", [__auth.tenantId, __draft.proposalRevisionId], ["tenantId","id"], "tenantId", __auth.tenantId);
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may read signature requests");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may write signature requests through commands");
    if (!((checkRole(user, "salesAccess") || checkRole(user, "clientAccess")))) throw new Error("Sales staff and clients may execute signature request commands");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.requestedAt == null))) throw new Error("Guard 1 failed");
    if (!((user.id != null))) throw new Error("Guard 2 failed");
    if (!((user.personId != null))) throw new Error("Guard 3 failed");
    if (!((__rel_proposalRevision != null))) throw new Error("Guard 4 failed");
    const doc: Record<string, any> = {
      ...__draft,
      proposalRevisionId: proposalRevisionId,
      proposalId: proposalId,
      recipientEmail: recipientEmail,
      recipientName: recipientName,
      recipientPersonId: recipientPersonId,
      recipientContactId: recipientContactId,
      provider: ((provider != null) ? provider : "internal"),
      expiresAt: ((expiresAt != null) ? expiresAt : (Date.now() + ((((30 * 24) * 60) * 60) * 1000))),
      status: "requested",
      requestedAt: Date.now(),
      requestedByPersonId: user.personId,
      version: 1,
    };
    const docId = await ctx.db.insert("signatureRequests", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, signatureRequestId: docId, tenantId: doc.tenantId, proposalRevisionId: doc.proposalRevisionId, recipientEmail: doc.recipientEmail, recipientName: doc.recipientName, provider: ((doc.provider != null) ? doc.provider : "internal"), expiresAt: ((doc.expiresAt != null) ? doc.expiresAt : (Date.now() + ((((30 * 24) * 60) * 60) * 1000))), _subject: { entity: "SignatureRequest", command: "requestSignature", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "SignatureRequested", entity: "SignatureRequest", entityId: docId, payload: { signatureRequestId: docId, tenantId: doc.tenantId, proposalRevisionId: doc.proposalRevisionId, recipientEmail: doc.recipientEmail, recipientName: doc.recipientName, provider: ((doc.provider != null) ? doc.provider : "internal"), expiresAt: ((doc.expiresAt != null) ? doc.expiresAt : (Date.now() + ((((30 * 24) * 60) * 60) * 1000))) }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "SignatureRequest_createViaRequestSignature", __result);
    }
    return __result;
  },
});

async function __runSignatureRequestRevoke(ctx: MutationCtx, { docId, revokeReason, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("SignatureRequest not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("SignatureRequest not found");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may read signature requests");
    if (!(checkRole(user, "salesAccess"))) throw new Error("Sales staff may write signature requests through commands");
    if (!((checkRole(user, "salesAccess") || checkRole(user, "clientAccess")))) throw new Error("Sales staff and clients may execute signature request commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.status === "requested"))) throw new Error("Guard 1 failed");
    if (!((doc.requestedAt != null))) throw new Error("Guard 2 failed");
    if (!((checkRole(user, "salesAccess") || checkRole(user, "salesManageAccess")))) throw new Error("Guard 3 failed");
    if (!((((revokeReason).trim()).length > 0))) throw new Error("Guard 4 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "revoked";
        const __allowed: Record<string, string[]> = { "requested": ["completed", "expired", "revoked"], "completed": [], "expired": [], "revoked": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "revoked",
      revokedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, signatureRequestId: docId, tenantId: __after.tenantId, proposalRevisionId: __after.proposalRevisionId, revokedByPersonId: ((user.id != null) ? user.id : ""), revokeReason: revokeReason, revokedAt: Date.now(), _subject: { entity: "SignatureRequest", command: "revoke", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "SignatureRevoked", entity: "SignatureRequest", entityId: docId, payload: { signatureRequestId: docId, tenantId: __after.tenantId, proposalRevisionId: __after.proposalRevisionId, revokedByPersonId: ((user.id != null) ? user.id : ""), revokeReason: revokeReason, revokedAt: Date.now() }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const SignatureRequest_revoke = mutation({
  args: {
    docId: v.id("signatureRequests"),
    revokeReason: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runSignatureRequestRevoke(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "SignatureRequest_revoke", __result);
    }
    return __result;
  },
});

async function __runStaffMessageMarkRead(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("StaffMessage not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("StaffMessage not found");
    const doc = await __decryptDoc(ctx, "StaffMessage", ["body"], __storedDoc) as Record<string, any>;
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may read staff messages");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may write staff messages through commands");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may execute staff message commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.readAt == null))) throw new Error("Guard 1 failed");
    if (!((user.id != null))) throw new Error("Guard 2 failed");
    if (!((doc.recipientAuthSubjectId != null))) throw new Error("Guard 3 failed");
    if (!((doc.recipientAuthSubjectId === user.id))) throw new Error("Guard 4 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      readAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "StaffMessage", ["body"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, staffMessageId: docId, tenantId: __after.tenantId, recipientPersonId: __after.recipientPersonId, readAt: __after.readAt, _subject: { entity: "StaffMessage", command: "markRead", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "StaffMessageRead", entity: "StaffMessage", entityId: docId, payload: { staffMessageId: docId, tenantId: __after.tenantId, recipientPersonId: __after.recipientPersonId, readAt: __after.readAt }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const StaffMessage_markRead = mutation({
  args: {
    docId: v.id("staffMessages"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runStaffMessageMarkRead(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "StaffMessage_markRead", __result);
    }
    return __result;
  },
});

async function __runStaffMessageSend(ctx: MutationCtx, { docId, senderPersonId, recipientPersonId, recipientAuthSubjectId, body, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("StaffMessage not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("StaffMessage not found");
    const doc = await __decryptDoc(ctx, "StaffMessage", ["body"], __storedDoc) as Record<string, any>;
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may read staff messages");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may write staff messages through commands");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may execute staff message commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!((user.id != null))) throw new Error("Guard 1 failed");
    if (!((((body).trim()).length > 0))) throw new Error("Message text is required");
    if (!((senderPersonId !== recipientPersonId))) throw new Error("Sender and recipient must be different people");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      senderPersonId: senderPersonId,
      recipientPersonId: recipientPersonId,
      senderAuthSubjectId: user.id,
      recipientAuthSubjectId: recipientAuthSubjectId,
      body: body,
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "StaffMessage", ["body"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, staffMessageId: docId, tenantId: __after.tenantId, senderPersonId: senderPersonId, recipientPersonId: recipientPersonId, _subject: { entity: "StaffMessage", command: "send", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "StaffMessageSent", entity: "StaffMessage", entityId: docId, payload: { staffMessageId: docId, tenantId: __after.tenantId, senderPersonId: senderPersonId, recipientPersonId: recipientPersonId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const StaffMessage_send = mutation({
  args: {
    docId: v.id("staffMessages"),
    senderPersonId: v.string(),
    recipientPersonId: v.string(),
    recipientAuthSubjectId: v.optional(v.string()),
    body: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runStaffMessageSend(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "StaffMessage_send", __result);
    }
    return __result;
  },
});

export const StaffMessage_createViaSend = mutation({
  args: {
    senderPersonId: v.string(),
    recipientPersonId: v.string(),
    recipientAuthSubjectId: v.optional(v.string()),
    body: v.string(),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { senderPersonId, recipientPersonId, recipientAuthSubjectId, body } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      createdAt: Date.now(),
      updatedAt: Date.now(),
      body: args.body,
      recipientAuthSubjectId: args.recipientAuthSubjectId,
      recipientPersonId: args.recipientPersonId,
      senderPersonId: args.senderPersonId
    };
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may read staff messages");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may write staff messages through commands");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may execute staff message commands");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!((user.id != null))) throw new Error("Guard 1 failed");
    if (!((((body).trim()).length > 0))) throw new Error("Message text is required");
    if (!((senderPersonId !== recipientPersonId))) throw new Error("Sender and recipient must be different people");
    const doc: Record<string, any> = {
      ...__draft,
      senderPersonId: senderPersonId,
      recipientPersonId: recipientPersonId,
      senderAuthSubjectId: user.id,
      recipientAuthSubjectId: recipientAuthSubjectId,
      body: body,
      version: 1,
    };
    const __storedDoc = await __encryptDoc(ctx, "StaffMessage", ["body"], doc);
    const docId = await ctx.db.insert("staffMessages", __storedDoc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, staffMessageId: docId, tenantId: doc.tenantId, senderPersonId: doc.senderPersonId, recipientPersonId: doc.recipientPersonId, _subject: { entity: "StaffMessage", command: "send", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "StaffMessageSent", entity: "StaffMessage", entityId: docId, payload: { staffMessageId: docId, tenantId: doc.tenantId, senderPersonId: doc.senderPersonId, recipientPersonId: doc.recipientPersonId }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "StaffMessage_createViaSend", __result);
    }
    return __result;
  },
});

async function __runStockCountLineConfirmLedgerMatch(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("StockCountLine not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("StockCountLine not found");
    const __rel_session = await __resolveRelation(ctx, "stockCountSessions", [__auth.tenantId, doc.stockCountSessionId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_inventoryItem = await __resolveRelation(ctx, "inventoryItems", [__auth.tenantId, doc.inventoryItemId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).stockCountSessionId;
      ((doc as any) as any).session = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    {
      const __fk = ((doc as any) as any).inventoryItemId;
      ((doc as any) as any).inventoryItem = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may read stock count lines");
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may write stock count lines through commands");
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may execute stock count line commands");
    if (!((doc.status === "counted"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(((__rel_session != null) && (__rel_session.status === "in_progress")))) throw new Error("Guard 2 failed");
    if (!(((__rel_inventoryItem != null) && (__rel_inventoryItem.deletedAt == null)))) throw new Error("Guard 3 failed");
    if (!((doc.countedQuantity === __rel_inventoryItem.quantityOnHand))) throw new Error("Guard 4 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "reconciled";
        const __allowed: Record<string, string[]> = { "pending": ["counted"], "counted": ["reconciled"], "reconciled": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      ledgerQuantityAtReconcile: __rel_inventoryItem.quantityOnHand,
      adjustmentQuantity: 0,
      reconciledById: user.id,
      reconciledAt: Date.now(),
      status: "reconciled",
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, stockCountLineId: docId, stockCountSessionId: __after.stockCountSessionId, tenantId: __after.tenantId, actorId: user.id, inventoryItemId: __after.inventoryItemId, expectedQuantity: __after.expectedQuantity, countedQuantity: __after.countedQuantity, varianceQuantity: __after.varianceQuantity, ledgerQuantityAtReconcile: __rel_inventoryItem.quantityOnHand, unit: __after.unit, status: "reconciled", _subject: { entity: "StockCountLine", command: "confirmLedgerMatch", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "StockCountLineMatched", entity: "StockCountLine", entityId: docId, payload: { stockCountLineId: docId, stockCountSessionId: __after.stockCountSessionId, tenantId: __after.tenantId, actorId: user.id, inventoryItemId: __after.inventoryItemId, expectedQuantity: __after.expectedQuantity, countedQuantity: __after.countedQuantity, varianceQuantity: __after.varianceQuantity, ledgerQuantityAtReconcile: __rel_inventoryItem.quantityOnHand, unit: __after.unit, status: "reconciled" }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const StockCountLine_confirmLedgerMatch = mutation({
  args: {
    docId: v.id("stockCountLines"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runStockCountLineConfirmLedgerMatch(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "StockCountLine_confirmLedgerMatch", __result);
    }
    return __result;
  },
});

async function __runStockCountLineFreeze(ctx: MutationCtx, { docId, stockCountSessionId, inventoryItemId, locationId, ingredientId, unit, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("StockCountLine not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("StockCountLine not found");
    const __rel_session = await __resolveRelation(ctx, "stockCountSessions", [__auth.tenantId, doc.stockCountSessionId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_inventoryItem = await __resolveRelation(ctx, "inventoryItems", [__auth.tenantId, doc.inventoryItemId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).stockCountSessionId;
      ((doc as any) as any).session = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    {
      const __fk = ((doc as any) as any).inventoryItemId;
      ((doc as any) as any).inventoryItem = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may read stock count lines");
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may write stock count lines through commands");
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may execute stock count line commands");
    if (!((doc.frozenAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.status === "pending"))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((((__rel_session != null) && (__rel_session.status === "in_progress")) && (__rel_session.deletedAt == null)))) throw new Error("Guard 3 failed");
    if (!((((__rel_inventoryItem != null) && (__rel_inventoryItem.stockedAt != null)) && (__rel_inventoryItem.deletedAt == null)))) throw new Error("Guard 4 failed");
    if (!((stockCountSessionId === doc.stockCountSessionId))) throw new Error("Count session must match the seeded session reference");
    if (!((inventoryItemId === doc.inventoryItemId))) throw new Error("Stock line must match the seeded inventory reference");
    if (!(((locationId === doc.locationId) && (locationId === __rel_inventoryItem.locationId)))) throw new Error("Count location must match the stock line location");
    if (!(((ingredientId === doc.ingredientId) && (ingredientId === __rel_inventoryItem.ingredientId)))) throw new Error("Count ingredient must match the stock line ingredient");
    if (!(((unit === doc.unit) && (unit === __rel_inventoryItem.unit)))) throw new Error("Count unit must match the stock line unit");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      stockCountSessionId: stockCountSessionId,
      inventoryItemId: inventoryItemId,
      locationId: locationId,
      ingredientId: ingredientId,
      unit: unit,
      expectedQuantity: __rel_inventoryItem.quantityOnHand,
      countedQuantity: 0,
      varianceQuantity: 0,
      ledgerQuantityAtReconcile: 0,
      adjustmentQuantity: 0,
      frozenAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const __rel_inventoryItem__post = await __resolveRelation(ctx, "inventoryItems", [__auth.tenantId, __after.inventoryItemId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, stockCountLineId: docId, stockCountSessionId: __after.stockCountSessionId, tenantId: __after.tenantId, actorId: user.id, inventoryItemId: __after.inventoryItemId, locationId: __after.locationId, ingredientId: __after.ingredientId, expectedQuantity: __rel_inventoryItem__post.quantityOnHand, unit: __after.unit, status: "pending", _subject: { entity: "StockCountLine", command: "freeze", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "StockCountLineFrozen", entity: "StockCountLine", entityId: docId, payload: { stockCountLineId: docId, stockCountSessionId: __after.stockCountSessionId, tenantId: __after.tenantId, actorId: user.id, inventoryItemId: __after.inventoryItemId, locationId: __after.locationId, ingredientId: __after.ingredientId, expectedQuantity: __rel_inventoryItem__post.quantityOnHand, unit: __after.unit, status: "pending" }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const StockCountLine_freeze = mutation({
  args: {
    docId: v.id("stockCountLines"),
    stockCountSessionId: v.string(),
    inventoryItemId: v.string(),
    locationId: v.string(),
    ingredientId: v.string(),
    unit: v.any(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runStockCountLineFreeze(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "StockCountLine_freeze", __result);
    }
    return __result;
  },
});

export const StockCountLine_createViaFreeze = mutation({
  args: {
    stockCountSessionId: v.string(),
    inventoryItemId: v.string(),
    locationId: v.string(),
    ingredientId: v.string(),
    unit: v.any(),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { stockCountSessionId, inventoryItemId, locationId, ingredientId, unit } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      status: "pending",
      createdAt: Date.now(),
      updatedAt: Date.now(),
      ingredientId: args.ingredientId,
      inventoryItemId: args.inventoryItemId,
      locationId: args.locationId,
      stockCountSessionId: args.stockCountSessionId,
      unit: args.unit
    };
    const __rel_session = await __resolveRelation(ctx, "stockCountSessions", [__auth.tenantId, __draft.stockCountSessionId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_inventoryItem = await __resolveRelation(ctx, "inventoryItems", [__auth.tenantId, __draft.inventoryItemId], ["tenantId","id"], "tenantId", __auth.tenantId);
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may read stock count lines");
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may write stock count lines through commands");
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may execute stock count line commands");
    if (!((__draft.frozenAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.status === "pending"))) throw new Error("Guard 1 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((((__rel_session != null) && (__rel_session.status === "in_progress")) && (__rel_session.deletedAt == null)))) throw new Error("Guard 3 failed");
    if (!((((__rel_inventoryItem != null) && (__rel_inventoryItem.stockedAt != null)) && (__rel_inventoryItem.deletedAt == null)))) throw new Error("Guard 4 failed");
    if (!((stockCountSessionId === __draft.stockCountSessionId))) throw new Error("Count session must match the seeded session reference");
    if (!((inventoryItemId === __draft.inventoryItemId))) throw new Error("Stock line must match the seeded inventory reference");
    if (!(((locationId === __draft.locationId) && (locationId === __rel_inventoryItem.locationId)))) throw new Error("Count location must match the stock line location");
    if (!(((ingredientId === __draft.ingredientId) && (ingredientId === __rel_inventoryItem.ingredientId)))) throw new Error("Count ingredient must match the stock line ingredient");
    if (!(((unit === __draft.unit) && (unit === __rel_inventoryItem.unit)))) throw new Error("Count unit must match the stock line unit");
    const doc: Record<string, any> = {
      ...__draft,
      stockCountSessionId: stockCountSessionId,
      inventoryItemId: inventoryItemId,
      locationId: locationId,
      ingredientId: ingredientId,
      unit: unit,
      expectedQuantity: __rel_inventoryItem.quantityOnHand,
      countedQuantity: 0,
      varianceQuantity: 0,
      ledgerQuantityAtReconcile: 0,
      adjustmentQuantity: 0,
      frozenAt: Date.now(),
      version: 1,
    };
    const docId = await ctx.db.insert("stockCountLines", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, stockCountLineId: docId, stockCountSessionId: doc.stockCountSessionId, tenantId: doc.tenantId, actorId: user.id, inventoryItemId: doc.inventoryItemId, locationId: doc.locationId, ingredientId: doc.ingredientId, expectedQuantity: __rel_inventoryItem.quantityOnHand, unit: doc.unit, status: "pending", _subject: { entity: "StockCountLine", command: "freeze", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "StockCountLineFrozen", entity: "StockCountLine", entityId: docId, payload: { stockCountLineId: docId, stockCountSessionId: doc.stockCountSessionId, tenantId: doc.tenantId, actorId: user.id, inventoryItemId: doc.inventoryItemId, locationId: doc.locationId, ingredientId: doc.ingredientId, expectedQuantity: __rel_inventoryItem.quantityOnHand, unit: doc.unit, status: "pending" }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "StockCountLine_createViaFreeze", __result);
    }
    return __result;
  },
});

async function __runStockCountLineReconcileVariance(ctx: MutationCtx, { docId, reason, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("StockCountLine not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("StockCountLine not found");
    const __rel_session = await __resolveRelation(ctx, "stockCountSessions", [__auth.tenantId, doc.stockCountSessionId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_inventoryItem = await __resolveRelation(ctx, "inventoryItems", [__auth.tenantId, doc.inventoryItemId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).stockCountSessionId;
      ((doc as any) as any).session = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    {
      const __fk = ((doc as any) as any).inventoryItemId;
      ((doc as any) as any).inventoryItem = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may read stock count lines");
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may write stock count lines through commands");
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may execute stock count line commands");
    if (!((doc.status === "counted"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(((__rel_session != null) && (__rel_session.status === "in_progress")))) throw new Error("Guard 2 failed");
    if (!(((__rel_inventoryItem != null) && (__rel_inventoryItem.deletedAt == null)))) throw new Error("Guard 3 failed");
    if (!((doc.countedQuantity !== __rel_inventoryItem.quantityOnHand))) throw new Error("Guard 4 failed");
    if (!((((reason).trim()).length > 0))) throw new Error("Adjustment reason is required");
    const ledgerQuantity = __rel_inventoryItem.quantityOnHand;
    const adjustmentQuantity = (doc.countedQuantity - ledgerQuantity);
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "reconciled";
        const __allowed: Record<string, string[]> = { "pending": ["counted"], "counted": ["reconciled"], "reconciled": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      ledgerQuantityAtReconcile: ledgerQuantity,
      adjustmentQuantity: adjustmentQuantity,
      reconciledById: user.id,
      reconciledAt: Date.now(),
      status: "reconciled",
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, stockCountLineId: docId, stockCountSessionId: __after.stockCountSessionId, tenantId: __after.tenantId, actorId: user.id, inventoryItemId: __after.inventoryItemId, expectedQuantity: __after.expectedQuantity, countedQuantity: __after.countedQuantity, varianceQuantity: __after.varianceQuantity, ledgerQuantityAtReconcile: ledgerQuantity, adjustmentQuantity: adjustmentQuantity, unit: __after.unit, reason: reason, status: "reconciled", _subject: { entity: "StockCountLine", command: "reconcileVariance", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "StockCountVarianceReconciled", entity: "StockCountLine", entityId: docId, payload: { stockCountLineId: docId, stockCountSessionId: __after.stockCountSessionId, tenantId: __after.tenantId, actorId: user.id, inventoryItemId: __after.inventoryItemId, expectedQuantity: __after.expectedQuantity, countedQuantity: __after.countedQuantity, varianceQuantity: __after.varianceQuantity, ledgerQuantityAtReconcile: ledgerQuantity, adjustmentQuantity: adjustmentQuantity, unit: __after.unit, reason: reason, status: "reconciled" }, createdAt: Date.now() });
    // Reactions
    const reactionTarget0 = payload.inventoryItemId;
    if (reactionTarget0) await __runInventoryItemAdjustQuantity(ctx, { docId: reactionTarget0, delta: payload.adjustmentQuantity, reason: payload.reason } as any);
    return { ...doc, ...updates };
}

export const StockCountLine_reconcileVariance = mutation({
  args: {
    docId: v.id("stockCountLines"),
    reason: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runStockCountLineReconcileVariance(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "StockCountLine_reconcileVariance", __result);
    }
    return __result;
  },
});

async function __runStockCountLineRecordCount(ctx: MutationCtx, { docId, countedQuantity, countNote, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("StockCountLine not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("StockCountLine not found");
    const __rel_session = await __resolveRelation(ctx, "stockCountSessions", [__auth.tenantId, doc.stockCountSessionId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).stockCountSessionId;
      ((doc as any) as any).session = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may read stock count lines");
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may write stock count lines through commands");
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may execute stock count line commands");
    if (!((doc.status === "pending"))) throw new Error("Guard 0 failed");
    if (!((doc.frozenAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!(((__rel_session != null) && (__rel_session.status === "in_progress")))) throw new Error("Guard 3 failed");
    if (!((countedQuantity >= 0))) throw new Error("Counted quantity cannot be negative");
    const varianceQuantity = (countedQuantity - doc.expectedQuantity);
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "counted";
        const __allowed: Record<string, string[]> = { "pending": ["counted"], "counted": ["reconciled"], "reconciled": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      countedQuantity: countedQuantity,
      varianceQuantity: varianceQuantity,
      countNote: countNote,
      countedById: user.id,
      countedAt: Date.now(),
      status: "counted",
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, stockCountLineId: docId, stockCountSessionId: __after.stockCountSessionId, tenantId: __after.tenantId, actorId: user.id, inventoryItemId: __after.inventoryItemId, expectedQuantity: __after.expectedQuantity, countedQuantity: countedQuantity, varianceQuantity: varianceQuantity, unit: __after.unit, status: "counted", _subject: { entity: "StockCountLine", command: "recordCount", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "StockCountLineCounted", entity: "StockCountLine", entityId: docId, payload: { stockCountLineId: docId, stockCountSessionId: __after.stockCountSessionId, tenantId: __after.tenantId, actorId: user.id, inventoryItemId: __after.inventoryItemId, expectedQuantity: __after.expectedQuantity, countedQuantity: countedQuantity, varianceQuantity: varianceQuantity, unit: __after.unit, status: "counted" }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const StockCountLine_recordCount = mutation({
  args: {
    docId: v.id("stockCountLines"),
    countedQuantity: v.number(),
    countNote: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runStockCountLineRecordCount(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "StockCountLine_recordCount", __result);
    }
    return __result;
  },
});

async function __runStockCountLineReviseCount(ctx: MutationCtx, { docId, countedQuantity, countNote, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("StockCountLine not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("StockCountLine not found");
    const __rel_session = await __resolveRelation(ctx, "stockCountSessions", [__auth.tenantId, doc.stockCountSessionId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).stockCountSessionId;
      ((doc as any) as any).session = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may read stock count lines");
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may write stock count lines through commands");
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may execute stock count line commands");
    if (!((doc.status === "counted"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(((__rel_session != null) && (__rel_session.status === "in_progress")))) throw new Error("Guard 2 failed");
    if (!((countedQuantity >= 0))) throw new Error("Counted quantity cannot be negative");
    const varianceQuantity = (countedQuantity - doc.expectedQuantity);
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      countedQuantity: countedQuantity,
      varianceQuantity: varianceQuantity,
      countNote: countNote,
      countedById: user.id,
      countedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, stockCountLineId: docId, stockCountSessionId: __after.stockCountSessionId, tenantId: __after.tenantId, actorId: user.id, inventoryItemId: __after.inventoryItemId, expectedQuantity: __after.expectedQuantity, countedQuantity: countedQuantity, varianceQuantity: varianceQuantity, unit: __after.unit, status: "counted", _subject: { entity: "StockCountLine", command: "reviseCount", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "StockCountLineCountRevised", entity: "StockCountLine", entityId: docId, payload: { stockCountLineId: docId, stockCountSessionId: __after.stockCountSessionId, tenantId: __after.tenantId, actorId: user.id, inventoryItemId: __after.inventoryItemId, expectedQuantity: __after.expectedQuantity, countedQuantity: countedQuantity, varianceQuantity: varianceQuantity, unit: __after.unit, status: "counted" }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const StockCountLine_reviseCount = mutation({
  args: {
    docId: v.id("stockCountLines"),
    countedQuantity: v.number(),
    countNote: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runStockCountLineReviseCount(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "StockCountLine_reviseCount", __result);
    }
    return __result;
  },
});

async function __runStockCountSessionClose(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("StockCountSession not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("StockCountSession not found");
    (doc as any).lines = await ctx.db.query("stockCountLines").withIndex("by_stockCountSessionId", (q: any) => q.eq("stockCountSessionId", docId)).collect();
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may read stock count sessions");
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may write stock count sessions through commands");
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may execute stock count session commands");
    if (!((doc.status === "in_progress"))) throw new Error("Guard 0 failed");
    if (!((doc.startedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((((doc.lines) ?? []).filter((line: Doc<"stockCountLines">) => ((line.deletedAt == null))).length === doc.lineCount))) throw new Error("Guard 3 failed");
    if (!((((doc.lines) ?? []).filter((line: Doc<"stockCountLines">) => (((line.deletedAt == null) && (line.status !== "reconciled")))).length === 0))) throw new Error("Guard 4 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "closed";
        const __allowed: Record<string, string[]> = { "in_progress": ["closed"], "closed": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "closed",
      closedById: user.id,
      closedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, stockCountSessionId: docId, tenantId: __after.tenantId, actorId: user.id, label: __after.label, locationIds: __after.locationIds, lineCount: __after.lineCount, status: "closed", _subject: { entity: "StockCountSession", command: "close", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "StockCountSessionClosed", entity: "StockCountSession", entityId: docId, payload: { stockCountSessionId: docId, tenantId: __after.tenantId, actorId: user.id, label: __after.label, locationIds: __after.locationIds, lineCount: __after.lineCount, status: "closed" }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const StockCountSession_close = mutation({
  args: {
    docId: v.id("stockCountSessions"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runStockCountSessionClose(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "StockCountSession_close", __result);
    }
    return __result;
  },
});

async function __runStockCountSessionStart(ctx: MutationCtx, { docId, label, locationIds, locationNames, lineCount, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("StockCountSession not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("StockCountSession not found");
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may read stock count sessions");
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may write stock count sessions through commands");
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may execute stock count session commands");
    if (!((doc.startedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.status === "in_progress"))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((((label).trim()).length > 0))) throw new Error("Count session name is required");
    if (!((((locationIds).trim()).length > 2))) throw new Error("Choose at least one storage location");
    if (!((((locationNames).trim()).length > 0))) throw new Error("Count session locations are required");
    if (!((lineCount >= 0))) throw new Error("Count line total cannot be negative");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      label: label,
      locationIds: locationIds,
      locationNames: locationNames,
      lineCount: lineCount,
      startedById: user.id,
      startedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, stockCountSessionId: docId, tenantId: __after.tenantId, actorId: user.id, label: label, locationIds: locationIds, locationNames: locationNames, lineCount: lineCount, status: "in_progress", _subject: { entity: "StockCountSession", command: "start", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "StockCountSessionStarted", entity: "StockCountSession", entityId: docId, payload: { stockCountSessionId: docId, tenantId: __after.tenantId, actorId: user.id, label: label, locationIds: locationIds, locationNames: locationNames, lineCount: lineCount, status: "in_progress" }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const StockCountSession_start = mutation({
  args: {
    docId: v.id("stockCountSessions"),
    label: v.string(),
    locationIds: v.string(),
    locationNames: v.string(),
    lineCount: v.any(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runStockCountSessionStart(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "StockCountSession_start", __result);
    }
    return __result;
  },
});

export const StockCountSession_createViaStart = mutation({
  args: {
    label: v.string(),
    locationIds: v.string(),
    locationNames: v.string(),
    lineCount: v.any(),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { label, locationIds, locationNames, lineCount } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      status: "in_progress",
      createdAt: Date.now(),
      updatedAt: Date.now(),
      label: args.label,
      lineCount: args.lineCount,
      locationIds: args.locationIds,
      locationNames: args.locationNames
    };
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may read stock count sessions");
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may write stock count sessions through commands");
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may execute stock count session commands");
    if (!((__draft.startedAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.status === "in_progress"))) throw new Error("Guard 1 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((((label).trim()).length > 0))) throw new Error("Count session name is required");
    if (!((((locationIds).trim()).length > 2))) throw new Error("Choose at least one storage location");
    if (!((((locationNames).trim()).length > 0))) throw new Error("Count session locations are required");
    if (!((lineCount >= 0))) throw new Error("Count line total cannot be negative");
    const doc: Record<string, any> = {
      ...__draft,
      label: label,
      locationIds: locationIds,
      locationNames: locationNames,
      lineCount: lineCount,
      startedById: user.id,
      startedAt: Date.now(),
      version: 1,
    };
    const docId = await ctx.db.insert("stockCountSessions", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, stockCountSessionId: docId, tenantId: doc.tenantId, actorId: user.id, label: doc.label, locationIds: doc.locationIds, locationNames: doc.locationNames, lineCount: doc.lineCount, status: "in_progress", _subject: { entity: "StockCountSession", command: "start", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "StockCountSessionStarted", entity: "StockCountSession", entityId: docId, payload: { stockCountSessionId: docId, tenantId: doc.tenantId, actorId: user.id, label: doc.label, locationIds: doc.locationIds, locationNames: doc.locationNames, lineCount: doc.lineCount, status: "in_progress" }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "StockCountSession_createViaStart", __result);
    }
    return __result;
  },
});

async function __runStockTransferRecord(ctx: MutationCtx, { docId, sourceInventoryItemId, destinationInventoryItemId, ingredientId, sourceLocationId, destinationLocationId, quantity, unit, notes, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("StockTransfer not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("StockTransfer not found");
    const __rel_sourceItem = await __resolveRelation(ctx, "inventoryItems", [__auth.tenantId, doc.sourceInventoryItemId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_destinationItem = await __resolveRelation(ctx, "inventoryItems", [__auth.tenantId, doc.destinationInventoryItemId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).sourceInventoryItemId;
      ((doc as any) as any).sourceItem = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    {
      const __fk = ((doc as any) as any).destinationInventoryItemId;
      ((doc as any) as any).destinationItem = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may read stock transfers");
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may write stock transfers through commands");
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may execute stock transfer commands");
    if (!((doc.transferredAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((((__rel_sourceItem != null) && (__rel_sourceItem.stockedAt != null)) && (__rel_sourceItem.deletedAt == null)))) throw new Error("Guard 2 failed");
    if (!((((__rel_destinationItem != null) && (__rel_destinationItem.stockedAt != null)) && (__rel_destinationItem.deletedAt == null)))) throw new Error("Guard 3 failed");
    if (!((sourceInventoryItemId === doc.sourceInventoryItemId))) throw new Error("Record sourceInventoryItemId must match the seeded source stock reference");
    if (!((destinationInventoryItemId === doc.destinationInventoryItemId))) throw new Error("Record destinationInventoryItemId must match the seeded destination stock reference");
    if (!((sourceInventoryItemId !== destinationInventoryItemId))) throw new Error("Source and destination stock lines must differ");
    if (!((ingredientId === __rel_sourceItem.ingredientId))) throw new Error("Transfer ingredientId must match the source stock ingredient");
    if (!((__rel_sourceItem.ingredientId === __rel_destinationItem.ingredientId))) throw new Error("Source and destination must hold the same ingredient");
    if (!((sourceLocationId === __rel_sourceItem.locationId))) throw new Error("Transfer sourceLocationId must match the source stock location");
    if (!((destinationLocationId === __rel_destinationItem.locationId))) throw new Error("Transfer destinationLocationId must match the destination stock location");
    if (!((sourceLocationId !== destinationLocationId))) throw new Error("Source and destination locations must differ");
    if (!((__rel_sourceItem.unit === __rel_destinationItem.unit))) throw new Error("Source and destination stock lines must use the same unit");
    if (!((quantity > 0))) throw new Error("Transfer quantity must be positive");
    if (!((__rel_sourceItem.quantityOnHand >= quantity))) throw new Error("Cannot transfer more than the source has on hand");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      sourceInventoryItemId: sourceInventoryItemId,
      destinationInventoryItemId: destinationInventoryItemId,
      ingredientId: ingredientId,
      sourceLocationId: sourceLocationId,
      destinationLocationId: destinationLocationId,
      quantity: quantity,
      unit: unit,
      notes: ((notes != null) ? notes : doc.notes),
      transferredAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, stockTransferId: docId, tenantId: __after.tenantId, sourceInventoryItemId: sourceInventoryItemId, destinationInventoryItemId: destinationInventoryItemId, ingredientId: ingredientId, sourceLocationId: sourceLocationId, destinationLocationId: destinationLocationId, quantity: quantity, unit: unit, _subject: { entity: "StockTransfer", command: "record", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "StockTransferRecorded", entity: "StockTransfer", entityId: docId, payload: { stockTransferId: docId, tenantId: __after.tenantId, sourceInventoryItemId: sourceInventoryItemId, destinationInventoryItemId: destinationInventoryItemId, ingredientId: ingredientId, sourceLocationId: sourceLocationId, destinationLocationId: destinationLocationId, quantity: quantity, unit: unit }, createdAt: Date.now() });
    // Reactions
    const reactionTarget0 = payload.sourceInventoryItemId;
    if (reactionTarget0) await __runInventoryItemTransferOut(ctx, { docId: reactionTarget0, quantity: payload.quantity, destinationLocationId: payload.destinationLocationId } as any);
    const reactionTarget1 = payload.destinationInventoryItemId;
    if (reactionTarget1) await __runInventoryItemTransferIn(ctx, { docId: reactionTarget1, quantity: payload.quantity, sourceLocationId: payload.sourceLocationId } as any);
    return { ...doc, ...updates };
}

export const StockTransfer_record = mutation({
  args: {
    docId: v.id("stockTransfers"),
    sourceInventoryItemId: v.string(),
    destinationInventoryItemId: v.string(),
    ingredientId: v.string(),
    sourceLocationId: v.string(),
    destinationLocationId: v.string(),
    quantity: v.number(),
    unit: v.any(),
    notes: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runStockTransferRecord(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "StockTransfer_record", __result);
    }
    return __result;
  },
});

export const StockTransfer_createViaRecord = mutation({
  args: {
    sourceInventoryItemId: v.string(),
    destinationInventoryItemId: v.string(),
    ingredientId: v.string(),
    sourceLocationId: v.string(),
    destinationLocationId: v.string(),
    quantity: v.number(),
    unit: v.any(),
    notes: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { sourceInventoryItemId, destinationInventoryItemId, ingredientId, sourceLocationId, destinationLocationId, quantity, unit, notes } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      createdAt: Date.now(),
      updatedAt: Date.now(),
      destinationInventoryItemId: args.destinationInventoryItemId,
      destinationLocationId: args.destinationLocationId,
      ingredientId: args.ingredientId,
      notes: args.notes,
      quantity: args.quantity,
      sourceInventoryItemId: args.sourceInventoryItemId,
      sourceLocationId: args.sourceLocationId,
      unit: args.unit
    };
    const __rel_sourceItem = await __resolveRelation(ctx, "inventoryItems", [__auth.tenantId, __draft.sourceInventoryItemId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_destinationItem = await __resolveRelation(ctx, "inventoryItems", [__auth.tenantId, __draft.destinationInventoryItemId], ["tenantId","id"], "tenantId", __auth.tenantId);
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may read stock transfers");
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may write stock transfers through commands");
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may execute stock transfer commands");
    if (!((__draft.transferredAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((((__rel_sourceItem != null) && (__rel_sourceItem.stockedAt != null)) && (__rel_sourceItem.deletedAt == null)))) throw new Error("Guard 2 failed");
    if (!((((__rel_destinationItem != null) && (__rel_destinationItem.stockedAt != null)) && (__rel_destinationItem.deletedAt == null)))) throw new Error("Guard 3 failed");
    if (!((sourceInventoryItemId === __draft.sourceInventoryItemId))) throw new Error("Record sourceInventoryItemId must match the seeded source stock reference");
    if (!((destinationInventoryItemId === __draft.destinationInventoryItemId))) throw new Error("Record destinationInventoryItemId must match the seeded destination stock reference");
    if (!((sourceInventoryItemId !== destinationInventoryItemId))) throw new Error("Source and destination stock lines must differ");
    if (!((ingredientId === __rel_sourceItem.ingredientId))) throw new Error("Transfer ingredientId must match the source stock ingredient");
    if (!((__rel_sourceItem.ingredientId === __rel_destinationItem.ingredientId))) throw new Error("Source and destination must hold the same ingredient");
    if (!((sourceLocationId === __rel_sourceItem.locationId))) throw new Error("Transfer sourceLocationId must match the source stock location");
    if (!((destinationLocationId === __rel_destinationItem.locationId))) throw new Error("Transfer destinationLocationId must match the destination stock location");
    if (!((sourceLocationId !== destinationLocationId))) throw new Error("Source and destination locations must differ");
    if (!((__rel_sourceItem.unit === __rel_destinationItem.unit))) throw new Error("Source and destination stock lines must use the same unit");
    if (!((quantity > 0))) throw new Error("Transfer quantity must be positive");
    if (!((__rel_sourceItem.quantityOnHand >= quantity))) throw new Error("Cannot transfer more than the source has on hand");
    const doc: Record<string, any> = {
      ...__draft,
      sourceInventoryItemId: sourceInventoryItemId,
      destinationInventoryItemId: destinationInventoryItemId,
      ingredientId: ingredientId,
      sourceLocationId: sourceLocationId,
      destinationLocationId: destinationLocationId,
      quantity: quantity,
      unit: unit,
      notes: ((notes != null) ? notes : __draft.notes),
      transferredAt: Date.now(),
      version: 1,
    };
    const docId = await ctx.db.insert("stockTransfers", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, stockTransferId: docId, tenantId: doc.tenantId, sourceInventoryItemId: doc.sourceInventoryItemId, destinationInventoryItemId: doc.destinationInventoryItemId, ingredientId: doc.ingredientId, sourceLocationId: doc.sourceLocationId, destinationLocationId: doc.destinationLocationId, quantity: doc.quantity, unit: doc.unit, _subject: { entity: "StockTransfer", command: "record", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "StockTransferRecorded", entity: "StockTransfer", entityId: docId, payload: { stockTransferId: docId, tenantId: doc.tenantId, sourceInventoryItemId: doc.sourceInventoryItemId, destinationInventoryItemId: doc.destinationInventoryItemId, ingredientId: doc.ingredientId, sourceLocationId: doc.sourceLocationId, destinationLocationId: doc.destinationLocationId, quantity: doc.quantity, unit: doc.unit }, createdAt: Date.now() });
    // Reactions
    const reactionTarget0 = payload.sourceInventoryItemId;
    if (reactionTarget0) await __runInventoryItemTransferOut(ctx, { docId: reactionTarget0, quantity: payload.quantity, destinationLocationId: payload.destinationLocationId } as any);
    const reactionTarget1 = payload.destinationInventoryItemId;
    if (reactionTarget1) await __runInventoryItemTransferIn(ctx, { docId: reactionTarget1, quantity: payload.quantity, sourceLocationId: payload.sourceLocationId } as any);
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "StockTransfer_createViaRecord", __result);
    }
    return __result;
  },
});

async function __runStorageLocationActivate(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("StorageLocation not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("StorageLocation not found");
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may read storage locations");
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may write storage locations through commands");
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may execute storage location commands");
    if (!((doc.status === "inactive"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(checkRole(user, "inventoryManageAccess"))) throw new Error("Guard 2 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "active";
        const __allowed: Record<string, string[]> = { "active": ["inactive"], "inactive": ["active"] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "active",
      deactivatedAt: null,
      deactivationReason: null,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, storageLocationId: docId, tenantId: __after.tenantId, _subject: { entity: "StorageLocation", command: "activate", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "StorageLocationActivated", entity: "StorageLocation", entityId: docId, payload: { storageLocationId: docId, tenantId: __after.tenantId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const StorageLocation_activate = mutation({
  args: {
    docId: v.id("storageLocations"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runStorageLocationActivate(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "StorageLocation_activate", __result);
    }
    return __result;
  },
});

async function __runStorageLocationDeactivate(ctx: MutationCtx, { docId, reason, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("StorageLocation not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("StorageLocation not found");
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may read storage locations");
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may write storage locations through commands");
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may execute storage location commands");
    if (!((doc.status === "active"))) throw new Error("Guard 0 failed");
    if (!((doc.registeredAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!(checkRole(user, "inventoryManageAccess"))) throw new Error("Guard 3 failed");
    if (!((((reason).trim()).length > 0))) throw new Error("Deactivation reason is required");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "inactive";
        const __allowed: Record<string, string[]> = { "active": ["inactive"], "inactive": ["active"] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "inactive",
      deactivatedAt: Date.now(),
      deactivationReason: reason,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, storageLocationId: docId, tenantId: __after.tenantId, reason: reason, _subject: { entity: "StorageLocation", command: "deactivate", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "StorageLocationDeactivated", entity: "StorageLocation", entityId: docId, payload: { storageLocationId: docId, tenantId: __after.tenantId, reason: reason }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const StorageLocation_deactivate = mutation({
  args: {
    docId: v.id("storageLocations"),
    reason: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runStorageLocationDeactivate(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "StorageLocation_deactivate", __result);
    }
    return __result;
  },
});

async function __runStorageLocationRegister(ctx: MutationCtx, { docId, name, locationType, temperatureZone, minTemperature, maxTemperature, temperatureUnit, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("StorageLocation not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("StorageLocation not found");
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may read storage locations");
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may write storage locations through commands");
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may execute storage location commands");
    if (!((doc.registeredAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((((name).trim()).length > 0))) throw new Error("Storage location name is required");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      name: name,
      locationType: locationType,
      temperatureZone: temperatureZone,
      minTemperature: minTemperature,
      maxTemperature: maxTemperature,
      temperatureUnit: ((temperatureUnit != null) ? temperatureUnit : "F"),
      registeredAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, storageLocationId: docId, tenantId: __after.tenantId, name: name, locationType: locationType, temperatureZone: temperatureZone, _subject: { entity: "StorageLocation", command: "register", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "StorageLocationRegistered", entity: "StorageLocation", entityId: docId, payload: { storageLocationId: docId, tenantId: __after.tenantId, name: name, locationType: locationType, temperatureZone: temperatureZone }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const StorageLocation_register = mutation({
  args: {
    docId: v.id("storageLocations"),
    name: v.string(),
    locationType: v.optional(v.string()),
    temperatureZone: v.optional(v.string()),
    minTemperature: v.optional(v.number()),
    maxTemperature: v.optional(v.number()),
    temperatureUnit: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runStorageLocationRegister(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "StorageLocation_register", __result);
    }
    return __result;
  },
});

export const StorageLocation_createViaRegister = mutation({
  args: {
    name: v.string(),
    locationType: v.optional(v.string()),
    temperatureZone: v.optional(v.string()),
    minTemperature: v.optional(v.number()),
    maxTemperature: v.optional(v.number()),
    temperatureUnit: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { name, locationType, temperatureZone, minTemperature, maxTemperature, temperatureUnit } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      status: "active",
      createdAt: Date.now(),
      updatedAt: Date.now(),
      locationType: args.locationType,
      maxTemperature: args.maxTemperature,
      minTemperature: args.minTemperature,
      name: args.name,
      temperatureUnit: args.temperatureUnit,
      temperatureZone: args.temperatureZone
    };
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may read storage locations");
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may write storage locations through commands");
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may execute storage location commands");
    if (!((__draft.registeredAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((((name).trim()).length > 0))) throw new Error("Storage location name is required");
    const doc: Record<string, any> = {
      ...__draft,
      name: name,
      locationType: locationType,
      temperatureZone: temperatureZone,
      minTemperature: minTemperature,
      maxTemperature: maxTemperature,
      temperatureUnit: ((temperatureUnit != null) ? temperatureUnit : "F"),
      registeredAt: Date.now(),
      version: 1,
    };
    const docId = await ctx.db.insert("storageLocations", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, storageLocationId: docId, tenantId: doc.tenantId, name: doc.name, locationType: doc.locationType, temperatureZone: doc.temperatureZone, _subject: { entity: "StorageLocation", command: "register", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "StorageLocationRegistered", entity: "StorageLocation", entityId: docId, payload: { storageLocationId: docId, tenantId: doc.tenantId, name: doc.name, locationType: doc.locationType, temperatureZone: doc.temperatureZone }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "StorageLocation_createViaRegister", __result);
    }
    return __result;
  },
});

async function __runStorageLocationReviseDetails(ctx: MutationCtx, { docId, name, locationType, temperatureZone, minTemperature, maxTemperature, temperatureUnit, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("StorageLocation not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("StorageLocation not found");
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may read storage locations");
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may write storage locations through commands");
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may execute storage location commands");
    if (!((doc.registeredAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.status === "active"))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((((name).trim()).length > 0))) throw new Error("Storage location name is required");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      name: name,
      locationType: locationType,
      temperatureZone: temperatureZone,
      minTemperature: minTemperature,
      maxTemperature: maxTemperature,
      temperatureUnit: ((temperatureUnit != null) ? temperatureUnit : doc.temperatureUnit),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, storageLocationId: docId, tenantId: __after.tenantId, name: name, locationType: locationType, temperatureZone: temperatureZone, _subject: { entity: "StorageLocation", command: "reviseDetails", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "StorageLocationDetailsRevised", entity: "StorageLocation", entityId: docId, payload: { storageLocationId: docId, tenantId: __after.tenantId, name: name, locationType: locationType, temperatureZone: temperatureZone }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const StorageLocation_reviseDetails = mutation({
  args: {
    docId: v.id("storageLocations"),
    name: v.string(),
    locationType: v.optional(v.string()),
    temperatureZone: v.optional(v.string()),
    minTemperature: v.optional(v.number()),
    maxTemperature: v.optional(v.number()),
    temperatureUnit: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runStorageLocationReviseDetails(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "StorageLocation_reviseDetails", __result);
    }
    return __result;
  },
});

async function __runSyncErrorMarkResolved(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("SyncError not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("SyncError not found");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may read sync errors");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may write sync errors through commands");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may execute sync error commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!((user.id != null))) throw new Error("Guard 1 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "resolved",
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, syncErrorId: docId, tenantId: __after.tenantId, _subject: { entity: "SyncError", command: "markResolved", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "SyncErrorResolved", entity: "SyncError", entityId: docId, payload: { syncErrorId: docId, tenantId: __after.tenantId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const SyncError_markResolved = mutation({
  args: {
    docId: v.id("syncErrors"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runSyncErrorMarkResolved(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "SyncError_markResolved", __result);
    }
    return __result;
  },
});

async function __runSyncErrorRecord(ctx: MutationCtx, { docId, sourceSystem, recordType, kind, errorMessage, externalId, rawPayload, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("SyncError not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("SyncError not found");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may read sync errors");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may write sync errors through commands");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may execute sync error commands");
    if (!((doc.recordedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((user.id != null))) throw new Error("Guard 2 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      sourceSystem: sourceSystem,
      recordType: recordType,
      kind: kind,
      errorMessage: errorMessage,
      externalId: externalId,
      rawPayload: rawPayload,
      recordedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, syncErrorId: docId, tenantId: __after.tenantId, sourceSystem: sourceSystem, recordType: recordType, kind: kind, _subject: { entity: "SyncError", command: "record", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "SyncErrorRecorded", entity: "SyncError", entityId: docId, payload: { syncErrorId: docId, tenantId: __after.tenantId, sourceSystem: sourceSystem, recordType: recordType, kind: kind }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const SyncError_record = mutation({
  args: {
    docId: v.id("syncErrors"),
    sourceSystem: v.string(),
    recordType: v.string(),
    kind: v.any(),
    errorMessage: v.string(),
    externalId: v.optional(v.string()),
    rawPayload: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runSyncErrorRecord(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "SyncError_record", __result);
    }
    return __result;
  },
});

export const SyncError_createViaRecord = mutation({
  args: {
    sourceSystem: v.string(),
    recordType: v.string(),
    kind: v.any(),
    errorMessage: v.string(),
    externalId: v.optional(v.string()),
    rawPayload: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { sourceSystem, recordType, kind, errorMessage, externalId, rawPayload } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      status: "pending",
      attempts: 0,
      createdAt: Date.now(),
      updatedAt: Date.now(),
      errorMessage: args.errorMessage,
      externalId: args.externalId,
      kind: args.kind,
      rawPayload: args.rawPayload,
      recordType: args.recordType,
      sourceSystem: args.sourceSystem
    };
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may read sync errors");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may write sync errors through commands");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may execute sync error commands");
    if (!((__draft.recordedAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((user.id != null))) throw new Error("Guard 2 failed");
    const doc: Record<string, any> = {
      ...__draft,
      sourceSystem: sourceSystem,
      recordType: recordType,
      kind: kind,
      errorMessage: errorMessage,
      externalId: externalId,
      rawPayload: rawPayload,
      recordedAt: Date.now(),
      version: 1,
    };
    const docId = await ctx.db.insert("syncErrors", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, syncErrorId: docId, tenantId: doc.tenantId, sourceSystem: doc.sourceSystem, recordType: doc.recordType, kind: doc.kind, _subject: { entity: "SyncError", command: "record", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "SyncErrorRecorded", entity: "SyncError", entityId: docId, payload: { syncErrorId: docId, tenantId: doc.tenantId, sourceSystem: doc.sourceSystem, recordType: doc.recordType, kind: doc.kind }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "SyncError_createViaRecord", __result);
    }
    return __result;
  },
});

async function __runSyncErrorReopen(ctx: MutationCtx, { docId, attempts, kind, errorMessage, rawPayload, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("SyncError not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("SyncError not found");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may read sync errors");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may write sync errors through commands");
    if (!(checkRole(user, "staffAccess"))) throw new Error("Staff may execute sync error commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!((user.id != null))) throw new Error("Guard 1 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "pending",
      attempts: attempts,
      kind: kind,
      errorMessage: errorMessage,
      rawPayload: rawPayload,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, syncErrorId: docId, tenantId: __after.tenantId, _subject: { entity: "SyncError", command: "reopen", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "SyncErrorReopened", entity: "SyncError", entityId: docId, payload: { syncErrorId: docId, tenantId: __after.tenantId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const SyncError_reopen = mutation({
  args: {
    docId: v.id("syncErrors"),
    attempts: v.number(),
    kind: v.any(),
    errorMessage: v.string(),
    rawPayload: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runSyncErrorReopen(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "SyncError_reopen", __result);
    }
    return __result;
  },
});

async function __runTaxRateDefine(ctx: MutationCtx, { docId, name, percentage, appliesToFood, appliesToService, appliesToRental, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("TaxRate not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("TaxRate not found");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may read tax rates");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may maintain tax rates");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may execute tax-rate commands");
    if (!((doc.configuredAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((((name).trim()).length > 0))) throw new Error("Tax rate name is required");
    if (!(((percentage > 0) && (percentage <= 100)))) throw new Error("Tax rate percentage must be greater than 0 and no more than 100");
    if (!(((appliesToFood || appliesToService) || appliesToRental))) throw new Error("Select at least one line-item category");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      name: name,
      percentage: percentage,
      appliesToFood: appliesToFood,
      appliesToService: appliesToService,
      appliesToRental: appliesToRental,
      active: true,
      configuredAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, taxRateId: docId, tenantId: __after.tenantId, name: name, percentage: percentage, _subject: { entity: "TaxRate", command: "define", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "TaxRateDefined", entity: "TaxRate", entityId: docId, payload: { taxRateId: docId, tenantId: __after.tenantId, name: name, percentage: percentage }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const TaxRate_define = mutation({
  args: {
    docId: v.id("taxRates"),
    name: v.string(),
    percentage: v.number(),
    appliesToFood: v.boolean(),
    appliesToService: v.boolean(),
    appliesToRental: v.boolean(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runTaxRateDefine(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "TaxRate_define", __result);
    }
    return __result;
  },
});

export const TaxRate_createViaDefine = mutation({
  args: {
    name: v.string(),
    percentage: v.number(),
    appliesToFood: v.boolean(),
    appliesToService: v.boolean(),
    appliesToRental: v.boolean(),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { name, percentage, appliesToFood, appliesToService, appliesToRental } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      createdAt: Date.now(),
      updatedAt: Date.now(),
      appliesToFood: args.appliesToFood,
      appliesToRental: args.appliesToRental,
      appliesToService: args.appliesToService,
      name: args.name,
      percentage: args.percentage
    };
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may read tax rates");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may maintain tax rates");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may execute tax-rate commands");
    if (!((__draft.configuredAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((((name).trim()).length > 0))) throw new Error("Tax rate name is required");
    if (!(((percentage > 0) && (percentage <= 100)))) throw new Error("Tax rate percentage must be greater than 0 and no more than 100");
    if (!(((appliesToFood || appliesToService) || appliesToRental))) throw new Error("Select at least one line-item category");
    const doc: Record<string, any> = {
      ...__draft,
      name: name,
      percentage: percentage,
      appliesToFood: appliesToFood,
      appliesToService: appliesToService,
      appliesToRental: appliesToRental,
      active: true,
      configuredAt: Date.now(),
      version: 1,
    };
    const docId = await ctx.db.insert("taxRates", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, taxRateId: docId, tenantId: doc.tenantId, name: doc.name, percentage: doc.percentage, _subject: { entity: "TaxRate", command: "define", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "TaxRateDefined", entity: "TaxRate", entityId: docId, payload: { taxRateId: docId, tenantId: doc.tenantId, name: doc.name, percentage: doc.percentage }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "TaxRate_createViaDefine", __result);
    }
    return __result;
  },
});

async function __runTaxRateRevise(ctx: MutationCtx, { docId, name, percentage, appliesToFood, appliesToService, appliesToRental, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("TaxRate not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("TaxRate not found");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may read tax rates");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may maintain tax rates");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may execute tax-rate commands");
    if (!((doc.configuredAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((((name).trim()).length > 0))) throw new Error("Tax rate name is required");
    if (!(((percentage > 0) && (percentage <= 100)))) throw new Error("Tax rate percentage must be greater than 0 and no more than 100");
    if (!(((appliesToFood || appliesToService) || appliesToRental))) throw new Error("Select at least one line-item category");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      name: name,
      percentage: percentage,
      appliesToFood: appliesToFood,
      appliesToService: appliesToService,
      appliesToRental: appliesToRental,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, taxRateId: docId, tenantId: __after.tenantId, name: name, percentage: percentage, _subject: { entity: "TaxRate", command: "revise", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "TaxRateRevised", entity: "TaxRate", entityId: docId, payload: { taxRateId: docId, tenantId: __after.tenantId, name: name, percentage: percentage }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const TaxRate_revise = mutation({
  args: {
    docId: v.id("taxRates"),
    name: v.string(),
    percentage: v.number(),
    appliesToFood: v.boolean(),
    appliesToService: v.boolean(),
    appliesToRental: v.boolean(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runTaxRateRevise(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "TaxRate_revise", __result);
    }
    return __result;
  },
});

async function __runTaxRateSetActive(ctx: MutationCtx, { docId, active, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("TaxRate not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("TaxRate not found");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may read tax rates");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may maintain tax rates");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may execute tax-rate commands");
    if (!((doc.configuredAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      active: active,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, taxRateId: docId, tenantId: __after.tenantId, active: active, _subject: { entity: "TaxRate", command: "setActive", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "TaxRateActivationChanged", entity: "TaxRate", entityId: docId, payload: { taxRateId: docId, tenantId: __after.tenantId, active: active }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const TaxRate_setActive = mutation({
  args: {
    docId: v.id("taxRates"),
    active: v.boolean(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runTaxRateSetActive(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "TaxRate_setActive", __result);
    }
    return __result;
  },
});

async function __runTimeOffRequestApprove(ctx: MutationCtx, { docId, responseNote, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("TimeOffRequest not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("TimeOffRequest not found");
    const doc = await __decryptDoc(ctx, "TimeOffRequest", ["reason","responseNote"], __storedDoc) as Record<string, any>;
    if (!(((checkRole(user, "workforceManageAccess") || ((user.id != null) && (doc.requesterAuthSubjectId === user.id))) || (doc.requesterAuthSubjectId == null)))) throw new Error("Staff may read their own time-off requests; workforce managers may review all requests");
    if (!((checkRole(user, "workforceManageAccess") || ((user.id != null) && ((doc.requesterAuthSubjectId == null) || (doc.requesterAuthSubjectId === user.id)))))) throw new Error("Staff may submit their own time-off requests; workforce managers may review them");
    if (!((checkRole(user, "workforceManageAccess") || ((user.id != null) && ((doc.requesterAuthSubjectId == null) || (doc.requesterAuthSubjectId === user.id)))))) throw new Error("Staff may submit their own time-off requests; workforce managers may review them");
    if (!((doc.status === "pending"))) throw new Error("Guard 0 failed");
    if (!((doc.submittedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Guard 3 failed");
    const previousStatus = doc.status;
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "approved";
        const __allowed: Record<string, string[]> = { "pending": ["approved", "denied"], "approved": [], "denied": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "approved",
      reviewedAt: Date.now(),
      reviewedByAuthSubjectId: user.id,
      responseNote: responseNote,
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "TimeOffRequest", ["reason","responseNote"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, timeOffRequestId: docId, tenantId: __after.tenantId, personId: __after.personId, startsAt: __after.startsAt, endsAt: __after.endsAt, previousStatus: previousStatus, status: "approved", _subject: { entity: "TimeOffRequest", command: "approve", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "TimeOffRequestApproved", entity: "TimeOffRequest", entityId: docId, payload: { timeOffRequestId: docId, tenantId: __after.tenantId, personId: __after.personId, startsAt: __after.startsAt, endsAt: __after.endsAt, previousStatus: previousStatus, status: "approved" }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const TimeOffRequest_approve = mutation({
  args: {
    docId: v.id("timeOffRequests"),
    responseNote: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runTimeOffRequestApprove(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "TimeOffRequest_approve", __result);
    }
    return __result;
  },
});

async function __runTimeOffRequestDecline(ctx: MutationCtx, { docId, responseNote, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("TimeOffRequest not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("TimeOffRequest not found");
    const doc = await __decryptDoc(ctx, "TimeOffRequest", ["reason","responseNote"], __storedDoc) as Record<string, any>;
    if (!(((checkRole(user, "workforceManageAccess") || ((user.id != null) && (doc.requesterAuthSubjectId === user.id))) || (doc.requesterAuthSubjectId == null)))) throw new Error("Staff may read their own time-off requests; workforce managers may review all requests");
    if (!((checkRole(user, "workforceManageAccess") || ((user.id != null) && ((doc.requesterAuthSubjectId == null) || (doc.requesterAuthSubjectId === user.id)))))) throw new Error("Staff may submit their own time-off requests; workforce managers may review them");
    if (!((checkRole(user, "workforceManageAccess") || ((user.id != null) && ((doc.requesterAuthSubjectId == null) || (doc.requesterAuthSubjectId === user.id)))))) throw new Error("Staff may submit their own time-off requests; workforce managers may review them");
    if (!((doc.status === "pending"))) throw new Error("Guard 0 failed");
    if (!((doc.submittedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Guard 3 failed");
    const previousStatus = doc.status;
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "denied";
        const __allowed: Record<string, string[]> = { "pending": ["approved", "denied"], "approved": [], "denied": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "denied",
      reviewedAt: Date.now(),
      reviewedByAuthSubjectId: user.id,
      responseNote: responseNote,
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "TimeOffRequest", ["reason","responseNote"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, timeOffRequestId: docId, tenantId: __after.tenantId, personId: __after.personId, startsAt: __after.startsAt, endsAt: __after.endsAt, previousStatus: previousStatus, status: "denied", _subject: { entity: "TimeOffRequest", command: "decline", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "TimeOffRequestDenied", entity: "TimeOffRequest", entityId: docId, payload: { timeOffRequestId: docId, tenantId: __after.tenantId, personId: __after.personId, startsAt: __after.startsAt, endsAt: __after.endsAt, previousStatus: previousStatus, status: "denied" }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const TimeOffRequest_decline = mutation({
  args: {
    docId: v.id("timeOffRequests"),
    responseNote: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runTimeOffRequestDecline(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "TimeOffRequest_decline", __result);
    }
    return __result;
  },
});

async function __runTimeOffRequestSubmit(ctx: MutationCtx, { docId, personId, startsAt, endsAt, reason, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("TimeOffRequest not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("TimeOffRequest not found");
    const doc = await __decryptDoc(ctx, "TimeOffRequest", ["reason","responseNote"], __storedDoc) as Record<string, any>;
    const __rel_person = await __resolveRelation(ctx, "people", [__auth.tenantId, doc.personId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).personId;
      ((doc as any) as any).person = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!(((checkRole(user, "workforceManageAccess") || ((user.id != null) && (doc.requesterAuthSubjectId === user.id))) || (doc.requesterAuthSubjectId == null)))) throw new Error("Staff may read their own time-off requests; workforce managers may review all requests");
    if (!((checkRole(user, "workforceManageAccess") || ((user.id != null) && ((doc.requesterAuthSubjectId == null) || (doc.requesterAuthSubjectId === user.id)))))) throw new Error("Staff may submit their own time-off requests; workforce managers may review them");
    if (!((checkRole(user, "workforceManageAccess") || ((user.id != null) && ((doc.requesterAuthSubjectId == null) || (doc.requesterAuthSubjectId === user.id)))))) throw new Error("Staff may submit their own time-off requests; workforce managers may review them");
    if (!((doc.submittedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.status === "pending"))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((user.id != null))) throw new Error("Guard 3 failed");
    if (!((((__rel_person != null) && (__rel_person.status === "active")) && (__rel_person.deletedAt == null)))) throw new Error("Guard 4 failed");
    if (!((((__rel_person.authSubjectId != null) && (__rel_person.authSubjectId === user.id)) || checkRole(user, "workforceManageAccess")))) throw new Error("Guard 5 failed");
    if (!((personId === doc.personId))) throw new Error("Time-off person must match the seeded staff profile");
    if (!((endsAt > startsAt))) throw new Error("Time-off end must be after its start");
    if (!((((reason).trim()).length > 0))) throw new Error("Tell your manager why you need the time off");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      personId: personId,
      requesterAuthSubjectId: __rel_person.authSubjectId,
      startsAt: startsAt,
      endsAt: endsAt,
      reason: reason,
      submittedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "TimeOffRequest", ["reason","responseNote"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, timeOffRequestId: docId, tenantId: __after.tenantId, personId: personId, startsAt: startsAt, endsAt: endsAt, status: "pending", _subject: { entity: "TimeOffRequest", command: "submit", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "TimeOffRequestSubmitted", entity: "TimeOffRequest", entityId: docId, payload: { timeOffRequestId: docId, tenantId: __after.tenantId, personId: personId, startsAt: startsAt, endsAt: endsAt, status: "pending" }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const TimeOffRequest_submit = mutation({
  args: {
    docId: v.id("timeOffRequests"),
    personId: v.string(),
    startsAt: v.number(),
    endsAt: v.number(),
    reason: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runTimeOffRequestSubmit(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "TimeOffRequest_submit", __result);
    }
    return __result;
  },
});

export const TimeOffRequest_createViaSubmit = mutation({
  args: {
    personId: v.string(),
    startsAt: v.number(),
    endsAt: v.number(),
    reason: v.string(),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { personId, startsAt, endsAt, reason } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      status: "pending",
      createdAt: Date.now(),
      updatedAt: Date.now(),
      endsAt: args.endsAt,
      personId: args.personId,
      reason: args.reason,
      startsAt: args.startsAt
    };
    const __rel_person = await __resolveRelation(ctx, "people", [__auth.tenantId, __draft.personId], ["tenantId","id"], "tenantId", __auth.tenantId);
    if (!(((checkRole(user, "workforceManageAccess") || ((user.id != null) && (__draft.requesterAuthSubjectId === user.id))) || (__draft.requesterAuthSubjectId == null)))) throw new Error("Staff may read their own time-off requests; workforce managers may review all requests");
    if (!((checkRole(user, "workforceManageAccess") || ((user.id != null) && ((__draft.requesterAuthSubjectId == null) || (__draft.requesterAuthSubjectId === user.id)))))) throw new Error("Staff may submit their own time-off requests; workforce managers may review them");
    if (!((checkRole(user, "workforceManageAccess") || ((user.id != null) && ((__draft.requesterAuthSubjectId == null) || (__draft.requesterAuthSubjectId === user.id)))))) throw new Error("Staff may submit their own time-off requests; workforce managers may review them");
    if (!((__draft.submittedAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.status === "pending"))) throw new Error("Guard 1 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((user.id != null))) throw new Error("Guard 3 failed");
    if (!((((__rel_person != null) && (__rel_person.status === "active")) && (__rel_person.deletedAt == null)))) throw new Error("Guard 4 failed");
    if (!((((__rel_person.authSubjectId != null) && (__rel_person.authSubjectId === user.id)) || checkRole(user, "workforceManageAccess")))) throw new Error("Guard 5 failed");
    if (!((personId === __draft.personId))) throw new Error("Time-off person must match the seeded staff profile");
    if (!((endsAt > startsAt))) throw new Error("Time-off end must be after its start");
    if (!((((reason).trim()).length > 0))) throw new Error("Tell your manager why you need the time off");
    const doc: Record<string, any> = {
      ...__draft,
      personId: personId,
      requesterAuthSubjectId: __rel_person.authSubjectId,
      startsAt: startsAt,
      endsAt: endsAt,
      reason: reason,
      submittedAt: Date.now(),
      version: 1,
    };
    const __storedDoc = await __encryptDoc(ctx, "TimeOffRequest", ["reason","responseNote"], doc);
    const docId = await ctx.db.insert("timeOffRequests", __storedDoc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, timeOffRequestId: docId, tenantId: doc.tenantId, personId: doc.personId, startsAt: doc.startsAt, endsAt: doc.endsAt, status: "pending", _subject: { entity: "TimeOffRequest", command: "submit", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "TimeOffRequestSubmitted", entity: "TimeOffRequest", entityId: docId, payload: { timeOffRequestId: docId, tenantId: doc.tenantId, personId: doc.personId, startsAt: doc.startsAt, endsAt: doc.endsAt, status: "pending" }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "TimeOffRequest_createViaSubmit", __result);
    }
    return __result;
  },
});

async function __runTimeRecordClockIn(ctx: MutationCtx, { docId, personId, shiftId, eventId, notes, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("TimeRecord not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("TimeRecord not found");
    const doc = await __decryptDoc(ctx, "TimeRecord", ["notes"], __storedDoc) as Record<string, any>;
    const __rel_person = await __resolveRelation(ctx, "people", [__auth.tenantId, doc.personId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).personId;
      ((doc as any) as any).person = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may read time records");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may write time records through commands");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may execute time record commands");
    if (!((doc.clockInAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.status === "open"))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((user.id != null))) throw new Error("Guard 3 failed");
    if (!(((personId === user.id) || checkRole(user, "workforceManageAccess")))) throw new Error("Guard 4 failed");
    if (!(((__rel_person != null) && (__rel_person.status === "active")))) throw new Error("Guard 5 failed");
    if (!((personId === doc.personId))) throw new Error("Clock-in personId must match the seeded person reference");
    if (!((((shiftId == null) || (doc.shiftId == null)) || (shiftId === doc.shiftId)))) throw new Error("Clock-in shiftId must match the seeded shift reference when provided");
    if (!((((eventId == null) || (doc.eventId == null)) || (eventId === doc.eventId)))) throw new Error("Clock-in eventId must match the seeded event reference when provided");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      personId: personId,
      shiftId: ((shiftId != null) ? shiftId : doc.shiftId),
      eventId: ((eventId != null) ? eventId : doc.eventId),
      notes: ((notes != null) ? notes : doc.notes),
      clockInAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "TimeRecord", ["notes"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, timeRecordId: docId, tenantId: __after.tenantId, personId: personId, shiftId: ((shiftId != null) ? shiftId : __after.shiftId), eventId: ((eventId != null) ? eventId : __after.eventId), clockInAt: __after.clockInAt, status: "open", _subject: { entity: "TimeRecord", command: "clockIn", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "TimeRecordClockedIn", entity: "TimeRecord", entityId: docId, payload: { timeRecordId: docId, tenantId: __after.tenantId, personId: personId, shiftId: ((shiftId != null) ? shiftId : __after.shiftId), eventId: ((eventId != null) ? eventId : __after.eventId), clockInAt: __after.clockInAt, status: "open" }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const TimeRecord_clockIn = mutation({
  args: {
    docId: v.id("timeRecords"),
    personId: v.string(),
    shiftId: v.optional(v.string()),
    eventId: v.optional(v.string()),
    notes: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runTimeRecordClockIn(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "TimeRecord_clockIn", __result);
    }
    return __result;
  },
});

export const TimeRecord_createViaClockIn = mutation({
  args: {
    personId: v.string(),
    shiftId: v.optional(v.string()),
    eventId: v.optional(v.string()),
    notes: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { personId, shiftId, eventId, notes } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      breakMinutes: 0,
      status: "open",
      createdAt: Date.now(),
      updatedAt: Date.now(),
      eventId: args.eventId,
      notes: args.notes,
      personId: args.personId,
      shiftId: args.shiftId
    };
    const __rel_person = await __resolveRelation(ctx, "people", [__auth.tenantId, __draft.personId], ["tenantId","id"], "tenantId", __auth.tenantId);
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may read time records");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may write time records through commands");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may execute time record commands");
    if (!((__draft.clockInAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.status === "open"))) throw new Error("Guard 1 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((user.id != null))) throw new Error("Guard 3 failed");
    if (!(((personId === user.id) || checkRole(user, "workforceManageAccess")))) throw new Error("Guard 4 failed");
    if (!(((__rel_person != null) && (__rel_person.status === "active")))) throw new Error("Guard 5 failed");
    if (!((personId === __draft.personId))) throw new Error("Clock-in personId must match the seeded person reference");
    if (!((((shiftId == null) || (__draft.shiftId == null)) || (shiftId === __draft.shiftId)))) throw new Error("Clock-in shiftId must match the seeded shift reference when provided");
    if (!((((eventId == null) || (__draft.eventId == null)) || (eventId === __draft.eventId)))) throw new Error("Clock-in eventId must match the seeded event reference when provided");
    const doc: Record<string, any> = {
      ...__draft,
      personId: personId,
      shiftId: ((shiftId != null) ? shiftId : __draft.shiftId),
      eventId: ((eventId != null) ? eventId : __draft.eventId),
      notes: ((notes != null) ? notes : __draft.notes),
      clockInAt: Date.now(),
      version: 1,
    };
    const __storedDoc = await __encryptDoc(ctx, "TimeRecord", ["notes"], doc);
    const docId = await ctx.db.insert("timeRecords", __storedDoc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, timeRecordId: docId, tenantId: doc.tenantId, personId: doc.personId, shiftId: ((doc.shiftId != null) ? doc.shiftId : doc.shiftId), eventId: ((doc.eventId != null) ? doc.eventId : doc.eventId), clockInAt: doc.clockInAt, status: "open", _subject: { entity: "TimeRecord", command: "clockIn", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "TimeRecordClockedIn", entity: "TimeRecord", entityId: docId, payload: { timeRecordId: docId, tenantId: doc.tenantId, personId: doc.personId, shiftId: ((doc.shiftId != null) ? doc.shiftId : doc.shiftId), eventId: ((doc.eventId != null) ? doc.eventId : doc.eventId), clockInAt: doc.clockInAt, status: "open" }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "TimeRecord_createViaClockIn", __result);
    }
    return __result;
  },
});

async function __runTimeRecordClockOut(ctx: MutationCtx, { docId, breakMinutes, notes, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("TimeRecord not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("TimeRecord not found");
    const doc = await __decryptDoc(ctx, "TimeRecord", ["notes"], __storedDoc) as Record<string, any>;
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may read time records");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may write time records through commands");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may execute time record commands");
    if (!((doc.status === "open"))) throw new Error("Guard 0 failed");
    if (!((doc.clockInAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.clockOutAt == null))) throw new Error("Guard 2 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 3 failed");
    if (!((user.id != null))) throw new Error("Guard 4 failed");
    if (!(((doc.personId === user.id) || checkRole(user, "workforceManageAccess")))) throw new Error("Guard 5 failed");
    if (!(((breakMinutes == null) || (breakMinutes >= 0)))) throw new Error("Break minutes must be non-negative");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "closed";
        const __allowed: Record<string, string[]> = { "open": ["closed"], "closed": ["corrected"], "corrected": ["corrected"] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      clockOutAt: Date.now(),
      breakMinutes: ((breakMinutes != null) ? breakMinutes : doc.breakMinutes),
      notes: ((notes != null) ? notes : doc.notes),
      status: "closed",
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "TimeRecord", ["notes"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, timeRecordId: docId, tenantId: __after.tenantId, personId: __after.personId, shiftId: __after.shiftId, eventId: __after.eventId, clockInAt: __after.clockInAt, clockOutAt: __after.clockOutAt, breakMinutes: ((breakMinutes != null) ? breakMinutes : __after.breakMinutes), status: "closed", _subject: { entity: "TimeRecord", command: "clockOut", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "TimeRecordClockedOut", entity: "TimeRecord", entityId: docId, payload: { timeRecordId: docId, tenantId: __after.tenantId, personId: __after.personId, shiftId: __after.shiftId, eventId: __after.eventId, clockInAt: __after.clockInAt, clockOutAt: __after.clockOutAt, breakMinutes: ((breakMinutes != null) ? breakMinutes : __after.breakMinutes), status: "closed" }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const TimeRecord_clockOut = mutation({
  args: {
    docId: v.id("timeRecords"),
    breakMinutes: v.optional(v.any()),
    notes: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runTimeRecordClockOut(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "TimeRecord_clockOut", __result);
    }
    return __result;
  },
});

async function __runTimeRecordCorrect(ctx: MutationCtx, { docId, clockInAt, clockOutAt, breakMinutes, notes, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("TimeRecord not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("TimeRecord not found");
    const doc = await __decryptDoc(ctx, "TimeRecord", ["notes"], __storedDoc) as Record<string, any>;
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may read time records");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may write time records through commands");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may execute time record commands");
    if (!(((doc.status === "closed") || (doc.status === "corrected")))) throw new Error("Guard 0 failed");
    if (!((doc.clockInAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.clockOutAt != null))) throw new Error("Guard 2 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 3 failed");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Guard 4 failed");
    if (!((clockOutAt >= clockInAt))) throw new Error("Corrected clock-out must be at or after clock-in");
    if (!(((breakMinutes == null) || (breakMinutes >= 0)))) throw new Error("Break minutes must be non-negative");
    const previousClockInAt = doc.clockInAt;
    const previousClockOutAt = doc.clockOutAt;
    const previousBreakMinutes = doc.breakMinutes;
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "corrected";
        const __allowed: Record<string, string[]> = { "open": ["closed"], "closed": ["corrected"], "corrected": ["corrected"] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      clockInAt: clockInAt,
      clockOutAt: clockOutAt,
      breakMinutes: ((breakMinutes != null) ? breakMinutes : doc.breakMinutes),
      notes: ((notes != null) ? notes : doc.notes),
      status: "corrected",
      correctedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "TimeRecord", ["notes"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, timeRecordId: docId, tenantId: __after.tenantId, personId: __after.personId, previousClockInAt: previousClockInAt, previousClockOutAt: previousClockOutAt, previousBreakMinutes: previousBreakMinutes, clockInAt: clockInAt, clockOutAt: clockOutAt, breakMinutes: ((breakMinutes != null) ? breakMinutes : __after.breakMinutes), status: "corrected", _subject: { entity: "TimeRecord", command: "correct", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "TimeRecordCorrected", entity: "TimeRecord", entityId: docId, payload: { timeRecordId: docId, tenantId: __after.tenantId, personId: __after.personId, previousClockInAt: previousClockInAt, previousClockOutAt: previousClockOutAt, previousBreakMinutes: previousBreakMinutes, clockInAt: clockInAt, clockOutAt: clockOutAt, breakMinutes: ((breakMinutes != null) ? breakMinutes : __after.breakMinutes), status: "corrected" }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const TimeRecord_correct = mutation({
  args: {
    docId: v.id("timeRecords"),
    clockInAt: v.number(),
    clockOutAt: v.number(),
    breakMinutes: v.optional(v.any()),
    notes: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runTimeRecordCorrect(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "TimeRecord_correct", __result);
    }
    return __result;
  },
});

async function __runTrainingCompletionRecord(ctx: MutationCtx, { docId, personId, trainingModuleId, completedAt, assessmentScore, notes, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("TrainingCompletion not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("TrainingCompletion not found");
    const doc = await __decryptDoc(ctx, "TrainingCompletion", ["notes"], __storedDoc) as Record<string, any>;
    const __rel_person = await __resolveRelation(ctx, "people", [__auth.tenantId, doc.personId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_trainingModule = await __resolveRelation(ctx, "trainingModules", [__auth.tenantId, doc.trainingModuleId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).personId;
      ((doc as any) as any).person = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    {
      const __fk = ((doc as any) as any).trainingModuleId;
      ((doc as any) as any).trainingModule = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may read training completions");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may write training completions through commands");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may execute training completion commands");
    if (!((doc.recordedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(((__rel_person != null) && (__rel_person.status === "active")))) throw new Error("Guard 2 failed");
    if (!((((__rel_trainingModule != null) && (__rel_trainingModule.status === "active")) && (__rel_trainingModule.deletedAt == null)))) throw new Error("Guard 3 failed");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Guard 4 failed");
    if (!((personId === doc.personId))) throw new Error("Completion person must match the selected staff member");
    if (!((trainingModuleId === doc.trainingModuleId))) throw new Error("Completion module must match the selected training module");
    if (!(((assessmentScore >= 0) && (assessmentScore <= 100)))) throw new Error("Assessment score must be between 0 and 100");
    if (!((assessmentScore >= __rel_trainingModule.passingScore))) throw new Error("Assessment score must meet the module passing score");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      personId: personId,
      trainingModuleId: trainingModuleId,
      completedAt: completedAt,
      assessmentScore: assessmentScore,
      notes: notes,
      recordedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "TrainingCompletion", ["notes"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, trainingCompletionId: docId, tenantId: __after.tenantId, personId: personId, trainingModuleId: trainingModuleId, completedAt: completedAt, assessmentScore: assessmentScore, _subject: { entity: "TrainingCompletion", command: "record", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "TrainingCompleted", entity: "TrainingCompletion", entityId: docId, payload: { trainingCompletionId: docId, tenantId: __after.tenantId, personId: personId, trainingModuleId: trainingModuleId, completedAt: completedAt, assessmentScore: assessmentScore }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const TrainingCompletion_record = mutation({
  args: {
    docId: v.id("trainingCompletions"),
    personId: v.string(),
    trainingModuleId: v.string(),
    completedAt: v.number(),
    assessmentScore: v.number(),
    notes: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runTrainingCompletionRecord(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "TrainingCompletion_record", __result);
    }
    return __result;
  },
});

export const TrainingCompletion_createViaRecord = mutation({
  args: {
    personId: v.string(),
    trainingModuleId: v.string(),
    completedAt: v.number(),
    assessmentScore: v.number(),
    notes: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { personId, trainingModuleId, completedAt, assessmentScore, notes } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      createdAt: Date.now(),
      updatedAt: Date.now(),
      assessmentScore: args.assessmentScore,
      completedAt: args.completedAt,
      notes: args.notes,
      personId: args.personId,
      trainingModuleId: args.trainingModuleId
    };
    const __rel_person = await __resolveRelation(ctx, "people", [__auth.tenantId, __draft.personId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_trainingModule = await __resolveRelation(ctx, "trainingModules", [__auth.tenantId, __draft.trainingModuleId], ["tenantId","id"], "tenantId", __auth.tenantId);
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may read training completions");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may write training completions through commands");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may execute training completion commands");
    if (!((__draft.recordedAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(((__rel_person != null) && (__rel_person.status === "active")))) throw new Error("Guard 2 failed");
    if (!((((__rel_trainingModule != null) && (__rel_trainingModule.status === "active")) && (__rel_trainingModule.deletedAt == null)))) throw new Error("Guard 3 failed");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Guard 4 failed");
    if (!((personId === __draft.personId))) throw new Error("Completion person must match the selected staff member");
    if (!((trainingModuleId === __draft.trainingModuleId))) throw new Error("Completion module must match the selected training module");
    if (!(((assessmentScore >= 0) && (assessmentScore <= 100)))) throw new Error("Assessment score must be between 0 and 100");
    if (!((assessmentScore >= __rel_trainingModule.passingScore))) throw new Error("Assessment score must meet the module passing score");
    const doc: Record<string, any> = {
      ...__draft,
      personId: personId,
      trainingModuleId: trainingModuleId,
      completedAt: completedAt,
      assessmentScore: assessmentScore,
      notes: notes,
      recordedAt: Date.now(),
      version: 1,
    };
    const __storedDoc = await __encryptDoc(ctx, "TrainingCompletion", ["notes"], doc);
    const docId = await ctx.db.insert("trainingCompletions", __storedDoc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, trainingCompletionId: docId, tenantId: doc.tenantId, personId: doc.personId, trainingModuleId: doc.trainingModuleId, completedAt: doc.completedAt, assessmentScore: doc.assessmentScore, _subject: { entity: "TrainingCompletion", command: "record", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "TrainingCompleted", entity: "TrainingCompletion", entityId: docId, payload: { trainingCompletionId: docId, tenantId: doc.tenantId, personId: doc.personId, trainingModuleId: doc.trainingModuleId, completedAt: doc.completedAt, assessmentScore: doc.assessmentScore }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "TrainingCompletion_createViaRecord", __result);
    }
    return __result;
  },
});

async function __runTrainingModuleDefine(ctx: MutationCtx, { docId, name, category, passingScore, description, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("TrainingModule not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("TrainingModule not found");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may read training modules");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may write training modules through commands");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may execute training module commands");
    if (!((doc.definedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Guard 2 failed");
    if (!((((name).trim()).length > 0))) throw new Error("Training module name is required");
    if (!(((passingScore >= 0) && (passingScore <= 100)))) throw new Error("Passing score must be between 0 and 100");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "active";
        const __allowed: Record<string, string[]> = { "active": ["retired"], "retired": ["active"] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      name: name,
      category: category,
      passingScore: passingScore,
      description: description,
      status: "active",
      definedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, trainingModuleId: docId, tenantId: __after.tenantId, name: name, category: category, passingScore: passingScore, _subject: { entity: "TrainingModule", command: "define", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "TrainingModuleDefined", entity: "TrainingModule", entityId: docId, payload: { trainingModuleId: docId, tenantId: __after.tenantId, name: name, category: category, passingScore: passingScore }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const TrainingModule_define = mutation({
  args: {
    docId: v.id("trainingModules"),
    name: v.string(),
    category: v.any(),
    passingScore: v.number(),
    description: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runTrainingModuleDefine(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "TrainingModule_define", __result);
    }
    return __result;
  },
});

export const TrainingModule_createViaDefine = mutation({
  args: {
    name: v.string(),
    category: v.any(),
    passingScore: v.number(),
    description: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { name, category, passingScore, description } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      createdAt: Date.now(),
      updatedAt: Date.now(),
      status: "active",
      category: args.category,
      description: args.description,
      name: args.name,
      passingScore: args.passingScore
    };
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may read training modules");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may write training modules through commands");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may execute training module commands");
    if (!((__draft.definedAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Guard 2 failed");
    if (!((((name).trim()).length > 0))) throw new Error("Training module name is required");
    if (!(((passingScore >= 0) && (passingScore <= 100)))) throw new Error("Passing score must be between 0 and 100");
    const doc: Record<string, any> = {
      ...__draft,
      name: name,
      category: category,
      passingScore: passingScore,
      description: description,
      status: "active",
      definedAt: Date.now(),
      version: 1,
    };
    const docId = await ctx.db.insert("trainingModules", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, trainingModuleId: docId, tenantId: doc.tenantId, name: doc.name, category: doc.category, passingScore: doc.passingScore, _subject: { entity: "TrainingModule", command: "define", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "TrainingModuleDefined", entity: "TrainingModule", entityId: docId, payload: { trainingModuleId: docId, tenantId: doc.tenantId, name: doc.name, category: doc.category, passingScore: doc.passingScore }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "TrainingModule_createViaDefine", __result);
    }
    return __result;
  },
});

async function __runTrainingModuleReactivate(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("TrainingModule not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("TrainingModule not found");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may read training modules");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may write training modules through commands");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may execute training module commands");
    if (!((doc.status === "retired"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Guard 2 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "active";
        const __allowed: Record<string, string[]> = { "active": ["retired"], "retired": ["active"] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "active",
      retiredAt: null,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, trainingModuleId: docId, tenantId: __after.tenantId, name: __after.name, _subject: { entity: "TrainingModule", command: "reactivate", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "TrainingModuleReactivated", entity: "TrainingModule", entityId: docId, payload: { trainingModuleId: docId, tenantId: __after.tenantId, name: __after.name }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const TrainingModule_reactivate = mutation({
  args: {
    docId: v.id("trainingModules"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runTrainingModuleReactivate(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "TrainingModule_reactivate", __result);
    }
    return __result;
  },
});

async function __runTrainingModuleRetire(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("TrainingModule not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("TrainingModule not found");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may read training modules");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may write training modules through commands");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may execute training module commands");
    if (!((doc.status === "active"))) throw new Error("Guard 0 failed");
    if (!((doc.definedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Guard 3 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "retired";
        const __allowed: Record<string, string[]> = { "active": ["retired"], "retired": ["active"] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "retired",
      retiredAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, trainingModuleId: docId, tenantId: __after.tenantId, name: __after.name, _subject: { entity: "TrainingModule", command: "retire", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "TrainingModuleRetired", entity: "TrainingModule", entityId: docId, payload: { trainingModuleId: docId, tenantId: __after.tenantId, name: __after.name }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const TrainingModule_retire = mutation({
  args: {
    docId: v.id("trainingModules"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runTrainingModuleRetire(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "TrainingModule_retire", __result);
    }
    return __result;
  },
});

async function __runVehicleRegister(ctx: MutationCtx, { docId, make, model, registration, ownership, payloadCapacityKg, operationalStatus, statusNote, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Vehicle not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Vehicle not found");
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may read vehicles");
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may write vehicles through commands");
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may execute vehicle commands");
    if (!((doc.registeredAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((((make).trim()).length > 0))) throw new Error("Vehicle make is required");
    if (!((((model).trim()).length > 0))) throw new Error("Vehicle model is required");
    if (!((((registration).trim()).length > 0))) throw new Error("Vehicle registration is required");
    if (!((payloadCapacityKg > 0))) throw new Error("Vehicle payload capacity must be positive");
    {
      const __cur = doc.operationalStatus;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = String(operationalStatus);
        const __allowed: Record<string, string[]> = { "available": ["in_use", "maintenance", "out_of_service", "retired"], "in_use": ["available", "maintenance", "out_of_service", "retired"], "maintenance": ["available", "in_use", "out_of_service", "retired"], "out_of_service": ["available", "in_use", "maintenance", "retired"], "retired": ["available", "in_use", "maintenance", "out_of_service"] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'operationalStatus'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      make: make,
      model: model,
      registration: registration,
      ownership: ownership,
      payloadCapacityKg: payloadCapacityKg,
      operationalStatus: operationalStatus,
      statusNote: statusNote,
      registeredAt: Date.now(),
      statusChangedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, vehicleId: docId, tenantId: __after.tenantId, make: make, model: model, registration: registration, ownership: ownership, payloadCapacityKg: payloadCapacityKg, operationalStatus: operationalStatus, _subject: { entity: "Vehicle", command: "register", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "VehicleRegistered", entity: "Vehicle", entityId: docId, payload: { vehicleId: docId, tenantId: __after.tenantId, make: make, model: model, registration: registration, ownership: ownership, payloadCapacityKg: payloadCapacityKg, operationalStatus: operationalStatus }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Vehicle_register = mutation({
  args: {
    docId: v.id("vehicles"),
    make: v.string(),
    model: v.string(),
    registration: v.string(),
    ownership: v.any(),
    payloadCapacityKg: v.number(),
    operationalStatus: v.any(),
    statusNote: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runVehicleRegister(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Vehicle_register", __result);
    }
    return __result;
  },
});

export const Vehicle_createViaRegister = mutation({
  args: {
    make: v.string(),
    model: v.string(),
    registration: v.string(),
    ownership: v.any(),
    payloadCapacityKg: v.number(),
    operationalStatus: v.any(),
    statusNote: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { make, model, registration, ownership, payloadCapacityKg, operationalStatus, statusNote } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      createdAt: Date.now(),
      updatedAt: Date.now(),
      operationalStatus: args.operationalStatus !== undefined ? args.operationalStatus : "available",
      make: args.make,
      model: args.model,
      ownership: args.ownership,
      payloadCapacityKg: args.payloadCapacityKg,
      registration: args.registration,
      statusNote: args.statusNote
    };
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may read vehicles");
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may write vehicles through commands");
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may execute vehicle commands");
    if (!((__draft.registeredAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((((make).trim()).length > 0))) throw new Error("Vehicle make is required");
    if (!((((model).trim()).length > 0))) throw new Error("Vehicle model is required");
    if (!((((registration).trim()).length > 0))) throw new Error("Vehicle registration is required");
    if (!((payloadCapacityKg > 0))) throw new Error("Vehicle payload capacity must be positive");
    const doc: Record<string, any> = {
      ...__draft,
      make: make,
      model: model,
      registration: registration,
      ownership: ownership,
      payloadCapacityKg: payloadCapacityKg,
      operationalStatus: operationalStatus,
      statusNote: statusNote,
      registeredAt: Date.now(),
      statusChangedAt: Date.now(),
      version: 1,
    };
    const docId = await ctx.db.insert("vehicles", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, vehicleId: docId, tenantId: doc.tenantId, make: doc.make, model: doc.model, registration: doc.registration, ownership: doc.ownership, payloadCapacityKg: doc.payloadCapacityKg, operationalStatus: doc.operationalStatus, _subject: { entity: "Vehicle", command: "register", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "VehicleRegistered", entity: "Vehicle", entityId: docId, payload: { vehicleId: docId, tenantId: doc.tenantId, make: doc.make, model: doc.model, registration: doc.registration, ownership: doc.ownership, payloadCapacityKg: doc.payloadCapacityKg, operationalStatus: doc.operationalStatus }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Vehicle_createViaRegister", __result);
    }
    return __result;
  },
});

async function __runVehicleReviseDetails(ctx: MutationCtx, { docId, make, model, registration, ownership, payloadCapacityKg, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Vehicle not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Vehicle not found");
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may read vehicles");
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may write vehicles through commands");
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may execute vehicle commands");
    if (!((doc.registeredAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((((make).trim()).length > 0))) throw new Error("Vehicle make is required");
    if (!((((model).trim()).length > 0))) throw new Error("Vehicle model is required");
    if (!((((registration).trim()).length > 0))) throw new Error("Vehicle registration is required");
    if (!((payloadCapacityKg > 0))) throw new Error("Vehicle payload capacity must be positive");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      make: make,
      model: model,
      registration: registration,
      ownership: ownership,
      payloadCapacityKg: payloadCapacityKg,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, vehicleId: docId, tenantId: __after.tenantId, make: make, model: model, registration: registration, ownership: ownership, payloadCapacityKg: payloadCapacityKg, _subject: { entity: "Vehicle", command: "reviseDetails", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "VehicleDetailsRevised", entity: "Vehicle", entityId: docId, payload: { vehicleId: docId, tenantId: __after.tenantId, make: make, model: model, registration: registration, ownership: ownership, payloadCapacityKg: payloadCapacityKg }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Vehicle_reviseDetails = mutation({
  args: {
    docId: v.id("vehicles"),
    make: v.string(),
    model: v.string(),
    registration: v.string(),
    ownership: v.any(),
    payloadCapacityKg: v.number(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runVehicleReviseDetails(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Vehicle_reviseDetails", __result);
    }
    return __result;
  },
});

async function __runVehicleUpdateOperationalStatus(ctx: MutationCtx, { docId, operationalStatus, statusNote, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("Vehicle not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("Vehicle not found");
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may read vehicles");
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may write vehicles through commands");
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may execute vehicle commands");
    if (!((doc.registeredAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    const previousStatus = doc.operationalStatus;
    {
      const __cur = doc.operationalStatus;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = String(operationalStatus);
        const __allowed: Record<string, string[]> = { "available": ["in_use", "maintenance", "out_of_service", "retired"], "in_use": ["available", "maintenance", "out_of_service", "retired"], "maintenance": ["available", "in_use", "out_of_service", "retired"], "out_of_service": ["available", "in_use", "maintenance", "retired"], "retired": ["available", "in_use", "maintenance", "out_of_service"] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'operationalStatus'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      operationalStatus: operationalStatus,
      statusNote: statusNote,
      statusChangedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, vehicleId: docId, tenantId: __after.tenantId, previousStatus: previousStatus, operationalStatus: operationalStatus, statusNote: statusNote, statusChangedAt: __after.statusChangedAt, _subject: { entity: "Vehicle", command: "updateOperationalStatus", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "VehicleOperationalStatusUpdated", entity: "Vehicle", entityId: docId, payload: { vehicleId: docId, tenantId: __after.tenantId, previousStatus: previousStatus, operationalStatus: operationalStatus, statusNote: statusNote, statusChangedAt: __after.statusChangedAt }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Vehicle_updateOperationalStatus = mutation({
  args: {
    docId: v.id("vehicles"),
    operationalStatus: v.any(),
    statusNote: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runVehicleUpdateOperationalStatus(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Vehicle_updateOperationalStatus", __result);
    }
    return __result;
  },
});

async function __runVehicleFuelLogRecord(ctx: MutationCtx, { docId, vehicleId, odometer, fuelCost, filledAt, notes, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("VehicleFuelLog not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("VehicleFuelLog not found");
    const __rel_vehicle = await __resolveRelation(ctx, "vehicles", [__auth.tenantId, doc.vehicleId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).vehicleId;
      ((doc as any) as any).vehicle = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may read fuel logs");
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may record fuel logs");
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may execute fuel log commands");
    if (!((doc.loggedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((__rel_vehicle != null))) throw new Error("Guard 2 failed");
    if (!((vehicleId === doc.vehicleId))) throw new Error("Fuel log vehicle must match the selected vehicle");
    if (!((odometer >= 0))) throw new Error("Odometer reading cannot be negative");
    if (!((fuelCost >= 0))) throw new Error("Fuel cost cannot be negative");
    if (!((filledAt <= Date.now()))) throw new Error("Fuel fill time cannot be in the future");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      odometer: odometer,
      fuelCost: fuelCost,
      filledAt: filledAt,
      notes: notes,
      loggedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, fuelLogId: docId, vehicleId: vehicleId, tenantId: __after.tenantId, odometer: odometer, fuelCost: fuelCost, filledAt: filledAt, _subject: { entity: "VehicleFuelLog", command: "record", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "VehicleFuelLogged", entity: "VehicleFuelLog", entityId: docId, payload: { fuelLogId: docId, vehicleId: vehicleId, tenantId: __after.tenantId, odometer: odometer, fuelCost: fuelCost, filledAt: filledAt }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const VehicleFuelLog_record = mutation({
  args: {
    docId: v.id("vehicleFuelLogs"),
    vehicleId: v.string(),
    odometer: v.number(),
    fuelCost: v.number(),
    filledAt: v.number(),
    notes: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runVehicleFuelLogRecord(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "VehicleFuelLog_record", __result);
    }
    return __result;
  },
});

export const VehicleFuelLog_createViaRecord = mutation({
  args: {
    vehicleId: v.string(),
    odometer: v.number(),
    fuelCost: v.number(),
    filledAt: v.number(),
    notes: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { vehicleId, odometer, fuelCost, filledAt, notes } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      createdAt: Date.now(),
      updatedAt: Date.now(),
      filledAt: args.filledAt,
      fuelCost: args.fuelCost,
      notes: args.notes,
      odometer: args.odometer,
      vehicleId: args.vehicleId
    };
    const __rel_vehicle = await __resolveRelation(ctx, "vehicles", [__auth.tenantId, __draft.vehicleId], ["tenantId","id"], "tenantId", __auth.tenantId);
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may read fuel logs");
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may record fuel logs");
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may execute fuel log commands");
    if (!((__draft.loggedAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((__rel_vehicle != null))) throw new Error("Guard 2 failed");
    if (!((vehicleId === __draft.vehicleId))) throw new Error("Fuel log vehicle must match the selected vehicle");
    if (!((odometer >= 0))) throw new Error("Odometer reading cannot be negative");
    if (!((fuelCost >= 0))) throw new Error("Fuel cost cannot be negative");
    if (!((filledAt <= Date.now()))) throw new Error("Fuel fill time cannot be in the future");
    const doc: Record<string, any> = {
      ...__draft,
      odometer: odometer,
      fuelCost: fuelCost,
      filledAt: filledAt,
      notes: notes,
      loggedAt: Date.now(),
      version: 1,
    };
    const docId = await ctx.db.insert("vehicleFuelLogs", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, fuelLogId: docId, vehicleId: doc.vehicleId, tenantId: doc.tenantId, odometer: doc.odometer, fuelCost: doc.fuelCost, filledAt: doc.filledAt, _subject: { entity: "VehicleFuelLog", command: "record", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "VehicleFuelLogged", entity: "VehicleFuelLog", entityId: docId, payload: { fuelLogId: docId, vehicleId: doc.vehicleId, tenantId: doc.tenantId, odometer: doc.odometer, fuelCost: doc.fuelCost, filledAt: doc.filledAt }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "VehicleFuelLog_createViaRecord", __result);
    }
    return __result;
  },
});

async function __runVehicleMaintenanceScheduleApplyService(ctx: MutationCtx, { docId, completedAt, odometer, nextDueAt, nextDueMileage, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("VehicleMaintenanceSchedule not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("VehicleMaintenanceSchedule not found");
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may read vehicle maintenance");
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may update vehicle maintenance");
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may execute vehicle maintenance commands");
    if (!((doc.scheduledAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    const isLatestService = ((doc.lastServicedAt == null) || (completedAt >= doc.lastServicedAt));
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      lastServicedAt: (isLatestService ? completedAt : doc.lastServicedAt),
      lastServicedMileage: (isLatestService ? odometer : doc.lastServicedMileage),
      nextDueAt: (isLatestService ? nextDueAt : doc.nextDueAt),
      nextDueMileage: (isLatestService ? nextDueMileage : doc.nextDueMileage),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, maintenanceScheduleId: docId, vehicleId: __after.vehicleId, tenantId: __after.tenantId, completedAt: completedAt, nextDueAt: (isLatestService ? nextDueAt : __after.nextDueAt), nextDueMileage: (isLatestService ? nextDueMileage : __after.nextDueMileage), _subject: { entity: "VehicleMaintenanceSchedule", command: "applyService", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "VehicleMaintenanceAdvanced", entity: "VehicleMaintenanceSchedule", entityId: docId, payload: { maintenanceScheduleId: docId, vehicleId: __after.vehicleId, tenantId: __after.tenantId, completedAt: completedAt, nextDueAt: (isLatestService ? nextDueAt : __after.nextDueAt), nextDueMileage: (isLatestService ? nextDueMileage : __after.nextDueMileage) }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const VehicleMaintenanceSchedule_applyService = mutation({
  args: {
    docId: v.id("vehicleMaintenanceSchedules"),
    completedAt: v.number(),
    odometer: v.number(),
    nextDueAt: v.optional(v.number()),
    nextDueMileage: v.optional(v.number()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runVehicleMaintenanceScheduleApplyService(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "VehicleMaintenanceSchedule_applyService", __result);
    }
    return __result;
  },
});

async function __runVehicleMaintenanceScheduleSchedule(ctx: MutationCtx, { docId, vehicleId, taskName, intervalType, intervalDays, intervalMiles, nextDueAt, nextDueMileage, instructions, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("VehicleMaintenanceSchedule not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("VehicleMaintenanceSchedule not found");
    const __rel_vehicle = await __resolveRelation(ctx, "vehicles", [__auth.tenantId, doc.vehicleId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).vehicleId;
      ((doc as any) as any).vehicle = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may read vehicle maintenance");
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may update vehicle maintenance");
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may execute vehicle maintenance commands");
    if (!((doc.scheduledAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((__rel_vehicle != null))) throw new Error("Guard 2 failed");
    if (!((vehicleId === doc.vehicleId))) throw new Error("Maintenance vehicle must match the selected vehicle");
    if (!((((taskName).trim()).length > 0))) throw new Error("Maintenance task is required");
    if (!(((((intervalType === "time") && (nextDueAt != null)) && ((intervalDays == null) || (intervalDays > 0))) || (((intervalType === "mileage") && (nextDueMileage != null)) && ((intervalMiles == null) || (intervalMiles > 0)))))) throw new Error("Time schedules need a first due date; mileage schedules need a due odometer");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      taskName: taskName,
      intervalType: intervalType,
      intervalDays: ((intervalDays != null) ? intervalDays : 90),
      intervalMiles: ((intervalMiles != null) ? intervalMiles : 5000),
      nextDueAt: nextDueAt,
      nextDueMileage: nextDueMileage,
      instructions: instructions,
      scheduledAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, maintenanceScheduleId: docId, vehicleId: vehicleId, tenantId: __after.tenantId, taskName: taskName, intervalType: intervalType, nextDueAt: nextDueAt, nextDueMileage: nextDueMileage, _subject: { entity: "VehicleMaintenanceSchedule", command: "schedule", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "VehicleMaintenanceScheduled", entity: "VehicleMaintenanceSchedule", entityId: docId, payload: { maintenanceScheduleId: docId, vehicleId: vehicleId, tenantId: __after.tenantId, taskName: taskName, intervalType: intervalType, nextDueAt: nextDueAt, nextDueMileage: nextDueMileage }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const VehicleMaintenanceSchedule_schedule = mutation({
  args: {
    docId: v.id("vehicleMaintenanceSchedules"),
    vehicleId: v.string(),
    taskName: v.string(),
    intervalType: v.any(),
    intervalDays: v.optional(v.number()),
    intervalMiles: v.optional(v.number()),
    nextDueAt: v.optional(v.number()),
    nextDueMileage: v.optional(v.number()),
    instructions: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runVehicleMaintenanceScheduleSchedule(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "VehicleMaintenanceSchedule_schedule", __result);
    }
    return __result;
  },
});

export const VehicleMaintenanceSchedule_createViaSchedule = mutation({
  args: {
    vehicleId: v.string(),
    taskName: v.string(),
    intervalType: v.any(),
    intervalDays: v.optional(v.number()),
    intervalMiles: v.optional(v.number()),
    nextDueAt: v.optional(v.number()),
    nextDueMileage: v.optional(v.number()),
    instructions: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { vehicleId, taskName, intervalType, intervalDays, intervalMiles, nextDueAt, nextDueMileage, instructions } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      intervalDays: args.intervalDays !== undefined ? args.intervalDays : 90,
      intervalMiles: args.intervalMiles !== undefined ? args.intervalMiles : 5000,
      createdAt: Date.now(),
      updatedAt: Date.now(),
      instructions: args.instructions,
      intervalType: args.intervalType,
      nextDueAt: args.nextDueAt,
      nextDueMileage: args.nextDueMileage,
      taskName: args.taskName,
      vehicleId: args.vehicleId
    };
    const __rel_vehicle = await __resolveRelation(ctx, "vehicles", [__auth.tenantId, __draft.vehicleId], ["tenantId","id"], "tenantId", __auth.tenantId);
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may read vehicle maintenance");
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may update vehicle maintenance");
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may execute vehicle maintenance commands");
    if (!((__draft.scheduledAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((__rel_vehicle != null))) throw new Error("Guard 2 failed");
    if (!((vehicleId === __draft.vehicleId))) throw new Error("Maintenance vehicle must match the selected vehicle");
    if (!((((taskName).trim()).length > 0))) throw new Error("Maintenance task is required");
    if (!(((((intervalType === "time") && (nextDueAt != null)) && ((intervalDays == null) || (intervalDays > 0))) || (((intervalType === "mileage") && (nextDueMileage != null)) && ((intervalMiles == null) || (intervalMiles > 0)))))) throw new Error("Time schedules need a first due date; mileage schedules need a due odometer");
    const doc: Record<string, any> = {
      ...__draft,
      taskName: taskName,
      intervalType: intervalType,
      intervalDays: ((intervalDays != null) ? intervalDays : 90),
      intervalMiles: ((intervalMiles != null) ? intervalMiles : 5000),
      nextDueAt: nextDueAt,
      nextDueMileage: nextDueMileage,
      instructions: instructions,
      scheduledAt: Date.now(),
      version: 1,
    };
    const docId = await ctx.db.insert("vehicleMaintenanceSchedules", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, maintenanceScheduleId: docId, vehicleId: doc.vehicleId, tenantId: doc.tenantId, taskName: doc.taskName, intervalType: doc.intervalType, nextDueAt: doc.nextDueAt, nextDueMileage: doc.nextDueMileage, _subject: { entity: "VehicleMaintenanceSchedule", command: "schedule", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "VehicleMaintenanceScheduled", entity: "VehicleMaintenanceSchedule", entityId: docId, payload: { maintenanceScheduleId: docId, vehicleId: doc.vehicleId, tenantId: doc.tenantId, taskName: doc.taskName, intervalType: doc.intervalType, nextDueAt: doc.nextDueAt, nextDueMileage: doc.nextDueMileage }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "VehicleMaintenanceSchedule_createViaSchedule", __result);
    }
    return __result;
  },
});

async function __runVehicleServiceEntryRecord(ctx: MutationCtx, { docId, maintenanceScheduleId, vehicleId, vendor, cost, odometer, completedAt, nextDueAt, nextDueMileage, notes, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("VehicleServiceEntry not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("VehicleServiceEntry not found");
    const __rel_maintenanceSchedule = await __resolveRelation(ctx, "vehicleMaintenanceSchedules", [__auth.tenantId, doc.maintenanceScheduleId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_vehicle = await __resolveRelation(ctx, "vehicles", [__auth.tenantId, doc.vehicleId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).maintenanceScheduleId;
      ((doc as any) as any).maintenanceSchedule = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    {
      const __fk = ((doc as any) as any).vehicleId;
      ((doc as any) as any).vehicle = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may read vehicle service history");
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may record vehicle service");
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may execute vehicle service commands");
    if (!((doc.loggedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((__rel_maintenanceSchedule != null))) throw new Error("Guard 2 failed");
    if (!((__rel_vehicle != null))) throw new Error("Guard 3 failed");
    if (!((maintenanceScheduleId === doc.maintenanceScheduleId))) throw new Error("Service entry must match the selected maintenance schedule");
    if (!(((vehicleId === doc.vehicleId) && (__rel_maintenanceSchedule.vehicleId === vehicleId)))) throw new Error("Service entry vehicle must match its maintenance schedule");
    if (!((((vendor).trim()).length > 0))) throw new Error("Vendor is required");
    if (!((cost >= 0))) throw new Error("Service cost cannot be negative");
    if (!((odometer >= 0))) throw new Error("Odometer reading cannot be negative");
    if (!((completedAt <= Date.now()))) throw new Error("Service completion cannot be in the future");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      vendor: vendor,
      cost: cost,
      odometer: odometer,
      completedAt: completedAt,
      nextDueAt: nextDueAt,
      nextDueMileage: nextDueMileage,
      notes: notes,
      loggedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, serviceEntryId: docId, maintenanceScheduleId: maintenanceScheduleId, vehicleId: vehicleId, tenantId: __after.tenantId, vendor: vendor, cost: cost, odometer: odometer, completedAt: completedAt, nextDueAt: nextDueAt, nextDueMileage: nextDueMileage, _subject: { entity: "VehicleServiceEntry", command: "record", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "VehicleServiceRecorded", entity: "VehicleServiceEntry", entityId: docId, payload: { serviceEntryId: docId, maintenanceScheduleId: maintenanceScheduleId, vehicleId: vehicleId, tenantId: __after.tenantId, vendor: vendor, cost: cost, odometer: odometer, completedAt: completedAt, nextDueAt: nextDueAt, nextDueMileage: nextDueMileage }, createdAt: Date.now() });
    // Reactions
    const reactionTarget0 = payload.maintenanceScheduleId;
    if (reactionTarget0) await __runVehicleMaintenanceScheduleApplyService(ctx, { docId: reactionTarget0, completedAt: payload.completedAt, odometer: payload.odometer, nextDueAt: payload.nextDueAt, nextDueMileage: payload.nextDueMileage } as any);
    return { ...doc, ...updates };
}

export const VehicleServiceEntry_record = mutation({
  args: {
    docId: v.id("vehicleServiceEntries"),
    maintenanceScheduleId: v.string(),
    vehicleId: v.string(),
    vendor: v.string(),
    cost: v.number(),
    odometer: v.number(),
    completedAt: v.number(),
    nextDueAt: v.optional(v.number()),
    nextDueMileage: v.optional(v.number()),
    notes: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runVehicleServiceEntryRecord(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "VehicleServiceEntry_record", __result);
    }
    return __result;
  },
});

export const VehicleServiceEntry_createViaRecord = mutation({
  args: {
    maintenanceScheduleId: v.string(),
    vehicleId: v.string(),
    vendor: v.string(),
    cost: v.number(),
    odometer: v.number(),
    completedAt: v.number(),
    nextDueAt: v.optional(v.number()),
    nextDueMileage: v.optional(v.number()),
    notes: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { maintenanceScheduleId, vehicleId, vendor, cost, odometer, completedAt, nextDueAt, nextDueMileage, notes } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      createdAt: Date.now(),
      updatedAt: Date.now(),
      completedAt: args.completedAt,
      cost: args.cost,
      maintenanceScheduleId: args.maintenanceScheduleId,
      nextDueAt: args.nextDueAt,
      nextDueMileage: args.nextDueMileage,
      notes: args.notes,
      odometer: args.odometer,
      vehicleId: args.vehicleId,
      vendor: args.vendor
    };
    const __rel_maintenanceSchedule = await __resolveRelation(ctx, "vehicleMaintenanceSchedules", [__auth.tenantId, __draft.maintenanceScheduleId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_vehicle = await __resolveRelation(ctx, "vehicles", [__auth.tenantId, __draft.vehicleId], ["tenantId","id"], "tenantId", __auth.tenantId);
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may read vehicle service history");
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may record vehicle service");
    if (!((checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) throw new Error("Logistics staff and managers may execute vehicle service commands");
    if (!((__draft.loggedAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((__rel_maintenanceSchedule != null))) throw new Error("Guard 2 failed");
    if (!((__rel_vehicle != null))) throw new Error("Guard 3 failed");
    if (!((maintenanceScheduleId === __draft.maintenanceScheduleId))) throw new Error("Service entry must match the selected maintenance schedule");
    if (!(((vehicleId === __draft.vehicleId) && (__rel_maintenanceSchedule.vehicleId === vehicleId)))) throw new Error("Service entry vehicle must match its maintenance schedule");
    if (!((((vendor).trim()).length > 0))) throw new Error("Vendor is required");
    if (!((cost >= 0))) throw new Error("Service cost cannot be negative");
    if (!((odometer >= 0))) throw new Error("Odometer reading cannot be negative");
    if (!((completedAt <= Date.now()))) throw new Error("Service completion cannot be in the future");
    const doc: Record<string, any> = {
      ...__draft,
      vendor: vendor,
      cost: cost,
      odometer: odometer,
      completedAt: completedAt,
      nextDueAt: nextDueAt,
      nextDueMileage: nextDueMileage,
      notes: notes,
      loggedAt: Date.now(),
      version: 1,
    };
    const docId = await ctx.db.insert("vehicleServiceEntries", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, serviceEntryId: docId, maintenanceScheduleId: doc.maintenanceScheduleId, vehicleId: doc.vehicleId, tenantId: doc.tenantId, vendor: doc.vendor, cost: doc.cost, odometer: doc.odometer, completedAt: doc.completedAt, nextDueAt: doc.nextDueAt, nextDueMileage: doc.nextDueMileage, _subject: { entity: "VehicleServiceEntry", command: "record", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "VehicleServiceRecorded", entity: "VehicleServiceEntry", entityId: docId, payload: { serviceEntryId: docId, maintenanceScheduleId: doc.maintenanceScheduleId, vehicleId: doc.vehicleId, tenantId: doc.tenantId, vendor: doc.vendor, cost: doc.cost, odometer: doc.odometer, completedAt: doc.completedAt, nextDueAt: doc.nextDueAt, nextDueMileage: doc.nextDueMileage }, createdAt: Date.now() });
    // Reactions
    const reactionTarget0 = payload.maintenanceScheduleId;
    if (reactionTarget0) await __runVehicleMaintenanceScheduleApplyService(ctx, { docId: reactionTarget0, completedAt: payload.completedAt, odometer: payload.odometer, nextDueAt: payload.nextDueAt, nextDueMileage: payload.nextDueMileage } as any);
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "VehicleServiceEntry_createViaRecord", __result);
    }
    return __result;
  },
});

async function __runVendorOnboard(ctx: MutationCtx, { docId, name, email, phone, addressLine1, city, region, postalCode, countryCode, paymentTermsDays, notes, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("Vendor not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("Vendor not found");
    const doc = await __decryptDoc(ctx, "Vendor", ["email","phone","addressLine1","city","region","postalCode","countryCode"], __storedDoc) as Record<string, any>;
    if (!(checkRole(user, "procurementAccess"))) throw new Error("Procurement staff may read vendors");
    if (!(checkRole(user, "procurementAccess"))) throw new Error("Procurement staff may write vendors through commands");
    if (!(checkRole(user, "procurementAccess"))) throw new Error("Procurement staff may execute vendor commands");
    if (!((doc.onboardedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((((name).trim()).length > 0))) throw new Error("Vendor name is required");
    if (!(((paymentTermsDays == null) || ((paymentTermsDays >= 0) && (paymentTermsDays <= 365))))) throw new Error("Payment terms must be between 0 and 365 days");
    if (!(((countryCode == null) || (((countryCode).trim()).length === 2)))) throw new Error("Country code must contain two characters");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      name: name,
      email: email,
      phone: phone,
      addressLine1: addressLine1,
      city: city,
      region: region,
      postalCode: postalCode,
      countryCode: countryCode,
      paymentTermsDays: ((paymentTermsDays != null) ? paymentTermsDays : 30),
      notes: notes,
      onboardedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "Vendor", ["email","phone","addressLine1","city","region","postalCode","countryCode"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, vendorId: docId, tenantId: __after.tenantId, name: name, paymentTermsDays: ((paymentTermsDays != null) ? paymentTermsDays : 30), _subject: { entity: "Vendor", command: "onboard", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "VendorOnboarded", entity: "Vendor", entityId: docId, payload: { vendorId: docId, tenantId: __after.tenantId, name: name, paymentTermsDays: ((paymentTermsDays != null) ? paymentTermsDays : 30) }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Vendor_onboard = mutation({
  args: {
    docId: v.id("vendors"),
    name: v.string(),
    email: v.optional(v.string()),
    phone: v.optional(v.string()),
    addressLine1: v.optional(v.string()),
    city: v.optional(v.string()),
    region: v.optional(v.string()),
    postalCode: v.optional(v.string()),
    countryCode: v.optional(v.string()),
    paymentTermsDays: v.optional(v.any()),
    notes: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runVendorOnboard(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Vendor_onboard", __result);
    }
    return __result;
  },
});

export const Vendor_createViaOnboard = mutation({
  args: {
    name: v.string(),
    email: v.optional(v.string()),
    phone: v.optional(v.string()),
    addressLine1: v.optional(v.string()),
    city: v.optional(v.string()),
    region: v.optional(v.string()),
    postalCode: v.optional(v.string()),
    countryCode: v.optional(v.string()),
    paymentTermsDays: v.optional(v.any()),
    notes: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { name, email, phone, addressLine1, city, region, postalCode, countryCode, paymentTermsDays, notes } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      paymentTermsDays: args.paymentTermsDays !== undefined ? args.paymentTermsDays : 30,
      status: "active",
      createdAt: Date.now(),
      updatedAt: Date.now(),
      addressLine1: args.addressLine1,
      city: args.city,
      countryCode: args.countryCode,
      email: args.email,
      name: args.name,
      notes: args.notes,
      phone: args.phone,
      postalCode: args.postalCode,
      region: args.region
    };
    if (!(checkRole(user, "procurementAccess"))) throw new Error("Procurement staff may read vendors");
    if (!(checkRole(user, "procurementAccess"))) throw new Error("Procurement staff may write vendors through commands");
    if (!(checkRole(user, "procurementAccess"))) throw new Error("Procurement staff may execute vendor commands");
    if (!((__draft.onboardedAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((((name).trim()).length > 0))) throw new Error("Vendor name is required");
    if (!(((paymentTermsDays == null) || ((paymentTermsDays >= 0) && (paymentTermsDays <= 365))))) throw new Error("Payment terms must be between 0 and 365 days");
    if (!(((countryCode == null) || (((countryCode).trim()).length === 2)))) throw new Error("Country code must contain two characters");
    const doc: Record<string, any> = {
      ...__draft,
      name: name,
      email: email,
      phone: phone,
      addressLine1: addressLine1,
      city: city,
      region: region,
      postalCode: postalCode,
      countryCode: countryCode,
      paymentTermsDays: ((paymentTermsDays != null) ? paymentTermsDays : 30),
      notes: notes,
      onboardedAt: Date.now(),
      version: 1,
    };
    const __storedDoc = await __encryptDoc(ctx, "Vendor", ["email","phone","addressLine1","city","region","postalCode","countryCode"], doc);
    const docId = await ctx.db.insert("vendors", __storedDoc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, vendorId: docId, tenantId: doc.tenantId, name: doc.name, paymentTermsDays: ((doc.paymentTermsDays != null) ? doc.paymentTermsDays : 30), _subject: { entity: "Vendor", command: "onboard", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "VendorOnboarded", entity: "Vendor", entityId: docId, payload: { vendorId: docId, tenantId: doc.tenantId, name: doc.name, paymentTermsDays: ((doc.paymentTermsDays != null) ? doc.paymentTermsDays : 30) }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Vendor_createViaOnboard", __result);
    }
    return __result;
  },
});

async function __runVendorReinstate(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("Vendor not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("Vendor not found");
    const doc = await __decryptDoc(ctx, "Vendor", ["email","phone","addressLine1","city","region","postalCode","countryCode"], __storedDoc) as Record<string, any>;
    if (!(checkRole(user, "procurementAccess"))) throw new Error("Procurement staff may read vendors");
    if (!(checkRole(user, "procurementAccess"))) throw new Error("Procurement staff may write vendors through commands");
    if (!(checkRole(user, "procurementAccess"))) throw new Error("Procurement staff may execute vendor commands");
    if (!((doc.onboardedAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.status === "suspended"))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!(checkRole(user, "inventoryManageAccess"))) throw new Error("Guard 3 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "active";
        const __allowed: Record<string, string[]> = { "active": ["suspended", "terminated"], "suspended": ["active", "terminated"], "terminated": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "active",
      suspendedAt: null,
      suspensionReason: null,
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "Vendor", ["email","phone","addressLine1","city","region","postalCode","countryCode"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, vendorId: docId, tenantId: __after.tenantId, name: __after.name, _subject: { entity: "Vendor", command: "reinstate", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "VendorReinstated", entity: "Vendor", entityId: docId, payload: { vendorId: docId, tenantId: __after.tenantId, name: __after.name }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Vendor_reinstate = mutation({
  args: {
    docId: v.id("vendors"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runVendorReinstate(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Vendor_reinstate", __result);
    }
    return __result;
  },
});

async function __runVendorSuspend(ctx: MutationCtx, { docId, reason, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("Vendor not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("Vendor not found");
    const doc = await __decryptDoc(ctx, "Vendor", ["email","phone","addressLine1","city","region","postalCode","countryCode"], __storedDoc) as Record<string, any>;
    if (!(checkRole(user, "procurementAccess"))) throw new Error("Procurement staff may read vendors");
    if (!(checkRole(user, "procurementAccess"))) throw new Error("Procurement staff may write vendors through commands");
    if (!(checkRole(user, "procurementAccess"))) throw new Error("Procurement staff may execute vendor commands");
    if (!((doc.onboardedAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.status === "active"))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!(checkRole(user, "inventoryManageAccess"))) throw new Error("Guard 3 failed");
    if (!((((reason).trim()).length > 0))) throw new Error("Suspension reason is required");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "suspended";
        const __allowed: Record<string, string[]> = { "active": ["suspended", "terminated"], "suspended": ["active", "terminated"], "terminated": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "suspended",
      suspendedAt: Date.now(),
      suspensionReason: reason,
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "Vendor", ["email","phone","addressLine1","city","region","postalCode","countryCode"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, vendorId: docId, tenantId: __after.tenantId, name: __after.name, reason: reason, _subject: { entity: "Vendor", command: "suspend", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "VendorSuspended", entity: "Vendor", entityId: docId, payload: { vendorId: docId, tenantId: __after.tenantId, name: __after.name, reason: reason }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Vendor_suspend = mutation({
  args: {
    docId: v.id("vendors"),
    reason: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runVendorSuspend(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Vendor_suspend", __result);
    }
    return __result;
  },
});

async function __runVendorTerminate(ctx: MutationCtx, { docId, reason, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("Vendor not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("Vendor not found");
    const doc = await __decryptDoc(ctx, "Vendor", ["email","phone","addressLine1","city","region","postalCode","countryCode"], __storedDoc) as Record<string, any>;
    if (!(checkRole(user, "procurementAccess"))) throw new Error("Procurement staff may read vendors");
    if (!(checkRole(user, "procurementAccess"))) throw new Error("Procurement staff may write vendors through commands");
    if (!(checkRole(user, "procurementAccess"))) throw new Error("Procurement staff may execute vendor commands");
    if (!((doc.onboardedAt != null))) throw new Error("Guard 0 failed");
    if (!(((doc.status === "active") || (doc.status === "suspended")))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!(checkRole(user, "inventoryManageAccess"))) throw new Error("Guard 3 failed");
    if (!((((reason).trim()).length > 0))) throw new Error("Termination reason is required");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "terminated";
        const __allowed: Record<string, string[]> = { "active": ["suspended", "terminated"], "suspended": ["active", "terminated"], "terminated": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "terminated",
      terminatedAt: Date.now(),
      terminationReason: reason,
      deletedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "Vendor", ["email","phone","addressLine1","city","region","postalCode","countryCode"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, vendorId: docId, tenantId: __after.tenantId, name: __after.name, reason: reason, _subject: { entity: "Vendor", command: "terminate", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "VendorTerminated", entity: "Vendor", entityId: docId, payload: { vendorId: docId, tenantId: __after.tenantId, name: __after.name, reason: reason }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Vendor_terminate = mutation({
  args: {
    docId: v.id("vendors"),
    reason: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runVendorTerminate(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Vendor_terminate", __result);
    }
    return __result;
  },
});

async function __runVendorUpdateDetails(ctx: MutationCtx, { docId, name, email, phone, addressLine1, city, region, postalCode, countryCode, paymentTermsDays, notes, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("Vendor not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("Vendor not found");
    const doc = await __decryptDoc(ctx, "Vendor", ["email","phone","addressLine1","city","region","postalCode","countryCode"], __storedDoc) as Record<string, any>;
    if (!(checkRole(user, "procurementAccess"))) throw new Error("Procurement staff may read vendors");
    if (!(checkRole(user, "procurementAccess"))) throw new Error("Procurement staff may write vendors through commands");
    if (!(checkRole(user, "procurementAccess"))) throw new Error("Procurement staff may execute vendor commands");
    if (!((doc.onboardedAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.status === "active"))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((((name).trim()).length > 0))) throw new Error("Vendor name is required");
    if (!(((paymentTermsDays == null) || ((paymentTermsDays >= 0) && (paymentTermsDays <= 365))))) throw new Error("Payment terms must be between 0 and 365 days");
    if (!(((countryCode == null) || (((countryCode).trim()).length === 2)))) throw new Error("Country code must contain two characters");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      name: name,
      email: ((email != null) ? email : doc.email),
      phone: ((phone != null) ? phone : doc.phone),
      addressLine1: ((addressLine1 != null) ? addressLine1 : doc.addressLine1),
      city: ((city != null) ? city : doc.city),
      region: ((region != null) ? region : doc.region),
      postalCode: ((postalCode != null) ? postalCode : doc.postalCode),
      countryCode: ((countryCode != null) ? countryCode : doc.countryCode),
      paymentTermsDays: ((paymentTermsDays != null) ? paymentTermsDays : doc.paymentTermsDays),
      notes: ((notes != null) ? notes : doc.notes),
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "Vendor", ["email","phone","addressLine1","city","region","postalCode","countryCode"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, vendorId: docId, tenantId: __after.tenantId, name: name, paymentTermsDays: ((paymentTermsDays != null) ? paymentTermsDays : __after.paymentTermsDays), _subject: { entity: "Vendor", command: "updateDetails", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "VendorDetailsUpdated", entity: "Vendor", entityId: docId, payload: { vendorId: docId, tenantId: __after.tenantId, name: name, paymentTermsDays: ((paymentTermsDays != null) ? paymentTermsDays : __after.paymentTermsDays) }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Vendor_updateDetails = mutation({
  args: {
    docId: v.id("vendors"),
    name: v.string(),
    email: v.optional(v.string()),
    phone: v.optional(v.string()),
    addressLine1: v.optional(v.string()),
    city: v.optional(v.string()),
    region: v.optional(v.string()),
    postalCode: v.optional(v.string()),
    countryCode: v.optional(v.string()),
    paymentTermsDays: v.optional(v.any()),
    notes: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runVendorUpdateDetails(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Vendor_updateDetails", __result);
    }
    return __result;
  },
});

async function __runVendorContactAdd(ctx: MutationCtx, { docId, vendorId, name, role, email, phone, notes, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("VendorContact not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("VendorContact not found");
    const doc = await __decryptDoc(ctx, "VendorContact", ["email","phone"], __storedDoc) as Record<string, any>;
    const __rel_vendor = await __resolveRelation(ctx, "vendors", [__auth.tenantId, doc.vendorId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).vendorId;
      ((doc as any) as any).vendor = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!(checkRole(user, "procurementAccess"))) throw new Error("Procurement staff may read vendor contacts");
    if (!(checkRole(user, "procurementAccess"))) throw new Error("Procurement staff may write vendor contacts through commands");
    if (!(checkRole(user, "procurementAccess"))) throw new Error("Procurement staff may execute vendor contact commands");
    if (!((doc.addedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(((__rel_vendor != null) && (__rel_vendor.deletedAt == null)))) throw new Error("Guard 2 failed");
    if (!((vendorId === doc.vendorId))) throw new Error("Add contact vendorId must match the seeded vendor reference");
    if (!((((name).trim()).length > 0))) throw new Error("Contact name is required");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      vendorId: vendorId,
      name: name,
      role: ((role != null) ? role : "general"),
      email: email,
      phone: phone,
      notes: notes,
      addedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "VendorContact", ["email","phone"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, vendorContactId: docId, tenantId: __after.tenantId, vendorId: vendorId, name: name, role: ((role != null) ? role : "general"), _subject: { entity: "VendorContact", command: "add", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "VendorContactAdded", entity: "VendorContact", entityId: docId, payload: { vendorContactId: docId, tenantId: __after.tenantId, vendorId: vendorId, name: name, role: ((role != null) ? role : "general") }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const VendorContact_add = mutation({
  args: {
    docId: v.id("vendorContacts"),
    vendorId: v.string(),
    name: v.string(),
    role: v.optional(v.any()),
    email: v.optional(v.string()),
    phone: v.optional(v.string()),
    notes: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runVendorContactAdd(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "VendorContact_add", __result);
    }
    return __result;
  },
});

export const VendorContact_createViaAdd = mutation({
  args: {
    vendorId: v.string(),
    name: v.string(),
    role: v.optional(v.any()),
    email: v.optional(v.string()),
    phone: v.optional(v.string()),
    notes: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { vendorId, name, role, email, phone, notes } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      role: args.role !== undefined ? args.role : "general",
      createdAt: Date.now(),
      updatedAt: Date.now(),
      email: args.email,
      name: args.name,
      notes: args.notes,
      phone: args.phone,
      vendorId: args.vendorId
    };
    const __rel_vendor = await __resolveRelation(ctx, "vendors", [__auth.tenantId, __draft.vendorId], ["tenantId","id"], "tenantId", __auth.tenantId);
    if (!(checkRole(user, "procurementAccess"))) throw new Error("Procurement staff may read vendor contacts");
    if (!(checkRole(user, "procurementAccess"))) throw new Error("Procurement staff may write vendor contacts through commands");
    if (!(checkRole(user, "procurementAccess"))) throw new Error("Procurement staff may execute vendor contact commands");
    if (!((__draft.addedAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(((__rel_vendor != null) && (__rel_vendor.deletedAt == null)))) throw new Error("Guard 2 failed");
    if (!((vendorId === __draft.vendorId))) throw new Error("Add contact vendorId must match the seeded vendor reference");
    if (!((((name).trim()).length > 0))) throw new Error("Contact name is required");
    const doc: Record<string, any> = {
      ...__draft,
      vendorId: vendorId,
      name: name,
      role: ((role != null) ? role : "general"),
      email: email,
      phone: phone,
      notes: notes,
      addedAt: Date.now(),
      version: 1,
    };
    const __storedDoc = await __encryptDoc(ctx, "VendorContact", ["email","phone"], doc);
    const docId = await ctx.db.insert("vendorContacts", __storedDoc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, vendorContactId: docId, tenantId: doc.tenantId, vendorId: doc.vendorId, name: doc.name, role: ((doc.role != null) ? doc.role : "general"), _subject: { entity: "VendorContact", command: "add", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "VendorContactAdded", entity: "VendorContact", entityId: docId, payload: { vendorContactId: docId, tenantId: doc.tenantId, vendorId: doc.vendorId, name: doc.name, role: ((doc.role != null) ? doc.role : "general") }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "VendorContact_createViaAdd", __result);
    }
    return __result;
  },
});

async function __runVendorContactRemove(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("VendorContact not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("VendorContact not found");
    const doc = await __decryptDoc(ctx, "VendorContact", ["email","phone"], __storedDoc) as Record<string, any>;
    if (!(checkRole(user, "procurementAccess"))) throw new Error("Procurement staff may read vendor contacts");
    if (!(checkRole(user, "procurementAccess"))) throw new Error("Procurement staff may write vendor contacts through commands");
    if (!(checkRole(user, "procurementAccess"))) throw new Error("Procurement staff may execute vendor contact commands");
    if (!((doc.addedAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      deletedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "VendorContact", ["email","phone"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, vendorContactId: docId, tenantId: __after.tenantId, vendorId: __after.vendorId, name: __after.name, _subject: { entity: "VendorContact", command: "remove", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "VendorContactRemoved", entity: "VendorContact", entityId: docId, payload: { vendorContactId: docId, tenantId: __after.tenantId, vendorId: __after.vendorId, name: __after.name }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const VendorContact_remove = mutation({
  args: {
    docId: v.id("vendorContacts"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runVendorContactRemove(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "VendorContact_remove", __result);
    }
    return __result;
  },
});

async function __runVendorContactUpdate(ctx: MutationCtx, { docId, name, role, email, phone, notes, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("VendorContact not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("VendorContact not found");
    const doc = await __decryptDoc(ctx, "VendorContact", ["email","phone"], __storedDoc) as Record<string, any>;
    if (!(checkRole(user, "procurementAccess"))) throw new Error("Procurement staff may read vendor contacts");
    if (!(checkRole(user, "procurementAccess"))) throw new Error("Procurement staff may write vendor contacts through commands");
    if (!(checkRole(user, "procurementAccess"))) throw new Error("Procurement staff may execute vendor contact commands");
    if (!((doc.addedAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((((name).trim()).length > 0))) throw new Error("Contact name is required");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      name: name,
      role: ((role != null) ? role : doc.role),
      email: ((email != null) ? email : doc.email),
      phone: ((phone != null) ? phone : doc.phone),
      notes: ((notes != null) ? notes : doc.notes),
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "VendorContact", ["email","phone"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, vendorContactId: docId, tenantId: __after.tenantId, vendorId: __after.vendorId, name: name, role: ((role != null) ? role : __after.role), _subject: { entity: "VendorContact", command: "update", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "VendorContactUpdated", entity: "VendorContact", entityId: docId, payload: { vendorContactId: docId, tenantId: __after.tenantId, vendorId: __after.vendorId, name: name, role: ((role != null) ? role : __after.role) }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const VendorContact_update = mutation({
  args: {
    docId: v.id("vendorContacts"),
    name: v.string(),
    role: v.optional(v.any()),
    email: v.optional(v.string()),
    phone: v.optional(v.string()),
    notes: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runVendorContactUpdate(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "VendorContact_update", __result);
    }
    return __result;
  },
});

async function __runVendorContractActivate(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("VendorContract not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("VendorContract not found");
    if (!(checkRole(user, "procurementAccess"))) throw new Error("Procurement staff may read vendor contracts");
    if (!(checkRole(user, "procurementAccess"))) throw new Error("Procurement staff may write vendor contracts through commands");
    if (!(checkRole(user, "procurementAccess"))) throw new Error("Procurement staff may execute vendor contract commands");
    if (!((doc.draftedAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.status === "draft"))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!(((doc.startsAt != null) && (doc.endsAt != null)))) throw new Error("Guard 3 failed");
    if (!((doc.endsAt > Date.now()))) throw new Error("Cannot activate a contract whose period has already ended");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "active";
        const __allowed: Record<string, string[]> = { "draft": ["active", "terminated"], "active": ["expired", "terminated"], "expired": [], "terminated": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "active",
      activatedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, vendorContractId: docId, tenantId: __after.tenantId, vendorId: __after.vendorId, title: __after.title, endsAt: __after.endsAt, _subject: { entity: "VendorContract", command: "activate", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "VendorContractActivated", entity: "VendorContract", entityId: docId, payload: { vendorContractId: docId, tenantId: __after.tenantId, vendorId: __after.vendorId, title: __after.title, endsAt: __after.endsAt }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const VendorContract_activate = mutation({
  args: {
    docId: v.id("vendorContracts"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runVendorContractActivate(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "VendorContract_activate", __result);
    }
    return __result;
  },
});

async function __runVendorContractDraft(ctx: MutationCtx, { docId, vendorId, title, startsAt, endsAt, contractNumber, paymentTermsDays, deliveryLeadTimeDays, documentUrl, notes, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("VendorContract not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("VendorContract not found");
    const __rel_vendor = await __resolveRelation(ctx, "vendors", [__auth.tenantId, doc.vendorId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).vendorId;
      ((doc as any) as any).vendor = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!(checkRole(user, "procurementAccess"))) throw new Error("Procurement staff may read vendor contracts");
    if (!(checkRole(user, "procurementAccess"))) throw new Error("Procurement staff may write vendor contracts through commands");
    if (!(checkRole(user, "procurementAccess"))) throw new Error("Procurement staff may execute vendor contract commands");
    if (!((doc.draftedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(((__rel_vendor != null) && (__rel_vendor.deletedAt == null)))) throw new Error("Guard 2 failed");
    if (!((vendorId === doc.vendorId))) throw new Error("Draft vendorId must match the seeded vendor reference");
    if (!((((title).trim()).length > 0))) throw new Error("Contract title is required");
    if (!((endsAt > startsAt))) throw new Error("Contract end must be after the start");
    if (!(((paymentTermsDays == null) || ((paymentTermsDays >= 0) && (paymentTermsDays <= 365))))) throw new Error("Payment terms must be between 0 and 365 days");
    if (!(((deliveryLeadTimeDays == null) || ((deliveryLeadTimeDays >= 0) && (deliveryLeadTimeDays <= 365))))) throw new Error("Delivery lead time must be between 0 and 365 days");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      vendorId: vendorId,
      title: title,
      startsAt: startsAt,
      endsAt: endsAt,
      contractNumber: contractNumber,
      paymentTermsDays: ((paymentTermsDays != null) ? paymentTermsDays : 30),
      deliveryLeadTimeDays: ((deliveryLeadTimeDays != null) ? deliveryLeadTimeDays : 0),
      documentUrl: documentUrl,
      notes: notes,
      draftedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, vendorContractId: docId, tenantId: __after.tenantId, vendorId: vendorId, title: title, _subject: { entity: "VendorContract", command: "draft", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "VendorContractDrafted", entity: "VendorContract", entityId: docId, payload: { vendorContractId: docId, tenantId: __after.tenantId, vendorId: vendorId, title: title }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const VendorContract_draft = mutation({
  args: {
    docId: v.id("vendorContracts"),
    vendorId: v.string(),
    title: v.string(),
    startsAt: v.number(),
    endsAt: v.number(),
    contractNumber: v.optional(v.string()),
    paymentTermsDays: v.optional(v.any()),
    deliveryLeadTimeDays: v.optional(v.any()),
    documentUrl: v.optional(v.string()),
    notes: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runVendorContractDraft(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "VendorContract_draft", __result);
    }
    return __result;
  },
});

export const VendorContract_createViaDraft = mutation({
  args: {
    vendorId: v.string(),
    title: v.string(),
    startsAt: v.number(),
    endsAt: v.number(),
    contractNumber: v.optional(v.string()),
    paymentTermsDays: v.optional(v.any()),
    deliveryLeadTimeDays: v.optional(v.any()),
    documentUrl: v.optional(v.string()),
    notes: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { vendorId, title, startsAt, endsAt, contractNumber, paymentTermsDays, deliveryLeadTimeDays, documentUrl, notes } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      paymentTermsDays: args.paymentTermsDays !== undefined ? args.paymentTermsDays : 30,
      deliveryLeadTimeDays: args.deliveryLeadTimeDays !== undefined ? args.deliveryLeadTimeDays : 0,
      status: "draft",
      createdAt: Date.now(),
      updatedAt: Date.now(),
      contractNumber: args.contractNumber,
      documentUrl: args.documentUrl,
      endsAt: args.endsAt,
      notes: args.notes,
      startsAt: args.startsAt,
      title: args.title,
      vendorId: args.vendorId
    };
    const __rel_vendor = await __resolveRelation(ctx, "vendors", [__auth.tenantId, __draft.vendorId], ["tenantId","id"], "tenantId", __auth.tenantId);
    if (!(checkRole(user, "procurementAccess"))) throw new Error("Procurement staff may read vendor contracts");
    if (!(checkRole(user, "procurementAccess"))) throw new Error("Procurement staff may write vendor contracts through commands");
    if (!(checkRole(user, "procurementAccess"))) throw new Error("Procurement staff may execute vendor contract commands");
    if (!((__draft.draftedAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(((__rel_vendor != null) && (__rel_vendor.deletedAt == null)))) throw new Error("Guard 2 failed");
    if (!((vendorId === __draft.vendorId))) throw new Error("Draft vendorId must match the seeded vendor reference");
    if (!((((title).trim()).length > 0))) throw new Error("Contract title is required");
    if (!((endsAt > startsAt))) throw new Error("Contract end must be after the start");
    if (!(((paymentTermsDays == null) || ((paymentTermsDays >= 0) && (paymentTermsDays <= 365))))) throw new Error("Payment terms must be between 0 and 365 days");
    if (!(((deliveryLeadTimeDays == null) || ((deliveryLeadTimeDays >= 0) && (deliveryLeadTimeDays <= 365))))) throw new Error("Delivery lead time must be between 0 and 365 days");
    const doc: Record<string, any> = {
      ...__draft,
      vendorId: vendorId,
      title: title,
      startsAt: startsAt,
      endsAt: endsAt,
      contractNumber: contractNumber,
      paymentTermsDays: ((paymentTermsDays != null) ? paymentTermsDays : 30),
      deliveryLeadTimeDays: ((deliveryLeadTimeDays != null) ? deliveryLeadTimeDays : 0),
      documentUrl: documentUrl,
      notes: notes,
      draftedAt: Date.now(),
      version: 1,
    };
    const docId = await ctx.db.insert("vendorContracts", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, vendorContractId: docId, tenantId: doc.tenantId, vendorId: doc.vendorId, title: doc.title, _subject: { entity: "VendorContract", command: "draft", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "VendorContractDrafted", entity: "VendorContract", entityId: docId, payload: { vendorContractId: docId, tenantId: doc.tenantId, vendorId: doc.vendorId, title: doc.title }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "VendorContract_createViaDraft", __result);
    }
    return __result;
  },
});

async function __runVendorContractMarkExpired(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("VendorContract not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("VendorContract not found");
    if (!(checkRole(user, "procurementAccess"))) throw new Error("Procurement staff may read vendor contracts");
    if (!(checkRole(user, "procurementAccess"))) throw new Error("Procurement staff may write vendor contracts through commands");
    if (!(checkRole(user, "procurementAccess"))) throw new Error("Procurement staff may execute vendor contract commands");
    if (!((doc.status === "active"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(((doc.endsAt != null) && (doc.endsAt <= Date.now())))) throw new Error("Guard 2 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "expired";
        const __allowed: Record<string, string[]> = { "draft": ["active", "terminated"], "active": ["expired", "terminated"], "expired": [], "terminated": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "expired",
      expiredAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, vendorContractId: docId, tenantId: __after.tenantId, vendorId: __after.vendorId, title: __after.title, _subject: { entity: "VendorContract", command: "markExpired", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "VendorContractExpired", entity: "VendorContract", entityId: docId, payload: { vendorContractId: docId, tenantId: __after.tenantId, vendorId: __after.vendorId, title: __after.title }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const VendorContract_markExpired = mutation({
  args: {
    docId: v.id("vendorContracts"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runVendorContractMarkExpired(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "VendorContract_markExpired", __result);
    }
    return __result;
  },
});

async function __runVendorContractTerminate(ctx: MutationCtx, { docId, reason, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("VendorContract not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("VendorContract not found");
    if (!(checkRole(user, "procurementAccess"))) throw new Error("Procurement staff may read vendor contracts");
    if (!(checkRole(user, "procurementAccess"))) throw new Error("Procurement staff may write vendor contracts through commands");
    if (!(checkRole(user, "procurementAccess"))) throw new Error("Procurement staff may execute vendor contract commands");
    if (!(((doc.status === "draft") || (doc.status === "active")))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((((reason).trim()).length > 0))) throw new Error("Termination reason is required");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "terminated";
        const __allowed: Record<string, string[]> = { "draft": ["active", "terminated"], "active": ["expired", "terminated"], "expired": [], "terminated": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "terminated",
      terminatedAt: Date.now(),
      terminationReason: reason,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, vendorContractId: docId, tenantId: __after.tenantId, vendorId: __after.vendorId, title: __after.title, reason: reason, _subject: { entity: "VendorContract", command: "terminate", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "VendorContractTerminated", entity: "VendorContract", entityId: docId, payload: { vendorContractId: docId, tenantId: __after.tenantId, vendorId: __after.vendorId, title: __after.title, reason: reason }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const VendorContract_terminate = mutation({
  args: {
    docId: v.id("vendorContracts"),
    reason: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runVendorContractTerminate(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "VendorContract_terminate", __result);
    }
    return __result;
  },
});

async function __runVendorContractUpdateTerms(ctx: MutationCtx, { docId, title, startsAt, endsAt, contractNumber, paymentTermsDays, deliveryLeadTimeDays, documentUrl, notes, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("VendorContract not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("VendorContract not found");
    if (!(checkRole(user, "procurementAccess"))) throw new Error("Procurement staff may read vendor contracts");
    if (!(checkRole(user, "procurementAccess"))) throw new Error("Procurement staff may write vendor contracts through commands");
    if (!(checkRole(user, "procurementAccess"))) throw new Error("Procurement staff may execute vendor contract commands");
    if (!((doc.draftedAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.status === "draft"))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((((title).trim()).length > 0))) throw new Error("Contract title is required");
    if (!((endsAt > startsAt))) throw new Error("Contract end must be after the start");
    if (!(((paymentTermsDays == null) || ((paymentTermsDays >= 0) && (paymentTermsDays <= 365))))) throw new Error("Payment terms must be between 0 and 365 days");
    if (!(((deliveryLeadTimeDays == null) || ((deliveryLeadTimeDays >= 0) && (deliveryLeadTimeDays <= 365))))) throw new Error("Delivery lead time must be between 0 and 365 days");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      title: title,
      startsAt: startsAt,
      endsAt: endsAt,
      contractNumber: ((contractNumber != null) ? contractNumber : doc.contractNumber),
      paymentTermsDays: ((paymentTermsDays != null) ? paymentTermsDays : doc.paymentTermsDays),
      deliveryLeadTimeDays: ((deliveryLeadTimeDays != null) ? deliveryLeadTimeDays : doc.deliveryLeadTimeDays),
      documentUrl: ((documentUrl != null) ? documentUrl : doc.documentUrl),
      notes: ((notes != null) ? notes : doc.notes),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, vendorContractId: docId, tenantId: __after.tenantId, vendorId: __after.vendorId, title: title, _subject: { entity: "VendorContract", command: "updateTerms", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "VendorContractTermsUpdated", entity: "VendorContract", entityId: docId, payload: { vendorContractId: docId, tenantId: __after.tenantId, vendorId: __after.vendorId, title: title }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const VendorContract_updateTerms = mutation({
  args: {
    docId: v.id("vendorContracts"),
    title: v.string(),
    startsAt: v.number(),
    endsAt: v.number(),
    contractNumber: v.optional(v.string()),
    paymentTermsDays: v.optional(v.any()),
    deliveryLeadTimeDays: v.optional(v.any()),
    documentUrl: v.optional(v.string()),
    notes: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runVendorContractUpdateTerms(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "VendorContract_updateTerms", __result);
    }
    return __result;
  },
});

async function __runVendorContractPriceTierAdd(ctx: MutationCtx, { docId, contractId, itemName, unitPrice, unit, minQuantity, notes, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("VendorContractPriceTier not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("VendorContractPriceTier not found");
    const __rel_contract = await __resolveRelation(ctx, "vendorContracts", [__auth.tenantId, doc.contractId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).contractId;
      ((doc as any) as any).contract = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!(checkRole(user, "procurementAccess"))) throw new Error("Procurement staff may read contract price tiers");
    if (!(checkRole(user, "procurementAccess"))) throw new Error("Procurement staff may write contract price tiers through commands");
    if (!(checkRole(user, "procurementAccess"))) throw new Error("Procurement staff may execute contract price tier commands");
    if (!((doc.addedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(((__rel_contract != null) && (__rel_contract.deletedAt == null)))) throw new Error("Guard 2 failed");
    if (!((__rel_contract.status === "draft"))) throw new Error("Guard 3 failed");
    if (!((contractId === doc.contractId))) throw new Error("Add tier contractId must match the seeded contract reference");
    if (!((((itemName).trim()).length > 0))) throw new Error("Item name is required");
    if (!(((minQuantity == null) || (minQuantity >= 0)))) throw new Error("Minimum quantity cannot be negative");
    if (!((unitPrice >= 0))) throw new Error("Unit price cannot be negative");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      contractId: contractId,
      itemName: itemName,
      unit: ((unit != null) ? unit : "each"),
      minQuantity: ((minQuantity != null) ? minQuantity : 0),
      unitPrice: unitPrice,
      notes: notes,
      addedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, vendorContractPriceTierId: docId, tenantId: __after.tenantId, vendorContractId: contractId, itemName: itemName, _subject: { entity: "VendorContractPriceTier", command: "add", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "VendorContractPriceTierAdded", entity: "VendorContractPriceTier", entityId: docId, payload: { vendorContractPriceTierId: docId, tenantId: __after.tenantId, vendorContractId: contractId, itemName: itemName }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const VendorContractPriceTier_add = mutation({
  args: {
    docId: v.id("vendorContractPriceTiers"),
    contractId: v.string(),
    itemName: v.string(),
    unitPrice: v.number(),
    unit: v.optional(v.string()),
    minQuantity: v.optional(v.any()),
    notes: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runVendorContractPriceTierAdd(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "VendorContractPriceTier_add", __result);
    }
    return __result;
  },
});

export const VendorContractPriceTier_createViaAdd = mutation({
  args: {
    contractId: v.string(),
    itemName: v.string(),
    unitPrice: v.number(),
    unit: v.optional(v.string()),
    minQuantity: v.optional(v.any()),
    notes: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { contractId, itemName, unitPrice, unit, minQuantity, notes } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      unit: args.unit !== undefined ? args.unit : "each",
      minQuantity: args.minQuantity !== undefined ? args.minQuantity : 0,
      createdAt: Date.now(),
      updatedAt: Date.now(),
      contractId: args.contractId,
      itemName: args.itemName,
      notes: args.notes,
      unitPrice: args.unitPrice
    };
    const __rel_contract = await __resolveRelation(ctx, "vendorContracts", [__auth.tenantId, __draft.contractId], ["tenantId","id"], "tenantId", __auth.tenantId);
    if (!(checkRole(user, "procurementAccess"))) throw new Error("Procurement staff may read contract price tiers");
    if (!(checkRole(user, "procurementAccess"))) throw new Error("Procurement staff may write contract price tiers through commands");
    if (!(checkRole(user, "procurementAccess"))) throw new Error("Procurement staff may execute contract price tier commands");
    if (!((__draft.addedAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(((__rel_contract != null) && (__rel_contract.deletedAt == null)))) throw new Error("Guard 2 failed");
    if (!((__rel_contract.status === "draft"))) throw new Error("Guard 3 failed");
    if (!((contractId === __draft.contractId))) throw new Error("Add tier contractId must match the seeded contract reference");
    if (!((((itemName).trim()).length > 0))) throw new Error("Item name is required");
    if (!(((minQuantity == null) || (minQuantity >= 0)))) throw new Error("Minimum quantity cannot be negative");
    if (!((unitPrice >= 0))) throw new Error("Unit price cannot be negative");
    const doc: Record<string, any> = {
      ...__draft,
      contractId: contractId,
      itemName: itemName,
      unit: ((unit != null) ? unit : "each"),
      minQuantity: ((minQuantity != null) ? minQuantity : 0),
      unitPrice: unitPrice,
      notes: notes,
      addedAt: Date.now(),
      version: 1,
    };
    const docId = await ctx.db.insert("vendorContractPriceTiers", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, vendorContractPriceTierId: docId, tenantId: doc.tenantId, vendorContractId: doc.contractId, itemName: doc.itemName, _subject: { entity: "VendorContractPriceTier", command: "add", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "VendorContractPriceTierAdded", entity: "VendorContractPriceTier", entityId: docId, payload: { vendorContractPriceTierId: docId, tenantId: doc.tenantId, vendorContractId: doc.contractId, itemName: doc.itemName }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "VendorContractPriceTier_createViaAdd", __result);
    }
    return __result;
  },
});

async function __runVendorContractPriceTierRemove(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("VendorContractPriceTier not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("VendorContractPriceTier not found");
    const __rel_contract = await __resolveRelation(ctx, "vendorContracts", [__auth.tenantId, doc.contractId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).contractId;
      ((doc as any) as any).contract = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!(checkRole(user, "procurementAccess"))) throw new Error("Procurement staff may read contract price tiers");
    if (!(checkRole(user, "procurementAccess"))) throw new Error("Procurement staff may write contract price tiers through commands");
    if (!(checkRole(user, "procurementAccess"))) throw new Error("Procurement staff may execute contract price tier commands");
    if (!((doc.addedAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(((__rel_contract != null) && (__rel_contract.status === "draft")))) throw new Error("Guard 2 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      deletedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, vendorContractPriceTierId: docId, tenantId: __after.tenantId, vendorContractId: __after.contractId, itemName: __after.itemName, _subject: { entity: "VendorContractPriceTier", command: "remove", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "VendorContractPriceTierRemoved", entity: "VendorContractPriceTier", entityId: docId, payload: { vendorContractPriceTierId: docId, tenantId: __after.tenantId, vendorContractId: __after.contractId, itemName: __after.itemName }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const VendorContractPriceTier_remove = mutation({
  args: {
    docId: v.id("vendorContractPriceTiers"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runVendorContractPriceTierRemove(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "VendorContractPriceTier_remove", __result);
    }
    return __result;
  },
});

async function __runVendorContractPriceTierUpdate(ctx: MutationCtx, { docId, itemName, unitPrice, unit, minQuantity, notes, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("VendorContractPriceTier not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("VendorContractPriceTier not found");
    const __rel_contract = await __resolveRelation(ctx, "vendorContracts", [__auth.tenantId, doc.contractId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).contractId;
      ((doc as any) as any).contract = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!(checkRole(user, "procurementAccess"))) throw new Error("Procurement staff may read contract price tiers");
    if (!(checkRole(user, "procurementAccess"))) throw new Error("Procurement staff may write contract price tiers through commands");
    if (!(checkRole(user, "procurementAccess"))) throw new Error("Procurement staff may execute contract price tier commands");
    if (!((doc.addedAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(((__rel_contract != null) && (__rel_contract.status === "draft")))) throw new Error("Guard 2 failed");
    if (!((((itemName).trim()).length > 0))) throw new Error("Item name is required");
    if (!(((minQuantity == null) || (minQuantity >= 0)))) throw new Error("Minimum quantity cannot be negative");
    if (!((unitPrice >= 0))) throw new Error("Unit price cannot be negative");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      itemName: itemName,
      unit: ((unit != null) ? unit : doc.unit),
      minQuantity: ((minQuantity != null) ? minQuantity : doc.minQuantity),
      unitPrice: unitPrice,
      notes: ((notes != null) ? notes : doc.notes),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, vendorContractPriceTierId: docId, tenantId: __after.tenantId, vendorContractId: __after.contractId, itemName: itemName, _subject: { entity: "VendorContractPriceTier", command: "update", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "VendorContractPriceTierUpdated", entity: "VendorContractPriceTier", entityId: docId, payload: { vendorContractPriceTierId: docId, tenantId: __after.tenantId, vendorContractId: __after.contractId, itemName: itemName }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const VendorContractPriceTier_update = mutation({
  args: {
    docId: v.id("vendorContractPriceTiers"),
    itemName: v.string(),
    unitPrice: v.number(),
    unit: v.optional(v.string()),
    minQuantity: v.optional(v.any()),
    notes: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runVendorContractPriceTierUpdate(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "VendorContractPriceTier_update", __result);
    }
    return __result;
  },
});

async function __runVendorOrderApprove(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("VendorOrder not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("VendorOrder not found");
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may read vendor orders");
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may write vendor orders through commands");
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may execute vendor order commands");
    if (!((doc.status === "pending_approval"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(checkRole(user, "manageAccess"))) throw new Error("Guard 2 failed");
    const previousStatus = doc.status;
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "submitted";
        const __allowed: Record<string, string[]> = { "draft": ["submitted", "pending_approval", "cancelled"], "pending_approval": ["submitted", "draft", "cancelled"], "submitted": ["confirmed", "cancelled"], "confirmed": ["partially_received", "received", "cancelled"], "partially_received": ["received", "cancelled"], "received": [], "cancelled": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "submitted",
      approvedAt: Date.now(),
      submittedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, vendorOrderId: docId, tenantId: __after.tenantId, vendorId: __after.vendorId, eventId: __after.eventId, totalAmount: __after.totalAmount, previousStatus: previousStatus, status: "submitted", _subject: { entity: "VendorOrder", command: "approve", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "VendorOrderApproved", entity: "VendorOrder", entityId: docId, payload: { vendorOrderId: docId, tenantId: __after.tenantId, vendorId: __after.vendorId, eventId: __after.eventId, totalAmount: __after.totalAmount }, createdAt: Date.now() });
    await ctx.db.insert("manifestEvents", { type: "VendorOrderSubmitted", entity: "VendorOrder", entityId: docId, payload: { vendorOrderId: docId, tenantId: __after.tenantId, vendorId: __after.vendorId, eventId: __after.eventId, previousStatus: previousStatus, status: "submitted", totalAmount: __after.totalAmount }, createdAt: Date.now() });
    // Reactions
    const fanRows0 = (await ctx.db.query("purchaseNeeds").withIndex("by_vendorOrderId", (q) => q.eq("vendorOrderId", payload.vendorOrderId)).collect()).filter((d) => (d as any).deletedAt == null);
    for (const __row of fanRows0) {
      const target = __row;
      await __runPurchaseNeedMarkDraftOrdered(ctx, { docId: (__row as any)._id } as any);
    }
    return { ...doc, ...updates };
}

export const VendorOrder_approve = mutation({
  args: {
    docId: v.id("vendorOrders"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runVendorOrderApprove(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "VendorOrder_approve", __result);
    }
    return __result;
  },
});

async function __runVendorOrderCancel(ctx: MutationCtx, { docId, reason, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("VendorOrder not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("VendorOrder not found");
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may read vendor orders");
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may write vendor orders through commands");
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may execute vendor order commands");
    if (!((((((doc.status === "draft") || (doc.status === "pending_approval")) || (doc.status === "submitted")) || (doc.status === "confirmed")) || (doc.status === "partially_received")))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((checkRole(user, "inventoryManageAccess") || (((doc.status === "draft") || (doc.status === "pending_approval")) && checkRole(user, "procurementAccess"))))) throw new Error("Guard 2 failed");
    if (!((((reason).trim()).length > 0))) throw new Error("Cancellation reason is required");
    const previousStatus = doc.status;
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "cancelled";
        const __allowed: Record<string, string[]> = { "draft": ["submitted", "pending_approval", "cancelled"], "pending_approval": ["submitted", "draft", "cancelled"], "submitted": ["confirmed", "cancelled"], "confirmed": ["partially_received", "received", "cancelled"], "partially_received": ["received", "cancelled"], "received": [], "cancelled": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "cancelled",
      cancelledAt: Date.now(),
      cancellationReason: reason,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, vendorOrderId: docId, tenantId: __after.tenantId, vendorId: __after.vendorId, eventId: __after.eventId, previousStatus: previousStatus, status: "cancelled", totalAmount: __after.totalAmount, reason: reason, _subject: { entity: "VendorOrder", command: "cancel", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "VendorOrderCancelled", entity: "VendorOrder", entityId: docId, payload: { vendorOrderId: docId, tenantId: __after.tenantId, vendorId: __after.vendorId, eventId: __after.eventId, previousStatus: previousStatus, status: "cancelled", totalAmount: __after.totalAmount, reason: reason }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const VendorOrder_cancel = mutation({
  args: {
    docId: v.id("vendorOrders"),
    reason: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runVendorOrderCancel(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "VendorOrder_cancel", __result);
    }
    return __result;
  },
});

async function __runVendorOrderConfirm(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("VendorOrder not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("VendorOrder not found");
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may read vendor orders");
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may write vendor orders through commands");
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may execute vendor order commands");
    if (!((doc.status === "submitted"))) throw new Error("Guard 0 failed");
    if (!((doc.submittedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    const previousStatus = doc.status;
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "confirmed";
        const __allowed: Record<string, string[]> = { "draft": ["submitted", "pending_approval", "cancelled"], "pending_approval": ["submitted", "draft", "cancelled"], "submitted": ["confirmed", "cancelled"], "confirmed": ["partially_received", "received", "cancelled"], "partially_received": ["received", "cancelled"], "received": [], "cancelled": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "confirmed",
      confirmedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, vendorOrderId: docId, tenantId: __after.tenantId, vendorId: __after.vendorId, eventId: __after.eventId, previousStatus: previousStatus, status: "confirmed", totalAmount: __after.totalAmount, _subject: { entity: "VendorOrder", command: "confirm", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "VendorOrderConfirmed", entity: "VendorOrder", entityId: docId, payload: { vendorOrderId: docId, tenantId: __after.tenantId, vendorId: __after.vendorId, eventId: __after.eventId, previousStatus: previousStatus, status: "confirmed", totalAmount: __after.totalAmount }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const VendorOrder_confirm = mutation({
  args: {
    docId: v.id("vendorOrders"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runVendorOrderConfirm(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "VendorOrder_confirm", __result);
    }
    return __result;
  },
});

async function __runVendorOrderEnsureWeeklyDraft(ctx: MutationCtx, { docId, vendorId, sourceRangeStart, sourceRangeEnd, purchaseNeedId, ingredientDemandId, ingredientId, requiredQuantity, unit, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("VendorOrder not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("VendorOrder not found");
    const __rel_vendor = await __resolveRelation(ctx, "vendors", [__auth.tenantId, doc.vendorId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).vendorId;
      ((doc as any) as any).vendor = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may read vendor orders");
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may write vendor orders through commands");
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may execute vendor order commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.status === "draft"))) throw new Error("Guard 1 failed");
    if (!((((__rel_vendor != null) && (__rel_vendor.status === "active")) && (__rel_vendor.onboardedAt != null)))) throw new Error("Guard 2 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      vendorId: vendorId,
      sourceRangeStart: sourceRangeStart,
      sourceRangeEnd: sourceRangeEnd,
      openedAt: ((doc.openedAt == null) ? Date.now() : doc.openedAt),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, vendorOrderId: docId, tenantId: __after.tenantId, vendorId: vendorId, purchasingWeekStart: sourceRangeStart, purchaseNeedId: purchaseNeedId, ingredientDemandId: ingredientDemandId, ingredientId: ingredientId, requiredQuantity: requiredQuantity, unit: unit, _subject: { entity: "VendorOrder", command: "ensureWeeklyDraft", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "VendorOrderWeeklyDraftEnsured", entity: "VendorOrder", entityId: docId, payload: { vendorOrderId: docId, tenantId: __after.tenantId, vendorId: vendorId, purchasingWeekStart: sourceRangeStart, purchaseNeedId: purchaseNeedId, ingredientDemandId: ingredientDemandId, ingredientId: ingredientId, requiredQuantity: requiredQuantity, unit: unit }, createdAt: Date.now() });
    // Reactions
    const __agg0_rows = await ctx.db.query("purchaseNeeds").withIndex("by_ingredientId", (q) => q.eq("ingredientId", payload.ingredientId)).collect();
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    const __agg0_rowsf = __agg0_rows.filter((d) => (d as any).purchasingWeekStart === payload.purchasingWeekStart).filter((d) => (d as any).status === "open").filter((d) => (d as any).deletedAt == null).filter((d) => (d as any).tenantId === __tenant);
    const __agg0 = __agg0_rowsf.reduce((acc, d) => { const n = Number((d as any).requiredQuantity); return acc + (Number.isFinite(n) ? n : 0); }, 0);
    const __agg1_rows = await ctx.db.query("inventoryItems").withIndex("by_ingredientId", (q) => q.eq("ingredientId", payload.ingredientId)).collect();
    const __agg1_rowsf = __agg1_rows.filter((d) => (d as any).deletedAt == null).filter((d) => (d as any).tenantId === __tenant);
    const __agg1 = __agg1_rowsf.reduce((acc, d) => { const n = Number((d as any).quantityOnHand); return acc + (Number.isFinite(n) ? n : 0); }, 0);
    const __match0_raw = await ctx.db.query("vendorOrderLines").withIndex("by_vendorOrderId", (q) => q.eq("vendorOrderId", payload.vendorOrderId)).collect();
    const __match0_rows = __match0_raw.filter((d) => (d as any).vendorOrderId === payload.vendorOrderId && (d as any).ingredientId === payload.ingredientId && (d as any).unit === payload.unit && (d as any).deletedAt == null).sort((a, b) => String((a as any)._id).localeCompare(String((b as any)._id)));
    const __match0_id = __match0_rows.length > 0 ? (__match0_rows[0] as any)._id : null;
    if (__match0_id) {
      await __runVendorOrderLineEnsureWeeklyLine(ctx, { docId: __match0_id, vendorOrderId: payload.vendorOrderId, ingredientId: payload.ingredientId, weekNeed: __agg0, onHand: __agg1, contributionQuantity: payload.requiredQuantity, unit: payload.unit, unitCost: 0, purchaseNeedId: payload.purchaseNeedId, ingredientDemandId: payload.ingredientDemandId } as any);
    } else {
      const __elseArgs: Record<string, any> = { vendorOrderId: payload.vendorOrderId, ingredientId: payload.ingredientId, weekNeed: __agg0, onHand: __agg1, contributionQuantity: payload.requiredQuantity, unit: payload.unit, unitCost: 0, purchaseNeedId: payload.purchaseNeedId, ingredientDemandId: payload.ingredientDemandId };
      const __elseDoc: Record<string, any> = {
        tenantId: __auth.tenantId,
        orderedQuantity: 0,
        receivedQuantity: 0,
        unit: "each",
        unitCost: 0,
        status: "pending",
        createdAt: Date.now(),
        updatedAt: Date.now(),
        version: 0,
      };
      for (const __k of ["deletedAt","vendorOrderId","ingredientId","ingredientDemandId","locationId","orderedQuantity","receivedQuantity","unit","unitCost","discrepancyQuantity","discrepancyNotes","status","addedAt","completedAt","cancelledAt","createdAt","updatedAt"] as string[]) {
        if (__elseArgs[__k] !== undefined) __elseDoc[__k] = __elseArgs[__k];
      }
      const __elseId = await ctx.db.insert("vendorOrderLines", __elseDoc as any);
      await __runVendorOrderLineEnsureWeeklyLine(ctx, { docId: __elseId, ...__elseArgs } as any);
    }
    return { ...doc, ...updates };
}

export const VendorOrder_ensureWeeklyDraft = mutation({
  args: {
    docId: v.id("vendorOrders"),
    vendorId: v.string(),
    sourceRangeStart: v.number(),
    sourceRangeEnd: v.number(),
    purchaseNeedId: v.string(),
    ingredientDemandId: v.string(),
    ingredientId: v.string(),
    requiredQuantity: v.number(),
    unit: v.any(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runVendorOrderEnsureWeeklyDraft(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "VendorOrder_ensureWeeklyDraft", __result);
    }
    return __result;
  },
});

async function __runVendorOrderMarkPartiallyReceived(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("VendorOrder not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("VendorOrder not found");
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may read vendor orders");
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may write vendor orders through commands");
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may execute vendor order commands");
    if (!((doc.status === "confirmed"))) throw new Error("Guard 0 failed");
    if (!((doc.confirmedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    const previousStatus = doc.status;
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "partially_received";
        const __allowed: Record<string, string[]> = { "draft": ["submitted", "pending_approval", "cancelled"], "pending_approval": ["submitted", "draft", "cancelled"], "submitted": ["confirmed", "cancelled"], "confirmed": ["partially_received", "received", "cancelled"], "partially_received": ["received", "cancelled"], "received": [], "cancelled": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "partially_received",
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, vendorOrderId: docId, tenantId: __after.tenantId, vendorId: __after.vendorId, eventId: __after.eventId, previousStatus: previousStatus, status: "partially_received", totalAmount: __after.totalAmount, _subject: { entity: "VendorOrder", command: "markPartiallyReceived", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "VendorOrderPartiallyReceived", entity: "VendorOrder", entityId: docId, payload: { vendorOrderId: docId, tenantId: __after.tenantId, vendorId: __after.vendorId, eventId: __after.eventId, previousStatus: previousStatus, status: "partially_received", totalAmount: __after.totalAmount }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const VendorOrder_markPartiallyReceived = mutation({
  args: {
    docId: v.id("vendorOrders"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runVendorOrderMarkPartiallyReceived(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "VendorOrder_markPartiallyReceived", __result);
    }
    return __result;
  },
});

async function __runVendorOrderMarkReceived(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("VendorOrder not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("VendorOrder not found");
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may read vendor orders");
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may write vendor orders through commands");
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may execute vendor order commands");
    if (!(((doc.status === "confirmed") || (doc.status === "partially_received")))) throw new Error("Guard 0 failed");
    if (!((doc.confirmedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    const previousStatus = doc.status;
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "received";
        const __allowed: Record<string, string[]> = { "draft": ["submitted", "pending_approval", "cancelled"], "pending_approval": ["submitted", "draft", "cancelled"], "submitted": ["confirmed", "cancelled"], "confirmed": ["partially_received", "received", "cancelled"], "partially_received": ["received", "cancelled"], "received": [], "cancelled": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "received",
      receivedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, vendorOrderId: docId, tenantId: __after.tenantId, vendorId: __after.vendorId, eventId: __after.eventId, previousStatus: previousStatus, status: "received", totalAmount: __after.totalAmount, _subject: { entity: "VendorOrder", command: "markReceived", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "VendorOrderReceived", entity: "VendorOrder", entityId: docId, payload: { vendorOrderId: docId, tenantId: __after.tenantId, vendorId: __after.vendorId, eventId: __after.eventId, previousStatus: previousStatus, status: "received", totalAmount: __after.totalAmount }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const VendorOrder_markReceived = mutation({
  args: {
    docId: v.id("vendorOrders"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runVendorOrderMarkReceived(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "VendorOrder_markReceived", __result);
    }
    return __result;
  },
});

async function __runVendorOrderOpen(ctx: MutationCtx, { docId, vendorId, eventId, sourceRangeStart, sourceRangeEnd, orderNumber, notes, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("VendorOrder not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("VendorOrder not found");
    const __rel_vendor = await __resolveRelation(ctx, "vendors", [__auth.tenantId, doc.vendorId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).vendorId;
      ((doc as any) as any).vendor = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may read vendor orders");
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may write vendor orders through commands");
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may execute vendor order commands");
    if (!((doc.openedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.status === "draft"))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((((__rel_vendor != null) && (__rel_vendor.status === "active")) && (__rel_vendor.onboardedAt != null)))) throw new Error("Guard 3 failed");
    if (!((vendorId === doc.vendorId))) throw new Error("Open vendorId must match the seeded vendor reference");
    if (!((((eventId == null) || (doc.eventId == null)) || (eventId === doc.eventId)))) throw new Error("Open eventId must match the seeded event reference when provided");
    if (!((((sourceRangeStart == null) && (sourceRangeEnd == null)) || ((sourceRangeStart != null) && (sourceRangeEnd != null))))) throw new Error("Open source range requires both a start and end");
    if (!((((sourceRangeStart == null) || (sourceRangeEnd == null)) || (sourceRangeStart <= sourceRangeEnd)))) throw new Error("Open source range end must not precede its start");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      vendorId: vendorId,
      eventId: ((eventId != null) ? eventId : doc.eventId),
      sourceRangeStart: ((sourceRangeStart != null) ? sourceRangeStart : doc.sourceRangeStart),
      sourceRangeEnd: ((sourceRangeEnd != null) ? sourceRangeEnd : doc.sourceRangeEnd),
      orderNumber: orderNumber,
      notes: notes,
      openedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, vendorOrderId: docId, tenantId: __after.tenantId, vendorId: vendorId, eventId: ((eventId != null) ? eventId : __after.eventId), sourceRangeStart: ((sourceRangeStart != null) ? sourceRangeStart : __after.sourceRangeStart), sourceRangeEnd: ((sourceRangeEnd != null) ? sourceRangeEnd : __after.sourceRangeEnd), orderNumber: orderNumber, status: "draft", _subject: { entity: "VendorOrder", command: "open", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "VendorOrderOpened", entity: "VendorOrder", entityId: docId, payload: { vendorOrderId: docId, tenantId: __after.tenantId, vendorId: vendorId, eventId: ((eventId != null) ? eventId : __after.eventId), sourceRangeStart: ((sourceRangeStart != null) ? sourceRangeStart : __after.sourceRangeStart), sourceRangeEnd: ((sourceRangeEnd != null) ? sourceRangeEnd : __after.sourceRangeEnd), orderNumber: orderNumber, status: "draft" }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const VendorOrder_open = mutation({
  args: {
    docId: v.id("vendorOrders"),
    vendorId: v.string(),
    eventId: v.optional(v.string()),
    sourceRangeStart: v.optional(v.number()),
    sourceRangeEnd: v.optional(v.number()),
    orderNumber: v.optional(v.string()),
    notes: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runVendorOrderOpen(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "VendorOrder_open", __result);
    }
    return __result;
  },
});

export const VendorOrder_createViaOpen = mutation({
  args: {
    vendorId: v.string(),
    eventId: v.optional(v.string()),
    sourceRangeStart: v.optional(v.number()),
    sourceRangeEnd: v.optional(v.number()),
    orderNumber: v.optional(v.string()),
    notes: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { vendorId, eventId, sourceRangeStart, sourceRangeEnd, orderNumber, notes } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      subtotal: 0,
      taxAmount: 0,
      shippingAmount: 0,
      totalAmount: 0,
      status: "draft",
      createdAt: Date.now(),
      updatedAt: Date.now(),
      eventId: args.eventId,
      notes: args.notes,
      orderNumber: args.orderNumber,
      sourceRangeEnd: args.sourceRangeEnd,
      sourceRangeStart: args.sourceRangeStart,
      vendorId: args.vendorId
    };
    const __rel_vendor = await __resolveRelation(ctx, "vendors", [__auth.tenantId, __draft.vendorId], ["tenantId","id"], "tenantId", __auth.tenantId);
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may read vendor orders");
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may write vendor orders through commands");
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may execute vendor order commands");
    if (!((__draft.openedAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.status === "draft"))) throw new Error("Guard 1 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((((__rel_vendor != null) && (__rel_vendor.status === "active")) && (__rel_vendor.onboardedAt != null)))) throw new Error("Guard 3 failed");
    if (!((vendorId === __draft.vendorId))) throw new Error("Open vendorId must match the seeded vendor reference");
    if (!((((eventId == null) || (__draft.eventId == null)) || (eventId === __draft.eventId)))) throw new Error("Open eventId must match the seeded event reference when provided");
    if (!((((sourceRangeStart == null) && (sourceRangeEnd == null)) || ((sourceRangeStart != null) && (sourceRangeEnd != null))))) throw new Error("Open source range requires both a start and end");
    if (!((((sourceRangeStart == null) || (sourceRangeEnd == null)) || (sourceRangeStart <= sourceRangeEnd)))) throw new Error("Open source range end must not precede its start");
    const doc: Record<string, any> = {
      ...__draft,
      vendorId: vendorId,
      eventId: ((eventId != null) ? eventId : __draft.eventId),
      sourceRangeStart: ((sourceRangeStart != null) ? sourceRangeStart : __draft.sourceRangeStart),
      sourceRangeEnd: ((sourceRangeEnd != null) ? sourceRangeEnd : __draft.sourceRangeEnd),
      orderNumber: orderNumber,
      notes: notes,
      openedAt: Date.now(),
      version: 1,
    };
    const docId = await ctx.db.insert("vendorOrders", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, vendorOrderId: docId, tenantId: doc.tenantId, vendorId: doc.vendorId, eventId: ((doc.eventId != null) ? doc.eventId : doc.eventId), sourceRangeStart: ((doc.sourceRangeStart != null) ? doc.sourceRangeStart : doc.sourceRangeStart), sourceRangeEnd: ((doc.sourceRangeEnd != null) ? doc.sourceRangeEnd : doc.sourceRangeEnd), orderNumber: doc.orderNumber, status: "draft", _subject: { entity: "VendorOrder", command: "open", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "VendorOrderOpened", entity: "VendorOrder", entityId: docId, payload: { vendorOrderId: docId, tenantId: doc.tenantId, vendorId: doc.vendorId, eventId: ((doc.eventId != null) ? doc.eventId : doc.eventId), sourceRangeStart: ((doc.sourceRangeStart != null) ? doc.sourceRangeStart : doc.sourceRangeStart), sourceRangeEnd: ((doc.sourceRangeEnd != null) ? doc.sourceRangeEnd : doc.sourceRangeEnd), orderNumber: doc.orderNumber, status: "draft" }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "VendorOrder_createViaOpen", __result);
    }
    return __result;
  },
});

async function __runVendorOrderRequestChanges(ctx: MutationCtx, { docId, notes, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("VendorOrder not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("VendorOrder not found");
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may read vendor orders");
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may write vendor orders through commands");
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may execute vendor order commands");
    if (!((doc.status === "pending_approval"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(checkRole(user, "manageAccess"))) throw new Error("Guard 2 failed");
    if (!((((notes).trim()).length > 0))) throw new Error("Modification request notes are required");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "draft";
        const __allowed: Record<string, string[]> = { "draft": ["submitted", "pending_approval", "cancelled"], "pending_approval": ["submitted", "draft", "cancelled"], "submitted": ["confirmed", "cancelled"], "confirmed": ["partially_received", "received", "cancelled"], "partially_received": ["received", "cancelled"], "received": [], "cancelled": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "draft",
      approvalNotes: notes,
      approvalRequestedAt: null,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, vendorOrderId: docId, tenantId: __after.tenantId, vendorId: __after.vendorId, eventId: __after.eventId, totalAmount: __after.totalAmount, notes: notes, _subject: { entity: "VendorOrder", command: "requestChanges", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "VendorOrderChangesRequested", entity: "VendorOrder", entityId: docId, payload: { vendorOrderId: docId, tenantId: __after.tenantId, vendorId: __after.vendorId, eventId: __after.eventId, totalAmount: __after.totalAmount, notes: notes }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const VendorOrder_requestChanges = mutation({
  args: {
    docId: v.id("vendorOrders"),
    notes: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runVendorOrderRequestChanges(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "VendorOrder_requestChanges", __result);
    }
    return __result;
  },
});

async function __runVendorOrderSubmit(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("VendorOrder not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("VendorOrder not found");
    const __rel_purchasingConfig = await __resolveRelation(ctx, "weeklyPurchasingConfigs", [__auth.tenantId], ["tenantId"], "tenantId", __auth.tenantId);
    {
      const __lookup = ((doc as any) as any).tenantId;
      ((doc as any) as any).purchasingConfig = __lookup != null
        ? await ctx.db.query("weeklyPurchasingConfigs").withIndex("by_tenantId", (q: any) => q.eq("tenantId", __lookup)).first()
        : null;
    }
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may read vendor orders");
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may write vendor orders through commands");
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may execute vendor order commands");
    if (!((doc.openedAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.status === "draft"))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((((__rel_purchasingConfig == null) || (__rel_purchasingConfig.orderApprovalThresholdAmount == null)) || (doc.totalAmount <= __rel_purchasingConfig.orderApprovalThresholdAmount)))) throw new Error("Orders above the spend approval threshold need manager approval — use submit for approval");
    const previousStatus = doc.status;
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "submitted";
        const __allowed: Record<string, string[]> = { "draft": ["submitted", "pending_approval", "cancelled"], "pending_approval": ["submitted", "draft", "cancelled"], "submitted": ["confirmed", "cancelled"], "confirmed": ["partially_received", "received", "cancelled"], "partially_received": ["received", "cancelled"], "received": [], "cancelled": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "submitted",
      submittedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, vendorOrderId: docId, tenantId: __after.tenantId, vendorId: __after.vendorId, eventId: __after.eventId, previousStatus: previousStatus, status: "submitted", totalAmount: __after.totalAmount, _subject: { entity: "VendorOrder", command: "submit", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "VendorOrderSubmitted", entity: "VendorOrder", entityId: docId, payload: { vendorOrderId: docId, tenantId: __after.tenantId, vendorId: __after.vendorId, eventId: __after.eventId, previousStatus: previousStatus, status: "submitted", totalAmount: __after.totalAmount }, createdAt: Date.now() });
    // Reactions
    const fanRows0 = (await ctx.db.query("purchaseNeeds").withIndex("by_vendorOrderId", (q) => q.eq("vendorOrderId", payload.vendorOrderId)).collect()).filter((d) => (d as any).deletedAt == null);
    for (const __row of fanRows0) {
      const target = __row;
      await __runPurchaseNeedMarkDraftOrdered(ctx, { docId: (__row as any)._id } as any);
    }
    return { ...doc, ...updates };
}

export const VendorOrder_submit = mutation({
  args: {
    docId: v.id("vendorOrders"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runVendorOrderSubmit(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "VendorOrder_submit", __result);
    }
    return __result;
  },
});

async function __runVendorOrderSubmitForApproval(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("VendorOrder not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("VendorOrder not found");
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may read vendor orders");
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may write vendor orders through commands");
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may execute vendor order commands");
    if (!((doc.openedAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.status === "draft"))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "pending_approval";
        const __allowed: Record<string, string[]> = { "draft": ["submitted", "pending_approval", "cancelled"], "pending_approval": ["submitted", "draft", "cancelled"], "submitted": ["confirmed", "cancelled"], "confirmed": ["partially_received", "received", "cancelled"], "partially_received": ["received", "cancelled"], "received": [], "cancelled": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "pending_approval",
      approvalRequestedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, vendorOrderId: docId, tenantId: __after.tenantId, vendorId: __after.vendorId, eventId: __after.eventId, totalAmount: __after.totalAmount, _subject: { entity: "VendorOrder", command: "submitForApproval", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "VendorOrderApprovalRequested", entity: "VendorOrder", entityId: docId, payload: { vendorOrderId: docId, tenantId: __after.tenantId, vendorId: __after.vendorId, eventId: __after.eventId, totalAmount: __after.totalAmount }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const VendorOrder_submitForApproval = mutation({
  args: {
    docId: v.id("vendorOrders"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runVendorOrderSubmitForApproval(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "VendorOrder_submitForApproval", __result);
    }
    return __result;
  },
});

async function __runVendorOrderUpdateTotals(ctx: MutationCtx, { docId, subtotal, taxAmount, shippingAmount, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("VendorOrder not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("VendorOrder not found");
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may read vendor orders");
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may write vendor orders through commands");
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may execute vendor order commands");
    if (!((doc.openedAt != null))) throw new Error("Guard 0 failed");
    if (!(((doc.status === "draft") || (doc.status === "submitted")))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((((subtotal >= 0) && (taxAmount >= 0)) && (shippingAmount >= 0)))) throw new Error("Order money amounts cannot be negative");
    const previousTotal = doc.totalAmount;
    const nextTotal = ((subtotal + taxAmount) + shippingAmount);
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      subtotal: subtotal,
      taxAmount: taxAmount,
      shippingAmount: shippingAmount,
      totalAmount: nextTotal,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, vendorOrderId: docId, tenantId: __after.tenantId, vendorId: __after.vendorId, eventId: __after.eventId, previousTotalAmount: previousTotal, subtotal: subtotal, taxAmount: taxAmount, shippingAmount: shippingAmount, totalAmount: nextTotal, _subject: { entity: "VendorOrder", command: "updateTotals", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "VendorOrderTotalsUpdated", entity: "VendorOrder", entityId: docId, payload: { vendorOrderId: docId, tenantId: __after.tenantId, vendorId: __after.vendorId, eventId: __after.eventId, previousTotalAmount: previousTotal, subtotal: subtotal, taxAmount: taxAmount, shippingAmount: shippingAmount, totalAmount: nextTotal }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const VendorOrder_updateTotals = mutation({
  args: {
    docId: v.id("vendorOrders"),
    subtotal: v.number(),
    taxAmount: v.number(),
    shippingAmount: v.number(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runVendorOrderUpdateTotals(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "VendorOrder_updateTotals", __result);
    }
    return __result;
  },
});

async function __runVendorOrderLineAddLine(ctx: MutationCtx, { docId, vendorOrderId, ingredientId, orderedQuantity, unit, unitCost, ingredientDemandId, locationId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("VendorOrderLine not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("VendorOrderLine not found");
    const __rel_vendorOrder = await __resolveRelation(ctx, "vendorOrders", [__auth.tenantId, doc.vendorOrderId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_ingredient = await __resolveRelation(ctx, "ingredients", [__auth.tenantId, doc.ingredientId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).vendorOrderId;
      ((doc as any) as any).vendorOrder = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    {
      const __fk = ((doc as any) as any).ingredientId;
      ((doc as any) as any).ingredient = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may read vendor order lines");
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may write vendor order lines through commands");
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may execute vendor order line commands");
    if (!((doc.addedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.status === "pending"))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((((__rel_vendorOrder != null) && (__rel_vendorOrder.status === "draft")) && (__rel_vendorOrder.openedAt != null)))) throw new Error("Guard 3 failed");
    if (!(((__rel_ingredient != null) && (__rel_ingredient.status === "active")))) throw new Error("Guard 4 failed");
    if (!((vendorOrderId === doc.vendorOrderId))) throw new Error("Add line vendorOrderId must match the seeded order reference");
    if (!((ingredientId === doc.ingredientId))) throw new Error("Add line ingredientId must match the seeded ingredient reference");
    if (!((((ingredientDemandId == null) || (doc.ingredientDemandId == null)) || (ingredientDemandId === doc.ingredientDemandId)))) throw new Error("Add line ingredientDemandId must match the seeded demand reference when provided");
    if (!((((locationId == null) || (doc.locationId == null)) || (locationId === doc.locationId)))) throw new Error("Add line locationId must match the seeded location reference when provided");
    if (!((orderedQuantity > 0))) throw new Error("Ordered quantity must be positive");
    if (!((unitCost >= 0))) throw new Error("Unit cost cannot be negative");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "added";
        const __allowed: Record<string, string[]> = { "pending": ["added", "cancelled"], "added": ["receiving", "complete", "cancelled"], "receiving": ["complete", "cancelled"], "complete": [], "cancelled": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      vendorOrderId: vendorOrderId,
      ingredientId: ingredientId,
      ingredientDemandId: ((ingredientDemandId != null) ? ingredientDemandId : doc.ingredientDemandId),
      locationId: ((locationId != null) ? locationId : doc.locationId),
      orderedQuantity: orderedQuantity,
      unit: unit,
      unitCost: unitCost,
      status: "added",
      addedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const __rel_vendorOrder__post = await __resolveRelation(ctx, "vendorOrders", [__auth.tenantId, __after.vendorOrderId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, vendorOrderLineId: docId, tenantId: __after.tenantId, vendorOrderId: vendorOrderId, ingredientId: ingredientId, ingredientDemandId: ((ingredientDemandId != null) ? ingredientDemandId : __after.ingredientDemandId), eventId: __rel_vendorOrder__post.eventId, orderedQuantity: orderedQuantity, unit: unit, unitCost: unitCost, _subject: { entity: "VendorOrderLine", command: "addLine", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "VendorOrderLineAdded", entity: "VendorOrderLine", entityId: docId, payload: { vendorOrderLineId: docId, tenantId: __after.tenantId, vendorOrderId: vendorOrderId, ingredientId: ingredientId, ingredientDemandId: ((ingredientDemandId != null) ? ingredientDemandId : __after.ingredientDemandId), eventId: __rel_vendorOrder__post.eventId, orderedQuantity: orderedQuantity, unit: unit, unitCost: unitCost }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const VendorOrderLine_addLine = mutation({
  args: {
    docId: v.id("vendorOrderLines"),
    vendorOrderId: v.string(),
    ingredientId: v.string(),
    orderedQuantity: v.number(),
    unit: v.any(),
    unitCost: v.number(),
    ingredientDemandId: v.optional(v.string()),
    locationId: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runVendorOrderLineAddLine(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "VendorOrderLine_addLine", __result);
    }
    return __result;
  },
});

export const VendorOrderLine_createViaAddLine = mutation({
  args: {
    vendorOrderId: v.string(),
    ingredientId: v.string(),
    orderedQuantity: v.number(),
    unit: v.any(),
    unitCost: v.number(),
    ingredientDemandId: v.optional(v.string()),
    locationId: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { vendorOrderId, ingredientId, orderedQuantity, unit, unitCost, ingredientDemandId, locationId } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      receivedQuantity: 0,
      createdAt: Date.now(),
      updatedAt: Date.now(),
      status: "pending",
      ingredientDemandId: args.ingredientDemandId,
      ingredientId: args.ingredientId,
      locationId: args.locationId,
      orderedQuantity: args.orderedQuantity,
      unit: args.unit,
      unitCost: args.unitCost,
      vendorOrderId: args.vendorOrderId
    };
    const __rel_vendorOrder = await __resolveRelation(ctx, "vendorOrders", [__auth.tenantId, __draft.vendorOrderId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_ingredient = await __resolveRelation(ctx, "ingredients", [__auth.tenantId, __draft.ingredientId], ["tenantId","id"], "tenantId", __auth.tenantId);
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may read vendor order lines");
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may write vendor order lines through commands");
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may execute vendor order line commands");
    if (!((__draft.addedAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.status === "pending"))) throw new Error("Guard 1 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((((__rel_vendorOrder != null) && (__rel_vendorOrder.status === "draft")) && (__rel_vendorOrder.openedAt != null)))) throw new Error("Guard 3 failed");
    if (!(((__rel_ingredient != null) && (__rel_ingredient.status === "active")))) throw new Error("Guard 4 failed");
    if (!((vendorOrderId === __draft.vendorOrderId))) throw new Error("Add line vendorOrderId must match the seeded order reference");
    if (!((ingredientId === __draft.ingredientId))) throw new Error("Add line ingredientId must match the seeded ingredient reference");
    if (!((((ingredientDemandId == null) || (__draft.ingredientDemandId == null)) || (ingredientDemandId === __draft.ingredientDemandId)))) throw new Error("Add line ingredientDemandId must match the seeded demand reference when provided");
    if (!((((locationId == null) || (__draft.locationId == null)) || (locationId === __draft.locationId)))) throw new Error("Add line locationId must match the seeded location reference when provided");
    if (!((orderedQuantity > 0))) throw new Error("Ordered quantity must be positive");
    if (!((unitCost >= 0))) throw new Error("Unit cost cannot be negative");
    const doc: Record<string, any> = {
      ...__draft,
      vendorOrderId: vendorOrderId,
      ingredientId: ingredientId,
      ingredientDemandId: ((ingredientDemandId != null) ? ingredientDemandId : __draft.ingredientDemandId),
      locationId: ((locationId != null) ? locationId : __draft.locationId),
      orderedQuantity: orderedQuantity,
      unit: unit,
      unitCost: unitCost,
      status: "added",
      addedAt: Date.now(),
      version: 1,
    };
    const docId = await ctx.db.insert("vendorOrderLines", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, vendorOrderLineId: docId, tenantId: doc.tenantId, vendorOrderId: doc.vendorOrderId, ingredientId: doc.ingredientId, ingredientDemandId: ((doc.ingredientDemandId != null) ? doc.ingredientDemandId : doc.ingredientDemandId), eventId: __rel_vendorOrder.eventId, orderedQuantity: doc.orderedQuantity, unit: doc.unit, unitCost: doc.unitCost, _subject: { entity: "VendorOrderLine", command: "addLine", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "VendorOrderLineAdded", entity: "VendorOrderLine", entityId: docId, payload: { vendorOrderLineId: docId, tenantId: doc.tenantId, vendorOrderId: doc.vendorOrderId, ingredientId: doc.ingredientId, ingredientDemandId: ((doc.ingredientDemandId != null) ? doc.ingredientDemandId : doc.ingredientDemandId), eventId: __rel_vendorOrder.eventId, orderedQuantity: doc.orderedQuantity, unit: doc.unit, unitCost: doc.unitCost }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "VendorOrderLine_createViaAddLine", __result);
    }
    return __result;
  },
});

async function __runVendorOrderLineCancelLine(ctx: MutationCtx, { docId, reason, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("VendorOrderLine not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("VendorOrderLine not found");
    const __rel_vendorOrder = await __resolveRelation(ctx, "vendorOrders", [__auth.tenantId, doc.vendorOrderId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).vendorOrderId;
      ((doc as any) as any).vendorOrder = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may read vendor order lines");
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may write vendor order lines through commands");
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may execute vendor order line commands");
    if (!((doc.addedAt != null))) throw new Error("Guard 0 failed");
    if (!(((doc.status === "added") || (doc.status === "receiving")))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!(((__rel_vendorOrder != null) && (((__rel_vendorOrder.status === "draft") || (__rel_vendorOrder.status === "submitted")) || (__rel_vendorOrder.status === "confirmed"))))) throw new Error("Guard 3 failed");
    if (!((checkRole(user, "inventoryManageAccess") || ((__rel_vendorOrder.status === "draft") && checkRole(user, "procurementAccess"))))) throw new Error("Guard 4 failed");
    if (!((((reason).trim()).length > 0))) throw new Error("Cancellation reason is required");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "cancelled";
        const __allowed: Record<string, string[]> = { "pending": ["added", "cancelled"], "added": ["receiving", "complete", "cancelled"], "receiving": ["complete", "cancelled"], "complete": [], "cancelled": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "cancelled",
      cancelledAt: Date.now(),
      deletedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, vendorOrderLineId: docId, tenantId: __after.tenantId, vendorOrderId: __after.vendorOrderId, ingredientId: __after.ingredientId, ingredientDemandId: __after.ingredientDemandId, eventId: __rel_vendorOrder.eventId, orderedQuantity: __after.orderedQuantity, receivedQuantity: __after.receivedQuantity, unit: __after.unit, reason: reason, _subject: { entity: "VendorOrderLine", command: "cancelLine", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "VendorOrderLineCancelled", entity: "VendorOrderLine", entityId: docId, payload: { vendorOrderLineId: docId, tenantId: __after.tenantId, vendorOrderId: __after.vendorOrderId, ingredientId: __after.ingredientId, ingredientDemandId: __after.ingredientDemandId, eventId: __rel_vendorOrder.eventId, orderedQuantity: __after.orderedQuantity, receivedQuantity: __after.receivedQuantity, unit: __after.unit, reason: reason }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const VendorOrderLine_cancelLine = mutation({
  args: {
    docId: v.id("vendorOrderLines"),
    reason: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runVendorOrderLineCancelLine(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "VendorOrderLine_cancelLine", __result);
    }
    return __result;
  },
});

async function __runVendorOrderLineEnsureWeeklyLine(ctx: MutationCtx, { docId, vendorOrderId, ingredientId, weekNeed, onHand, contributionQuantity, unit, unitCost, purchaseNeedId, ingredientDemandId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("VendorOrderLine not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("VendorOrderLine not found");
    const __rel_vendorOrder = await __resolveRelation(ctx, "vendorOrders", [__auth.tenantId, doc.vendorOrderId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_ingredient = await __resolveRelation(ctx, "ingredients", [__auth.tenantId, doc.ingredientId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).vendorOrderId;
      ((doc as any) as any).vendorOrder = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    {
      const __fk = ((doc as any) as any).ingredientId;
      ((doc as any) as any).ingredient = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may read vendor order lines");
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may write vendor order lines through commands");
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may execute vendor order line commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!(((__rel_vendorOrder != null) && (__rel_vendorOrder.status === "draft")))) throw new Error("Guard 1 failed");
    if (!(((__rel_ingredient != null) && (__rel_ingredient.status === "active")))) throw new Error("Guard 2 failed");
    if (!((contributionQuantity > 0))) throw new Error("Contribution quantity must be positive");
    const orderQty = Math.max(0.0001, (weekNeed - onHand));
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = String(((doc.addedAt == null) ? "added" : doc.status));
        const __allowed: Record<string, string[]> = { "pending": ["added", "cancelled"], "added": ["receiving", "complete", "cancelled"], "receiving": ["complete", "cancelled"], "complete": [], "cancelled": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      vendorOrderId: vendorOrderId,
      ingredientId: ingredientId,
      ingredientDemandId: ingredientDemandId,
      orderedQuantity: orderQty,
      unit: unit,
      unitCost: unitCost,
      status: ((doc.addedAt == null) ? "added" : doc.status),
      addedAt: ((doc.addedAt == null) ? Date.now() : doc.addedAt),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, vendorOrderLineId: docId, tenantId: __after.tenantId, vendorOrderId: vendorOrderId, purchaseNeedId: purchaseNeedId, ingredientDemandId: ingredientDemandId, ingredientId: ingredientId, orderedQuantity: orderQty, contributionQuantity: contributionQuantity, unit: unit, _subject: { entity: "VendorOrderLine", command: "ensureWeeklyLine", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "VendorOrderLineWeeklyEnsured", entity: "VendorOrderLine", entityId: docId, payload: { vendorOrderLineId: docId, tenantId: __after.tenantId, vendorOrderId: vendorOrderId, purchaseNeedId: purchaseNeedId, ingredientDemandId: ingredientDemandId, ingredientId: ingredientId, orderedQuantity: orderQty, contributionQuantity: contributionQuantity, unit: unit }, createdAt: Date.now() });
    // Reactions
    const reactionTarget0 = payload.purchaseNeedId;
    if (reactionTarget0) await __runPurchaseNeedAssignToDraft(ctx, { docId: reactionTarget0, vendorOrderId: payload.vendorOrderId, vendorOrderLineId: payload.vendorOrderLineId } as any);
    const __match1_raw = await ctx.db.query("vendorOrderLineDemands").withIndex("by_vendorOrderLineId", (q) => q.eq("vendorOrderLineId", payload.vendorOrderLineId)).collect();
    const __match1_rows = __match1_raw.filter((d) => (d as any).vendorOrderLineId === payload.vendorOrderLineId && (d as any).ingredientDemandId === payload.ingredientDemandId && (d as any).deletedAt == null).sort((a, b) => String((a as any)._id).localeCompare(String((b as any)._id)));
    const __match1_id = __match1_rows.length > 0 ? (__match1_rows[0] as any)._id : null;
    if (__match1_id) {
      await __runVendorOrderLineDemandLink(ctx, { docId: __match1_id, vendorOrderLineId: payload.vendorOrderLineId, ingredientDemandId: payload.ingredientDemandId, vendorOrderId: payload.vendorOrderId, contributionQuantity: payload.contributionQuantity, unit: payload.unit } as any);
    } else {
      const __elseArgs: Record<string, any> = { vendorOrderLineId: payload.vendorOrderLineId, ingredientDemandId: payload.ingredientDemandId, vendorOrderId: payload.vendorOrderId, contributionQuantity: payload.contributionQuantity, unit: payload.unit };
      const __elseDoc: Record<string, any> = {
        tenantId: __auth.tenantId,
        contributionQuantity: 0,
        unit: "each",
        createdAt: Date.now(),
        updatedAt: Date.now(),
        version: 0,
      };
      for (const __k of ["deletedAt","vendorOrderLineId","ingredientDemandId","vendorOrderId","contributionQuantity","unit","linkedAt","removedAt","createdAt","updatedAt"] as string[]) {
        if (__elseArgs[__k] !== undefined) __elseDoc[__k] = __elseArgs[__k];
      }
      const __elseId = await ctx.db.insert("vendorOrderLineDemands", __elseDoc as any);
      await __runVendorOrderLineDemandLink(ctx, { docId: __elseId, ...__elseArgs } as any);
    }
    return { ...doc, ...updates };
}

export const VendorOrderLine_ensureWeeklyLine = mutation({
  args: {
    docId: v.id("vendorOrderLines"),
    vendorOrderId: v.string(),
    ingredientId: v.string(),
    weekNeed: v.number(),
    onHand: v.number(),
    contributionQuantity: v.number(),
    unit: v.any(),
    unitCost: v.number(),
    purchaseNeedId: v.string(),
    ingredientDemandId: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runVendorOrderLineEnsureWeeklyLine(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "VendorOrderLine_ensureWeeklyLine", __result);
    }
    return __result;
  },
});

async function __runVendorOrderLineRecordReceipt(ctx: MutationCtx, { docId, quantity, locationId, unitPrice, supplierLotNumber, discrepancyQuantity, discrepancyNotes, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("VendorOrderLine not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("VendorOrderLine not found");
    const __rel_vendorOrder = await __resolveRelation(ctx, "vendorOrders", [__auth.tenantId, doc.vendorOrderId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).vendorOrderId;
      ((doc as any) as any).vendorOrder = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may read vendor order lines");
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may write vendor order lines through commands");
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may execute vendor order line commands");
    if (!((doc.addedAt != null))) throw new Error("Guard 0 failed");
    if (!(((doc.status === "added") || (doc.status === "receiving")))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!(((__rel_vendorOrder != null) && ((__rel_vendorOrder.status === "confirmed") || (__rel_vendorOrder.status === "partially_received"))))) throw new Error("Guard 3 failed");
    if (!(((doc.locationId == null) || (locationId === doc.locationId)))) throw new Error("Receipt locationId must match the seeded location when one was pre-set");
    if (!((quantity > 0))) throw new Error("Received quantity must be positive");
    if (!(((doc.receivedQuantity + quantity) <= doc.orderedQuantity))) throw new Error("Received quantity cannot exceed ordered quantity");
    if (!((unitPrice >= 0))) throw new Error("Receipt unit price cannot be negative");
    if (!((((supplierLotNumber).trim()).length > 0))) throw new Error("Supplier lot number is required for traceability");
    if (!(((discrepancyQuantity == null) || (discrepancyQuantity >= 0)))) throw new Error("Discrepancy quantity cannot be negative");
    const previousReceived = doc.receivedQuantity;
    const nextReceived = (doc.receivedQuantity + quantity);
    const nextStatus = ((nextReceived === doc.orderedQuantity) ? "complete" : "receiving");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = String(nextStatus);
        const __allowed: Record<string, string[]> = { "pending": ["added", "cancelled"], "added": ["receiving", "complete", "cancelled"], "receiving": ["complete", "cancelled"], "complete": [], "cancelled": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      locationId: locationId,
      receivedQuantity: nextReceived,
      unitCost: unitPrice,
      discrepancyQuantity: ((discrepancyQuantity != null) ? discrepancyQuantity : doc.discrepancyQuantity),
      discrepancyNotes: ((discrepancyNotes != null) ? discrepancyNotes : doc.discrepancyNotes),
      status: nextStatus,
      completedAt: ((nextReceived === doc.orderedQuantity) ? Date.now() : doc.completedAt),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, vendorOrderLineId: docId, tenantId: __after.tenantId, vendorOrderId: __after.vendorOrderId, vendorId: __rel_vendorOrder.vendorId, ingredientId: __after.ingredientId, ingredientDemandId: __after.ingredientDemandId, eventId: __rel_vendorOrder.eventId, locationId: locationId, supplierLotNumber: supplierLotNumber, previousReceivedQuantity: previousReceived, receivedQuantity: nextReceived, receiptQuantity: quantity, orderedQuantity: __after.orderedQuantity, unit: __after.unit, unitCost: unitPrice, unitPrice: unitPrice, discrepancyQuantity: ((discrepancyQuantity != null) ? discrepancyQuantity : __after.discrepancyQuantity), _subject: { entity: "VendorOrderLine", command: "recordReceipt", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "VendorOrderLineReceived", entity: "VendorOrderLine", entityId: docId, payload: { vendorOrderLineId: docId, tenantId: __after.tenantId, vendorOrderId: __after.vendorOrderId, vendorId: __rel_vendorOrder.vendorId, ingredientId: __after.ingredientId, ingredientDemandId: __after.ingredientDemandId, eventId: __rel_vendorOrder.eventId, locationId: locationId, supplierLotNumber: supplierLotNumber, previousReceivedQuantity: previousReceived, receivedQuantity: nextReceived, receiptQuantity: quantity, orderedQuantity: __after.orderedQuantity, unit: __after.unit, unitCost: unitPrice, unitPrice: unitPrice, discrepancyQuantity: ((discrepancyQuantity != null) ? discrepancyQuantity : __after.discrepancyQuantity) }, createdAt: Date.now() });
    // Reactions
    const __match0_raw = await ctx.db.query("ingredientPriceObservations").withIndex("by_vendorOrderLineId", (q) => q.eq("vendorOrderLineId", payload.vendorOrderLineId)).collect();
    const __match0_rows = __match0_raw.filter((d) => (d as any).vendorOrderLineId === payload.vendorOrderLineId && (d as any).cumulativeReceivedQuantity === payload.receivedQuantity && (d as any).deletedAt == null).sort((a, b) => String((a as any)._id).localeCompare(String((b as any)._id)));
    const __match0_id = __match0_rows.length > 0 ? (__match0_rows[0] as any)._id : null;
    if (__match0_id) {
      await __runIngredientPriceObservationRecord(ctx, { docId: __match0_id, ingredientId: payload.ingredientId, vendorId: payload.vendorId, vendorOrderId: payload.vendorOrderId, vendorOrderLineId: payload.vendorOrderLineId, receiptQuantity: payload.receiptQuantity, cumulativeReceivedQuantity: payload.receivedQuantity, unit: payload.unit, unitPrice: payload.unitPrice } as any);
    } else {
      const __elseArgs: Record<string, any> = { ingredientId: payload.ingredientId, vendorId: payload.vendorId, vendorOrderId: payload.vendorOrderId, vendorOrderLineId: payload.vendorOrderLineId, receiptQuantity: payload.receiptQuantity, cumulativeReceivedQuantity: payload.receivedQuantity, unit: payload.unit, unitPrice: payload.unitPrice };
      const __elseDoc: Record<string, any> = {
        tenantId: __auth.tenantId,
        receiptQuantity: 0,
        cumulativeReceivedQuantity: 0,
        unit: "each",
        unitPrice: 0,
        createdAt: Date.now(),
        updatedAt: Date.now(),
        version: 0,
      };
      for (const __k of ["deletedAt","ingredientId","vendorId","vendorOrderId","vendorOrderLineId","receiptQuantity","cumulativeReceivedQuantity","unit","unitPrice","observedAt","createdAt","updatedAt"] as string[]) {
        if (__elseArgs[__k] !== undefined) __elseDoc[__k] = __elseArgs[__k];
      }
      const __elseId = await ctx.db.insert("ingredientPriceObservations", __elseDoc as any);
      await __runIngredientPriceObservationRecord(ctx, { docId: __elseId, ...__elseArgs } as any);
    }
    const __match1_raw = await ctx.db.query("inventoryLots").withIndex("by_vendorOrderLineId", (q) => q.eq("vendorOrderLineId", payload.vendorOrderLineId)).collect();
    const __match1_rows = __match1_raw.filter((d) => (d as any).vendorOrderLineId === payload.vendorOrderLineId && (d as any).cumulativeReceivedQuantity === payload.receivedQuantity && (d as any).deletedAt == null).sort((a, b) => String((a as any)._id).localeCompare(String((b as any)._id)));
    const __match1_id = __match1_rows.length > 0 ? (__match1_rows[0] as any)._id : null;
    if (__match1_id) {
      await __runInventoryLotRecord(ctx, { docId: __match1_id, supplierLotNumber: payload.supplierLotNumber, vendorOrderLineId: payload.vendorOrderLineId, vendorOrderId: payload.vendorOrderId, vendorId: payload.vendorId, ingredientId: payload.ingredientId, ingredientDemandId: payload.ingredientDemandId, eventId: payload.eventId, locationId: payload.locationId, receiptQuantity: payload.receiptQuantity, cumulativeReceivedQuantity: payload.receivedQuantity, unit: payload.unit, unitCost: payload.unitCost } as any);
    } else {
      const __elseArgs: Record<string, any> = { supplierLotNumber: payload.supplierLotNumber, vendorOrderLineId: payload.vendorOrderLineId, vendorOrderId: payload.vendorOrderId, vendorId: payload.vendorId, ingredientId: payload.ingredientId, ingredientDemandId: payload.ingredientDemandId, eventId: payload.eventId, locationId: payload.locationId, receiptQuantity: payload.receiptQuantity, cumulativeReceivedQuantity: payload.receivedQuantity, unit: payload.unit, unitCost: payload.unitCost };
      const __elseDoc: Record<string, any> = {
        tenantId: __auth.tenantId,
        supplierLotNumber: "",
        receiptQuantity: 0,
        cumulativeReceivedQuantity: 0,
        unit: "each",
        unitCost: 0,
        createdAt: Date.now(),
        updatedAt: Date.now(),
        version: 0,
      };
      for (const __k of ["deletedAt","supplierLotNumber","vendorOrderLineId","vendorOrderId","vendorId","ingredientId","ingredientDemandId","eventId","locationId","receiptQuantity","cumulativeReceivedQuantity","unit","unitCost","receivedAt","createdAt","updatedAt"] as string[]) {
        if (__elseArgs[__k] !== undefined) __elseDoc[__k] = __elseArgs[__k];
      }
      const __elseId = await ctx.db.insert("inventoryLots", __elseDoc as any);
      await __runInventoryLotRecord(ctx, { docId: __elseId, ...__elseArgs } as any);
    }
    return { ...doc, ...updates };
}

export const VendorOrderLine_recordReceipt = mutation({
  args: {
    docId: v.id("vendorOrderLines"),
    quantity: v.number(),
    locationId: v.string(),
    unitPrice: v.number(),
    supplierLotNumber: v.string(),
    discrepancyQuantity: v.optional(v.number()),
    discrepancyNotes: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runVendorOrderLineRecordReceipt(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "VendorOrderLine_recordReceipt", __result);
    }
    return __result;
  },
});

async function __runVendorOrderLineReviseQuantity(ctx: MutationCtx, { docId, orderedQuantity, unitCost, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("VendorOrderLine not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("VendorOrderLine not found");
    const __rel_vendorOrder = await __resolveRelation(ctx, "vendorOrders", [__auth.tenantId, doc.vendorOrderId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).vendorOrderId;
      ((doc as any) as any).vendorOrder = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may read vendor order lines");
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may write vendor order lines through commands");
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may execute vendor order line commands");
    if (!((doc.status === "added"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(((__rel_vendorOrder != null) && (__rel_vendorOrder.status === "draft")))) throw new Error("Guard 2 failed");
    if (!((orderedQuantity > 0))) throw new Error("Ordered quantity must be positive");
    if (!(((unitCost == null) || (unitCost >= 0)))) throw new Error("Unit cost cannot be negative");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      orderedQuantity: orderedQuantity,
      unitCost: ((unitCost != null) ? unitCost : doc.unitCost),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, vendorOrderLineId: docId, tenantId: __after.tenantId, vendorOrderId: __after.vendorOrderId, ingredientId: __after.ingredientId, orderedQuantity: orderedQuantity, unit: __after.unit, unitCost: ((unitCost != null) ? unitCost : __after.unitCost), _subject: { entity: "VendorOrderLine", command: "reviseQuantity", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "VendorOrderLineQuantityRevised", entity: "VendorOrderLine", entityId: docId, payload: { vendorOrderLineId: docId, tenantId: __after.tenantId, vendorOrderId: __after.vendorOrderId, ingredientId: __after.ingredientId, orderedQuantity: orderedQuantity, unit: __after.unit, unitCost: ((unitCost != null) ? unitCost : __after.unitCost) }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const VendorOrderLine_reviseQuantity = mutation({
  args: {
    docId: v.id("vendorOrderLines"),
    orderedQuantity: v.number(),
    unitCost: v.optional(v.number()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runVendorOrderLineReviseQuantity(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "VendorOrderLine_reviseQuantity", __result);
    }
    return __result;
  },
});

async function __runVendorOrderLineDemandLink(ctx: MutationCtx, { docId, vendorOrderLineId, ingredientDemandId, vendorOrderId, contributionQuantity, unit, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("VendorOrderLineDemand not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("VendorOrderLineDemand not found");
    const __rel_vendorOrderLine = await __resolveRelation(ctx, "vendorOrderLines", [__auth.tenantId, doc.vendorOrderLineId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_ingredientDemand = await __resolveRelation(ctx, "ingredientDemands", [__auth.tenantId, doc.ingredientDemandId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_vendorOrder = await __resolveRelation(ctx, "vendorOrders", [__auth.tenantId, doc.vendorOrderId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).vendorOrderLineId;
      ((doc as any) as any).vendorOrderLine = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    {
      const __fk = ((doc as any) as any).vendorOrderId;
      ((doc as any) as any).vendorOrder = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    {
      const __fk = ((doc as any) as any).ingredientDemandId;
      ((doc as any) as any).ingredientDemand = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may read order demand links");
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may write order demand links through commands");
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may execute order demand link commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!((__rel_vendorOrderLine != null))) throw new Error("Guard 1 failed");
    if (!(((__rel_vendorOrder != null) && (__rel_vendorOrder.status === "draft")))) throw new Error("Guard 2 failed");
    if (!((__rel_ingredientDemand != null))) throw new Error("Guard 3 failed");
    if (!(((doc.linkedAt == null) || ((doc.vendorOrderLineId === vendorOrderLineId) && (doc.ingredientDemandId === ingredientDemandId))))) throw new Error("Guard 4 failed");
    if (!(((doc.linkedAt == null) || (vendorOrderLineId === doc.vendorOrderLineId)))) throw new Error("Demand link vendorOrderLineId must match the seeded line reference");
    if (!(((doc.linkedAt == null) || (ingredientDemandId === doc.ingredientDemandId)))) throw new Error("Demand link ingredientDemandId must match the seeded demand reference");
    if (!(((doc.linkedAt == null) || (vendorOrderId === doc.vendorOrderId)))) throw new Error("Demand link vendorOrderId must match the seeded order reference");
    if (!((vendorOrderId === __rel_vendorOrderLine.vendorOrderId))) throw new Error("Demand link vendorOrderId must own the vendor order line");
    if (!((contributionQuantity > 0))) throw new Error("Demand link contribution must be positive");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      vendorOrderLineId: vendorOrderLineId,
      ingredientDemandId: ingredientDemandId,
      vendorOrderId: vendorOrderId,
      contributionQuantity: contributionQuantity,
      unit: unit,
      linkedAt: ((doc.linkedAt == null) ? Date.now() : doc.linkedAt),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, vendorOrderLineDemandId: docId, tenantId: __after.tenantId, vendorOrderLineId: vendorOrderLineId, ingredientDemandId: ingredientDemandId, contributionQuantity: contributionQuantity, unit: unit, _subject: { entity: "VendorOrderLineDemand", command: "link", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "VendorOrderLineDemandLinked", entity: "VendorOrderLineDemand", entityId: docId, payload: { vendorOrderLineDemandId: docId, tenantId: __after.tenantId, vendorOrderLineId: vendorOrderLineId, ingredientDemandId: ingredientDemandId, contributionQuantity: contributionQuantity, unit: unit }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const VendorOrderLineDemand_link = mutation({
  args: {
    docId: v.id("vendorOrderLineDemands"),
    vendorOrderLineId: v.string(),
    ingredientDemandId: v.string(),
    vendorOrderId: v.string(),
    contributionQuantity: v.number(),
    unit: v.any(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runVendorOrderLineDemandLink(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "VendorOrderLineDemand_link", __result);
    }
    return __result;
  },
});

export const VendorOrderLineDemand_createViaLink = mutation({
  args: {
    vendorOrderLineId: v.string(),
    ingredientDemandId: v.string(),
    vendorOrderId: v.string(),
    contributionQuantity: v.number(),
    unit: v.any(),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { vendorOrderLineId, ingredientDemandId, vendorOrderId, contributionQuantity, unit } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      createdAt: Date.now(),
      updatedAt: Date.now(),
      contributionQuantity: args.contributionQuantity,
      ingredientDemandId: args.ingredientDemandId,
      unit: args.unit,
      vendorOrderId: args.vendorOrderId,
      vendorOrderLineId: args.vendorOrderLineId
    };
    const __rel_vendorOrderLine = await __resolveRelation(ctx, "vendorOrderLines", [__auth.tenantId, __draft.vendorOrderLineId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_ingredientDemand = await __resolveRelation(ctx, "ingredientDemands", [__auth.tenantId, __draft.ingredientDemandId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_vendorOrder = await __resolveRelation(ctx, "vendorOrders", [__auth.tenantId, __draft.vendorOrderId], ["tenantId","id"], "tenantId", __auth.tenantId);
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may read order demand links");
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may write order demand links through commands");
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may execute order demand link commands");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!((__rel_vendorOrderLine != null))) throw new Error("Guard 1 failed");
    if (!(((__rel_vendorOrder != null) && (__rel_vendorOrder.status === "draft")))) throw new Error("Guard 2 failed");
    if (!((__rel_ingredientDemand != null))) throw new Error("Guard 3 failed");
    if (!(((__draft.linkedAt == null) || ((__draft.vendorOrderLineId === vendorOrderLineId) && (__draft.ingredientDemandId === ingredientDemandId))))) throw new Error("Guard 4 failed");
    if (!(((__draft.linkedAt == null) || (vendorOrderLineId === __draft.vendorOrderLineId)))) throw new Error("Demand link vendorOrderLineId must match the seeded line reference");
    if (!(((__draft.linkedAt == null) || (ingredientDemandId === __draft.ingredientDemandId)))) throw new Error("Demand link ingredientDemandId must match the seeded demand reference");
    if (!(((__draft.linkedAt == null) || (vendorOrderId === __draft.vendorOrderId)))) throw new Error("Demand link vendorOrderId must match the seeded order reference");
    if (!((vendorOrderId === __rel_vendorOrderLine.vendorOrderId))) throw new Error("Demand link vendorOrderId must own the vendor order line");
    if (!((contributionQuantity > 0))) throw new Error("Demand link contribution must be positive");
    const doc: Record<string, any> = {
      ...__draft,
      vendorOrderLineId: vendorOrderLineId,
      ingredientDemandId: ingredientDemandId,
      vendorOrderId: vendorOrderId,
      contributionQuantity: contributionQuantity,
      unit: unit,
      linkedAt: ((__draft.linkedAt == null) ? Date.now() : __draft.linkedAt),
      version: 1,
    };
    const docId = await ctx.db.insert("vendorOrderLineDemands", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, vendorOrderLineDemandId: docId, tenantId: doc.tenantId, vendorOrderLineId: doc.vendorOrderLineId, ingredientDemandId: doc.ingredientDemandId, contributionQuantity: doc.contributionQuantity, unit: doc.unit, _subject: { entity: "VendorOrderLineDemand", command: "link", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "VendorOrderLineDemandLinked", entity: "VendorOrderLineDemand", entityId: docId, payload: { vendorOrderLineDemandId: docId, tenantId: doc.tenantId, vendorOrderLineId: doc.vendorOrderLineId, ingredientDemandId: doc.ingredientDemandId, contributionQuantity: doc.contributionQuantity, unit: doc.unit }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "VendorOrderLineDemand_createViaLink", __result);
    }
    return __result;
  },
});

async function __runVendorOrderLineDemandRetire(ctx: MutationCtx, { docId, reason, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("VendorOrderLineDemand not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("VendorOrderLineDemand not found");
    const __rel_vendorOrder = await __resolveRelation(ctx, "vendorOrders", [__auth.tenantId, doc.vendorOrderId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).vendorOrderId;
      ((doc as any) as any).vendorOrder = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may read order demand links");
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may write order demand links through commands");
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may execute order demand link commands");
    if (!((doc.linkedAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(((__rel_vendorOrder != null) && (__rel_vendorOrder.status === "draft")))) throw new Error("Guard 2 failed");
    if (!((((reason).trim()).length > 0))) throw new Error("Demand link retirement reason is required");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      removedAt: Date.now(),
      deletedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, vendorOrderLineDemandId: docId, tenantId: __after.tenantId, vendorOrderLineId: __after.vendorOrderLineId, ingredientDemandId: __after.ingredientDemandId, reason: reason, _subject: { entity: "VendorOrderLineDemand", command: "retire", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "VendorOrderLineDemandRetired", entity: "VendorOrderLineDemand", entityId: docId, payload: { vendorOrderLineDemandId: docId, tenantId: __after.tenantId, vendorOrderLineId: __after.vendorOrderLineId, ingredientDemandId: __after.ingredientDemandId, reason: reason }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const VendorOrderLineDemand_retire = mutation({
  args: {
    docId: v.id("vendorOrderLineDemands"),
    reason: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runVendorOrderLineDemandRetire(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "VendorOrderLineDemand_retire", __result);
    }
    return __result;
  },
});

async function __runVendorOrderLineDemandRevise(ctx: MutationCtx, { docId, contributionQuantity, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("VendorOrderLineDemand not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("VendorOrderLineDemand not found");
    const __rel_vendorOrder = await __resolveRelation(ctx, "vendorOrders", [__auth.tenantId, doc.vendorOrderId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).vendorOrderId;
      ((doc as any) as any).vendorOrder = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may read order demand links");
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may write order demand links through commands");
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may execute order demand link commands");
    if (!((doc.linkedAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(((__rel_vendorOrder != null) && (__rel_vendorOrder.status === "draft")))) throw new Error("Guard 2 failed");
    if (!((contributionQuantity > 0))) throw new Error("Demand link contribution must be positive");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      contributionQuantity: contributionQuantity,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, vendorOrderLineDemandId: docId, tenantId: __after.tenantId, vendorOrderLineId: __after.vendorOrderLineId, ingredientDemandId: __after.ingredientDemandId, contributionQuantity: contributionQuantity, unit: __after.unit, _subject: { entity: "VendorOrderLineDemand", command: "revise", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "VendorOrderLineDemandRevised", entity: "VendorOrderLineDemand", entityId: docId, payload: { vendorOrderLineDemandId: docId, tenantId: __after.tenantId, vendorOrderLineId: __after.vendorOrderLineId, ingredientDemandId: __after.ingredientDemandId, contributionQuantity: contributionQuantity, unit: __after.unit }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const VendorOrderLineDemand_revise = mutation({
  args: {
    docId: v.id("vendorOrderLineDemands"),
    contributionQuantity: v.number(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runVendorOrderLineDemandRevise(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "VendorOrderLineDemand_revise", __result);
    }
    return __result;
  },
});

async function __runVenueActivate(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("Venue not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("Venue not found");
    const doc = await __decryptDoc(ctx, "Venue", ["addressLine1","addressLine2","city","region","postalCode","countryCode","contactName","contactEmail","contactPhone"], __storedDoc) as Record<string, any>;
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may read venues");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may write venues through commands");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may execute venue commands");
    if (!((doc.status === "inactive"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(checkRole(user, "eventManageAccess"))) throw new Error("Guard 2 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "active";
        const __allowed: Record<string, string[]> = { "active": ["inactive"], "inactive": ["active"] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "active",
      deactivatedAt: null,
      deactivationReason: null,
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "Venue", ["addressLine1","addressLine2","city","region","postalCode","countryCode","contactName","contactEmail","contactPhone"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, venueId: docId, tenantId: __after.tenantId, _subject: { entity: "Venue", command: "activate", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "VenueActivated", entity: "Venue", entityId: docId, payload: { venueId: docId, tenantId: __after.tenantId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Venue_activate = mutation({
  args: {
    docId: v.id("venues"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runVenueActivate(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Venue_activate", __result);
    }
    return __result;
  },
});

async function __runVenueChangeCapacity(ctx: MutationCtx, { docId, capacity, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("Venue not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("Venue not found");
    const doc = await __decryptDoc(ctx, "Venue", ["addressLine1","addressLine2","city","region","postalCode","countryCode","contactName","contactEmail","contactPhone"], __storedDoc) as Record<string, any>;
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may read venues");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may write venues through commands");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may execute venue commands");
    if (!((doc.status === "active"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((capacity >= 0))) throw new Error("Venue capacity cannot be negative");
    const previousCapacity = doc.capacity;
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      capacity: capacity,
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "Venue", ["addressLine1","addressLine2","city","region","postalCode","countryCode","contactName","contactEmail","contactPhone"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, venueId: docId, tenantId: __after.tenantId, previousCapacity: previousCapacity, capacity: capacity, _subject: { entity: "Venue", command: "changeCapacity", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "VenueCapacityChanged", entity: "Venue", entityId: docId, payload: { venueId: docId, tenantId: __after.tenantId, previousCapacity: previousCapacity, capacity: capacity }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Venue_changeCapacity = mutation({
  args: {
    docId: v.id("venues"),
    capacity: v.any(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runVenueChangeCapacity(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Venue_changeCapacity", __result);
    }
    return __result;
  },
});

async function __runVenueDeactivate(ctx: MutationCtx, { docId, reason, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("Venue not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("Venue not found");
    const doc = await __decryptDoc(ctx, "Venue", ["addressLine1","addressLine2","city","region","postalCode","countryCode","contactName","contactEmail","contactPhone"], __storedDoc) as Record<string, any>;
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may read venues");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may write venues through commands");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may execute venue commands");
    if (!((doc.status === "active"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(checkRole(user, "eventManageAccess"))) throw new Error("Guard 2 failed");
    if (!((((reason).trim()).length > 0))) throw new Error("Venue deactivation reason is required");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "inactive";
        const __allowed: Record<string, string[]> = { "active": ["inactive"], "inactive": ["active"] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "inactive",
      deactivatedAt: Date.now(),
      deactivationReason: reason,
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "Venue", ["addressLine1","addressLine2","city","region","postalCode","countryCode","contactName","contactEmail","contactPhone"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, venueId: docId, tenantId: __after.tenantId, reason: reason, _subject: { entity: "Venue", command: "deactivate", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "VenueDeactivated", entity: "Venue", entityId: docId, payload: { venueId: docId, tenantId: __after.tenantId, reason: reason }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Venue_deactivate = mutation({
  args: {
    docId: v.id("venues"),
    reason: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runVenueDeactivate(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Venue_deactivate", __result);
    }
    return __result;
  },
});

async function __runVenueRegister(ctx: MutationCtx, { docId, name, venueType, capacity, onPremise, kitchenAccess, parkingAvailable, hasFreightElevator, storageAvailable, logisticsNotes, loadInInstructions, powerAvailable, waterAccess, hasStairs, wasteRules, permitsInsuranceNotes, restrictions, addressLine1, addressLine2, city, region, postalCode, countryCode, contactName, contactEmail, contactPhone, accessNotes, cateringNotes, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("Venue not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("Venue not found");
    const doc = await __decryptDoc(ctx, "Venue", ["addressLine1","addressLine2","city","region","postalCode","countryCode","contactName","contactEmail","contactPhone"], __storedDoc) as Record<string, any>;
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may read venues");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may write venues through commands");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may execute venue commands");
    if (!((doc.registeredAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(checkRole(user, "eventManageAccess"))) throw new Error("Guard 2 failed");
    if (!((((name).trim()).length > 0))) throw new Error("Venue name is required");
    if (!((capacity >= 0))) throw new Error("Venue capacity cannot be negative");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      name: name,
      venueType: venueType,
      capacity: capacity,
      onPremise: onPremise,
      kitchenAccess: kitchenAccess,
      parkingAvailable: parkingAvailable,
      hasFreightElevator: hasFreightElevator,
      storageAvailable: storageAvailable,
      logisticsNotes: logisticsNotes,
      loadInInstructions: loadInInstructions,
      powerAvailable: powerAvailable,
      waterAccess: waterAccess,
      hasStairs: hasStairs,
      wasteRules: wasteRules,
      permitsInsuranceNotes: permitsInsuranceNotes,
      restrictions: restrictions,
      addressLine1: addressLine1,
      addressLine2: addressLine2,
      city: city,
      region: region,
      postalCode: postalCode,
      countryCode: countryCode,
      contactName: contactName,
      contactEmail: contactEmail,
      contactPhone: contactPhone,
      accessNotes: accessNotes,
      cateringNotes: cateringNotes,
      registeredAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "Venue", ["addressLine1","addressLine2","city","region","postalCode","countryCode","contactName","contactEmail","contactPhone"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, venueId: docId, tenantId: __after.tenantId, name: name, venueType: venueType, capacity: capacity, onPremise: onPremise, _subject: { entity: "Venue", command: "register", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "VenueRegistered", entity: "Venue", entityId: docId, payload: { venueId: docId, tenantId: __after.tenantId, name: name, venueType: venueType, capacity: capacity, onPremise: onPremise }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Venue_register = mutation({
  args: {
    docId: v.id("venues"),
    name: v.string(),
    venueType: v.any(),
    capacity: v.any(),
    onPremise: v.optional(v.boolean()),
    kitchenAccess: v.optional(v.string()),
    parkingAvailable: v.optional(v.boolean()),
    hasFreightElevator: v.optional(v.boolean()),
    storageAvailable: v.optional(v.boolean()),
    logisticsNotes: v.optional(v.string()),
    loadInInstructions: v.optional(v.string()),
    powerAvailable: v.optional(v.boolean()),
    waterAccess: v.optional(v.boolean()),
    hasStairs: v.optional(v.boolean()),
    wasteRules: v.optional(v.string()),
    permitsInsuranceNotes: v.optional(v.string()),
    restrictions: v.optional(v.string()),
    addressLine1: v.optional(v.string()),
    addressLine2: v.optional(v.string()),
    city: v.optional(v.string()),
    region: v.optional(v.string()),
    postalCode: v.optional(v.string()),
    countryCode: v.optional(v.string()),
    contactName: v.optional(v.string()),
    contactEmail: v.optional(v.string()),
    contactPhone: v.optional(v.string()),
    accessNotes: v.optional(v.string()),
    cateringNotes: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runVenueRegister(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Venue_register", __result);
    }
    return __result;
  },
});

export const Venue_createViaRegister = mutation({
  args: {
    name: v.string(),
    venueType: v.any(),
    capacity: v.any(),
    onPremise: v.optional(v.boolean()),
    kitchenAccess: v.optional(v.string()),
    parkingAvailable: v.optional(v.boolean()),
    hasFreightElevator: v.optional(v.boolean()),
    storageAvailable: v.optional(v.boolean()),
    logisticsNotes: v.optional(v.string()),
    loadInInstructions: v.optional(v.string()),
    powerAvailable: v.optional(v.boolean()),
    waterAccess: v.optional(v.boolean()),
    hasStairs: v.optional(v.boolean()),
    wasteRules: v.optional(v.string()),
    permitsInsuranceNotes: v.optional(v.string()),
    restrictions: v.optional(v.string()),
    addressLine1: v.optional(v.string()),
    addressLine2: v.optional(v.string()),
    city: v.optional(v.string()),
    region: v.optional(v.string()),
    postalCode: v.optional(v.string()),
    countryCode: v.optional(v.string()),
    contactName: v.optional(v.string()),
    contactEmail: v.optional(v.string()),
    contactPhone: v.optional(v.string()),
    accessNotes: v.optional(v.string()),
    cateringNotes: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { name, venueType, capacity, onPremise, kitchenAccess, parkingAvailable, hasFreightElevator, storageAvailable, logisticsNotes, loadInInstructions, powerAvailable, waterAccess, hasStairs, wasteRules, permitsInsuranceNotes, restrictions, addressLine1, addressLine2, city, region, postalCode, countryCode, contactName, contactEmail, contactPhone, accessNotes, cateringNotes } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      status: "active",
      createdAt: Date.now(),
      updatedAt: Date.now(),
      accessNotes: args.accessNotes,
      addressLine1: args.addressLine1,
      addressLine2: args.addressLine2,
      capacity: args.capacity,
      cateringNotes: args.cateringNotes,
      city: args.city,
      contactEmail: args.contactEmail,
      contactName: args.contactName,
      contactPhone: args.contactPhone,
      countryCode: args.countryCode,
      hasFreightElevator: args.hasFreightElevator,
      hasStairs: args.hasStairs,
      kitchenAccess: args.kitchenAccess,
      loadInInstructions: args.loadInInstructions,
      logisticsNotes: args.logisticsNotes,
      name: args.name,
      onPremise: args.onPremise,
      parkingAvailable: args.parkingAvailable,
      permitsInsuranceNotes: args.permitsInsuranceNotes,
      postalCode: args.postalCode,
      powerAvailable: args.powerAvailable,
      region: args.region,
      restrictions: args.restrictions,
      storageAvailable: args.storageAvailable,
      venueType: args.venueType,
      wasteRules: args.wasteRules,
      waterAccess: args.waterAccess
    };
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may read venues");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may write venues through commands");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may execute venue commands");
    if (!((__draft.registeredAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(checkRole(user, "eventManageAccess"))) throw new Error("Guard 2 failed");
    if (!((((name).trim()).length > 0))) throw new Error("Venue name is required");
    if (!((capacity >= 0))) throw new Error("Venue capacity cannot be negative");
    const doc: Record<string, any> = {
      ...__draft,
      name: name,
      venueType: venueType,
      capacity: capacity,
      onPremise: onPremise,
      kitchenAccess: kitchenAccess,
      parkingAvailable: parkingAvailable,
      hasFreightElevator: hasFreightElevator,
      storageAvailable: storageAvailable,
      logisticsNotes: logisticsNotes,
      loadInInstructions: loadInInstructions,
      powerAvailable: powerAvailable,
      waterAccess: waterAccess,
      hasStairs: hasStairs,
      wasteRules: wasteRules,
      permitsInsuranceNotes: permitsInsuranceNotes,
      restrictions: restrictions,
      addressLine1: addressLine1,
      addressLine2: addressLine2,
      city: city,
      region: region,
      postalCode: postalCode,
      countryCode: countryCode,
      contactName: contactName,
      contactEmail: contactEmail,
      contactPhone: contactPhone,
      accessNotes: accessNotes,
      cateringNotes: cateringNotes,
      registeredAt: Date.now(),
      version: 1,
    };
    const __storedDoc = await __encryptDoc(ctx, "Venue", ["addressLine1","addressLine2","city","region","postalCode","countryCode","contactName","contactEmail","contactPhone"], doc);
    const docId = await ctx.db.insert("venues", __storedDoc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, venueId: docId, tenantId: doc.tenantId, name: doc.name, venueType: doc.venueType, capacity: doc.capacity, onPremise: doc.onPremise, _subject: { entity: "Venue", command: "register", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "VenueRegistered", entity: "Venue", entityId: docId, payload: { venueId: docId, tenantId: doc.tenantId, name: doc.name, venueType: doc.venueType, capacity: doc.capacity, onPremise: doc.onPremise }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Venue_createViaRegister", __result);
    }
    return __result;
  },
});

async function __runVenueUpdateDetails(ctx: MutationCtx, { docId, name, venueType, onPremise, kitchenAccess, parkingAvailable, hasFreightElevator, storageAvailable, logisticsNotes, loadInInstructions, powerAvailable, waterAccess, hasStairs, wasteRules, permitsInsuranceNotes, restrictions, addressLine1, addressLine2, city, region, postalCode, countryCode, contactName, contactEmail, contactPhone, accessNotes, cateringNotes, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const __storedDoc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!__storedDoc) throw new Error("Venue not found");
    if ((__storedDoc as any).tenantId !== __auth.tenantId) throw new Error("Venue not found");
    const doc = await __decryptDoc(ctx, "Venue", ["addressLine1","addressLine2","city","region","postalCode","countryCode","contactName","contactEmail","contactPhone"], __storedDoc) as Record<string, any>;
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may read venues");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may write venues through commands");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may execute venue commands");
    if (!((doc.status === "active"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((((name).trim()).length > 0))) throw new Error("Venue name is required");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      name: name,
      venueType: venueType,
      onPremise: onPremise,
      kitchenAccess: kitchenAccess,
      parkingAvailable: parkingAvailable,
      hasFreightElevator: hasFreightElevator,
      storageAvailable: storageAvailable,
      logisticsNotes: logisticsNotes,
      loadInInstructions: loadInInstructions,
      powerAvailable: powerAvailable,
      waterAccess: waterAccess,
      hasStairs: hasStairs,
      wasteRules: wasteRules,
      permitsInsuranceNotes: permitsInsuranceNotes,
      restrictions: restrictions,
      addressLine1: addressLine1,
      addressLine2: addressLine2,
      city: city,
      region: region,
      postalCode: postalCode,
      countryCode: countryCode,
      contactName: contactName,
      contactEmail: contactEmail,
      contactPhone: contactPhone,
      accessNotes: accessNotes,
      cateringNotes: cateringNotes,
      version: ((doc as any).version ?? 0) + 1
    };
    const __storedUpdates = await __encryptDoc(ctx, "Venue", ["addressLine1","addressLine2","city","region","postalCode","countryCode","contactName","contactEmail","contactPhone"], updates);
    await ctx.db.patch(docId, __storedUpdates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, venueId: docId, tenantId: __after.tenantId, _subject: { entity: "Venue", command: "updateDetails", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "VenueDetailsUpdated", entity: "Venue", entityId: docId, payload: { venueId: docId, tenantId: __after.tenantId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const Venue_updateDetails = mutation({
  args: {
    docId: v.id("venues"),
    name: v.string(),
    venueType: v.any(),
    onPremise: v.optional(v.boolean()),
    kitchenAccess: v.optional(v.string()),
    parkingAvailable: v.optional(v.boolean()),
    hasFreightElevator: v.optional(v.boolean()),
    storageAvailable: v.optional(v.boolean()),
    logisticsNotes: v.optional(v.string()),
    loadInInstructions: v.optional(v.string()),
    powerAvailable: v.optional(v.boolean()),
    waterAccess: v.optional(v.boolean()),
    hasStairs: v.optional(v.boolean()),
    wasteRules: v.optional(v.string()),
    permitsInsuranceNotes: v.optional(v.string()),
    restrictions: v.optional(v.string()),
    addressLine1: v.optional(v.string()),
    addressLine2: v.optional(v.string()),
    city: v.optional(v.string()),
    region: v.optional(v.string()),
    postalCode: v.optional(v.string()),
    countryCode: v.optional(v.string()),
    contactName: v.optional(v.string()),
    contactEmail: v.optional(v.string()),
    contactPhone: v.optional(v.string()),
    accessNotes: v.optional(v.string()),
    cateringNotes: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runVenueUpdateDetails(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "Venue_updateDetails", __result);
    }
    return __result;
  },
});

async function __runVenueCommissionTermDefine(ctx: MutationCtx, { docId, venueId, commissionPercent, effectiveStartDate, effectiveEndDate, notes, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("VenueCommissionTerm not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("VenueCommissionTerm not found");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may read venue commission terms");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may write venue commission terms");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may execute venue term commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.definedAt == null))) throw new Error("Guard 1 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      venueId: venueId,
      commissionPercent: commissionPercent,
      effectiveStartDate: effectiveStartDate,
      effectiveEndDate: effectiveEndDate,
      notes: notes,
      definedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    return { ...doc, ...updates };
}

export const VenueCommissionTerm_define = mutation({
  args: {
    docId: v.id("venueCommissionTerms"),
    venueId: v.string(),
    commissionPercent: v.number(),
    effectiveStartDate: v.number(),
    effectiveEndDate: v.optional(v.number()),
    notes: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runVenueCommissionTermDefine(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "VenueCommissionTerm_define", __result);
    }
    return __result;
  },
});

export const VenueCommissionTerm_createViaDefine = mutation({
  args: {
    venueId: v.string(),
    commissionPercent: v.number(),
    effectiveStartDate: v.number(),
    effectiveEndDate: v.optional(v.number()),
    notes: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { venueId, commissionPercent, effectiveStartDate, effectiveEndDate, notes } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      status: "active",
      createdAt: Date.now(),
      updatedAt: Date.now(),
      commissionPercent: args.commissionPercent,
      effectiveEndDate: args.effectiveEndDate,
      effectiveStartDate: args.effectiveStartDate,
      notes: args.notes,
      venueId: args.venueId
    };
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may read venue commission terms");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may write venue commission terms");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may execute venue term commands");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.definedAt == null))) throw new Error("Guard 1 failed");
    const doc: Record<string, any> = {
      ...__draft,
      venueId: venueId,
      commissionPercent: commissionPercent,
      effectiveStartDate: effectiveStartDate,
      effectiveEndDate: effectiveEndDate,
      notes: notes,
      definedAt: Date.now(),
      version: 1,
    };
    const docId = await ctx.db.insert("venueCommissionTerms", doc as any);
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "VenueCommissionTerm_createViaDefine", __result);
    }
    return __result;
  },
});

async function __runVenueCommissionTermRetire(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("VenueCommissionTerm not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("VenueCommissionTerm not found");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may read venue commission terms");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may write venue commission terms");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may execute venue term commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.definedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.retiredAt == null))) throw new Error("Guard 2 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      retiredAt: Date.now(),
      effectiveEndDate: Date.now(),
      status: "retired",
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    return { ...doc, ...updates };
}

export const VenueCommissionTerm_retire = mutation({
  args: {
    docId: v.id("venueCommissionTerms"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runVenueCommissionTermRetire(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "VenueCommissionTerm_retire", __result);
    }
    return __result;
  },
});

async function __runVenueCommissionTermRevise(ctx: MutationCtx, { docId, commissionPercent, effectiveEndDate, notes, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("VenueCommissionTerm not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("VenueCommissionTerm not found");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may read venue commission terms");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may write venue commission terms");
    if (!(checkRole(user, "financeAccess"))) throw new Error("Finance staff may execute venue term commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.definedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.retiredAt == null))) throw new Error("Guard 2 failed");
    if (!(((commissionPercent == null) || ((commissionPercent >= 0) && (commissionPercent <= 100))))) throw new Error("Guard 3 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      commissionPercent: ((commissionPercent != null) ? commissionPercent : doc.commissionPercent),
      effectiveEndDate: ((effectiveEndDate != null) ? effectiveEndDate : doc.effectiveEndDate),
      notes: ((notes != null) ? notes : doc.notes),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    return { ...doc, ...updates };
}

export const VenueCommissionTerm_revise = mutation({
  args: {
    docId: v.id("venueCommissionTerms"),
    commissionPercent: v.optional(v.number()),
    effectiveEndDate: v.optional(v.number()),
    notes: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runVenueCommissionTermRevise(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "VenueCommissionTerm_revise", __result);
    }
    return __result;
  },
});

async function __runVenueLayoutTemplateArchive(ctx: MutationCtx, { docId, reason, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("VenueLayoutTemplate not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("VenueLayoutTemplate not found");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may read venue layout templates");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may write venue layout templates through commands");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may execute venue layout template commands");
    if (!((doc.status === "active"))) throw new Error("Guard 0 failed");
    if (!((doc.definedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "archived";
        const __allowed: Record<string, string[]> = { "active": ["archived"], "archived": ["active"] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "archived",
      archivedAt: Date.now(),
      archiveReason: reason,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, templateId: docId, tenantId: __after.tenantId, reason: reason, _subject: { entity: "VenueLayoutTemplate", command: "archive", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "VenueLayoutTemplateArchived", entity: "VenueLayoutTemplate", entityId: docId, payload: { templateId: docId, tenantId: __after.tenantId, reason: reason }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const VenueLayoutTemplate_archive = mutation({
  args: {
    docId: v.id("venueLayoutTemplates"),
    reason: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runVenueLayoutTemplateArchive(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "VenueLayoutTemplate_archive", __result);
    }
    return __result;
  },
});

async function __runVenueLayoutTemplateDefine(ctx: MutationCtx, { docId, venueId, name, description, sections, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("VenueLayoutTemplate not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("VenueLayoutTemplate not found");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may read venue layout templates");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may write venue layout templates through commands");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may execute venue layout template commands");
    if (!((doc.definedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((((name).trim()).length > 0))) throw new Error("Template name is required");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      venueId: venueId,
      name: name,
      description: description,
      sections: ((sections != null) ? sections : "[]"),
      definedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, templateId: docId, tenantId: __after.tenantId, venueId: venueId, name: name, _subject: { entity: "VenueLayoutTemplate", command: "define", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "VenueLayoutTemplateDefined", entity: "VenueLayoutTemplate", entityId: docId, payload: { templateId: docId, tenantId: __after.tenantId, venueId: venueId, name: name }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const VenueLayoutTemplate_define = mutation({
  args: {
    docId: v.id("venueLayoutTemplates"),
    venueId: v.string(),
    name: v.string(),
    description: v.optional(v.string()),
    sections: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runVenueLayoutTemplateDefine(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "VenueLayoutTemplate_define", __result);
    }
    return __result;
  },
});

export const VenueLayoutTemplate_createViaDefine = mutation({
  args: {
    venueId: v.string(),
    name: v.string(),
    description: v.optional(v.string()),
    sections: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { venueId, name, description, sections } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      sections: args.sections !== undefined ? args.sections : "[]",
      status: "active",
      createdAt: Date.now(),
      updatedAt: Date.now(),
      description: args.description,
      name: args.name,
      venueId: args.venueId
    };
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may read venue layout templates");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may write venue layout templates through commands");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may execute venue layout template commands");
    if (!((__draft.definedAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((((name).trim()).length > 0))) throw new Error("Template name is required");
    const doc: Record<string, any> = {
      ...__draft,
      venueId: venueId,
      name: name,
      description: description,
      sections: ((sections != null) ? sections : "[]"),
      definedAt: Date.now(),
      version: 1,
    };
    const docId = await ctx.db.insert("venueLayoutTemplates", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, templateId: docId, tenantId: doc.tenantId, venueId: doc.venueId, name: doc.name, _subject: { entity: "VenueLayoutTemplate", command: "define", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "VenueLayoutTemplateDefined", entity: "VenueLayoutTemplate", entityId: docId, payload: { templateId: docId, tenantId: doc.tenantId, venueId: doc.venueId, name: doc.name }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "VenueLayoutTemplate_createViaDefine", __result);
    }
    return __result;
  },
});

async function __runVenueLayoutTemplateReactivate(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("VenueLayoutTemplate not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("VenueLayoutTemplate not found");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may read venue layout templates");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may write venue layout templates through commands");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may execute venue layout template commands");
    if (!((doc.status === "archived"))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "active";
        const __allowed: Record<string, string[]> = { "active": ["archived"], "archived": ["active"] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "active",
      archivedAt: null,
      archiveReason: null,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, templateId: docId, tenantId: __after.tenantId, _subject: { entity: "VenueLayoutTemplate", command: "reactivate", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "VenueLayoutTemplateReactivated", entity: "VenueLayoutTemplate", entityId: docId, payload: { templateId: docId, tenantId: __after.tenantId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const VenueLayoutTemplate_reactivate = mutation({
  args: {
    docId: v.id("venueLayoutTemplates"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runVenueLayoutTemplateReactivate(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "VenueLayoutTemplate_reactivate", __result);
    }
    return __result;
  },
});

async function __runVenueLayoutTemplateRevise(ctx: MutationCtx, { docId, name, description, sections, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("VenueLayoutTemplate not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("VenueLayoutTemplate not found");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may read venue layout templates");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may write venue layout templates through commands");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may execute venue layout template commands");
    if (!((doc.definedAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.status === "active"))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((((name).trim()).length > 0))) throw new Error("Template name is required");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      name: name,
      description: description,
      sections: ((sections != null) ? sections : doc.sections),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, templateId: docId, tenantId: __after.tenantId, venueId: __after.venueId, name: name, _subject: { entity: "VenueLayoutTemplate", command: "revise", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "VenueLayoutTemplateRevised", entity: "VenueLayoutTemplate", entityId: docId, payload: { templateId: docId, tenantId: __after.tenantId, venueId: __after.venueId, name: name }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const VenueLayoutTemplate_revise = mutation({
  args: {
    docId: v.id("venueLayoutTemplates"),
    name: v.string(),
    description: v.optional(v.string()),
    sections: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runVenueLayoutTemplateRevise(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "VenueLayoutTemplate_revise", __result);
    }
    return __result;
  },
});

async function __runVenueNotePin(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("VenueNote not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("VenueNote not found");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may read venue notes");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may write venue notes through commands");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may execute venue note commands");
    if (!((doc.postedAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((user.id != null))) throw new Error("Guard 2 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      isPinned: true,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, noteId: docId, tenantId: __after.tenantId, venueId: __after.venueId, _subject: { entity: "VenueNote", command: "pin", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "VenueNotePinned", entity: "VenueNote", entityId: docId, payload: { noteId: docId, tenantId: __after.tenantId, venueId: __after.venueId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const VenueNote_pin = mutation({
  args: {
    docId: v.id("venueNotes"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runVenueNotePin(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "VenueNote_pin", __result);
    }
    return __result;
  },
});

async function __runVenueNotePost(ctx: MutationCtx, { docId, venueId, eventId, authorPersonId, authorName, category, content, visibility, isPinned, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("VenueNote not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("VenueNote not found");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may read venue notes");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may write venue notes through commands");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may execute venue note commands");
    if (!((doc.postedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((user.id != null))) throw new Error("Guard 2 failed");
    if (!((((content).trim()).length > 0))) throw new Error("Note content is required");
    if (!((((authorName).trim()).length > 0))) throw new Error("Author name is required");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      venueId: venueId,
      eventId: eventId,
      authorPersonId: authorPersonId,
      authorName: authorName,
      authorAuthSubjectId: user.id,
      category: category,
      content: content,
      visibility: visibility,
      isPinned: isPinned,
      postedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, noteId: docId, tenantId: __after.tenantId, venueId: venueId, eventId: eventId, category: category, isPinned: isPinned, _subject: { entity: "VenueNote", command: "post", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "VenueNotePosted", entity: "VenueNote", entityId: docId, payload: { noteId: docId, tenantId: __after.tenantId, venueId: venueId, eventId: eventId, category: category, isPinned: isPinned }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const VenueNote_post = mutation({
  args: {
    docId: v.id("venueNotes"),
    venueId: v.string(),
    eventId: v.optional(v.string()),
    authorPersonId: v.string(),
    authorName: v.string(),
    category: v.any(),
    content: v.string(),
    visibility: v.optional(v.any()),
    isPinned: v.optional(v.boolean()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runVenueNotePost(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "VenueNote_post", __result);
    }
    return __result;
  },
});

export const VenueNote_createViaPost = mutation({
  args: {
    venueId: v.string(),
    eventId: v.optional(v.string()),
    authorPersonId: v.string(),
    authorName: v.string(),
    category: v.any(),
    content: v.string(),
    visibility: v.optional(v.any()),
    isPinned: v.optional(v.boolean()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { venueId, eventId, authorPersonId, authorName, category, content, visibility, isPinned } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      isPinned: args.isPinned !== undefined ? args.isPinned : false,
      visibility: args.visibility !== undefined ? args.visibility : "internal",
      createdAt: Date.now(),
      updatedAt: Date.now(),
      authorName: args.authorName,
      authorPersonId: args.authorPersonId,
      category: args.category,
      content: args.content,
      eventId: args.eventId,
      venueId: args.venueId
    };
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may read venue notes");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may write venue notes through commands");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may execute venue note commands");
    if (!((__draft.postedAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((user.id != null))) throw new Error("Guard 2 failed");
    if (!((((content).trim()).length > 0))) throw new Error("Note content is required");
    if (!((((authorName).trim()).length > 0))) throw new Error("Author name is required");
    const doc: Record<string, any> = {
      ...__draft,
      venueId: venueId,
      eventId: eventId,
      authorPersonId: authorPersonId,
      authorName: authorName,
      authorAuthSubjectId: user.id,
      category: category,
      content: content,
      visibility: visibility,
      isPinned: isPinned,
      postedAt: Date.now(),
      version: 1,
    };
    const docId = await ctx.db.insert("venueNotes", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, noteId: docId, tenantId: doc.tenantId, venueId: doc.venueId, eventId: doc.eventId, category: doc.category, isPinned: doc.isPinned, _subject: { entity: "VenueNote", command: "post", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "VenueNotePosted", entity: "VenueNote", entityId: docId, payload: { noteId: docId, tenantId: doc.tenantId, venueId: doc.venueId, eventId: doc.eventId, category: doc.category, isPinned: doc.isPinned }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "VenueNote_createViaPost", __result);
    }
    return __result;
  },
});

async function __runVenueNoteRemove(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("VenueNote not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("VenueNote not found");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may read venue notes");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may write venue notes through commands");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may execute venue note commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!(((doc.authorAuthSubjectId === user.id) || checkRole(user, "adminAccess")))) throw new Error("Guard 1 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      deletedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, noteId: docId, tenantId: __after.tenantId, venueId: __after.venueId, eventId: __after.eventId, _subject: { entity: "VenueNote", command: "remove", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "VenueNoteRemoved", entity: "VenueNote", entityId: docId, payload: { noteId: docId, tenantId: __after.tenantId, venueId: __after.venueId, eventId: __after.eventId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const VenueNote_remove = mutation({
  args: {
    docId: v.id("venueNotes"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runVenueNoteRemove(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "VenueNote_remove", __result);
    }
    return __result;
  },
});

async function __runVenueNoteRevise(ctx: MutationCtx, { docId, content, category, visibility, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("VenueNote not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("VenueNote not found");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may read venue notes");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may write venue notes through commands");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may execute venue note commands");
    if (!((doc.postedAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((user.id != null))) throw new Error("Guard 2 failed");
    if (!((((content).trim()).length > 0))) throw new Error("Note content is required");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      content: content,
      category: category,
      visibility: visibility,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, noteId: docId, tenantId: __after.tenantId, venueId: __after.venueId, category: category, visibility: visibility, _subject: { entity: "VenueNote", command: "revise", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "VenueNoteRevised", entity: "VenueNote", entityId: docId, payload: { noteId: docId, tenantId: __after.tenantId, venueId: __after.venueId, category: category, visibility: visibility }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const VenueNote_revise = mutation({
  args: {
    docId: v.id("venueNotes"),
    content: v.string(),
    category: v.optional(v.any()),
    visibility: v.optional(v.any()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runVenueNoteRevise(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "VenueNote_revise", __result);
    }
    return __result;
  },
});

async function __runVenueNoteUnpin(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("VenueNote not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("VenueNote not found");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may read venue notes");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may write venue notes through commands");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may execute venue note commands");
    if (!((doc.postedAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((user.id != null))) throw new Error("Guard 2 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      isPinned: false,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, noteId: docId, tenantId: __after.tenantId, venueId: __after.venueId, _subject: { entity: "VenueNote", command: "unpin", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "VenueNoteUnpinned", entity: "VenueNote", entityId: docId, payload: { noteId: docId, tenantId: __after.tenantId, venueId: __after.venueId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const VenueNote_unpin = mutation({
  args: {
    docId: v.id("venueNotes"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runVenueNoteUnpin(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "VenueNote_unpin", __result);
    }
    return __result;
  },
});

async function __runVenueRoomAdd(ctx: MutationCtx, { docId, venueId, name, roomType, capacity, squareFootage, description, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("VenueRoom not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("VenueRoom not found");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may read venue rooms");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may write venue rooms through commands");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may execute venue room commands");
    if (!((doc.addedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((user.id != null))) throw new Error("Guard 2 failed");
    if (!((((name).trim()).length > 0))) throw new Error("Room name is required");
    if (!((capacity >= 0))) throw new Error("Room capacity cannot be negative");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      venueId: venueId,
      name: name,
      roomType: roomType,
      capacity: capacity,
      squareFootage: squareFootage,
      description: description,
      addedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, roomId: docId, tenantId: __after.tenantId, venueId: venueId, name: name, roomType: roomType, capacity: capacity, _subject: { entity: "VenueRoom", command: "add", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "VenueRoomAdded", entity: "VenueRoom", entityId: docId, payload: { roomId: docId, tenantId: __after.tenantId, venueId: venueId, name: name, roomType: roomType, capacity: capacity }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const VenueRoom_add = mutation({
  args: {
    docId: v.id("venueRooms"),
    venueId: v.string(),
    name: v.string(),
    roomType: v.any(),
    capacity: v.any(),
    squareFootage: v.optional(v.any()),
    description: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runVenueRoomAdd(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "VenueRoom_add", __result);
    }
    return __result;
  },
});

export const VenueRoom_createViaAdd = mutation({
  args: {
    venueId: v.string(),
    name: v.string(),
    roomType: v.any(),
    capacity: v.any(),
    squareFootage: v.optional(v.any()),
    description: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { venueId, name, roomType, capacity, squareFootage, description } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      createdAt: Date.now(),
      updatedAt: Date.now(),
      capacity: args.capacity,
      description: args.description,
      name: args.name,
      roomType: args.roomType,
      squareFootage: args.squareFootage,
      venueId: args.venueId
    };
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may read venue rooms");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may write venue rooms through commands");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may execute venue room commands");
    if (!((__draft.addedAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((user.id != null))) throw new Error("Guard 2 failed");
    if (!((((name).trim()).length > 0))) throw new Error("Room name is required");
    if (!((capacity >= 0))) throw new Error("Room capacity cannot be negative");
    const doc: Record<string, any> = {
      ...__draft,
      venueId: venueId,
      name: name,
      roomType: roomType,
      capacity: capacity,
      squareFootage: squareFootage,
      description: description,
      addedAt: Date.now(),
      version: 1,
    };
    const docId = await ctx.db.insert("venueRooms", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, roomId: docId, tenantId: doc.tenantId, venueId: doc.venueId, name: doc.name, roomType: doc.roomType, capacity: doc.capacity, _subject: { entity: "VenueRoom", command: "add", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "VenueRoomAdded", entity: "VenueRoom", entityId: docId, payload: { roomId: docId, tenantId: doc.tenantId, venueId: doc.venueId, name: doc.name, roomType: doc.roomType, capacity: doc.capacity }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "VenueRoom_createViaAdd", __result);
    }
    return __result;
  },
});

async function __runVenueRoomRemove(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("VenueRoom not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("VenueRoom not found");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may read venue rooms");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may write venue rooms through commands");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may execute venue room commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!((user.id != null))) throw new Error("Guard 1 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      deletedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, roomId: docId, tenantId: __after.tenantId, venueId: __after.venueId, _subject: { entity: "VenueRoom", command: "remove", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "VenueRoomRemoved", entity: "VenueRoom", entityId: docId, payload: { roomId: docId, tenantId: __after.tenantId, venueId: __after.venueId }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const VenueRoom_remove = mutation({
  args: {
    docId: v.id("venueRooms"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runVenueRoomRemove(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "VenueRoom_remove", __result);
    }
    return __result;
  },
});

async function __runVenueRoomRevise(ctx: MutationCtx, { docId, name, roomType, capacity, squareFootage, description, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("VenueRoom not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("VenueRoom not found");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may read venue rooms");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may write venue rooms through commands");
    if (!(checkRole(user, "eventAccess"))) throw new Error("Event staff may execute venue room commands");
    if (!((doc.addedAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((user.id != null))) throw new Error("Guard 2 failed");
    if (!((((name).trim()).length > 0))) throw new Error("Room name is required");
    if (!((capacity >= 0))) throw new Error("Room capacity cannot be negative");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      name: name,
      roomType: roomType,
      capacity: capacity,
      squareFootage: squareFootage,
      description: description,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, roomId: docId, tenantId: __after.tenantId, venueId: __after.venueId, name: name, roomType: roomType, capacity: capacity, _subject: { entity: "VenueRoom", command: "revise", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "VenueRoomRevised", entity: "VenueRoom", entityId: docId, payload: { roomId: docId, tenantId: __after.tenantId, venueId: __after.venueId, name: name, roomType: roomType, capacity: capacity }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const VenueRoom_revise = mutation({
  args: {
    docId: v.id("venueRooms"),
    name: v.string(),
    roomType: v.any(),
    capacity: v.any(),
    squareFootage: v.optional(v.any()),
    description: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runVenueRoomRevise(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "VenueRoom_revise", __result);
    }
    return __result;
  },
});

async function __runVenueVendorRelationshipEstablish(ctx: MutationCtx, { docId, venueId, vendorId, category, status, effectiveFrom, effectiveUntil, primaryContactId, insuranceCertificate, insuranceExpiry, complianceNotes, discountPercent, paymentTerms, minimumOrder, notes, establishedByPersonId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("VenueVendorRelationship not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("VenueVendorRelationship not found");
    if (!(checkRole(user, "facilityAccess"))) throw new Error("Facility staff may read venue vendor relationships");
    if (!(checkRole(user, "facilityManageAccess"))) throw new Error("Facility managers may write venue vendor relationships");
    if (!(checkRole(user, "facilityManageAccess"))) throw new Error("Facility managers may execute relationship commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!(((venueId != null) && (((venueId).trim()).length > 0)))) throw new Error("Venue is required");
    if (!(((vendorId != null) && (((vendorId).trim()).length > 0)))) throw new Error("Vendor is required");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = String(((status != null) ? status : "approved"));
        const __allowed: Record<string, string[]> = { "preferred": ["approved", "restricted", "banned"], "approved": ["preferred", "restricted", "banned"], "restricted": ["preferred", "approved", "banned"], "banned": ["preferred", "approved", "restricted"] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      venueId: venueId,
      vendorId: vendorId,
      category: category,
      status: ((status != null) ? status : "approved"),
      effectiveFrom: effectiveFrom,
      effectiveUntil: effectiveUntil,
      primaryContactId: primaryContactId,
      insuranceCertificate: insuranceCertificate,
      insuranceExpiry: insuranceExpiry,
      complianceNotes: complianceNotes,
      discountPercent: discountPercent,
      paymentTerms: paymentTerms,
      minimumOrder: minimumOrder,
      notes: notes,
      establishedByPersonId: establishedByPersonId,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, relationshipId: docId, tenantId: __after.tenantId, venueId: venueId, vendorId: vendorId, category: category, status: ((status != null) ? status : "approved"), _subject: { entity: "VenueVendorRelationship", command: "establish", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "VenueVendorRelationshipEstablished", entity: "VenueVendorRelationship", entityId: docId, payload: { relationshipId: docId, tenantId: __after.tenantId, venueId: venueId, vendorId: vendorId, category: category, status: ((status != null) ? status : "approved") }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const VenueVendorRelationship_establish = mutation({
  args: {
    docId: v.id("venueVendorRelationships"),
    venueId: v.string(),
    vendorId: v.string(),
    category: v.any(),
    status: v.optional(v.any()),
    effectiveFrom: v.optional(v.number()),
    effectiveUntil: v.optional(v.number()),
    primaryContactId: v.optional(v.string()),
    insuranceCertificate: v.optional(v.string()),
    insuranceExpiry: v.optional(v.number()),
    complianceNotes: v.optional(v.string()),
    discountPercent: v.optional(v.number()),
    paymentTerms: v.optional(v.string()),
    minimumOrder: v.optional(v.number()),
    notes: v.optional(v.string()),
    establishedByPersonId: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runVenueVendorRelationshipEstablish(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "VenueVendorRelationship_establish", __result);
    }
    return __result;
  },
});

export const VenueVendorRelationship_createViaEstablish = mutation({
  args: {
    venueId: v.string(),
    vendorId: v.string(),
    category: v.any(),
    status: v.optional(v.any()),
    effectiveFrom: v.optional(v.number()),
    effectiveUntil: v.optional(v.number()),
    primaryContactId: v.optional(v.string()),
    insuranceCertificate: v.optional(v.string()),
    insuranceExpiry: v.optional(v.number()),
    complianceNotes: v.optional(v.string()),
    discountPercent: v.optional(v.number()),
    paymentTerms: v.optional(v.string()),
    minimumOrder: v.optional(v.number()),
    notes: v.optional(v.string()),
    establishedByPersonId: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { venueId, vendorId, category, status, effectiveFrom, effectiveUntil, primaryContactId, insuranceCertificate, insuranceExpiry, complianceNotes, discountPercent, paymentTerms, minimumOrder, notes, establishedByPersonId } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      status: args.status !== undefined ? args.status : "approved",
      createdAt: Date.now(),
      updatedAt: Date.now(),
      category: args.category,
      complianceNotes: args.complianceNotes,
      discountPercent: args.discountPercent,
      effectiveFrom: args.effectiveFrom,
      effectiveUntil: args.effectiveUntil,
      establishedByPersonId: args.establishedByPersonId,
      insuranceCertificate: args.insuranceCertificate,
      insuranceExpiry: args.insuranceExpiry,
      minimumOrder: args.minimumOrder,
      notes: args.notes,
      paymentTerms: args.paymentTerms,
      primaryContactId: args.primaryContactId,
      vendorId: args.vendorId,
      venueId: args.venueId
    };
    if (!(checkRole(user, "facilityAccess"))) throw new Error("Facility staff may read venue vendor relationships");
    if (!(checkRole(user, "facilityManageAccess"))) throw new Error("Facility managers may write venue vendor relationships");
    if (!(checkRole(user, "facilityManageAccess"))) throw new Error("Facility managers may execute relationship commands");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!(((venueId != null) && (((venueId).trim()).length > 0)))) throw new Error("Venue is required");
    if (!(((vendorId != null) && (((vendorId).trim()).length > 0)))) throw new Error("Vendor is required");
    const doc: Record<string, any> = {
      ...__draft,
      venueId: venueId,
      vendorId: vendorId,
      category: category,
      status: ((status != null) ? status : "approved"),
      effectiveFrom: effectiveFrom,
      effectiveUntil: effectiveUntil,
      primaryContactId: primaryContactId,
      insuranceCertificate: insuranceCertificate,
      insuranceExpiry: insuranceExpiry,
      complianceNotes: complianceNotes,
      discountPercent: discountPercent,
      paymentTerms: paymentTerms,
      minimumOrder: minimumOrder,
      notes: notes,
      establishedByPersonId: establishedByPersonId,
      version: 1,
    };
    const docId = await ctx.db.insert("venueVendorRelationships", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, relationshipId: docId, tenantId: doc.tenantId, venueId: doc.venueId, vendorId: doc.vendorId, category: doc.category, status: ((doc.status != null) ? doc.status : "approved"), _subject: { entity: "VenueVendorRelationship", command: "establish", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "VenueVendorRelationshipEstablished", entity: "VenueVendorRelationship", entityId: docId, payload: { relationshipId: docId, tenantId: doc.tenantId, venueId: doc.venueId, vendorId: doc.vendorId, category: doc.category, status: ((doc.status != null) ? doc.status : "approved") }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "VenueVendorRelationship_createViaEstablish", __result);
    }
    return __result;
  },
});

async function __runVenueVendorRelationshipRetire(ctx: MutationCtx, { docId, reason, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("VenueVendorRelationship not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("VenueVendorRelationship not found");
    if (!(checkRole(user, "facilityAccess"))) throw new Error("Facility staff may read venue vendor relationships");
    if (!(checkRole(user, "facilityManageAccess"))) throw new Error("Facility managers may write venue vendor relationships");
    if (!(checkRole(user, "facilityManageAccess"))) throw new Error("Facility managers may execute relationship commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!((((reason).trim()).length > 0))) throw new Error("Retirement reason is required");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      deletedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, relationshipId: docId, tenantId: __after.tenantId, venueId: __after.venueId, vendorId: __after.vendorId, reason: reason, _subject: { entity: "VenueVendorRelationship", command: "retire", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "VenueVendorRelationshipRetired", entity: "VenueVendorRelationship", entityId: docId, payload: { relationshipId: docId, tenantId: __after.tenantId, venueId: __after.venueId, vendorId: __after.vendorId, reason: reason }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const VenueVendorRelationship_retire = mutation({
  args: {
    docId: v.id("venueVendorRelationships"),
    reason: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runVenueVendorRelationshipRetire(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "VenueVendorRelationship_retire", __result);
    }
    return __result;
  },
});

async function __runVenueVendorRelationshipReviseDetails(ctx: MutationCtx, { docId, category, effectiveFrom, effectiveUntil, primaryContactId, insuranceCertificate, insuranceExpiry, complianceNotes, discountPercent, paymentTerms, minimumOrder, notes, revisedByPersonId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("VenueVendorRelationship not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("VenueVendorRelationship not found");
    if (!(checkRole(user, "facilityAccess"))) throw new Error("Facility staff may read venue vendor relationships");
    if (!(checkRole(user, "facilityManageAccess"))) throw new Error("Facility managers may write venue vendor relationships");
    if (!(checkRole(user, "facilityManageAccess"))) throw new Error("Facility managers may execute relationship commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!((((effectiveFrom == null) || (effectiveUntil == null)) || (effectiveFrom <= effectiveUntil)))) throw new Error("Effective from must be before effective until");
    if (!(((discountPercent == null) || ((discountPercent >= 0) && (discountPercent <= 100))))) throw new Error("Discount percent must be between 0 and 100");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      category: ((category != null) ? category : doc.category),
      effectiveFrom: ((effectiveFrom != null) ? effectiveFrom : doc.effectiveFrom),
      effectiveUntil: ((effectiveUntil != null) ? effectiveUntil : doc.effectiveUntil),
      primaryContactId: ((primaryContactId != null) ? primaryContactId : doc.primaryContactId),
      insuranceCertificate: ((insuranceCertificate != null) ? insuranceCertificate : doc.insuranceCertificate),
      insuranceExpiry: ((insuranceExpiry != null) ? insuranceExpiry : doc.insuranceExpiry),
      complianceNotes: ((complianceNotes != null) ? complianceNotes : doc.complianceNotes),
      discountPercent: ((discountPercent != null) ? discountPercent : doc.discountPercent),
      paymentTerms: ((paymentTerms != null) ? paymentTerms : doc.paymentTerms),
      minimumOrder: ((minimumOrder != null) ? minimumOrder : doc.minimumOrder),
      notes: ((notes != null) ? notes : doc.notes),
      revisedByPersonId: ((revisedByPersonId != null) ? revisedByPersonId : doc.revisedByPersonId),
      revisedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, relationshipId: docId, tenantId: __after.tenantId, venueId: __after.venueId, vendorId: __after.vendorId, revisedBy: ((revisedByPersonId != null) ? revisedByPersonId : __after.revisedByPersonId), _subject: { entity: "VenueVendorRelationship", command: "reviseDetails", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "VenueVendorRelationshipRevised", entity: "VenueVendorRelationship", entityId: docId, payload: { relationshipId: docId, tenantId: __after.tenantId, venueId: __after.venueId, vendorId: __after.vendorId, revisedBy: ((revisedByPersonId != null) ? revisedByPersonId : __after.revisedByPersonId) }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const VenueVendorRelationship_reviseDetails = mutation({
  args: {
    docId: v.id("venueVendorRelationships"),
    category: v.optional(v.any()),
    effectiveFrom: v.optional(v.number()),
    effectiveUntil: v.optional(v.number()),
    primaryContactId: v.optional(v.string()),
    insuranceCertificate: v.optional(v.string()),
    insuranceExpiry: v.optional(v.number()),
    complianceNotes: v.optional(v.string()),
    discountPercent: v.optional(v.number()),
    paymentTerms: v.optional(v.string()),
    minimumOrder: v.optional(v.number()),
    notes: v.optional(v.string()),
    revisedByPersonId: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runVenueVendorRelationshipReviseDetails(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "VenueVendorRelationship_reviseDetails", __result);
    }
    return __result;
  },
});

async function __runVenueVendorRelationshipReviseStatus(ctx: MutationCtx, { docId, status, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("VenueVendorRelationship not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("VenueVendorRelationship not found");
    if (!(checkRole(user, "facilityAccess"))) throw new Error("Facility staff may read venue vendor relationships");
    if (!(checkRole(user, "facilityManageAccess"))) throw new Error("Facility managers may write venue vendor relationships");
    if (!(checkRole(user, "facilityManageAccess"))) throw new Error("Facility managers may execute relationship commands");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = String(status);
        const __allowed: Record<string, string[]> = { "preferred": ["approved", "restricted", "banned"], "approved": ["preferred", "restricted", "banned"], "restricted": ["preferred", "approved", "banned"], "banned": ["preferred", "approved", "restricted"] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: status,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, relationshipId: docId, tenantId: __after.tenantId, venueId: __after.venueId, vendorId: __after.vendorId, oldStatus: __after.status, newStatus: status, _subject: { entity: "VenueVendorRelationship", command: "reviseStatus", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "VenueVendorRelationshipStatusChanged", entity: "VenueVendorRelationship", entityId: docId, payload: { relationshipId: docId, tenantId: __after.tenantId, venueId: __after.venueId, vendorId: __after.vendorId, oldStatus: __after.status, newStatus: status }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const VenueVendorRelationship_reviseStatus = mutation({
  args: {
    docId: v.id("venueVendorRelationships"),
    status: v.any(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runVenueVendorRelationshipReviseStatus(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "VenueVendorRelationship_reviseStatus", __result);
    }
    return __result;
  },
});

async function __runWasteRecordRecord(ctx: MutationCtx, { docId, ingredientId, locationId, quantity, unit, reason, eventId, inventoryItemId, unitCost, notes, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("WasteRecord not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("WasteRecord not found");
    const __rel_ingredient = await __resolveRelation(ctx, "ingredients", [__auth.tenantId, doc.ingredientId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_location = await __resolveRelation(ctx, "storageLocations", [__auth.tenantId, doc.locationId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).ingredientId;
      ((doc as any) as any).ingredient = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    {
      const __fk = ((doc as any) as any).locationId;
      ((doc as any) as any).location = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may read waste records");
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may write waste records through commands");
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may execute waste record commands");
    if (!((doc.recordedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.status === "pending"))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!(((__rel_ingredient != null) && (__rel_ingredient.status === "active")))) throw new Error("Guard 3 failed");
    if (!(((__rel_location != null) && (__rel_location.status === "active")))) throw new Error("Guard 4 failed");
    if (!((ingredientId === doc.ingredientId))) throw new Error("Record ingredientId must match the seeded ingredient reference");
    if (!((locationId === doc.locationId))) throw new Error("Record locationId must match the seeded location reference");
    if (!((((inventoryItemId == null) || (doc.inventoryItemId == null)) || (inventoryItemId === doc.inventoryItemId)))) throw new Error("Record inventoryItemId must match the seeded stock reference when provided");
    if (!((quantity > 0))) throw new Error("Waste quantity must be positive");
    if (!(((unitCost == null) || (unitCost >= 0)))) throw new Error("Waste unit cost cannot be negative");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "recorded";
        const __allowed: Record<string, string[]> = { "pending": ["recorded"], "recorded": ["voided"], "voided": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      ingredientId: ingredientId,
      locationId: locationId,
      eventId: ((eventId != null) ? eventId : doc.eventId),
      inventoryItemId: ((inventoryItemId != null) ? inventoryItemId : doc.inventoryItemId),
      quantity: quantity,
      unit: unit,
      reason: reason,
      unitCost: ((unitCost != null) ? unitCost : doc.unitCost),
      notes: ((notes != null) ? notes : doc.notes),
      status: "recorded",
      recordedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, wasteRecordId: docId, tenantId: __after.tenantId, ingredientId: ingredientId, locationId: locationId, eventId: ((eventId != null) ? eventId : __after.eventId), inventoryItemId: ((inventoryItemId != null) ? inventoryItemId : __after.inventoryItemId), quantity: quantity, unit: unit, reason: reason, unitCost: ((unitCost != null) ? unitCost : __after.unitCost), _subject: { entity: "WasteRecord", command: "record", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "WasteRecorded", entity: "WasteRecord", entityId: docId, payload: { wasteRecordId: docId, tenantId: __after.tenantId, ingredientId: ingredientId, locationId: locationId, eventId: ((eventId != null) ? eventId : __after.eventId), inventoryItemId: ((inventoryItemId != null) ? inventoryItemId : __after.inventoryItemId), quantity: quantity, unit: unit, reason: reason, unitCost: ((unitCost != null) ? unitCost : __after.unitCost) }, createdAt: Date.now() });
    // Reactions
    const reactionTarget0 = payload.inventoryItemId;
    if (reactionTarget0) await __runInventoryItemAdjustQuantity(ctx, { docId: reactionTarget0, delta: (0 - payload.quantity), reason: "Waste" } as any);
    return { ...doc, ...updates };
}

export const WasteRecord_record = mutation({
  args: {
    docId: v.id("wasteRecords"),
    ingredientId: v.string(),
    locationId: v.string(),
    quantity: v.number(),
    unit: v.any(),
    reason: v.any(),
    eventId: v.optional(v.string()),
    inventoryItemId: v.optional(v.string()),
    unitCost: v.optional(v.number()),
    notes: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runWasteRecordRecord(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "WasteRecord_record", __result);
    }
    return __result;
  },
});

export const WasteRecord_createViaRecord = mutation({
  args: {
    ingredientId: v.string(),
    locationId: v.string(),
    quantity: v.number(),
    unit: v.any(),
    reason: v.any(),
    eventId: v.optional(v.string()),
    inventoryItemId: v.optional(v.string()),
    unitCost: v.optional(v.number()),
    notes: v.optional(v.string()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { ingredientId, locationId, quantity, unit, reason, eventId, inventoryItemId, unitCost, notes } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      unitCost: args.unitCost !== undefined ? args.unitCost : 0,
      createdAt: Date.now(),
      updatedAt: Date.now(),
      status: "pending",
      eventId: args.eventId,
      ingredientId: args.ingredientId,
      inventoryItemId: args.inventoryItemId,
      locationId: args.locationId,
      notes: args.notes,
      quantity: args.quantity,
      reason: args.reason,
      unit: args.unit
    };
    const __rel_ingredient = await __resolveRelation(ctx, "ingredients", [__auth.tenantId, __draft.ingredientId], ["tenantId","id"], "tenantId", __auth.tenantId);
    const __rel_location = await __resolveRelation(ctx, "storageLocations", [__auth.tenantId, __draft.locationId], ["tenantId","id"], "tenantId", __auth.tenantId);
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may read waste records");
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may write waste records through commands");
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may execute waste record commands");
    if (!((__draft.recordedAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.status === "pending"))) throw new Error("Guard 1 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!(((__rel_ingredient != null) && (__rel_ingredient.status === "active")))) throw new Error("Guard 3 failed");
    if (!(((__rel_location != null) && (__rel_location.status === "active")))) throw new Error("Guard 4 failed");
    if (!((ingredientId === __draft.ingredientId))) throw new Error("Record ingredientId must match the seeded ingredient reference");
    if (!((locationId === __draft.locationId))) throw new Error("Record locationId must match the seeded location reference");
    if (!((((inventoryItemId == null) || (__draft.inventoryItemId == null)) || (inventoryItemId === __draft.inventoryItemId)))) throw new Error("Record inventoryItemId must match the seeded stock reference when provided");
    if (!((quantity > 0))) throw new Error("Waste quantity must be positive");
    if (!(((unitCost == null) || (unitCost >= 0)))) throw new Error("Waste unit cost cannot be negative");
    const doc: Record<string, any> = {
      ...__draft,
      ingredientId: ingredientId,
      locationId: locationId,
      eventId: ((eventId != null) ? eventId : __draft.eventId),
      inventoryItemId: ((inventoryItemId != null) ? inventoryItemId : __draft.inventoryItemId),
      quantity: quantity,
      unit: unit,
      reason: reason,
      unitCost: ((unitCost != null) ? unitCost : __draft.unitCost),
      notes: ((notes != null) ? notes : __draft.notes),
      status: "recorded",
      recordedAt: Date.now(),
      version: 1,
    };
    const docId = await ctx.db.insert("wasteRecords", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, wasteRecordId: docId, tenantId: doc.tenantId, ingredientId: doc.ingredientId, locationId: doc.locationId, eventId: ((doc.eventId != null) ? doc.eventId : doc.eventId), inventoryItemId: ((doc.inventoryItemId != null) ? doc.inventoryItemId : doc.inventoryItemId), quantity: doc.quantity, unit: doc.unit, reason: doc.reason, unitCost: ((doc.unitCost != null) ? doc.unitCost : doc.unitCost), _subject: { entity: "WasteRecord", command: "record", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "WasteRecorded", entity: "WasteRecord", entityId: docId, payload: { wasteRecordId: docId, tenantId: doc.tenantId, ingredientId: doc.ingredientId, locationId: doc.locationId, eventId: ((doc.eventId != null) ? doc.eventId : doc.eventId), inventoryItemId: ((doc.inventoryItemId != null) ? doc.inventoryItemId : doc.inventoryItemId), quantity: doc.quantity, unit: doc.unit, reason: doc.reason, unitCost: ((doc.unitCost != null) ? doc.unitCost : doc.unitCost) }, createdAt: Date.now() });
    // Reactions
    const reactionTarget0 = payload.inventoryItemId;
    if (reactionTarget0) await __runInventoryItemAdjustQuantity(ctx, { docId: reactionTarget0, delta: (0 - payload.quantity), reason: "Waste" } as any);
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "WasteRecord_createViaRecord", __result);
    }
    return __result;
  },
});

async function __runWasteRecordVoidRecord(ctx: MutationCtx, { docId, reason, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("WasteRecord not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("WasteRecord not found");
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may read waste records");
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may write waste records through commands");
    if (!(checkRole(user, "inventoryAccess"))) throw new Error("Inventory staff may execute waste record commands");
    if (!((doc.status === "recorded"))) throw new Error("Guard 0 failed");
    if (!((doc.recordedAt != null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!(checkRole(user, "inventoryManageAccess"))) throw new Error("Guard 3 failed");
    if (!((((reason).trim()).length > 0))) throw new Error("Void reason is required");
    {
      const __cur = doc.status;
      if (__cur !== undefined) {
        const __from = String(__cur);
        const __to = "voided";
        const __allowed: Record<string, string[]> = { "pending": ["recorded"], "recorded": ["voided"], "voided": [] };
        if (__from !== __to && Object.hasOwn(__allowed, __from) && !__allowed[__from].includes(__to)) {
          const __opts = __allowed[__from].map((v) => "'" + v + "'").join(", ");
          throw new Error("Invalid state transition for " + "'status'" + ": '" + __from + "' -> '" + __to + "' is not allowed. Allowed from '" + __from + "': [" + __opts + "]");
        }
      }
    }
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      status: "voided",
      voidedAt: Date.now(),
      voidReason: reason,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, wasteRecordId: docId, tenantId: __after.tenantId, ingredientId: __after.ingredientId, locationId: __after.locationId, eventId: __after.eventId, inventoryItemId: __after.inventoryItemId, quantity: __after.quantity, unit: __after.unit, reason: reason, _subject: { entity: "WasteRecord", command: "voidRecord", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "WasteVoided", entity: "WasteRecord", entityId: docId, payload: { wasteRecordId: docId, tenantId: __after.tenantId, ingredientId: __after.ingredientId, locationId: __after.locationId, eventId: __after.eventId, inventoryItemId: __after.inventoryItemId, quantity: __after.quantity, unit: __after.unit, reason: reason }, createdAt: Date.now() });
    // Reactions
    const reactionTarget0 = payload.inventoryItemId;
    if (reactionTarget0) await __runInventoryItemAdjustQuantity(ctx, { docId: reactionTarget0, delta: payload.quantity, reason: payload.reason } as any);
    return { ...doc, ...updates };
}

export const WasteRecord_voidRecord = mutation({
  args: {
    docId: v.id("wasteRecords"),
    reason: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runWasteRecordVoidRecord(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "WasteRecord_voidRecord", __result);
    }
    return __result;
  },
});

async function __runWeeklyPurchasingConfigConfigure(ctx: MutationCtx, { docId, defaultVendorId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("WeeklyPurchasingConfig not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("WeeklyPurchasingConfig not found");
    const __rel_defaultVendor = await __resolveRelation(ctx, "vendors", [__auth.tenantId, doc.defaultVendorId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).defaultVendorId;
      ((doc as any) as any).defaultVendor = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may read weekly purchasing config");
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may write weekly purchasing config");
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may execute weekly purchasing config");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!(((__rel_defaultVendor != null) && (__rel_defaultVendor.status === "active")))) throw new Error("Guard 1 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      defaultVendorId: defaultVendorId,
      configuredAt: ((doc.configuredAt != null) ? doc.configuredAt : Date.now()),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    return { ...doc, ...updates };
}

export const WeeklyPurchasingConfig_configure = mutation({
  args: {
    docId: v.id("weeklyPurchasingConfigs"),
    defaultVendorId: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runWeeklyPurchasingConfigConfigure(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "WeeklyPurchasingConfig_configure", __result);
    }
    return __result;
  },
});

export const WeeklyPurchasingConfig_createViaConfigure = mutation({
  args: {
    defaultVendorId: v.string(),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { defaultVendorId } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      createdAt: Date.now(),
      updatedAt: Date.now(),
      defaultVendorId: args.defaultVendorId
    };
    const __rel_defaultVendor = await __resolveRelation(ctx, "vendors", [__auth.tenantId, __draft.defaultVendorId], ["tenantId","id"], "tenantId", __auth.tenantId);
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may read weekly purchasing config");
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may write weekly purchasing config");
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may execute weekly purchasing config");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!(((__rel_defaultVendor != null) && (__rel_defaultVendor.status === "active")))) throw new Error("Guard 1 failed");
    const doc: Record<string, any> = {
      ...__draft,
      defaultVendorId: defaultVendorId,
      configuredAt: ((__draft.configuredAt != null) ? __draft.configuredAt : Date.now()),
      version: 1,
    };
    const docId = await ctx.db.insert("weeklyPurchasingConfigs", doc as any);
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "WeeklyPurchasingConfig_createViaConfigure", __result);
    }
    return __result;
  },
});

async function __runWeeklyPurchasingConfigRouteNeed(ctx: MutationCtx, { docId, purchaseNeedId, eventId, ingredientDemandId, ingredientId, requiredQuantity, unit, purchasingWeekStart, preferredVendorId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("WeeklyPurchasingConfig not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("WeeklyPurchasingConfig not found");
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may read weekly purchasing config");
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may write weekly purchasing config");
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may execute weekly purchasing config");
    if (!((doc.configuredAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, purchaseNeedId: purchaseNeedId, tenantId: __after.tenantId, eventId: eventId, ingredientDemandId: ingredientDemandId, ingredientId: ingredientId, requiredQuantity: requiredQuantity, unit: unit, purchasingWeekStart: purchasingWeekStart, vendorId: ((preferredVendorId != null) ? preferredVendorId : __after.defaultVendorId), _subject: { entity: "WeeklyPurchasingConfig", command: "routeNeed", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "PurchaseNeedReadyForWeeklyDraft", entity: "WeeklyPurchasingConfig", entityId: docId, payload: { purchaseNeedId: purchaseNeedId, tenantId: __after.tenantId, eventId: eventId, ingredientDemandId: ingredientDemandId, ingredientId: ingredientId, requiredQuantity: requiredQuantity, unit: unit, purchasingWeekStart: purchasingWeekStart, vendorId: ((preferredVendorId != null) ? preferredVendorId : __after.defaultVendorId) }, createdAt: Date.now() });
    // Reactions
    const __match0_raw = await ctx.db.query("vendorOrders").withIndex("by_vendorId", (q) => q.eq("vendorId", payload.vendorId)).collect();
    const __match0_rows = __match0_raw.filter((d) => (d as any).vendorId === payload.vendorId && (d as any).sourceRangeStart === payload.purchasingWeekStart && (d as any).status === "draft" && (d as any).deletedAt == null).sort((a, b) => String((a as any)._id).localeCompare(String((b as any)._id)));
    const __match0_id = __match0_rows.length > 0 ? (__match0_rows[0] as any)._id : null;
    if (__match0_id) {
      await __runVendorOrderEnsureWeeklyDraft(ctx, { docId: __match0_id, vendorId: payload.vendorId, sourceRangeStart: payload.purchasingWeekStart, sourceRangeEnd: payload.purchasingWeekStart, purchaseNeedId: payload.purchaseNeedId, ingredientDemandId: payload.ingredientDemandId, ingredientId: payload.ingredientId, requiredQuantity: payload.requiredQuantity, unit: payload.unit } as any);
    } else {
      const __elseArgs: Record<string, any> = { vendorId: payload.vendorId, sourceRangeStart: payload.purchasingWeekStart, sourceRangeEnd: payload.purchasingWeekStart, purchaseNeedId: payload.purchaseNeedId, ingredientDemandId: payload.ingredientDemandId, ingredientId: payload.ingredientId, requiredQuantity: payload.requiredQuantity, unit: payload.unit };
      const __elseDoc: Record<string, any> = {
        tenantId: __auth.tenantId,
        subtotal: 0,
        taxAmount: 0,
        shippingAmount: 0,
        totalAmount: 0,
        status: "draft",
        createdAt: Date.now(),
        updatedAt: Date.now(),
        version: 0,
      };
      for (const __k of ["deletedAt","vendorId","eventId","sourceRangeStart","sourceRangeEnd","orderNumber","subtotal","taxAmount","shippingAmount","totalAmount","notes","status","openedAt","submittedAt","confirmedAt","receivedAt","cancelledAt","cancellationReason","approvalRequestedAt","approvedAt","approvalNotes","createdAt","updatedAt"] as string[]) {
        if (__elseArgs[__k] !== undefined) __elseDoc[__k] = __elseArgs[__k];
      }
      const __elseId = await ctx.db.insert("vendorOrders", __elseDoc as any);
      await __runVendorOrderEnsureWeeklyDraft(ctx, { docId: __elseId, ...__elseArgs } as any);
    }
    return { ...doc, ...updates };
}

export const WeeklyPurchasingConfig_routeNeed = mutation({
  args: {
    docId: v.id("weeklyPurchasingConfigs"),
    purchaseNeedId: v.string(),
    eventId: v.string(),
    ingredientDemandId: v.string(),
    ingredientId: v.string(),
    requiredQuantity: v.number(),
    unit: v.any(),
    purchasingWeekStart: v.number(),
    preferredVendorId: v.optional(v.string()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runWeeklyPurchasingConfigRouteNeed(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "WeeklyPurchasingConfig_routeNeed", __result);
    }
    return __result;
  },
});

async function __runWeeklyPurchasingConfigSetOrderApprovalThreshold(ctx: MutationCtx, { docId, amount, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("WeeklyPurchasingConfig not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("WeeklyPurchasingConfig not found");
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may read weekly purchasing config");
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may write weekly purchasing config");
    if (!((checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) throw new Error("Procurement and managers may execute weekly purchasing config");
    if (!((doc.deletedAt == null))) throw new Error("Guard 0 failed");
    if (!(checkRole(user, "manageAccess"))) throw new Error("Guard 1 failed");
    if (!(((amount == null) || (amount >= 0)))) throw new Error("Approval threshold cannot be negative");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      orderApprovalThresholdAmount: amount,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    return { ...doc, ...updates };
}

export const WeeklyPurchasingConfig_setOrderApprovalThreshold = mutation({
  args: {
    docId: v.id("weeklyPurchasingConfigs"),
    amount: v.optional(v.number()),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runWeeklyPurchasingConfigSetOrderApprovalThreshold(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "WeeklyPurchasingConfig_setOrderApprovalThreshold", __result);
    }
    return __result;
  },
});

async function __runWeeklyScheduleNoticeAcknowledge(ctx: MutationCtx, { docId, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("WeeklyScheduleNotice not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("WeeklyScheduleNotice not found");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may read schedule notices");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may write schedule notices through commands");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may execute schedule notice commands");
    if (!((doc.publishedAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.acknowledgedAt == null))) throw new Error("Guard 1 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 2 failed");
    if (!((user.id != null))) throw new Error("Guard 3 failed");
    if (!((doc.recipientAuthSubjectId != null))) throw new Error("Guard 4 failed");
    if (!((doc.recipientAuthSubjectId === user.id))) throw new Error("Guard 5 failed");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      acknowledgedAt: Date.now(),
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, noticeId: docId, tenantId: __after.tenantId, personId: __after.personId, weekStartsAt: __after.weekStartsAt, acknowledgedAt: __after.acknowledgedAt, _subject: { entity: "WeeklyScheduleNotice", command: "acknowledge", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "WeeklyScheduleAcknowledged", entity: "WeeklyScheduleNotice", entityId: docId, payload: { noticeId: docId, tenantId: __after.tenantId, personId: __after.personId, weekStartsAt: __after.weekStartsAt, acknowledgedAt: __after.acknowledgedAt }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const WeeklyScheduleNotice_acknowledge = mutation({
  args: {
    docId: v.id("weeklyScheduleNotices"),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runWeeklyScheduleNoticeAcknowledge(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "WeeklyScheduleNotice_acknowledge", __result);
    }
    return __result;
  },
});

async function __runWeeklyScheduleNoticePublishSchedule(ctx: MutationCtx, { docId, personId, recipientAuthSubjectId, weekStartsAt, weekEndsAt, shiftCount, shiftSummary, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("WeeklyScheduleNotice not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("WeeklyScheduleNotice not found");
    const __rel_person = await __resolveRelation(ctx, "people", [__auth.tenantId, doc.personId], ["tenantId","id"], "tenantId", __auth.tenantId);
    {
      const __fk = ((doc as any) as any).personId;
      ((doc as any) as any).person = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may read schedule notices");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may write schedule notices through commands");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may execute schedule notice commands");
    if (!((doc.publishedAt == null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((__rel_person != null))) throw new Error("Guard 2 failed");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Guard 3 failed");
    if (!((personId === doc.personId))) throw new Error("Schedule recipient must match the seeded person reference");
    if (!((weekEndsAt > weekStartsAt))) throw new Error("Work week end must be after its start");
    if (!((shiftCount > 0))) throw new Error("Publish requires at least one scheduled shift");
    if (!((((shiftSummary).trim()).length > 0))) throw new Error("Publish requires a shift summary");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      personId: personId,
      recipientAuthSubjectId: recipientAuthSubjectId,
      weekStartsAt: weekStartsAt,
      weekEndsAt: weekEndsAt,
      shiftCount: shiftCount,
      shiftSummary: shiftSummary,
      publishedAt: Date.now(),
      acknowledgedAt: null,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, noticeId: docId, tenantId: __after.tenantId, personId: personId, weekStartsAt: weekStartsAt, weekEndsAt: weekEndsAt, shiftCount: shiftCount, _subject: { entity: "WeeklyScheduleNotice", command: "publishSchedule", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "WeeklySchedulePublished", entity: "WeeklyScheduleNotice", entityId: docId, payload: { noticeId: docId, tenantId: __after.tenantId, personId: personId, weekStartsAt: weekStartsAt, weekEndsAt: weekEndsAt, shiftCount: shiftCount }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const WeeklyScheduleNotice_publishSchedule = mutation({
  args: {
    docId: v.id("weeklyScheduleNotices"),
    personId: v.string(),
    recipientAuthSubjectId: v.optional(v.string()),
    weekStartsAt: v.number(),
    weekEndsAt: v.number(),
    shiftCount: v.number(),
    shiftSummary: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runWeeklyScheduleNoticePublishSchedule(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "WeeklyScheduleNotice_publishSchedule", __result);
    }
    return __result;
  },
});

export const WeeklyScheduleNotice_createViaPublishSchedule = mutation({
  args: {
    personId: v.string(),
    recipientAuthSubjectId: v.optional(v.string()),
    weekStartsAt: v.number(),
    weekEndsAt: v.number(),
    shiftCount: v.number(),
    shiftSummary: v.string(),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args: any) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const { personId, recipientAuthSubjectId, weekStartsAt, weekEndsAt, shiftCount, shiftSummary } = args;
    const __draft: Record<string, any> = {
      tenantId: __auth.tenantId,
      createdAt: Date.now(),
      updatedAt: Date.now(),
      personId: args.personId,
      recipientAuthSubjectId: args.recipientAuthSubjectId,
      shiftCount: args.shiftCount,
      shiftSummary: args.shiftSummary,
      weekEndsAt: args.weekEndsAt,
      weekStartsAt: args.weekStartsAt
    };
    const __rel_person = await __resolveRelation(ctx, "people", [__auth.tenantId, __draft.personId], ["tenantId","id"], "tenantId", __auth.tenantId);
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may read schedule notices");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may write schedule notices through commands");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may execute schedule notice commands");
    if (!((__draft.publishedAt == null))) throw new Error("Guard 0 failed");
    if (!((__draft.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!((__rel_person != null))) throw new Error("Guard 2 failed");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Guard 3 failed");
    if (!((personId === __draft.personId))) throw new Error("Schedule recipient must match the seeded person reference");
    if (!((weekEndsAt > weekStartsAt))) throw new Error("Work week end must be after its start");
    if (!((shiftCount > 0))) throw new Error("Publish requires at least one scheduled shift");
    if (!((((shiftSummary).trim()).length > 0))) throw new Error("Publish requires a shift summary");
    const doc: Record<string, any> = {
      ...__draft,
      personId: personId,
      recipientAuthSubjectId: recipientAuthSubjectId,
      weekStartsAt: weekStartsAt,
      weekEndsAt: weekEndsAt,
      shiftCount: shiftCount,
      shiftSummary: shiftSummary,
      publishedAt: Date.now(),
      acknowledgedAt: null,
      version: 1,
    };
    const docId = await ctx.db.insert("weeklyScheduleNotices", doc as any);
    const payload: Record<string, any> = { _id: docId, id: docId, ...doc, result: { _id: docId, id: docId, ...doc }, noticeId: docId, tenantId: doc.tenantId, personId: doc.personId, weekStartsAt: doc.weekStartsAt, weekEndsAt: doc.weekEndsAt, shiftCount: doc.shiftCount, _subject: { entity: "WeeklyScheduleNotice", command: "publishSchedule", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "WeeklySchedulePublished", entity: "WeeklyScheduleNotice", entityId: docId, payload: { noticeId: docId, tenantId: doc.tenantId, personId: doc.personId, weekStartsAt: doc.weekStartsAt, weekEndsAt: doc.weekEndsAt, shiftCount: doc.shiftCount }, createdAt: Date.now() });
    const __result = { docId };
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "WeeklyScheduleNotice_createViaPublishSchedule", __result);
    }
    return __result;
  },
});

async function __runWeeklyScheduleNoticeRepublishSchedule(ctx: MutationCtx, { docId, recipientAuthSubjectId, shiftCount, shiftSummary, version }: any, __creation = false) {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = __auth;
    const doc = await ctx.db.get(docId) as Record<string, any> | null;
    if (!doc) throw new Error("WeeklyScheduleNotice not found");
    if ((doc as any).tenantId !== __auth.tenantId) throw new Error("WeeklyScheduleNotice not found");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may read schedule notices");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may write schedule notices through commands");
    if (!(checkRole(user, "workforceAccess"))) throw new Error("Workforce staff may execute schedule notice commands");
    if (!((doc.publishedAt != null))) throw new Error("Guard 0 failed");
    if (!((doc.deletedAt == null))) throw new Error("Guard 1 failed");
    if (!(checkRole(user, "workforceManageAccess"))) throw new Error("Guard 2 failed");
    if (!((shiftCount > 0))) throw new Error("Publish requires at least one scheduled shift");
    if (!((((shiftSummary).trim()).length > 0))) throw new Error("Publish requires a shift summary");
    if (version !== undefined && (doc as any).version !== version) {
      throw new Error("ConcurrencyConflict: VERSION_MISMATCH" + ` expected ${version} actual ${(doc as any).version}`);
    }
    const updates = {
      recipientAuthSubjectId: recipientAuthSubjectId,
      shiftCount: shiftCount,
      shiftSummary: shiftSummary,
      publishedAt: Date.now(),
      acknowledgedAt: null,
      version: ((doc as any).version ?? 0) + 1
    };
    await ctx.db.patch(docId, updates as any);
    const __after: Record<string, any> = { ...doc, ...updates };
    const payload: Record<string, any> = { id: docId, ...__after, result: { id: docId, ...__after }, noticeId: docId, tenantId: __after.tenantId, personId: __after.personId, weekStartsAt: __after.weekStartsAt, weekEndsAt: __after.weekEndsAt, shiftCount: shiftCount, _subject: { entity: "WeeklyScheduleNotice", command: "republishSchedule", id: docId } };
    await ctx.db.insert("manifestEvents", { type: "WeeklyScheduleRepublished", entity: "WeeklyScheduleNotice", entityId: docId, payload: { noticeId: docId, tenantId: __after.tenantId, personId: __after.personId, weekStartsAt: __after.weekStartsAt, weekEndsAt: __after.weekEndsAt, shiftCount: shiftCount }, createdAt: Date.now() });
    return { ...doc, ...updates };
}

export const WeeklyScheduleNotice_republishSchedule = mutation({
  args: {
    docId: v.id("weeklyScheduleNotices"),
    recipientAuthSubjectId: v.optional(v.string()),
    shiftCount: v.number(),
    shiftSummary: v.string(),
    version: v.optional(v.number()),
    idempotencyKey: v.optional(v.string())
  },
  handler: async (ctx, args) => {
    if (args.idempotencyKey !== undefined) {
      const __cached = await __getCommandIdempotency(ctx, args.idempotencyKey);
      if (__cached !== undefined) return __cached;
    }
    const __result = await __runWeeklyScheduleNoticeRepublishSchedule(ctx, args);
    if (args.idempotencyKey !== undefined) {
      await __setCommandIdempotency(ctx, args.idempotencyKey, "WeeklyScheduleNotice_republishSchedule", __result);
    }
    return __result;
  },
});
