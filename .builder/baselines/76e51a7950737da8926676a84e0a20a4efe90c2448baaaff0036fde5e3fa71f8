/** Generated by Manifest convex.react — do not edit by hand. */
import { useQuery, useMutation } from "convex/react";
import { api } from "../../convex/_generated/api";
import {
  AnnouncementDismissalDismissParamsSchema,
  AnnouncementPostParamsSchema,
  AnnouncementRemoveParamsSchema,
  AttachmentAttachParamsSchema,
  AttachmentRemoveParamsSchema,
  AttachmentSetSurveySelectionParamsSchema,
  AvailabilityWindowDeclareParamsSchema,
  AvailabilityWindowWithdrawParamsSchema,
  CandidateAdvanceParamsSchema,
  CandidateApplyParamsSchema,
  CandidateHireParamsSchema,
  CandidateRejectParamsSchema,
  ClientArchiveParamsSchema,
  ClientAssignOwnerParamsSchema,
  ClientChangeBillingProfileParamsSchema,
  ClientChangeContactParamsSchema,
  ClientCommunicationRecordParamsSchema,
  ClientContactAddParamsSchema,
  ClientContactReassignClientParamsSchema,
  ClientContactRemoveParamsSchema,
  ClientContactSetPrimaryParamsSchema,
  ClientContactStageClientMergeParamsSchema,
  ClientContactUpdateDetailsParamsSchema,
  ClientMarkMergedParamsSchema,
  ClientMergeMergeParamsSchema,
  ClientOutreachTaskCompleteParamsSchema,
  ClientOutreachTaskDismissParamsSchema,
  ClientOutreachTaskOpenParamsSchema,
  ClientReactivateParamsSchema,
  ClientRegisterParamsSchema,
  ClientStageClientMergeParamsSchema,
  ComponentDraftParamsSchema,
  ComponentImportApproveReviewParamsSchema,
  ComponentImportBeginFinalizationParamsSchema,
  ComponentImportBeginReviewParamsSchema,
  ComponentImportCancelParamsSchema,
  ComponentImportCompleteParamsSchema,
  ComponentImportLineAttachCreatedIngredientParamsSchema,
  ComponentImportLineConfirmExistingParamsSchema,
  ComponentImportLineConfirmNewParamsSchema,
  ComponentImportLineDiscardParamsSchema,
  ComponentImportLineMarkNewParamsSchema,
  ComponentImportLineResetResolutionParamsSchema,
  ComponentImportLineStageParamsSchema,
  ComponentImportLineSuggestExactMatchParamsSchema,
  ComponentImportLineSuggestPossibleMatchesParamsSchema,
  ComponentImportMarkFailedParamsSchema,
  ComponentImportRecordComponentParamsSchema,
  ComponentImportRecordParseParamsSchema,
  ComponentImportRecordResolutionProgressParamsSchema,
  ComponentImportResumeReviewParamsSchema,
  ComponentImportUploadParamsSchema,
  ComponentIngredientAddParamsSchema,
  ComponentIngredientAdjustQuantityParamsSchema,
  ComponentIngredientRemoveParamsSchema,
  ComponentIngredientSetWasteFactorParamsSchema,
  ComponentPublishVersionParamsSchema,
  ComponentPurgeParamsSchema,
  ComponentRetireParamsSchema,
  ComponentRetractParamsSchema,
  ComponentReviseDraftParamsSchema,
  ComponentSetServesPerYieldParamsSchema,
  ComponentSnapshotCaptureParamsSchema,
  ComponentStepAddParamsSchema,
  ComponentStepRemoveParamsSchema,
  ComponentStepReviseParamsSchema,
  ContractDraftParamsSchema,
  ContractExpireParamsSchema,
  ContractMarkViewedParamsSchema,
  ContractMarkVoidedParamsSchema,
  ContractReassignClientParamsSchema,
  ContractSendParamsSchema,
  ContractSignParamsSchema,
  ContractStageClientMergeParamsSchema,
  CorrectiveActionCloseParamsSchema,
  CorrectiveActionOpenParamsSchema,
  CreditMemoIssueParamsSchema,
  CreditMemoReassignClientParamsSchema,
  CreditMemoStageClientMergeParamsSchema,
  CutoverDecisionCreateParamsSchema,
  CutoverDecisionExecuteParamsSchema,
  CutoverDecisionRecordApprovalsParamsSchema,
  CutoverDecisionRollbackParamsSchema,
  CutoverDecisionSetTppReadOnlyParamsSchema,
  DeliveryCancelParamsSchema,
  DeliveryConfirmDeliveryParamsSchema,
  DeliveryMarkFailedParamsSchema,
  DeliveryScheduleParamsSchema,
  DeliveryStartTransitParamsSchema,
  DishClassifyAllergensParamsSchema,
  DishClearPrimaryImageParamsSchema,
  DishComponentAttachParamsSchema,
  DishComponentDetachParamsSchema,
  DishContainerDefineParamsSchema,
  DishContainerReinstateParamsSchema,
  DishContainerRetireParamsSchema,
  DishContainerReviseParamsSchema,
  DishIngredientAddParamsSchema,
  DishIngredientAdjustQuantityParamsSchema,
  DishIngredientRemoveParamsSchema,
  DishIntroduceParamsSchema,
  DishLinkAsEditionParamsSchema,
  DishMergeIntoParamsSchema,
  DishPurgeParamsSchema,
  DishReinstateParamsSchema,
  DishRetireParamsSchema,
  DishReviseDetailsParamsSchema,
  DishSetPrimaryImageParamsSchema,
  DishTaskAddParamsSchema,
  DishTaskBackfillActiveKeyParamsSchema,
  DishTaskRetireParamsSchema,
  DishTaskReviseParamsSchema,
  DishUpdatePortioningParamsSchema,
  EmailNotificationSubscriptionConfigureParamsSchema,
  EmailNotificationSubscriptionUpdateSubscriptionsParamsSchema,
  EquipmentMaintenanceTaskApplyServiceParamsSchema,
  EquipmentMaintenanceTaskScheduleParamsSchema,
  EquipmentReactivateParamsSchema,
  EquipmentRecountParamsSchema,
  EquipmentRegisterParamsSchema,
  EquipmentReservationCancelParamsSchema,
  EquipmentReservationCheckOutParamsSchema,
  EquipmentReservationMarkReturnedParamsSchema,
  EquipmentRetireParamsSchema,
  EquipmentReviseDetailsParamsSchema,
  EquipmentServiceEntryRecordParamsSchema,
  EquipmentUpdateConditionParamsSchema,
  EventAllergenCheckRecordParamsSchema,
  EventApproveParamsSchema,
  EventAssignOwnerParamsSchema,
  EventAssignmentAssignParamsSchema,
  EventAssignmentCheckInParamsSchema,
  EventAssignmentCheckOutParamsSchema,
  EventAssignmentConfirmParamsSchema,
  EventAssignmentMarkNoShowParamsSchema,
  EventAssignmentUnassignParamsSchema,
  EventBeginExecutionParamsSchema,
  EventCancelParamsSchema,
  EventChangeHeadcountParamsSchema,
  EventChangePricingParamsSchema,
  EventChangePrimaryContactParamsSchema,
  EventChangeRequirementsParamsSchema,
  EventChangeVenueParamsSchema,
  EventCloseOutParamsSchema,
  EventCloseoutCaptureParamsSchema,
  EventCloseoutFinalizeParamsSchema,
  EventCompleteParamsSchema,
  EventConfigureRecurrenceParamsSchema,
  EventConfirmSalesLockParamsSchema,
  EventDishAddToEventParamsSchema,
  EventDishAdjustServingsParamsSchema,
  EventDishChangeCourseParamsSchema,
  EventDishComponentSeedSeedParamsSchema,
  EventDishConfirmFromProposalParamsSchema,
  EventDishRemoveParamsSchema,
  EventDishRequestContainerPackParamsSchema,
  EventDishSetHeadcountOverrideParamsSchema,
  EventDishUpdateInstructionsParamsSchema,
  EventFinalizeEventParamsSchema,
  EventGuestAssignTableParamsSchema,
  EventGuestCheckInParamsSchema,
  EventGuestInviteParamsSchema,
  EventGuestRsvpConfirmParamsSchema,
  EventGuestRsvpDeclineParamsSchema,
  EventGuestWithdrawParamsSchema,
  EventIngredientContributionRecordParamsSchema,
  EventIngredientContributionRetireParamsSchema,
  EventIngredientContributionReviseParamsSchema,
  EventLayoutSectionAddParamsSchema,
  EventLayoutSectionRemoveParamsSchema,
  EventLayoutSectionUpdateParamsSchema,
  EventLockForSalesParamsSchema,
  EventPlanEngagementParamsSchema,
  EventReassignClientParamsSchema,
  EventRescheduleParamsSchema,
  EventReturnToPlanningParamsSchema,
  EventStaffNeedCancelParamsSchema,
  EventStaffNeedClaimParamsSchema,
  EventStaffNeedFillParamsSchema,
  EventStaffNeedPostOpenParamsSchema,
  EventStaffNeedReleaseClaimParamsSchema,
  EventStageClientMergeParamsSchema,
  EventStopRecurrenceParamsSchema,
  EventSubmitForApprovalParamsSchema,
  EventTemplateArchiveParamsSchema,
  EventTemplateDefineParamsSchema,
  EventTemplateReactivateParamsSchema,
  EventTemplateReviseParamsSchema,
  EventTimelineActivityAdjustParamsSchema,
  EventTimelineActivityRemoveParamsSchema,
  EventTimelineActivityScheduleParamsSchema,
  EventTimelineCommentPostParamsSchema,
  EventTimelineCommentRemoveParamsSchema,
  ExternalRecordLinkDiscardParamsSchema,
  ExternalRecordLinkLinkParamsSchema,
  ExternalRecordLinkResolveConflictParamsSchema,
  ExternalRecordLinkRetireParamsSchema,
  ExternalRecordLinkUnlinkExternalRecordParamsSchema,
  ExternalRecordLinkUpdateCapsuleIdParamsSchema,
  ExternalRecordLinkVerifyLinkParamsSchema,
  ImportDatasetActivateParamsSchema,
  ImportDatasetDeactivateParamsSchema,
  ImportDatasetRecordLastImportParamsSchema,
  ImportDatasetRegisterParamsSchema,
  ImportDatasetUpdateConfigParamsSchema,
  ImportRunApproveReviewParamsSchema,
  ImportRunBeginReviewParamsSchema,
  ImportRunCommitParamsSchema,
  ImportRunMarkFailedParamsSchema,
  ImportRunRecordParseParamsSchema,
  ImportRunRevertParamsSchema,
  ImportRunStartParamsSchema,
  ImportRunValidateParamsSchema,
  IncidentBeginInvestigationParamsSchema,
  IncidentClearCorrectiveActionLockParamsSchema,
  IncidentDismissParamsSchema,
  IncidentMarkResolvedParamsSchema,
  IncidentReportParamsSchema,
  IngredientClassifyAllergensParamsSchema,
  IngredientClearPrimaryImageParamsSchema,
  IngredientConfigureSubstitutesParamsSchema,
  IngredientDemandCalculateParamsSchema,
  IngredientDemandConfirmParamsSchema,
  IngredientDemandEnsurePurchaseEligibleParamsSchema,
  IngredientDemandFulfillParamsSchema,
  IngredientDemandMarkReleasedParamsSchema,
  IngredientDemandRecalculateParamsSchema,
  IngredientDemandSupersedeParamsSchema,
  IngredientDemandSyncFromContributionsParamsSchema,
  IngredientDiscontinueParamsSchema,
  IngredientIntroduceParamsSchema,
  IngredientLinkAsEditionParamsSchema,
  IngredientMergeIntoParamsSchema,
  IngredientPriceObservationRecordParamsSchema,
  IngredientPurgeParamsSchema,
  IngredientReinstateParamsSchema,
  IngredientSetNutritionParamsSchema,
  IngredientSetPreferredVendorParamsSchema,
  IngredientSetPreferredVendorsParamsSchema,
  IngredientSetPrimaryImageParamsSchema,
  IngredientUpdateCostingParamsSchema,
  IngredientUpdateDetailsParamsSchema,
  IntegrationConnectionAuthorizeParamsSchema,
  IntegrationConnectionDisconnectParamsSchema,
  IntegrationConnectionMarkConnectedParamsSchema,
  IntegrationConnectionReauthorizeParamsSchema,
  IntegrationConnectionRecordFailureParamsSchema,
  IntegrationConnectionRecordSyncSuccessParamsSchema,
  InterviewRecordOutcomeParamsSchema,
  InterviewScheduleParamsSchema,
  InventoryItemAdjustQuantityParamsSchema,
  InventoryItemOpenParamsSchema,
  InventoryItemReceiveStockParamsSchema,
  InventoryItemRecountParamsSchema,
  InventoryItemRemoveParamsSchema,
  InventoryItemSetExpiryParamsSchema,
  InventoryItemTransferInParamsSchema,
  InventoryItemTransferOutParamsSchema,
  InventoryItemUpdateLevelsParamsSchema,
  InventoryLotRecordParamsSchema,
  InventoryReservationConsumeParamsSchema,
  InventoryReservationReleaseParamsSchema,
  InventoryReservationReserveParamsSchema,
  InvoiceApplyCreditParamsSchema,
  InvoiceApplyPaymentParamsSchema,
  InvoiceIssueParamsSchema,
  InvoiceMarkDepositPaidParamsSchema,
  InvoiceMarkOverdueParamsSchema,
  InvoiceMarkViewedParamsSchema,
  InvoiceMarkVoidedParamsSchema,
  InvoiceReassignClientParamsSchema,
  InvoiceRecordCreditMemoParamsSchema,
  InvoiceRecordRefundParamsSchema,
  InvoiceSendBalanceReminderParamsSchema,
  InvoiceSendParamsSchema,
  InvoiceSetDepositParamsSchema,
  InvoiceStageClientMergeParamsSchema,
  InvoiceWriteOffParamsSchema,
  LeadCaptureParamsSchema,
  LeadConfirmConversionParamsSchema,
  LeadConfirmProposalSentParamsSchema,
  LeadReviseDetailsParamsSchema,
  LeadStageConversionParamsSchema,
  LeadStageProposalParamsSchema,
  LeadUpdatePipelineParamsSchema,
  MenuArchiveParamsSchema,
  MenuDishAddParamsSchema,
  MenuDishRemoveParamsSchema,
  MenuDishUpdateDetailsParamsSchema,
  MenuDishUpdateSellingPriceParamsSchema,
  MenuDraftParamsSchema,
  MenuMarkPublishedParamsSchema,
  MenuRestoreParamsSchema,
  MenuReviseDetailsParamsSchema,
  MenuUnpublishParamsSchema,
  MenuUpdatePricingParamsSchema,
  MessagePostParamsSchema,
  MessageSetDeliveryParamsSchema,
  MessageThreadCreateParamsSchema,
  MessageThreadLinkContactParamsSchema,
  MessageThreadLinkLeadParamsSchema,
  MessageThreadSetStatusParamsSchema,
  OccasionActivateParamsSchema,
  OccasionDeactivateParamsSchema,
  OccasionRegisterParamsSchema,
  OccasionReviseDetailsParamsSchema,
  OneOnOneActionCaptureParamsSchema,
  OneOnOneActionCloseParamsSchema,
  OneOnOneHoldParamsSchema,
  OrganizationCapabilitySettingRegisterParamsSchema,
  OrganizationCapabilitySettingSetEnabledParamsSchema,
  OrganizationConfigureBrandingParamsSchema,
  OrganizationDeactivateParamsSchema,
  OrganizationReactivateParamsSchema,
  OrganizationRegisterParamsSchema,
  OrganizationRenameParamsSchema,
  OrganizationSetDefaultCurrencyParamsSchema,
  OrganizationSuspendParamsSchema,
  PackListCancelParamsSchema,
  PackListDispatchParamsSchema,
  PackListItemAddItemParamsSchema,
  PackListItemAdjustQuantityParamsSchema,
  PackListItemMarkMissingParamsSchema,
  PackListItemMarkPackedParamsSchema,
  PackListMarkLoadedParamsSchema,
  PackListMarkPackedParamsSchema,
  PackListOpenParamsSchema,
  PackListStartPackingParamsSchema,
  PackListTemplateArchiveParamsSchema,
  PackListTemplateDefineParamsSchema,
  PackListTemplateReactivateParamsSchema,
  PackListTemplateReviseParamsSchema,
  PaymentBeginProcessingParamsSchema,
  PaymentDisputeReconciliationParamsSchema,
  PaymentFailParamsSchema,
  PaymentMarkMatchedParamsSchema,
  PaymentMethodClearDefaultParamsSchema,
  PaymentMethodExpireParamsSchema,
  PaymentMethodInvalidateParamsSchema,
  PaymentMethodMakeDefaultParamsSchema,
  PaymentMethodReactivateParamsSchema,
  PaymentMethodReassignClientParamsSchema,
  PaymentMethodRegisterParamsSchema,
  PaymentMethodRemoveParamsSchema,
  PaymentMethodStageClientMergeParamsSchema,
  PaymentReassignClientParamsSchema,
  PaymentRecordParamsSchema,
  PaymentRefundParamsSchema,
  PaymentSettleParamsSchema,
  PaymentStageClientMergeParamsSchema,
  PaymentUpdateProviderTransactionIdsParamsSchema,
  PaymentVerifyReconciliationParamsSchema,
  PayrollInputFinalizeParamsSchema,
  PayrollInputMarkVoidedParamsSchema,
  PayrollInputPrepareParamsSchema,
  PerformanceReviewRecordParamsSchema,
  PersonAssignRoleParamsSchema,
  PersonCorrectIdentityParamsSchema,
  PersonDeactivateParamsSchema,
  PersonHireParamsSchema,
  PersonLinkAccountParamsSchema,
  PersonReactivateParamsSchema,
  PersonSetEmployeeNumberParamsSchema,
  PersonSetPayRateParamsSchema,
  PersonSetSmsAlertsParamsSchema,
  PersonTerminateParamsSchema,
  PersonUnlinkAccountParamsSchema,
  PrepTaskAssignParamsSchema,
  PrepTaskCancelParamsSchema,
  PrepTaskClaimParamsSchema,
  PrepTaskCommentEditParamsSchema,
  PrepTaskCommentPostParamsSchema,
  PrepTaskCompleteParamsSchema,
  PrepTaskDependencyDeclareParamsSchema,
  PrepTaskDependencySatisfyParamsSchema,
  PrepTaskMarkBlockedParamsSchema,
  PrepTaskOpenParamsSchema,
  PrepTaskRefreshGeneratedParamsSchema,
  PrepTaskReleaseParamsSchema,
  PrepTaskReviseParamsSchema,
  PrepTaskStandDownParamsSchema,
  PrepTaskStartParamsSchema,
  PrepTaskUnblockParamsSchema,
  ProductionBatchCancelParamsSchema,
  ProductionBatchCompleteParamsSchema,
  ProductionBatchPlanParamsSchema,
  ProductionBatchStartParamsSchema,
  ProposalAcceptParamsSchema,
  ProposalDeclineParamsSchema,
  ProposalDishSelectionAdjustServingsParamsSchema,
  ProposalDishSelectionRemoveParamsSchema,
  ProposalDishSelectionSelectParamsSchema,
  ProposalDraftParamsSchema,
  ProposalEnhancementOfferParamsSchema,
  ProposalEnhancementReviseParamsSchema,
  ProposalEnhancementWithdrawParamsSchema,
  ProposalExpireParamsSchema,
  ProposalLineItemAddLineParamsSchema,
  ProposalLineItemRemoveLineParamsSchema,
  ProposalLineItemReviseLineParamsSchema,
  ProposalLinkEventParamsSchema,
  ProposalMarkViewedParamsSchema,
  ProposalReassignClientParamsSchema,
  ProposalRevisionCaptureParamsSchema,
  ProposalSendParamsSchema,
  ProposalStageClientMergeParamsSchema,
  ProposalStageEventLinkParamsSchema,
  ProposalSupersedeParamsSchema,
  ProposalTemplateArchiveParamsSchema,
  ProposalTemplateDefineParamsSchema,
  ProposalTemplateReactivateParamsSchema,
  ProposalTemplateReviseParamsSchema,
  PurchaseNeedAssignToDraftParamsSchema,
  PurchaseNeedCancelParamsSchema,
  PurchaseNeedCreateParamsSchema,
  PurchaseNeedMarkDraftOrderedParamsSchema,
  PurchaseNeedMarkFulfilledParamsSchema,
  PurchaseNeedMarkOrderedParamsSchema,
  PurchaseNeedReviseRequiredParamsSchema,
  PushSubscriptionRegisterParamsSchema,
  PushSubscriptionUnregisterParamsSchema,
  QualificationExpireParamsSchema,
  QualificationGrantParamsSchema,
  QualificationRevokeParamsSchema,
  QualityCheckFailParamsSchema,
  QualityCheckOpenParamsSchema,
  QualityCheckPassParamsSchema,
  QualityCheckReinspectParamsSchema,
  QuoteSubmissionCompleteParamsSchema,
  QuoteSubmissionCreateParamsSchema,
  QuoteSubmissionFailParamsSchema,
  QuoteSubmissionStartProcessingParamsSchema,
  RecurringAvailabilityDeclareParamsSchema,
  RecurringAvailabilityWithdrawParamsSchema,
  ReferralSourceActivateParamsSchema,
  ReferralSourceDeactivateParamsSchema,
  ReferralSourceRegisterParamsSchema,
  ReferralSourceReviseDetailsParamsSchema,
  RevenueAttributionApplyParamsSchema,
  RevenueAttributionApproveParamsSchema,
  RevenueAttributionCreateParamsSchema,
  RevenueAttributionRejectParamsSchema,
  RevenueAttributionRequestApprovalParamsSchema,
  RevenueAttributionUpdateParamsSchema,
  RoleScorecardArchiveParamsSchema,
  RoleScorecardDefineParamsSchema,
  RoleScorecardReactivateParamsSchema,
  SavedReportDefinitionArchiveParamsSchema,
  SavedReportDefinitionChangeSharingParamsSchema,
  SavedReportDefinitionCreateDefinitionParamsSchema,
  SavedReportDefinitionRenameParamsSchema,
  SavedReportDefinitionRestoreParamsSchema,
  SavedReportDefinitionUpdateDefinitionParamsSchema,
  ServiceStyleActivateParamsSchema,
  ServiceStyleDeactivateParamsSchema,
  ServiceStyleRegisterParamsSchema,
  ServiceStyleReviseDetailsParamsSchema,
  ShareLinkCreateParamsSchema,
  ShareLinkRevokeParamsSchema,
  ShiftApplyApprovedSwapParamsSchema,
  ShiftCancelParamsSchema,
  ShiftCompleteParamsSchema,
  ShiftMarkNoShowParamsSchema,
  ShiftScheduleParamsSchema,
  ShiftStageApprovedSwapParamsSchema,
  ShiftStartParamsSchema,
  ShiftSwapRequestAcceptParamsSchema,
  ShiftSwapRequestApproveParamsSchema,
  ShiftSwapRequestDeclineParamsSchema,
  ShiftSwapRequestProposeParamsSchema,
  ShiftSwapRequestRejectParamsSchema,
  ShiftSwapRequestWithdrawParamsSchema,
  ShiftTypeDefineParamsSchema,
  ShiftTypeReactivateParamsSchema,
  ShiftTypeRetireParamsSchema,
  SignatureRequestCompleteParamsSchema,
  SignatureRequestExpireParamsSchema,
  SignatureRequestRequestSignatureParamsSchema,
  SignatureRequestRevokeParamsSchema,
  StaffChatReadCursorOpenParamsSchema,
  StaffChatReadCursorTouchParamsSchema,
  StaffMessageEditParamsSchema,
  StaffMessageMarkReadParamsSchema,
  StaffMessageRemoveParamsSchema,
  StaffMessageSendParamsSchema,
  StockCountLineConfirmLedgerMatchParamsSchema,
  StockCountLineFreezeParamsSchema,
  StockCountLineReconcileVarianceParamsSchema,
  StockCountLineRecordCountParamsSchema,
  StockCountLineReviseCountParamsSchema,
  StockCountSessionCloseParamsSchema,
  StockCountSessionStartParamsSchema,
  StockTransferRecordParamsSchema,
  StorageLocationActivateParamsSchema,
  StorageLocationDeactivateParamsSchema,
  StorageLocationRegisterParamsSchema,
  StorageLocationReviseDetailsParamsSchema,
  SyncErrorMarkResolvedParamsSchema,
  SyncErrorRecordParamsSchema,
  SyncErrorReopenParamsSchema,
  TaxRateDefineParamsSchema,
  TaxRateReviseParamsSchema,
  TaxRateSetActiveParamsSchema,
  TimeOffRequestApproveParamsSchema,
  TimeOffRequestDeclineParamsSchema,
  TimeOffRequestSubmitParamsSchema,
  TimeRecordClockInParamsSchema,
  TimeRecordClockOutParamsSchema,
  TimeRecordCorrectParamsSchema,
  TrainingCompletionRecordParamsSchema,
  TrainingModuleDefineParamsSchema,
  TrainingModuleReactivateParamsSchema,
  TrainingModuleRetireParamsSchema,
  VehicleFuelLogRecordParamsSchema,
  VehicleMaintenanceScheduleApplyServiceParamsSchema,
  VehicleMaintenanceScheduleScheduleParamsSchema,
  VehicleRegisterParamsSchema,
  VehicleReviseDetailsParamsSchema,
  VehicleServiceEntryRecordParamsSchema,
  VehicleUpdateOperationalStatusParamsSchema,
  VendorContactAddParamsSchema,
  VendorContactRemoveParamsSchema,
  VendorContactUpdateParamsSchema,
  VendorContractActivateParamsSchema,
  VendorContractDraftParamsSchema,
  VendorContractMarkExpiredParamsSchema,
  VendorContractPriceTierAddParamsSchema,
  VendorContractPriceTierRemoveParamsSchema,
  VendorContractPriceTierUpdateParamsSchema,
  VendorContractTerminateParamsSchema,
  VendorContractUpdateTermsParamsSchema,
  VendorOnboardParamsSchema,
  VendorOrderApproveParamsSchema,
  VendorOrderCancelParamsSchema,
  VendorOrderConfirmParamsSchema,
  VendorOrderEnsureWeeklyDraftParamsSchema,
  VendorOrderLineAddLineParamsSchema,
  VendorOrderLineCancelLineParamsSchema,
  VendorOrderLineDemandLinkParamsSchema,
  VendorOrderLineDemandRetireParamsSchema,
  VendorOrderLineDemandReviseParamsSchema,
  VendorOrderLineEnsureWeeklyLineParamsSchema,
  VendorOrderLineRecordReceiptParamsSchema,
  VendorOrderLineReviseQuantityParamsSchema,
  VendorOrderMarkPartiallyReceivedParamsSchema,
  VendorOrderMarkReceivedParamsSchema,
  VendorOrderOpenParamsSchema,
  VendorOrderRequestChangesParamsSchema,
  VendorOrderSubmitForApprovalParamsSchema,
  VendorOrderSubmitParamsSchema,
  VendorOrderSyncLineTotalsParamsSchema,
  VendorOrderUpdateTotalsParamsSchema,
  VendorReinstateParamsSchema,
  VendorSuspendParamsSchema,
  VendorTerminateParamsSchema,
  VendorUpdateDetailsParamsSchema,
  VenueActivateParamsSchema,
  VenueChangeCapacityParamsSchema,
  VenueCommissionTermDefineParamsSchema,
  VenueCommissionTermRetireParamsSchema,
  VenueCommissionTermReviseParamsSchema,
  VenueDeactivateParamsSchema,
  VenueLayoutTemplateArchiveParamsSchema,
  VenueLayoutTemplateDefineParamsSchema,
  VenueLayoutTemplateReactivateParamsSchema,
  VenueLayoutTemplateReviseParamsSchema,
  VenueNotePinParamsSchema,
  VenueNotePostParamsSchema,
  VenueNoteRemoveParamsSchema,
  VenueNoteReviseParamsSchema,
  VenueNoteUnpinParamsSchema,
  VenueRegisterParamsSchema,
  VenueRoomAddParamsSchema,
  VenueRoomRemoveParamsSchema,
  VenueRoomReviseParamsSchema,
  VenueUpdateDetailsParamsSchema,
  VenueVendorRelationshipEstablishParamsSchema,
  VenueVendorRelationshipRetireParamsSchema,
  VenueVendorRelationshipReviseDetailsParamsSchema,
  VenueVendorRelationshipReviseStatusParamsSchema,
  WasteRecordRecordParamsSchema,
  WasteRecordVoidRecordParamsSchema,
  WeeklyPurchasingConfigConfigureParamsSchema,
  WeeklyPurchasingConfigRouteNeedParamsSchema,
  WeeklyPurchasingConfigSetOrderApprovalThresholdParamsSchema,
  WeeklyScheduleNoticeAcknowledgeParamsSchema,
  WeeklyScheduleNoticePublishScheduleParamsSchema,
  WeeklyScheduleNoticeRepublishScheduleParamsSchema,
} from "../../schemas/manifest-schemas";

function __convexArgsFromZod(parsed: Record<string, unknown>): Record<string, unknown> {
  const out: Record<string, unknown> = {};
  for (const [key, value] of Object.entries(parsed)) {
    out[key] = value instanceof Date ? value.getTime() : value;
  }
  return out;
}

/** Reactive list for Announcement. */
export function useListAnnouncement() {
  return useQuery(api.queries.listAnnouncement);
}

/** Reactive get-by-id for Announcement. Pass "skip" to suspend. */
export function useGetAnnouncement(id: string | "skip") {
  return useQuery(api.queries.getAnnouncement, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for Announcement.post. */
export function useAnnouncementPost() {
  const mutate = useMutation(api.mutations.Announcement_post);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = AnnouncementPostParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Announcement.remove. */
export function useAnnouncementRemove() {
  const mutate = useMutation(api.mutations.Announcement_remove);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = AnnouncementRemoveParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for Announcement.post. */
export function useCreateAnnouncement() {
  const mutate = useMutation(api.mutations.Announcement_createViaPost);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = AnnouncementPostParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for AnnouncementDismissal. */
export function useListAnnouncementDismissal() {
  return useQuery(api.queries.listAnnouncementDismissal);
}

/** Reactive get-by-id for AnnouncementDismissal. Pass "skip" to suspend. */
export function useGetAnnouncementDismissal(id: string | "skip") {
  return useQuery(api.queries.getAnnouncementDismissal, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for AnnouncementDismissal.dismiss. */
export function useAnnouncementDismissalDismiss() {
  const mutate = useMutation(api.mutations.AnnouncementDismissal_dismiss);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = AnnouncementDismissalDismissParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for AnnouncementDismissal.dismiss. */
export function useCreateAnnouncementDismissal() {
  const mutate = useMutation(api.mutations.AnnouncementDismissal_createViaDismiss);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = AnnouncementDismissalDismissParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for Attachment. */
export function useListAttachment() {
  return useQuery(api.queries.listAttachment);
}

/** Reactive get-by-id for Attachment. Pass "skip" to suspend. */
export function useGetAttachment(id: string | "skip") {
  return useQuery(api.queries.getAttachment, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for Attachment.attach. */
export function useAttachmentAttach() {
  const mutate = useMutation(api.mutations.Attachment_attach);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = AttachmentAttachParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Attachment.remove. */
export function useAttachmentRemove() {
  const mutate = useMutation(api.mutations.Attachment_remove);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = AttachmentRemoveParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Attachment.setSurveySelection. */
export function useAttachmentSetSurveySelection() {
  const mutate = useMutation(api.mutations.Attachment_setSurveySelection);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = AttachmentSetSurveySelectionParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for Attachment.attach. */
export function useCreateAttachment() {
  const mutate = useMutation(api.mutations.Attachment_createViaAttach);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = AttachmentAttachParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for AvailabilityWindow. */
export function useListAvailabilityWindow() {
  return useQuery(api.queries.listAvailabilityWindow);
}

/** Reactive get-by-id for AvailabilityWindow. Pass "skip" to suspend. */
export function useGetAvailabilityWindow(id: string | "skip") {
  return useQuery(api.queries.getAvailabilityWindow, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for AvailabilityWindow.declare. */
export function useAvailabilityWindowDeclare() {
  const mutate = useMutation(api.mutations.AvailabilityWindow_declare);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = AvailabilityWindowDeclareParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for AvailabilityWindow.withdraw. */
export function useAvailabilityWindowWithdraw() {
  const mutate = useMutation(api.mutations.AvailabilityWindow_withdraw);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = AvailabilityWindowWithdrawParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for AvailabilityWindow.declare. */
export function useCreateAvailabilityWindow() {
  const mutate = useMutation(api.mutations.AvailabilityWindow_createViaDeclare);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = AvailabilityWindowDeclareParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for Candidate. */
export function useListCandidate() {
  return useQuery(api.queries.listCandidate);
}

/** Reactive get-by-id for Candidate. Pass "skip" to suspend. */
export function useGetCandidate(id: string | "skip") {
  return useQuery(api.queries.getCandidate, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for Candidate.advance. */
export function useCandidateAdvance() {
  const mutate = useMutation(api.mutations.Candidate_advance);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = CandidateAdvanceParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Candidate.apply. */
export function useCandidateApply() {
  const mutate = useMutation(api.mutations.Candidate_apply);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = CandidateApplyParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Candidate.hire. */
export function useCandidateHire() {
  const mutate = useMutation(api.mutations.Candidate_hire);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = CandidateHireParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Candidate.reject. */
export function useCandidateReject() {
  const mutate = useMutation(api.mutations.Candidate_reject);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = CandidateRejectParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for Candidate.apply. */
export function useCreateCandidate() {
  const mutate = useMutation(api.mutations.Candidate_createViaApply);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = CandidateApplyParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for ChatNotifyPreference. */
export function useListChatNotifyPreference() {
  return useQuery(api.queries.listChatNotifyPreference);
}

/** Reactive get-by-id for ChatNotifyPreference. Pass "skip" to suspend. */
export function useGetChatNotifyPreference(id: string | "skip") {
  return useQuery(api.queries.getChatNotifyPreference, id === "skip" ? "skip" : { id: id as any });
}

/** Reactive list for Client. */
export function useListClient() {
  return useQuery(api.queries.listClient);
}

/** Reactive get-by-id for Client. Pass "skip" to suspend. */
export function useGetClient(id: string | "skip") {
  return useQuery(api.queries.getClient, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for Client.archive. */
export function useClientArchive() {
  const mutate = useMutation(api.mutations.Client_archive);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ClientArchiveParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Client.assignOwner. */
export function useClientAssignOwner() {
  const mutate = useMutation(api.mutations.Client_assignOwner);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ClientAssignOwnerParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Client.changeBillingProfile. */
export function useClientChangeBillingProfile() {
  const mutate = useMutation(api.mutations.Client_changeBillingProfile);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ClientChangeBillingProfileParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Client.changeContact. */
export function useClientChangeContact() {
  const mutate = useMutation(api.mutations.Client_changeContact);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ClientChangeContactParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Client.markMerged. */
export function useClientMarkMerged() {
  const mutate = useMutation(api.mutations.Client_markMerged);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ClientMarkMergedParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Client.reactivate. */
export function useClientReactivate() {
  const mutate = useMutation(api.mutations.Client_reactivate);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ClientReactivateParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Client.register. */
export function useClientRegister() {
  const mutate = useMutation(api.mutations.Client_register);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ClientRegisterParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Client.stageClientMerge. */
export function useClientStageClientMerge() {
  const mutate = useMutation(api.mutations.Client_stageClientMerge);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ClientStageClientMergeParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for Client.register. */
export function useCreateClient() {
  const mutate = useMutation(api.mutations.Client_createViaRegister);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = ClientRegisterParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for ClientCommunication. */
export function useListClientCommunication() {
  return useQuery(api.queries.listClientCommunication);
}

/** Reactive get-by-id for ClientCommunication. Pass "skip" to suspend. */
export function useGetClientCommunication(id: string | "skip") {
  return useQuery(api.queries.getClientCommunication, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for ClientCommunication.record. */
export function useClientCommunicationRecord() {
  const mutate = useMutation(api.mutations.ClientCommunication_record);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ClientCommunicationRecordParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for ClientCommunication.record. */
export function useCreateClientCommunication() {
  const mutate = useMutation(api.mutations.ClientCommunication_createViaRecord);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = ClientCommunicationRecordParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for ClientContact. */
export function useListClientContact() {
  return useQuery(api.queries.listClientContact);
}

/** Reactive get-by-id for ClientContact. Pass "skip" to suspend. */
export function useGetClientContact(id: string | "skip") {
  return useQuery(api.queries.getClientContact, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for ClientContact.add. */
export function useClientContactAdd() {
  const mutate = useMutation(api.mutations.ClientContact_add);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ClientContactAddParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for ClientContact.reassignClient. */
export function useClientContactReassignClient() {
  const mutate = useMutation(api.mutations.ClientContact_reassignClient);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ClientContactReassignClientParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for ClientContact.remove. */
export function useClientContactRemove() {
  const mutate = useMutation(api.mutations.ClientContact_remove);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ClientContactRemoveParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for ClientContact.setPrimary. */
export function useClientContactSetPrimary() {
  const mutate = useMutation(api.mutations.ClientContact_setPrimary);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ClientContactSetPrimaryParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for ClientContact.stageClientMerge. */
export function useClientContactStageClientMerge() {
  const mutate = useMutation(api.mutations.ClientContact_stageClientMerge);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ClientContactStageClientMergeParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for ClientContact.updateDetails. */
export function useClientContactUpdateDetails() {
  const mutate = useMutation(api.mutations.ClientContact_updateDetails);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ClientContactUpdateDetailsParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for ClientContact.add. */
export function useCreateClientContact() {
  const mutate = useMutation(api.mutations.ClientContact_createViaAdd);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = ClientContactAddParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for ClientMerge. */
export function useListClientMerge() {
  return useQuery(api.queries.listClientMerge);
}

/** Reactive get-by-id for ClientMerge. Pass "skip" to suspend. */
export function useGetClientMerge(id: string | "skip") {
  return useQuery(api.queries.getClientMerge, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for ClientMerge.merge. */
export function useClientMergeMerge() {
  const mutate = useMutation(api.mutations.ClientMerge_merge);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ClientMergeMergeParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for ClientMerge.merge. */
export function useCreateClientMerge() {
  const mutate = useMutation(api.mutations.ClientMerge_createViaMerge);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = ClientMergeMergeParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for ClientOutreachTask. */
export function useListClientOutreachTask() {
  return useQuery(api.queries.listClientOutreachTask);
}

/** Reactive get-by-id for ClientOutreachTask. Pass "skip" to suspend. */
export function useGetClientOutreachTask(id: string | "skip") {
  return useQuery(api.queries.getClientOutreachTask, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for ClientOutreachTask.complete. */
export function useClientOutreachTaskComplete() {
  const mutate = useMutation(api.mutations.ClientOutreachTask_complete);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ClientOutreachTaskCompleteParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for ClientOutreachTask.dismiss. */
export function useClientOutreachTaskDismiss() {
  const mutate = useMutation(api.mutations.ClientOutreachTask_dismiss);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ClientOutreachTaskDismissParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for ClientOutreachTask.open. */
export function useClientOutreachTaskOpen() {
  const mutate = useMutation(api.mutations.ClientOutreachTask_open);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ClientOutreachTaskOpenParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for ClientOutreachTask.open. */
export function useCreateClientOutreachTask() {
  const mutate = useMutation(api.mutations.ClientOutreachTask_createViaOpen);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = ClientOutreachTaskOpenParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for Component. */
export function useListComponent() {
  return useQuery(api.queries.listComponent);
}

/** Reactive get-by-id for Component. Pass "skip" to suspend. */
export function useGetComponent(id: string | "skip") {
  return useQuery(api.queries.getComponent, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for Component.draft. */
export function useComponentDraft() {
  const mutate = useMutation(api.mutations.Component_draft);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ComponentDraftParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Component.publishVersion. */
export function useComponentPublishVersion() {
  const mutate = useMutation(api.mutations.Component_publishVersion);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ComponentPublishVersionParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Component.purge. */
export function useComponentPurge() {
  const mutate = useMutation(api.mutations.Component_purge);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ComponentPurgeParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Component.retire. */
export function useComponentRetire() {
  const mutate = useMutation(api.mutations.Component_retire);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ComponentRetireParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Component.retract. */
export function useComponentRetract() {
  const mutate = useMutation(api.mutations.Component_retract);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ComponentRetractParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Component.reviseDraft. */
export function useComponentReviseDraft() {
  const mutate = useMutation(api.mutations.Component_reviseDraft);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ComponentReviseDraftParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Component.setServesPerYield. */
export function useComponentSetServesPerYield() {
  const mutate = useMutation(api.mutations.Component_setServesPerYield);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ComponentSetServesPerYieldParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for Component.draft. */
export function useCreateComponent() {
  const mutate = useMutation(api.mutations.Component_createViaDraft);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = ComponentDraftParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for ComponentImport. */
export function useListComponentImport() {
  return useQuery(api.queries.listComponentImport);
}

/** Reactive get-by-id for ComponentImport. Pass "skip" to suspend. */
export function useGetComponentImport(id: string | "skip") {
  return useQuery(api.queries.getComponentImport, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for ComponentImport.approveReview. */
export function useComponentImportApproveReview() {
  const mutate = useMutation(api.mutations.ComponentImport_approveReview);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ComponentImportApproveReviewParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for ComponentImport.beginFinalization. */
export function useComponentImportBeginFinalization() {
  const mutate = useMutation(api.mutations.ComponentImport_beginFinalization);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ComponentImportBeginFinalizationParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for ComponentImport.beginReview. */
export function useComponentImportBeginReview() {
  const mutate = useMutation(api.mutations.ComponentImport_beginReview);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ComponentImportBeginReviewParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for ComponentImport.cancel. */
export function useComponentImportCancel() {
  const mutate = useMutation(api.mutations.ComponentImport_cancel);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ComponentImportCancelParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for ComponentImport.complete. */
export function useComponentImportComplete() {
  const mutate = useMutation(api.mutations.ComponentImport_complete);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ComponentImportCompleteParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for ComponentImport.markFailed. */
export function useComponentImportMarkFailed() {
  const mutate = useMutation(api.mutations.ComponentImport_markFailed);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ComponentImportMarkFailedParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for ComponentImport.recordComponent. */
export function useComponentImportRecordComponent() {
  const mutate = useMutation(api.mutations.ComponentImport_recordComponent);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ComponentImportRecordComponentParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for ComponentImport.recordParse. */
export function useComponentImportRecordParse() {
  const mutate = useMutation(api.mutations.ComponentImport_recordParse);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ComponentImportRecordParseParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for ComponentImport.recordResolutionProgress. */
export function useComponentImportRecordResolutionProgress() {
  const mutate = useMutation(api.mutations.ComponentImport_recordResolutionProgress);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ComponentImportRecordResolutionProgressParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for ComponentImport.resumeReview. */
export function useComponentImportResumeReview() {
  const mutate = useMutation(api.mutations.ComponentImport_resumeReview);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ComponentImportResumeReviewParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for ComponentImport.upload. */
export function useComponentImportUpload() {
  const mutate = useMutation(api.mutations.ComponentImport_upload);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ComponentImportUploadParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for ComponentImport.upload. */
export function useCreateComponentImport() {
  const mutate = useMutation(api.mutations.ComponentImport_createViaUpload);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = ComponentImportUploadParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for ComponentImportLine. */
export function useListComponentImportLine() {
  return useQuery(api.queries.listComponentImportLine);
}

/** Reactive get-by-id for ComponentImportLine. Pass "skip" to suspend. */
export function useGetComponentImportLine(id: string | "skip") {
  return useQuery(api.queries.getComponentImportLine, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for ComponentImportLine.attachCreatedIngredient. */
export function useComponentImportLineAttachCreatedIngredient() {
  const mutate = useMutation(api.mutations.ComponentImportLine_attachCreatedIngredient);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ComponentImportLineAttachCreatedIngredientParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for ComponentImportLine.confirmExisting. */
export function useComponentImportLineConfirmExisting() {
  const mutate = useMutation(api.mutations.ComponentImportLine_confirmExisting);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ComponentImportLineConfirmExistingParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for ComponentImportLine.confirmNew. */
export function useComponentImportLineConfirmNew() {
  const mutate = useMutation(api.mutations.ComponentImportLine_confirmNew);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ComponentImportLineConfirmNewParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for ComponentImportLine.discard. */
export function useComponentImportLineDiscard() {
  const mutate = useMutation(api.mutations.ComponentImportLine_discard);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ComponentImportLineDiscardParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for ComponentImportLine.markNew. */
export function useComponentImportLineMarkNew() {
  const mutate = useMutation(api.mutations.ComponentImportLine_markNew);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ComponentImportLineMarkNewParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for ComponentImportLine.resetResolution. */
export function useComponentImportLineResetResolution() {
  const mutate = useMutation(api.mutations.ComponentImportLine_resetResolution);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ComponentImportLineResetResolutionParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for ComponentImportLine.stage. */
export function useComponentImportLineStage() {
  const mutate = useMutation(api.mutations.ComponentImportLine_stage);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ComponentImportLineStageParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for ComponentImportLine.suggestExactMatch. */
export function useComponentImportLineSuggestExactMatch() {
  const mutate = useMutation(api.mutations.ComponentImportLine_suggestExactMatch);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ComponentImportLineSuggestExactMatchParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for ComponentImportLine.suggestPossibleMatches. */
export function useComponentImportLineSuggestPossibleMatches() {
  const mutate = useMutation(api.mutations.ComponentImportLine_suggestPossibleMatches);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ComponentImportLineSuggestPossibleMatchesParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for ComponentImportLine.stage. */
export function useCreateComponentImportLine() {
  const mutate = useMutation(api.mutations.ComponentImportLine_createViaStage);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = ComponentImportLineStageParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for ComponentIngredient. */
export function useListComponentIngredient() {
  return useQuery(api.queries.listComponentIngredient);
}

/** Reactive get-by-id for ComponentIngredient. Pass "skip" to suspend. */
export function useGetComponentIngredient(id: string | "skip") {
  return useQuery(api.queries.getComponentIngredient, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for ComponentIngredient.add. */
export function useComponentIngredientAdd() {
  const mutate = useMutation(api.mutations.ComponentIngredient_add);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ComponentIngredientAddParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for ComponentIngredient.adjustQuantity. */
export function useComponentIngredientAdjustQuantity() {
  const mutate = useMutation(api.mutations.ComponentIngredient_adjustQuantity);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ComponentIngredientAdjustQuantityParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for ComponentIngredient.remove. */
export function useComponentIngredientRemove() {
  const mutate = useMutation(api.mutations.ComponentIngredient_remove);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ComponentIngredientRemoveParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for ComponentIngredient.setWasteFactor. */
export function useComponentIngredientSetWasteFactor() {
  const mutate = useMutation(api.mutations.ComponentIngredient_setWasteFactor);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ComponentIngredientSetWasteFactorParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for ComponentIngredient.add. */
export function useCreateComponentIngredient() {
  const mutate = useMutation(api.mutations.ComponentIngredient_createViaAdd);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = ComponentIngredientAddParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for ComponentSnapshot. */
export function useListComponentSnapshot() {
  return useQuery(api.queries.listComponentSnapshot);
}

/** Reactive get-by-id for ComponentSnapshot. Pass "skip" to suspend. */
export function useGetComponentSnapshot(id: string | "skip") {
  return useQuery(api.queries.getComponentSnapshot, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for ComponentSnapshot.capture. */
export function useComponentSnapshotCapture() {
  const mutate = useMutation(api.mutations.ComponentSnapshot_capture);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ComponentSnapshotCaptureParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for ComponentSnapshot.capture. */
export function useCreateComponentSnapshot() {
  const mutate = useMutation(api.mutations.ComponentSnapshot_createViaCapture);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = ComponentSnapshotCaptureParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for ComponentStep. */
export function useListComponentStep() {
  return useQuery(api.queries.listComponentStep);
}

/** Reactive get-by-id for ComponentStep. Pass "skip" to suspend. */
export function useGetComponentStep(id: string | "skip") {
  return useQuery(api.queries.getComponentStep, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for ComponentStep.add. */
export function useComponentStepAdd() {
  const mutate = useMutation(api.mutations.ComponentStep_add);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ComponentStepAddParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for ComponentStep.remove. */
export function useComponentStepRemove() {
  const mutate = useMutation(api.mutations.ComponentStep_remove);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ComponentStepRemoveParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for ComponentStep.revise. */
export function useComponentStepRevise() {
  const mutate = useMutation(api.mutations.ComponentStep_revise);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ComponentStepReviseParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for ComponentStep.add. */
export function useCreateComponentStep() {
  const mutate = useMutation(api.mutations.ComponentStep_createViaAdd);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = ComponentStepAddParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for Contract. */
export function useListContract() {
  return useQuery(api.queries.listContract);
}

/** Reactive get-by-id for Contract. Pass "skip" to suspend. */
export function useGetContract(id: string | "skip") {
  return useQuery(api.queries.getContract, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for Contract.draft. */
export function useContractDraft() {
  const mutate = useMutation(api.mutations.Contract_draft);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ContractDraftParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Contract.expire. */
export function useContractExpire() {
  const mutate = useMutation(api.mutations.Contract_expire);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ContractExpireParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Contract.markViewed. */
export function useContractMarkViewed() {
  const mutate = useMutation(api.mutations.Contract_markViewed);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ContractMarkViewedParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Contract.markVoided. */
export function useContractMarkVoided() {
  const mutate = useMutation(api.mutations.Contract_markVoided);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ContractMarkVoidedParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Contract.reassignClient. */
export function useContractReassignClient() {
  const mutate = useMutation(api.mutations.Contract_reassignClient);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ContractReassignClientParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Contract.send. */
export function useContractSend() {
  const mutate = useMutation(api.mutations.Contract_send);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ContractSendParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Contract.sign. */
export function useContractSign() {
  const mutate = useMutation(api.mutations.Contract_sign);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ContractSignParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Contract.stageClientMerge. */
export function useContractStageClientMerge() {
  const mutate = useMutation(api.mutations.Contract_stageClientMerge);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ContractStageClientMergeParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for Contract.draft. */
export function useCreateContract() {
  const mutate = useMutation(api.mutations.Contract_createViaDraft);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = ContractDraftParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for CorrectiveAction. */
export function useListCorrectiveAction() {
  return useQuery(api.queries.listCorrectiveAction);
}

/** Reactive get-by-id for CorrectiveAction. Pass "skip" to suspend. */
export function useGetCorrectiveAction(id: string | "skip") {
  return useQuery(api.queries.getCorrectiveAction, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for CorrectiveAction.close. */
export function useCorrectiveActionClose() {
  const mutate = useMutation(api.mutations.CorrectiveAction_close);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = CorrectiveActionCloseParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for CorrectiveAction.open. */
export function useCorrectiveActionOpen() {
  const mutate = useMutation(api.mutations.CorrectiveAction_open);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = CorrectiveActionOpenParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for CorrectiveAction.open. */
export function useCreateCorrectiveAction() {
  const mutate = useMutation(api.mutations.CorrectiveAction_createViaOpen);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = CorrectiveActionOpenParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for CreditMemo. */
export function useListCreditMemo() {
  return useQuery(api.queries.listCreditMemo);
}

/** Reactive get-by-id for CreditMemo. Pass "skip" to suspend. */
export function useGetCreditMemo(id: string | "skip") {
  return useQuery(api.queries.getCreditMemo, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for CreditMemo.issue. */
export function useCreditMemoIssue() {
  const mutate = useMutation(api.mutations.CreditMemo_issue);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = CreditMemoIssueParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for CreditMemo.reassignClient. */
export function useCreditMemoReassignClient() {
  const mutate = useMutation(api.mutations.CreditMemo_reassignClient);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = CreditMemoReassignClientParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for CreditMemo.stageClientMerge. */
export function useCreditMemoStageClientMerge() {
  const mutate = useMutation(api.mutations.CreditMemo_stageClientMerge);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = CreditMemoStageClientMergeParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for CreditMemo.issue. */
export function useCreateCreditMemo() {
  const mutate = useMutation(api.mutations.CreditMemo_createViaIssue);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = CreditMemoIssueParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for CutoverDecision. */
export function useListCutoverDecision() {
  return useQuery(api.queries.listCutoverDecision);
}

/** Reactive get-by-id for CutoverDecision. Pass "skip" to suspend. */
export function useGetCutoverDecision(id: string | "skip") {
  return useQuery(api.queries.getCutoverDecision, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for CutoverDecision.create. */
export function useCutoverDecisionCreate() {
  const mutate = useMutation(api.mutations.CutoverDecision_create);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = CutoverDecisionCreateParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for CutoverDecision.execute. */
export function useCutoverDecisionExecute() {
  const mutate = useMutation(api.mutations.CutoverDecision_execute);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = CutoverDecisionExecuteParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for CutoverDecision.recordApprovals. */
export function useCutoverDecisionRecordApprovals() {
  const mutate = useMutation(api.mutations.CutoverDecision_recordApprovals);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = CutoverDecisionRecordApprovalsParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for CutoverDecision.rollback. */
export function useCutoverDecisionRollback() {
  const mutate = useMutation(api.mutations.CutoverDecision_rollback);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = CutoverDecisionRollbackParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for CutoverDecision.setTppReadOnly. */
export function useCutoverDecisionSetTppReadOnly() {
  const mutate = useMutation(api.mutations.CutoverDecision_setTppReadOnly);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = CutoverDecisionSetTppReadOnlyParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Reactive list for Delivery. */
export function useListDelivery() {
  return useQuery(api.queries.listDelivery);
}

/** Reactive get-by-id for Delivery. Pass "skip" to suspend. */
export function useGetDelivery(id: string | "skip") {
  return useQuery(api.queries.getDelivery, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for Delivery.cancel. */
export function useDeliveryCancel() {
  const mutate = useMutation(api.mutations.Delivery_cancel);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = DeliveryCancelParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Delivery.confirmDelivery. */
export function useDeliveryConfirmDelivery() {
  const mutate = useMutation(api.mutations.Delivery_confirmDelivery);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = DeliveryConfirmDeliveryParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Delivery.markFailed. */
export function useDeliveryMarkFailed() {
  const mutate = useMutation(api.mutations.Delivery_markFailed);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = DeliveryMarkFailedParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Delivery.schedule. */
export function useDeliverySchedule() {
  const mutate = useMutation(api.mutations.Delivery_schedule);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = DeliveryScheduleParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Delivery.startTransit. */
export function useDeliveryStartTransit() {
  const mutate = useMutation(api.mutations.Delivery_startTransit);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = DeliveryStartTransitParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for Delivery.schedule. */
export function useCreateDelivery() {
  const mutate = useMutation(api.mutations.Delivery_createViaSchedule);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = DeliveryScheduleParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for Dish. */
export function useListDish() {
  return useQuery(api.queries.listDish);
}

/** Reactive get-by-id for Dish. Pass "skip" to suspend. */
export function useGetDish(id: string | "skip") {
  return useQuery(api.queries.getDish, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for Dish.classifyAllergens. */
export function useDishClassifyAllergens() {
  const mutate = useMutation(api.mutations.Dish_classifyAllergens);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = DishClassifyAllergensParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Dish.clearPrimaryImage. */
export function useDishClearPrimaryImage() {
  const mutate = useMutation(api.mutations.Dish_clearPrimaryImage);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = DishClearPrimaryImageParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Dish.introduce. */
export function useDishIntroduce() {
  const mutate = useMutation(api.mutations.Dish_introduce);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = DishIntroduceParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Dish.linkAsEdition. */
export function useDishLinkAsEdition() {
  const mutate = useMutation(api.mutations.Dish_linkAsEdition);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = DishLinkAsEditionParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Dish.mergeInto. */
export function useDishMergeInto() {
  const mutate = useMutation(api.mutations.Dish_mergeInto);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = DishMergeIntoParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Dish.purge. */
export function useDishPurge() {
  const mutate = useMutation(api.mutations.Dish_purge);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = DishPurgeParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Dish.reinstate. */
export function useDishReinstate() {
  const mutate = useMutation(api.mutations.Dish_reinstate);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = DishReinstateParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Dish.retire. */
export function useDishRetire() {
  const mutate = useMutation(api.mutations.Dish_retire);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = DishRetireParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Dish.reviseDetails. */
export function useDishReviseDetails() {
  const mutate = useMutation(api.mutations.Dish_reviseDetails);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = DishReviseDetailsParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Dish.setPrimaryImage. */
export function useDishSetPrimaryImage() {
  const mutate = useMutation(api.mutations.Dish_setPrimaryImage);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = DishSetPrimaryImageParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Dish.updatePortioning. */
export function useDishUpdatePortioning() {
  const mutate = useMutation(api.mutations.Dish_updatePortioning);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = DishUpdatePortioningParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for Dish.introduce. */
export function useCreateDish() {
  const mutate = useMutation(api.mutations.Dish_createViaIntroduce);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = DishIntroduceParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for DishComponent. */
export function useListDishComponent() {
  return useQuery(api.queries.listDishComponent);
}

/** Reactive get-by-id for DishComponent. Pass "skip" to suspend. */
export function useGetDishComponent(id: string | "skip") {
  return useQuery(api.queries.getDishComponent, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for DishComponent.attach. */
export function useDishComponentAttach() {
  const mutate = useMutation(api.mutations.DishComponent_attach);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = DishComponentAttachParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for DishComponent.detach. */
export function useDishComponentDetach() {
  const mutate = useMutation(api.mutations.DishComponent_detach);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = DishComponentDetachParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for DishComponent.attach. */
export function useCreateDishComponent() {
  const mutate = useMutation(api.mutations.DishComponent_createViaAttach);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = DishComponentAttachParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for DishContainer. */
export function useListDishContainer() {
  return useQuery(api.queries.listDishContainer);
}

/** Reactive get-by-id for DishContainer. Pass "skip" to suspend. */
export function useGetDishContainer(id: string | "skip") {
  return useQuery(api.queries.getDishContainer, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for DishContainer.define. */
export function useDishContainerDefine() {
  const mutate = useMutation(api.mutations.DishContainer_define);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = DishContainerDefineParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for DishContainer.reinstate. */
export function useDishContainerReinstate() {
  const mutate = useMutation(api.mutations.DishContainer_reinstate);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = DishContainerReinstateParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for DishContainer.retire. */
export function useDishContainerRetire() {
  const mutate = useMutation(api.mutations.DishContainer_retire);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = DishContainerRetireParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for DishContainer.revise. */
export function useDishContainerRevise() {
  const mutate = useMutation(api.mutations.DishContainer_revise);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = DishContainerReviseParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for DishContainer.define. */
export function useCreateDishContainer() {
  const mutate = useMutation(api.mutations.DishContainer_createViaDefine);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = DishContainerDefineParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for DishIngredient. */
export function useListDishIngredient() {
  return useQuery(api.queries.listDishIngredient);
}

/** Reactive get-by-id for DishIngredient. Pass "skip" to suspend. */
export function useGetDishIngredient(id: string | "skip") {
  return useQuery(api.queries.getDishIngredient, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for DishIngredient.add. */
export function useDishIngredientAdd() {
  const mutate = useMutation(api.mutations.DishIngredient_add);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = DishIngredientAddParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for DishIngredient.adjustQuantity. */
export function useDishIngredientAdjustQuantity() {
  const mutate = useMutation(api.mutations.DishIngredient_adjustQuantity);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = DishIngredientAdjustQuantityParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for DishIngredient.remove. */
export function useDishIngredientRemove() {
  const mutate = useMutation(api.mutations.DishIngredient_remove);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = DishIngredientRemoveParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for DishIngredient.add. */
export function useCreateDishIngredient() {
  const mutate = useMutation(api.mutations.DishIngredient_createViaAdd);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = DishIngredientAddParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for DishTask. */
export function useListDishTask() {
  return useQuery(api.queries.listDishTask);
}

/** Reactive get-by-id for DishTask. Pass "skip" to suspend. */
export function useGetDishTask(id: string | "skip") {
  return useQuery(api.queries.getDishTask, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for DishTask.add. */
export function useDishTaskAdd() {
  const mutate = useMutation(api.mutations.DishTask_add);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = DishTaskAddParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for DishTask.backfillActiveKey. */
export function useDishTaskBackfillActiveKey() {
  const mutate = useMutation(api.mutations.DishTask_backfillActiveKey);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = DishTaskBackfillActiveKeyParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for DishTask.retire. */
export function useDishTaskRetire() {
  const mutate = useMutation(api.mutations.DishTask_retire);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = DishTaskRetireParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for DishTask.revise. */
export function useDishTaskRevise() {
  const mutate = useMutation(api.mutations.DishTask_revise);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = DishTaskReviseParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for DishTask.add. */
export function useCreateDishTask() {
  const mutate = useMutation(api.mutations.DishTask_createViaAdd);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = DishTaskAddParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for EmailNotificationSubscription. */
export function useListEmailNotificationSubscription() {
  return useQuery(api.queries.listEmailNotificationSubscription);
}

/** Reactive get-by-id for EmailNotificationSubscription. Pass "skip" to suspend. */
export function useGetEmailNotificationSubscription(id: string | "skip") {
  return useQuery(api.queries.getEmailNotificationSubscription, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for EmailNotificationSubscription.configure. */
export function useEmailNotificationSubscriptionConfigure() {
  const mutate = useMutation(api.mutations.EmailNotificationSubscription_configure);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = EmailNotificationSubscriptionConfigureParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for EmailNotificationSubscription.updateSubscriptions. */
export function useEmailNotificationSubscriptionUpdateSubscriptions() {
  const mutate = useMutation(api.mutations.EmailNotificationSubscription_updateSubscriptions);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = EmailNotificationSubscriptionUpdateSubscriptionsParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for EmailNotificationSubscription.configure. */
export function useCreateEmailNotificationSubscription() {
  const mutate = useMutation(api.mutations.EmailNotificationSubscription_createViaConfigure);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = EmailNotificationSubscriptionConfigureParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for Equipment. */
export function useListEquipment() {
  return useQuery(api.queries.listEquipment);
}

/** Reactive get-by-id for Equipment. Pass "skip" to suspend. */
export function useGetEquipment(id: string | "skip") {
  return useQuery(api.queries.getEquipment, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for Equipment.reactivate. */
export function useEquipmentReactivate() {
  const mutate = useMutation(api.mutations.Equipment_reactivate);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = EquipmentReactivateParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Equipment.recount. */
export function useEquipmentRecount() {
  const mutate = useMutation(api.mutations.Equipment_recount);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = EquipmentRecountParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Equipment.register. */
export function useEquipmentRegister() {
  const mutate = useMutation(api.mutations.Equipment_register);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = EquipmentRegisterParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Equipment.retire. */
export function useEquipmentRetire() {
  const mutate = useMutation(api.mutations.Equipment_retire);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = EquipmentRetireParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Equipment.reviseDetails. */
export function useEquipmentReviseDetails() {
  const mutate = useMutation(api.mutations.Equipment_reviseDetails);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = EquipmentReviseDetailsParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Equipment.updateCondition. */
export function useEquipmentUpdateCondition() {
  const mutate = useMutation(api.mutations.Equipment_updateCondition);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = EquipmentUpdateConditionParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for Equipment.register. */
export function useCreateEquipment() {
  const mutate = useMutation(api.mutations.Equipment_createViaRegister);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = EquipmentRegisterParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for EquipmentMaintenanceTask. */
export function useListEquipmentMaintenanceTask() {
  return useQuery(api.queries.listEquipmentMaintenanceTask);
}

/** Reactive get-by-id for EquipmentMaintenanceTask. Pass "skip" to suspend. */
export function useGetEquipmentMaintenanceTask(id: string | "skip") {
  return useQuery(api.queries.getEquipmentMaintenanceTask, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for EquipmentMaintenanceTask.applyService. */
export function useEquipmentMaintenanceTaskApplyService() {
  const mutate = useMutation(api.mutations.EquipmentMaintenanceTask_applyService);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = EquipmentMaintenanceTaskApplyServiceParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for EquipmentMaintenanceTask.schedule. */
export function useEquipmentMaintenanceTaskSchedule() {
  const mutate = useMutation(api.mutations.EquipmentMaintenanceTask_schedule);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = EquipmentMaintenanceTaskScheduleParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for EquipmentMaintenanceTask.schedule. */
export function useCreateEquipmentMaintenanceTask() {
  const mutate = useMutation(api.mutations.EquipmentMaintenanceTask_createViaSchedule);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = EquipmentMaintenanceTaskScheduleParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for EquipmentReservation. */
export function useListEquipmentReservation() {
  return useQuery(api.queries.listEquipmentReservation);
}

/** Reactive get-by-id for EquipmentReservation. Pass "skip" to suspend. */
export function useGetEquipmentReservation(id: string | "skip") {
  return useQuery(api.queries.getEquipmentReservation, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for EquipmentReservation.cancel. */
export function useEquipmentReservationCancel() {
  const mutate = useMutation(api.mutations.EquipmentReservation_cancel);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = EquipmentReservationCancelParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for EquipmentReservation.checkOut. */
export function useEquipmentReservationCheckOut() {
  const mutate = useMutation(api.mutations.EquipmentReservation_checkOut);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = EquipmentReservationCheckOutParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for EquipmentReservation.markReturned. */
export function useEquipmentReservationMarkReturned() {
  const mutate = useMutation(api.mutations.EquipmentReservation_markReturned);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = EquipmentReservationMarkReturnedParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Reactive list for EquipmentServiceEntry. */
export function useListEquipmentServiceEntry() {
  return useQuery(api.queries.listEquipmentServiceEntry);
}

/** Reactive get-by-id for EquipmentServiceEntry. Pass "skip" to suspend. */
export function useGetEquipmentServiceEntry(id: string | "skip") {
  return useQuery(api.queries.getEquipmentServiceEntry, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for EquipmentServiceEntry.record. */
export function useEquipmentServiceEntryRecord() {
  const mutate = useMutation(api.mutations.EquipmentServiceEntry_record);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = EquipmentServiceEntryRecordParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for EquipmentServiceEntry.record. */
export function useCreateEquipmentServiceEntry() {
  const mutate = useMutation(api.mutations.EquipmentServiceEntry_createViaRecord);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = EquipmentServiceEntryRecordParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for Event. */
export function useListEvent() {
  return useQuery(api.queries.listEvent);
}

/** Reactive get-by-id for Event. Pass "skip" to suspend. */
export function useGetEvent(id: string | "skip") {
  return useQuery(api.queries.getEvent, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for Event.approve. */
export function useEventApprove() {
  const mutate = useMutation(api.mutations.Event_approve);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = EventApproveParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Event.assignOwner. */
export function useEventAssignOwner() {
  const mutate = useMutation(api.mutations.Event_assignOwner);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = EventAssignOwnerParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Event.beginExecution. */
export function useEventBeginExecution() {
  const mutate = useMutation(api.mutations.Event_beginExecution);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = EventBeginExecutionParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Event.cancel. */
export function useEventCancel() {
  const mutate = useMutation(api.mutations.Event_cancel);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = EventCancelParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Event.changeHeadcount. */
export function useEventChangeHeadcount() {
  const mutate = useMutation(api.mutations.Event_changeHeadcount);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = EventChangeHeadcountParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Event.changePricing. */
export function useEventChangePricing() {
  const mutate = useMutation(api.mutations.Event_changePricing);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = EventChangePricingParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Event.changePrimaryContact. */
export function useEventChangePrimaryContact() {
  const mutate = useMutation(api.mutations.Event_changePrimaryContact);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = EventChangePrimaryContactParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Event.changeRequirements. */
export function useEventChangeRequirements() {
  const mutate = useMutation(api.mutations.Event_changeRequirements);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = EventChangeRequirementsParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Event.changeVenue. */
export function useEventChangeVenue() {
  const mutate = useMutation(api.mutations.Event_changeVenue);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = EventChangeVenueParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Event.closeOut. */
export function useEventCloseOut() {
  const mutate = useMutation(api.mutations.Event_closeOut);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = EventCloseOutParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Event.complete. */
export function useEventComplete() {
  const mutate = useMutation(api.mutations.Event_complete);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = EventCompleteParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Event.configureRecurrence. */
export function useEventConfigureRecurrence() {
  const mutate = useMutation(api.mutations.Event_configureRecurrence);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = EventConfigureRecurrenceParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Event.confirmSalesLock. */
export function useEventConfirmSalesLock() {
  const mutate = useMutation(api.mutations.Event_confirmSalesLock);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = EventConfirmSalesLockParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Event.finalizeEvent. */
export function useEventFinalizeEvent() {
  const mutate = useMutation(api.mutations.Event_finalizeEvent);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = EventFinalizeEventParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Event.lockForSales. */
export function useEventLockForSales() {
  const mutate = useMutation(api.mutations.Event_lockForSales);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = EventLockForSalesParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Event.planEngagement. */
export function useEventPlanEngagement() {
  const mutate = useMutation(api.mutations.Event_planEngagement);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = EventPlanEngagementParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Event.reassignClient. */
export function useEventReassignClient() {
  const mutate = useMutation(api.mutations.Event_reassignClient);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = EventReassignClientParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Event.reschedule. */
export function useEventReschedule() {
  const mutate = useMutation(api.mutations.Event_reschedule);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = EventRescheduleParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Event.returnToPlanning. */
export function useEventReturnToPlanning() {
  const mutate = useMutation(api.mutations.Event_returnToPlanning);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = EventReturnToPlanningParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Event.stageClientMerge. */
export function useEventStageClientMerge() {
  const mutate = useMutation(api.mutations.Event_stageClientMerge);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = EventStageClientMergeParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Event.stopRecurrence. */
export function useEventStopRecurrence() {
  const mutate = useMutation(api.mutations.Event_stopRecurrence);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = EventStopRecurrenceParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Event.submitForApproval. */
export function useEventSubmitForApproval() {
  const mutate = useMutation(api.mutations.Event_submitForApproval);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = EventSubmitForApprovalParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for Event.planEngagement. */
export function useCreateEvent() {
  const mutate = useMutation(api.mutations.Event_createViaPlanEngagement);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = EventPlanEngagementParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for EventAllergenCheck. */
export function useListEventAllergenCheck() {
  return useQuery(api.queries.listEventAllergenCheck);
}

/** Reactive get-by-id for EventAllergenCheck. Pass "skip" to suspend. */
export function useGetEventAllergenCheck(id: string | "skip") {
  return useQuery(api.queries.getEventAllergenCheck, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for EventAllergenCheck.record. */
export function useEventAllergenCheckRecord() {
  const mutate = useMutation(api.mutations.EventAllergenCheck_record);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = EventAllergenCheckRecordParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for EventAllergenCheck.record. */
export function useCreateEventAllergenCheck() {
  const mutate = useMutation(api.mutations.EventAllergenCheck_createViaRecord);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = EventAllergenCheckRecordParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for EventAssignment. */
export function useListEventAssignment() {
  return useQuery(api.queries.listEventAssignment);
}

/** Reactive get-by-id for EventAssignment. Pass "skip" to suspend. */
export function useGetEventAssignment(id: string | "skip") {
  return useQuery(api.queries.getEventAssignment, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for EventAssignment.assign. */
export function useEventAssignmentAssign() {
  const mutate = useMutation(api.mutations.EventAssignment_assign);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = EventAssignmentAssignParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for EventAssignment.checkIn. */
export function useEventAssignmentCheckIn() {
  const mutate = useMutation(api.mutations.EventAssignment_checkIn);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = EventAssignmentCheckInParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for EventAssignment.checkOut. */
export function useEventAssignmentCheckOut() {
  const mutate = useMutation(api.mutations.EventAssignment_checkOut);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = EventAssignmentCheckOutParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for EventAssignment.confirm. */
export function useEventAssignmentConfirm() {
  const mutate = useMutation(api.mutations.EventAssignment_confirm);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = EventAssignmentConfirmParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for EventAssignment.markNoShow. */
export function useEventAssignmentMarkNoShow() {
  const mutate = useMutation(api.mutations.EventAssignment_markNoShow);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = EventAssignmentMarkNoShowParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for EventAssignment.unassign. */
export function useEventAssignmentUnassign() {
  const mutate = useMutation(api.mutations.EventAssignment_unassign);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = EventAssignmentUnassignParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for EventAssignment.assign. */
export function useCreateEventAssignment() {
  const mutate = useMutation(api.mutations.EventAssignment_createViaAssign);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = EventAssignmentAssignParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for EventCloseout. */
export function useListEventCloseout() {
  return useQuery(api.queries.listEventCloseout);
}

/** Reactive get-by-id for EventCloseout. Pass "skip" to suspend. */
export function useGetEventCloseout(id: string | "skip") {
  return useQuery(api.queries.getEventCloseout, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for EventCloseout.capture. */
export function useEventCloseoutCapture() {
  const mutate = useMutation(api.mutations.EventCloseout_capture);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = EventCloseoutCaptureParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for EventCloseout.finalize. */
export function useEventCloseoutFinalize() {
  const mutate = useMutation(api.mutations.EventCloseout_finalize);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = EventCloseoutFinalizeParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for EventCloseout.capture. */
export function useCreateEventCloseout() {
  const mutate = useMutation(api.mutations.EventCloseout_createViaCapture);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = EventCloseoutCaptureParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for EventDish. */
export function useListEventDish() {
  return useQuery(api.queries.listEventDish);
}

/** Reactive get-by-id for EventDish. Pass "skip" to suspend. */
export function useGetEventDish(id: string | "skip") {
  return useQuery(api.queries.getEventDish, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for EventDish.addToEvent. */
export function useEventDishAddToEvent() {
  const mutate = useMutation(api.mutations.EventDish_addToEvent);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = EventDishAddToEventParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for EventDish.adjustServings. */
export function useEventDishAdjustServings() {
  const mutate = useMutation(api.mutations.EventDish_adjustServings);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = EventDishAdjustServingsParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for EventDish.changeCourse. */
export function useEventDishChangeCourse() {
  const mutate = useMutation(api.mutations.EventDish_changeCourse);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = EventDishChangeCourseParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for EventDish.confirmFromProposal. */
export function useEventDishConfirmFromProposal() {
  const mutate = useMutation(api.mutations.EventDish_confirmFromProposal);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = EventDishConfirmFromProposalParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for EventDish.remove. */
export function useEventDishRemove() {
  const mutate = useMutation(api.mutations.EventDish_remove);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = EventDishRemoveParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for EventDish.requestContainerPack. */
export function useEventDishRequestContainerPack() {
  const mutate = useMutation(api.mutations.EventDish_requestContainerPack);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = EventDishRequestContainerPackParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for EventDish.setHeadcountOverride. */
export function useEventDishSetHeadcountOverride() {
  const mutate = useMutation(api.mutations.EventDish_setHeadcountOverride);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = EventDishSetHeadcountOverrideParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for EventDish.updateInstructions. */
export function useEventDishUpdateInstructions() {
  const mutate = useMutation(api.mutations.EventDish_updateInstructions);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = EventDishUpdateInstructionsParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for EventDish.addToEvent. */
export function useCreateEventDish() {
  const mutate = useMutation(api.mutations.EventDish_createViaAddToEvent);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = EventDishAddToEventParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for EventDishComponentSeed. */
export function useListEventDishComponentSeed() {
  return useQuery(api.queries.listEventDishComponentSeed);
}

/** Reactive get-by-id for EventDishComponentSeed. Pass "skip" to suspend. */
export function useGetEventDishComponentSeed(id: string | "skip") {
  return useQuery(api.queries.getEventDishComponentSeed, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for EventDishComponentSeed.seed. */
export function useEventDishComponentSeedSeed() {
  const mutate = useMutation(api.mutations.EventDishComponentSeed_seed);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = EventDishComponentSeedSeedParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for EventDishComponentSeed.seed. */
export function useCreateEventDishComponentSeed() {
  const mutate = useMutation(api.mutations.EventDishComponentSeed_createViaSeed);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = EventDishComponentSeedSeedParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for EventGuest. */
export function useListEventGuest() {
  return useQuery(api.queries.listEventGuest);
}

/** Reactive get-by-id for EventGuest. Pass "skip" to suspend. */
export function useGetEventGuest(id: string | "skip") {
  return useQuery(api.queries.getEventGuest, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for EventGuest.assignTable. */
export function useEventGuestAssignTable() {
  const mutate = useMutation(api.mutations.EventGuest_assignTable);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = EventGuestAssignTableParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for EventGuest.checkIn. */
export function useEventGuestCheckIn() {
  const mutate = useMutation(api.mutations.EventGuest_checkIn);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = EventGuestCheckInParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for EventGuest.invite. */
export function useEventGuestInvite() {
  const mutate = useMutation(api.mutations.EventGuest_invite);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = EventGuestInviteParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for EventGuest.rsvpConfirm. */
export function useEventGuestRsvpConfirm() {
  const mutate = useMutation(api.mutations.EventGuest_rsvpConfirm);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = EventGuestRsvpConfirmParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for EventGuest.rsvpDecline. */
export function useEventGuestRsvpDecline() {
  const mutate = useMutation(api.mutations.EventGuest_rsvpDecline);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = EventGuestRsvpDeclineParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for EventGuest.withdraw. */
export function useEventGuestWithdraw() {
  const mutate = useMutation(api.mutations.EventGuest_withdraw);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = EventGuestWithdrawParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for EventGuest.invite. */
export function useCreateEventGuest() {
  const mutate = useMutation(api.mutations.EventGuest_createViaInvite);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = EventGuestInviteParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for EventIngredientContribution. */
export function useListEventIngredientContribution() {
  return useQuery(api.queries.listEventIngredientContribution);
}

/** Reactive get-by-id for EventIngredientContribution. Pass "skip" to suspend. */
export function useGetEventIngredientContribution(id: string | "skip") {
  return useQuery(api.queries.getEventIngredientContribution, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for EventIngredientContribution.record. */
export function useEventIngredientContributionRecord() {
  const mutate = useMutation(api.mutations.EventIngredientContribution_record);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = EventIngredientContributionRecordParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for EventIngredientContribution.retire. */
export function useEventIngredientContributionRetire() {
  const mutate = useMutation(api.mutations.EventIngredientContribution_retire);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = EventIngredientContributionRetireParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for EventIngredientContribution.revise. */
export function useEventIngredientContributionRevise() {
  const mutate = useMutation(api.mutations.EventIngredientContribution_revise);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = EventIngredientContributionReviseParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for EventIngredientContribution.record. */
export function useCreateEventIngredientContribution() {
  const mutate = useMutation(api.mutations.EventIngredientContribution_createViaRecord);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = EventIngredientContributionRecordParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for EventLayoutSection. */
export function useListEventLayoutSection() {
  return useQuery(api.queries.listEventLayoutSection);
}

/** Reactive get-by-id for EventLayoutSection. Pass "skip" to suspend. */
export function useGetEventLayoutSection(id: string | "skip") {
  return useQuery(api.queries.getEventLayoutSection, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for EventLayoutSection.add. */
export function useEventLayoutSectionAdd() {
  const mutate = useMutation(api.mutations.EventLayoutSection_add);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = EventLayoutSectionAddParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for EventLayoutSection.remove. */
export function useEventLayoutSectionRemove() {
  const mutate = useMutation(api.mutations.EventLayoutSection_remove);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = EventLayoutSectionRemoveParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for EventLayoutSection.update. */
export function useEventLayoutSectionUpdate() {
  const mutate = useMutation(api.mutations.EventLayoutSection_update);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = EventLayoutSectionUpdateParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for EventLayoutSection.add. */
export function useCreateEventLayoutSection() {
  const mutate = useMutation(api.mutations.EventLayoutSection_createViaAdd);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = EventLayoutSectionAddParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for EventStaffNeed. */
export function useListEventStaffNeed() {
  return useQuery(api.queries.listEventStaffNeed);
}

/** Reactive get-by-id for EventStaffNeed. Pass "skip" to suspend. */
export function useGetEventStaffNeed(id: string | "skip") {
  return useQuery(api.queries.getEventStaffNeed, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for EventStaffNeed.cancel. */
export function useEventStaffNeedCancel() {
  const mutate = useMutation(api.mutations.EventStaffNeed_cancel);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = EventStaffNeedCancelParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for EventStaffNeed.claim. */
export function useEventStaffNeedClaim() {
  const mutate = useMutation(api.mutations.EventStaffNeed_claim);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = EventStaffNeedClaimParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for EventStaffNeed.fill. */
export function useEventStaffNeedFill() {
  const mutate = useMutation(api.mutations.EventStaffNeed_fill);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = EventStaffNeedFillParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for EventStaffNeed.postOpen. */
export function useEventStaffNeedPostOpen() {
  const mutate = useMutation(api.mutations.EventStaffNeed_postOpen);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = EventStaffNeedPostOpenParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for EventStaffNeed.releaseClaim. */
export function useEventStaffNeedReleaseClaim() {
  const mutate = useMutation(api.mutations.EventStaffNeed_releaseClaim);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = EventStaffNeedReleaseClaimParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for EventStaffNeed.postOpen. */
export function useCreateEventStaffNeed() {
  const mutate = useMutation(api.mutations.EventStaffNeed_createViaPostOpen);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = EventStaffNeedPostOpenParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for EventTemplate. */
export function useListEventTemplate() {
  return useQuery(api.queries.listEventTemplate);
}

/** Reactive get-by-id for EventTemplate. Pass "skip" to suspend. */
export function useGetEventTemplate(id: string | "skip") {
  return useQuery(api.queries.getEventTemplate, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for EventTemplate.archive. */
export function useEventTemplateArchive() {
  const mutate = useMutation(api.mutations.EventTemplate_archive);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = EventTemplateArchiveParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for EventTemplate.define. */
export function useEventTemplateDefine() {
  const mutate = useMutation(api.mutations.EventTemplate_define);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = EventTemplateDefineParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for EventTemplate.reactivate. */
export function useEventTemplateReactivate() {
  const mutate = useMutation(api.mutations.EventTemplate_reactivate);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = EventTemplateReactivateParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for EventTemplate.revise. */
export function useEventTemplateRevise() {
  const mutate = useMutation(api.mutations.EventTemplate_revise);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = EventTemplateReviseParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for EventTemplate.define. */
export function useCreateEventTemplate() {
  const mutate = useMutation(api.mutations.EventTemplate_createViaDefine);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = EventTemplateDefineParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for EventTimelineActivity. */
export function useListEventTimelineActivity() {
  return useQuery(api.queries.listEventTimelineActivity);
}

/** Reactive get-by-id for EventTimelineActivity. Pass "skip" to suspend. */
export function useGetEventTimelineActivity(id: string | "skip") {
  return useQuery(api.queries.getEventTimelineActivity, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for EventTimelineActivity.adjust. */
export function useEventTimelineActivityAdjust() {
  const mutate = useMutation(api.mutations.EventTimelineActivity_adjust);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = EventTimelineActivityAdjustParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for EventTimelineActivity.remove. */
export function useEventTimelineActivityRemove() {
  const mutate = useMutation(api.mutations.EventTimelineActivity_remove);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = EventTimelineActivityRemoveParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for EventTimelineActivity.schedule. */
export function useEventTimelineActivitySchedule() {
  const mutate = useMutation(api.mutations.EventTimelineActivity_schedule);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = EventTimelineActivityScheduleParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for EventTimelineActivity.schedule. */
export function useCreateEventTimelineActivity() {
  const mutate = useMutation(api.mutations.EventTimelineActivity_createViaSchedule);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = EventTimelineActivityScheduleParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for EventTimelineComment. */
export function useListEventTimelineComment() {
  return useQuery(api.queries.listEventTimelineComment);
}

/** Reactive get-by-id for EventTimelineComment. Pass "skip" to suspend. */
export function useGetEventTimelineComment(id: string | "skip") {
  return useQuery(api.queries.getEventTimelineComment, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for EventTimelineComment.post. */
export function useEventTimelineCommentPost() {
  const mutate = useMutation(api.mutations.EventTimelineComment_post);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = EventTimelineCommentPostParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for EventTimelineComment.remove. */
export function useEventTimelineCommentRemove() {
  const mutate = useMutation(api.mutations.EventTimelineComment_remove);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = EventTimelineCommentRemoveParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for EventTimelineComment.post. */
export function useCreateEventTimelineComment() {
  const mutate = useMutation(api.mutations.EventTimelineComment_createViaPost);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = EventTimelineCommentPostParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for ExternalRecordLink. */
export function useListExternalRecordLink() {
  return useQuery(api.queries.listExternalRecordLink);
}

/** Reactive get-by-id for ExternalRecordLink. Pass "skip" to suspend. */
export function useGetExternalRecordLink(id: string | "skip") {
  return useQuery(api.queries.getExternalRecordLink, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for ExternalRecordLink.discard. */
export function useExternalRecordLinkDiscard() {
  const mutate = useMutation(api.mutations.ExternalRecordLink_discard);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ExternalRecordLinkDiscardParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for ExternalRecordLink.link. */
export function useExternalRecordLinkLink() {
  const mutate = useMutation(api.mutations.ExternalRecordLink_link);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ExternalRecordLinkLinkParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for ExternalRecordLink.resolveConflict. */
export function useExternalRecordLinkResolveConflict() {
  const mutate = useMutation(api.mutations.ExternalRecordLink_resolveConflict);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ExternalRecordLinkResolveConflictParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for ExternalRecordLink.retire. */
export function useExternalRecordLinkRetire() {
  const mutate = useMutation(api.mutations.ExternalRecordLink_retire);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ExternalRecordLinkRetireParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for ExternalRecordLink.unlinkExternalRecord. */
export function useExternalRecordLinkUnlinkExternalRecord() {
  const mutate = useMutation(api.mutations.ExternalRecordLink_unlinkExternalRecord);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ExternalRecordLinkUnlinkExternalRecordParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for ExternalRecordLink.updateCapsuleId. */
export function useExternalRecordLinkUpdateCapsuleId() {
  const mutate = useMutation(api.mutations.ExternalRecordLink_updateCapsuleId);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ExternalRecordLinkUpdateCapsuleIdParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for ExternalRecordLink.verifyLink. */
export function useExternalRecordLinkVerifyLink() {
  const mutate = useMutation(api.mutations.ExternalRecordLink_verifyLink);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ExternalRecordLinkVerifyLinkParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for ExternalRecordLink.discard. */
export function useCreateExternalRecordLink() {
  const mutate = useMutation(api.mutations.ExternalRecordLink_createViaDiscard);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = ExternalRecordLinkDiscardParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for ImportDataset. */
export function useListImportDataset() {
  return useQuery(api.queries.listImportDataset);
}

/** Reactive get-by-id for ImportDataset. Pass "skip" to suspend. */
export function useGetImportDataset(id: string | "skip") {
  return useQuery(api.queries.getImportDataset, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for ImportDataset.activate. */
export function useImportDatasetActivate() {
  const mutate = useMutation(api.mutations.ImportDataset_activate);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ImportDatasetActivateParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for ImportDataset.deactivate. */
export function useImportDatasetDeactivate() {
  const mutate = useMutation(api.mutations.ImportDataset_deactivate);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ImportDatasetDeactivateParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for ImportDataset.recordLastImport. */
export function useImportDatasetRecordLastImport() {
  const mutate = useMutation(api.mutations.ImportDataset_recordLastImport);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ImportDatasetRecordLastImportParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for ImportDataset.register. */
export function useImportDatasetRegister() {
  const mutate = useMutation(api.mutations.ImportDataset_register);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ImportDatasetRegisterParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for ImportDataset.updateConfig. */
export function useImportDatasetUpdateConfig() {
  const mutate = useMutation(api.mutations.ImportDataset_updateConfig);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ImportDatasetUpdateConfigParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Reactive list for ImportRun. */
export function useListImportRun() {
  return useQuery(api.queries.listImportRun);
}

/** Reactive get-by-id for ImportRun. Pass "skip" to suspend. */
export function useGetImportRun(id: string | "skip") {
  return useQuery(api.queries.getImportRun, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for ImportRun.approveReview. */
export function useImportRunApproveReview() {
  const mutate = useMutation(api.mutations.ImportRun_approveReview);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ImportRunApproveReviewParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for ImportRun.beginReview. */
export function useImportRunBeginReview() {
  const mutate = useMutation(api.mutations.ImportRun_beginReview);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ImportRunBeginReviewParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for ImportRun.commit. */
export function useImportRunCommit() {
  const mutate = useMutation(api.mutations.ImportRun_commit);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ImportRunCommitParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for ImportRun.markFailed. */
export function useImportRunMarkFailed() {
  const mutate = useMutation(api.mutations.ImportRun_markFailed);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ImportRunMarkFailedParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for ImportRun.recordParse. */
export function useImportRunRecordParse() {
  const mutate = useMutation(api.mutations.ImportRun_recordParse);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ImportRunRecordParseParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for ImportRun.revert. */
export function useImportRunRevert() {
  const mutate = useMutation(api.mutations.ImportRun_revert);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ImportRunRevertParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for ImportRun.start. */
export function useImportRunStart() {
  const mutate = useMutation(api.mutations.ImportRun_start);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ImportRunStartParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for ImportRun.validate. */
export function useImportRunValidate() {
  const mutate = useMutation(api.mutations.ImportRun_validate);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ImportRunValidateParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Reactive list for Incident. */
export function useListIncident() {
  return useQuery(api.queries.listIncident);
}

/** Reactive get-by-id for Incident. Pass "skip" to suspend. */
export function useGetIncident(id: string | "skip") {
  return useQuery(api.queries.getIncident, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for Incident.beginInvestigation. */
export function useIncidentBeginInvestigation() {
  const mutate = useMutation(api.mutations.Incident_beginInvestigation);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = IncidentBeginInvestigationParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Incident.clearCorrectiveActionLock. */
export function useIncidentClearCorrectiveActionLock() {
  const mutate = useMutation(api.mutations.Incident_clearCorrectiveActionLock);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = IncidentClearCorrectiveActionLockParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Incident.dismiss. */
export function useIncidentDismiss() {
  const mutate = useMutation(api.mutations.Incident_dismiss);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = IncidentDismissParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Incident.markResolved. */
export function useIncidentMarkResolved() {
  const mutate = useMutation(api.mutations.Incident_markResolved);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = IncidentMarkResolvedParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Incident.report. */
export function useIncidentReport() {
  const mutate = useMutation(api.mutations.Incident_report);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = IncidentReportParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for Incident.report. */
export function useCreateIncident() {
  const mutate = useMutation(api.mutations.Incident_createViaReport);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = IncidentReportParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for Ingredient. */
export function useListIngredient() {
  return useQuery(api.queries.listIngredient);
}

/** Reactive get-by-id for Ingredient. Pass "skip" to suspend. */
export function useGetIngredient(id: string | "skip") {
  return useQuery(api.queries.getIngredient, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for Ingredient.classifyAllergens. */
export function useIngredientClassifyAllergens() {
  const mutate = useMutation(api.mutations.Ingredient_classifyAllergens);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = IngredientClassifyAllergensParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Ingredient.clearPrimaryImage. */
export function useIngredientClearPrimaryImage() {
  const mutate = useMutation(api.mutations.Ingredient_clearPrimaryImage);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = IngredientClearPrimaryImageParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Ingredient.configureSubstitutes. */
export function useIngredientConfigureSubstitutes() {
  const mutate = useMutation(api.mutations.Ingredient_configureSubstitutes);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = IngredientConfigureSubstitutesParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Ingredient.discontinue. */
export function useIngredientDiscontinue() {
  const mutate = useMutation(api.mutations.Ingredient_discontinue);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = IngredientDiscontinueParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Ingredient.introduce. */
export function useIngredientIntroduce() {
  const mutate = useMutation(api.mutations.Ingredient_introduce);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = IngredientIntroduceParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Ingredient.linkAsEdition. */
export function useIngredientLinkAsEdition() {
  const mutate = useMutation(api.mutations.Ingredient_linkAsEdition);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = IngredientLinkAsEditionParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Ingredient.mergeInto. */
export function useIngredientMergeInto() {
  const mutate = useMutation(api.mutations.Ingredient_mergeInto);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = IngredientMergeIntoParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Ingredient.purge. */
export function useIngredientPurge() {
  const mutate = useMutation(api.mutations.Ingredient_purge);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = IngredientPurgeParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Ingredient.reinstate. */
export function useIngredientReinstate() {
  const mutate = useMutation(api.mutations.Ingredient_reinstate);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = IngredientReinstateParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Ingredient.setNutrition. */
export function useIngredientSetNutrition() {
  const mutate = useMutation(api.mutations.Ingredient_setNutrition);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = IngredientSetNutritionParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Ingredient.setPreferredVendor. */
export function useIngredientSetPreferredVendor() {
  const mutate = useMutation(api.mutations.Ingredient_setPreferredVendor);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = IngredientSetPreferredVendorParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Ingredient.setPreferredVendors. */
export function useIngredientSetPreferredVendors() {
  const mutate = useMutation(api.mutations.Ingredient_setPreferredVendors);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = IngredientSetPreferredVendorsParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Ingredient.setPrimaryImage. */
export function useIngredientSetPrimaryImage() {
  const mutate = useMutation(api.mutations.Ingredient_setPrimaryImage);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = IngredientSetPrimaryImageParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Ingredient.updateCosting. */
export function useIngredientUpdateCosting() {
  const mutate = useMutation(api.mutations.Ingredient_updateCosting);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = IngredientUpdateCostingParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Ingredient.updateDetails. */
export function useIngredientUpdateDetails() {
  const mutate = useMutation(api.mutations.Ingredient_updateDetails);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = IngredientUpdateDetailsParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for Ingredient.introduce. */
export function useCreateIngredient() {
  const mutate = useMutation(api.mutations.Ingredient_createViaIntroduce);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = IngredientIntroduceParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for IngredientDemand. */
export function useListIngredientDemand() {
  return useQuery(api.queries.listIngredientDemand);
}

/** Reactive get-by-id for IngredientDemand. Pass "skip" to suspend. */
export function useGetIngredientDemand(id: string | "skip") {
  return useQuery(api.queries.getIngredientDemand, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for IngredientDemand.calculate. */
export function useIngredientDemandCalculate() {
  const mutate = useMutation(api.mutations.IngredientDemand_calculate);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = IngredientDemandCalculateParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for IngredientDemand.confirm. */
export function useIngredientDemandConfirm() {
  const mutate = useMutation(api.mutations.IngredientDemand_confirm);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = IngredientDemandConfirmParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for IngredientDemand.ensurePurchaseEligible. */
export function useIngredientDemandEnsurePurchaseEligible() {
  const mutate = useMutation(api.mutations.IngredientDemand_ensurePurchaseEligible);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = IngredientDemandEnsurePurchaseEligibleParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for IngredientDemand.fulfill. */
export function useIngredientDemandFulfill() {
  const mutate = useMutation(api.mutations.IngredientDemand_fulfill);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = IngredientDemandFulfillParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for IngredientDemand.markReleased. */
export function useIngredientDemandMarkReleased() {
  const mutate = useMutation(api.mutations.IngredientDemand_markReleased);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = IngredientDemandMarkReleasedParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for IngredientDemand.recalculate. */
export function useIngredientDemandRecalculate() {
  const mutate = useMutation(api.mutations.IngredientDemand_recalculate);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = IngredientDemandRecalculateParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for IngredientDemand.supersede. */
export function useIngredientDemandSupersede() {
  const mutate = useMutation(api.mutations.IngredientDemand_supersede);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = IngredientDemandSupersedeParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for IngredientDemand.syncFromContributions. */
export function useIngredientDemandSyncFromContributions() {
  const mutate = useMutation(api.mutations.IngredientDemand_syncFromContributions);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = IngredientDemandSyncFromContributionsParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for IngredientDemand.calculate. */
export function useCreateIngredientDemand() {
  const mutate = useMutation(api.mutations.IngredientDemand_createViaCalculate);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = IngredientDemandCalculateParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for IngredientPriceObservation. */
export function useListIngredientPriceObservation() {
  return useQuery(api.queries.listIngredientPriceObservation);
}

/** Reactive get-by-id for IngredientPriceObservation. Pass "skip" to suspend. */
export function useGetIngredientPriceObservation(id: string | "skip") {
  return useQuery(api.queries.getIngredientPriceObservation, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for IngredientPriceObservation.record. */
export function useIngredientPriceObservationRecord() {
  const mutate = useMutation(api.mutations.IngredientPriceObservation_record);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = IngredientPriceObservationRecordParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for IngredientPriceObservation.record. */
export function useCreateIngredientPriceObservation() {
  const mutate = useMutation(api.mutations.IngredientPriceObservation_createViaRecord);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = IngredientPriceObservationRecordParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for IntegrationConnection. */
export function useListIntegrationConnection() {
  return useQuery(api.queries.listIntegrationConnection);
}

/** Reactive get-by-id for IntegrationConnection. Pass "skip" to suspend. */
export function useGetIntegrationConnection(id: string | "skip") {
  return useQuery(api.queries.getIntegrationConnection, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for IntegrationConnection.authorize. */
export function useIntegrationConnectionAuthorize() {
  const mutate = useMutation(api.mutations.IntegrationConnection_authorize);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = IntegrationConnectionAuthorizeParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for IntegrationConnection.disconnect. */
export function useIntegrationConnectionDisconnect() {
  const mutate = useMutation(api.mutations.IntegrationConnection_disconnect);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = IntegrationConnectionDisconnectParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for IntegrationConnection.markConnected. */
export function useIntegrationConnectionMarkConnected() {
  const mutate = useMutation(api.mutations.IntegrationConnection_markConnected);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = IntegrationConnectionMarkConnectedParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for IntegrationConnection.reauthorize. */
export function useIntegrationConnectionReauthorize() {
  const mutate = useMutation(api.mutations.IntegrationConnection_reauthorize);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = IntegrationConnectionReauthorizeParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for IntegrationConnection.recordFailure. */
export function useIntegrationConnectionRecordFailure() {
  const mutate = useMutation(api.mutations.IntegrationConnection_recordFailure);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = IntegrationConnectionRecordFailureParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for IntegrationConnection.recordSyncSuccess. */
export function useIntegrationConnectionRecordSyncSuccess() {
  const mutate = useMutation(api.mutations.IntegrationConnection_recordSyncSuccess);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = IntegrationConnectionRecordSyncSuccessParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for IntegrationConnection.authorize. */
export function useCreateIntegrationConnection() {
  const mutate = useMutation(api.mutations.IntegrationConnection_createViaAuthorize);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = IntegrationConnectionAuthorizeParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for Interview. */
export function useListInterview() {
  return useQuery(api.queries.listInterview);
}

/** Reactive get-by-id for Interview. Pass "skip" to suspend. */
export function useGetInterview(id: string | "skip") {
  return useQuery(api.queries.getInterview, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for Interview.recordOutcome. */
export function useInterviewRecordOutcome() {
  const mutate = useMutation(api.mutations.Interview_recordOutcome);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = InterviewRecordOutcomeParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Interview.schedule. */
export function useInterviewSchedule() {
  const mutate = useMutation(api.mutations.Interview_schedule);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = InterviewScheduleParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for Interview.schedule. */
export function useCreateInterview() {
  const mutate = useMutation(api.mutations.Interview_createViaSchedule);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = InterviewScheduleParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for InventoryItem. */
export function useListInventoryItem() {
  return useQuery(api.queries.listInventoryItem);
}

/** Reactive get-by-id for InventoryItem. Pass "skip" to suspend. */
export function useGetInventoryItem(id: string | "skip") {
  return useQuery(api.queries.getInventoryItem, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for InventoryItem.adjustQuantity. */
export function useInventoryItemAdjustQuantity() {
  const mutate = useMutation(api.mutations.InventoryItem_adjustQuantity);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = InventoryItemAdjustQuantityParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for InventoryItem.open. */
export function useInventoryItemOpen() {
  const mutate = useMutation(api.mutations.InventoryItem_open);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = InventoryItemOpenParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for InventoryItem.receiveStock. */
export function useInventoryItemReceiveStock() {
  const mutate = useMutation(api.mutations.InventoryItem_receiveStock);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = InventoryItemReceiveStockParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for InventoryItem.recount. */
export function useInventoryItemRecount() {
  const mutate = useMutation(api.mutations.InventoryItem_recount);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = InventoryItemRecountParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for InventoryItem.remove. */
export function useInventoryItemRemove() {
  const mutate = useMutation(api.mutations.InventoryItem_remove);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = InventoryItemRemoveParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for InventoryItem.setExpiry. */
export function useInventoryItemSetExpiry() {
  const mutate = useMutation(api.mutations.InventoryItem_setExpiry);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = InventoryItemSetExpiryParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for InventoryItem.transferIn. */
export function useInventoryItemTransferIn() {
  const mutate = useMutation(api.mutations.InventoryItem_transferIn);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = InventoryItemTransferInParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for InventoryItem.transferOut. */
export function useInventoryItemTransferOut() {
  const mutate = useMutation(api.mutations.InventoryItem_transferOut);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = InventoryItemTransferOutParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for InventoryItem.updateLevels. */
export function useInventoryItemUpdateLevels() {
  const mutate = useMutation(api.mutations.InventoryItem_updateLevels);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = InventoryItemUpdateLevelsParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for InventoryItem.open. */
export function useCreateInventoryItem() {
  const mutate = useMutation(api.mutations.InventoryItem_createViaOpen);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = InventoryItemOpenParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for InventoryLot. */
export function useListInventoryLot() {
  return useQuery(api.queries.listInventoryLot);
}

/** Reactive get-by-id for InventoryLot. Pass "skip" to suspend. */
export function useGetInventoryLot(id: string | "skip") {
  return useQuery(api.queries.getInventoryLot, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for InventoryLot.record. */
export function useInventoryLotRecord() {
  const mutate = useMutation(api.mutations.InventoryLot_record);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = InventoryLotRecordParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for InventoryLot.record. */
export function useCreateInventoryLot() {
  const mutate = useMutation(api.mutations.InventoryLot_createViaRecord);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = InventoryLotRecordParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for InventoryReservation. */
export function useListInventoryReservation() {
  return useQuery(api.queries.listInventoryReservation);
}

/** Reactive get-by-id for InventoryReservation. Pass "skip" to suspend. */
export function useGetInventoryReservation(id: string | "skip") {
  return useQuery(api.queries.getInventoryReservation, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for InventoryReservation.consume. */
export function useInventoryReservationConsume() {
  const mutate = useMutation(api.mutations.InventoryReservation_consume);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = InventoryReservationConsumeParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for InventoryReservation.release. */
export function useInventoryReservationRelease() {
  const mutate = useMutation(api.mutations.InventoryReservation_release);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = InventoryReservationReleaseParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for InventoryReservation.reserve. */
export function useInventoryReservationReserve() {
  const mutate = useMutation(api.mutations.InventoryReservation_reserve);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = InventoryReservationReserveParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for InventoryReservation.reserve. */
export function useCreateInventoryReservation() {
  const mutate = useMutation(api.mutations.InventoryReservation_createViaReserve);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = InventoryReservationReserveParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for Invoice. */
export function useListInvoice() {
  return useQuery(api.queries.listInvoice);
}

/** Reactive get-by-id for Invoice. Pass "skip" to suspend. */
export function useGetInvoice(id: string | "skip") {
  return useQuery(api.queries.getInvoice, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for Invoice.applyCredit. */
export function useInvoiceApplyCredit() {
  const mutate = useMutation(api.mutations.Invoice_applyCredit);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = InvoiceApplyCreditParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Invoice.applyPayment. */
export function useInvoiceApplyPayment() {
  const mutate = useMutation(api.mutations.Invoice_applyPayment);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = InvoiceApplyPaymentParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Invoice.issue. */
export function useInvoiceIssue() {
  const mutate = useMutation(api.mutations.Invoice_issue);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = InvoiceIssueParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Invoice.markDepositPaid. */
export function useInvoiceMarkDepositPaid() {
  const mutate = useMutation(api.mutations.Invoice_markDepositPaid);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = InvoiceMarkDepositPaidParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Invoice.markOverdue. */
export function useInvoiceMarkOverdue() {
  const mutate = useMutation(api.mutations.Invoice_markOverdue);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = InvoiceMarkOverdueParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Invoice.markViewed. */
export function useInvoiceMarkViewed() {
  const mutate = useMutation(api.mutations.Invoice_markViewed);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = InvoiceMarkViewedParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Invoice.markVoided. */
export function useInvoiceMarkVoided() {
  const mutate = useMutation(api.mutations.Invoice_markVoided);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = InvoiceMarkVoidedParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Invoice.reassignClient. */
export function useInvoiceReassignClient() {
  const mutate = useMutation(api.mutations.Invoice_reassignClient);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = InvoiceReassignClientParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Invoice.recordCreditMemo. */
export function useInvoiceRecordCreditMemo() {
  const mutate = useMutation(api.mutations.Invoice_recordCreditMemo);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = InvoiceRecordCreditMemoParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Invoice.recordRefund. */
export function useInvoiceRecordRefund() {
  const mutate = useMutation(api.mutations.Invoice_recordRefund);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = InvoiceRecordRefundParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Invoice.send. */
export function useInvoiceSend() {
  const mutate = useMutation(api.mutations.Invoice_send);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = InvoiceSendParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Invoice.sendBalanceReminder. */
export function useInvoiceSendBalanceReminder() {
  const mutate = useMutation(api.mutations.Invoice_sendBalanceReminder);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = InvoiceSendBalanceReminderParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Invoice.setDeposit. */
export function useInvoiceSetDeposit() {
  const mutate = useMutation(api.mutations.Invoice_setDeposit);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = InvoiceSetDepositParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Invoice.stageClientMerge. */
export function useInvoiceStageClientMerge() {
  const mutate = useMutation(api.mutations.Invoice_stageClientMerge);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = InvoiceStageClientMergeParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Invoice.writeOff. */
export function useInvoiceWriteOff() {
  const mutate = useMutation(api.mutations.Invoice_writeOff);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = InvoiceWriteOffParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for Invoice.issue. */
export function useCreateInvoice() {
  const mutate = useMutation(api.mutations.Invoice_createViaIssue);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = InvoiceIssueParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for Lead. */
export function useListLead() {
  return useQuery(api.queries.listLead);
}

/** Reactive get-by-id for Lead. Pass "skip" to suspend. */
export function useGetLead(id: string | "skip") {
  return useQuery(api.queries.getLead, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for Lead.capture. */
export function useLeadCapture() {
  const mutate = useMutation(api.mutations.Lead_capture);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = LeadCaptureParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Lead.confirmConversion. */
export function useLeadConfirmConversion() {
  const mutate = useMutation(api.mutations.Lead_confirmConversion);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = LeadConfirmConversionParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Lead.confirmProposalSent. */
export function useLeadConfirmProposalSent() {
  const mutate = useMutation(api.mutations.Lead_confirmProposalSent);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = LeadConfirmProposalSentParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Lead.reviseDetails. */
export function useLeadReviseDetails() {
  const mutate = useMutation(api.mutations.Lead_reviseDetails);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = LeadReviseDetailsParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Lead.stageConversion. */
export function useLeadStageConversion() {
  const mutate = useMutation(api.mutations.Lead_stageConversion);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = LeadStageConversionParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Lead.stageProposal. */
export function useLeadStageProposal() {
  const mutate = useMutation(api.mutations.Lead_stageProposal);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = LeadStageProposalParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Lead.updatePipeline. */
export function useLeadUpdatePipeline() {
  const mutate = useMutation(api.mutations.Lead_updatePipeline);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = LeadUpdatePipelineParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for Lead.capture. */
export function useCreateLead() {
  const mutate = useMutation(api.mutations.Lead_createViaCapture);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = LeadCaptureParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for Menu. */
export function useListMenu() {
  return useQuery(api.queries.listMenu);
}

/** Reactive get-by-id for Menu. Pass "skip" to suspend. */
export function useGetMenu(id: string | "skip") {
  return useQuery(api.queries.getMenu, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for Menu.archive. */
export function useMenuArchive() {
  const mutate = useMutation(api.mutations.Menu_archive);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = MenuArchiveParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Menu.draft. */
export function useMenuDraft() {
  const mutate = useMutation(api.mutations.Menu_draft);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = MenuDraftParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Menu.markPublished. */
export function useMenuMarkPublished() {
  const mutate = useMutation(api.mutations.Menu_markPublished);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = MenuMarkPublishedParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Menu.restore. */
export function useMenuRestore() {
  const mutate = useMutation(api.mutations.Menu_restore);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = MenuRestoreParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Menu.reviseDetails. */
export function useMenuReviseDetails() {
  const mutate = useMutation(api.mutations.Menu_reviseDetails);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = MenuReviseDetailsParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Menu.unpublish. */
export function useMenuUnpublish() {
  const mutate = useMutation(api.mutations.Menu_unpublish);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = MenuUnpublishParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Menu.updatePricing. */
export function useMenuUpdatePricing() {
  const mutate = useMutation(api.mutations.Menu_updatePricing);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = MenuUpdatePricingParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for Menu.draft. */
export function useCreateMenu() {
  const mutate = useMutation(api.mutations.Menu_createViaDraft);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = MenuDraftParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for MenuDish. */
export function useListMenuDish() {
  return useQuery(api.queries.listMenuDish);
}

/** Reactive get-by-id for MenuDish. Pass "skip" to suspend. */
export function useGetMenuDish(id: string | "skip") {
  return useQuery(api.queries.getMenuDish, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for MenuDish.add. */
export function useMenuDishAdd() {
  const mutate = useMutation(api.mutations.MenuDish_add);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = MenuDishAddParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for MenuDish.remove. */
export function useMenuDishRemove() {
  const mutate = useMutation(api.mutations.MenuDish_remove);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = MenuDishRemoveParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for MenuDish.updateDetails. */
export function useMenuDishUpdateDetails() {
  const mutate = useMutation(api.mutations.MenuDish_updateDetails);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = MenuDishUpdateDetailsParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for MenuDish.updateSellingPrice. */
export function useMenuDishUpdateSellingPrice() {
  const mutate = useMutation(api.mutations.MenuDish_updateSellingPrice);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = MenuDishUpdateSellingPriceParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for MenuDish.add. */
export function useCreateMenuDish() {
  const mutate = useMutation(api.mutations.MenuDish_createViaAdd);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = MenuDishAddParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for Message. */
export function useListMessage() {
  return useQuery(api.queries.listMessage);
}

/** Reactive get-by-id for Message. Pass "skip" to suspend. */
export function useGetMessage(id: string | "skip") {
  return useQuery(api.queries.getMessage, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for Message.post. */
export function useMessagePost() {
  const mutate = useMutation(api.mutations.Message_post);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = MessagePostParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Message.setDelivery. */
export function useMessageSetDelivery() {
  const mutate = useMutation(api.mutations.Message_setDelivery);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = MessageSetDeliveryParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for Message.post. */
export function useCreateMessage() {
  const mutate = useMutation(api.mutations.Message_createViaPost);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = MessagePostParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for MessageThread. */
export function useListMessageThread() {
  return useQuery(api.queries.listMessageThread);
}

/** Reactive get-by-id for MessageThread. Pass "skip" to suspend. */
export function useGetMessageThread(id: string | "skip") {
  return useQuery(api.queries.getMessageThread, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for MessageThread.create. */
export function useMessageThreadCreate() {
  const mutate = useMutation(api.mutations.MessageThread_create);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = MessageThreadCreateParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for MessageThread.linkContact. */
export function useMessageThreadLinkContact() {
  const mutate = useMutation(api.mutations.MessageThread_linkContact);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = MessageThreadLinkContactParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for MessageThread.linkLead. */
export function useMessageThreadLinkLead() {
  const mutate = useMutation(api.mutations.MessageThread_linkLead);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = MessageThreadLinkLeadParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for MessageThread.setStatus. */
export function useMessageThreadSetStatus() {
  const mutate = useMutation(api.mutations.MessageThread_setStatus);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = MessageThreadSetStatusParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Reactive list for Occasion. */
export function useListOccasion() {
  return useQuery(api.queries.listOccasion);
}

/** Reactive get-by-id for Occasion. Pass "skip" to suspend. */
export function useGetOccasion(id: string | "skip") {
  return useQuery(api.queries.getOccasion, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for Occasion.activate. */
export function useOccasionActivate() {
  const mutate = useMutation(api.mutations.Occasion_activate);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = OccasionActivateParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Occasion.deactivate. */
export function useOccasionDeactivate() {
  const mutate = useMutation(api.mutations.Occasion_deactivate);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = OccasionDeactivateParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Occasion.register. */
export function useOccasionRegister() {
  const mutate = useMutation(api.mutations.Occasion_register);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = OccasionRegisterParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Occasion.reviseDetails. */
export function useOccasionReviseDetails() {
  const mutate = useMutation(api.mutations.Occasion_reviseDetails);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = OccasionReviseDetailsParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for Occasion.register. */
export function useCreateOccasion() {
  const mutate = useMutation(api.mutations.Occasion_createViaRegister);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = OccasionRegisterParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for OneOnOne. */
export function useListOneOnOne() {
  return useQuery(api.queries.listOneOnOne);
}

/** Reactive get-by-id for OneOnOne. Pass "skip" to suspend. */
export function useGetOneOnOne(id: string | "skip") {
  return useQuery(api.queries.getOneOnOne, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for OneOnOne.hold. */
export function useOneOnOneHold() {
  const mutate = useMutation(api.mutations.OneOnOne_hold);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = OneOnOneHoldParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for OneOnOne.hold. */
export function useCreateOneOnOne() {
  const mutate = useMutation(api.mutations.OneOnOne_createViaHold);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = OneOnOneHoldParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for OneOnOneAction. */
export function useListOneOnOneAction() {
  return useQuery(api.queries.listOneOnOneAction);
}

/** Reactive get-by-id for OneOnOneAction. Pass "skip" to suspend. */
export function useGetOneOnOneAction(id: string | "skip") {
  return useQuery(api.queries.getOneOnOneAction, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for OneOnOneAction.capture. */
export function useOneOnOneActionCapture() {
  const mutate = useMutation(api.mutations.OneOnOneAction_capture);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = OneOnOneActionCaptureParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for OneOnOneAction.close. */
export function useOneOnOneActionClose() {
  const mutate = useMutation(api.mutations.OneOnOneAction_close);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = OneOnOneActionCloseParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for OneOnOneAction.capture. */
export function useCreateOneOnOneAction() {
  const mutate = useMutation(api.mutations.OneOnOneAction_createViaCapture);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = OneOnOneActionCaptureParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for Organization. */
export function useListOrganization() {
  return useQuery(api.queries.listOrganization);
}

/** Reactive get-by-id for Organization. Pass "skip" to suspend. */
export function useGetOrganization(id: string | "skip") {
  return useQuery(api.queries.getOrganization, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for Organization.configureBranding. */
export function useOrganizationConfigureBranding() {
  const mutate = useMutation(api.mutations.Organization_configureBranding);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = OrganizationConfigureBrandingParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Organization.deactivate. */
export function useOrganizationDeactivate() {
  const mutate = useMutation(api.mutations.Organization_deactivate);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = OrganizationDeactivateParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Organization.reactivate. */
export function useOrganizationReactivate() {
  const mutate = useMutation(api.mutations.Organization_reactivate);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = OrganizationReactivateParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Organization.register. */
export function useOrganizationRegister() {
  const mutate = useMutation(api.mutations.Organization_register);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = OrganizationRegisterParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Organization.rename. */
export function useOrganizationRename() {
  const mutate = useMutation(api.mutations.Organization_rename);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = OrganizationRenameParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Organization.setDefaultCurrency. */
export function useOrganizationSetDefaultCurrency() {
  const mutate = useMutation(api.mutations.Organization_setDefaultCurrency);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = OrganizationSetDefaultCurrencyParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Organization.suspend. */
export function useOrganizationSuspend() {
  const mutate = useMutation(api.mutations.Organization_suspend);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = OrganizationSuspendParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for Organization.register. */
export function useCreateOrganization() {
  const mutate = useMutation(api.mutations.Organization_createViaRegister);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = OrganizationRegisterParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for OrganizationCapabilitySetting. */
export function useListOrganizationCapabilitySetting() {
  return useQuery(api.queries.listOrganizationCapabilitySetting);
}

/** Reactive get-by-id for OrganizationCapabilitySetting. Pass "skip" to suspend. */
export function useGetOrganizationCapabilitySetting(id: string | "skip") {
  return useQuery(api.queries.getOrganizationCapabilitySetting, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for OrganizationCapabilitySetting.register. */
export function useOrganizationCapabilitySettingRegister() {
  const mutate = useMutation(api.mutations.OrganizationCapabilitySetting_register);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = OrganizationCapabilitySettingRegisterParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for OrganizationCapabilitySetting.setEnabled. */
export function useOrganizationCapabilitySettingSetEnabled() {
  const mutate = useMutation(api.mutations.OrganizationCapabilitySetting_setEnabled);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = OrganizationCapabilitySettingSetEnabledParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for OrganizationCapabilitySetting.register. */
export function useCreateOrganizationCapabilitySetting() {
  const mutate = useMutation(api.mutations.OrganizationCapabilitySetting_createViaRegister);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = OrganizationCapabilitySettingRegisterParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for PackList. */
export function useListPackList() {
  return useQuery(api.queries.listPackList);
}

/** Reactive get-by-id for PackList. Pass "skip" to suspend. */
export function useGetPackList(id: string | "skip") {
  return useQuery(api.queries.getPackList, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for PackList.cancel. */
export function usePackListCancel() {
  const mutate = useMutation(api.mutations.PackList_cancel);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = PackListCancelParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for PackList.dispatch. */
export function usePackListDispatch() {
  const mutate = useMutation(api.mutations.PackList_dispatch);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = PackListDispatchParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for PackList.markLoaded. */
export function usePackListMarkLoaded() {
  const mutate = useMutation(api.mutations.PackList_markLoaded);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = PackListMarkLoadedParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for PackList.markPacked. */
export function usePackListMarkPacked() {
  const mutate = useMutation(api.mutations.PackList_markPacked);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = PackListMarkPackedParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for PackList.open. */
export function usePackListOpen() {
  const mutate = useMutation(api.mutations.PackList_open);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = PackListOpenParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for PackList.startPacking. */
export function usePackListStartPacking() {
  const mutate = useMutation(api.mutations.PackList_startPacking);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = PackListStartPackingParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for PackList.open. */
export function useCreatePackList() {
  const mutate = useMutation(api.mutations.PackList_createViaOpen);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = PackListOpenParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for PackListItem. */
export function useListPackListItem() {
  return useQuery(api.queries.listPackListItem);
}

/** Reactive get-by-id for PackListItem. Pass "skip" to suspend. */
export function useGetPackListItem(id: string | "skip") {
  return useQuery(api.queries.getPackListItem, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for PackListItem.addItem. */
export function usePackListItemAddItem() {
  const mutate = useMutation(api.mutations.PackListItem_addItem);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = PackListItemAddItemParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for PackListItem.adjustQuantity. */
export function usePackListItemAdjustQuantity() {
  const mutate = useMutation(api.mutations.PackListItem_adjustQuantity);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = PackListItemAdjustQuantityParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for PackListItem.markMissing. */
export function usePackListItemMarkMissing() {
  const mutate = useMutation(api.mutations.PackListItem_markMissing);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = PackListItemMarkMissingParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for PackListItem.markPacked. */
export function usePackListItemMarkPacked() {
  const mutate = useMutation(api.mutations.PackListItem_markPacked);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = PackListItemMarkPackedParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for PackListItem.addItem. */
export function useCreatePackListItem() {
  const mutate = useMutation(api.mutations.PackListItem_createViaAddItem);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = PackListItemAddItemParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for PackListTemplate. */
export function useListPackListTemplate() {
  return useQuery(api.queries.listPackListTemplate);
}

/** Reactive get-by-id for PackListTemplate. Pass "skip" to suspend. */
export function useGetPackListTemplate(id: string | "skip") {
  return useQuery(api.queries.getPackListTemplate, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for PackListTemplate.archive. */
export function usePackListTemplateArchive() {
  const mutate = useMutation(api.mutations.PackListTemplate_archive);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = PackListTemplateArchiveParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for PackListTemplate.define. */
export function usePackListTemplateDefine() {
  const mutate = useMutation(api.mutations.PackListTemplate_define);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = PackListTemplateDefineParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for PackListTemplate.reactivate. */
export function usePackListTemplateReactivate() {
  const mutate = useMutation(api.mutations.PackListTemplate_reactivate);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = PackListTemplateReactivateParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for PackListTemplate.revise. */
export function usePackListTemplateRevise() {
  const mutate = useMutation(api.mutations.PackListTemplate_revise);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = PackListTemplateReviseParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for PackListTemplate.define. */
export function useCreatePackListTemplate() {
  const mutate = useMutation(api.mutations.PackListTemplate_createViaDefine);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = PackListTemplateDefineParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for Payment. */
export function useListPayment() {
  return useQuery(api.queries.listPayment);
}

/** Reactive get-by-id for Payment. Pass "skip" to suspend. */
export function useGetPayment(id: string | "skip") {
  return useQuery(api.queries.getPayment, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for Payment.beginProcessing. */
export function usePaymentBeginProcessing() {
  const mutate = useMutation(api.mutations.Payment_beginProcessing);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = PaymentBeginProcessingParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Payment.disputeReconciliation. */
export function usePaymentDisputeReconciliation() {
  const mutate = useMutation(api.mutations.Payment_disputeReconciliation);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = PaymentDisputeReconciliationParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Payment.fail. */
export function usePaymentFail() {
  const mutate = useMutation(api.mutations.Payment_fail);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = PaymentFailParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Payment.markMatched. */
export function usePaymentMarkMatched() {
  const mutate = useMutation(api.mutations.Payment_markMatched);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = PaymentMarkMatchedParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Payment.reassignClient. */
export function usePaymentReassignClient() {
  const mutate = useMutation(api.mutations.Payment_reassignClient);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = PaymentReassignClientParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Payment.record. */
export function usePaymentRecord() {
  const mutate = useMutation(api.mutations.Payment_record);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = PaymentRecordParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Payment.refund. */
export function usePaymentRefund() {
  const mutate = useMutation(api.mutations.Payment_refund);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = PaymentRefundParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Payment.settle. */
export function usePaymentSettle() {
  const mutate = useMutation(api.mutations.Payment_settle);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = PaymentSettleParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Payment.stageClientMerge. */
export function usePaymentStageClientMerge() {
  const mutate = useMutation(api.mutations.Payment_stageClientMerge);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = PaymentStageClientMergeParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Payment.updateProviderTransactionIds. */
export function usePaymentUpdateProviderTransactionIds() {
  const mutate = useMutation(api.mutations.Payment_updateProviderTransactionIds);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = PaymentUpdateProviderTransactionIdsParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Payment.verifyReconciliation. */
export function usePaymentVerifyReconciliation() {
  const mutate = useMutation(api.mutations.Payment_verifyReconciliation);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = PaymentVerifyReconciliationParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for Payment.record. */
export function useCreatePayment() {
  const mutate = useMutation(api.mutations.Payment_createViaRecord);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = PaymentRecordParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for PaymentMethod. */
export function useListPaymentMethod() {
  return useQuery(api.queries.listPaymentMethod);
}

/** Reactive get-by-id for PaymentMethod. Pass "skip" to suspend. */
export function useGetPaymentMethod(id: string | "skip") {
  return useQuery(api.queries.getPaymentMethod, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for PaymentMethod.clearDefault. */
export function usePaymentMethodClearDefault() {
  const mutate = useMutation(api.mutations.PaymentMethod_clearDefault);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = PaymentMethodClearDefaultParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for PaymentMethod.expire. */
export function usePaymentMethodExpire() {
  const mutate = useMutation(api.mutations.PaymentMethod_expire);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = PaymentMethodExpireParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for PaymentMethod.invalidate. */
export function usePaymentMethodInvalidate() {
  const mutate = useMutation(api.mutations.PaymentMethod_invalidate);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = PaymentMethodInvalidateParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for PaymentMethod.makeDefault. */
export function usePaymentMethodMakeDefault() {
  const mutate = useMutation(api.mutations.PaymentMethod_makeDefault);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = PaymentMethodMakeDefaultParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for PaymentMethod.reactivate. */
export function usePaymentMethodReactivate() {
  const mutate = useMutation(api.mutations.PaymentMethod_reactivate);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = PaymentMethodReactivateParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for PaymentMethod.reassignClient. */
export function usePaymentMethodReassignClient() {
  const mutate = useMutation(api.mutations.PaymentMethod_reassignClient);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = PaymentMethodReassignClientParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for PaymentMethod.register. */
export function usePaymentMethodRegister() {
  const mutate = useMutation(api.mutations.PaymentMethod_register);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = PaymentMethodRegisterParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for PaymentMethod.remove. */
export function usePaymentMethodRemove() {
  const mutate = useMutation(api.mutations.PaymentMethod_remove);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = PaymentMethodRemoveParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for PaymentMethod.stageClientMerge. */
export function usePaymentMethodStageClientMerge() {
  const mutate = useMutation(api.mutations.PaymentMethod_stageClientMerge);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = PaymentMethodStageClientMergeParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for PaymentMethod.register. */
export function useCreatePaymentMethod() {
  const mutate = useMutation(api.mutations.PaymentMethod_createViaRegister);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = PaymentMethodRegisterParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for PayrollInput. */
export function useListPayrollInput() {
  return useQuery(api.queries.listPayrollInput);
}

/** Reactive get-by-id for PayrollInput. Pass "skip" to suspend. */
export function useGetPayrollInput(id: string | "skip") {
  return useQuery(api.queries.getPayrollInput, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for PayrollInput.finalize. */
export function usePayrollInputFinalize() {
  const mutate = useMutation(api.mutations.PayrollInput_finalize);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = PayrollInputFinalizeParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for PayrollInput.markVoided. */
export function usePayrollInputMarkVoided() {
  const mutate = useMutation(api.mutations.PayrollInput_markVoided);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = PayrollInputMarkVoidedParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for PayrollInput.prepare. */
export function usePayrollInputPrepare() {
  const mutate = useMutation(api.mutations.PayrollInput_prepare);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = PayrollInputPrepareParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for PayrollInput.prepare. */
export function useCreatePayrollInput() {
  const mutate = useMutation(api.mutations.PayrollInput_createViaPrepare);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = PayrollInputPrepareParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for PerformanceReview. */
export function useListPerformanceReview() {
  return useQuery(api.queries.listPerformanceReview);
}

/** Reactive get-by-id for PerformanceReview. Pass "skip" to suspend. */
export function useGetPerformanceReview(id: string | "skip") {
  return useQuery(api.queries.getPerformanceReview, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for PerformanceReview.record. */
export function usePerformanceReviewRecord() {
  const mutate = useMutation(api.mutations.PerformanceReview_record);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = PerformanceReviewRecordParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for PerformanceReview.record. */
export function useCreatePerformanceReview() {
  const mutate = useMutation(api.mutations.PerformanceReview_createViaRecord);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = PerformanceReviewRecordParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for Person. */
export function useListPerson() {
  return useQuery(api.queries.listPerson);
}

/** Reactive get-by-id for Person. Pass "skip" to suspend. */
export function useGetPerson(id: string | "skip") {
  return useQuery(api.queries.getPerson, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for Person.assignRole. */
export function usePersonAssignRole() {
  const mutate = useMutation(api.mutations.Person_assignRole);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = PersonAssignRoleParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Person.correctIdentity. */
export function usePersonCorrectIdentity() {
  const mutate = useMutation(api.mutations.Person_correctIdentity);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = PersonCorrectIdentityParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Person.deactivate. */
export function usePersonDeactivate() {
  const mutate = useMutation(api.mutations.Person_deactivate);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = PersonDeactivateParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Person.hire. */
export function usePersonHire() {
  const mutate = useMutation(api.mutations.Person_hire);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = PersonHireParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Person.linkAccount. */
export function usePersonLinkAccount() {
  const mutate = useMutation(api.mutations.Person_linkAccount);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = PersonLinkAccountParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Person.reactivate. */
export function usePersonReactivate() {
  const mutate = useMutation(api.mutations.Person_reactivate);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = PersonReactivateParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Person.setEmployeeNumber. */
export function usePersonSetEmployeeNumber() {
  const mutate = useMutation(api.mutations.Person_setEmployeeNumber);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = PersonSetEmployeeNumberParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Person.setPayRate. */
export function usePersonSetPayRate() {
  const mutate = useMutation(api.mutations.Person_setPayRate);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = PersonSetPayRateParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Person.setSmsAlerts. */
export function usePersonSetSmsAlerts() {
  const mutate = useMutation(api.mutations.Person_setSmsAlerts);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = PersonSetSmsAlertsParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Person.terminate. */
export function usePersonTerminate() {
  const mutate = useMutation(api.mutations.Person_terminate);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = PersonTerminateParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Person.unlinkAccount. */
export function usePersonUnlinkAccount() {
  const mutate = useMutation(api.mutations.Person_unlinkAccount);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = PersonUnlinkAccountParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for Person.hire. */
export function useCreatePerson() {
  const mutate = useMutation(api.mutations.Person_createViaHire);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = PersonHireParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for PrepTask. */
export function useListPrepTask() {
  return useQuery(api.queries.listPrepTask);
}

/** Reactive get-by-id for PrepTask. Pass "skip" to suspend. */
export function useGetPrepTask(id: string | "skip") {
  return useQuery(api.queries.getPrepTask, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for PrepTask.assign. */
export function usePrepTaskAssign() {
  const mutate = useMutation(api.mutations.PrepTask_assign);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = PrepTaskAssignParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for PrepTask.cancel. */
export function usePrepTaskCancel() {
  const mutate = useMutation(api.mutations.PrepTask_cancel);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = PrepTaskCancelParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for PrepTask.claim. */
export function usePrepTaskClaim() {
  const mutate = useMutation(api.mutations.PrepTask_claim);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = PrepTaskClaimParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for PrepTask.complete. */
export function usePrepTaskComplete() {
  const mutate = useMutation(api.mutations.PrepTask_complete);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = PrepTaskCompleteParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for PrepTask.markBlocked. */
export function usePrepTaskMarkBlocked() {
  const mutate = useMutation(api.mutations.PrepTask_markBlocked);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = PrepTaskMarkBlockedParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for PrepTask.open. */
export function usePrepTaskOpen() {
  const mutate = useMutation(api.mutations.PrepTask_open);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = PrepTaskOpenParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for PrepTask.refreshGenerated. */
export function usePrepTaskRefreshGenerated() {
  const mutate = useMutation(api.mutations.PrepTask_refreshGenerated);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = PrepTaskRefreshGeneratedParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for PrepTask.release. */
export function usePrepTaskRelease() {
  const mutate = useMutation(api.mutations.PrepTask_release);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = PrepTaskReleaseParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for PrepTask.revise. */
export function usePrepTaskRevise() {
  const mutate = useMutation(api.mutations.PrepTask_revise);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = PrepTaskReviseParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for PrepTask.standDown. */
export function usePrepTaskStandDown() {
  const mutate = useMutation(api.mutations.PrepTask_standDown);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = PrepTaskStandDownParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for PrepTask.start. */
export function usePrepTaskStart() {
  const mutate = useMutation(api.mutations.PrepTask_start);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = PrepTaskStartParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for PrepTask.unblock. */
export function usePrepTaskUnblock() {
  const mutate = useMutation(api.mutations.PrepTask_unblock);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = PrepTaskUnblockParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for PrepTask.open. */
export function useCreatePrepTask() {
  const mutate = useMutation(api.mutations.PrepTask_createViaOpen);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = PrepTaskOpenParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for PrepTaskComment. */
export function useListPrepTaskComment() {
  return useQuery(api.queries.listPrepTaskComment);
}

/** Reactive get-by-id for PrepTaskComment. Pass "skip" to suspend. */
export function useGetPrepTaskComment(id: string | "skip") {
  return useQuery(api.queries.getPrepTaskComment, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for PrepTaskComment.edit. */
export function usePrepTaskCommentEdit() {
  const mutate = useMutation(api.mutations.PrepTaskComment_edit);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = PrepTaskCommentEditParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for PrepTaskComment.post. */
export function usePrepTaskCommentPost() {
  const mutate = useMutation(api.mutations.PrepTaskComment_post);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = PrepTaskCommentPostParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for PrepTaskComment.post. */
export function useCreatePrepTaskComment() {
  const mutate = useMutation(api.mutations.PrepTaskComment_createViaPost);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = PrepTaskCommentPostParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for PrepTaskDependency. */
export function useListPrepTaskDependency() {
  return useQuery(api.queries.listPrepTaskDependency);
}

/** Reactive get-by-id for PrepTaskDependency. Pass "skip" to suspend. */
export function useGetPrepTaskDependency(id: string | "skip") {
  return useQuery(api.queries.getPrepTaskDependency, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for PrepTaskDependency.declare. */
export function usePrepTaskDependencyDeclare() {
  const mutate = useMutation(api.mutations.PrepTaskDependency_declare);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = PrepTaskDependencyDeclareParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for PrepTaskDependency.satisfy. */
export function usePrepTaskDependencySatisfy() {
  const mutate = useMutation(api.mutations.PrepTaskDependency_satisfy);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = PrepTaskDependencySatisfyParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for PrepTaskDependency.declare. */
export function useCreatePrepTaskDependency() {
  const mutate = useMutation(api.mutations.PrepTaskDependency_createViaDeclare);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = PrepTaskDependencyDeclareParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for ProductionBatch. */
export function useListProductionBatch() {
  return useQuery(api.queries.listProductionBatch);
}

/** Reactive get-by-id for ProductionBatch. Pass "skip" to suspend. */
export function useGetProductionBatch(id: string | "skip") {
  return useQuery(api.queries.getProductionBatch, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for ProductionBatch.cancel. */
export function useProductionBatchCancel() {
  const mutate = useMutation(api.mutations.ProductionBatch_cancel);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ProductionBatchCancelParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for ProductionBatch.complete. */
export function useProductionBatchComplete() {
  const mutate = useMutation(api.mutations.ProductionBatch_complete);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ProductionBatchCompleteParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for ProductionBatch.plan. */
export function useProductionBatchPlan() {
  const mutate = useMutation(api.mutations.ProductionBatch_plan);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ProductionBatchPlanParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for ProductionBatch.start. */
export function useProductionBatchStart() {
  const mutate = useMutation(api.mutations.ProductionBatch_start);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ProductionBatchStartParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for ProductionBatch.plan. */
export function useCreateProductionBatch() {
  const mutate = useMutation(api.mutations.ProductionBatch_createViaPlan);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = ProductionBatchPlanParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for Proposal. */
export function useListProposal() {
  return useQuery(api.queries.listProposal);
}

/** Reactive get-by-id for Proposal. Pass "skip" to suspend. */
export function useGetProposal(id: string | "skip") {
  return useQuery(api.queries.getProposal, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for Proposal.accept. */
export function useProposalAccept() {
  const mutate = useMutation(api.mutations.Proposal_accept);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ProposalAcceptParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Proposal.decline. */
export function useProposalDecline() {
  const mutate = useMutation(api.mutations.Proposal_decline);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ProposalDeclineParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Proposal.draft. */
export function useProposalDraft() {
  const mutate = useMutation(api.mutations.Proposal_draft);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ProposalDraftParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Proposal.expire. */
export function useProposalExpire() {
  const mutate = useMutation(api.mutations.Proposal_expire);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ProposalExpireParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Proposal.linkEvent. */
export function useProposalLinkEvent() {
  const mutate = useMutation(api.mutations.Proposal_linkEvent);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ProposalLinkEventParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Proposal.markViewed. */
export function useProposalMarkViewed() {
  const mutate = useMutation(api.mutations.Proposal_markViewed);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ProposalMarkViewedParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Proposal.reassignClient. */
export function useProposalReassignClient() {
  const mutate = useMutation(api.mutations.Proposal_reassignClient);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ProposalReassignClientParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Proposal.send. */
export function useProposalSend() {
  const mutate = useMutation(api.mutations.Proposal_send);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ProposalSendParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Proposal.stageClientMerge. */
export function useProposalStageClientMerge() {
  const mutate = useMutation(api.mutations.Proposal_stageClientMerge);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ProposalStageClientMergeParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Proposal.stageEventLink. */
export function useProposalStageEventLink() {
  const mutate = useMutation(api.mutations.Proposal_stageEventLink);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ProposalStageEventLinkParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Proposal.supersede. */
export function useProposalSupersede() {
  const mutate = useMutation(api.mutations.Proposal_supersede);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ProposalSupersedeParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for Proposal.draft. */
export function useCreateProposal() {
  const mutate = useMutation(api.mutations.Proposal_createViaDraft);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = ProposalDraftParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for ProposalDishSelection. */
export function useListProposalDishSelection() {
  return useQuery(api.queries.listProposalDishSelection);
}

/** Reactive get-by-id for ProposalDishSelection. Pass "skip" to suspend. */
export function useGetProposalDishSelection(id: string | "skip") {
  return useQuery(api.queries.getProposalDishSelection, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for ProposalDishSelection.adjustServings. */
export function useProposalDishSelectionAdjustServings() {
  const mutate = useMutation(api.mutations.ProposalDishSelection_adjustServings);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ProposalDishSelectionAdjustServingsParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for ProposalDishSelection.remove. */
export function useProposalDishSelectionRemove() {
  const mutate = useMutation(api.mutations.ProposalDishSelection_remove);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ProposalDishSelectionRemoveParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for ProposalDishSelection.select. */
export function useProposalDishSelectionSelect() {
  const mutate = useMutation(api.mutations.ProposalDishSelection_select);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ProposalDishSelectionSelectParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for ProposalDishSelection.select. */
export function useCreateProposalDishSelection() {
  const mutate = useMutation(api.mutations.ProposalDishSelection_createViaSelect);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = ProposalDishSelectionSelectParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for ProposalEnhancement. */
export function useListProposalEnhancement() {
  return useQuery(api.queries.listProposalEnhancement);
}

/** Reactive get-by-id for ProposalEnhancement. Pass "skip" to suspend. */
export function useGetProposalEnhancement(id: string | "skip") {
  return useQuery(api.queries.getProposalEnhancement, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for ProposalEnhancement.offer. */
export function useProposalEnhancementOffer() {
  const mutate = useMutation(api.mutations.ProposalEnhancement_offer);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ProposalEnhancementOfferParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for ProposalEnhancement.revise. */
export function useProposalEnhancementRevise() {
  const mutate = useMutation(api.mutations.ProposalEnhancement_revise);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ProposalEnhancementReviseParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for ProposalEnhancement.withdraw. */
export function useProposalEnhancementWithdraw() {
  const mutate = useMutation(api.mutations.ProposalEnhancement_withdraw);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ProposalEnhancementWithdrawParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for ProposalEnhancement.offer. */
export function useCreateProposalEnhancement() {
  const mutate = useMutation(api.mutations.ProposalEnhancement_createViaOffer);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = ProposalEnhancementOfferParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for ProposalLineItem. */
export function useListProposalLineItem() {
  return useQuery(api.queries.listProposalLineItem);
}

/** Reactive get-by-id for ProposalLineItem. Pass "skip" to suspend. */
export function useGetProposalLineItem(id: string | "skip") {
  return useQuery(api.queries.getProposalLineItem, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for ProposalLineItem.addLine. */
export function useProposalLineItemAddLine() {
  const mutate = useMutation(api.mutations.ProposalLineItem_addLine);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ProposalLineItemAddLineParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for ProposalLineItem.removeLine. */
export function useProposalLineItemRemoveLine() {
  const mutate = useMutation(api.mutations.ProposalLineItem_removeLine);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ProposalLineItemRemoveLineParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for ProposalLineItem.reviseLine. */
export function useProposalLineItemReviseLine() {
  const mutate = useMutation(api.mutations.ProposalLineItem_reviseLine);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ProposalLineItemReviseLineParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for ProposalLineItem.addLine. */
export function useCreateProposalLineItem() {
  const mutate = useMutation(api.mutations.ProposalLineItem_createViaAddLine);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = ProposalLineItemAddLineParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for ProposalRevision. */
export function useListProposalRevision() {
  return useQuery(api.queries.listProposalRevision);
}

/** Reactive get-by-id for ProposalRevision. Pass "skip" to suspend. */
export function useGetProposalRevision(id: string | "skip") {
  return useQuery(api.queries.getProposalRevision, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for ProposalRevision.capture. */
export function useProposalRevisionCapture() {
  const mutate = useMutation(api.mutations.ProposalRevision_capture);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ProposalRevisionCaptureParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for ProposalRevision.capture. */
export function useCreateProposalRevision() {
  const mutate = useMutation(api.mutations.ProposalRevision_createViaCapture);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = ProposalRevisionCaptureParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for ProposalTemplate. */
export function useListProposalTemplate() {
  return useQuery(api.queries.listProposalTemplate);
}

/** Reactive get-by-id for ProposalTemplate. Pass "skip" to suspend. */
export function useGetProposalTemplate(id: string | "skip") {
  return useQuery(api.queries.getProposalTemplate, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for ProposalTemplate.archive. */
export function useProposalTemplateArchive() {
  const mutate = useMutation(api.mutations.ProposalTemplate_archive);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ProposalTemplateArchiveParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for ProposalTemplate.define. */
export function useProposalTemplateDefine() {
  const mutate = useMutation(api.mutations.ProposalTemplate_define);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ProposalTemplateDefineParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for ProposalTemplate.reactivate. */
export function useProposalTemplateReactivate() {
  const mutate = useMutation(api.mutations.ProposalTemplate_reactivate);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ProposalTemplateReactivateParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for ProposalTemplate.revise. */
export function useProposalTemplateRevise() {
  const mutate = useMutation(api.mutations.ProposalTemplate_revise);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ProposalTemplateReviseParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for ProposalTemplate.define. */
export function useCreateProposalTemplate() {
  const mutate = useMutation(api.mutations.ProposalTemplate_createViaDefine);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = ProposalTemplateDefineParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for PurchaseNeed. */
export function useListPurchaseNeed() {
  return useQuery(api.queries.listPurchaseNeed);
}

/** Reactive get-by-id for PurchaseNeed. Pass "skip" to suspend. */
export function useGetPurchaseNeed(id: string | "skip") {
  return useQuery(api.queries.getPurchaseNeed, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for PurchaseNeed.assignToDraft. */
export function usePurchaseNeedAssignToDraft() {
  const mutate = useMutation(api.mutations.PurchaseNeed_assignToDraft);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = PurchaseNeedAssignToDraftParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for PurchaseNeed.cancel. */
export function usePurchaseNeedCancel() {
  const mutate = useMutation(api.mutations.PurchaseNeed_cancel);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = PurchaseNeedCancelParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for PurchaseNeed.create. */
export function usePurchaseNeedCreate() {
  const mutate = useMutation(api.mutations.PurchaseNeed_create);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = PurchaseNeedCreateParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for PurchaseNeed.markDraftOrdered. */
export function usePurchaseNeedMarkDraftOrdered() {
  const mutate = useMutation(api.mutations.PurchaseNeed_markDraftOrdered);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = PurchaseNeedMarkDraftOrderedParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for PurchaseNeed.markFulfilled. */
export function usePurchaseNeedMarkFulfilled() {
  const mutate = useMutation(api.mutations.PurchaseNeed_markFulfilled);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = PurchaseNeedMarkFulfilledParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for PurchaseNeed.markOrdered. */
export function usePurchaseNeedMarkOrdered() {
  const mutate = useMutation(api.mutations.PurchaseNeed_markOrdered);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = PurchaseNeedMarkOrderedParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for PurchaseNeed.reviseRequired. */
export function usePurchaseNeedReviseRequired() {
  const mutate = useMutation(api.mutations.PurchaseNeed_reviseRequired);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = PurchaseNeedReviseRequiredParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Reactive list for PushSubscription. */
export function useListPushSubscription() {
  return useQuery(api.queries.listPushSubscription);
}

/** Reactive get-by-id for PushSubscription. Pass "skip" to suspend. */
export function useGetPushSubscription(id: string | "skip") {
  return useQuery(api.queries.getPushSubscription, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for PushSubscription.register. */
export function usePushSubscriptionRegister() {
  const mutate = useMutation(api.mutations.PushSubscription_register);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = PushSubscriptionRegisterParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for PushSubscription.unregister. */
export function usePushSubscriptionUnregister() {
  const mutate = useMutation(api.mutations.PushSubscription_unregister);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = PushSubscriptionUnregisterParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for PushSubscription.register. */
export function useCreatePushSubscription() {
  const mutate = useMutation(api.mutations.PushSubscription_createViaRegister);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = PushSubscriptionRegisterParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for Qualification. */
export function useListQualification() {
  return useQuery(api.queries.listQualification);
}

/** Reactive get-by-id for Qualification. Pass "skip" to suspend. */
export function useGetQualification(id: string | "skip") {
  return useQuery(api.queries.getQualification, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for Qualification.expire. */
export function useQualificationExpire() {
  const mutate = useMutation(api.mutations.Qualification_expire);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = QualificationExpireParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Qualification.grant. */
export function useQualificationGrant() {
  const mutate = useMutation(api.mutations.Qualification_grant);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = QualificationGrantParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Qualification.revoke. */
export function useQualificationRevoke() {
  const mutate = useMutation(api.mutations.Qualification_revoke);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = QualificationRevokeParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for Qualification.grant. */
export function useCreateQualification() {
  const mutate = useMutation(api.mutations.Qualification_createViaGrant);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = QualificationGrantParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for QualityCheck. */
export function useListQualityCheck() {
  return useQuery(api.queries.listQualityCheck);
}

/** Reactive get-by-id for QualityCheck. Pass "skip" to suspend. */
export function useGetQualityCheck(id: string | "skip") {
  return useQuery(api.queries.getQualityCheck, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for QualityCheck.fail. */
export function useQualityCheckFail() {
  const mutate = useMutation(api.mutations.QualityCheck_fail);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = QualityCheckFailParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for QualityCheck.open. */
export function useQualityCheckOpen() {
  const mutate = useMutation(api.mutations.QualityCheck_open);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = QualityCheckOpenParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for QualityCheck.pass. */
export function useQualityCheckPass() {
  const mutate = useMutation(api.mutations.QualityCheck_pass);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = QualityCheckPassParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for QualityCheck.reinspect. */
export function useQualityCheckReinspect() {
  const mutate = useMutation(api.mutations.QualityCheck_reinspect);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = QualityCheckReinspectParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for QualityCheck.open. */
export function useCreateQualityCheck() {
  const mutate = useMutation(api.mutations.QualityCheck_createViaOpen);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = QualityCheckOpenParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for QuoteSubmission. */
export function useListQuoteSubmission() {
  return useQuery(api.queries.listQuoteSubmission);
}

/** Reactive get-by-id for QuoteSubmission. Pass "skip" to suspend. */
export function useGetQuoteSubmission(id: string | "skip") {
  return useQuery(api.queries.getQuoteSubmission, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for QuoteSubmission.complete. */
export function useQuoteSubmissionComplete() {
  const mutate = useMutation(api.mutations.QuoteSubmission_complete);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = QuoteSubmissionCompleteParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for QuoteSubmission.create. */
export function useQuoteSubmissionCreate() {
  const mutate = useMutation(api.mutations.QuoteSubmission_create);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = QuoteSubmissionCreateParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for QuoteSubmission.fail. */
export function useQuoteSubmissionFail() {
  const mutate = useMutation(api.mutations.QuoteSubmission_fail);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = QuoteSubmissionFailParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for QuoteSubmission.startProcessing. */
export function useQuoteSubmissionStartProcessing() {
  const mutate = useMutation(api.mutations.QuoteSubmission_startProcessing);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = QuoteSubmissionStartProcessingParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Reactive list for RecurringAvailability. */
export function useListRecurringAvailability() {
  return useQuery(api.queries.listRecurringAvailability);
}

/** Reactive get-by-id for RecurringAvailability. Pass "skip" to suspend. */
export function useGetRecurringAvailability(id: string | "skip") {
  return useQuery(api.queries.getRecurringAvailability, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for RecurringAvailability.declare. */
export function useRecurringAvailabilityDeclare() {
  const mutate = useMutation(api.mutations.RecurringAvailability_declare);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = RecurringAvailabilityDeclareParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for RecurringAvailability.withdraw. */
export function useRecurringAvailabilityWithdraw() {
  const mutate = useMutation(api.mutations.RecurringAvailability_withdraw);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = RecurringAvailabilityWithdrawParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for RecurringAvailability.declare. */
export function useCreateRecurringAvailability() {
  const mutate = useMutation(api.mutations.RecurringAvailability_createViaDeclare);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = RecurringAvailabilityDeclareParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for ReferralSource. */
export function useListReferralSource() {
  return useQuery(api.queries.listReferralSource);
}

/** Reactive get-by-id for ReferralSource. Pass "skip" to suspend. */
export function useGetReferralSource(id: string | "skip") {
  return useQuery(api.queries.getReferralSource, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for ReferralSource.activate. */
export function useReferralSourceActivate() {
  const mutate = useMutation(api.mutations.ReferralSource_activate);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ReferralSourceActivateParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for ReferralSource.deactivate. */
export function useReferralSourceDeactivate() {
  const mutate = useMutation(api.mutations.ReferralSource_deactivate);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ReferralSourceDeactivateParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for ReferralSource.register. */
export function useReferralSourceRegister() {
  const mutate = useMutation(api.mutations.ReferralSource_register);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ReferralSourceRegisterParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for ReferralSource.reviseDetails. */
export function useReferralSourceReviseDetails() {
  const mutate = useMutation(api.mutations.ReferralSource_reviseDetails);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ReferralSourceReviseDetailsParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for ReferralSource.register. */
export function useCreateReferralSource() {
  const mutate = useMutation(api.mutations.ReferralSource_createViaRegister);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = ReferralSourceRegisterParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for RevenueAttribution. */
export function useListRevenueAttribution() {
  return useQuery(api.queries.listRevenueAttribution);
}

/** Reactive get-by-id for RevenueAttribution. Pass "skip" to suspend. */
export function useGetRevenueAttribution(id: string | "skip") {
  return useQuery(api.queries.getRevenueAttribution, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for RevenueAttribution.apply. */
export function useRevenueAttributionApply() {
  const mutate = useMutation(api.mutations.RevenueAttribution_apply);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = RevenueAttributionApplyParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for RevenueAttribution.approve. */
export function useRevenueAttributionApprove() {
  const mutate = useMutation(api.mutations.RevenueAttribution_approve);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = RevenueAttributionApproveParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for RevenueAttribution.create. */
export function useRevenueAttributionCreate() {
  const mutate = useMutation(api.mutations.RevenueAttribution_create);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = RevenueAttributionCreateParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for RevenueAttribution.reject. */
export function useRevenueAttributionReject() {
  const mutate = useMutation(api.mutations.RevenueAttribution_reject);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = RevenueAttributionRejectParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for RevenueAttribution.requestApproval. */
export function useRevenueAttributionRequestApproval() {
  const mutate = useMutation(api.mutations.RevenueAttribution_requestApproval);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = RevenueAttributionRequestApprovalParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for RevenueAttribution.update. */
export function useRevenueAttributionUpdate() {
  const mutate = useMutation(api.mutations.RevenueAttribution_update);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = RevenueAttributionUpdateParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Reactive list for RoleScorecard. */
export function useListRoleScorecard() {
  return useQuery(api.queries.listRoleScorecard);
}

/** Reactive get-by-id for RoleScorecard. Pass "skip" to suspend. */
export function useGetRoleScorecard(id: string | "skip") {
  return useQuery(api.queries.getRoleScorecard, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for RoleScorecard.archive. */
export function useRoleScorecardArchive() {
  const mutate = useMutation(api.mutations.RoleScorecard_archive);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = RoleScorecardArchiveParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for RoleScorecard.define. */
export function useRoleScorecardDefine() {
  const mutate = useMutation(api.mutations.RoleScorecard_define);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = RoleScorecardDefineParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for RoleScorecard.reactivate. */
export function useRoleScorecardReactivate() {
  const mutate = useMutation(api.mutations.RoleScorecard_reactivate);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = RoleScorecardReactivateParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for RoleScorecard.define. */
export function useCreateRoleScorecard() {
  const mutate = useMutation(api.mutations.RoleScorecard_createViaDefine);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = RoleScorecardDefineParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for SavedReportDefinition. */
export function useListSavedReportDefinition() {
  return useQuery(api.queries.listSavedReportDefinition);
}

/** Reactive get-by-id for SavedReportDefinition. Pass "skip" to suspend. */
export function useGetSavedReportDefinition(id: string | "skip") {
  return useQuery(api.queries.getSavedReportDefinition, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for SavedReportDefinition.archive. */
export function useSavedReportDefinitionArchive() {
  const mutate = useMutation(api.mutations.SavedReportDefinition_archive);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = SavedReportDefinitionArchiveParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for SavedReportDefinition.changeSharing. */
export function useSavedReportDefinitionChangeSharing() {
  const mutate = useMutation(api.mutations.SavedReportDefinition_changeSharing);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = SavedReportDefinitionChangeSharingParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for SavedReportDefinition.createDefinition. */
export function useSavedReportDefinitionCreateDefinition() {
  const mutate = useMutation(api.mutations.SavedReportDefinition_createDefinition);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = SavedReportDefinitionCreateDefinitionParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for SavedReportDefinition.rename. */
export function useSavedReportDefinitionRename() {
  const mutate = useMutation(api.mutations.SavedReportDefinition_rename);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = SavedReportDefinitionRenameParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for SavedReportDefinition.restore. */
export function useSavedReportDefinitionRestore() {
  const mutate = useMutation(api.mutations.SavedReportDefinition_restore);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = SavedReportDefinitionRestoreParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for SavedReportDefinition.updateDefinition. */
export function useSavedReportDefinitionUpdateDefinition() {
  const mutate = useMutation(api.mutations.SavedReportDefinition_updateDefinition);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = SavedReportDefinitionUpdateDefinitionParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for SavedReportDefinition.createDefinition. */
export function useCreateSavedReportDefinition() {
  const mutate = useMutation(api.mutations.SavedReportDefinition_createViaCreateDefinition);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = SavedReportDefinitionCreateDefinitionParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for ServiceStyle. */
export function useListServiceStyle() {
  return useQuery(api.queries.listServiceStyle);
}

/** Reactive get-by-id for ServiceStyle. Pass "skip" to suspend. */
export function useGetServiceStyle(id: string | "skip") {
  return useQuery(api.queries.getServiceStyle, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for ServiceStyle.activate. */
export function useServiceStyleActivate() {
  const mutate = useMutation(api.mutations.ServiceStyle_activate);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ServiceStyleActivateParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for ServiceStyle.deactivate. */
export function useServiceStyleDeactivate() {
  const mutate = useMutation(api.mutations.ServiceStyle_deactivate);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ServiceStyleDeactivateParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for ServiceStyle.register. */
export function useServiceStyleRegister() {
  const mutate = useMutation(api.mutations.ServiceStyle_register);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ServiceStyleRegisterParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for ServiceStyle.reviseDetails. */
export function useServiceStyleReviseDetails() {
  const mutate = useMutation(api.mutations.ServiceStyle_reviseDetails);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ServiceStyleReviseDetailsParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for ServiceStyle.register. */
export function useCreateServiceStyle() {
  const mutate = useMutation(api.mutations.ServiceStyle_createViaRegister);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = ServiceStyleRegisterParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for ShareLink. */
export function useListShareLink() {
  return useQuery(api.queries.listShareLink);
}

/** Reactive get-by-id for ShareLink. Pass "skip" to suspend. */
export function useGetShareLink(id: string | "skip") {
  return useQuery(api.queries.getShareLink, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for ShareLink.create. */
export function useShareLinkCreate() {
  const mutate = useMutation(api.mutations.ShareLink_create);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ShareLinkCreateParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for ShareLink.revoke. */
export function useShareLinkRevoke() {
  const mutate = useMutation(api.mutations.ShareLink_revoke);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ShareLinkRevokeParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Reactive list for Shift. */
export function useListShift() {
  return useQuery(api.queries.listShift);
}

/** Reactive get-by-id for Shift. Pass "skip" to suspend. */
export function useGetShift(id: string | "skip") {
  return useQuery(api.queries.getShift, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for Shift.applyApprovedSwap. */
export function useShiftApplyApprovedSwap() {
  const mutate = useMutation(api.mutations.Shift_applyApprovedSwap);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ShiftApplyApprovedSwapParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Shift.cancel. */
export function useShiftCancel() {
  const mutate = useMutation(api.mutations.Shift_cancel);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ShiftCancelParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Shift.complete. */
export function useShiftComplete() {
  const mutate = useMutation(api.mutations.Shift_complete);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ShiftCompleteParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Shift.markNoShow. */
export function useShiftMarkNoShow() {
  const mutate = useMutation(api.mutations.Shift_markNoShow);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ShiftMarkNoShowParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Shift.schedule. */
export function useShiftSchedule() {
  const mutate = useMutation(api.mutations.Shift_schedule);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ShiftScheduleParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Shift.stageApprovedSwap. */
export function useShiftStageApprovedSwap() {
  const mutate = useMutation(api.mutations.Shift_stageApprovedSwap);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ShiftStageApprovedSwapParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Shift.start. */
export function useShiftStart() {
  const mutate = useMutation(api.mutations.Shift_start);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ShiftStartParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for Shift.schedule. */
export function useCreateShift() {
  const mutate = useMutation(api.mutations.Shift_createViaSchedule);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = ShiftScheduleParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for ShiftSwapRequest. */
export function useListShiftSwapRequest() {
  return useQuery(api.queries.listShiftSwapRequest);
}

/** Reactive get-by-id for ShiftSwapRequest. Pass "skip" to suspend. */
export function useGetShiftSwapRequest(id: string | "skip") {
  return useQuery(api.queries.getShiftSwapRequest, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for ShiftSwapRequest.accept. */
export function useShiftSwapRequestAccept() {
  const mutate = useMutation(api.mutations.ShiftSwapRequest_accept);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ShiftSwapRequestAcceptParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for ShiftSwapRequest.approve. */
export function useShiftSwapRequestApprove() {
  const mutate = useMutation(api.mutations.ShiftSwapRequest_approve);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ShiftSwapRequestApproveParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for ShiftSwapRequest.decline. */
export function useShiftSwapRequestDecline() {
  const mutate = useMutation(api.mutations.ShiftSwapRequest_decline);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ShiftSwapRequestDeclineParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for ShiftSwapRequest.propose. */
export function useShiftSwapRequestPropose() {
  const mutate = useMutation(api.mutations.ShiftSwapRequest_propose);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ShiftSwapRequestProposeParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for ShiftSwapRequest.reject. */
export function useShiftSwapRequestReject() {
  const mutate = useMutation(api.mutations.ShiftSwapRequest_reject);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ShiftSwapRequestRejectParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for ShiftSwapRequest.withdraw. */
export function useShiftSwapRequestWithdraw() {
  const mutate = useMutation(api.mutations.ShiftSwapRequest_withdraw);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ShiftSwapRequestWithdrawParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for ShiftSwapRequest.propose. */
export function useCreateShiftSwapRequest() {
  const mutate = useMutation(api.mutations.ShiftSwapRequest_createViaPropose);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = ShiftSwapRequestProposeParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for ShiftType. */
export function useListShiftType() {
  return useQuery(api.queries.listShiftType);
}

/** Reactive get-by-id for ShiftType. Pass "skip" to suspend. */
export function useGetShiftType(id: string | "skip") {
  return useQuery(api.queries.getShiftType, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for ShiftType.define. */
export function useShiftTypeDefine() {
  const mutate = useMutation(api.mutations.ShiftType_define);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ShiftTypeDefineParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for ShiftType.reactivate. */
export function useShiftTypeReactivate() {
  const mutate = useMutation(api.mutations.ShiftType_reactivate);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ShiftTypeReactivateParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for ShiftType.retire. */
export function useShiftTypeRetire() {
  const mutate = useMutation(api.mutations.ShiftType_retire);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = ShiftTypeRetireParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for ShiftType.define. */
export function useCreateShiftType() {
  const mutate = useMutation(api.mutations.ShiftType_createViaDefine);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = ShiftTypeDefineParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for SignatureRequest. */
export function useListSignatureRequest() {
  return useQuery(api.queries.listSignatureRequest);
}

/** Reactive get-by-id for SignatureRequest. Pass "skip" to suspend. */
export function useGetSignatureRequest(id: string | "skip") {
  return useQuery(api.queries.getSignatureRequest, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for SignatureRequest.complete. */
export function useSignatureRequestComplete() {
  const mutate = useMutation(api.mutations.SignatureRequest_complete);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = SignatureRequestCompleteParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for SignatureRequest.expire. */
export function useSignatureRequestExpire() {
  const mutate = useMutation(api.mutations.SignatureRequest_expire);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = SignatureRequestExpireParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for SignatureRequest.requestSignature. */
export function useSignatureRequestRequestSignature() {
  const mutate = useMutation(api.mutations.SignatureRequest_requestSignature);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = SignatureRequestRequestSignatureParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for SignatureRequest.revoke. */
export function useSignatureRequestRevoke() {
  const mutate = useMutation(api.mutations.SignatureRequest_revoke);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = SignatureRequestRevokeParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for SignatureRequest.requestSignature. */
export function useCreateSignatureRequest() {
  const mutate = useMutation(api.mutations.SignatureRequest_createViaRequestSignature);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = SignatureRequestRequestSignatureParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for StaffChatReadCursor. */
export function useListStaffChatReadCursor() {
  return useQuery(api.queries.listStaffChatReadCursor);
}

/** Reactive get-by-id for StaffChatReadCursor. Pass "skip" to suspend. */
export function useGetStaffChatReadCursor(id: string | "skip") {
  return useQuery(api.queries.getStaffChatReadCursor, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for StaffChatReadCursor.open. */
export function useStaffChatReadCursorOpen() {
  const mutate = useMutation(api.mutations.StaffChatReadCursor_open);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = StaffChatReadCursorOpenParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for StaffChatReadCursor.touch. */
export function useStaffChatReadCursorTouch() {
  const mutate = useMutation(api.mutations.StaffChatReadCursor_touch);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = StaffChatReadCursorTouchParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for StaffChatReadCursor.open. */
export function useCreateStaffChatReadCursor() {
  const mutate = useMutation(api.mutations.StaffChatReadCursor_createViaOpen);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = StaffChatReadCursorOpenParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for StaffMessage. */
export function useListStaffMessage() {
  return useQuery(api.queries.listStaffMessage);
}

/** Reactive get-by-id for StaffMessage. Pass "skip" to suspend. */
export function useGetStaffMessage(id: string | "skip") {
  return useQuery(api.queries.getStaffMessage, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for StaffMessage.edit. */
export function useStaffMessageEdit() {
  const mutate = useMutation(api.mutations.StaffMessage_edit);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = StaffMessageEditParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for StaffMessage.markRead. */
export function useStaffMessageMarkRead() {
  const mutate = useMutation(api.mutations.StaffMessage_markRead);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = StaffMessageMarkReadParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for StaffMessage.remove. */
export function useStaffMessageRemove() {
  const mutate = useMutation(api.mutations.StaffMessage_remove);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = StaffMessageRemoveParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for StaffMessage.send. */
export function useStaffMessageSend() {
  const mutate = useMutation(api.mutations.StaffMessage_send);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = StaffMessageSendParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for StaffMessage.send. */
export function useCreateStaffMessage() {
  const mutate = useMutation(api.mutations.StaffMessage_createViaSend);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = StaffMessageSendParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for StockCountLine. */
export function useListStockCountLine() {
  return useQuery(api.queries.listStockCountLine);
}

/** Reactive get-by-id for StockCountLine. Pass "skip" to suspend. */
export function useGetStockCountLine(id: string | "skip") {
  return useQuery(api.queries.getStockCountLine, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for StockCountLine.confirmLedgerMatch. */
export function useStockCountLineConfirmLedgerMatch() {
  const mutate = useMutation(api.mutations.StockCountLine_confirmLedgerMatch);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = StockCountLineConfirmLedgerMatchParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for StockCountLine.freeze. */
export function useStockCountLineFreeze() {
  const mutate = useMutation(api.mutations.StockCountLine_freeze);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = StockCountLineFreezeParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for StockCountLine.reconcileVariance. */
export function useStockCountLineReconcileVariance() {
  const mutate = useMutation(api.mutations.StockCountLine_reconcileVariance);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = StockCountLineReconcileVarianceParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for StockCountLine.recordCount. */
export function useStockCountLineRecordCount() {
  const mutate = useMutation(api.mutations.StockCountLine_recordCount);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = StockCountLineRecordCountParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for StockCountLine.reviseCount. */
export function useStockCountLineReviseCount() {
  const mutate = useMutation(api.mutations.StockCountLine_reviseCount);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = StockCountLineReviseCountParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for StockCountLine.freeze. */
export function useCreateStockCountLine() {
  const mutate = useMutation(api.mutations.StockCountLine_createViaFreeze);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = StockCountLineFreezeParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for StockCountSession. */
export function useListStockCountSession() {
  return useQuery(api.queries.listStockCountSession);
}

/** Reactive get-by-id for StockCountSession. Pass "skip" to suspend. */
export function useGetStockCountSession(id: string | "skip") {
  return useQuery(api.queries.getStockCountSession, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for StockCountSession.close. */
export function useStockCountSessionClose() {
  const mutate = useMutation(api.mutations.StockCountSession_close);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = StockCountSessionCloseParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for StockCountSession.start. */
export function useStockCountSessionStart() {
  const mutate = useMutation(api.mutations.StockCountSession_start);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = StockCountSessionStartParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for StockCountSession.start. */
export function useCreateStockCountSession() {
  const mutate = useMutation(api.mutations.StockCountSession_createViaStart);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = StockCountSessionStartParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for StockTransfer. */
export function useListStockTransfer() {
  return useQuery(api.queries.listStockTransfer);
}

/** Reactive get-by-id for StockTransfer. Pass "skip" to suspend. */
export function useGetStockTransfer(id: string | "skip") {
  return useQuery(api.queries.getStockTransfer, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for StockTransfer.record. */
export function useStockTransferRecord() {
  const mutate = useMutation(api.mutations.StockTransfer_record);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = StockTransferRecordParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for StockTransfer.record. */
export function useCreateStockTransfer() {
  const mutate = useMutation(api.mutations.StockTransfer_createViaRecord);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = StockTransferRecordParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for StorageLocation. */
export function useListStorageLocation() {
  return useQuery(api.queries.listStorageLocation);
}

/** Reactive get-by-id for StorageLocation. Pass "skip" to suspend. */
export function useGetStorageLocation(id: string | "skip") {
  return useQuery(api.queries.getStorageLocation, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for StorageLocation.activate. */
export function useStorageLocationActivate() {
  const mutate = useMutation(api.mutations.StorageLocation_activate);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = StorageLocationActivateParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for StorageLocation.deactivate. */
export function useStorageLocationDeactivate() {
  const mutate = useMutation(api.mutations.StorageLocation_deactivate);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = StorageLocationDeactivateParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for StorageLocation.register. */
export function useStorageLocationRegister() {
  const mutate = useMutation(api.mutations.StorageLocation_register);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = StorageLocationRegisterParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for StorageLocation.reviseDetails. */
export function useStorageLocationReviseDetails() {
  const mutate = useMutation(api.mutations.StorageLocation_reviseDetails);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = StorageLocationReviseDetailsParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for StorageLocation.register. */
export function useCreateStorageLocation() {
  const mutate = useMutation(api.mutations.StorageLocation_createViaRegister);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = StorageLocationRegisterParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for SyncError. */
export function useListSyncError() {
  return useQuery(api.queries.listSyncError);
}

/** Reactive get-by-id for SyncError. Pass "skip" to suspend. */
export function useGetSyncError(id: string | "skip") {
  return useQuery(api.queries.getSyncError, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for SyncError.markResolved. */
export function useSyncErrorMarkResolved() {
  const mutate = useMutation(api.mutations.SyncError_markResolved);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = SyncErrorMarkResolvedParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for SyncError.record. */
export function useSyncErrorRecord() {
  const mutate = useMutation(api.mutations.SyncError_record);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = SyncErrorRecordParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for SyncError.reopen. */
export function useSyncErrorReopen() {
  const mutate = useMutation(api.mutations.SyncError_reopen);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = SyncErrorReopenParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for SyncError.record. */
export function useCreateSyncError() {
  const mutate = useMutation(api.mutations.SyncError_createViaRecord);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = SyncErrorRecordParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for TaxRate. */
export function useListTaxRate() {
  return useQuery(api.queries.listTaxRate);
}

/** Reactive get-by-id for TaxRate. Pass "skip" to suspend. */
export function useGetTaxRate(id: string | "skip") {
  return useQuery(api.queries.getTaxRate, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for TaxRate.define. */
export function useTaxRateDefine() {
  const mutate = useMutation(api.mutations.TaxRate_define);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = TaxRateDefineParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for TaxRate.revise. */
export function useTaxRateRevise() {
  const mutate = useMutation(api.mutations.TaxRate_revise);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = TaxRateReviseParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for TaxRate.setActive. */
export function useTaxRateSetActive() {
  const mutate = useMutation(api.mutations.TaxRate_setActive);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = TaxRateSetActiveParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for TaxRate.define. */
export function useCreateTaxRate() {
  const mutate = useMutation(api.mutations.TaxRate_createViaDefine);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = TaxRateDefineParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for TimeOffRequest. */
export function useListTimeOffRequest() {
  return useQuery(api.queries.listTimeOffRequest);
}

/** Reactive get-by-id for TimeOffRequest. Pass "skip" to suspend. */
export function useGetTimeOffRequest(id: string | "skip") {
  return useQuery(api.queries.getTimeOffRequest, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for TimeOffRequest.approve. */
export function useTimeOffRequestApprove() {
  const mutate = useMutation(api.mutations.TimeOffRequest_approve);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = TimeOffRequestApproveParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for TimeOffRequest.decline. */
export function useTimeOffRequestDecline() {
  const mutate = useMutation(api.mutations.TimeOffRequest_decline);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = TimeOffRequestDeclineParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for TimeOffRequest.submit. */
export function useTimeOffRequestSubmit() {
  const mutate = useMutation(api.mutations.TimeOffRequest_submit);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = TimeOffRequestSubmitParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for TimeOffRequest.submit. */
export function useCreateTimeOffRequest() {
  const mutate = useMutation(api.mutations.TimeOffRequest_createViaSubmit);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = TimeOffRequestSubmitParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for TimeRecord. */
export function useListTimeRecord() {
  return useQuery(api.queries.listTimeRecord);
}

/** Reactive get-by-id for TimeRecord. Pass "skip" to suspend. */
export function useGetTimeRecord(id: string | "skip") {
  return useQuery(api.queries.getTimeRecord, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for TimeRecord.clockIn. */
export function useTimeRecordClockIn() {
  const mutate = useMutation(api.mutations.TimeRecord_clockIn);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = TimeRecordClockInParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for TimeRecord.clockOut. */
export function useTimeRecordClockOut() {
  const mutate = useMutation(api.mutations.TimeRecord_clockOut);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = TimeRecordClockOutParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for TimeRecord.correct. */
export function useTimeRecordCorrect() {
  const mutate = useMutation(api.mutations.TimeRecord_correct);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = TimeRecordCorrectParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for TimeRecord.clockIn. */
export function useCreateTimeRecord() {
  const mutate = useMutation(api.mutations.TimeRecord_createViaClockIn);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = TimeRecordClockInParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for TppReportFavorite. */
export function useListTppReportFavorite() {
  return useQuery(api.queries.listTppReportFavorite);
}

/** Reactive get-by-id for TppReportFavorite. Pass "skip" to suspend. */
export function useGetTppReportFavorite(id: string | "skip") {
  return useQuery(api.queries.getTppReportFavorite, id === "skip" ? "skip" : { id: id as any });
}

/** Reactive list for TrainingCompletion. */
export function useListTrainingCompletion() {
  return useQuery(api.queries.listTrainingCompletion);
}

/** Reactive get-by-id for TrainingCompletion. Pass "skip" to suspend. */
export function useGetTrainingCompletion(id: string | "skip") {
  return useQuery(api.queries.getTrainingCompletion, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for TrainingCompletion.record. */
export function useTrainingCompletionRecord() {
  const mutate = useMutation(api.mutations.TrainingCompletion_record);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = TrainingCompletionRecordParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for TrainingCompletion.record. */
export function useCreateTrainingCompletion() {
  const mutate = useMutation(api.mutations.TrainingCompletion_createViaRecord);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = TrainingCompletionRecordParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for TrainingModule. */
export function useListTrainingModule() {
  return useQuery(api.queries.listTrainingModule);
}

/** Reactive get-by-id for TrainingModule. Pass "skip" to suspend. */
export function useGetTrainingModule(id: string | "skip") {
  return useQuery(api.queries.getTrainingModule, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for TrainingModule.define. */
export function useTrainingModuleDefine() {
  const mutate = useMutation(api.mutations.TrainingModule_define);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = TrainingModuleDefineParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for TrainingModule.reactivate. */
export function useTrainingModuleReactivate() {
  const mutate = useMutation(api.mutations.TrainingModule_reactivate);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = TrainingModuleReactivateParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for TrainingModule.retire. */
export function useTrainingModuleRetire() {
  const mutate = useMutation(api.mutations.TrainingModule_retire);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = TrainingModuleRetireParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for TrainingModule.define. */
export function useCreateTrainingModule() {
  const mutate = useMutation(api.mutations.TrainingModule_createViaDefine);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = TrainingModuleDefineParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for Vehicle. */
export function useListVehicle() {
  return useQuery(api.queries.listVehicle);
}

/** Reactive get-by-id for Vehicle. Pass "skip" to suspend. */
export function useGetVehicle(id: string | "skip") {
  return useQuery(api.queries.getVehicle, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for Vehicle.register. */
export function useVehicleRegister() {
  const mutate = useMutation(api.mutations.Vehicle_register);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = VehicleRegisterParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Vehicle.reviseDetails. */
export function useVehicleReviseDetails() {
  const mutate = useMutation(api.mutations.Vehicle_reviseDetails);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = VehicleReviseDetailsParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Vehicle.updateOperationalStatus. */
export function useVehicleUpdateOperationalStatus() {
  const mutate = useMutation(api.mutations.Vehicle_updateOperationalStatus);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = VehicleUpdateOperationalStatusParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for Vehicle.register. */
export function useCreateVehicle() {
  const mutate = useMutation(api.mutations.Vehicle_createViaRegister);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = VehicleRegisterParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for VehicleFuelLog. */
export function useListVehicleFuelLog() {
  return useQuery(api.queries.listVehicleFuelLog);
}

/** Reactive get-by-id for VehicleFuelLog. Pass "skip" to suspend. */
export function useGetVehicleFuelLog(id: string | "skip") {
  return useQuery(api.queries.getVehicleFuelLog, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for VehicleFuelLog.record. */
export function useVehicleFuelLogRecord() {
  const mutate = useMutation(api.mutations.VehicleFuelLog_record);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = VehicleFuelLogRecordParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for VehicleFuelLog.record. */
export function useCreateVehicleFuelLog() {
  const mutate = useMutation(api.mutations.VehicleFuelLog_createViaRecord);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = VehicleFuelLogRecordParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for VehicleMaintenanceSchedule. */
export function useListVehicleMaintenanceSchedule() {
  return useQuery(api.queries.listVehicleMaintenanceSchedule);
}

/** Reactive get-by-id for VehicleMaintenanceSchedule. Pass "skip" to suspend. */
export function useGetVehicleMaintenanceSchedule(id: string | "skip") {
  return useQuery(api.queries.getVehicleMaintenanceSchedule, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for VehicleMaintenanceSchedule.applyService. */
export function useVehicleMaintenanceScheduleApplyService() {
  const mutate = useMutation(api.mutations.VehicleMaintenanceSchedule_applyService);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = VehicleMaintenanceScheduleApplyServiceParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for VehicleMaintenanceSchedule.schedule. */
export function useVehicleMaintenanceScheduleSchedule() {
  const mutate = useMutation(api.mutations.VehicleMaintenanceSchedule_schedule);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = VehicleMaintenanceScheduleScheduleParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for VehicleMaintenanceSchedule.schedule. */
export function useCreateVehicleMaintenanceSchedule() {
  const mutate = useMutation(api.mutations.VehicleMaintenanceSchedule_createViaSchedule);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = VehicleMaintenanceScheduleScheduleParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for VehicleServiceEntry. */
export function useListVehicleServiceEntry() {
  return useQuery(api.queries.listVehicleServiceEntry);
}

/** Reactive get-by-id for VehicleServiceEntry. Pass "skip" to suspend. */
export function useGetVehicleServiceEntry(id: string | "skip") {
  return useQuery(api.queries.getVehicleServiceEntry, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for VehicleServiceEntry.record. */
export function useVehicleServiceEntryRecord() {
  const mutate = useMutation(api.mutations.VehicleServiceEntry_record);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = VehicleServiceEntryRecordParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for VehicleServiceEntry.record. */
export function useCreateVehicleServiceEntry() {
  const mutate = useMutation(api.mutations.VehicleServiceEntry_createViaRecord);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = VehicleServiceEntryRecordParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for Vendor. */
export function useListVendor() {
  return useQuery(api.queries.listVendor);
}

/** Reactive get-by-id for Vendor. Pass "skip" to suspend. */
export function useGetVendor(id: string | "skip") {
  return useQuery(api.queries.getVendor, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for Vendor.onboard. */
export function useVendorOnboard() {
  const mutate = useMutation(api.mutations.Vendor_onboard);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = VendorOnboardParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Vendor.reinstate. */
export function useVendorReinstate() {
  const mutate = useMutation(api.mutations.Vendor_reinstate);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = VendorReinstateParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Vendor.suspend. */
export function useVendorSuspend() {
  const mutate = useMutation(api.mutations.Vendor_suspend);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = VendorSuspendParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Vendor.terminate. */
export function useVendorTerminate() {
  const mutate = useMutation(api.mutations.Vendor_terminate);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = VendorTerminateParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Vendor.updateDetails. */
export function useVendorUpdateDetails() {
  const mutate = useMutation(api.mutations.Vendor_updateDetails);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = VendorUpdateDetailsParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for Vendor.onboard. */
export function useCreateVendor() {
  const mutate = useMutation(api.mutations.Vendor_createViaOnboard);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = VendorOnboardParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for VendorContact. */
export function useListVendorContact() {
  return useQuery(api.queries.listVendorContact);
}

/** Reactive get-by-id for VendorContact. Pass "skip" to suspend. */
export function useGetVendorContact(id: string | "skip") {
  return useQuery(api.queries.getVendorContact, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for VendorContact.add. */
export function useVendorContactAdd() {
  const mutate = useMutation(api.mutations.VendorContact_add);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = VendorContactAddParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for VendorContact.remove. */
export function useVendorContactRemove() {
  const mutate = useMutation(api.mutations.VendorContact_remove);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = VendorContactRemoveParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for VendorContact.update. */
export function useVendorContactUpdate() {
  const mutate = useMutation(api.mutations.VendorContact_update);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = VendorContactUpdateParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for VendorContact.add. */
export function useCreateVendorContact() {
  const mutate = useMutation(api.mutations.VendorContact_createViaAdd);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = VendorContactAddParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for VendorContract. */
export function useListVendorContract() {
  return useQuery(api.queries.listVendorContract);
}

/** Reactive get-by-id for VendorContract. Pass "skip" to suspend. */
export function useGetVendorContract(id: string | "skip") {
  return useQuery(api.queries.getVendorContract, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for VendorContract.activate. */
export function useVendorContractActivate() {
  const mutate = useMutation(api.mutations.VendorContract_activate);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = VendorContractActivateParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for VendorContract.draft. */
export function useVendorContractDraft() {
  const mutate = useMutation(api.mutations.VendorContract_draft);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = VendorContractDraftParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for VendorContract.markExpired. */
export function useVendorContractMarkExpired() {
  const mutate = useMutation(api.mutations.VendorContract_markExpired);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = VendorContractMarkExpiredParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for VendorContract.terminate. */
export function useVendorContractTerminate() {
  const mutate = useMutation(api.mutations.VendorContract_terminate);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = VendorContractTerminateParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for VendorContract.updateTerms. */
export function useVendorContractUpdateTerms() {
  const mutate = useMutation(api.mutations.VendorContract_updateTerms);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = VendorContractUpdateTermsParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for VendorContract.draft. */
export function useCreateVendorContract() {
  const mutate = useMutation(api.mutations.VendorContract_createViaDraft);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = VendorContractDraftParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for VendorContractPriceTier. */
export function useListVendorContractPriceTier() {
  return useQuery(api.queries.listVendorContractPriceTier);
}

/** Reactive get-by-id for VendorContractPriceTier. Pass "skip" to suspend. */
export function useGetVendorContractPriceTier(id: string | "skip") {
  return useQuery(api.queries.getVendorContractPriceTier, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for VendorContractPriceTier.add. */
export function useVendorContractPriceTierAdd() {
  const mutate = useMutation(api.mutations.VendorContractPriceTier_add);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = VendorContractPriceTierAddParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for VendorContractPriceTier.remove. */
export function useVendorContractPriceTierRemove() {
  const mutate = useMutation(api.mutations.VendorContractPriceTier_remove);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = VendorContractPriceTierRemoveParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for VendorContractPriceTier.update. */
export function useVendorContractPriceTierUpdate() {
  const mutate = useMutation(api.mutations.VendorContractPriceTier_update);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = VendorContractPriceTierUpdateParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for VendorContractPriceTier.add. */
export function useCreateVendorContractPriceTier() {
  const mutate = useMutation(api.mutations.VendorContractPriceTier_createViaAdd);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = VendorContractPriceTierAddParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for VendorOrder. */
export function useListVendorOrder() {
  return useQuery(api.queries.listVendorOrder);
}

/** Reactive get-by-id for VendorOrder. Pass "skip" to suspend. */
export function useGetVendorOrder(id: string | "skip") {
  return useQuery(api.queries.getVendorOrder, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for VendorOrder.approve. */
export function useVendorOrderApprove() {
  const mutate = useMutation(api.mutations.VendorOrder_approve);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = VendorOrderApproveParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for VendorOrder.cancel. */
export function useVendorOrderCancel() {
  const mutate = useMutation(api.mutations.VendorOrder_cancel);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = VendorOrderCancelParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for VendorOrder.confirm. */
export function useVendorOrderConfirm() {
  const mutate = useMutation(api.mutations.VendorOrder_confirm);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = VendorOrderConfirmParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for VendorOrder.ensureWeeklyDraft. */
export function useVendorOrderEnsureWeeklyDraft() {
  const mutate = useMutation(api.mutations.VendorOrder_ensureWeeklyDraft);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = VendorOrderEnsureWeeklyDraftParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for VendorOrder.markPartiallyReceived. */
export function useVendorOrderMarkPartiallyReceived() {
  const mutate = useMutation(api.mutations.VendorOrder_markPartiallyReceived);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = VendorOrderMarkPartiallyReceivedParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for VendorOrder.markReceived. */
export function useVendorOrderMarkReceived() {
  const mutate = useMutation(api.mutations.VendorOrder_markReceived);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = VendorOrderMarkReceivedParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for VendorOrder.open. */
export function useVendorOrderOpen() {
  const mutate = useMutation(api.mutations.VendorOrder_open);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = VendorOrderOpenParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for VendorOrder.requestChanges. */
export function useVendorOrderRequestChanges() {
  const mutate = useMutation(api.mutations.VendorOrder_requestChanges);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = VendorOrderRequestChangesParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for VendorOrder.submit. */
export function useVendorOrderSubmit() {
  const mutate = useMutation(api.mutations.VendorOrder_submit);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = VendorOrderSubmitParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for VendorOrder.submitForApproval. */
export function useVendorOrderSubmitForApproval() {
  const mutate = useMutation(api.mutations.VendorOrder_submitForApproval);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = VendorOrderSubmitForApprovalParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for VendorOrder.syncLineTotals. */
export function useVendorOrderSyncLineTotals() {
  const mutate = useMutation(api.mutations.VendorOrder_syncLineTotals);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = VendorOrderSyncLineTotalsParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for VendorOrder.updateTotals. */
export function useVendorOrderUpdateTotals() {
  const mutate = useMutation(api.mutations.VendorOrder_updateTotals);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = VendorOrderUpdateTotalsParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for VendorOrder.open. */
export function useCreateVendorOrder() {
  const mutate = useMutation(api.mutations.VendorOrder_createViaOpen);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = VendorOrderOpenParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for VendorOrderLine. */
export function useListVendorOrderLine() {
  return useQuery(api.queries.listVendorOrderLine);
}

/** Reactive get-by-id for VendorOrderLine. Pass "skip" to suspend. */
export function useGetVendorOrderLine(id: string | "skip") {
  return useQuery(api.queries.getVendorOrderLine, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for VendorOrderLine.addLine. */
export function useVendorOrderLineAddLine() {
  const mutate = useMutation(api.mutations.VendorOrderLine_addLine);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = VendorOrderLineAddLineParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for VendorOrderLine.cancelLine. */
export function useVendorOrderLineCancelLine() {
  const mutate = useMutation(api.mutations.VendorOrderLine_cancelLine);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = VendorOrderLineCancelLineParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for VendorOrderLine.ensureWeeklyLine. */
export function useVendorOrderLineEnsureWeeklyLine() {
  const mutate = useMutation(api.mutations.VendorOrderLine_ensureWeeklyLine);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = VendorOrderLineEnsureWeeklyLineParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for VendorOrderLine.recordReceipt. */
export function useVendorOrderLineRecordReceipt() {
  const mutate = useMutation(api.mutations.VendorOrderLine_recordReceipt);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = VendorOrderLineRecordReceiptParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for VendorOrderLine.reviseQuantity. */
export function useVendorOrderLineReviseQuantity() {
  const mutate = useMutation(api.mutations.VendorOrderLine_reviseQuantity);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = VendorOrderLineReviseQuantityParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for VendorOrderLine.addLine. */
export function useCreateVendorOrderLine() {
  const mutate = useMutation(api.mutations.VendorOrderLine_createViaAddLine);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = VendorOrderLineAddLineParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for VendorOrderLineDemand. */
export function useListVendorOrderLineDemand() {
  return useQuery(api.queries.listVendorOrderLineDemand);
}

/** Reactive get-by-id for VendorOrderLineDemand. Pass "skip" to suspend. */
export function useGetVendorOrderLineDemand(id: string | "skip") {
  return useQuery(api.queries.getVendorOrderLineDemand, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for VendorOrderLineDemand.link. */
export function useVendorOrderLineDemandLink() {
  const mutate = useMutation(api.mutations.VendorOrderLineDemand_link);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = VendorOrderLineDemandLinkParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for VendorOrderLineDemand.retire. */
export function useVendorOrderLineDemandRetire() {
  const mutate = useMutation(api.mutations.VendorOrderLineDemand_retire);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = VendorOrderLineDemandRetireParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for VendorOrderLineDemand.revise. */
export function useVendorOrderLineDemandRevise() {
  const mutate = useMutation(api.mutations.VendorOrderLineDemand_revise);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = VendorOrderLineDemandReviseParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for VendorOrderLineDemand.link. */
export function useCreateVendorOrderLineDemand() {
  const mutate = useMutation(api.mutations.VendorOrderLineDemand_createViaLink);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = VendorOrderLineDemandLinkParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for Venue. */
export function useListVenue() {
  return useQuery(api.queries.listVenue);
}

/** Reactive get-by-id for Venue. Pass "skip" to suspend. */
export function useGetVenue(id: string | "skip") {
  return useQuery(api.queries.getVenue, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for Venue.activate. */
export function useVenueActivate() {
  const mutate = useMutation(api.mutations.Venue_activate);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = VenueActivateParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Venue.changeCapacity. */
export function useVenueChangeCapacity() {
  const mutate = useMutation(api.mutations.Venue_changeCapacity);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = VenueChangeCapacityParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Venue.deactivate. */
export function useVenueDeactivate() {
  const mutate = useMutation(api.mutations.Venue_deactivate);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = VenueDeactivateParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Venue.register. */
export function useVenueRegister() {
  const mutate = useMutation(api.mutations.Venue_register);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = VenueRegisterParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for Venue.updateDetails. */
export function useVenueUpdateDetails() {
  const mutate = useMutation(api.mutations.Venue_updateDetails);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = VenueUpdateDetailsParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for Venue.register. */
export function useCreateVenue() {
  const mutate = useMutation(api.mutations.Venue_createViaRegister);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = VenueRegisterParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for VenueCommissionTerm. */
export function useListVenueCommissionTerm() {
  return useQuery(api.queries.listVenueCommissionTerm);
}

/** Reactive get-by-id for VenueCommissionTerm. Pass "skip" to suspend. */
export function useGetVenueCommissionTerm(id: string | "skip") {
  return useQuery(api.queries.getVenueCommissionTerm, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for VenueCommissionTerm.define. */
export function useVenueCommissionTermDefine() {
  const mutate = useMutation(api.mutations.VenueCommissionTerm_define);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = VenueCommissionTermDefineParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for VenueCommissionTerm.retire. */
export function useVenueCommissionTermRetire() {
  const mutate = useMutation(api.mutations.VenueCommissionTerm_retire);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = VenueCommissionTermRetireParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for VenueCommissionTerm.revise. */
export function useVenueCommissionTermRevise() {
  const mutate = useMutation(api.mutations.VenueCommissionTerm_revise);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = VenueCommissionTermReviseParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for VenueCommissionTerm.define. */
export function useCreateVenueCommissionTerm() {
  const mutate = useMutation(api.mutations.VenueCommissionTerm_createViaDefine);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = VenueCommissionTermDefineParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for VenueLayoutTemplate. */
export function useListVenueLayoutTemplate() {
  return useQuery(api.queries.listVenueLayoutTemplate);
}

/** Reactive get-by-id for VenueLayoutTemplate. Pass "skip" to suspend. */
export function useGetVenueLayoutTemplate(id: string | "skip") {
  return useQuery(api.queries.getVenueLayoutTemplate, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for VenueLayoutTemplate.archive. */
export function useVenueLayoutTemplateArchive() {
  const mutate = useMutation(api.mutations.VenueLayoutTemplate_archive);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = VenueLayoutTemplateArchiveParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for VenueLayoutTemplate.define. */
export function useVenueLayoutTemplateDefine() {
  const mutate = useMutation(api.mutations.VenueLayoutTemplate_define);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = VenueLayoutTemplateDefineParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for VenueLayoutTemplate.reactivate. */
export function useVenueLayoutTemplateReactivate() {
  const mutate = useMutation(api.mutations.VenueLayoutTemplate_reactivate);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = VenueLayoutTemplateReactivateParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for VenueLayoutTemplate.revise. */
export function useVenueLayoutTemplateRevise() {
  const mutate = useMutation(api.mutations.VenueLayoutTemplate_revise);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = VenueLayoutTemplateReviseParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for VenueLayoutTemplate.define. */
export function useCreateVenueLayoutTemplate() {
  const mutate = useMutation(api.mutations.VenueLayoutTemplate_createViaDefine);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = VenueLayoutTemplateDefineParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for VenueNote. */
export function useListVenueNote() {
  return useQuery(api.queries.listVenueNote);
}

/** Reactive get-by-id for VenueNote. Pass "skip" to suspend. */
export function useGetVenueNote(id: string | "skip") {
  return useQuery(api.queries.getVenueNote, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for VenueNote.pin. */
export function useVenueNotePin() {
  const mutate = useMutation(api.mutations.VenueNote_pin);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = VenueNotePinParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for VenueNote.post. */
export function useVenueNotePost() {
  const mutate = useMutation(api.mutations.VenueNote_post);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = VenueNotePostParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for VenueNote.remove. */
export function useVenueNoteRemove() {
  const mutate = useMutation(api.mutations.VenueNote_remove);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = VenueNoteRemoveParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for VenueNote.revise. */
export function useVenueNoteRevise() {
  const mutate = useMutation(api.mutations.VenueNote_revise);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = VenueNoteReviseParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for VenueNote.unpin. */
export function useVenueNoteUnpin() {
  const mutate = useMutation(api.mutations.VenueNote_unpin);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = VenueNoteUnpinParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for VenueNote.post. */
export function useCreateVenueNote() {
  const mutate = useMutation(api.mutations.VenueNote_createViaPost);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = VenueNotePostParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for VenueRoom. */
export function useListVenueRoom() {
  return useQuery(api.queries.listVenueRoom);
}

/** Reactive get-by-id for VenueRoom. Pass "skip" to suspend. */
export function useGetVenueRoom(id: string | "skip") {
  return useQuery(api.queries.getVenueRoom, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for VenueRoom.add. */
export function useVenueRoomAdd() {
  const mutate = useMutation(api.mutations.VenueRoom_add);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = VenueRoomAddParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for VenueRoom.remove. */
export function useVenueRoomRemove() {
  const mutate = useMutation(api.mutations.VenueRoom_remove);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = VenueRoomRemoveParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for VenueRoom.revise. */
export function useVenueRoomRevise() {
  const mutate = useMutation(api.mutations.VenueRoom_revise);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = VenueRoomReviseParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for VenueRoom.add. */
export function useCreateVenueRoom() {
  const mutate = useMutation(api.mutations.VenueRoom_createViaAdd);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = VenueRoomAddParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for VenueVendorRelationship. */
export function useListVenueVendorRelationship() {
  return useQuery(api.queries.listVenueVendorRelationship);
}

/** Reactive get-by-id for VenueVendorRelationship. Pass "skip" to suspend. */
export function useGetVenueVendorRelationship(id: string | "skip") {
  return useQuery(api.queries.getVenueVendorRelationship, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for VenueVendorRelationship.establish. */
export function useVenueVendorRelationshipEstablish() {
  const mutate = useMutation(api.mutations.VenueVendorRelationship_establish);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = VenueVendorRelationshipEstablishParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for VenueVendorRelationship.retire. */
export function useVenueVendorRelationshipRetire() {
  const mutate = useMutation(api.mutations.VenueVendorRelationship_retire);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = VenueVendorRelationshipRetireParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for VenueVendorRelationship.reviseDetails. */
export function useVenueVendorRelationshipReviseDetails() {
  const mutate = useMutation(api.mutations.VenueVendorRelationship_reviseDetails);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = VenueVendorRelationshipReviseDetailsParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for VenueVendorRelationship.reviseStatus. */
export function useVenueVendorRelationshipReviseStatus() {
  const mutate = useMutation(api.mutations.VenueVendorRelationship_reviseStatus);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = VenueVendorRelationshipReviseStatusParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for VenueVendorRelationship.establish. */
export function useCreateVenueVendorRelationship() {
  const mutate = useMutation(api.mutations.VenueVendorRelationship_createViaEstablish);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = VenueVendorRelationshipEstablishParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for WasteRecord. */
export function useListWasteRecord() {
  return useQuery(api.queries.listWasteRecord);
}

/** Reactive get-by-id for WasteRecord. Pass "skip" to suspend. */
export function useGetWasteRecord(id: string | "skip") {
  return useQuery(api.queries.getWasteRecord, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for WasteRecord.record. */
export function useWasteRecordRecord() {
  const mutate = useMutation(api.mutations.WasteRecord_record);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = WasteRecordRecordParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for WasteRecord.voidRecord. */
export function useWasteRecordVoidRecord() {
  const mutate = useMutation(api.mutations.WasteRecord_voidRecord);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = WasteRecordVoidRecordParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for WasteRecord.record. */
export function useCreateWasteRecord() {
  const mutate = useMutation(api.mutations.WasteRecord_createViaRecord);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = WasteRecordRecordParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for WeeklyPurchasingConfig. */
export function useListWeeklyPurchasingConfig() {
  return useQuery(api.queries.listWeeklyPurchasingConfig);
}

/** Reactive get-by-id for WeeklyPurchasingConfig. Pass "skip" to suspend. */
export function useGetWeeklyPurchasingConfig(id: string | "skip") {
  return useQuery(api.queries.getWeeklyPurchasingConfig, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for WeeklyPurchasingConfig.configure. */
export function useWeeklyPurchasingConfigConfigure() {
  const mutate = useMutation(api.mutations.WeeklyPurchasingConfig_configure);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = WeeklyPurchasingConfigConfigureParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for WeeklyPurchasingConfig.routeNeed. */
export function useWeeklyPurchasingConfigRouteNeed() {
  const mutate = useMutation(api.mutations.WeeklyPurchasingConfig_routeNeed);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = WeeklyPurchasingConfigRouteNeedParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for WeeklyPurchasingConfig.setOrderApprovalThreshold. */
export function useWeeklyPurchasingConfigSetOrderApprovalThreshold() {
  const mutate = useMutation(api.mutations.WeeklyPurchasingConfig_setOrderApprovalThreshold);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = WeeklyPurchasingConfigSetOrderApprovalThresholdParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for WeeklyPurchasingConfig.configure. */
export function useCreateWeeklyPurchasingConfig() {
  const mutate = useMutation(api.mutations.WeeklyPurchasingConfig_createViaConfigure);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = WeeklyPurchasingConfigConfigureParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

/** Reactive list for WeeklyScheduleNotice. */
export function useListWeeklyScheduleNotice() {
  return useQuery(api.queries.listWeeklyScheduleNotice);
}

/** Reactive get-by-id for WeeklyScheduleNotice. Pass "skip" to suspend. */
export function useGetWeeklyScheduleNotice(id: string | "skip") {
  return useQuery(api.queries.getWeeklyScheduleNotice, id === "skip" ? "skip" : { id: id as any });
}

/** Mutation hook for WeeklyScheduleNotice.acknowledge. */
export function useWeeklyScheduleNoticeAcknowledge() {
  const mutate = useMutation(api.mutations.WeeklyScheduleNotice_acknowledge);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = WeeklyScheduleNoticeAcknowledgeParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for WeeklyScheduleNotice.publishSchedule. */
export function useWeeklyScheduleNoticePublishSchedule() {
  const mutate = useMutation(api.mutations.WeeklyScheduleNotice_publishSchedule);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = WeeklyScheduleNoticePublishScheduleParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Mutation hook for WeeklyScheduleNotice.republishSchedule. */
export function useWeeklyScheduleNoticeRepublishSchedule() {
  const mutate = useMutation(api.mutations.WeeklyScheduleNotice_republishSchedule);
  return (args: any) => {
    const { docId, version, idempotencyKey, ...params } = args ?? {};
    const parsed = WeeklyScheduleNoticeRepublishScheduleParamsSchema.parse(params) as Record<string, unknown>;
    return mutate({ docId, version, idempotencyKey, ...__convexArgsFromZod(parsed) } as any);
  };
}

/** Governed creation hook for WeeklyScheduleNotice.publishSchedule. */
export function useCreateWeeklyScheduleNotice() {
  const mutate = useMutation(api.mutations.WeeklyScheduleNotice_createViaPublishSchedule);
  return (args: any) => {
    const { idempotencyKey, ...params } = args ?? {};
    const parsed = WeeklyScheduleNoticePublishScheduleParamsSchema.parse(params) as Record<string, unknown>;
    const body = __convexArgsFromZod(parsed);
    return mutate((idempotencyKey !== undefined ? { ...body, idempotencyKey } : body) as any);
  };
}

export const MANIFEST_CONVEX_REACT_HOOK_COUNT = 982 as const;
