// GENERATED by the Manifest → Convex projection. DO NOT EDIT.
// 685 query function(s).

import { query, internalQuery } from "./_generated/server";
import { v } from "convex/values";
import type { Doc } from "./_generated/dataModel";
import { getAuthContext } from "./lib/authContext";
import { decrypt } from "./lib/encryption";

async function __decryptDoc(ctx: any, entity: string, fields: readonly string[], doc: any): Promise<any> {
  if (!doc) return doc;
  const out = { ...doc } as Record<string, any>;
  for (const property of fields) {
    const raw = out[property];
    if (typeof raw !== "string") continue;
    let envelope: any;
    try { envelope = JSON.parse(raw); } catch { continue; }
    const looksEncrypted = envelope && typeof envelope === "object" && "v" in envelope && "kid" in envelope && "ct" in envelope;
    if (!looksEncrypted) continue;
    if (envelope.v !== 1) throw new Error(`Unsupported Manifest encryption envelope version: ${String(envelope.v)}`);
    out[property] = await decrypt(envelope.ct, envelope.kid, { ctx, entity, property });
  }
  return out;
}

function __allowsRead(policy: string, entity: string, check: () => unknown): boolean {
  try {
    return !!check();
  } catch (error) {
    console.warn(`[Manifest Convex] read policy '${policy}' evaluation error for '${entity}' (read denied)`, error);
    return false;
  }
}

const ROLE_PERMISSIONS: Record<string, { action: string; target?: string }[]> = {
  "admin": [
    {
      "action": "adminAccess"
    },
    {
      "action": "eventAccess"
    },
    {
      "action": "eventManageAccess"
    },
    {
      "action": "financeAccess"
    },
    {
      "action": "financeManageAccess"
    },
    {
      "action": "importAccess"
    },
    {
      "action": "inventoryAccess"
    },
    {
      "action": "inventoryManageAccess"
    },
    {
      "action": "kitchenAccess"
    },
    {
      "action": "kitchenLeadAccess"
    },
    {
      "action": "kitchenManageAccess"
    },
    {
      "action": "logisticsAccess"
    },
    {
      "action": "logisticsManageAccess"
    },
    {
      "action": "manageAccess"
    },
    {
      "action": "procurementAccess"
    },
    {
      "action": "salesAccess"
    },
    {
      "action": "salesManageAccess"
    },
    {
      "action": "staffAccess"
    },
    {
      "action": "workforceAccess"
    },
    {
      "action": "workforceManageAccess"
    }
  ],
  "driver": [
    {
      "action": "logisticsAccess"
    },
    {
      "action": "staffAccess"
    }
  ],
  "event_manager": [
    {
      "action": "eventAccess"
    },
    {
      "action": "eventManageAccess"
    },
    {
      "action": "importAccess"
    },
    {
      "action": "manageAccess"
    },
    {
      "action": "staffAccess"
    }
  ],
  "event_staff": [
    {
      "action": "eventAccess"
    },
    {
      "action": "staffAccess"
    }
  ],
  "finance_manager": [
    {
      "action": "financeAccess"
    },
    {
      "action": "financeManageAccess"
    },
    {
      "action": "importAccess"
    },
    {
      "action": "manageAccess"
    },
    {
      "action": "staffAccess"
    }
  ],
  "finance_staff": [
    {
      "action": "financeAccess"
    },
    {
      "action": "staffAccess"
    }
  ],
  "inventory_manager": [
    {
      "action": "importAccess"
    },
    {
      "action": "inventoryAccess"
    },
    {
      "action": "inventoryManageAccess"
    },
    {
      "action": "manageAccess"
    },
    {
      "action": "procurementAccess"
    },
    {
      "action": "staffAccess"
    }
  ],
  "inventory_staff": [
    {
      "action": "inventoryAccess"
    },
    {
      "action": "staffAccess"
    }
  ],
  "kitchen_lead": [
    {
      "action": "kitchenAccess"
    },
    {
      "action": "kitchenLeadAccess"
    },
    {
      "action": "staffAccess"
    }
  ],
  "kitchen_manager": [
    {
      "action": "importAccess"
    },
    {
      "action": "kitchenAccess"
    },
    {
      "action": "kitchenLeadAccess"
    },
    {
      "action": "kitchenManageAccess"
    },
    {
      "action": "manageAccess"
    },
    {
      "action": "staffAccess"
    }
  ],
  "kitchen_staff": [
    {
      "action": "kitchenAccess"
    },
    {
      "action": "staffAccess"
    }
  ],
  "logistics_manager": [
    {
      "action": "importAccess"
    },
    {
      "action": "logisticsAccess"
    },
    {
      "action": "logisticsManageAccess"
    },
    {
      "action": "manageAccess"
    },
    {
      "action": "staffAccess"
    }
  ],
  "logistics_staff": [
    {
      "action": "logisticsAccess"
    },
    {
      "action": "staffAccess"
    }
  ],
  "manager": [
    {
      "action": "importAccess"
    },
    {
      "action": "manageAccess"
    },
    {
      "action": "staffAccess"
    }
  ],
  "owner": [
    {
      "action": "adminAccess"
    },
    {
      "action": "eventAccess"
    },
    {
      "action": "eventManageAccess"
    },
    {
      "action": "financeAccess"
    },
    {
      "action": "financeManageAccess"
    },
    {
      "action": "importAccess"
    },
    {
      "action": "inventoryAccess"
    },
    {
      "action": "inventoryManageAccess"
    },
    {
      "action": "kitchenAccess"
    },
    {
      "action": "kitchenLeadAccess"
    },
    {
      "action": "kitchenManageAccess"
    },
    {
      "action": "logisticsAccess"
    },
    {
      "action": "logisticsManageAccess"
    },
    {
      "action": "manageAccess"
    },
    {
      "action": "procurementAccess"
    },
    {
      "action": "salesAccess"
    },
    {
      "action": "salesManageAccess"
    },
    {
      "action": "staffAccess"
    },
    {
      "action": "workforceAccess"
    },
    {
      "action": "workforceManageAccess"
    }
  ],
  "procurement_staff": [
    {
      "action": "inventoryAccess"
    },
    {
      "action": "procurementAccess"
    },
    {
      "action": "staffAccess"
    }
  ],
  "sales_manager": [
    {
      "action": "importAccess"
    },
    {
      "action": "manageAccess"
    },
    {
      "action": "salesAccess"
    },
    {
      "action": "salesManageAccess"
    },
    {
      "action": "staffAccess"
    }
  ],
  "sales_staff": [
    {
      "action": "salesAccess"
    },
    {
      "action": "staffAccess"
    }
  ],
  "staff": [
    {
      "action": "staffAccess"
    }
  ],
  "system": [
    {
      "action": "adminAccess"
    },
    {
      "action": "eventAccess"
    },
    {
      "action": "eventManageAccess"
    },
    {
      "action": "financeAccess"
    },
    {
      "action": "financeManageAccess"
    },
    {
      "action": "importAccess"
    },
    {
      "action": "inventoryAccess"
    },
    {
      "action": "inventoryManageAccess"
    },
    {
      "action": "kitchenAccess"
    },
    {
      "action": "kitchenLeadAccess"
    },
    {
      "action": "kitchenManageAccess"
    },
    {
      "action": "logisticsAccess"
    },
    {
      "action": "logisticsManageAccess"
    },
    {
      "action": "manageAccess"
    },
    {
      "action": "procurementAccess"
    },
    {
      "action": "salesAccess"
    },
    {
      "action": "salesManageAccess"
    },
    {
      "action": "staffAccess"
    },
    {
      "action": "workforceAccess"
    },
    {
      "action": "workforceManageAccess"
    }
  ],
  "workforce_manager": [
    {
      "action": "importAccess"
    },
    {
      "action": "manageAccess"
    },
    {
      "action": "staffAccess"
    },
    {
      "action": "workforceAccess"
    },
    {
      "action": "workforceManageAccess"
    }
  ],
  "workforce_staff": [
    {
      "action": "staffAccess"
    },
    {
      "action": "workforceAccess"
    }
  ]
};

function checkRole(userOrRole: unknown, action: unknown, target?: unknown): boolean {
  let userRole: unknown;
  let disabledCapabilities: unknown;
  if (typeof userOrRole === "string") {
    userRole = userOrRole;
  } else if (userOrRole !== null && typeof userOrRole === "object") {
    const auth = userOrRole as { role?: unknown; disabledCapabilities?: unknown };
    userRole = auth.role;
    disabledCapabilities = auth.disabledCapabilities;
  } else {
    return false;
  }
  if (typeof userRole !== "string" || typeof action !== "string") return false;
  if (__orgCapabilityDeniesAction(action, disabledCapabilities)) return false;
  const perms = ROLE_PERMISSIONS[userRole];
  const requestedTarget = typeof target === "string" ? target : undefined;
  return perms ? perms.some((permission) =>
    (permission.action === action || permission.action === "all") &&
    (permission.target === undefined || permission.target === requestedTarget)
  ) : false;
}

function __orgCapabilityDeniesAction(action: string, disabled: unknown): boolean {
  if (!Array.isArray(disabled) || disabled.length === 0) return false;
  const capability = __orgCapabilityForAction(action);
  if (capability === null) return false;
  return disabled.some((entry) => entry === capability);
}

function __orgCapabilityForAction(action: string): string | null {
  if (action === "staffAccess" || action === "manageAccess" || action === "adminAccess") return null;
  if (action.startsWith("kitchen")) return "kitchen";
  if (action.startsWith("inventory")) return "inventory";
  if (action.startsWith("procurement")) return "procurement";
  if (action.startsWith("event")) return "events";
  if (action.startsWith("sales")) return "sales";
  if (action.startsWith("logistics")) return "logistics";
  if (action.startsWith("workforce")) return "workforce";
  if (action.startsWith("finance")) return "finance";
  return null;
}

export const listAnnouncement = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("announcementRead", "Announcement", () => checkRole(user, "staffAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("announcements").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const getAnnouncement = query({
  args: { id: v.id("announcements") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("announcementRead", "Announcement", () => checkRole(user, "staffAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    return __doc;
  },
});

export const listAnnouncementByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("announcementRead", "Announcement", () => checkRole(user, "staffAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("announcements").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listAnnouncementDismissal = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("announcementDismissalRead", "AnnouncementDismissal", () => checkRole(user, "staffAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("announcementDismissals").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    const __plainRows = rows;
    return __plainRows;
  },
});

export const getAnnouncementDismissal = query({
  args: { id: v.id("announcementDismissals") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("announcementDismissalRead", "AnnouncementDismissal", () => checkRole(user, "staffAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    return __doc;
  },
});

export const listAnnouncementDismissalByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("announcementDismissalRead", "AnnouncementDismissal", () => checkRole(user, "staffAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("announcementDismissals").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listAnnouncementDismissalByAnnouncementId = query({
  args: { announcementId: v.id("announcements") },
  handler: async (ctx, { announcementId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("announcementDismissalRead", "AnnouncementDismissal", () => checkRole(user, "staffAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("announcementDismissals").withIndex("by_announcementId", (q) => q.eq("announcementId", announcementId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listAnnouncementDismissalByAuthSubjectId = query({
  args: { authSubjectId: v.string() },
  handler: async (ctx, { authSubjectId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("announcementDismissalRead", "AnnouncementDismissal", () => checkRole(user, "staffAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("announcementDismissals").withIndex("by_authSubjectId", (q) => q.eq("authSubjectId", authSubjectId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listAttachment = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("attachmentRead", "Attachment", () => checkRole(user, "staffAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("attachments").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const getAttachment = query({
  args: { id: v.id("attachments") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("attachmentRead", "Attachment", () => checkRole(user, "staffAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    return __doc;
  },
});

export const listAttachmentByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("attachmentRead", "Attachment", () => checkRole(user, "staffAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("attachments").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listAttachmentByParentId = query({
  args: { parentId: v.string() },
  handler: async (ctx, { parentId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("attachmentRead", "Attachment", () => checkRole(user, "staffAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("attachments").withIndex("by_parentId", (q) => q.eq("parentId", parentId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listAvailabilityWindow = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("availabilityRead", "AvailabilityWindow", () => checkRole(user, "workforceAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("availabilityWindows").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "AvailabilityWindow", ["notes"], row)));
    return __plainRows;
  },
});

export const getAvailabilityWindow = query({
  args: { id: v.id("availabilityWindows") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("availabilityRead", "AvailabilityWindow", () => checkRole(user, "workforceAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = await __decryptDoc(ctx, "AvailabilityWindow", ["notes"], __rawDoc);
    return __doc;
  },
});

export const listAvailabilityWindowByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("availabilityRead", "AvailabilityWindow", () => checkRole(user, "workforceAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("availabilityWindows").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "AvailabilityWindow", ["notes"], row)));
    return __plainRows;
  },
});

export const listAvailabilityWindowByPersonId = query({
  args: { personId: v.id("people") },
  handler: async (ctx, { personId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("availabilityRead", "AvailabilityWindow", () => checkRole(user, "workforceAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("availabilityWindows").withIndex("by_personId", (q) => q.eq("personId", personId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "AvailabilityWindow", ["notes"], row)));
    return __plainRows;
  },
});

export const listCandidate = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("candidateRead", "Candidate", () => checkRole(user, "workforceManageAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("candidates").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const getCandidate = query({
  args: { id: v.id("candidates") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("candidateRead", "Candidate", () => checkRole(user, "workforceManageAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    return __doc;
  },
});

export const listCandidateByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("candidateRead", "Candidate", () => checkRole(user, "workforceManageAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("candidates").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listCandidateByHiredPersonId = query({
  args: { hiredPersonId: v.id("people") },
  handler: async (ctx, { hiredPersonId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("candidateRead", "Candidate", () => checkRole(user, "workforceManageAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("candidates").withIndex("by_hiredPersonId", (q) => q.eq("hiredPersonId", hiredPersonId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listClient = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("clientRead", "Client", () => (checkRole(user, "salesAccess") || checkRole(user, "financeAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("clients").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "Client", ["email","phone","addressLine1","addressLine2","city","region","postalCode","countryCode","taxId"], row)));
    return ((__plainRows).map((__row) => { (__row as any).displayName = (((__row as any).clientType === "company") ? (__row as any).companyName : (((__row as any).givenName + " ") + (__row as any).familyName)); (__row as any).isArchived = ((__row as any).status === "archived"); return { ...(__row as any), displayName: (__row as any).displayName, isArchived: (__row as any).isArchived }; })).map((d) => {
      const o = { ...(d as any) };
      delete (o as any).taxId;
      return o;
    });
  },
});

export const getClient = query({
  args: { id: v.id("clients") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("clientRead", "Client", () => (checkRole(user, "salesAccess") || checkRole(user, "financeAccess")))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = await __decryptDoc(ctx, "Client", ["email","phone","addressLine1","addressLine2","city","region","postalCode","countryCode","taxId"], __rawDoc);
    (__doc as any).displayName = (((__doc as any).clientType === "company") ? (__doc as any).companyName : (((__doc as any).givenName + " ") + (__doc as any).familyName));
    (__doc as any).isArchived = ((__doc as any).status === "archived");
    const __hydrated = { ...(__doc as any), displayName: (__doc as any).displayName, isArchived: (__doc as any).isArchived };
    const __final = __hydrated;
    if (!__final) return __final;
    const __out = { ...(__final as any) };
    delete (__out as any).taxId;
    return __out;
  },
});

export const listClientByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("clientRead", "Client", () => (checkRole(user, "salesAccess") || checkRole(user, "financeAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("clients").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "Client", ["email","phone","addressLine1","addressLine2","city","region","postalCode","countryCode","taxId"], row)));
    return ((__plainRows).map((__row) => { (__row as any).displayName = (((__row as any).clientType === "company") ? (__row as any).companyName : (((__row as any).givenName + " ") + (__row as any).familyName)); (__row as any).isArchived = ((__row as any).status === "archived"); return { ...(__row as any), displayName: (__row as any).displayName, isArchived: (__row as any).isArchived }; })).map((d) => {
      const o = { ...(d as any) };
      delete (o as any).taxId;
      return o;
    });
  },
});

export const listClientByAssignedToId = query({
  args: { assignedToId: v.id("people") },
  handler: async (ctx, { assignedToId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("clientRead", "Client", () => (checkRole(user, "salesAccess") || checkRole(user, "financeAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("clients").withIndex("by_assignedToId", (q) => q.eq("assignedToId", assignedToId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "Client", ["email","phone","addressLine1","addressLine2","city","region","postalCode","countryCode","taxId"], row)));
    return ((__plainRows).map((__row) => { (__row as any).displayName = (((__row as any).clientType === "company") ? (__row as any).companyName : (((__row as any).givenName + " ") + (__row as any).familyName)); (__row as any).isArchived = ((__row as any).status === "archived"); return { ...(__row as any), displayName: (__row as any).displayName, isArchived: (__row as any).isArchived }; })).map((d) => {
      const o = { ...(d as any) };
      delete (o as any).taxId;
      return o;
    });
  },
});

export const listClientByClientMergeAuthorizationId = query({
  args: { clientMergeAuthorizationId: v.id("clientMerges") },
  handler: async (ctx, { clientMergeAuthorizationId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("clientRead", "Client", () => (checkRole(user, "salesAccess") || checkRole(user, "financeAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("clients").withIndex("by_clientMergeAuthorizationId", (q) => q.eq("clientMergeAuthorizationId", clientMergeAuthorizationId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "Client", ["email","phone","addressLine1","addressLine2","city","region","postalCode","countryCode","taxId"], row)));
    return ((__plainRows).map((__row) => { (__row as any).displayName = (((__row as any).clientType === "company") ? (__row as any).companyName : (((__row as any).givenName + " ") + (__row as any).familyName)); (__row as any).isArchived = ((__row as any).status === "archived"); return { ...(__row as any), displayName: (__row as any).displayName, isArchived: (__row as any).isArchived }; })).map((d) => {
      const o = { ...(d as any) };
      delete (o as any).taxId;
      return o;
    });
  },
});

export const listClientByMergedIntoClientId = query({
  args: { mergedIntoClientId: v.id("clients") },
  handler: async (ctx, { mergedIntoClientId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("clientRead", "Client", () => (checkRole(user, "salesAccess") || checkRole(user, "financeAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("clients").withIndex("by_mergedIntoClientId", (q) => q.eq("mergedIntoClientId", mergedIntoClientId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "Client", ["email","phone","addressLine1","addressLine2","city","region","postalCode","countryCode","taxId"], row)));
    return ((__plainRows).map((__row) => { (__row as any).displayName = (((__row as any).clientType === "company") ? (__row as any).companyName : (((__row as any).givenName + " ") + (__row as any).familyName)); (__row as any).isArchived = ((__row as any).status === "archived"); return { ...(__row as any), displayName: (__row as any).displayName, isArchived: (__row as any).isArchived }; })).map((d) => {
      const o = { ...(d as any) };
      delete (o as any).taxId;
      return o;
    });
  },
});

export const listClientCommunication = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("clientCommunicationRead", "ClientCommunication", () => checkRole(user, "staffAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("clientCommunications").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    const __plainRows = rows;
    return __plainRows;
  },
});

export const getClientCommunication = query({
  args: { id: v.id("clientCommunications") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("clientCommunicationRead", "ClientCommunication", () => checkRole(user, "staffAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    return __doc;
  },
});

export const listClientCommunicationByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("clientCommunicationRead", "ClientCommunication", () => checkRole(user, "staffAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("clientCommunications").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listClientCommunicationByClientContactId = query({
  args: { clientContactId: v.id("clientContacts") },
  handler: async (ctx, { clientContactId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("clientCommunicationRead", "ClientCommunication", () => checkRole(user, "staffAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("clientCommunications").withIndex("by_clientContactId", (q) => q.eq("clientContactId", clientContactId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listClientCommunicationByEventId = query({
  args: { eventId: v.id("events") },
  handler: async (ctx, { eventId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("clientCommunicationRead", "ClientCommunication", () => checkRole(user, "staffAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("clientCommunications").withIndex("by_eventId", (q) => q.eq("eventId", eventId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listClientContact = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("clientContactRead", "ClientContact", () => checkRole(user, "salesAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("clientContacts").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "ClientContact", ["email","phone","mobile"], row)));
    return __plainRows;
  },
});

export const getClientContact = query({
  args: { id: v.id("clientContacts") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("clientContactRead", "ClientContact", () => checkRole(user, "salesAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = await __decryptDoc(ctx, "ClientContact", ["email","phone","mobile"], __rawDoc);
    return __doc;
  },
});

export const listClientContactByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("clientContactRead", "ClientContact", () => checkRole(user, "salesAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("clientContacts").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "ClientContact", ["email","phone","mobile"], row)));
    return __plainRows;
  },
});

export const listClientContactByClientId = query({
  args: { clientId: v.id("clients") },
  handler: async (ctx, { clientId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("clientContactRead", "ClientContact", () => checkRole(user, "salesAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("clientContacts").withIndex("by_clientId", (q) => q.eq("clientId", clientId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "ClientContact", ["email","phone","mobile"], row)));
    return __plainRows;
  },
});

export const listClientContactByClientMergeAuthorizationId = query({
  args: { clientMergeAuthorizationId: v.id("clientMerges") },
  handler: async (ctx, { clientMergeAuthorizationId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("clientContactRead", "ClientContact", () => checkRole(user, "salesAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("clientContacts").withIndex("by_clientMergeAuthorizationId", (q) => q.eq("clientMergeAuthorizationId", clientMergeAuthorizationId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "ClientContact", ["email","phone","mobile"], row)));
    return __plainRows;
  },
});

export const listClientContactByMergeTargetClientId = query({
  args: { mergeTargetClientId: v.id("clients") },
  handler: async (ctx, { mergeTargetClientId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("clientContactRead", "ClientContact", () => checkRole(user, "salesAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("clientContacts").withIndex("by_mergeTargetClientId", (q) => q.eq("mergeTargetClientId", mergeTargetClientId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "ClientContact", ["email","phone","mobile"], row)));
    return __plainRows;
  },
});

export const listClientMerge = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("clientMergeRead", "ClientMerge", () => checkRole(user, "salesAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("clientMerges").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const getClientMerge = query({
  args: { id: v.id("clientMerges") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("clientMergeRead", "ClientMerge", () => checkRole(user, "salesAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    return __doc;
  },
});

export const listClientMergeByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("clientMergeRead", "ClientMerge", () => checkRole(user, "salesAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("clientMerges").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listClientMergeByPrimaryClientId = query({
  args: { primaryClientId: v.id("clients") },
  handler: async (ctx, { primaryClientId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("clientMergeRead", "ClientMerge", () => checkRole(user, "salesAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("clientMerges").withIndex("by_primaryClientId", (q) => q.eq("primaryClientId", primaryClientId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listClientMergeByDuplicateClientId = query({
  args: { duplicateClientId: v.id("clients") },
  handler: async (ctx, { duplicateClientId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("clientMergeRead", "ClientMerge", () => checkRole(user, "salesAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("clientMerges").withIndex("by_duplicateClientId", (q) => q.eq("duplicateClientId", duplicateClientId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listClientOutreachTask = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("clientFollowUpReminderRead", "ClientOutreachTask", () => checkRole(user, "salesAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("clientOutreachTasks").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    const __plainRows = rows;
    return __plainRows;
  },
});

export const getClientOutreachTask = query({
  args: { id: v.id("clientOutreachTasks") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("clientFollowUpReminderRead", "ClientOutreachTask", () => checkRole(user, "salesAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    return __doc;
  },
});

export const listClientOutreachTaskByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("clientFollowUpReminderRead", "ClientOutreachTask", () => checkRole(user, "salesAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("clientOutreachTasks").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listClientOutreachTaskByClientId = query({
  args: { clientId: v.id("clients") },
  handler: async (ctx, { clientId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("clientFollowUpReminderRead", "ClientOutreachTask", () => checkRole(user, "salesAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("clientOutreachTasks").withIndex("by_clientId", (q) => q.eq("clientId", clientId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listComponent = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("componentRead", "Component", () => checkRole(user, "kitchenAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("components").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    const __projectedRows: any[] = [];
    for (const __row of __plainRows as any[]) {
      (__row as any).ingredientLines = await ctx.db.query("componentIngredients").withIndex("by_componentId", (q: any) => q.eq("componentId", __row._id)).collect();
      for (const __agg0 of ((__row as any).ingredientLines ?? []) as any[]) {
      {
      const __fk = (__agg0 as any).ingredientId;
      (__agg0 as any).ingredient = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      }
      (__row as any).isPublished = ((__row as any).status === "published");
      (__row as any).isEditable = ((__row as any).status === "draft");
      (__row as any).liveBatchCost = (((((__row as any).ingredientLines) ?? []).filter((line: Doc<"componentIngredients">) => (((line.deletedAt == null) && (line.addedAt != null))))) ?? []).map((line: Record<string, any>) => (((line.unit === line.ingredient.unit) ? ((line.quantity * ((line.wasteFactor != null) ? line.wasteFactor : 1)) * line.ingredient.costPerUnit) : 0))).reduce((acc: number, v: unknown) => acc + (typeof v === "number" ? v : 0), 0);
      (__row as any).liveCostPerGuest = (((__row as any).yieldQuantity > 0) ? ((((((__row as any).ingredientLines) ?? []).filter((line: Doc<"componentIngredients">) => (((line.deletedAt == null) && (line.addedAt != null))))) ?? []).map((line: Record<string, any>) => (((line.unit === line.ingredient.unit) ? ((line.quantity * ((line.wasteFactor != null) ? line.wasteFactor : 1)) * line.ingredient.costPerUnit) : 0))).reduce((acc: number, v: unknown) => acc + (typeof v === "number" ? v : 0), 0) / (__row as any).yieldQuantity) : 0);
      __projectedRows.push({ ...(__row as any), isPublished: (__row as any).isPublished, isEditable: (__row as any).isEditable, liveBatchCost: (__row as any).liveBatchCost, liveCostPerGuest: (__row as any).liveCostPerGuest });
    }
    return __projectedRows;
  },
});

export const getComponent = query({
  args: { id: v.id("components") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("componentRead", "Component", () => checkRole(user, "kitchenAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    (__doc as any).ingredientLines = await ctx.db.query("componentIngredients").withIndex("by_componentId", (q: any) => q.eq("componentId", __doc._id)).collect();
    for (const __agg0 of ((__doc as any).ingredientLines ?? []) as any[]) {
      {
        const __fk = (__agg0 as any).ingredientId;
        (__agg0 as any).ingredient = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
    }
    (__doc as any).isPublished = ((__doc as any).status === "published");
    (__doc as any).isEditable = ((__doc as any).status === "draft");
    (__doc as any).liveBatchCost = (((((__doc as any).ingredientLines) ?? []).filter((line: Doc<"componentIngredients">) => (((line.deletedAt == null) && (line.addedAt != null))))) ?? []).map((line: Record<string, any>) => (((line.unit === line.ingredient.unit) ? ((line.quantity * ((line.wasteFactor != null) ? line.wasteFactor : 1)) * line.ingredient.costPerUnit) : 0))).reduce((acc: number, v: unknown) => acc + (typeof v === "number" ? v : 0), 0);
    (__doc as any).liveCostPerGuest = (((__doc as any).yieldQuantity > 0) ? ((((((__doc as any).ingredientLines) ?? []).filter((line: Doc<"componentIngredients">) => (((line.deletedAt == null) && (line.addedAt != null))))) ?? []).map((line: Record<string, any>) => (((line.unit === line.ingredient.unit) ? ((line.quantity * ((line.wasteFactor != null) ? line.wasteFactor : 1)) * line.ingredient.costPerUnit) : 0))).reduce((acc: number, v: unknown) => acc + (typeof v === "number" ? v : 0), 0) / (__doc as any).yieldQuantity) : 0);
    const __hydrated = { ...(__doc as any), isPublished: (__doc as any).isPublished, isEditable: (__doc as any).isEditable, liveBatchCost: (__doc as any).liveBatchCost, liveCostPerGuest: (__doc as any).liveCostPerGuest };
    return __hydrated;
  },
});

export const listComponentByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("componentRead", "Component", () => checkRole(user, "kitchenAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("components").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    const __projectedRows: any[] = [];
    for (const __row of __plainRows as any[]) {
      (__row as any).ingredientLines = await ctx.db.query("componentIngredients").withIndex("by_componentId", (q: any) => q.eq("componentId", __row._id)).collect();
      for (const __agg0 of ((__row as any).ingredientLines ?? []) as any[]) {
      {
      const __fk = (__agg0 as any).ingredientId;
      (__agg0 as any).ingredient = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      }
      (__row as any).isPublished = ((__row as any).status === "published");
      (__row as any).isEditable = ((__row as any).status === "draft");
      (__row as any).liveBatchCost = (((((__row as any).ingredientLines) ?? []).filter((line: Doc<"componentIngredients">) => (((line.deletedAt == null) && (line.addedAt != null))))) ?? []).map((line: Record<string, any>) => (((line.unit === line.ingredient.unit) ? ((line.quantity * ((line.wasteFactor != null) ? line.wasteFactor : 1)) * line.ingredient.costPerUnit) : 0))).reduce((acc: number, v: unknown) => acc + (typeof v === "number" ? v : 0), 0);
      (__row as any).liveCostPerGuest = (((__row as any).yieldQuantity > 0) ? ((((((__row as any).ingredientLines) ?? []).filter((line: Doc<"componentIngredients">) => (((line.deletedAt == null) && (line.addedAt != null))))) ?? []).map((line: Record<string, any>) => (((line.unit === line.ingredient.unit) ? ((line.quantity * ((line.wasteFactor != null) ? line.wasteFactor : 1)) * line.ingredient.costPerUnit) : 0))).reduce((acc: number, v: unknown) => acc + (typeof v === "number" ? v : 0), 0) / (__row as any).yieldQuantity) : 0);
      __projectedRows.push({ ...(__row as any), isPublished: (__row as any).isPublished, isEditable: (__row as any).isEditable, liveBatchCost: (__row as any).liveBatchCost, liveCostPerGuest: (__row as any).liveCostPerGuest });
    }
    return __projectedRows;
  },
});

export const listComponentImport = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("componentImportRead", "ComponentImport", () => checkRole(user, "kitchenAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("componentImports").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const getComponentImport = query({
  args: { id: v.id("componentImports") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("componentImportRead", "ComponentImport", () => checkRole(user, "kitchenAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    return __doc;
  },
});

export const listComponentImportByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("componentImportRead", "ComponentImport", () => checkRole(user, "kitchenAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("componentImports").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listComponentImportByResultingComponentId = query({
  args: { resultingComponentId: v.id("components") },
  handler: async (ctx, { resultingComponentId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("componentImportRead", "ComponentImport", () => checkRole(user, "kitchenAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("componentImports").withIndex("by_resultingComponentId", (q) => q.eq("resultingComponentId", resultingComponentId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listComponentImportLine = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("componentImportLineRead", "ComponentImportLine", () => checkRole(user, "kitchenAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("componentImportLines").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const getComponentImportLine = query({
  args: { id: v.id("componentImportLines") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("componentImportLineRead", "ComponentImportLine", () => checkRole(user, "kitchenAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    return __doc;
  },
});

export const listComponentImportLineByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("componentImportLineRead", "ComponentImportLine", () => checkRole(user, "kitchenAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("componentImportLines").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listComponentImportLineByImportId = query({
  args: { importId: v.id("componentImports") },
  handler: async (ctx, { importId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("componentImportLineRead", "ComponentImportLine", () => checkRole(user, "kitchenAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("componentImportLines").withIndex("by_importId", (q) => q.eq("importId", importId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listComponentImportLineByMatchedIngredientId = query({
  args: { matchedIngredientId: v.id("ingredients") },
  handler: async (ctx, { matchedIngredientId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("componentImportLineRead", "ComponentImportLine", () => checkRole(user, "kitchenAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("componentImportLines").withIndex("by_matchedIngredientId", (q) => q.eq("matchedIngredientId", matchedIngredientId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listComponentIngredient = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("componentIngredientRead", "ComponentIngredient", () => checkRole(user, "kitchenAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("componentIngredients").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    const __projectedRows: any[] = [];
    for (const __row of __plainRows as any[]) {
      {
      const __fk = ((__row as any) as any).ingredientId;
      ((__row as any) as any).ingredient = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      (__row as any).adjustedQuantity = ((__row as any).quantity * (((__row as any).wasteFactor != null) ? (__row as any).wasteFactor : 1));
      (__row as any).unitsMatchIngredientPrice = ((__row as any).unit === (__row as any).ingredient.unit);
      (__row as any).liveIngredientCost = (((__row as any).unit === (__row as any).ingredient.unit) ? (((__row as any).quantity * (((__row as any).wasteFactor != null) ? (__row as any).wasteFactor : 1)) * (__row as any).ingredient.costPerUnit) : 0);
      __projectedRows.push({ ...(__row as any), adjustedQuantity: (__row as any).adjustedQuantity, unitsMatchIngredientPrice: (__row as any).unitsMatchIngredientPrice, liveIngredientCost: (__row as any).liveIngredientCost });
    }
    return __projectedRows;
  },
});

export const getComponentIngredient = query({
  args: { id: v.id("componentIngredients") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("componentIngredientRead", "ComponentIngredient", () => checkRole(user, "kitchenAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    {
      const __fk = ((__doc as any) as any).ingredientId;
      ((__doc as any) as any).ingredient = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    (__doc as any).adjustedQuantity = ((__doc as any).quantity * (((__doc as any).wasteFactor != null) ? (__doc as any).wasteFactor : 1));
    (__doc as any).unitsMatchIngredientPrice = ((__doc as any).unit === (__doc as any).ingredient.unit);
    (__doc as any).liveIngredientCost = (((__doc as any).unit === (__doc as any).ingredient.unit) ? (((__doc as any).quantity * (((__doc as any).wasteFactor != null) ? (__doc as any).wasteFactor : 1)) * (__doc as any).ingredient.costPerUnit) : 0);
    const __hydrated = { ...(__doc as any), adjustedQuantity: (__doc as any).adjustedQuantity, unitsMatchIngredientPrice: (__doc as any).unitsMatchIngredientPrice, liveIngredientCost: (__doc as any).liveIngredientCost };
    return __hydrated;
  },
});

export const listComponentIngredientByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("componentIngredientRead", "ComponentIngredient", () => checkRole(user, "kitchenAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("componentIngredients").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    const __projectedRows: any[] = [];
    for (const __row of __plainRows as any[]) {
      {
      const __fk = ((__row as any) as any).ingredientId;
      ((__row as any) as any).ingredient = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      (__row as any).adjustedQuantity = ((__row as any).quantity * (((__row as any).wasteFactor != null) ? (__row as any).wasteFactor : 1));
      (__row as any).unitsMatchIngredientPrice = ((__row as any).unit === (__row as any).ingredient.unit);
      (__row as any).liveIngredientCost = (((__row as any).unit === (__row as any).ingredient.unit) ? (((__row as any).quantity * (((__row as any).wasteFactor != null) ? (__row as any).wasteFactor : 1)) * (__row as any).ingredient.costPerUnit) : 0);
      __projectedRows.push({ ...(__row as any), adjustedQuantity: (__row as any).adjustedQuantity, unitsMatchIngredientPrice: (__row as any).unitsMatchIngredientPrice, liveIngredientCost: (__row as any).liveIngredientCost });
    }
    return __projectedRows;
  },
});

export const listComponentIngredientByComponentId = query({
  args: { componentId: v.id("components") },
  handler: async (ctx, { componentId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("componentIngredientRead", "ComponentIngredient", () => checkRole(user, "kitchenAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("componentIngredients").withIndex("by_componentId", (q) => q.eq("componentId", componentId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    const __projectedRows: any[] = [];
    for (const __row of __plainRows as any[]) {
      {
      const __fk = ((__row as any) as any).ingredientId;
      ((__row as any) as any).ingredient = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      (__row as any).adjustedQuantity = ((__row as any).quantity * (((__row as any).wasteFactor != null) ? (__row as any).wasteFactor : 1));
      (__row as any).unitsMatchIngredientPrice = ((__row as any).unit === (__row as any).ingredient.unit);
      (__row as any).liveIngredientCost = (((__row as any).unit === (__row as any).ingredient.unit) ? (((__row as any).quantity * (((__row as any).wasteFactor != null) ? (__row as any).wasteFactor : 1)) * (__row as any).ingredient.costPerUnit) : 0);
      __projectedRows.push({ ...(__row as any), adjustedQuantity: (__row as any).adjustedQuantity, unitsMatchIngredientPrice: (__row as any).unitsMatchIngredientPrice, liveIngredientCost: (__row as any).liveIngredientCost });
    }
    return __projectedRows;
  },
});

export const listComponentIngredientByIngredientId = query({
  args: { ingredientId: v.id("ingredients") },
  handler: async (ctx, { ingredientId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("componentIngredientRead", "ComponentIngredient", () => checkRole(user, "kitchenAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("componentIngredients").withIndex("by_ingredientId", (q) => q.eq("ingredientId", ingredientId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    const __projectedRows: any[] = [];
    for (const __row of __plainRows as any[]) {
      {
      const __fk = ((__row as any) as any).ingredientId;
      ((__row as any) as any).ingredient = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      (__row as any).adjustedQuantity = ((__row as any).quantity * (((__row as any).wasteFactor != null) ? (__row as any).wasteFactor : 1));
      (__row as any).unitsMatchIngredientPrice = ((__row as any).unit === (__row as any).ingredient.unit);
      (__row as any).liveIngredientCost = (((__row as any).unit === (__row as any).ingredient.unit) ? (((__row as any).quantity * (((__row as any).wasteFactor != null) ? (__row as any).wasteFactor : 1)) * (__row as any).ingredient.costPerUnit) : 0);
      __projectedRows.push({ ...(__row as any), adjustedQuantity: (__row as any).adjustedQuantity, unitsMatchIngredientPrice: (__row as any).unitsMatchIngredientPrice, liveIngredientCost: (__row as any).liveIngredientCost });
    }
    return __projectedRows;
  },
});

export const listComponentSnapshot = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("componentSnapshotRead", "ComponentSnapshot", () => checkRole(user, "kitchenAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("componentSnapshots").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const getComponentSnapshot = query({
  args: { id: v.id("componentSnapshots") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("componentSnapshotRead", "ComponentSnapshot", () => checkRole(user, "kitchenAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    return __doc;
  },
});

export const listComponentSnapshotByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("componentSnapshotRead", "ComponentSnapshot", () => checkRole(user, "kitchenAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("componentSnapshots").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listComponentSnapshotByComponentId = query({
  args: { componentId: v.id("components") },
  handler: async (ctx, { componentId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("componentSnapshotRead", "ComponentSnapshot", () => checkRole(user, "kitchenAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("componentSnapshots").withIndex("by_componentId", (q) => q.eq("componentId", componentId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listComponentStep = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("componentStepRead", "ComponentStep", () => checkRole(user, "kitchenAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("componentSteps").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const getComponentStep = query({
  args: { id: v.id("componentSteps") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("componentStepRead", "ComponentStep", () => checkRole(user, "kitchenAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    return __doc;
  },
});

export const listComponentStepByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("componentStepRead", "ComponentStep", () => checkRole(user, "kitchenAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("componentSteps").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listComponentStepByComponentId = query({
  args: { componentId: v.id("components") },
  handler: async (ctx, { componentId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("componentStepRead", "ComponentStep", () => checkRole(user, "kitchenAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("componentSteps").withIndex("by_componentId", (q) => q.eq("componentId", componentId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listContract = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("contractRead", "Contract", () => checkRole(user, "salesAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("contracts").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const getContract = query({
  args: { id: v.id("contracts") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("contractRead", "Contract", () => checkRole(user, "salesAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    return __doc;
  },
});

export const listContractByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("contractRead", "Contract", () => checkRole(user, "salesAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("contracts").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listContractByEventId = query({
  args: { eventId: v.id("events") },
  handler: async (ctx, { eventId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("contractRead", "Contract", () => checkRole(user, "salesAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("contracts").withIndex("by_eventId", (q) => q.eq("eventId", eventId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listContractByClientId = query({
  args: { clientId: v.id("clients") },
  handler: async (ctx, { clientId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("contractRead", "Contract", () => checkRole(user, "salesAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("contracts").withIndex("by_clientId", (q) => q.eq("clientId", clientId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listContractByClientMergeAuthorizationId = query({
  args: { clientMergeAuthorizationId: v.id("clientMerges") },
  handler: async (ctx, { clientMergeAuthorizationId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("contractRead", "Contract", () => checkRole(user, "salesAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("contracts").withIndex("by_clientMergeAuthorizationId", (q) => q.eq("clientMergeAuthorizationId", clientMergeAuthorizationId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listContractByMergeTargetClientId = query({
  args: { mergeTargetClientId: v.id("clients") },
  handler: async (ctx, { mergeTargetClientId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("contractRead", "Contract", () => checkRole(user, "salesAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("contracts").withIndex("by_mergeTargetClientId", (q) => q.eq("mergeTargetClientId", mergeTargetClientId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listCorrectiveAction = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("correctiveActionRead", "CorrectiveAction", () => (checkRole(user, "eventAccess") || checkRole(user, "kitchenAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("correctiveActions").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const getCorrectiveAction = query({
  args: { id: v.id("correctiveActions") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("correctiveActionRead", "CorrectiveAction", () => (checkRole(user, "eventAccess") || checkRole(user, "kitchenAccess")))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    return __doc;
  },
});

export const listCorrectiveActionByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("correctiveActionRead", "CorrectiveAction", () => (checkRole(user, "eventAccess") || checkRole(user, "kitchenAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("correctiveActions").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listCorrectiveActionByIncidentId = query({
  args: { incidentId: v.id("incidents") },
  handler: async (ctx, { incidentId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("correctiveActionRead", "CorrectiveAction", () => (checkRole(user, "eventAccess") || checkRole(user, "kitchenAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("correctiveActions").withIndex("by_incidentId", (q) => q.eq("incidentId", incidentId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listCorrectiveActionByEventId = query({
  args: { eventId: v.id("events") },
  handler: async (ctx, { eventId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("correctiveActionRead", "CorrectiveAction", () => (checkRole(user, "eventAccess") || checkRole(user, "kitchenAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("correctiveActions").withIndex("by_eventId", (q) => q.eq("eventId", eventId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listCreditMemo = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("creditMemoRead", "CreditMemo", () => checkRole(user, "financeAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("creditMemos").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const getCreditMemo = query({
  args: { id: v.id("creditMemos") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("creditMemoRead", "CreditMemo", () => checkRole(user, "financeAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    return __doc;
  },
});

export const listCreditMemoByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("creditMemoRead", "CreditMemo", () => checkRole(user, "financeAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("creditMemos").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listCreditMemoBySourceInvoiceId = query({
  args: { sourceInvoiceId: v.id("invoices") },
  handler: async (ctx, { sourceInvoiceId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("creditMemoRead", "CreditMemo", () => checkRole(user, "financeAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("creditMemos").withIndex("by_sourceInvoiceId", (q) => q.eq("sourceInvoiceId", sourceInvoiceId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listCreditMemoByClientId = query({
  args: { clientId: v.id("clients") },
  handler: async (ctx, { clientId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("creditMemoRead", "CreditMemo", () => checkRole(user, "financeAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("creditMemos").withIndex("by_clientId", (q) => q.eq("clientId", clientId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listCreditMemoByClientMergeAuthorizationId = query({
  args: { clientMergeAuthorizationId: v.id("clientMerges") },
  handler: async (ctx, { clientMergeAuthorizationId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("creditMemoRead", "CreditMemo", () => checkRole(user, "financeAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("creditMemos").withIndex("by_clientMergeAuthorizationId", (q) => q.eq("clientMergeAuthorizationId", clientMergeAuthorizationId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listCreditMemoByMergeTargetClientId = query({
  args: { mergeTargetClientId: v.id("clients") },
  handler: async (ctx, { mergeTargetClientId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("creditMemoRead", "CreditMemo", () => checkRole(user, "financeAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("creditMemos").withIndex("by_mergeTargetClientId", (q) => q.eq("mergeTargetClientId", mergeTargetClientId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listCreditMemoByTargetInvoiceId = query({
  args: { targetInvoiceId: v.id("invoices") },
  handler: async (ctx, { targetInvoiceId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("creditMemoRead", "CreditMemo", () => checkRole(user, "financeAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("creditMemos").withIndex("by_targetInvoiceId", (q) => q.eq("targetInvoiceId", targetInvoiceId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listCreditMemoByEventId = query({
  args: { eventId: v.id("events") },
  handler: async (ctx, { eventId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("creditMemoRead", "CreditMemo", () => checkRole(user, "financeAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("creditMemos").withIndex("by_eventId", (q) => q.eq("eventId", eventId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listCutoverDecision = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("cutoverDecisionRead", "CutoverDecision", () => (user.id != null))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("cutoverDecisions").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    const __plainRows = rows;
    return __plainRows;
  },
});

export const getCutoverDecision = query({
  args: { id: v.id("cutoverDecisions") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("cutoverDecisionRead", "CutoverDecision", () => (user.id != null))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    return __doc;
  },
});

export const listCutoverDecisionByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("cutoverDecisionRead", "CutoverDecision", () => (user.id != null))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("cutoverDecisions").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listDelivery = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("deliveryRead", "Delivery", () => (checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("deliveries").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "Delivery", ["notes"], row)));
    return __plainRows;
  },
});

export const getDelivery = query({
  args: { id: v.id("deliveries") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("deliveryRead", "Delivery", () => (checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = await __decryptDoc(ctx, "Delivery", ["notes"], __rawDoc);
    return __doc;
  },
});

export const listDeliveryByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("deliveryRead", "Delivery", () => (checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("deliveries").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "Delivery", ["notes"], row)));
    return __plainRows;
  },
});

export const listDeliveryByPackListId = query({
  args: { packListId: v.id("packLists") },
  handler: async (ctx, { packListId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("deliveryRead", "Delivery", () => (checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("deliveries").withIndex("by_packListId", (q) => q.eq("packListId", packListId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "Delivery", ["notes"], row)));
    return __plainRows;
  },
});

export const listDeliveryByEventId = query({
  args: { eventId: v.id("events") },
  handler: async (ctx, { eventId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("deliveryRead", "Delivery", () => (checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("deliveries").withIndex("by_eventId", (q) => q.eq("eventId", eventId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "Delivery", ["notes"], row)));
    return __plainRows;
  },
});

export const listDeliveryByDriverId = query({
  args: { driverId: v.id("people") },
  handler: async (ctx, { driverId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("deliveryRead", "Delivery", () => (checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("deliveries").withIndex("by_driverId", (q) => q.eq("driverId", driverId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "Delivery", ["notes"], row)));
    return __plainRows;
  },
});

export const listDeliveryByVehicleId = query({
  args: { vehicleId: v.id("vehicles") },
  handler: async (ctx, { vehicleId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("deliveryRead", "Delivery", () => (checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("deliveries").withIndex("by_vehicleId", (q) => q.eq("vehicleId", vehicleId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "Delivery", ["notes"], row)));
    return __plainRows;
  },
});

export const listDish = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("dishRead", "Dish", () => (checkRole(user, "kitchenAccess") || checkRole(user, "salesAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("dishes").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return (__plainRows).map((__row) => { (__row as any).isActive = ((__row as any).status === "active"); (__row as any).isRetired = ((__row as any).status === "retired"); (__row as any).isCanonicalRoot = (((__row as any).canonicalDishId == null) && ((__row as any).mergedIntoDishId == null)); return { ...(__row as any), isActive: (__row as any).isActive, isRetired: (__row as any).isRetired, isCanonicalRoot: (__row as any).isCanonicalRoot }; });
  },
});

export const getDish = query({
  args: { id: v.id("dishes") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("dishRead", "Dish", () => (checkRole(user, "kitchenAccess") || checkRole(user, "salesAccess")))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    (__doc as any).isActive = ((__doc as any).status === "active");
    (__doc as any).isRetired = ((__doc as any).status === "retired");
    (__doc as any).isCanonicalRoot = (((__doc as any).canonicalDishId == null) && ((__doc as any).mergedIntoDishId == null));
    const __hydrated = { ...(__doc as any), isActive: (__doc as any).isActive, isRetired: (__doc as any).isRetired, isCanonicalRoot: (__doc as any).isCanonicalRoot };
    return __hydrated;
  },
});

export const listDishByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("dishRead", "Dish", () => (checkRole(user, "kitchenAccess") || checkRole(user, "salesAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("dishes").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return (__plainRows).map((__row) => { (__row as any).isActive = ((__row as any).status === "active"); (__row as any).isRetired = ((__row as any).status === "retired"); (__row as any).isCanonicalRoot = (((__row as any).canonicalDishId == null) && ((__row as any).mergedIntoDishId == null)); return { ...(__row as any), isActive: (__row as any).isActive, isRetired: (__row as any).isRetired, isCanonicalRoot: (__row as any).isCanonicalRoot }; });
  },
});

export const listDishByCanonicalDishId = query({
  args: { canonicalDishId: v.id("dishes") },
  handler: async (ctx, { canonicalDishId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("dishRead", "Dish", () => (checkRole(user, "kitchenAccess") || checkRole(user, "salesAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("dishes").withIndex("by_canonicalDishId", (q) => q.eq("canonicalDishId", canonicalDishId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return (__plainRows).map((__row) => { (__row as any).isActive = ((__row as any).status === "active"); (__row as any).isRetired = ((__row as any).status === "retired"); (__row as any).isCanonicalRoot = (((__row as any).canonicalDishId == null) && ((__row as any).mergedIntoDishId == null)); return { ...(__row as any), isActive: (__row as any).isActive, isRetired: (__row as any).isRetired, isCanonicalRoot: (__row as any).isCanonicalRoot }; });
  },
});

export const listDishByMergedIntoDishId = query({
  args: { mergedIntoDishId: v.id("dishes") },
  handler: async (ctx, { mergedIntoDishId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("dishRead", "Dish", () => (checkRole(user, "kitchenAccess") || checkRole(user, "salesAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("dishes").withIndex("by_mergedIntoDishId", (q) => q.eq("mergedIntoDishId", mergedIntoDishId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return (__plainRows).map((__row) => { (__row as any).isActive = ((__row as any).status === "active"); (__row as any).isRetired = ((__row as any).status === "retired"); (__row as any).isCanonicalRoot = (((__row as any).canonicalDishId == null) && ((__row as any).mergedIntoDishId == null)); return { ...(__row as any), isActive: (__row as any).isActive, isRetired: (__row as any).isRetired, isCanonicalRoot: (__row as any).isCanonicalRoot }; });
  },
});

export const listDishComponent = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("dishComponentRead", "DishComponent", () => checkRole(user, "kitchenAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("dishComponents").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const getDishComponent = query({
  args: { id: v.id("dishComponents") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("dishComponentRead", "DishComponent", () => checkRole(user, "kitchenAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    return __doc;
  },
});

export const listDishComponentByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("dishComponentRead", "DishComponent", () => checkRole(user, "kitchenAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("dishComponents").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listDishComponentByDishId = query({
  args: { dishId: v.id("dishes") },
  handler: async (ctx, { dishId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("dishComponentRead", "DishComponent", () => checkRole(user, "kitchenAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("dishComponents").withIndex("by_dishId", (q) => q.eq("dishId", dishId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listDishComponentByComponentId = query({
  args: { componentId: v.id("components") },
  handler: async (ctx, { componentId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("dishComponentRead", "DishComponent", () => checkRole(user, "kitchenAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("dishComponents").withIndex("by_componentId", (q) => q.eq("componentId", componentId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listDishContainer = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("dishContainerRead", "DishContainer", () => checkRole(user, "kitchenAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("dishContainers").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const getDishContainer = query({
  args: { id: v.id("dishContainers") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("dishContainerRead", "DishContainer", () => checkRole(user, "kitchenAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    return __doc;
  },
});

export const listDishContainerByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("dishContainerRead", "DishContainer", () => checkRole(user, "kitchenAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("dishContainers").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listDishContainerByDishId = query({
  args: { dishId: v.id("dishes") },
  handler: async (ctx, { dishId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("dishContainerRead", "DishContainer", () => checkRole(user, "kitchenAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("dishContainers").withIndex("by_dishId", (q) => q.eq("dishId", dishId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listDishIngredient = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("dishIngredientRead", "DishIngredient", () => checkRole(user, "kitchenAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("dishIngredients").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    const __projectedRows: any[] = [];
    for (const __row of __plainRows as any[]) {
      {
      const __fk = ((__row as any) as any).ingredientId;
      ((__row as any) as any).ingredient = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      (__row as any).adjustedQuantity = ((__row as any).quantity * (((__row as any).wasteFactor != null) ? (__row as any).wasteFactor : 1));
      (__row as any).unitsMatchIngredientPrice = ((__row as any).unit === (__row as any).ingredient.unit);
      (__row as any).liveIngredientCost = (((__row as any).unit === (__row as any).ingredient.unit) ? (((__row as any).quantity * (((__row as any).wasteFactor != null) ? (__row as any).wasteFactor : 1)) * (__row as any).ingredient.costPerUnit) : 0);
      __projectedRows.push({ ...(__row as any), adjustedQuantity: (__row as any).adjustedQuantity, unitsMatchIngredientPrice: (__row as any).unitsMatchIngredientPrice, liveIngredientCost: (__row as any).liveIngredientCost });
    }
    return __projectedRows;
  },
});

export const getDishIngredient = query({
  args: { id: v.id("dishIngredients") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("dishIngredientRead", "DishIngredient", () => checkRole(user, "kitchenAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    {
      const __fk = ((__doc as any) as any).ingredientId;
      ((__doc as any) as any).ingredient = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    (__doc as any).adjustedQuantity = ((__doc as any).quantity * (((__doc as any).wasteFactor != null) ? (__doc as any).wasteFactor : 1));
    (__doc as any).unitsMatchIngredientPrice = ((__doc as any).unit === (__doc as any).ingredient.unit);
    (__doc as any).liveIngredientCost = (((__doc as any).unit === (__doc as any).ingredient.unit) ? (((__doc as any).quantity * (((__doc as any).wasteFactor != null) ? (__doc as any).wasteFactor : 1)) * (__doc as any).ingredient.costPerUnit) : 0);
    const __hydrated = { ...(__doc as any), adjustedQuantity: (__doc as any).adjustedQuantity, unitsMatchIngredientPrice: (__doc as any).unitsMatchIngredientPrice, liveIngredientCost: (__doc as any).liveIngredientCost };
    return __hydrated;
  },
});

export const listDishIngredientByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("dishIngredientRead", "DishIngredient", () => checkRole(user, "kitchenAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("dishIngredients").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    const __projectedRows: any[] = [];
    for (const __row of __plainRows as any[]) {
      {
      const __fk = ((__row as any) as any).ingredientId;
      ((__row as any) as any).ingredient = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      (__row as any).adjustedQuantity = ((__row as any).quantity * (((__row as any).wasteFactor != null) ? (__row as any).wasteFactor : 1));
      (__row as any).unitsMatchIngredientPrice = ((__row as any).unit === (__row as any).ingredient.unit);
      (__row as any).liveIngredientCost = (((__row as any).unit === (__row as any).ingredient.unit) ? (((__row as any).quantity * (((__row as any).wasteFactor != null) ? (__row as any).wasteFactor : 1)) * (__row as any).ingredient.costPerUnit) : 0);
      __projectedRows.push({ ...(__row as any), adjustedQuantity: (__row as any).adjustedQuantity, unitsMatchIngredientPrice: (__row as any).unitsMatchIngredientPrice, liveIngredientCost: (__row as any).liveIngredientCost });
    }
    return __projectedRows;
  },
});

export const listDishIngredientByDishId = query({
  args: { dishId: v.id("dishes") },
  handler: async (ctx, { dishId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("dishIngredientRead", "DishIngredient", () => checkRole(user, "kitchenAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("dishIngredients").withIndex("by_dishId", (q) => q.eq("dishId", dishId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    const __projectedRows: any[] = [];
    for (const __row of __plainRows as any[]) {
      {
      const __fk = ((__row as any) as any).ingredientId;
      ((__row as any) as any).ingredient = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      (__row as any).adjustedQuantity = ((__row as any).quantity * (((__row as any).wasteFactor != null) ? (__row as any).wasteFactor : 1));
      (__row as any).unitsMatchIngredientPrice = ((__row as any).unit === (__row as any).ingredient.unit);
      (__row as any).liveIngredientCost = (((__row as any).unit === (__row as any).ingredient.unit) ? (((__row as any).quantity * (((__row as any).wasteFactor != null) ? (__row as any).wasteFactor : 1)) * (__row as any).ingredient.costPerUnit) : 0);
      __projectedRows.push({ ...(__row as any), adjustedQuantity: (__row as any).adjustedQuantity, unitsMatchIngredientPrice: (__row as any).unitsMatchIngredientPrice, liveIngredientCost: (__row as any).liveIngredientCost });
    }
    return __projectedRows;
  },
});

export const listDishIngredientByIngredientId = query({
  args: { ingredientId: v.id("ingredients") },
  handler: async (ctx, { ingredientId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("dishIngredientRead", "DishIngredient", () => checkRole(user, "kitchenAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("dishIngredients").withIndex("by_ingredientId", (q) => q.eq("ingredientId", ingredientId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    const __projectedRows: any[] = [];
    for (const __row of __plainRows as any[]) {
      {
      const __fk = ((__row as any) as any).ingredientId;
      ((__row as any) as any).ingredient = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      (__row as any).adjustedQuantity = ((__row as any).quantity * (((__row as any).wasteFactor != null) ? (__row as any).wasteFactor : 1));
      (__row as any).unitsMatchIngredientPrice = ((__row as any).unit === (__row as any).ingredient.unit);
      (__row as any).liveIngredientCost = (((__row as any).unit === (__row as any).ingredient.unit) ? (((__row as any).quantity * (((__row as any).wasteFactor != null) ? (__row as any).wasteFactor : 1)) * (__row as any).ingredient.costPerUnit) : 0);
      __projectedRows.push({ ...(__row as any), adjustedQuantity: (__row as any).adjustedQuantity, unitsMatchIngredientPrice: (__row as any).unitsMatchIngredientPrice, liveIngredientCost: (__row as any).liveIngredientCost });
    }
    return __projectedRows;
  },
});

export const listDishTask = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("dishTaskRead", "DishTask", () => checkRole(user, "kitchenAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("dishTasks").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const getDishTask = query({
  args: { id: v.id("dishTasks") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("dishTaskRead", "DishTask", () => checkRole(user, "kitchenAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    return __doc;
  },
});

export const listDishTaskByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("dishTaskRead", "DishTask", () => checkRole(user, "kitchenAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("dishTasks").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listDishTaskByActiveDishId = query({
  args: { activeDishId: v.string() },
  handler: async (ctx, { activeDishId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("dishTaskRead", "DishTask", () => checkRole(user, "kitchenAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("dishTasks").withIndex("by_activeDishId", (q) => q.eq("activeDishId", activeDishId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listDishTaskByDishId = query({
  args: { dishId: v.id("dishes") },
  handler: async (ctx, { dishId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("dishTaskRead", "DishTask", () => checkRole(user, "kitchenAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("dishTasks").withIndex("by_dishId", (q) => q.eq("dishId", dishId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listDishTaskByComponentId = query({
  args: { componentId: v.id("components") },
  handler: async (ctx, { componentId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("dishTaskRead", "DishTask", () => checkRole(user, "kitchenAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("dishTasks").withIndex("by_componentId", (q) => q.eq("componentId", componentId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listDishTaskByIngredientId = query({
  args: { ingredientId: v.id("ingredients") },
  handler: async (ctx, { ingredientId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("dishTaskRead", "DishTask", () => checkRole(user, "kitchenAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("dishTasks").withIndex("by_ingredientId", (q) => q.eq("ingredientId", ingredientId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listEmailNotificationSubscription = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("emailNotificationSubscriptions").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    const __plainRows = rows;
    const __visibleRows: any[] = [];
    for (const __row of __plainRows) {
      if (!__allowsRead("emailNotificationSubscriptionRead", "EmailNotificationSubscription", () => ((__row.ownerId == null) || (__row.ownerId === user.id)))) continue;
      __visibleRows.push(__row);
    }
    return __visibleRows;
  },
});

export const getEmailNotificationSubscription = query({
  args: { id: v.id("emailNotificationSubscriptions") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    if (!__allowsRead("emailNotificationSubscriptionRead", "EmailNotificationSubscription", () => ((__doc.ownerId == null) || (__doc.ownerId === user.id)))) return null;
    return __doc;
  },
});

export const listEmailNotificationSubscriptionByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("emailNotificationSubscriptions").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    const __plainRows = rows;
    const __visibleRows: any[] = [];
    for (const __row of __plainRows) {
      if (!__allowsRead("emailNotificationSubscriptionRead", "EmailNotificationSubscription", () => ((__row.ownerId == null) || (__row.ownerId === user.id)))) continue;
      __visibleRows.push(__row);
    }
    return __visibleRows;
  },
});

export const listEmailNotificationSubscriptionByOwnerId = query({
  args: { ownerId: v.string() },
  handler: async (ctx, { ownerId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("emailNotificationSubscriptions").withIndex("by_ownerId", (q) => q.eq("ownerId", ownerId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    const __plainRows = rows;
    const __visibleRows: any[] = [];
    for (const __row of __plainRows) {
      if (!__allowsRead("emailNotificationSubscriptionRead", "EmailNotificationSubscription", () => ((__row.ownerId == null) || (__row.ownerId === user.id)))) continue;
      __visibleRows.push(__row);
    }
    return __visibleRows;
  },
});

export const listEquipment = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("equipmentRead", "Equipment", () => (checkRole(user, "inventoryAccess") || checkRole(user, "logisticsAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("equipments").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return (__plainRows).map((__row) => { (__row as any).isActive = ((__row as any).status === "active"); (__row as any).totalValue = ((__row as any).quantity * (__row as any).purchaseValue); return { ...(__row as any), isActive: (__row as any).isActive, totalValue: (__row as any).totalValue }; });
  },
});

export const getEquipment = query({
  args: { id: v.id("equipments") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("equipmentRead", "Equipment", () => (checkRole(user, "inventoryAccess") || checkRole(user, "logisticsAccess")))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    (__doc as any).isActive = ((__doc as any).status === "active");
    (__doc as any).totalValue = ((__doc as any).quantity * (__doc as any).purchaseValue);
    const __hydrated = { ...(__doc as any), isActive: (__doc as any).isActive, totalValue: (__doc as any).totalValue };
    return __hydrated;
  },
});

export const listEquipmentByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("equipmentRead", "Equipment", () => (checkRole(user, "inventoryAccess") || checkRole(user, "logisticsAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("equipments").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return (__plainRows).map((__row) => { (__row as any).isActive = ((__row as any).status === "active"); (__row as any).totalValue = ((__row as any).quantity * (__row as any).purchaseValue); return { ...(__row as any), isActive: (__row as any).isActive, totalValue: (__row as any).totalValue }; });
  },
});

export const listEquipmentMaintenanceTask = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("maintenanceTaskRead", "EquipmentMaintenanceTask", () => (checkRole(user, "inventoryAccess") || checkRole(user, "logisticsAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("equipmentMaintenanceTasks").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const getEquipmentMaintenanceTask = query({
  args: { id: v.id("equipmentMaintenanceTasks") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("maintenanceTaskRead", "EquipmentMaintenanceTask", () => (checkRole(user, "inventoryAccess") || checkRole(user, "logisticsAccess")))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    return __doc;
  },
});

export const listEquipmentMaintenanceTaskByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("maintenanceTaskRead", "EquipmentMaintenanceTask", () => (checkRole(user, "inventoryAccess") || checkRole(user, "logisticsAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("equipmentMaintenanceTasks").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listEquipmentMaintenanceTaskByEquipmentId = query({
  args: { equipmentId: v.id("equipments") },
  handler: async (ctx, { equipmentId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("maintenanceTaskRead", "EquipmentMaintenanceTask", () => (checkRole(user, "inventoryAccess") || checkRole(user, "logisticsAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("equipmentMaintenanceTasks").withIndex("by_equipmentId", (q) => q.eq("equipmentId", equipmentId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listEquipmentReservation = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("equipmentReservationRead", "EquipmentReservation", () => (checkRole(user, "inventoryAccess") || checkRole(user, "logisticsAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("equipmentReservations").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const getEquipmentReservation = query({
  args: { id: v.id("equipmentReservations") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("equipmentReservationRead", "EquipmentReservation", () => (checkRole(user, "inventoryAccess") || checkRole(user, "logisticsAccess")))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    return __doc;
  },
});

export const listEquipmentReservationByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("equipmentReservationRead", "EquipmentReservation", () => (checkRole(user, "inventoryAccess") || checkRole(user, "logisticsAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("equipmentReservations").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listEquipmentReservationByEquipmentId = query({
  args: { equipmentId: v.id("equipments") },
  handler: async (ctx, { equipmentId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("equipmentReservationRead", "EquipmentReservation", () => (checkRole(user, "inventoryAccess") || checkRole(user, "logisticsAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("equipmentReservations").withIndex("by_equipmentId", (q) => q.eq("equipmentId", equipmentId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listEquipmentReservationByEventId = query({
  args: { eventId: v.id("events") },
  handler: async (ctx, { eventId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("equipmentReservationRead", "EquipmentReservation", () => (checkRole(user, "inventoryAccess") || checkRole(user, "logisticsAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("equipmentReservations").withIndex("by_eventId", (q) => q.eq("eventId", eventId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listEquipmentServiceEntry = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("equipmentServiceRead", "EquipmentServiceEntry", () => (checkRole(user, "inventoryAccess") || checkRole(user, "logisticsAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("equipmentServiceEntries").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const getEquipmentServiceEntry = query({
  args: { id: v.id("equipmentServiceEntries") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("equipmentServiceRead", "EquipmentServiceEntry", () => (checkRole(user, "inventoryAccess") || checkRole(user, "logisticsAccess")))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    return __doc;
  },
});

export const listEquipmentServiceEntryByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("equipmentServiceRead", "EquipmentServiceEntry", () => (checkRole(user, "inventoryAccess") || checkRole(user, "logisticsAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("equipmentServiceEntries").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listEquipmentServiceEntryByMaintenanceTaskId = query({
  args: { maintenanceTaskId: v.id("equipmentMaintenanceTasks") },
  handler: async (ctx, { maintenanceTaskId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("equipmentServiceRead", "EquipmentServiceEntry", () => (checkRole(user, "inventoryAccess") || checkRole(user, "logisticsAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("equipmentServiceEntries").withIndex("by_maintenanceTaskId", (q) => q.eq("maintenanceTaskId", maintenanceTaskId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listEquipmentServiceEntryByEquipmentId = query({
  args: { equipmentId: v.id("equipments") },
  handler: async (ctx, { equipmentId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("equipmentServiceRead", "EquipmentServiceEntry", () => (checkRole(user, "inventoryAccess") || checkRole(user, "logisticsAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("equipmentServiceEntries").withIndex("by_equipmentId", (q) => q.eq("equipmentId", equipmentId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listEvent = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("eventRead", "Event", () => (checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("events").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "Event", ["primaryContactName","primaryContactEmail","primaryContactPhone"], row)));
    const __projectedRows: any[] = [];
    for (const __row of __plainRows as any[]) {
      {
      const __fk = ((__row as any) as any).clientId;
      ((__row as any) as any).client = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      (__row as any).eventDishes = await ctx.db.query("eventDishes").withIndex("by_eventId", (q: any) => q.eq("eventId", __row._id)).collect();
      for (const __agg0 of ((__row as any).eventDishes ?? []) as any[]) {
      {
      const __fk = (__agg0 as any).eventId;
      (__agg0 as any).event = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      {
      const __fk = (__agg0 as any).dishId;
      (__agg0 as any).dish = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      if ((__agg0 as any).dish) {
      (__agg0 as any).dish.componentLines = await ctx.db.query("dishComponents").withIndex("by_dishId", (q: any) => q.eq("dishId", (__agg0 as any).dish._id)).collect();
      for (const __agg1 of ((__agg0 as any).dish.componentLines ?? []) as any[]) {
      {
      const __fk = (__agg1 as any).eventId;
      (__agg1 as any).event = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      {
      const __fk = (__agg1 as any).componentId;
      (__agg1 as any).component = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      if ((__agg1 as any).component) {
      (__agg1 as any).component.ingredientLines = await ctx.db.query("componentIngredients").withIndex("by_componentId", (q: any) => q.eq("componentId", (__agg1 as any).component._id)).collect();
      for (const __agg2 of ((__agg1 as any).component.ingredientLines ?? []) as any[]) {
      {
      const __fk = (__agg2 as any).ingredientId;
      (__agg2 as any).ingredient = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      }
      }
      }
      (__agg0 as any).dish.ingredientLines = await ctx.db.query("dishIngredients").withIndex("by_dishId", (q: any) => q.eq("dishId", (__agg0 as any).dish._id)).collect();
      for (const __agg3 of ((__agg0 as any).dish.ingredientLines ?? []) as any[]) {
      {
      const __fk = (__agg3 as any).ingredientId;
      (__agg3 as any).ingredient = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      }
      }
      __agg0.targetHeadcount = (((__agg0.headcountOverride != null) && (__agg0.headcountOverride > 0)) ? __agg0.headcountOverride : __agg0.event.expectedHeadcount);
      __agg0.estimatedCost = (((((__agg0.dish.componentLines) ?? []).filter((line: Record<string, any>) => (((line.deletedAt == null) && (line.attachedAt != null))))) ?? []).map((line: Record<string, any>) => (((((line.yieldQuantity > 0) ? Math.ceil((__agg0.targetHeadcount / line.yieldQuantity)) : 0) * line.batchMultiplier) * ((((line.component.ingredientLines) ?? []).filter((il: Record<string, any>) => (((il.deletedAt == null) && (il.addedAt != null))))) ?? []).map((il: Record<string, any>) => (((il.unit === il.ingredient.unit) ? ((il.quantity * ((il.wasteFactor != null) ? il.wasteFactor : 1)) * il.ingredient.costPerUnit) : 0))).reduce((acc: number, v: unknown) => acc + (typeof v === "number" ? v : 0), 0)))).reduce((acc: number, v: unknown) => acc + (typeof v === "number" ? v : 0), 0) + (__agg0.targetHeadcount * ((((__agg0.dish.ingredientLines) ?? []).filter((dl: Record<string, any>) => (((dl.deletedAt == null) && (dl.addedAt != null))))) ?? []).map((dl: Record<string, any>) => (((dl.unit === dl.ingredient.unit) ? ((dl.quantity * ((dl.wasteFactor != null) ? dl.wasteFactor : 1)) * dl.ingredient.costPerUnit) : 0))).reduce((acc: number, v: unknown) => acc + (typeof v === "number" ? v : 0), 0)));
      }
      (__row as any).assignments = await ctx.db.query("eventAssignments").withIndex("by_eventId", (q: any) => q.eq("eventId", __row._id)).collect();
      (__row as any).prepTasks = await ctx.db.query("prepTasks").withIndex("by_eventId", (q: any) => q.eq("eventId", __row._id)).collect();
      (__row as any).packLists = await ctx.db.query("packLists").withIndex("by_eventId", (q: any) => q.eq("eventId", __row._id)).collect();
      (__row as any).deliveries = await ctx.db.query("deliveries").withIndex("by_eventId", (q: any) => q.eq("eventId", __row._id)).collect();
      (__row as any).isTerminal = (((__row as any).stage === "cancelled") || ((__row as any).stage === "closed_out"));
      (__row as any).isEditable = ((((__row as any).stage === "planning") || ((__row as any).stage === "pending_approval")) || ((__row as any).stage === "approved"));
      (__row as any).hasAssignedClient = (((__row as any).client != null) && ((__row as any).client.deletedAt == null));
      (__row as any).hasExpectedHeadcount = ((__row as any).expectedHeadcount > 0);
      (__row as any).hasMenuDishes = ((((__row as any).eventDishes) ?? []).filter((d: Doc<"eventDishes">) => (((d.deletedAt == null) && (d.removedAt == null)))).length > 0);
      (__row as any).hasStaffAssigned = ((((__row as any).assignments) ?? []).filter((a: Doc<"eventAssignments">) => (((a.deletedAt == null) && (a.status !== "unassigned")))).length > 0);
      (__row as any).isSetupReady = ((((__row as any).hasAssignedClient && (__row as any).hasExpectedHeadcount) && (__row as any).hasMenuDishes) && (__row as any).hasStaffAssigned);
      (__row as any).isReadyForExecution = ((((((__row as any).prepTasks) ?? []).filter((t: Doc<"prepTasks">) => (((t.status !== "completed") && (t.status !== "cancelled")))).length === 0) && ((((__row as any).packLists) ?? []).filter((p: Doc<"packLists">) => ((((p.status !== "dispatched") && (p.status !== "cancelled")) && (p.status !== "draft")))).length === 0)) && ((((__row as any).deliveries) ?? []).filter((d: Doc<"deliveries">) => ((((d.status !== "delivered") && (d.status !== "cancelled")) && (d.status !== "failed")))).length === 0));
      (__row as any).estimatedFoodCost = (((((__row as any).eventDishes) ?? []).filter((item: Doc<"eventDishes">) => (((item.deletedAt == null) && (item.addedAt != null))))) ?? []).map((item: Record<string, any>) => (item.estimatedCost)).reduce((acc: number, v: unknown) => acc + (typeof v === "number" ? v : 0), 0);
      __projectedRows.push({ ...(__row as any), isTerminal: (__row as any).isTerminal, isEditable: (__row as any).isEditable, hasAssignedClient: (__row as any).hasAssignedClient, hasExpectedHeadcount: (__row as any).hasExpectedHeadcount, hasMenuDishes: (__row as any).hasMenuDishes, hasStaffAssigned: (__row as any).hasStaffAssigned, isSetupReady: (__row as any).isSetupReady, isReadyForExecution: (__row as any).isReadyForExecution, estimatedFoodCost: (__row as any).estimatedFoodCost });
    }
    return __projectedRows;
  },
});

export const getEvent = query({
  args: { id: v.id("events") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("eventRead", "Event", () => (checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = await __decryptDoc(ctx, "Event", ["primaryContactName","primaryContactEmail","primaryContactPhone"], __rawDoc);
    {
      const __fk = ((__doc as any) as any).clientId;
      ((__doc as any) as any).client = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    (__doc as any).eventDishes = await ctx.db.query("eventDishes").withIndex("by_eventId", (q: any) => q.eq("eventId", __doc._id)).collect();
    for (const __agg0 of ((__doc as any).eventDishes ?? []) as any[]) {
      {
        const __fk = (__agg0 as any).eventId;
        (__agg0 as any).event = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      {
        const __fk = (__agg0 as any).dishId;
        (__agg0 as any).dish = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      if ((__agg0 as any).dish) {
        (__agg0 as any).dish.componentLines = await ctx.db.query("dishComponents").withIndex("by_dishId", (q: any) => q.eq("dishId", (__agg0 as any).dish._id)).collect();
        for (const __agg1 of ((__agg0 as any).dish.componentLines ?? []) as any[]) {
          {
            const __fk = (__agg1 as any).eventId;
            (__agg1 as any).event = __fk != null ? await ctx.db.get(__fk as any) : null;
          }
          {
            const __fk = (__agg1 as any).componentId;
            (__agg1 as any).component = __fk != null ? await ctx.db.get(__fk as any) : null;
          }
          if ((__agg1 as any).component) {
            (__agg1 as any).component.ingredientLines = await ctx.db.query("componentIngredients").withIndex("by_componentId", (q: any) => q.eq("componentId", (__agg1 as any).component._id)).collect();
            for (const __agg2 of ((__agg1 as any).component.ingredientLines ?? []) as any[]) {
              {
                const __fk = (__agg2 as any).ingredientId;
                (__agg2 as any).ingredient = __fk != null ? await ctx.db.get(__fk as any) : null;
              }
            }
          }
        }
        (__agg0 as any).dish.ingredientLines = await ctx.db.query("dishIngredients").withIndex("by_dishId", (q: any) => q.eq("dishId", (__agg0 as any).dish._id)).collect();
        for (const __agg3 of ((__agg0 as any).dish.ingredientLines ?? []) as any[]) {
          {
            const __fk = (__agg3 as any).ingredientId;
            (__agg3 as any).ingredient = __fk != null ? await ctx.db.get(__fk as any) : null;
          }
        }
      }
      __agg0.targetHeadcount = (((__agg0.headcountOverride != null) && (__agg0.headcountOverride > 0)) ? __agg0.headcountOverride : __agg0.event.expectedHeadcount);
      __agg0.estimatedCost = (((((__agg0.dish.componentLines) ?? []).filter((line: Record<string, any>) => (((line.deletedAt == null) && (line.attachedAt != null))))) ?? []).map((line: Record<string, any>) => (((((line.yieldQuantity > 0) ? Math.ceil((__agg0.targetHeadcount / line.yieldQuantity)) : 0) * line.batchMultiplier) * ((((line.component.ingredientLines) ?? []).filter((il: Record<string, any>) => (((il.deletedAt == null) && (il.addedAt != null))))) ?? []).map((il: Record<string, any>) => (((il.unit === il.ingredient.unit) ? ((il.quantity * ((il.wasteFactor != null) ? il.wasteFactor : 1)) * il.ingredient.costPerUnit) : 0))).reduce((acc: number, v: unknown) => acc + (typeof v === "number" ? v : 0), 0)))).reduce((acc: number, v: unknown) => acc + (typeof v === "number" ? v : 0), 0) + (__agg0.targetHeadcount * ((((__agg0.dish.ingredientLines) ?? []).filter((dl: Record<string, any>) => (((dl.deletedAt == null) && (dl.addedAt != null))))) ?? []).map((dl: Record<string, any>) => (((dl.unit === dl.ingredient.unit) ? ((dl.quantity * ((dl.wasteFactor != null) ? dl.wasteFactor : 1)) * dl.ingredient.costPerUnit) : 0))).reduce((acc: number, v: unknown) => acc + (typeof v === "number" ? v : 0), 0)));
    }
    (__doc as any).assignments = await ctx.db.query("eventAssignments").withIndex("by_eventId", (q: any) => q.eq("eventId", __doc._id)).collect();
    (__doc as any).prepTasks = await ctx.db.query("prepTasks").withIndex("by_eventId", (q: any) => q.eq("eventId", __doc._id)).collect();
    (__doc as any).packLists = await ctx.db.query("packLists").withIndex("by_eventId", (q: any) => q.eq("eventId", __doc._id)).collect();
    (__doc as any).deliveries = await ctx.db.query("deliveries").withIndex("by_eventId", (q: any) => q.eq("eventId", __doc._id)).collect();
    (__doc as any).isTerminal = (((__doc as any).stage === "cancelled") || ((__doc as any).stage === "closed_out"));
    (__doc as any).isEditable = ((((__doc as any).stage === "planning") || ((__doc as any).stage === "pending_approval")) || ((__doc as any).stage === "approved"));
    (__doc as any).hasAssignedClient = (((__doc as any).client != null) && ((__doc as any).client.deletedAt == null));
    (__doc as any).hasExpectedHeadcount = ((__doc as any).expectedHeadcount > 0);
    (__doc as any).hasMenuDishes = ((((__doc as any).eventDishes) ?? []).filter((d: Doc<"eventDishes">) => (((d.deletedAt == null) && (d.removedAt == null)))).length > 0);
    (__doc as any).hasStaffAssigned = ((((__doc as any).assignments) ?? []).filter((a: Doc<"eventAssignments">) => (((a.deletedAt == null) && (a.status !== "unassigned")))).length > 0);
    (__doc as any).isSetupReady = ((((__doc as any).hasAssignedClient && (__doc as any).hasExpectedHeadcount) && (__doc as any).hasMenuDishes) && (__doc as any).hasStaffAssigned);
    (__doc as any).isReadyForExecution = ((((((__doc as any).prepTasks) ?? []).filter((t: Doc<"prepTasks">) => (((t.status !== "completed") && (t.status !== "cancelled")))).length === 0) && ((((__doc as any).packLists) ?? []).filter((p: Doc<"packLists">) => ((((p.status !== "dispatched") && (p.status !== "cancelled")) && (p.status !== "draft")))).length === 0)) && ((((__doc as any).deliveries) ?? []).filter((d: Doc<"deliveries">) => ((((d.status !== "delivered") && (d.status !== "cancelled")) && (d.status !== "failed")))).length === 0));
    (__doc as any).estimatedFoodCost = (((((__doc as any).eventDishes) ?? []).filter((item: Doc<"eventDishes">) => (((item.deletedAt == null) && (item.addedAt != null))))) ?? []).map((item: Record<string, any>) => (item.estimatedCost)).reduce((acc: number, v: unknown) => acc + (typeof v === "number" ? v : 0), 0);
    const __hydrated = { ...(__doc as any), isTerminal: (__doc as any).isTerminal, isEditable: (__doc as any).isEditable, hasAssignedClient: (__doc as any).hasAssignedClient, hasExpectedHeadcount: (__doc as any).hasExpectedHeadcount, hasMenuDishes: (__doc as any).hasMenuDishes, hasStaffAssigned: (__doc as any).hasStaffAssigned, isSetupReady: (__doc as any).isSetupReady, isReadyForExecution: (__doc as any).isReadyForExecution, estimatedFoodCost: (__doc as any).estimatedFoodCost };
    return __hydrated;
  },
});

export const listEventByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("eventRead", "Event", () => (checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("events").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "Event", ["primaryContactName","primaryContactEmail","primaryContactPhone"], row)));
    const __projectedRows: any[] = [];
    for (const __row of __plainRows as any[]) {
      {
      const __fk = ((__row as any) as any).clientId;
      ((__row as any) as any).client = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      (__row as any).eventDishes = await ctx.db.query("eventDishes").withIndex("by_eventId", (q: any) => q.eq("eventId", __row._id)).collect();
      for (const __agg0 of ((__row as any).eventDishes ?? []) as any[]) {
      {
      const __fk = (__agg0 as any).eventId;
      (__agg0 as any).event = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      {
      const __fk = (__agg0 as any).dishId;
      (__agg0 as any).dish = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      if ((__agg0 as any).dish) {
      (__agg0 as any).dish.componentLines = await ctx.db.query("dishComponents").withIndex("by_dishId", (q: any) => q.eq("dishId", (__agg0 as any).dish._id)).collect();
      for (const __agg1 of ((__agg0 as any).dish.componentLines ?? []) as any[]) {
      {
      const __fk = (__agg1 as any).eventId;
      (__agg1 as any).event = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      {
      const __fk = (__agg1 as any).componentId;
      (__agg1 as any).component = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      if ((__agg1 as any).component) {
      (__agg1 as any).component.ingredientLines = await ctx.db.query("componentIngredients").withIndex("by_componentId", (q: any) => q.eq("componentId", (__agg1 as any).component._id)).collect();
      for (const __agg2 of ((__agg1 as any).component.ingredientLines ?? []) as any[]) {
      {
      const __fk = (__agg2 as any).ingredientId;
      (__agg2 as any).ingredient = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      }
      }
      }
      (__agg0 as any).dish.ingredientLines = await ctx.db.query("dishIngredients").withIndex("by_dishId", (q: any) => q.eq("dishId", (__agg0 as any).dish._id)).collect();
      for (const __agg3 of ((__agg0 as any).dish.ingredientLines ?? []) as any[]) {
      {
      const __fk = (__agg3 as any).ingredientId;
      (__agg3 as any).ingredient = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      }
      }
      __agg0.targetHeadcount = (((__agg0.headcountOverride != null) && (__agg0.headcountOverride > 0)) ? __agg0.headcountOverride : __agg0.event.expectedHeadcount);
      __agg0.estimatedCost = (((((__agg0.dish.componentLines) ?? []).filter((line: Record<string, any>) => (((line.deletedAt == null) && (line.attachedAt != null))))) ?? []).map((line: Record<string, any>) => (((((line.yieldQuantity > 0) ? Math.ceil((__agg0.targetHeadcount / line.yieldQuantity)) : 0) * line.batchMultiplier) * ((((line.component.ingredientLines) ?? []).filter((il: Record<string, any>) => (((il.deletedAt == null) && (il.addedAt != null))))) ?? []).map((il: Record<string, any>) => (((il.unit === il.ingredient.unit) ? ((il.quantity * ((il.wasteFactor != null) ? il.wasteFactor : 1)) * il.ingredient.costPerUnit) : 0))).reduce((acc: number, v: unknown) => acc + (typeof v === "number" ? v : 0), 0)))).reduce((acc: number, v: unknown) => acc + (typeof v === "number" ? v : 0), 0) + (__agg0.targetHeadcount * ((((__agg0.dish.ingredientLines) ?? []).filter((dl: Record<string, any>) => (((dl.deletedAt == null) && (dl.addedAt != null))))) ?? []).map((dl: Record<string, any>) => (((dl.unit === dl.ingredient.unit) ? ((dl.quantity * ((dl.wasteFactor != null) ? dl.wasteFactor : 1)) * dl.ingredient.costPerUnit) : 0))).reduce((acc: number, v: unknown) => acc + (typeof v === "number" ? v : 0), 0)));
      }
      (__row as any).assignments = await ctx.db.query("eventAssignments").withIndex("by_eventId", (q: any) => q.eq("eventId", __row._id)).collect();
      (__row as any).prepTasks = await ctx.db.query("prepTasks").withIndex("by_eventId", (q: any) => q.eq("eventId", __row._id)).collect();
      (__row as any).packLists = await ctx.db.query("packLists").withIndex("by_eventId", (q: any) => q.eq("eventId", __row._id)).collect();
      (__row as any).deliveries = await ctx.db.query("deliveries").withIndex("by_eventId", (q: any) => q.eq("eventId", __row._id)).collect();
      (__row as any).isTerminal = (((__row as any).stage === "cancelled") || ((__row as any).stage === "closed_out"));
      (__row as any).isEditable = ((((__row as any).stage === "planning") || ((__row as any).stage === "pending_approval")) || ((__row as any).stage === "approved"));
      (__row as any).hasAssignedClient = (((__row as any).client != null) && ((__row as any).client.deletedAt == null));
      (__row as any).hasExpectedHeadcount = ((__row as any).expectedHeadcount > 0);
      (__row as any).hasMenuDishes = ((((__row as any).eventDishes) ?? []).filter((d: Doc<"eventDishes">) => (((d.deletedAt == null) && (d.removedAt == null)))).length > 0);
      (__row as any).hasStaffAssigned = ((((__row as any).assignments) ?? []).filter((a: Doc<"eventAssignments">) => (((a.deletedAt == null) && (a.status !== "unassigned")))).length > 0);
      (__row as any).isSetupReady = ((((__row as any).hasAssignedClient && (__row as any).hasExpectedHeadcount) && (__row as any).hasMenuDishes) && (__row as any).hasStaffAssigned);
      (__row as any).isReadyForExecution = ((((((__row as any).prepTasks) ?? []).filter((t: Doc<"prepTasks">) => (((t.status !== "completed") && (t.status !== "cancelled")))).length === 0) && ((((__row as any).packLists) ?? []).filter((p: Doc<"packLists">) => ((((p.status !== "dispatched") && (p.status !== "cancelled")) && (p.status !== "draft")))).length === 0)) && ((((__row as any).deliveries) ?? []).filter((d: Doc<"deliveries">) => ((((d.status !== "delivered") && (d.status !== "cancelled")) && (d.status !== "failed")))).length === 0));
      (__row as any).estimatedFoodCost = (((((__row as any).eventDishes) ?? []).filter((item: Doc<"eventDishes">) => (((item.deletedAt == null) && (item.addedAt != null))))) ?? []).map((item: Record<string, any>) => (item.estimatedCost)).reduce((acc: number, v: unknown) => acc + (typeof v === "number" ? v : 0), 0);
      __projectedRows.push({ ...(__row as any), isTerminal: (__row as any).isTerminal, isEditable: (__row as any).isEditable, hasAssignedClient: (__row as any).hasAssignedClient, hasExpectedHeadcount: (__row as any).hasExpectedHeadcount, hasMenuDishes: (__row as any).hasMenuDishes, hasStaffAssigned: (__row as any).hasStaffAssigned, isSetupReady: (__row as any).isSetupReady, isReadyForExecution: (__row as any).isReadyForExecution, estimatedFoodCost: (__row as any).estimatedFoodCost });
    }
    return __projectedRows;
  },
});

export const listEventByClientId = query({
  args: { clientId: v.id("clients") },
  handler: async (ctx, { clientId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("eventRead", "Event", () => (checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("events").withIndex("by_clientId", (q) => q.eq("clientId", clientId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "Event", ["primaryContactName","primaryContactEmail","primaryContactPhone"], row)));
    const __projectedRows: any[] = [];
    for (const __row of __plainRows as any[]) {
      {
      const __fk = ((__row as any) as any).clientId;
      ((__row as any) as any).client = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      (__row as any).eventDishes = await ctx.db.query("eventDishes").withIndex("by_eventId", (q: any) => q.eq("eventId", __row._id)).collect();
      for (const __agg0 of ((__row as any).eventDishes ?? []) as any[]) {
      {
      const __fk = (__agg0 as any).eventId;
      (__agg0 as any).event = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      {
      const __fk = (__agg0 as any).dishId;
      (__agg0 as any).dish = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      if ((__agg0 as any).dish) {
      (__agg0 as any).dish.componentLines = await ctx.db.query("dishComponents").withIndex("by_dishId", (q: any) => q.eq("dishId", (__agg0 as any).dish._id)).collect();
      for (const __agg1 of ((__agg0 as any).dish.componentLines ?? []) as any[]) {
      {
      const __fk = (__agg1 as any).eventId;
      (__agg1 as any).event = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      {
      const __fk = (__agg1 as any).componentId;
      (__agg1 as any).component = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      if ((__agg1 as any).component) {
      (__agg1 as any).component.ingredientLines = await ctx.db.query("componentIngredients").withIndex("by_componentId", (q: any) => q.eq("componentId", (__agg1 as any).component._id)).collect();
      for (const __agg2 of ((__agg1 as any).component.ingredientLines ?? []) as any[]) {
      {
      const __fk = (__agg2 as any).ingredientId;
      (__agg2 as any).ingredient = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      }
      }
      }
      (__agg0 as any).dish.ingredientLines = await ctx.db.query("dishIngredients").withIndex("by_dishId", (q: any) => q.eq("dishId", (__agg0 as any).dish._id)).collect();
      for (const __agg3 of ((__agg0 as any).dish.ingredientLines ?? []) as any[]) {
      {
      const __fk = (__agg3 as any).ingredientId;
      (__agg3 as any).ingredient = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      }
      }
      __agg0.targetHeadcount = (((__agg0.headcountOverride != null) && (__agg0.headcountOverride > 0)) ? __agg0.headcountOverride : __agg0.event.expectedHeadcount);
      __agg0.estimatedCost = (((((__agg0.dish.componentLines) ?? []).filter((line: Record<string, any>) => (((line.deletedAt == null) && (line.attachedAt != null))))) ?? []).map((line: Record<string, any>) => (((((line.yieldQuantity > 0) ? Math.ceil((__agg0.targetHeadcount / line.yieldQuantity)) : 0) * line.batchMultiplier) * ((((line.component.ingredientLines) ?? []).filter((il: Record<string, any>) => (((il.deletedAt == null) && (il.addedAt != null))))) ?? []).map((il: Record<string, any>) => (((il.unit === il.ingredient.unit) ? ((il.quantity * ((il.wasteFactor != null) ? il.wasteFactor : 1)) * il.ingredient.costPerUnit) : 0))).reduce((acc: number, v: unknown) => acc + (typeof v === "number" ? v : 0), 0)))).reduce((acc: number, v: unknown) => acc + (typeof v === "number" ? v : 0), 0) + (__agg0.targetHeadcount * ((((__agg0.dish.ingredientLines) ?? []).filter((dl: Record<string, any>) => (((dl.deletedAt == null) && (dl.addedAt != null))))) ?? []).map((dl: Record<string, any>) => (((dl.unit === dl.ingredient.unit) ? ((dl.quantity * ((dl.wasteFactor != null) ? dl.wasteFactor : 1)) * dl.ingredient.costPerUnit) : 0))).reduce((acc: number, v: unknown) => acc + (typeof v === "number" ? v : 0), 0)));
      }
      (__row as any).assignments = await ctx.db.query("eventAssignments").withIndex("by_eventId", (q: any) => q.eq("eventId", __row._id)).collect();
      (__row as any).prepTasks = await ctx.db.query("prepTasks").withIndex("by_eventId", (q: any) => q.eq("eventId", __row._id)).collect();
      (__row as any).packLists = await ctx.db.query("packLists").withIndex("by_eventId", (q: any) => q.eq("eventId", __row._id)).collect();
      (__row as any).deliveries = await ctx.db.query("deliveries").withIndex("by_eventId", (q: any) => q.eq("eventId", __row._id)).collect();
      (__row as any).isTerminal = (((__row as any).stage === "cancelled") || ((__row as any).stage === "closed_out"));
      (__row as any).isEditable = ((((__row as any).stage === "planning") || ((__row as any).stage === "pending_approval")) || ((__row as any).stage === "approved"));
      (__row as any).hasAssignedClient = (((__row as any).client != null) && ((__row as any).client.deletedAt == null));
      (__row as any).hasExpectedHeadcount = ((__row as any).expectedHeadcount > 0);
      (__row as any).hasMenuDishes = ((((__row as any).eventDishes) ?? []).filter((d: Doc<"eventDishes">) => (((d.deletedAt == null) && (d.removedAt == null)))).length > 0);
      (__row as any).hasStaffAssigned = ((((__row as any).assignments) ?? []).filter((a: Doc<"eventAssignments">) => (((a.deletedAt == null) && (a.status !== "unassigned")))).length > 0);
      (__row as any).isSetupReady = ((((__row as any).hasAssignedClient && (__row as any).hasExpectedHeadcount) && (__row as any).hasMenuDishes) && (__row as any).hasStaffAssigned);
      (__row as any).isReadyForExecution = ((((((__row as any).prepTasks) ?? []).filter((t: Doc<"prepTasks">) => (((t.status !== "completed") && (t.status !== "cancelled")))).length === 0) && ((((__row as any).packLists) ?? []).filter((p: Doc<"packLists">) => ((((p.status !== "dispatched") && (p.status !== "cancelled")) && (p.status !== "draft")))).length === 0)) && ((((__row as any).deliveries) ?? []).filter((d: Doc<"deliveries">) => ((((d.status !== "delivered") && (d.status !== "cancelled")) && (d.status !== "failed")))).length === 0));
      (__row as any).estimatedFoodCost = (((((__row as any).eventDishes) ?? []).filter((item: Doc<"eventDishes">) => (((item.deletedAt == null) && (item.addedAt != null))))) ?? []).map((item: Record<string, any>) => (item.estimatedCost)).reduce((acc: number, v: unknown) => acc + (typeof v === "number" ? v : 0), 0);
      __projectedRows.push({ ...(__row as any), isTerminal: (__row as any).isTerminal, isEditable: (__row as any).isEditable, hasAssignedClient: (__row as any).hasAssignedClient, hasExpectedHeadcount: (__row as any).hasExpectedHeadcount, hasMenuDishes: (__row as any).hasMenuDishes, hasStaffAssigned: (__row as any).hasStaffAssigned, isSetupReady: (__row as any).isSetupReady, isReadyForExecution: (__row as any).isReadyForExecution, estimatedFoodCost: (__row as any).estimatedFoodCost });
    }
    return __projectedRows;
  },
});

export const listEventByClientMergeAuthorizationId = query({
  args: { clientMergeAuthorizationId: v.id("clientMerges") },
  handler: async (ctx, { clientMergeAuthorizationId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("eventRead", "Event", () => (checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("events").withIndex("by_clientMergeAuthorizationId", (q) => q.eq("clientMergeAuthorizationId", clientMergeAuthorizationId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "Event", ["primaryContactName","primaryContactEmail","primaryContactPhone"], row)));
    const __projectedRows: any[] = [];
    for (const __row of __plainRows as any[]) {
      {
      const __fk = ((__row as any) as any).clientId;
      ((__row as any) as any).client = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      (__row as any).eventDishes = await ctx.db.query("eventDishes").withIndex("by_eventId", (q: any) => q.eq("eventId", __row._id)).collect();
      for (const __agg0 of ((__row as any).eventDishes ?? []) as any[]) {
      {
      const __fk = (__agg0 as any).eventId;
      (__agg0 as any).event = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      {
      const __fk = (__agg0 as any).dishId;
      (__agg0 as any).dish = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      if ((__agg0 as any).dish) {
      (__agg0 as any).dish.componentLines = await ctx.db.query("dishComponents").withIndex("by_dishId", (q: any) => q.eq("dishId", (__agg0 as any).dish._id)).collect();
      for (const __agg1 of ((__agg0 as any).dish.componentLines ?? []) as any[]) {
      {
      const __fk = (__agg1 as any).eventId;
      (__agg1 as any).event = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      {
      const __fk = (__agg1 as any).componentId;
      (__agg1 as any).component = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      if ((__agg1 as any).component) {
      (__agg1 as any).component.ingredientLines = await ctx.db.query("componentIngredients").withIndex("by_componentId", (q: any) => q.eq("componentId", (__agg1 as any).component._id)).collect();
      for (const __agg2 of ((__agg1 as any).component.ingredientLines ?? []) as any[]) {
      {
      const __fk = (__agg2 as any).ingredientId;
      (__agg2 as any).ingredient = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      }
      }
      }
      (__agg0 as any).dish.ingredientLines = await ctx.db.query("dishIngredients").withIndex("by_dishId", (q: any) => q.eq("dishId", (__agg0 as any).dish._id)).collect();
      for (const __agg3 of ((__agg0 as any).dish.ingredientLines ?? []) as any[]) {
      {
      const __fk = (__agg3 as any).ingredientId;
      (__agg3 as any).ingredient = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      }
      }
      __agg0.targetHeadcount = (((__agg0.headcountOverride != null) && (__agg0.headcountOverride > 0)) ? __agg0.headcountOverride : __agg0.event.expectedHeadcount);
      __agg0.estimatedCost = (((((__agg0.dish.componentLines) ?? []).filter((line: Record<string, any>) => (((line.deletedAt == null) && (line.attachedAt != null))))) ?? []).map((line: Record<string, any>) => (((((line.yieldQuantity > 0) ? Math.ceil((__agg0.targetHeadcount / line.yieldQuantity)) : 0) * line.batchMultiplier) * ((((line.component.ingredientLines) ?? []).filter((il: Record<string, any>) => (((il.deletedAt == null) && (il.addedAt != null))))) ?? []).map((il: Record<string, any>) => (((il.unit === il.ingredient.unit) ? ((il.quantity * ((il.wasteFactor != null) ? il.wasteFactor : 1)) * il.ingredient.costPerUnit) : 0))).reduce((acc: number, v: unknown) => acc + (typeof v === "number" ? v : 0), 0)))).reduce((acc: number, v: unknown) => acc + (typeof v === "number" ? v : 0), 0) + (__agg0.targetHeadcount * ((((__agg0.dish.ingredientLines) ?? []).filter((dl: Record<string, any>) => (((dl.deletedAt == null) && (dl.addedAt != null))))) ?? []).map((dl: Record<string, any>) => (((dl.unit === dl.ingredient.unit) ? ((dl.quantity * ((dl.wasteFactor != null) ? dl.wasteFactor : 1)) * dl.ingredient.costPerUnit) : 0))).reduce((acc: number, v: unknown) => acc + (typeof v === "number" ? v : 0), 0)));
      }
      (__row as any).assignments = await ctx.db.query("eventAssignments").withIndex("by_eventId", (q: any) => q.eq("eventId", __row._id)).collect();
      (__row as any).prepTasks = await ctx.db.query("prepTasks").withIndex("by_eventId", (q: any) => q.eq("eventId", __row._id)).collect();
      (__row as any).packLists = await ctx.db.query("packLists").withIndex("by_eventId", (q: any) => q.eq("eventId", __row._id)).collect();
      (__row as any).deliveries = await ctx.db.query("deliveries").withIndex("by_eventId", (q: any) => q.eq("eventId", __row._id)).collect();
      (__row as any).isTerminal = (((__row as any).stage === "cancelled") || ((__row as any).stage === "closed_out"));
      (__row as any).isEditable = ((((__row as any).stage === "planning") || ((__row as any).stage === "pending_approval")) || ((__row as any).stage === "approved"));
      (__row as any).hasAssignedClient = (((__row as any).client != null) && ((__row as any).client.deletedAt == null));
      (__row as any).hasExpectedHeadcount = ((__row as any).expectedHeadcount > 0);
      (__row as any).hasMenuDishes = ((((__row as any).eventDishes) ?? []).filter((d: Doc<"eventDishes">) => (((d.deletedAt == null) && (d.removedAt == null)))).length > 0);
      (__row as any).hasStaffAssigned = ((((__row as any).assignments) ?? []).filter((a: Doc<"eventAssignments">) => (((a.deletedAt == null) && (a.status !== "unassigned")))).length > 0);
      (__row as any).isSetupReady = ((((__row as any).hasAssignedClient && (__row as any).hasExpectedHeadcount) && (__row as any).hasMenuDishes) && (__row as any).hasStaffAssigned);
      (__row as any).isReadyForExecution = ((((((__row as any).prepTasks) ?? []).filter((t: Doc<"prepTasks">) => (((t.status !== "completed") && (t.status !== "cancelled")))).length === 0) && ((((__row as any).packLists) ?? []).filter((p: Doc<"packLists">) => ((((p.status !== "dispatched") && (p.status !== "cancelled")) && (p.status !== "draft")))).length === 0)) && ((((__row as any).deliveries) ?? []).filter((d: Doc<"deliveries">) => ((((d.status !== "delivered") && (d.status !== "cancelled")) && (d.status !== "failed")))).length === 0));
      (__row as any).estimatedFoodCost = (((((__row as any).eventDishes) ?? []).filter((item: Doc<"eventDishes">) => (((item.deletedAt == null) && (item.addedAt != null))))) ?? []).map((item: Record<string, any>) => (item.estimatedCost)).reduce((acc: number, v: unknown) => acc + (typeof v === "number" ? v : 0), 0);
      __projectedRows.push({ ...(__row as any), isTerminal: (__row as any).isTerminal, isEditable: (__row as any).isEditable, hasAssignedClient: (__row as any).hasAssignedClient, hasExpectedHeadcount: (__row as any).hasExpectedHeadcount, hasMenuDishes: (__row as any).hasMenuDishes, hasStaffAssigned: (__row as any).hasStaffAssigned, isSetupReady: (__row as any).isSetupReady, isReadyForExecution: (__row as any).isReadyForExecution, estimatedFoodCost: (__row as any).estimatedFoodCost });
    }
    return __projectedRows;
  },
});

export const listEventByMergeTargetClientId = query({
  args: { mergeTargetClientId: v.id("clients") },
  handler: async (ctx, { mergeTargetClientId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("eventRead", "Event", () => (checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("events").withIndex("by_mergeTargetClientId", (q) => q.eq("mergeTargetClientId", mergeTargetClientId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "Event", ["primaryContactName","primaryContactEmail","primaryContactPhone"], row)));
    const __projectedRows: any[] = [];
    for (const __row of __plainRows as any[]) {
      {
      const __fk = ((__row as any) as any).clientId;
      ((__row as any) as any).client = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      (__row as any).eventDishes = await ctx.db.query("eventDishes").withIndex("by_eventId", (q: any) => q.eq("eventId", __row._id)).collect();
      for (const __agg0 of ((__row as any).eventDishes ?? []) as any[]) {
      {
      const __fk = (__agg0 as any).eventId;
      (__agg0 as any).event = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      {
      const __fk = (__agg0 as any).dishId;
      (__agg0 as any).dish = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      if ((__agg0 as any).dish) {
      (__agg0 as any).dish.componentLines = await ctx.db.query("dishComponents").withIndex("by_dishId", (q: any) => q.eq("dishId", (__agg0 as any).dish._id)).collect();
      for (const __agg1 of ((__agg0 as any).dish.componentLines ?? []) as any[]) {
      {
      const __fk = (__agg1 as any).eventId;
      (__agg1 as any).event = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      {
      const __fk = (__agg1 as any).componentId;
      (__agg1 as any).component = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      if ((__agg1 as any).component) {
      (__agg1 as any).component.ingredientLines = await ctx.db.query("componentIngredients").withIndex("by_componentId", (q: any) => q.eq("componentId", (__agg1 as any).component._id)).collect();
      for (const __agg2 of ((__agg1 as any).component.ingredientLines ?? []) as any[]) {
      {
      const __fk = (__agg2 as any).ingredientId;
      (__agg2 as any).ingredient = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      }
      }
      }
      (__agg0 as any).dish.ingredientLines = await ctx.db.query("dishIngredients").withIndex("by_dishId", (q: any) => q.eq("dishId", (__agg0 as any).dish._id)).collect();
      for (const __agg3 of ((__agg0 as any).dish.ingredientLines ?? []) as any[]) {
      {
      const __fk = (__agg3 as any).ingredientId;
      (__agg3 as any).ingredient = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      }
      }
      __agg0.targetHeadcount = (((__agg0.headcountOverride != null) && (__agg0.headcountOverride > 0)) ? __agg0.headcountOverride : __agg0.event.expectedHeadcount);
      __agg0.estimatedCost = (((((__agg0.dish.componentLines) ?? []).filter((line: Record<string, any>) => (((line.deletedAt == null) && (line.attachedAt != null))))) ?? []).map((line: Record<string, any>) => (((((line.yieldQuantity > 0) ? Math.ceil((__agg0.targetHeadcount / line.yieldQuantity)) : 0) * line.batchMultiplier) * ((((line.component.ingredientLines) ?? []).filter((il: Record<string, any>) => (((il.deletedAt == null) && (il.addedAt != null))))) ?? []).map((il: Record<string, any>) => (((il.unit === il.ingredient.unit) ? ((il.quantity * ((il.wasteFactor != null) ? il.wasteFactor : 1)) * il.ingredient.costPerUnit) : 0))).reduce((acc: number, v: unknown) => acc + (typeof v === "number" ? v : 0), 0)))).reduce((acc: number, v: unknown) => acc + (typeof v === "number" ? v : 0), 0) + (__agg0.targetHeadcount * ((((__agg0.dish.ingredientLines) ?? []).filter((dl: Record<string, any>) => (((dl.deletedAt == null) && (dl.addedAt != null))))) ?? []).map((dl: Record<string, any>) => (((dl.unit === dl.ingredient.unit) ? ((dl.quantity * ((dl.wasteFactor != null) ? dl.wasteFactor : 1)) * dl.ingredient.costPerUnit) : 0))).reduce((acc: number, v: unknown) => acc + (typeof v === "number" ? v : 0), 0)));
      }
      (__row as any).assignments = await ctx.db.query("eventAssignments").withIndex("by_eventId", (q: any) => q.eq("eventId", __row._id)).collect();
      (__row as any).prepTasks = await ctx.db.query("prepTasks").withIndex("by_eventId", (q: any) => q.eq("eventId", __row._id)).collect();
      (__row as any).packLists = await ctx.db.query("packLists").withIndex("by_eventId", (q: any) => q.eq("eventId", __row._id)).collect();
      (__row as any).deliveries = await ctx.db.query("deliveries").withIndex("by_eventId", (q: any) => q.eq("eventId", __row._id)).collect();
      (__row as any).isTerminal = (((__row as any).stage === "cancelled") || ((__row as any).stage === "closed_out"));
      (__row as any).isEditable = ((((__row as any).stage === "planning") || ((__row as any).stage === "pending_approval")) || ((__row as any).stage === "approved"));
      (__row as any).hasAssignedClient = (((__row as any).client != null) && ((__row as any).client.deletedAt == null));
      (__row as any).hasExpectedHeadcount = ((__row as any).expectedHeadcount > 0);
      (__row as any).hasMenuDishes = ((((__row as any).eventDishes) ?? []).filter((d: Doc<"eventDishes">) => (((d.deletedAt == null) && (d.removedAt == null)))).length > 0);
      (__row as any).hasStaffAssigned = ((((__row as any).assignments) ?? []).filter((a: Doc<"eventAssignments">) => (((a.deletedAt == null) && (a.status !== "unassigned")))).length > 0);
      (__row as any).isSetupReady = ((((__row as any).hasAssignedClient && (__row as any).hasExpectedHeadcount) && (__row as any).hasMenuDishes) && (__row as any).hasStaffAssigned);
      (__row as any).isReadyForExecution = ((((((__row as any).prepTasks) ?? []).filter((t: Doc<"prepTasks">) => (((t.status !== "completed") && (t.status !== "cancelled")))).length === 0) && ((((__row as any).packLists) ?? []).filter((p: Doc<"packLists">) => ((((p.status !== "dispatched") && (p.status !== "cancelled")) && (p.status !== "draft")))).length === 0)) && ((((__row as any).deliveries) ?? []).filter((d: Doc<"deliveries">) => ((((d.status !== "delivered") && (d.status !== "cancelled")) && (d.status !== "failed")))).length === 0));
      (__row as any).estimatedFoodCost = (((((__row as any).eventDishes) ?? []).filter((item: Doc<"eventDishes">) => (((item.deletedAt == null) && (item.addedAt != null))))) ?? []).map((item: Record<string, any>) => (item.estimatedCost)).reduce((acc: number, v: unknown) => acc + (typeof v === "number" ? v : 0), 0);
      __projectedRows.push({ ...(__row as any), isTerminal: (__row as any).isTerminal, isEditable: (__row as any).isEditable, hasAssignedClient: (__row as any).hasAssignedClient, hasExpectedHeadcount: (__row as any).hasExpectedHeadcount, hasMenuDishes: (__row as any).hasMenuDishes, hasStaffAssigned: (__row as any).hasStaffAssigned, isSetupReady: (__row as any).isSetupReady, isReadyForExecution: (__row as any).isReadyForExecution, estimatedFoodCost: (__row as any).estimatedFoodCost });
    }
    return __projectedRows;
  },
});

export const listEventByServiceStyleId = query({
  args: { serviceStyleId: v.id("serviceStyles") },
  handler: async (ctx, { serviceStyleId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("eventRead", "Event", () => (checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("events").withIndex("by_serviceStyleId", (q) => q.eq("serviceStyleId", serviceStyleId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "Event", ["primaryContactName","primaryContactEmail","primaryContactPhone"], row)));
    const __projectedRows: any[] = [];
    for (const __row of __plainRows as any[]) {
      {
      const __fk = ((__row as any) as any).clientId;
      ((__row as any) as any).client = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      (__row as any).eventDishes = await ctx.db.query("eventDishes").withIndex("by_eventId", (q: any) => q.eq("eventId", __row._id)).collect();
      for (const __agg0 of ((__row as any).eventDishes ?? []) as any[]) {
      {
      const __fk = (__agg0 as any).eventId;
      (__agg0 as any).event = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      {
      const __fk = (__agg0 as any).dishId;
      (__agg0 as any).dish = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      if ((__agg0 as any).dish) {
      (__agg0 as any).dish.componentLines = await ctx.db.query("dishComponents").withIndex("by_dishId", (q: any) => q.eq("dishId", (__agg0 as any).dish._id)).collect();
      for (const __agg1 of ((__agg0 as any).dish.componentLines ?? []) as any[]) {
      {
      const __fk = (__agg1 as any).eventId;
      (__agg1 as any).event = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      {
      const __fk = (__agg1 as any).componentId;
      (__agg1 as any).component = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      if ((__agg1 as any).component) {
      (__agg1 as any).component.ingredientLines = await ctx.db.query("componentIngredients").withIndex("by_componentId", (q: any) => q.eq("componentId", (__agg1 as any).component._id)).collect();
      for (const __agg2 of ((__agg1 as any).component.ingredientLines ?? []) as any[]) {
      {
      const __fk = (__agg2 as any).ingredientId;
      (__agg2 as any).ingredient = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      }
      }
      }
      (__agg0 as any).dish.ingredientLines = await ctx.db.query("dishIngredients").withIndex("by_dishId", (q: any) => q.eq("dishId", (__agg0 as any).dish._id)).collect();
      for (const __agg3 of ((__agg0 as any).dish.ingredientLines ?? []) as any[]) {
      {
      const __fk = (__agg3 as any).ingredientId;
      (__agg3 as any).ingredient = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      }
      }
      __agg0.targetHeadcount = (((__agg0.headcountOverride != null) && (__agg0.headcountOverride > 0)) ? __agg0.headcountOverride : __agg0.event.expectedHeadcount);
      __agg0.estimatedCost = (((((__agg0.dish.componentLines) ?? []).filter((line: Record<string, any>) => (((line.deletedAt == null) && (line.attachedAt != null))))) ?? []).map((line: Record<string, any>) => (((((line.yieldQuantity > 0) ? Math.ceil((__agg0.targetHeadcount / line.yieldQuantity)) : 0) * line.batchMultiplier) * ((((line.component.ingredientLines) ?? []).filter((il: Record<string, any>) => (((il.deletedAt == null) && (il.addedAt != null))))) ?? []).map((il: Record<string, any>) => (((il.unit === il.ingredient.unit) ? ((il.quantity * ((il.wasteFactor != null) ? il.wasteFactor : 1)) * il.ingredient.costPerUnit) : 0))).reduce((acc: number, v: unknown) => acc + (typeof v === "number" ? v : 0), 0)))).reduce((acc: number, v: unknown) => acc + (typeof v === "number" ? v : 0), 0) + (__agg0.targetHeadcount * ((((__agg0.dish.ingredientLines) ?? []).filter((dl: Record<string, any>) => (((dl.deletedAt == null) && (dl.addedAt != null))))) ?? []).map((dl: Record<string, any>) => (((dl.unit === dl.ingredient.unit) ? ((dl.quantity * ((dl.wasteFactor != null) ? dl.wasteFactor : 1)) * dl.ingredient.costPerUnit) : 0))).reduce((acc: number, v: unknown) => acc + (typeof v === "number" ? v : 0), 0)));
      }
      (__row as any).assignments = await ctx.db.query("eventAssignments").withIndex("by_eventId", (q: any) => q.eq("eventId", __row._id)).collect();
      (__row as any).prepTasks = await ctx.db.query("prepTasks").withIndex("by_eventId", (q: any) => q.eq("eventId", __row._id)).collect();
      (__row as any).packLists = await ctx.db.query("packLists").withIndex("by_eventId", (q: any) => q.eq("eventId", __row._id)).collect();
      (__row as any).deliveries = await ctx.db.query("deliveries").withIndex("by_eventId", (q: any) => q.eq("eventId", __row._id)).collect();
      (__row as any).isTerminal = (((__row as any).stage === "cancelled") || ((__row as any).stage === "closed_out"));
      (__row as any).isEditable = ((((__row as any).stage === "planning") || ((__row as any).stage === "pending_approval")) || ((__row as any).stage === "approved"));
      (__row as any).hasAssignedClient = (((__row as any).client != null) && ((__row as any).client.deletedAt == null));
      (__row as any).hasExpectedHeadcount = ((__row as any).expectedHeadcount > 0);
      (__row as any).hasMenuDishes = ((((__row as any).eventDishes) ?? []).filter((d: Doc<"eventDishes">) => (((d.deletedAt == null) && (d.removedAt == null)))).length > 0);
      (__row as any).hasStaffAssigned = ((((__row as any).assignments) ?? []).filter((a: Doc<"eventAssignments">) => (((a.deletedAt == null) && (a.status !== "unassigned")))).length > 0);
      (__row as any).isSetupReady = ((((__row as any).hasAssignedClient && (__row as any).hasExpectedHeadcount) && (__row as any).hasMenuDishes) && (__row as any).hasStaffAssigned);
      (__row as any).isReadyForExecution = ((((((__row as any).prepTasks) ?? []).filter((t: Doc<"prepTasks">) => (((t.status !== "completed") && (t.status !== "cancelled")))).length === 0) && ((((__row as any).packLists) ?? []).filter((p: Doc<"packLists">) => ((((p.status !== "dispatched") && (p.status !== "cancelled")) && (p.status !== "draft")))).length === 0)) && ((((__row as any).deliveries) ?? []).filter((d: Doc<"deliveries">) => ((((d.status !== "delivered") && (d.status !== "cancelled")) && (d.status !== "failed")))).length === 0));
      (__row as any).estimatedFoodCost = (((((__row as any).eventDishes) ?? []).filter((item: Doc<"eventDishes">) => (((item.deletedAt == null) && (item.addedAt != null))))) ?? []).map((item: Record<string, any>) => (item.estimatedCost)).reduce((acc: number, v: unknown) => acc + (typeof v === "number" ? v : 0), 0);
      __projectedRows.push({ ...(__row as any), isTerminal: (__row as any).isTerminal, isEditable: (__row as any).isEditable, hasAssignedClient: (__row as any).hasAssignedClient, hasExpectedHeadcount: (__row as any).hasExpectedHeadcount, hasMenuDishes: (__row as any).hasMenuDishes, hasStaffAssigned: (__row as any).hasStaffAssigned, isSetupReady: (__row as any).isSetupReady, isReadyForExecution: (__row as any).isReadyForExecution, estimatedFoodCost: (__row as any).estimatedFoodCost });
    }
    return __projectedRows;
  },
});

export const listEventByOccasionId = query({
  args: { occasionId: v.id("occasions") },
  handler: async (ctx, { occasionId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("eventRead", "Event", () => (checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("events").withIndex("by_occasionId", (q) => q.eq("occasionId", occasionId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "Event", ["primaryContactName","primaryContactEmail","primaryContactPhone"], row)));
    const __projectedRows: any[] = [];
    for (const __row of __plainRows as any[]) {
      {
      const __fk = ((__row as any) as any).clientId;
      ((__row as any) as any).client = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      (__row as any).eventDishes = await ctx.db.query("eventDishes").withIndex("by_eventId", (q: any) => q.eq("eventId", __row._id)).collect();
      for (const __agg0 of ((__row as any).eventDishes ?? []) as any[]) {
      {
      const __fk = (__agg0 as any).eventId;
      (__agg0 as any).event = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      {
      const __fk = (__agg0 as any).dishId;
      (__agg0 as any).dish = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      if ((__agg0 as any).dish) {
      (__agg0 as any).dish.componentLines = await ctx.db.query("dishComponents").withIndex("by_dishId", (q: any) => q.eq("dishId", (__agg0 as any).dish._id)).collect();
      for (const __agg1 of ((__agg0 as any).dish.componentLines ?? []) as any[]) {
      {
      const __fk = (__agg1 as any).eventId;
      (__agg1 as any).event = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      {
      const __fk = (__agg1 as any).componentId;
      (__agg1 as any).component = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      if ((__agg1 as any).component) {
      (__agg1 as any).component.ingredientLines = await ctx.db.query("componentIngredients").withIndex("by_componentId", (q: any) => q.eq("componentId", (__agg1 as any).component._id)).collect();
      for (const __agg2 of ((__agg1 as any).component.ingredientLines ?? []) as any[]) {
      {
      const __fk = (__agg2 as any).ingredientId;
      (__agg2 as any).ingredient = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      }
      }
      }
      (__agg0 as any).dish.ingredientLines = await ctx.db.query("dishIngredients").withIndex("by_dishId", (q: any) => q.eq("dishId", (__agg0 as any).dish._id)).collect();
      for (const __agg3 of ((__agg0 as any).dish.ingredientLines ?? []) as any[]) {
      {
      const __fk = (__agg3 as any).ingredientId;
      (__agg3 as any).ingredient = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      }
      }
      __agg0.targetHeadcount = (((__agg0.headcountOverride != null) && (__agg0.headcountOverride > 0)) ? __agg0.headcountOverride : __agg0.event.expectedHeadcount);
      __agg0.estimatedCost = (((((__agg0.dish.componentLines) ?? []).filter((line: Record<string, any>) => (((line.deletedAt == null) && (line.attachedAt != null))))) ?? []).map((line: Record<string, any>) => (((((line.yieldQuantity > 0) ? Math.ceil((__agg0.targetHeadcount / line.yieldQuantity)) : 0) * line.batchMultiplier) * ((((line.component.ingredientLines) ?? []).filter((il: Record<string, any>) => (((il.deletedAt == null) && (il.addedAt != null))))) ?? []).map((il: Record<string, any>) => (((il.unit === il.ingredient.unit) ? ((il.quantity * ((il.wasteFactor != null) ? il.wasteFactor : 1)) * il.ingredient.costPerUnit) : 0))).reduce((acc: number, v: unknown) => acc + (typeof v === "number" ? v : 0), 0)))).reduce((acc: number, v: unknown) => acc + (typeof v === "number" ? v : 0), 0) + (__agg0.targetHeadcount * ((((__agg0.dish.ingredientLines) ?? []).filter((dl: Record<string, any>) => (((dl.deletedAt == null) && (dl.addedAt != null))))) ?? []).map((dl: Record<string, any>) => (((dl.unit === dl.ingredient.unit) ? ((dl.quantity * ((dl.wasteFactor != null) ? dl.wasteFactor : 1)) * dl.ingredient.costPerUnit) : 0))).reduce((acc: number, v: unknown) => acc + (typeof v === "number" ? v : 0), 0)));
      }
      (__row as any).assignments = await ctx.db.query("eventAssignments").withIndex("by_eventId", (q: any) => q.eq("eventId", __row._id)).collect();
      (__row as any).prepTasks = await ctx.db.query("prepTasks").withIndex("by_eventId", (q: any) => q.eq("eventId", __row._id)).collect();
      (__row as any).packLists = await ctx.db.query("packLists").withIndex("by_eventId", (q: any) => q.eq("eventId", __row._id)).collect();
      (__row as any).deliveries = await ctx.db.query("deliveries").withIndex("by_eventId", (q: any) => q.eq("eventId", __row._id)).collect();
      (__row as any).isTerminal = (((__row as any).stage === "cancelled") || ((__row as any).stage === "closed_out"));
      (__row as any).isEditable = ((((__row as any).stage === "planning") || ((__row as any).stage === "pending_approval")) || ((__row as any).stage === "approved"));
      (__row as any).hasAssignedClient = (((__row as any).client != null) && ((__row as any).client.deletedAt == null));
      (__row as any).hasExpectedHeadcount = ((__row as any).expectedHeadcount > 0);
      (__row as any).hasMenuDishes = ((((__row as any).eventDishes) ?? []).filter((d: Doc<"eventDishes">) => (((d.deletedAt == null) && (d.removedAt == null)))).length > 0);
      (__row as any).hasStaffAssigned = ((((__row as any).assignments) ?? []).filter((a: Doc<"eventAssignments">) => (((a.deletedAt == null) && (a.status !== "unassigned")))).length > 0);
      (__row as any).isSetupReady = ((((__row as any).hasAssignedClient && (__row as any).hasExpectedHeadcount) && (__row as any).hasMenuDishes) && (__row as any).hasStaffAssigned);
      (__row as any).isReadyForExecution = ((((((__row as any).prepTasks) ?? []).filter((t: Doc<"prepTasks">) => (((t.status !== "completed") && (t.status !== "cancelled")))).length === 0) && ((((__row as any).packLists) ?? []).filter((p: Doc<"packLists">) => ((((p.status !== "dispatched") && (p.status !== "cancelled")) && (p.status !== "draft")))).length === 0)) && ((((__row as any).deliveries) ?? []).filter((d: Doc<"deliveries">) => ((((d.status !== "delivered") && (d.status !== "cancelled")) && (d.status !== "failed")))).length === 0));
      (__row as any).estimatedFoodCost = (((((__row as any).eventDishes) ?? []).filter((item: Doc<"eventDishes">) => (((item.deletedAt == null) && (item.addedAt != null))))) ?? []).map((item: Record<string, any>) => (item.estimatedCost)).reduce((acc: number, v: unknown) => acc + (typeof v === "number" ? v : 0), 0);
      __projectedRows.push({ ...(__row as any), isTerminal: (__row as any).isTerminal, isEditable: (__row as any).isEditable, hasAssignedClient: (__row as any).hasAssignedClient, hasExpectedHeadcount: (__row as any).hasExpectedHeadcount, hasMenuDishes: (__row as any).hasMenuDishes, hasStaffAssigned: (__row as any).hasStaffAssigned, isSetupReady: (__row as any).isSetupReady, isReadyForExecution: (__row as any).isReadyForExecution, estimatedFoodCost: (__row as any).estimatedFoodCost });
    }
    return __projectedRows;
  },
});

export const listEventByVenueId = query({
  args: { venueId: v.id("venues") },
  handler: async (ctx, { venueId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("eventRead", "Event", () => (checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("events").withIndex("by_venueId", (q) => q.eq("venueId", venueId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "Event", ["primaryContactName","primaryContactEmail","primaryContactPhone"], row)));
    const __projectedRows: any[] = [];
    for (const __row of __plainRows as any[]) {
      {
      const __fk = ((__row as any) as any).clientId;
      ((__row as any) as any).client = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      (__row as any).eventDishes = await ctx.db.query("eventDishes").withIndex("by_eventId", (q: any) => q.eq("eventId", __row._id)).collect();
      for (const __agg0 of ((__row as any).eventDishes ?? []) as any[]) {
      {
      const __fk = (__agg0 as any).eventId;
      (__agg0 as any).event = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      {
      const __fk = (__agg0 as any).dishId;
      (__agg0 as any).dish = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      if ((__agg0 as any).dish) {
      (__agg0 as any).dish.componentLines = await ctx.db.query("dishComponents").withIndex("by_dishId", (q: any) => q.eq("dishId", (__agg0 as any).dish._id)).collect();
      for (const __agg1 of ((__agg0 as any).dish.componentLines ?? []) as any[]) {
      {
      const __fk = (__agg1 as any).eventId;
      (__agg1 as any).event = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      {
      const __fk = (__agg1 as any).componentId;
      (__agg1 as any).component = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      if ((__agg1 as any).component) {
      (__agg1 as any).component.ingredientLines = await ctx.db.query("componentIngredients").withIndex("by_componentId", (q: any) => q.eq("componentId", (__agg1 as any).component._id)).collect();
      for (const __agg2 of ((__agg1 as any).component.ingredientLines ?? []) as any[]) {
      {
      const __fk = (__agg2 as any).ingredientId;
      (__agg2 as any).ingredient = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      }
      }
      }
      (__agg0 as any).dish.ingredientLines = await ctx.db.query("dishIngredients").withIndex("by_dishId", (q: any) => q.eq("dishId", (__agg0 as any).dish._id)).collect();
      for (const __agg3 of ((__agg0 as any).dish.ingredientLines ?? []) as any[]) {
      {
      const __fk = (__agg3 as any).ingredientId;
      (__agg3 as any).ingredient = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      }
      }
      __agg0.targetHeadcount = (((__agg0.headcountOverride != null) && (__agg0.headcountOverride > 0)) ? __agg0.headcountOverride : __agg0.event.expectedHeadcount);
      __agg0.estimatedCost = (((((__agg0.dish.componentLines) ?? []).filter((line: Record<string, any>) => (((line.deletedAt == null) && (line.attachedAt != null))))) ?? []).map((line: Record<string, any>) => (((((line.yieldQuantity > 0) ? Math.ceil((__agg0.targetHeadcount / line.yieldQuantity)) : 0) * line.batchMultiplier) * ((((line.component.ingredientLines) ?? []).filter((il: Record<string, any>) => (((il.deletedAt == null) && (il.addedAt != null))))) ?? []).map((il: Record<string, any>) => (((il.unit === il.ingredient.unit) ? ((il.quantity * ((il.wasteFactor != null) ? il.wasteFactor : 1)) * il.ingredient.costPerUnit) : 0))).reduce((acc: number, v: unknown) => acc + (typeof v === "number" ? v : 0), 0)))).reduce((acc: number, v: unknown) => acc + (typeof v === "number" ? v : 0), 0) + (__agg0.targetHeadcount * ((((__agg0.dish.ingredientLines) ?? []).filter((dl: Record<string, any>) => (((dl.deletedAt == null) && (dl.addedAt != null))))) ?? []).map((dl: Record<string, any>) => (((dl.unit === dl.ingredient.unit) ? ((dl.quantity * ((dl.wasteFactor != null) ? dl.wasteFactor : 1)) * dl.ingredient.costPerUnit) : 0))).reduce((acc: number, v: unknown) => acc + (typeof v === "number" ? v : 0), 0)));
      }
      (__row as any).assignments = await ctx.db.query("eventAssignments").withIndex("by_eventId", (q: any) => q.eq("eventId", __row._id)).collect();
      (__row as any).prepTasks = await ctx.db.query("prepTasks").withIndex("by_eventId", (q: any) => q.eq("eventId", __row._id)).collect();
      (__row as any).packLists = await ctx.db.query("packLists").withIndex("by_eventId", (q: any) => q.eq("eventId", __row._id)).collect();
      (__row as any).deliveries = await ctx.db.query("deliveries").withIndex("by_eventId", (q: any) => q.eq("eventId", __row._id)).collect();
      (__row as any).isTerminal = (((__row as any).stage === "cancelled") || ((__row as any).stage === "closed_out"));
      (__row as any).isEditable = ((((__row as any).stage === "planning") || ((__row as any).stage === "pending_approval")) || ((__row as any).stage === "approved"));
      (__row as any).hasAssignedClient = (((__row as any).client != null) && ((__row as any).client.deletedAt == null));
      (__row as any).hasExpectedHeadcount = ((__row as any).expectedHeadcount > 0);
      (__row as any).hasMenuDishes = ((((__row as any).eventDishes) ?? []).filter((d: Doc<"eventDishes">) => (((d.deletedAt == null) && (d.removedAt == null)))).length > 0);
      (__row as any).hasStaffAssigned = ((((__row as any).assignments) ?? []).filter((a: Doc<"eventAssignments">) => (((a.deletedAt == null) && (a.status !== "unassigned")))).length > 0);
      (__row as any).isSetupReady = ((((__row as any).hasAssignedClient && (__row as any).hasExpectedHeadcount) && (__row as any).hasMenuDishes) && (__row as any).hasStaffAssigned);
      (__row as any).isReadyForExecution = ((((((__row as any).prepTasks) ?? []).filter((t: Doc<"prepTasks">) => (((t.status !== "completed") && (t.status !== "cancelled")))).length === 0) && ((((__row as any).packLists) ?? []).filter((p: Doc<"packLists">) => ((((p.status !== "dispatched") && (p.status !== "cancelled")) && (p.status !== "draft")))).length === 0)) && ((((__row as any).deliveries) ?? []).filter((d: Doc<"deliveries">) => ((((d.status !== "delivered") && (d.status !== "cancelled")) && (d.status !== "failed")))).length === 0));
      (__row as any).estimatedFoodCost = (((((__row as any).eventDishes) ?? []).filter((item: Doc<"eventDishes">) => (((item.deletedAt == null) && (item.addedAt != null))))) ?? []).map((item: Record<string, any>) => (item.estimatedCost)).reduce((acc: number, v: unknown) => acc + (typeof v === "number" ? v : 0), 0);
      __projectedRows.push({ ...(__row as any), isTerminal: (__row as any).isTerminal, isEditable: (__row as any).isEditable, hasAssignedClient: (__row as any).hasAssignedClient, hasExpectedHeadcount: (__row as any).hasExpectedHeadcount, hasMenuDishes: (__row as any).hasMenuDishes, hasStaffAssigned: (__row as any).hasStaffAssigned, isSetupReady: (__row as any).isSetupReady, isReadyForExecution: (__row as any).isReadyForExecution, estimatedFoodCost: (__row as any).estimatedFoodCost });
    }
    return __projectedRows;
  },
});

export const listEventByAssignedToId = query({
  args: { assignedToId: v.id("people") },
  handler: async (ctx, { assignedToId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("eventRead", "Event", () => (checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("events").withIndex("by_assignedToId", (q) => q.eq("assignedToId", assignedToId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "Event", ["primaryContactName","primaryContactEmail","primaryContactPhone"], row)));
    const __projectedRows: any[] = [];
    for (const __row of __plainRows as any[]) {
      {
      const __fk = ((__row as any) as any).clientId;
      ((__row as any) as any).client = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      (__row as any).eventDishes = await ctx.db.query("eventDishes").withIndex("by_eventId", (q: any) => q.eq("eventId", __row._id)).collect();
      for (const __agg0 of ((__row as any).eventDishes ?? []) as any[]) {
      {
      const __fk = (__agg0 as any).eventId;
      (__agg0 as any).event = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      {
      const __fk = (__agg0 as any).dishId;
      (__agg0 as any).dish = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      if ((__agg0 as any).dish) {
      (__agg0 as any).dish.componentLines = await ctx.db.query("dishComponents").withIndex("by_dishId", (q: any) => q.eq("dishId", (__agg0 as any).dish._id)).collect();
      for (const __agg1 of ((__agg0 as any).dish.componentLines ?? []) as any[]) {
      {
      const __fk = (__agg1 as any).eventId;
      (__agg1 as any).event = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      {
      const __fk = (__agg1 as any).componentId;
      (__agg1 as any).component = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      if ((__agg1 as any).component) {
      (__agg1 as any).component.ingredientLines = await ctx.db.query("componentIngredients").withIndex("by_componentId", (q: any) => q.eq("componentId", (__agg1 as any).component._id)).collect();
      for (const __agg2 of ((__agg1 as any).component.ingredientLines ?? []) as any[]) {
      {
      const __fk = (__agg2 as any).ingredientId;
      (__agg2 as any).ingredient = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      }
      }
      }
      (__agg0 as any).dish.ingredientLines = await ctx.db.query("dishIngredients").withIndex("by_dishId", (q: any) => q.eq("dishId", (__agg0 as any).dish._id)).collect();
      for (const __agg3 of ((__agg0 as any).dish.ingredientLines ?? []) as any[]) {
      {
      const __fk = (__agg3 as any).ingredientId;
      (__agg3 as any).ingredient = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      }
      }
      __agg0.targetHeadcount = (((__agg0.headcountOverride != null) && (__agg0.headcountOverride > 0)) ? __agg0.headcountOverride : __agg0.event.expectedHeadcount);
      __agg0.estimatedCost = (((((__agg0.dish.componentLines) ?? []).filter((line: Record<string, any>) => (((line.deletedAt == null) && (line.attachedAt != null))))) ?? []).map((line: Record<string, any>) => (((((line.yieldQuantity > 0) ? Math.ceil((__agg0.targetHeadcount / line.yieldQuantity)) : 0) * line.batchMultiplier) * ((((line.component.ingredientLines) ?? []).filter((il: Record<string, any>) => (((il.deletedAt == null) && (il.addedAt != null))))) ?? []).map((il: Record<string, any>) => (((il.unit === il.ingredient.unit) ? ((il.quantity * ((il.wasteFactor != null) ? il.wasteFactor : 1)) * il.ingredient.costPerUnit) : 0))).reduce((acc: number, v: unknown) => acc + (typeof v === "number" ? v : 0), 0)))).reduce((acc: number, v: unknown) => acc + (typeof v === "number" ? v : 0), 0) + (__agg0.targetHeadcount * ((((__agg0.dish.ingredientLines) ?? []).filter((dl: Record<string, any>) => (((dl.deletedAt == null) && (dl.addedAt != null))))) ?? []).map((dl: Record<string, any>) => (((dl.unit === dl.ingredient.unit) ? ((dl.quantity * ((dl.wasteFactor != null) ? dl.wasteFactor : 1)) * dl.ingredient.costPerUnit) : 0))).reduce((acc: number, v: unknown) => acc + (typeof v === "number" ? v : 0), 0)));
      }
      (__row as any).assignments = await ctx.db.query("eventAssignments").withIndex("by_eventId", (q: any) => q.eq("eventId", __row._id)).collect();
      (__row as any).prepTasks = await ctx.db.query("prepTasks").withIndex("by_eventId", (q: any) => q.eq("eventId", __row._id)).collect();
      (__row as any).packLists = await ctx.db.query("packLists").withIndex("by_eventId", (q: any) => q.eq("eventId", __row._id)).collect();
      (__row as any).deliveries = await ctx.db.query("deliveries").withIndex("by_eventId", (q: any) => q.eq("eventId", __row._id)).collect();
      (__row as any).isTerminal = (((__row as any).stage === "cancelled") || ((__row as any).stage === "closed_out"));
      (__row as any).isEditable = ((((__row as any).stage === "planning") || ((__row as any).stage === "pending_approval")) || ((__row as any).stage === "approved"));
      (__row as any).hasAssignedClient = (((__row as any).client != null) && ((__row as any).client.deletedAt == null));
      (__row as any).hasExpectedHeadcount = ((__row as any).expectedHeadcount > 0);
      (__row as any).hasMenuDishes = ((((__row as any).eventDishes) ?? []).filter((d: Doc<"eventDishes">) => (((d.deletedAt == null) && (d.removedAt == null)))).length > 0);
      (__row as any).hasStaffAssigned = ((((__row as any).assignments) ?? []).filter((a: Doc<"eventAssignments">) => (((a.deletedAt == null) && (a.status !== "unassigned")))).length > 0);
      (__row as any).isSetupReady = ((((__row as any).hasAssignedClient && (__row as any).hasExpectedHeadcount) && (__row as any).hasMenuDishes) && (__row as any).hasStaffAssigned);
      (__row as any).isReadyForExecution = ((((((__row as any).prepTasks) ?? []).filter((t: Doc<"prepTasks">) => (((t.status !== "completed") && (t.status !== "cancelled")))).length === 0) && ((((__row as any).packLists) ?? []).filter((p: Doc<"packLists">) => ((((p.status !== "dispatched") && (p.status !== "cancelled")) && (p.status !== "draft")))).length === 0)) && ((((__row as any).deliveries) ?? []).filter((d: Doc<"deliveries">) => ((((d.status !== "delivered") && (d.status !== "cancelled")) && (d.status !== "failed")))).length === 0));
      (__row as any).estimatedFoodCost = (((((__row as any).eventDishes) ?? []).filter((item: Doc<"eventDishes">) => (((item.deletedAt == null) && (item.addedAt != null))))) ?? []).map((item: Record<string, any>) => (item.estimatedCost)).reduce((acc: number, v: unknown) => acc + (typeof v === "number" ? v : 0), 0);
      __projectedRows.push({ ...(__row as any), isTerminal: (__row as any).isTerminal, isEditable: (__row as any).isEditable, hasAssignedClient: (__row as any).hasAssignedClient, hasExpectedHeadcount: (__row as any).hasExpectedHeadcount, hasMenuDishes: (__row as any).hasMenuDishes, hasStaffAssigned: (__row as any).hasStaffAssigned, isSetupReady: (__row as any).isSetupReady, isReadyForExecution: (__row as any).isReadyForExecution, estimatedFoodCost: (__row as any).estimatedFoodCost });
    }
    return __projectedRows;
  },
});

export const listEventByReferralSourceId = query({
  args: { referralSourceId: v.id("referralSources") },
  handler: async (ctx, { referralSourceId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("eventRead", "Event", () => (checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("events").withIndex("by_referralSourceId", (q) => q.eq("referralSourceId", referralSourceId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "Event", ["primaryContactName","primaryContactEmail","primaryContactPhone"], row)));
    const __projectedRows: any[] = [];
    for (const __row of __plainRows as any[]) {
      {
      const __fk = ((__row as any) as any).clientId;
      ((__row as any) as any).client = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      (__row as any).eventDishes = await ctx.db.query("eventDishes").withIndex("by_eventId", (q: any) => q.eq("eventId", __row._id)).collect();
      for (const __agg0 of ((__row as any).eventDishes ?? []) as any[]) {
      {
      const __fk = (__agg0 as any).eventId;
      (__agg0 as any).event = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      {
      const __fk = (__agg0 as any).dishId;
      (__agg0 as any).dish = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      if ((__agg0 as any).dish) {
      (__agg0 as any).dish.componentLines = await ctx.db.query("dishComponents").withIndex("by_dishId", (q: any) => q.eq("dishId", (__agg0 as any).dish._id)).collect();
      for (const __agg1 of ((__agg0 as any).dish.componentLines ?? []) as any[]) {
      {
      const __fk = (__agg1 as any).eventId;
      (__agg1 as any).event = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      {
      const __fk = (__agg1 as any).componentId;
      (__agg1 as any).component = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      if ((__agg1 as any).component) {
      (__agg1 as any).component.ingredientLines = await ctx.db.query("componentIngredients").withIndex("by_componentId", (q: any) => q.eq("componentId", (__agg1 as any).component._id)).collect();
      for (const __agg2 of ((__agg1 as any).component.ingredientLines ?? []) as any[]) {
      {
      const __fk = (__agg2 as any).ingredientId;
      (__agg2 as any).ingredient = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      }
      }
      }
      (__agg0 as any).dish.ingredientLines = await ctx.db.query("dishIngredients").withIndex("by_dishId", (q: any) => q.eq("dishId", (__agg0 as any).dish._id)).collect();
      for (const __agg3 of ((__agg0 as any).dish.ingredientLines ?? []) as any[]) {
      {
      const __fk = (__agg3 as any).ingredientId;
      (__agg3 as any).ingredient = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      }
      }
      __agg0.targetHeadcount = (((__agg0.headcountOverride != null) && (__agg0.headcountOverride > 0)) ? __agg0.headcountOverride : __agg0.event.expectedHeadcount);
      __agg0.estimatedCost = (((((__agg0.dish.componentLines) ?? []).filter((line: Record<string, any>) => (((line.deletedAt == null) && (line.attachedAt != null))))) ?? []).map((line: Record<string, any>) => (((((line.yieldQuantity > 0) ? Math.ceil((__agg0.targetHeadcount / line.yieldQuantity)) : 0) * line.batchMultiplier) * ((((line.component.ingredientLines) ?? []).filter((il: Record<string, any>) => (((il.deletedAt == null) && (il.addedAt != null))))) ?? []).map((il: Record<string, any>) => (((il.unit === il.ingredient.unit) ? ((il.quantity * ((il.wasteFactor != null) ? il.wasteFactor : 1)) * il.ingredient.costPerUnit) : 0))).reduce((acc: number, v: unknown) => acc + (typeof v === "number" ? v : 0), 0)))).reduce((acc: number, v: unknown) => acc + (typeof v === "number" ? v : 0), 0) + (__agg0.targetHeadcount * ((((__agg0.dish.ingredientLines) ?? []).filter((dl: Record<string, any>) => (((dl.deletedAt == null) && (dl.addedAt != null))))) ?? []).map((dl: Record<string, any>) => (((dl.unit === dl.ingredient.unit) ? ((dl.quantity * ((dl.wasteFactor != null) ? dl.wasteFactor : 1)) * dl.ingredient.costPerUnit) : 0))).reduce((acc: number, v: unknown) => acc + (typeof v === "number" ? v : 0), 0)));
      }
      (__row as any).assignments = await ctx.db.query("eventAssignments").withIndex("by_eventId", (q: any) => q.eq("eventId", __row._id)).collect();
      (__row as any).prepTasks = await ctx.db.query("prepTasks").withIndex("by_eventId", (q: any) => q.eq("eventId", __row._id)).collect();
      (__row as any).packLists = await ctx.db.query("packLists").withIndex("by_eventId", (q: any) => q.eq("eventId", __row._id)).collect();
      (__row as any).deliveries = await ctx.db.query("deliveries").withIndex("by_eventId", (q: any) => q.eq("eventId", __row._id)).collect();
      (__row as any).isTerminal = (((__row as any).stage === "cancelled") || ((__row as any).stage === "closed_out"));
      (__row as any).isEditable = ((((__row as any).stage === "planning") || ((__row as any).stage === "pending_approval")) || ((__row as any).stage === "approved"));
      (__row as any).hasAssignedClient = (((__row as any).client != null) && ((__row as any).client.deletedAt == null));
      (__row as any).hasExpectedHeadcount = ((__row as any).expectedHeadcount > 0);
      (__row as any).hasMenuDishes = ((((__row as any).eventDishes) ?? []).filter((d: Doc<"eventDishes">) => (((d.deletedAt == null) && (d.removedAt == null)))).length > 0);
      (__row as any).hasStaffAssigned = ((((__row as any).assignments) ?? []).filter((a: Doc<"eventAssignments">) => (((a.deletedAt == null) && (a.status !== "unassigned")))).length > 0);
      (__row as any).isSetupReady = ((((__row as any).hasAssignedClient && (__row as any).hasExpectedHeadcount) && (__row as any).hasMenuDishes) && (__row as any).hasStaffAssigned);
      (__row as any).isReadyForExecution = ((((((__row as any).prepTasks) ?? []).filter((t: Doc<"prepTasks">) => (((t.status !== "completed") && (t.status !== "cancelled")))).length === 0) && ((((__row as any).packLists) ?? []).filter((p: Doc<"packLists">) => ((((p.status !== "dispatched") && (p.status !== "cancelled")) && (p.status !== "draft")))).length === 0)) && ((((__row as any).deliveries) ?? []).filter((d: Doc<"deliveries">) => ((((d.status !== "delivered") && (d.status !== "cancelled")) && (d.status !== "failed")))).length === 0));
      (__row as any).estimatedFoodCost = (((((__row as any).eventDishes) ?? []).filter((item: Doc<"eventDishes">) => (((item.deletedAt == null) && (item.addedAt != null))))) ?? []).map((item: Record<string, any>) => (item.estimatedCost)).reduce((acc: number, v: unknown) => acc + (typeof v === "number" ? v : 0), 0);
      __projectedRows.push({ ...(__row as any), isTerminal: (__row as any).isTerminal, isEditable: (__row as any).isEditable, hasAssignedClient: (__row as any).hasAssignedClient, hasExpectedHeadcount: (__row as any).hasExpectedHeadcount, hasMenuDishes: (__row as any).hasMenuDishes, hasStaffAssigned: (__row as any).hasStaffAssigned, isSetupReady: (__row as any).isSetupReady, isReadyForExecution: (__row as any).isReadyForExecution, estimatedFoodCost: (__row as any).estimatedFoodCost });
    }
    return __projectedRows;
  },
});

export const listEventByRecurrenceTemplateEventId = query({
  args: { recurrenceTemplateEventId: v.id("events") },
  handler: async (ctx, { recurrenceTemplateEventId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("eventRead", "Event", () => (checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("events").withIndex("by_recurrenceTemplateEventId", (q) => q.eq("recurrenceTemplateEventId", recurrenceTemplateEventId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "Event", ["primaryContactName","primaryContactEmail","primaryContactPhone"], row)));
    const __projectedRows: any[] = [];
    for (const __row of __plainRows as any[]) {
      {
      const __fk = ((__row as any) as any).clientId;
      ((__row as any) as any).client = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      (__row as any).eventDishes = await ctx.db.query("eventDishes").withIndex("by_eventId", (q: any) => q.eq("eventId", __row._id)).collect();
      for (const __agg0 of ((__row as any).eventDishes ?? []) as any[]) {
      {
      const __fk = (__agg0 as any).eventId;
      (__agg0 as any).event = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      {
      const __fk = (__agg0 as any).dishId;
      (__agg0 as any).dish = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      if ((__agg0 as any).dish) {
      (__agg0 as any).dish.componentLines = await ctx.db.query("dishComponents").withIndex("by_dishId", (q: any) => q.eq("dishId", (__agg0 as any).dish._id)).collect();
      for (const __agg1 of ((__agg0 as any).dish.componentLines ?? []) as any[]) {
      {
      const __fk = (__agg1 as any).eventId;
      (__agg1 as any).event = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      {
      const __fk = (__agg1 as any).componentId;
      (__agg1 as any).component = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      if ((__agg1 as any).component) {
      (__agg1 as any).component.ingredientLines = await ctx.db.query("componentIngredients").withIndex("by_componentId", (q: any) => q.eq("componentId", (__agg1 as any).component._id)).collect();
      for (const __agg2 of ((__agg1 as any).component.ingredientLines ?? []) as any[]) {
      {
      const __fk = (__agg2 as any).ingredientId;
      (__agg2 as any).ingredient = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      }
      }
      }
      (__agg0 as any).dish.ingredientLines = await ctx.db.query("dishIngredients").withIndex("by_dishId", (q: any) => q.eq("dishId", (__agg0 as any).dish._id)).collect();
      for (const __agg3 of ((__agg0 as any).dish.ingredientLines ?? []) as any[]) {
      {
      const __fk = (__agg3 as any).ingredientId;
      (__agg3 as any).ingredient = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      }
      }
      __agg0.targetHeadcount = (((__agg0.headcountOverride != null) && (__agg0.headcountOverride > 0)) ? __agg0.headcountOverride : __agg0.event.expectedHeadcount);
      __agg0.estimatedCost = (((((__agg0.dish.componentLines) ?? []).filter((line: Record<string, any>) => (((line.deletedAt == null) && (line.attachedAt != null))))) ?? []).map((line: Record<string, any>) => (((((line.yieldQuantity > 0) ? Math.ceil((__agg0.targetHeadcount / line.yieldQuantity)) : 0) * line.batchMultiplier) * ((((line.component.ingredientLines) ?? []).filter((il: Record<string, any>) => (((il.deletedAt == null) && (il.addedAt != null))))) ?? []).map((il: Record<string, any>) => (((il.unit === il.ingredient.unit) ? ((il.quantity * ((il.wasteFactor != null) ? il.wasteFactor : 1)) * il.ingredient.costPerUnit) : 0))).reduce((acc: number, v: unknown) => acc + (typeof v === "number" ? v : 0), 0)))).reduce((acc: number, v: unknown) => acc + (typeof v === "number" ? v : 0), 0) + (__agg0.targetHeadcount * ((((__agg0.dish.ingredientLines) ?? []).filter((dl: Record<string, any>) => (((dl.deletedAt == null) && (dl.addedAt != null))))) ?? []).map((dl: Record<string, any>) => (((dl.unit === dl.ingredient.unit) ? ((dl.quantity * ((dl.wasteFactor != null) ? dl.wasteFactor : 1)) * dl.ingredient.costPerUnit) : 0))).reduce((acc: number, v: unknown) => acc + (typeof v === "number" ? v : 0), 0)));
      }
      (__row as any).assignments = await ctx.db.query("eventAssignments").withIndex("by_eventId", (q: any) => q.eq("eventId", __row._id)).collect();
      (__row as any).prepTasks = await ctx.db.query("prepTasks").withIndex("by_eventId", (q: any) => q.eq("eventId", __row._id)).collect();
      (__row as any).packLists = await ctx.db.query("packLists").withIndex("by_eventId", (q: any) => q.eq("eventId", __row._id)).collect();
      (__row as any).deliveries = await ctx.db.query("deliveries").withIndex("by_eventId", (q: any) => q.eq("eventId", __row._id)).collect();
      (__row as any).isTerminal = (((__row as any).stage === "cancelled") || ((__row as any).stage === "closed_out"));
      (__row as any).isEditable = ((((__row as any).stage === "planning") || ((__row as any).stage === "pending_approval")) || ((__row as any).stage === "approved"));
      (__row as any).hasAssignedClient = (((__row as any).client != null) && ((__row as any).client.deletedAt == null));
      (__row as any).hasExpectedHeadcount = ((__row as any).expectedHeadcount > 0);
      (__row as any).hasMenuDishes = ((((__row as any).eventDishes) ?? []).filter((d: Doc<"eventDishes">) => (((d.deletedAt == null) && (d.removedAt == null)))).length > 0);
      (__row as any).hasStaffAssigned = ((((__row as any).assignments) ?? []).filter((a: Doc<"eventAssignments">) => (((a.deletedAt == null) && (a.status !== "unassigned")))).length > 0);
      (__row as any).isSetupReady = ((((__row as any).hasAssignedClient && (__row as any).hasExpectedHeadcount) && (__row as any).hasMenuDishes) && (__row as any).hasStaffAssigned);
      (__row as any).isReadyForExecution = ((((((__row as any).prepTasks) ?? []).filter((t: Doc<"prepTasks">) => (((t.status !== "completed") && (t.status !== "cancelled")))).length === 0) && ((((__row as any).packLists) ?? []).filter((p: Doc<"packLists">) => ((((p.status !== "dispatched") && (p.status !== "cancelled")) && (p.status !== "draft")))).length === 0)) && ((((__row as any).deliveries) ?? []).filter((d: Doc<"deliveries">) => ((((d.status !== "delivered") && (d.status !== "cancelled")) && (d.status !== "failed")))).length === 0));
      (__row as any).estimatedFoodCost = (((((__row as any).eventDishes) ?? []).filter((item: Doc<"eventDishes">) => (((item.deletedAt == null) && (item.addedAt != null))))) ?? []).map((item: Record<string, any>) => (item.estimatedCost)).reduce((acc: number, v: unknown) => acc + (typeof v === "number" ? v : 0), 0);
      __projectedRows.push({ ...(__row as any), isTerminal: (__row as any).isTerminal, isEditable: (__row as any).isEditable, hasAssignedClient: (__row as any).hasAssignedClient, hasExpectedHeadcount: (__row as any).hasExpectedHeadcount, hasMenuDishes: (__row as any).hasMenuDishes, hasStaffAssigned: (__row as any).hasStaffAssigned, isSetupReady: (__row as any).isSetupReady, isReadyForExecution: (__row as any).isReadyForExecution, estimatedFoodCost: (__row as any).estimatedFoodCost });
    }
    return __projectedRows;
  },
});

export const listEventAllergenCheck = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("allergenCheckRead", "EventAllergenCheck", () => (checkRole(user, "eventAccess") || checkRole(user, "kitchenAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("eventAllergenChecks").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const getEventAllergenCheck = query({
  args: { id: v.id("eventAllergenChecks") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("allergenCheckRead", "EventAllergenCheck", () => (checkRole(user, "eventAccess") || checkRole(user, "kitchenAccess")))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    return __doc;
  },
});

export const listEventAllergenCheckByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("allergenCheckRead", "EventAllergenCheck", () => (checkRole(user, "eventAccess") || checkRole(user, "kitchenAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("eventAllergenChecks").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listEventAllergenCheckByEventId = query({
  args: { eventId: v.id("events") },
  handler: async (ctx, { eventId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("allergenCheckRead", "EventAllergenCheck", () => (checkRole(user, "eventAccess") || checkRole(user, "kitchenAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("eventAllergenChecks").withIndex("by_eventId", (q) => q.eq("eventId", eventId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listEventAllergenCheckByEventDishId = query({
  args: { eventDishId: v.id("eventDishes") },
  handler: async (ctx, { eventDishId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("allergenCheckRead", "EventAllergenCheck", () => (checkRole(user, "eventAccess") || checkRole(user, "kitchenAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("eventAllergenChecks").withIndex("by_eventDishId", (q) => q.eq("eventDishId", eventDishId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listEventAllergenCheckByDishId = query({
  args: { dishId: v.id("dishes") },
  handler: async (ctx, { dishId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("allergenCheckRead", "EventAllergenCheck", () => (checkRole(user, "eventAccess") || checkRole(user, "kitchenAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("eventAllergenChecks").withIndex("by_dishId", (q) => q.eq("dishId", dishId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listEventAllergenCheckByCheckedById = query({
  args: { checkedById: v.id("people") },
  handler: async (ctx, { checkedById }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("allergenCheckRead", "EventAllergenCheck", () => (checkRole(user, "eventAccess") || checkRole(user, "kitchenAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("eventAllergenChecks").withIndex("by_checkedById", (q) => q.eq("checkedById", checkedById)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listEventAssignment = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("eventAssignmentRead", "EventAssignment", () => checkRole(user, "workforceAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("eventAssignments").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "EventAssignment", ["notes"], row)));
    return __plainRows;
  },
});

export const getEventAssignment = query({
  args: { id: v.id("eventAssignments") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("eventAssignmentRead", "EventAssignment", () => checkRole(user, "workforceAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = await __decryptDoc(ctx, "EventAssignment", ["notes"], __rawDoc);
    return __doc;
  },
});

export const listEventAssignmentByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("eventAssignmentRead", "EventAssignment", () => checkRole(user, "workforceAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("eventAssignments").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "EventAssignment", ["notes"], row)));
    return __plainRows;
  },
});

export const listEventAssignmentByEventId = query({
  args: { eventId: v.id("events") },
  handler: async (ctx, { eventId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("eventAssignmentRead", "EventAssignment", () => checkRole(user, "workforceAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("eventAssignments").withIndex("by_eventId", (q) => q.eq("eventId", eventId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "EventAssignment", ["notes"], row)));
    return __plainRows;
  },
});

export const listEventAssignmentByPersonId = query({
  args: { personId: v.id("people") },
  handler: async (ctx, { personId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("eventAssignmentRead", "EventAssignment", () => checkRole(user, "workforceAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("eventAssignments").withIndex("by_personId", (q) => q.eq("personId", personId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "EventAssignment", ["notes"], row)));
    return __plainRows;
  },
});

export const listEventCloseout = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("eventCloseoutRead", "EventCloseout", () => (checkRole(user, "financeAccess") || checkRole(user, "eventManageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("eventCloseouts").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const getEventCloseout = query({
  args: { id: v.id("eventCloseouts") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("eventCloseoutRead", "EventCloseout", () => (checkRole(user, "financeAccess") || checkRole(user, "eventManageAccess")))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    return __doc;
  },
});

export const listEventCloseoutByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("eventCloseoutRead", "EventCloseout", () => (checkRole(user, "financeAccess") || checkRole(user, "eventManageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("eventCloseouts").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listEventCloseoutByEventId = query({
  args: { eventId: v.id("events") },
  handler: async (ctx, { eventId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("eventCloseoutRead", "EventCloseout", () => (checkRole(user, "financeAccess") || checkRole(user, "eventManageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("eventCloseouts").withIndex("by_eventId", (q) => q.eq("eventId", eventId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listEventDish = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("eventDishRead", "EventDish", () => checkRole(user, "staffAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("eventDishes").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    const __projectedRows: any[] = [];
    for (const __row of __plainRows as any[]) {
      {
      const __fk = ((__row as any) as any).eventId;
      ((__row as any) as any).event = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      {
      const __fk = ((__row as any) as any).dishId;
      ((__row as any) as any).dish = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      if (((__row as any) as any).dish) {
      ((__row as any) as any).dish.componentLines = await ctx.db.query("dishComponents").withIndex("by_dishId", (q: any) => q.eq("dishId", ((__row as any) as any).dish._id)).collect();
      for (const __agg0 of (((__row as any) as any).dish.componentLines ?? []) as any[]) {
      {
      const __fk = (__agg0 as any).eventId;
      (__agg0 as any).event = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      {
      const __fk = (__agg0 as any).componentId;
      (__agg0 as any).component = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      if ((__agg0 as any).component) {
      (__agg0 as any).component.ingredientLines = await ctx.db.query("componentIngredients").withIndex("by_componentId", (q: any) => q.eq("componentId", (__agg0 as any).component._id)).collect();
      for (const __agg1 of ((__agg0 as any).component.ingredientLines ?? []) as any[]) {
      {
      const __fk = (__agg1 as any).ingredientId;
      (__agg1 as any).ingredient = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      }
      }
      }
      ((__row as any) as any).dish.ingredientLines = await ctx.db.query("dishIngredients").withIndex("by_dishId", (q: any) => q.eq("dishId", ((__row as any) as any).dish._id)).collect();
      for (const __agg2 of (((__row as any) as any).dish.ingredientLines ?? []) as any[]) {
      {
      const __fk = (__agg2 as any).ingredientId;
      (__agg2 as any).ingredient = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      }
      }
      (__row as any).targetHeadcount = ((((__row as any).headcountOverride != null) && ((__row as any).headcountOverride > 0)) ? (__row as any).headcountOverride : (__row as any).event.expectedHeadcount);
      (__row as any).requiredBatches = (() => { const __vals = (((((__row as any).dish.componentLines) ?? []).filter((line: Record<string, any>) => (((line.deletedAt == null) && (line.attachedAt != null))))) ?? []).map((line: Record<string, any>) => (((line.yieldQuantity > 0) ? Math.ceil(((__row as any).targetHeadcount / line.yieldQuantity)) : 0))).filter((v: unknown): v is number => typeof v === "number"); return __vals.length === 0 ? undefined : Math.max(...__vals); })();
      (__row as any).requiredYieldQuantity = (() => { const __vals = (((((__row as any).dish.componentLines) ?? []).filter((line: Record<string, any>) => (((line.deletedAt == null) && (line.attachedAt != null))))) ?? []).map((line: Record<string, any>) => ((((line.yieldQuantity > 0) ? Math.ceil(((__row as any).targetHeadcount / line.yieldQuantity)) : 0) * line.yieldQuantity))).filter((v: unknown): v is number => typeof v === "number"); return __vals.length === 0 ? undefined : Math.max(...__vals); })();
      (__row as any).estimatedCost = ((((((__row as any).dish.componentLines) ?? []).filter((line: Record<string, any>) => (((line.deletedAt == null) && (line.attachedAt != null))))) ?? []).map((line: Record<string, any>) => (((((line.yieldQuantity > 0) ? Math.ceil(((__row as any).targetHeadcount / line.yieldQuantity)) : 0) * line.batchMultiplier) * ((((line.component.ingredientLines) ?? []).filter((il: Record<string, any>) => (((il.deletedAt == null) && (il.addedAt != null))))) ?? []).map((il: Record<string, any>) => (((il.unit === il.ingredient.unit) ? ((il.quantity * ((il.wasteFactor != null) ? il.wasteFactor : 1)) * il.ingredient.costPerUnit) : 0))).reduce((acc: number, v: unknown) => acc + (typeof v === "number" ? v : 0), 0)))).reduce((acc: number, v: unknown) => acc + (typeof v === "number" ? v : 0), 0) + ((__row as any).targetHeadcount * (((((__row as any).dish.ingredientLines) ?? []).filter((dl: Record<string, any>) => (((dl.deletedAt == null) && (dl.addedAt != null))))) ?? []).map((dl: Record<string, any>) => (((dl.unit === dl.ingredient.unit) ? ((dl.quantity * ((dl.wasteFactor != null) ? dl.wasteFactor : 1)) * dl.ingredient.costPerUnit) : 0))).reduce((acc: number, v: unknown) => acc + (typeof v === "number" ? v : 0), 0)));
      __projectedRows.push({ ...(__row as any), targetHeadcount: (__row as any).targetHeadcount, requiredBatches: (__row as any).requiredBatches, requiredYieldQuantity: (__row as any).requiredYieldQuantity, estimatedCost: (__row as any).estimatedCost });
    }
    return __projectedRows;
  },
});

export const getEventDish = query({
  args: { id: v.id("eventDishes") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("eventDishRead", "EventDish", () => checkRole(user, "staffAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    {
      const __fk = ((__doc as any) as any).eventId;
      ((__doc as any) as any).event = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    {
      const __fk = ((__doc as any) as any).dishId;
      ((__doc as any) as any).dish = __fk != null ? await ctx.db.get(__fk as any) : null;
    }
    if (((__doc as any) as any).dish) {
      ((__doc as any) as any).dish.componentLines = await ctx.db.query("dishComponents").withIndex("by_dishId", (q: any) => q.eq("dishId", ((__doc as any) as any).dish._id)).collect();
      for (const __agg0 of (((__doc as any) as any).dish.componentLines ?? []) as any[]) {
        {
          const __fk = (__agg0 as any).eventId;
          (__agg0 as any).event = __fk != null ? await ctx.db.get(__fk as any) : null;
        }
        {
          const __fk = (__agg0 as any).componentId;
          (__agg0 as any).component = __fk != null ? await ctx.db.get(__fk as any) : null;
        }
        if ((__agg0 as any).component) {
          (__agg0 as any).component.ingredientLines = await ctx.db.query("componentIngredients").withIndex("by_componentId", (q: any) => q.eq("componentId", (__agg0 as any).component._id)).collect();
          for (const __agg1 of ((__agg0 as any).component.ingredientLines ?? []) as any[]) {
            {
              const __fk = (__agg1 as any).ingredientId;
              (__agg1 as any).ingredient = __fk != null ? await ctx.db.get(__fk as any) : null;
            }
          }
        }
      }
      ((__doc as any) as any).dish.ingredientLines = await ctx.db.query("dishIngredients").withIndex("by_dishId", (q: any) => q.eq("dishId", ((__doc as any) as any).dish._id)).collect();
      for (const __agg2 of (((__doc as any) as any).dish.ingredientLines ?? []) as any[]) {
        {
          const __fk = (__agg2 as any).ingredientId;
          (__agg2 as any).ingredient = __fk != null ? await ctx.db.get(__fk as any) : null;
        }
      }
    }
    (__doc as any).targetHeadcount = ((((__doc as any).headcountOverride != null) && ((__doc as any).headcountOverride > 0)) ? (__doc as any).headcountOverride : (__doc as any).event.expectedHeadcount);
    (__doc as any).requiredBatches = (() => { const __vals = (((((__doc as any).dish.componentLines) ?? []).filter((line: Record<string, any>) => (((line.deletedAt == null) && (line.attachedAt != null))))) ?? []).map((line: Record<string, any>) => (((line.yieldQuantity > 0) ? Math.ceil(((__doc as any).targetHeadcount / line.yieldQuantity)) : 0))).filter((v: unknown): v is number => typeof v === "number"); return __vals.length === 0 ? undefined : Math.max(...__vals); })();
    (__doc as any).requiredYieldQuantity = (() => { const __vals = (((((__doc as any).dish.componentLines) ?? []).filter((line: Record<string, any>) => (((line.deletedAt == null) && (line.attachedAt != null))))) ?? []).map((line: Record<string, any>) => ((((line.yieldQuantity > 0) ? Math.ceil(((__doc as any).targetHeadcount / line.yieldQuantity)) : 0) * line.yieldQuantity))).filter((v: unknown): v is number => typeof v === "number"); return __vals.length === 0 ? undefined : Math.max(...__vals); })();
    (__doc as any).estimatedCost = ((((((__doc as any).dish.componentLines) ?? []).filter((line: Record<string, any>) => (((line.deletedAt == null) && (line.attachedAt != null))))) ?? []).map((line: Record<string, any>) => (((((line.yieldQuantity > 0) ? Math.ceil(((__doc as any).targetHeadcount / line.yieldQuantity)) : 0) * line.batchMultiplier) * ((((line.component.ingredientLines) ?? []).filter((il: Record<string, any>) => (((il.deletedAt == null) && (il.addedAt != null))))) ?? []).map((il: Record<string, any>) => (((il.unit === il.ingredient.unit) ? ((il.quantity * ((il.wasteFactor != null) ? il.wasteFactor : 1)) * il.ingredient.costPerUnit) : 0))).reduce((acc: number, v: unknown) => acc + (typeof v === "number" ? v : 0), 0)))).reduce((acc: number, v: unknown) => acc + (typeof v === "number" ? v : 0), 0) + ((__doc as any).targetHeadcount * (((((__doc as any).dish.ingredientLines) ?? []).filter((dl: Record<string, any>) => (((dl.deletedAt == null) && (dl.addedAt != null))))) ?? []).map((dl: Record<string, any>) => (((dl.unit === dl.ingredient.unit) ? ((dl.quantity * ((dl.wasteFactor != null) ? dl.wasteFactor : 1)) * dl.ingredient.costPerUnit) : 0))).reduce((acc: number, v: unknown) => acc + (typeof v === "number" ? v : 0), 0)));
    const __hydrated = { ...(__doc as any), targetHeadcount: (__doc as any).targetHeadcount, requiredBatches: (__doc as any).requiredBatches, requiredYieldQuantity: (__doc as any).requiredYieldQuantity, estimatedCost: (__doc as any).estimatedCost };
    return __hydrated;
  },
});

export const listEventDishByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("eventDishRead", "EventDish", () => checkRole(user, "staffAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("eventDishes").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    const __projectedRows: any[] = [];
    for (const __row of __plainRows as any[]) {
      {
      const __fk = ((__row as any) as any).eventId;
      ((__row as any) as any).event = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      {
      const __fk = ((__row as any) as any).dishId;
      ((__row as any) as any).dish = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      if (((__row as any) as any).dish) {
      ((__row as any) as any).dish.componentLines = await ctx.db.query("dishComponents").withIndex("by_dishId", (q: any) => q.eq("dishId", ((__row as any) as any).dish._id)).collect();
      for (const __agg0 of (((__row as any) as any).dish.componentLines ?? []) as any[]) {
      {
      const __fk = (__agg0 as any).eventId;
      (__agg0 as any).event = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      {
      const __fk = (__agg0 as any).componentId;
      (__agg0 as any).component = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      if ((__agg0 as any).component) {
      (__agg0 as any).component.ingredientLines = await ctx.db.query("componentIngredients").withIndex("by_componentId", (q: any) => q.eq("componentId", (__agg0 as any).component._id)).collect();
      for (const __agg1 of ((__agg0 as any).component.ingredientLines ?? []) as any[]) {
      {
      const __fk = (__agg1 as any).ingredientId;
      (__agg1 as any).ingredient = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      }
      }
      }
      ((__row as any) as any).dish.ingredientLines = await ctx.db.query("dishIngredients").withIndex("by_dishId", (q: any) => q.eq("dishId", ((__row as any) as any).dish._id)).collect();
      for (const __agg2 of (((__row as any) as any).dish.ingredientLines ?? []) as any[]) {
      {
      const __fk = (__agg2 as any).ingredientId;
      (__agg2 as any).ingredient = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      }
      }
      (__row as any).targetHeadcount = ((((__row as any).headcountOverride != null) && ((__row as any).headcountOverride > 0)) ? (__row as any).headcountOverride : (__row as any).event.expectedHeadcount);
      (__row as any).requiredBatches = (() => { const __vals = (((((__row as any).dish.componentLines) ?? []).filter((line: Record<string, any>) => (((line.deletedAt == null) && (line.attachedAt != null))))) ?? []).map((line: Record<string, any>) => (((line.yieldQuantity > 0) ? Math.ceil(((__row as any).targetHeadcount / line.yieldQuantity)) : 0))).filter((v: unknown): v is number => typeof v === "number"); return __vals.length === 0 ? undefined : Math.max(...__vals); })();
      (__row as any).requiredYieldQuantity = (() => { const __vals = (((((__row as any).dish.componentLines) ?? []).filter((line: Record<string, any>) => (((line.deletedAt == null) && (line.attachedAt != null))))) ?? []).map((line: Record<string, any>) => ((((line.yieldQuantity > 0) ? Math.ceil(((__row as any).targetHeadcount / line.yieldQuantity)) : 0) * line.yieldQuantity))).filter((v: unknown): v is number => typeof v === "number"); return __vals.length === 0 ? undefined : Math.max(...__vals); })();
      (__row as any).estimatedCost = ((((((__row as any).dish.componentLines) ?? []).filter((line: Record<string, any>) => (((line.deletedAt == null) && (line.attachedAt != null))))) ?? []).map((line: Record<string, any>) => (((((line.yieldQuantity > 0) ? Math.ceil(((__row as any).targetHeadcount / line.yieldQuantity)) : 0) * line.batchMultiplier) * ((((line.component.ingredientLines) ?? []).filter((il: Record<string, any>) => (((il.deletedAt == null) && (il.addedAt != null))))) ?? []).map((il: Record<string, any>) => (((il.unit === il.ingredient.unit) ? ((il.quantity * ((il.wasteFactor != null) ? il.wasteFactor : 1)) * il.ingredient.costPerUnit) : 0))).reduce((acc: number, v: unknown) => acc + (typeof v === "number" ? v : 0), 0)))).reduce((acc: number, v: unknown) => acc + (typeof v === "number" ? v : 0), 0) + ((__row as any).targetHeadcount * (((((__row as any).dish.ingredientLines) ?? []).filter((dl: Record<string, any>) => (((dl.deletedAt == null) && (dl.addedAt != null))))) ?? []).map((dl: Record<string, any>) => (((dl.unit === dl.ingredient.unit) ? ((dl.quantity * ((dl.wasteFactor != null) ? dl.wasteFactor : 1)) * dl.ingredient.costPerUnit) : 0))).reduce((acc: number, v: unknown) => acc + (typeof v === "number" ? v : 0), 0)));
      __projectedRows.push({ ...(__row as any), targetHeadcount: (__row as any).targetHeadcount, requiredBatches: (__row as any).requiredBatches, requiredYieldQuantity: (__row as any).requiredYieldQuantity, estimatedCost: (__row as any).estimatedCost });
    }
    return __projectedRows;
  },
});

export const listEventDishByEventId = query({
  args: { eventId: v.id("events") },
  handler: async (ctx, { eventId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("eventDishRead", "EventDish", () => checkRole(user, "staffAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("eventDishes").withIndex("by_eventId", (q) => q.eq("eventId", eventId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    const __projectedRows: any[] = [];
    for (const __row of __plainRows as any[]) {
      {
      const __fk = ((__row as any) as any).eventId;
      ((__row as any) as any).event = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      {
      const __fk = ((__row as any) as any).dishId;
      ((__row as any) as any).dish = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      if (((__row as any) as any).dish) {
      ((__row as any) as any).dish.componentLines = await ctx.db.query("dishComponents").withIndex("by_dishId", (q: any) => q.eq("dishId", ((__row as any) as any).dish._id)).collect();
      for (const __agg0 of (((__row as any) as any).dish.componentLines ?? []) as any[]) {
      {
      const __fk = (__agg0 as any).eventId;
      (__agg0 as any).event = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      {
      const __fk = (__agg0 as any).componentId;
      (__agg0 as any).component = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      if ((__agg0 as any).component) {
      (__agg0 as any).component.ingredientLines = await ctx.db.query("componentIngredients").withIndex("by_componentId", (q: any) => q.eq("componentId", (__agg0 as any).component._id)).collect();
      for (const __agg1 of ((__agg0 as any).component.ingredientLines ?? []) as any[]) {
      {
      const __fk = (__agg1 as any).ingredientId;
      (__agg1 as any).ingredient = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      }
      }
      }
      ((__row as any) as any).dish.ingredientLines = await ctx.db.query("dishIngredients").withIndex("by_dishId", (q: any) => q.eq("dishId", ((__row as any) as any).dish._id)).collect();
      for (const __agg2 of (((__row as any) as any).dish.ingredientLines ?? []) as any[]) {
      {
      const __fk = (__agg2 as any).ingredientId;
      (__agg2 as any).ingredient = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      }
      }
      (__row as any).targetHeadcount = ((((__row as any).headcountOverride != null) && ((__row as any).headcountOverride > 0)) ? (__row as any).headcountOverride : (__row as any).event.expectedHeadcount);
      (__row as any).requiredBatches = (() => { const __vals = (((((__row as any).dish.componentLines) ?? []).filter((line: Record<string, any>) => (((line.deletedAt == null) && (line.attachedAt != null))))) ?? []).map((line: Record<string, any>) => (((line.yieldQuantity > 0) ? Math.ceil(((__row as any).targetHeadcount / line.yieldQuantity)) : 0))).filter((v: unknown): v is number => typeof v === "number"); return __vals.length === 0 ? undefined : Math.max(...__vals); })();
      (__row as any).requiredYieldQuantity = (() => { const __vals = (((((__row as any).dish.componentLines) ?? []).filter((line: Record<string, any>) => (((line.deletedAt == null) && (line.attachedAt != null))))) ?? []).map((line: Record<string, any>) => ((((line.yieldQuantity > 0) ? Math.ceil(((__row as any).targetHeadcount / line.yieldQuantity)) : 0) * line.yieldQuantity))).filter((v: unknown): v is number => typeof v === "number"); return __vals.length === 0 ? undefined : Math.max(...__vals); })();
      (__row as any).estimatedCost = ((((((__row as any).dish.componentLines) ?? []).filter((line: Record<string, any>) => (((line.deletedAt == null) && (line.attachedAt != null))))) ?? []).map((line: Record<string, any>) => (((((line.yieldQuantity > 0) ? Math.ceil(((__row as any).targetHeadcount / line.yieldQuantity)) : 0) * line.batchMultiplier) * ((((line.component.ingredientLines) ?? []).filter((il: Record<string, any>) => (((il.deletedAt == null) && (il.addedAt != null))))) ?? []).map((il: Record<string, any>) => (((il.unit === il.ingredient.unit) ? ((il.quantity * ((il.wasteFactor != null) ? il.wasteFactor : 1)) * il.ingredient.costPerUnit) : 0))).reduce((acc: number, v: unknown) => acc + (typeof v === "number" ? v : 0), 0)))).reduce((acc: number, v: unknown) => acc + (typeof v === "number" ? v : 0), 0) + ((__row as any).targetHeadcount * (((((__row as any).dish.ingredientLines) ?? []).filter((dl: Record<string, any>) => (((dl.deletedAt == null) && (dl.addedAt != null))))) ?? []).map((dl: Record<string, any>) => (((dl.unit === dl.ingredient.unit) ? ((dl.quantity * ((dl.wasteFactor != null) ? dl.wasteFactor : 1)) * dl.ingredient.costPerUnit) : 0))).reduce((acc: number, v: unknown) => acc + (typeof v === "number" ? v : 0), 0)));
      __projectedRows.push({ ...(__row as any), targetHeadcount: (__row as any).targetHeadcount, requiredBatches: (__row as any).requiredBatches, requiredYieldQuantity: (__row as any).requiredYieldQuantity, estimatedCost: (__row as any).estimatedCost });
    }
    return __projectedRows;
  },
});

export const listEventDishByDishId = query({
  args: { dishId: v.id("dishes") },
  handler: async (ctx, { dishId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("eventDishRead", "EventDish", () => checkRole(user, "staffAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("eventDishes").withIndex("by_dishId", (q) => q.eq("dishId", dishId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    const __projectedRows: any[] = [];
    for (const __row of __plainRows as any[]) {
      {
      const __fk = ((__row as any) as any).eventId;
      ((__row as any) as any).event = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      {
      const __fk = ((__row as any) as any).dishId;
      ((__row as any) as any).dish = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      if (((__row as any) as any).dish) {
      ((__row as any) as any).dish.componentLines = await ctx.db.query("dishComponents").withIndex("by_dishId", (q: any) => q.eq("dishId", ((__row as any) as any).dish._id)).collect();
      for (const __agg0 of (((__row as any) as any).dish.componentLines ?? []) as any[]) {
      {
      const __fk = (__agg0 as any).eventId;
      (__agg0 as any).event = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      {
      const __fk = (__agg0 as any).componentId;
      (__agg0 as any).component = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      if ((__agg0 as any).component) {
      (__agg0 as any).component.ingredientLines = await ctx.db.query("componentIngredients").withIndex("by_componentId", (q: any) => q.eq("componentId", (__agg0 as any).component._id)).collect();
      for (const __agg1 of ((__agg0 as any).component.ingredientLines ?? []) as any[]) {
      {
      const __fk = (__agg1 as any).ingredientId;
      (__agg1 as any).ingredient = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      }
      }
      }
      ((__row as any) as any).dish.ingredientLines = await ctx.db.query("dishIngredients").withIndex("by_dishId", (q: any) => q.eq("dishId", ((__row as any) as any).dish._id)).collect();
      for (const __agg2 of (((__row as any) as any).dish.ingredientLines ?? []) as any[]) {
      {
      const __fk = (__agg2 as any).ingredientId;
      (__agg2 as any).ingredient = __fk != null ? await ctx.db.get(__fk as any) : null;
      }
      }
      }
      (__row as any).targetHeadcount = ((((__row as any).headcountOverride != null) && ((__row as any).headcountOverride > 0)) ? (__row as any).headcountOverride : (__row as any).event.expectedHeadcount);
      (__row as any).requiredBatches = (() => { const __vals = (((((__row as any).dish.componentLines) ?? []).filter((line: Record<string, any>) => (((line.deletedAt == null) && (line.attachedAt != null))))) ?? []).map((line: Record<string, any>) => (((line.yieldQuantity > 0) ? Math.ceil(((__row as any).targetHeadcount / line.yieldQuantity)) : 0))).filter((v: unknown): v is number => typeof v === "number"); return __vals.length === 0 ? undefined : Math.max(...__vals); })();
      (__row as any).requiredYieldQuantity = (() => { const __vals = (((((__row as any).dish.componentLines) ?? []).filter((line: Record<string, any>) => (((line.deletedAt == null) && (line.attachedAt != null))))) ?? []).map((line: Record<string, any>) => ((((line.yieldQuantity > 0) ? Math.ceil(((__row as any).targetHeadcount / line.yieldQuantity)) : 0) * line.yieldQuantity))).filter((v: unknown): v is number => typeof v === "number"); return __vals.length === 0 ? undefined : Math.max(...__vals); })();
      (__row as any).estimatedCost = ((((((__row as any).dish.componentLines) ?? []).filter((line: Record<string, any>) => (((line.deletedAt == null) && (line.attachedAt != null))))) ?? []).map((line: Record<string, any>) => (((((line.yieldQuantity > 0) ? Math.ceil(((__row as any).targetHeadcount / line.yieldQuantity)) : 0) * line.batchMultiplier) * ((((line.component.ingredientLines) ?? []).filter((il: Record<string, any>) => (((il.deletedAt == null) && (il.addedAt != null))))) ?? []).map((il: Record<string, any>) => (((il.unit === il.ingredient.unit) ? ((il.quantity * ((il.wasteFactor != null) ? il.wasteFactor : 1)) * il.ingredient.costPerUnit) : 0))).reduce((acc: number, v: unknown) => acc + (typeof v === "number" ? v : 0), 0)))).reduce((acc: number, v: unknown) => acc + (typeof v === "number" ? v : 0), 0) + ((__row as any).targetHeadcount * (((((__row as any).dish.ingredientLines) ?? []).filter((dl: Record<string, any>) => (((dl.deletedAt == null) && (dl.addedAt != null))))) ?? []).map((dl: Record<string, any>) => (((dl.unit === dl.ingredient.unit) ? ((dl.quantity * ((dl.wasteFactor != null) ? dl.wasteFactor : 1)) * dl.ingredient.costPerUnit) : 0))).reduce((acc: number, v: unknown) => acc + (typeof v === "number" ? v : 0), 0)));
      __projectedRows.push({ ...(__row as any), targetHeadcount: (__row as any).targetHeadcount, requiredBatches: (__row as any).requiredBatches, requiredYieldQuantity: (__row as any).requiredYieldQuantity, estimatedCost: (__row as any).estimatedCost });
    }
    return __projectedRows;
  },
});

export const listEventDishComponentSeed = query({
  args: {},
  handler: async (ctx) => {
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("eventDishComponentSeeds").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    return rows;
  },
});

export const getEventDishComponentSeed = query({
  args: { id: v.id("eventDishComponentSeeds") },
  handler: async (ctx, { id }) => {
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    return doc;
  },
});

export const listEventDishComponentSeedByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("eventDishComponentSeeds").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    return rows;
  },
});

export const listEventDishComponentSeedByEventDishId = query({
  args: { eventDishId: v.id("eventDishes") },
  handler: async (ctx, { eventDishId }) => {
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("eventDishComponentSeeds").withIndex("by_eventDishId", (q) => q.eq("eventDishId", eventDishId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    return rows;
  },
});

export const listEventDishComponentSeedByComponentId = query({
  args: { componentId: v.string() },
  handler: async (ctx, { componentId }) => {
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("eventDishComponentSeeds").withIndex("by_componentId", (q) => q.eq("componentId", componentId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    return rows;
  },
});

export const listEventGuest = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("eventGuestRead", "EventGuest", () => checkRole(user, "eventAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("eventGuests").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "EventGuest", ["email","phone"], row)));
    return (__plainRows).map((__row) => { (__row as any).isCheckedIn = ((__row as any).checkedInAt != null); return { ...(__row as any), isCheckedIn: (__row as any).isCheckedIn }; });
  },
});

export const getEventGuest = query({
  args: { id: v.id("eventGuests") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("eventGuestRead", "EventGuest", () => checkRole(user, "eventAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = await __decryptDoc(ctx, "EventGuest", ["email","phone"], __rawDoc);
    (__doc as any).isCheckedIn = ((__doc as any).checkedInAt != null);
    const __hydrated = { ...(__doc as any), isCheckedIn: (__doc as any).isCheckedIn };
    return __hydrated;
  },
});

export const listEventGuestByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("eventGuestRead", "EventGuest", () => checkRole(user, "eventAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("eventGuests").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "EventGuest", ["email","phone"], row)));
    return (__plainRows).map((__row) => { (__row as any).isCheckedIn = ((__row as any).checkedInAt != null); return { ...(__row as any), isCheckedIn: (__row as any).isCheckedIn }; });
  },
});

export const listEventGuestByEventId = query({
  args: { eventId: v.id("events") },
  handler: async (ctx, { eventId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("eventGuestRead", "EventGuest", () => checkRole(user, "eventAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("eventGuests").withIndex("by_eventId", (q) => q.eq("eventId", eventId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "EventGuest", ["email","phone"], row)));
    return (__plainRows).map((__row) => { (__row as any).isCheckedIn = ((__row as any).checkedInAt != null); return { ...(__row as any), isCheckedIn: (__row as any).isCheckedIn }; });
  },
});

export const listEventIngredientContribution = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("eventIngredientContributionRead", "EventIngredientContribution", () => (checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("eventIngredientContributions").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const getEventIngredientContribution = query({
  args: { id: v.id("eventIngredientContributions") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("eventIngredientContributionRead", "EventIngredientContribution", () => (checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    return __doc;
  },
});

export const listEventIngredientContributionByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("eventIngredientContributionRead", "EventIngredientContribution", () => (checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("eventIngredientContributions").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listEventIngredientContributionByEventId = query({
  args: { eventId: v.id("events") },
  handler: async (ctx, { eventId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("eventIngredientContributionRead", "EventIngredientContribution", () => (checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("eventIngredientContributions").withIndex("by_eventId", (q) => q.eq("eventId", eventId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listEventIngredientContributionByEventDishId = query({
  args: { eventDishId: v.id("eventDishes") },
  handler: async (ctx, { eventDishId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("eventIngredientContributionRead", "EventIngredientContribution", () => (checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("eventIngredientContributions").withIndex("by_eventDishId", (q) => q.eq("eventDishId", eventDishId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listEventIngredientContributionByIngredientId = query({
  args: { ingredientId: v.id("ingredients") },
  handler: async (ctx, { ingredientId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("eventIngredientContributionRead", "EventIngredientContribution", () => (checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("eventIngredientContributions").withIndex("by_ingredientId", (q) => q.eq("ingredientId", ingredientId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listEventLayoutSection = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("eventLayoutSectionRead", "EventLayoutSection", () => checkRole(user, "eventAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("eventLayoutSections").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const getEventLayoutSection = query({
  args: { id: v.id("eventLayoutSections") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("eventLayoutSectionRead", "EventLayoutSection", () => checkRole(user, "eventAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    return __doc;
  },
});

export const listEventLayoutSectionByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("eventLayoutSectionRead", "EventLayoutSection", () => checkRole(user, "eventAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("eventLayoutSections").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listEventLayoutSectionByEventId = query({
  args: { eventId: v.id("events") },
  handler: async (ctx, { eventId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("eventLayoutSectionRead", "EventLayoutSection", () => checkRole(user, "eventAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("eventLayoutSections").withIndex("by_eventId", (q) => q.eq("eventId", eventId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listEventStaffNeed = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("eventStaffNeedRead", "EventStaffNeed", () => checkRole(user, "workforceAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("eventStaffNeeds").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const getEventStaffNeed = query({
  args: { id: v.id("eventStaffNeeds") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("eventStaffNeedRead", "EventStaffNeed", () => checkRole(user, "workforceAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    return __doc;
  },
});

export const listEventStaffNeedByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("eventStaffNeedRead", "EventStaffNeed", () => checkRole(user, "workforceAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("eventStaffNeeds").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listEventStaffNeedByEventId = query({
  args: { eventId: v.id("events") },
  handler: async (ctx, { eventId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("eventStaffNeedRead", "EventStaffNeed", () => checkRole(user, "workforceAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("eventStaffNeeds").withIndex("by_eventId", (q) => q.eq("eventId", eventId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listEventStaffNeedByClaimedByPersonId = query({
  args: { claimedByPersonId: v.id("people") },
  handler: async (ctx, { claimedByPersonId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("eventStaffNeedRead", "EventStaffNeed", () => checkRole(user, "workforceAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("eventStaffNeeds").withIndex("by_claimedByPersonId", (q) => q.eq("claimedByPersonId", claimedByPersonId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listEventStaffNeedByFilledByPersonId = query({
  args: { filledByPersonId: v.id("people") },
  handler: async (ctx, { filledByPersonId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("eventStaffNeedRead", "EventStaffNeed", () => checkRole(user, "workforceAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("eventStaffNeeds").withIndex("by_filledByPersonId", (q) => q.eq("filledByPersonId", filledByPersonId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listEventTemplate = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("eventTemplateRead", "EventTemplate", () => (checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("eventTemplates").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return (__plainRows).map((__row) => { (__row as any).isActive = ((__row as any).status === "active"); return { ...(__row as any), isActive: (__row as any).isActive }; });
  },
});

export const getEventTemplate = query({
  args: { id: v.id("eventTemplates") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("eventTemplateRead", "EventTemplate", () => (checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    (__doc as any).isActive = ((__doc as any).status === "active");
    const __hydrated = { ...(__doc as any), isActive: (__doc as any).isActive };
    return __hydrated;
  },
});

export const listEventTemplateByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("eventTemplateRead", "EventTemplate", () => (checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("eventTemplates").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return (__plainRows).map((__row) => { (__row as any).isActive = ((__row as any).status === "active"); return { ...(__row as any), isActive: (__row as any).isActive }; });
  },
});

export const listEventTimelineActivity = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("eventTimelineActivityRead", "EventTimelineActivity", () => checkRole(user, "eventAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("eventTimelineActivities").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const getEventTimelineActivity = query({
  args: { id: v.id("eventTimelineActivities") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("eventTimelineActivityRead", "EventTimelineActivity", () => checkRole(user, "eventAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    return __doc;
  },
});

export const listEventTimelineActivityByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("eventTimelineActivityRead", "EventTimelineActivity", () => checkRole(user, "eventAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("eventTimelineActivities").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listEventTimelineActivityByEventId = query({
  args: { eventId: v.id("events") },
  handler: async (ctx, { eventId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("eventTimelineActivityRead", "EventTimelineActivity", () => checkRole(user, "eventAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("eventTimelineActivities").withIndex("by_eventId", (q) => q.eq("eventId", eventId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listEventTimelineComment = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("eventTimelineCommentRead", "EventTimelineComment", () => checkRole(user, "eventAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("eventTimelineComments").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const getEventTimelineComment = query({
  args: { id: v.id("eventTimelineComments") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("eventTimelineCommentRead", "EventTimelineComment", () => checkRole(user, "eventAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    return __doc;
  },
});

export const listEventTimelineCommentByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("eventTimelineCommentRead", "EventTimelineComment", () => checkRole(user, "eventAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("eventTimelineComments").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listEventTimelineCommentByEventId = query({
  args: { eventId: v.id("events") },
  handler: async (ctx, { eventId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("eventTimelineCommentRead", "EventTimelineComment", () => checkRole(user, "eventAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("eventTimelineComments").withIndex("by_eventId", (q) => q.eq("eventId", eventId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listEventTimelineCommentByAuthorPersonId = query({
  args: { authorPersonId: v.id("people") },
  handler: async (ctx, { authorPersonId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("eventTimelineCommentRead", "EventTimelineComment", () => checkRole(user, "eventAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("eventTimelineComments").withIndex("by_authorPersonId", (q) => q.eq("authorPersonId", authorPersonId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listExternalRecordLink = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("externalRecordLinkRead", "ExternalRecordLink", () => checkRole(user, "importAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("externalRecordLinks").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const getExternalRecordLink = query({
  args: { id: v.id("externalRecordLinks") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("externalRecordLinkRead", "ExternalRecordLink", () => checkRole(user, "importAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    return __doc;
  },
});

export const listExternalRecordLinkByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("externalRecordLinkRead", "ExternalRecordLink", () => checkRole(user, "importAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("externalRecordLinks").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listExternalRecordLinkBySourceImportRunId = query({
  args: { sourceImportRunId: v.id("importRuns") },
  handler: async (ctx, { sourceImportRunId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("externalRecordLinkRead", "ExternalRecordLink", () => checkRole(user, "importAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("externalRecordLinks").withIndex("by_sourceImportRunId", (q) => q.eq("sourceImportRunId", sourceImportRunId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listImportDataset = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("importDatasetRead", "ImportDataset", () => checkRole(user, "importAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("importDatasets").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const getImportDataset = query({
  args: { id: v.id("importDatasets") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("importDatasetRead", "ImportDataset", () => checkRole(user, "importAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    return __doc;
  },
});

export const listImportDatasetByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("importDatasetRead", "ImportDataset", () => checkRole(user, "importAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("importDatasets").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listImportRun = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("importRunRead", "ImportRun", () => checkRole(user, "importAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("importRuns").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const getImportRun = query({
  args: { id: v.id("importRuns") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("importRunRead", "ImportRun", () => checkRole(user, "importAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    return __doc;
  },
});

export const listImportRunByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("importRunRead", "ImportRun", () => checkRole(user, "importAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("importRuns").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listIncident = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("incidentRead", "Incident", () => (checkRole(user, "eventAccess") || checkRole(user, "kitchenAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("incidents").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const getIncident = query({
  args: { id: v.id("incidents") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("incidentRead", "Incident", () => (checkRole(user, "eventAccess") || checkRole(user, "kitchenAccess")))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    return __doc;
  },
});

export const listIncidentByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("incidentRead", "Incident", () => (checkRole(user, "eventAccess") || checkRole(user, "kitchenAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("incidents").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listIncidentByEventId = query({
  args: { eventId: v.id("events") },
  handler: async (ctx, { eventId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("incidentRead", "Incident", () => (checkRole(user, "eventAccess") || checkRole(user, "kitchenAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("incidents").withIndex("by_eventId", (q) => q.eq("eventId", eventId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listIncidentByPrepTaskId = query({
  args: { prepTaskId: v.id("prepTasks") },
  handler: async (ctx, { prepTaskId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("incidentRead", "Incident", () => (checkRole(user, "eventAccess") || checkRole(user, "kitchenAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("incidents").withIndex("by_prepTaskId", (q) => q.eq("prepTaskId", prepTaskId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listIncidentByDeliveryId = query({
  args: { deliveryId: v.id("deliveries") },
  handler: async (ctx, { deliveryId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("incidentRead", "Incident", () => (checkRole(user, "eventAccess") || checkRole(user, "kitchenAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("incidents").withIndex("by_deliveryId", (q) => q.eq("deliveryId", deliveryId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listIncidentByShiftId = query({
  args: { shiftId: v.id("shifts") },
  handler: async (ctx, { shiftId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("incidentRead", "Incident", () => (checkRole(user, "eventAccess") || checkRole(user, "kitchenAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("incidents").withIndex("by_shiftId", (q) => q.eq("shiftId", shiftId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listIngredient = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("ingredientRead", "Ingredient", () => checkRole(user, "kitchenAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("ingredients").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return (__plainRows).map((__row) => { (__row as any).isActive = ((__row as any).status === "active"); (__row as any).isDiscontinued = ((__row as any).status === "discontinued"); (__row as any).isCanonicalRoot = (((__row as any).canonicalIngredientId == null) && ((__row as any).mergedIntoIngredientId == null)); return { ...(__row as any), isActive: (__row as any).isActive, isDiscontinued: (__row as any).isDiscontinued, isCanonicalRoot: (__row as any).isCanonicalRoot }; });
  },
});

export const getIngredient = query({
  args: { id: v.id("ingredients") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("ingredientRead", "Ingredient", () => checkRole(user, "kitchenAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    (__doc as any).isActive = ((__doc as any).status === "active");
    (__doc as any).isDiscontinued = ((__doc as any).status === "discontinued");
    (__doc as any).isCanonicalRoot = (((__doc as any).canonicalIngredientId == null) && ((__doc as any).mergedIntoIngredientId == null));
    const __hydrated = { ...(__doc as any), isActive: (__doc as any).isActive, isDiscontinued: (__doc as any).isDiscontinued, isCanonicalRoot: (__doc as any).isCanonicalRoot };
    return __hydrated;
  },
});

export const listIngredientByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("ingredientRead", "Ingredient", () => checkRole(user, "kitchenAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("ingredients").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return (__plainRows).map((__row) => { (__row as any).isActive = ((__row as any).status === "active"); (__row as any).isDiscontinued = ((__row as any).status === "discontinued"); (__row as any).isCanonicalRoot = (((__row as any).canonicalIngredientId == null) && ((__row as any).mergedIntoIngredientId == null)); return { ...(__row as any), isActive: (__row as any).isActive, isDiscontinued: (__row as any).isDiscontinued, isCanonicalRoot: (__row as any).isCanonicalRoot }; });
  },
});

export const listIngredientByCanonicalIngredientId = query({
  args: { canonicalIngredientId: v.id("ingredients") },
  handler: async (ctx, { canonicalIngredientId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("ingredientRead", "Ingredient", () => checkRole(user, "kitchenAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("ingredients").withIndex("by_canonicalIngredientId", (q) => q.eq("canonicalIngredientId", canonicalIngredientId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return (__plainRows).map((__row) => { (__row as any).isActive = ((__row as any).status === "active"); (__row as any).isDiscontinued = ((__row as any).status === "discontinued"); (__row as any).isCanonicalRoot = (((__row as any).canonicalIngredientId == null) && ((__row as any).mergedIntoIngredientId == null)); return { ...(__row as any), isActive: (__row as any).isActive, isDiscontinued: (__row as any).isDiscontinued, isCanonicalRoot: (__row as any).isCanonicalRoot }; });
  },
});

export const listIngredientByMergedIntoIngredientId = query({
  args: { mergedIntoIngredientId: v.id("ingredients") },
  handler: async (ctx, { mergedIntoIngredientId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("ingredientRead", "Ingredient", () => checkRole(user, "kitchenAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("ingredients").withIndex("by_mergedIntoIngredientId", (q) => q.eq("mergedIntoIngredientId", mergedIntoIngredientId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return (__plainRows).map((__row) => { (__row as any).isActive = ((__row as any).status === "active"); (__row as any).isDiscontinued = ((__row as any).status === "discontinued"); (__row as any).isCanonicalRoot = (((__row as any).canonicalIngredientId == null) && ((__row as any).mergedIntoIngredientId == null)); return { ...(__row as any), isActive: (__row as any).isActive, isDiscontinued: (__row as any).isDiscontinued, isCanonicalRoot: (__row as any).isCanonicalRoot }; });
  },
});

export const listIngredientDemand = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("ingredientDemandRead", "IngredientDemand", () => (checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("ingredientDemands").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const getIngredientDemand = query({
  args: { id: v.id("ingredientDemands") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("ingredientDemandRead", "IngredientDemand", () => (checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    return __doc;
  },
});

export const listIngredientDemandByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("ingredientDemandRead", "IngredientDemand", () => (checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("ingredientDemands").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listIngredientDemandByPurchaseEligibleEventId = query({
  args: { purchaseEligibleEventId: v.string() },
  handler: async (ctx, { purchaseEligibleEventId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("ingredientDemandRead", "IngredientDemand", () => (checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("ingredientDemands").withIndex("by_purchaseEligibleEventId", (q) => q.eq("purchaseEligibleEventId", purchaseEligibleEventId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listIngredientDemandByEventId = query({
  args: { eventId: v.id("events") },
  handler: async (ctx, { eventId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("ingredientDemandRead", "IngredientDemand", () => (checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("ingredientDemands").withIndex("by_eventId", (q) => q.eq("eventId", eventId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listIngredientDemandByIngredientId = query({
  args: { ingredientId: v.id("ingredients") },
  handler: async (ctx, { ingredientId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("ingredientDemandRead", "IngredientDemand", () => (checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("ingredientDemands").withIndex("by_ingredientId", (q) => q.eq("ingredientId", ingredientId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listIngredientDemandByDishId = query({
  args: { dishId: v.id("dishes") },
  handler: async (ctx, { dishId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("ingredientDemandRead", "IngredientDemand", () => (checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("ingredientDemands").withIndex("by_dishId", (q) => q.eq("dishId", dishId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listIngredientPriceObservation = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("ingredientPriceObservationRead", "IngredientPriceObservation", () => ((checkRole(user, "kitchenAccess") || checkRole(user, "procurementAccess")) || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("ingredientPriceObservations").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const getIngredientPriceObservation = query({
  args: { id: v.id("ingredientPriceObservations") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("ingredientPriceObservationRead", "IngredientPriceObservation", () => ((checkRole(user, "kitchenAccess") || checkRole(user, "procurementAccess")) || checkRole(user, "manageAccess")))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    return __doc;
  },
});

export const listIngredientPriceObservationByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("ingredientPriceObservationRead", "IngredientPriceObservation", () => ((checkRole(user, "kitchenAccess") || checkRole(user, "procurementAccess")) || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("ingredientPriceObservations").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listIngredientPriceObservationByIngredientId = query({
  args: { ingredientId: v.id("ingredients") },
  handler: async (ctx, { ingredientId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("ingredientPriceObservationRead", "IngredientPriceObservation", () => ((checkRole(user, "kitchenAccess") || checkRole(user, "procurementAccess")) || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("ingredientPriceObservations").withIndex("by_ingredientId", (q) => q.eq("ingredientId", ingredientId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listIngredientPriceObservationByVendorId = query({
  args: { vendorId: v.id("vendors") },
  handler: async (ctx, { vendorId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("ingredientPriceObservationRead", "IngredientPriceObservation", () => ((checkRole(user, "kitchenAccess") || checkRole(user, "procurementAccess")) || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("ingredientPriceObservations").withIndex("by_vendorId", (q) => q.eq("vendorId", vendorId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listIngredientPriceObservationByVendorOrderLineId = query({
  args: { vendorOrderLineId: v.id("vendorOrderLines") },
  handler: async (ctx, { vendorOrderLineId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("ingredientPriceObservationRead", "IngredientPriceObservation", () => ((checkRole(user, "kitchenAccess") || checkRole(user, "procurementAccess")) || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("ingredientPriceObservations").withIndex("by_vendorOrderLineId", (q) => q.eq("vendorOrderLineId", vendorOrderLineId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listIngredientPriceObservationByVendorOrderId = query({
  args: { vendorOrderId: v.id("vendorOrders") },
  handler: async (ctx, { vendorOrderId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("ingredientPriceObservationRead", "IngredientPriceObservation", () => ((checkRole(user, "kitchenAccess") || checkRole(user, "procurementAccess")) || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("ingredientPriceObservations").withIndex("by_vendorOrderId", (q) => q.eq("vendorOrderId", vendorOrderId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listIntegrationConnection = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("integrationConnectionRead", "IntegrationConnection", () => checkRole(user, "manageAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("integrationConnections").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return (__plainRows).map((__row) => { (__row as any).isUsable = (((__row as any).status === "connected") && ((__row as any).deletedAt == null)); (__row as any).canAcceptCharges = ((((__row as any).status === "connected") && (__row as any).chargesEnabled) && ((__row as any).deletedAt == null)); return { ...(__row as any), isUsable: (__row as any).isUsable, canAcceptCharges: (__row as any).canAcceptCharges }; });
  },
});

export const getIntegrationConnection = query({
  args: { id: v.id("integrationConnections") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("integrationConnectionRead", "IntegrationConnection", () => checkRole(user, "manageAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    (__doc as any).isUsable = (((__doc as any).status === "connected") && ((__doc as any).deletedAt == null));
    (__doc as any).canAcceptCharges = ((((__doc as any).status === "connected") && (__doc as any).chargesEnabled) && ((__doc as any).deletedAt == null));
    const __hydrated = { ...(__doc as any), isUsable: (__doc as any).isUsable, canAcceptCharges: (__doc as any).canAcceptCharges };
    return __hydrated;
  },
});

export const listIntegrationConnectionByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("integrationConnectionRead", "IntegrationConnection", () => checkRole(user, "manageAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("integrationConnections").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return (__plainRows).map((__row) => { (__row as any).isUsable = (((__row as any).status === "connected") && ((__row as any).deletedAt == null)); (__row as any).canAcceptCharges = ((((__row as any).status === "connected") && (__row as any).chargesEnabled) && ((__row as any).deletedAt == null)); return { ...(__row as any), isUsable: (__row as any).isUsable, canAcceptCharges: (__row as any).canAcceptCharges }; });
  },
});

export const listInterview = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("interviewRead", "Interview", () => checkRole(user, "workforceManageAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("interviews").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const getInterview = query({
  args: { id: v.id("interviews") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("interviewRead", "Interview", () => checkRole(user, "workforceManageAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    return __doc;
  },
});

export const listInterviewByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("interviewRead", "Interview", () => checkRole(user, "workforceManageAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("interviews").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listInterviewByCandidateId = query({
  args: { candidateId: v.id("candidates") },
  handler: async (ctx, { candidateId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("interviewRead", "Interview", () => checkRole(user, "workforceManageAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("interviews").withIndex("by_candidateId", (q) => q.eq("candidateId", candidateId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listInterviewByInterviewerPersonId = query({
  args: { interviewerPersonId: v.id("people") },
  handler: async (ctx, { interviewerPersonId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("interviewRead", "Interview", () => checkRole(user, "workforceManageAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("interviews").withIndex("by_interviewerPersonId", (q) => q.eq("interviewerPersonId", interviewerPersonId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listInventoryItem = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("inventoryItemRead", "InventoryItem", () => checkRole(user, "inventoryAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("inventoryItems").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    const __projectedRows: any[] = [];
    for (const __row of __plainRows as any[]) {
      (__row as any).reservations = await ctx.db.query("inventoryReservations").withIndex("by_inventoryItemId", (q: any) => q.eq("inventoryItemId", __row._id)).collect();
      (__row as any).isBelowPar = (((__row as any).parLevel > 0) && ((__row as any).quantityOnHand < (__row as any).parLevel));
      (__row as any).isBelowReorder = (((__row as any).reorderThreshold > 0) && ((__row as any).quantityOnHand < (__row as any).reorderThreshold));
      (__row as any).inventoryValue = ((__row as any).quantityOnHand * (__row as any).unitCost);
      (__row as any).totalReserved = (((__row as any).reservations) ?? []).map((r: Doc<"inventoryReservations">) => (((r.status === "active") ? r.quantity : 0))).reduce((acc: number, v: unknown) => acc + (typeof v === "number" ? v : 0), 0);
      (__row as any).availableQuantity = ((__row as any).quantityOnHand - (__row as any).totalReserved);
      __projectedRows.push({ ...(__row as any), isBelowPar: (__row as any).isBelowPar, isBelowReorder: (__row as any).isBelowReorder, inventoryValue: (__row as any).inventoryValue, totalReserved: (__row as any).totalReserved, availableQuantity: (__row as any).availableQuantity });
    }
    return __projectedRows;
  },
});

export const getInventoryItem = query({
  args: { id: v.id("inventoryItems") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("inventoryItemRead", "InventoryItem", () => checkRole(user, "inventoryAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    (__doc as any).reservations = await ctx.db.query("inventoryReservations").withIndex("by_inventoryItemId", (q: any) => q.eq("inventoryItemId", __doc._id)).collect();
    (__doc as any).isBelowPar = (((__doc as any).parLevel > 0) && ((__doc as any).quantityOnHand < (__doc as any).parLevel));
    (__doc as any).isBelowReorder = (((__doc as any).reorderThreshold > 0) && ((__doc as any).quantityOnHand < (__doc as any).reorderThreshold));
    (__doc as any).inventoryValue = ((__doc as any).quantityOnHand * (__doc as any).unitCost);
    (__doc as any).totalReserved = (((__doc as any).reservations) ?? []).map((r: Doc<"inventoryReservations">) => (((r.status === "active") ? r.quantity : 0))).reduce((acc: number, v: unknown) => acc + (typeof v === "number" ? v : 0), 0);
    (__doc as any).availableQuantity = ((__doc as any).quantityOnHand - (__doc as any).totalReserved);
    const __hydrated = { ...(__doc as any), isBelowPar: (__doc as any).isBelowPar, isBelowReorder: (__doc as any).isBelowReorder, inventoryValue: (__doc as any).inventoryValue, totalReserved: (__doc as any).totalReserved, availableQuantity: (__doc as any).availableQuantity };
    return __hydrated;
  },
});

export const listInventoryItemByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("inventoryItemRead", "InventoryItem", () => checkRole(user, "inventoryAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("inventoryItems").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    const __projectedRows: any[] = [];
    for (const __row of __plainRows as any[]) {
      (__row as any).reservations = await ctx.db.query("inventoryReservations").withIndex("by_inventoryItemId", (q: any) => q.eq("inventoryItemId", __row._id)).collect();
      (__row as any).isBelowPar = (((__row as any).parLevel > 0) && ((__row as any).quantityOnHand < (__row as any).parLevel));
      (__row as any).isBelowReorder = (((__row as any).reorderThreshold > 0) && ((__row as any).quantityOnHand < (__row as any).reorderThreshold));
      (__row as any).inventoryValue = ((__row as any).quantityOnHand * (__row as any).unitCost);
      (__row as any).totalReserved = (((__row as any).reservations) ?? []).map((r: Doc<"inventoryReservations">) => (((r.status === "active") ? r.quantity : 0))).reduce((acc: number, v: unknown) => acc + (typeof v === "number" ? v : 0), 0);
      (__row as any).availableQuantity = ((__row as any).quantityOnHand - (__row as any).totalReserved);
      __projectedRows.push({ ...(__row as any), isBelowPar: (__row as any).isBelowPar, isBelowReorder: (__row as any).isBelowReorder, inventoryValue: (__row as any).inventoryValue, totalReserved: (__row as any).totalReserved, availableQuantity: (__row as any).availableQuantity });
    }
    return __projectedRows;
  },
});

export const listInventoryItemByIngredientId = query({
  args: { ingredientId: v.id("ingredients") },
  handler: async (ctx, { ingredientId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("inventoryItemRead", "InventoryItem", () => checkRole(user, "inventoryAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("inventoryItems").withIndex("by_ingredientId", (q) => q.eq("ingredientId", ingredientId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    const __projectedRows: any[] = [];
    for (const __row of __plainRows as any[]) {
      (__row as any).reservations = await ctx.db.query("inventoryReservations").withIndex("by_inventoryItemId", (q: any) => q.eq("inventoryItemId", __row._id)).collect();
      (__row as any).isBelowPar = (((__row as any).parLevel > 0) && ((__row as any).quantityOnHand < (__row as any).parLevel));
      (__row as any).isBelowReorder = (((__row as any).reorderThreshold > 0) && ((__row as any).quantityOnHand < (__row as any).reorderThreshold));
      (__row as any).inventoryValue = ((__row as any).quantityOnHand * (__row as any).unitCost);
      (__row as any).totalReserved = (((__row as any).reservations) ?? []).map((r: Doc<"inventoryReservations">) => (((r.status === "active") ? r.quantity : 0))).reduce((acc: number, v: unknown) => acc + (typeof v === "number" ? v : 0), 0);
      (__row as any).availableQuantity = ((__row as any).quantityOnHand - (__row as any).totalReserved);
      __projectedRows.push({ ...(__row as any), isBelowPar: (__row as any).isBelowPar, isBelowReorder: (__row as any).isBelowReorder, inventoryValue: (__row as any).inventoryValue, totalReserved: (__row as any).totalReserved, availableQuantity: (__row as any).availableQuantity });
    }
    return __projectedRows;
  },
});

export const listInventoryItemByLocationId = query({
  args: { locationId: v.id("storageLocations") },
  handler: async (ctx, { locationId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("inventoryItemRead", "InventoryItem", () => checkRole(user, "inventoryAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("inventoryItems").withIndex("by_locationId", (q) => q.eq("locationId", locationId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    const __projectedRows: any[] = [];
    for (const __row of __plainRows as any[]) {
      (__row as any).reservations = await ctx.db.query("inventoryReservations").withIndex("by_inventoryItemId", (q: any) => q.eq("inventoryItemId", __row._id)).collect();
      (__row as any).isBelowPar = (((__row as any).parLevel > 0) && ((__row as any).quantityOnHand < (__row as any).parLevel));
      (__row as any).isBelowReorder = (((__row as any).reorderThreshold > 0) && ((__row as any).quantityOnHand < (__row as any).reorderThreshold));
      (__row as any).inventoryValue = ((__row as any).quantityOnHand * (__row as any).unitCost);
      (__row as any).totalReserved = (((__row as any).reservations) ?? []).map((r: Doc<"inventoryReservations">) => (((r.status === "active") ? r.quantity : 0))).reduce((acc: number, v: unknown) => acc + (typeof v === "number" ? v : 0), 0);
      (__row as any).availableQuantity = ((__row as any).quantityOnHand - (__row as any).totalReserved);
      __projectedRows.push({ ...(__row as any), isBelowPar: (__row as any).isBelowPar, isBelowReorder: (__row as any).isBelowReorder, inventoryValue: (__row as any).inventoryValue, totalReserved: (__row as any).totalReserved, availableQuantity: (__row as any).availableQuantity });
    }
    return __projectedRows;
  },
});

export const listInventoryLot = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("inventoryLotRead", "InventoryLot", () => ((checkRole(user, "inventoryAccess") || checkRole(user, "procurementAccess")) || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("inventoryLots").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const getInventoryLot = query({
  args: { id: v.id("inventoryLots") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("inventoryLotRead", "InventoryLot", () => ((checkRole(user, "inventoryAccess") || checkRole(user, "procurementAccess")) || checkRole(user, "manageAccess")))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    return __doc;
  },
});

export const listInventoryLotByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("inventoryLotRead", "InventoryLot", () => ((checkRole(user, "inventoryAccess") || checkRole(user, "procurementAccess")) || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("inventoryLots").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listInventoryLotBySupplierLotNumber = query({
  args: { supplierLotNumber: v.string() },
  handler: async (ctx, { supplierLotNumber }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("inventoryLotRead", "InventoryLot", () => ((checkRole(user, "inventoryAccess") || checkRole(user, "procurementAccess")) || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("inventoryLots").withIndex("by_supplierLotNumber", (q) => q.eq("supplierLotNumber", supplierLotNumber)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listInventoryLotByVendorOrderLineId = query({
  args: { vendorOrderLineId: v.id("vendorOrderLines") },
  handler: async (ctx, { vendorOrderLineId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("inventoryLotRead", "InventoryLot", () => ((checkRole(user, "inventoryAccess") || checkRole(user, "procurementAccess")) || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("inventoryLots").withIndex("by_vendorOrderLineId", (q) => q.eq("vendorOrderLineId", vendorOrderLineId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listInventoryLotByVendorId = query({
  args: { vendorId: v.id("vendors") },
  handler: async (ctx, { vendorId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("inventoryLotRead", "InventoryLot", () => ((checkRole(user, "inventoryAccess") || checkRole(user, "procurementAccess")) || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("inventoryLots").withIndex("by_vendorId", (q) => q.eq("vendorId", vendorId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listInventoryLotByIngredientId = query({
  args: { ingredientId: v.id("ingredients") },
  handler: async (ctx, { ingredientId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("inventoryLotRead", "InventoryLot", () => ((checkRole(user, "inventoryAccess") || checkRole(user, "procurementAccess")) || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("inventoryLots").withIndex("by_ingredientId", (q) => q.eq("ingredientId", ingredientId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listInventoryLotByLocationId = query({
  args: { locationId: v.id("storageLocations") },
  handler: async (ctx, { locationId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("inventoryLotRead", "InventoryLot", () => ((checkRole(user, "inventoryAccess") || checkRole(user, "procurementAccess")) || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("inventoryLots").withIndex("by_locationId", (q) => q.eq("locationId", locationId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listInventoryLotByVendorOrderId = query({
  args: { vendorOrderId: v.id("vendorOrders") },
  handler: async (ctx, { vendorOrderId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("inventoryLotRead", "InventoryLot", () => ((checkRole(user, "inventoryAccess") || checkRole(user, "procurementAccess")) || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("inventoryLots").withIndex("by_vendorOrderId", (q) => q.eq("vendorOrderId", vendorOrderId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listInventoryLotByIngredientDemandId = query({
  args: { ingredientDemandId: v.id("ingredientDemands") },
  handler: async (ctx, { ingredientDemandId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("inventoryLotRead", "InventoryLot", () => ((checkRole(user, "inventoryAccess") || checkRole(user, "procurementAccess")) || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("inventoryLots").withIndex("by_ingredientDemandId", (q) => q.eq("ingredientDemandId", ingredientDemandId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listInventoryLotByEventId = query({
  args: { eventId: v.id("events") },
  handler: async (ctx, { eventId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("inventoryLotRead", "InventoryLot", () => ((checkRole(user, "inventoryAccess") || checkRole(user, "procurementAccess")) || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("inventoryLots").withIndex("by_eventId", (q) => q.eq("eventId", eventId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listInventoryReservation = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("inventoryReservationRead", "InventoryReservation", () => (checkRole(user, "inventoryAccess") || checkRole(user, "eventManageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("inventoryReservations").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const getInventoryReservation = query({
  args: { id: v.id("inventoryReservations") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("inventoryReservationRead", "InventoryReservation", () => (checkRole(user, "inventoryAccess") || checkRole(user, "eventManageAccess")))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    return __doc;
  },
});

export const listInventoryReservationByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("inventoryReservationRead", "InventoryReservation", () => (checkRole(user, "inventoryAccess") || checkRole(user, "eventManageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("inventoryReservations").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listInventoryReservationByInventoryLotId = query({
  args: { inventoryLotId: v.id("inventoryLots") },
  handler: async (ctx, { inventoryLotId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("inventoryReservationRead", "InventoryReservation", () => (checkRole(user, "inventoryAccess") || checkRole(user, "eventManageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("inventoryReservations").withIndex("by_inventoryLotId", (q) => q.eq("inventoryLotId", inventoryLotId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listInventoryReservationByInventoryItemId = query({
  args: { inventoryItemId: v.id("inventoryItems") },
  handler: async (ctx, { inventoryItemId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("inventoryReservationRead", "InventoryReservation", () => (checkRole(user, "inventoryAccess") || checkRole(user, "eventManageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("inventoryReservations").withIndex("by_inventoryItemId", (q) => q.eq("inventoryItemId", inventoryItemId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listInventoryReservationByEventId = query({
  args: { eventId: v.id("events") },
  handler: async (ctx, { eventId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("inventoryReservationRead", "InventoryReservation", () => (checkRole(user, "inventoryAccess") || checkRole(user, "eventManageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("inventoryReservations").withIndex("by_eventId", (q) => q.eq("eventId", eventId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listInventoryReservationByIngredientId = query({
  args: { ingredientId: v.id("ingredients") },
  handler: async (ctx, { ingredientId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("inventoryReservationRead", "InventoryReservation", () => (checkRole(user, "inventoryAccess") || checkRole(user, "eventManageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("inventoryReservations").withIndex("by_ingredientId", (q) => q.eq("ingredientId", ingredientId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listInvoice = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("invoiceRead", "Invoice", () => (checkRole(user, "financeAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("invoices").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return (__plainRows).map((__row) => { (__row as any).invoiceCurrencyCode = ((((__row as any).currencyCode != null) && ((((__row as any).currencyCode).trim()).length === 3)) ? (__row as any).currencyCode : "USD"); (__row as any).safeExchangeRate = ((((__row as any).exchangeRate != null) && ((__row as any).exchangeRate > 0)) ? (__row as any).exchangeRate : 1); (__row as any).functionalCurrencyTotal = Math.round(((__row as any).total * (__row as any).safeExchangeRate)); (__row as any).functionalCurrencyAmountDue = Math.round(((__row as any).amountDue * (__row as any).safeExchangeRate)); return { ...(__row as any), invoiceCurrencyCode: (__row as any).invoiceCurrencyCode, safeExchangeRate: (__row as any).safeExchangeRate, functionalCurrencyTotal: (__row as any).functionalCurrencyTotal, functionalCurrencyAmountDue: (__row as any).functionalCurrencyAmountDue }; });
  },
});

export const getInvoice = query({
  args: { id: v.id("invoices") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("invoiceRead", "Invoice", () => (checkRole(user, "financeAccess") || checkRole(user, "manageAccess")))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    (__doc as any).invoiceCurrencyCode = ((((__doc as any).currencyCode != null) && ((((__doc as any).currencyCode).trim()).length === 3)) ? (__doc as any).currencyCode : "USD");
    (__doc as any).safeExchangeRate = ((((__doc as any).exchangeRate != null) && ((__doc as any).exchangeRate > 0)) ? (__doc as any).exchangeRate : 1);
    (__doc as any).functionalCurrencyTotal = Math.round(((__doc as any).total * (__doc as any).safeExchangeRate));
    (__doc as any).functionalCurrencyAmountDue = Math.round(((__doc as any).amountDue * (__doc as any).safeExchangeRate));
    const __hydrated = { ...(__doc as any), invoiceCurrencyCode: (__doc as any).invoiceCurrencyCode, safeExchangeRate: (__doc as any).safeExchangeRate, functionalCurrencyTotal: (__doc as any).functionalCurrencyTotal, functionalCurrencyAmountDue: (__doc as any).functionalCurrencyAmountDue };
    return __hydrated;
  },
});

export const listInvoiceByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("invoiceRead", "Invoice", () => (checkRole(user, "financeAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("invoices").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return (__plainRows).map((__row) => { (__row as any).invoiceCurrencyCode = ((((__row as any).currencyCode != null) && ((((__row as any).currencyCode).trim()).length === 3)) ? (__row as any).currencyCode : "USD"); (__row as any).safeExchangeRate = ((((__row as any).exchangeRate != null) && ((__row as any).exchangeRate > 0)) ? (__row as any).exchangeRate : 1); (__row as any).functionalCurrencyTotal = Math.round(((__row as any).total * (__row as any).safeExchangeRate)); (__row as any).functionalCurrencyAmountDue = Math.round(((__row as any).amountDue * (__row as any).safeExchangeRate)); return { ...(__row as any), invoiceCurrencyCode: (__row as any).invoiceCurrencyCode, safeExchangeRate: (__row as any).safeExchangeRate, functionalCurrencyTotal: (__row as any).functionalCurrencyTotal, functionalCurrencyAmountDue: (__row as any).functionalCurrencyAmountDue }; });
  },
});

export const listInvoiceByClientId = query({
  args: { clientId: v.id("clients") },
  handler: async (ctx, { clientId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("invoiceRead", "Invoice", () => (checkRole(user, "financeAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("invoices").withIndex("by_clientId", (q) => q.eq("clientId", clientId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return (__plainRows).map((__row) => { (__row as any).invoiceCurrencyCode = ((((__row as any).currencyCode != null) && ((((__row as any).currencyCode).trim()).length === 3)) ? (__row as any).currencyCode : "USD"); (__row as any).safeExchangeRate = ((((__row as any).exchangeRate != null) && ((__row as any).exchangeRate > 0)) ? (__row as any).exchangeRate : 1); (__row as any).functionalCurrencyTotal = Math.round(((__row as any).total * (__row as any).safeExchangeRate)); (__row as any).functionalCurrencyAmountDue = Math.round(((__row as any).amountDue * (__row as any).safeExchangeRate)); return { ...(__row as any), invoiceCurrencyCode: (__row as any).invoiceCurrencyCode, safeExchangeRate: (__row as any).safeExchangeRate, functionalCurrencyTotal: (__row as any).functionalCurrencyTotal, functionalCurrencyAmountDue: (__row as any).functionalCurrencyAmountDue }; });
  },
});

export const listInvoiceByClientMergeAuthorizationId = query({
  args: { clientMergeAuthorizationId: v.id("clientMerges") },
  handler: async (ctx, { clientMergeAuthorizationId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("invoiceRead", "Invoice", () => (checkRole(user, "financeAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("invoices").withIndex("by_clientMergeAuthorizationId", (q) => q.eq("clientMergeAuthorizationId", clientMergeAuthorizationId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return (__plainRows).map((__row) => { (__row as any).invoiceCurrencyCode = ((((__row as any).currencyCode != null) && ((((__row as any).currencyCode).trim()).length === 3)) ? (__row as any).currencyCode : "USD"); (__row as any).safeExchangeRate = ((((__row as any).exchangeRate != null) && ((__row as any).exchangeRate > 0)) ? (__row as any).exchangeRate : 1); (__row as any).functionalCurrencyTotal = Math.round(((__row as any).total * (__row as any).safeExchangeRate)); (__row as any).functionalCurrencyAmountDue = Math.round(((__row as any).amountDue * (__row as any).safeExchangeRate)); return { ...(__row as any), invoiceCurrencyCode: (__row as any).invoiceCurrencyCode, safeExchangeRate: (__row as any).safeExchangeRate, functionalCurrencyTotal: (__row as any).functionalCurrencyTotal, functionalCurrencyAmountDue: (__row as any).functionalCurrencyAmountDue }; });
  },
});

export const listInvoiceByMergeTargetClientId = query({
  args: { mergeTargetClientId: v.id("clients") },
  handler: async (ctx, { mergeTargetClientId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("invoiceRead", "Invoice", () => (checkRole(user, "financeAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("invoices").withIndex("by_mergeTargetClientId", (q) => q.eq("mergeTargetClientId", mergeTargetClientId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return (__plainRows).map((__row) => { (__row as any).invoiceCurrencyCode = ((((__row as any).currencyCode != null) && ((((__row as any).currencyCode).trim()).length === 3)) ? (__row as any).currencyCode : "USD"); (__row as any).safeExchangeRate = ((((__row as any).exchangeRate != null) && ((__row as any).exchangeRate > 0)) ? (__row as any).exchangeRate : 1); (__row as any).functionalCurrencyTotal = Math.round(((__row as any).total * (__row as any).safeExchangeRate)); (__row as any).functionalCurrencyAmountDue = Math.round(((__row as any).amountDue * (__row as any).safeExchangeRate)); return { ...(__row as any), invoiceCurrencyCode: (__row as any).invoiceCurrencyCode, safeExchangeRate: (__row as any).safeExchangeRate, functionalCurrencyTotal: (__row as any).functionalCurrencyTotal, functionalCurrencyAmountDue: (__row as any).functionalCurrencyAmountDue }; });
  },
});

export const listInvoiceByEventId = query({
  args: { eventId: v.id("events") },
  handler: async (ctx, { eventId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("invoiceRead", "Invoice", () => (checkRole(user, "financeAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("invoices").withIndex("by_eventId", (q) => q.eq("eventId", eventId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return (__plainRows).map((__row) => { (__row as any).invoiceCurrencyCode = ((((__row as any).currencyCode != null) && ((((__row as any).currencyCode).trim()).length === 3)) ? (__row as any).currencyCode : "USD"); (__row as any).safeExchangeRate = ((((__row as any).exchangeRate != null) && ((__row as any).exchangeRate > 0)) ? (__row as any).exchangeRate : 1); (__row as any).functionalCurrencyTotal = Math.round(((__row as any).total * (__row as any).safeExchangeRate)); (__row as any).functionalCurrencyAmountDue = Math.round(((__row as any).amountDue * (__row as any).safeExchangeRate)); return { ...(__row as any), invoiceCurrencyCode: (__row as any).invoiceCurrencyCode, safeExchangeRate: (__row as any).safeExchangeRate, functionalCurrencyTotal: (__row as any).functionalCurrencyTotal, functionalCurrencyAmountDue: (__row as any).functionalCurrencyAmountDue }; });
  },
});

export const listLead = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("leadRead", "Lead", () => checkRole(user, "salesAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("leads").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "Lead", ["email","phone"], row)));
    return (__plainRows).map((__row) => { (__row as any).displayName = (((__row as any).leadType === "company") ? (__row as any).companyName : (((__row as any).givenName + " ") + (__row as any).familyName)); return { ...(__row as any), displayName: (__row as any).displayName }; });
  },
});

export const getLead = query({
  args: { id: v.id("leads") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("leadRead", "Lead", () => checkRole(user, "salesAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = await __decryptDoc(ctx, "Lead", ["email","phone"], __rawDoc);
    (__doc as any).displayName = (((__doc as any).leadType === "company") ? (__doc as any).companyName : (((__doc as any).givenName + " ") + (__doc as any).familyName));
    const __hydrated = { ...(__doc as any), displayName: (__doc as any).displayName };
    return __hydrated;
  },
});

export const listLeadByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("leadRead", "Lead", () => checkRole(user, "salesAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("leads").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "Lead", ["email","phone"], row)));
    return (__plainRows).map((__row) => { (__row as any).displayName = (((__row as any).leadType === "company") ? (__row as any).companyName : (((__row as any).givenName + " ") + (__row as any).familyName)); return { ...(__row as any), displayName: (__row as any).displayName }; });
  },
});

export const listLeadByClientId = query({
  args: { clientId: v.id("clients") },
  handler: async (ctx, { clientId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("leadRead", "Lead", () => checkRole(user, "salesAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("leads").withIndex("by_clientId", (q) => q.eq("clientId", clientId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "Lead", ["email","phone"], row)));
    return (__plainRows).map((__row) => { (__row as any).displayName = (((__row as any).leadType === "company") ? (__row as any).companyName : (((__row as any).givenName + " ") + (__row as any).familyName)); return { ...(__row as any), displayName: (__row as any).displayName }; });
  },
});

export const listLeadByClientContactId = query({
  args: { clientContactId: v.id("clientContacts") },
  handler: async (ctx, { clientContactId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("leadRead", "Lead", () => checkRole(user, "salesAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("leads").withIndex("by_clientContactId", (q) => q.eq("clientContactId", clientContactId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "Lead", ["email","phone"], row)));
    return (__plainRows).map((__row) => { (__row as any).displayName = (((__row as any).leadType === "company") ? (__row as any).companyName : (((__row as any).givenName + " ") + (__row as any).familyName)); return { ...(__row as any), displayName: (__row as any).displayName }; });
  },
});

export const listLeadByProposalId = query({
  args: { proposalId: v.id("proposals") },
  handler: async (ctx, { proposalId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("leadRead", "Lead", () => checkRole(user, "salesAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("leads").withIndex("by_proposalId", (q) => q.eq("proposalId", proposalId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "Lead", ["email","phone"], row)));
    return (__plainRows).map((__row) => { (__row as any).displayName = (((__row as any).leadType === "company") ? (__row as any).companyName : (((__row as any).givenName + " ") + (__row as any).familyName)); return { ...(__row as any), displayName: (__row as any).displayName }; });
  },
});

export const listLeadByReferralSourceId = query({
  args: { referralSourceId: v.id("referralSources") },
  handler: async (ctx, { referralSourceId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("leadRead", "Lead", () => checkRole(user, "salesAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("leads").withIndex("by_referralSourceId", (q) => q.eq("referralSourceId", referralSourceId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "Lead", ["email","phone"], row)));
    return (__plainRows).map((__row) => { (__row as any).displayName = (((__row as any).leadType === "company") ? (__row as any).companyName : (((__row as any).givenName + " ") + (__row as any).familyName)); return { ...(__row as any), displayName: (__row as any).displayName }; });
  },
});

export const listMenu = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("menuRead", "Menu", () => (checkRole(user, "kitchenAccess") || checkRole(user, "salesAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("menus").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return (__plainRows).map((__row) => { (__row as any).isPublished = ((__row as any).status === "published"); (__row as any).isEditable = ((__row as any).status === "draft"); return { ...(__row as any), isPublished: (__row as any).isPublished, isEditable: (__row as any).isEditable }; });
  },
});

export const getMenu = query({
  args: { id: v.id("menus") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("menuRead", "Menu", () => (checkRole(user, "kitchenAccess") || checkRole(user, "salesAccess")))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    (__doc as any).isPublished = ((__doc as any).status === "published");
    (__doc as any).isEditable = ((__doc as any).status === "draft");
    const __hydrated = { ...(__doc as any), isPublished: (__doc as any).isPublished, isEditable: (__doc as any).isEditable };
    return __hydrated;
  },
});

export const listMenuByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("menuRead", "Menu", () => (checkRole(user, "kitchenAccess") || checkRole(user, "salesAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("menus").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return (__plainRows).map((__row) => { (__row as any).isPublished = ((__row as any).status === "published"); (__row as any).isEditable = ((__row as any).status === "draft"); return { ...(__row as any), isPublished: (__row as any).isPublished, isEditable: (__row as any).isEditable }; });
  },
});

export const listMenuDish = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("menuDishRead", "MenuDish", () => (checkRole(user, "kitchenAccess") || checkRole(user, "salesAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("menuDishes").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const getMenuDish = query({
  args: { id: v.id("menuDishes") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("menuDishRead", "MenuDish", () => (checkRole(user, "kitchenAccess") || checkRole(user, "salesAccess")))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    return __doc;
  },
});

export const listMenuDishByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("menuDishRead", "MenuDish", () => (checkRole(user, "kitchenAccess") || checkRole(user, "salesAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("menuDishes").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listMenuDishByMenuId = query({
  args: { menuId: v.id("menus") },
  handler: async (ctx, { menuId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("menuDishRead", "MenuDish", () => (checkRole(user, "kitchenAccess") || checkRole(user, "salesAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("menuDishes").withIndex("by_menuId", (q) => q.eq("menuId", menuId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listMenuDishByDishId = query({
  args: { dishId: v.id("dishes") },
  handler: async (ctx, { dishId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("menuDishRead", "MenuDish", () => (checkRole(user, "kitchenAccess") || checkRole(user, "salesAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("menuDishes").withIndex("by_dishId", (q) => q.eq("dishId", dishId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listMessage = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("messageRead", "Message", () => checkRole(user, "staffAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("messages").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const getMessage = query({
  args: { id: v.id("messages") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("messageRead", "Message", () => checkRole(user, "staffAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    return __doc;
  },
});

export const listMessageByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("messageRead", "Message", () => checkRole(user, "staffAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("messages").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listMessageByThreadId = query({
  args: { threadId: v.id("messageThreads") },
  handler: async (ctx, { threadId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("messageRead", "Message", () => checkRole(user, "staffAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("messages").withIndex("by_threadId", (q) => q.eq("threadId", threadId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listMessageByProviderMessageId = query({
  args: { providerMessageId: v.string() },
  handler: async (ctx, { providerMessageId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("messageRead", "Message", () => checkRole(user, "staffAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("messages").withIndex("by_providerMessageId", (q) => q.eq("providerMessageId", providerMessageId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listMessageThread = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("messageThreadRead", "MessageThread", () => checkRole(user, "staffAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("messageThreads").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const getMessageThread = query({
  args: { id: v.id("messageThreads") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("messageThreadRead", "MessageThread", () => checkRole(user, "staffAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    return __doc;
  },
});

export const listMessageThreadByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("messageThreadRead", "MessageThread", () => checkRole(user, "staffAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("messageThreads").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listMessageThreadByProviderAccountId = query({
  args: { providerAccountId: v.string() },
  handler: async (ctx, { providerAccountId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("messageThreadRead", "MessageThread", () => checkRole(user, "staffAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("messageThreads").withIndex("by_providerAccountId", (q) => q.eq("providerAccountId", providerAccountId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listMessageThreadByProviderThreadId = query({
  args: { providerThreadId: v.string() },
  handler: async (ctx, { providerThreadId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("messageThreadRead", "MessageThread", () => checkRole(user, "staffAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("messageThreads").withIndex("by_providerThreadId", (q) => q.eq("providerThreadId", providerThreadId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listMessageThreadByContactId = query({
  args: { contactId: v.id("clientContacts") },
  handler: async (ctx, { contactId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("messageThreadRead", "MessageThread", () => checkRole(user, "staffAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("messageThreads").withIndex("by_contactId", (q) => q.eq("contactId", contactId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listMessageThreadByLeadId = query({
  args: { leadId: v.id("leads") },
  handler: async (ctx, { leadId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("messageThreadRead", "MessageThread", () => checkRole(user, "staffAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("messageThreads").withIndex("by_leadId", (q) => q.eq("leadId", leadId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listOccasion = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("occasionRead", "Occasion", () => (checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("occasions").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return (__plainRows).map((__row) => { (__row as any).isActive = ((__row as any).status === "active"); return { ...(__row as any), isActive: (__row as any).isActive }; });
  },
});

export const getOccasion = query({
  args: { id: v.id("occasions") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("occasionRead", "Occasion", () => (checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    (__doc as any).isActive = ((__doc as any).status === "active");
    const __hydrated = { ...(__doc as any), isActive: (__doc as any).isActive };
    return __hydrated;
  },
});

export const listOccasionByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("occasionRead", "Occasion", () => (checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("occasions").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return (__plainRows).map((__row) => { (__row as any).isActive = ((__row as any).status === "active"); return { ...(__row as any), isActive: (__row as any).isActive }; });
  },
});

export const listOneOnOne = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("oneOnOneRead", "OneOnOne", () => checkRole(user, "workforceManageAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("oneOnOnes").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const getOneOnOne = query({
  args: { id: v.id("oneOnOnes") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("oneOnOneRead", "OneOnOne", () => checkRole(user, "workforceManageAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    return __doc;
  },
});

export const listOneOnOneByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("oneOnOneRead", "OneOnOne", () => checkRole(user, "workforceManageAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("oneOnOnes").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listOneOnOneByLeadPersonId = query({
  args: { leadPersonId: v.id("people") },
  handler: async (ctx, { leadPersonId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("oneOnOneRead", "OneOnOne", () => checkRole(user, "workforceManageAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("oneOnOnes").withIndex("by_leadPersonId", (q) => q.eq("leadPersonId", leadPersonId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listOneOnOneByStaffMemberId = query({
  args: { staffMemberId: v.id("people") },
  handler: async (ctx, { staffMemberId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("oneOnOneRead", "OneOnOne", () => checkRole(user, "workforceManageAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("oneOnOnes").withIndex("by_staffMemberId", (q) => q.eq("staffMemberId", staffMemberId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listOneOnOneAction = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("oneOnOneActionRead", "OneOnOneAction", () => checkRole(user, "workforceManageAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("oneOnOneActions").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const getOneOnOneAction = query({
  args: { id: v.id("oneOnOneActions") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("oneOnOneActionRead", "OneOnOneAction", () => checkRole(user, "workforceManageAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    return __doc;
  },
});

export const listOneOnOneActionByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("oneOnOneActionRead", "OneOnOneAction", () => checkRole(user, "workforceManageAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("oneOnOneActions").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listOneOnOneActionByOneOnOneId = query({
  args: { oneOnOneId: v.id("oneOnOnes") },
  handler: async (ctx, { oneOnOneId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("oneOnOneActionRead", "OneOnOneAction", () => checkRole(user, "workforceManageAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("oneOnOneActions").withIndex("by_oneOnOneId", (q) => q.eq("oneOnOneId", oneOnOneId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listOneOnOneActionByOwnerPersonId = query({
  args: { ownerPersonId: v.id("people") },
  handler: async (ctx, { ownerPersonId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("oneOnOneActionRead", "OneOnOneAction", () => checkRole(user, "workforceManageAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("oneOnOneActions").withIndex("by_ownerPersonId", (q) => q.eq("ownerPersonId", ownerPersonId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listOrganization = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("organizationRead", "Organization", () => checkRole(user, "staffAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("organizations").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return (__plainRows).map((__row) => { (__row as any).isOperable = (((__row as any).status === "active") && ((__row as any).deletedAt == null)); (__row as any).functionalCurrencyCode = ((((__row as any).defaultCurrencyCode != null) && ((((__row as any).defaultCurrencyCode).trim()).length === 3)) ? (__row as any).defaultCurrencyCode : "USD"); return { ...(__row as any), isOperable: (__row as any).isOperable, functionalCurrencyCode: (__row as any).functionalCurrencyCode }; });
  },
});

export const getOrganization = query({
  args: { id: v.id("organizations") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("organizationRead", "Organization", () => checkRole(user, "staffAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    (__doc as any).isOperable = (((__doc as any).status === "active") && ((__doc as any).deletedAt == null));
    (__doc as any).functionalCurrencyCode = ((((__doc as any).defaultCurrencyCode != null) && ((((__doc as any).defaultCurrencyCode).trim()).length === 3)) ? (__doc as any).defaultCurrencyCode : "USD");
    const __hydrated = { ...(__doc as any), isOperable: (__doc as any).isOperable, functionalCurrencyCode: (__doc as any).functionalCurrencyCode };
    return __hydrated;
  },
});

export const listOrganizationByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("organizationRead", "Organization", () => checkRole(user, "staffAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("organizations").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return (__plainRows).map((__row) => { (__row as any).isOperable = (((__row as any).status === "active") && ((__row as any).deletedAt == null)); (__row as any).functionalCurrencyCode = ((((__row as any).defaultCurrencyCode != null) && ((((__row as any).defaultCurrencyCode).trim()).length === 3)) ? (__row as any).defaultCurrencyCode : "USD"); return { ...(__row as any), isOperable: (__row as any).isOperable, functionalCurrencyCode: (__row as any).functionalCurrencyCode }; });
  },
});

export const listOrganizationCapabilitySetting = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("capabilitySettingRead", "OrganizationCapabilitySetting", () => (user.id != null))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("organizationCapabilitySettings").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const getOrganizationCapabilitySetting = query({
  args: { id: v.id("organizationCapabilitySettings") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("capabilitySettingRead", "OrganizationCapabilitySetting", () => (user.id != null))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    return __doc;
  },
});

export const listOrganizationCapabilitySettingByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("capabilitySettingRead", "OrganizationCapabilitySetting", () => (user.id != null))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("organizationCapabilitySettings").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listPackList = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("packListRead", "PackList", () => (checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("packLists").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "PackList", ["notes"], row)));
    return __plainRows;
  },
});

export const getPackList = query({
  args: { id: v.id("packLists") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("packListRead", "PackList", () => (checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = await __decryptDoc(ctx, "PackList", ["notes"], __rawDoc);
    return __doc;
  },
});

export const listPackListByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("packListRead", "PackList", () => (checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("packLists").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "PackList", ["notes"], row)));
    return __plainRows;
  },
});

export const listPackListByEventId = query({
  args: { eventId: v.id("events") },
  handler: async (ctx, { eventId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("packListRead", "PackList", () => (checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("packLists").withIndex("by_eventId", (q) => q.eq("eventId", eventId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "PackList", ["notes"], row)));
    return __plainRows;
  },
});

export const listPackListItem = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("packListItemRead", "PackListItem", () => checkRole(user, "logisticsAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("packListItems").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const getPackListItem = query({
  args: { id: v.id("packListItems") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("packListItemRead", "PackListItem", () => checkRole(user, "logisticsAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    return __doc;
  },
});

export const listPackListItemByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("packListItemRead", "PackListItem", () => checkRole(user, "logisticsAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("packListItems").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listPackListItemByPackListId = query({
  args: { packListId: v.id("packLists") },
  handler: async (ctx, { packListId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("packListItemRead", "PackListItem", () => checkRole(user, "logisticsAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("packListItems").withIndex("by_packListId", (q) => q.eq("packListId", packListId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listPackListItemByDishId = query({
  args: { dishId: v.id("dishes") },
  handler: async (ctx, { dishId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("packListItemRead", "PackListItem", () => checkRole(user, "logisticsAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("packListItems").withIndex("by_dishId", (q) => q.eq("dishId", dishId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listPackListItemByProductionBatchId = query({
  args: { productionBatchId: v.id("productionBatches") },
  handler: async (ctx, { productionBatchId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("packListItemRead", "PackListItem", () => checkRole(user, "logisticsAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("packListItems").withIndex("by_productionBatchId", (q) => q.eq("productionBatchId", productionBatchId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listPackListTemplate = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("packListTemplateRead", "PackListTemplate", () => (checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("packListTemplates").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return (__plainRows).map((__row) => { (__row as any).isActive = ((__row as any).status === "active"); return { ...(__row as any), isActive: (__row as any).isActive }; });
  },
});

export const getPackListTemplate = query({
  args: { id: v.id("packListTemplates") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("packListTemplateRead", "PackListTemplate", () => (checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    (__doc as any).isActive = ((__doc as any).status === "active");
    const __hydrated = { ...(__doc as any), isActive: (__doc as any).isActive };
    return __hydrated;
  },
});

export const listPackListTemplateByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("packListTemplateRead", "PackListTemplate", () => (checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("packListTemplates").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return (__plainRows).map((__row) => { (__row as any).isActive = ((__row as any).status === "active"); return { ...(__row as any), isActive: (__row as any).isActive }; });
  },
});

export const listPayment = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("paymentRead", "Payment", () => checkRole(user, "financeAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("payments").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const getPayment = query({
  args: { id: v.id("payments") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("paymentRead", "Payment", () => checkRole(user, "financeAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    return __doc;
  },
});

export const listPaymentByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("paymentRead", "Payment", () => checkRole(user, "financeAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("payments").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listPaymentByInvoiceId = query({
  args: { invoiceId: v.id("invoices") },
  handler: async (ctx, { invoiceId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("paymentRead", "Payment", () => checkRole(user, "financeAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("payments").withIndex("by_invoiceId", (q) => q.eq("invoiceId", invoiceId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listPaymentByClientId = query({
  args: { clientId: v.id("clients") },
  handler: async (ctx, { clientId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("paymentRead", "Payment", () => checkRole(user, "financeAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("payments").withIndex("by_clientId", (q) => q.eq("clientId", clientId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listPaymentByClientMergeAuthorizationId = query({
  args: { clientMergeAuthorizationId: v.id("clientMerges") },
  handler: async (ctx, { clientMergeAuthorizationId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("paymentRead", "Payment", () => checkRole(user, "financeAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("payments").withIndex("by_clientMergeAuthorizationId", (q) => q.eq("clientMergeAuthorizationId", clientMergeAuthorizationId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listPaymentByMergeTargetClientId = query({
  args: { mergeTargetClientId: v.id("clients") },
  handler: async (ctx, { mergeTargetClientId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("paymentRead", "Payment", () => checkRole(user, "financeAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("payments").withIndex("by_mergeTargetClientId", (q) => q.eq("mergeTargetClientId", mergeTargetClientId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listPaymentByEventId = query({
  args: { eventId: v.id("events") },
  handler: async (ctx, { eventId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("paymentRead", "Payment", () => checkRole(user, "financeAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("payments").withIndex("by_eventId", (q) => q.eq("eventId", eventId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listPaymentByPaymentMethodId = query({
  args: { paymentMethodId: v.id("paymentMethods") },
  handler: async (ctx, { paymentMethodId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("paymentRead", "Payment", () => checkRole(user, "financeAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("payments").withIndex("by_paymentMethodId", (q) => q.eq("paymentMethodId", paymentMethodId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listPaymentMethod = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("paymentMethodRead", "PaymentMethod", () => checkRole(user, "financeAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("paymentMethods").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const getPaymentMethod = query({
  args: { id: v.id("paymentMethods") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("paymentMethodRead", "PaymentMethod", () => checkRole(user, "financeAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    return __doc;
  },
});

export const listPaymentMethodByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("paymentMethodRead", "PaymentMethod", () => checkRole(user, "financeAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("paymentMethods").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listPaymentMethodByClientId = query({
  args: { clientId: v.id("clients") },
  handler: async (ctx, { clientId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("paymentMethodRead", "PaymentMethod", () => checkRole(user, "financeAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("paymentMethods").withIndex("by_clientId", (q) => q.eq("clientId", clientId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listPaymentMethodByClientMergeAuthorizationId = query({
  args: { clientMergeAuthorizationId: v.id("clientMerges") },
  handler: async (ctx, { clientMergeAuthorizationId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("paymentMethodRead", "PaymentMethod", () => checkRole(user, "financeAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("paymentMethods").withIndex("by_clientMergeAuthorizationId", (q) => q.eq("clientMergeAuthorizationId", clientMergeAuthorizationId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listPaymentMethodByMergeTargetClientId = query({
  args: { mergeTargetClientId: v.id("clients") },
  handler: async (ctx, { mergeTargetClientId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("paymentMethodRead", "PaymentMethod", () => checkRole(user, "financeAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("paymentMethods").withIndex("by_mergeTargetClientId", (q) => q.eq("mergeTargetClientId", mergeTargetClientId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listPayrollInput = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("payrollInputRead", "PayrollInput", () => checkRole(user, "financeManageAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("payrollInputs").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "PayrollInput", ["hourlyRate","overtimeRate","grossAmount","notes"], row)));
    return (__plainRows).map((d) => {
      const o = { ...(d as any) };
      delete (o as any).hourlyRate; delete (o as any).overtimeRate; delete (o as any).grossAmount;
      return o;
    });
  },
});

export const getPayrollInput = query({
  args: { id: v.id("payrollInputs") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("payrollInputRead", "PayrollInput", () => checkRole(user, "financeManageAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = await __decryptDoc(ctx, "PayrollInput", ["hourlyRate","overtimeRate","grossAmount","notes"], __rawDoc);
    const __final = __doc;
    if (!__final) return __final;
    const __out = { ...(__final as any) };
    delete (__out as any).hourlyRate; delete (__out as any).overtimeRate; delete (__out as any).grossAmount;
    return __out;
  },
});

export const listPayrollInputByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("payrollInputRead", "PayrollInput", () => checkRole(user, "financeManageAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("payrollInputs").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "PayrollInput", ["hourlyRate","overtimeRate","grossAmount","notes"], row)));
    return (__plainRows).map((d) => {
      const o = { ...(d as any) };
      delete (o as any).hourlyRate; delete (o as any).overtimeRate; delete (o as any).grossAmount;
      return o;
    });
  },
});

export const listPayrollInputByPersonId = query({
  args: { personId: v.id("people") },
  handler: async (ctx, { personId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("payrollInputRead", "PayrollInput", () => checkRole(user, "financeManageAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("payrollInputs").withIndex("by_personId", (q) => q.eq("personId", personId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "PayrollInput", ["hourlyRate","overtimeRate","grossAmount","notes"], row)));
    return (__plainRows).map((d) => {
      const o = { ...(d as any) };
      delete (o as any).hourlyRate; delete (o as any).overtimeRate; delete (o as any).grossAmount;
      return o;
    });
  },
});

export const listPayrollInputByEventId = query({
  args: { eventId: v.id("events") },
  handler: async (ctx, { eventId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("payrollInputRead", "PayrollInput", () => checkRole(user, "financeManageAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("payrollInputs").withIndex("by_eventId", (q) => q.eq("eventId", eventId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "PayrollInput", ["hourlyRate","overtimeRate","grossAmount","notes"], row)));
    return (__plainRows).map((d) => {
      const o = { ...(d as any) };
      delete (o as any).hourlyRate; delete (o as any).overtimeRate; delete (o as any).grossAmount;
      return o;
    });
  },
});

export const listPayrollInputByShiftId = query({
  args: { shiftId: v.id("shifts") },
  handler: async (ctx, { shiftId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("payrollInputRead", "PayrollInput", () => checkRole(user, "financeManageAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("payrollInputs").withIndex("by_shiftId", (q) => q.eq("shiftId", shiftId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "PayrollInput", ["hourlyRate","overtimeRate","grossAmount","notes"], row)));
    return (__plainRows).map((d) => {
      const o = { ...(d as any) };
      delete (o as any).hourlyRate; delete (o as any).overtimeRate; delete (o as any).grossAmount;
      return o;
    });
  },
});

export const listPerformanceReview = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("performanceReviewRead", "PerformanceReview", () => checkRole(user, "manageAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("performanceReviews").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "PerformanceReview", ["notes"], row)));
    return __plainRows;
  },
});

export const getPerformanceReview = query({
  args: { id: v.id("performanceReviews") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("performanceReviewRead", "PerformanceReview", () => checkRole(user, "manageAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = await __decryptDoc(ctx, "PerformanceReview", ["notes"], __rawDoc);
    return __doc;
  },
});

export const listPerformanceReviewByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("performanceReviewRead", "PerformanceReview", () => checkRole(user, "manageAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("performanceReviews").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "PerformanceReview", ["notes"], row)));
    return __plainRows;
  },
});

export const listPerformanceReviewByPersonId = query({
  args: { personId: v.id("people") },
  handler: async (ctx, { personId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("performanceReviewRead", "PerformanceReview", () => checkRole(user, "manageAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("performanceReviews").withIndex("by_personId", (q) => q.eq("personId", personId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "PerformanceReview", ["notes"], row)));
    return __plainRows;
  },
});

export const listPerformanceReviewByReviewerId = query({
  args: { reviewerId: v.id("people") },
  handler: async (ctx, { reviewerId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("performanceReviewRead", "PerformanceReview", () => checkRole(user, "manageAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("performanceReviews").withIndex("by_reviewerId", (q) => q.eq("reviewerId", reviewerId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "PerformanceReview", ["notes"], row)));
    return __plainRows;
  },
});

export const listPerformanceReviewByEventId = query({
  args: { eventId: v.id("events") },
  handler: async (ctx, { eventId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("performanceReviewRead", "PerformanceReview", () => checkRole(user, "manageAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("performanceReviews").withIndex("by_eventId", (q) => q.eq("eventId", eventId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "PerformanceReview", ["notes"], row)));
    return __plainRows;
  },
});

export const listPerson = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("personRead", "Person", () => checkRole(user, "staffAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("people").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "Person", ["email","phone"], row)));
    return ((__plainRows).map((__row) => { (__row as any).displayName = (((__row as any).givenName + " ") + (__row as any).familyName); (__row as any).isAssignable = (((__row as any).status === "active") && ((__row as any).deletedAt == null)); return { ...(__row as any), displayName: (__row as any).displayName, isAssignable: (__row as any).isAssignable }; })).map((d) => {
      const o = { ...(d as any) };
      delete (o as any).hourlyRate;
      return o;
    });
  },
});

export const getPerson = query({
  args: { id: v.id("people") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("personRead", "Person", () => checkRole(user, "staffAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = await __decryptDoc(ctx, "Person", ["email","phone"], __rawDoc);
    (__doc as any).displayName = (((__doc as any).givenName + " ") + (__doc as any).familyName);
    (__doc as any).isAssignable = (((__doc as any).status === "active") && ((__doc as any).deletedAt == null));
    const __hydrated = { ...(__doc as any), displayName: (__doc as any).displayName, isAssignable: (__doc as any).isAssignable };
    const __final = __hydrated;
    if (!__final) return __final;
    const __out = { ...(__final as any) };
    delete (__out as any).hourlyRate;
    return __out;
  },
});

export const listPersonByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("personRead", "Person", () => checkRole(user, "staffAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("people").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "Person", ["email","phone"], row)));
    return ((__plainRows).map((__row) => { (__row as any).displayName = (((__row as any).givenName + " ") + (__row as any).familyName); (__row as any).isAssignable = (((__row as any).status === "active") && ((__row as any).deletedAt == null)); return { ...(__row as any), displayName: (__row as any).displayName, isAssignable: (__row as any).isAssignable }; })).map((d) => {
      const o = { ...(d as any) };
      delete (o as any).hourlyRate;
      return o;
    });
  },
});

export const listPersonByAuthSubjectId = query({
  args: { authSubjectId: v.string() },
  handler: async (ctx, { authSubjectId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("personRead", "Person", () => checkRole(user, "staffAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("people").withIndex("by_authSubjectId", (q) => q.eq("authSubjectId", authSubjectId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "Person", ["email","phone"], row)));
    return ((__plainRows).map((__row) => { (__row as any).displayName = (((__row as any).givenName + " ") + (__row as any).familyName); (__row as any).isAssignable = (((__row as any).status === "active") && ((__row as any).deletedAt == null)); return { ...(__row as any), displayName: (__row as any).displayName, isAssignable: (__row as any).isAssignable }; })).map((d) => {
      const o = { ...(d as any) };
      delete (o as any).hourlyRate;
      return o;
    });
  },
});

export const listPrepTask = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("prepTaskRead", "PrepTask", () => (checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("prepTasks").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const getPrepTask = query({
  args: { id: v.id("prepTasks") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("prepTaskRead", "PrepTask", () => (checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    return __doc;
  },
});

export const listPrepTaskByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("prepTaskRead", "PrepTask", () => (checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("prepTasks").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listPrepTaskByEventDishId = query({
  args: { eventDishId: v.id("eventDishes") },
  handler: async (ctx, { eventDishId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("prepTaskRead", "PrepTask", () => (checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("prepTasks").withIndex("by_eventDishId", (q) => q.eq("eventDishId", eventDishId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listPrepTaskByEventId = query({
  args: { eventId: v.id("events") },
  handler: async (ctx, { eventId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("prepTaskRead", "PrepTask", () => (checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("prepTasks").withIndex("by_eventId", (q) => q.eq("eventId", eventId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listPrepTaskByDishTaskId = query({
  args: { dishTaskId: v.id("dishTasks") },
  handler: async (ctx, { dishTaskId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("prepTaskRead", "PrepTask", () => (checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("prepTasks").withIndex("by_dishTaskId", (q) => q.eq("dishTaskId", dishTaskId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listPrepTaskByDishId = query({
  args: { dishId: v.id("dishes") },
  handler: async (ctx, { dishId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("prepTaskRead", "PrepTask", () => (checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("prepTasks").withIndex("by_dishId", (q) => q.eq("dishId", dishId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listPrepTaskByIngredientId = query({
  args: { ingredientId: v.id("ingredients") },
  handler: async (ctx, { ingredientId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("prepTaskRead", "PrepTask", () => (checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("prepTasks").withIndex("by_ingredientId", (q) => q.eq("ingredientId", ingredientId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listPrepTaskByIngredientDemandId = query({
  args: { ingredientDemandId: v.id("ingredientDemands") },
  handler: async (ctx, { ingredientDemandId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("prepTaskRead", "PrepTask", () => (checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("prepTasks").withIndex("by_ingredientDemandId", (q) => q.eq("ingredientDemandId", ingredientDemandId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listPrepTaskByComponentId = query({
  args: { componentId: v.id("components") },
  handler: async (ctx, { componentId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("prepTaskRead", "PrepTask", () => (checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("prepTasks").withIndex("by_componentId", (q) => q.eq("componentId", componentId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listPrepTaskByAssignedToId = query({
  args: { assignedToId: v.id("people") },
  handler: async (ctx, { assignedToId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("prepTaskRead", "PrepTask", () => (checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("prepTasks").withIndex("by_assignedToId", (q) => q.eq("assignedToId", assignedToId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listPrepTaskComment = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("prepTaskCommentRead", "PrepTaskComment", () => (checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("prepTaskComments").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const getPrepTaskComment = query({
  args: { id: v.id("prepTaskComments") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("prepTaskCommentRead", "PrepTaskComment", () => (checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    return __doc;
  },
});

export const listPrepTaskCommentByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("prepTaskCommentRead", "PrepTaskComment", () => (checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("prepTaskComments").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listPrepTaskCommentByPrepTaskId = query({
  args: { prepTaskId: v.id("prepTasks") },
  handler: async (ctx, { prepTaskId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("prepTaskCommentRead", "PrepTaskComment", () => (checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("prepTaskComments").withIndex("by_prepTaskId", (q) => q.eq("prepTaskId", prepTaskId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listPrepTaskCommentByEventId = query({
  args: { eventId: v.id("events") },
  handler: async (ctx, { eventId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("prepTaskCommentRead", "PrepTaskComment", () => (checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("prepTaskComments").withIndex("by_eventId", (q) => q.eq("eventId", eventId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listPrepTaskCommentByAuthorAuthSubjectId = query({
  args: { authorAuthSubjectId: v.string() },
  handler: async (ctx, { authorAuthSubjectId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("prepTaskCommentRead", "PrepTaskComment", () => (checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("prepTaskComments").withIndex("by_authorAuthSubjectId", (q) => q.eq("authorAuthSubjectId", authorAuthSubjectId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listPrepTaskCommentByEventDishId = query({
  args: { eventDishId: v.id("eventDishes") },
  handler: async (ctx, { eventDishId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("prepTaskCommentRead", "PrepTaskComment", () => (checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("prepTaskComments").withIndex("by_eventDishId", (q) => q.eq("eventDishId", eventDishId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listPrepTaskCommentByAuthorPersonId = query({
  args: { authorPersonId: v.id("people") },
  handler: async (ctx, { authorPersonId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("prepTaskCommentRead", "PrepTaskComment", () => (checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("prepTaskComments").withIndex("by_authorPersonId", (q) => q.eq("authorPersonId", authorPersonId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listPrepTaskCommentByTaskOwnerAssignedToId = query({
  args: { taskOwnerAssignedToId: v.id("people") },
  handler: async (ctx, { taskOwnerAssignedToId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("prepTaskCommentRead", "PrepTaskComment", () => (checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("prepTaskComments").withIndex("by_taskOwnerAssignedToId", (q) => q.eq("taskOwnerAssignedToId", taskOwnerAssignedToId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listPrepTaskDependency = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("prepTaskDependencyRead", "PrepTaskDependency", () => (checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("prepTaskDependencies").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    const __plainRows = rows;
    return __plainRows;
  },
});

export const getPrepTaskDependency = query({
  args: { id: v.id("prepTaskDependencies") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("prepTaskDependencyRead", "PrepTaskDependency", () => (checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    return __doc;
  },
});

export const listPrepTaskDependencyByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("prepTaskDependencyRead", "PrepTaskDependency", () => (checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("prepTaskDependencies").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listPrepTaskDependencyByDependentTaskId = query({
  args: { dependentTaskId: v.id("prepTasks") },
  handler: async (ctx, { dependentTaskId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("prepTaskDependencyRead", "PrepTaskDependency", () => (checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("prepTaskDependencies").withIndex("by_dependentTaskId", (q) => q.eq("dependentTaskId", dependentTaskId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listPrepTaskDependencyByPredecessorTaskId = query({
  args: { predecessorTaskId: v.id("prepTasks") },
  handler: async (ctx, { predecessorTaskId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("prepTaskDependencyRead", "PrepTaskDependency", () => (checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("prepTaskDependencies").withIndex("by_predecessorTaskId", (q) => q.eq("predecessorTaskId", predecessorTaskId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listProductionBatch = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("productionBatchRead", "ProductionBatch", () => (checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("productionBatches").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const getProductionBatch = query({
  args: { id: v.id("productionBatches") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("productionBatchRead", "ProductionBatch", () => (checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    return __doc;
  },
});

export const listProductionBatchByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("productionBatchRead", "ProductionBatch", () => (checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("productionBatches").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listProductionBatchByComponentId = query({
  args: { componentId: v.id("components") },
  handler: async (ctx, { componentId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("productionBatchRead", "ProductionBatch", () => (checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("productionBatches").withIndex("by_componentId", (q) => q.eq("componentId", componentId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listProductionBatchByEventId = query({
  args: { eventId: v.id("events") },
  handler: async (ctx, { eventId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("productionBatchRead", "ProductionBatch", () => (checkRole(user, "kitchenAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("productionBatches").withIndex("by_eventId", (q) => q.eq("eventId", eventId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listProposal = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("proposalRead", "Proposal", () => checkRole(user, "salesAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("proposals").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const getProposal = query({
  args: { id: v.id("proposals") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("proposalRead", "Proposal", () => checkRole(user, "salesAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    return __doc;
  },
});

export const listProposalByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("proposalRead", "Proposal", () => checkRole(user, "salesAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("proposals").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listProposalByClientId = query({
  args: { clientId: v.id("clients") },
  handler: async (ctx, { clientId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("proposalRead", "Proposal", () => checkRole(user, "salesAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("proposals").withIndex("by_clientId", (q) => q.eq("clientId", clientId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listProposalByClientMergeAuthorizationId = query({
  args: { clientMergeAuthorizationId: v.id("clientMerges") },
  handler: async (ctx, { clientMergeAuthorizationId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("proposalRead", "Proposal", () => checkRole(user, "salesAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("proposals").withIndex("by_clientMergeAuthorizationId", (q) => q.eq("clientMergeAuthorizationId", clientMergeAuthorizationId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listProposalByMergeTargetClientId = query({
  args: { mergeTargetClientId: v.id("clients") },
  handler: async (ctx, { mergeTargetClientId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("proposalRead", "Proposal", () => checkRole(user, "salesAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("proposals").withIndex("by_mergeTargetClientId", (q) => q.eq("mergeTargetClientId", mergeTargetClientId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listProposalByEventId = query({
  args: { eventId: v.id("events") },
  handler: async (ctx, { eventId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("proposalRead", "Proposal", () => checkRole(user, "salesAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("proposals").withIndex("by_eventId", (q) => q.eq("eventId", eventId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listProposalByPendingEventId = query({
  args: { pendingEventId: v.id("events") },
  handler: async (ctx, { pendingEventId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("proposalRead", "Proposal", () => checkRole(user, "salesAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("proposals").withIndex("by_pendingEventId", (q) => q.eq("pendingEventId", pendingEventId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listProposalBySupersededById = query({
  args: { supersededById: v.id("proposals") },
  handler: async (ctx, { supersededById }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("proposalRead", "Proposal", () => checkRole(user, "salesAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("proposals").withIndex("by_supersededById", (q) => q.eq("supersededById", supersededById)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listProposalByReplacesProposalId = query({
  args: { replacesProposalId: v.id("proposals") },
  handler: async (ctx, { replacesProposalId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("proposalRead", "Proposal", () => checkRole(user, "salesAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("proposals").withIndex("by_replacesProposalId", (q) => q.eq("replacesProposalId", replacesProposalId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listProposalDishSelection = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("proposalDishSelectionRead", "ProposalDishSelection", () => checkRole(user, "salesAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("proposalDishSelections").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const getProposalDishSelection = query({
  args: { id: v.id("proposalDishSelections") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("proposalDishSelectionRead", "ProposalDishSelection", () => checkRole(user, "salesAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    return __doc;
  },
});

export const listProposalDishSelectionByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("proposalDishSelectionRead", "ProposalDishSelection", () => checkRole(user, "salesAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("proposalDishSelections").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listProposalDishSelectionByProposalId = query({
  args: { proposalId: v.id("proposals") },
  handler: async (ctx, { proposalId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("proposalDishSelectionRead", "ProposalDishSelection", () => checkRole(user, "salesAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("proposalDishSelections").withIndex("by_proposalId", (q) => q.eq("proposalId", proposalId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listProposalDishSelectionByMenuId = query({
  args: { menuId: v.id("menus") },
  handler: async (ctx, { menuId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("proposalDishSelectionRead", "ProposalDishSelection", () => checkRole(user, "salesAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("proposalDishSelections").withIndex("by_menuId", (q) => q.eq("menuId", menuId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listProposalDishSelectionByDishId = query({
  args: { dishId: v.id("dishes") },
  handler: async (ctx, { dishId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("proposalDishSelectionRead", "ProposalDishSelection", () => checkRole(user, "salesAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("proposalDishSelections").withIndex("by_dishId", (q) => q.eq("dishId", dishId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listProposalEnhancement = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("proposalEnhancementRead", "ProposalEnhancement", () => checkRole(user, "salesAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("proposalEnhancements").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const getProposalEnhancement = query({
  args: { id: v.id("proposalEnhancements") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("proposalEnhancementRead", "ProposalEnhancement", () => checkRole(user, "salesAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    return __doc;
  },
});

export const listProposalEnhancementByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("proposalEnhancementRead", "ProposalEnhancement", () => checkRole(user, "salesAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("proposalEnhancements").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listProposalEnhancementByProposalId = query({
  args: { proposalId: v.id("proposals") },
  handler: async (ctx, { proposalId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("proposalEnhancementRead", "ProposalEnhancement", () => checkRole(user, "salesAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("proposalEnhancements").withIndex("by_proposalId", (q) => q.eq("proposalId", proposalId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listProposalLineItem = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("proposalLineItemRead", "ProposalLineItem", () => checkRole(user, "salesAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("proposalLineItems").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const getProposalLineItem = query({
  args: { id: v.id("proposalLineItems") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("proposalLineItemRead", "ProposalLineItem", () => checkRole(user, "salesAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    return __doc;
  },
});

export const listProposalLineItemByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("proposalLineItemRead", "ProposalLineItem", () => checkRole(user, "salesAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("proposalLineItems").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listProposalLineItemByProposalId = query({
  args: { proposalId: v.id("proposals") },
  handler: async (ctx, { proposalId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("proposalLineItemRead", "ProposalLineItem", () => checkRole(user, "salesAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("proposalLineItems").withIndex("by_proposalId", (q) => q.eq("proposalId", proposalId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listProposalRevision = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("proposalRevisionRead", "ProposalRevision", () => checkRole(user, "salesAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("proposalRevisions").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const getProposalRevision = query({
  args: { id: v.id("proposalRevisions") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("proposalRevisionRead", "ProposalRevision", () => checkRole(user, "salesAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    return __doc;
  },
});

export const listProposalRevisionByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("proposalRevisionRead", "ProposalRevision", () => checkRole(user, "salesAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("proposalRevisions").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listProposalRevisionByProposalId = query({
  args: { proposalId: v.id("proposals") },
  handler: async (ctx, { proposalId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("proposalRevisionRead", "ProposalRevision", () => checkRole(user, "salesAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("proposalRevisions").withIndex("by_proposalId", (q) => q.eq("proposalId", proposalId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listProposalTemplate = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("proposalTemplateRead", "ProposalTemplate", () => checkRole(user, "salesAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("proposalTemplates").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return (__plainRows).map((__row) => { (__row as any).isActive = ((__row as any).status === "active"); return { ...(__row as any), isActive: (__row as any).isActive }; });
  },
});

export const getProposalTemplate = query({
  args: { id: v.id("proposalTemplates") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("proposalTemplateRead", "ProposalTemplate", () => checkRole(user, "salesAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    (__doc as any).isActive = ((__doc as any).status === "active");
    const __hydrated = { ...(__doc as any), isActive: (__doc as any).isActive };
    return __hydrated;
  },
});

export const listProposalTemplateByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("proposalTemplateRead", "ProposalTemplate", () => checkRole(user, "salesAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("proposalTemplates").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return (__plainRows).map((__row) => { (__row as any).isActive = ((__row as any).status === "active"); return { ...(__row as any), isActive: (__row as any).isActive }; });
  },
});

export const listPurchaseNeed = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("purchaseNeedRead", "PurchaseNeed", () => (checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("purchaseNeeds").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const getPurchaseNeed = query({
  args: { id: v.id("purchaseNeeds") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("purchaseNeedRead", "PurchaseNeed", () => (checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    return __doc;
  },
});

export const listPurchaseNeedByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("purchaseNeedRead", "PurchaseNeed", () => (checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("purchaseNeeds").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listPurchaseNeedByEventId = query({
  args: { eventId: v.id("events") },
  handler: async (ctx, { eventId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("purchaseNeedRead", "PurchaseNeed", () => (checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("purchaseNeeds").withIndex("by_eventId", (q) => q.eq("eventId", eventId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listPurchaseNeedByIngredientDemandId = query({
  args: { ingredientDemandId: v.id("ingredientDemands") },
  handler: async (ctx, { ingredientDemandId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("purchaseNeedRead", "PurchaseNeed", () => (checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("purchaseNeeds").withIndex("by_ingredientDemandId", (q) => q.eq("ingredientDemandId", ingredientDemandId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listPurchaseNeedByIngredientId = query({
  args: { ingredientId: v.id("ingredients") },
  handler: async (ctx, { ingredientId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("purchaseNeedRead", "PurchaseNeed", () => (checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("purchaseNeeds").withIndex("by_ingredientId", (q) => q.eq("ingredientId", ingredientId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listPurchaseNeedByPreferredVendorId = query({
  args: { preferredVendorId: v.id("vendors") },
  handler: async (ctx, { preferredVendorId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("purchaseNeedRead", "PurchaseNeed", () => (checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("purchaseNeeds").withIndex("by_preferredVendorId", (q) => q.eq("preferredVendorId", preferredVendorId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listPurchaseNeedByVendorOrderId = query({
  args: { vendorOrderId: v.id("vendorOrders") },
  handler: async (ctx, { vendorOrderId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("purchaseNeedRead", "PurchaseNeed", () => (checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("purchaseNeeds").withIndex("by_vendorOrderId", (q) => q.eq("vendorOrderId", vendorOrderId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listPurchaseNeedByVendorOrderLineId = query({
  args: { vendorOrderLineId: v.id("vendorOrderLines") },
  handler: async (ctx, { vendorOrderLineId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("purchaseNeedRead", "PurchaseNeed", () => (checkRole(user, "inventoryAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("purchaseNeeds").withIndex("by_vendorOrderLineId", (q) => q.eq("vendorOrderLineId", vendorOrderLineId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listQualification = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("qualificationRead", "Qualification", () => checkRole(user, "workforceAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("qualifications").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "Qualification", ["notes"], row)));
    return __plainRows;
  },
});

export const getQualification = query({
  args: { id: v.id("qualifications") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("qualificationRead", "Qualification", () => checkRole(user, "workforceAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = await __decryptDoc(ctx, "Qualification", ["notes"], __rawDoc);
    return __doc;
  },
});

export const listQualificationByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("qualificationRead", "Qualification", () => checkRole(user, "workforceAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("qualifications").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "Qualification", ["notes"], row)));
    return __plainRows;
  },
});

export const listQualificationByPersonId = query({
  args: { personId: v.id("people") },
  handler: async (ctx, { personId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("qualificationRead", "Qualification", () => checkRole(user, "workforceAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("qualifications").withIndex("by_personId", (q) => q.eq("personId", personId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "Qualification", ["notes"], row)));
    return __plainRows;
  },
});

export const listQualityCheck = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("qualityCheckRead", "QualityCheck", () => checkRole(user, "kitchenAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("qualityChecks").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const getQualityCheck = query({
  args: { id: v.id("qualityChecks") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("qualityCheckRead", "QualityCheck", () => checkRole(user, "kitchenAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    return __doc;
  },
});

export const listQualityCheckByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("qualityCheckRead", "QualityCheck", () => checkRole(user, "kitchenAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("qualityChecks").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listQualityCheckByPrepTaskId = query({
  args: { prepTaskId: v.id("prepTasks") },
  handler: async (ctx, { prepTaskId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("qualityCheckRead", "QualityCheck", () => checkRole(user, "kitchenAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("qualityChecks").withIndex("by_prepTaskId", (q) => q.eq("prepTaskId", prepTaskId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listQualityCheckByProductionBatchId = query({
  args: { productionBatchId: v.id("productionBatches") },
  handler: async (ctx, { productionBatchId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("qualityCheckRead", "QualityCheck", () => checkRole(user, "kitchenAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("qualityChecks").withIndex("by_productionBatchId", (q) => q.eq("productionBatchId", productionBatchId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listQualityCheckByCheckedById = query({
  args: { checkedById: v.id("people") },
  handler: async (ctx, { checkedById }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("qualityCheckRead", "QualityCheck", () => checkRole(user, "kitchenAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("qualityChecks").withIndex("by_checkedById", (q) => q.eq("checkedById", checkedById)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listQuoteSubmission = query({
  args: {},
  handler: async (ctx) => {
    if (!__allowsRead("quoteSubmissionRead", "QuoteSubmission", () => true)) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("quoteSubmissions").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const getQuoteSubmission = query({
  args: { id: v.id("quoteSubmissions") },
  handler: async (ctx, { id }) => {
    if (!__allowsRead("quoteSubmissionRead", "QuoteSubmission", () => true)) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    return __doc;
  },
});

export const listQuoteSubmissionByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    if (!__allowsRead("quoteSubmissionRead", "QuoteSubmission", () => true)) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("quoteSubmissions").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listQuoteSubmissionByClientId = query({
  args: { clientId: v.id("clients") },
  handler: async (ctx, { clientId }) => {
    if (!__allowsRead("quoteSubmissionRead", "QuoteSubmission", () => true)) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("quoteSubmissions").withIndex("by_clientId", (q) => q.eq("clientId", clientId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listQuoteSubmissionByLeadId = query({
  args: { leadId: v.id("leads") },
  handler: async (ctx, { leadId }) => {
    if (!__allowsRead("quoteSubmissionRead", "QuoteSubmission", () => true)) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("quoteSubmissions").withIndex("by_leadId", (q) => q.eq("leadId", leadId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listQuoteSubmissionByEventId = query({
  args: { eventId: v.id("events") },
  handler: async (ctx, { eventId }) => {
    if (!__allowsRead("quoteSubmissionRead", "QuoteSubmission", () => true)) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("quoteSubmissions").withIndex("by_eventId", (q) => q.eq("eventId", eventId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listQuoteSubmissionByProposalId = query({
  args: { proposalId: v.id("proposals") },
  handler: async (ctx, { proposalId }) => {
    if (!__allowsRead("quoteSubmissionRead", "QuoteSubmission", () => true)) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("quoteSubmissions").withIndex("by_proposalId", (q) => q.eq("proposalId", proposalId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listQuoteSubmissionByServiceStyleId = query({
  args: { serviceStyleId: v.id("serviceStyles") },
  handler: async (ctx, { serviceStyleId }) => {
    if (!__allowsRead("quoteSubmissionRead", "QuoteSubmission", () => true)) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("quoteSubmissions").withIndex("by_serviceStyleId", (q) => q.eq("serviceStyleId", serviceStyleId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listQuoteSubmissionByOccasionId = query({
  args: { occasionId: v.id("occasions") },
  handler: async (ctx, { occasionId }) => {
    if (!__allowsRead("quoteSubmissionRead", "QuoteSubmission", () => true)) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("quoteSubmissions").withIndex("by_occasionId", (q) => q.eq("occasionId", occasionId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listRecurringAvailability = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("recurringAvailabilityRead", "RecurringAvailability", () => checkRole(user, "workforceAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("recurringAvailabilities").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "RecurringAvailability", ["notes"], row)));
    return __plainRows;
  },
});

export const getRecurringAvailability = query({
  args: { id: v.id("recurringAvailabilities") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("recurringAvailabilityRead", "RecurringAvailability", () => checkRole(user, "workforceAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = await __decryptDoc(ctx, "RecurringAvailability", ["notes"], __rawDoc);
    return __doc;
  },
});

export const listRecurringAvailabilityByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("recurringAvailabilityRead", "RecurringAvailability", () => checkRole(user, "workforceAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("recurringAvailabilities").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "RecurringAvailability", ["notes"], row)));
    return __plainRows;
  },
});

export const listRecurringAvailabilityByPersonId = query({
  args: { personId: v.id("people") },
  handler: async (ctx, { personId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("recurringAvailabilityRead", "RecurringAvailability", () => checkRole(user, "workforceAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("recurringAvailabilities").withIndex("by_personId", (q) => q.eq("personId", personId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "RecurringAvailability", ["notes"], row)));
    return __plainRows;
  },
});

export const listReferralSource = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("referralSourceRead", "ReferralSource", () => (checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("referralSources").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return (__plainRows).map((__row) => { (__row as any).isActive = ((__row as any).status === "active"); return { ...(__row as any), isActive: (__row as any).isActive }; });
  },
});

export const getReferralSource = query({
  args: { id: v.id("referralSources") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("referralSourceRead", "ReferralSource", () => (checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    (__doc as any).isActive = ((__doc as any).status === "active");
    const __hydrated = { ...(__doc as any), isActive: (__doc as any).isActive };
    return __hydrated;
  },
});

export const listReferralSourceByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("referralSourceRead", "ReferralSource", () => (checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("referralSources").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return (__plainRows).map((__row) => { (__row as any).isActive = ((__row as any).status === "active"); return { ...(__row as any), isActive: (__row as any).isActive }; });
  },
});

export const listRevenueAttribution = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("attributionRead", "RevenueAttribution", () => (checkRole(user, "financeAccess") || checkRole(user, "salesAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("revenueAttributions").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const getRevenueAttribution = query({
  args: { id: v.id("revenueAttributions") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("attributionRead", "RevenueAttribution", () => (checkRole(user, "financeAccess") || checkRole(user, "salesAccess")))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    return __doc;
  },
});

export const listRevenueAttributionByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("attributionRead", "RevenueAttribution", () => (checkRole(user, "financeAccess") || checkRole(user, "salesAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("revenueAttributions").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listRevenueAttributionByEventId = query({
  args: { eventId: v.id("events") },
  handler: async (ctx, { eventId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("attributionRead", "RevenueAttribution", () => (checkRole(user, "financeAccess") || checkRole(user, "salesAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("revenueAttributions").withIndex("by_eventId", (q) => q.eq("eventId", eventId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listRevenueAttributionByVenueId = query({
  args: { venueId: v.id("venues") },
  handler: async (ctx, { venueId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("attributionRead", "RevenueAttribution", () => (checkRole(user, "financeAccess") || checkRole(user, "salesAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("revenueAttributions").withIndex("by_venueId", (q) => q.eq("venueId", venueId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listRevenueAttributionBySalespersonId = query({
  args: { salespersonId: v.id("people") },
  handler: async (ctx, { salespersonId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("attributionRead", "RevenueAttribution", () => (checkRole(user, "financeAccess") || checkRole(user, "salesAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("revenueAttributions").withIndex("by_salespersonId", (q) => q.eq("salespersonId", salespersonId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listRevenueAttributionByReferralSourceId = query({
  args: { referralSourceId: v.id("referralSources") },
  handler: async (ctx, { referralSourceId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("attributionRead", "RevenueAttribution", () => (checkRole(user, "financeAccess") || checkRole(user, "salesAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("revenueAttributions").withIndex("by_referralSourceId", (q) => q.eq("referralSourceId", referralSourceId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listRevenueAttributionByPartnerPersonId = query({
  args: { partnerPersonId: v.id("people") },
  handler: async (ctx, { partnerPersonId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("attributionRead", "RevenueAttribution", () => (checkRole(user, "financeAccess") || checkRole(user, "salesAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("revenueAttributions").withIndex("by_partnerPersonId", (q) => q.eq("partnerPersonId", partnerPersonId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listRevenueAttributionByPartnerClientId = query({
  args: { partnerClientId: v.id("clients") },
  handler: async (ctx, { partnerClientId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("attributionRead", "RevenueAttribution", () => (checkRole(user, "financeAccess") || checkRole(user, "salesAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("revenueAttributions").withIndex("by_partnerClientId", (q) => q.eq("partnerClientId", partnerClientId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listRevenueAttributionByRequestedById = query({
  args: { requestedById: v.id("people") },
  handler: async (ctx, { requestedById }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("attributionRead", "RevenueAttribution", () => (checkRole(user, "financeAccess") || checkRole(user, "salesAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("revenueAttributions").withIndex("by_requestedById", (q) => q.eq("requestedById", requestedById)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listRevenueAttributionByApprovedById = query({
  args: { approvedById: v.id("people") },
  handler: async (ctx, { approvedById }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("attributionRead", "RevenueAttribution", () => (checkRole(user, "financeAccess") || checkRole(user, "salesAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("revenueAttributions").withIndex("by_approvedById", (q) => q.eq("approvedById", approvedById)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listRoleScorecard = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("roleScorecardRead", "RoleScorecard", () => checkRole(user, "workforceManageAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("roleScorecards").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const getRoleScorecard = query({
  args: { id: v.id("roleScorecards") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("roleScorecardRead", "RoleScorecard", () => checkRole(user, "workforceManageAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    return __doc;
  },
});

export const listRoleScorecardByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("roleScorecardRead", "RoleScorecard", () => checkRole(user, "workforceManageAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("roleScorecards").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listSavedReportDefinition = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("savedReportDefinitions").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    const __visibleRows: any[] = [];
    for (const __row of __plainRows) {
      if (!__allowsRead("savedReportRead", "SavedReportDefinition", () => ((((__row.ownerId == null) || (__row.ownerId === user.id)) || (__row.sharingScope !== "owner_only")) || checkRole(user, "manageAccess")))) continue;
      __visibleRows.push(__row);
    }
    return __visibleRows;
  },
});

export const getSavedReportDefinition = query({
  args: { id: v.id("savedReportDefinitions") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    if (!__allowsRead("savedReportRead", "SavedReportDefinition", () => ((((__doc.ownerId == null) || (__doc.ownerId === user.id)) || (__doc.sharingScope !== "owner_only")) || checkRole(user, "manageAccess")))) return null;
    return __doc;
  },
});

export const listSavedReportDefinitionByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("savedReportDefinitions").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    const __visibleRows: any[] = [];
    for (const __row of __plainRows) {
      if (!__allowsRead("savedReportRead", "SavedReportDefinition", () => ((((__row.ownerId == null) || (__row.ownerId === user.id)) || (__row.sharingScope !== "owner_only")) || checkRole(user, "manageAccess")))) continue;
      __visibleRows.push(__row);
    }
    return __visibleRows;
  },
});

export const listSavedReportDefinitionByOwnerId = query({
  args: { ownerId: v.id("people") },
  handler: async (ctx, { ownerId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("savedReportDefinitions").withIndex("by_ownerId", (q) => q.eq("ownerId", ownerId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    const __visibleRows: any[] = [];
    for (const __row of __plainRows) {
      if (!__allowsRead("savedReportRead", "SavedReportDefinition", () => ((((__row.ownerId == null) || (__row.ownerId === user.id)) || (__row.sharingScope !== "owner_only")) || checkRole(user, "manageAccess")))) continue;
      __visibleRows.push(__row);
    }
    return __visibleRows;
  },
});

export const listServiceStyle = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("serviceStyleRead", "ServiceStyle", () => (checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("serviceStyles").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return (__plainRows).map((__row) => { (__row as any).isActive = ((__row as any).status === "active"); return { ...(__row as any), isActive: (__row as any).isActive }; });
  },
});

export const getServiceStyle = query({
  args: { id: v.id("serviceStyles") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("serviceStyleRead", "ServiceStyle", () => (checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    (__doc as any).isActive = ((__doc as any).status === "active");
    const __hydrated = { ...(__doc as any), isActive: (__doc as any).isActive };
    return __hydrated;
  },
});

export const listServiceStyleByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("serviceStyleRead", "ServiceStyle", () => (checkRole(user, "eventAccess") || checkRole(user, "salesAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("serviceStyles").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return (__plainRows).map((__row) => { (__row as any).isActive = ((__row as any).status === "active"); return { ...(__row as any), isActive: (__row as any).isActive }; });
  },
});

export const listShareLink = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("shareLinkRead", "ShareLink", () => checkRole(user, "salesAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("shareLinks").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const getShareLink = query({
  args: { id: v.id("shareLinks") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("shareLinkRead", "ShareLink", () => checkRole(user, "salesAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    return __doc;
  },
});

export const listShareLinkByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("shareLinkRead", "ShareLink", () => checkRole(user, "salesAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("shareLinks").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listShareLinkByProposalId = query({
  args: { proposalId: v.id("proposals") },
  handler: async (ctx, { proposalId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("shareLinkRead", "ShareLink", () => checkRole(user, "salesAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("shareLinks").withIndex("by_proposalId", (q) => q.eq("proposalId", proposalId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listShareLinkByProposalRevisionId = query({
  args: { proposalRevisionId: v.id("proposalRevisions") },
  handler: async (ctx, { proposalRevisionId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("shareLinkRead", "ShareLink", () => checkRole(user, "salesAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("shareLinks").withIndex("by_proposalRevisionId", (q) => q.eq("proposalRevisionId", proposalRevisionId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listShift = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("shiftRead", "Shift", () => checkRole(user, "workforceAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("shifts").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "Shift", ["notes"], row)));
    return __plainRows;
  },
});

export const getShift = query({
  args: { id: v.id("shifts") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("shiftRead", "Shift", () => checkRole(user, "workforceAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = await __decryptDoc(ctx, "Shift", ["notes"], __rawDoc);
    return __doc;
  },
});

export const listShiftByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("shiftRead", "Shift", () => checkRole(user, "workforceAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("shifts").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "Shift", ["notes"], row)));
    return __plainRows;
  },
});

export const listShiftByPersonId = query({
  args: { personId: v.id("people") },
  handler: async (ctx, { personId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("shiftRead", "Shift", () => checkRole(user, "workforceAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("shifts").withIndex("by_personId", (q) => q.eq("personId", personId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "Shift", ["notes"], row)));
    return __plainRows;
  },
});

export const listShiftByEventId = query({
  args: { eventId: v.id("events") },
  handler: async (ctx, { eventId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("shiftRead", "Shift", () => checkRole(user, "workforceAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("shifts").withIndex("by_eventId", (q) => q.eq("eventId", eventId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "Shift", ["notes"], row)));
    return __plainRows;
  },
});

export const listShiftByShiftTypeId = query({
  args: { shiftTypeId: v.id("shiftTypes") },
  handler: async (ctx, { shiftTypeId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("shiftRead", "Shift", () => checkRole(user, "workforceAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("shifts").withIndex("by_shiftTypeId", (q) => q.eq("shiftTypeId", shiftTypeId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "Shift", ["notes"], row)));
    return __plainRows;
  },
});

export const listShiftByRequiredQualificationId = query({
  args: { requiredQualificationId: v.id("qualifications") },
  handler: async (ctx, { requiredQualificationId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("shiftRead", "Shift", () => checkRole(user, "workforceAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("shifts").withIndex("by_requiredQualificationId", (q) => q.eq("requiredQualificationId", requiredQualificationId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "Shift", ["notes"], row)));
    return __plainRows;
  },
});

export const listShiftByRequiredTrainingCompletionId = query({
  args: { requiredTrainingCompletionId: v.id("trainingCompletions") },
  handler: async (ctx, { requiredTrainingCompletionId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("shiftRead", "Shift", () => checkRole(user, "workforceAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("shifts").withIndex("by_requiredTrainingCompletionId", (q) => q.eq("requiredTrainingCompletionId", requiredTrainingCompletionId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "Shift", ["notes"], row)));
    return __plainRows;
  },
});

export const listShiftBySwapAuthorizationId = query({
  args: { swapAuthorizationId: v.id("shiftSwapRequests") },
  handler: async (ctx, { swapAuthorizationId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("shiftRead", "Shift", () => checkRole(user, "workforceAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("shifts").withIndex("by_swapAuthorizationId", (q) => q.eq("swapAuthorizationId", swapAuthorizationId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "Shift", ["notes"], row)));
    return __plainRows;
  },
});

export const listShiftBySwapTargetPersonId = query({
  args: { swapTargetPersonId: v.id("people") },
  handler: async (ctx, { swapTargetPersonId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("shiftRead", "Shift", () => checkRole(user, "workforceAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("shifts").withIndex("by_swapTargetPersonId", (q) => q.eq("swapTargetPersonId", swapTargetPersonId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "Shift", ["notes"], row)));
    return __plainRows;
  },
});

export const listShiftBySwapTargetQualificationId = query({
  args: { swapTargetQualificationId: v.id("qualifications") },
  handler: async (ctx, { swapTargetQualificationId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("shiftRead", "Shift", () => checkRole(user, "workforceAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("shifts").withIndex("by_swapTargetQualificationId", (q) => q.eq("swapTargetQualificationId", swapTargetQualificationId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "Shift", ["notes"], row)));
    return __plainRows;
  },
});

export const listShiftBySwapTargetTrainingCompletionId = query({
  args: { swapTargetTrainingCompletionId: v.id("trainingCompletions") },
  handler: async (ctx, { swapTargetTrainingCompletionId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("shiftRead", "Shift", () => checkRole(user, "workforceAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("shifts").withIndex("by_swapTargetTrainingCompletionId", (q) => q.eq("swapTargetTrainingCompletionId", swapTargetTrainingCompletionId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "Shift", ["notes"], row)));
    return __plainRows;
  },
});

export const listShiftSwapRequest = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("shiftSwapRequestRead", "ShiftSwapRequest", () => checkRole(user, "workforceAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("shiftSwapRequests").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "ShiftSwapRequest", ["reason","reviewNote"], row)));
    return __plainRows;
  },
});

export const getShiftSwapRequest = query({
  args: { id: v.id("shiftSwapRequests") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("shiftSwapRequestRead", "ShiftSwapRequest", () => checkRole(user, "workforceAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = await __decryptDoc(ctx, "ShiftSwapRequest", ["reason","reviewNote"], __rawDoc);
    return __doc;
  },
});

export const listShiftSwapRequestByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("shiftSwapRequestRead", "ShiftSwapRequest", () => checkRole(user, "workforceAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("shiftSwapRequests").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "ShiftSwapRequest", ["reason","reviewNote"], row)));
    return __plainRows;
  },
});

export const listShiftSwapRequestByShiftId = query({
  args: { shiftId: v.id("shifts") },
  handler: async (ctx, { shiftId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("shiftSwapRequestRead", "ShiftSwapRequest", () => checkRole(user, "workforceAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("shiftSwapRequests").withIndex("by_shiftId", (q) => q.eq("shiftId", shiftId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "ShiftSwapRequest", ["reason","reviewNote"], row)));
    return __plainRows;
  },
});

export const listShiftSwapRequestByRequesterPersonId = query({
  args: { requesterPersonId: v.id("people") },
  handler: async (ctx, { requesterPersonId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("shiftSwapRequestRead", "ShiftSwapRequest", () => checkRole(user, "workforceAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("shiftSwapRequests").withIndex("by_requesterPersonId", (q) => q.eq("requesterPersonId", requesterPersonId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "ShiftSwapRequest", ["reason","reviewNote"], row)));
    return __plainRows;
  },
});

export const listShiftSwapRequestByRecipientPersonId = query({
  args: { recipientPersonId: v.id("people") },
  handler: async (ctx, { recipientPersonId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("shiftSwapRequestRead", "ShiftSwapRequest", () => checkRole(user, "workforceAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("shiftSwapRequests").withIndex("by_recipientPersonId", (q) => q.eq("recipientPersonId", recipientPersonId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "ShiftSwapRequest", ["reason","reviewNote"], row)));
    return __plainRows;
  },
});

export const listShiftSwapRequestByShiftTypeId = query({
  args: { shiftTypeId: v.id("shiftTypes") },
  handler: async (ctx, { shiftTypeId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("shiftSwapRequestRead", "ShiftSwapRequest", () => checkRole(user, "workforceAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("shiftSwapRequests").withIndex("by_shiftTypeId", (q) => q.eq("shiftTypeId", shiftTypeId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "ShiftSwapRequest", ["reason","reviewNote"], row)));
    return __plainRows;
  },
});

export const listShiftSwapRequestBySourceQualificationId = query({
  args: { sourceQualificationId: v.id("qualifications") },
  handler: async (ctx, { sourceQualificationId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("shiftSwapRequestRead", "ShiftSwapRequest", () => checkRole(user, "workforceAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("shiftSwapRequests").withIndex("by_sourceQualificationId", (q) => q.eq("sourceQualificationId", sourceQualificationId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "ShiftSwapRequest", ["reason","reviewNote"], row)));
    return __plainRows;
  },
});

export const listShiftSwapRequestByTargetQualificationId = query({
  args: { targetQualificationId: v.id("qualifications") },
  handler: async (ctx, { targetQualificationId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("shiftSwapRequestRead", "ShiftSwapRequest", () => checkRole(user, "workforceAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("shiftSwapRequests").withIndex("by_targetQualificationId", (q) => q.eq("targetQualificationId", targetQualificationId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "ShiftSwapRequest", ["reason","reviewNote"], row)));
    return __plainRows;
  },
});

export const listShiftSwapRequestByTargetTrainingCompletionId = query({
  args: { targetTrainingCompletionId: v.id("trainingCompletions") },
  handler: async (ctx, { targetTrainingCompletionId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("shiftSwapRequestRead", "ShiftSwapRequest", () => checkRole(user, "workforceAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("shiftSwapRequests").withIndex("by_targetTrainingCompletionId", (q) => q.eq("targetTrainingCompletionId", targetTrainingCompletionId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "ShiftSwapRequest", ["reason","reviewNote"], row)));
    return __plainRows;
  },
});

export const listShiftType = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("shiftTypeRead", "ShiftType", () => checkRole(user, "workforceAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("shiftTypes").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const getShiftType = query({
  args: { id: v.id("shiftTypes") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("shiftTypeRead", "ShiftType", () => checkRole(user, "workforceAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    return __doc;
  },
});

export const listShiftTypeByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("shiftTypeRead", "ShiftType", () => checkRole(user, "workforceAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("shiftTypes").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listShiftTypeByRequiredTrainingModuleId = query({
  args: { requiredTrainingModuleId: v.id("trainingModules") },
  handler: async (ctx, { requiredTrainingModuleId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("shiftTypeRead", "ShiftType", () => checkRole(user, "workforceAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("shiftTypes").withIndex("by_requiredTrainingModuleId", (q) => q.eq("requiredTrainingModuleId", requiredTrainingModuleId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listSignatureRequest = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("signatureRequestRead", "SignatureRequest", () => checkRole(user, "salesAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("signatureRequests").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const getSignatureRequest = query({
  args: { id: v.id("signatureRequests") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("signatureRequestRead", "SignatureRequest", () => checkRole(user, "salesAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    return __doc;
  },
});

export const listSignatureRequestByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("signatureRequestRead", "SignatureRequest", () => checkRole(user, "salesAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("signatureRequests").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listSignatureRequestByProposalRevisionId = query({
  args: { proposalRevisionId: v.id("proposalRevisions") },
  handler: async (ctx, { proposalRevisionId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("signatureRequestRead", "SignatureRequest", () => checkRole(user, "salesAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("signatureRequests").withIndex("by_proposalRevisionId", (q) => q.eq("proposalRevisionId", proposalRevisionId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listSignatureRequestByRecipientPersonId = query({
  args: { recipientPersonId: v.id("people") },
  handler: async (ctx, { recipientPersonId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("signatureRequestRead", "SignatureRequest", () => checkRole(user, "salesAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("signatureRequests").withIndex("by_recipientPersonId", (q) => q.eq("recipientPersonId", recipientPersonId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listSignatureRequestByRequestedByPersonId = query({
  args: { requestedByPersonId: v.id("people") },
  handler: async (ctx, { requestedByPersonId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("signatureRequestRead", "SignatureRequest", () => checkRole(user, "salesAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("signatureRequests").withIndex("by_requestedByPersonId", (q) => q.eq("requestedByPersonId", requestedByPersonId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listStaffMessage = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("staffMessageRead", "StaffMessage", () => checkRole(user, "staffAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("staffMessages").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "StaffMessage", ["body"], row)));
    return __plainRows;
  },
});

export const getStaffMessage = query({
  args: { id: v.id("staffMessages") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("staffMessageRead", "StaffMessage", () => checkRole(user, "staffAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = await __decryptDoc(ctx, "StaffMessage", ["body"], __rawDoc);
    return __doc;
  },
});

export const listStaffMessageByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("staffMessageRead", "StaffMessage", () => checkRole(user, "staffAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("staffMessages").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "StaffMessage", ["body"], row)));
    return __plainRows;
  },
});

export const listStaffMessageBySenderPersonId = query({
  args: { senderPersonId: v.id("people") },
  handler: async (ctx, { senderPersonId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("staffMessageRead", "StaffMessage", () => checkRole(user, "staffAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("staffMessages").withIndex("by_senderPersonId", (q) => q.eq("senderPersonId", senderPersonId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "StaffMessage", ["body"], row)));
    return __plainRows;
  },
});

export const listStaffMessageByRecipientPersonId = query({
  args: { recipientPersonId: v.id("people") },
  handler: async (ctx, { recipientPersonId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("staffMessageRead", "StaffMessage", () => checkRole(user, "staffAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("staffMessages").withIndex("by_recipientPersonId", (q) => q.eq("recipientPersonId", recipientPersonId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "StaffMessage", ["body"], row)));
    return __plainRows;
  },
});

export const listStockCountLine = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("stockCountLineRead", "StockCountLine", () => checkRole(user, "inventoryAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("stockCountLines").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const getStockCountLine = query({
  args: { id: v.id("stockCountLines") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("stockCountLineRead", "StockCountLine", () => checkRole(user, "inventoryAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    return __doc;
  },
});

export const listStockCountLineByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("stockCountLineRead", "StockCountLine", () => checkRole(user, "inventoryAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("stockCountLines").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listStockCountLineByStockCountSessionId = query({
  args: { stockCountSessionId: v.id("stockCountSessions") },
  handler: async (ctx, { stockCountSessionId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("stockCountLineRead", "StockCountLine", () => checkRole(user, "inventoryAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("stockCountLines").withIndex("by_stockCountSessionId", (q) => q.eq("stockCountSessionId", stockCountSessionId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listStockCountLineByInventoryItemId = query({
  args: { inventoryItemId: v.id("inventoryItems") },
  handler: async (ctx, { inventoryItemId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("stockCountLineRead", "StockCountLine", () => checkRole(user, "inventoryAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("stockCountLines").withIndex("by_inventoryItemId", (q) => q.eq("inventoryItemId", inventoryItemId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listStockCountLineByLocationId = query({
  args: { locationId: v.id("storageLocations") },
  handler: async (ctx, { locationId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("stockCountLineRead", "StockCountLine", () => checkRole(user, "inventoryAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("stockCountLines").withIndex("by_locationId", (q) => q.eq("locationId", locationId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listStockCountLineByIngredientId = query({
  args: { ingredientId: v.id("ingredients") },
  handler: async (ctx, { ingredientId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("stockCountLineRead", "StockCountLine", () => checkRole(user, "inventoryAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("stockCountLines").withIndex("by_ingredientId", (q) => q.eq("ingredientId", ingredientId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listStockCountSession = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("stockCountSessionRead", "StockCountSession", () => checkRole(user, "inventoryAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("stockCountSessions").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const getStockCountSession = query({
  args: { id: v.id("stockCountSessions") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("stockCountSessionRead", "StockCountSession", () => checkRole(user, "inventoryAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    return __doc;
  },
});

export const listStockCountSessionByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("stockCountSessionRead", "StockCountSession", () => checkRole(user, "inventoryAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("stockCountSessions").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listStockTransfer = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("stockTransferRead", "StockTransfer", () => checkRole(user, "inventoryAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("stockTransfers").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const getStockTransfer = query({
  args: { id: v.id("stockTransfers") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("stockTransferRead", "StockTransfer", () => checkRole(user, "inventoryAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    return __doc;
  },
});

export const listStockTransferByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("stockTransferRead", "StockTransfer", () => checkRole(user, "inventoryAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("stockTransfers").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listStockTransferBySourceInventoryItemId = query({
  args: { sourceInventoryItemId: v.id("inventoryItems") },
  handler: async (ctx, { sourceInventoryItemId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("stockTransferRead", "StockTransfer", () => checkRole(user, "inventoryAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("stockTransfers").withIndex("by_sourceInventoryItemId", (q) => q.eq("sourceInventoryItemId", sourceInventoryItemId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listStockTransferByDestinationInventoryItemId = query({
  args: { destinationInventoryItemId: v.id("inventoryItems") },
  handler: async (ctx, { destinationInventoryItemId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("stockTransferRead", "StockTransfer", () => checkRole(user, "inventoryAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("stockTransfers").withIndex("by_destinationInventoryItemId", (q) => q.eq("destinationInventoryItemId", destinationInventoryItemId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listStockTransferByIngredientId = query({
  args: { ingredientId: v.id("ingredients") },
  handler: async (ctx, { ingredientId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("stockTransferRead", "StockTransfer", () => checkRole(user, "inventoryAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("stockTransfers").withIndex("by_ingredientId", (q) => q.eq("ingredientId", ingredientId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listStockTransferBySourceLocationId = query({
  args: { sourceLocationId: v.id("storageLocations") },
  handler: async (ctx, { sourceLocationId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("stockTransferRead", "StockTransfer", () => checkRole(user, "inventoryAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("stockTransfers").withIndex("by_sourceLocationId", (q) => q.eq("sourceLocationId", sourceLocationId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listStockTransferByDestinationLocationId = query({
  args: { destinationLocationId: v.id("storageLocations") },
  handler: async (ctx, { destinationLocationId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("stockTransferRead", "StockTransfer", () => checkRole(user, "inventoryAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("stockTransfers").withIndex("by_destinationLocationId", (q) => q.eq("destinationLocationId", destinationLocationId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listStorageLocation = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("storageLocationRead", "StorageLocation", () => checkRole(user, "inventoryAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("storageLocations").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return (__plainRows).map((__row) => { (__row as any).isActive = ((__row as any).status === "active"); return { ...(__row as any), isActive: (__row as any).isActive }; });
  },
});

export const getStorageLocation = query({
  args: { id: v.id("storageLocations") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("storageLocationRead", "StorageLocation", () => checkRole(user, "inventoryAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    (__doc as any).isActive = ((__doc as any).status === "active");
    const __hydrated = { ...(__doc as any), isActive: (__doc as any).isActive };
    return __hydrated;
  },
});

export const listStorageLocationByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("storageLocationRead", "StorageLocation", () => checkRole(user, "inventoryAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("storageLocations").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return (__plainRows).map((__row) => { (__row as any).isActive = ((__row as any).status === "active"); return { ...(__row as any), isActive: (__row as any).isActive }; });
  },
});

export const listSyncError = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("syncErrorRead", "SyncError", () => checkRole(user, "staffAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("syncErrors").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const getSyncError = query({
  args: { id: v.id("syncErrors") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("syncErrorRead", "SyncError", () => checkRole(user, "staffAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    return __doc;
  },
});

export const listSyncErrorByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("syncErrorRead", "SyncError", () => checkRole(user, "staffAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("syncErrors").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listTaxRate = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("taxRateRead", "TaxRate", () => checkRole(user, "financeAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("taxRates").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return (__plainRows).map((__row) => { (__row as any).appliesToAny = (((__row as any).appliesToFood || (__row as any).appliesToService) || (__row as any).appliesToRental); return { ...(__row as any), appliesToAny: (__row as any).appliesToAny }; });
  },
});

export const getTaxRate = query({
  args: { id: v.id("taxRates") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("taxRateRead", "TaxRate", () => checkRole(user, "financeAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    (__doc as any).appliesToAny = (((__doc as any).appliesToFood || (__doc as any).appliesToService) || (__doc as any).appliesToRental);
    const __hydrated = { ...(__doc as any), appliesToAny: (__doc as any).appliesToAny };
    return __hydrated;
  },
});

export const listTaxRateByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("taxRateRead", "TaxRate", () => checkRole(user, "financeAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("taxRates").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return (__plainRows).map((__row) => { (__row as any).appliesToAny = (((__row as any).appliesToFood || (__row as any).appliesToService) || (__row as any).appliesToRental); return { ...(__row as any), appliesToAny: (__row as any).appliesToAny }; });
  },
});

export const listTimeOffRequest = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("timeOffRequests").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "TimeOffRequest", ["reason","responseNote"], row)));
    const __visibleRows: any[] = [];
    for (const __row of __plainRows) {
      if (!__allowsRead("timeOffRequestRead", "TimeOffRequest", () => ((checkRole(user, "workforceManageAccess") || ((user.id != null) && (__row.requesterAuthSubjectId === user.id))) || (__row.requesterAuthSubjectId == null)))) continue;
      __visibleRows.push(__row);
    }
    return __visibleRows;
  },
});

export const getTimeOffRequest = query({
  args: { id: v.id("timeOffRequests") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = await __decryptDoc(ctx, "TimeOffRequest", ["reason","responseNote"], __rawDoc);
    if (!__allowsRead("timeOffRequestRead", "TimeOffRequest", () => ((checkRole(user, "workforceManageAccess") || ((user.id != null) && (__doc.requesterAuthSubjectId === user.id))) || (__doc.requesterAuthSubjectId == null)))) return null;
    return __doc;
  },
});

export const listTimeOffRequestByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("timeOffRequests").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "TimeOffRequest", ["reason","responseNote"], row)));
    const __visibleRows: any[] = [];
    for (const __row of __plainRows) {
      if (!__allowsRead("timeOffRequestRead", "TimeOffRequest", () => ((checkRole(user, "workforceManageAccess") || ((user.id != null) && (__row.requesterAuthSubjectId === user.id))) || (__row.requesterAuthSubjectId == null)))) continue;
      __visibleRows.push(__row);
    }
    return __visibleRows;
  },
});

export const listTimeOffRequestByPersonId = query({
  args: { personId: v.id("people") },
  handler: async (ctx, { personId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("timeOffRequests").withIndex("by_personId", (q) => q.eq("personId", personId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "TimeOffRequest", ["reason","responseNote"], row)));
    const __visibleRows: any[] = [];
    for (const __row of __plainRows) {
      if (!__allowsRead("timeOffRequestRead", "TimeOffRequest", () => ((checkRole(user, "workforceManageAccess") || ((user.id != null) && (__row.requesterAuthSubjectId === user.id))) || (__row.requesterAuthSubjectId == null)))) continue;
      __visibleRows.push(__row);
    }
    return __visibleRows;
  },
});

export const listTimeRecord = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("timeRecordRead", "TimeRecord", () => checkRole(user, "workforceAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("timeRecords").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "TimeRecord", ["notes"], row)));
    return __plainRows;
  },
});

export const getTimeRecord = query({
  args: { id: v.id("timeRecords") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("timeRecordRead", "TimeRecord", () => checkRole(user, "workforceAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = await __decryptDoc(ctx, "TimeRecord", ["notes"], __rawDoc);
    return __doc;
  },
});

export const listTimeRecordByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("timeRecordRead", "TimeRecord", () => checkRole(user, "workforceAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("timeRecords").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "TimeRecord", ["notes"], row)));
    return __plainRows;
  },
});

export const listTimeRecordByPersonId = query({
  args: { personId: v.id("people") },
  handler: async (ctx, { personId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("timeRecordRead", "TimeRecord", () => checkRole(user, "workforceAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("timeRecords").withIndex("by_personId", (q) => q.eq("personId", personId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "TimeRecord", ["notes"], row)));
    return __plainRows;
  },
});

export const listTimeRecordByShiftId = query({
  args: { shiftId: v.id("shifts") },
  handler: async (ctx, { shiftId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("timeRecordRead", "TimeRecord", () => checkRole(user, "workforceAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("timeRecords").withIndex("by_shiftId", (q) => q.eq("shiftId", shiftId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "TimeRecord", ["notes"], row)));
    return __plainRows;
  },
});

export const listTimeRecordByEventId = query({
  args: { eventId: v.id("events") },
  handler: async (ctx, { eventId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("timeRecordRead", "TimeRecord", () => checkRole(user, "workforceAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("timeRecords").withIndex("by_eventId", (q) => q.eq("eventId", eventId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "TimeRecord", ["notes"], row)));
    return __plainRows;
  },
});

export const listTrainingCompletion = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("trainingCompletionRead", "TrainingCompletion", () => checkRole(user, "workforceAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("trainingCompletions").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "TrainingCompletion", ["notes"], row)));
    return __plainRows;
  },
});

export const getTrainingCompletion = query({
  args: { id: v.id("trainingCompletions") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("trainingCompletionRead", "TrainingCompletion", () => checkRole(user, "workforceAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = await __decryptDoc(ctx, "TrainingCompletion", ["notes"], __rawDoc);
    return __doc;
  },
});

export const listTrainingCompletionByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("trainingCompletionRead", "TrainingCompletion", () => checkRole(user, "workforceAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("trainingCompletions").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "TrainingCompletion", ["notes"], row)));
    return __plainRows;
  },
});

export const listTrainingCompletionByPersonId = query({
  args: { personId: v.id("people") },
  handler: async (ctx, { personId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("trainingCompletionRead", "TrainingCompletion", () => checkRole(user, "workforceAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("trainingCompletions").withIndex("by_personId", (q) => q.eq("personId", personId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "TrainingCompletion", ["notes"], row)));
    return __plainRows;
  },
});

export const listTrainingCompletionByTrainingModuleId = query({
  args: { trainingModuleId: v.id("trainingModules") },
  handler: async (ctx, { trainingModuleId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("trainingCompletionRead", "TrainingCompletion", () => checkRole(user, "workforceAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("trainingCompletions").withIndex("by_trainingModuleId", (q) => q.eq("trainingModuleId", trainingModuleId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "TrainingCompletion", ["notes"], row)));
    return __plainRows;
  },
});

export const listTrainingModule = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("trainingModuleRead", "TrainingModule", () => checkRole(user, "workforceAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("trainingModules").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const getTrainingModule = query({
  args: { id: v.id("trainingModules") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("trainingModuleRead", "TrainingModule", () => checkRole(user, "workforceAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    return __doc;
  },
});

export const listTrainingModuleByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("trainingModuleRead", "TrainingModule", () => checkRole(user, "workforceAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("trainingModules").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listVehicle = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("vehicleRead", "Vehicle", () => (checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("vehicles").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return (__plainRows).map((__row) => { (__row as any).isOperational = (((__row as any).operationalStatus === "available") || ((__row as any).operationalStatus === "in_use")); return { ...(__row as any), isOperational: (__row as any).isOperational }; });
  },
});

export const getVehicle = query({
  args: { id: v.id("vehicles") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("vehicleRead", "Vehicle", () => (checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    (__doc as any).isOperational = (((__doc as any).operationalStatus === "available") || ((__doc as any).operationalStatus === "in_use"));
    const __hydrated = { ...(__doc as any), isOperational: (__doc as any).isOperational };
    return __hydrated;
  },
});

export const listVehicleByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("vehicleRead", "Vehicle", () => (checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("vehicles").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return (__plainRows).map((__row) => { (__row as any).isOperational = (((__row as any).operationalStatus === "available") || ((__row as any).operationalStatus === "in_use")); return { ...(__row as any), isOperational: (__row as any).isOperational }; });
  },
});

export const listVehicleFuelLog = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("vehicleFuelRead", "VehicleFuelLog", () => (checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("vehicleFuelLogs").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const getVehicleFuelLog = query({
  args: { id: v.id("vehicleFuelLogs") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("vehicleFuelRead", "VehicleFuelLog", () => (checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    return __doc;
  },
});

export const listVehicleFuelLogByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("vehicleFuelRead", "VehicleFuelLog", () => (checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("vehicleFuelLogs").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listVehicleFuelLogByVehicleId = query({
  args: { vehicleId: v.id("vehicles") },
  handler: async (ctx, { vehicleId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("vehicleFuelRead", "VehicleFuelLog", () => (checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("vehicleFuelLogs").withIndex("by_vehicleId", (q) => q.eq("vehicleId", vehicleId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listVehicleMaintenanceSchedule = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("vehicleMaintenanceRead", "VehicleMaintenanceSchedule", () => (checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("vehicleMaintenanceSchedules").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const getVehicleMaintenanceSchedule = query({
  args: { id: v.id("vehicleMaintenanceSchedules") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("vehicleMaintenanceRead", "VehicleMaintenanceSchedule", () => (checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    return __doc;
  },
});

export const listVehicleMaintenanceScheduleByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("vehicleMaintenanceRead", "VehicleMaintenanceSchedule", () => (checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("vehicleMaintenanceSchedules").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listVehicleMaintenanceScheduleByVehicleId = query({
  args: { vehicleId: v.id("vehicles") },
  handler: async (ctx, { vehicleId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("vehicleMaintenanceRead", "VehicleMaintenanceSchedule", () => (checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("vehicleMaintenanceSchedules").withIndex("by_vehicleId", (q) => q.eq("vehicleId", vehicleId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listVehicleServiceEntry = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("vehicleServiceRead", "VehicleServiceEntry", () => (checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("vehicleServiceEntries").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const getVehicleServiceEntry = query({
  args: { id: v.id("vehicleServiceEntries") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("vehicleServiceRead", "VehicleServiceEntry", () => (checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    return __doc;
  },
});

export const listVehicleServiceEntryByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("vehicleServiceRead", "VehicleServiceEntry", () => (checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("vehicleServiceEntries").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listVehicleServiceEntryByMaintenanceScheduleId = query({
  args: { maintenanceScheduleId: v.id("vehicleMaintenanceSchedules") },
  handler: async (ctx, { maintenanceScheduleId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("vehicleServiceRead", "VehicleServiceEntry", () => (checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("vehicleServiceEntries").withIndex("by_maintenanceScheduleId", (q) => q.eq("maintenanceScheduleId", maintenanceScheduleId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listVehicleServiceEntryByVehicleId = query({
  args: { vehicleId: v.id("vehicles") },
  handler: async (ctx, { vehicleId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("vehicleServiceRead", "VehicleServiceEntry", () => (checkRole(user, "logisticsAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("vehicleServiceEntries").withIndex("by_vehicleId", (q) => q.eq("vehicleId", vehicleId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listVendor = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("vendorRead", "Vendor", () => checkRole(user, "procurementAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("vendors").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "Vendor", ["email","phone","addressLine1","city","region","postalCode","countryCode"], row)));
    return __plainRows;
  },
});

export const getVendor = query({
  args: { id: v.id("vendors") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("vendorRead", "Vendor", () => checkRole(user, "procurementAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = await __decryptDoc(ctx, "Vendor", ["email","phone","addressLine1","city","region","postalCode","countryCode"], __rawDoc);
    return __doc;
  },
});

export const listVendorByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("vendorRead", "Vendor", () => checkRole(user, "procurementAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("vendors").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "Vendor", ["email","phone","addressLine1","city","region","postalCode","countryCode"], row)));
    return __plainRows;
  },
});

export const listVendorContact = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("vendorContactRead", "VendorContact", () => checkRole(user, "procurementAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("vendorContacts").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "VendorContact", ["email","phone"], row)));
    return __plainRows;
  },
});

export const getVendorContact = query({
  args: { id: v.id("vendorContacts") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("vendorContactRead", "VendorContact", () => checkRole(user, "procurementAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = await __decryptDoc(ctx, "VendorContact", ["email","phone"], __rawDoc);
    return __doc;
  },
});

export const listVendorContactByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("vendorContactRead", "VendorContact", () => checkRole(user, "procurementAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("vendorContacts").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "VendorContact", ["email","phone"], row)));
    return __plainRows;
  },
});

export const listVendorContactByVendorId = query({
  args: { vendorId: v.id("vendors") },
  handler: async (ctx, { vendorId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("vendorContactRead", "VendorContact", () => checkRole(user, "procurementAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("vendorContacts").withIndex("by_vendorId", (q) => q.eq("vendorId", vendorId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "VendorContact", ["email","phone"], row)));
    return __plainRows;
  },
});

export const listVendorContract = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("vendorContractRead", "VendorContract", () => checkRole(user, "procurementAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("vendorContracts").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const getVendorContract = query({
  args: { id: v.id("vendorContracts") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("vendorContractRead", "VendorContract", () => checkRole(user, "procurementAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    return __doc;
  },
});

export const listVendorContractByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("vendorContractRead", "VendorContract", () => checkRole(user, "procurementAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("vendorContracts").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listVendorContractByVendorId = query({
  args: { vendorId: v.id("vendors") },
  handler: async (ctx, { vendorId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("vendorContractRead", "VendorContract", () => checkRole(user, "procurementAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("vendorContracts").withIndex("by_vendorId", (q) => q.eq("vendorId", vendorId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listVendorContractPriceTier = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("vendorContractTierRead", "VendorContractPriceTier", () => checkRole(user, "procurementAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("vendorContractPriceTiers").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const getVendorContractPriceTier = query({
  args: { id: v.id("vendorContractPriceTiers") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("vendorContractTierRead", "VendorContractPriceTier", () => checkRole(user, "procurementAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    return __doc;
  },
});

export const listVendorContractPriceTierByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("vendorContractTierRead", "VendorContractPriceTier", () => checkRole(user, "procurementAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("vendorContractPriceTiers").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listVendorContractPriceTierByContractId = query({
  args: { contractId: v.id("vendorContracts") },
  handler: async (ctx, { contractId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("vendorContractTierRead", "VendorContractPriceTier", () => checkRole(user, "procurementAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("vendorContractPriceTiers").withIndex("by_contractId", (q) => q.eq("contractId", contractId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listVendorOrder = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("vendorOrderRead", "VendorOrder", () => (checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("vendorOrders").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    const __projectedRows: any[] = [];
    for (const __row of __plainRows as any[]) {
      {
      const __lookup = ((__row as any) as any).tenantId;
      ((__row as any) as any).purchasingConfig = __lookup != null
      ? await ctx.db.query("weeklyPurchasingConfigs").withIndex("by_tenantId", (q: any) => q.eq("tenantId", __lookup)).first()
      : null;
      }
      (__row as any).lines = await ctx.db.query("vendorOrderLines").withIndex("by_vendorOrderId", (q: any) => q.eq("vendorOrderId", __row._id)).collect();
      (__row as any).isDraft = ((__row as any).status === "draft");
      (__row as any).isPendingApproval = ((__row as any).status === "pending_approval");
      (__row as any).isOpenForReceiving = (((__row as any).status === "confirmed") || ((__row as any).status === "partially_received"));
      (__row as any).needsSpendApproval = ((((__row as any).purchasingConfig != null) && ((__row as any).purchasingConfig.orderApprovalThresholdAmount != null)) && ((__row as any).totalAmount > (__row as any).purchasingConfig.orderApprovalThresholdAmount));
      (__row as any).hasIncompleteLines = ((((__row as any).lines) ?? []).filter((line: Doc<"vendorOrderLines">) => ((((line.deletedAt == null) && (line.status !== "cancelled")) && (line.status !== "complete")))).length > 0);
      (__row as any).liveTotalAmount = (((((__row as any).lines) ?? []).map((line: Doc<"vendorOrderLines">) => ((((line.deletedAt == null) && (line.lineTotalAmount != null)) ? line.lineTotalAmount : 0))).reduce((acc: number, v: unknown) => acc + (typeof v === "number" ? v : 0), 0) + (__row as any).taxAmount) + (__row as any).shippingAmount);
      __projectedRows.push({ ...(__row as any), isDraft: (__row as any).isDraft, isPendingApproval: (__row as any).isPendingApproval, isOpenForReceiving: (__row as any).isOpenForReceiving, needsSpendApproval: (__row as any).needsSpendApproval, hasIncompleteLines: (__row as any).hasIncompleteLines, liveTotalAmount: (__row as any).liveTotalAmount });
    }
    return __projectedRows;
  },
});

export const getVendorOrder = query({
  args: { id: v.id("vendorOrders") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("vendorOrderRead", "VendorOrder", () => (checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    {
      const __lookup = ((__doc as any) as any).tenantId;
      ((__doc as any) as any).purchasingConfig = __lookup != null
        ? await ctx.db.query("weeklyPurchasingConfigs").withIndex("by_tenantId", (q: any) => q.eq("tenantId", __lookup)).first()
        : null;
    }
    (__doc as any).lines = await ctx.db.query("vendorOrderLines").withIndex("by_vendorOrderId", (q: any) => q.eq("vendorOrderId", __doc._id)).collect();
    (__doc as any).isDraft = ((__doc as any).status === "draft");
    (__doc as any).isPendingApproval = ((__doc as any).status === "pending_approval");
    (__doc as any).isOpenForReceiving = (((__doc as any).status === "confirmed") || ((__doc as any).status === "partially_received"));
    (__doc as any).needsSpendApproval = ((((__doc as any).purchasingConfig != null) && ((__doc as any).purchasingConfig.orderApprovalThresholdAmount != null)) && ((__doc as any).totalAmount > (__doc as any).purchasingConfig.orderApprovalThresholdAmount));
    (__doc as any).hasIncompleteLines = ((((__doc as any).lines) ?? []).filter((line: Doc<"vendorOrderLines">) => ((((line.deletedAt == null) && (line.status !== "cancelled")) && (line.status !== "complete")))).length > 0);
    (__doc as any).liveTotalAmount = (((((__doc as any).lines) ?? []).map((line: Doc<"vendorOrderLines">) => ((((line.deletedAt == null) && (line.lineTotalAmount != null)) ? line.lineTotalAmount : 0))).reduce((acc: number, v: unknown) => acc + (typeof v === "number" ? v : 0), 0) + (__doc as any).taxAmount) + (__doc as any).shippingAmount);
    const __hydrated = { ...(__doc as any), isDraft: (__doc as any).isDraft, isPendingApproval: (__doc as any).isPendingApproval, isOpenForReceiving: (__doc as any).isOpenForReceiving, needsSpendApproval: (__doc as any).needsSpendApproval, hasIncompleteLines: (__doc as any).hasIncompleteLines, liveTotalAmount: (__doc as any).liveTotalAmount };
    return __hydrated;
  },
});

export const listVendorOrderByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("vendorOrderRead", "VendorOrder", () => (checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("vendorOrders").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    const __projectedRows: any[] = [];
    for (const __row of __plainRows as any[]) {
      {
      const __lookup = ((__row as any) as any).tenantId;
      ((__row as any) as any).purchasingConfig = __lookup != null
      ? await ctx.db.query("weeklyPurchasingConfigs").withIndex("by_tenantId", (q: any) => q.eq("tenantId", __lookup)).first()
      : null;
      }
      (__row as any).lines = await ctx.db.query("vendorOrderLines").withIndex("by_vendorOrderId", (q: any) => q.eq("vendorOrderId", __row._id)).collect();
      (__row as any).isDraft = ((__row as any).status === "draft");
      (__row as any).isPendingApproval = ((__row as any).status === "pending_approval");
      (__row as any).isOpenForReceiving = (((__row as any).status === "confirmed") || ((__row as any).status === "partially_received"));
      (__row as any).needsSpendApproval = ((((__row as any).purchasingConfig != null) && ((__row as any).purchasingConfig.orderApprovalThresholdAmount != null)) && ((__row as any).totalAmount > (__row as any).purchasingConfig.orderApprovalThresholdAmount));
      (__row as any).hasIncompleteLines = ((((__row as any).lines) ?? []).filter((line: Doc<"vendorOrderLines">) => ((((line.deletedAt == null) && (line.status !== "cancelled")) && (line.status !== "complete")))).length > 0);
      (__row as any).liveTotalAmount = (((((__row as any).lines) ?? []).map((line: Doc<"vendorOrderLines">) => ((((line.deletedAt == null) && (line.lineTotalAmount != null)) ? line.lineTotalAmount : 0))).reduce((acc: number, v: unknown) => acc + (typeof v === "number" ? v : 0), 0) + (__row as any).taxAmount) + (__row as any).shippingAmount);
      __projectedRows.push({ ...(__row as any), isDraft: (__row as any).isDraft, isPendingApproval: (__row as any).isPendingApproval, isOpenForReceiving: (__row as any).isOpenForReceiving, needsSpendApproval: (__row as any).needsSpendApproval, hasIncompleteLines: (__row as any).hasIncompleteLines, liveTotalAmount: (__row as any).liveTotalAmount });
    }
    return __projectedRows;
  },
});

export const listVendorOrderByVendorId = query({
  args: { vendorId: v.id("vendors") },
  handler: async (ctx, { vendorId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("vendorOrderRead", "VendorOrder", () => (checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("vendorOrders").withIndex("by_vendorId", (q) => q.eq("vendorId", vendorId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    const __projectedRows: any[] = [];
    for (const __row of __plainRows as any[]) {
      {
      const __lookup = ((__row as any) as any).tenantId;
      ((__row as any) as any).purchasingConfig = __lookup != null
      ? await ctx.db.query("weeklyPurchasingConfigs").withIndex("by_tenantId", (q: any) => q.eq("tenantId", __lookup)).first()
      : null;
      }
      (__row as any).lines = await ctx.db.query("vendorOrderLines").withIndex("by_vendorOrderId", (q: any) => q.eq("vendorOrderId", __row._id)).collect();
      (__row as any).isDraft = ((__row as any).status === "draft");
      (__row as any).isPendingApproval = ((__row as any).status === "pending_approval");
      (__row as any).isOpenForReceiving = (((__row as any).status === "confirmed") || ((__row as any).status === "partially_received"));
      (__row as any).needsSpendApproval = ((((__row as any).purchasingConfig != null) && ((__row as any).purchasingConfig.orderApprovalThresholdAmount != null)) && ((__row as any).totalAmount > (__row as any).purchasingConfig.orderApprovalThresholdAmount));
      (__row as any).hasIncompleteLines = ((((__row as any).lines) ?? []).filter((line: Doc<"vendorOrderLines">) => ((((line.deletedAt == null) && (line.status !== "cancelled")) && (line.status !== "complete")))).length > 0);
      (__row as any).liveTotalAmount = (((((__row as any).lines) ?? []).map((line: Doc<"vendorOrderLines">) => ((((line.deletedAt == null) && (line.lineTotalAmount != null)) ? line.lineTotalAmount : 0))).reduce((acc: number, v: unknown) => acc + (typeof v === "number" ? v : 0), 0) + (__row as any).taxAmount) + (__row as any).shippingAmount);
      __projectedRows.push({ ...(__row as any), isDraft: (__row as any).isDraft, isPendingApproval: (__row as any).isPendingApproval, isOpenForReceiving: (__row as any).isOpenForReceiving, needsSpendApproval: (__row as any).needsSpendApproval, hasIncompleteLines: (__row as any).hasIncompleteLines, liveTotalAmount: (__row as any).liveTotalAmount });
    }
    return __projectedRows;
  },
});

export const listVendorOrderByEventId = query({
  args: { eventId: v.id("events") },
  handler: async (ctx, { eventId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("vendorOrderRead", "VendorOrder", () => (checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("vendorOrders").withIndex("by_eventId", (q) => q.eq("eventId", eventId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    const __projectedRows: any[] = [];
    for (const __row of __plainRows as any[]) {
      {
      const __lookup = ((__row as any) as any).tenantId;
      ((__row as any) as any).purchasingConfig = __lookup != null
      ? await ctx.db.query("weeklyPurchasingConfigs").withIndex("by_tenantId", (q: any) => q.eq("tenantId", __lookup)).first()
      : null;
      }
      (__row as any).lines = await ctx.db.query("vendorOrderLines").withIndex("by_vendorOrderId", (q: any) => q.eq("vendorOrderId", __row._id)).collect();
      (__row as any).isDraft = ((__row as any).status === "draft");
      (__row as any).isPendingApproval = ((__row as any).status === "pending_approval");
      (__row as any).isOpenForReceiving = (((__row as any).status === "confirmed") || ((__row as any).status === "partially_received"));
      (__row as any).needsSpendApproval = ((((__row as any).purchasingConfig != null) && ((__row as any).purchasingConfig.orderApprovalThresholdAmount != null)) && ((__row as any).totalAmount > (__row as any).purchasingConfig.orderApprovalThresholdAmount));
      (__row as any).hasIncompleteLines = ((((__row as any).lines) ?? []).filter((line: Doc<"vendorOrderLines">) => ((((line.deletedAt == null) && (line.status !== "cancelled")) && (line.status !== "complete")))).length > 0);
      (__row as any).liveTotalAmount = (((((__row as any).lines) ?? []).map((line: Doc<"vendorOrderLines">) => ((((line.deletedAt == null) && (line.lineTotalAmount != null)) ? line.lineTotalAmount : 0))).reduce((acc: number, v: unknown) => acc + (typeof v === "number" ? v : 0), 0) + (__row as any).taxAmount) + (__row as any).shippingAmount);
      __projectedRows.push({ ...(__row as any), isDraft: (__row as any).isDraft, isPendingApproval: (__row as any).isPendingApproval, isOpenForReceiving: (__row as any).isOpenForReceiving, needsSpendApproval: (__row as any).needsSpendApproval, hasIncompleteLines: (__row as any).hasIncompleteLines, liveTotalAmount: (__row as any).liveTotalAmount });
    }
    return __projectedRows;
  },
});

export const listVendorOrderByPurchasingConfigId = query({
  args: { purchasingConfigId: v.id("weeklyPurchasingConfigs") },
  handler: async (ctx, { purchasingConfigId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("vendorOrderRead", "VendorOrder", () => (checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("vendorOrders").withIndex("by_purchasingConfigId", (q) => q.eq("purchasingConfigId", purchasingConfigId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    const __projectedRows: any[] = [];
    for (const __row of __plainRows as any[]) {
      {
      const __lookup = ((__row as any) as any).tenantId;
      ((__row as any) as any).purchasingConfig = __lookup != null
      ? await ctx.db.query("weeklyPurchasingConfigs").withIndex("by_tenantId", (q: any) => q.eq("tenantId", __lookup)).first()
      : null;
      }
      (__row as any).lines = await ctx.db.query("vendorOrderLines").withIndex("by_vendorOrderId", (q: any) => q.eq("vendorOrderId", __row._id)).collect();
      (__row as any).isDraft = ((__row as any).status === "draft");
      (__row as any).isPendingApproval = ((__row as any).status === "pending_approval");
      (__row as any).isOpenForReceiving = (((__row as any).status === "confirmed") || ((__row as any).status === "partially_received"));
      (__row as any).needsSpendApproval = ((((__row as any).purchasingConfig != null) && ((__row as any).purchasingConfig.orderApprovalThresholdAmount != null)) && ((__row as any).totalAmount > (__row as any).purchasingConfig.orderApprovalThresholdAmount));
      (__row as any).hasIncompleteLines = ((((__row as any).lines) ?? []).filter((line: Doc<"vendorOrderLines">) => ((((line.deletedAt == null) && (line.status !== "cancelled")) && (line.status !== "complete")))).length > 0);
      (__row as any).liveTotalAmount = (((((__row as any).lines) ?? []).map((line: Doc<"vendorOrderLines">) => ((((line.deletedAt == null) && (line.lineTotalAmount != null)) ? line.lineTotalAmount : 0))).reduce((acc: number, v: unknown) => acc + (typeof v === "number" ? v : 0), 0) + (__row as any).taxAmount) + (__row as any).shippingAmount);
      __projectedRows.push({ ...(__row as any), isDraft: (__row as any).isDraft, isPendingApproval: (__row as any).isPendingApproval, isOpenForReceiving: (__row as any).isOpenForReceiving, needsSpendApproval: (__row as any).needsSpendApproval, hasIncompleteLines: (__row as any).hasIncompleteLines, liveTotalAmount: (__row as any).liveTotalAmount });
    }
    return __projectedRows;
  },
});

export const listVendorOrderLine = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("vendorOrderLineRead", "VendorOrderLine", () => (checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("vendorOrderLines").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return (__plainRows).map((__row) => { (__row as any).lineTotal = ((__row as any).orderedQuantity * (__row as any).unitCost); (__row as any).remainingQuantity = ((__row as any).orderedQuantity - (__row as any).receivedQuantity); (__row as any).isFullyReceived = (((__row as any).orderedQuantity > 0) && ((__row as any).receivedQuantity >= (__row as any).orderedQuantity)); (__row as any).hasReceivingDiscrepancy = (((__row as any).discrepancyQuantity != null) && ((__row as any).discrepancyQuantity > 0)); return { ...(__row as any), lineTotal: (__row as any).lineTotal, remainingQuantity: (__row as any).remainingQuantity, isFullyReceived: (__row as any).isFullyReceived, hasReceivingDiscrepancy: (__row as any).hasReceivingDiscrepancy }; });
  },
});

export const getVendorOrderLine = query({
  args: { id: v.id("vendorOrderLines") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("vendorOrderLineRead", "VendorOrderLine", () => (checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    (__doc as any).lineTotal = ((__doc as any).orderedQuantity * (__doc as any).unitCost);
    (__doc as any).remainingQuantity = ((__doc as any).orderedQuantity - (__doc as any).receivedQuantity);
    (__doc as any).isFullyReceived = (((__doc as any).orderedQuantity > 0) && ((__doc as any).receivedQuantity >= (__doc as any).orderedQuantity));
    (__doc as any).hasReceivingDiscrepancy = (((__doc as any).discrepancyQuantity != null) && ((__doc as any).discrepancyQuantity > 0));
    const __hydrated = { ...(__doc as any), lineTotal: (__doc as any).lineTotal, remainingQuantity: (__doc as any).remainingQuantity, isFullyReceived: (__doc as any).isFullyReceived, hasReceivingDiscrepancy: (__doc as any).hasReceivingDiscrepancy };
    return __hydrated;
  },
});

export const listVendorOrderLineByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("vendorOrderLineRead", "VendorOrderLine", () => (checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("vendorOrderLines").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return (__plainRows).map((__row) => { (__row as any).lineTotal = ((__row as any).orderedQuantity * (__row as any).unitCost); (__row as any).remainingQuantity = ((__row as any).orderedQuantity - (__row as any).receivedQuantity); (__row as any).isFullyReceived = (((__row as any).orderedQuantity > 0) && ((__row as any).receivedQuantity >= (__row as any).orderedQuantity)); (__row as any).hasReceivingDiscrepancy = (((__row as any).discrepancyQuantity != null) && ((__row as any).discrepancyQuantity > 0)); return { ...(__row as any), lineTotal: (__row as any).lineTotal, remainingQuantity: (__row as any).remainingQuantity, isFullyReceived: (__row as any).isFullyReceived, hasReceivingDiscrepancy: (__row as any).hasReceivingDiscrepancy }; });
  },
});

export const listVendorOrderLineByVendorOrderId = query({
  args: { vendorOrderId: v.id("vendorOrders") },
  handler: async (ctx, { vendorOrderId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("vendorOrderLineRead", "VendorOrderLine", () => (checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("vendorOrderLines").withIndex("by_vendorOrderId", (q) => q.eq("vendorOrderId", vendorOrderId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return (__plainRows).map((__row) => { (__row as any).lineTotal = ((__row as any).orderedQuantity * (__row as any).unitCost); (__row as any).remainingQuantity = ((__row as any).orderedQuantity - (__row as any).receivedQuantity); (__row as any).isFullyReceived = (((__row as any).orderedQuantity > 0) && ((__row as any).receivedQuantity >= (__row as any).orderedQuantity)); (__row as any).hasReceivingDiscrepancy = (((__row as any).discrepancyQuantity != null) && ((__row as any).discrepancyQuantity > 0)); return { ...(__row as any), lineTotal: (__row as any).lineTotal, remainingQuantity: (__row as any).remainingQuantity, isFullyReceived: (__row as any).isFullyReceived, hasReceivingDiscrepancy: (__row as any).hasReceivingDiscrepancy }; });
  },
});

export const listVendorOrderLineByIngredientId = query({
  args: { ingredientId: v.id("ingredients") },
  handler: async (ctx, { ingredientId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("vendorOrderLineRead", "VendorOrderLine", () => (checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("vendorOrderLines").withIndex("by_ingredientId", (q) => q.eq("ingredientId", ingredientId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return (__plainRows).map((__row) => { (__row as any).lineTotal = ((__row as any).orderedQuantity * (__row as any).unitCost); (__row as any).remainingQuantity = ((__row as any).orderedQuantity - (__row as any).receivedQuantity); (__row as any).isFullyReceived = (((__row as any).orderedQuantity > 0) && ((__row as any).receivedQuantity >= (__row as any).orderedQuantity)); (__row as any).hasReceivingDiscrepancy = (((__row as any).discrepancyQuantity != null) && ((__row as any).discrepancyQuantity > 0)); return { ...(__row as any), lineTotal: (__row as any).lineTotal, remainingQuantity: (__row as any).remainingQuantity, isFullyReceived: (__row as any).isFullyReceived, hasReceivingDiscrepancy: (__row as any).hasReceivingDiscrepancy }; });
  },
});

export const listVendorOrderLineByIngredientDemandId = query({
  args: { ingredientDemandId: v.id("ingredientDemands") },
  handler: async (ctx, { ingredientDemandId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("vendorOrderLineRead", "VendorOrderLine", () => (checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("vendorOrderLines").withIndex("by_ingredientDemandId", (q) => q.eq("ingredientDemandId", ingredientDemandId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return (__plainRows).map((__row) => { (__row as any).lineTotal = ((__row as any).orderedQuantity * (__row as any).unitCost); (__row as any).remainingQuantity = ((__row as any).orderedQuantity - (__row as any).receivedQuantity); (__row as any).isFullyReceived = (((__row as any).orderedQuantity > 0) && ((__row as any).receivedQuantity >= (__row as any).orderedQuantity)); (__row as any).hasReceivingDiscrepancy = (((__row as any).discrepancyQuantity != null) && ((__row as any).discrepancyQuantity > 0)); return { ...(__row as any), lineTotal: (__row as any).lineTotal, remainingQuantity: (__row as any).remainingQuantity, isFullyReceived: (__row as any).isFullyReceived, hasReceivingDiscrepancy: (__row as any).hasReceivingDiscrepancy }; });
  },
});

export const listVendorOrderLineByLocationId = query({
  args: { locationId: v.id("storageLocations") },
  handler: async (ctx, { locationId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("vendorOrderLineRead", "VendorOrderLine", () => (checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("vendorOrderLines").withIndex("by_locationId", (q) => q.eq("locationId", locationId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return (__plainRows).map((__row) => { (__row as any).lineTotal = ((__row as any).orderedQuantity * (__row as any).unitCost); (__row as any).remainingQuantity = ((__row as any).orderedQuantity - (__row as any).receivedQuantity); (__row as any).isFullyReceived = (((__row as any).orderedQuantity > 0) && ((__row as any).receivedQuantity >= (__row as any).orderedQuantity)); (__row as any).hasReceivingDiscrepancy = (((__row as any).discrepancyQuantity != null) && ((__row as any).discrepancyQuantity > 0)); return { ...(__row as any), lineTotal: (__row as any).lineTotal, remainingQuantity: (__row as any).remainingQuantity, isFullyReceived: (__row as any).isFullyReceived, hasReceivingDiscrepancy: (__row as any).hasReceivingDiscrepancy }; });
  },
});

export const listVendorOrderLineDemand = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("vendorOrderLineDemandRead", "VendorOrderLineDemand", () => (checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("vendorOrderLineDemands").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const getVendorOrderLineDemand = query({
  args: { id: v.id("vendorOrderLineDemands") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("vendorOrderLineDemandRead", "VendorOrderLineDemand", () => (checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    return __doc;
  },
});

export const listVendorOrderLineDemandByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("vendorOrderLineDemandRead", "VendorOrderLineDemand", () => (checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("vendorOrderLineDemands").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listVendorOrderLineDemandByVendorOrderLineId = query({
  args: { vendorOrderLineId: v.id("vendorOrderLines") },
  handler: async (ctx, { vendorOrderLineId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("vendorOrderLineDemandRead", "VendorOrderLineDemand", () => (checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("vendorOrderLineDemands").withIndex("by_vendorOrderLineId", (q) => q.eq("vendorOrderLineId", vendorOrderLineId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listVendorOrderLineDemandByIngredientDemandId = query({
  args: { ingredientDemandId: v.id("ingredientDemands") },
  handler: async (ctx, { ingredientDemandId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("vendorOrderLineDemandRead", "VendorOrderLineDemand", () => (checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("vendorOrderLineDemands").withIndex("by_ingredientDemandId", (q) => q.eq("ingredientDemandId", ingredientDemandId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listVendorOrderLineDemandByVendorOrderId = query({
  args: { vendorOrderId: v.id("vendorOrders") },
  handler: async (ctx, { vendorOrderId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("vendorOrderLineDemandRead", "VendorOrderLineDemand", () => (checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("vendorOrderLineDemands").withIndex("by_vendorOrderId", (q) => q.eq("vendorOrderId", vendorOrderId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listVenue = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("venueRead", "Venue", () => checkRole(user, "eventAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("venues").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "Venue", ["addressLine1","addressLine2","city","region","postalCode","countryCode","contactName","contactEmail","contactPhone"], row)));
    return (__plainRows).map((__row) => { (__row as any).isActive = ((__row as any).status === "active"); return { ...(__row as any), isActive: (__row as any).isActive }; });
  },
});

export const getVenue = query({
  args: { id: v.id("venues") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("venueRead", "Venue", () => checkRole(user, "eventAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = await __decryptDoc(ctx, "Venue", ["addressLine1","addressLine2","city","region","postalCode","countryCode","contactName","contactEmail","contactPhone"], __rawDoc);
    (__doc as any).isActive = ((__doc as any).status === "active");
    const __hydrated = { ...(__doc as any), isActive: (__doc as any).isActive };
    return __hydrated;
  },
});

export const listVenueByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("venueRead", "Venue", () => checkRole(user, "eventAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("venues").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = await Promise.all((rows).map((row) => __decryptDoc(ctx, "Venue", ["addressLine1","addressLine2","city","region","postalCode","countryCode","contactName","contactEmail","contactPhone"], row)));
    return (__plainRows).map((__row) => { (__row as any).isActive = ((__row as any).status === "active"); return { ...(__row as any), isActive: (__row as any).isActive }; });
  },
});

export const listVenueCommissionTerm = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("venueTermRead", "VenueCommissionTerm", () => checkRole(user, "financeAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("venueCommissionTerms").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const getVenueCommissionTerm = query({
  args: { id: v.id("venueCommissionTerms") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("venueTermRead", "VenueCommissionTerm", () => checkRole(user, "financeAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    return __doc;
  },
});

export const listVenueCommissionTermByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("venueTermRead", "VenueCommissionTerm", () => checkRole(user, "financeAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("venueCommissionTerms").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listVenueCommissionTermByVenueId = query({
  args: { venueId: v.id("venues") },
  handler: async (ctx, { venueId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("venueTermRead", "VenueCommissionTerm", () => checkRole(user, "financeAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("venueCommissionTerms").withIndex("by_venueId", (q) => q.eq("venueId", venueId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listVenueLayoutTemplate = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("venueLayoutTemplateRead", "VenueLayoutTemplate", () => checkRole(user, "eventAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("venueLayoutTemplates").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return (__plainRows).map((__row) => { (__row as any).isActive = ((__row as any).status === "active"); return { ...(__row as any), isActive: (__row as any).isActive }; });
  },
});

export const getVenueLayoutTemplate = query({
  args: { id: v.id("venueLayoutTemplates") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("venueLayoutTemplateRead", "VenueLayoutTemplate", () => checkRole(user, "eventAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    (__doc as any).isActive = ((__doc as any).status === "active");
    const __hydrated = { ...(__doc as any), isActive: (__doc as any).isActive };
    return __hydrated;
  },
});

export const listVenueLayoutTemplateByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("venueLayoutTemplateRead", "VenueLayoutTemplate", () => checkRole(user, "eventAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("venueLayoutTemplates").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return (__plainRows).map((__row) => { (__row as any).isActive = ((__row as any).status === "active"); return { ...(__row as any), isActive: (__row as any).isActive }; });
  },
});

export const listVenueLayoutTemplateByVenueId = query({
  args: { venueId: v.id("venues") },
  handler: async (ctx, { venueId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("venueLayoutTemplateRead", "VenueLayoutTemplate", () => checkRole(user, "eventAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("venueLayoutTemplates").withIndex("by_venueId", (q) => q.eq("venueId", venueId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return (__plainRows).map((__row) => { (__row as any).isActive = ((__row as any).status === "active"); return { ...(__row as any), isActive: (__row as any).isActive }; });
  },
});

export const listVenueNote = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("venueNoteRead", "VenueNote", () => checkRole(user, "eventAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("venueNotes").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const getVenueNote = query({
  args: { id: v.id("venueNotes") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("venueNoteRead", "VenueNote", () => checkRole(user, "eventAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    return __doc;
  },
});

export const listVenueNoteByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("venueNoteRead", "VenueNote", () => checkRole(user, "eventAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("venueNotes").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listVenueNoteByVenueId = query({
  args: { venueId: v.id("venues") },
  handler: async (ctx, { venueId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("venueNoteRead", "VenueNote", () => checkRole(user, "eventAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("venueNotes").withIndex("by_venueId", (q) => q.eq("venueId", venueId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listVenueNoteByEventId = query({
  args: { eventId: v.id("events") },
  handler: async (ctx, { eventId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("venueNoteRead", "VenueNote", () => checkRole(user, "eventAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("venueNotes").withIndex("by_eventId", (q) => q.eq("eventId", eventId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listVenueNoteByAuthorPersonId = query({
  args: { authorPersonId: v.id("people") },
  handler: async (ctx, { authorPersonId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("venueNoteRead", "VenueNote", () => checkRole(user, "eventAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("venueNotes").withIndex("by_authorPersonId", (q) => q.eq("authorPersonId", authorPersonId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listVenueRoom = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("venueRoomRead", "VenueRoom", () => checkRole(user, "eventAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("venueRooms").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const getVenueRoom = query({
  args: { id: v.id("venueRooms") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("venueRoomRead", "VenueRoom", () => checkRole(user, "eventAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    return __doc;
  },
});

export const listVenueRoomByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("venueRoomRead", "VenueRoom", () => checkRole(user, "eventAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("venueRooms").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listVenueRoomByVenueId = query({
  args: { venueId: v.id("venues") },
  handler: async (ctx, { venueId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("venueRoomRead", "VenueRoom", () => checkRole(user, "eventAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("venueRooms").withIndex("by_venueId", (q) => q.eq("venueId", venueId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listVenueVendorRelationship = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("venueVendorRelationshipRead", "VenueVendorRelationship", () => checkRole(user, "facilityAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("venueVendorRelationships").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return (__plainRows).map((__row) => { (__row as any).isPreferred = ((__row as any).status === "preferred"); (__row as any).isBanned = ((__row as any).status === "banned"); (__row as any).isRestricted = ((__row as any).status === "restricted"); (__row as any).isActive = (((__row as any).effectiveFrom == null) || ((__row as any).effectiveFrom <= Date.now())); (__row as any).isExpired = (((__row as any).effectiveUntil != null) && ((__row as any).effectiveUntil < Date.now())); return { ...(__row as any), isPreferred: (__row as any).isPreferred, isBanned: (__row as any).isBanned, isRestricted: (__row as any).isRestricted, isActive: (__row as any).isActive, isExpired: (__row as any).isExpired }; });
  },
});

export const getVenueVendorRelationship = query({
  args: { id: v.id("venueVendorRelationships") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("venueVendorRelationshipRead", "VenueVendorRelationship", () => checkRole(user, "facilityAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    (__doc as any).isPreferred = ((__doc as any).status === "preferred");
    (__doc as any).isBanned = ((__doc as any).status === "banned");
    (__doc as any).isRestricted = ((__doc as any).status === "restricted");
    (__doc as any).isActive = (((__doc as any).effectiveFrom == null) || ((__doc as any).effectiveFrom <= Date.now()));
    (__doc as any).isExpired = (((__doc as any).effectiveUntil != null) && ((__doc as any).effectiveUntil < Date.now()));
    const __hydrated = { ...(__doc as any), isPreferred: (__doc as any).isPreferred, isBanned: (__doc as any).isBanned, isRestricted: (__doc as any).isRestricted, isActive: (__doc as any).isActive, isExpired: (__doc as any).isExpired };
    return __hydrated;
  },
});

export const listVenueVendorRelationshipByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("venueVendorRelationshipRead", "VenueVendorRelationship", () => checkRole(user, "facilityAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("venueVendorRelationships").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return (__plainRows).map((__row) => { (__row as any).isPreferred = ((__row as any).status === "preferred"); (__row as any).isBanned = ((__row as any).status === "banned"); (__row as any).isRestricted = ((__row as any).status === "restricted"); (__row as any).isActive = (((__row as any).effectiveFrom == null) || ((__row as any).effectiveFrom <= Date.now())); (__row as any).isExpired = (((__row as any).effectiveUntil != null) && ((__row as any).effectiveUntil < Date.now())); return { ...(__row as any), isPreferred: (__row as any).isPreferred, isBanned: (__row as any).isBanned, isRestricted: (__row as any).isRestricted, isActive: (__row as any).isActive, isExpired: (__row as any).isExpired }; });
  },
});

export const listVenueVendorRelationshipByVenueId = query({
  args: { venueId: v.id("venues") },
  handler: async (ctx, { venueId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("venueVendorRelationshipRead", "VenueVendorRelationship", () => checkRole(user, "facilityAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("venueVendorRelationships").withIndex("by_venueId", (q) => q.eq("venueId", venueId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return (__plainRows).map((__row) => { (__row as any).isPreferred = ((__row as any).status === "preferred"); (__row as any).isBanned = ((__row as any).status === "banned"); (__row as any).isRestricted = ((__row as any).status === "restricted"); (__row as any).isActive = (((__row as any).effectiveFrom == null) || ((__row as any).effectiveFrom <= Date.now())); (__row as any).isExpired = (((__row as any).effectiveUntil != null) && ((__row as any).effectiveUntil < Date.now())); return { ...(__row as any), isPreferred: (__row as any).isPreferred, isBanned: (__row as any).isBanned, isRestricted: (__row as any).isRestricted, isActive: (__row as any).isActive, isExpired: (__row as any).isExpired }; });
  },
});

export const listVenueVendorRelationshipByVendorId = query({
  args: { vendorId: v.id("vendors") },
  handler: async (ctx, { vendorId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("venueVendorRelationshipRead", "VenueVendorRelationship", () => checkRole(user, "facilityAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("venueVendorRelationships").withIndex("by_vendorId", (q) => q.eq("vendorId", vendorId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return (__plainRows).map((__row) => { (__row as any).isPreferred = ((__row as any).status === "preferred"); (__row as any).isBanned = ((__row as any).status === "banned"); (__row as any).isRestricted = ((__row as any).status === "restricted"); (__row as any).isActive = (((__row as any).effectiveFrom == null) || ((__row as any).effectiveFrom <= Date.now())); (__row as any).isExpired = (((__row as any).effectiveUntil != null) && ((__row as any).effectiveUntil < Date.now())); return { ...(__row as any), isPreferred: (__row as any).isPreferred, isBanned: (__row as any).isBanned, isRestricted: (__row as any).isRestricted, isActive: (__row as any).isActive, isExpired: (__row as any).isExpired }; });
  },
});

export const listVenueVendorRelationshipByPrimaryContactId = query({
  args: { primaryContactId: v.id("vendorContacts") },
  handler: async (ctx, { primaryContactId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("venueVendorRelationshipRead", "VenueVendorRelationship", () => checkRole(user, "facilityAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("venueVendorRelationships").withIndex("by_primaryContactId", (q) => q.eq("primaryContactId", primaryContactId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return (__plainRows).map((__row) => { (__row as any).isPreferred = ((__row as any).status === "preferred"); (__row as any).isBanned = ((__row as any).status === "banned"); (__row as any).isRestricted = ((__row as any).status === "restricted"); (__row as any).isActive = (((__row as any).effectiveFrom == null) || ((__row as any).effectiveFrom <= Date.now())); (__row as any).isExpired = (((__row as any).effectiveUntil != null) && ((__row as any).effectiveUntil < Date.now())); return { ...(__row as any), isPreferred: (__row as any).isPreferred, isBanned: (__row as any).isBanned, isRestricted: (__row as any).isRestricted, isActive: (__row as any).isActive, isExpired: (__row as any).isExpired }; });
  },
});

export const listVenueVendorRelationshipByEstablishedByPersonId = query({
  args: { establishedByPersonId: v.id("people") },
  handler: async (ctx, { establishedByPersonId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("venueVendorRelationshipRead", "VenueVendorRelationship", () => checkRole(user, "facilityAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("venueVendorRelationships").withIndex("by_establishedByPersonId", (q) => q.eq("establishedByPersonId", establishedByPersonId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return (__plainRows).map((__row) => { (__row as any).isPreferred = ((__row as any).status === "preferred"); (__row as any).isBanned = ((__row as any).status === "banned"); (__row as any).isRestricted = ((__row as any).status === "restricted"); (__row as any).isActive = (((__row as any).effectiveFrom == null) || ((__row as any).effectiveFrom <= Date.now())); (__row as any).isExpired = (((__row as any).effectiveUntil != null) && ((__row as any).effectiveUntil < Date.now())); return { ...(__row as any), isPreferred: (__row as any).isPreferred, isBanned: (__row as any).isBanned, isRestricted: (__row as any).isRestricted, isActive: (__row as any).isActive, isExpired: (__row as any).isExpired }; });
  },
});

export const listVenueVendorRelationshipByRevisedByPersonId = query({
  args: { revisedByPersonId: v.id("people") },
  handler: async (ctx, { revisedByPersonId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("venueVendorRelationshipRead", "VenueVendorRelationship", () => checkRole(user, "facilityAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("venueVendorRelationships").withIndex("by_revisedByPersonId", (q) => q.eq("revisedByPersonId", revisedByPersonId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return (__plainRows).map((__row) => { (__row as any).isPreferred = ((__row as any).status === "preferred"); (__row as any).isBanned = ((__row as any).status === "banned"); (__row as any).isRestricted = ((__row as any).status === "restricted"); (__row as any).isActive = (((__row as any).effectiveFrom == null) || ((__row as any).effectiveFrom <= Date.now())); (__row as any).isExpired = (((__row as any).effectiveUntil != null) && ((__row as any).effectiveUntil < Date.now())); return { ...(__row as any), isPreferred: (__row as any).isPreferred, isBanned: (__row as any).isBanned, isRestricted: (__row as any).isRestricted, isActive: (__row as any).isActive, isExpired: (__row as any).isExpired }; });
  },
});

export const listWasteRecord = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("wasteRecordRead", "WasteRecord", () => checkRole(user, "inventoryAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("wasteRecords").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return (__plainRows).map((__row) => { (__row as any).costImpact = ((__row as any).quantity * (__row as any).unitCost); return { ...(__row as any), costImpact: (__row as any).costImpact }; });
  },
});

export const getWasteRecord = query({
  args: { id: v.id("wasteRecords") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("wasteRecordRead", "WasteRecord", () => checkRole(user, "inventoryAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    (__doc as any).costImpact = ((__doc as any).quantity * (__doc as any).unitCost);
    const __hydrated = { ...(__doc as any), costImpact: (__doc as any).costImpact };
    return __hydrated;
  },
});

export const listWasteRecordByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("wasteRecordRead", "WasteRecord", () => checkRole(user, "inventoryAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("wasteRecords").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return (__plainRows).map((__row) => { (__row as any).costImpact = ((__row as any).quantity * (__row as any).unitCost); return { ...(__row as any), costImpact: (__row as any).costImpact }; });
  },
});

export const listWasteRecordByIngredientId = query({
  args: { ingredientId: v.id("ingredients") },
  handler: async (ctx, { ingredientId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("wasteRecordRead", "WasteRecord", () => checkRole(user, "inventoryAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("wasteRecords").withIndex("by_ingredientId", (q) => q.eq("ingredientId", ingredientId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return (__plainRows).map((__row) => { (__row as any).costImpact = ((__row as any).quantity * (__row as any).unitCost); return { ...(__row as any), costImpact: (__row as any).costImpact }; });
  },
});

export const listWasteRecordByLocationId = query({
  args: { locationId: v.id("storageLocations") },
  handler: async (ctx, { locationId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("wasteRecordRead", "WasteRecord", () => checkRole(user, "inventoryAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("wasteRecords").withIndex("by_locationId", (q) => q.eq("locationId", locationId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return (__plainRows).map((__row) => { (__row as any).costImpact = ((__row as any).quantity * (__row as any).unitCost); return { ...(__row as any), costImpact: (__row as any).costImpact }; });
  },
});

export const listWasteRecordByEventId = query({
  args: { eventId: v.id("events") },
  handler: async (ctx, { eventId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("wasteRecordRead", "WasteRecord", () => checkRole(user, "inventoryAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("wasteRecords").withIndex("by_eventId", (q) => q.eq("eventId", eventId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return (__plainRows).map((__row) => { (__row as any).costImpact = ((__row as any).quantity * (__row as any).unitCost); return { ...(__row as any), costImpact: (__row as any).costImpact }; });
  },
});

export const listWasteRecordByInventoryItemId = query({
  args: { inventoryItemId: v.id("inventoryItems") },
  handler: async (ctx, { inventoryItemId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("wasteRecordRead", "WasteRecord", () => checkRole(user, "inventoryAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("wasteRecords").withIndex("by_inventoryItemId", (q) => q.eq("inventoryItemId", inventoryItemId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return (__plainRows).map((__row) => { (__row as any).costImpact = ((__row as any).quantity * (__row as any).unitCost); return { ...(__row as any), costImpact: (__row as any).costImpact }; });
  },
});

export const listWeeklyPurchasingConfig = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("weeklyPurchasingConfigRead", "WeeklyPurchasingConfig", () => (checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("weeklyPurchasingConfigs").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const getWeeklyPurchasingConfig = query({
  args: { id: v.id("weeklyPurchasingConfigs") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("weeklyPurchasingConfigRead", "WeeklyPurchasingConfig", () => (checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    return __doc;
  },
});

export const listWeeklyPurchasingConfigByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("weeklyPurchasingConfigRead", "WeeklyPurchasingConfig", () => (checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("weeklyPurchasingConfigs").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listWeeklyPurchasingConfigByDefaultVendorId = query({
  args: { defaultVendorId: v.id("vendors") },
  handler: async (ctx, { defaultVendorId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("weeklyPurchasingConfigRead", "WeeklyPurchasingConfig", () => (checkRole(user, "procurementAccess") || checkRole(user, "manageAccess")))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("weeklyPurchasingConfigs").withIndex("by_defaultVendorId", (q) => q.eq("defaultVendorId", defaultVendorId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listWeeklyScheduleNotice = query({
  args: {},
  handler: async (ctx) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("weeklyScheduleNoticeRead", "WeeklyScheduleNotice", () => checkRole(user, "workforceAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    let rows = await ctx.db.query("weeklyScheduleNotices").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const getWeeklyScheduleNotice = query({
  args: { id: v.id("weeklyScheduleNotices") },
  handler: async (ctx, { id }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("weeklyScheduleNoticeRead", "WeeklyScheduleNotice", () => checkRole(user, "workforceAccess"))) return null;
    const doc = await ctx.db.get(id);
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (doc && (doc as any).tenantId !== __tenant) return null;
    if (doc && (doc as any).deletedAt != null) return null;
    const __rawDoc = doc;
    if (!__rawDoc) return __rawDoc;
    const __doc = __rawDoc;
    return __doc;
  },
});

export const listWeeklyScheduleNoticeByTenantId = query({
  args: { tenantId: v.string() },
  handler: async (ctx, { tenantId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("weeklyScheduleNoticeRead", "WeeklyScheduleNotice", () => checkRole(user, "workforceAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("weeklyScheduleNotices").withIndex("by_tenantId", (q) => q.eq("tenantId", __tenant)).collect();
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listWeeklyScheduleNoticeByPersonId = query({
  args: { personId: v.id("people") },
  handler: async (ctx, { personId }) => {
    const __auth = (await getAuthContext(ctx)) as any;
    const user = (__auth.user ?? __auth) as any;
    if (!__allowsRead("weeklyScheduleNoticeRead", "WeeklyScheduleNotice", () => checkRole(user, "workforceAccess"))) return [];
    const __tenant = ((await getAuthContext(ctx)) as any).tenantId ?? null;
    if (__tenant == null) return [];
    let rows = await ctx.db.query("weeklyScheduleNotices").withIndex("by_personId", (q) => q.eq("personId", personId)).collect();
    rows = rows.filter((d) => (d as any).tenantId === __tenant);
    rows = rows.filter((d) => (d as any).deletedAt == null);
    const __plainRows = rows;
    return __plainRows;
  },
});

export const listRecentEvents = internalQuery({
  args: {},
  handler: async (ctx) => {
    return await ctx.db.query("manifestEvents").order("desc").take(50);
  },
});

export const listEventsByType = internalQuery({
  args: { type: v.string() },
  handler: async (ctx, { type }) => {
    return await ctx.db.query("manifestEvents").withIndex("by_type", (q) => q.eq("type", type)).collect();
  },
});

export const listEventsByEntity = internalQuery({
  args: { entity: v.string() },
  handler: async (ctx, { entity }) => {
    return await ctx.db.query("manifestEvents").withIndex("by_entity", (q) => q.eq("entity", entity)).collect();
  },
});

export const listEventsByEntityId = internalQuery({
  args: { entityId: v.string() },
  handler: async (ctx, { entityId }) => {
    return await ctx.db.query("manifestEvents").withIndex("by_entityId", (q) => q.eq("entityId", entityId)).collect();
  },
});
