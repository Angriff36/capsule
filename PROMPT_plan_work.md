0a. Study all files recursively under `specs/` with up to ${RALPH_MAX_READ_AGENTS} parallel ${MODEL_REVIEW} subagents to learn the application specifications.
0b. Study @IMPLEMENTATION_PLAN.md (if present) to understand the plan so far.
0c. Study all files recursively under `src/lib/` with up to ${RALPH_MAX_READ_AGENTS} parallel ${MODEL_REVIEW} subagents to understand shared utilities & components.
0d. For reference, the application source code is all files recursively under `src/`.

1. You are creating a SCOPED implementation plan for this work: "${WORK_SCOPE}". Study @IMPLEMENTATION_PLAN.md (if present; it may be incorrect) and use up to ${RALPH_MAX_READ_AGENTS} ${MODEL_REVIEW} subagents to study existing source code recursively under `src/` and compare it against all files recursively under `specs/`. Use one ${MODEL_PLAN} subagent to analyze findings, prioritize tasks, and create/update @IMPLEMENTATION_PLAN.md as a bullet point list — containing ONLY items for this work — sorted in priority of items yet to be implemented. Ultrathink. Consider searching for TODO, minimal implementations, placeholders, skipped/flaky tests, and inconsistent patterns. Study @IMPLEMENTATION_PLAN.md to determine starting point for research and keep it up to date with items considered complete/incomplete using subagents.

IMPORTANT: This is SCOPED PLANNING for "${WORK_SCOPE}" only. @IMPLEMENTATION_PLAN.md must contain ONLY tasks directly related to this work scope — scoping happens now, at plan creation, so the build loop can pick the "most important" item with zero runtime filtering. Be conservative: if uncertain whether a task belongs to this work, exclude it (the plan is disposable and can be regenerated if it turns out too narrow). Plan only. Do NOT implement anything. Do NOT assume functionality is missing; confirm with code search first. Treat `src/lib` as the project's standard library for shared utilities and components. Prefer consolidated, idiomatic implementations there over ad-hoc copies.

ULTIMATE GOAL: Deliver the scoped work "${WORK_SCOPE}". Consider missing elements related to this work and plan accordingly. If an element is missing, search first to confirm it doesn't exist, then if needed author the specification at specs/FILENAME.md. If you create a new element then document the plan to implement it in @IMPLEMENTATION_PLAN.md using a subagent.

NOTE: This scoped plan lives on the current git branch and is the single source of truth for this branch's work. Create the work branch before scoped planning with `./loop.sh plan-work "..." --branch <name>` (loop.sh checks out or creates the branch) or by checking a branch out manually first. Then build from this scoped plan with `./loop.sh --branch <name>`.
