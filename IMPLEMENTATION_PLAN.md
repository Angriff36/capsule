# Whole-spec implementation plan

Completed (2026-09-23): the **PL-PLAIN-WORDS leftover identity READ string slice** is proven; the PL-PLAIN-WORDS box stays unchecked (leftover READ strings in later areas still open). Leftover identity READ refusals no longer say may read people / unowned creation drafts or their own email subscriptions — they now say Staff may see people and Users may see unfinished email subscriptions or their own email subscriptions, matching the already-landed write leftovers (Workforce managers may update/change people; Users may update/configure only their own email subscriptions). Policy names (personRead, emailNotificationSubscriptionRead) and their write/execute twins stayed. Write leftovers stayed. Constraint leftovers stayed (Hired people require first name, last name, and email / Configured email subscriptions require an owner). Regen ran; generated copies in convex/mutations.ts picked up the new wording. No Confirmed stage, no `final` change. Do NOT mark any AC PASS — PL-PLAIN-WORDS has no AC-###. P: `tests/plain-words-identity.test.ts` now 1 (added "keeps leftover identity READ copy free of read jargon"). Orchestrator re-ran proving 2 files / 2 tests (identity 1 + admin 1). Next remaining PL-PLAIN-WORDS work is leftover admin/operations/logistics/sales READ, leftover sales finance READ (payments / payment methods / tax rates / credit memos), leftover tpp-report-favorite READ, leftover constraint "Closed or corrected time records require clock-out…", leftover constraint "Recorded inventory lots require a supplier lot number…", leftover constraint "Equipment reservations require a valid date range…", leftover DishComponentsPanel "Component unavailable", leftover kitchen nav label "Components", leftover constraint "Portion spec name is required", leftover "Event number sequences are private implementation records", leftover event-packet READ (seam / projected), and leftover status lines such as "never synced on this connection". Finding recorded, not fixed: specs/ralph/production-06-booking-handoff.md PR06-06 still lists "confirmed" as a lifecycle outcome; specs/capsule-complete-feature-spec.md still shows a Confirmed stage while the plan forbids adding one; repo-root and specs/capsule-backend-end-state-spec.md still disagree on what setting final means. User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

Completed (2026-09-23): the **PL-PLAIN-WORDS leftover facilities equipment READ string slice** is proven; the PL-PLAIN-WORDS box stays unchecked (leftover READ strings in later areas still open). Leftover equipment READ refusals no longer say may read equipment / equipment reservations / equipment maintenance / equipment service history — they now say Inventory or logistics staff may see equipment / equipment handoffs / equipment maintenance / equipment service history, matching the already-landed write leftovers (handoffs, not reservations). Policy names (equipmentRead, equipmentReservationRead, maintenanceTaskRead, equipmentServiceRead) and their write/execute twins stayed. Write leftovers stayed. Constraint leftover stayed (Equipment reservations require a valid date range and positive quantity). Regen ran; generated copies in convex/mutations.ts picked up the new wording. No Confirmed stage, no `final` change. Do NOT mark any AC PASS — PL-PLAIN-WORDS has no AC-###. P: `tests/plain-words-facilities.test.ts` now 1 (added "keeps leftover facilities equipment READ copy free of read jargon"). Orchestrator re-ran proving 3 files / 4 tests (facilities 1 + event-packet 2 + logistics 1); the full suite 312 files / 1308 tests, typecheck, format:check, and bun run build green. Next remaining PL-PLAIN-WORDS work is leftover identity/admin/operations/logistics/sales READ, leftover sales finance READ (payments / payment methods / tax rates / credit memos), leftover tpp-report-favorite READ, leftover constraint "Closed or corrected time records require clock-out…", leftover constraint "Recorded inventory lots require a supplier lot number…", leftover constraint "Equipment reservations require a valid date range…", leftover DishComponentsPanel "Component unavailable", leftover kitchen nav label "Components", leftover constraint "Portion spec name is required", leftover "Event number sequences are private implementation records", leftover event-packet READ (seam / projected), and leftover status lines such as "never synced on this connection". Finding recorded, not fixed: specs/ralph/production-06-booking-handoff.md PR06-06 still lists "confirmed" as a lifecycle outcome; specs/capsule-complete-feature-spec.md still shows a Confirmed stage while the plan forbids adding one; repo-root and specs/capsule-backend-end-state-spec.md still disagree on what setting final means. User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

Completed (2026-09-23): the **PL-PLAIN-WORDS leftover event-number READ string slice** is proven; the PL-PLAIN-WORDS box stays unchecked (leftover READ strings in later areas still open). Leftover event-number READ refusal no longer says Staff may read event numbers — it now says Staff may see event numbers, matching the already-landed write leftover Only Capsule can assign event numbers. Policy names (eventNumberAssignmentRead, eventNumberSequenceRead) and their write/execute twins stayed. Write leftovers stayed. Constraint leftover stayed (Event number sequences are private implementation records). Regen ran; generated copies in manifest-context-summary.json / wiring hashes picked up the new wording (convex/mutations.ts had no throw for this READ policy). No Confirmed stage, no `final` change. Do NOT mark any AC PASS — PL-PLAIN-WORDS has no AC-###. P: `tests/plain-words-event-packet.test.ts` now 2 (added "keeps leftover event-number READ copy free of read jargon"). Orchestrator re-ran proving 7 files / 26 tests (event-packet 2 + import 10 + operations 1 + culinary 3 + finance 2 + sales 2 + kitchen 6); the full suite 311 files / 1307 tests, typecheck, format:check, and bun run build green. Next remaining PL-PLAIN-WORDS work is leftover facilities/identity/admin/operations/logistics/sales READ, leftover sales finance READ (payments / payment methods / tax rates / credit memos), leftover tpp-report-favorite READ, leftover constraint "Closed or corrected time records require clock-out…", leftover constraint "Recorded inventory lots require a supplier lot number…", leftover DishComponentsPanel "Component unavailable", leftover kitchen nav label "Components", leftover constraint "Portion spec name is required", leftover "Event number sequences are private implementation records", leftover event-packet READ (seam / projected), and leftover status lines such as "never synced on this connection". Finding recorded, not fixed: specs/ralph/production-06-booking-handoff.md PR06-06 still lists "confirmed" as a lifecycle outcome; specs/capsule-complete-feature-spec.md still shows a Confirmed stage while the plan forbids adding one; repo-root and specs/capsule-backend-end-state-spec.md still disagree on what setting final means. User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

Completed (2026-09-23): the **PL-PLAIN-WORDS leftover import READ string slice** is proven; the PL-PLAIN-WORDS box stays unchecked (leftover READ strings in later areas still open). Leftover import READ refusals no longer say may read import runs / import artifacts / external record links / import conflicts — they now say Staff may see imports / imported files / import matches / import conflicts, matching the already-landed write leftovers. Policy names (importRunRead, importArtifactRead, externalRecordLinkRead, importConflictRead) and their write/execute twins stayed. Write leftovers stayed. Constraint leftovers stayed (Import run id is required / Artifact name is required). Regen ran; generated copies in convex/mutations.ts picked up the new wording. No Confirmed stage, no `final` change. Do NOT mark any AC PASS — PL-PLAIN-WORDS has no AC-###. P: `tests/plain-words-import.test.ts` now 10 (added "keeps leftover import READ copy free of read jargon"). Orchestrator re-ran proving 10/10; the full suite 311 files / 1306 tests, typecheck, format:check, and bun run build green. Next remaining PL-PLAIN-WORDS work is leftover event-number READ, leftover facilities/identity/admin/operations/logistics/sales READ, leftover sales finance READ (payments / payment methods / tax rates / credit memos), leftover tpp-report-favorite READ, leftover constraint "Closed or corrected time records require clock-out…", leftover constraint "Recorded inventory lots require a supplier lot number…", leftover DishComponentsPanel "Component unavailable", leftover kitchen nav label "Components", leftover constraint "Portion spec name is required", and leftover status lines such as "never synced on this connection". Finding recorded, not fixed: specs/ralph/production-06-booking-handoff.md PR06-06 still lists "confirmed" as a lifecycle outcome; specs/capsule-complete-feature-spec.md still shows a Confirmed stage while the plan forbids adding one; repo-root and specs/capsule-backend-end-state-spec.md still disagree on what setting final means. User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

Completed (2026-09-23): the **PL-PLAIN-WORDS leftover kitchen portion-spec screen slice** is proven; the PL-PLAIN-WORDS box stays unchecked (leftover READ strings in later areas still open). Leftover DishComponentPortionSpecPanel copy no longer says Portion spec saved / Could not save the portion spec / Portion specs / Component unavailable / No portion specs on this subrecipe — define one on its page first / Portion spec / Plain yield (no spec) / Save portion spec — it now says Portion size saved / Could not save the portion size / Portion sizes / Recipe unavailable / No portion sizes on this subrecipe — add one on its page first / Portion size / Plain yield (no size) / Save portion size. Identifiers, comments, and data-testid stayed. DishComponentsPanel "Component unavailable" stayed as a later leftover. No manifest change, no regen, no Confirmed stage, no `final` change. Do NOT mark any AC PASS — PL-PLAIN-WORDS has no AC-###. P: `tests/plain-words-kitchen.test.ts` now 6 (added "keeps leftover kitchen portion-spec screen copy free of spec jargon"). Orchestrator re-ran proving 6/6; the full suite 311 files / 1305 tests, typecheck, format:check, and bun run build green. Next remaining PL-PLAIN-WORDS work is leftover import READ (external record links / import artifacts / import runs), leftover event-number READ, leftover facilities/identity/admin/operations/logistics/sales READ, leftover sales finance READ (payments / payment methods / tax rates / credit memos), leftover tpp-report-favorite READ, leftover constraint "Closed or corrected time records require clock-out…", leftover constraint "Recorded inventory lots require a supplier lot number…", leftover DishComponentsPanel "Component unavailable", leftover kitchen nav label "Components", leftover constraint "Portion spec name is required", and leftover status lines such as "never synced on this connection". Finding recorded, not fixed: specs/ralph/production-06-booking-handoff.md PR06-06 still lists "confirmed" as a lifecycle outcome; specs/capsule-complete-feature-spec.md still shows a Confirmed stage while the plan forbids adding one; repo-root and specs/capsule-backend-end-state-spec.md still disagree on what setting final means. User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

Completed (2026-09-23): the **PL-PLAIN-WORDS leftover culinary READ leftover-may-read slice** is proven; the PL-PLAIN-WORDS box stays unchecked (leftover READ strings in later areas still open). Leftover culinary READ refusals no longer say may read nested recipe lines / dishes / dish ingredient lines / dish task templates / task materials / dish containers / ingredients / unit mappings / menus / menu dish lines / event dishes / event dish overrides — they now say Kitchen staff may see nested recipe lines / dish ingredient lines / dish containers / ingredients; Kitchen, sales and managers may see dishes; Kitchen staff and managers may see dish task templates / task materials; Kitchen, inventory and managers may see unit mappings; Kitchen and sales staff may see menus / menu dish lines; Employed staff may see event dishes / event dish overrides. Policy names (componentComponentRead, dishRead, dishIngredientRead, dishTaskRead, dishTaskMaterialRead, dishContainerRead, ingredientRead, itemUnitMappingRead, menuRead, menuDishRead, eventDishRead, eventDishLineOverrideRead) and their write/execute twins stayed. Write leftovers stayed. Constraint leftover stayed (Portion spec name is required). Kitchen screen leftover stayed: DishComponentPortionSpecPanel "No portion specs on this subrecipe". Regen ran; generated copies in convex/mutations.ts picked up the new wording. No Confirmed stage, no `final` change. Do NOT mark any AC PASS — PL-PLAIN-WORDS has no AC-###. P: `tests/plain-words-culinary.test.ts` now 3 (added "keeps leftover culinary READ copy free of read jargon"). Orchestrator re-ran proving 3/3 plus related 14 files / 41 tests (culinary 3 + procurement 2 + production 2 + finance 2 + quality 2 + inventory 2 + workforce 2 + import 9 + documents 3 + cutover 7 + admin 1 + integrations 2 + insights 2 + foundation 2); the full suite 311 files / 1304 tests, typecheck, format:check, and bun run build green. Next remaining PL-PLAIN-WORDS work is leftover kitchen screen "No portion specs on this subrecipe", leftover import READ (external record links / import artifacts / import runs), leftover event-number READ, leftover facilities/identity/admin/operations/logistics/sales READ, leftover sales finance READ (payments / payment methods / tax rates / credit memos), leftover tpp-report-favorite READ, leftover constraint "Closed or corrected time records require clock-out…", leftover constraint "Recorded inventory lots require a supplier lot number…", and leftover status lines such as "never synced on this connection". Finding recorded, not fixed: specs/ralph/production-06-booking-handoff.md PR06-06 still lists "confirmed" as a lifecycle outcome; specs/capsule-complete-feature-spec.md still shows a Confirmed stage while the plan forbids adding one; repo-root and specs/capsule-backend-end-state-spec.md still disagree on what setting final means. User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

Completed (2026-09-23): the **PL-PLAIN-WORDS leftover procurement READ string slice** is proven; the PL-PLAIN-WORDS box stays unchecked (leftover READ strings in later areas still open). Leftover procurement READ refusals no longer say may read purchase needs / vendor orders / vendor order lines / receipt lots / confirmed ingredient prices / order demand links / weekly purchasing config / event ingredient contributions / vendors / vendor contacts / vendor contracts / contract price tiers — they now say Inventory staff and managers may see purchase needs; Procurement and managers may see vendor orders / vendor order lines / order demand links / weekly purchasing config; Inventory, procurement, and managers may see receipt lots; Kitchen, procurement, and managers may see confirmed ingredient prices; Inventory and managers may see event ingredient contributions; Procurement staff may see vendors / vendor contacts / vendor contracts / contract price tiers. Policy names (purchaseNeedRead, vendorOrderRead, vendorOrderLineRead, inventoryLotRead, ingredientPriceObservationRead, vendorOrderLineDemandRead, weeklyPurchasingConfigRead, eventIngredientContributionRead, vendorRead, vendorContactRead, vendorContractRead, vendorContractTierRead) and their write/execute twins stayed. Write leftovers stayed. Constraint leftover stayed (Recorded inventory lots require a supplier lot number and positive receipt quantities). Regen ran; generated copies in convex/mutations.ts picked up the new wording. No Confirmed stage, no `final` change. Do NOT mark any AC PASS — PL-PLAIN-WORDS has no AC-###. P: `tests/plain-words-procurement.test.ts` now 2 (added "keeps leftover procurement READ copy free of read jargon"). Orchestrator re-ran proving 14 files / 40 tests (procurement 2 + production 2 + finance 2 + quality 2 + inventory 2 + workforce 2 + culinary 2 + import 9 + documents 3 + cutover 7 + admin 1 + integrations 2 + insights 2 + foundation 2); the full suite 311 files / 1303 tests, typecheck, format:check, and bun run build green. Next remaining PL-PLAIN-WORDS work is leftover culinary nested "Kitchen staff may read nested recipe lines", leftover kitchen screen "No portion specs on this subrecipe", leftover culinary dish/menu/ingredient READ strings that still say "may read", leftover import READ (external record links / import artifacts / import runs), leftover event-number READ, leftover facilities/identity/admin/operations/logistics/sales READ, leftover sales finance READ (payments / payment methods / tax rates / credit memos), leftover tpp-report-favorite READ, leftover constraint "Closed or corrected time records require clock-out…", leftover constraint "Recorded inventory lots require a supplier lot number…", and leftover status lines such as "never synced on this connection". Finding recorded, not fixed: specs/ralph/production-06-booking-handoff.md PR06-06 still lists "confirmed" as a lifecycle outcome; specs/capsule-complete-feature-spec.md still shows a Confirmed stage while the plan forbids adding one; repo-root and specs/capsule-backend-end-state-spec.md still disagree on what setting final means. User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

Completed (2026-09-23): the **PL-PLAIN-WORDS leftover production READ string slice** is proven; the PL-PLAIN-WORDS box stays unchecked (leftover READ strings in later areas still open). Leftover production READ refusals no longer say may read prep tasks / prep task dependencies / prep task materials / stations / production batches / batch allocations / prep task comments / quality checks — they now say Kitchen and event managers may see prep tasks / prep task dependencies / prep task materials; Employed staff may see stations; Kitchen staff and managers may see production batches / batch allocations; Kitchen staff and event managers may see prep task comments; Kitchen staff may see quality checks. Policy names (prepTaskRead, prepTaskDependencyRead, prepTaskMaterialRead, stationRead, productionBatchRead, productionBatchAllocationRead, prepTaskCommentRead, qualityCheckRead) and their write/execute twins stayed. Write leftovers stayed, including "Kitchen and event managers may declare prep task dependencies". Constraint leftovers stayed. Regen ran; generated copies in convex/mutations.ts picked up the new wording. No Confirmed stage, no `final` change. Do NOT mark any AC PASS — PL-PLAIN-WORDS has no AC-###. P: `tests/plain-words-production.test.ts` now 2 (added "keeps leftover production READ copy free of read jargon"). Orchestrator re-ran proving 14 files / 39 tests (production 2 + finance 2 + quality 2 + inventory 2 + workforce 2 + culinary 2 + import 9 + documents 3 + cutover 7 + admin 1 + integrations 2 + insights 2 + foundation 2 + procurement 1); the full suite 311 files / 1302 tests, typecheck, format:check, and bun run build green. Next remaining PL-PLAIN-WORDS work is leftover READ strings (procurement), leftover culinary nested "Kitchen staff may read nested recipe lines", leftover kitchen screen "No portion specs on this subrecipe", leftover culinary dish/menu/ingredient READ strings that still say "may read", leftover import READ (external record links / import artifacts / import runs), leftover event-number READ, leftover facilities/identity/admin/operations/logistics/sales READ, leftover sales finance READ (payments / payment methods / tax rates / credit memos), leftover tpp-report-favorite READ, and leftover constraint "Closed or corrected time records require clock-out…". Finding recorded, not fixed: specs/ralph/production-06-booking-handoff.md PR06-06 still lists "confirmed" as a lifecycle outcome; specs/capsule-complete-feature-spec.md still shows a Confirmed stage while the plan forbids adding one; repo-root and specs/capsule-backend-end-state-spec.md still disagree on what setting final means. User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

Completed (2026-09-23): the **PL-PLAIN-WORDS leftover finance READ string slice** is proven; the PL-PLAIN-WORDS box stays unchecked (leftover READ strings in later areas still open). Leftover finance READ refusals no longer say may read payroll inputs / event closeouts / venue commission terms / attributions — they now say Finance managers may see payroll inputs; Finance staff and event coordinators may see event closeouts; Finance staff may see venue commission terms; Finance and sales staff may see attributions. Policy names (payrollInputRead, eventCloseoutRead, venueTermRead, attributionRead) and their write/execute twins stayed. Write leftovers stayed. Sales leftover READ strings stayed (Finance staff may read payments / payment methods / tax rates / credit memos). Regen ran; generated copies in convex/mutations.ts picked up the new wording. No Confirmed stage, no `final` change. Do NOT mark any AC PASS — PL-PLAIN-WORDS has no AC-###. P: `tests/plain-words-finance.test.ts` now 2 (added "keeps leftover finance READ copy free of read jargon"). Orchestrator re-ran proving 12 files / 36 tests (finance 2 + quality 2 + inventory 2 + workforce 2 + culinary 2 + import 9 + documents 3 + cutover 7 + admin 1 + integrations 2 + insights 2 + foundation 2); the full suite 311 files / 1301 tests, typecheck, format:check, and bun run build green. Next remaining PL-PLAIN-WORDS work is leftover READ strings (production, procurement), leftover culinary nested "Kitchen staff may read nested recipe lines", leftover kitchen screen "No portion specs on this subrecipe", leftover culinary dish/menu/ingredient READ strings that still say "may read", leftover import READ (external record links / import artifacts / import runs), leftover event-number READ, leftover facilities/identity/admin/operations/logistics/sales READ, leftover sales finance READ (payments / payment methods / tax rates / credit memos), leftover tpp-report-favorite READ, and leftover constraint "Closed or corrected time records require clock-out…". Finding recorded, not fixed: kitchen screen still says "No portion specs on this subrecipe"; specs/ralph do not mention leftover READ refusals (owner-directed PL-PLAIN-WORDS); specs/capsule-complete-feature-spec.md still shows a Confirmed stage while the plan forbids adding one; repo-root and specs/capsule-backend-end-state-spec.md still disagree on what setting final means. User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

Completed (2026-09-23): the **PL-PLAIN-WORDS leftover quality READ string slice** is proven; the PL-PLAIN-WORDS box stays unchecked (leftover READ strings in later areas still open). Leftover quality READ refusals no longer say may read incidents / corrective actions / event allergen checks / review flags — they now say Event and kitchen staff may see incidents; Event and kitchen staff may see corrective actions; Event and kitchen staff may see event allergen checks; Any staff member may see review flags. Policy names (incidentRead, correctiveActionRead, allergenCheckRead, reviewFlagRead) and their write/execute twins stayed. Write leftovers stayed. Constraint leftovers stayed (Corrective actions require a description / A review flag needs the question it is raising / Recorded allergen checks are immutable). Regen ran; generated copies in convex/mutations.ts picked up the new wording. No Confirmed stage, no `final` change. Do NOT mark any AC PASS — PL-PLAIN-WORDS has no AC-###. P: `tests/plain-words-quality.test.ts` now 2 (added "keeps leftover quality READ copy free of read jargon"). Orchestrator re-ran proving 11 files / 34 tests (quality 2 + inventory 2 + workforce 2 + culinary 2 + import 9 + documents 3 + cutover 7 + admin 1 + integrations 2 + insights 2 + foundation 2); the full suite 311 files / 1300 tests, typecheck, format:check, and bun run build green. Next remaining PL-PLAIN-WORDS work is leftover READ strings (finance, production, procurement), leftover culinary nested "Kitchen staff may read nested recipe lines", leftover kitchen screen "No portion specs on this subrecipe", leftover culinary dish/menu/ingredient READ strings that still say "may read", leftover import READ (external record links / import artifacts / import runs), leftover event-number READ, leftover facilities/identity/admin/operations/logistics/sales READ, leftover tpp-report-favorite READ, and leftover constraint "Closed or corrected time records require clock-out…". Finding recorded, not fixed: kitchen screen still says "No portion specs on this subrecipe"; specs/ralph do not mention leftover READ refusals (owner-directed PL-PLAIN-WORDS); specs/capsule-complete-feature-spec.md still shows a Confirmed stage while the plan forbids adding one. User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

Completed (2026-09-23): the **PL-PLAIN-WORDS leftover inventory READ string slice** is proven; the PL-PLAIN-WORDS box stays unchecked (leftover READ strings in later areas still open). Leftover inventory READ refusals no longer say may read stock items / inventory reservations / ingredient demand / waste records / stock transfers / stock count sessions / stock count lines / storage locations — they now say Inventory staff and managers may see stock items; Inventory staff or event managers may see inventory reservations; Inventory staff and event managers may see ingredient demand; Inventory staff may see waste entries / stock transfers / stock count sessions / stock count lines / storage locations. Waste READ now matches the already-landed write leftover (waste entries) and drops the banned word record. Policy names (inventoryItemRead, inventoryReservationRead, ingredientDemandRead, wasteRecordRead, stockTransferRead, stockCountSessionRead, stockCountLineRead, storageLocationRead) and their conditions stayed. Write leftovers stayed. Regen ran; generated copies in convex/mutations.ts picked up the new wording. No Confirmed stage, no `final` change. Do NOT mark any AC PASS — PL-PLAIN-WORDS has no AC-###. P: `tests/plain-words-inventory.test.ts` now 2 (added "keeps leftover inventory READ copy free of read jargon"). Orchestrator re-ran proving 10 files / 32 tests (inventory 2 + workforce 2 + culinary 2 + import 9 + documents 3 + cutover 7 + admin 1 + integrations 2 + insights 2 + foundation 2); the full suite 311 files / 1299 tests, typecheck, format:check, and bun run build green. Next remaining PL-PLAIN-WORDS work is leftover READ strings (finance, production, procurement, quality), leftover culinary nested "Kitchen staff may read nested recipe lines", leftover kitchen screen "No portion specs on this subrecipe", leftover culinary dish/menu/ingredient READ strings that still say "may read", leftover import READ (external record links / import artifacts / import runs), leftover event-number READ, leftover facilities/identity/admin/operations/logistics READ, and leftover constraint "Closed or corrected time records require clock-out…". Finding recorded, not fixed: kitchen screen still says "No portion specs on this subrecipe"; specs/ralph do not mention leftover READ refusals (owner-directed PL-PLAIN-WORDS); specs/capsule-complete-feature-spec.md still shows a Confirmed stage while the plan forbids adding one. User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

Completed (2026-09-23): the **PL-PLAIN-WORDS leftover workforce READ string slice** is proven; the PL-PLAIN-WORDS box stays unchecked (leftover READ strings in later areas still open). Leftover workforce READ refusals no longer say may read training modules / training completions / shift types / push devices / performance reviews / one-on-ones / one-on-one actions / shared event staffing / time-off requests / availability windows / recurring availability / channel messages / chat read cursors / role scorecards / chat notification preference / candidates / interviews / shift swaps / time records / qualifications / shifts / schedule notices — they now say Workforce staff may see training modules / training completions / shift types; Staff may see their own push devices; Managers may see performance reviews; Workforce managers may see one-on-ones / one-on-one actions / role scorecards / candidates / interviews; Crew may see shared event staffing / their own shift swaps; Staff may see their own time-off requests; Workforce staff or the linked person may see availability windows / recurring availability / time entries / schedule notices; Staff may see channel messages and their own direct messages; Staff may see how far they have gotten in a chat; Users may see their own chat notification preference; Workforce staff may see qualifications; Workforce staff may see shifts; crew may see their own shifts and the shared event schedule. Policy names (trainingModuleRead, trainingCompletionRead, shiftTypeRead, pushSubscriptionRead, performanceReviewRead, oneOnOneRead, oneOnOneActionRead, eventAssignmentRead, eventStaffNeedRead, timeOffRequestRead, availabilityRead, recurringAvailabilityRead, staffMessageRead, staffChatReadCursorRead, roleScorecardRead, chatNotifyPreferenceRead, candidateRead, interviewRead, shiftSwapRequestRead, timeRecordRead, qualificationRead, shiftRead, weeklyScheduleNoticeRead) and their conditions stayed. Write leftovers stayed. Regen ran; generated copies in convex/mutations.ts picked up the new wording. No Confirmed stage, no `final` change. Do NOT mark any AC PASS — PL-PLAIN-WORDS has no AC-###. P: `tests/plain-words-workforce.test.ts` now 2 (added "keeps leftover workforce READ copy free of read jargon"). Orchestrator re-ran proving 9 files / 30 tests (workforce 2 + culinary 2 + import 9 + documents 3 + cutover 7 + admin 1 + integrations 2 + insights 2 + foundation 2); the full suite 311 files / 1298 tests, typecheck, format:check, and bun run build green. Next remaining PL-PLAIN-WORDS work is leftover READ strings (inventory, finance, production, procurement, quality), leftover culinary nested "Kitchen staff may read nested recipe lines", leftover kitchen screen "No portion specs on this subrecipe", leftover culinary dish/menu READ strings that still say "may read", and leftover constraint "Closed or corrected time records require clock-out…". Finding recorded, not fixed: kitchen screen still says "No portion specs on this subrecipe". User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

Completed (2026-09-23): the **PL-PLAIN-WORDS leftover culinary READ string slice** is proven; the PL-PLAIN-WORDS box stays unchecked (leftover READ strings in later areas still open). Leftover culinary READ refusals no longer say component / component imports / portion specs / dish component composition — they now say Kitchen staff may see recipes / recipe ingredient lines / recipe steps / recipe versions / portion sizes / recipe imports / recipe import lines / dish recipes, matching the already-landed write leftovers. Policy names (componentRead, componentImportRead, componentIngredientRead, componentStepRead, componentSnapshotRead, componentPortionSpecRead, componentImportLineRead, dishComponentRead) and their conditions stayed. Write leftovers stayed. Constraint leftovers stayed (Portion spec name is required). Nested leftover stayed (Kitchen staff may read nested recipe lines). Other culinary READ leftovers that already use catering nouns (menus, dishes, ingredients, event dishes) stayed. Kitchen screen leftover stayed: DishComponentPortionSpecPanel "No portion specs on this subrecipe". Regen ran; generated copies in convex/mutations.ts picked up the new wording. No Confirmed stage, no `final` change. Do NOT mark any AC PASS — PL-PLAIN-WORDS has no AC-###. P: `tests/plain-words-culinary.test.ts` now 2 (added "keeps leftover culinary READ copy free of component jargon"). Orchestrator re-ran proving 8 files / 28 tests (culinary 2 + import 9 + documents 3 + cutover 7 + admin 1 + integrations 2 + insights 2 + foundation 2); the full suite 311 files / 1297 tests, typecheck, format:check, and bun run build green. Next remaining PL-PLAIN-WORDS work is leftover READ strings (workforce, inventory, finance, production, procurement, quality). Finding recorded, not fixed: kitchen screen still says "No portion specs on this subrecipe".

Completed (2026-09-23): the **PL-PLAIN-WORDS leftover import-dataset READ string slice** is proven; the PL-PLAIN-WORDS box stays unchecked (leftover READ strings in later areas still open). Leftover ImportDataset READ refusal no longer says Staff may read import datasets — it now says Staff may see import lists, matching the already-landed write leftover Staff may update import lists. Policy name importDatasetRead and its condition stayed. Write leftover stayed (Staff may update import lists). Constraint leftovers stayed (Dataset category is required / Dataset config is required / Import order must be positive / Import run ID is required). Regen ran; generated copies in convex/mutations.ts picked up the new wording (5 throws). No Confirmed stage, no `final` change. Do NOT mark any AC PASS — PL-PLAIN-WORDS has no AC-###. P: `tests/plain-words-import.test.ts` now 9 (added "keeps leftover import-dataset READ copy free of read-dataset jargon"). Orchestrator re-ran proving 7 files / 26 tests (import 9 + documents 3 + cutover 7 + admin 1 + integrations 2 + insights 2 + foundation 2); the full suite 311 files / 1296 tests, typecheck, format:check, and bun run build green. Next remaining PL-PLAIN-WORDS work is leftover READ strings (culinary, workforce, inventory, finance, production, procurement, quality). Event-packet READ is already gone (no "may read" left in src/operations/event-packet.manifest).

Completed (2026-09-23): the **PL-PLAIN-WORDS leftover documents READ string slice** is proven; the PL-PLAIN-WORDS box stays unchecked (leftover READ strings in later areas still open). Leftover Attachment READ refusal no longer says Staff may read attachments; chat files are read through their message — it now says Staff may see attached files; chat files show up with their message. Policy name attachmentRead and its condition stayed. Write/execute leftovers stayed (Staff may update/change attachments). Constraint leftover stayed (Chat files are attached through their message). Regen ran; generated copies in convex/mutations.ts picked up the new wording. No Confirmed stage, no `final` change. Do NOT mark any AC PASS — PL-PLAIN-WORDS has no AC-###. P: `tests/plain-words-documents.test.ts` now 3 (added "keeps leftover documents READ copy free of read-attachment jargon"). Orchestrator re-ran proving 7 files / 25 tests (documents 3 + import 8 + cutover 7 + admin 1 + integrations 2 + insights 2 + foundation 2); the full suite 311 files / 1295 tests, typecheck, format:check, and bun run build green. Next remaining PL-PLAIN-WORDS work is leftover READ strings (import-dataset, culinary, workforce, inventory, finance, production, procurement, quality, event-packet).

Completed (2026-09-23): the **PL-PLAIN-WORDS leftover evaluateProviderReadiness strings slice** is proven; the PL-PLAIN-WORDS box stays unchecked (leftover READ strings in later areas still open). Leftover readiness messages no longer say before cutover / Run a sync / Provider readiness needs attention / Integrations / No integrations in use (OK for cutover) / Finish Stripe onboarding — they now say Connect it again or take it off before you switch; Sync it before you switch; Sync clean before you switch; Finish Stripe setup before you switch; Outside services need attention; Outside services; No outside services in use (OK to switch). Function name evaluateProviderReadiness, identifier providerReadiness, comments, status lines (`connected` / `sync failed` / `not payout-ready` / `never synced on this connection`), and the #386 proof phrases stayed. One runtime assertion updated: "No outside services in use". No manifest change, no regen, no Confirmed stage, no `final` change. Do NOT mark any AC PASS — PL-PLAIN-WORDS has no AC-###. P: `tests/plain-words-cutover.test.ts` now 7 (added "keeps leftover evaluateProviderReadiness strings in catering English"). Orchestrator re-ran proving 8 files / 25 tests (cutover 7 + import 8 + documents 2 + admin 1 + integrations 2 + insights 2 + foundation 2 + cutover-provider-readiness 1). Next remaining PL-PLAIN-WORDS work is leftover READ strings (documents, import-dataset, culinary, workforce, inventory, finance, production, procurement, quality, event-packet), leftover status lines such as "never synced on this connection", and leftover validateCutoverReadiness placeholder "Checking integrations...".

Completed (2026-09-23): the **PL-PLAIN-WORDS leftover Admin nav Cutover label slice** is proven; the PL-PLAIN-WORDS box stays unchecked (leftover evaluateProviderReadiness strings still open). Leftover Admin nav no longer says Cutover — it now says Switch from TPP. Path `/admin/cutover` stayed. evaluateProviderReadiness strings stayed (#386). No manifest change, no regen, no Confirmed stage, no `final` change. Do NOT mark any AC PASS — PL-PLAIN-WORDS has no AC-###. P: `tests/plain-words-cutover.test.ts` now 6 (added "keeps leftover Admin nav Cutover label in catering English"). Orchestrator re-ran proving 7 files / 23 tests (cutover 6 + import 8 + documents 2 + admin 1 + integrations 2 + insights 2 + foundation 2); the full suite 311 files / 1293 tests, typecheck, format:check, and bun run build green. Next remaining PL-PLAIN-WORDS work is leftover evaluateProviderReadiness strings.

Completed (2026-09-23): the **PL-PLAIN-WORDS leftover Final Delta Import card title slice** is proven; the PL-PLAIN-WORDS box stays unchecked (leftover evaluateProviderReadiness strings, leftover Admin nav label "Cutover" still open). Leftover CutoverPage card no longer says Final Delta Import — it now says Last import. Identifier finalDeltaImport and the View Imports link stayed. evaluateProviderReadiness strings stayed (#386). Admin nav label "Cutover" stayed. No manifest change, no regen, no Confirmed stage, no `final` change. Do NOT mark any AC PASS — PL-PLAIN-WORDS has no AC-###. P: `tests/plain-words-cutover.test.ts` now 5 (added "keeps leftover Final Delta Import card title in catering English"). Orchestrator re-ran proving 7 files / 22 tests (cutover 5 + import 8 + documents 2 + admin 1 + integrations 2 + insights 2 + foundation 2); the full suite 311 files / 1292 tests, typecheck, format:check, and bun run build green. Next remaining PL-PLAIN-WORDS work is leftover evaluateProviderReadiness strings and leftover Admin nav label "Cutover".

Completed (2026-09-23): the **PL-PLAIN-WORDS leftover foundation READ string slice** is proven; the PL-PLAIN-WORDS box stays unchecked (leftover evaluateProviderReadiness strings, leftover CutoverPage "Final Delta Import" card title, leftover Admin nav label "Cutover" still open). Leftover Organization READ refusal no longer says Staff may read organization records — it now says Staff may see the company profile. Leftover MaterializationReceipt READ refusal no longer says Materialization receipts are private implementation records — it now says These receipts stay behind the scenes. Write/execute leftovers stayed (Managers may update/change the company profile; The system keeps these receipts; Staff cannot change these receipts). Constraint leftovers stayed (Organization name is required / Currency code must be a three-letter ISO 4217 code). Regen ran; generated copies in convex/mutations.ts picked up the new wording. No Confirmed stage, no `final` change. Do NOT mark any AC PASS — PL-PLAIN-WORDS has no AC-###. P: `tests/plain-words-foundation.test.ts` now 2 (added "keeps leftover foundation READ copy free of record jargon"). Orchestrator re-ran proving 7 files / 21 tests (foundation 2 + insights 2 + integrations 2 + cutover 4 + import 8 + documents 2 + admin 1); the full suite 311 files / 1291 tests, typecheck, format:check, and bun run build green. Next remaining PL-PLAIN-WORDS work is leftover evaluateProviderReadiness strings, leftover CutoverPage "Final Delta Import" card title, and leftover Admin nav label "Cutover".

Completed (2026-09-23): the **PL-PLAIN-WORDS leftover insights READ string slice** is proven; the PL-PLAIN-WORDS box stays unchecked (leftover foundation READ strings, leftover evaluateProviderReadiness strings, leftover CutoverPage "Final Delta Import" card title, leftover Admin nav label "Cutover" still open). Leftover SavedReportDefinition READ refusal no longer says Undefined drafts are readable by staff; defined owner_only reports require ownership or manageAccess — it now says Staff may see unfinished reports; a just-for-you report can only be seen by its owner or a manager. Write/execute leftovers stayed (Staff may update/change saved reports). Constraint leftovers stayed (Defined reports require a name, chart type, and owner / Report name is required / Chart type is required / Chart type cannot be blank). tpp-report-favorite.manifest left untouched. Regen ran; generated copies in convex/mutations.ts picked up the new wording. No Confirmed stage, no `final` change. Do NOT mark any AC PASS — PL-PLAIN-WORDS has no AC-###. P: `tests/plain-words-insights.test.ts` now 2 (added "keeps leftover insights READ copy free of draft-and-access jargon"). Orchestrator re-ran proving 7 files / 20 tests (insights 2 + integrations 2 + cutover 4 + import 8 + documents 2 + admin 1 + foundation 1); the full suite 311 files / 1290 tests, typecheck, format:check, and bun run build green. Next remaining PL-PLAIN-WORDS work is leftover READ strings (area by area: foundation), leftover evaluateProviderReadiness strings, leftover CutoverPage "Final Delta Import" card title, and leftover Admin nav label "Cutover".

Completed (2026-09-23): the **PL-PLAIN-WORDS leftover integrations READ string slice** is proven; the PL-PLAIN-WORDS box stays unchecked (leftover insights/foundation READ strings, leftover evaluateProviderReadiness strings, leftover CutoverPage "Final Delta Import" card title, leftover Admin nav label "Cutover" still open). Leftover IntegrationConnection READ refusal no longer says Managers may read integration connections — it now says Managers may see outside-service connections. Write/execute leftovers stayed (Admins may update/change integration connections). Regen ran; generated copies in convex/mutations.ts picked up the new wording. No Confirmed stage, no `final` change. Do NOT mark any AC PASS — PL-PLAIN-WORDS has no AC-###. P: `tests/plain-words-integrations.test.ts` now 2 (added "keeps leftover integrations READ copy free of read-integration jargon"). Orchestrator re-ran proving 7 files / 19 tests (integrations 2 + cutover 4 + import 8 + documents 2 + admin 1 + insights 1 + foundation 1); the full suite 311 files / 1289 tests, typecheck, format:check, and bun run build green. Next remaining PL-PLAIN-WORDS work is leftover READ strings (area by area: insights, then foundation), leftover evaluateProviderReadiness strings, leftover CutoverPage "Final Delta Import" card title, and leftover Admin nav label "Cutover".

Completed (2026-09-23): the **PL-PLAIN-WORDS leftover Provider Readiness card title/details slice** is proven; the PL-PLAIN-WORDS box stays unchecked (leftover READ strings, leftover evaluateProviderReadiness strings still open). Leftover CutoverPage card no longer says Provider Readiness / QuickBooks, Calendar, SMS integrations operational — it now says Outside services ready; QuickBooks, calendar, and texts are working. Identifier providerReadiness and evaluateProviderReadiness strings stayed (#386). No manifest change, no regen, no Confirmed stage, no `final` change. Do NOT mark any AC PASS — PL-PLAIN-WORDS has no AC-###. P: `tests/plain-words-cutover.test.ts` now 4 (added "keeps leftover Provider Readiness card title and details in catering English"). Orchestrator re-ran proving 4 files / 15 tests (plain-words-cutover 4 + import 8 + documents 2 + admin 1); the full suite 311 files / 1288 tests, typecheck, format:check, and bun run build green. Next remaining PL-PLAIN-WORDS work is leftover READ strings (area by area: integrations, insights, foundation) and leftover evaluateProviderReadiness strings.

Completed (2026-09-23): the **PL-PLAIN-WORDS leftover cutover check-status slice** is proven; the PL-PLAIN-WORDS box stays unchecked (leftover READ strings, leftover Provider Readiness card title/details, leftover evaluateProviderReadiness strings still open). Leftover check-status lines no longer say "Business validation requires explicit approval" / "Rollback plan not documented" / "Pending manual sign-off" / "Requires manual sign-off" / "Business sign-off confirmed" / "Rollback plan documented" / "Rollback plan must be documented before cutover" — they now say Waiting for a manager to sign off; A manager has signed off; A manager still needs to sign off; A manager still needs to sign off on this switch; No switch-back plan written yet; Switch-back plan is written; Write the switch-back plan before you switch. The CutoverPage card no longer says Business Validation / Requires manual sign-off from business stakeholders / I approve this cutover / Cutover approved / Cutover rejected — it now says Manager sign-off; A manager still needs to sign off; I approve this switch; Switch approved - every check passed. Switch-back plan: …; Switch stopped - …. Identifiers (`businessValidation`, `rollbackPlan`, `recordCutoverApprovals`) stayed. `evaluateProviderReadiness` and `title="Provider Readiness"` stayed. No manifest change, no regen, no Confirmed stage, no `final` change. Do NOT mark any AC PASS — PL-PLAIN-WORDS has no AC-###. P: `tests/plain-words-cutover.test.ts` now 3 (added "keeps leftover cutover check-status copy free of validation jargon"). Orchestrator re-ran proving 3 files / 5 tests (plain-words-cutover 3 + admin 1 + cutover-provider-readiness 1); the full suite 311 files / 1287 tests, typecheck, format:check, and bun run build green. Next remaining PL-PLAIN-WORDS work is leftover READ strings and leftover Provider Readiness card title/details. Remaining PL-RECONCILIATION legs are still AC-191 / AC-388 remainder. User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

Completed (2026-09-23): the **PL-PLAIN-WORDS leftover import match labels and compare help slice** is proven; the PL-PLAIN-WORDS box stays unchecked (leftover READ strings, leftover check-status lines "Rollback plan not documented" / "Business validation requires explicit approval" still open). Leftover import leftover-match labels no longer say Needs mapping / matching queue / link(s) superseded / Drill-down / raw Type values such as event_record — they now say Needs a match; payment reference in the leftover match list; Reverted — N match(es) marked as replaced; Look closer: Click View to open individual items; Type column uses Event / Payment / … labels. The needs_mapping key and link.capsuleEntity === "event_record" comparisons stayed. Path `/admin/reconcile` stayed. No manifest change, no regen, no Confirmed stage, no `final` change. Do NOT mark any AC PASS — PL-PLAIN-WORDS has no AC-###. P: `tests/plain-words-import.test.ts` now 8 (added "keeps leftover import match labels and compare help free of mapping jargon"); `tests/import-evidence-behavior.test.ts` now expects "Needs a match". Orchestrator re-ran proving 5 files / 15 tests (plain-words-import 8 + documents 2 + cutover 2 + admin 1 + import-evidence 2). Next remaining PL-PLAIN-WORDS work is leftover READ strings and leftover check-status lines. Remaining PL-RECONCILIATION legs are still AC-191 / AC-388 remainder. Finding: ParallelRunDashboardPage.tsx is already over 500 lines (pre-existing; do not split in a copy-only slice). User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

Completed (2026-09-23): the **PL-PLAIN-WORDS leftover Import Records column / Record Parse slice** is proven; the PL-PLAIN-WORDS box stays unchecked (leftover READ strings, leftover check-status lines "Rollback plan not documented" / "Business validation requires explicit approval", leftover "Drill-down" help label, leftover raw Type values such as event_record still open). Import list / import details / quick-file import no longer say Records / Record Parse / record counts / real records / reconcile queue — they now say Items; Count items; Item counts; Enter how many of each kind of item were found in the file; Confirm final item counts; Final item counts; Total items; Unaccounted items; Item counts by type; not live kitchen or office items; create the real items; The items this import linked are marked as replaced; Those counts aren't valid. Check the numbers and try again; Items that need review are in the leftover match list. Path `/admin/reconcile` stayed. Identifiers (useImportRunRecordParse, recordCounts, handleRecordParse) stayed. No manifest change, no regen, no Confirmed stage, no `final` change. Do NOT mark any AC PASS — PL-PLAIN-WORDS has no AC-###. P: `tests/plain-words-import.test.ts` now 7 (added "keeps leftover import records column and record-parse copy free of record jargon"). Orchestrator re-ran proving 4 files / 12 tests (plain-words-import 7 + documents 2 + cutover 2 + admin 1); the full suite 311 files / 1285 tests, typecheck, format:check, and bun run build green. Next remaining PL-PLAIN-WORDS work is leftover READ strings, leftover check-status lines, leftover "Drill-down" help label, leftover raw Type values such as event_record, leftover "Needs mapping" disposition label, leftover revert notice "link(s) superseded", and leftover "matching queue" on the paste-rows help. Remaining PL-RECONCILIATION legs are still AC-191 / AC-388 remainder. Finding: ImportRunDetailPage.tsx is already over 500 lines (pre-existing; do not split in a copy-only slice). User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

Completed (2026-09-23): the **PL-PLAIN-WORDS leftover compare/match table and body copy slice** is proven; the PL-PLAIN-WORDS box stays unchecked (leftover READ strings, leftover check-status lines "Rollback plan not documented" / "Business validation requires explicit approval", leftover Import "Records" column / "Record Parse" still open). Leftover Compare-with-TPP / Match leftover items table and body copy no longer say record / records / Unmapped / mappings / menu import run/runs — they now say Imported items that still need to be checked or matched up; In Capsule; Everything is matched; Not matched; leftover items; Items; What these numbers mean; Imported items to double-check; individual items; confirm each item; Checked N item(s); Couldn't check/skip those items; N item(s) selected; Items marked Conflict; Select multiple items; Skipped while matching leftover items; Matched to a Capsule payment while matching leftover items. Paths stayed `/admin/parallel-run` and `/admin/reconcile`. Headings from the prior slice stay. No manifest change, no regen, no Confirmed stage, no `final` change. Do NOT mark any AC PASS — PL-PLAIN-WORDS has no AC-###. P: `tests/plain-words-import.test.ts` now 6 (added "keeps leftover compare and match table and body copy free of run and record jargon"). Orchestrator re-ran proving 4 files / 11 tests (plain-words-import 6 + documents 2 + cutover 2 + admin 1); the full suite 311 files / 1284 tests, typecheck, format:check, and bun run build green. Next remaining PL-PLAIN-WORDS work is leftover READ strings, leftover check-status lines, leftover Import "Records" column / "Record Parse", leftover "Drill-down" help label, and leftover raw Type values such as event_record. Remaining PL-RECONCILIATION legs are still AC-191 / AC-388 remainder. User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

Completed (2026-09-23): the **PL-PLAIN-WORDS leftover compare/match page headings slice** is proven; the PL-PLAIN-WORDS box stays unchecked (leftover READ strings, leftover check-status lines "Rollback plan not documented" / "Business validation requires explicit approval", leftover "Records" column / "Record Parse", leftover table/body copy on those two pages still open). Leftover Compare-with-TPP / Match leftover items page headings no longer say "Parallel Run Dashboard", "Record Counts Comparison", "Records still to match", "Unresolved Mappings", or "Imported records to match up" — they now say Compare with TPP, How the counts compare, Still to match, Not matched yet, and Match leftover items. Paths stayed `/admin/parallel-run` and `/admin/reconcile`. No manifest change, no regen, no Confirmed stage, no `final` change. Do NOT mark any AC PASS — PL-PLAIN-WORDS has no AC-###. P: `tests/plain-words-import.test.ts` now 5 (added "keeps leftover compare and match page headings free of run and record jargon"). Orchestrator re-ran proving 4 files / 10 tests (plain-words-import 5 + documents 2 + cutover 2 + admin 1); the full suite 311 files / 1283 tests, typecheck, format:check, and bun run build green. Next remaining PL-PLAIN-WORDS work is leftover READ strings, leftover "Records" column / "Record Parse", leftover table/body copy on Compare with TPP and Match leftover items (for example "Imported records that still need…", "Capsule record", "Unmapped", "menu import run/runs"), and leftover check-status lines. Remaining PL-RECONCILIATION legs are still AC-191 / AC-388 remainder. User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

Completed (2026-09-23): the **PL-PLAIN-WORDS leftover admin-nav labels slice** is proven; the PL-PLAIN-WORDS box stays unchecked (leftover READ strings, leftover check-status lines "Rollback plan not documented" / "Business validation requires explicit approval", leftover "Records" column / "Record Parse" still open). Leftover admin nav no longer says "Parallel run" / "Reconcile records" — it now says Compare with TPP and Match leftover items. Paths stayed `/admin/parallel-run` and `/admin/reconcile`. No manifest change, no regen, no Confirmed stage, no `final` change. Do NOT mark any AC PASS — PL-PLAIN-WORDS has no AC-###. P: `tests/plain-words-import.test.ts` now 4 (added "keeps leftover admin nav labels free of run and record jargon"). Orchestrator re-ran proving 4 files / 9 tests (plain-words-import 4 + documents 2 + cutover 2 + admin 1); the full suite 311 files / 1282 tests, typecheck, format:check, and bun run build green. Next remaining PL-PLAIN-WORDS work is leftover READ strings, leftover "Records" column / "Record Parse", leftover ParallelRunDashboardPage / ExternalRecordsReconcilePage headings, and leftover check-status lines. Remaining PL-RECONCILIATION legs are still AC-191 / AC-388 remainder. User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

Completed (2026-09-23): the **PL-PLAIN-WORDS leftover documents parent-constraint slice** is proven; the PL-PLAIN-WORDS box stays unchecked (leftover READ strings, leftover check-status lines "Rollback plan not documented" / "Business validation requires explicit approval", leftover "Parallel run" / "Reconcile records" nav, leftover "Records" column / "Record Parse" still open). Leftover documents constraint copy no longer says "Parent record is required" — it now says This file needs something to attach to. Regen ran; generated copies in convex/mutations.ts picked up the new wording. READ leftover left as-is: "Staff may read attachments; chat files are read through their message". Other constraint leftover left as-is: "Chat files are attached through their message". No Confirmed stage, no `final` change. Do NOT mark any AC PASS — PL-PLAIN-WORDS has no AC-###. P: `tests/plain-words-documents.test.ts` now 2 (added "keeps leftover documents parent-constraint copy free of record jargon"). Orchestrator re-ran proving 4 files / 8 tests (plain-words-documents 2 + import 3 + cutover 2 + admin 1). Next remaining PL-PLAIN-WORDS work is leftover READ strings, leftover "Parallel run" / "Reconcile records" nav, leftover "Records" column / "Record Parse", and leftover check-status lines. Remaining PL-RECONCILIATION legs are still AC-191 / AC-388 remainder. User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

Completed (2026-09-23): the **PL-PLAIN-WORDS leftover import-dataset write string slice** is proven; the PL-PLAIN-WORDS box stays unchecked (leftover documents constraint "Parent record is required", leftover READ strings, leftover check-status lines "Rollback plan not documented" / "Business validation requires explicit approval", leftover "Parallel run" / "Reconcile records" nav, leftover "Records" column / "Record Parse" still open). Leftover import-dataset write refusal copy no longer says "Staff may write import datasets" — it now says Staff may update import lists. Regen ran; generated copies in convex/mutations.ts picked up the new wording. READ leftover left as-is: "Staff may read import datasets". Constraint leftovers left as-is. No Confirmed stage, no `final` change. Do NOT mark any AC PASS — PL-PLAIN-WORDS has no AC-###. P: `tests/plain-words-import.test.ts` now 3 (added "keeps leftover import-dataset write copy free of write-dataset jargon"). Orchestrator re-ran proving 4 files / 7 tests (plain-words-import 3 + documents 1 + cutover 2 + admin 1); the full suite 311 files / 1280 tests, typecheck, format:check, and bun run build green. Next remaining PL-PLAIN-WORDS work is leftover documents constraint "Parent record is required", leftover READ strings, leftover "Parallel run" / "Reconcile records" nav, leftover "Records" column / "Record Parse", and leftover check-status lines. Remaining PL-RECONCILIATION legs are still AC-191 / AC-388 remainder. User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

Completed (2026-09-23): the **PL-PLAIN-WORDS leftover import-screen copy slice** is proven; the PL-PLAIN-WORDS box stays unchecked (leftover import-dataset write string, leftover documents constraint "Parent record is required", leftover READ strings, leftover check-status lines "Rollback plan not documented" / "Business validation requires explicit approval", leftover "Parallel run" / "Reconcile records" nav, leftover "Records" column / "Record Parse" still open). Leftover import-screen copy no longer says "import run" / "Import Runs" / "this run" — it now says Imports; New import; Start new import; No imports found; Start a new import; How an import works; See the import details; stuck imports; Write why this import failed so the next attempt knows what went wrong; Import not found; Back to imports; Import details; What you can do; Copy import ID; Recent imports. Comments and identifiers left as-is. No manifest change, no regen, no Confirmed stage, no `final` change. Do NOT mark any AC PASS — PL-PLAIN-WORDS has no AC-###. P: `tests/plain-words-import.test.ts` now 2 (added "keeps leftover import-screen copy free of import-run jargon"). Orchestrator re-ran proving 3 files / 5 tests (plain-words-import 2 + cutover 2 + admin 1) plus related import-evidence 2/2; the full suite 311 files / 1279 tests, typecheck, format:check, and bun run build green. Next remaining PL-PLAIN-WORDS work is leftover import-dataset write string, leftover documents constraint "Parent record is required", leftover READ strings, leftover "Parallel run" / "Reconcile records" nav, leftover "Records" column / "Record Parse", and leftover check-status lines. Remaining PL-RECONCILIATION legs are still AC-191 / AC-388 remainder. User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

Completed (2026-09-23): the **PL-PLAIN-WORDS leftover CutoverPage screen slice** is proven; the PL-PLAIN-WORDS box stays unchecked (leftover import-dataset write string, leftover import-screen copy ("import run"), leftover documents constraint "Parent record is required", leftover READ strings, and leftover check-status lines "Rollback plan not documented" / "Business validation requires explicit approval" still open). Leftover CutoverPage and the user-visible cutover.ts messages no longer say record / run / mapped / execute GO — they now say Switch from TPP; Last checks before Capsule takes over from TPP; Nothing left to match; Every leftover TPP item is matched up; Finish matching; Approve the switch; Don't switch yet; Do one last import; Match up every leftover imported item; Only admins can approve or stop this switch. evaluateProviderReadiness strings left as-is (#386 proof). Finding: changing executeCutoverDecision refusal text breaks tests/proofs/cutover-provider-readiness.runtime.test.ts if the assertion still looks for "provider readiness" — that one expect now matches the catering refusal. No Confirmed stage, no `final` change. Do NOT mark any AC PASS — PL-PLAIN-WORDS has no AC-###. P: `tests/plain-words-cutover.test.ts` (2) › leftover CutoverPage + leftover cutover server copy. Orchestrator re-ran proving 3 files / 4 tests (plain-words-cutover 2 + admin 1 + cutover-provider-readiness 1); typecheck, format:check, and bun run build green. Next remaining PL-PLAIN-WORDS work is leftover import-dataset write string, leftover import-screen copy ("import run"), leftover documents constraint "Parent record is required", and leftover READ strings. Remaining PL-RECONCILIATION legs are still AC-191 / AC-388 remainder. User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

Completed (2026-09-23): the **PL-PLAIN-WORDS leftover event-packet / event-number-sequence slice** is proven; the PL-PLAIN-WORDS box stays unchecked (leftover CutoverPage screen, leftover import-dataset write string, leftover import-screen copy ("import run"), leftover documents constraint "Parent record is required", and leftover READ strings still open). Leftover event-packet / event-number-sequence write/execute refusal copy no longer says "generated commands", "source records", or "numbering seam" — it now says The event packet keeps the source files; Staff cannot change packet files/issues/decisions/prints; Only Capsule can update event number counters; Event number counters stay behind the scenes; Only Capsule can assign event numbers; Assigned event numbers stay behind the scenes. Regen ran; generated copies in manifest-context-summary.json picked up the new wording (these entities have no generated mutations, so convex/mutations.ts was unchanged). READ leftover left as-is: "Source bytes and private evidence require the scoped packet seam", "Private issue evidence is projected through the packet seam", "Event number sequences are private implementation records". Constraint leftovers and remaining write strings left as-is: "Reconciliation owns issue lifecycle", "Authenticated transactional decisions are immutable", "Only exact-current immutable prints are recorded". CutoverPage, import-dataset.manifest, import screens, leftover documents constraint "Parent record is required", and leftover READ strings stay as later leftovers. No Confirmed stage, no `final` change. Do NOT mark any AC PASS — PL-PLAIN-WORDS has no AC-###. P: `tests/plain-words-event-packet.test.ts` (1) › "keeps leftover event-packet policy copy free of command jargon". Orchestrator re-ran proving 1/1 plus related 5/5 (plain-words-event-packet 1 + foundation 1 + operations 1 + sales 2); typecheck, format:check, and bun run build green. Next remaining PL-PLAIN-WORDS work is leftover CutoverPage screen, leftover import-dataset write string, leftover import-screen copy ("import run"), leftover documents constraint "Parent record is required", and leftover READ strings. Remaining PL-RECONCILIATION legs are still AC-191 / AC-388 remainder. Finding recorded, not rewritten: specs/ralph/production-06-booking-handoff.md and specs/capsule-complete-feature-spec.md still name Confirmed as a stage — backend spec §4.2 + owner rule remain the authority (no Confirmed stage; do not change `final`). Finding recorded, not rewritten: repo-root `capsule-backend-end-state-spec.md` is a stale user-owned copy (the specs/ copy is the authority). User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

Completed (2026-09-23): the **PL-PLAIN-WORDS leftover foundation-manifests slice** is proven; the PL-PLAIN-WORDS box stays unchecked (leftover event-packet / event-number-sequence, leftover CutoverPage screen, leftover import-dataset write string, leftover import-screen copy ("import run"), leftover documents constraint "Parent record is required", and leftover READ strings still open). Leftover foundation-manifest write/execute refusal copy no longer says "write organization records", "execute organization commands", "transaction seam", or "generated commands" — it now says Managers may update the company profile; Managers may change the company profile; The system keeps these receipts; Staff cannot change these receipts. Regen ran; generated copies in convex/mutations.ts picked up the new wording. READ leftover left as-is: "Staff may read organization records", "Materialization receipts are private implementation records". Constraint leftovers left as-is: "Organization name is required", "Currency code must be a three-letter ISO 4217 code". event-packet.manifest, event-number-sequence.manifest, CutoverPage, import-dataset.manifest, import screens, leftover documents constraint "Parent record is required", and leftover READ strings stay as later leftovers. No Confirmed stage, no `final` change. Do NOT mark any AC PASS — PL-PLAIN-WORDS has no AC-###. P: `tests/plain-words-foundation.test.ts` (1) › "keeps leftover foundation-manifest policy copy free of command jargon". Orchestrator re-ran proving 1/1 plus related 4/4 (plain-words-foundation 1 + insights 1 + integrations 1 + documents 1); the full suite 309 files / 1275 tests, typecheck, format:check, and bun run build green. Next remaining PL-PLAIN-WORDS work is leftover event-packet / event-number-sequence, leftover CutoverPage screen, leftover import-dataset write string, leftover import-screen copy ("import run"), leftover documents constraint "Parent record is required", and leftover READ strings. Remaining PL-RECONCILIATION legs are still AC-191 / AC-388 remainder. Finding recorded, not rewritten: specs/ralph/production-06-booking-handoff.md and specs/capsule-complete-feature-spec.md still name Confirmed as a stage — backend spec §4.2 + owner rule remain the authority (no Confirmed stage; do not change `final`). Finding recorded, not rewritten: repo-root `capsule-backend-end-state-spec.md` is a stale user-owned copy (the specs/ copy is the authority). User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

Completed (2026-09-23): the **PL-PLAIN-WORDS leftover insights-manifests slice** is proven; the PL-PLAIN-WORDS box stays unchecked (leftover foundation manifests, leftover event-packet / event-number-sequence, leftover CutoverPage screen, leftover import-dataset write string, leftover import-screen copy ("import run"), leftover READ strings, and the leftover documents constraint "Parent record is required" still open). Leftover insights-manifest write/execute refusal copy no longer says "through commands", "execute saved report commands", or "write saved reports" — it now says Staff may update saved reports; Staff may change saved reports. Regen ran; generated copies in convex/mutations.ts picked up the new wording. READ leftover left as-is: "Undefined drafts are readable by staff; defined owner_only reports require ownership or manageAccess". Constraint leftovers left as-is: "Defined reports require a name, chart type, and owner", "Report name is required", "Chart type is required", "Chart type cannot be blank". tpp-report-favorite.manifest left untouched (already plain). Foundation manifests left as later leftovers. No Confirmed stage, no `final` change. Do NOT mark any AC PASS — PL-PLAIN-WORDS has no AC-###. P: `tests/plain-words-insights.test.ts` (1) › "keeps leftover insights-manifest policy copy free of command jargon". Orchestrator re-ran proving 1/1 plus related 3/3 (plain-words-insights 1 + integrations 1 + documents 1); the full suite 308 files / 1274 tests, typecheck, format:check, and bun run build green. Next remaining PL-PLAIN-WORDS work is leftover foundation manifests, leftover event-packet / event-number-sequence, leftover CutoverPage screen, leftover import-dataset write string, leftover import-screen copy ("import run"), and leftover READ strings. Remaining PL-RECONCILIATION legs are still AC-191 / AC-388 remainder. Finding recorded, not rewritten: specs/ralph/production-06-booking-handoff.md and specs/capsule-complete-feature-spec.md still name Confirmed as a stage — backend spec §4.2 + owner rule remain the authority (no Confirmed stage; do not change `final`). Finding recorded, not rewritten: repo-root `capsule-backend-end-state-spec.md` is a stale user-owned copy (the specs/ copy is the authority). User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

Completed (2026-09-23): the **PL-PLAIN-WORDS leftover integrations-manifests slice** is proven; the PL-PLAIN-WORDS box stays unchecked (leftover insights / foundation manifests, leftover event-packet / event-number-sequence, leftover CutoverPage screen, leftover import-dataset write string, leftover import-screen copy ("import run"), leftover READ strings, and the leftover documents constraint "Parent record is required" still open). Leftover integrations-manifest write/execute refusal copy no longer says "through commands", "execute integration connection commands", or "write integration connections" — it now says Admins may update integration connections; Admins may change integration connections. Regen ran; generated copies in convex/mutations.ts picked up the new wording. READ leftover left as-is: "Managers may read integration connections". No Confirmed stage, no `final` change. Do NOT mark any AC PASS — PL-PLAIN-WORDS has no AC-###. P: `tests/plain-words-integrations.test.ts` (1) › "keeps leftover integrations-manifest policy copy free of command jargon". Orchestrator re-ran proving 1/1 plus related 3/3 (plain-words-integrations 1 + documents 1 + quality 1); the full suite 307 files / 1273 tests, typecheck, format:check, and bun run build green. Next remaining PL-PLAIN-WORDS work is leftover insights / foundation manifests, leftover event-packet / event-number-sequence, leftover CutoverPage screen, leftover import-dataset write string, leftover import-screen copy ("import run"), and leftover READ strings. Remaining PL-RECONCILIATION legs are still AC-191 / AC-388 remainder. Finding recorded, not rewritten: specs/ralph/production-06-booking-handoff.md and specs/capsule-complete-feature-spec.md still name Confirmed as a stage — backend spec §4.2 + owner rule remain the authority (no Confirmed stage; do not change `final`). Finding recorded, not rewritten: repo-root `capsule-backend-end-state-spec.md` is a stale user-owned copy (the specs/ copy is the authority). User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

Completed (2026-09-23): the **PL-PLAIN-WORDS leftover documents-manifests slice** is proven; the PL-PLAIN-WORDS box stays unchecked (leftover integrations / insights / foundation manifests, leftover event-packet / event-number-sequence, leftover CutoverPage screen, leftover import-dataset write string, leftover import-screen copy ("import run"), and leftover READ strings still open). Leftover documents-manifest write/execute refusal copy no longer says "through commands", "execute attachment commands", or "write attachments" — it now says Staff may update attachments; Staff may change attachments. Regen ran; generated copies in convex/mutations.ts picked up the new wording. READ leftover left as-is: "Staff may read attachments; chat files are read through their message". Constraint leftovers left as-is: "Parent record is required", "Chat files are attached through their message". No Confirmed stage, no `final` change. Do NOT mark any AC PASS — PL-PLAIN-WORDS has no AC-###. P: `tests/plain-words-documents.test.ts` (1) › "keeps leftover documents-manifest policy copy free of command jargon". Orchestrator re-ran proving 1/1 plus related 5/5 (plain-words-documents 1 + quality 1 + task-6-mounted-interactions 3); the full suite 306 files / 1272 tests, typecheck, format:check, and bun run build green. Next remaining PL-PLAIN-WORDS work is leftover integrations / insights / foundation manifests, leftover event-packet / event-number-sequence, leftover CutoverPage screen, leftover import-dataset write string, leftover import-screen copy ("import run"), and leftover READ strings. Remaining PL-RECONCILIATION legs are still AC-191 / AC-388 remainder. Finding recorded, not rewritten: specs/ralph/production-06-booking-handoff.md and specs/capsule-complete-feature-spec.md still name Confirmed as a stage — backend spec §4.2 + owner rule remain the authority (no Confirmed stage; do not change `final`). Finding recorded, not rewritten: repo-root `capsule-backend-end-state-spec.md` is a stale user-owned copy (the specs/ copy is the authority). User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

Completed (2026-09-23): the **PL-PLAIN-WORDS leftover quality-manifests slice** is proven; the PL-PLAIN-WORDS box stays unchecked (leftover documents / integrations / insights / foundation manifests, leftover event-packet / event-number-sequence, leftover CutoverPage screen, leftover import-dataset write string, leftover import-screen copy ("import run"), and leftover READ strings still open). Leftover quality-manifest write/execute refusal copy no longer says "through commands", "execute … commands", "write event allergen checks", "write incidents", or "write corrective actions" — it now says Any staff member may raise or settle a review flag; Event and kitchen staff may update/change event allergen checks; Event and kitchen staff may update/change incidents; Event and kitchen staff may update/change corrective actions. Regen ran; generated copies in convex/mutations.ts picked up the new wording. READ leftover left as-is: "Any staff member may read review flags", "Event and kitchen staff may read event allergen checks", "Event and kitchen staff may read incidents", "Event and kitchen staff may read corrective actions". Constraint "Recorded allergen checks are immutable" left as a later leftover. No Confirmed stage, no `final` change. Do NOT mark any AC PASS — PL-PLAIN-WORDS has no AC-###. P: `tests/plain-words-quality.test.ts` (1) › "keeps leftover quality-manifest policy copy free of command jargon". Orchestrator re-ran proving 1/1 plus related 6/6 (plain-words-quality 1 + procurement 1 + quality-check-fail-block 3 + incident-allergen-corrective-action 2); the full suite 305 files / 1271 tests, typecheck, format:check, and bun run build green. Next remaining PL-PLAIN-WORDS work is leftover documents / integrations / insights / foundation manifests, leftover event-packet / event-number-sequence, leftover CutoverPage screen, leftover import-dataset write string, leftover import-screen copy ("import run"), and leftover READ strings. Remaining PL-RECONCILIATION legs are still AC-191 / AC-388 remainder. Finding recorded, not rewritten: specs/ralph/production-06-booking-handoff.md and specs/capsule-complete-feature-spec.md still name Confirmed as a stage — backend spec §4.2 + owner rule remain the authority (no Confirmed stage; do not change `final`). Finding recorded, not rewritten: repo-root `capsule-backend-end-state-spec.md` is a stale user-owned copy (the specs/ copy is the authority). User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

Completed (2026-09-23): the **PL-PLAIN-WORDS leftover procurement-manifests slice** is proven; the PL-PLAIN-WORDS box stays unchecked (leftover quality / documents / integrations / insights / foundation manifests, leftover event-packet / event-number-sequence, leftover CutoverPage screen, leftover import-dataset write string, leftover import-screen copy ("import run"), and leftover READ strings still open). Leftover procurement-manifest refusal copy no longer says "through commands", "execute … commands", "write purchase needs", "write vendor orders", "write vendors", or "record receipt lots" — it now says Inventory staff and managers may update/change purchase needs; Procurement and managers may update/change vendor orders, vendor order lines, receipt lots, confirmed ingredient prices, order demand links, and weekly purchasing config; Inventory and managers may update/change event ingredient contributions; Procurement staff may update/change vendors, vendor contacts, vendor contracts, and contract price tiers. Regen ran; generated copies in convex/mutations.ts picked up the new wording. READ leftover left as-is. Constraint "Recorded inventory lots require a supplier lot number…" left as a later leftover. No Confirmed stage, no `final` change. Do NOT mark any AC PASS — PL-PLAIN-WORDS has no AC-###. P: `tests/plain-words-procurement.test.ts` (1) › "keeps leftover procurement-manifest policy copy free of command jargon". Orchestrator re-ran proving 1/1 plus related 24/24 (plain-words-procurement 1 + production 1 + import 1 + supply-lifecycle-policy 3 + supply-manifest-integration-guard 4 + vendor-order-title 2 + vendor-order-header-total 8 + vendor-order-pricing-totals 1 + event-weekly-purchasing 1 + purchasing-folio 2); the full suite 304 files / 1270 tests, typecheck, format:check, and bun run build green. Next remaining PL-PLAIN-WORDS work is leftover quality / documents / integrations / insights / foundation manifests, leftover event-packet / event-number-sequence, leftover CutoverPage screen, leftover import-dataset write string, leftover import-screen copy ("import run"), and leftover READ strings. Remaining PL-RECONCILIATION legs are still AC-191 / AC-388 remainder. Finding recorded, not rewritten: specs/ralph/production-06-booking-handoff.md and specs/capsule-complete-feature-spec.md still name Confirmed as a stage — backend spec §4.2 + owner rule remain the authority (no Confirmed stage; do not change `final`). Finding recorded, not rewritten: repo-root `capsule-backend-end-state-spec.md` is a stale user-owned copy (the specs/ copy is the authority). User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

Completed (2026-09-23): the **PL-PLAIN-WORDS leftover production-manifests slice** is proven; the PL-PLAIN-WORDS box stays unchecked (leftover procurement / quality / documents / integrations / insights / foundation manifests, leftover event-packet / event-number-sequence, leftover CutoverPage screen, leftover import-dataset write string, leftover import-screen copy ("import run"), and leftover READ strings still open). Leftover production-manifest refusal copy no longer says "through commands", "execute … commands", "write prep tasks", "write stations", "write production batches", "write batch allocations", "post prep task comments", or "write quality checks" — it now says Kitchen and event managers may update/change prep tasks, prep task materials, and prep task dependencies; Kitchen staff and managers may update/change stations, production batches, and batch allocations; Kitchen staff and event managers may update/change prep task comments; Kitchen staff may update/change quality checks. Regen ran; generated copies in convex/mutations.ts picked up the new wording. READ leftover left as-is: "Kitchen and event managers may read prep tasks", "Employed staff may read stations", "Kitchen staff and managers may read production batches", "Kitchen staff may read quality checks". "Kitchen and event managers may declare prep task dependencies" left untouched (already no command jargon). No Confirmed stage, no `final` change. Do NOT mark any AC PASS — PL-PLAIN-WORDS has no AC-###. P: `tests/plain-words-production.test.ts` (1) › "keeps leftover production-manifest policy copy free of command jargon". Orchestrator re-ran proving 1/1 plus related 20/20 (plain-words-production 1 + import 1 + culinary 1 + kitchen 5 + production-manifest-integration-guard 4 + prep-task-override 1 + event-approve-plans-production-batch 1 + quality-check-fail-block 3 + shared-batch 3); the full suite 303 files / 1269 tests, typecheck, format:check, and bun run build green. Next remaining PL-PLAIN-WORDS work is leftover procurement / quality / documents / integrations / insights / foundation manifests, leftover event-packet / event-number-sequence, leftover CutoverPage screen, leftover import-dataset write string, leftover import-screen copy ("import run"), and leftover READ strings. Remaining PL-RECONCILIATION legs are still AC-191 / AC-388 remainder. Finding recorded, not rewritten: specs/ralph/production-06-booking-handoff.md and specs/capsule-complete-feature-spec.md still name Confirmed as a stage — backend spec §4.2 + owner rule remain the authority (no Confirmed stage; do not change `final`). Finding recorded, not rewritten: repo-root `capsule-backend-end-state-spec.md` is a stale user-owned copy (the specs/ copy is the authority). User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

Completed (2026-09-23): the **PL-PLAIN-WORDS leftover import-manifests slice** is proven; the PL-PLAIN-WORDS box stays unchecked (leftover production / procurement / quality / documents / integrations / insights / foundation manifests, leftover event-packet / event-number-sequence, leftover CutoverPage screen, and leftover READ strings still open). Leftover import-manifest refusal copy no longer says "through commands", "execute … commands", "write import runs", "write import artifacts", "write external record links", or "write import conflicts" — it now says Staff may update/change imports; Staff may update/change imported files; Staff may update/change import matches; Staff may update/change import conflicts. Regen ran; generated copies in convex/mutations.ts picked up the new wording. READ leftover left as-is: "Staff may read import runs", "Staff may read import artifacts", "Staff may read external record links", "Staff may read import conflicts". `src/import/import-dataset.manifest` left untouched ("Staff may write import datasets" — later leftover). Import screens under `src/features/admin/import/` left untouched (still say "import run" — later leftover). No Confirmed stage, no `final` change. Do NOT mark any AC PASS — PL-PLAIN-WORDS has no AC-###. P: `tests/plain-words-import.test.ts` (1) › "keeps leftover import-manifest policy copy free of command jargon". Orchestrator re-ran 1/1 proving, related 5/5 (plain-words-import 1 + plain-words-admin 1 + plain-words-finance 1 + import-provenance 2), the full suite 302 files / 1268 tests, typecheck, format:check, and `bun run build` green. Next remaining PL-PLAIN-WORDS work is leftover production / procurement / quality / documents / integrations / insights / foundation manifests, leftover event-packet / event-number-sequence, leftover CutoverPage screen, leftover import-dataset write string, leftover import-screen copy ("import run"), and leftover READ strings. Remaining PL-RECONCILIATION legs are still AC-191 / AC-388 remainder. Finding recorded, not rewritten: specs/ralph/production-06-booking-handoff.md and specs/capsule-complete-feature-spec.md still name Confirmed as a stage — backend spec §4.2 + owner rule remain the authority (no Confirmed stage; do not change `final`). Finding recorded, not rewritten: repo-root `capsule-backend-end-state-spec.md` is a stale user-owned copy (the specs/ copy is the authority). User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

Completed (2026-09-23): the **PL-PLAIN-WORDS leftover admin / identity / facilities-manifests slice** is proven; the PL-PLAIN-WORDS box stays unchecked (leftover import / production / procurement / quality / documents / integrations / insights / foundation manifests, leftover event-packet / event-number-sequence, leftover CutoverPage screen, and leftover READ strings still open). Leftover admin / identity / facilities refusal copy no longer says "through commands", "execute … commands", "write announcements", "write people", "write equipment", or "record equipment service" — it now says Staff may update/change announcements and update their own announcement dismissals; Managers and admins may update/change assistant configuration; Staff may change assistant uploads; Authenticated staff may update/change capability settings and cutover decisions; Workforce managers may update/change people; Users may update only their own email subscriptions; Inventory or logistics staff may update/change equipment, change equipment handoffs and maintenance, and add/change equipment service. Regen ran; generated copies in convex/mutations.ts picked up the new wording. READ leftover left as-is. CutoverPage.tsx left untouched (still says "execute cutover decisions" — later leftover). event-packet.manifest and event-number-sequence.manifest left as later leftovers ("generated commands" / "records" / "seam"). No Confirmed stage, no `final` change. Do NOT mark any AC PASS — PL-PLAIN-WORDS has no AC-###. P: `tests/plain-words-admin.test.ts` (1) › "keeps leftover admin identity and facilities-manifest policy copy free of command jargon". Orchestrator re-ran proving 1/1 plus related 23/23 (plain-words-admin 1 + finance 1 + person-auth-pick 15 + cutover-provider-readiness 1 + personal-views 5); the full suite 301 files / 1267 tests, typecheck, format:check, and bun run build green. Next remaining PL-PLAIN-WORDS work is leftover production / procurement / quality / documents / integrations / insights / foundation manifests, leftover event-packet / event-number-sequence, leftover CutoverPage screen, leftover import-dataset write string, leftover import-screen copy ("import run"), and leftover READ strings. Remaining PL-RECONCILIATION legs are still AC-191 / AC-388 remainder. Finding recorded, not rewritten: specs/ralph/production-06-booking-handoff.md and specs/capsule-complete-feature-spec.md still name Confirmed as a stage — backend spec §4.2 + owner rule remain the authority (no Confirmed stage; do not change `final`). Finding recorded, not rewritten: repo-root `capsule-backend-end-state-spec.md` is a stale user-owned copy (the specs/ copy is the authority). User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

Completed (2026-09-23): the **PL-PLAIN-WORDS leftover finance-manifests slice** is proven; the PL-PLAIN-WORDS box stays unchecked (admin / identity / facilities manifests still open). Leftover finance-manifest refusal copy no longer says "through commands", "execute … commands", "write payroll inputs", "write event closeouts", "write venue commission terms", or "write attributions" — it now says Finance managers may update/change payroll inputs; Finance staff and event coordinators may update/change event closeouts; Finance staff may update/change venue commission terms and attributions. Regen ran; generated copies in convex/mutations.ts picked up the new wording. READ leftover left as-is: "Finance managers may read payroll inputs", "Finance staff and event coordinators may read event closeouts", "Finance staff may read venue commission terms", "Finance and sales staff may read attributions". closeout.manifest (compile entry) left untouched. No Confirmed stage, no `final` change. Do NOT mark any AC PASS — PL-PLAIN-WORDS has no AC-###. P: `tests/plain-words-finance.test.ts` (1) › "keeps leftover finance-manifest policy copy free of command jargon". Orchestrator re-ran proving 1/1 plus related 19/19 (plain-words-finance 1 + operations 1 + payroll-input-lifecycle 3 + event-closeout-lifecycle 3 + payroll-manifest-integration-guard 3 + closeout-manifest-integration-guard 3 + payroll-export-preview 4 + payroll-preview-behavior 1); the full suite 300 files / 1266 tests, typecheck, format:check, and bun run build green. Next remaining PL-PLAIN-WORDS work at the time was leftover admin / identity / facilities manifests (since done, 2026-09-23). Remaining PL-RECONCILIATION legs are still AC-191 / AC-388 remainder. Finding recorded, not rewritten: specs/ralph/production-06-booking-handoff.md and specs/capsule-complete-feature-spec.md still name Confirmed as a stage — backend spec §4.2 + owner rule remain the authority (no Confirmed stage; do not change `final`). Finding recorded, not rewritten: repo-root `capsule-backend-end-state-spec.md` is a stale user-owned copy (the specs/ copy is the authority). User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

Completed (2026-09-23): the **PL-PLAIN-WORDS leftover operations-remainder manifests slice** is proven; the PL-PLAIN-WORDS box stays unchecked (finance / admin / identity / facilities manifests still open). Leftover operations-remainder refusal copy no longer says "through commands", "execute … commands", or "write venue vendor relationships" — it now says Event staff may update/change venue notes, venue rooms, and venue layout templates; Event managers, logistics staff and managers may update/change service style kits; Event managers may update/change service styles and occasions; Event and sales staff may update/change event templates; Facility managers may update/change venue vendor relationships. Regen ran; generated copies in convex/mutations.ts picked up the new wording. event.manifest left untouched (already done). event-packet.manifest and event-number-sequence.manifest left as later leftovers ("generated commands" / "records" / "seam"). READ leftover left as-is. No Confirmed stage, no `final` change. Do NOT mark any AC PASS — PL-PLAIN-WORDS has no AC-###. P: `tests/plain-words-operations.test.ts` (1) › "keeps leftover operations-manifest policy copy free of command jargon". Orchestrator re-ran proving 1/1 plus related 37/37 (plain-words-operations 1 + events 10 + inventory 1 + catalogs 3 + occasion-label 6 + service-style-label 6 + service-style-retired 1 + event-planning-foundation 4 + venue-notes 3 + venue-note-visibility 2); the full suite 299 files / 1265 tests, typecheck, format:check, and bun run build green. Next remaining PL-PLAIN-WORDS work at the time was leftover admin / identity / facilities manifests (since done, 2026-09-23). Remaining PL-RECONCILIATION legs are still AC-191 / AC-388 remainder. Finding recorded, not rewritten: specs/ralph/production-06-booking-handoff.md and specs/capsule-complete-feature-spec.md still name Confirmed as a stage — backend spec §4.2 + owner rule remain the authority (no Confirmed stage; do not change `final`). Finding recorded, not rewritten: repo-root `capsule-backend-end-state-spec.md` is a stale user-owned copy (the specs/ copy is the authority). User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

Completed (2026-09-23): the **PL-PLAIN-WORDS leftover inventory-manifests slice** is proven; the PL-PLAIN-WORDS box stays unchecked (operations remainder / finance / admin / identity / facilities manifests still open). Leftover inventory-manifest refusal copy no longer says "through commands", "execute … commands", or "write waste records" — it now says Inventory staff and managers may update/change stock items; Inventory staff or event managers may update/change inventory reservations; Inventory staff and event managers may update/change ingredient demand; Inventory staff may update/change waste entries, stock transfers, stock count sessions, stock count lines, and storage locations. Regen ran; generated copies in convex/mutations.ts picked up the new wording. READ leftover left as-is: "Inventory staff may read waste records" and the constraints "Waste quantity must be positive once recorded" / "Transfer quantity must be positive once recorded" stay. No Confirmed stage, no `final` change. Do NOT mark any AC PASS — PL-PLAIN-WORDS has no AC-###. P: `tests/plain-words-inventory.test.ts` (1) › "keeps leftover inventory-manifest policy copy free of command jargon". Orchestrator re-ran proving 1/1 plus related 9/9 (plain-words-inventory 1 + workforce 1 + logistics 1 + supply-manifest-integration-guard 4 + inventory-stock-unit-catalog-lock 2); the full suite 298 files / 1264 tests, typecheck, format:check, and bun run build green. Next remaining PL-PLAIN-WORDS work is leftover operations remainder / finance / admin / identity / facilities manifests. Remaining PL-RECONCILIATION legs are still AC-191 / AC-388 remainder. Finding recorded, not rewritten: specs/ralph/production-06-booking-handoff.md and specs/capsule-complete-feature-spec.md still name Confirmed as a stage — backend spec §4.2 + owner rule remain the authority (no Confirmed stage; do not change `final`). Finding recorded, not rewritten: repo-root `capsule-backend-end-state-spec.md` is a stale user-owned copy (the specs/ copy is the authority). User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

Completed (2026-09-23): the **PL-PLAIN-WORDS leftover workforce-manifests slice** is proven; the PL-PLAIN-WORDS box stays unchecked (inventory / operations remainder / finance / admin / identity / facilities manifests still open). Leftover workforce-manifest refusal copy no longer says "through commands", "execute … commands", "use commands", or "time record(s)" on write/execute strings — it now says update/change time entries, qualifications, shifts, schedule notices, candidates, interviews, availability, recurring availability, shared event staffing, staff messages, which chats they have read, performance reviews, one-on-ones, one-on-one actions, role scorecards, training modules, training completions, and shift types. Regen ran; generated copies in convex/mutations.ts picked up the new wording. READ leftover left as-is: "Workforce staff or the linked person may read time records" and the constraint "Closed or corrected time records require clock-out…" stay; shift-swap / push-subscription / chat-notify-preference were already catering English and stay unchanged. No Confirmed stage, no `final` change. Do NOT mark any AC PASS — PL-PLAIN-WORDS has no AC-###. P: `tests/plain-words-workforce.test.ts` (1) › "keeps leftover workforce-manifest policy copy free of command jargon". Orchestrator re-ran proving 1/1 plus related 8/8 (plain-words-workforce 1 + shift-lifecycle 3 + workforce-manifest-integration-guard 4); the full suite 297 files / 1263 tests, typecheck, format:check, and bun run build green. Next remaining PL-PLAIN-WORDS work is leftover operations remainder / finance / admin / identity / facilities manifests. Remaining PL-RECONCILIATION legs are still AC-191 / AC-388 remainder. Finding recorded, not rewritten: specs/ralph/production-06-booking-handoff.md and specs/capsule-complete-feature-spec.md still name Confirmed as a stage — backend spec §4.2 + owner rule remain the authority (no Confirmed stage; do not change `final`). Finding recorded, not rewritten: repo-root `capsule-backend-end-state-spec.md` is a stale user-owned copy (the specs/ copy is the authority). User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

Completed (2026-09-23): the **PL-PLAIN-WORDS leftover logistics-manifests slice** is proven; the PL-PLAIN-WORDS box stays unchecked (workforce / inventory / operations remainder / finance / admin / identity / facilities manifests still open). Leftover logistics-manifest refusal copy no longer says "through commands", "execute … commands", or "record fuel/service" — it now says Kitchen, logistics, event and sales staff and managers may update/change pack lists and pack list items; Logistics staff and managers may update/change vehicles, trailers, deliveries, pack list templates, and vehicle maintenance; may add/change fuel logs and vehicle service; Event, sales and logistics staff and managers may update/change event vehicle assignments. Regen ran; generated copies in convex/mutations.ts picked up the new wording. Command names including `command record` on fuel/service were left as-is (not user-visible). "Insurance policy number is required" was left as insurance English — a later leftover if the owner wants that word gone too. No Confirmed stage, no `final` change. Do NOT mark any AC PASS — PL-PLAIN-WORDS has no AC-###. P: `tests/plain-words-logistics.test.ts` (1) › "keeps leftover logistics-manifest policy copy free of command jargon". Orchestrator re-ran proving 1/1, related 31/31 (plain-words-logistics 1 + culinary 1 + sales 2 + kitchen 5 + events 10 + logistics-routes 3 + logistics-manifest-integration-guard 4 + pack-list-delivery-lifecycle 3 + event-approve-opens-packlist 1 + packlist-packed-schedules-delivery 1), the full suite 296 files / 1262 tests, typecheck, format:check, and `bun run build` green. Next remaining PL-PLAIN-WORDS work is leftover workforce / inventory / operations remainder / finance / admin / identity / facilities manifests. Remaining PL-RECONCILIATION legs are still AC-191 / AC-388 remainder. Finding recorded, not rewritten: specs/ralph/production-06-booking-handoff.md and specs/capsule-complete-feature-spec.md still name Confirmed as a stage — backend spec §4.2 + owner rule remain the authority (no Confirmed stage; do not change `final`). Finding recorded, not rewritten: repo-root `capsule-backend-end-state-spec.md` is a stale user-owned copy (the specs/ copy is the authority). User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

Completed (2026-09-23): the **PL-PLAIN-WORDS leftover culinary-manifests slice** is proven; the PL-PLAIN-WORDS box stays unchecked (workforce / logistics / inventory / operations remainder / finance / admin / identity / facilities manifests still open). Leftover culinary-manifest refusal copy no longer says "through commands" or "execute … commands" — it now says Managers, sales and kitchen staff may update/change event dishes; Kitchen staff may update/change ingredients, recipes, menus, dish recipes, recipe versions, portion sizes, recipe imports; Kitchen, inventory and managers may update/change unit mappings. Regen ran; generated copies in convex/mutations.ts picked up the new wording. Pinned denials still match: "Kitchen, inventory and managers" and "Kitchen staff". Culinary READ policy strings were left as-is (still say "components", "component imports", "portion specs") — a later leftover if needed; do not redo the write/execute strings. No Confirmed stage, no `final` change. Do NOT mark any AC PASS — PL-PLAIN-WORDS has no AC-###. P: `tests/plain-words-culinary.test.ts` (1) › "keeps leftover culinary-manifest policy copy free of command jargon". Orchestrator re-ran proving 1/1, related 13/13 (plain-words-culinary 1 + plain-words-kitchen 5 + shared-batch 3 + component-import-finalize 2 + agent-document-enter 2), the full suite 295 files / 1261 tests, typecheck, format:check, and `bun run build` green. Next remaining PL-PLAIN-WORDS work is leftover workforce / logistics / inventory / operations remainder / finance / admin / identity / facilities manifests. Remaining PL-RECONCILIATION legs are still AC-191 / AC-388 remainder. Finding recorded, not rewritten: specs/ralph/production-06-booking-handoff.md and specs/capsule-complete-feature-spec.md still name Confirmed as a stage — backend spec §4.2 + owner rule remain the authority (no Confirmed stage; do not change `final`). Finding recorded, not rewritten: repo-root `capsule-backend-end-state-spec.md` is a stale user-owned copy (the specs/ copy is the authority). User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

Completed (2026-09-23): the **PL-PLAIN-WORDS leftover sales-manifests slice** is proven; the PL-PLAIN-WORDS box stays unchecked (culinary / logistics / workforce / finance / admin / inventory / identity / facilities / leftover operations manifests still open). Sales-manifest refusal copy no longer says "through commands", "execute … commands", "record client communication", "numbering seam", "implementation records", or "generated commands" — it now says update/change/add/send and "Invoice numbers are assigned automatically". Regen ran; generated copies in convex/mutations.ts picked up the new wording. Pinned denials still match: "Sales staff may read proposals"; Finance staff and managers may … invoices; Finance staff may … payment methods; Sales staff may … client follow-up. No Confirmed stage, no `final` change. Do NOT mark any AC PASS — PL-PLAIN-WORDS has no AC-###. P: `tests/plain-words-sales.test.ts` now 2 (added "keeps leftover sales-manifest policy copy free of command jargon"). Orchestrator re-ran proving 28/28 (plain-words-sales 2 + invoice-payment 3 + payment-method 3 + personal-views 5 + signature-acceptance 10 + crm-command 2 + quote-review 3), the full suite 294 files / 1260 tests, typecheck, format:check, and `bun run build` green. Next PL-PLAIN-WORDS work is leftover culinary manifests (largest leftover "through commands" / "execute … commands" count), then workforce / logistics / inventory / operations remainder / finance / admin / identity / facilities. Finding recorded, not rewritten this slice: MessageInboxPage still says "ingest"; deliveryHonesty still says "Legacy queued". Remaining PL-RECONCILIATION legs are still AC-191 / AC-388 remainder. Finding recorded, not rewritten: specs/ralph/production-06-booking-handoff.md and specs/capsule-complete-feature-spec.md still name Confirmed as a stage — backend spec §4.2 + owner rule remain the authority (no Confirmed stage; do not change `final`). User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

Completed (2026-09-23): the **PL-PLAIN-WORDS leftover sales-screens slice** is proven; the PL-PLAIN-WORDS box stays unchecked (sales / logistics / workforce / finance / admin manifests still open). Leftover sales / client screens no longer say record / recorded / tenant / mapped / policy / Recording to the user — QuoteSubmissionsReviewPage, QuoteSubmissionPage, ProposalAcceptancePage, ClientCommunicationPanel, ContractsPage, ContractDocumentPage, contractPdf.ts, ClientDetailPage, and ProposalReadinessNotice now say company profile; Anything already created is linked; privacy notice; Your acceptance is saved; Couldn't save your acceptance; Saving…; This acceptance is for; No date on file; before saving a conversation; mark it sent; Mark sent; another workspace; No extra terms; If you cancel; Signed in Capsule by; Not on file; Client signed; copied fields. Kept: "public quote form is offline" and the Admin → Branding link. No manifest change, no regen, no Confirmed stage, no `final` change. Do NOT mark any AC PASS — PL-PLAIN-WORDS has no AC-###. P: `tests/plain-words-sales.test.ts` (1) › "keeps leftover sales quote contract and acceptance copy free of record jargon"; crm-command-behavior now clicks "Mark sent". Orchestrator re-ran proving 6/6 (plain-words-sales 1 + crm-command 2 + quote-review 3), related 28/28, the full suite 294 files / 1259 tests, typecheck, format:check, and `bun run build` green. Next PL-PLAIN-WORDS work is sales / logistics / workforce / finance / admin manifests (sales manifests still say "through commands" / "execute … commands"). Remaining PL-RECONCILIATION legs are still AC-191 / AC-388 remainder. Finding recorded, not rewritten: specs/ralph/production-06-booking-handoff.md and specs/capsule-complete-feature-spec.md still name Confirmed as a stage — backend spec §4.2 + owner rule remain the authority (no Confirmed stage; do not change `final`). User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

Completed (2026-09-23): the **PL-PLAIN-WORDS leftover kitchen shortage-banner slice** is proven; the PL-PLAIN-WORDS box stays unchecked (sales / logistics / workforce / finance / admin, leftover kitchen screens if any remain, and leftover manifests still open). EventMenuStockShortageBanner no longer says "Mapped alternatives", "Component demand could not be fully held", or "source component has not been changed" — it says "Not enough of this ingredient is on hold. Saved substitutes are ranked for the kitchen; the original ingredient is unchanged." No manifest change, no regen, no Confirmed stage, no `final` change. Do NOT mark any AC PASS — PL-PLAIN-WORDS has no AC-###. P: `tests/plain-words-kitchen.test.ts` now 5 (added "keeps leftover kitchen shortage banner copy free of mapped jargon"). Orchestrator re-ran 5/5 proving, related 22/22 (plain-words-kitchen 5 + event-menu-stock-sync-lifecycle 2 + event-menu-recipe-editor-leftovers 6 + event-menu-behavior 9), the full suite 293 files / 1258 tests, typecheck, format:check, and `bun run build` green. Next PL-PLAIN-WORDS commit is leftover kitchen screens if any remain, then sales screens and sales / logistics / workforce / finance / admin manifests (sales manifests still say "through commands" / "execute … commands"). Remaining PL-RECONCILIATION legs are still AC-191 / AC-388 remainder (proposal-draft auto-create is the missing create path). Finding recorded, not rewritten: specs/ralph/production-06-booking-handoff.md and specs/capsule-complete-feature-spec.md still name Confirmed as a stage — backend spec §4.2 + owner rule remain the authority (no Confirmed stage; do not change `final`). User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

Completed (2026-09-23): the **PL-PLAIN-WORDS leftover event.manifest policy/constraint slice** is proven; the PL-PLAIN-WORDS box stays unchecked (sales / logistics / workforce / finance / admin, leftover kitchen screens, and other manifests still open). Event refusal and timing-rule messages no longer say "through commands", "execute … commands", "record timeline work", or "recorded timing/times/time". They now say update/change/add and "saved timing/times/time". Regen ran; generated copies in convex/mutations.ts picked up the new wording. No Confirmed stage, no `final` change. Do NOT mark any AC PASS — PL-PLAIN-WORDS has no AC-###. P: `tests/plain-words-events.test.ts` now 10 (added "keeps leftover event.manifest policy and constraint copy free of command jargon"); `tests/proofs/lifecycle-completion-correction.runtime.test.ts` now expects "Event and sales staff may update events". Orchestrator re-ran 17/17 proving (plain-words-events 10 + event-planning-foundation 4 + lifecycle-completion-correction 3), related 28/28, the full suite 293 files / 1257 tests, typecheck, format:check, and `bun run build` green. Next PL-PLAIN-WORDS commit is leftover kitchen screens (if any remain) then sales / logistics / workforce / finance / admin manifests. Remaining PL-RECONCILIATION legs are still AC-191 / AC-388 remainder (proposal-draft auto-create is the missing create path). Finding recorded, not rewritten: specs/ralph/production-06-booking-handoff.md and specs/capsule-complete-feature-spec.md still name Confirmed as a stage — backend spec §4.2 + owner rule remain the authority (no Confirmed stage; do not change `final`). User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

Completed (2026-09-23): the **PL-PLAIN-WORDS leftover kitchen Command deck slice** is proven; the PL-PLAIN-WORDS box stays unchecked (sales / logistics / workforce / finance / admin, leftover kitchen screens, and event.manifest messages still open). Kitchen nav tab no longer says "Command deck" — it says "Prep board". Event Prep tab button no longer says "Open command deck" — it says "Open prep board". Both still go to `/kitchen/prep`. No manifest change, no regen, no Confirmed stage, no `final` change. Do NOT mark any AC PASS — PL-PLAIN-WORDS has no AC-###. P: `tests/plain-words-kitchen.test.ts` now 4 (added "keeps leftover kitchen command-deck copy free of command jargon"). Orchestrator re-ran 4/4 proving, related 24/24 (plain-words-kitchen 4 + kitchenRoutes 2 + plain-words-events 9 + event-menu 9), the full suite 293 files / 1256 tests, typecheck, format:check, and `bun run build` green. Next PL-PLAIN-WORDS commit is event.manifest policy/constraint strings (regen after), then sales / logistics / workforce / finance / admin. Remaining PL-RECONCILIATION legs are still AC-191 / AC-388 remainder (proposal-draft auto-create is the missing create path). Finding recorded, not rewritten: specs/ralph/production-06-booking-handoff.md and specs/capsule-complete-feature-spec.md still name Confirmed as a stage — backend spec §4.2 + owner rule remain the authority (no Confirmed stage; do not change `final`). A read worker incorrectly treated Confirmed as a fitting "milestone"; do not adopt that. User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

Completed (2026-09-23): the **PL-PLAIN-WORDS leftover events dietary-conflict slice** is proven; the PL-PLAIN-WORDS box stays unchecked (sales / logistics / workforce / finance / admin, leftover kitchen "Command deck" surfaces, and event.manifest messages still open). EventMenuDietaryConflictsCard no longer shows "Catalog record ↗" — the link says "Open dish ↗" and still opens the shared dish. No manifest change, no regen, no Confirmed stage, no `final` change. Do NOT mark any AC PASS — PL-PLAIN-WORDS has no AC-###. P: `tests/plain-words-events.test.ts` now 9 (added "keeps leftover event dietary conflict copy free of record jargon"). Orchestrator re-ran 9/9 proving, related 31/31 (plain-words-events 9 + kitchen 3 + event-menu 9 + event-overview 10), the full suite 293 files / 1255 tests, typecheck, format:check, and `bun run build` green. Remaining events-area jargon: `src/operations/event.manifest` policy/constraint strings (regen after), including "Staff may record timeline work through commands" and "execute … commands". Next PL-PLAIN-WORDS commit is event.manifest (regen after) or leftover kitchen "Command deck" (`EventPrepTab` "Open command deck"; `src/features/kitchen/kitchenRoutes.ts` label "Command deck"), then sales / logistics / workforce / finance / admin. Remaining PL-RECONCILIATION legs are still AC-191 / AC-388 remainder (proposal-draft auto-create is the missing create path). Finding recorded, not rewritten: specs/ralph/production-06-booking-handoff.md still names Confirmed as a stage — backend spec + owner rule remain the authority. User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

Completed (2026-09-23): the **PL-PLAIN-WORDS leftover kitchen cleanup/prep/substitute slice** is proven; the PL-PLAIN-WORDS box stays unchecked (sales / logistics / workforce / finance / admin, leftover events surfaces, and event.manifest messages still open). Catalog cleanup, serving-container empty, recipe-status empty, settled prep amount, picker duplicate warning, allergen-matrix footnote, leftover-prep error, vendor empty, and substitute headings/empty/shortage copy no longer show "record" / "mapped" / "tenant" / a script path to the user. No manifest change, no regen, no Confirmed stage, no `final` change. Do NOT mark any AC PASS — PL-PLAIN-WORDS has no AC-###. P: `tests/plain-words-kitchen.test.ts` now 3 (added "keeps leftover kitchen cleanup prep and substitute copy free of record jargon"). Orchestrator re-ran 3/3 proving after Prettier wrap, related 3/3, the full suite 293 files / 1254 tests, typecheck, format:check, and `bun run build` green. Remaining events-area jargon: EventMenuDietaryConflictsCard "Catalog record ↗"; `src/operations/event.manifest` policy/constraint strings (regen after), including "Staff may record timeline work through commands" and "execute … commands". Kitchen leftover user-visible jargon from the prior list is gone; next PL-PLAIN-WORDS commit is events remainder, then sales / logistics / workforce / finance / admin. Remaining PL-RECONCILIATION legs are still AC-191 / AC-388 remainder (proposal-draft auto-create is the missing create path). Finding recorded, not rewritten: specs/ralph/production-06-booking-handoff.md still names Confirmed as a stage — backend spec + owner rule remain the authority. User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

Completed (2026-09-23): the **PL-PLAIN-WORDS leftover kitchen catalog/record slice** is proven; the PL-PLAIN-WORDS box stays unchecked (sales / logistics / workforce / finance / admin, leftover kitchen screens, event.manifest messages, and remaining events surfaces still open). Kitchen catalog list/create/preview, dish/ingredient/menu editor eyebrows, dashboard blocked empty reason, ingredient allergen empty, unit-conversion empty/button, method/portion/version empty states, and catalog card fallbacks no longer show "record" to the user. No manifest change, no regen, no Confirmed stage, no `final` change. Do NOT mark any AC PASS — PL-PLAIN-WORDS has no AC-###. P: `tests/plain-words-kitchen.test.ts` now 2 (added "keeps leftover kitchen catalog copy free of record jargon"). Orchestrator re-ran 2/2 proving after Prettier wrap, related 18/18, the full suite 293 files / 1253 tests, typecheck, format:check, and `bun run build` green. Remaining events-area jargon: EventMenuDietaryConflictsCard "Catalog record ↗"; `src/operations/event.manifest` policy/constraint strings (regen after), including "Staff may record timeline work through commands" and "execute … commands". Remaining kitchen jargon (next kitchen commit, not this one): KitchenCatalogCleanupPage "new record" / "No suggestions recorded yet. Run the planner script"; DishContainersPanel "No serving containers recorded."; ComponentRecipeStatusPanel "unavailable for this record."; ComponentPrepContext "Recorded prep amount"; CulinaryRecordPicker "distinct record"; AllergenMatrixPage "no allergen is recorded"; EventPrepTaskSynchronizer "recorded work has incompatible units". Finding recorded, not rewritten: specs/ralph/production-06-booking-handoff.md still names Confirmed as a stage — backend spec + owner rule remain the authority. User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

Completed (2026-09-23): the **PL-PLAIN-WORDS kitchen nutrition first slice** is proven; the PL-PLAIN-WORDS box stays unchecked (sales / logistics / workforce / finance / admin, leftover kitchen screens, event.manifest messages, and remaining events surfaces still open). Menu detail coverage note and the shared nutrition panel no longer show "recorded nutrition" to the user — they say "nutrition on file". No manifest change, no regen, no Confirmed stage, no `final` change. P: `tests/plain-words-kitchen.test.ts` (1) › "keeps kitchen nutrition copy free of record jargon". Orchestrator re-ran 1/1 proving, the full suite 293 files / 1252 tests, typecheck, format:check, and `bun run build` green. Remaining events-area jargon: EventMenuDietaryConflictsCard "Catalog record ↗"; `src/operations/event.manifest` policy/constraint strings (regen after), including "Staff may record timeline work through commands" and "execute … commands". Remaining kitchen jargon (next kitchen commit, not this one): DishDetailsEditor / IngredientDetailsEditor "Catalog record"; MenuDetailsEditor "Menu record"; KitchenCatalogCreateForm "New record"; KitchenCatalogPage "All records" / "No records match this search."; KitchenCatalogCards "matching records" / "Open full record" / "Record details"; KitchenDashboardPage "Blocked — reason not recorded."; IngredientDetailPage "None recorded"; ItemUnitMappingsPanel "No source recorded" / "Record conversion"; ComponentMethodStepsPanel "No method recorded."; ComponentVersionHistoryPanel "No prior versions recorded yet."; ComponentPortionSpecsPanel "No portion size recorded."; CulinaryCatalogCardCopy "No description recorded"; CulinaryCatalogCardTone "Record". Remaining PL-RECONCILIATION legs are still AC-191 / AC-388 remainder (proposal-draft auto-create is the missing create path). Finding recorded, not rewritten: specs/ralph/production-06-booking-handoff.md still names Confirmed as a stage — backend spec + owner rule remain the authority. User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

Completed (2026-09-23): the **PL-PLAIN-WORDS leftover events timeline/venue/guest/import slice** is proven; the PL-PLAIN-WORDS box stays unchecked (sales / kitchen / logistics / workforce / finance / admin, event.manifest messages, and remaining events surfaces still open). Timeline empty hint, block picker, sidebar window/questions, run-sheet header, planning-notes description, venue-unavailable label, guest invited-tile note, event menu nutrition coverage, and import unsupported-step errors no longer show "run sheet" / "this run" / "record" / "cannot run" / Entity.command names to the user. No manifest change, no regen, no Confirmed stage, no `final` change. P: `tests/plain-words-events.test.ts` now 8 (added "keeps leftover event timeline venue guest and import copy free of run and record jargon"); `tests/features/events/event-venue-label.test.ts` now expects "Venue isn't available". Orchestrator re-ran 15/15 proving (plain-words-events 8 + event-venue-label 7), related event create/overview 26 green (16+10), the full suite 292 files / 1251 tests, typecheck, format:check, and `bun run build` green. Remaining events-area jargon (next events commit, not this one): `src/operations/event.manifest` policy/constraint strings (regen after). Kitchen leftover (next kitchen commit, not this one): MenuDetailPage / ComponentNutritionPanel "recorded nutrition". Do not redo EventDay "Run of show" (catering English) or "BOH run food." User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

Completed (2026-09-23): the **PL-PLAIN-WORDS leftover events packet/menu/staffing/guest slice** is proven; the PL-PLAIN-WORDS box stays unchecked (sales / kitchen / logistics / workforce / finance / admin, event.manifest messages, and remaining events surfaces still open). Event packet, menu, guest sidebar/summary, overview rail, staffing, photos, prep list, equipment return, timing planner, venue picker, mobile event cards, create-venue capacity note, source panel, Photos & incidents tab label, timeline intro, and live profitability copy no longer show "record" / "recorded" / "run" to the user. No manifest change, no regen, no Confirmed stage, no `final` change. P: `tests/plain-words-events.test.ts` now 7 (added "keeps leftover event packet menu staffing and guest copy free of record jargon"). Orchestrator re-ran 7/7 proving, related event/create/overview/routes 40+17 green, the full suite 292 files / 1250 tests, typecheck, format:check, and `bun run build` green. Remaining events-area jargon (next events commit, not this one): EventTimelinePanel "day-of run sheet"; TimelineBlockPicker "Already on this run"; EventTimelineSidebar "Questions on this run sheet"; eventVenueLabel "Venue record unavailable" (tests/features/events/event-venue-label.test.ts asserts the old string — update that test with the swap); EventGuestPanel note="recorded"; useEventMenuNutrition "recorded nutrition"; import executor/runner "cannot run"; and `src/operations/event.manifest` policy/constraint strings (regen after). Remaining PL-RECONCILIATION legs are still AC-191 / AC-388 remainder (proposal-draft auto-create is the missing create path). User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

This file now serves two roles. The audit below it was plan only: it audited shared `dev` at `be4288fb`, beginning 2026-09-20, against all 22 files under `specs/` and all 115 files under `src/lib/`, with no application or test implementation. Since 2026-09-21 the plan also tracks real implementation: the first PL-BUILD increment (#380 root-count repair) is done and verified on Windows, with evidence in [build-gate-evidence-2026-09-21.md](codex-plans/whole-spec-audit-2026-09-20/build-gate-evidence-2026-09-21.md). Source hashes, stable criterion mappings, current code evidence and shared-library reuse are recorded in [source-coverage.json](codex-plans/whole-spec-audit-2026-09-20/source-coverage.json), [shared-library.md](codex-plans/whole-spec-audit-2026-09-20/shared-library.md) and [review-decisions.md](codex-plans/whole-spec-audit-2026-09-20/review-decisions.md).

The outcome is the full audience job: Josh sells once and operates from one Event; sales carries every client decision forward; Kayden can prep, pack, work and record time from a phone; clients can request, accept and pay; Tim can reconcile live money and operations. Every in-spec gap remains below. Priority orders dependencies, not a release ceiling or an owner deferral.

Preserve the working transactional booking and menu copy, quote conversion checkpoints, recipe saved review/finalization, exact unit helpers, culinary demand, shared weekly purchasing, inventory/staffing cancellation callbacks, atomic templates, live dashboards, payroll CSV, durable invoice reminders, number sequence, scoped file reads and authorized self-hosted release orchestrator. Where code exists without full proof, the task is qualification followed only by demonstrated repairs. Reuse `src/lib` and authored seams; no parallel CRM, pricing engine, command API, domain model or workflow engine.

The backend contract permits backend repairs plus minimum typed consumer compatibility, not a UI redesign. Separate older product requirements still need their normal user-facing workflows. DESIGN.md front matter controls presentation; incomplete facts create scoped issues, not global service freezes. Generated output changes only through `bun run manifest:regen`. Each future implementation task gets a narrow worker brief with exact owners, a neighboring pattern, prohibited changes and its named proving command; only one editing/testing worker runs at a time.

Open meaning decision: backend §4.2 uses `final` for service finished; §14.2 uses it for Ops Final review. Older complete-product §3.3 also distinguishes Confirmed from execution. The owner has been asked to choose. Preserve both required outcomes and plan the lifecycle mapping explicitly; proposed reconciliation is to keep the existing post-service `final` and represent pre-departure Ops Final as readiness, but this proposal is **not owner approval**. This does not block independent work. Missing authoritative manual/KPI details and business-specific financial/retention/pay terms must be obtained before asserting parity or activating their calculations; safe implementation and evidence preparation remain planned.

Acceptance is [ACCEPTANCE_TESTS.md](ACCEPTANCE_TESTS.md). A checkbox in this plan is not proof. Each task below names all its required AC IDs; the source ledger contains the precise gap/evidence per criterion. Run the named criterion tests (or extend their existing owning suite), applicable runtime/browser/J evidence, deterministic regeneration when relevant, and `bun run check`. Never create speculative implementation-only tests or restore removed source-string tests. Independent different-model APPROVE is required before merge; production release requires explicit current-conversation release authorization and the approved orchestrator.

Current maintenance: required upstream `1c065a3e` is ancestral; local `dev` HEAD will be this leftover identity READ string increment. This leftover identity READ string increment is done; next remaining PL-PLAIN-WORDS work is leftover admin/operations/logistics/sales READ, leftover sales finance READ (payments / payment methods / tax rates / credit memos), leftover tpp-report-favorite READ, leftover constraint "Closed or corrected time records require clock-out…", leftover constraint "Recorded inventory lots require a supplier lot number…", leftover constraint "Equipment reservations require a valid date range…", leftover DishComponentsPanel "Component unavailable", leftover kitchen nav label "Components", leftover constraint "Portion spec name is required", leftover "Event number sequences are private implementation records", leftover event-packet READ (seam / projected), and leftover status lines such as "never synced on this connection". Orchestrator verified proving 2 files / 2 tests (identity 1 + admin 1); typecheck, format:check, and bun run build green. Finding recorded, not fixed: repo-root `capsule-backend-end-state-spec.md` still says setting final means Ops Final is complete; `specs/capsule-backend-end-state-spec.md` and §4.2 say the opposite. Both files are user-owned untracked — leave them. Preview `http://127.0.0.1:7813` serves this checkout. Local Convex `127.0.0.1:3210`. User-owned dirty files stay unstaged. Cursor issue [#394](https://github.com/Angriff36/capsule/issues/394) still intercepts `git commit` from bash — commit through `pwsh -NoProfile -File` so project `.githooks/pre-commit` runs. Do not use `--no-verify`. This is local verification, not production. PL-PLAIN-WORDS stays unchecked, and remaining PL-RECONCILIATION is AC-191 / AC-388 remainder. PL-LIFECYCLE remaining is AC-227 / AC-055 (owner #397). Do not invent a Confirmed stage. Do not change `final`.

Validation (updated 2026-09-21): the PL-BUILD first increment is green on BOTH build environments at commit `fcf2238af52a457f920fc1513cc2dc3e4e6f3349` (`dev`, tag v0.0.71, draft PR #388). Windows (working tree based on `89c103bc` plus the tested increment): `bun run check` passes end to end — typecheck, format, secrets, nine Manifest integration guards, coverage ratchet (194 files / 896 tests), build, and the index-based baseline check (`baseline-decay: ok`); `bun run manifest:regen:check` reports generated output current. Linux CI: run [35599405184](https://github.com/Angriff36/capsule/actions/runs/35599405184) on ubuntu-latest at that exact commit succeeded — "Verify generated output" and "Run repository check gate" both green, same 194/896 suite footer. Evidence: `.artifacts/iteration-pl-build/check-after.log`, `regen-check.log`, `test-after.log`, the CI logs, and the receipt. This satisfies the six PL-BUILD criteria only (AC-162, AC-356, AC-362, AC-395, AC-396, AC-439); the whole application delivery is NOT complete. Earlier audit-time facts stand: a worker returning provider 429 with exit 0 was rejected (#381), and source-confirmed issues #382–386 cover release evidence, QBO recovery, lifecycle, venue-note privacy and cutover readiness. Fix or qualify these through the tasks below; do not disguise them by changing unrelated user work.

Evidence (2026-09-21, pre-commit local working tree; Git commit/push and the post-commit regeneration gate are separate evidence): the 6 new real tests in `tests/proofs/accepted-revision-link.runtime.test.ts` pass, plus the focused run (4 files / 25 tests), the full suite (196 files / 904 tests) and full `bun run check` — all green. Review APPROVE: gpt-5.6-sol. The receipt holds source identity, gate logs, and preview details.

Pending tasks, sorted by dependency priority (P0 correctness foundations, P1 connected operations, P2 complete breadth and qualification; all are required):

- [ ] **P0 · PL-PLAIN-WORDS — Every word a user can see is plain catering English (owner, 2026-09-23).** Sweep ALL user-facing text: screen copy, buttons, warnings, empty states, toasts, import notes, and every constraint/guard/policy message in `src/**/*.manifest` (regen after). Replace developer words (entity.command names, reaction, guard, policy, constraint, idempotency, tenant, seam, projection, canonical, hydrate, mapped, record, run, directory, step counts, Manifest/Convex/Builder terms) with what a catering manager would say: what happened and what to do next. Example already done: the event import screen (93e17ea4). 2026-09-23 leftover events slice also done (CommandFailure / create aside / guests intro / import copy / import preview step chip / tab error banner), the 2026-09-23 "record" leftover slice also done (BEO PDF / Client tab including billing + contacts / Capacity planner / Allergen briefing / guest-list coverage notice), and the 2026-09-23 packet/menu/staffing/guest slice also done (Event packet, menu, guest sidebar/summary, overview rail, staffing, photos, prep list, equipment return, timing planner, venue picker, mobile event cards, create-venue capacity note, source panel, Photos & incidents tab label, timeline intro, live profitability) — do not redo those strings; the box stays unchecked. This slice also done (timeline / venue / guest / nutrition / import errors) — do not redo those strings. The 2026-09-23 kitchen nutrition first slice is also done — MenuDetailPage / ComponentNutritionPanel "recorded nutrition" now say "nutrition on file" — do not redo those two strings. The 2026-09-23 leftover kitchen catalog/record slice is also done — kitchen catalog list/create/preview, dish/ingredient/menu editor eyebrows, dashboard blocked empty reason, ingredient allergen empty, unit-conversion empty/button, method/portion/version empty states, and catalog card fallbacks no longer show "record" (This dish / This ingredient / This menu / New item / All items / Nothing matches this search / matching items / Open full card / Item details / Blocked — no reason on file / None on file / No source on file / Save conversion / Add one when a pack / No method on file / No earlier versions on file yet / No portion size on file / No description on file / Kitchen item / nutrition on file) — do not redo those strings. The 2026-09-23 leftover event.manifest policy/constraint slice is also done — `src/operations/event.manifest` event refusal and timing-rule messages no longer say "through commands", "execute … commands", "record timeline work", or "recorded timing/times/time"; they now say update/change/add and "saved timing/times/time", and the regen picked the wording up in convex/mutations.ts — do not redo those strings. The 2026-09-23 leftover events dietary-conflict slice is also done — EventMenuDietaryConflictsCard now says "Open dish ↗" (it still opens the shared dish) — do not redo that string and do not put "Catalog record ↗" back. The 2026-09-23 leftover kitchen Command deck slice is also done — the kitchen nav tab label in `src/features/kitchen/kitchenRoutes.ts` now says "Prep board" and the `EventPrepTab` button now says "Open prep board" (both still go to `/kitchen/prep`) — do not redo those strings. The 2026-09-23 leftover kitchen cleanup/prep/substitute slice is also done — catalog cleanup, serving-container empty, recipe-status empty, settled prep amount, picker duplicate warning, allergen-matrix footnote, leftover-prep error, vendor empty, and substitute headings/empty/shortage copy now say new item / No suggestions on file yet / No serving containers on file / isn't available for this item / Prep amount on file / separate item / no allergen is on file / leftover prep / this kitchen's usual vendor / Saved substitutes / No substitutes on file yet / Add same-unit ingredients / leftover stock — do not redo those strings. The 2026-09-23 leftover kitchen shortage-banner slice is also done — EventMenuStockShortageBanner now says "Not enough of this ingredient is on hold. Saved substitutes are ranked for the kitchen; the original ingredient is unchanged." — do not redo that string and do not put "Mapped alternatives" back. The 2026-09-23 leftover sales-screens slice is also done — QuoteSubmissionsReviewPage, QuoteSubmissionPage, ProposalAcceptancePage, ClientCommunicationPanel, ContractsPage, ContractDocumentPage, contractPdf.ts, ClientDetailPage, and ProposalReadinessNotice no longer show record / recorded / tenant / mapped / policy / Recording to the user (company profile; Anything already created is linked; privacy notice; Your acceptance is saved; Couldn't save your acceptance; Saving…; This acceptance is for; No date on file; before saving a conversation; mark it sent; Mark sent; another workspace; No extra terms; If you cancel; Signed in Capsule by; Not on file; Client signed; copied fields; "public quote form is offline" and the Admin → Branding link stay) — do not redo those strings. The 2026-09-23 leftover sales-manifests slice is also done — sales policy/constraint strings no longer say "through commands" / "execute … commands" / "record client communication" / "numbering seam" / "implementation records" / "generated commands" (update/change/add/send; Invoice numbers are assigned automatically; Anyone may send a quote request; Staff may add client notes) — do not redo those strings. The 2026-09-23 leftover culinary-manifests slice is also done — culinary write/execute policy strings no longer say "through commands" / "execute … commands" (Managers, sales and kitchen staff may update/change event dishes; Kitchen staff may update/change ingredients, recipes, menus, dish recipes, recipe versions, portion sizes, recipe imports; Kitchen, inventory and managers may update/change unit mappings; pinned denials "Kitchen, inventory and managers" and "Kitchen staff" still match) — do not redo those strings; culinary READ strings still say "components" / "component imports" / "portion specs" and stay open as a later leftover. The 2026-09-23 leftover logistics-manifests slice is also done — pack-list, vehicle, event-vehicle, delivery, and pack-list-template write/execute strings no longer say "through commands" / "execute … commands" / "record fuel/service" (Kitchen, logistics, event and sales staff and managers may update/change pack lists and pack list items; Logistics staff and managers may update/change vehicles, trailers, deliveries, pack list templates, and vehicle maintenance; may add/change fuel logs and vehicle service; Event, sales and logistics staff and managers may update/change event vehicle assignments) — do not redo those strings; `command record` names and "Insurance policy number is required" stay as later leftovers. The 2026-09-23 leftover workforce-manifests slice is also done — workforce write/execute policy strings no longer say "through commands" / "execute … commands" / "use commands" / "time record(s)" (update/change time entries, qualifications, shifts, schedule notices, candidates, interviews, availability, recurring availability, shared event staffing, staff messages, which chats they have read, performance reviews, one-on-ones, one-on-one actions, role scorecards, training modules, training completions, and shift types; the READ string "Workforce staff or the linked person may read time records" and the "Closed or corrected time records require clock-out…" constraint stay as later leftovers; shift-swap / push-subscription / chat-notify-preference were already catering English) — do not redo those strings. The 2026-09-23 leftover inventory-manifests slice is also done — inventory write/execute strings no longer say "through commands" / "execute … commands" / "write waste records" (Inventory staff and managers may update/change stock items; Inventory staff or event managers may update/change inventory reservations; Inventory staff and event managers may update/change ingredient demand; Inventory staff may update/change waste entries, stock transfers, stock count sessions, stock count lines, and storage locations; the READ string "Inventory staff may read waste records" and the "Waste quantity must be positive once recorded" / "Transfer quantity must be positive once recorded" constraints stay as later leftovers) — do not redo those strings. The 2026-09-23 leftover operations-remainder manifests slice is also done — venue-note, service-style-kit, venue-room, venue-layout-template, venue-vendor-relationship, service-style, occasion, and event-template write/execute strings no longer say "through commands" / "execute … commands" / "write venue vendor relationships" (Event staff may update/change venue notes, venue rooms, and venue layout templates; Event managers, logistics staff and managers may update/change service style kits; Event managers may update/change service styles and occasions; Event and sales staff may update/change event templates; Facility managers may update/change venue vendor relationships) — do not redo those strings; event.manifest stays untouched; event-packet.manifest and event-number-sequence.manifest stay as later leftovers. The 2026-09-23 leftover finance-manifests slice is also done — payroll-input, event-closeout, and revenue-attribution write/execute strings no longer say "through commands" / "execute … commands" / "write payroll inputs" / "write event closeouts" / "write venue commission terms" / "write attributions" (Finance managers may update/change payroll inputs; Finance staff and event coordinators may update/change event closeouts; Finance staff may update/change venue commission terms and attributions; the READ strings "Finance managers may read payroll inputs" / "Finance staff and event coordinators may read event closeouts" / "Finance staff may read venue commission terms" / "Finance and sales staff may read attributions" stay as later leftovers; closeout.manifest compile entry untouched) — do not redo those strings. The 2026-09-23 leftover admin / identity / facilities-manifests slice is also done — write/execute strings no longer say "through commands" / "execute … commands" / "write announcements" / "write people" / "write equipment" / "record equipment service" (Staff may update/change announcements; Managers and admins may update/change assistant configuration; Authenticated staff may update/change capability settings and cutover decisions; Workforce managers may update/change people; Users may update only their own email subscriptions; Inventory or logistics staff may update/change equipment and add/change equipment service; READ strings stay as later leftovers; CutoverPage.tsx untouched) — do not redo those strings. The 2026-09-23 leftover production-manifests slice is also done — write/execute strings no longer say "through commands" / "execute … commands" / "write prep tasks" / "write stations" / "write production batches" / "write batch allocations" / "post prep task comments" / "write quality checks" (Kitchen and event managers may update/change prep tasks, prep task materials, and prep task dependencies; Kitchen staff and managers may update/change stations, production batches, and batch allocations; Kitchen staff and event managers may update/change prep task comments; Kitchen staff may update/change quality checks; READ strings and "may declare prep task dependencies" stay) — do not redo those strings. The 2026-09-23 leftover procurement-manifests slice is also done — write/execute strings no longer say "through commands" / "execute … commands" / "write purchase needs" / "write vendor orders" / "write vendors" / "record receipt lots" (Inventory staff and managers may update/change purchase needs; Procurement and managers may update/change vendor orders, vendor order lines, receipt lots, confirmed ingredient prices, order demand links, and weekly purchasing config; Inventory and managers may update/change event ingredient contributions; Procurement staff may update/change vendors, vendor contacts, vendor contracts, and contract price tiers; READ strings and the "Recorded inventory lots…" constraint stay) — do not redo those strings. The 2026-09-23 leftover quality-manifests slice is also done — write/execute refusal strings no longer say "through commands" / "execute … commands" / "write event allergen checks" / "write incidents" / "write corrective actions" (Any staff member may raise or settle a review flag; Event and kitchen staff may update/change event allergen checks; Event and kitchen staff may update/change incidents; Event and kitchen staff may update/change corrective actions; READ strings and the "Recorded allergen checks are immutable" constraint stay) — do not redo those strings. The 2026-09-23 leftover documents-manifests slice is also done — documents write/execute refusal strings no longer say "through commands" / "execute attachment commands" / "write attachments" (Staff may update attachments; Staff may change attachments), and the 2026-09-23 leftover documents parent-constraint slice is also done — the parent-constraint refusal no longer says "Parent record is required" (This file needs something to attach to; do not redo it); the 2026-09-23 leftover documents READ string slice is also done — the READ refusal no longer says Staff may read attachments; chat files are read through their message (Staff may see attached files; chat files show up with their message); write/execute leftovers stayed; the "Chat files are attached through their message" constraint stays as a later leftover — do not redo those strings. The 2026-09-23 leftover integrations-manifests slice is also done — integrations write/execute refusal strings no longer say "through commands" / "execute integration connection commands" / "write integration connections" (Admins may update integration connections; Admins may change integration connections) — do not redo those strings. The 2026-09-23 leftover integrations READ string slice is also done — the READ refusal no longer says Managers may read integration connections (Managers may see outside-service connections); write/execute leftovers stayed — do not redo that READ string. The 2026-09-23 leftover insights-manifests slice is also done — insights write/execute refusal strings no longer say "through commands" / "execute saved report commands" / "write saved reports" (Staff may update saved reports; Staff may change saved reports) — do not redo those strings. The 2026-09-23 leftover insights READ string slice is also done — the READ refusal no longer says Undefined drafts are readable by staff; defined owner_only reports require ownership or manageAccess (Staff may see unfinished reports; a just-for-you report can only be seen by its owner or a manager); write/execute leftovers stayed; constraint leftovers stayed; tpp-report-favorite.manifest left untouched — do not redo that READ string. The 2026-09-23 leftover foundation-manifests slice is also done — foundation write/execute refusal strings no longer say "write organization records" / "execute organization commands" / "transaction seam" / "generated commands" (Managers may update the company profile; Managers may change the company profile; The system keeps these receipts; Staff cannot change these receipts) — do not redo those strings. The 2026-09-23 leftover foundation READ string slice is also done — the READ refusals no longer say Staff may read organization records / Materialization receipts are private implementation records (Staff may see the company profile; These receipts stay behind the scenes); write/execute leftovers stayed; constraint leftovers stayed — do not redo those READ strings. The 2026-09-23 leftover event-packet / event-number-sequence slice is also done — event-packet / event-number-sequence write/execute refusal strings no longer say "generated commands" / "source records" / "numbering seam" (The event packet keeps the source files; Staff cannot change packet files/issues/decisions/prints; Only Capsule can update event number counters; Event number counters stay behind the scenes; Only Capsule can assign event numbers; Assigned event numbers stay behind the scenes; the READ strings "Source bytes and private evidence require the scoped packet seam" / "Private issue evidence is projected through the packet seam" / "Event number sequences are private implementation records" and the constraints "Reconciliation owns issue lifecycle" / "Authenticated transactional decisions are immutable" / "Only exact-current immutable prints are recorded" stay as later leftovers) — do not redo those strings. The 2026-09-23 leftover CutoverPage screen is also done — CutoverPage and the user-visible cutover.ts messages no longer say record / run / mapped / execute GO (Switch from TPP / Last checks before Capsule takes over from TPP / Nothing left to match / Every leftover TPP item is matched up / Finish matching / Approve the switch / Don't switch yet / Do one last import / Match up every leftover imported item / Only admins can approve or stop this switch) — do not redo those strings and do not change evaluateProviderReadiness (#386). The 2026-09-23 leftover import-screen copy slice is also done — ImportRunsListPage / ImportRunDetailPage / QuickFileImport / ParallelRunDashboardPage / AdminWorkspaceNav no longer say "import run" / "Import Runs" / "this run" (Imports; New import; Start new import; No imports found; Start a new import; How an import works; See the import details; stuck imports; Write why this import failed; Import not found; Back to imports; Import details; What you can do; Copy import ID; Recent imports) — do not redo those strings. The 2026-09-23 leftover admin-nav labels slice is also done — AdminWorkspaceNav now says Compare with TPP and Match leftover items (same paths `/admin/parallel-run` and `/admin/reconcile`) — do not redo those two strings. The 2026-09-23 leftover compare/match page headings slice is also done — the ParallelRunDashboardPage / ExternalRecordsReconcilePage headings now say Compare with TPP, How the counts compare, Still to match, Not matched yet, and Match leftover items — do not redo those headings. The 2026-09-23 leftover compare/match table and body copy slice is also done — those two pages no longer say record / Unmapped / mappings / menu import run in table or body copy (Imported items… / In Capsule / Everything is matched / Not matched / leftover items / Items / What these numbers mean / Checked N item(s) / item(s) selected / Items marked Conflict) — do not redo those strings. The 2026-09-23 leftover Import Records column / Record Parse slice is also done — ImportRunsListPage column, ImportRunDetailPage Count items / item counts / Total items / Unaccounted items / Confirm final item counts, and QuickFileImport leftover match list — do not redo those strings. The 2026-09-23 leftover import match labels / compare help slice is also done — the leftover match list no longer says Needs mapping / matching queue / link(s) superseded / Drill-down / raw Type values such as event_record; it now says Needs a match, payment reference in the leftover match list, Reverted — N match(es) marked as replaced, Look closer: Click View to open individual items, and the Type column uses Event / Payment / … labels (the needs_mapping key and link.capsuleEntity === "event_record" comparisons stayed) — do not redo those strings. The 2026-09-23 leftover cutover check-status slice is also done — the leftover check-status lines and the CutoverPage card no longer say Business Validation / business sign-off / rollback plan / manual sign-off / I approve this cutover / Cutover approved / Cutover rejected (Waiting for a manager to sign off; A manager has signed off; A manager still needs to sign off; A manager still needs to sign off on this switch; No switch-back plan written yet; Switch-back plan is written; Write the switch-back plan before you switch; Manager sign-off; A manager still needs to sign off; I approve this switch; Switch approved - every check passed. Switch-back plan: …; Switch stopped - …); the identifiers businessValidation, rollbackPlan, recordCutoverApprovals, and evaluateProviderReadiness stayed — do not redo those strings. The 2026-09-23 leftover Provider Readiness card title/details slice is also done — the card no longer says Provider Readiness / QuickBooks, Calendar, SMS integrations operational (Outside services ready; QuickBooks, calendar, and texts are working); identifier providerReadiness and evaluateProviderReadiness strings stayed (#386) — do not redo those two card strings. The 2026-09-23 leftover Final Delta Import card title slice is also done — the card no longer says Final Delta Import (Last import); identifier finalDeltaImport and the View Imports link stayed — do not redo that title. The 2026-09-23 leftover Admin nav Cutover label slice is also done — the nav no longer says Cutover (Switch from TPP); path `/admin/cutover` stayed — do not redo that label. The 2026-09-23 leftover evaluateProviderReadiness strings slice is also done — the readiness messages no longer say before cutover / Run a sync / Provider readiness needs attention / Integrations / No integrations in use (OK for cutover) / Finish Stripe onboarding (Connect it again or take it off before you switch; Sync it before you switch; Sync clean before you switch; Finish Stripe setup before you switch; Outside services need attention; Outside services; No outside services in use (OK to switch)); function name evaluateProviderReadiness, identifier providerReadiness, status lines, and the #386 proof phrases stayed — do not redo those strings. The 2026-09-23 leftover culinary READ string slice is also done — leftover culinary READ refusals no longer say component / component imports / portion specs / dish component composition (Kitchen staff may see recipes / recipe ingredient lines / recipe steps / recipe versions / portion sizes / recipe imports / recipe import lines / dish recipes); write leftovers stayed; nested leftover "Kitchen staff may read nested recipe lines" stayed; "Portion spec name is required" stayed; kitchen screen leftover "No portion specs on this subrecipe" stayed — do not redo those strings. The 2026-09-23 leftover culinary READ leftover-may-read slice is also done — leftover culinary READ refusals no longer say may read nested recipe lines / dishes / dish ingredient lines / dish task templates / task materials / dish containers / ingredients / unit mappings / menus / menu dish lines / event dishes / event dish overrides (Kitchen staff may see nested recipe lines / dish ingredient lines / dish containers / ingredients; Kitchen, sales and managers may see dishes; Kitchen staff and managers may see dish task templates / task materials; Kitchen, inventory and managers may see unit mappings; Kitchen and sales staff may see menus / menu dish lines; Employed staff may see event dishes / event dish overrides) — do not redo those strings. The 2026-09-23 leftover kitchen portion-spec screen slice is also done — leftover DishComponentPortionSpecPanel copy no longer says portion spec / Component unavailable (Portion size saved / Could not save the portion size / Portion sizes / Recipe unavailable / No portion sizes on this subrecipe — add one on its page first / Portion size / Plain yield (no size) / Save portion size) — do not redo those strings. The 2026-09-23 leftover identity READ string slice is also done — the identity READ refusals no longer say may read people / unowned creation drafts or their own email subscriptions (Staff may see people; Users may see unfinished email subscriptions or their own email subscriptions); write/execute leftovers stayed; policy names (personRead, emailNotificationSubscriptionRead) stayed — do not redo Staff may see people / Users may see unfinished email subscriptions or their own email subscriptions. Next remaining PL-PLAIN-WORDS work is leftover import READ (external record links / import artifacts / import runs), leftover event-number READ, leftover facilities/admin/operations/logistics/sales READ, leftover sales finance READ (payments / payment methods / tax rates / credit memos), leftover tpp-report-favorite READ, leftover constraint "Closed or corrected time records require clock-out…", leftover constraint "Recorded inventory lots require a supplier lot number…", leftover kitchen nav label "Components", and leftover status lines such as "never synced on this connection"; the box stays unchecked. Do it area by area (integrations, insights, foundation), one commit per area, and keep the meaning exact. AC: a reviewer reading every string a user can see finds no developer word.
- [ ] **P0 · PL-LIFECYCLE — Align lifecycle commands, cancellation and proportionate readiness.** Resolve the two meanings of final, map legacy confirmed/reopen/archive outcomes, reconcile cancel guards with declared transitions, and inspect beginExecution/confirmSalesLock consistency (#384). Domain issues must not globally freeze reasonable service edits. Keep committed actuals and cancellation history. Owners/reuse: `src/operations/event.manifest; convex/lib/eventCancellation.ts; convex/lib/operationalEvents.ts`. Acceptance: AC-055, AC-099, AC-223, AC-224, AC-225, AC-226, AC-227, AC-228, AC-234, AC-235, AC-236, AC-237, AC-238, AC-239, AC-399, AC-400, AC-612. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs. The #384 first slice (cancel guard + beginExecution overgate repair, AC-226 PASS) landed 2026-09-22, AC-400 is now PASS via `tests/proofs/lifecycle-event-contract.runtime.test.ts` (each canonical transition emits exactly one typed ledger row; confirmSalesLock emits EventSalesLockConfirmed and zero EventExecutionStarted; cancel is idempotent-refused; returnToPlanning from pending_approval only — see the completed-increment history in git), the `returnToPlanning` guard is now aligned with the transition table (quote/pending_approval only — the dead `approved` leg is closed), and the AC-399 quote-stage zero-downstream proof now exists (`tests/proofs/lifecycle-quote-zero-downstream.runtime.test.ts`), followed by the planning/pending_approval provisional-drafts proof (`tests/proofs/lifecycle-planning-provisional-drafts.runtime.test.ts` — that slice is done, so the next AC-399 slice is no longer "planning/pending_approval provisional-drafts-only"); the sales_lock headcount slice is also done — `Event.changeHeadcount` now allows sales_lock (pricing still refused there), proven by `tests/proofs/lifecycle-sales-lock-ops-edits.runtime.test.ts`; the EventDish sales_lock ops-edits slice is done too — the seven EventDish live-ops command guards in `src/culinary/event-dish.manifest` now include sales_lock, proven by `tests/proofs/lifecycle-sales-lock-event-dish.runtime.test.ts`; the approved-ensure slice is also done — approve ensures invoice draft + pack list always (when quotedPrice exists), production batch when a dish seed exists, purchase need when calculated demand exists, and invents no staff-need shells, proven by `tests/proofs/lifecycle-approved-ensure.runtime.test.ts` (staffing-from-templates remains AC-495 / PL-STAFFING); the executing day-of-actions slice is also done — proven by `tests/proofs/lifecycle-executing-actions.runtime.test.ts`; the remaining legs are still open — the rest of the stage meanings (AC-399 stays PENDING; the next AC-399 slice is final / completed / closed_out / cancelled behaviors), the confirmed mapping (AC-099/AC-223 stay PENDING), and the owner final meaning (AC-612 stays PENDING). AC-226 and AC-400 remain PASS. The final-actuals slice is done — proven by `tests/proofs/lifecycle-final-actuals.runtime.test.ts` — so the next remaining AC-399 slice is completed / closed_out / cancelled behaviors; AC-399 stays PENDING, AC-226 and AC-400 remain PASS, and AC-099 / AC-223 / AC-612 stay PENDING. The InventoryReservation.reserve sales_lock hole is now closed — `reserve` in `src/inventory/stock.manifest` allows sales_lock, proven by `tests/proofs/lifecycle-sales-lock-reserve.runtime.test.ts` (see the completed note above); do not re-do that slice. Holds already reserved at approved still survive through sales_lock, which is why the terminal-meanings proof seeds at approved. The sales_lock Event ops-revise slice is also done — `Event.reschedule`, `Event.changeVenue`, `Event.changePrimaryContact`, `Event.changeRequirements` and the Event `isEditable` hydration now include sales_lock (tracker stage lists updated to match), proven by `tests/proofs/lifecycle-sales-lock-event-ops.runtime.test.ts` (see the completed note above); do not re-do that slice. AC-399 is now PASS via `tests/proofs/lifecycle-stage-meanings-matrix.runtime.test.ts` (see the completed note above), AC-225 is now PASS via `tests/proofs/lifecycle-transition-sequence.runtime.test.ts` (see the completed note above), and the remaining work on this task is AC-099 / AC-223 / AC-612 (owner Confirmed/final) plus AC-055 / AC-227 / AC-228 (AC-224 is now PASS via `tests/proofs/lifecycle-tpp-status-mapping.runtime.test.ts`). AC-238 is now PASS via `tests/proofs/lifecycle-event-version-conflict.runtime.test.ts` — two same-version edits prove the conflict state with nothing overwritten and a current-version edit still succeeds (see the completed note above); do not re-do that slice, and do not hand-edit the generated `Event_changeHeadcount` to make its optional `version` required. AC-237 is now PASS via `tests/proofs/lifecycle-plan-engagement-validation.runtime.test.ts` — inverted dates and headcount 0/100001 refuse with zero rows, a valid create succeeds, and refused edits leave the row untouched while a current-version edit succeeds (see the completed note above); do not re-do that slice. AC-236 is now PASS via `tests/proofs/lifecycle-venue-snapshot-stability.runtime.test.ts` — Venue_updateDetails / Venue_changeCapacity change only the catalog row while the Event snapshot stays put, and an explicit Event_changeVenue writes a new snapshot (see the completed note above); do not re-do that slice. The remaining work on this task is AC-099 / AC-223 / AC-612 (owner Confirmed/final), AC-055 / AC-227 / AC-228, plus AC-235 still PENDING. AC-234 is now PASS via `tests/proofs/lifecycle-event-tpp-fields.runtime.test.ts`. Finding recorded, not fixed: numbered TPP labels such as "2- Sales Lock" still fall through `mapTppEventStage` to `planning` — do not invent those keys without an approved data-map source. The three recorded sales_lock holes are now closed — Event.assignOwner, Event.normalizePurchasingWeek and EventDish.confirmFromProposal allow sales_lock, proven by `tests/proofs/lifecycle-sales-lock-remaining-ops.runtime.test.ts` (see the completed note above); do not re-do that slice. AC-099 / AC-223 / AC-612 stay PENDING (owner Confirmed/final still open) and the box stays unchecked. AC-239 is now PASS via `tests/proofs/lifecycle-event-duplicate.runtime.test.ts` and `tests/features/events/event-duplicate-planning.test.ts` (duplicate slice proven; see the completed note above), so the remaining work on this task is AC-099 / AC-223 / AC-612 plus AC-055 / AC-227 / AC-228 / AC-235. AC-234 is now PASS via `tests/proofs/lifecycle-event-tpp-fields.runtime.test.ts`. AC-235 is now PASS via the convert test in `tests/proofs/quote-conversion.runtime.test.ts` (see the completed note above), so the remaining work on this task is AC-099 / AC-223 / AC-612 (owner Confirmed/final) plus AC-055 / AC-227 / AC-228. AC-228 is now PASS via `tests/proofs/lifecycle-completion-correction.runtime.test.ts` (Event.correctCommercial at completed/closed_out with a required reason; see the completed note above), so the remaining work on this task is AC-099 / AC-223 / AC-612 (owner Confirmed/final) plus AC-055 / AC-227. AC-099 and AC-223 are now PASS via `tests/proofs/event-lifecycle-map.runtime.test.ts` (outcome map + no Confirmed stage; see the completed note above) — remaining work is AC-612 plus AC-055 / AC-227, and the box stays unchecked. AC-612 is now PASS via `tests/proofs/event-final-readiness.runtime.test.ts` and `tests/features/events/event-final-meaning.test.ts` (owner final-meaning slice; see the completed note above), so the remaining work on this task is AC-227 / AC-055, and the box stays unchecked. AC-227 owner decision filed as https://github.com/Angriff36/capsule/issues/397 — do not add venue/style gates on lockForSales until the owner answers. Remaining PL-LIFECYCLE work is still AC-227 / AC-055.

- [x] **P0 · PL-BOOKING — Make both booking paths converge and replay to the same Event.** Preserve the working transactional booking/menu cascade. The accepted-revision reference is stored and read, replay already returns the existing Event, public signature acceptance now runs the canonical operator path, and a quote-conversion retry links the reused draft to one canonical Event (AC-413, AC-434, AC-414, AC-435, AC-412, AC-433 PASS; see the completed-increment notes above — do not rebuild them). Still to prove on this task: the acceptance/booking projections stay in agreement, operator/signature/quote races cannot duplicate revisions, selections or effects, and venue mismatch stays visible — AC-410 (the broader booking-identity criterion). The AC-436 concurrent race slice is done — `tests/proofs/booking-race.runtime.test.ts` › "two concurrent createEventFromAcceptedProposal calls create exactly one event" plus the accept and revision tests in the same file prove overlapping Promise.allSettled owner.mutation calls land one live Event, one ProposalAccepted, and one proposalRevisions row with no product-code change — do not rebuild it. AC-006 is now PASS (2026-09-22 carry-over preview + J review — create-event lists the eight carried values and the linking reason before commit), and AC-398, AC-415, and AC-437 are PASS. The AC-100 field-staff booking-read proof is done — `tests/proofs/field-staff-booking-read.runtime.test.ts` proves event_staff reads guest count, style, venue, menu, timeline, and enhancement names and is denied proposal totals (getProposal null, listProposalByEventId [], getEventBookingDetails enhancement price null / canOpenProposal false for event_staff; sales_staff still reads total 1300 and Valet parking 400; cross-tenant getEventBookingDetails null) — do not rebuild it. Finding recorded, not fixed: Event.quotedPrice remains readable via getEvent to staffAccess (the AC-398 commercial seed); the AC-100 slice proves proposal totals are denied and does NOT hide Event.quotedPrice. The #393 unique-versus-ambiguous venue auto-select slice is done — `ProposalEventPrefill.matchVenue` returns unique / ambiguous / none and `EventCreatePage` auto-fills only a unique match, proven by `tests/features/events/proposal-event-prefill.test.ts` and `tests/event-create-behavior.test.ts` › "blocks venue auto-fill when two saved venues share the proposal's name and books the one the operator picks" — do not rebuild that proof. The AC-398 venue-display snapshot slice is also done — `eventVenueLabel` prefers Event.venueName and the Event detail header prints `venueLabel`, proven by `tests/features/events/event-venue-label.test.ts` and `tests/event-overview-behavior.test.ts` › "prints the booked venue snapshot in the header, not a later catalog rename" — do not rebuild it; AC-398 stays PENDING for the rest of BE-4.1. The AC-398 service-style snapshot slice is also done — Event stores `serviceStyleName`, `planEngagement` stamps it, `changeServiceStyle` preserves the stored snapshot when the name is omitted, and `eventServiceStyleLabel` is snapshot-first, proven by `tests/proofs/lifecycle-service-style-snapshot-stability.runtime.test.ts`, `tests/features/events/event-service-style-label.test.ts`, the mapper case in `tests/features/events/event-plan-engagement-form-mapper.test.ts`, and `tests/event-overview-behavior.test.ts` › "prints the booked service style snapshot in the details card, not a later catalog rename" — do not rebuild it; AC-398 stays PENDING for the rest of BE-4.1. The AC-398 occasion snapshot slice is also done — Event stores `occasionName`, `planEngagement` stamps it, and `eventOccasionLabel` is snapshot-first, proven by `tests/proofs/lifecycle-occasion-snapshot-stability.runtime.test.ts`, `tests/features/events/event-occasion-label.test.ts`, the mapper case in `tests/features/events/event-plan-engagement-form-mapper.test.ts`, and `tests/event-overview-behavior.test.ts` › "prints the booked occasion snapshot in the details card, not a later catalog rename" — do not rebuild it; AC-398 stays PENDING for the rest of BE-4.1. The AC-398 client-name snapshot slice is also done — Event stores `clientName`, `planEngagement` stamps it, and `eventClientLabel` is snapshot-first, proven by `tests/proofs/lifecycle-client-snapshot-stability.runtime.test.ts`, `tests/features/events/event-client-label.test.ts`, the mapper case in `tests/features/events/event-plan-engagement-form-mapper.test.ts`, and `tests/event-overview-behavior.test.ts` › "prints the booked client snapshot in the details card, not a later catalog rename" — do not rebuild it; AC-398 stays PENDING for menu/timing/readiness. The commercial-total-seed slice is also done — `eventCommercialQuotedPrice` never falls back to a live proposal total, proven by `tests/proofs/lifecycle-commercial-snapshot-stability.runtime.test.ts`, `tests/features/events/event-commercial-seed.test.ts`, and `tests/event-overview-behavior.test.ts` › "prints the booked commercial seed on the budget card, not a later proposal total" — do not rebuild it. The AC-398 menu dish-name snapshot slice is also done — EventDish stores `dishName`, the UI add path stamps it, and `eventDishLabel` is snapshot-first, proven by `tests/proofs/lifecycle-menu-snapshot-stability.runtime.test.ts`, `tests/features/events/event-dish-label.test.ts`, and `tests/event-menu-behavior.test.ts` › "prints the booked dish snapshot on the menu, not a later catalog rename" — do not rebuild it; AC-398 stays PENDING for timing/readiness. The AC-398 owner-name snapshot slice is also done — Event stores `ownerName`, `planEngagement` stamps it, `assignOwner` writes/clears/preserves it, a real `Person_correctIdentity` rename leaves it, and `eventOwnerLabel` is snapshot-first in the Assigned-owner rail card, proven by `tests/proofs/lifecycle-owner-snapshot-stability.runtime.test.ts`, `tests/features/events/event-owner-label.test.ts`, the mapper case in `tests/features/events/event-plan-engagement-form-mapper.test.ts`, and `tests/event-overview-behavior.test.ts` › "prints the booked owner snapshot in the assigned-owner card, not a later catalog rename" — do not rebuild it; AC-398 stays PENDING for readiness (AC-401, live projections). The AC-398 timing-input snapshot slice is also done — stored `Event.timing*` minutes and `serviceStartsAt` stay when a later `ServiceStyle_reviseDetails` renames the style, `Event_configureTiming` still writes a new snapshot, and `eventTimingStoredMinutes` returns stored finite minutes (including 0) and never the live `timingSuggestedSetupMinutes`, proven by `tests/proofs/lifecycle-timing-snapshot-stability.runtime.test.ts` and `tests/features/events/event-timing-inputs.test.ts` — do not rebuild it. The AC-398 source-evidence link slice is also done — the stored `Event.importSourceKey` is the source-evidence link (`Event_createViaCaptureDraft` writes it, an explicit `Event_updateImportDraft` rewrites it while omitted fields preserve, and a later `ServiceStyle_reviseDetails` catalog rename leaves it), `eventSourceEvidenceKey` never returns a later live source file name as the link, and the import panel prints the stored key, proven by `tests/proofs/lifecycle-source-evidence-link-stability.runtime.test.ts`, `tests/features/events/event-source-evidence-link.test.ts`, and `tests/event-overview-behavior.test.ts` › "prints the booked source-evidence key on the import draft, not a later live file name" — do not rebuild it; AC-398 stays PENDING for readiness (AC-401, live projections). Owners/reuse: `convex/lib/proposalEventCreation.ts; convex/quoteBuilder.ts; src/sales/proposal.manifest; src/sales/proposal-dish-selection.manifest`. Acceptance: AC-001, AC-002, AC-003, AC-004, AC-005, AC-006, AC-100, AC-398, AC-410, AC-411, AC-412, AC-413, AC-414, AC-415, AC-433, AC-434, AC-435, AC-436, AC-437. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs. AC-401 readiness projections now exist (see PL-READINESS). The AC-398 live readiness-display slice is done — `EventReadinessCard` prints the live `getEventReadiness` nine domains (reason · severity · resolvingAction) at the top of the Event overview rail, a later query result changes the card, and there is no Event readiness field, proven by `tests/features/events/event-readiness-summary.test.ts` (5) and `tests/event-overview-behavior.test.ts` › "prints the live nine-domain readiness summary, not a frozen Event readiness name" — do not rebuild it; AC-398 is PASS and BE-4.1 is fully proven. The AC-415 / AC-437 no-premature-effects slice is done — `tests/proofs/no-premature-effects.runtime.test.ts` (3) proves draft/send/view and accept/book create none of the invoice/message/provider/staff-notification/order/reservation/production rows, and approve after booking seeds one draft invoice — do not rebuild it; AC-415 and AC-437 are PASS. The PL-BOOKING box stays unchecked for the remaining AC-410 leg. AC-410 is now PASS via tests/proofs/booking-identity.runtime.test.ts; this box is checked.

- [x] **P0 · PL-COMMAND-REPLAY — Qualify canonical command identity and concurrent writes.** Use the same authenticated command contract for UI, external callers and M2M. Extend existing receipts and version handling for scoped replay, changed-input conflicts and privacy; investigate #281 without creating another command API. Multi-record invariants remain transactional. Owners/reuse: `api/manifest.ts; convex/lib/authContext.ts; convex/lib/materializationReceipt.ts; src/agent/`. Acceptance: AC-041, AC-397, AC-404. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs. 2026-09-22: AC-397 is now PASS via the gateway + CommandApiIdempotencyGate proofs (see Completed above) — do not rebuild that slice. 2026-09-22: the AC-404 identity formulas exist in `src/lib/workflowIdempotencyIdentity.ts` (do not rebuild them); remaining work was the AC-404 runtime reaction-replay proof — it is now done (see the Completed paragraph at the top). AC-404 is now PASS via tests/proofs/reaction-replay-identity.runtime.test.ts; this box is checked (AC-041, AC-397, AC-404).

- [x] **P0 · PL-PRICING — Make money and commercial projections agree exactly.** Preserve the central pricing path and immutable snapshots. Define deterministic smallest-unit rounding and supported currency behavior across preview, PDF, signature, acceptance, invoice, tax, allocation and reporting. Do not use display float rounding as proof of ledger correctness. Owners/reuse: `src/lib/pricing.ts; convex/lib/proposalPricing.ts; convex/lib/proposalRevision.ts; src/sales/invoice-core.manifest`. Acceptance: AC-089, AC-213, AC-263, AC-264, AC-265, AC-406, AC-418, AC-432. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs. AC-263 is now PASS via `tests/finance-precision-reconciliation.test.ts`; remaining legs are AC-213 / AC-264 / AC-265 / AC-406 / AC-418 / AC-432. AC-089 is now PASS via `tests/finance-precision-reconciliation.test.ts`. AC-213 is now PASS via `tests/finance-precision-reconciliation.test.ts`; remaining legs are AC-264 / AC-265 / AC-406 / AC-418 / AC-432. AC-406 is now PASS via `tests/quantity-money.test.ts` (with `tests/recorded-unit-mappings.test.ts`, the `tests/event-menu-cost.test.ts` mapping case, and `tests/proofs/menu-profitability-direct-ingredient.runtime.test.ts`); remaining legs are AC-264 / AC-265 / AC-418 / AC-432. AC-265 is now PASS via `tests/proofs/proposal-publish-blocks.runtime.test.ts`. AC-264 is now PASS via `tests/proofs/proposal-central-path-snapshot.runtime.test.ts`. AC-418 and AC-432 are now PASS via `tests/proofs/proposal-projection-agreement.runtime.test.ts`; no PL-PRICING legs remain. Do not re-do the five-bases / LedgerMoney / invoice-tax / AC-089 ledger slice, and do not re-do the commission / food-cost `CommercialMoney` slice.

- [ ] **P0 · PL-RECONCILIATION — Complete bounded Event-change reconciliation and receipts.** Retain working timing, staffing, inventory, purchasing, prep and cancellation callbacks. Complete missing triggers and receipts with source versions, per-domain counts/issues, replay checkpoints and preservation reasons. Unresolved data in one domain must remain scoped; do not introduce a second workflow engine. Owners/reuse: `convex/lib/operationalEvents.ts; convex/lib/materializationReceipt.ts; authored domain reconcilers`. Acceptance: AC-037, AC-191, AC-205, AC-388, AC-390, AC-407, AC-422, AC-423, AC-424. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs. AC-205 newly PASS via tests/proofs/cancellation-release.runtime.test.ts. AC-422 is now PASS via tests/proofs/canonical-change-events.runtime.test.ts. AC-423 is now PASS via tests/proofs/reconciliation-receipts.runtime.test.ts (timing-reconciler slice). AC-424 is now PASS via tests/proofs/reconciliation-failure-isolation.runtime.test.ts (Event.cancel timing-vs-purchasing isolation slice); remaining legs are AC-191 / AC-388 / AC-390 / AC-407. The AC-388 EventDish headcount-override slice is done via tests/proofs/override-survival.runtime.test.ts; AC-388 stays PENDING until the other override classes (pack quantity, purchasing buyer qty, staffing window) are proven. The AC-388 pack-quantity override slice is done via tests/proofs/pack-quantity-override-survival.runtime.test.ts. The AC-388 purchasing buyer-qty override slice is done via tests/proofs/buyer-qty-override-survival.runtime.test.ts. The AC-388 staffing-window override class is now proven via tests/proofs/staffing-window-override-survival.runtime.test.ts › "reschedule keeps a planTiming override and moves a following need" + "a second reschedule still leaves the override and moves the follower" (orchestrator re-ran 2/2). AC-388 stays PENDING — the remaining BE-1.3 work is auto-create/provenance of proposal draft, answers, documents, and invoice/closeout. The AC-390 schedule/staffing receipt slice is done via tests/proofs/single-reconciliation.runtime.test.ts; AC-390 stays PENDING for the rest of §1.5. The AC-390 headcount/menu receipt slice is done via tests/proofs/headcount-single-reconciliation.runtime.test.ts (Event.changeHeadcount persists exactly one EventHeadcountChanged menu receipt, override stays, following dish scales, replay is a no-op). The AC-390 headcount/pack receipt slice is done via tests/proofs/headcount-pack-reconciliation.runtime.test.ts (Event.changeHeadcount persists exactly one EventHeadcountChanged pack receipt, adjusted pack line stays 7, following container line scales 4→6, menu receipt still once, replay is a no-op). The AC-390 headcount/demand receipt slice is done via tests/proofs/headcount-demand-reconciliation.runtime.test.ts (Event.changeHeadcount persists exactly one EventHeadcountChanged demand receipt, override demand stays 25, following demand scales 40→60, menu receipt still once, replay is a no-op); The AC-390 headcount/prep receipt slice is done via tests/proofs/headcount-prep-reconciliation.runtime.test.ts (Event.changeHeadcount persists exactly one EventHeadcountChanged prep receipt, override prep stays 25, following prep scales 40→60, menu receipt still once, replay is a no-op). Spec finding: specs/capsule-complete-feature-spec.md §10.6 says mark prep for review when finalized inputs change, while specs/capsule-backend-end-state-spec.md §1.5 / §11.2 (the AC-390 source) updates unstarted generated prep in place — this slice follows the backend spec and the existing reconcileEventPrepWork path; the complete-feature spec stays planning reference, not rewritten here. The AC-390 headcount/staffing receipt slice is done via tests/proofs/headcount-staffing-reconciliation.runtime.test.ts (Event.changeHeadcount persists exactly one EventHeadcountChanged staffing receipt, captain planTiming override and following server need stay put, preservedCount 2 / updatedCount 0, menu receipt still once, replay is a no-op). Staffing does not scale with guest count — no ratio was invented (specs/ralph/production-09-workforce.md has no guest-count ratio; backend spec §12.2 only moves timing-following needs). The AC-390 headcount/proposal receipt slice is done via tests/proofs/headcount-proposal-reconciliation.runtime.test.ts (Event.changeHeadcount persists exactly one EventHeadcountChanged proposal receipt, the accepted proposal stays accepted / guestCount 40 / total 1300, unresolved is proposal_change_required, menu receipt still once, replay is a no-op). The signed document is not rewritten — no change-order command was invented. The AC-390 headcount/packet receipt slice is done via tests/proofs/headcount-packet-reconciliation.runtime.test.ts (Event.changeHeadcount persists exactly one EventHeadcountChanged packet receipt, the issued revision stays fingerprint seed-packet-40 / stage review / not superseded, unresolved is packet_stale, the issued historical packet is not rewritten — backend spec §14.1, menu receipt still once, replay is a no-op). The AC-390 recipe-quantity receipt slice is done via tests/proofs/recipe-quantity-reconciliation.runtime.test.ts (ComponentIngredient.adjustQuantity 1→2 persists exactly one ComponentIngredientQuantityAdjusted recipe receipt, Follow demand scales 40→80, Other demand stays 40, replay is a no-op). The AC-390 venue-change receipt slice is done via tests/proofs/venue-change-reconciliation.runtime.test.ts (Event.changeVenue persists exactly one EventVenueChanged venue receipt, Event.venueName Lakeside Pavilion, issued packet stays seed-packet-40 / not superseded, unresolved is packet_stale, replay is a no-op). The AC-390 rental-reschedule receipt slice is done via tests/proofs/rental-reschedule-reconciliation.runtime.test.ts (Event.reschedule persists exactly one EventScheduleChanged rental receipt, reserved hold moves, checked-out hold stays, replay is a no-op). The AC-390 cancellation-receipt slice is done via tests/proofs/cancellation-reconciliation.runtime.test.ts (Event.cancel persists exactly one EventCancelled cancellation receipt, reserved hold cancels, assigned crew unassigns, checked-out / checked-in stay, a second cancel is refused). The AC-390 style-receipt slice is done via tests/proofs/style-change-reconciliation.runtime.test.ts (Event.changeServiceStyle persists exactly one EventServiceStyleChanged style receipt, Limited Service snapshot, issued packet stays seed-packet-40 / not superseded, unresolved is packet_stale, replay is a no-op). AC-390 is now PASS — every named change type has a once-only receipt proof. Remaining PL-RECONCILIATION legs are AC-191 / AC-388 remainder / AC-407. The AC-407 submitted/ordered slice is done via tests/proofs/plan-vs-fact-matrix.runtime.test.ts (submitted 40 stays 40; new draft 20; replay is a no-op). The AC-407 partially-received slice is done via tests/proofs/plan-vs-fact-partial-receipt.runtime.test.ts (receive 15 of 40, headcount 40→60 keeps the receipt and adds one new draft at 20; replay of 60 is a no-op). The AC-407 completed-prep slice is done via tests/proofs/plan-vs-fact-completed-prep.runtime.test.ts (finish 40, headcount 40→60 keeps the finished row and opens one leftover pending at 20; replay of 60 is a no-op). The AC-407 consumed-stock slice is done via tests/proofs/plan-vs-fact-consumed-stock.runtime.test.ts (consume 40, headcount 40→60 keeps the used hold; replay of 60 is a no-op). The AC-407 custody slice is done via tests/proofs/plan-vs-fact-custody.runtime.test.ts (check out 1, headcount 40→60 keeps the custody hold; replay of 60 is a no-op). The AC-407 sent-invoice slice is done via tests/proofs/plan-vs-fact-sent-invoice.runtime.test.ts (approve+send the 4500 bill, headcount 40→60 keeps the sent invoice; replay of 60 is a no-op). The AC-407 finalized-closeout slice is done via tests/proofs/plan-vs-fact-finalized-closeout.runtime.test.ts (closeOut + recapture + finalize, then Event.correctCommercial 4000/6000 keeps the frozen snapshot; replay is a no-op). AC-407 is now PASS — every §6.5 row has a proof. Remaining PL-RECONCILIATION legs are AC-191 / AC-388 remainder.
- [x] **P0 · PL-READINESS — Expose domain readiness and safe resolving actions.** Replace incomplete boolean-only coverage with the nine required domain projections: issue code, affected IDs, severity, reason and permitted action. Readiness stays separate from lifecycle; optional unknowns remain informational and real harm gates only its affected action. Owners/reuse: `src/operations/event.manifest; authored event-domain read seams`. Acceptance: AC-401. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs. AC-401 newly PASS via tests/proofs/readiness-projections.runtime.test.ts (see the completed note above). Event overview UI not wired — remaining AC-398 display only.

- [ ] **P0 · PL-AUTH — Qualify tenant, actor, Person and command boundaries.** Preserve person-first workspaces and server-owned actor/tenant identity. Verify indexed reads, commands, background work, agent calls and cached state across supported roles and tenants. Do not add specialty read roles or organization-only onboarding restrictions. Owners/reuse: `convex/lib/authContext.ts; convex/lib/personAuthPick.ts; src/identity/; src/agent/`. Acceptance: AC-028, AC-151, AC-210, AC-212, AC-372, AC-403. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P0 · PL-STOCK-RESERVE — Enforce competing stock reservations transactionally.** Move any availability invariant currently enforced only by the client into the canonical transaction. Two Events competing for the last units produce one valid hold and an actionable line shortage, with substitute/transfer/purchase choices rather than freezing the Event. Owners/reuse: `src/inventory/stock.manifest; convex/lib/inventoryEvents.ts; event stock coordinator owners`. Acceptance: AC-080, AC-461, AC-462, AC-463, AC-464, AC-465, AC-466. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [x] **P0 · PL-VENUE-NOTES — Carry categorized venue notes with correct visibility.** Qualify author/time, Event link, category, pin/priority, archive and confidential visibility. Normal operational notes reach the crew while sensitive commercial notes stay out of client artifacts. Owners/reuse: `src/facilities/; src/features/facilities/`. Acceptance: AC-316, AC-317. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs. AC-316 is now PASS via `tests/proofs/venue-notes.runtime.test.ts` + helpers `tests/proofs/venue-notes.runtime.helpers.ts`; AC-317 is now PASS via `tests/proofs/venue-note-visibility.runtime.test.ts` › "a management_only note is refused to a non-management role and absent from every client-facing projection", so the PL-VENUE-NOTES box is checked.

- [ ] **P0 · PL-FILE-PRIVACY — Complete file parent authorization and revoked-share behavior.** Keep passing cross-tenant/parent ownership proofs. Cover upload association, metadata, download/export and expired/revoked share issuance end to end (AC-029 reopened); direct storage or attachment IDs never grant unrelated access. Owners/reuse: `convex/fileStorage.ts; convex/lib/blobs.ts; src/lib/fileStorageClient.ts; public share owners`. Acceptance: AC-029. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P0 · PL-PUBLIC-ACCESS — Give all scoped public links expiry and revocation semantics.** Preserve existing scoped token verification and proposal share lifecycle. Portal HMAC tokens currently lack expiry/revocation; complete that contract and verify private notes/cost/personnel isolation, changed revisions and stale cached documents across quote/proposal/payment surfaces. Owners/reuse: `convex/clientPortal.ts; convex/lib/clientPortalToken.ts; src/sales/share-link.manifest; public route owners`. Acceptance: AC-152. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P0 · PL-SESSION-ACCESS — Apply account and role changes to active sessions.** Qualify suspension/revocation/role changes on subscriptions and account-scoped offline caches. Keep supported personal and organization onboarding with correct branding; cross-account restoration and stale privileged state must be denied server-side without inventing blanket organization requirements. Owners/reuse: `src/app/ClaimGate.tsx; src/lib/useAuthStatus.ts; src/lib/offlineAuthSnapshot.ts; identity/device owners`. Acceptance: AC-150, AC-153. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-PROPOSAL-DRAFT — Generate and refresh sourced rich proposal drafts.** Extend the existing draft transaction into one retry-safe generation/refresh path from inquiry/Event facts. Cover every required section, supported visuals and customer choices, preserving authored wording, quantities, imagery, exclusions and override reasons. Missing facts and stale sections are explicit; publishing/sending stays an explicit action. Owners/reuse: `convex/lib/proposalDraft.ts; convex/lib/proposalRevision.ts; src/sales/; src/features/clients/`. Acceptance: AC-096, AC-259, AC-260, AC-261, AC-262, AC-377, AC-409, AC-416, AC-417, AC-431, AC-547. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-REVISION — Complete immutable publication, sharing and acceptance.** Keep existing publication snapshots and central renderers. Prove terms/version identity, expiry, revocation, supersession and signature/operator replay; historical acceptance carries source evidence without inventing a signature. Qualify desktop/mobile/PDF quality against DESIGN.md with real artifacts. Owners/reuse: `src/sales/proposal-revision.manifest; src/sales/share-link.manifest; src/sales/signature-request.manifest; convex/lib/proposalRevision.ts`. Acceptance: AC-039, AC-097, AC-098, AC-253, AC-254, AC-255, AC-256, AC-257, AC-258, AC-266, AC-267, AC-268. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-COMMERCIAL-CHANGE — Provide an audited post-acceptance commercial change path.** Accepted proposals currently freeze without a complete change-order path. Link a new commercial revision/correction to the accepted one and Event while preserving operational substitutions and quantities; never rewrite the signed agreement or require reopening the whole Event for a reasonable live correction. Owners/reuse: `src/sales/proposal.manifest; src/sales/proposal-revision.manifest; src/operations/event.manifest`. Acceptance: AC-101, AC-378, AC-420, AC-547. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-PUBLIC-MENU — Publish effective catalog prices and proven eligibility.** Use the existing catalog and pricing authority for public menu, quote, proposal and portal. Complete categories, package/season/effective-date/minimum/style eligibility, supported allergens and starting-at behavior; internal costs, margin and personnel remain private. Owners/reuse: `src/culinary/; convex/quoteBuilder.ts; src/features/clients/; src/lib/pricing.ts`. Acceptance: AC-064, AC-094, AC-199, AC-240, AC-241, AC-242, AC-332. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-QUOTE — Carry structured mobile quote selections into the sales workflow.** Preserve checkpointed conversion, deduplication, dismissal and working proposal deep links. Complete structured menu/package/enhancement/consent/attribution and estimate assumptions, plus the pipeline-to-queue leg and failure recovery; empty catalogs retain free-text facts. Owners/reuse: `convex/quoteBuilder.ts; src/features/clients/; tests/quote-review-behavior.test.ts; tests/lead-pipeline-behavior.test.ts`. Acceptance: AC-007, AC-008, AC-009, AC-010, AC-011, AC-018, AC-019, AC-095, AC-243, AC-244, AC-245, AC-246. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-CLIENT-PAYMENT — Finish scoped mobile client payment and portal results.** Retain existing staff payment links and confirmation sync. Wire the scoped client deposit/balance journey, actual invoice currency, pending/failed/succeeded states, authoritative balance refresh, lost-response recovery and callback replay without duplicate charges/allocations. Owners/reuse: `convex/clientPortal.ts; convex/invoicePayments.ts; src/lib/invoicePaymentActions.ts; src/features/clients/`. Acceptance: AC-102, AC-547. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-ROUTES — Obtain authoritative HQ-to-venue route facts.** Add tenant-selected operating locations, validated address/timezone facts and server-owned provider results with provenance, basis, traffic policy and staleness. Preserve unknown travel on failure. The labeled browser straight-line helper remains an estimate and must never drive authoritative schedule calculations. Owners/reuse: `src/operations/event.manifest; src/facilities/; convex/lib/eventTimingOperations.ts; authored route-provider seam (new if no owner exists)`. Acceptance: AC-382, AC-425, AC-426. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-TIMING — Complete route-backed timing rules and safe recalculation.** Extend existing backward/forward computations with safety buffer, briefing lead, configurable 180/90-minute setup defaults and source-based load rules. Cover all stale-input triggers and preserve reasoned overrides and last-known stale route evidence; materialize future milestones once. Owners/reuse: `src/operations/event.manifest; convex/lib/eventTimingOperations.ts; src/lib/eventTimingMilestones.ts`. Acceptance: AC-427, AC-428, AC-430. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-ROUTE-LEGS — Model separate vehicle and crew route windows.** Extend the single Event timing chain for multiple vehicles, split/venue-direct crews, outside vendors and trips. Derive each route, dispatch and staff window from the same canonical milestones with capacity/overlap constraints and source explanations. Owners/reuse: `src/logistics/; convex/lib/vehicleDeliveryAvailability.ts; convex/lib/eventTimingOperations.ts`. Acceptance: AC-429. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-SCHEDULE-CORRECTION — Preserve communicated schedules during timing changes.** Keep manual shift windows and existing weekly notices. Draft following shifts may reconcile; published or acknowledged call times need a visible proposed delta, explicit correction and durable notification/acknowledgment rather than silent movement. Owners/reuse: `convex/lib/eventStaffingOperations.ts; convex/lib/shiftSchedulingEvents.ts; src/workforce/shift.manifest`. Acceptance: AC-326, AC-430, AC-508. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-RECIPE-IMPORT — Extend durable recipe review to nested formulas.** Preserve the proven flat paste/CSV saved-review and atomic-finalization path. Add nested formulas, clear ingredient/subrecipe/instruction classification, correction and normal recipe-book use with raw source still accessible. Missing/cyclic formulas affect only their recipe. Owners/reuse: `src/features/kitchen/import/ComponentImportPage.tsx; convex/lib/culinaryOperations.ts; src/lib/tppRecipeRepair.ts; src/lib/safeCulinaryOperations.ts`. Acceptance: AC-036, AC-043, AC-044, AC-045, AC-066, AC-069, AC-073, AC-440. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-FORMULA-IDENTITY — Reconcile repeated and changed source formulas.** Deduplicate repeated category exports and identical scaled formulas by source identity/fingerprint without merging distinct same-name recipes. Preserve original serving basis and present differing formulas as explicit revisions for resolution. Owners/reuse: `src/lib/tppMenuCsv.ts; src/lib/tppRecipeRepair.ts; src/culinary/; convex/lib/culinaryOperations.ts`. Acceptance: AC-067. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-UNITS — Preserve dimensional quantities and unknown conversions.** Reuse exact ratios and measurement correction. Qualify mass/volume/count/batch/yield/waste/purchase-pack semantics, tiny values, fractions and fluid-versus-mass ounces. Nutrition density/display estimates are not stock/purchase conversion authority. Owners/reuse: `src/lib/recipeUnitConversion.ts; src/lib/componentBatchScale.ts; convex/lib/culinaryModel/units.ts; src/culinary/`. Acceptance: AC-042, AC-068, AC-441. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-DEMAND — Reconcile versioned culinary demand without duplicating work.** Preserve the existing demand engine and follow-versus-fixed servings. Complete eligible formula-change propagation, stable source contributions, substitutions/86 and overrides; retain completed work and historical versions while recalculating only remaining demand once. Owners/reuse: `convex/lib/culinaryModel/; src/procurement/event-purchasing.manifest; src/lib/culinaryDemandClient.ts`. Acceptance: AC-070, AC-072, AC-333, AC-379, AC-442, AC-443, AC-444, AC-445, AC-446, AC-447, AC-448, AC-450, AC-451, AC-459. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-FOOD-COST — Version ingredient costs and report honest coverage.** Retain known-cost plus missing-cost displays. Complete units, effective dates and source provenance, historical costing snapshots and event actual-versus-plan reconciliation. Zero, absent, unmatched and estimated costs have distinct meanings and cannot imply a complete margin. Owners/reuse: `src/culinary/; convex/lib/ingredientLookupApplyCost.ts; src/lib/culinaryDemandClient.ts`. Acceptance: AC-071, AC-334, AC-449, AC-454. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-NUTRITION — Qualify allergen and nutrition evidence through the kitchen catalog.** Preserve evidence-backed unknowns and the rich recipe/catalog operating job: methods, stations, media, portions and supported nutrition. Do not infer allergen-free from absence or count-unit weights from volume. Review customer/crew wording on actual artifacts. Owners/reuse: `src/lib/catalogUnitGrams.ts; src/lib/nutritionUnitScale.ts; src/culinary/; src/features/kitchen/`. Acceptance: AC-074. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-PREP — Preserve prep baselines, overrides and partial work.** Reuse existing remaining-work balancing and exact EventDish identity. Complete generated source/version baseline, manual adjustments, dependencies and safe retirement; never regenerate completed prep or treat unknown duration as a measured time. Owners/reuse: `convex/lib/culinaryOperations.ts; src/lib/prepWorkBalance.ts; src/lib/eventPrepGroups.ts; src/production/`. Acceptance: AC-337, AC-484, AC-485, AC-486, AC-487, AC-488, AC-489, AC-490, AC-492. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-BATCH — Record batch yield, quality and waste as actuals.** Preserve operator-entered zero yield and historical provenance distinctions. Complete batch/quality/waste actors, reasons, quantities, unit/location/Event attribution and partial correction paths; observed physical work is never inferred from a generated plan. Owners/reuse: `src/production/; src/features/kitchen/; tests/proofs/`. Acceptance: AC-032, AC-335, AC-458, AC-491. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-OPENING-STOCK — Establish reviewed opening stock with source and as-of basis.** Separate ingredients, reusable equipment, disposables, components and instructions. Preserve quantity, location, unit, timestamp and uncertainty; review incompatible/conflicting/unverified counts per record. The owner selects the cutover opening basis after the reconciliation is concrete. Owners/reuse: `src/inventory/; src/import/; convex/importCommit.ts`. Acceptance: AC-075, AC-076. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-STOCK-LEDGER — Reconcile all stock movements through one calculation.** Keep real receipt/issue/transfer/waste paths. Prove opening, receipt, adjustment, reservation, consumption, return and waste reconcile exactly once with traceable reasons and correct available stock; no independent UI arithmetic becomes authority. Owners/reuse: `src/inventory/stock.manifest; convex/lib/inventoryEvents.ts; src/lib/inventoryAuditClient.ts`. Acceptance: AC-077, AC-336, AC-460. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-WEEKLY-PURCHASING — Preserve contributions and buyer changes in shared weekly drafts.** Keep the existing shared-week draft and contribution model. Prove concurrent reconcilers, old/new week moves, removal/cancellation and manual adjustments converge. Submitted orders remain committed and receive explicit deltas/surplus actions; recipe use alone does not create needless production. Owners/reuse: `src/procurement/event-purchasing.manifest; convex/lib/purchasingEvents.ts; tests/proofs/event-weekly-purchasing.runtime.test.ts`. Acceptance: AC-078, AC-467, AC-468, AC-469, AC-470, AC-471, AC-472, AC-473, AC-474. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-RECEIVING — Correct partial receipts without rewriting history.** Connect partial receipt to stock and outstanding order balance exactly once. Corrections retain the receipt and apply a reasoned difference with actual vendor/location/unit provenance, including already consumed quantities. Owners/reuse: `src/procurement/; src/inventory/; convex/lib/purchasingEvents.ts`. Acceptance: AC-079, AC-475, AC-476, AC-477, AC-478. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-PURCHASE-UNITS — Expose purchasing units and explicit package rounding.** Retain fractional need and record compatible conversion, package size, ordered quantity and remainder. New location/vendor choices preserve the in-progress form. Draft import/recalculation never submits an order or contacts a vendor. Owners/reuse: `src/procurement/; src/lib/recipeUnitConversion.ts; src/features/inventory/`. Acceptance: AC-082, AC-083, AC-483. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-PEOPLE — Complete one-Person worker identity lifecycle.** Preserve existing hire, revoke, sign-in and email-correction flows. Complete suspension/reinstatement and conflicting login recovery without changing another identity or deleting historical assignments, time or payroll. External identity steps need replay-safe compensation. Owners/reuse: `convex/lib/personAuthPick.ts; src/lib/hiringPipeline.ts; src/features/admin/; src/workforce/`. Acceptance: AC-122, AC-123, AC-149, AC-328. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-STAFFING — Derive coverage and qualify assignment conflicts.** Reuse staffing needs and transactional scheduling. Complete template-based coverage, availability/time-off/overlap/qualification-expiry explanations and safe following windows. Optional qualifications must not block unrelated work; no automatic unqualified assignment. Owners/reuse: `convex/lib/eventStaffingOperations.ts; convex/workforceScheduling.ts; src/lib/workforceScheduling.ts; src/lib/timeOff.ts`. Acceptance: AC-124, AC-324, AC-325. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-STAFF-SELF-SERVICE — Complete native schedule-to-work self-service.** Preserve My Day, availability and swaps. Qualify offers, claim, acknowledgment, decline, waitlist, replacement and swap with one Person identity, latest local-time/location/instructions and operational-only visibility; vendor workers follow the same identity model. Owners/reuse: `src/workforce/; src/lib/staffShiftSwaps.ts; src/lib/eventDayBriefing.ts; src/features/staff/`. Acceptance: AC-125, AC-331, AC-383, AC-493, AC-494, AC-495, AC-496, AC-497, AC-498, AC-499, AC-500, AC-501, AC-503, AC-504, AC-505, AC-506, AC-507, AC-511, AC-512, AC-513, AC-514, AC-515, AC-516, AC-517, AC-518. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-TIME — Make time and break capture attributable and correct.** Qualify real clock-in/out, paid breaks/unpaid lunch, overnight and DST elapsed time, shift/Event inheritance and manager corrections with actor/reason/timezone. Stable operations must not create duplicate punches; do not substitute scheduled time for actual time. Owners/reuse: `src/workforce/time.manifest; src/features/staff/; src/lib/eventScopedQueries.ts`. Acceptance: AC-126, AC-502, AC-509. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-OFFLINE-TIME — Reconcile pending time and operational changes after connection loss.** Preserve last-confirmed reads and per-account isolation. Persist the original mutation identity across refresh and reconcile uncertain server success before retry; show pending/conflict versus confirmed state honestly on time, packing, load and return workflows. Owners/reuse: `src/features/staff/offlineStore.ts; src/lib/offlineAuthSnapshot.ts; src/lib/offlineAuthContext.tsx; src/lib/pendingOperationKey.ts`. Acceptance: AC-127, AC-139. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-PAYROLL — Derive payroll input and reconcile exported corrections.** Preserve current CSV profiles and actual-time preview. Derive approved payable minutes and effective terms, expose missing/disputed/overlapping inputs, and persist per-worker/period export/acknowledgment/correction evidence. Live Nowsta sync remains a separate provider requirement. Owners/reuse: `src/finance/payroll-input.manifest; src/features/finance/payrollExport.ts; src/features/finance/PayrollPreparePayloadBuilder.ts`. Acceptance: AC-128, AC-130, AC-510, AC-519, AC-627. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-PACK-RULES — Generate explainable pack lines from all Event facts.** Extend existing dish/style/template rules for servings, production notes, venue/setup/weather, rentals/decor, beverages/bar and standard kits. Retain compatible source contributions and manual baselines/overrides/exclusions; a required excluded capability needs a real replacement only at its relevant readiness check. Owners/reuse: `src/logistics/pack-list.manifest; convex/lib/operationalEvents.ts; src/lib/safeMaterialization.ts`. Acceptance: AC-034, AC-132, AC-340, AC-380, AC-520, AC-521, AC-522, AC-523, AC-524, AC-525, AC-527, AC-528, AC-529, AC-530, AC-531, AC-532, AC-533, AC-534, AC-536, AC-537, AC-538, AC-539. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-PACK-VIEWS — Project one pack-line set into four working views.** Reference, warehouse-category, vehicle/load and return views must share line IDs, quantities and actuals. Preserve source explanations and ownership/return rules; updating a quantity or packing in any view immediately agrees in all other views. Owners/reuse: `src/logistics/; src/lib/packingDisplay.ts; src/features/logistics/`. Acceptance: AC-229, AC-230, AC-231, AC-232, AC-233, AC-371, AC-381, AC-540. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-PACK-ACTUALS — Record partial packing and physical custody truthfully.** Preserve atomic template application and legal early packing. Complete actual counts, scans, substitutes, missing items, actor/time and custody changes with safe retry and server acknowledgment. No generated requirement may imply something was packed, loaded or checked. Owners/reuse: `src/logistics/; src/features/logistics/; src/lib/safeMaterialization.ts`. Acceptance: AC-134. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-ASSET-CATALOG — Complete owned, rental and client-provided asset responsibility.** Reuse Equipment instead of food stock. Cover serialized/bulk assets, bundles, pricing/cost, photos, locations, sub-rental need and client-provided responsibility, so sourcing and return obligations remain explicit through proposal, pack and finance. Owners/reuse: `src/facilities/equipment.manifest; src/logistics/; src/features/facilities/`. Acceptance: AC-131, AC-339, AC-526, AC-545, AC-548. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-ASSET-AVAILABILITY — Honor equipment condition and concurrent availability.** Keep transactional overlap checks; add missing maintenance/out-of-service condition semantics and verify two competing Events. Show conflicting window/quantity/location/condition and usable replacement, transfer or rental choices. Preserve event-owned reservation cleanup separately from existing staffing cleanup. Owners/reuse: `convex/equipmentCheckout.ts; convex/lib/equipmentReservationAvailability.ts; src/facilities/equipment.manifest`. Acceptance: AC-133, AC-341, AC-419, AC-541. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-RETURNS — Reconcile returns, damage and maintenance obligations.** Record expected/received/outstanding amounts and location/condition/loss/damage/cleaning with actor/time and billing responsibility. Maintenance removes future availability until restored; cancellation never erases dispatched items, vendor obligations or missing returns. Owners/reuse: `src/logistics/; src/facilities/; convex/lib/eventCancellation.ts`. Acceptance: AC-136, AC-137, AC-342, AC-543, AC-544, AC-546, AC-549, AC-551. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-DELIVERY — Qualify loading, dispatch, delivery and vehicle constraints.** Keep existing delivery records and vehicle allocation. Complete actual load/destination/person/time, capacity/overlap, acknowledgment/incidents and one movement per operation. Changes to dispatched work become explicit corrections rather than automatic rewrites. Owners/reuse: `src/logistics/; convex/lib/vehicleDeliveryAvailability.ts; convex/driverAssignment.ts`. Acceptance: AC-135, AC-535, AC-542, AC-550. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-FINAL-LOCK — Derive every office Final Lock answer from evidence.** Implement deterministic versioned policy for all fifteen office question groups with answered/not_applicable/unresolved/field_confirmation, sources, explanation, rule version, authorized override and responsible resolver. Missing facts produce exact scoped issues; model output cannot invent an authoritative answer. Owners/reuse: `src/operations/event.manifest; src/operations/event-packet.manifest; src/lib/eventPacket/; convex/lib/eventPacket/`. Acceptance: AC-384, AC-571, AC-572, AC-573, AC-574, AC-576, AC-577, AC-578, AC-579, AC-580, AC-581, AC-583, AC-584, AC-585, AC-586, AC-587, AC-588, AC-589, AC-590, AC-591, AC-592, AC-593, AC-594, AC-595, AC-596, AC-597, AC-608, AC-609, AC-611, AC-614, AC-616. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-FIELD-CONFIRMATION — Assign physical confirmations without pre-completing them.** Keep all ten field forms as named-person physical work, including two-person before-takeoff and arrival checks. Record actual actor/time/evidence and escalation. Office readiness and successful PDF generation cannot mark physical observations complete. Owners/reuse: `src/operations/event-packet.manifest; src/lib/eventPacket/; convex/lib/eventPacket/`. Acceptance: AC-389, AC-582, AC-598, AC-599, AC-600, AC-601, AC-602, AC-603, AC-604, AC-605, AC-606, AC-607, AC-610. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-PACKET — Generate and version the complete native Event packet.** Use native records as the primary source while preserving uploaded evidence. Complete all eight ordered sections, route/map/setup diagram/rental/staff content, binder instructions, versioned regeneration and stale sections. Printing is optional; readiness means complete native content, not merely an existing PDF. Owners/reuse: `src/lib/eventPacket/prepareNativeWorkbook.ts; src/lib/eventPacket/renderWorkbook.ts; convex/lib/eventPacket/; src/operations/event-packet.manifest`. Acceptance: AC-385, AC-557, AC-558, AC-559, AC-560, AC-561, AC-562, AC-563, AC-564, AC-565, AC-566, AC-567, AC-568, AC-569, AC-570, AC-613, AC-615, AC-617. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-PACKET-EVIDENCE — Retain source observations and canonical reconciliation.** Keep original documents, observations, contradictions and decisions. Resolve into canonical records with actor/provenance, then regenerate affected projections; never treat an imported workbook or free-text answer as a second operational source of truth. Owners/reuse: `src/lib/eventPacket/extractObservations.ts; src/lib/eventPacket/reconcile.ts; src/lib/eventPacket/resolveIssue.ts; convex/lib/eventPacket/reconcileNative.ts`. Acceptance: AC-555, AC-556, AC-575. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-INVOICE-NUMBER — Prove one sourced draft invoice and safe number allocation.** Preserve the transactional number sequence and unsent draft on approval. Qualify concurrent/repeated approval/import/closeout paths, billable-source linkage and historical numbers; Invoice.issue naming does not mean the draft was delivered. No duplicate placeholder or auto-send. Owners/reuse: `src/sales/invoice.manifest; src/sales/invoice-core.manifest; convex/lib/invoiceNumbering.ts; convex/lib/invoiceNumberSequence.ts`. Acceptance: AC-087, AC-182, AC-618. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-FINANCIAL-CLASSIFICATION — Classify source money before ledger effects.** Distinguish quote, invoice, payment, allocation, fee, gratuity, refund/return, credit, balance and aggregate report. Keep every row accounted for and prevent overlapping views from posting the same principal twice. Owners/reuse: `src/import/; convex/importCommit.ts; src/lib/tppReports/`. Acceptance: AC-084. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-FINANCIAL-REFERENCE — Preserve complete reference-only financial history.** Keep positive, zero and negative values with source/date/type/evidence. Reference mode excludes ledger totals and cannot trigger payments, provider invoices, reminders or old tasks; current parsers that drop zero/negative rows need explicit correction. Owners/reuse: `convex/importCommit.ts; src/lib/tppReports/reportValues.ts; src/features/admin/import/`. Acceptance: AC-085, AC-091. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-FINANCIAL-RECONSTRUCTION — Preview and reconcile explicit historical ledger reconstruction.** Keep reference mode distinct. Preview source numbers, currency, lines, taxes, charges, deposits, credits, effective dates and unpaid balances; incomplete detail stays unresolved. Apply reviewed canonical records without invented taxable lines or unintended external actions. Owners/reuse: `src/import/; src/sales/invoice-core.manifest; src/sales/payment.manifest; src/features/admin/import/`. Acceptance: AC-086. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-ACCOUNTING — Preserve distinct payment, fee, allocation and correction semantics.** Extend current payment lifecycle for partial allocations, overpayment, fees, refunds, chargebacks/ACH returns, credits and gratuity with original cause/effective date. Exact provider IDs reconcile; similarity only suggests. A transaction is never allocated twice. Owners/reuse: `src/sales/payment.manifest; src/sales/invoice-core.manifest; convex/invoicePayments.ts`. Acceptance: AC-088, AC-090, AC-092, AC-283, AC-284, AC-619, AC-620, AC-621, AC-622, AC-623, AC-624. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-FINANCE-RECONCILIATION — Make financial discrepancies inspectable by source and period.** Save reconciliation evidence by period/status/currency, including excluded duplicates, source totals, ledger totals and mismatch drill-down. Preserve actual-versus-import date and do not claim complete profit when cost coverage is incomplete. Owners/reuse: `src/features/finance/; src/features/admin/import/; src/features/reports/`. Acceptance: AC-093, AC-183. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-CLOSEOUT — Compute and freeze source-backed Event actuals.** Replace manual zero-seed completion with a canonical coverage-aware projection of revenue, food, waste, labor, vendor/rental/transport/damage, commission and headcount. Reuse known values without retyping; finalize source IDs/versions and add an audited correction result that preserves prior snapshots. Owners/reuse: `src/finance/event-closeout.manifest; src/features/finance/CloseoutBillingTruth.tsx; authored closeout projection seam`. Acceptance: AC-386, AC-551, AC-625, AC-626, AC-628. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-ARCHIVE — Expose complete archive accounting through ordinary intake.** Preserve the passing 90-versus-70 inventory/disposition proofs. Connect those stages to product intake, account for every workbook/section/row including unsupported/reference content, and enforce zero unaccounted records without implying every row became a native record. Owners/reuse: `convex/archiveInventory.ts; convex/archiveDisposition.ts; src/import/; src/features/admin/import/`. Acceptance: AC-020, AC-021, AC-027, AC-177, AC-178, AC-629. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-PROVENANCE — Keep source bytes and typed interpretation inspectable.** Reuse typed XLSX and provenance readers. Qualify original/checksum/parser/coordinates/raw/normalized/date-system/timezone evidence from the ordinary authorized workflow; bounded archive failures, formulas/macros, sparse cells and missing cached values remain explicit. Owners/reuse: `convex/archiveProvenance.ts; src/lib/tppReports/xlsxReader.ts; src/lib/tppReports/workbookProvenance.ts; src/lib/sourceProvenance.ts`. Acceptance: AC-022, AC-025, AC-026, AC-271. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-SOURCE-DELTA — Apply source revisions as reviewed deltas.** Preserve checksum-based duplicate detection. Complete changed-source versus native-edit ownership and replay across worker restarts/devices without second business copies; same source revision and changed source revision must have distinguishable outcomes. Owners/reuse: `src/lib/tppReports/archiveDelta.ts; convex/archiveInventory.ts; convex/importCommit.ts`. Acceptance: AC-023, AC-272, AC-273. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-IMPORT-RESUME — Finish durable import checkpoints through attachments.** Retain proven parent/child/deposit recovery and add attachment/imported-history coverage. Every checkpoint survives lost acknowledgment, browser closure and worker restart; resume creates only missing owned work and never replaces later user edits (AC-024 reopened). Owners/reuse: `convex/importCommit.ts; src/import/import-run.manifest; src/import/import-dataset.manifest; tests/proofs/import-resume-fault-injection.runtime.test.ts`. Acceptance: AC-024, AC-269, AC-631. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-IMPORT-CANCEL — Cancel and compensate only eligible run-owned work.** Stop unstarted work, preserve checkpoints and report compensation progress. Remove or reverse only unchanged records created by this run; later edits, shared records, committed effects and unresolved corrections remain visible. Owners/reuse: `convex/importCommit.ts; src/import/import-run.manifest; durable import scheduling owners`. Acceptance: AC-056. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-SOURCE-DATASETS — Complete the full mapped dataset and field coverage.** Keep all existing event/contact/venue/menu/pack/payment importers. Add missing company/contact history, staff/address, catalog/package/season/effective-price, reference, equipment and supplemental fields from the established maps; every field is normalized, preserved or explicitly unresolved. Owners/reuse: `src/import/; convex/importCommit.ts; src/lib/tppReports/; existing migration/data-map documents`. Acceptance: AC-057, AC-063, AC-274, AC-275, AC-276, AC-277, AC-278, AC-279, AC-280, AC-281, AC-282, AC-374. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-SOURCE-IDENTITY — Use stable source identity without coercing ambiguous people.** Exact source IDs can match automatically. Preserve single names, companies without contacts, international address/phone text and original spelling. Same-name/shared-email/renamed/truncated variants become per-record decisions rather than global confidence merges or fake defaults. Owners/reuse: `src/import/external-record-link.manifest; convex/importCommit.ts; src/lib/tppReports/reportValues.ts`. Acceptance: AC-058, AC-060, AC-179, AC-180, AC-181, AC-270, AC-408, AC-630. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-SOURCE-RESOLUTION — Persist reviewed source mapping and preview bulk effects.** Expose choose-existing/create-correct-type/relink beside source and result. Reuse the stable resolution after reload/replay and show affected records before bulk application; imported and native records use the same ordinary edit/search/archive/permission paths. Owners/reuse: `src/features/admin/import/ExternalRecordsReconcilePage.tsx; src/import/; convex/importCommit.ts`. Acceptance: AC-059, AC-065. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-SOURCE-MERGE — Make merge and relink preserve dependent history.** Preserve source links and native field ownership while moving dependent records. Complete atomic or checkpointed correction with progress and failed-retry recovery; no orphaned communication, event, invoice or payroll evidence. Owners/reuse: `src/import/; src/sales/; convex/importCommit.ts; authored client-merge seam`. Acceptance: AC-061. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-SOURCE-HISTORY — Make imported communications and tasks normal readable history.** Materialize original timestamps/completion/source under the actual client/Event for search and navigation. Attachments alone do not meet the history workflow; importing never sends messages, reopens completed tasks or schedules anniversaries/reminders implicitly. Owners/reuse: `src/import/; src/features/clients/; src/features/workforce/; inbox/task owners`. Acceptance: AC-062, AC-111. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-DELIVERY-RECOVERY — Close ambiguous external-success and retry windows.** Use stable operation IDs and provider reconciliation after remote success/lost receipt (#383). Bound attempts/backoff, retain uncertain/dead-letter state and scoped retry. Deliberate new reminders stay possible; provider failures do not roll back unrelated business edits. Owners/reuse: `convex/qboSync.ts; convex/googleCalendar.ts; convex/invoiceReminders.ts; durable delivery owners`. Acceptance: AC-108, AC-118, AC-119, AC-163, AC-206, AC-207, AC-208, AC-346, AC-347, AC-348, AC-361, AC-373, AC-634. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-AUDIT — Make consequential command and delivery evidence observable.** Reuse the event ledger and receipts for tenant/subject/Person/command/entity/time/operation/version attribution. Observe failures, queue age and preserved/unresolved work without leaking tokens/payment details or hand-editing generated mutations. Owners/reuse: `convex/lib/operationalEvents.ts; convex/lib/materializationReceipt.ts; supported audit sink and provider delivery owners`. Acceptance: AC-154, AC-193, AC-209, AC-635, AC-636. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-UI-CONTRACT — Document and qualify existing read/action/error/refresh contracts.** Map all ten screen areas to exact current functions, input sources, permissions/states, idempotency/version requirements, result IDs, effects, errors and refresh. Preserve generated envelopes; add source/rule/override/why/staleness explanations. Backend repairs use minimal typed adapters, not redesign. Owners/reuse: `src/lib/api.ts; src/lib/convexActionErrorMessage.ts; src/lib/publicErrorMessage.ts; owning authored read and command seams`. Acceptance: AC-031, AC-186, AC-187, AC-188, AC-192, AC-194, AC-195, AC-196, AC-197, AC-198, AC-200, AC-201, AC-202, AC-203, AC-204, AC-211, AC-360, AC-367, AC-368, AC-369, AC-375, AC-392, AC-393, AC-394, AC-402, AC-421, AC-438, AC-553, AC-554, AC-637, AC-638, AC-639, AC-640, AC-641, AC-642, AC-643, AC-644, AC-645, AC-646, AC-647, AC-648, AC-649, AC-650, AC-651, AC-652, AC-705. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-CATALOGS — Complete empty-catalog recovery and reactive selectors.** Preserve working register/relabel/retire/reactivate and free-text quote fallback. Prove Event selectors update from admin changes and missing dependent catalogs can be created without losing the form; retired values remain readable on existing records. Owners/reuse: `src/features/admin/CatalogsSection.tsx; src/features/events/EventCreateServiceStyleField.tsx; src/lib/eventCreateCatalogClient.ts`. Acceptance: AC-012, AC-013, AC-014, AC-015, AC-016, AC-138, AC-218, AC-219, AC-220, AC-221, AC-222. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-FIELD-REGRESSIONS — Qualify the remaining recorded field defects.** Refresh issue states and reproduce behavior before changes. Cover actionable pack-row errors, reserve explanations, copy metadata/tags, shift/Event inheritance, real date typing/blur and compatible stock units. Existing fixes need behavioral proof, not reinstated source-string tests. Owners/reuse: `specs/ralph/known-bugs-142-to-151.md; owning pack/equipment/menu/time/date/stock features`. Acceptance: AC-047, AC-048, AC-049, AC-050, AC-051, AC-052, AC-053. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-CANCELLATION-PROOF — Prove cancellation across all mutable and committed states.** Exercise all nine cancellation cases across the six allowed lifecycle stages, preserving submitted/received/consumed/completed/dispatched/paid facts. Inventory and staffing cleanup already run; extend their proof and close demonstrated gaps, especially equipment reservations. Owners/reuse: `tests/proofs/; convex/lib/eventCancellation.ts; convex/lib/inventoryEvents.ts; domain reconcilers`. Acceptance: AC-081, AC-479, AC-480, AC-481, AC-482, AC-675, AC-676, AC-677, AC-678, AC-679, AC-680, AC-681, AC-682, AC-683. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-ROLE-PROOF — Qualify the complete thirteen-role access matrix.** Exercise allowed and denied representative operations for every specified role, M2M, foreign tenant, unlinked and anonymous caller, plus storage/public/cache paths. Prove useful ordinary work succeeds as carefully as private access fails. Owners/reuse: `tests/proofs/; canonical command/read/auth seams`. Acceptance: AC-156, AC-357, AC-684, AC-685, AC-686, AC-687, AC-688, AC-689, AC-690, AC-691, AC-692, AC-693, AC-694, AC-695, AC-696. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-RACE-PROOF — Prove cross-domain races and recovery checkpoints.** Exercise all eight concurrency/recovery cases with genuine transaction/failure boundaries: last stock, booking, weekly order, parent/child crash, callback replay/order, import resume, stale UI and deterministic regen. Keep confirmed results on lost responses without weakening assertions. Owners/reuse: `tests/proofs/; existing materialization and import/runtime fixtures`. Acceptance: AC-173, AC-358, AC-405, AC-697, AC-698, AC-699, AC-700, AC-701, AC-702, AC-703, AC-704. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-LOOP-COMPLETION — Reject false worker success and stale completion claims.** A provider 429 with no task result currently exits zero (#381). Require usable completion evidence, separate checks/push/integration/deployment/authentication states, invalidate all-complete after spec changes and diagnose bounded nonconvergence. Owners/reuse: `ralph-worker.ps1; loop.sh; acceptance/coverage consumers`. Acceptance: AC-160. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-RELEASE-RECEIPT — Bind release evidence to the actual frontend and backend.** Keep the approved self-hosted release orchestrator. Replace equal-command-count/backend-cloud assumptions with exact deployed code/schema identity and a real authenticated product workflow (#382, AC-030 reopened). Missing evidence remains PARTIAL; execute production legs only after release authorization. Owners/reuse: `src/lib/releaseReceipt.ts; scripts/release-receipt.ts; scripts/deploy-production.sh`. Acceptance: AC-030, AC-161. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-CULINARY-CATALOG — Complete reusable food and recipe operating facts.** Preserve existing ComponentStep methods, DishStep station fields and dish/ingredient image storage. Complete demonstrated gaps in shelf-life/storage, recipe media and approved substitution context, equipment, plating, cooling/holding/reheat standards. Keep catalogs useful to cooks while incomplete cost or nutrition remains explicitly unknown. Owners/reuse: `src/culinary/component.manifest; src/culinary/dish.manifest; src/culinary/ingredient.manifest`. Acceptance: AC-452, AC-453, AC-457. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-PRODUCTION-PLAN — Consolidate daily and make-ahead kitchen production.** Project the same Event demand/prep records by day, Event, station, recipe and batch, with make-ahead windows and dependencies. Preserve actual output and per-Event source identity while pooling compatible work; do not create a separate prep database. Owners/reuse: `src/production/; convex/lib/culinaryModel/; existing kitchen planning queries`. Acceptance: AC-455. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-VENDOR-INVOICE-MATCH — Connect vendor bills to orders, receipts and actual costs.** Add the missing vendor-invoice matching leg to the existing demand→order→receipt chain. Preserve order/receipt/bill identities and quantities/prices; expose partial/disputed differences with reviewed correction and closeout provenance, without reposting source-reference money. Owners/reuse: `src/procurement/; src/finance/; existing receipt and reconciliation seams`. Acceptance: AC-456. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P2 · PL-HR — Complete restricted operational people follow-up.** Qualify role scorecard competencies/weights, hiring/interviews, event feedback, performance follow-up and monthly one-on-ones using current records and routes. Limit sensitive personnel notes by the intended policy without adding a general HRIS or hiding operational information from crew. Owners/reuse: `src/workforce/; src/lib/staffSelfReviews.ts; src/features/workforce/`. Acceptance: AC-129, AC-327, AC-329, AC-330. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P2 · PL-CUTOVER — Reconcile parallel operation and qualify each cutover.** Complete daily identity-based comparison, assignable discrepancies, final delta, opening-stock timestamp, financial mode, provider ownership, archive/rollback and disabling scheduled imports. Qualify the test year then full dataset. Owner go/no-go occurs on a concrete dossier; safe preparation stays in scope. Owners/reuse: `src/features/admin/import/; existing migration/data-map documents; docs/operations/`. Acceptance: AC-169, AC-174, AC-285, AC-286, AC-287, AC-288, AC-289, AC-290, AC-291, AC-292, AC-293, AC-632. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P2 · PL-VENUE-PROFILE — Complete structured venue operating facts.** Preserve current profiles and complete all specified capacities/spaces, access/kitchen/power/water, load-in/parking/elevators/storage/waste, permits/insurance, media and operating classifications. Feed authoritative fields into proposals, packing, staffing and reports without re-entry. Owners/reuse: `src/operations/event.manifest; src/features/facilities/`. Acceptance: AC-312, AC-313, AC-323. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P2 · PL-VENUE-LAYOUT — Version venue templates and Event logistics snapshots.** Keep venue templates separate from Event copies. Complete layout/logistics snapshot identity, edits, template preview/deltas and immutable publication/finalization context; event overrides never rewrite the reusable venue template. Owners/reuse: `src/facilities/; src/operations/; src/lib/safeMaterialization.ts; src/features/events/`. Acceptance: AC-314, AC-315. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P2 · PL-VENDOR-POLICY — Apply effective venue-vendor relationships proportionately.** Preserve existing preferred-vendor records; complete category/contact/effective/insurance evidence and approved/restricted/banned states. Show the applicable warning or narrowly justified block in the Event/proposal flow; no blanket approval bureaucracy. Owners/reuse: `src/facilities/; src/procurement/; src/features/facilities/`. Acceptance: AC-318, AC-319. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P2 · PL-ATTRIBUTION — Snapshot effective revenue and commission terms.** Complete percent/fixed basis, effective dates, source/type/reason, reasoned exceptions and Event snapshots. Reconcile gross, venue-produced, allocated and retained revenue from approved terms, including the specified 3% basis; never guess an unresolved definition. Owners/reuse: `src/finance/revenue-attribution.manifest; src/features/finance/; reporting owners`. Acceptance: AC-300, AC-301, AC-302, AC-303, AC-320, AC-321, AC-322. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P2 · PL-REPORT-INVENTORY — Account for every supplied report purpose.** Map all 90 actual workbooks and 70 indexed purposes to native output, source-only history or explicit unsupported requirement with owner/reason. The current 89-item catalog count is not evidence of equivalence; different purposes cannot collapse by similar names. Owners/reuse: `src/features/reports/; existing TPP report playbook; src/lib/tppReports/`. Acceptance: AC-140. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P2 · PL-REPORT-RENDERING — Qualify distinct operational report content.** Preserve existing dedicated renderers and complete each BEO/service/prep/recipe/purchasing/staffing/packing/finance field and grouping. Imported and native Events must produce usable ordinary report sets with explicit missing-source notices. Owners/reuse: `src/features/reports/; src/lib/eventPacket/; src/features/kitchen/`. Acceptance: AC-141, AC-148. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P2 · PL-REPORT-METRICS — Define and reconcile report measures and filters.** Reuse live report queries and filters. Record basis/timezone/currency/status/refund/fee/tax/ledger-or-reference semantics for every measure; qualify shareable dimensions, excluded/unresolved counts, contributor/source drill-down and honest unknown coverage. Owners/reuse: `src/features/reports/; src/features/finance/; approved TPP KPI playbook`. Acceptance: AC-142, AC-144, AC-145, AC-294, AC-295, AC-296, AC-297, AC-298, AC-299, AC-552. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P2 · PL-DASHBOARDS — Complete all seven live leadership dashboards.** Preserve existing routed live dashboards. Finish Tim KPIs, scorecard target/trend/owner/status, L10 history/actions, average-value drivers, compensation evidence, sales/3% basis and Mangia Round 4 parity with real queries and empty/error states. Review visual hierarchy within DESIGN.md. Owners/reuse: `src/features/reports/; authenticated dashboard routes; approved TPP KPI playbook`. Acceptance: AC-143, AC-304, AC-305, AC-306, AC-307, AC-308, AC-309, AC-310, AC-311, AC-370. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P2 · PL-REPORT-EXPORT — Prevent truncated or inconsistent report exports.** Replace silent fixed-row truncation with bounded pagination and truthful counts. Screen/print/download must share filters, revision, totals, missing notices and permissions; verify long-page layout and column completeness with rendered/exported artifacts. Owners/reuse: `src/features/reports/; authored report queries/export owners`. Acceptance: AC-146. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P2 · PL-REPORT-SNAPSHOT — Expose report freshness and reproducible snapshots.** Record source revision/as-of time and keep saved/finalized results reproducible. Acknowledged changes update live reports; refresh errors keep stale data visibly stale rather than presenting it as current. Owners/reuse: `src/features/reports/; src/finance/event-closeout.manifest`. Acceptance: AC-147. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P2 · PL-INBOX — Bring real inbound conversations into the sales workflow.** Keep existing manual conversation capture and honest reply states. Add supported account connect and signed/polled ingress with stable account/thread/message/media IDs, cursor overlap/restart recovery, client/lead/Event linking and retryable raw-source errors. Owners/reuse: `src/features/clients/; src/integrations/; authored email/social ingress seams`. Acceptance: AC-033, AC-104, AC-105, AC-106, AC-112, AC-247, AC-248, AC-249. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P2 · PL-OUTBOUND — Deliver exact proposal, invoice and reply artifacts durably.** Preserve working reminders and staff email. Bind recipients/sender/template/immutable revision/attachments to durable intent, with provider-accepted/delivered/bounced/unknown states. A queued record or manual status transition is not proof of delivery. Owners/reuse: `convex/invoiceReminders.ts; src/lib/invoiceReminderEmail.ts; src/lib/emailNotifications.ts; integration delivery owners`. Acceptance: AC-040, AC-107, AC-109, AC-352. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P2 · PL-CONSENT — Enforce current delivery preferences at send time.** Complete consent, suppression, opt-out, enabled channels and applicable quiet-hour checks for scheduled work across devices. Disconnect affects only work owned by that provider connection, not unrelated delivery services. Owners/reuse: `src/lib/emailNotifications.ts; convex/emailNotifications.ts; messaging/device registration owners`. Acceptance: AC-110, AC-120. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P2 · PL-CONNECTIONS — Complete provider ownership, connection and recovery contracts.** Record account/tenant/external identity, scopes, field ownership, cutover/history basis and durable run progress. Expose connect/reconnect/status/backlog/scoped retry/disconnect with safe errors; OAuth success alone is not synchronization. Confirm actual credentials only during qualification. Owners/reuse: `src/integrations/integration-connection.manifest; convex/qboSync.ts; convex/googleCalendar.ts; integration UI owners`. Acceptance: AC-113, AC-114, AC-121, AC-344, AC-345, AC-355, AC-633. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P2 · PL-QBO — Reconcile QuickBooks tax, corrections and history safely.** Preserve existing customer/invoice/payment sync. Complete field/tax/correction mapping, source ownership and reference-history exclusion by stable IDs; provider receipts and local balances agree without reposting archival money or creating edit loops. Owners/reuse: `convex/qboSync.ts; convex/lib/qboSync.ts; source-link and reconciliation owners`. Acceptance: AC-115, AC-349. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P2 · PL-CALENDAR — Qualify calendar update, cancellation and reconnect.** Preserve deterministic external identity and current sync. Prove material update, cancellation, revocation, target changes and overlapping retries yield one item per Event/target with visible recovery state. Owners/reuse: `convex/googleCalendar.ts; related calendar helper and generated integration bindings`. Acceptance: AC-116, AC-351. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P2 · PL-NOWSTA — Implement and qualify the specified live Nowsta mapping.** Inventory the agreed worker/role/shift/time/pay mappings and supported API boundary, then implement stable incremental sync, conflicts, corrections and acknowledgments. Existing payroll CSV is useful but cannot be relabeled live Nowsta integration; native replacement jobs remain required too. Owners/reuse: `src/integrations/; src/workforce/; src/features/finance/payrollExport.ts`. Acceptance: AC-117, AC-350. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P2 · PL-SMS-SOCIAL — Complete SMS and social provider-specific outcomes.** Reuse existing Twilio/connection mechanisms and complete authenticated callbacks, out-of-order state, supported outbound types and social account/thread/media behavior. Provider capabilities and actual live qualification evidence must be explicit without guessing account availability. Owners/reuse: `src/integrations/; convex/smsAlerts.ts; communication/provider owners`. Acceptance: AC-353, AC-354. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P2 · PL-RETENTION — Provide scoped retention, export and deletion with holds.** Preserve the existing export path. Inventory affected records and legal/business holds, preview authorized retention/deletion, retain required finance/employment history and audit the action. Resolve business retention values before activation without imposing approvals on ordinary edits. Owners/reuse: `src/features/admin/PersonalDataExportPage.tsx; privacy/authored data-operation owners`. Acceptance: AC-155. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P2 · PL-NATIVE-JOURNEY — Qualify the full native Event and replacement job chain.** Run all 22 golden-event steps on one tenant-isolated Event and stable identities, with realistic second-Event stock/order competition. Stub provider boundaries in runtime proofs and use actual authenticated workflows for browser/live evidence; no partial recipe or booking proof qualifies the whole chain. Owners/reuse: `tests/proofs/; authenticated existing product routes; qualification scripts and docs`. Acceptance: AC-017, AC-103, AC-168, AC-184, AC-189, AC-190, AC-338, AC-343, AC-376, AC-653, AC-654, AC-655, AC-656, AC-657, AC-658, AC-659, AC-660, AC-661, AC-662, AC-663, AC-664, AC-665, AC-666, AC-667, AC-668, AC-669, AC-670, AC-671, AC-672, AC-673, AC-674. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P2 · PL-REPLACEMENT-PROOF — Prepare separate legacy-product replacement dossiers.** Map recurring TPP/Nowsta/Galley/Goodshuffle/binder jobs to working normal paths, migrated history/documents, observable providers and operator trials. Obtain missing authoritative manual/KPI detail instead of inventing it. Explicit owner cutover approval comes after side-by-side and rollback evidence is reviewable. Owners/reuse: `AUDIENCE_JTBD.md; current Mangia job inventories; qualification docs`. Acceptance: AC-176, AC-387, AC-706, AC-707, AC-708, AC-709, AC-710, AC-711, AC-712. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P2 · PL-ROUTE-STATES — Qualify all shipped routes and remove actual dead behavior.** Inventory loading/empty/populated/denied/validation/conflict/error behavior and every enabled primary control. Preserve proven fixes and finish only actual in-spec stubs/dead paths; file platform blockers. Placeholder form hints and intentionally local UI state are not fake business functionality. Owners/reuse: `src/app/; src/features/; integration-guard and behavior suites`. Acceptance: AC-035, AC-038, AC-054, AC-170, AC-214, AC-215, AC-216, AC-217, AC-359. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P2 · PL-MOBILE-ACCESSIBILITY — Qualify critical client and field workflows at phone width.** Exercise 360px and desktop workflows with touch, keyboard, focus, screen-reader status, reduced motion and nested-dialog ownership. Keep DESIGN.md front-matter authority and review real Capsule artifacts for clarity/quality with recorded human or model judgment, not generic sample review criteria. Owners/reuse: `existing client/field/event UI; DESIGN.md; src/lib/llm-review.ts`. Acceptance: AC-046, AC-171, AC-185, AC-250, AC-251, AC-252. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P2 · PL-SCALE — Measure full-size tenant pagination and interaction performance.** Use at least 10,000 Events and 5,000 dishes with proportionate relations. Record hardware/network and at least 100 samples for proposed p95 under 1s list/detail and under 200ms interaction targets; separate cold start/export/import. Fix bounded paging/rendering problems before making scale claims. Owners/reuse: `report/list query owners; frontend list renderers; synthetic qualification harness (proposed)`. Acceptance: AC-172. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P2 · PL-GIT-MAINTENANCE — Qualify upstream integration and checkout-owned preview.** Preserve shared dev and user changes. Prove clean/diverged/dirty integration and wrong-checkout/occupied-port preview cases, reporting actual SHA/process/URL and both local serving identities; branch pushes never imply deployment. Owners/reuse: `ralph-sync.sh; ralph-preview.ps1; .ralph.env; loop.sh`. Acceptance: AC-157, AC-158, AC-159. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P2 · PL-BACKUPS — Prove isolated backup restoration and rollback boundaries.** Include database, required assets and configuration references under encrypted access/retention. Run an isolated restore validating relationships, counts, files and authentication; document irreversible provider effects and safe forward/correction paths. Owners/reuse: `docs/operations/; approved backup/deploy scripts and self-hosted backend owners`. Acceptance: AC-164. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P2 · PL-MONITORING — Make production and durable-job failures actionable.** Qualify failed deployment/backend/stuck queue/expiring connection alerts with safe diagnostics, tenant fairness and runbook recovery. Preserve business edits during outages and distinguish uncertain external effects from terminal failures. Owners/reuse: `approved production health/deploy scripts; provider/import/reconciliation owners`. Acceptance: AC-165. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P2 · PL-STALE-ASSETS — Recover from frontend version changes without losing forms.** Preserve atomic cache staging and auth exclusions. Prove incompatible assets and failed chunks expose actual version/recovery, preserving unsaved form drafts and never calling an old cached release current. Owners/reuse: `public/sw.js; src/app/shell/RouteErrorBoundary.tsx; src/ui/formDraft.tsx`. Acceptance: AC-166. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P2 · PL-COMPLETION-DOSSIER — Maintain full evidence and obtain final independent qualification.** Keep each requirement tied to owners, route/command, test, environment/SHA and actual result. Complete all definition-of-done and replacement legs, eligible different-model approval before merge, green required gates and authorized Git/release receipts. No global production-ready claim follows from this plan or the local preview. Owners/reuse: `ACCEPTANCE_TESTS.md; IMPLEMENTATION_PLAN.md; codex-plans/; repository review/release policy`. Acceptance: AC-167, AC-175, AC-363, AC-364, AC-365, AC-366, AC-391, AC-713, AC-714, AC-715, AC-716, AC-717, AC-718, AC-719, AC-720, AC-721, AC-722, AC-723, AC-724, AC-725, AC-726, AC-727. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P2 · PL-DECK-SHARING — Share published presentation artifacts with the same revision policy.** Complete the explicitly required presentation-deck sharing path using immutable published artifacts, sensitivity-appropriate scope, expiry/revocation and view tracking. Search and reuse existing document/publication ownership before adding a narrowly scoped artifact type; do not build a second presentation platform. Owners/reuse: `src/sales/share-link.manifest; existing proposal/document publication seams`. Acceptance: AC-253. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

Preserved completion evidence: AC-001, AC-002, AC-003, AC-004, AC-005, AC-007, AC-008, AC-009, AC-010, AC-011, AC-014, AC-015, AC-016, AC-017, AC-018, AC-019, AC-020, AC-021, AC-022, AC-023, AC-025, AC-026, AC-027, AC-028, AC-031, AC-032, AC-033, AC-034, AC-035, AC-036, AC-037, AC-038, AC-039, AC-040, AC-041, AC-042, AC-043, AC-044, AC-045, AC-046. These remain the narrower historical contracts, with renamed mounted-test references refreshed. They are not a substitute for any broader pending criterion. No source requirement was removed or parked as future work.
