# Whole-spec implementation plan

Completed (2026-09-23): the **PL-PLAIN-WORDS leftover events packet/menu/staffing/guest slice** is proven; the PL-PLAIN-WORDS box stays unchecked (sales / kitchen / logistics / workforce / finance / admin, event.manifest messages, and remaining events surfaces still open). Event packet, menu, guest sidebar/summary, overview rail, staffing, photos, prep list, equipment return, timing planner, venue picker, mobile event cards, create-venue capacity note, source panel, Photos & incidents tab label, timeline intro, and live profitability copy no longer show "record" / "recorded" / "run" to the user. No manifest change, no regen, no Confirmed stage, no `final` change. P: `tests/plain-words-events.test.ts` now 7 (added "keeps leftover event packet menu staffing and guest copy free of record jargon"). Orchestrator re-ran 7/7 proving, related event/create/overview/routes 40+17 green, the full suite 292 files / 1250 tests, typecheck, format:check, and `bun run build` green. Remaining events-area jargon (next events commit, not this one): EventTimelinePanel "day-of run sheet"; TimelineBlockPicker "Already on this run"; EventTimelineSidebar "Questions on this run sheet"; eventVenueLabel "Venue record unavailable" (tests/features/events/event-venue-label.test.ts asserts the old string — update that test with the swap); EventGuestPanel note="recorded"; useEventMenuNutrition "recorded nutrition"; import executor/runner "cannot run"; and `src/operations/event.manifest` policy/constraint strings (regen after). Remaining PL-RECONCILIATION legs are still AC-191 / AC-388 remainder (proposal-draft auto-create is the missing create path). User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

Completed (2026-09-23): the **PL-PLAIN-WORDS events "record" leftover slice** is proven; the PL-PLAIN-WORDS box stays unchecked (sales / kitchen / logistics / workforce / finance / admin, event.manifest messages, EventPacketPanel, EventMenuTab, EventGuestSidebar, EventOverviewRail, EventStaffingTab, and other leftover events surfaces still open). BEO PDF, Client tab (including billing + contacts), Capacity planner, Allergen briefing, and guest-list coverage notice no longer show "record" / "recorded" to the user. No manifest change, no regen, no Confirmed stage, no `final` change. P: `tests/plain-words-events.test.ts` now 6 (added "keeps leftover event screens free of record jargon"). Orchestrator re-ran 8/8 proving (plain-words-events + service-menu-output-behavior), the full suite 292 files / 1249 tests, typecheck, format:check, and `bun run build` green. Remaining events-area jargon (next events commit, not this one): EventPacketPanel ("Not recorded", "Update native record", "Current Capsule records", "Recorded revision", "Record decision"); EventMenuTab guest-count / catalog-record copy; EventGuestSidebar / eventGuestSummary "recorded"; EventOverviewRail "No operational requirements recorded."; EventStaffingTab "recorded work"; EventPhotosAlbums "Time not recorded"; EventPrepList; EventEquipmentPanel; EventTimingPlanner; EventTimelineTab; venuePickerSummary; LiveEventProfitabilityWidget; mobile event cards; EventSourceProvenancePanel; eventRoutes "Records" tab; and `src/operations/event.manifest` policy/constraint strings (regen after). Remaining PL-RECONCILIATION legs are still AC-191 / AC-388 remainder (proposal-draft auto-create is the missing create path). User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

Completed (2026-09-23): the **PL-PLAIN-WORDS events leftover slice** is proven; the PL-PLAIN-WORDS box stays unchecked (sales / kitchen / logistics / workforce / finance / admin, event.manifest messages, and remaining events surfaces still open). Event create aside, guests intro, import leftover copy, import preview step-count chip, tab error banner, and CommandFailure titles/details no longer show developer words. Detection regexes still match incoming Guard / policy / tenant / CONVEX errors. No manifest change, no regen, no Confirmed stage, no `final` change. P: `tests/plain-words-events.test.ts` (5) plus updated `tests/bulk-run-failure.test.ts` and `tests/event-create-behavior.test.ts`. Orchestrator re-ran 27/27 proving, the full suite 292 files / 1248 tests, typecheck, format:check, and `bun run build` green. Remaining events-area jargon (next events commit, not this one): `beoPdf.ts` "Not recorded" / "Live event record", EventClientBillingPanel / EventClientTab / EventCapacityPlannerPage / EventAllergenBriefingPage "Not recorded", and `src/operations/event.manifest` policy/constraint strings (regen after). Remaining PL-RECONCILIATION legs are still AC-191 / AC-388 remainder (proposal-draft auto-create is the missing create path). User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

Completed (2026-09-23): the **PL-RECONCILIATION AC-407 finalized-closeout slice** is proven, and AC-407 is now PASS — every §6.5 row has a real passing proof. No product-code, manifest, or generated-file change — Event.closeOut already seeds one draft EventCloseout; EventCloseout.capture / finalize freeze the snapshot; Event.correctCommercial writes only Event quotedPrice / budgetAmount; Event.changeHeadcount is refused at closed_out. After closeOut + recapture (4500 / 1950 / 38 actual guests) + finalize, Event.correctCommercial 3000/4500 → 4000/6000 keeps the finalized closeout (same id / status finalized / actualRevenue 4500 / budgetedRevenue 4500 / expectedHeadcount 40 / actualHeadcount 38 / same capturedAt / same finalizedAt) and opens no second closeout. Replaying the same correction writes no second closeout and does not rewrite the frozen snapshot. Draft/user-adjusted §6.5 rows remain the AC-388 override proofs; submitted/ordered remains `tests/proofs/plan-vs-fact-matrix.runtime.test.ts`; partially received remains `tests/proofs/plan-vs-fact-partial-receipt.runtime.test.ts`; completed prep remains `tests/proofs/plan-vs-fact-completed-prep.runtime.test.ts`; consumed stock remains `tests/proofs/plan-vs-fact-consumed-stock.runtime.test.ts`; custody remains `tests/proofs/plan-vs-fact-custody.runtime.test.ts`; sent invoice remains `tests/proofs/plan-vs-fact-sent-invoice.runtime.test.ts`. P: `tests/proofs/plan-vs-fact-finalized-closeout.runtime.test.ts` (2) + helpers `tests/proofs/plan-vs-fact-finalized-closeout.runtime.helpers.ts`. Orchestrator re-ran the new proof 2/2, related 13/13 (this file + sent-invoice + event-closeout-lifecycle + terminal-meanings + completion-correction), the full suite 291 files / 1243 tests, typecheck, format:check, and `bun run build` green. Remaining PL-RECONCILIATION legs are AC-191 / AC-388 remainder (auto-create of proposal draft, answers, documents, invoice/closeout). Do not invent Confirmed / change `final` / add lockForSales gates. User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

Completed (2026-09-23): the **PL-RECONCILIATION AC-407 sent-invoice slice** is proven; the PL-RECONCILIATION box stays unchecked and AC-407 stays PENDING (do NOT mark PASS). No product-code, manifest, or generated-file change — Event.approve already seeds one draft invoice from quotedPrice and Invoice.send stamps sentAt / status sent; Event.changeHeadcount does not write invoices. After approve+send of the 4500 quoted bill, Event.changeHeadcount 40→60 keeps the sent invoice (same id / invoiceNumber / status sent / total 4500 / amountDue 4500 / issuedAt / sentAt) and opens no second invoice. Replaying 60 writes no second invoice and does not rewrite the sent bill. Draft/user-adjusted §6.5 rows remain the AC-388 override proofs; submitted/ordered remains `tests/proofs/plan-vs-fact-matrix.runtime.test.ts`; partially received remains `tests/proofs/plan-vs-fact-partial-receipt.runtime.test.ts`; completed prep remains `tests/proofs/plan-vs-fact-completed-prep.runtime.test.ts`; consumed stock remains `tests/proofs/plan-vs-fact-consumed-stock.runtime.test.ts`; custody remains `tests/proofs/plan-vs-fact-custody.runtime.test.ts`. P: `tests/proofs/plan-vs-fact-sent-invoice.runtime.test.ts` (2) + helpers `tests/proofs/plan-vs-fact-sent-invoice.runtime.helpers.ts`. Orchestrator re-ran the new proof 2/2, related 11/11 (this file + custody + consumed-stock + invoice-payment + projection-agreement), the full suite 290 files / 1241 tests, typecheck, format:check, and `bun run build` green. Remaining AC-407 row: finalized closeout. Remaining PL-RECONCILIATION legs are AC-191 / AC-388 remainder (auto-create of proposal draft, answers, documents, invoice/closeout) / AC-407 remainder. Do not invent Confirmed / change `final` / add lockForSales gates. User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

Completed (2026-09-23): the **PL-RECONCILIATION AC-407 custody slice** is proven; the PL-RECONCILIATION box stays unchecked and AC-407 stays PENDING (do NOT mark PASS). No product-code, manifest, or generated-file change — EquipmentReservation.checkOut already stamps checkedOutAt / checkoutCondition and has no path back to reserved; Event.changeHeadcount does not run the rental reconciler and does not rewrite equipment holds. After reserve+checkOut of 1 chafer, Event.changeHeadcount 40→60 keeps the custody hold (same id / quantity 1 / status checked_out / checkedOutAt / window Oct 18 17:00–22:00 / condition good) and opens no leftover hold. Replaying 60 writes no second reservation and does not rewrite the handoff. Draft/user-adjusted §6.5 rows remain the AC-388 override proofs; submitted/ordered remains `tests/proofs/plan-vs-fact-matrix.runtime.test.ts`; partially received remains `tests/proofs/plan-vs-fact-partial-receipt.runtime.test.ts`; completed prep remains `tests/proofs/plan-vs-fact-completed-prep.runtime.test.ts`; consumed stock remains `tests/proofs/plan-vs-fact-consumed-stock.runtime.test.ts`. P: `tests/proofs/plan-vs-fact-custody.runtime.test.ts` (2) + helpers `tests/proofs/plan-vs-fact-custody.runtime.helpers.ts`. Orchestrator re-ran the new proof 2/2, related 8/8 (this file + consumed-stock + rental-reschedule + cancellation), the full suite 289 files / 1239 tests, typecheck, format:check, and `bun run build` green. Remaining AC-407 rows: sent invoice, finalized closeout. Remaining PL-RECONCILIATION legs are AC-191 / AC-388 remainder (auto-create of proposal draft, answers, documents, invoice/closeout) / AC-407 remainder. Do not invent Confirmed / change `final` / add lockForSales gates. User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

Completed (2026-09-23): the **PL-RECONCILIATION AC-407 consumed-stock slice** is proven; the PL-RECONCILIATION box stays unchecked and AC-407 stays PENDING (do NOT mark PASS). No product-code, manifest, or generated-file change — InventoryReservation.consume already stamps consumedAt and has no path back to active; Event.changeHeadcount does not rewrite reservations; inventoryEvents only releases active holds on cancel/complete. After reserve+consume of 40, Event.changeHeadcount 40→60 keeps the used hold (same id / quantity 40 / status consumed / consumedAt / reservedAt) and opens no leftover hold. Replaying 60 writes no second reservation and does not rewrite the used hold. Draft/user-adjusted §6.5 rows remain the AC-388 override proofs; submitted/ordered remains `tests/proofs/plan-vs-fact-matrix.runtime.test.ts`; partially received remains `tests/proofs/plan-vs-fact-partial-receipt.runtime.test.ts`; completed prep remains `tests/proofs/plan-vs-fact-completed-prep.runtime.test.ts`. P: `tests/proofs/plan-vs-fact-consumed-stock.runtime.test.ts` (2) + helpers `tests/proofs/plan-vs-fact-consumed-stock.runtime.helpers.ts`. Orchestrator re-ran the new proof 2/2, related 10/10 (this file + completed-prep + submitted + partial-receipt + headcount-prep), the full suite 288 files / 1237 tests, typecheck, format:check, and `bun run build` green. Remaining AC-407 rows: custody, sent invoice, finalized closeout. Remaining PL-RECONCILIATION legs are AC-191 / AC-388 remainder (auto-create of proposal draft, answers, documents, invoice/closeout) / AC-407 remainder. Do not invent Confirmed / change `final` / add lockForSales gates. User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

Completed (2026-09-23): the **PL-RECONCILIATION AC-407 completed-prep slice** is proven; the PL-RECONCILIATION box stays unchecked and AC-407 stays PENDING (do NOT mark PASS). No product-code, manifest, or generated-file change — kitchen already credits finished output and only resizes open lines (`convex/lib/prepWorkReconciliation.ts`; `PrepTask.reconcileRemainingWork` refuses completed). After one dish/prep line at 40 is assigned, started, and completed at 40, Event.changeHeadcount 40→60 keeps the finished row (same id / quantity 40 / completedQuantity 40 / completedAt / cook) and opens exactly one new pending generated leftover at 20 on the same recipe step. Replaying 60 writes no second leftover and does not rewrite the finished row. Draft/user-adjusted §6.5 rows remain the AC-388 override proofs; submitted/ordered remains `tests/proofs/plan-vs-fact-matrix.runtime.test.ts`; partially received remains `tests/proofs/plan-vs-fact-partial-receipt.runtime.test.ts`. P: `tests/proofs/plan-vs-fact-completed-prep.runtime.test.ts` (2) + helpers `tests/proofs/plan-vs-fact-completed-prep.runtime.helpers.ts`. Orchestrator re-ran the new proof 2/2, related 8/8 (this file + headcount-prep + submitted + partial-receipt), the full suite 287 files / 1235 tests, typecheck, format:check, and `bun run build` green. Remaining AC-407 rows: consumed stock, custody, sent invoice, finalized closeout. Remaining PL-RECONCILIATION legs are AC-191 / AC-388 remainder (auto-create of proposal draft, answers, documents, invoice/closeout) / AC-407 remainder. Do not invent Confirmed / change `final` / add lockForSales gates. User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

Completed (2026-09-23): the **PL-RECONCILIATION AC-407 partially-received slice** is proven; the PL-RECONCILIATION box stays unchecked and AC-407 stays PENDING (do NOT mark PASS). No product-code, manifest, or generated-file change — after VendorOrder.submit + confirm + VendorOrderLine.recordReceipt of 15 of 40 on both lines + VendorOrder.markPartiallyReceived, Event.changeHeadcount 40→60 keeps the original order (same id / submittedAt / both lines ordered 40 received 15 pending 25) and both InventoryLot receipts (same ids / receiptQuantity 15), and adds exactly one new draft at 20 per ingredient (60 − 25 still owed − 15 on the shelf). Replaying 60 writes no second draft and does not rewrite the receipt. Draft/user-adjusted §6.5 rows remain the AC-388 override proofs; submitted/ordered remains `tests/proofs/plan-vs-fact-matrix.runtime.test.ts`. P: `tests/proofs/plan-vs-fact-partial-receipt.runtime.test.ts` (2) + helpers `tests/proofs/plan-vs-fact-partial-receipt.runtime.helpers.ts`. Orchestrator re-ran the new proof 2/2, the submitted/ordered pair 2/2, related buyer-qty 2/2, the full suite 286 files / 1233 tests, typecheck, format:check, and `bun run build` green. Remaining AC-407 rows: completed prep, consumed stock, custody, sent invoice, finalized closeout. Remaining PL-RECONCILIATION legs are AC-191 / AC-388 remainder (auto-create of proposal draft, answers, documents, invoice/closeout) / AC-407 remainder. Do not invent Confirmed / change `final` / add lockForSales gates. User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

Completed (2026-09-23): the **PL-RECONCILIATION AC-407 submitted/ordered first slice** is proven; the PL-RECONCILIATION box stays unchecked and AC-407 stays PENDING (do NOT mark PASS). No product-code, manifest, or generated-file change — weekly purchasing already creates a new draft for the leftover after VendorOrder.submit (ensureWeeklyDraft matches status=draft else create; submitted lines stay historical via commitSupply pendingSupply). P: `tests/proofs/plan-vs-fact-matrix.runtime.test.ts` (2) + helpers `tests/proofs/plan-vs-fact-matrix.runtime.helpers.ts`. The proof shows VendorOrder.submit of the 40-guest weekly draft, then Event.changeHeadcount 40→60, keeps exactly one submitted order (same id / submittedAt / both lines 40) and adds exactly one new draft with 20 per ingredient; replaying 60 writes no second draft and does not rewrite the committed 40. Draft/user-adjusted §6.5 rows remain covered by the existing AC-388 override proofs (do not rebuild them). Remaining AC-407 rows: partially received, completed prep, consumed stock, custody, sent invoice, finalized closeout. Remaining PL-RECONCILIATION legs are AC-191 / AC-388 remainder (auto-create of proposal draft, answers, documents, invoice/closeout) / AC-407 remainder. Do not invent Confirmed / change `final` / add lockForSales gates. User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813. Orchestrator re-ran the new proof 2/2, related buyer-qty 2/2, full suite 285 files / 1231 tests, typecheck, format:check, and `bun run build` green.

Completed (2026-09-23): the **PL-RECONCILIATION AC-390 Event.changeServiceStyle style-receipt slice** is proven — every change type named in AC-390 now has a once-only receipt proof, so AC-390 is PASS. The PL-RECONCILIATION box stays unchecked (AC-191 / AC-388 remainder / AC-407 still PENDING). Files: `convex/lib/styleReconciliation.ts` (new EventStyleReconciliation on the shared eventReconciliation family, same-input replay early-return, domain `style`) and `convex/lib/operationalEvents.ts` (EventServiceStyleChanged wiring is one run call). `bunx convex codegen --typecheck disable` registered `convex/lib/styleReconciliation.ts` in `convex/_generated/api.d.ts`. No manifest change — Event.changeServiceStyle already writes the style snapshot and the generated pack-kit fanOut already runs; this class never writes Event/pack/packet/timing/staffing rows and never re-runs applyServiceStyleKit. P: `tests/proofs/style-change-reconciliation.runtime.test.ts` (2). The proof shows Event.changeServiceStyle to Limited Service persists exactly one EventServiceStyleChanged style receipt (updatedCount 1 / preservedCount 1), Event.serviceStyleName is Limited Service, the issued packet stays seed-packet-40 / not superseded, unresolved is packet_stale, and replaying the same style against unchanged packet input is a no-op. Do not invent Confirmed / change `final` / add lockForSales gates / invent timing recalc or staffing templates. Next remaining PL-RECONCILIATION work is AC-407 plan-vs-fact (or AC-191 / AC-388 auto-create of proposal draft, answers, documents, invoice/closeout). Finding recorded, not rewritten: `convex/lib/operationalEvents.ts` is 373 lines after this insert — the next Event handler should split the file before it crosses 400. User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813. Orchestrator re-ran the new proof 2/2, related 10/10, full suite 284 files / 1229 tests, typecheck, increment Prettier, and `bun run build` green.

Completed (2026-09-23): the **PL-RECONCILIATION AC-390 Event.cancel cancellation-receipt slice** is proven; the PL-RECONCILIATION box stays unchecked and AC-390 stays PENDING (do NOT mark PASS). Files: `convex/lib/cancellationReconciliation.ts` (new EventCancellationReconciliation on the shared eventReconciliation family, same-input replay early-return, domain `cancellation`) and `convex/lib/operationalEvents.ts` (EventCancelled wiring is one run call; the existing stand-down sequence moved into the class, including AC-424 timing/purchasing isolation). `bun run codegen` registered `convex/lib/cancellationReconciliation.ts` in `convex/_generated/api.d.ts`. No manifest change — reserved holds cancel and assigned crew unassign; checked-out holds and checked-in assignments stay as history. P: `tests/proofs/cancellation-reconciliation.runtime.test.ts` (2). The proof shows Event.cancel persists exactly one EventCancelled cancellation receipt (updatedCount 2 / preservedCount 2), the reserved hold is cancelled with reason "Client postponed", the assigned crew is unassigned, the checked-out hold stays checked_out, the checked-in assignment stays checked_in, and a second cancel is refused with no row diff and no second receipt. Do not invent Confirmed / change `final` / add lockForSales gates / skip the cancelled stage (EventCancelled fires after the Event is already cancelled). Next remaining AC-390 work is service-style. User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813. Orchestrator re-ran the new proof 2/2, related 13/13, full suite 283 files / 1227 tests, typecheck, increment Prettier, and `bun run build` green.

Completed (2026-09-23): the **PL-RECONCILIATION AC-390 Event.reschedule rental-receipt slice** is proven; the PL-RECONCILIATION box stays unchecked and AC-390 stays PENDING (do NOT mark PASS). Files: `convex/lib/rentalReconciliation.ts` (new EventRentalReconciliation on the shared eventReconciliation family, same-input replay early-return, domain `rental`) and `convex/lib/operationalEvents.ts` (EventScheduleChanged wiring routes through the rental class after timing + staffing; EventTimingConfigured does not). `bun run codegen` registered `convex/lib/rentalReconciliation.ts` in `convex/_generated/api.d.ts`. No manifest change — reserved holds move onto the new Event window; checked-out holds stay as history. P: `tests/proofs/rental-reschedule-reconciliation.runtime.test.ts` (2). The proof shows Event.reschedule Oct 18→19 persists exactly one EventScheduleChanged rental receipt (updatedCount 1 / preservedCount 1), the reserved hold moves to Oct 19 17:00–22:00, the checked-out hold stays Oct 18 17:00–22:00 / checked_out, and replaying the same schedule against unchanged rental input is a no-op. Do not invent Confirmed / change `final` / add lockForSales gates / invent followsEventTiming on reservations. Next remaining is cancellation. User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813. Orchestrator re-ran the new proof 2/2, related 10/10, full suite 282 files / 1225 tests, typecheck, increment Prettier, and `bun run build` green.

Completed (2026-09-23): the **PL-RECONCILIATION AC-390 EventVenueChanged venue-receipt slice** is proven; the PL-RECONCILIATION box stays unchecked and AC-390 stays PENDING (do NOT mark PASS). Files: `convex/lib/venueReconciliation.ts` (new EventVenueReconciliation on the shared eventReconciliation family, same-input replay early-return, domain `venue`) and `convex/lib/operationalEvents.ts` (EventVenueChanged wiring routes through the venue class). `CONVEX_DEPLOYMENT=befitting-armadillo-283 bunx convex codegen --typecheck disable` (or `bun run codegen`) registered `convex/lib/venueReconciliation.ts` in `convex/_generated/api.d.ts`. No manifest change — Event.changeVenue already writes the venue snapshot and emits EventVenueChanged. P: `tests/proofs/venue-change-reconciliation.runtime.test.ts` (2). The proof shows Event.changeVenue to Lakeside Pavilion / 12 Water Rd / 120 persists exactly one EventVenueChanged venue receipt (updatedCount 1 / preservedCount 1), Event.venueName is Lakeside Pavilion, the issued packet stays fingerprint seed-packet-40 / stage review / not superseded, unresolved is [{ code: "packet_stale", recordIds: [revisionId] }], and replaying the same venue against unchanged packet input is a no-op. Do not invent Confirmed / change `final` / add lockForSales gates / invent a route engine or purchasing-draft split. Next remaining is rentals / cancellation. User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813. Orchestrator re-ran the new proof 2/2, related 8/8 (venue + packet + canonical-change), related recon 8/8, full suite 281 files / 1223 tests, typecheck, increment Prettier, and `bun run build` green.

Completed (2026-09-23): the **PL-RECONCILIATION AC-390 Event recipe-quantity receipt slice** is proven; the PL-RECONCILIATION box stays unchecked and AC-390 stays PENDING (do NOT mark PASS). Files: `convex/lib/recipeReconciliation.ts` (new EventRecipeReconciliation on the shared eventReconciliation family, same-input replay early-return, domain `recipe`) and `convex/lib/operationalEvents.ts` (ComponentIngredientQuantityAdjusted wiring routes through the recipe class). `CONVEX_DEPLOYMENT=befitting-armadillo-283 bunx convex codegen --typecheck disable` (or `bun run codegen`) registered `convex/lib/recipeReconciliation.ts` in `convex/_generated/api.d.ts`. No manifest change — the ComponentIngredientQuantityAdjusted → EventDishComponentSeed.refresh → EventIngredientContribution.record → IngredientDemand.syncFromContributions fan-out already existed. P: `tests/proofs/recipe-quantity-reconciliation.runtime.test.ts` (2). The proof shows ComponentIngredient.adjustQuantity 1→2 persists exactly one ComponentIngredientQuantityAdjusted recipe receipt (updatedCount 1 / preservedCount 1), Follow demand scales 40→80 by `(quantity * batch * servings) / yield`, Other demand stays 40, and replaying 2 against unchanged recipe input is a no-op. Do not invent Confirmed / change `final` / add lockForSales gates / invent a second demand engine. Next remaining is rentals / venue / cancellation. User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

Completed (2026-09-23): the **PL-RECONCILIATION AC-390 EventHeadcountChanged packet-receipt slice** is proven; the PL-RECONCILIATION box stays unchecked and AC-390 stays PENDING (do NOT mark PASS). Files: `convex/lib/packetReconciliation.ts` (new packet reconciler on the shared eventReconciliation family, same-input replay early-return) and `convex/lib/operationalEvents.ts` (EventHeadcountChanged wiring routes through the packet class after proposal). `bun run codegen` registered `convex/lib/packetReconciliation.ts` in `convex/_generated/api.d.ts`. P: `tests/proofs/headcount-packet-reconciliation.runtime.test.ts` (2). The proof shows Event.changeHeadcount 40→60 persists exactly one EventHeadcountChanged packet receipt (updatedCount 0 / preservedCount 1), the issued revision stays fingerprint seed-packet-40 / stage review / not superseded, unresolved is [{ code: "packet_stale", recordIds: [revisionId] }], the menu receipt still exists once, and replaying 60 against unchanged packet input is a no-op. The issued historical packet is not rewritten — backend spec §14.1. Do not invent Confirmed / change `final` / add lockForSales gates / invent a change-order command / invent a guest-count ratio. Next remaining is recipe / rentals / venue / cancellation. User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

Completed (2026-09-23): the **PL-RECONCILIATION AC-390 EventHeadcountChanged proposal-receipt slice** is proven; the PL-RECONCILIATION box stays unchecked and AC-390 stays PENDING (do NOT mark PASS). Files: `convex/lib/proposalReconciliation.ts` (new proposal reconciler on the shared eventReconciliation family, same-input replay early-return) and `convex/lib/operationalEvents.ts` (EventHeadcountChanged wiring routes through the proposal class). `bun run codegen` registered `convex/lib/proposalReconciliation.ts` in `convex/_generated/api.d.ts`. P: `tests/proofs/headcount-proposal-reconciliation.runtime.test.ts` (2). The proof shows Event.changeHeadcount 40→60 persists exactly one EventHeadcountChanged proposal receipt (updatedCount 0 / preservedCount 1), the accepted proposal stays accepted / guestCount 40 / total 1300, unresolved is [{ code: "proposal_change_required", recordIds: [proposalId] }], the menu receipt still exists once, and replaying 60 against unchanged proposal input is a no-op. The signed document is not rewritten — backend spec §7.2 / golden 13 treat this as a recorded change requirement, not a rewrite of the accepted proposal. Do not invent Confirmed / change `final` / add lockForSales gates / invent a change-order command. Next remaining is headcount packet (then recipe / rentals / venue / cancellation).

Completed (2026-09-23): the **PL-RECONCILIATION AC-390 EventHeadcountChanged staffing-receipt slice** is proven; the PL-RECONCILIATION box stays unchecked and AC-390 stays PENDING (do NOT mark PASS). Files: `convex/lib/headcountStaffingReconciliation.ts` (new staffing reconciler on the shared eventReconciliation family, same-input replay early-return) and `convex/lib/operationalEvents.ts` (EventHeadcountChanged wiring routes through the staffing class). `bun run codegen` registered `convex/lib/headcountStaffingReconciliation.ts` in `convex/_generated/api.d.ts`. P: `tests/proofs/headcount-staffing-reconciliation.runtime.test.ts` (2). The proof shows Event.changeHeadcount 40→60 persists exactly one EventHeadcountChanged staffing receipt (preservedCount 2 / updatedCount 0), the captain planTiming override stays 14:00–18:00 / followsEventTiming false, the following server need stays open, the menu receipt still exists once, and replaying 60 against unchanged staffing input is a no-op. Staffing does not scale with guest count — no ratio was invented: specs/ralph/production-09-workforce.md has no guest-count ratio, and backend spec §12.2 only moves timing-following needs. Do not invent Confirmed / change `final` / add lockForSales gates / invent a guest-count ratio. Next remaining is headcount proposal/packet (then recipe / rentals / venue / cancellation).

Completed (2026-09-23): the **PL-RECONCILIATION AC-390 EventHeadcountChanged prep-receipt slice** is proven; the PL-RECONCILIATION box stays unchecked and AC-390 stays PENDING (do NOT mark PASS). Files: `convex/lib/prepReconciliation.ts` (new prep reconciler on the shared eventReconciliation family, same-input replay early-return) and `convex/lib/operationalEvents.ts` (EventHeadcountChanged wiring routes through the prep class). `bun run codegen` registered `convex/lib/prepReconciliation.ts` in `convex/_generated/api.d.ts`. P: `tests/proofs/headcount-prep-reconciliation.runtime.test.ts` (2). The proof shows Event.changeHeadcount 40→60 persists exactly one EventHeadcountChanged prep receipt (updatedCount 1 / preservedCount 1), the override prep stays 25, the following prep scales to 60, the menu receipt still exists once, and replaying 60 against unchanged prep input is a no-op. Do not invent Confirmed / change `final` / add lockForSales gates. Finding recorded, not rewritten: specs/capsule-complete-feature-spec.md §10.6 says mark prep for review when finalized inputs change, while specs/capsule-backend-end-state-spec.md §1.5 / §11.2 (the AC-390 source) updates unstarted generated prep in place — this slice follows the backend spec and the existing reconcileEventPrepWork path; do not rewrite the complete-feature spec here (it is planning reference, not a ralph spec). Next remaining is headcount staffing (then proposal/packet, then recipe / rentals / venue / cancellation).

Completed (2026-09-22): the **PL-RECONCILIATION AC-390 EventHeadcountChanged pack-receipt slice** is proven; the PL-RECONCILIATION box stays unchecked (AC-191 / AC-388 / AC-390 / AC-407 still PENDING — AC-390 is NOT PASS: schedule/staffing, headcount/menu, and this headcount/pack receipt legs are proven; recipe / rentals / venue / cancellation change types stay open). Files: `convex/lib/packReconciliation.ts` (new `EventPackReconciliation` + singleton — §8.2 receipt via the shared eventReconciliation family, same-input replay early-return, domain `pack`), `convex/lib/operationalEvents.ts` (EventHeadcountChanged now also routes through the pack class after the menu class). `bun run codegen` registered `convex/lib/packReconciliation.ts` in `convex/_generated/api.d.ts` (owning tool, two import lines). No manifest change. P: `tests/proofs/headcount-pack-reconciliation.runtime.test.ts` (2). Orchestrator re-ran 2/2, the related 6/6 (this file + headcount-single-reconciliation + pack-quantity-override-survival), the full suite 274 files / 1209 tests, typecheck, increment Prettier, and `bun run build` green. The proof shows Event.changeHeadcount 40→60 keeps an adjusted pack line at 7, scales the following container line to 6, and persists exactly one EventHeadcountChanged pack receipt plus the existing menu receipt; replaying 60 against unchanged input writes no pack-item diff and no second pack receipt. Do not invent a Confirmed stage. Do not change `final`. Do not add venue/style completeness gates on lockForSales. Do not mark AC-390 PASS from this slice. Staffing-from-headcount is skipped: specs/ralph/production-09-workforce.md has no guest-count ratio. Next remaining PL-RECONCILIATION work is the rest of AC-390 (headcount demand/prep/staffing/proposal/packet from §20.1 test 13, then recipe / rentals / venue / cancellation), then AC-407 / AC-191 / remaining AC-388 legs (proposal draft / answers / documents / invoice/closeout auto-create). Findings recorded, not fixed: (a) EventVenueChanged and EventServiceStyleChanged still have no operationalEvents handler; (b) EventStaffNeed open/claimed rows are still not cancelled on Event.cancel (AC-205 finding); (c) specs/ralph/production-06-booking-handoff.md still names Confirmed as a stage — backend spec + owner rule remain the authority. User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

Completed (2026-09-22): the **PL-RECONCILIATION AC-390 EventHeadcountChanged menu-receipt slice** is proven; the PL-RECONCILIATION box stays unchecked (AC-191 / AC-388 / AC-390 / AC-407 still PENDING — AC-390 is NOT PASS: only the schedule/staffing receipt leg AND this headcount/menu receipt leg are proven; recipe / rentals / venue / cancellation change types stay open, and staffing-from-headcount is not this slice). Files: `convex/lib/headcountReconciliation.ts` (new `EventHeadcountReconciliation` + singleton — §8.2 receipt via the shared eventReconciliation family, same-input replay early-return, domain `menu`), `convex/lib/operationalEvents.ts` (EventHeadcountChanged routes through the new class). `bunx convex codegen --typecheck disable` registered `convex/lib/headcountReconciliation.ts` in `convex/_generated/api.d.ts` (owning tool, two import lines). No manifest change. P: `tests/proofs/headcount-single-reconciliation.runtime.test.ts` (2). Orchestrator re-ran 2/2, the related 6/6 (this file + single-reconciliation + override-survival), the full suite 273 files / 1207 tests, typecheck, increment Prettier, and `bun run build` green. The proof shows Event.changeHeadcount 40→60 keeps an overridden dish at 25, scales the following dish to 60, and persists exactly one EventHeadcountChanged menu receipt; replaying 60 against unchanged input writes no dish diff and no second receipt. Do not invent a Confirmed stage. Do not change `final`. Do not add venue/style completeness gates on lockForSales. Do not mark AC-390 PASS from this slice. Next remaining PL-RECONCILIATION work is the rest of AC-390 (recipe / rentals / venue / cancellation, and staffing-from-headcount if a guest-count ratio exists), then AC-407 / AC-191 / remaining AC-388 legs (proposal draft / answers / documents / invoice/closeout auto-create). Findings recorded, not fixed: (a) §20.1 test 13 wants dishes+demand+prep+pack+staffing+proposal+packet on one headcount change — this slice is menu receipt only; (b) EventStaffNeed open/claimed rows are still not cancelled on Event.cancel (AC-205 finding). User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

Completed (2026-09-22): the **PL-RECONCILIATION AC-390 first slice (staffing receipts + single-reconciliation proof)** is proven; the PL-RECONCILIATION box stays unchecked (AC-191 / AC-388 / AC-390 / AC-407 still PENDING — AC-390 is NOT PASS: only the Event.reschedule / configureTiming staffing receipt leg is proven; headcount / menu / recipe / rentals / venue / cancellation change types stay open, and EventHeadcountChanged handling is a later slice). Files: `convex/lib/staffingReconciliation.ts` (new `EventStaffingReconciliation` + singleton — §8.2 receipt via the shared eventReconciliation family, same-input replay early-return, snapshots of eventStaffNeeds/eventAssignments/shifts before and after `reconcileEventStaffing`), `convex/lib/operationalEvents.ts` (EventTimingConfigured / EventScheduleChanged / EventTimelineCalculatedTimingRequested now route staffing through the new class; all other `reconcileEventStaffing` call sites unchanged). No manifest change. `bunx convex codegen --typecheck disable` registered `convex/lib/staffingReconciliation.ts` in `convex/_generated/api.d.ts` (owning tool, two import lines). P: `tests/proofs/single-reconciliation.runtime.test.ts` (2) + helpers `tests/proofs/single-reconciliation.runtime.helpers.ts`. Orchestrator re-ran the new proof 2/2, the related receipt/staffing/isolation regressions 8/8 after tightening the AC-423 timing-receipt lookup to `affectedDomains === ["timing"]` (staffing receipts now also exist on configureTiming), the full suite 272 files / 1205 tests, typecheck, increment Prettier, and `bun run build` green. One Event.reschedule moves the following staff needs exactly once and persists exactly one staffing receipt (triggerType EventScheduleChanged, affectedDomains ["staffing"]) alongside exactly one timing receipt; replaying the same R1 schedule against unchanged input writes no staffing rows and no second receipt for either domain. Do not invent a Confirmed stage. Do not change `final`. Do not add venue/style completeness gates on lockForSales. Do not mark AC-390 PASS from this slice. Next remaining PL-RECONCILIATION work is the rest of AC-390, then AC-407 / AC-191 / remaining AC-388 legs. User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

Completed (2026-09-22): the **PL-RECONCILIATION AC-388 staffing-window override slice** is proven; the PL-RECONCILIATION box stays unchecked (AC-191 / AC-388 / AC-390 / AC-407 still PENDING). AC-037, AC-205, AC-422, AC-423, and AC-424 stay PASS. No product-code, manifest, or generated-file change. P: `tests/proofs/staffing-window-override-survival.runtime.test.ts` (2) + helpers `tests/proofs/staffing-window-override-survival.runtime.helpers.ts`. Orchestrator re-ran 2/2 on the new file, the related override/staffing regressions 9/9, the full suite 271 files / 1203 tests, typecheck, format:check, and `bun run build` green: "reschedule keeps a planTiming override and moves a following need" + "a second reschedule still leaves the override and moves the follower" — EventStaffNeed.planTiming (followsEventTiming false) 14:00–18:00 UTC on 2026-10-18 survives Event.reschedule to 10-19 and to 10-20, and the following server need moves with the crew window (12:15 → next-day 12:15 each time). Do not invent a Confirmed stage. Do not change `final`. Do not add venue/style completeness gates on lockForSales. Do not re-do EventDish headcount, pack quantity, purchasing buyer qty, or this staffing slice. Next remaining PL-RECONCILIATION work is AC-390 / AC-407 / AC-191. The AC-388 staffing window override class is done — all four named override classes (dish, pack, buyer qty, staffing window) are now proven; AC-388 stays PENDING for the remaining BE-1.3 legs (auto-create/provenance of proposal draft, answers, documents, invoice/closeout). User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

Completed (2026-09-22): the **PL-RECONCILIATION AC-388 purchasing buyer-qty override slice** is proven; the PL-RECONCILIATION box stays unchecked (AC-191 / AC-388 / AC-390 / AC-407 still PENDING). AC-037, AC-205, AC-422, AC-423, and AC-424 stay PASS. No product-code, manifest, or generated-file change. P: `tests/proofs/buyer-qty-override-survival.runtime.test.ts` (2) + helpers `tests/proofs/buyer-qty-override-survival.runtime.helpers.ts`. Orchestrator re-ran 2/2, the related override/pack/weekly-purchasing regressions 7/7, the full suite 270 files / 1201 tests, typecheck, format:check, and `bun run build` green. VendorOrderLine_reviseQuantity(7) survives Event.changeHeadcount 40→60 and 60→80 (buyer line stays 7 / quantityIsManual true / plannedQuantity updates 60 then 80; following auto line scales 40→60→80). Do not invent a Confirmed stage. Do not change `final`. Do not add venue/style completeness gates on lockForSales. Do not re-do EventDish headcount, pack quantity, or this buyer-qty slice. Next remaining AC-388 work is staffing window (`EventStaffNeed.planTiming` with followsEventTiming false — no dedicated override command exists). Next remaining PL-RECONCILIATION work after that, or in parallel next iteration, is AC-390 / AC-407 / AC-191. Findings recorded, not fixed: (a) no operator revoke/follow-again for pack lines — adjustQuantity always sets followsDishServings false; (b) catalog DishContainer revise on a completed event's still-live following pack line could now resize because eligible no longer reads event.stage (changeHeadcount still refuses terminal stages); (c) EventStaffNeed open/claimed rows are still not cancelled on Event.cancel (AC-205 finding); (d) `src/logistics/pack-list.manifest` is ~844 lines (over the 400/500 split rule) — do not split in this slice; (e) root `capsule-backend-end-state-spec.md` and `specs/capsule-backend-end-state-spec.md` differ at the `final`/Ops Final paragraph (specs/ copy is newer: finalizeEvent must not require Ops Final) — user-owned untracked copies, do not edit them here. User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

Completed (2026-09-22): the **PL-RECONCILIATION AC-388 pack-quantity override slice** is proven; the PL-RECONCILIATION box stays unchecked (AC-191 / AC-388 / AC-390 / AC-407 still PENDING). AC-037, AC-205, AC-422, AC-423, and AC-424 stay PASS. Files: `src/logistics/pack-list.manifest` (`PackListItem.syncContainerServings` eligible now uses 1-hop EventDish liveness + pack-list status; `self.packList.event` never loads in generated Convex so following lines never resized), plus `bun run manifest:regen`. P: `tests/proofs/pack-quantity-override-survival.runtime.test.ts` (2) + helpers `tests/proofs/pack-quantity-override-survival.runtime.helpers.ts`. Orchestrator re-ran 2/2, the related override/pack regressions 5/5, the full suite 269 files / 1199 tests, typecheck, format:check, and `bun run build` green. PackListItem.adjustQuantity(7) survives Event.changeHeadcount 40→60 and 60→80 (adjusted line stays 7; following container line scales 4→6→8). Do not invent a Confirmed stage. Do not change `final`. Do not add venue/style completeness gates on lockForSales. Do not re-do EventDish headcount or this pack slice. Next remaining AC-388 work is purchasing buyer qty, then staffing window. Findings recorded, not fixed: (a) no operator revoke/follow-again for pack lines — adjustQuantity always sets followsDishServings false; (b) catalog DishContainer revise on a completed event's still-live following pack line could now resize because eligible no longer reads event.stage (changeHeadcount still refuses terminal stages); (c) EventStaffNeed open/claimed rows are still not cancelled on Event.cancel (AC-205 finding); (d) `src/logistics/pack-list.manifest` is ~844 lines (over the 400/500 split rule) — do not split in this slice. User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

Completed (2026-09-22): the **PL-RECONCILIATION AC-388 EventDish headcount-override slice** is proven; the PL-RECONCILIATION box stays unchecked (AC-191 / AC-388 / AC-390 / AC-407 still PENDING). AC-037, AC-205, AC-422, AC-423, and AC-424 stay PASS. No product-code, manifest, or generated-file change. P: `tests/proofs/override-survival.runtime.test.ts` (2) + helpers `tests/proofs/override-survival.runtime.helpers.ts`. Orchestrator re-ran 2/2, the full suite 268 files / 1197 tests, typecheck, format:check, and `bun run build` green. EventDish.setHeadcountOverride(25) survives Event.changeHeadcount 40→60 (overridden line stays 25; following line scales to 60); setHeadcountOverride(0) then changeHeadcount 60→80 scales both lines to 80. Do not invent a Confirmed stage. Do not change `final`. Do not add venue/style completeness gates on lockForSales. Next remaining AC-388 work is other override classes (pack quantity, purchasing buyer qty, staffing window) — do not re-do EventDish headcount. Next remaining PL-RECONCILIATION work after those classes, or in parallel next iteration, is AC-390 / AC-407 / AC-191. Findings recorded, not fixed: (a) prep-task override already has its own proof (`tests/proofs/prep-task-override-survives-regeneration.runtime.test.ts`) and is not this slice; (b) EventStaffNeed open/claimed rows are still not cancelled on Event.cancel (AC-205 finding). User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

Completed (2026-09-22): the **PL-RECONCILIATION AC-424 failure-isolation slice** is proven for Event.cancel (timing throw vs purchasing stand-down); the PL-RECONCILIATION box stays unchecked (AC-191 / AC-388 / AC-390 / AC-407 still PENDING). AC-037, AC-205, AC-422, and AC-423 stay PASS. Files: `convex/lib/reconciliationIsolation.ts` (`EventReconciliationIsolation` — catch per domain, exception + parent receipts on existing materializationReceipts), `convex/lib/reconciliationReceipt.ts` (optional `fullyReconciled`), `convex/lib/eventTimingOperations.ts` (cancelled parent throws the planTiming active-event message), `convex/lib/operationalEvents.ts` (EventCancelled isolates timing + purchasing). P: `tests/proofs/reconciliation-failure-isolation.runtime.test.ts` (2) + helpers `tests/proofs/reconciliation-failure-isolation.runtime.helpers.ts`. Orchestrator re-ran 2/2, the related receipts/cancel regressions 4/4, the full suite 267 files / 1195 tests, typecheck, format:check, and `bun run build` green. Do not invent a Confirmed stage. Do not change `final`. Do not add venue/style completeness gates on lockForSales. Next remaining PL-RECONCILIATION work is AC-191 / AC-388 / AC-390 / AC-407 (override survival, single-reconciliation, plan-vs-fact, typed-event/outbox). Findings recorded, not fixed: (a) EventTimingConfigured / EventScheduleChanged still run timing then staffing unguarded — a timing throw there still blocks staffing; this slice is Event.cancel only; (b) staffing / prep / inventory reconcilers still do not persist §8.2 receipts; (c) EventStaffNeed open/claimed rows are still not cancelled on Event.cancel (AC-205 finding); (d) logistics/equipment/assignments remain outside the isolator, so a throw there still blocks purchasing. User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

Completed (2026-09-22): the **PL-RECONCILIATION AC-423 receipts slice** is proven for the Event timing reconciler; the PL-RECONCILIATION box stays unchecked (AC-191 / AC-388 / AC-390 / AC-407 / AC-424 still PENDING). AC-037, AC-205, and AC-422 stay PASS. Files: `convex/lib/reconciliationReceipt.ts` (`EventReconciliationReceipt` — family eventReconciliation, stored in existing materializationReceipts), `convex/lib/eventTimingOperations.ts` (persist + same-input early-return), `convex/lib/operationalEvents.ts` (trigger id/type on the two timing calls). P: `tests/proofs/reconciliation-receipts.runtime.test.ts` (2) + helpers `tests/proofs/reconciliation-receipts.runtime.helpers.ts`. Orchestrator re-ran 2/2, typecheck, format:check, and `bun run build` green. Do not invent a Confirmed stage. Do not change `final`. Do not add venue/style completeness gates on lockForSales. Next remaining PL-RECONCILIATION work is AC-424 failure isolation (then AC-191 / AC-388 / AC-390 / AC-407). Findings recorded, not fixed: (a) staffing / prep / purchasing / inventory reconcilers still do not persist §8.2 receipts — this slice is timing only; (b) TIMING_REMOVED / TIMING_AMBIGUOUS unresolved codes are recorded but not exercised by the two proofs; (c) EventStaffNeed open/claimed rows are still not cancelled on Event.cancel (AC-205 finding). User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

Completed (2026-09-22): the **PL-RECONCILIATION AC-422 canonical-change-events slice** is proven; the PL-RECONCILIATION box stays unchecked (AC-191 / AC-388 / AC-390 / AC-407 / AC-423 / AC-424 still PENDING). AC-037 and AC-205 stay PASS. No product-code, manifest, or generated-file change. P: `tests/proofs/canonical-change-events.runtime.test.ts` (4) + helpers `tests/proofs/canonical-change-events.runtime.helpers.ts`. Orchestrator re-ran 4/4. Do not invent a Confirmed stage. Do not change `final`. Do not add venue/style completeness gates on lockForSales. Next remaining PL-RECONCILIATION work is AC-423 receipts (then AC-424 failure isolation). Findings recorded, not fixed: (a) there is no EventDish.substitute command — §8.1 "substituted" is EventDishLineOverride kind replace; (b) EventBooked does not exist — booked-from-proposal still emits EventPlanned + ProposalEventLinked; (c) EventStaffNeed open/claimed rows are still not cancelled on Event.cancel (AC-205 finding). User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

Completed (2026-09-22): the **PL-RECONCILIATION AC-205 cancellation-release slice** is proven; the PL-RECONCILIATION box stays unchecked (AC-191 / AC-388 / AC-390 / AC-407 / AC-422 / AC-423 / AC-424 still PENDING). AC-037 stays PASS. Files: `src/facilities/equipment.manifest` (eventManageAccess on EquipmentReservation policies), `convex/lib/eventCancellation.ts` (`standDownEventEquipmentReservations` + `standDownEventAssignments`), `convex/lib/operationalEvents.ts` (EventCancelled calls them); `bun run manifest:regen` wrote the generated mutations/queries. P: `tests/proofs/cancellation-release.runtime.test.ts` (2). Orchestrator re-ran 2/2, the full suite 264 files / 1187 tests, typecheck, format:check, and `bun run build` green. Do not invent a Confirmed stage. Do not change `final`. Do not add venue/style completeness gates on lockForSales. Next remaining PL-RECONCILIATION work is AC-422 canonical change events (or AC-423 receipts), not another cancel stand-down. Findings recorded, not fixed: (a) EventStaffNeed open/claimed rows are not cancelled on Event.cancel — this slice is assignments + equipment only; (b) event_manager can now execute any EquipmentReservation command, the same widening InventoryReservation already had; (c) AC-081 stock-reservation cancel proof is still a separate proposed test. User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

Completed (2026-09-22): the **PL-PRICING AC-418 / AC-432 projection-agreement slice** is proven; the PL-PRICING box is now checked (AC-089 / AC-213 / AC-263 / AC-264 / AC-265 / AC-406 / AC-418 / AC-432 all PASS). No product-code, manifest, or generated-file change. P: `tests/proofs/proposal-projection-agreement.runtime.test.ts` (2). Orchestrator re-ran 2/2, the full suite 263 files / 1185 tests, typecheck, format:check, and `bun run build` green. Preview 720/40/15/745; send freezes the snapshot; PDF with live bait 9999, share portal, and SignatureRequest on that revision still read 745; accept keeps it; `proposalEventPrefill.values` booking seed is 745; Event.quotedPrice is 745; Event.approve seeds one draft invoice whose total/amountDue is 745. Finding recorded, not fixed: invoice.manifest EventApproved still seeds subtotal=quotedPrice, tax=0, discount=0 (lump-sum of Event.quotedPrice) — composition is not the revision tax/discount split; this slice proves totals. Booking still accepts a caller quotedPrice; this slice proves the UI prefill path, not a lying-caller block. Do not redo LedgerMoney / invoice-tax / AC-089 / AC-213 / AC-406 / AC-264 / AC-265. Do not invent a Confirmed stage. Do not change `final`. User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

Completed (2026-09-22): the **PL-PRICING AC-264 publication snapshot-agreement slice** is proven; the PL-PRICING box stays unchecked (AC-418 / AC-432 still PENDING). No product-code, manifest, or generated-file change. P: `tests/proofs/proposal-central-path-snapshot.runtime.test.ts` (2). Orchestrator re-ran 2/2, the full suite 262 files / 1183 tests, typecheck, format:check, and `bun run build` green. Preview (`computeProposalPricing` on per_person 48×10 + flat 120 + 20% of 600) is 720 / 40 / 15 / 745; `draftProposalWithLines` with provisional 1/1 stores those same totals; send freezes them into the revision snapshot; `projectProposalPdf` with live bait 9999 still reads 720/40/15/745 and recomputing the PDF's pricingLines through the same function matches; `Proposal_accept` + `getSharedProposal` still read that object; a second accepted proposal shown a live total of 1 still reads 720/40/15/745. Do not redo LedgerMoney / invoice-tax / AC-089 / AC-213 / AC-406 / AC-265. Do not invent a Confirmed stage. Do not change `final`. Next remaining PL-PRICING work is AC-418 / AC-432 projection agreement (invoice seed and Event booking). User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

Completed (2026-09-22): the **PL-PRICING AC-265 publish-blocks slice** is proven; the PL-PRICING box stays unchecked (AC-264 / AC-418 / AC-432 still PENDING). No product-code, manifest, or generated-file change. P: `tests/proofs/proposal-publish-blocks.runtime.test.ts` (3). Orchestrator re-ran 3/3, the full suite 261 files / 1181 tests, typecheck, format:check, and `bun run build` green. (a) catalog unitPrice 60 vs sellingPrice 48 with no overrideReason → sendProposalWithRevisionCapture throws, proposal stays draft, zero revisions; (b) same + reason "Client asked for plated upgrade" → sent, snapshot freezes unitPrice 60 / catalogPrice 48 / the reason; (c) addLine quantity -1 throws "Line quantity cannot be negative" and writes zero live lines. Do not redo LedgerMoney / invoice-tax / AC-089 / AC-213 / AC-406. Do not invent a Confirmed stage. Do not change `final`. Next remaining PL-PRICING work is AC-264 publication snapshot agreement, then AC-418 / AC-432 projection agreement. User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

Completed (2026-09-22): the **PL-PRICING AC-406 unit/unknown-mapping slice** is proven; the PL-PRICING box stays unchecked (AC-264 / AC-265 / AC-418 / AC-432 still PENDING). Files: `src/lib/quantityMoney.ts` (`QuantityMoney` — convert refuses without a recorded pack/density mapping, line cost in cents, pack-round 4-tuple, complete coverage requires priced lines), `src/lib/recordedUnitMappings.ts` (live ItemUnitMapping rows → engine shape), `src/features/events/eventMenuLinePricer.ts`, `src/features/kitchen/ComponentCostCalculator.ts` (wrapper + mappings), `src/features/events/eventMenuCost.ts` + Event Menu / Margin tabs, dish plate cost, component detail, menu profitability. P: `tests/quantity-money.test.ts` (3), `tests/recorded-unit-mappings.test.ts` (2), `tests/event-menu-cost.test.ts` mapping case, `tests/proofs/menu-profitability-direct-ingredient.runtime.test.ts` mapping case. Orchestrator re-ran proving 36/36, full suite 260 files / 1178 tests, typecheck, format:check, and `bun run build` green. Do not redo LedgerMoney / invoice-tax / AC-089 / AC-213. Do not invent a commission rate. Do not invent a Confirmed stage. Do not change `final`. Next remaining PL-PRICING work is AC-264 / AC-265 publication snapshot agreement, then AC-418 / AC-432 projection agreement. Findings recorded, not fixed: (a) `QuantityMoney.packRound` is proven but purchasing screens do not yet print required/package/rounded/remainder; `convex/lib/culinaryModel/demand.ts` `applyRounding` still returns only the rounded number; (b) `profitMarginReport.ts` still adds dollars with `+=`; (c) Sales Dashboard still prints a hardcoded "3%" commission sentence; (d) AC-227 stays owner-gated — GitHub issue https://github.com/Angriff36/capsule/issues/397. User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

Completed (2026-09-22): the **PL-PRICING AC-213 commission / food-cost slice** is proven; the PL-PRICING box stays unchecked (AC-264 / AC-265 / AC-406 / AC-418 / AC-432 still PENDING). Files: `src/lib/commercialMoney.ts` (new `CommercialMoney` — percent-of, allocate, food-cost ratio, cent sums), `src/features/finance/revenueAttributionValues.ts` (`allocateRevenueShare`), `src/features/finance/RevenueAttributionDetailPage.tsx` (apply preview uses the utility), `src/features/reports/compMasterValues.ts` (applied commission sums in cents), `src/features/finance/foodCostPercentage.ts` (period/total sums + ratio in cents), `src/features/events/eventMenuCost.ts` (dish rollup sum only). P: `tests/finance-precision-reconciliation.test.ts` (11). Orchestrator re-ran 11/11, proving set 45/45, full suite 258 files / 1171 tests, typecheck, format:check, and `bun run build` green. Do not redo LedgerMoney / invoice-tax / AC-089 allocation. Do not invent a commission rate. Do not invent a Confirmed stage. Do not change `final`. Next remaining PL-PRICING work is AC-406 unit/unknown mapping, then AC-264 / AC-265 / AC-418 / AC-432 projection agreement. Findings recorded, not fixed: (a) `profitMarginReport.ts` still adds food/labor/equipment dollars with `+=` (468 lines — do not split unless that is the task); (b) Sales Dashboard still prints a hardcoded "3%" commission sentence — do not invent a real rate; (c) `RevenueAttributionDetailPage.tsx` is 626 lines (over the 500 cap; was 624); (d) per-line recipe `converted * waste * costPerUnit` stays AC-406; (e) AC-227 stays owner-gated — GitHub issue https://github.com/Angriff36/capsule/issues/397. User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

Completed (2026-09-22): the **PL-PRICING AC-089 allocation/aging/statement/reporting slice** is proven; the PL-PRICING box stays unchecked (AC-213 / AC-264 / AC-265 / AC-406 / AC-418 / AC-432 still PENDING). Files: `src/lib/invoiceMoneyLedger.ts` (new `InvoiceMoneyLedger` — statement due, payment waterfall, cent sums, dashboard aging buckets), `src/features/finance/invoiceBilling.ts` (billed/collected/draft rollup in cents), `src/features/home/DashboardWidgetPolicy.ts` (aging + overdue + 30-day receivables via the ledger; file 447→443), `src/features/reports/liveReportBuilders.ts` (finance KPIs only). P: `tests/finance-precision-reconciliation.test.ts` (8). Orchestrator re-ran 8/8, proving set 39/39, full suite 258 files / 1168 tests, typecheck, format:check, and `bun run build` green. Do not redo the five-bases / LedgerMoney / invoice-tax slice. Do not invent a Confirmed stage. Do not change `final`. Next remaining PL-PRICING work is AC-213 commissions/food-cost utilities, then AC-406 unit/unknown mapping, then AC-418 / AC-432 projection agreement. Findings recorded, not fixed: (a) dashboard overdue metric (status overdue OR past dueDate) is not the same number as aging.overdue (non-current buckets) — preserved; (b) `liveReportBuilders.ts` remains ~753 lines (over the 500 cap) — do not split unless that is the task; (c) AC-227 stays owner-gated — GitHub issue https://github.com/Angriff36/capsule/issues/397. User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

Completed (2026-09-22): the **PL-PRICING AC-263 five-bases + integer-cent slice** is proven; the PL-PRICING box stays unchecked (AC-089 / AC-213 / AC-406 / AC-418 / AC-432 still PENDING). Files: `src/lib/ledgerMoney.ts` (new `LedgerMoney` class — integer cents), `src/lib/pricing.ts` (central path sums in cents; EPSILON `round2` removed), `src/features/finance/invoiceTax.ts` (`roundMoney` + line/tax totals via LedgerMoney; unitPrice.times(quantity)), `tests/finance-precision-reconciliation.test.ts` (4). No generated-file change, no Confirmed stage, no `final` change, no lockForSales completeness gates. P: 4/4 in the proving file; orchestrator re-ran the full suite 258 files / 1164 tests, typecheck, format:check, and `bun run build` green. Next remaining PL-PRICING work is AC-089 allocation/aging/statement/reporting legs and AC-213 commissions/food-cost utilities. AC-227 stays owner-gated — GitHub issue https://github.com/Angriff36/capsule/issues/397. User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

Completed (2026-09-22): the **PL-COMMAND-REPLAY AC-404 reaction-replay slice** is proven, and the PL-COMMAND-REPLAY box is now checked. Invoice.issue is now replay-safe (same event keeps number/money/issuedAt; autoNumbered false on replay so numbering does not remint). PackList.open and ProductionBatch.plan were already replay-safe. P: `tests/proofs/reaction-replay-identity.runtime.test.ts` (3) + helpers `tests/proofs/reaction-replay-identity.runtime.helpers.ts`; identity formulas remain `tests/workflow-idempotency-identity.test.ts` (6). Orchestrator re-ran the new proof 3/3, typecheck green. Finding recorded, not fixed: generated PurchaseNeed_create is insert-only and schema has no unique [tenantId, ingredientDemandId] index — EventApproved still converges because markReleased clears purchaseEligibleEventId; GitHub issue [#396](https://github.com/Angriff36/capsule/issues/396). Do not rebuild demand/purchasing/webhook/import ad-hoc keys. Do not invent a Confirmed stage. Do not change `final`. User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813. Next remaining P0 after this box is no longer PL-COMMAND-REPLAY.

Completed (2026-09-22): the **PL-COMMAND-REPLAY AC-404 identity-formula slice** is proven; the PL-COMMAND-REPLAY box STAYS unchecked (AC-404 stays PENDING). Files: `src/lib/workflowIdempotencyIdentity.ts`, `tests/workflow-idempotency-identity.test.ts` (6). No generated-file change, no second command API, no Confirmed stage, no `final` change. P: 6/6 in the identity test file. Next remaining PL-COMMAND-REPLAY work is the AC-404 runtime reaction-replay proof (proposed `tests/proofs/reaction-replay-identity.runtime.test.ts`). Finding recorded, not fixed: demand/purchasing/webhooks/imports still use their old ad-hoc keys; this slice does not rewrite them. User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

Completed (2026-09-22): the **PL-COMMAND-REPLAY AC-397 dispatcher-key slice** is proven; the PL-COMMAND-REPLAY box STAYS unchecked (AC-404 still PENDING). Files: `src/agent/CommandApiIdempotencyGate.ts`, `src/agent/CapsuleApiKeyGateway.ts`, `tests/agent/command-api-idempotency-gate.test.ts`, `tests/agent/capsule-api-key-gateway.test.ts`. No generated-file change, no second command API, no Confirmed stage, no `final` change. P: 15/15 in the two agent test files; orchestrator re-ran them. AC-397 is newly PASS. Next remaining PL-COMMAND-REPLAY work is AC-404. Finding recorded, not fixed: generated `convex/http.ts` still treats idempotencyKey as optional for JWT-direct callers (cannot hand-edit). PL-LIFECYCLE remaining AC-227 stays owner-gated (do not add venue/style completeness on lockForSales); AC-055 still PENDING. User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

Completed (2026-09-22): the **PL-LIFECYCLE AC-612 owner final-meaning slice** is proven; the PL-LIFECYCLE box stays unchecked (AC-227 and AC-055 still PENDING). No event.manifest change, no Confirmed stage, no finalize gate, no generated-file change. `EventFinalMeaning` records §4.2: `final` = service finished; Ops Final = packet readiness. P: `tests/proofs/event-final-readiness.runtime.test.ts` (3) + `tests/features/events/event-final-meaning.test.ts` (3). Orchestrator re-ran 6/6, the full suite 254 files / 1144 tests, typecheck, increment Prettier, and `bun run build` green. Do not invent a Confirmed stage. Do not change `final`. Do not add venue/style completeness gates on lockForSales (AC-227 stays an owner-gated decision; readiness is the projection). Next remaining PL-LIFECYCLE work is AC-227 / AC-055. User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

Completed (2026-09-22): the **PL-VENUE-NOTES AC-317 client-facing proof** is proven; the PL-VENUE-NOTES box is NOW CHECKED (AC-316 and AC-317 both PASS). No product-code, manifest, or generated-file change. P: `tests/proofs/venue-note-visibility.runtime.test.ts` (2) + helpers `tests/proofs/venue-note-visibility.runtime.helpers.ts`; postNote visibility union in `tests/proofs/venue-notes.runtime.helpers.ts` widened to management_only. Orchestrator re-ran 2/2, the full suite 252 files / 1138 tests, typecheck, increment Prettier, and `bun run build` green. Do not invent a Confirmed stage. Do not change `final`. Next remaining P0 after this box: PL-LIFECYCLE AC-612 (owner final meaning) plus AC-227 / AC-055. Findings recorded, not fixed: keep the existing (a)(b) from the AC-316 note (createViaPost visibility default; VenueNotesPanel revise/eventId); (d) `getSharedProposal` already omits VenueNote records and venue snapshot accessNotes/cateringNotes/logisticsNotes — this proof asserts unique VenueNote markers never leak, it does not add a new filter. User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813.

Completed (2026-09-22): the **PL-VENUE-NOTES AC-316 post/pin/revise/remove proof** is proven; the PL-VENUE-NOTES box stays unchecked (AC-317 still PENDING — client-facing proposal/share absence is unproven; the existing `tests/proofs/venue-note-visibility.runtime.test.ts` only covers staff/manager/tenant reads). No product-code or manifest change. P: `tests/proofs/venue-notes.runtime.test.ts` (3) + helpers `tests/proofs/venue-notes.runtime.helpers.ts`. Orchestrator re-ran 3/3, the full suite 252 files / 1137 tests, typecheck, increment Prettier, and `bun run build` green. Do not invent a Confirmed stage. Do not change `final`. Next remaining PL-VENUE-NOTES work is AC-317 client-facing projections. Findings recorded, not fixed: (a) VenueNote_createViaPost does not apply the stored visibility default — callers must pass visibility; (b) VenueNotesPanel imports revise but has no revise control and does not send eventId; (c) PL-LIFECYCLE remaining AC-612 (owner final meaning) and AC-227 (owner-required completeness gate set — venue/style still optional; readiness is a projection; do not invent gates) plus AC-055 (standing no-weaken-guards policy, not a slice). User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813 (orchestrator will verify).

Completed (2026-09-22): the **PL-LIFECYCLE AC-099 / AC-223 outcome-map slice** is proven; the PL-LIFECYCLE box stays unchecked. Files: `src/features/events/eventLifecycleOutcomeMap.ts`, `tests/features/events/event-lifecycle-outcome-map.test.ts` (3), `tests/proofs/event-lifecycle-map.runtime.test.ts` (3). No event.manifest change, no Confirmed stage, no `final` meaning change, `confirmSalesLock` untouched. The orchestrator re-ran the two proving files 6/6 green, the full suite 251 files / 1134 tests green, typecheck green, format:check green, and `bun run build` green. AC-099 and AC-223 are newly PASS. Remaining PL-LIFECYCLE work: AC-612 (owner final meaning) plus AC-055 / AC-227. Findings recorded, not fixed: (a) `confirmSalesLock` still moves sales_lock → executing as a sales-role alias of beginExecution, and the EventLifecyclePolicy label is still "Confirm sales lock & start execution"; (b) signatureRequests have no eventId, so the duplicate signature count is vacuously zero — invoice/payment counts are the real copy proof; (c) no public command creates a quote-stage Event (enum + TPP map only). User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813 (orchestrator will verify).

Completed (2026-09-22): the **PL-LIFECYCLE AC-228 completion-correction slice** is proven; the PL-LIFECYCLE box stays unchecked. `Event.correctCommercial(reason, budgetAmount, quotedPrice)` was added in `src/operations/event.manifest` after `changePricing`. It is allowed only at completed and closed_out, requires a non-empty reason, and uses the same roles as changePricing (eventManageAccess or salesManageAccess). It emits one EventCommercialCorrected ledger row. Event.changePricing guards were NOT changed (still planning | pending_approval | approved only), and the Event default write/execute policies were NOT widened (no finance on the Event front door). No UI this slice. No EventCloseout reopen (AC-626 stays PENDING). No Confirmed stage. No `final` meaning change. Proof: `tests/proofs/lifecycle-completion-correction.runtime.test.ts` (3/3); the orchestrator re-ran it green, plus the full suite 249 files / 1128 tests, typecheck, increment Prettier, and `bun run build` green. AC-228 is newly PASS. Remaining PL-LIFECYCLE work: AC-099 / AC-223 / AC-612 (owner Confirmed/final) plus AC-055 / AC-227. User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813 (orchestrator will verify).

Completed (2026-09-22): the **PL-LIFECYCLE AC-235 linked-contact proof** is proven; the PL-LIFECYCLE box stays unchecked. Quote conversion now creates/reuses one ClientContact and passes `clientContactId` to `Lead_stageConversion`; Event still stores `primaryContactName` as a snapshot (no `Event.primaryContactId` invented). Proof is the existing convert test in `tests/proofs/quote-conversion.runtime.test.ts` (10/10) — it now also asserts Event.clientId, Lead.clientId, Lead.clientContactId, Proposal.clientId, and one live ClientContact belonging to that client. Orchestrator re-ran that file green. AC-235 is newly PASS. Do not invent a Confirmed stage. Do not change `final`. Next remaining PL-LIFECYCLE work is AC-099 / AC-223 / AC-612 (owner Confirmed/final) plus AC-055 / AC-227 / AC-228. Findings recorded, not fixed: (a) Event still has no `primaryContactId` — contact identity lives on Lead.clientContactId + Client; `Event.primaryContactName` remains the required snapshot. (b) `convex/quoteBuilder.ts` is already over 500 lines (~900 after this slice) — do not split unless that is the task. (c) Reused contacts are any non-deleted contact on the client, including status=removed. (d) AC-228 / AC-626 still share the missing EventCloseout correction/reopen path — do not invent it here. User-owned dirty files stay unstaged.

Completed (2026-09-22): the **PL-LIFECYCLE AC-234 TPP-fields persistence proof** is proven; the PL-LIFECYCLE box stays unchecked. `tests/proofs/lifecycle-event-tpp-fields.runtime.test.ts` (2 tests: "stores occasion, style, venue, salesperson, and referral as linked ids" / "books without salesperson or referral when those catalogs are empty") proves a booked Event stores occasion, service style, venue, salesperson, and referral source as linked ids, and that booking still succeeds when the salesperson/referral catalogs are empty. The mapper case `tests/features/events/event-plan-engagement-form-mapper.test.ts` › "stamps the referral source id" proves the create form feeds the referral source id to the booking command. The create form already had all seven mapped-field selectors — no product-code change. Orchestrator re-ran 2 files / 9 tests green. AC-234 is newly PASS. AC-239 stays PASS. Do not invent a Confirmed stage. Do not change `final`. Next remaining PL-LIFECYCLE work is AC-099 / AC-223 / AC-612 (owner Confirmed/final) plus AC-055 / AC-227 / AC-228 / AC-235. User-owned dirty files stay unstaged.

Completed (2026-09-22): the **PL-LIFECYCLE AC-239 duplicate slice is proven**; the PL-LIFECYCLE box stays unchecked. Authored `convex/lib/eventDuplicate.ts` `duplicateEvent` orchestrates `Event_createViaPlanEngagement` + `EventDish_createViaAddToEvent` (same pattern as `proposalEventCreation.ts`). The copy is a new planning Event titled "{title} (copy)" with the source client, dates, type, guest count, money seed, venue/contact/style/occasion/owner snapshots, and live menu lines as NEW rows. Invoices, payments, signatures, reservations, prep, archive flags, and stage timestamps are not copied. Kitchen and cross-tenant ids are refused ("Event not found" for foreign). UI: More menu "Duplicate event" (`data-testid="event-duplicate"`) opens the copy. Helpers: `EventDuplicatePlanning`, `EventDuplicateMenuItem`, `useEventDuplicate`. P: `tests/proofs/lifecycle-event-duplicate.runtime.test.ts` (3) + `tests/features/events/event-duplicate-planning.test.ts` (6). Orchestrator re-ran 9/9, full suite 247 files / 1122 tests, typecheck, format:check, and `bun run build` green. AC-239 is now PASS (create, edit, archive/reactivate, and duplicate paths exist). Do not invent a Confirmed stage. Do not change `final`. Next remaining PL-LIFECYCLE work is AC-099 / AC-223 / AC-612 (owner Confirmed/final) plus AC-055 / AC-227 / AC-228 / AC-235. Finding recorded, not a new task: AC-099's "duplicate does not copy payments/signatures/completed work" invoice leg is now proven here; the rest of AC-099 (canonical lifecycle map) is still PENDING. User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813 (orchestrator will verify).

Completed (2026-09-22): the **PL-LIFECYCLE AC-239 first slice (archive / reactivate) is proven**; the PL-LIFECYCLE box stays unchecked. Event stores `archivedAt` + `archiveReason` (a visibility flag, not a stage). `Event.archive(reason)` / `Event.reactivate()` require eventManageAccess; kitchen cannot archive, an empty reason is refused, and a second archive is refused. Stage, quotedPrice, budgetAmount, expectedHeadcount, and deletedAt stay put. P: `tests/proofs/lifecycle-event-archive.runtime.test.ts` (3 tests: "archives a planning event without changing stage or money", "reactivates an archived event and keeps the same stage", "refuses a second archive, a kitchen archive, and reactivate of a live event"). UI: the Events list hides archived by default with a "Show archived" toggle (`data-testid="events-show-archived"`); the event brief More menu has "Archive event" (reason) and "Bring back". Helpers: `EventArchiveVisibility`, `EventArchiveMenuItems`. Unit: `tests/features/events/event-archive-visibility.test.ts` (4). AC-239 stays PENDING — duplicate path is still missing. Do not invent a Confirmed stage. Do not change `final`. Next remaining PL-LIFECYCLE work is Event.duplicate (still AC-239) plus AC-099 / AC-223 / AC-612 (owner Confirmed/final) and AC-055 / AC-227 / AC-228 / AC-234 / AC-235. User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813 (orchestrator will verify). The AC-239 archive/reactivate slice is done via tests/proofs/lifecycle-event-archive.runtime.test.ts; AC-239 stays PENDING until duplicate exists.

Completed (2026-09-22): the **PL-BOOKING AC-410 venue-identity slice** is proven, and the PL-BOOKING box is now checked. `ProposalBookingVenue` in `convex/lib/proposalBookingVenue.ts` decides unique / ambiguous / none / picked. `createEventFromAcceptedProposal` now attaches a unique saved venue when the caller omits venueId; two same-named venues throw "Several saved venues share this name. Pick one before booking." and create zero Events; an operator-picked id wins; no match books with the name only. P: `tests/proofs/booking-identity.runtime.test.ts` (4 tests: "links the unique saved venue when booking omits venueId", "refuses to pick when two saved venues share the proposal name", "books the venue the operator picked when two saved venues share the name", "seeds venue name without a venueId when no saved venue matches"). AC-410 is now PASS (replay + menu + date/client/guest already existed; this slice closed venue identity on the canonical command). The **PL-BOOKING box is now checked** because every assigned AC is PASS (AC-001..006, AC-100, AC-398, AC-410, AC-411..415, AC-433..437). Do not invent a Confirmed stage. Do not change `final`. Next P0 work is no longer PL-BOOKING; leave other unchecked boxes alone (PL-LIFECYCLE remaining AC-099 / AC-223 / AC-612, etc.). User-owned dirty files stay unstaged. Preview http://127.0.0.1:7813 (orchestrator will verify).

Completed (2026-09-22): the **PL-BOOKING AC-436 concurrent race slice** is proven; the full PL-BOOKING box STAYS unchecked (AC-410 still PENDING). `tests/proofs/booking-race.runtime.test.ts` › "two concurrent createEventFromAcceptedProposal calls create exactly one event" — overlapping Promise.allSettled owner.mutation calls land exactly one live Event, one ProposalAccepted, and one proposalRevisions row — plus the accept and revision tests in the same file cover the remaining retry legs (no duplicate acceptance or revision). Green 2026-09-22: orchestrator re-ran the proof 3/3, the full suite 242 files / 1102 tests, typecheck, and `bun run build`. No product-code, manifest, or generated-file change — the existing already-linked replay plus transaction ordering already covered the race. Do not invent a Confirmed stage. Do not change `final`. Next remaining PL-BOOKING work is AC-410 (broader booking-identity beyond replay + unique/ambiguous venue).

Completed (2026-09-22): the **PL-BOOKING AC-006 carry-over preview** is proven; the full PL-BOOKING box STAYS unchecked (AC-410, AC-436 still PENDING). Create-event now lists the eight proposal values (title, type, date, times, headcount, venue, menu count, enhancement count) and the linking reason before commit. Helper: `ProposalEventPrefill.carryoverPreview`. UI: `ProposalEventCarryoverPreview` in the Proposal aside (`data-testid="proposal-carryover-preview"`). EventCreatePage 1199 → 1152 (dump replaced, CTAs and venue hints kept). P: `tests/features/events/proposal-event-prefill.test.ts` › "preview lists carried values" + `tests/event-create-behavior.test.ts` › "lists which proposal values will carry over before create" (helpers extracted to `tests/event-create-behavior.helpers.ts` so the mounted file stays under 500). J: [ac006-carryover-preview-evidence-2026-09-22.md](codex-plans/whole-spec-audit-2026-09-20/ac006-carryover-preview-evidence-2026-09-22.md) (orchestrator cursor-grok-4.6-high-fast; UX-01 PASS; ANTHROPIC_API_KEY unset so llm-review.ts was not called). Do not invent a Confirmed stage. Do not change `final`. Next remaining PL-BOOKING work is AC-410 (broader booking-identity) and AC-436 (true concurrent retry/race).

Completed (2026-09-22): the **PL-BOOKING AC-415 / AC-437 no-premature-effects slice** is proven; the full PL-BOOKING box STAYS unchecked (AC-006, AC-410, AC-436 still PENDING). `tests/proofs/no-premature-effects.runtime.test.ts` (3 tests) proves `Proposal_createViaDraft` + `Proposal_send` + `Proposal_markViewed` leave zero live invoices, messages, staffMessages, clientCommunications, vendorOrders, inventoryReservations, equipmentReservations, productionBatches, prepTasks, deliveries, weeklyScheduleNotices, purchaseNeeds, and externalRecordLinks; `Proposal_accept` + `createEventFromAcceptedProposal` still leave those tables at zero (the one allowed write is the copied EventDish); the control then `Event_submitForApproval` + `Event_approve` seeds exactly one draft unsent invoice, so the zeros are not a blind counter. Orchestrator re-ran the proof 3/3 green, the full suite 241 files / 1097 tests green, typecheck green, format:check green, and `bun run build` green. No product-code, manifest, or generated-file change. AC-415 and AC-437 are newly PASS. Do not invent a Confirmed stage. Do not change `final`. Next remaining PL-BOOKING work is AC-006 (create-screen carry-over preview + J review), AC-410 (broader booking-identity beyond replay + unique/ambiguous venue), and AC-436 (true concurrent retry/race — sequential replay already exists). Finding recorded, not a new contradiction: the complete-feature spec still names Quote → Sales Lock → Confirmed; the backend spec's ten stages remain the authority.

Completed (2026-09-22): the **PL-BOOKING AC-100 field-staff booking-read slice** is proven; the full PL-BOOKING box STAYS unchecked (AC-006, AC-398, AC-410, AC-415, AC-436, AC-437 still PENDING). `tests/proofs/field-staff-booking-read.runtime.test.ts` (2 tests) proves event_staff reads guest count, style, venue, menu, timeline, and enhancement names, and is denied proposal totals: getProposal returns null (no throw) and listProposalByEventId returns [] for event_staff; getEventBookingDetails redacts enhancement prices (price null, canOpenProposal false) for event_staff while sales_staff still sees total 1300 and Valet parking 400; cross-tenant getEventBookingDetails returns null. Orchestrator re-ran the proof 2/2 green, the full suite 240 files / 1094 tests green, typecheck green, and `bun run format:check` green. No product-code, manifest, or generated-file change. Finding recorded on PL-BOOKING, not fixed: Event.quotedPrice remains on the staff-readable Event document via getEvent (that is the AC-398 commercial seed) — this slice proves proposal totals are denied; it does not hide Event.quotedPrice. Do not mark a new redaction task as done from this slice. Do not invent a Confirmed stage. Do not change `final`.

Completed (2026-09-22): the **PL-BOOKING AC-398 live readiness-display slice** is proven; the full PL-BOOKING box STAYS unchecked (AC-006, AC-100, AC-410, AC-415, AC-436, AC-437 still PENDING). `src/features/events/EventReadinessCard.tsx` sits at the top of the Event overview rail (above Setup readiness) and calls the hook `src/lib/useEventReadiness.ts`, which queries `api.eventReadiness.getEventReadiness` — Event features cannot import convex/react, so the card uses this lib hook; `src/features/events/eventReadinessSummary.ts` formats the live projection only — no Event field. Unit: `tests/features/events/event-readiness-summary.test.ts` (5). Mounted: `tests/event-overview-behavior.test.ts` › "prints the live nine-domain readiness summary, not a frozen Event readiness name". Orchestrator re-ran the proving files 2 files / 15 tests green, typecheck green, increment Prettier green. No Event readiness field, no Confirmed stage, no `final` meaning change, no resolve/override buttons; EventSetupProgress (4 boolean flags) stays. AC-398 newly PASS — BE-4.1 is fully proven: venue, service style, occasion, client, commercial seed, menu dish, owner, timing, source-evidence, and live readiness display. Next remaining PL-BOOKING work is AC-006 / AC-100 / AC-410 / AC-415 / AC-436 / AC-437 (J review, booking-identity, races) — NOT another snapshot.

Completed (2026-09-22): the **PL-READINESS AC-401 first slice** is proven; the PL-READINESS box is now checked. Authored query `convex/eventReadiness.ts` `getEventReadiness` plus pure projector `convex/lib/eventReadinessProjection.ts` expose the nine §4.3 domains on every read (commercial, planning, kitchen, purchasing, staffing, packing, packet, execution, closeout). Each issue carries code, affectedIds, severity, reason, resolvingAction. Optional unknowns are info (venue, staffing). A missing quoted price is a warning, quotedPrice 0 is a real seed. Execution issues reuse the same in-flight prep/pack/delivery facts as `isReadyForExecution` but NEVER emit blocking and do not gate beginExecution. Closeout issues appear only after completed/closed_out or when a draft closeout exists. No Event field, no manifest change, no command guard, no Confirmed stage, no `final` meaning change. Runtime: `tests/proofs/readiness-projections.runtime.test.ts` (3). Unit: `tests/features/events/event-readiness.test.ts` (7). AC-401 newly PASS. AC-398 stays PENDING — Event overview still shows the boolean EventSetupProgress card, not the nine-domain live summary. Finding recorded, not fixed: no UI this slice (honesty spec — do not add a Ready badge or resolve button until the card calls a real command). Next remaining PL-BOOKING AC-398 work is printing the live readiness summary on the Event overview. Next remaining PL-LIFECYCLE work is still AC-099 / AC-223 / AC-612 (owner Confirmed/final) plus AC-055 / AC-227 / AC-228 / AC-234 / AC-235 / AC-239.

This file now serves two roles. The audit below it was plan only: it audited shared `dev` at `be4288fb`, beginning 2026-09-20, against all 22 files under `specs/` and all 115 files under `src/lib/`, with no application or test implementation. Since 2026-09-21 the plan also tracks real implementation: the first PL-BUILD increment (#380 root-count repair) is done and verified on Windows, with evidence in [build-gate-evidence-2026-09-21.md](codex-plans/whole-spec-audit-2026-09-20/build-gate-evidence-2026-09-21.md). Source hashes, stable criterion mappings, current code evidence and shared-library reuse are recorded in [source-coverage.json](codex-plans/whole-spec-audit-2026-09-20/source-coverage.json), [shared-library.md](codex-plans/whole-spec-audit-2026-09-20/shared-library.md) and [review-decisions.md](codex-plans/whole-spec-audit-2026-09-20/review-decisions.md).

Completed (2026-09-22): the **PL-BOOKING AC-398 source-evidence link slice** is proven; the full PL-BOOKING box stays unchecked. No new Event fields, no manifest or generated-file change, no `changeSource` command, and `importSourceKey` is NOT added to `planEngagement` — the link is the existing stored `Event.importSourceKey`, written by `Event_createViaCaptureDraft` / `Event_updateImportDraft` (snapshot, not freeze: an explicit `Event_updateImportDraft` with only `{ docId, importSourceKey }` rewrites the key while every omitted field preserves). Runtime proof: `tests/proofs/lifecycle-source-evidence-link-stability.runtime.test.ts` (2 tests) — a catalog `ServiceStyle_reviseDetails` rename ("Full Service" → "Full Service RENAMED", then "…RENAMED 2") leaves the stored key `beo-worksheet-v1` (then `beo-worksheet-v2`), quotedPrice 4500 and stage planning untouched, with `serviceStyleId` still the live catalog link. Display: `src/features/events/eventSourceEvidenceLink.ts` `eventSourceEvidenceKey` — a trimmed non-empty stored key wins and a missing/blank stored key stays missing (null); it NEVER returns a later live source file name as the link; the Event overview import panel prints it as `Source link: …` (`data-testid="event-source-evidence-key"`), with the file names still shown in the Source files list. Unit proof: `tests/features/events/event-source-evidence-link.test.ts` (6). UI proof: `tests/event-overview-behavior.test.ts` › "prints the booked source-evidence key on the import draft, not a later live file name". The proving command ran 3 files / 17 tests green. AC-398 stays PENDING — the remaining BE-4.1 item on this task is readiness (AC-401, live projections; readiness stays live, not a frozen name). Do not invent a Confirmed stage. Do not change `final`. Do not snapshot readiness as a frozen name.

Completed (2026-09-22): the **PL-BOOKING AC-398 timing-input snapshot slice** is proven; the full PL-BOOKING box stays unchecked. No new Event fields — the snapshot is the existing stored timing inputs `serviceStartsAt`, `timingSetupMinutes`, `timingLoadMinutes`, `timingOutboundTravelMinutes`, `timingCleanupMinutes`, `timingReturnTravelMinutes`, `timingUnloadMinutes`, `timingConfiguredAt`; the writer is the existing `Event.configureTiming` (omitted values still clear stored minutes — pass every minute when rewriting). `timingSuggestedSetupMinutes` stays a live suggestion from `ServiceStyle.name`; it is not the recorded snapshot. Runtime proof: `tests/proofs/lifecycle-timing-snapshot-stability.runtime.test.ts` (2 tests) — a catalog `ServiceStyle_reviseDetails` rename ("Full Service" → "Full Service RENAMED") leaves the stored 180/60/45/60/40/30 minutes and `serviceStartsAt` untouched (Event quotedPrice 4500 / stage planning); a second `Event_configureTiming` writes setup 90 (other minutes restated) and a later rename ("Full Service RENAMED 2") leaves that 90. Display: `src/features/events/eventTimingInputs.ts` `eventTimingStoredMinutes` — a finite stored value including 0 wins, and it never returns the live suggestion as the recorded value; the planner `EventTimingPlanner.tsx` uses the helper in draft fill, with draft-building in `EventTimingPlannerDraft.ts` because the page hit 404 lines. Unit proof: `tests/features/events/event-timing-inputs.test.ts` (6). Orchestrator re-ran the proving files 2 files / 8 tests green and typecheck green. AC-398 stays PENDING — the remaining BE-4.1 item on this task is current readiness summaries (live projections, PL-READINESS / AC-401 — readiness stays live, not a frozen name), and the source-evidence link is still unproven. Do not invent a Confirmed stage. Do not change `final`. Do not snapshot readiness as a frozen name. Finding recorded, not fixed: `git commit` is still blocked by Cursor issue [#394](https://github.com/Angriff36/capsule/issues/394) (PowerShell wrapper eval'd in bash; the project `.githooks/pre-commit` never runs), so this increment and the owner-name snapshot files remain uncommitted. Do not use `--no-verify`.

Completed (2026-09-22): the **PL-BOOKING AC-398 owner-name snapshot slice** is proven; the full PL-BOOKING box stays unchecked (remaining snapshots are timing/readiness). Event now stores `ownerName` (property immediately after `assignedToId` in `src/operations/event.manifest`); `planEngagement` accepts and stamps it (`mutate ownerName = ownerName` after `mutate assignedToId`); `assignOwner` accepts optional `ownerName` — clearing the owner id clears the snapshot, reassigning without a name preserves the old snapshot, and passing a name writes it; `bun run manifest:regen` wrote the generated schema/mutations. `captureDraft` / `updateImportDraft` stay unwired for `ownerName`, and there is no `changeOwner` / `changeOwnerName` command — do not invent one. Runtime proof: `tests/proofs/lifecycle-owner-snapshot-stability.runtime.test.ts` (2 tests) — a real `Person_correctIdentity` rename ("Pat Owner" → "Owner RENAMED") leaves Event.ownerName and the live `assignedToId` link untouched (Event quotedPrice 4500 / stage planning); two events share one snapshot under the rename; an explicit reassignment stamps "Kim Lead"; a later Kim rename leaves it; clearing the owner clears the snapshot. Display: `eventOwnerLabel` is snapshot-first (then live printed name for legacy events, then loading/unavailable, then "No owner assigned to this event."); the overview rail prints it in the Assigned-owner card (`data-testid="event-assigned-owner-name"`), keeping the role line only when the live person row exists; the create-form mapper stamps the chosen salesperson's printed name; the tracker owner picker stamps the name on assign. Proof: `tests/features/events/event-owner-label.test.ts` (6), the mapper case in `tests/features/events/event-plan-engagement-form-mapper.test.ts`, and `tests/event-overview-behavior.test.ts` › "prints the booked owner snapshot in the assigned-owner card, not a later catalog rename"; the proving command ran 22/22 green plus typecheck green. AC-398 stays PENDING — venue + style + occasion + client + commercial-seed + menu + owner slices exist; the timing/readiness snapshots are still unproven. Do not invent a Confirmed stage. Do not change `final`. Orchestrator re-ran the proving files 4 files / 22 tests green, the full suite 232 files / 1059 tests, typecheck, increment Prettier, and bun run build green. AC-398 stays PENDING — remaining BE-4.1 snapshots are timing inputs and current readiness projections (readiness is live, not a frozen name). Next remaining PL-BOOKING AC-398 slice is timing-inputs stability (stored Event.timing* minutes must stay when ServiceStyle is later renamed; configureTiming still writes). Do not invent a Confirmed stage. Do not change final. Do not snapshot readiness as a frozen name.

Completed (2026-09-22): the **PL-BOOKING AC-398 menu dish-name snapshot slice** is proven; the full PL-BOOKING box stays unchecked. EventDish now stores `dishName` (property immediately after `dishId` in `src/culinary/event-dish.manifest`); `addToEvent` accepts and stamps it (`mutate dishName = dishName` after `mutate dishId`); `confirmFromProposal` accepts an optional `dishName` and preserves an existing snapshot when the cascade omits it (`dishName != null ? dishName : self.dishName`); `bun run manifest:regen` wrote the generated schema/mutations. The `ProposalAccepted` / `ProposalEventLinked` cascades could NOT hydrate `self.dish.name` (regen compile error), so their params stay unchanged — the UI add path stamps the snapshot. Runtime proof: `tests/proofs/lifecycle-menu-snapshot-stability.runtime.test.ts` (2 tests) — a catalog `Dish_reviseDetails` rename ("Garden salad" → "Garden salad RENAMED") leaves EventDish.dishName and the live `dishId` link untouched with Event quotedPrice 4500 / stage planning; a second event's line and an explicit `"House salad"` add each keep their snapshots under further renames. Display: `eventDishLabel` is snapshot-first (then live name for legacy lines, then loading/unavailable, then "Unknown dish"); the Event menu tab passes `dishName` at picker add time and prints `eventDishLabel` for the line title (`data-testid="event-menu-dish-name"`), sidebar notes, kitchen-note dialog, headcount hints, sell rollup and line-override labels — catalog link, allergens and description stay live facts. Proof: `tests/features/events/event-dish-label.test.ts` (6) and `tests/event-menu-behavior.test.ts` › "prints the booked dish snapshot on the menu, not a later catalog rename". Orchestrator re-ran the proving files 3/17 green, the full suite 230 files / 1049 tests, typecheck, increment Prettier, and `bun run build` green. AC-398 stays PENDING — venue + style + occasion + client + commercial-seed + menu slices exist; the timing/readiness snapshots are still unproven. Finding recorded, not fixed: PR03-07 also wants a pinned recipe-version pointer on the Event line (not a copy of ingredient rows); this slice is the printed name only. Do not invent a Confirmed stage. Do not change `final`. Not wired in this slice: template materialize, catering packages, agent/seed/import writers, and the BEO export still omit `dishName` (legacy lines print the live name).

Completed (2026-09-22): the **PL-BOOKING AC-398 commercial-total-seed slice** is proven; the full PL-BOOKING box stays unchecked. No new Event fields, no manifest or generated-file change, and no `changeCommercial` command — the seed is the existing `quotedPrice` / `budgetAmount` pair on the Event (written by `planEngagement`, rewritten only by an explicit `Event.changePricing`; the sales_lock refuse guard stays). Runtime proof: `tests/proofs/lifecycle-commercial-snapshot-stability.runtime.test.ts` (2 tests) — a later `Proposal_createViaDraft` total (9999, then a second at 100) leaves Event.quotedPrice 4500 / budgetAmount 3000 (then 5200 / 2800 after an explicit changePricing) and stage planning untouched; a following changePricing to 5300 / 2900 still succeeds, so refusal-to-cascade is a snapshot, not a freeze. Display: `eventCommercialQuotedPrice` returns the stored seed (0 is a real seed; missing or non-finite stays missing) and never falls back to a live proposal total; the EventBudgetCard Quoted-price tile and the EventDetailPage hero "Budget / quoted" line print it. Proof: `tests/features/events/event-commercial-seed.test.ts` (6) and `tests/event-overview-behavior.test.ts` › "prints the booked commercial seed on the budget card, not a later proposal total" (the card's `formatMoney` prints whole-dollar USD without cents, so the case asserts `$2,000` / `$1,000` and not `$9,999`); the proving command ran 15/15 green. Orchestrator re-ran the proving files 15/15, the prior AC-398 snapshot set 36/36, the full suite 228 files / 1040 tests, typecheck, increment Prettier, and `bun run build` green. AC-398 stays PENDING — venue + style + occasion + client + commercial-seed slices exist; the menu/timing/readiness snapshots are still unproven. Do not invent a Confirmed stage. Do not change `final`. Findings recorded, not fixed: (a) the EventDetailPage hero/desktop header and the BEO export still print the live client catalog name via `clientDisplayName`, not `eventClientLabel`; (b) the commercial runtime proof creates a later same-client Proposal (not a linked-then-revised booking) — there is no Proposal.reviseTotals command, so a linked-revision cascade is still unproven. The billing side stays AC-413 (accepted revision), untouched here. This session staged the increment on `dev` at HEAD `4566afda` but could not commit/tag/push because Cursor issue [#394](https://github.com/Angriff36/capsule/issues/394) still intercepts `git commit` (PowerShell wrapper eval'd in bash; project `.githooks/pre-commit` never ran). User-owned dirty files were left unstaged. Preview `http://127.0.0.1:7813` serves this checkout; local Convex `127.0.0.1:3210` answered HTTP 200.

Completed (2026-09-22): the **PL-BOOKING AC-398 client-name snapshot slice** is proven; the full PL-BOOKING box stays unchecked. Orchestrator re-ran the proving files 19/19, the full suite 226 files / 1031 tests, typecheck, increment Prettier, and `bun run build` green. Event now stores `clientName` (property immediately after `clientId` in `src/operations/event.manifest`); `planEngagement` accepts and stamps it (`mutate clientName = clientName` immediately after `mutate clientId`); `bun run manifest:regen` wrote the generated schema/mutations. `captureDraft` / `updateImportDraft` / `reassignClient` stay unwired for `clientName`, and there is no `Event.changeClient` command — do not invent one. Runtime proof: `tests/proofs/lifecycle-client-snapshot-stability.runtime.test.ts` (2 tests) — Client has no rename command, so a raw `db.patch` of `companyName` simulates the later catalog edit, and Event.clientName stays put while the live link (`clientId`) keeps pointing at the row. Display: `eventClientLabel` is snapshot-first (then live name for legacy events, then honest loading/unavailable, then "—" — the details card always shows a Client row); the Event details card prints the snapshot and keeps the `/clients/{id}` link; the create form mapper stamps the chosen client's printed name. Proof: `tests/features/events/event-client-label.test.ts` (6), the mapper case in `tests/features/events/event-plan-engagement-form-mapper.test.ts`, and `tests/event-overview-behavior.test.ts` › "prints the booked client snapshot in the details card, not a later catalog rename". AC-398 stays PENDING — venue + style + occasion + client slices exist; the menu/commercial/timing/readiness snapshots are still unproven. Do not invent a Confirmed stage. Do not change `final`. Findings recorded, not fixed: (a) Client has no rename command — the proof uses `db.patch`; (b) `captureDraft` / `updateImportDraft` / `reassignClient` omit `clientName`; (c) the `EventDetailsCard` referral row still uses live catalog names; (d) do not invent `changeClient`. The increment is staged on `dev` at HEAD `4566afda` plus this working tree (client snapshot plus the already-proven venue/style/occasion slices). This session could not commit/tag/push because Cursor issue [#394](https://github.com/Angriff36/capsule/issues/394) still intercepts `git commit`. User-owned dirty files were left unstaged. Preview `http://127.0.0.1:7813` serves this checkout; local Convex `127.0.0.1:3210` answered `authStatus:getAuthStatus` as anonymous.

Completed (2026-09-22): the **PL-BOOKING AC-398 occasion snapshot slice** is proven; the full PL-BOOKING box stays unchecked. Event now stores `occasionName` (property after `occasionId` in `src/operations/event.manifest`); `planEngagement` accepts and stamps it (`mutate occasionName = occasionName`); there is still no `changeOccasion` command — do not invent one, the occasion snapshot is written only at planEngagement; `bun run manifest:regen` wrote the generated schema/mutations. Runtime proof: `tests/proofs/lifecycle-occasion-snapshot-stability.runtime.test.ts` (2 tests) — a catalog `Occasion_reviseDetails` rename leaves Event.occasionName; a second Event carries its own snapshot and a later catalog rename leaves that snapshot; orchestrator re-ran 2/2. Display: `eventOccasionLabel` is snapshot-first; the Event details card prints the snapshot; the create form mapper stamps the chosen name. Proof: `tests/features/events/event-occasion-label.test.ts` (6), the mapper case in `tests/features/events/event-plan-engagement-form-mapper.test.ts`, and `tests/event-overview-behavior.test.ts` › "prints the booked occasion snapshot in the details card, not a later catalog rename"; orchestrator re-ran the four proving files 17/17. AC-398 stays PENDING — venue + style + occasion slices exist; the client/menu/commercial/timing/readiness snapshots are still unproven. Do not invent a Confirmed stage. Do not change `final`. Findings recorded, not fixed: (a) `captureDraft` / `updateImportDraft` still omit `occasionName` (same as `serviceStyleName`); (b) no `changeOccasion` command exists, so a booked Event cannot change occasion after create; (c) `EventOverviewTab` does not pass `occasionsLoading` (same as `serviceStylesLoading`); (d) `EventDetailsCard` client/referral still use live catalog names; (e) `EventCreatePage.tsx` is already ~1176 lines — do not split it unless that is the task. After the slice, the orchestrator re-ran the full suite (224 files / 1021 tests green), typecheck green, increment Prettier green, and `bun run build` green. Preview `http://127.0.0.1:7813` serves this checkout (7812 is the sibling checkout); local Convex `127.0.0.1:3210` answered `authStatus:getAuthStatus` as anonymous. AC-398 stays PENDING. Do not invent a Confirmed stage. Do not change `final`. The increment is staged on `dev` at HEAD `4566afda` plus this working tree (the occasion snapshot plus the already-proven venue-display and service-style slices that earlier sessions could not land). This session could not commit/tag/push because Cursor issue [#394](https://github.com/Angriff36/capsule/issues/394) still intercepts `git commit` (PowerShell wrapper eval'd in bash, including when the orchestrator launched `pwsh -NoProfile -Command git commit`); the project `.githooks/pre-commit` was run by hand and passed: secrets, builder-regen-guard, lint-staged. User-owned dirty files were left unstaged: AGENTS.md, CLAUDE.md, STATE.md, convex/_generated/*, loop-ledger.json, loop-run-log.md, untracked spec copies.

Completed (2026-09-22): the **PL-BOOKING AC-398 service-style snapshot slice** is proven; the full PL-BOOKING box stays unchecked. Event now stores `serviceStyleName` (property after `serviceStyleId` in `src/operations/event.manifest`); `planEngagement` accepts and stamps it; `changeServiceStyle` takes optional `serviceStyleName` and preserves the stored snapshot when the name is omitted (`serviceStyleName != null ? serviceStyleName : self.serviceStyleName`), so existing callers that only pass the id do not wipe it; `bun run manifest:regen` wrote the generated schema/mutations. Runtime proof: `tests/proofs/lifecycle-service-style-snapshot-stability.runtime.test.ts` (2 tests) — a catalog `ServiceStyle_reviseDetails` rename leaves Event.serviceStyleName; an explicit `Event_changeServiceStyle` writes a new snapshot; a later catalog rename leaves that snapshot; orchestrator re-ran 2/2. Display: `eventServiceStyleLabel` is snapshot-first; the Event details card prints the snapshot; the create form, `EventServiceStyleForm`, and tracker send the chosen name. Proof: `tests/features/events/event-service-style-label.test.ts` (6), the mapper case in `tests/features/events/event-plan-engagement-form-mapper.test.ts`, and `tests/event-overview-behavior.test.ts` › "prints the booked service style snapshot in the details card, not a later catalog rename"; orchestrator re-ran the four proving files 15/15. AC-398 stays PENDING — the venue + style slices exist; the client/occasion/menu/commercial/timing/readiness snapshots are still unproven. Do not invent a Confirmed stage. Do not change `final`. Findings recorded, not fixed: `captureDraft` / `updateImportDraft` still omit `serviceStyleName`; packet/agent/import `Event_changeServiceStyle` callers still omit the name (preserve-if-omitted keeps the old snapshot); `binderColor` and `timingSuggestedSetupMinutes` still read live `self.serviceStyle.name`; EventDetailsCard occasion/client still use live catalog names; `EventCreatePage.tsx` is already ~1172 lines — do not split unless that is the task. After the slice, the orchestrator re-ran the full suite (222 files / 1011 tests green), typecheck green, increment Prettier green, and `bun run build` green. Preview `http://127.0.0.1:7813` serves this checkout (7812 is the sibling checkout); local Convex `127.0.0.1:3210` answered `authStatus:getAuthStatus` as anonymous. The increment is staged on `dev` at HEAD `4566afda` plus this working tree, but this session could not commit/tag/push because Cursor issue [#394](https://github.com/Angriff36/capsule/issues/394) still intercepts `git commit` (PowerShell wrapper eval'd in bash; the project `.githooks/pre-commit` was run by hand and passed: secrets, builder-regen-guard, lint-staged). User-owned dirty files were left unstaged: AGENTS.md, CLAUDE.md, STATE.md, convex/_generated/*, loop-ledger.json, loop-run-log.md, untracked spec copies.

Completed (2026-09-22): the **PL-BOOKING AC-398 venue-display snapshot slice** is proven; the full PL-BOOKING and PL-LIFECYCLE boxes stay unchecked. `eventVenueLabel` now returns Event.venueName first, then the live Venue.name (legacy), then loading/unavailable / "No venue yet"; `EventDetailPage` prints `venueLabel` in the header, not `venue.name`, and the `/facilities` link stays when venueId is set. Proof: `tests/features/events/event-venue-label.test.ts` (7 tests) and `tests/event-overview-behavior.test.ts` › "prints the booked venue snapshot in the header, not a later catalog rename"; the orchestrator re-ran the proving files 10/10, the full suite 220 files / 1003 tests, typecheck, increment format and build green. AC-398 stays PENDING — this is only the venue-header display slice; the client/occasion/style/menu/commercial/timing/readiness snapshots are still unproven. AC-236 stays PASS (stored fields). Do not invent a Confirmed stage. Do not change `final`. Finding recorded, not fixed: the venue *pickers* (Event detail edit-basics, EventTrackerPage select, EventCreatePage) still list live catalog names — that is how an operator chooses a venue, not a booked-name lie. `EventDetailPage.tsx` remains ~846 lines; do not split it in a follow-up unless that is the task. This increment is proven and staged on `dev` at HEAD `4566afda`, but this session could not commit/tag/push because Cursor issue [#394](https://github.com/Angriff36/capsule/issues/394) still intercepts `git commit` (PowerShell wrapper eval'd in bash; project `.githooks/pre-commit` never ran). Hand-run `bun run secrets`, `builder-regen-guard --check-staged`, and `lint-staged` passed. User-owned dirty files were left unstaged. Preview http://127.0.0.1:7813 serves this checkout; local Convex 3210 answered authStatus:getAuthStatus.

Completed (2026-09-22): the **PL-BOOKING #393 unique-versus-ambiguous venue auto-select slice** is proven; the full PL-BOOKING box stays unchecked. `ProposalEventPrefill.matchVenue` now returns unique / ambiguous / none; `EventCreatePage` auto-fills only a unique name match, two same-named saved venues show a pick hint instead of a silent first-match, and the operator's pick books that venue. Proven by three new cases in `tests/features/events/proposal-event-prefill.test.ts` and `tests/event-create-behavior.test.ts` › "blocks venue auto-fill when two saved venues share the proposal's name and books the one the operator picks"; the orchestrator re-ran the proving files 19/19, the full suite 219 files / 995 tests, typecheck, increment format and build green. AC-410 stays PENDING — the replay leg and this ambiguous-venue slice exist, but the rest of BE-7.1 is still unproven. Do not invent a Confirmed stage. Do not change `final`. Finding superseded (2026-09-22 venue-display slice): the AC-398 display preference in `eventVenueLabel` is now snapshot-first, so a later worker does not re-do it.

Completed (2026-09-22): the **PL-LIFECYCLE AC-236 venue-snapshot stability proof** is proven; the full PL-LIFECYCLE box stays unchecked. `tests/proofs/lifecycle-venue-snapshot-stability.runtime.test.ts` (2 tests; orchestrator re-ran 2/2 green) shows Event.venueName/venueAddress/venueCapacity stay Garden Hall / 100 Oak St / 80 after Venue_updateDetails renames the catalog row and Venue_changeCapacity sets 200 (the Venue row does change); an explicit Event_changeVenue still writes Lakeside Pavilion / 12 Water Rd / 120, and a later catalog rename/capacity 999 leaves that snapshot, so refusal-to-cascade is a snapshot, not a freeze. AC-236 is newly PASS. AC-099 / AC-223 / AC-227 / AC-228 / AC-234 / AC-235 / AC-239 / AC-612 stay PENDING. No product-code, manifest or generated-file change. Finding superseded (2026-09-22 venue-display slice): the Event detail header now prints the stored snapshot via `eventVenueLabel`, not the renamed catalog. Next remaining PL-LIFECYCLE work is owner Confirmed/final (AC-099 / AC-223 / AC-612) plus AC-227 / AC-228 / AC-234 / AC-235 / AC-239. Do not invent a Confirmed stage. Do not change `final` meaning. Do not add completeness gates on lockForSales.

Completed (2026-09-22): the **PL-LIFECYCLE AC-237 plan-engagement validation proof** is proven; the full PL-LIFECYCLE box stays unchecked. `tests/proofs/lifecycle-plan-engagement-validation.runtime.test.ts` (3 tests; orchestrator re-ran 3/3 green) shows inverted dates and headcount 0/100001 on Event_createViaPlanEngagement throw ("Event end must be after its start" / "Headcount must be between 1 and 100000") with zero Event rows; a valid create still succeeds (planning, headcount 40, version 1); inverted Event_reschedule and Event_changeHeadcount 0 leave the row untouched; a following current-version edit to 55 succeeds (version 2), so refusal is the constraint, not a freeze. AC-237 is newly PASS. AC-099 / AC-223 / AC-227 / AC-228 / AC-234 / AC-235 / AC-236 / AC-239 / AC-612 stay PENDING. No product-code, manifest or generated-file change. Next remaining PL-LIFECYCLE work is AC-236 venue-snapshot stability (Event.venueName/venueAddress/venueCapacity must not change when Venue.updateDetails / changeCapacity later revises the catalog row) plus owner Confirmed/final (AC-099 / AC-223 / AC-612) and AC-227 / AC-228 / AC-239. Do not invent a Confirmed stage. Do not change `final` meaning. Do not add completeness gates on lockForSales (AC-227 is a separate owner-gated decision; backend §4.3 readiness is a projection). This session staged the increment (plus the already-staged AC-224 / AC-238 proofs) on `dev` at `9085775b` but could not commit/tag/push because Cursor issue [#394](https://github.com/Angriff36/capsule/issues/394) still intercepts `git commit` (PowerShell wrapper eval'd in bash; project `.githooks/pre-commit` never ran). Hand-run `bun run secrets` and increment Prettier check passed. Full suite 218 files / 989 tests, typecheck, and build green. Preview http://127.0.0.1:7813 serves this checkout; local Convex 3210 answered authStatus:getAuthStatus. User-owned dirty files were left unstaged.

The outcome is the full audience job: Josh sells once and operates from one Event; sales carries every client decision forward; Kayden can prep, pack, work and record time from a phone; clients can request, accept and pay; Tim can reconcile live money and operations. Every in-spec gap remains below. Priority orders dependencies, not a release ceiling or an owner deferral.

Preserve the working transactional booking and menu copy, quote conversion checkpoints, recipe saved review/finalization, exact unit helpers, culinary demand, shared weekly purchasing, inventory/staffing cancellation callbacks, atomic templates, live dashboards, payroll CSV, durable invoice reminders, number sequence, scoped file reads and authorized self-hosted release orchestrator. Where code exists without full proof, the task is qualification followed only by demonstrated repairs. Reuse `src/lib` and authored seams; no parallel CRM, pricing engine, command API, domain model or workflow engine.

The backend contract permits backend repairs plus minimum typed consumer compatibility, not a UI redesign. Separate older product requirements still need their normal user-facing workflows. DESIGN.md front matter controls presentation; incomplete facts create scoped issues, not global service freezes. Generated output changes only through `bun run manifest:regen`. Each future implementation task gets a narrow worker brief with exact owners, a neighboring pattern, prohibited changes and its named proving command; only one editing/testing worker runs at a time.

Open meaning decision: backend §4.2 uses `final` for service finished; §14.2 uses it for Ops Final review. Older complete-product §3.3 also distinguishes Confirmed from execution. The owner has been asked to choose. Preserve both required outcomes and plan the lifecycle mapping explicitly; proposed reconciliation is to keep the existing post-service `final` and represent pre-departure Ops Final as readiness, but this proposal is **not owner approval**. This does not block independent work. Missing authoritative manual/KPI details and business-specific financial/retention/pay terms must be obtained before asserting parity or activating their calculations; safe implementation and evidence preparation remain planned.

Acceptance is [ACCEPTANCE_TESTS.md](ACCEPTANCE_TESTS.md). A checkbox in this plan is not proof. Each task below names all its required AC IDs; the source ledger contains the precise gap/evidence per criterion. Run the named criterion tests (or extend their existing owning suite), applicable runtime/browser/J evidence, deterministic regeneration when relevant, and `bun run check`. Never create speculative implementation-only tests or restore removed source-string tests. Independent different-model APPROVE is required before merge; production release requires explicit current-conversation release authorization and the approved orchestrator.

Current maintenance: required upstream `1c065a3e` is ancestral; local `dev` HEAD will be this AC-407 consumed-stock increment. Orchestrator verified the new proof 2/2, related 10/10, the full suite 288 files / 1237 tests, typecheck, format:check, and `bun run build` green. Preview `http://127.0.0.1:7813` serves this checkout. Local Convex `127.0.0.1:3210` answered `authStatus:getAuthStatus` as anonymous. User-owned dirty files stay unstaged (AGENTS.md, CLAUDE.md, STATE.md, loop-ledger.json, loop-run-log.md, untracked spec copies and .builder baselines). Cursor issue [#394](https://github.com/Angriff36/capsule/issues/394) still intercepts `git commit` from bash — commit through `pwsh -NoProfile -File` so project `.githooks/pre-commit` runs. Do not use `--no-verify`. This is local verification, not production. Next remaining AC-407 work is custody (PL-RECONCILIATION stays unchecked). PL-LIFECYCLE remaining is AC-227 / AC-055 (owner #397). Do not invent a Confirmed stage. Do not change `final`.

Completed (2026-09-22): the **PL-LIFECYCLE AC-238 version-conflict proof** is proven; the full PL-LIFECYCLE box stays unchecked. `tests/proofs/lifecycle-event-version-conflict.runtime.test.ts` (2 tests; orchestrator re-ran 2/2 green) shows two edits at version 1 — the first (headcount 48) wins, the second (99) throws ConcurrencyConflict / VERSION_MISMATCH with the row still at headcount 48, version 2 — and a following current-version edit succeeds (55, version 3), so the conflict is a conflict, not a freeze. AC-238 is newly PASS. AC-099 / AC-223 / AC-227 / AC-228 / AC-612 stay PENDING. No product-code, manifest or generated-file change. Finding: `Event_changeHeadcount`'s `version` arg is `v.optional`, so a caller that omits it skips OCC entirely — do not hand-edit the generated mutation to change that. This session staged the increment (plus the already-staged AC-224 TPP mapping proof) on `dev` at `9085775b` but could not commit/tag/push because Cursor issue [#394](https://github.com/Angriff36/capsule/issues/394) still intercepts `git commit` (PowerShell wrapper eval'd in bash; project `.githooks/pre-commit` never ran). Hand-run `bun run secrets`, `builder-regen-guard --check-staged`, and `lint-staged` passed. User-owned dirty files were left unstaged.

Completed (2026-09-22): the **PL-LIFECYCLE AC-224 TPP status-mapping proof** is proven; the full PL-LIFECYCLE task remains unchecked. New proof `tests/proofs/lifecycle-tpp-status-mapping.runtime.test.ts` (3 tests; orchestrator re-ran 3/3, full suite 216 files / 984 tests, typecheck, increment format, and build green; preview http://127.0.0.1:7813 serves this checkout; local Convex 3210 answered authStatus:getAuthStatus). The increment is staged (ACCEPTANCE_TESTS.md, IMPLEMENTATION_PLAN.md, the new proof) but this session could not commit/tag/push because Cursor issue [#394](https://github.com/Angriff36/capsule/issues/394) still intercepts `git commit` (PowerShell wrapper eval'd in bash; project `.githooks/pre-commit` never ran). Hand-run `bun run secrets`, `builder-regen-guard --check-staged`, and `lint-staged` passed. User-owned dirty files were left unstaged. Proof: `mapTppEventStage` is exactly the documented ten pairs with unknown statuses ("Confirmed", "2- Sales Lock") falling back to `planning`; a real `quickImport.importFile` commit (one contact + two events, statuses "Sales Lock"/"Complete") leaves each event ExternalRecordLink resolved with the raw TPP EventStatus readable in `rawSourceData` (`rawEventStatus` literal) alongside the mapped `stage` (link-only) while the created Event stays at `planning` (Event_createViaPlanEngagement hardcodes planning); and a second import of the same EventIDs skips idempotently without wiping the stored raw status. AC-224 is newly PASS. AC-225 / AC-399 / AC-226 / AC-400 remain PASS. AC-099 / AC-223 / AC-612 / AC-227 / AC-228 / AC-055 stay PENDING. No mapper change, no Confirmed stage, no `final` meaning change, no TPP stage applied at Event create, no manifest or generated-file edit; focused run 3/3 green.

Completed (2026-09-22): the **PL-LIFECYCLE AC-225 full-sequence proof** is proven; the full PL-LIFECYCLE task remains unchecked. New proof `tests/proofs/lifecycle-transition-sequence.runtime.test.ts` (3 tests): the canonical ladder create→submit→approve→lock→execute→finalize→complete→closeOut is driven by guarded commands, each gate records time on the Event (plannedAt, approvedAt, salesLockedAt, executionStartedAt, finalizedAt, completedAt, closedOutAt — each monotonic ≥ the prior gate) and emits exactly one typed ledger row (submitForApproval sets no Event timestamp; time is the ledger createdAt), with the final check confirming every ladder type is still exactly one row; cancel from approved records cancelledAt + cancellationReason + one EventCancelled and a second cancel is refused with the count still 1; a kitchen caller is refused on approve (stage, approvedAt and the EventApproved count untouched) while event_manager approve then succeeds. AC-225 is newly PASS. AC-399 / AC-226 / AC-400 remain PASS. AC-099 / AC-223 / AC-612 / AC-055 / AC-224 / AC-227 / AC-228 stay PENDING. No Confirmed stage, no `final` meaning change, no manifest or generated-file edit.

Completed (2026-09-22): the **PL-LIFECYCLE remaining sales_lock ops slice** is proven; the full PL-LIFECYCLE task remains unchecked. Three commands now allow sales_lock: `Event.assignOwner`, `Event.normalizePurchasingWeek`, and `EventDish.confirmFromProposal`. Regen via `bun run manifest:regen` wrote the generated guards. Proof: `tests/proofs/lifecycle-sales-lock-remaining-ops.runtime.test.ts` (2 tests) — the three commands succeed on sales_lock, cancelled and final refuse, and quotedPrice 4500 / budgetAmount 3000 stay untouched. AC-399 remains PASS (already, via the matrix); AC-226 and AC-400 remain PASS. AC-099 / AC-223 / AC-612 stay PENDING (owner Confirmed/final still open); AC-055 stays PENDING. No `final` meaning change, no Confirmed stage. Evidence this session: focused new file 2/2 green; focused lifecycle set 7 files / 17 tests green; typecheck green; full suite 214 files / 978 tests green; build green; increment format green. Findings recorded, not fixed: Event.assignOwner still omits executing (only add sales_lock); EventDish.confirmFromProposal still requires manageAccess or salesAccess (kitchen cannot confirm); an optional assignedToId JSON null fails validation before the guard (omit the field to clear). Next remaining PL-LIFECYCLE work is AC-099 / AC-223 / AC-612 (owner decision) plus AC-055 / AC-224 / AC-225 / AC-227 / AC-228; do not invent a Confirmed stage. This session staged the increment on dev at HEAD 4e331a34 but could not commit/tag/push because Cursor issue [#394](https://github.com/Angriff36/capsule/issues/394) persists (PowerShell wrapper eval'd in bash; project .githooks/pre-commit never ran). Hand-run bun run secrets, builder-regen-guard --check-staged, and lint-staged passed. User-owned dirty files were left unstaged.

Completed (2026-09-22): the **PL-LIFECYCLE #384 first-slice increment** is proven; the full PL-LIFECYCLE task remains unchecked. The Event cancel guard now matches the declared transition table exactly — cancel is legal in quote, planning, pending_approval, approved, sales_lock, executing and final, and refused elsewhere — beginExecution is sales_lock-only and no longer freezes on unfinished prep/pack/delivery work, and confirmSalesLock is unchanged (salesAccess / EventSalesLockConfirmed). `isReadyForExecution` remains a projection only (backend §4.3). The `final` meaning is NOT changed: AC-612 stays PENDING and the owner decision stays open. AC-226 is newly PASS; the AC-399 cancel-matrix legs now exist in the same proof file (`tests/proofs/lifecycle-meanings.runtime.test.ts`) but the full ten-stage §4.2 behavior contract stays PENDING; all other PL-LIFECYCLE ACs are unchanged. Evidence: focused run 3 files / 12 tests green, full suite 198 files / 934 tests green, typecheck green; format:check is red only on the pre-existing owner-owned `loop-ledger.json`, which was not touched. No commit SHA, CI, browser or release is claimed.

Completed (2026-09-22): the **PL-LIFECYCLE sales_lock ops-edits slice** is proven; the full PL-LIFECYCLE task remains unchecked and AC-399 stays PENDING. Required upstream was integrated first: `origin/main` `1c065a3e` is now an ancestor and local `dev` was fast-forwarded to `origin/dev` `4cb2a804` (generated-file restore on backend deploys), with user-owned dirty files preserved. `Event.changeHeadcount` now allows `sales_lock` (it previously stopped at approved/executing — a hole between approved and executing); `Event.changePricing` is unchanged and still refused on sales_lock. Regen via `bun run manifest:regen` updated `convex/mutations.ts` Guard 0 for `__runEventChangeHeadcount` to include sales_lock. New proof `tests/proofs/lifecycle-sales-lock-ops-edits.runtime.test.ts` (2 tests): changeHeadcount on sales_lock updates expectedHeadcount to 48 while quotedPrice, budgetAmount and the stage stay untouched; changePricing is refused with zero writes. Evidence run by this session: new file 2/2 green; focused lifecycle set 7 files / 25 tests green; typecheck green; build green; full suite 204 files / 951 tests green. AC-226 and AC-400 remain PASS. AC-099 / AC-223 / AC-612 stay PENDING (owner Confirmed/final meaning still open). Finding recorded, not fixed: EventDish live-ops commands (`addToEvent`, `adjustServings`, `syncHeadcount`, `setHeadcountOverride`, `changeCourse`, `updateInstructions`, `remove`) still omit `sales_lock` while allowing `executing` — same hole as headcount was — and `Event.manifest` `isEditable` still omits sales_lock while the UI's `EventLifecyclePolicy.isEditableStage` includes it; do not "fix" isEditable this increment. No `final` meaning change, no Confirmed stage. This session staged the increment (including the earlier returnToPlanning / quote / planning proofs) on top of `4cb2a804` but could not commit/tag/push because Cursor's pre-commit wrapper still evals PowerShell inside bash (`syntax error near unexpected token '&'`, issue [#394](https://github.com/Angriff36/capsule/issues/394)); the project's `.githooks/pre-commit` never ran. Hand-run `bun run secrets` and `builder-regen-guard --check-staged` passed.

Completed (2026-09-22): the **PL-LIFECYCLE EventDish sales_lock ops-edits slice** is proven; the full PL-LIFECYCLE task remains unchecked and AC-399 stays PENDING. Seven EventDish live-ops command guards in `src/culinary/event-dish.manifest` now include sales_lock (addToEvent, adjustServings, syncHeadcount, setHeadcountOverride, changeCourse, updateInstructions, remove); confirmFromProposal was left unchanged (still omits sales_lock while allowing executing). Regen via `bun run manifest:regen` wrote the generated guards. Proof: `tests/proofs/lifecycle-sales-lock-event-dish.runtime.test.ts` (2 tests) — the six ops commands succeed on sales_lock while Event quotedPrice/budgetAmount/expectedHeadcount/stage stay untouched, and a cancelled Event still refuses addToEvent. Evidence this session: focused new file 2/2 green; full suite 205 files / 953 tests green; typecheck green; build green; increment format green (repo format:check still red only on owner-owned `loop-ledger.json`). AC-226 and AC-400 remain PASS. AC-099 / AC-223 / AC-612 stay PENDING (owner Confirmed/final meaning still open). Findings recorded, not fixed: (a) `confirmFromProposal` still omits sales_lock; (b) `Event.manifest` `isEditable` still omits sales_lock while `EventLifecyclePolicy.isEditableStage` includes it — do not "fix" isEditable unless the next slice is that UI/computed alignment; (c) `src/culinary/event-dish.manifest` is 565 lines (already over 500 before this increment). Next remaining AC-399 slice: approved ensure (invoice draft / pack list / prep / purchasing / staffing on approve), then executing/final/completed/closed_out/cancelled behaviors. Do not change `final` meaning. Do not add a Confirmed stage. No commit/tag/push is claimed here; the orchestrator handles git. This session staged the EventDish increment (plus the earlier returnToPlanning / quote / planning / headcount proofs) on top of 4cb2a804 but could not commit/tag/push because Cursor's pre-commit wrapper still evals PowerShell inside bash (syntax error near unexpected token '&', issue #394); the project's .githooks/pre-commit never ran.

Completed (2026-09-22): the **PL-LIFECYCLE approved-ensure slice** is proven; the full PL-LIFECYCLE task remains unchecked and AC-399 stays PENDING. New proof `tests/proofs/lifecycle-approved-ensure.runtime.test.ts` (3 tests): a bare approve yields exactly 1 draft unsent invoice + 1 draft pack list + 0 productionBatches + 0 purchaseNeeds + 0 eventStaffNeeds; with a dish seed and calculated demand the same approve yields 1 planned productionBatch (yield 40, portion) + 1 open purchaseNeed (4.5) while still 1 draft invoice + 1 draft pack; and a posted captain EventStaffNeed survives approve — approve invents no extra roles. No manifest or generated-file edit was made in this slice: the Invoice.issue / PackList.open / ProductionBatch.plan / PurchaseNeed.create reactions were already in place. Finding recorded, not fixed: EventStaffNeed is still not created from templates on approve — that is AC-495 / PL-STAFFING; do not invent a template entity here. Next remaining AC-399 slice: executing / final / completed / closed_out / cancelled behaviors. Do not change `final` meaning. Do not add a Confirmed stage. AC-226 and AC-400 remain PASS. AC-099 / AC-223 / AC-612 stay PENDING (owner Confirmed/final meaning still open). Evidence this session: focused new file 3/3 green; focused lifecycle set 8 files / 26 tests green; full suite 206 files / 956 tests green; typecheck green; build green; increment format green (repo format:check still red only on owner-owned loop-ledger.json). Preview http://127.0.0.1:7813 serves this checkout; local Convex on 3210 answered authStatus:getAuthStatus. This session staged the approved-ensure proof (plus the earlier returnToPlanning / quote / planning / headcount / EventDish proofs) on top of 4cb2a804 but could not commit/tag/push because Cursor's pre-commit wrapper still evals PowerShell inside bash (syntax error near unexpected token '&', issue #394); the project's .githooks/pre-commit never ran. Hand-run bun run secrets and builder-regen-guard --check-staged passed.

Completed (2026-09-22): the **PL-LIFECYCLE executing day-of-actions slice** is proven; the full PL-LIFECYCLE task remains unchecked and AC-399 stays PENDING. New proof `tests/proofs/lifecycle-executing-actions.runtime.test.ts` (3 tests): on an executing Event, the §4.2 day-of commands all succeed — EventGuest invite→rsvpConfirm→checkIn (rsvpStatus confirmed, checkedInAt set) with Event quotedPrice/budgetAmount/expectedHeadcount/stage untouched, EventTimelineActivity createViaSchedule→complete (completedAt set), and Incident_createViaReport (open, correct eventId, reportedAt set) with the Event still executing — and the exit from executing is finalizeEvent, not complete: Event_complete is refused from executing with zero writes (same stage and version) and Event_finalizeEvent moves the Event to `final`. No manifest or generated-file edit, no new stage guard, no `final` meaning change, no Confirmed stage. Next remaining AC-399 slice: final / completed / closed_out / cancelled behaviors. AC-226 and AC-400 remain PASS. AC-099 / AC-223 / AC-612 stay PENDING (owner Confirmed/final meaning still open). Evidence this session: focused new file 3/3 green (`vitest run tests/proofs/lifecycle-executing-actions.runtime.test.ts`, exit 0).

Completed (2026-09-22): the **PL-LIFECYCLE final-stage actuals slice** is proven; the full PL-LIFECYCLE task remains unchecked and AC-399 stays PENDING. New proof `tests/proofs/lifecycle-final-actuals.runtime.test.ts` (3 tests): a `final` Event (walker create → submitForApproval → approve → lockForSales → beginExecution → finalizeEvent) accepts actuals — EventGuest invite→rsvpConfirm→checkIn (confirmed, checkedInAt set) and Incident_createViaReport (open, correct eventId, reportedAt set) with Event quotedPrice/budgetAmount/expectedHeadcount/stage untouched — refuses new speculative menu plans (EventDish_createViaAddToEvent rejected with zero writes, stage and version unchanged), refuses Event_closeOut with zero writes, and exits via Event_complete to `completed`. No `final` meaning change, no Confirmed stage, no manifest or generated-file edit. Findings recorded, not fixed: EventDish.adjustServings and setHeadcountOverride omit `final` from their allowed stages while allowing completed/closed_out; Event.changeHeadcount also omits `final`; ProductionBatch.plan has no event-stage guard at all; EventCloseout has capture+finalize but no audited correction/reopen path after finalize — that correction/reopen gap is AC-626 / PL-CLOSEOUT, not this slice. Next remaining AC-399 slice: completed / closed_out / cancelled behaviors. AC-226 and AC-400 remain PASS. AC-099 / AC-223 / AC-612 stay PENDING (owner Confirmed/final meaning still open). Evidence this session: focused new file 3/3 green (`bun run test -- tests/proofs/lifecycle-final-actuals.runtime.test.ts`, exit 0). A later independent session re-verified the proof (3/3), the focused lifecycle run (4 files / 15 tests), the full suite (209 files / 963 tests), typecheck, build, increment format, and preview http://127.0.0.1:7813, but could not commit/tag/push because Cursor's pre-commit wrapper still evals PowerShell inside bash (syntax error near unexpected token '&', issue #394), so the project's `.githooks/pre-commit` never ran; the work stays staged on `dev` at `1362b4f0`.

Completed (2026-09-22): the **PL-LIFECYCLE terminal-meanings slice** is proven; the full PL-LIFECYCLE task remains unchecked and AC-399 stays PENDING. New proof `tests/proofs/lifecycle-terminal-meanings.runtime.test.ts` (3 tests): Event_complete releases the remaining ACTIVE inventory hold and leaves the consumed hold as history (Event completed, commercial fields untouched, EventDish_createViaAddToEvent still refused); Event_closeOut seeds exactly one draft EventCloseout (budgetedRevenue 4500, actualRevenue 0, capturedAt set) and closed_out refuses Event_changePricing, Event_changeHeadcount and Event_cancel, and after EventCloseout_finalize a second EventCloseout_capture with the same money fields is refused with the closeout still finalized; Event_cancel from approved cancels the draft pack list, voids the unpaid draft invoice and releases the active hold while both rows remain present (deletedAt null). No manifest or generated-file edit, no product-code change, no `final` meaning change, no Confirmed stage. Finding recorded, not fixed: EventCloseout still has no audited correction/reopen path after finalize — that gap is AC-626 / PL-CLOSEOUT; do not invent a reopen/correct command here. Also recorded, not fixed: `InventoryReservation.reserve` in `src/inventory/stock.manifest` allows planning, pending_approval, approved, and executing and omits sales_lock and final — the same hole class as EventDish before the sales_lock slice — and holds already reserved at approved survive through sales_lock (which is why the terminal-meanings proof seeds at approved); do not fix the reserve guard in this increment. Next remaining AC-399 work is the full ten-stage §4.2 meaning matrix together with the owner Confirmed+final decision (AC-099 / AC-223 / AC-612), not another terminal-stage slice. AC-226 and AC-400 remain PASS. AC-099 / AC-223 / AC-612 stay PENDING. Evidence this session: focused new file 3/3 green (`bun run test -- tests/proofs/lifecycle-terminal-meanings.runtime.test.ts`, exit 0). A later session still cannot commit/tag/push this increment because Cursor issue #394 (PowerShell wrapper eval'd in bash) persists and the project `.githooks/pre-commit` never ran; hand-run `bun run secrets`, `builder-regen-guard --check-staged` and `lint-staged` all passed, and the increment stays staged on `dev` at HEAD `c830f6cb`.

Completed (2026-09-22): the **PL-LIFECYCLE InventoryReservation sales_lock reserve slice** is proven; the full PL-LIFECYCLE task remains unchecked and AC-399 stays PENDING. `InventoryReservation.reserve` in `src/inventory/stock.manifest` now allows sales_lock (planning, pending_approval, approved, sales_lock, executing). Regen via `bun run manifest:regen` wrote Guard 3 on `__runInventoryReservationReserve` / `InventoryReservation_createViaReserve`. Proof: `tests/proofs/lifecycle-sales-lock-reserve.runtime.test.ts` (2 tests) — reserve on sales_lock creates an active hold (quantity 8) while Event quotedPrice/budgetAmount/expectedHeadcount/stage stay untouched; cancelled and final still refuse reserve. Do not add `final` (no new speculative plans). No Confirmed stage. No `final` meaning change. AC-226 and AC-400 remain PASS. AC-099 / AC-223 / AC-612 stay PENDING (owner Confirmed/final meaning still open). Finding recorded, not fixed: `Event.manifest` `isEditable` still omits sales_lock while `EventLifecyclePolicy.isEditableStage` includes it; `EventDish.confirmFromProposal` still omits sales_lock. Next remaining AC-399 work is still the full ten-stage §4.2 meaning matrix together with the owner Confirmed+final decision — not another terminal-stage slice. The orchestrator handles git.

Completed (2026-09-22): the **PL-LIFECYCLE sales_lock Event ops-revise slice** is proven; the full PL-LIFECYCLE task remains unchecked and AC-399 stays PENDING. `Event.reschedule`, `Event.changeVenue`, `Event.changePrimaryContact`, and `Event.changeRequirements` now allow sales_lock (they previously stopped at approved). `Event.isEditable` now includes sales_lock so generated `getEvent` matches `EventLifecyclePolicy.isEditableStage`. Regen via `bun run manifest:regen` wrote the generated guards and query hydration. `EventTrackerPage` RESCHEDULE_STAGES / HEADCOUNT_STAGES now include sales_lock so the tracker does not hide edits the commands accept. Proof: `tests/proofs/lifecycle-sales-lock-event-ops.runtime.test.ts` (2 tests) — reschedule/changeVenue/changePrimaryContact/changeRequirements succeed on sales_lock while quotedPrice 4500 / budgetAmount 3000 / stage stay untouched and getEvent.isEditable is true; changePricing still refused with zero writes; cancelled refuses reschedule; executing getEvent.isEditable is false. Policy unit: `tests/event-lifecycle-policy.test.ts` isEditableStage(sales_lock)=true, executing=false, canChangeHeadcount(sales_lock)=true. Evidence this session: focused new file + policy 2 files / 5 tests green; focused lifecycle set 5 files / 16 tests green; typecheck green. Do not mark AC-399 PASS. No `final` meaning change, no Confirmed stage. Findings recorded, not fixed: (a) Event detail still enables Save pricing on sales_lock because canRevise gates the commercial form too — the command correctly refuses; (b) Event.assignOwner still omits sales_lock; (c) EventDish.confirmFromProposal still omits sales_lock; (d) Event.normalizePurchasingWeek still omits sales_lock while allowing executing. Next remaining AC-399 work is still the full ten-stage §4.2 meaning matrix together with the owner Confirmed+final decision (AC-099 / AC-223 / AC-612). This session could not commit/tag/push because Cursor issue #394 (PowerShell wrapper eval'd in bash) persists and the project `.githooks/pre-commit` never ran; hand-run `bun run secrets` passed, and the work stays staged on `dev` at HEAD `c830f6cb`.

Completed (2026-09-22): the **PL-LIFECYCLE AC-399 ten-stage meaning matrix** is proven. New file `tests/proofs/lifecycle-stage-meanings-matrix.runtime.test.ts` (4 tests) walks all ten §4.2 stages in one place: quote/planning/pending_approval have zero automatic invoice/pack/purchase/staff rows; approved ensures 1 draft unsent invoice + 1 draft pack; sales_lock freezes quotedPrice/budgetAmount and allows changeHeadcount; executing permits attendance and exits via finalizeEvent (complete refused); final refuses speculative EventDish add and closeOut and exits via complete; completed refuses cancel and exits via closeOut; closed_out refuses pricing/headcount/cancel; cancelled from approved stands down the draft pack and voids the unpaid draft invoice. AC-399 newly PASS. AC-226 and AC-400 remain PASS. AC-099 / AC-223 / AC-612 stay PENDING (owner Confirmed/final meaning still open). The full PL-LIFECYCLE task remains unchecked. Findings recorded, not fixed: Event.assignOwner and Event.normalizePurchasingWeek still omit sales_lock; EventDish.confirmFromProposal still omits sales_lock; EventCloseout still has no audited correction/reopen after finalize (AC-626 / PL-CLOSEOUT). Evidence this session: focused new file 4/4 green; focused lifecycle set 10 files / 32 tests green; full suite 213 files / 976 tests green; typecheck green; build green; increment format green. No product-code change, no `final` meaning change, no Confirmed stage. Commit/tag/push is blocked again by Cursor issue [#394](https://github.com/Angriff36/capsule/issues/394) (PowerShell wrapper eval'd in bash; project `.githooks/pre-commit` never ran). Work stays staged on `dev` at `c830f6cb`.

Completed (2026-09-22): the **PL-BOOKING booking-UI increment** (AC-411, issue #392) is proven; the full PL-BOOKING task remains unchecked. `EventCreatePage` no longer generic-creates an unlinked Event from proposal-routed contexts: only no-proposal routes keep generic creation, accepted-unlinked proposals book through the existing canonical seam with the returned Event id, a live linked proposal offers one primary Open event action, loading/missing/draft/nonaccepted/stale-deleted contexts show truthful next steps with no create CTA, and a seam failure keeps the entered details with no generic fallback. UI-only — no backend, Manifest or design-token change. Evidence: 14 mounted page tests in `tests/event-create-behavior.test.ts` (original 3 retained), focused 6 files/53 GATE_EXIT=0 (`focused-final.log`), full 197 files/929 GATE_EXIT=0 (`test-final.log`), full `bun run check` GATE_EXIT=0, begun at shared HEAD `3a4217c6` and completed with shared HEAD `a8a2d0c8` (shared history advanced while it ran; this increment's diff was unchanged throughout — local dirty-working-tree evidence, not clean-commit proof) (`check.log`), release-argv targeted run 5/5 (`release-test-orchestrator.log` — that correction is shared commit `3a4217c6`, not this diff), re-verified preview 7813→backend 3210 with a measured 40px Open event action at 360px, and independent review APPROVE + perceptual PASS (gpt-5.6-sol, `review-verdict.txt`) after one REJECT that correctly gated the unmatched-venue hint on `proposalLinkable`. Read-only browser scope only: no live booking writes, no real concurrency, no CI, and no release claimed. Stale historical spec status notes were reconciled without changing any normative requirement (`specs/capsule-complete-feature-spec.json` title/header/section 0; Ralph spec historical status corrections — checkbox updates in the no-fake-data spec and historical-defect wording in the dropdown-lists spec) — [ACCEPTANCE_TESTS.md](ACCEPTANCE_TESTS.md) still governs qualification. Source-inspection issue [#393](https://github.com/Angriff36/capsule/issues/393) (duplicate-name venue auto-selection) stays open under AC-410. Receipt: [proposal-route-evidence-2026-09-22.md](codex-plans/whole-spec-audit-2026-09-20/proposal-route-evidence-2026-09-22.md).

Validation (updated 2026-09-21): the PL-BUILD first increment is green on BOTH build environments at commit `fcf2238af52a457f920fc1513cc2dc3e4e6f3349` (`dev`, tag v0.0.71, draft PR #388). Windows (working tree based on `89c103bc` plus the tested increment): `bun run check` passes end to end — typecheck, format, secrets, nine Manifest integration guards, coverage ratchet (194 files / 896 tests), build, and the index-based baseline check (`baseline-decay: ok`); `bun run manifest:regen:check` reports generated output current. Linux CI: run [35599405184](https://github.com/Angriff36/capsule/actions/runs/35599405184) on ubuntu-latest at that exact commit succeeded — "Verify generated output" and "Run repository check gate" both green, same 194/896 suite footer. Evidence: `.artifacts/iteration-pl-build/check-after.log`, `regen-check.log`, `test-after.log`, the CI logs, and the receipt. This satisfies the six PL-BUILD criteria only (AC-162, AC-356, AC-362, AC-395, AC-396, AC-439); the whole application delivery is NOT complete. Earlier audit-time facts stand: a worker returning provider 429 with exit 0 was rejected (#381), and source-confirmed issues #382–386 cover release evidence, QBO recovery, lifecycle, venue-note privacy and cutover readiness. Fix or qualify these through the tasks below; do not disguise them by changing unrelated user work.

Completed (2026-09-21): **PL-BUILD — Restore truthful, reproducible repository gates** is done. The #380 root-count repair landed in its owner (`scripts/check-baseline-decay.ts` counts distinct Git-index root entries; 4 regression tests in `tests/baseline-decay.test.ts`), with `bun run check` + `bun run manifest:regen:check` green on Windows and Linux CI run [35599405184](https://github.com/Angriff36/capsule/actions/runs/35599405184) green at `fcf2238a` on ubuntu-latest. Acceptance AC-162, AC-356, AC-362, AC-395, AC-396, AC-439 all PASS. Receipt: [build-gate-evidence-2026-09-21.md](codex-plans/whole-spec-audit-2026-09-20/build-gate-evidence-2026-09-21.md). The branch is pushed and the draft PR #388 is open; nothing merged to `main` and nothing deployed.

Completed (2026-09-21): the **PL-BOOKING accepted-revision identity increment** is proven; the full PL-BOOKING task remains unchecked. Acceptance now records an already-captured revision as the stable `acceptedRevisionId`: the operator accept path records the highest-numbered live captured revision at acceptance (an explicit null with no revision stays allowed), the generated and public signature paths bind the exact signed revision, and a mutable or foreign revision is refused atomically. A later revision never relabels stored acceptance evidence, and the event booking read resolves the stored reference. Replay already returns the saved Event (commit `305c7473`) — unchanged, not rebuilt. Only AC-413 and AC-434 are PASS; all other criterion statuses are unchanged. The public menu-cascade gap was still open as issue #390 at that increment; it is repaired by the public-signature increment below. The replay-policy inspection concern (the replay early-return resolves the linked Event before any generated sales-access/event policy runs) remains explicitly unconfirmed. Receipt: [accepted-revision-evidence-2026-09-21.md](codex-plans/whole-spec-audit-2026-09-20/accepted-revision-evidence-2026-09-21.md).

Evidence (2026-09-21, pre-commit local working tree; Git commit/push and the post-commit regeneration gate are separate evidence): the 6 new real tests in `tests/proofs/accepted-revision-link.runtime.test.ts` pass, plus the focused run (4 files / 25 tests), the full suite (196 files / 904 tests) and full `bun run check` — all green. Review APPROVE: gpt-5.6-sol. The receipt holds source identity, gate logs, and preview details.

Completed (2026-09-21): the **PL-BOOKING public-signature equivalence increment** fixes issue #390 — public signature acceptance parity — and is proven; the full PL-BOOKING task remains unchecked. The public anonymous seam (`convex/signatureAcceptance.ts`) no longer re-implements acceptance: after its unchanged token/revision validations it runs the existing canonical generated `Proposal_accept` in the current transaction via `convex/lib/tenantSystemCommandRunner.ts` pinned to the validated request tenant, so the generated policies/guards, ProposalAccepted ledger row, stored accepted revision and the menu cascade now apply to public acceptance exactly as to operator acceptance. Only AC-414 and AC-435 are newly PASS; all other criterion statuses are unchanged. Evidence: `tests/proofs/signature-acceptance-equivalence.runtime.test.ts` 10 tests green after an initial RED 5 failed/5 passed, focused run 4 files / 29 tests, full suite 197 files / 914 tests, full `bun run check` exit 0, independent review APPROVE (gpt-5.6-sol, after one REJECT on a brittle guard-number assertion that was replaced). These are pre-commit local gates; no commit/push, CI, browser, visual or J proof is claimed, AC-006 stays pending, and the lifecycle meaning ambiguity stays unresolved. Receipt: [public-signature-evidence-2026-09-21.md](codex-plans/whole-spec-audit-2026-09-20/public-signature-evidence-2026-09-21.md). The stale section/source references in `specs/capsule-complete-feature-spec.md` and `specs/capsule-complete-feature-spec.json` were corrected without changing any normative requirement; the source-coverage hashes remain an audit snapshot.

Completed (2026-09-21): the **PL-BOOKING quote retry-link increment** fixes issue #391 — a quote-conversion retry that reused a saved unlinked draft proposal completed conversion and could book a second Event at acceptance — and is proven; the full PL-BOOKING task remains unchecked. The retry now recovers a lost Event checkpoint from the saved proposal's `eventId` pointer before creating another Event and validates the recovered Event before completion, and the canonical link (`Proposal_stageEventLink` → `Proposal_linkEvent`, via the new internal `convex/lib/proposalEventCreation.ts` `linkConvertedQuoteProposal` through generated authorized reads) runs before conversion completes: an unlinked draft links with no unaccepted menu copy (acceptance copies the menu exactly once), an already-valid same-event link is a no-op, conflicting recorded ids and a deleted/unavailable Event fail with ids preserved. Only AC-412 and AC-433 are newly PASS; all other criterion statuses are unchanged. Evidence: 4 new real runtime cases (seeded durable checkpoint state + real retry/actions/commands/readback, not fault injection or actual parallel races) in `tests/proofs/quote-conversion.runtime.test.ts` after RED 6 passed/1 failed, focused 5 files / 39 tests, full suite 197 files / 918 tests, full `bun run check` exit 0, independent review APPROVE (gpt-5.6-sol, scoped to this increment). These are pre-commit local gates; no commit/push, CI, browser, visual/J or OCC-concurrency proof is claimed. Still open on this task: the #392 `EventCreatePage.tsx` generic `useCreateEvent` fallback gap (AC-411, no UI edits in this increment) and the explicitly unconfirmed public booking replay-role policy concern. Receipt: [quote-retry-evidence-2026-09-21.md](codex-plans/whole-spec-audit-2026-09-20/quote-retry-evidence-2026-09-21.md). After a shared-checkout race committed `0d203713`, a fresh full `bun run check` (197/918, build and baseline ok, `check-shared-head.log`) passed there with the increment's helper/import landed in that commit; the remaining increment files follow in a separate commit.

Pending tasks, sorted by dependency priority (P0 correctness foundations, P1 connected operations, P2 complete breadth and qualification; all are required):

- [ ] **P0 · PL-PLAIN-WORDS — Every word a user can see is plain catering English (owner, 2026-09-23).** Sweep ALL user-facing text: screen copy, buttons, warnings, empty states, toasts, import notes, and every constraint/guard/policy message in `src/**/*.manifest` (regen after). Replace developer words (entity.command names, reaction, guard, policy, constraint, idempotency, tenant, seam, projection, canonical, hydrate, mapped, record, run, directory, step counts, Manifest/Convex/Builder terms) with what a catering manager would say: what happened and what to do next. Example already done: the event import screen (93e17ea4). 2026-09-23 leftover events slice also done (CommandFailure / create aside / guests intro / import copy / import preview step chip / tab error banner), the 2026-09-23 "record" leftover slice also done (BEO PDF / Client tab including billing + contacts / Capacity planner / Allergen briefing / guest-list coverage notice), and the 2026-09-23 packet/menu/staffing/guest slice also done (Event packet, menu, guest sidebar/summary, overview rail, staffing, photos, prep list, equipment return, timing planner, venue picker, mobile event cards, create-venue capacity note, source panel, Photos & incidents tab label, timeline intro, live profitability) — do not redo those strings; the box stays unchecked. Remaining events-area jargon: EventTimelinePanel "day-of run sheet"; TimelineBlockPicker "Already on this run"; EventTimelineSidebar "Questions on this run sheet"; eventVenueLabel "Venue record unavailable" (tests/features/events/event-venue-label.test.ts asserts the old string — update that test with the swap); EventGuestPanel note="recorded"; useEventMenuNutrition "recorded nutrition"; import executor/runner "cannot run"; and `src/operations/event.manifest` policy/constraint strings (regen after). Do it area by area (events remainder, sales, kitchen, logistics, workforce, finance, admin), one commit per area, and keep the meaning exact. AC: a reviewer reading every string a user can see finds no developer word.
- [ ] **P0 · PL-LIFECYCLE — Align lifecycle commands, cancellation and proportionate readiness.** Resolve the two meanings of final, map legacy confirmed/reopen/archive outcomes, reconcile cancel guards with declared transitions, and inspect beginExecution/confirmSalesLock consistency (#384). Domain issues must not globally freeze reasonable service edits. Keep committed actuals and cancellation history. Owners/reuse: `src/operations/event.manifest; convex/lib/eventCancellation.ts; convex/lib/operationalEvents.ts`. Acceptance: AC-055, AC-099, AC-223, AC-224, AC-225, AC-226, AC-227, AC-228, AC-234, AC-235, AC-236, AC-237, AC-238, AC-239, AC-399, AC-400, AC-612. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs. The #384 first slice (cancel guard + beginExecution overgate repair, AC-226 PASS) landed 2026-09-22, AC-400 is now PASS via `tests/proofs/lifecycle-event-contract.runtime.test.ts` (each canonical transition emits exactly one typed ledger row; confirmSalesLock emits EventSalesLockConfirmed and zero EventExecutionStarted; cancel is idempotent-refused; returnToPlanning from pending_approval only — see the completed-increment history in git), the `returnToPlanning` guard is now aligned with the transition table (quote/pending_approval only — the dead `approved` leg is closed), and the AC-399 quote-stage zero-downstream proof now exists (`tests/proofs/lifecycle-quote-zero-downstream.runtime.test.ts`), followed by the planning/pending_approval provisional-drafts proof (`tests/proofs/lifecycle-planning-provisional-drafts.runtime.test.ts` — that slice is done, so the next AC-399 slice is no longer "planning/pending_approval provisional-drafts-only"); the sales_lock headcount slice is also done — `Event.changeHeadcount` now allows sales_lock (pricing still refused there), proven by `tests/proofs/lifecycle-sales-lock-ops-edits.runtime.test.ts`; the EventDish sales_lock ops-edits slice is done too — the seven EventDish live-ops command guards in `src/culinary/event-dish.manifest` now include sales_lock, proven by `tests/proofs/lifecycle-sales-lock-event-dish.runtime.test.ts`; the approved-ensure slice is also done — approve ensures invoice draft + pack list always (when quotedPrice exists), production batch when a dish seed exists, purchase need when calculated demand exists, and invents no staff-need shells, proven by `tests/proofs/lifecycle-approved-ensure.runtime.test.ts` (staffing-from-templates remains AC-495 / PL-STAFFING); the executing day-of-actions slice is also done — proven by `tests/proofs/lifecycle-executing-actions.runtime.test.ts`; the remaining legs are still open — the rest of the stage meanings (AC-399 stays PENDING; the next AC-399 slice is final / completed / closed_out / cancelled behaviors), the confirmed mapping (AC-099/AC-223 stay PENDING), and the owner final meaning (AC-612 stays PENDING). AC-226 and AC-400 remain PASS. The final-actuals slice is done — proven by `tests/proofs/lifecycle-final-actuals.runtime.test.ts` — so the next remaining AC-399 slice is completed / closed_out / cancelled behaviors; AC-399 stays PENDING, AC-226 and AC-400 remain PASS, and AC-099 / AC-223 / AC-612 stay PENDING. The InventoryReservation.reserve sales_lock hole is now closed — `reserve` in `src/inventory/stock.manifest` allows sales_lock, proven by `tests/proofs/lifecycle-sales-lock-reserve.runtime.test.ts` (see the completed note above); do not re-do that slice. Holds already reserved at approved still survive through sales_lock, which is why the terminal-meanings proof seeds at approved. The sales_lock Event ops-revise slice is also done — `Event.reschedule`, `Event.changeVenue`, `Event.changePrimaryContact`, `Event.changeRequirements` and the Event `isEditable` hydration now include sales_lock (tracker stage lists updated to match), proven by `tests/proofs/lifecycle-sales-lock-event-ops.runtime.test.ts` (see the completed note above); do not re-do that slice. AC-399 is now PASS via `tests/proofs/lifecycle-stage-meanings-matrix.runtime.test.ts` (see the completed note above), AC-225 is now PASS via `tests/proofs/lifecycle-transition-sequence.runtime.test.ts` (see the completed note above), and the remaining work on this task is AC-099 / AC-223 / AC-612 (owner Confirmed/final) plus AC-055 / AC-227 / AC-228 (AC-224 is now PASS via `tests/proofs/lifecycle-tpp-status-mapping.runtime.test.ts`). AC-238 is now PASS via `tests/proofs/lifecycle-event-version-conflict.runtime.test.ts` — two same-version edits prove the conflict state with nothing overwritten and a current-version edit still succeeds (see the completed note above); do not re-do that slice, and do not hand-edit the generated `Event_changeHeadcount` to make its optional `version` required. AC-237 is now PASS via `tests/proofs/lifecycle-plan-engagement-validation.runtime.test.ts` — inverted dates and headcount 0/100001 refuse with zero rows, a valid create succeeds, and refused edits leave the row untouched while a current-version edit succeeds (see the completed note above); do not re-do that slice. AC-236 is now PASS via `tests/proofs/lifecycle-venue-snapshot-stability.runtime.test.ts` — Venue_updateDetails / Venue_changeCapacity change only the catalog row while the Event snapshot stays put, and an explicit Event_changeVenue writes a new snapshot (see the completed note above); do not re-do that slice. The remaining work on this task is AC-099 / AC-223 / AC-612 (owner Confirmed/final), AC-055 / AC-227 / AC-228, plus AC-235 still PENDING. AC-234 is now PASS via `tests/proofs/lifecycle-event-tpp-fields.runtime.test.ts`. Finding recorded, not fixed: numbered TPP labels such as "2- Sales Lock" still fall through `mapTppEventStage` to `planning` — do not invent those keys without an approved data-map source. The three recorded sales_lock holes are now closed — Event.assignOwner, Event.normalizePurchasingWeek and EventDish.confirmFromProposal allow sales_lock, proven by `tests/proofs/lifecycle-sales-lock-remaining-ops.runtime.test.ts` (see the completed note above); do not re-do that slice. AC-099 / AC-223 / AC-612 stay PENDING (owner Confirmed/final still open) and the box stays unchecked. AC-239 is now PASS via `tests/proofs/lifecycle-event-duplicate.runtime.test.ts` and `tests/features/events/event-duplicate-planning.test.ts` (duplicate slice proven; see the completed note above), so the remaining work on this task is AC-099 / AC-223 / AC-612 plus AC-055 / AC-227 / AC-228 / AC-235. AC-234 is now PASS via `tests/proofs/lifecycle-event-tpp-fields.runtime.test.ts`. AC-235 is now PASS via the convert test in `tests/proofs/quote-conversion.runtime.test.ts` (see the completed note above), so the remaining work on this task is AC-099 / AC-223 / AC-612 (owner Confirmed/final) plus AC-055 / AC-227 / AC-228. AC-228 is now PASS via `tests/proofs/lifecycle-completion-correction.runtime.test.ts` (Event.correctCommercial at completed/closed_out with a required reason; see the completed note above), so the remaining work on this task is AC-099 / AC-223 / AC-612 (owner Confirmed/final) plus AC-055 / AC-227. AC-099 and AC-223 are now PASS via `tests/proofs/event-lifecycle-map.runtime.test.ts` (outcome map + no Confirmed stage; see the completed note above) — remaining work is AC-612 plus AC-055 / AC-227, and the box stays unchecked. AC-612 is now PASS via `tests/proofs/event-final-readiness.runtime.test.ts` and `tests/features/events/event-final-meaning.test.ts` (owner final-meaning slice; see the completed note above), so the remaining work on this task is AC-227 / AC-055, and the box stays unchecked. AC-227 owner decision filed as https://github.com/Angriff36/capsule/issues/397 — do not add venue/style gates on lockForSales until the owner answers. Remaining PL-LIFECYCLE work is still AC-227 / AC-055.

- [x] **P0 · PL-BOOKING — Make both booking paths converge and replay to the same Event.** Preserve the working transactional booking/menu cascade. The accepted-revision reference is stored and read, replay already returns the existing Event, public signature acceptance now runs the canonical operator path, and a quote-conversion retry links the reused draft to one canonical Event (AC-413, AC-434, AC-414, AC-435, AC-412, AC-433 PASS; see the completed-increment notes above — do not rebuild them). Still to prove on this task: the acceptance/booking projections stay in agreement, operator/signature/quote races cannot duplicate revisions, selections or effects, and venue mismatch stays visible — AC-410 (the broader booking-identity criterion). The AC-436 concurrent race slice is done — `tests/proofs/booking-race.runtime.test.ts` › "two concurrent createEventFromAcceptedProposal calls create exactly one event" plus the accept and revision tests in the same file prove overlapping Promise.allSettled owner.mutation calls land one live Event, one ProposalAccepted, and one proposalRevisions row with no product-code change — do not rebuild it. AC-006 is now PASS (2026-09-22 carry-over preview + J review — create-event lists the eight carried values and the linking reason before commit), and AC-398, AC-415, and AC-437 are PASS. The AC-100 field-staff booking-read proof is done — `tests/proofs/field-staff-booking-read.runtime.test.ts` proves event_staff reads guest count, style, venue, menu, timeline, and enhancement names and is denied proposal totals (getProposal null, listProposalByEventId [], getEventBookingDetails enhancement price null / canOpenProposal false for event_staff; sales_staff still reads total 1300 and Valet parking 400; cross-tenant getEventBookingDetails null) — do not rebuild it. Finding recorded, not fixed: Event.quotedPrice remains readable via getEvent to staffAccess (the AC-398 commercial seed); the AC-100 slice proves proposal totals are denied and does NOT hide Event.quotedPrice. The #393 unique-versus-ambiguous venue auto-select slice is done — `ProposalEventPrefill.matchVenue` returns unique / ambiguous / none and `EventCreatePage` auto-fills only a unique match, proven by `tests/features/events/proposal-event-prefill.test.ts` and `tests/event-create-behavior.test.ts` › "blocks venue auto-fill when two saved venues share the proposal's name and books the one the operator picks" — do not rebuild that proof. The AC-398 venue-display snapshot slice is also done — `eventVenueLabel` prefers Event.venueName and the Event detail header prints `venueLabel`, proven by `tests/features/events/event-venue-label.test.ts` and `tests/event-overview-behavior.test.ts` › "prints the booked venue snapshot in the header, not a later catalog rename" — do not rebuild it; AC-398 stays PENDING for the rest of BE-4.1. The AC-398 service-style snapshot slice is also done — Event stores `serviceStyleName`, `planEngagement` stamps it, `changeServiceStyle` preserves the stored snapshot when the name is omitted, and `eventServiceStyleLabel` is snapshot-first, proven by `tests/proofs/lifecycle-service-style-snapshot-stability.runtime.test.ts`, `tests/features/events/event-service-style-label.test.ts`, the mapper case in `tests/features/events/event-plan-engagement-form-mapper.test.ts`, and `tests/event-overview-behavior.test.ts` › "prints the booked service style snapshot in the details card, not a later catalog rename" — do not rebuild it; AC-398 stays PENDING for the rest of BE-4.1. The AC-398 occasion snapshot slice is also done — Event stores `occasionName`, `planEngagement` stamps it, and `eventOccasionLabel` is snapshot-first, proven by `tests/proofs/lifecycle-occasion-snapshot-stability.runtime.test.ts`, `tests/features/events/event-occasion-label.test.ts`, the mapper case in `tests/features/events/event-plan-engagement-form-mapper.test.ts`, and `tests/event-overview-behavior.test.ts` › "prints the booked occasion snapshot in the details card, not a later catalog rename" — do not rebuild it; AC-398 stays PENDING for the rest of BE-4.1. The AC-398 client-name snapshot slice is also done — Event stores `clientName`, `planEngagement` stamps it, and `eventClientLabel` is snapshot-first, proven by `tests/proofs/lifecycle-client-snapshot-stability.runtime.test.ts`, `tests/features/events/event-client-label.test.ts`, the mapper case in `tests/features/events/event-plan-engagement-form-mapper.test.ts`, and `tests/event-overview-behavior.test.ts` › "prints the booked client snapshot in the details card, not a later catalog rename" — do not rebuild it; AC-398 stays PENDING for menu/timing/readiness. The commercial-total-seed slice is also done — `eventCommercialQuotedPrice` never falls back to a live proposal total, proven by `tests/proofs/lifecycle-commercial-snapshot-stability.runtime.test.ts`, `tests/features/events/event-commercial-seed.test.ts`, and `tests/event-overview-behavior.test.ts` › "prints the booked commercial seed on the budget card, not a later proposal total" — do not rebuild it. The AC-398 menu dish-name snapshot slice is also done — EventDish stores `dishName`, the UI add path stamps it, and `eventDishLabel` is snapshot-first, proven by `tests/proofs/lifecycle-menu-snapshot-stability.runtime.test.ts`, `tests/features/events/event-dish-label.test.ts`, and `tests/event-menu-behavior.test.ts` › "prints the booked dish snapshot on the menu, not a later catalog rename" — do not rebuild it; AC-398 stays PENDING for timing/readiness. The AC-398 owner-name snapshot slice is also done — Event stores `ownerName`, `planEngagement` stamps it, `assignOwner` writes/clears/preserves it, a real `Person_correctIdentity` rename leaves it, and `eventOwnerLabel` is snapshot-first in the Assigned-owner rail card, proven by `tests/proofs/lifecycle-owner-snapshot-stability.runtime.test.ts`, `tests/features/events/event-owner-label.test.ts`, the mapper case in `tests/features/events/event-plan-engagement-form-mapper.test.ts`, and `tests/event-overview-behavior.test.ts` › "prints the booked owner snapshot in the assigned-owner card, not a later catalog rename" — do not rebuild it; AC-398 stays PENDING for readiness (AC-401, live projections). The AC-398 timing-input snapshot slice is also done — stored `Event.timing*` minutes and `serviceStartsAt` stay when a later `ServiceStyle_reviseDetails` renames the style, `Event_configureTiming` still writes a new snapshot, and `eventTimingStoredMinutes` returns stored finite minutes (including 0) and never the live `timingSuggestedSetupMinutes`, proven by `tests/proofs/lifecycle-timing-snapshot-stability.runtime.test.ts` and `tests/features/events/event-timing-inputs.test.ts` — do not rebuild it. The AC-398 source-evidence link slice is also done — the stored `Event.importSourceKey` is the source-evidence link (`Event_createViaCaptureDraft` writes it, an explicit `Event_updateImportDraft` rewrites it while omitted fields preserve, and a later `ServiceStyle_reviseDetails` catalog rename leaves it), `eventSourceEvidenceKey` never returns a later live source file name as the link, and the import panel prints the stored key, proven by `tests/proofs/lifecycle-source-evidence-link-stability.runtime.test.ts`, `tests/features/events/event-source-evidence-link.test.ts`, and `tests/event-overview-behavior.test.ts` › "prints the booked source-evidence key on the import draft, not a later live file name" — do not rebuild it; AC-398 stays PENDING for readiness (AC-401, live projections). Owners/reuse: `convex/lib/proposalEventCreation.ts; convex/quoteBuilder.ts; src/sales/proposal.manifest; src/sales/proposal-dish-selection.manifest`. Acceptance: AC-001, AC-002, AC-003, AC-004, AC-005, AC-006, AC-100, AC-398, AC-410, AC-411, AC-412, AC-413, AC-414, AC-415, AC-433, AC-434, AC-435, AC-436, AC-437. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs. AC-401 readiness projections now exist (see PL-READINESS). The AC-398 live readiness-display slice is done — `EventReadinessCard` prints the live `getEventReadiness` nine domains (reason · severity · resolvingAction) at the top of the Event overview rail, a later query result changes the card, and there is no Event readiness field, proven by `tests/features/events/event-readiness-summary.test.ts` (5) and `tests/event-overview-behavior.test.ts` › "prints the live nine-domain readiness summary, not a frozen Event readiness name" — do not rebuild it; AC-398 is PASS and BE-4.1 is fully proven. The AC-415 / AC-437 no-premature-effects slice is done — `tests/proofs/no-premature-effects.runtime.test.ts` (3) proves draft/send/view and accept/book create none of the invoice/message/provider/staff-notification/order/reservation/production rows, and approve after booking seeds one draft invoice — do not rebuild it; AC-415 and AC-437 are PASS. The PL-BOOKING box stays unchecked for the remaining AC-410 leg. AC-410 is now PASS via tests/proofs/booking-identity.runtime.test.ts; this box is checked.

- [x] **P0 · PL-COMMAND-REPLAY — Qualify canonical command identity and concurrent writes.** Use the same authenticated command contract for UI, external callers and M2M. Extend existing receipts and version handling for scoped replay, changed-input conflicts and privacy; investigate #281 without creating another command API. Multi-record invariants remain transactional. Owners/reuse: `api/manifest.ts; convex/lib/authContext.ts; convex/lib/materializationReceipt.ts; src/agent/`. Acceptance: AC-041, AC-397, AC-404. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs. 2026-09-22: AC-397 is now PASS via the gateway + CommandApiIdempotencyGate proofs (see Completed above) — do not rebuild that slice. 2026-09-22: the AC-404 identity formulas exist in `src/lib/workflowIdempotencyIdentity.ts` (do not rebuild them); remaining work was the AC-404 runtime reaction-replay proof — it is now done (see the Completed paragraph at the top). AC-404 is now PASS via tests/proofs/reaction-replay-identity.runtime.test.ts; this box is checked (AC-041, AC-397, AC-404).

- [x] **P0 · PL-PRICING — Make money and commercial projections agree exactly.** Preserve the central pricing path and immutable snapshots. Define deterministic smallest-unit rounding and supported currency behavior across preview, PDF, signature, acceptance, invoice, tax, allocation and reporting. Do not use display float rounding as proof of ledger correctness. Owners/reuse: `src/lib/pricing.ts; convex/lib/proposalPricing.ts; convex/lib/proposalRevision.ts; src/sales/invoice-core.manifest`. Acceptance: AC-089, AC-213, AC-263, AC-264, AC-265, AC-406, AC-418, AC-432. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs. AC-263 is now PASS via `tests/finance-precision-reconciliation.test.ts`; remaining legs are AC-213 / AC-264 / AC-265 / AC-406 / AC-418 / AC-432. AC-089 is now PASS via `tests/finance-precision-reconciliation.test.ts`. AC-213 is now PASS via `tests/finance-precision-reconciliation.test.ts`; remaining legs are AC-264 / AC-265 / AC-406 / AC-418 / AC-432. AC-406 is now PASS via `tests/quantity-money.test.ts` (with `tests/recorded-unit-mappings.test.ts`, the `tests/event-menu-cost.test.ts` mapping case, and `tests/proofs/menu-profitability-direct-ingredient.runtime.test.ts`); remaining legs are AC-264 / AC-265 / AC-418 / AC-432. AC-265 is now PASS via `tests/proofs/proposal-publish-blocks.runtime.test.ts`. AC-264 is now PASS via `tests/proofs/proposal-central-path-snapshot.runtime.test.ts`. AC-418 and AC-432 are now PASS via `tests/proofs/proposal-projection-agreement.runtime.test.ts`; no PL-PRICING legs remain. Do not re-do the five-bases / LedgerMoney / invoice-tax / AC-089 ledger slice, and do not re-do the commission / food-cost `CommercialMoney` slice.

- [ ] **P0 · PL-RECONCILIATION — Complete bounded Event-change reconciliation and receipts.** Retain working timing, staffing, inventory, purchasing, prep and cancellation callbacks. Complete missing triggers and receipts with source versions, per-domain counts/issues, replay checkpoints and preservation reasons. Unresolved data in one domain must remain scoped; do not introduce a second workflow engine. Owners/reuse: `convex/lib/operationalEvents.ts; convex/lib/materializationReceipt.ts; authored domain reconcilers`. Acceptance: AC-037, AC-191, AC-205, AC-388, AC-390, AC-407, AC-422, AC-423, AC-424. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs. AC-205 newly PASS via tests/proofs/cancellation-release.runtime.test.ts. AC-422 is now PASS via tests/proofs/canonical-change-events.runtime.test.ts. AC-423 is now PASS via tests/proofs/reconciliation-receipts.runtime.test.ts (timing-reconciler slice). AC-424 is now PASS via tests/proofs/reconciliation-failure-isolation.runtime.test.ts (Event.cancel timing-vs-purchasing isolation slice); remaining legs are AC-191 / AC-388 / AC-390 / AC-407. The AC-388 EventDish headcount-override slice is done via tests/proofs/override-survival.runtime.test.ts; AC-388 stays PENDING until the other override classes (pack quantity, purchasing buyer qty, staffing window) are proven. The AC-388 pack-quantity override slice is done via tests/proofs/pack-quantity-override-survival.runtime.test.ts. The AC-388 purchasing buyer-qty override slice is done via tests/proofs/buyer-qty-override-survival.runtime.test.ts. The AC-388 staffing-window override class is now proven via tests/proofs/staffing-window-override-survival.runtime.test.ts › "reschedule keeps a planTiming override and moves a following need" + "a second reschedule still leaves the override and moves the follower" (orchestrator re-ran 2/2). AC-388 stays PENDING — the remaining BE-1.3 work is auto-create/provenance of proposal draft, answers, documents, and invoice/closeout. The AC-390 schedule/staffing receipt slice is done via tests/proofs/single-reconciliation.runtime.test.ts; AC-390 stays PENDING for the rest of §1.5. The AC-390 headcount/menu receipt slice is done via tests/proofs/headcount-single-reconciliation.runtime.test.ts (Event.changeHeadcount persists exactly one EventHeadcountChanged menu receipt, override stays, following dish scales, replay is a no-op). The AC-390 headcount/pack receipt slice is done via tests/proofs/headcount-pack-reconciliation.runtime.test.ts (Event.changeHeadcount persists exactly one EventHeadcountChanged pack receipt, adjusted pack line stays 7, following container line scales 4→6, menu receipt still once, replay is a no-op). The AC-390 headcount/demand receipt slice is done via tests/proofs/headcount-demand-reconciliation.runtime.test.ts (Event.changeHeadcount persists exactly one EventHeadcountChanged demand receipt, override demand stays 25, following demand scales 40→60, menu receipt still once, replay is a no-op); The AC-390 headcount/prep receipt slice is done via tests/proofs/headcount-prep-reconciliation.runtime.test.ts (Event.changeHeadcount persists exactly one EventHeadcountChanged prep receipt, override prep stays 25, following prep scales 40→60, menu receipt still once, replay is a no-op). Spec finding: specs/capsule-complete-feature-spec.md §10.6 says mark prep for review when finalized inputs change, while specs/capsule-backend-end-state-spec.md §1.5 / §11.2 (the AC-390 source) updates unstarted generated prep in place — this slice follows the backend spec and the existing reconcileEventPrepWork path; the complete-feature spec stays planning reference, not rewritten here. The AC-390 headcount/staffing receipt slice is done via tests/proofs/headcount-staffing-reconciliation.runtime.test.ts (Event.changeHeadcount persists exactly one EventHeadcountChanged staffing receipt, captain planTiming override and following server need stay put, preservedCount 2 / updatedCount 0, menu receipt still once, replay is a no-op). Staffing does not scale with guest count — no ratio was invented (specs/ralph/production-09-workforce.md has no guest-count ratio; backend spec §12.2 only moves timing-following needs). The AC-390 headcount/proposal receipt slice is done via tests/proofs/headcount-proposal-reconciliation.runtime.test.ts (Event.changeHeadcount persists exactly one EventHeadcountChanged proposal receipt, the accepted proposal stays accepted / guestCount 40 / total 1300, unresolved is proposal_change_required, menu receipt still once, replay is a no-op). The signed document is not rewritten — no change-order command was invented. The AC-390 headcount/packet receipt slice is done via tests/proofs/headcount-packet-reconciliation.runtime.test.ts (Event.changeHeadcount persists exactly one EventHeadcountChanged packet receipt, the issued revision stays fingerprint seed-packet-40 / stage review / not superseded, unresolved is packet_stale, the issued historical packet is not rewritten — backend spec §14.1, menu receipt still once, replay is a no-op). The AC-390 recipe-quantity receipt slice is done via tests/proofs/recipe-quantity-reconciliation.runtime.test.ts (ComponentIngredient.adjustQuantity 1→2 persists exactly one ComponentIngredientQuantityAdjusted recipe receipt, Follow demand scales 40→80, Other demand stays 40, replay is a no-op). The AC-390 venue-change receipt slice is done via tests/proofs/venue-change-reconciliation.runtime.test.ts (Event.changeVenue persists exactly one EventVenueChanged venue receipt, Event.venueName Lakeside Pavilion, issued packet stays seed-packet-40 / not superseded, unresolved is packet_stale, replay is a no-op). The AC-390 rental-reschedule receipt slice is done via tests/proofs/rental-reschedule-reconciliation.runtime.test.ts (Event.reschedule persists exactly one EventScheduleChanged rental receipt, reserved hold moves, checked-out hold stays, replay is a no-op). The AC-390 cancellation-receipt slice is done via tests/proofs/cancellation-reconciliation.runtime.test.ts (Event.cancel persists exactly one EventCancelled cancellation receipt, reserved hold cancels, assigned crew unassigns, checked-out / checked-in stay, a second cancel is refused). The AC-390 style-receipt slice is done via tests/proofs/style-change-reconciliation.runtime.test.ts (Event.changeServiceStyle persists exactly one EventServiceStyleChanged style receipt, Limited Service snapshot, issued packet stays seed-packet-40 / not superseded, unresolved is packet_stale, replay is a no-op). AC-390 is now PASS — every named change type has a once-only receipt proof. Remaining PL-RECONCILIATION legs are AC-191 / AC-388 remainder / AC-407. The AC-407 submitted/ordered slice is done via tests/proofs/plan-vs-fact-matrix.runtime.test.ts (submitted 40 stays 40; new draft 20; replay is a no-op). The AC-407 partially-received slice is done via tests/proofs/plan-vs-fact-partial-receipt.runtime.test.ts (receive 15 of 40, headcount 40→60 keeps the receipt and adds one new draft at 20; replay of 60 is a no-op). The AC-407 completed-prep slice is done via tests/proofs/plan-vs-fact-completed-prep.runtime.test.ts (finish 40, headcount 40→60 keeps the finished row and opens one leftover pending at 20; replay of 60 is a no-op). The AC-407 consumed-stock slice is done via tests/proofs/plan-vs-fact-consumed-stock.runtime.test.ts (consume 40, headcount 40→60 keeps the used hold; replay of 60 is a no-op). The AC-407 custody slice is done via tests/proofs/plan-vs-fact-custody.runtime.test.ts (check out 1, headcount 40→60 keeps the custody hold; replay of 60 is a no-op). The AC-407 sent-invoice slice is done via tests/proofs/plan-vs-fact-sent-invoice.runtime.test.ts (approve+send the 4500 bill, headcount 40→60 keeps the sent invoice; replay of 60 is a no-op). The AC-407 finalized-closeout slice is done via tests/proofs/plan-vs-fact-finalized-closeout.runtime.test.ts (closeOut + recapture + finalize, then Event.correctCommercial 4000/6000 keeps the frozen snapshot; replay is a no-op). AC-407 is now PASS — every §6.5 row has a proof. Remaining PL-RECONCILIATION legs are AC-191 / AC-388 remainder.
- [x] **P0 · PL-READINESS — Expose domain readiness and safe resolving actions.** Replace incomplete boolean-only coverage with the nine required domain projections: issue code, affected IDs, severity, reason and permitted action. Readiness stays separate from lifecycle; optional unknowns remain informational and real harm gates only its affected action. Owners/reuse: `src/operations/event.manifest; authored event-domain read seams`. Acceptance: AC-401. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs. AC-401 newly PASS via tests/proofs/readiness-projections.runtime.test.ts (see the completed note above). Event overview UI not wired — remaining AC-398 display only.

- [ ] **P0 · PL-AUTH — Qualify tenant, actor, Person and command boundaries.** Preserve person-first workspaces and server-owned actor/tenant identity. Verify indexed reads, commands, background work, agent calls and cached state across supported roles and tenants. Do not add specialty read roles or organization-only onboarding restrictions. Owners/reuse: `convex/lib/authContext.ts; convex/lib/personAuthPick.ts; src/identity/; src/agent/`. Acceptance: AC-028, AC-151, AC-210, AC-212, AC-372, AC-403. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P0 · PL-STOCK-RESERVE — Enforce competing stock reservations transactionally.** Move any availability invariant currently enforced only by the client into the canonical transaction. Two Events competing for the last units produce one valid hold and an actionable line shortage, with substitute/transfer/purchase choices rather than freezing the Event. Owners/reuse: `src/inventory/stock.manifest; convex/lib/inventoryEvents.ts; event stock coordinator owners`. Acceptance: AC-080, AC-461, AC-462, AC-463, AC-464, AC-465, AC-466. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [x] **P0 · PL-VENUE-NOTES — Carry categorized venue notes with correct visibility.** Qualify author/time, Event link, category, pin/priority, archive and confidential visibility. Normal operational notes reach the crew while sensitive commercial notes stay out of client artifacts. Owners/reuse: `src/facilities/; src/features/facilities/`. Acceptance: AC-316, AC-317. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs. AC-316 is now PASS via `tests/proofs/venue-notes.runtime.test.ts` + helpers `tests/proofs/venue-notes.runtime.helpers.ts`; AC-317 is now PASS via `tests/proofs/venue-note-visibility.runtime.test.ts` › "a management_only note is refused to a non-management role and absent from every client-facing projection", so the PL-VENUE-NOTES box is checked.

- [ ] **P0 · PL-FILE-PRIVACY — Complete file parent authorization and revoked-share behavior.** Keep passing cross-tenant/parent ownership proofs. Cover upload association, metadata, download/export and expired/revoked share issuance end to end (AC-029 reopened); direct storage or attachment IDs never grant unrelated access. Owners/reuse: `convex/fileStorage.ts; convex/lib/blobs.ts; src/lib/fileStorageClient.ts; public share owners`. Acceptance: AC-029. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P0 · PL-PUBLIC-ACCESS — Give all scoped public links expiry and revocation semantics.** Preserve existing scoped token verification and proposal share lifecycle. Portal HMAC tokens currently lack expiry/revocation; complete that contract and verify private notes/cost/personnel isolation, changed revisions and stale cached documents across quote/proposal/payment surfaces. Owners/reuse: `convex/clientPortal.ts; convex/lib/clientPortalToken.ts; src/sales/share-link.manifest; public route owners`. Acceptance: AC-152. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P0 · PL-SESSION-ACCESS — Apply account and role changes to active sessions.** Qualify suspension/revocation/role changes on subscriptions and account-scoped offline caches. Keep supported personal and organization onboarding with correct branding; cross-account restoration and stale privileged state must be denied server-side without inventing blanket organization requirements. Owners/reuse: `src/app/ClaimGate.tsx; src/lib/useAuthStatus.ts; src/lib/offlineAuthSnapshot.ts; identity/device owners`. Acceptance: AC-150, AC-153. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-PROPOSAL-DRAFT — Generate and refresh sourced rich proposal drafts.** Extend the existing draft transaction into one retry-safe generation/refresh path from inquiry/Event facts. Cover every required section, supported visuals and customer choices, preserving authored wording, quantities, imagery, exclusions and override reasons. Missing facts and stale sections are explicit; publishing/sending stays an explicit action. Owners/reuse: `convex/lib/proposalDraft.ts; convex/lib/proposalRevision.ts; src/sales/; src/features/clients/`. Acceptance: AC-096, AC-259, AC-260, AC-261, AC-262, AC-377, AC-409, AC-416, AC-417, AC-431, AC-547. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-REVISION — Complete immutable publication, sharing and acceptance.** Keep existing publication snapshots and central renderers. Prove terms/version identity, expiry, revocation, supersession and signature/operator replay; historical acceptance carries source evidence without inventing a signature. Qualify desktop/mobile/PDF quality against DESIGN.md with real artifacts. Owners/reuse: `src/sales/proposal-revision.manifest; src/sales/share-link.manifest; src/sales/signature-request.manifest; convex/lib/proposalRevision.ts`. Acceptance: AC-039, AC-097, AC-098, AC-253, AC-254, AC-255, AC-256, AC-257, AC-258, AC-266, AC-267, AC-268. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-COMMERCIAL-CHANGE — Provide an audited post-acceptance commercial change path.** Accepted proposals currently freeze without a complete change-order path. Link a new commercial revision/correction to the accepted one and Event while preserving operational substitutions and quantities; never rewrite the signed agreement or require reopening the whole Event for a reasonable live correction. Owners/reuse: `src/sales/proposal.manifest; src/sales/proposal-revision.manifest; src/operations/event.manifest`. Acceptance: AC-101, AC-378, AC-420, AC-547. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-PUBLIC-MENU — Publish effective catalog prices and proven eligibility.** Use the existing catalog and pricing authority for public menu, quote, proposal and portal. Complete categories, package/season/effective-date/minimum/style eligibility, supported allergens and starting-at behavior; internal costs, margin and personnel remain private. Owners/reuse: `src/culinary/; convex/quoteBuilder.ts; src/features/clients/; src/lib/pricing.ts`. Acceptance: AC-064, AC-094, AC-199, AC-240, AC-241, AC-242, AC-332. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-QUOTE — Carry structured mobile quote selections into the sales workflow.** Preserve checkpointed conversion, deduplication, dismissal and working proposal deep links. Complete structured menu/package/enhancement/consent/attribution and estimate assumptions, plus the pipeline-to-queue leg and failure recovery; empty catalogs retain free-text facts. Owners/reuse: `convex/quoteBuilder.ts; src/features/clients/; tests/quote-review-behavior.test.ts; tests/lead-pipeline-behavior.test.ts`. Acceptance: AC-007, AC-008, AC-009, AC-010, AC-011, AC-018, AC-019, AC-095, AC-243, AC-244, AC-245, AC-246. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-CLIENT-PAYMENT — Finish scoped mobile client payment and portal results.** Retain existing staff payment links and confirmation sync. Wire the scoped client deposit/balance journey, actual invoice currency, pending/failed/succeeded states, authoritative balance refresh, lost-response recovery and callback replay without duplicate charges/allocations. Owners/reuse: `convex/clientPortal.ts; convex/invoicePayments.ts; src/lib/invoicePaymentActions.ts; src/features/clients/`. Acceptance: AC-102, AC-547. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-ROUTES — Obtain authoritative HQ-to-venue route facts.** Add tenant-selected operating locations, validated address/timezone facts and server-owned provider results with provenance, basis, traffic policy and staleness. Preserve unknown travel on failure. The labeled browser straight-line helper remains an estimate and must never drive authoritative schedule calculations. Owners/reuse: `src/operations/event.manifest; src/facilities/; convex/lib/eventTimingOperations.ts; authored route-provider seam (new if no owner exists)`. Acceptance: AC-382, AC-425, AC-426. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-TIMING — Complete route-backed timing rules and safe recalculation.** Extend existing backward/forward computations with safety buffer, briefing lead, configurable 180/90-minute setup defaults and source-based load rules. Cover all stale-input triggers and preserve reasoned overrides and last-known stale route evidence; materialize future milestones once. Owners/reuse: `src/operations/event.manifest; convex/lib/eventTimingOperations.ts; src/lib/eventTimingMilestones.ts`. Acceptance: AC-427, AC-428, AC-430. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-ROUTE-LEGS — Model separate vehicle and crew route windows.** Extend the single Event timing chain for multiple vehicles, split/venue-direct crews, outside vendors and trips. Derive each route, dispatch and staff window from the same canonical milestones with capacity/overlap constraints and source explanations. Owners/reuse: `src/logistics/; convex/lib/vehicleDeliveryAvailability.ts; convex/lib/eventTimingOperations.ts`. Acceptance: AC-429. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-SCHEDULE-CORRECTION — Preserve communicated schedules during timing changes.** Keep manual shift windows and existing weekly notices. Draft following shifts may reconcile; published or acknowledged call times need a visible proposed delta, explicit correction and durable notification/acknowledgment rather than silent movement. Owners/reuse: `convex/lib/eventStaffingOperations.ts; convex/lib/shiftSchedulingEvents.ts; src/workforce/shift.manifest`. Acceptance: AC-326, AC-430, AC-508. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-RECIPE-IMPORT — Extend durable recipe review to nested formulas.** Preserve the proven flat paste/CSV saved-review and atomic-finalization path. Add nested formulas, clear ingredient/subrecipe/instruction classification, correction and normal recipe-book use with raw source still accessible. Missing/cyclic formulas affect only their recipe. Owners/reuse: `src/features/kitchen/import/ComponentImportPage.tsx; convex/lib/culinaryOperations.ts; src/lib/tppRecipeRepair.ts; src/lib/safeCulinaryOperations.ts`. Acceptance: AC-036, AC-043, AC-044, AC-045, AC-066, AC-069, AC-073, AC-440. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-FORMULA-IDENTITY — Reconcile repeated and changed source formulas.** Deduplicate repeated category exports and identical scaled formulas by source identity/fingerprint without merging distinct same-name recipes. Preserve original serving basis and present differing formulas as explicit revisions for resolution. Owners/reuse: `src/lib/tppMenuCsv.ts; src/lib/tppRecipeRepair.ts; src/culinary/; convex/lib/culinaryOperations.ts`. Acceptance: AC-067. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-UNITS — Preserve dimensional quantities and unknown conversions.** Reuse exact ratios and measurement correction. Qualify mass/volume/count/batch/yield/waste/purchase-pack semantics, tiny values, fractions and fluid-versus-mass ounces. Nutrition density/display estimates are not stock/purchase conversion authority. Owners/reuse: `src/lib/recipeUnitConversion.ts; src/lib/componentBatchScale.ts; convex/lib/culinaryModel/units.ts; src/culinary/`. Acceptance: AC-042, AC-068, AC-441. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-DEMAND — Reconcile versioned culinary demand without duplicating work.** Preserve the existing demand engine and follow-versus-fixed servings. Complete eligible formula-change propagation, stable source contributions, substitutions/86 and overrides; retain completed work and historical versions while recalculating only remaining demand once. Owners/reuse: `convex/lib/culinaryModel/; src/procurement/event-purchasing.manifest; src/lib/culinaryDemandClient.ts`. Acceptance: AC-070, AC-072, AC-333, AC-379, AC-442, AC-443, AC-444, AC-445, AC-446, AC-447, AC-448, AC-450, AC-451, AC-459. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-FOOD-COST — Version ingredient costs and report honest coverage.** Retain known-cost plus missing-cost displays. Complete units, effective dates and source provenance, historical costing snapshots and event actual-versus-plan reconciliation. Zero, absent, unmatched and estimated costs have distinct meanings and cannot imply a complete margin. Owners/reuse: `src/culinary/; convex/lib/ingredientLookupApplyCost.ts; src/lib/culinaryDemandClient.ts`. Acceptance: AC-071, AC-334, AC-449, AC-454. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-NUTRITION — Qualify allergen and nutrition evidence through the kitchen catalog.** Preserve evidence-backed unknowns and the rich recipe/catalog operating job: methods, stations, media, portions and supported nutrition. Do not infer allergen-free from absence or count-unit weights from volume. Review customer/crew wording on actual artifacts. Owners/reuse: `src/lib/catalogUnitGrams.ts; src/lib/nutritionUnitScale.ts; src/culinary/; src/features/kitchen/`. Acceptance: AC-074. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-PREP — Preserve prep baselines, overrides and partial work.** Reuse existing remaining-work balancing and exact EventDish identity. Complete generated source/version baseline, manual adjustments, dependencies and safe retirement; never regenerate completed prep or treat unknown duration as a measured time. Owners/reuse: `convex/lib/culinaryOperations.ts; src/lib/prepWorkBalance.ts; src/lib/eventPrepGroups.ts; src/production/`. Acceptance: AC-337, AC-484, AC-485, AC-486, AC-487, AC-488, AC-489, AC-490, AC-492. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-BATCH — Record batch yield, quality and waste as actuals.** Preserve operator-entered zero yield and historical provenance distinctions. Complete batch/quality/waste actors, reasons, quantities, unit/location/Event attribution and partial correction paths; observed physical work is never inferred from a generated plan. Owners/reuse: `src/production/; src/features/kitchen/; tests/proofs/`. Acceptance: AC-032, AC-335, AC-458, AC-491. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-OPENING-STOCK — Establish reviewed opening stock with source and as-of basis.** Separate ingredients, reusable equipment, disposables, components and instructions. Preserve quantity, location, unit, timestamp and uncertainty; review incompatible/conflicting/unverified counts per record. The owner selects the cutover opening basis after the reconciliation is concrete. Owners/reuse: `src/inventory/; src/import/; convex/importCommit.ts`. Acceptance: AC-075, AC-076. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-STOCK-LEDGER — Reconcile all stock movements through one calculation.** Keep real receipt/issue/transfer/waste paths. Prove opening, receipt, adjustment, reservation, consumption, return and waste reconcile exactly once with traceable reasons and correct available stock; no independent UI arithmetic becomes authority. Owners/reuse: `src/inventory/stock.manifest; convex/lib/inventoryEvents.ts; src/lib/inventoryAuditClient.ts`. Acceptance: AC-077, AC-336, AC-460. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-WEEKLY-PURCHASING — Preserve contributions and buyer changes in shared weekly drafts.** Keep the existing shared-week draft and contribution model. Prove concurrent reconcilers, old/new week moves, removal/cancellation and manual adjustments converge. Submitted orders remain committed and receive explicit deltas/surplus actions; recipe use alone does not create needless production. Owners/reuse: `src/procurement/event-purchasing.manifest; convex/lib/purchasingEvents.ts; tests/proofs/event-weekly-purchasing.runtime.test.ts`. Acceptance: AC-078, AC-467, AC-468, AC-469, AC-470, AC-471, AC-472, AC-473, AC-474. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-RECEIVING — Correct partial receipts without rewriting history.** Connect partial receipt to stock and outstanding order balance exactly once. Corrections retain the receipt and apply a reasoned difference with actual vendor/location/unit provenance, including already consumed quantities. Owners/reuse: `src/procurement/; src/inventory/; convex/lib/purchasingEvents.ts`. Acceptance: AC-079, AC-475, AC-476, AC-477, AC-478. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-PURCHASE-UNITS — Expose purchasing units and explicit package rounding.** Retain fractional need and record compatible conversion, package size, ordered quantity and remainder. New location/vendor choices preserve the in-progress form. Draft import/recalculation never submits an order or contacts a vendor. Owners/reuse: `src/procurement/; src/lib/recipeUnitConversion.ts; src/features/inventory/`. Acceptance: AC-082, AC-083, AC-483. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-PEOPLE — Complete one-Person worker identity lifecycle.** Preserve existing hire, revoke, sign-in and email-correction flows. Complete suspension/reinstatement and conflicting login recovery without changing another identity or deleting historical assignments, time or payroll. External identity steps need replay-safe compensation. Owners/reuse: `convex/lib/personAuthPick.ts; src/lib/hiringPipeline.ts; src/features/admin/; src/workforce/`. Acceptance: AC-122, AC-123, AC-149, AC-328. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-STAFFING — Derive coverage and qualify assignment conflicts.** Reuse staffing needs and transactional scheduling. Complete template-based coverage, availability/time-off/overlap/qualification-expiry explanations and safe following windows. Optional qualifications must not block unrelated work; no automatic unqualified assignment. Owners/reuse: `convex/lib/eventStaffingOperations.ts; convex/workforceScheduling.ts; src/lib/workforceScheduling.ts; src/lib/timeOff.ts`. Acceptance: AC-124, AC-324, AC-325. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-STAFF-SELF-SERVICE — Complete native schedule-to-work self-service.** Preserve My Day, availability and swaps. Qualify offers, claim, acknowledgment, decline, waitlist, replacement and swap with one Person identity, latest local-time/location/instructions and operational-only visibility; vendor workers follow the same identity model. Owners/reuse: `src/workforce/; src/lib/staffShiftSwaps.ts; src/lib/eventDayBriefing.ts; src/features/staff/`. Acceptance: AC-125, AC-331, AC-383, AC-493, AC-494, AC-495, AC-496, AC-497, AC-498, AC-499, AC-500, AC-501, AC-503, AC-504, AC-505, AC-506, AC-507, AC-511, AC-512, AC-513, AC-514, AC-515, AC-516, AC-517, AC-518. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-TIME — Make time and break capture attributable and correct.** Qualify real clock-in/out, paid breaks/unpaid lunch, overnight and DST elapsed time, shift/Event inheritance and manager corrections with actor/reason/timezone. Stable operations must not create duplicate punches; do not substitute scheduled time for actual time. Owners/reuse: `src/workforce/time.manifest; src/features/staff/; src/lib/eventScopedQueries.ts`. Acceptance: AC-126, AC-502, AC-509. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-OFFLINE-TIME — Reconcile pending time and operational changes after connection loss.** Preserve last-confirmed reads and per-account isolation. Persist the original mutation identity across refresh and reconcile uncertain server success before retry; show pending/conflict versus confirmed state honestly on time, packing, load and return workflows. Owners/reuse: `src/features/staff/offlineStore.ts; src/lib/offlineAuthSnapshot.ts; src/lib/offlineAuthContext.tsx; src/lib/pendingOperationKey.ts`. Acceptance: AC-127, AC-139. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-PAYROLL — Derive payroll input and reconcile exported corrections.** Preserve current CSV profiles and actual-time preview. Derive approved payable minutes and effective terms, expose missing/disputed/overlapping inputs, and persist per-worker/period export/acknowledgment/correction evidence. Live Nowsta sync remains a separate provider requirement. Owners/reuse: `src/finance/payroll-input.manifest; src/features/finance/payrollExport.ts; src/features/finance/PayrollPreparePayloadBuilder.ts`. Acceptance: AC-128, AC-130, AC-510, AC-519, AC-627. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-PACK-RULES — Generate explainable pack lines from all Event facts.** Extend existing dish/style/template rules for servings, production notes, venue/setup/weather, rentals/decor, beverages/bar and standard kits. Retain compatible source contributions and manual baselines/overrides/exclusions; a required excluded capability needs a real replacement only at its relevant readiness check. Owners/reuse: `src/logistics/pack-list.manifest; convex/lib/operationalEvents.ts; src/lib/safeMaterialization.ts`. Acceptance: AC-034, AC-132, AC-340, AC-380, AC-520, AC-521, AC-522, AC-523, AC-524, AC-525, AC-527, AC-528, AC-529, AC-530, AC-531, AC-532, AC-533, AC-534, AC-536, AC-537, AC-538, AC-539. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-PACK-VIEWS — Project one pack-line set into four working views.** Reference, warehouse-category, vehicle/load and return views must share line IDs, quantities and actuals. Preserve source explanations and ownership/return rules; updating a quantity or packing in any view immediately agrees in all other views. Owners/reuse: `src/logistics/; src/lib/packingDisplay.ts; src/features/logistics/`. Acceptance: AC-229, AC-230, AC-231, AC-232, AC-233, AC-371, AC-381, AC-540. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-PACK-ACTUALS — Record partial packing and physical custody truthfully.** Preserve atomic template application and legal early packing. Complete actual counts, scans, substitutes, missing items, actor/time and custody changes with safe retry and server acknowledgment. No generated requirement may imply something was packed, loaded or checked. Owners/reuse: `src/logistics/; src/features/logistics/; src/lib/safeMaterialization.ts`. Acceptance: AC-134. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-ASSET-CATALOG — Complete owned, rental and client-provided asset responsibility.** Reuse Equipment instead of food stock. Cover serialized/bulk assets, bundles, pricing/cost, photos, locations, sub-rental need and client-provided responsibility, so sourcing and return obligations remain explicit through proposal, pack and finance. Owners/reuse: `src/facilities/equipment.manifest; src/logistics/; src/features/facilities/`. Acceptance: AC-131, AC-339, AC-526, AC-545, AC-548. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-ASSET-AVAILABILITY — Honor equipment condition and concurrent availability.** Keep transactional overlap checks; add missing maintenance/out-of-service condition semantics and verify two competing Events. Show conflicting window/quantity/location/condition and usable replacement, transfer or rental choices. Preserve event-owned reservation cleanup separately from existing staffing cleanup. Owners/reuse: `convex/equipmentCheckout.ts; convex/lib/equipmentReservationAvailability.ts; src/facilities/equipment.manifest`. Acceptance: AC-133, AC-341, AC-419, AC-541. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-RETURNS — Reconcile returns, damage and maintenance obligations.** Record expected/received/outstanding amounts and location/condition/loss/damage/cleaning with actor/time and billing responsibility. Maintenance removes future availability until restored; cancellation never erases dispatched items, vendor obligations or missing returns. Owners/reuse: `src/logistics/; src/facilities/; convex/lib/eventCancellation.ts`. Acceptance: AC-136, AC-137, AC-342, AC-543, AC-544, AC-546, AC-549, AC-551. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-DELIVERY — Qualify loading, dispatch, delivery and vehicle constraints.** Keep existing delivery records and vehicle allocation. Complete actual load/destination/person/time, capacity/overlap, acknowledgment/incidents and one movement per operation. Changes to dispatched work become explicit corrections rather than automatic rewrites. Owners/reuse: `src/logistics/; convex/lib/vehicleDeliveryAvailability.ts; convex/driverAssignment.ts`. Acceptance: AC-135, AC-535, AC-542, AC-550. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-FINAL-LOCK — Derive every office Final Lock answer from evidence.** Implement deterministic versioned policy for all fifteen office question groups with answered/not_applicable/unresolved/field_confirmation, sources, explanation, rule version, authorized override and responsible resolver. Missing facts produce exact scoped issues; model output cannot invent an authoritative answer. Owners/reuse: `src/operations/event.manifest; src/operations/event-packet.manifest; src/lib/eventPacket/; convex/lib/eventPacket/`. Acceptance: AC-384, AC-571, AC-572, AC-573, AC-574, AC-576, AC-577, AC-578, AC-579, AC-580, AC-581, AC-583, AC-584, AC-585, AC-586, AC-587, AC-588, AC-589, AC-590, AC-591, AC-592, AC-593, AC-594, AC-595, AC-596, AC-597, AC-608, AC-609, AC-611, AC-614, AC-616. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-FIELD-CONFIRMATION — Assign physical confirmations without pre-completing them.** Keep all ten field forms as named-person physical work, including two-person before-takeoff and arrival checks. Record actual actor/time/evidence and escalation. Office readiness and successful PDF generation cannot mark physical observations complete. Owners/reuse: `src/operations/event-packet.manifest; src/lib/eventPacket/; convex/lib/eventPacket/`. Acceptance: AC-389, AC-582, AC-598, AC-599, AC-600, AC-601, AC-602, AC-603, AC-604, AC-605, AC-606, AC-607, AC-610. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-PACKET — Generate and version the complete native Event packet.** Use native records as the primary source while preserving uploaded evidence. Complete all eight ordered sections, route/map/setup diagram/rental/staff content, binder instructions, versioned regeneration and stale sections. Printing is optional; readiness means complete native content, not merely an existing PDF. Owners/reuse: `src/lib/eventPacket/prepareNativeWorkbook.ts; src/lib/eventPacket/renderWorkbook.ts; convex/lib/eventPacket/; src/operations/event-packet.manifest`. Acceptance: AC-385, AC-557, AC-558, AC-559, AC-560, AC-561, AC-562, AC-563, AC-564, AC-565, AC-566, AC-567, AC-568, AC-569, AC-570, AC-613, AC-615, AC-617. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-PACKET-EVIDENCE — Retain source observations and canonical reconciliation.** Keep original documents, observations, contradictions and decisions. Resolve into canonical records with actor/provenance, then regenerate affected projections; never treat an imported workbook or free-text answer as a second operational source of truth. Owners/reuse: `src/lib/eventPacket/extractObservations.ts; src/lib/eventPacket/reconcile.ts; src/lib/eventPacket/resolveIssue.ts; convex/lib/eventPacket/reconcileNative.ts`. Acceptance: AC-555, AC-556, AC-575. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-INVOICE-NUMBER — Prove one sourced draft invoice and safe number allocation.** Preserve the transactional number sequence and unsent draft on approval. Qualify concurrent/repeated approval/import/closeout paths, billable-source linkage and historical numbers; Invoice.issue naming does not mean the draft was delivered. No duplicate placeholder or auto-send. Owners/reuse: `src/sales/invoice.manifest; src/sales/invoice-core.manifest; convex/lib/invoiceNumbering.ts; convex/lib/invoiceNumberSequence.ts`. Acceptance: AC-087, AC-182, AC-618. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-FINANCIAL-CLASSIFICATION — Classify source money before ledger effects.** Distinguish quote, invoice, payment, allocation, fee, gratuity, refund/return, credit, balance and aggregate report. Keep every row accounted for and prevent overlapping views from posting the same principal twice. Owners/reuse: `src/import/; convex/importCommit.ts; src/lib/tppReports/`. Acceptance: AC-084. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-FINANCIAL-REFERENCE — Preserve complete reference-only financial history.** Keep positive, zero and negative values with source/date/type/evidence. Reference mode excludes ledger totals and cannot trigger payments, provider invoices, reminders or old tasks; current parsers that drop zero/negative rows need explicit correction. Owners/reuse: `convex/importCommit.ts; src/lib/tppReports/reportValues.ts; src/features/admin/import/`. Acceptance: AC-085, AC-091. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-FINANCIAL-RECONSTRUCTION — Preview and reconcile explicit historical ledger reconstruction.** Keep reference mode distinct. Preview source numbers, currency, lines, taxes, charges, deposits, credits, effective dates and unpaid balances; incomplete detail stays unresolved. Apply reviewed canonical records without invented taxable lines or unintended external actions. Owners/reuse: `src/import/; src/sales/invoice-core.manifest; src/sales/payment.manifest; src/features/admin/import/`. Acceptance: AC-086. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-ACCOUNTING — Preserve distinct payment, fee, allocation and correction semantics.** Extend current payment lifecycle for partial allocations, overpayment, fees, refunds, chargebacks/ACH returns, credits and gratuity with original cause/effective date. Exact provider IDs reconcile; similarity only suggests. A transaction is never allocated twice. Owners/reuse: `src/sales/payment.manifest; src/sales/invoice-core.manifest; convex/invoicePayments.ts`. Acceptance: AC-088, AC-090, AC-092, AC-283, AC-284, AC-619, AC-620, AC-621, AC-622, AC-623, AC-624. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-FINANCE-RECONCILIATION — Make financial discrepancies inspectable by source and period.** Save reconciliation evidence by period/status/currency, including excluded duplicates, source totals, ledger totals and mismatch drill-down. Preserve actual-versus-import date and do not claim complete profit when cost coverage is incomplete. Owners/reuse: `src/features/finance/; src/features/admin/import/; src/features/reports/`. Acceptance: AC-093, AC-183. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-CLOSEOUT — Compute and freeze source-backed Event actuals.** Replace manual zero-seed completion with a canonical coverage-aware projection of revenue, food, waste, labor, vendor/rental/transport/damage, commission and headcount. Reuse known values without retyping; finalize source IDs/versions and add an audited correction result that preserves prior snapshots. Owners/reuse: `src/finance/event-closeout.manifest; src/features/finance/CloseoutBillingTruth.tsx; authored closeout projection seam`. Acceptance: AC-386, AC-551, AC-625, AC-626, AC-628. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-ARCHIVE — Expose complete archive accounting through ordinary intake.** Preserve the passing 90-versus-70 inventory/disposition proofs. Connect those stages to product intake, account for every workbook/section/row including unsupported/reference content, and enforce zero unaccounted records without implying every row became a native record. Owners/reuse: `convex/archiveInventory.ts; convex/archiveDisposition.ts; src/import/; src/features/admin/import/`. Acceptance: AC-020, AC-021, AC-027, AC-177, AC-178, AC-629. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-PROVENANCE — Keep source bytes and typed interpretation inspectable.** Reuse typed XLSX and provenance readers. Qualify original/checksum/parser/coordinates/raw/normalized/date-system/timezone evidence from the ordinary authorized workflow; bounded archive failures, formulas/macros, sparse cells and missing cached values remain explicit. Owners/reuse: `convex/archiveProvenance.ts; src/lib/tppReports/xlsxReader.ts; src/lib/tppReports/workbookProvenance.ts; src/lib/sourceProvenance.ts`. Acceptance: AC-022, AC-025, AC-026, AC-271. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-SOURCE-DELTA — Apply source revisions as reviewed deltas.** Preserve checksum-based duplicate detection. Complete changed-source versus native-edit ownership and replay across worker restarts/devices without second business copies; same source revision and changed source revision must have distinguishable outcomes. Owners/reuse: `src/lib/tppReports/archiveDelta.ts; convex/archiveInventory.ts; convex/importCommit.ts`. Acceptance: AC-023, AC-272, AC-273. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-IMPORT-RESUME — Finish durable import checkpoints through attachments.** Retain proven parent/child/deposit recovery and add attachment/imported-history coverage. Every checkpoint survives lost acknowledgment, browser closure and worker restart; resume creates only missing owned work and never replaces later user edits (AC-024 reopened). Owners/reuse: `convex/importCommit.ts; src/import/import-run.manifest; src/import/import-dataset.manifest; tests/proofs/import-resume-fault-injection.runtime.test.ts`. Acceptance: AC-024, AC-269, AC-631. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-IMPORT-CANCEL — Cancel and compensate only eligible run-owned work.** Stop unstarted work, preserve checkpoints and report compensation progress. Remove or reverse only unchanged records created by this run; later edits, shared records, committed effects and unresolved corrections remain visible. Owners/reuse: `convex/importCommit.ts; src/import/import-run.manifest; durable import scheduling owners`. Acceptance: AC-056. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-SOURCE-DATASETS — Complete the full mapped dataset and field coverage.** Keep all existing event/contact/venue/menu/pack/payment importers. Add missing company/contact history, staff/address, catalog/package/season/effective-price, reference, equipment and supplemental fields from the established maps; every field is normalized, preserved or explicitly unresolved. Owners/reuse: `src/import/; convex/importCommit.ts; src/lib/tppReports/; existing migration/data-map documents`. Acceptance: AC-057, AC-063, AC-274, AC-275, AC-276, AC-277, AC-278, AC-279, AC-280, AC-281, AC-282, AC-374. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-SOURCE-IDENTITY — Use stable source identity without coercing ambiguous people.** Exact source IDs can match automatically. Preserve single names, companies without contacts, international address/phone text and original spelling. Same-name/shared-email/renamed/truncated variants become per-record decisions rather than global confidence merges or fake defaults. Owners/reuse: `src/import/external-record-link.manifest; convex/importCommit.ts; src/lib/tppReports/reportValues.ts`. Acceptance: AC-058, AC-060, AC-179, AC-180, AC-181, AC-270, AC-408, AC-630. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-SOURCE-RESOLUTION — Persist reviewed source mapping and preview bulk effects.** Expose choose-existing/create-correct-type/relink beside source and result. Reuse the stable resolution after reload/replay and show affected records before bulk application; imported and native records use the same ordinary edit/search/archive/permission paths. Owners/reuse: `src/features/admin/import/ExternalRecordsReconcilePage.tsx; src/import/; convex/importCommit.ts`. Acceptance: AC-059, AC-065. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-SOURCE-MERGE — Make merge and relink preserve dependent history.** Preserve source links and native field ownership while moving dependent records. Complete atomic or checkpointed correction with progress and failed-retry recovery; no orphaned communication, event, invoice or payroll evidence. Owners/reuse: `src/import/; src/sales/; convex/importCommit.ts; authored client-merge seam`. Acceptance: AC-061. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-SOURCE-HISTORY — Make imported communications and tasks normal readable history.** Materialize original timestamps/completion/source under the actual client/Event for search and navigation. Attachments alone do not meet the history workflow; importing never sends messages, reopens completed tasks or schedules anniversaries/reminders implicitly. Owners/reuse: `src/import/; src/features/clients/; src/features/workforce/; inbox/task owners`. Acceptance: AC-062, AC-111. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-DELIVERY-RECOVERY — Close ambiguous external-success and retry windows.** Use stable operation IDs and provider reconciliation after remote success/lost receipt (#383). Bound attempts/backoff, retain uncertain/dead-letter state and scoped retry. Deliberate new reminders stay possible; provider failures do not roll back unrelated business edits. Owners/reuse: `convex/qboSync.ts; convex/googleCalendar.ts; convex/invoiceReminders.ts; durable delivery owners`. Acceptance: AC-108, AC-118, AC-119, AC-163, AC-206, AC-207, AC-208, AC-346, AC-347, AC-348, AC-361, AC-373, AC-634. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-AUDIT — Make consequential command and delivery evidence observable.** Reuse the event ledger and receipts for tenant/subject/Person/command/entity/time/operation/version attribution. Observe failures, queue age and preserved/unresolved work without leaking tokens/payment details or hand-editing generated mutations. Owners/reuse: `convex/lib/operationalEvents.ts; convex/lib/materializationReceipt.ts; supported audit sink and provider delivery owners`. Acceptance: AC-154, AC-193, AC-209, AC-635, AC-636. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-UI-CONTRACT — Document and qualify existing read/action/error/refresh contracts.** Map all ten screen areas to exact current functions, input sources, permissions/states, idempotency/version requirements, result IDs, effects, errors and refresh. Preserve generated envelopes; add source/rule/override/why/staleness explanations. Backend repairs use minimal typed adapters, not redesign. Owners/reuse: `src/lib/api.ts; src/lib/convexActionErrorMessage.ts; src/lib/publicErrorMessage.ts; owning authored read and command seams`. Acceptance: AC-031, AC-186, AC-187, AC-188, AC-192, AC-194, AC-195, AC-196, AC-197, AC-198, AC-200, AC-201, AC-202, AC-203, AC-204, AC-211, AC-360, AC-367, AC-368, AC-369, AC-375, AC-392, AC-393, AC-394, AC-402, AC-421, AC-438, AC-553, AC-554, AC-637, AC-638, AC-639, AC-640, AC-641, AC-642, AC-643, AC-644, AC-645, AC-646, AC-647, AC-648, AC-649, AC-650, AC-651, AC-652, AC-705. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-CATALOGS — Complete empty-catalog recovery and reactive selectors.** Preserve working register/relabel/retire/reactivate and free-text quote fallback. Prove Event selectors update from admin changes and missing dependent catalogs can be created without losing the form; retired values remain readable on existing records. Owners/reuse: `src/features/admin/CatalogsSection.tsx; src/features/events/EventCreateServiceStyleField.tsx; src/lib/eventCreateCatalogClient.ts`. Acceptance: AC-012, AC-013, AC-014, AC-015, AC-016, AC-138, AC-218, AC-219, AC-220, AC-221, AC-222. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-FIELD-REGRESSIONS — Qualify the remaining recorded field defects.** Refresh issue states and reproduce behavior before changes. Cover actionable pack-row errors, reserve explanations, copy metadata/tags, shift/Event inheritance, real date typing/blur and compatible stock units. Existing fixes need behavioral proof, not reinstated source-string tests. Owners/reuse: `specs/ralph/known-bugs-142-to-151.md; owning pack/equipment/menu/time/date/stock features`. Acceptance: AC-047, AC-048, AC-049, AC-050, AC-051, AC-052, AC-053. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-CANCELLATION-PROOF — Prove cancellation across all mutable and committed states.** Exercise all nine cancellation cases across the six allowed lifecycle stages, preserving submitted/received/consumed/completed/dispatched/paid facts. Inventory and staffing cleanup already run; extend their proof and close demonstrated gaps, especially equipment reservations. Owners/reuse: `tests/proofs/; convex/lib/eventCancellation.ts; convex/lib/inventoryEvents.ts; domain reconcilers`. Acceptance: AC-081, AC-479, AC-480, AC-481, AC-482, AC-675, AC-676, AC-677, AC-678, AC-679, AC-680, AC-681, AC-682, AC-683. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-ROLE-PROOF — Qualify the complete thirteen-role access matrix.** Exercise allowed and denied representative operations for every specified role, M2M, foreign tenant, unlinked and anonymous caller, plus storage/public/cache paths. Prove useful ordinary work succeeds as carefully as private access fails. Owners/reuse: `tests/proofs/; canonical command/read/auth seams`. Acceptance: AC-156, AC-357, AC-684, AC-685, AC-686, AC-687, AC-688, AC-689, AC-690, AC-691, AC-692, AC-693, AC-694, AC-695, AC-696. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-RACE-PROOF — Prove cross-domain races and recovery checkpoints.** Exercise all eight concurrency/recovery cases with genuine transaction/failure boundaries: last stock, booking, weekly order, parent/child crash, callback replay/order, import resume, stale UI and deterministic regen. Keep confirmed results on lost responses without weakening assertions. Owners/reuse: `tests/proofs/; existing materialization and import/runtime fixtures`. Acceptance: AC-173, AC-358, AC-405, AC-697, AC-698, AC-699, AC-700, AC-701, AC-702, AC-703, AC-704. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-LOOP-COMPLETION — Reject false worker success and stale completion claims.** A provider 429 with no task result currently exits zero (#381). Require usable completion evidence, separate checks/push/integration/deployment/authentication states, invalidate all-complete after spec changes and diagnose bounded nonconvergence. Owners/reuse: `ralph-worker.ps1; loop.sh; acceptance/coverage consumers`. Acceptance: AC-160. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-RELEASE-RECEIPT — Bind release evidence to the actual frontend and backend.** Keep the approved self-hosted release orchestrator. Replace equal-command-count/backend-cloud assumptions with exact deployed code/schema identity and a real authenticated product workflow (#382, AC-030 reopened). Missing evidence remains PARTIAL; execute production legs only after release authorization. Owners/reuse: `src/lib/releaseReceipt.ts; scripts/release-receipt.ts; scripts/deploy-production.sh`. Acceptance: AC-030, AC-161. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-CULINARY-CATALOG — Complete reusable food and recipe operating facts.** Preserve existing ComponentStep methods, DishStep station fields and dish/ingredient image storage. Complete demonstrated gaps in shelf-life/storage, recipe media and approved substitution context, equipment, plating, cooling/holding/reheat standards. Keep catalogs useful to cooks while incomplete cost or nutrition remains explicitly unknown. Owners/reuse: `src/culinary/component.manifest; src/culinary/dish.manifest; src/culinary/ingredient.manifest`. Acceptance: AC-452, AC-453, AC-457. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-PRODUCTION-PLAN — Consolidate daily and make-ahead kitchen production.** Project the same Event demand/prep records by day, Event, station, recipe and batch, with make-ahead windows and dependencies. Preserve actual output and per-Event source identity while pooling compatible work; do not create a separate prep database. Owners/reuse: `src/production/; convex/lib/culinaryModel/; existing kitchen planning queries`. Acceptance: AC-455. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P1 · PL-VENDOR-INVOICE-MATCH — Connect vendor bills to orders, receipts and actual costs.** Add the missing vendor-invoice matching leg to the existing demand→order→receipt chain. Preserve order/receipt/bill identities and quantities/prices; expose partial/disputed differences with reviewed correction and closeout provenance, without reposting source-reference money. Owners/reuse: `src/procurement/; src/finance/; existing receipt and reconciliation seams`. Acceptance: AC-456. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P2 · PL-HR — Complete restricted operational people follow-up.** Qualify role scorecard competencies/weights, hiring/interviews, event feedback, performance follow-up and monthly one-on-ones using current records and routes. Limit sensitive personnel notes by the intended policy without adding a general HRIS or hiding operational information from crew. Owners/reuse: `src/workforce/; src/lib/staffSelfReviews.ts; src/features/workforce/`. Acceptance: AC-129, AC-327, AC-329, AC-330. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P2 · PL-CUTOVER — Reconcile parallel operation and qualify each cutover.** Complete daily identity-based comparison, assignable discrepancies, final delta, opening-stock timestamp, financial mode, provider ownership, archive/rollback and disabling scheduled imports. Qualify the test year then full dataset. Owner go/no-go occurs on a concrete dossier; safe preparation stays in scope. Owners/reuse: `src/features/admin/import/; existing migration/data-map documents; docs/operations/`. Acceptance: AC-169, AC-174, AC-285, AC-286, AC-287, AC-288, AC-289, AC-290, AC-291, AC-292, AC-293, AC-632. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P2 · PL-VENUE-PROFILE — Complete structured venue operating facts.** Preserve current profiles and complete all specified capacities/spaces, access/kitchen/power/water, load-in/parking/elevators/storage/waste, permits/insurance, media and operating classifications. Feed authoritative fields into proposals, packing, staffing and reports without re-entry. Owners/reuse: `src/operations/event.manifest; src/features/facilities/`. Acceptance: AC-312, AC-313, AC-323. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P2 · PL-VENUE-LAYOUT — Version venue templates and Event logistics snapshots.** Keep venue templates separate from Event copies. Complete layout/logistics snapshot identity, edits, template preview/deltas and immutable publication/finalization context; event overrides never rewrite the reusable venue template. Owners/reuse: `src/facilities/; src/operations/; src/lib/safeMaterialization.ts; src/features/events/`. Acceptance: AC-314, AC-315. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P2 · PL-VENDOR-POLICY — Apply effective venue-vendor relationships proportionately.** Preserve existing preferred-vendor records; complete category/contact/effective/insurance evidence and approved/restricted/banned states. Show the applicable warning or narrowly justified block in the Event/proposal flow; no blanket approval bureaucracy. Owners/reuse: `src/facilities/; src/procurement/; src/features/facilities/`. Acceptance: AC-318, AC-319. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P2 · PL-ATTRIBUTION — Snapshot effective revenue and commission terms.** Complete percent/fixed basis, effective dates, source/type/reason, reasoned exceptions and Event snapshots. Reconcile gross, venue-produced, allocated and retained revenue from approved terms, including the specified 3% basis; never guess an unresolved definition. Owners/reuse: `src/finance/revenue-attribution.manifest; src/features/finance/; reporting owners`. Acceptance: AC-300, AC-301, AC-302, AC-303, AC-320, AC-321, AC-322. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P2 · PL-REPORT-INVENTORY — Account for every supplied report purpose.** Map all 90 actual workbooks and 70 indexed purposes to native output, source-only history or explicit unsupported requirement with owner/reason. The current 89-item catalog count is not evidence of equivalence; different purposes cannot collapse by similar names. Owners/reuse: `src/features/reports/; existing TPP report playbook; src/lib/tppReports/`. Acceptance: AC-140. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P2 · PL-REPORT-RENDERING — Qualify distinct operational report content.** Preserve existing dedicated renderers and complete each BEO/service/prep/recipe/purchasing/staffing/packing/finance field and grouping. Imported and native Events must produce usable ordinary report sets with explicit missing-source notices. Owners/reuse: `src/features/reports/; src/lib/eventPacket/; src/features/kitchen/`. Acceptance: AC-141, AC-148. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P2 · PL-REPORT-METRICS — Define and reconcile report measures and filters.** Reuse live report queries and filters. Record basis/timezone/currency/status/refund/fee/tax/ledger-or-reference semantics for every measure; qualify shareable dimensions, excluded/unresolved counts, contributor/source drill-down and honest unknown coverage. Owners/reuse: `src/features/reports/; src/features/finance/; approved TPP KPI playbook`. Acceptance: AC-142, AC-144, AC-145, AC-294, AC-295, AC-296, AC-297, AC-298, AC-299, AC-552. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P2 · PL-DASHBOARDS — Complete all seven live leadership dashboards.** Preserve existing routed live dashboards. Finish Tim KPIs, scorecard target/trend/owner/status, L10 history/actions, average-value drivers, compensation evidence, sales/3% basis and Mangia Round 4 parity with real queries and empty/error states. Review visual hierarchy within DESIGN.md. Owners/reuse: `src/features/reports/; authenticated dashboard routes; approved TPP KPI playbook`. Acceptance: AC-143, AC-304, AC-305, AC-306, AC-307, AC-308, AC-309, AC-310, AC-311, AC-370. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P2 · PL-REPORT-EXPORT — Prevent truncated or inconsistent report exports.** Replace silent fixed-row truncation with bounded pagination and truthful counts. Screen/print/download must share filters, revision, totals, missing notices and permissions; verify long-page layout and column completeness with rendered/exported artifacts. Owners/reuse: `src/features/reports/; authored report queries/export owners`. Acceptance: AC-146. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P2 · PL-REPORT-SNAPSHOT — Expose report freshness and reproducible snapshots.** Record source revision/as-of time and keep saved/finalized results reproducible. Acknowledged changes update live reports; refresh errors keep stale data visibly stale rather than presenting it as current. Owners/reuse: `src/features/reports/; src/finance/event-closeout.manifest`. Acceptance: AC-147. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P2 · PL-INBOX — Bring real inbound conversations into the sales workflow.** Keep existing manual conversation capture and honest reply states. Add supported account connect and signed/polled ingress with stable account/thread/message/media IDs, cursor overlap/restart recovery, client/lead/Event linking and retryable raw-source errors. Owners/reuse: `src/features/clients/; src/integrations/; authored email/social ingress seams`. Acceptance: AC-033, AC-104, AC-105, AC-106, AC-112, AC-247, AC-248, AC-249. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P2 · PL-OUTBOUND — Deliver exact proposal, invoice and reply artifacts durably.** Preserve working reminders and staff email. Bind recipients/sender/template/immutable revision/attachments to durable intent, with provider-accepted/delivered/bounced/unknown states. A queued record or manual status transition is not proof of delivery. Owners/reuse: `convex/invoiceReminders.ts; src/lib/invoiceReminderEmail.ts; src/lib/emailNotifications.ts; integration delivery owners`. Acceptance: AC-040, AC-107, AC-109, AC-352. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P2 · PL-CONSENT — Enforce current delivery preferences at send time.** Complete consent, suppression, opt-out, enabled channels and applicable quiet-hour checks for scheduled work across devices. Disconnect affects only work owned by that provider connection, not unrelated delivery services. Owners/reuse: `src/lib/emailNotifications.ts; convex/emailNotifications.ts; messaging/device registration owners`. Acceptance: AC-110, AC-120. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P2 · PL-CONNECTIONS — Complete provider ownership, connection and recovery contracts.** Record account/tenant/external identity, scopes, field ownership, cutover/history basis and durable run progress. Expose connect/reconnect/status/backlog/scoped retry/disconnect with safe errors; OAuth success alone is not synchronization. Confirm actual credentials only during qualification. Owners/reuse: `src/integrations/integration-connection.manifest; convex/qboSync.ts; convex/googleCalendar.ts; integration UI owners`. Acceptance: AC-113, AC-114, AC-121, AC-344, AC-345, AC-355, AC-633. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P2 · PL-QBO — Reconcile QuickBooks tax, corrections and history safely.** Preserve existing customer/invoice/payment sync. Complete field/tax/correction mapping, source ownership and reference-history exclusion by stable IDs; provider receipts and local balances agree without reposting archival money or creating edit loops. Owners/reuse: `convex/qboSync.ts; convex/lib/qboSync.ts; source-link and reconciliation owners`. Acceptance: AC-115, AC-349. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P2 · PL-CALENDAR — Qualify calendar update, cancellation and reconnect.** Preserve deterministic external identity and current sync. Prove material update, cancellation, revocation, target changes and overlapping retries yield one item per Event/target with visible recovery state. Owners/reuse: `convex/googleCalendar.ts; related calendar helper and generated integration bindings`. Acceptance: AC-116, AC-351. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P2 · PL-NOWSTA — Implement and qualify the specified live Nowsta mapping.** Inventory the agreed worker/role/shift/time/pay mappings and supported API boundary, then implement stable incremental sync, conflicts, corrections and acknowledgments. Existing payroll CSV is useful but cannot be relabeled live Nowsta integration; native replacement jobs remain required too. Owners/reuse: `src/integrations/; src/workforce/; src/features/finance/payrollExport.ts`. Acceptance: AC-117, AC-350. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P2 · PL-SMS-SOCIAL — Complete SMS and social provider-specific outcomes.** Reuse existing Twilio/connection mechanisms and complete authenticated callbacks, out-of-order state, supported outbound types and social account/thread/media behavior. Provider capabilities and actual live qualification evidence must be explicit without guessing account availability. Owners/reuse: `src/integrations/; convex/smsAlerts.ts; communication/provider owners`. Acceptance: AC-353, AC-354. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P2 · PL-RETENTION — Provide scoped retention, export and deletion with holds.** Preserve the existing export path. Inventory affected records and legal/business holds, preview authorized retention/deletion, retain required finance/employment history and audit the action. Resolve business retention values before activation without imposing approvals on ordinary edits. Owners/reuse: `src/features/admin/PersonalDataExportPage.tsx; privacy/authored data-operation owners`. Acceptance: AC-155. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P2 · PL-NATIVE-JOURNEY — Qualify the full native Event and replacement job chain.** Run all 22 golden-event steps on one tenant-isolated Event and stable identities, with realistic second-Event stock/order competition. Stub provider boundaries in runtime proofs and use actual authenticated workflows for browser/live evidence; no partial recipe or booking proof qualifies the whole chain. Owners/reuse: `tests/proofs/; authenticated existing product routes; qualification scripts and docs`. Acceptance: AC-017, AC-103, AC-168, AC-184, AC-189, AC-190, AC-338, AC-343, AC-376, AC-653, AC-654, AC-655, AC-656, AC-657, AC-658, AC-659, AC-660, AC-661, AC-662, AC-663, AC-664, AC-665, AC-666, AC-667, AC-668, AC-669, AC-670, AC-671, AC-672, AC-673, AC-674. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P2 · PL-REPLACEMENT-PROOF — Prepare separate legacy-product replacement dossiers.** Map recurring TPP/Nowsta/Galley/Goodshuffle/binder jobs to working normal paths, migrated history/documents, observable providers and operator trials. Obtain missing authoritative manual/KPI detail instead of inventing it. Explicit owner cutover approval comes after side-by-side and rollback evidence is reviewable. Owners/reuse: `AUDIENCE_JTBD.md; current Mangia job inventories; qualification docs`. Acceptance: AC-176, AC-387, AC-706, AC-707, AC-708, AC-709, AC-710, AC-711, AC-712. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P2 · PL-ROUTE-STATES — Qualify all shipped routes and remove actual dead behavior.** Inventory loading/empty/populated/denied/validation/conflict/error behavior and every enabled primary control. Preserve proven fixes and finish only actual in-spec stubs/dead paths; file platform blockers. Placeholder form hints and intentionally local UI state are not fake business functionality. Owners/reuse: `src/app/; src/features/; integration-guard and behavior suites`. Acceptance: AC-035, AC-038, AC-054, AC-170, AC-214, AC-215, AC-216, AC-217, AC-359. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P2 · PL-MOBILE-ACCESSIBILITY — Qualify critical client and field workflows at phone width.** Exercise 360px and desktop workflows with touch, keyboard, focus, screen-reader status, reduced motion and nested-dialog ownership. Keep DESIGN.md front-matter authority and review real Capsule artifacts for clarity/quality with recorded human or model judgment, not generic sample review criteria. Owners/reuse: `existing client/field/event UI; DESIGN.md; src/lib/llm-review.ts`. Acceptance: AC-046, AC-171, AC-185, AC-250, AC-251, AC-252. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P2 · PL-SCALE — Measure full-size tenant pagination and interaction performance.** Use at least 10,000 Events and 5,000 dishes with proportionate relations. Record hardware/network and at least 100 samples for proposed p95 under 1s list/detail and under 200ms interaction targets; separate cold start/export/import. Fix bounded paging/rendering problems before making scale claims. Owners/reuse: `report/list query owners; frontend list renderers; synthetic qualification harness (proposed)`. Acceptance: AC-172. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P2 · PL-GIT-MAINTENANCE — Qualify upstream integration and checkout-owned preview.** Preserve shared dev and user changes. Prove clean/diverged/dirty integration and wrong-checkout/occupied-port preview cases, reporting actual SHA/process/URL and both local serving identities; branch pushes never imply deployment. Owners/reuse: `ralph-sync.sh; ralph-preview.ps1; .ralph.env; loop.sh`. Acceptance: AC-157, AC-158, AC-159. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P2 · PL-BACKUPS — Prove isolated backup restoration and rollback boundaries.** Include database, required assets and configuration references under encrypted access/retention. Run an isolated restore validating relationships, counts, files and authentication; document irreversible provider effects and safe forward/correction paths. Owners/reuse: `docs/operations/; approved backup/deploy scripts and self-hosted backend owners`. Acceptance: AC-164. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P2 · PL-MONITORING — Make production and durable-job failures actionable.** Qualify failed deployment/backend/stuck queue/expiring connection alerts with safe diagnostics, tenant fairness and runbook recovery. Preserve business edits during outages and distinguish uncertain external effects from terminal failures. Owners/reuse: `approved production health/deploy scripts; provider/import/reconciliation owners`. Acceptance: AC-165. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P2 · PL-STALE-ASSETS — Recover from frontend version changes without losing forms.** Preserve atomic cache staging and auth exclusions. Prove incompatible assets and failed chunks expose actual version/recovery, preserving unsaved form drafts and never calling an old cached release current. Owners/reuse: `public/sw.js; src/app/shell/RouteErrorBoundary.tsx; src/ui/formDraft.tsx`. Acceptance: AC-166. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P2 · PL-COMPLETION-DOSSIER — Maintain full evidence and obtain final independent qualification.** Keep each requirement tied to owners, route/command, test, environment/SHA and actual result. Complete all definition-of-done and replacement legs, eligible different-model approval before merge, green required gates and authorized Git/release receipts. No global production-ready claim follows from this plan or the local preview. Owners/reuse: `ACCEPTANCE_TESTS.md; IMPLEMENTATION_PLAN.md; codex-plans/; repository review/release policy`. Acceptance: AC-167, AC-175, AC-363, AC-364, AC-365, AC-366, AC-391, AC-713, AC-714, AC-715, AC-716, AC-717, AC-718, AC-719, AC-720, AC-721, AC-722, AC-723, AC-724, AC-725, AC-726, AC-727. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

- [ ] **P2 · PL-DECK-SHARING — Share published presentation artifacts with the same revision policy.** Complete the explicitly required presentation-deck sharing path using immutable published artifacts, sensitivity-appropriate scope, expiry/revocation and view tracking. Search and reuse existing document/publication ownership before adding a narrowly scoped artifact type; do not build a second presentation platform. Owners/reuse: `src/sales/share-link.manifest; existing proposal/document publication seams`. Acceptance: AC-253. Verify every named test/review on those rows; retain any PASS legs and finish the PENDING legs.

Preserved completion evidence: AC-001, AC-002, AC-003, AC-004, AC-005, AC-007, AC-008, AC-009, AC-010, AC-011, AC-014, AC-015, AC-016, AC-017, AC-018, AC-019, AC-020, AC-021, AC-022, AC-023, AC-025, AC-026, AC-027, AC-028, AC-031, AC-032, AC-033, AC-034, AC-035, AC-036, AC-037, AC-038, AC-039, AC-040, AC-041, AC-042, AC-043, AC-044, AC-045, AC-046. These remain the narrower historical contracts, with renamed mounted-test references refreshed. They are not a substitute for any broader pending criterion. No source requirement was removed or parked as future work.
